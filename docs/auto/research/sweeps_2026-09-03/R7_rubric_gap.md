# R7 — Rubric gap analysis for the 2026-10-18 KCF finals (WildfireGuardian)

Date: 2026-09-03. Read-only analysis; nothing under `Korea Code Fair/` was modified.

## 0. What was read, and what could not be verified

Local (all read in full unless noted): `서식1_최종본_붙여넣기용.md`, `서식2_본문_Ⅰ-Ⅱ.md`, `서식2_본문_Ⅲ-붙임.md`, `WildfireGuardian_예상질의_노트.md`, `WildfireGuardian_개정_대조표.md`, `WildfireGuardian_2차예선_발표_발표자노트수정.pptx` (text extracted from XML), repo `README.md`, `docs/HANDOFF_ROUND3.md` §1/§3/§4/§4-B/§5, `docs/BLOCKERS.md`, `docs/SESSION22/20/19/18(Phase 4)/17/16_REPORT.md` (16/17 headers), `docs/finals_demo_plan.md`, `docs/FINALS_DEMO.md`, `docs/decision_shift.md`, `docs/routing_limitations.md`, `docs/firefighter_consultation.md`, `docs/dispatch_ordering.md` §0, `docs/global_portability.md` §3–4, `docs/MODEL_CARD.md` (recall section), `docs/NUMBERS.json` (260 entries, 244 reproducible / 16 verified-not-reproducible), `web/finals.html` (2,083,656 bytes; section ids `#view-live / #view-system / #view-evidence / #view-reliability`, payload constants `DATA`/`STR`/`SYS`, ~1.6 MB after stripping 3 base64 PNGs + vendored fonts; 17 tests in `tests/test_finals_screen.py`).

External:
- KCF 운영요강 page `https://kcf.or.kr/84/?bmode=view&idx=171493723` → **HTTP 403 from this environment**. The rubric text (붙임1) is therefore taken **as supplied in the task brief — UNVERIFIED by me**.
- Namu wiki 한국코드페어 (`https://namu.wiki/w/한국코드페어`) confirms: 본선 2차 = in-person booth, "심사위원 5명이 돌아가면서 작품을 보고 Q&A를 하는 형식", Wi-Fi unreliable, 은상 이상 → ISEF 한국대표단 선발 자격, age 만 15–19 in the ISEF year. It does **not** state a "must not change theme" rule — that rule is **UNVERIFIED** here; treat it as binding anyway because the brief says so.
- KCF press pages (`https://kcf.or.kr/71/?bmode=view&idx=172288990`, `https://work.or.kr/...`) confirm 2026 theme "AI와 데이터로 한국의 사회 문제 해결", finals in October.

---

## 1. Track decision and scoring

### 1.1 Which track

**Track B — SW 연구.** Evidence: 서식1 subtitle "…구조 인지 라우팅 **연구**"; 서식2 is organised exactly as the Track-B rows (Ⅰ 연구 목적 with 3 research questions + 3 hypotheses, Ⅱ 설계와 방법론 with a 변수 정의·제어 table and OFAT, Ⅲ 데이터 수집·분석·결과, Ⅳ 창의성, Ⅴ 결론). The 예선 deck (slide 3) says "확산 예측은 입력이고 … 뒤 두 단계가 본 연구의 기여". A judge who has the 서식 in hand will score it on Track B. The risk is that a booth judge sees the finals screen (an operator console with a live trigger) and instinctively applies Track-A "구현 및 유용성" expectations — so the second scoring below matters for Q&A even if it is not the official track.

### 1.2 Track B score, current state (0–20 per row)

| Row | Score | Evidence for the score | What loses points |
|---|---:|---|---|
| 연구 목적 (theme fit · focused objective · social contribution · verifiable approach) | **17** | Theme fit is exact (AI+data, social problem, 2025 경북 산불, 8 deaths in 영덕). Objective is a single sentence ("이미 발생한 산불이 어디로 확산하며, 이 가구의 주민이 걸어서 빠져나올 수 있는가, 없다면 누구에게 먼저 구조대를 보내야 하는가"). Three RQs each with a named evaluation. Firefighter consultation §3 gives field confirmation of the premise ("지금 불이 안 보이니까" non-evacuation). | RQ2 (severity vs direction) is now **withdrawn as a headline** (README TL;DR; 서식2 Ⅲ-5 already says 미확립) — one of three RQs is dead. Contribution ② as submitted ("시한 임박 순 배정 목록") was measured and **loses 0/180 at the committed W=75** (`docs/dispatch_ordering.md` §0). The purpose still stands but two of its three pillars were narrowed after submission. |
| 설계와 방법론 (prior-work difference · method/design fit · variable definition & control · technical excellence · schedule/roles) | **14** | LOGO-CV by fire is the right design; 변수 table with controlled/manipulated/dependent variables; OFAT sweeps; time-expanded graph pseudocode; 24-rule discipline (`HANDOFF` §5), 12 gates, 1,116 tests, `NUMBERS.json` registry. | **Prior work is a 3-row table** (재난문자, 산불상황도, FARSITE/WUI-NITY) + two Cova citations; `decision_shift.md` §1 records "신규성 검증하지 않았습니다 — 선행연구 부재 조사를 하지 않았습니다". Time-dependent Dijkstra and ingress-survival are not positioned against the evacuation-routing literature. **Schedule & roles is one line** ("진행: 데이터 수집 → … → 문서화"), no dates, no role split with the 지도교사. Method defects recorded but not fixed: the router minimises a right-endpoint rounded-bin functional, not the reported exposure (`routing_limitations.md` §2); search not provably optimal (§4); `fa_exceeds_budget` bucket name asserts a cause the code does not establish (§1). |
| 데이터 수집·분석·해석 (systematic collection · stable/reproducible · appropriate stats · logical interpretation) | **14** | Six real fires, four public rasters, provenance manifests; DeLong CIs per fold, bootstrap pooled CI, ML baselines on identical folds/seed, calibration (Brier/ECE), 244/260 registry entries reproducible, network-drift experiment, forecast-robustness Monte Carlo, dilation perturbation, self-correction trail (Ⅲ-14). | (a) **n = 6 fires** and the unit of generalisation is the fire; (b) **recall at the operating threshold 0.3 is 0.138 pooled, 0.087 mean-of-folds, and 3 of 6 folds catch zero ignitions** (`MODEL_CARD.md`; `oof_classification_metrics.json`) — not in 서식2, not in the 예상질의 notes; (c) the #1 feature `days_since_rain` anchors to the acquisition window for 3/6 fires and **dropping it raises AUC by +0.027** (`weather_dependency.md`); (d) **DEM defect**: `spread_v2_lofo.json` was trained with the sea filled to −497 m for one fire; the corrected re-run exists but the submission still cites the defective lineage (HANDOFF §4); (e) 영덕 walk-bbox coverage **32.6 %** of the fire core — the demo fire is the worst-covered of the three; (f) H2 미확립, H3a 미지지, and the Session-17 null-hazard control shows the household vulnerability layer's failing set is **identical with no fire at all** (`tautology_decomposition.json`); (g) many numbers in 서식2 are now superseded (see §1.4). Interpretation is unusually honest — that keeps the row at 14 rather than 11 — but the sheer number of nulls will read to a fire-science judge as "the prediction layer is weak and the routing layer mostly restates reachability". |
| 창의성 | **16** | Reframing detection→delivery/rescue; elderly as beneficiary not user; (position, time) nodes with hazard scored at *arrival* time; ingress-corridor survival as a routing constraint; per-home `ingress_survival_time_min` as the deliverable; refuge-placement enumeration (Session 22: one refuge covers 20/24, two cover all); a project-wide "numbers must be re-derivable" gate. Judges who read the 자기수정 절 will see scientific thinking. | The pieces individually are known (time-dependent shortest paths, Cova trigger buffers, WUI-NITY couplings, FlamMap MTT-style arrival times); novelty unverified; the withdrawn 44× claim and the dead ordering make the "creative" contribution ② look like an engineering assembly whose distinctive part did not pan out. |
| 제출 자료 (logical composition · clear graphics/legends · sources cited) | **15** | 서식2 is dense, cross-referenced, 20 references + data sources; figures with lineage captions; `finals.html` has 「근거」 buttons per card and a SYSTEM INTEGRITY panel; three delivery formats exist as real files (`outputs/dispatch*`). | **Consistency**: submitted 서식2 numbers vs current repo differ in at least seven places (§1.4). A judge who reads 서식2 and then the screen sees 438/18/3 in one and 414/42/2 in the other. README's own table shows RF 0.914 while 서식2 says 0.920. Ⅲ-9 says "DEM 경사 미적용"; Round 3 applied it. The 예선 deck said "출동이 30분 늦어지면 6→24"; README says "6→34 as delay goes 0→60". No poster/panel artefact for the booth is in the repo (UNVERIFIED whether one exists elsewhere). `finals.html` EVIDENCE cards do not yet show Session 20 (horizon grounding, 2,008 fires) or Session 22 (refuge placement). |
| **Total (Track B)** | **76 / 100** | | Weakest rows: **데이터·분석·해석 (14)** and **설계·방법론 (14)**, then 제출자료 (15). |

### 1.3 Track A score (if a judge applies the application rubric)

| Row | Score | Why |
|---|---:|---|
| 개발 목적 | 17 | Same strengths as above; "constraints understood" is strong (offline, no-app elderly, 이장 마을방송, no-coordinates output backed by firefighter §7). |
| 설계와 방법론 | 14 | Difference from 네이버/카카오/재난문자 is clear; technical completeness good; **schedule/roles absent**. |
| 구현 및 유용성 | **11** | README and the RELIABILITY tab say plainly "운영용 소프트웨어가 아닙니다 … 실무자 평가를 거치지 않았고". ERA5 has ~5-day latency so the "live" trigger routes on a fixed reference-time field (PHASE 14 stopped); the email channel's verification send never completed (PHASE 7); no live trigger has ever fired on a real detection; walking speed, vehicle speed, W = 75 are `# ASSUMED`; refuges are OSM parks/pavilions; 영덕 coverage 32.6 %. Feasibility is demonstrated (trigger→dispatch list ≈ 10–14 s, offline single file), but "resolves the need" is not. |
| 창의성 | 15 | As above, minus points because as an *application* the user-facing product is an A4 sheet and SMS draft that were never field-tested. |
| 제출 자료 | 15 | As above. |
| **Total (Track A)** | **72 / 100** | Weakest row by far: **구현 및 유용성**. |

Reading: on either track the project sits in the low/mid-70s. 은상 needs (UNVERIFIED cut-offs) roughly top-third of 25 finalists; the rows that can still move by Oct 10 are 제출자료 (consistency), 설계·방법론 (prior work, schedule/roles, one more controlled variable) and 데이터·해석 (turn two open questions into measured answers).

### 1.4 The superseded-number list a judge can trip over (서식2 as submitted → repo now)

| Item in 서식2 | Submitted value | Current canonical value | Source |
|---|---|---|---|
| Ⅲ-9 459-series split | 459 · 438 / 18 / 3 (`routing_demo.npz`, sha 5bed5026…) | 458 · **414 / 42 / 2** on `routing_demo_canonical.npz` (sha 81b4e4d1…); FA-only 3.70 % → **9.17 %** | HANDOFF §1.1, §2-A; `decision_shift.md` §3.3 (no per-origin ledger for this pair) |
| Ⅲ-9 "핵심 영역 241→244 셀, 거의 변화 없음" | quasi-static core | core growth **+316 %** (249 → 1,036 cells); the quasi-static reading was a property of the reverted field | HANDOFF §1.1 |
| Ⅲ-9 잔여 한계 "OSM 간선에 DEM 경사를 적용하지 않아" | not applied | **applied** at 60 m sampling (+26.6 % traversal time), null on buckets | HANDOFF PHASE 2 |
| Ⅲ-11 ML baseline table | RF 0.920 ± 0.036, LR 0.903, GBM 0.889 ± 0.107 | committed `ml_baselines.json`: RF **0.914 ± 0.044**, LR 0.903 ± 0.060, GBM **0.894 ± 0.092** (ordering unchanged) | README |
| Ⅲ-4 far-band AUC 0.925 (n=3) | 0.925 | committed `auc_intervals.json` **0.904 ± 0.100**; corrected-DEM pooled far-band 0.8408 (vs 0.877) | README, MODEL_CARD |
| Ⅴ-2 "PR-AUC·Brier 미산출, 향후 과제" | not computed | **computed**: AP 0.169 vs 0.0197 baseline; Brier 0.0183, ECE 0.0086 (RF 0.0174/0.0068) | `oof_classification_metrics.json`, `calibration_metrics.json` |
| Ⅲ-6-2 / Fig. 3 "0/30/60분 → 6/24/66곳" | 6→24→66 | README §rescue routing said "6 → 34 as delay goes 0 → 60 min" — **internal inconsistency inside the repo**. ✅ **RESOLVED 2026-09-03 (WFG-004).** This sweep's own question "which artifact is 34?" had the right instinct and the answer is: the superseded pre-flip **452계열** baseline, `data/processed/rescue_baseline_synthetic/rescue_verify.json` → `[6, 15, 20, 25, 34]`. Not a typo — a lineage mix inside a 439계열 paragraph. See `docs/ssot_audit_2026-09-03.md` §1 | README vs 서식2/개정대조표 |
| Ⅲ-4 "DEM" lineage of the headline AUC | 0.890 mean-of-folds | unchanged as headline, but trained on a raster with the sea at −497 m; corrected re-run +0.0048 mean-of-folds (not adopted) | HANDOFF §4 |
| Ⅱ-2 contribution ② "여유 오름차순 우선 출동 배정 목록" | ordering is the contribution | **restated 2026-08-10**: the *information* (`ingress_survival_time_min`) is the contribution; the ordering is only valid when W exceeds corridor closure, and W=75 does not | `dispatch_ordering.md` §0 |

None of these contradicts the submitted *purpose/theme*; all are corrections in the direction the 서식 itself invites (Ⅴ-4 lists most of them as future work). But the student must be able to recite this table without notes, or a judge will conclude the documents were not written by the person at the booth.

---

## 2. Ten hardest questions the 예상질의 notes do not answer well

The notes (A-1…F) cover: number provenance, retired numbers, Build A, 38 %/11 %, f = 0.3, Monte Carlo 21 %/86 %, probability-scale null, MC lineage, DNN choice, physics-vs-data, IoU table removal, the three routing configurations, 9-hour fixed observed area, forward-sim drift, two bboxes, acquisition automation, why-needed vs 국가산불위험예보, the 0-change rescue constraint, where future-aware is validated, unverified assumptions. They were written 2026-07-23, **before** Round 3–5 (canonical field, multi-region, dispatch ordering, recall at threshold, vulnerability layer, GK2A, horizon grounding, refuge placement). Everything below is new ground.

**Q1 (ML researcher / statistician). "AUC 0.89 is a ranking score. At the threshold your simulator and router actually use, what fraction of real ignitions does the model catch?"**
Answer to give: "0.138 pooled; 0.087 mean-of-folds; three of six held-out fires have zero true positives at 0.3, and almost all recall comes from 영덕 (0.456). We report it in the model card above the AUC. The router does not apply that cut — it consumes the probability surface — but the forward simulation's advance rule does, so the hazard field's *extent* inherits it. The honest ranking summary is average precision 0.169 against a 0.0197 no-skill baseline, 8.6×. The threshold was never tuned; the F1-optimal cut (0.14 → F1 0.218) is recorded and deliberately not adopted because choosing it on the scored probabilities is optimistic." Proof: `docs/MODEL_CARD.md` (recall section), `data/processed/oof_classification_metrics.json`, `docs/SESSION18_REPORT.md` Phase 4. What does not exist: any experiment showing how bucket counts move if the advance threshold changes (see improvement #4/#5).

**Q2 (fire scientist). "ERA5 is 0.25° and published with ~5 days' delay. Your 'live trigger' cannot be using real weather. What field does it route on?"**
Answer: "A pre-computed field at a fixed reference time. We measured the upper bound of the loss from swapping instantaneous weather for a forecast-like proxy — mean-of-folds −0.020 — but the real substitution was never run; PHASE 14 stopped there and we say so on the RELIABILITY tab: '완전한 실시간 운영이 아닙니다'. LDAPS is recorded, not used." Proof: `docs/weather_dependency.md`, `docs/live_pipeline.md`, HANDOFF §14. Evidence that a real-time weather source would help: **does not exist**.

**Q3 (statistician). "Your top permutation feature, days-since-rain, is pinned to the download-window start for half your fires, and removing it *improves* out-of-fold AUC. Isn't the model partly learning how you built the dataset?"**
Answer: "Yes, for that feature, and we measured it rather than hid it: +0.027 mean-of-folds, +0.053 far-band when dropped. We did not retrain the committed model because every downstream number would move and the submission's lineage would break; the corrected re-run exists as a separate file. The routing layer consumes the committed field. The lesson is registered: an acquisition parameter leaked into a feature." Proof: `docs/weather_dependency.md` §②, README. What does not exist: a *committed* canonical retrain without the feature (frozen by §5 rule 2; a user decision).

**Q4 (emergency-management official). "Your submitted contribution ② is a deadline-sorted dispatch list. You then measured it against nearest-first and it never wins at your own 75-minute window. So what exactly is the contribution?"**
Answer: "The per-home quantity `ingress_survival_time_min` — when each approach road closes — which nothing in the compared systems computes. Sorting on it is only informative when the operating window outlasts corridor closure; at W = 75 all homes share ~2 deadlines and the sort carries no information, so nearest-first wins by saving 6–13 min per trip. We left the shipped order unchanged and state the condition. A serving firefighter told us independently that a fixed time budget does not exist in field decisions — the decision is 'can we get out alive', which is our survival term, not our clock term." Proof: `docs/dispatch_ordering.md` §0/§6, `docs/firefighter_consultation.md` §1.2–1.3. Do not say "verified" — N = 1 and the two sources are independent, not mutually confirming.

**Q5 (fire scientist). "Your household vulnerability and refuge-placement layer: you showed the failing set is identical with the fire removed. So the fire contributes nothing there — is this a wildfire result at all?"**
Answer: "For that layer, at 영덕, with the elliptical hazard: correct, and we retracted 'distance drives vulnerability' to 'this is a reachability restatement'. The fire *does* bind on the routing axis — 42 of 458 origins at 영덕 and 91 of 368 at 의성·안동 reach a refuge only on the time-aware route — but the household layer sits on a hazard that never reaches the refuges (0/120 survival-filter hits). We kept the survival check outside the objective and labelled the placement result 'geometric recommendation under stated assumptions'. What would make the fire bite there is listed and untried: stronger scenario, ignition prior inside the household cluster, arrival-time survival buffer, a site whose refuges actually burn." Proof: `docs/SESSION17_REPORT.md`, `docs/SESSION22_REPORT.md` §Phase 3, `BLOCKERS.md` Session 17 future work. Evidence of a fire-conditional household result: **does not exist**.

**Q6 (fire scientist / modeller). "Why is 'ignition probability ≥ 0.5 by the next satellite overpass' the definition of an impassable road? Ignition in a 375 m cell is not flame on the road."**
Answer: "It is a threshold we chose, not one we derived; we treat it as a controlled variable with p_cut swept 0.4–0.6 for walking and 0.5–0.9 for vehicles in the 439 series, and the honest statement is that a probability of detection-by-next-overpass is a proxy for 'the fire will be there', coarse in both space (375/500 m) and time (3-hour slices). A branch adopted 0.30 and re-ran everything; we did not merge it because it would change every committed count and the registry would hold two cutoffs." Proof: 서식2 Ⅱ-3 (조작 변수), HANDOFF §4 (hazard-resolution branch, `p_cut = 0.30`), `docs/routing_limitations.md` §3. A physically grounded impassability criterion (flame length, radiant flux): **does not exist**.

**Q7 (statistician). "Your walk bbox covers 32.6 % of the demonstration fire's core; the other two regions cover 99 % and 82 %. Isn't 영덕 — the fire you built the story on — the least valid case?"**
<!-- collision-ok: 24.7 -->
Answer: "Its absolute rates are rates on the covered third, and every one carries that caveat; the paired contrasts (same origin, two routes) do not depend on the frame. The fix is not a bbox edit — the bbox is coupled to the simulation canvas, so it is a full re-simulation, and we recorded the decision not to. On the region with 99 % coverage and an advancing front, the same method gives a time-aware-only share of 24.7 %, seven times 영덕's. We do not rank the three regions: n = 3, and coverage, core growth and envelope area move together." Proof: HANDOFF §2-A, §5 rules 14/19, README Round-3 table, `docs/multi_region.md` §8.

**Q8 (emergency-management official). "What are your 'refuges'? Are any of them designated 대피소?"**
Answer: "Fifty OSM POIs at 영덕, 46 snapped to the network; the RELIABILITY tab says it plainly — most are `leisure=park`/정자-type tags, unverified against 행안부 지정 대피소. The 공공데이터포털 national shelter file has a loader with a labelled synthetic fallback, but the committed runs use OSM. Between-region 'shelter density' differences are partly mapping practice. Refuge *survival* under the fire is a separate filter and never bound in these runs." Proof: 서식2 Ⅲ-9-1, `finals.html` RELIABILITY strings ("대피 지점의 다수는 OSM의 공원·정자류 태그입니다"), `docs/global_portability.md` §3b. A cross-check against the official shelter list: **does not exist** (portal download is JS/CAPTCHA-gated; see student list).

<!-- forbidden-ok: en-ordering -->
> **⚠ WITHDRAWN 2026-09-04 — do not drill answer Q9 as written (WFG-053, WFG-063,
> WFG-070).** This sweep is a record of what was believed on 2026-09-03 and its prose is
> not edited. Q9's answer says a satellite trigger 「would still have fired 22–64 minutes
> *after* the human report in every fire we could test, so the trigger interface is
> designed report-first, satellite-confirm」. Every clause of that is withdrawn: the <!-- forbidden-ok: en report-first -->
> delays are measured from a **recorded occurrence time**, not a report
> (`docs/data_provenance/fire_manifest.json`: `start/end/reported_ha are provenance
> only`); no committed artifact holds a 신고접수시각, so **neither** direction of the
> comparison can be stated; 「every fire we could test」 is 3 of 6 and this file's own
> preamble forbids 「every fire」; and the size floor rules the **satellite out**, not the
> **human in**. Current wording: `docs/detection_floor.md` §9–§10, `docs/auto/JUDGE_QA.md`
> Q10 · Q10d. Open question: NH-019. Found by the English claim gate added in WFG-070,
> not by a reader — this instance was missed by the manual grep that raised the row.

<!-- forbidden-ok: en-ordering -->
**Q9 (fire scientist / official). "The 2025 fire reached 영덕 from 안동 in about 40 minutes. Your hazard slices are 3 hours apart and satellites pass every 6–16 hours. Can this system say anything on the timescale that killed people?"**
**⚠ WITHDRAWN 2026-09-04 — the detection sentence in this answer must not be drilled; see the block above Q9.** <!-- forbidden-ok: en-ordering, en after the human report, en report-first, en human report is primary -->
Answer: "Not at minute resolution, and the A4 sheet's 「남은 시간」 can only read 180/360/540/720/확인 불가 on this field; the '< 30 min → 긴급' tag is structurally unreachable. We measured the detection floor too: with GK2A at 2-minute cadence, a satellite trigger would still have fired 22–64 minutes *after* the human report in every fire we could test, so the trigger interface is designed report-first, satellite-confirm. What the system is for is the 4-hour behavioural window — 79 % of Korean fires are contained within 240 min of report (n = 2,008), while fires ≥ 100 ha run a median 67 hours — so 'act now on foot' vs 'send a vehicle' is the decision it informs, not 'run left or right in the next ten minutes'." Proof: `docs/routing_limitations.md` §3, `docs/SESSION19_REPORT.md` / `detection_floor.md`, `docs/horizon_grounding.md`. Finer-slice re-run of the routing axis: **does not exist** (PHASE 2-C-3 never started).

**Q10 (software engineer / any judge). "You publish 24 'never do' rules, a registry of retired numbers and a record of five fabricated citations caught in one week. Why should I trust *today's* numbers, and which numbers in your submitted 서식 are wrong now?"**
Answer: "Because you can check them: 244 of 260 registered values re-derive from committed artifacts under `make verify`, the other 16 are labelled 'verified, not reproducible' because the OSM graph behind them was overwritten on 2026-07-24. The gates catch a retired number being re-quoted and a region literal; they cannot catch a number that was never registered or an event that never happened — that class was caught by looking things up, and we wrote the failure mode down. Here is the list of what moved since submission —" and then §1.4 above, from memory, in this order: 438/18/3 → 414/42/2 (why: reverted-run field, corrected DEM), RF 0.920 → 0.914, far-band 0.925 → 0.904, slope now applied, PR/Brier now computed, contribution ② restated. Proof: HANDOFF §1.1, §4-B, `docs/NUMBERS.json`, `scripts/verify_numbers.py`.

Three more, lower probability but lethal if unprepared:

- **Q11 (official). "Elderly residents don't leave because they can't see the fire. Which line of your system changes that?"** → None; compliance is out of scope; the only link is a hypothesis that showing *future* closure time addresses the perception gap; unmeasured. `firefighter_consultation.md` §3.2–3.3.
- **Q12 (any). "Who from the field validated this?"** → One serving firefighter, informal oral, N = 1, affiliation/date/consent **unrecorded** (§8 of that doc); three academic advisers named in HANDOFF §8 with the same "judgment, not measurement" status. Never say "현장에서 검증".
- **Q13 (software engineer). "Your Dijkstra minimises a different quantity from the one you report and isn't provably optimal. Why trust the routes?"** → Contrasts are valid (both arms scored by `_evaluate_path`), absolute exposures should not be quoted as minimised, no counterexample constructed at 10-min bins, results are deterministic and reproduce. `routing_limitations.md` §2/§4. A fixed router: **does not exist**, on purpose (would move every committed count).

---

## 3. Improvements before the 2026-10-10 freeze

### 3.1 Constraints that decide what the autonomous agent can and cannot do

- **The 1.3 GB raw bundle (FIRMS/ERA5/DEM) is git-ignored and absent in a fresh sandbox.** Anything that retrains `spread_v2`, re-runs `auc_intervals.py`/`ml_baselines.py`, or re-simulates a hazard field is **blocked** in the cloud loop (scripts exit 2 by design). FIRMS archive needs a MAP_KEY, ERA5 needs CDS credentials, OpenTopography needs a key (HANDOFF §4).
- **What is committed and usable offline**: `data/processed/*.json`, the three hazard `.npz` files (`routing_demo_canonical.npz`, `hazard_uiseong_andong_2025.npz`, `hazard_uljin_samcheok_2022.npz`), `data/snapshots/` (OSM graphml.gz, SRTM tif with MANIFEST), `spread_v2_lofo_oof_cells.csv.gz`, `outputs/live/replay/**`. Session 18 booted the console and API from an empty clone; the **routing axis (459 series, three regions) is re-runnable** on snapshots (60–90 s per region). **Verify this on the first run before promising anything.**
- **`docs/HANDOFF_ROUND3.md` §5** — 24 rules. The ones every item below must respect: never modify a committed artifact (new filenames), never regenerate `docs/figures/*.png`, never re-acquire 영덕 OSM, never change the shipped dispatch order, never push to `Main`, never route on a partial DEM, every cross-region number with the completeness covariates, every absolute 영덕 rate with the 32.6 % caveat, never merge the two four-way classifications.
- **KCF theme rule** (as given): the finals work must not contradict the submitted purpose "확산 예측 → 가구 단위 대피·구조 의사결정". Flagged below where an item drifts.
<!-- forbidden-ok: 154 -->
- **Freeze discipline**: the repo already has an "Arm A frozen" convention (`check_arm_isolation.py`, 154 entries). New experiments go in new arms; nothing here changes a default.

### 3.2 Ranked list — 12 items for the agent

| # | Improvement | Rubric row(s) → points | Effort | Feasible in sandbox? | Flags |
|---|---|---|---|---|---|
| 1 | **Submitted-vs-current reconciliation sheet** (`docs/submission_reconciliation.md`, Korean, one page): every 서식1/2 number → current canonical value → why it moved → artifact + commit, gated by `verify_numbers` (register the pairs). Include the 6→24→66 vs 6→34 discrepancy resolved by reading `rescue_verify.json`. Feed the same table into a `finals.html` RELIABILITY card ("제출본 대비 정본 대조"). | 제출자료 +2, 데이터·해석 +1 | hours | yes (docs + JSON only) | Must not edit 서식 files; this is a repo doc. Registers *pairs*, so `check_number_collisions` must be told the old values are "submitted lineage", not retired. |
| 2 | **Related-work section with verified citations** (`docs/related_work.md` + a 1-page table for the booth): time-dependent evacuation routing (e.g. Cova & Johnson 2003 network evacuation; Cova et al. 2005 trigger buffers; Ronchi et al. 2021 WUI-NITY; FlamMap MTT / Finney 2002; Li et al. 2019 "Evacuation routing with fire spread"; recent ML fire-spread papers on FIRMS/VIIRS). For each: what it computes, what it does not (household-level, rescuer ingress, non-smartphone delivery), with DOI resolved by the agent and the exact sentence quoted. Then rewrite the "차별점" claim as *positioning*, not novelty ("we did not find X in the papers surveyed" ≠ "first"). | 설계·방법론 +2, 창의성 +1 | days | yes (web access needed; sandbox has it) | `check_forbidden` has `kind="claim"` rules against 「최초」/「처음」 — comply. Every citation must be resolved to a DOI/URL; the repo's own §4-B history is exactly about invented citations. |
| 3 | **Operating-point evidence package**: one figure (PR curve + the 0.3 operating point + F1-optimal point, from the committed 51-point PR curve in `oof_classification_metrics.json`) + a per-fold recall table + the paragraph "AUC ranks, recall counts". Add as an EVIDENCE card to `finals.html`. | 데이터·해석 +1.5 | hours | yes (artifact committed) | New figure file name; do not touch `docs/figures/*.png` that the 서식 cites. |
| 4 | **p_cut sensitivity on the routing axis** (Q6): re-run the 459-series classifier for 영덕/의성·안동/울진·삼척 at p_cut ∈ {0.3, 0.4, 0.5, 0.6, 0.7} on the canonical npz + snapshots, output `real_roads_real_hazard_pcut_sweep.json`, register the bucket counts per cell with covariates. Report *monotonic direction*, not a ranking. | 설계·방법론 (변수 통제) +1.5, 데이터·해석 +1 | days | probably (needs `osmnx==2.0.7`, snapshots, npz; ~15 runs × 60–90 s) | New arm, new filenames; do NOT import the `hazard-resolution` branch's 0.30 artifacts (HANDOFF §4 decision). Quote every 영덕 cell with the 32.6 % caveat. |
| 5 | **Hazard time-resolution experiment** (PHASE 2-C-3, Q9): interpolate the 5 slices (0–720 min) to 30/60/90-min slices using the router's own bilinear/linear rule and measure (a) bucket counts, (b) the 「남은 시간」 distribution (does the 긴급 tag ever become reachable), (c) dispatch-list closure-time multiplicity. Output `hazard_time_resolution_main.json`. | 데이터·해석 +1, 설계·방법론 +1 | days | probably (same stack as #4) | Interpolated slices are *derived*, label them so on screen (finals_demo_plan already does this for road state). No default changes. |
| 6 | **Shelter-density decimation** (requested 2026-08-02, sequenced, never started; answers E-2 with a measurement): hold network and terrain, remove refuges at 100/75/50/25 % with 20 seeds, measure FA-only and `no_safe_route` per region → the condition under which the rescue-aware constraint binds. | 데이터·해석 +1.5, 창의성 +0.5 | days | probably | HANDOFF §4 says "the user will confirm before it starts" — **student must approve** in the brief before the agent runs it. |
| 7 | **Pre-movement-time sensitivity** (answers E-4 with numbers): add 0/5/10/20/30-min pre-movement delay to the walking departure on the 459 axis (three regions) and to the 439 axis if the synthetic envelope runs offline; report how `no_safe_route`/FA-only move. | 데이터·해석 +1, 설계·방법론 +0.5 | hours–days | probably | New arm; never quote a short-budget w without its budget (§5 rule 10). |
| 8 | **`finals.html` refresh**: add EVIDENCE/RELIABILITY cards for Session 20 (240-min horizon: 79.23 % of 2,008 fires contained; ≥100 ha median 4,025 min), Session 22 (refuge placement 20/24 → 24/24, with all three red caveats), #1 reconciliation, #3 operating point; rebuild with `--verify` so SYSTEM INTEGRITY records the gates (the committed build's fallback string "이 빌드는 게이트를 실행하지 않았습니다" must not be what judges see — check). Re-run `check_screen_assets.py` and the 17 finals tests. | 제출자료 +1.5, 창의성 +0.5 | days | uncertain — the builder needs the geospatial stack (`wfg311`); `build_finals.py` reads snapshots and npz. Try; if the sandbox cannot build, produce the card content as JSON + instructions for the student to run `make finals` locally. | Offline gate is strict (no `fetch(`, no external URL). Region literals forbidden in template. |
| 9 | **Booth Q&A cards v2** (`docs/finals_qa_cards.md`, printable): the 20 existing notes + the 13 questions in §2 above, each with ① one-sentence answer ② artifact path ③ "what does not exist" line; generated from `NUMBERS.json` so every number carries its caveat and forbidden phrasing. Also an English version (ISEF-style abstract, 250 words) from the same registry. | 제출자료 +1 | hours | yes | Pure docs. Do not include unregistered numbers. |
| 10 | **Tests for the untested code + regression pin for the found defect**: `refuge_placement.py`, `ingest_kfs_statistics.py` (CP949 decode, HH:MM parse, the 12 negative durations), `detection/gk2a.py` (unit and bit-width checks that caught real bugs), and a directed-vs-undirected distance regression (Session 22 found `_distances` folding a DiGraph). Three consecutive session reports list "no tests" as an unverified item. | 설계·방법론 (기술적 완성도) +1 | days | yes (synthetic fixtures; gk2a needs a tiny fake granule) | Do not change behaviour; pin it. |
| 11 | **CI on a fresh clone** (GitHub Actions on the dev branch): `pip install -r requirements.txt && pip install -e . --no-deps && make all-checks` with the bundle absent (data tests self-skip), plus `check_screen_assets.py web/finals.html`. Badge in README. This is the cheapest external proof of "stable/reproducible". | 데이터·해석 (재현성) +1 | hours | yes | §5 rule 1: never push to `Main` — workflow lives on the dev branch; the student merges. `osmnx==2.0.7` must resolve on ubuntu. |
| 12 | **Schedule & roles section** (Track-B row explicitly lists 일정·역할): reconstruct the real timeline from `git log` (245 commits, tags `round2-submitted`, session reports with dates) into a dated Gantt-style table + a roles statement (student / 지도교사 / AI-assisted sessions disclosed as tooling — the repo already discloses "autonomous sessions" in its reports; hiding it at the booth would be worse than stating it). | 설계·방법론 +1 | hours | yes | Truthful attribution only. UNVERIFIED how KCF treats AI-assisted development; the student should check the 운영요강 (see §3.4). |

Deliberately **not** on the list (blocked, frozen, or theme-risk):
- Retraining `spread_v2` without `days_since_rain` / on the corrected DEM as the *published* lineage — needs the bundle, and §4 says it is the user's decision.
- Changing the dispatch sort to nearest-first — measured to rescue more, but HANDOFF §1.2-c says the shipped order "is not to be" changed, and it would contradict submitted contribution ② rather than restate it.
- **Ignition-likelihood layer** from the KFS 원인·장소 columns (Session 20 §Phase 4) — this is *pre-ignition risk forecasting*, i.e. the exact thing 서식1 says the project is **not** ("확산 예측 자체가 아니라 그 위에 세운 두 계층"; "기존 체계가 답하는 질문은 「이 행정구역에 산불이 날 위험이 높은가」"). **Theme-drift risk under the KCF rule — do not headline it before Oct 18.**
- **US Camp Fire transfer arm** (`us-arm-b` stash, PHASE 13 stopped) — fine as an ISEF appendix later; at KCF it dilutes "한국 농촌 고령자" and consumes agent time. Keep out.
- Any change to `p_cut`, `W`, `HORIZON_MIN`, walking speed defaults — sweep, never adopt (Arm A frozen).

### 3.3 Points arithmetic (rough)

If #1–#5, #8, #9, #12 land: 제출자료 15→18, 설계·방법론 14→17, 데이터·해석 14→17, 창의성 16→17 → ≈ **85 / 100** on Track B. Items #6/#7/#10/#11 add ~2 more but two of them (#6, #8) have a confirmation or environment dependency.

### 3.4 The five things only the student can do

1. **Rehearse the booth cycle, not a speech.** Five judges × (5 min demo + 5 min Q&A), reset with **G** between judges (`docs/FINALS_DEMO.md`). Practise three different 5-minute openings (fire-scientist-led, official-led, engineer-led) and answer the 13 questions in §2 cold, pointing at the artifact on screen. Practise drawing the (position, time) graph and the ingress-survival min(·,·) on paper — judges reward explanation over recitation. Time it with a stopwatch; the demo must fit even if a judge interrupts at minute 2.
2. **Hardware and print.** Primary laptop with `web/finals.html` opened from `file://` with Wi-Fi *off* (verified Session 18 on a clean clone), a second laptop or USB with the same file, HDMI/USB-C adapter, power strip; printed A4 samples from `outputs/dispatch*` (the 이장 sheet, SMS draft, 마을방송 script), the #1 reconciliation sheet, #2 related-work table, #9 Q&A cards. Check the KCF booth/poster specification (UNVERIFIED here — 운영요강 was 403) and produce the panel; the repo has no poster asset.
3. **Logged-in / CAPTCHA downloads the agent must not do**: 도로명주소 건물 layer (`business.juso.go.kr`, account + approval, `BLOCKERS.md` Session 8) → moves all household counts off the 124-building OSM snapshot; 공공데이터포털 national shelter file (answers Q8); KMA API Hub key (GK2A L2 FF); OpenTopography and CDS keys if any re-simulation is ever wanted. Drop files under `data/raw/...` as the blockers specify and tell the agent.
4. **Expert record-keeping.** Get the firefighter's affiliation/rank, interview date and **written consent** for anonymous vs named attribution (`firefighter_consultation.md` §8 — all four blank); ask the three academic advisers whether a one-line quoted judgment may be shown at the booth; if possible a second practitioner (읍·면 재난 담당) so N = 2 — still "judgment, not measurement". Cross-check the 의성 crew-isolation anecdote against a news source before ever citing it.
5. **Forms, rules and decisions.** Confirm with the KCF 사무국 the exact wording of the no-theme-change rule and how AI-assisted development must be disclosed; confirm ISEF age eligibility for the 2027 fair (만 15–19 during the ISEF year, per namu wiki); decide the two open user decisions in HANDOFF §4 — *which field to publish* (canonical 414/42/2 is the honest choice, with the reconciliation sheet) and whether the corrected-DEM LOFO ever replaces `spread_v2_lofo.json` — and approve or veto item #6 in writing so the agent can proceed.

---

## 4. Blunt summary

The science is more honest than most university submissions, and that is the project's real asset for "scientific thinking". The problems are (i) too many of its own hypotheses are dead (H2, H3a, the ordering, the household-layer fire effect), (ii) the prediction layer is weak at the operating point (recall 0.138, 3/6 folds catch nothing, one leaked feature), (iii) the submitted 서식 and the repo disagree on at least seven numbers, and (iv) prior work / schedule / roles — explicit Track-B rows — are thin. Items #1–#5 fix (iii) and (iv) and turn two of the hardest questions into measured answers within days; nothing on the list touches the frozen lineage or the submitted theme. The student's own critical path is rehearsal against §2 and the two portal downloads.
