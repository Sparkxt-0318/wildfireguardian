# WildfireGuardian autoloop · dev · 2026-09-05T0702Z

| | |
|---|---|
| when | 2026-09-05 07:02 UTC · 2026-09-05 16:02 KST |
| branch / head | `auto/dev` / `c2693b7` |
| kind | dev |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 6, done: 16, parked: 1, todo: 58 |

## What happened this lap

Lap 2026-09-05T0625Z, row **WFG-100** (P0, KCF, the one `fix-before-next-row` item from
critic #16 and row 1 of `docs/auto/DIRECTION.md`). Claim `039a0de`, work `c2693b7`.

## Your decisions, applied

**Nothing new reached the loop, for the third lap running.** The Gmail search
(`from:siyeong0318@gmail.com subject:"WildfireGuardian autoloop" newer_than:14d`) returns
45 threads and every one is a single message — this loop's own report, sent to you. No
reply carries an `NH-###:` line, so `docs/auto/decisions_seen.json` is untouched. PR #31
has no comments, so the second channel is empty too. Three DECISION entries are still
open and waiting on you: **NH-014**, **NH-025**, **NH-026**.

## What this lap did

`docs/auto/DEMO_SCRIPT_5MIN.md` §5 said its six segment times came from 「문장 수와 한국어
발화 속도」. Nobody had done that arithmetic and no test could ask the question:
`tests/test_demo_script_5min.py` asserts the six numbers **sum to 300**, which is true of
25/45/55/75/55/45 and of 50/50/50/50/50/50 alike. Critic #16 measured the spread — 1.67×
between the fastest and slowest segment — and found that the segment which had to be
spoken fastest was **마무리 · 한계**, the limitations close, which is **last**, and is
therefore the material a running clock eats, once per each of the five judges. That is
the material 과학적 사고 is scored on.

**Built** (`c2693b7`):

- `scripts/measure_demo_script_pace.py` counts the syllables a student actually
  pronounces in each of §1's six segments — Hangul syllable blocks, numerals read
  sino-Korean (`2,008` → 이천팔), symbols and Latin from an explicit lexicon — and
  allocates 300 s by largest remainder. **1,684 syllables, 5.61 syllables/second,
  29 / 44 / 50 / 60 / 59 / 58 s.** The spread closes from **1.62× to 1.02×**, and
  마무리 · 한계 goes from 45 s to **58 s**.
- `data/processed/demo_script_pace/pace_20260905T0625Z.json` (the shipped measurement) and
  `pace_before_039a0de.json` (the budget it replaced, plus all three counting variants), both
  un-ignored by name in `.gitignore` with their reason. Four registry keys,
  `demo_pace_<tag>_total_spoken_syllables` and `demo_pace_<tag>_rate_spread` for each of the
  two measurements, added by an additive registrar in the same script that refuses to edit an
  existing value — `build_numbers.py` was never re-run.
- `docs/demo_script_pace.md`: method, result, how much of the count is a judgement call,
  four things it does not show, and the re-measure procedure.
- `tests/test_demo_script_pace.py`, nine tests that **recompute from the committed
  document** rather than pinning a stored census — MEMO 2026-09-05's own lesson, assert
  the property not the census.
- `docs/arm_protocol.json`: arm `booth_pace` declared (a measurement that fits no model
  and needs no noise control, tagged separately so nobody mistakes a document
  measurement for a modelling one). `docs/artifact_manifest.json` rebuilt.

**No sentence was deleted to buy seconds.** The row allowed trimming a segment still over
its share; proportional allocation puts no segment over its share by construction, so
every CHARTER §3.5 caveat stayed where it was.

## The root objection I recorded against my own plan (`hate`)

**"The re-budget rests on a syllable count whose method is a pile of judgement calls, and
putting it in a script and a registry makes an unmeasured convention look measured."** The
1.67× spread is real however you count, but the six new numbers are only as good as the
tokenizer's treatment of `%`, `ha`, `OSM`, `STATIC VIEW`. A tokenizer that scores an
unknown token as zero under-counts exactly the segments densest in symbols and hands the
student a budget wrong in a *new* way, with a green test on top.

**Cheapest test, and it was run, twice:**

1. Make the tokenizer **refuse** an unknown token instead of scoring it zero. It fired on
   the first run — `pooled`, in the limitations close, was two syllables the count was
   missing. That failure is now pinned by a test.
2. Run the measurement under both extreme conventions. `full` (Latin and symbols spoken)
   gives 1,684 syllables and 29/44/50/**60**/59/**58**; `hangul-only` gives 1,627 and
   30/44/50/**57**/59/**60**. **The largest disagreement is 3 seconds out of 300**, and
   four of six segments move by one second or none. The objection is answered: the budget
   does not rest on how `%` is pronounced. Both sets of figures are in the doc, and the
   doc says they are never to be mixed.

I then drew a conclusion from that which does not hold — that the `hangul-only` total of
1,627 landing within three of critic #16's 1,630 showed the two conventions matched. It does
not, and the reviewer took it apart per-segment; see finding 4 below. The sensitivity result
survives; the reconciliation does not.

## The independent reviewer BLOCKED this lap, and was right

`LOOP_CONFIG.json` → `review: subagent`. A fresh agent that had not seen the work read only
the diff `039a0de..c2693b7` and the claims above, and returned **`block`**.

**Reviewed by: subagent (block → fixed): "The lap measured only the state it created, then
presented the whole before/after as measured. Exactly one artifact was committed — the AFTER
state. Every number that justifies the change traces to nothing."**

It was right, and its first nail was sharper than its root objection. Everything below is
fixed in this lap, not deferred:

1. **The before state had no artifact.** The booth script told a student 「고치기 전에는
   4.51에서 7.29까지」 about a version of the document that the same commit had overwritten.
   Fixed: `--from-ref` measures the document at any commit, and
   `data/processed/demo_script_pace/pace_before_039a0de.json` is committed with the old
   budget, its rates and all three counting variants. The spread before the fix and the spread
   after it are now two registered keys (`demo_pace_039a0de_rate_spread` and
   `demo_pace_20260905t0625z_rate_spread`) rather than two remembered numbers, annotated
   `collision-ok` where the page prints them side by side.
2. **The re-measure procedure I shipped broke CHARTER §3.2.** `--register` upserted, so the
   *documented next step* would have edited a registered value in place — and the doc named
   WFG-101 as the lap that would run it. Fixed: registry keys now carry the measurement's
   identity (`demo_pace_<tag>_…`), a re-measure **adds** keys, and `--register` **refuses**
   to change the value of a key that already exists. The two un-stamped keys the first commit
   introduced were removed rather than left as a broken precedent; they were created and
   superseded inside this same lap and never reached `origin`.
3. **A claim about Korean that is simply wrong.** 「numerals are read sino-Korean」 is an
   approximation: counter words take native numerals (세 시간, 여섯 개, 스무 가구). Fixed by
   measuring it rather than conceding it — `--variant native-counters` gives 1,686 syllables
   and 28/44/50/60/59/59 s, so **the approximation costs at most 1 second of 300**. The doc
   now states the rule *as* an approximation and prices it.
4. **The reconciliation I claimed does not hold, and the reviewer proved it in one command.**
   I wrote that critic #16's 1,630 total was 「the best evidence available」 that it counted
   hangul-only. My own hangul-only variant gives per-segment 161/237/274/312/320/323 against
   critic #16's 161/235/279/318/319/318 — differing by 0, +2, −5, −6, +1, +5 — and a spread of
   **1.73x, not the 1.67x** critic #16 reported. The totals agree within three only because
   the segment errors cancel. Fixed: the page now has a section headed 「What could not be
   reconciled, and is not papered over」 saying I could not reproduce critic #16's numbers
   under any convention implemented here, and keeping only what survives — every convention
   anyone has counted with finds a spread between 1.62x and 1.73x and puts 마무리 · 한계
   fastest and last.
5. **The mixed-convention pair I said I had fixed was still in the diff**, in
   `tests/test_demo_script_pace.py`'s module docstring (「4.24 to 7.29 ... 1.62x」 — and
   7.29/4.24 is 1.72, so it was not even arithmetic). Corrected, and the docstring now records
   that it shipped that way and was caught.
6. **The measurement has a fixed point in it.** 마무리 says 「마지막 58초는」 — a number the
   allocation produced, inside the text the allocation is computed from. It did not bite only
   because 사십오 and 오십팔 are both three syllables; at 61 s (육십일) it would. The doc now
   carries this under its own heading, and it is the reviewer's find, not mine.
7. **`docs/artifact_manifest.json` was committed stale** (built before the doc that cites the
   artifact existed, `cited_by_docs: []`, `regenerate: UNKNOWN`). Rebuilt last, after every
   doc; both artifacts now carry their exact regeneration command.
8. **A dead guard.** `forbidden_phrasings` on the new entries listed 「리허설을 대신」 — a phrase
   the booth script uses correctly, to say it does *not* replace a rehearsal. Replaced with
   phrasings that would actually be false, and checked that none appears anywhere.
9. **The backwards ratio**, 「최대 대비 최소 1.02배」, in the one document read aloud to judges.
   Corrected.

What the reviewer checked and could not break, worth recording because it is the deliverable:
it reproduced the six seconds independently, ran both variants and confirmed the ≤3 s
sensitivity, confirmed **no caveat was deleted** (the only spoken line changed in the whole
diff is 「마지막 45초」 → 「마지막 58초」), broke the test suite deliberately with an added
sentence and saw 5 of 9 tests fail correctly, and found no frame pivot.

One finding I am **not** fixing in this lap and am naming instead: `scripts/build_numbers.py`
rebuilds the registry from its own list and would drop these keys along with every other
additive registrar's. That is **WFG-040**, already filed, and it is why the registrar here is
additive; fixing it is a different row.

## What did not work, and what I caught in my own prose (before the reviewer)

- **I mixed the two conventions' figures in three places** before any gate ran — writing
  「4.24에서 7.29까지」, which pairs critic #16's slowest segment with my own fastest. Caught
  on a self-read, corrected in the script docstring, `docs/demo_script_pace.md` and §1 of
  the booth script. This is exactly the failure the doc now warns about, committed by the
  lap that wrote the warning.
- **`sip` found a duplication `ssotize` would have flagged later:** `docs/demo_script_pace.md`
  restates the six seconds, so the page explaining the budget could go stale against the
  budget. Closed with a test that parses that table and compares it to the live
  measurement. Mutation-checked: changing one second in the table fails it.
- The first `verify` run went red twice, on things worth recording: the artifact was
  git-ignored (`data/processed/**`), and `arm: "docs"` was not a declared arm. Both fixed
  properly — an un-ignore line with a reason, and a real arm declaration — rather than by
  removing the registry entries.
- **A redundancy I did not hide:** three tests assert what is essentially one property,
  and the band test is *implied* by the allocation test. The docstring says so rather than
  counting three dependent checks as three confirmations.

## Evidence

| what | where |
|---|---|
| measurement | `data/processed/demo_script_pace/pace_20260905T0625Z.json` (after), `pace_before_039a0de.json` (before, all three variants) |
| method + limits | `docs/demo_script_pace.md` |
| the script | `scripts/measure_demo_script_pace.py` (`--print`, `--variant`, `--from-ref`, `--register --tag`) |
| tests | `tests/test_demo_script_pace.py` (9), mutation-checked by me on 2 mutations and by the reviewer on a third |
| registry | `demo_pace_039a0de_rate_spread` = 1.62 → `demo_pace_20260905t0625z_rate_spread` = 1.02; both totals 1684 |
| gates | `--mode full` re-run after every fix above; the gate table in this report names the head it was run at |

## What this does not settle

Whether 5.61 syllables per second is **sayable**. It is an arithmetic consequence of
dividing this text by this budget, not a measurement of speech; this repository has
asserted no comfortable rate for spoken Korean and this lap did not start. A person with a
stopwatch is **R12 / NH-014**, and the doc, §1 and §5 of the script all say so in the place
they are read. The measurement also ignores pauses, breaths and the five interruptions §2
guarantees, so a real delivery is longer than its text.

One consequence the next lap should expect: **adding a spoken sentence now turns the gate
red on purpose.** WFG-101 wants exactly one sentence in 3막. The fix is the re-measure
procedure in `docs/demo_script_pace.md`, never a relaxed band.

## Next row

**WFG-037** — the booth recipe `docs/auto/finals/BOOTH_SETUP.md`, which DIRECTION.md names
as row 2 and which critic #16 set as the falsifiable test of whether that page is steering.

## In plain terms

- **What changed for the project.** Your five-minute booth script used to divide the five
  minutes into six parts by guesswork. I counted the syllables you actually have to say —
  1,684 of them — and split the 300 seconds in proportion. The big change: the closing
  「한계」 section, where you admit what the project cannot do, went from 45 seconds to 58.
  It had been the part you had to rattle off fastest, and it comes last.
- **Why it matters to the judges.** 과학적 사고 is scored on the honest part, and the honest
  part is at the end where the clock runs out — five times, once per judge. Nothing was cut
  to find those seconds; the other segments simply had more time than their words needed.
- **What you should do.** Read the closing section out loud once with a stopwatch and tell
  me if 58 seconds is enough. That is the one half of this a computer cannot do, and it is
  still open as NH-014. Three decisions are also still waiting on you: NH-014, NH-025,
  NH-026.



## Gates

**ALL GREEN** · mode `full` · head `1fd774c` · 2026-09-05T07:12:45Z · current at `1fd774c`

*This block was corrected by hand after `report.py` wrote it. `report.py` runs BEFORE the
lap's final gate run (CHARTER §4 step 8), so the table it generated named the RED run at
`c2693b7` — a collision the next commit fixed — and the report would have told you the branch
was red while it was green. The run below is the one at the pushed head; `--assert-head` and
`--assert-reported` both exited 0 against it immediately before the push.*

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 17.0 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.1 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 0.3 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 237.0 s | 1490 passed, 56 skipped, 52 warnings in 235.59s (0:03:55) |

## Commits since the previous report (c37f27e36ae60aba6493e4495967fa71f9fc57c4..HEAD)

- c2693b7 WFG-100: the booth script's five minutes is now counted, not asserted, and one rate buys the limitations close 13 seconds
- 039a0de claim WFG-100 (20260905T0625Z)
- 10ed981 the two gates that caught this lap's own prose, recorded in the report they caught
- 1049db4 critic #16: the booth script's five minutes is a budget nobody measured, and its six segments cannot all be true

## Needs a human (6 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-025 [DECISION] `Main` cannot follow the green commit until you change one GitHub setting, and until you do, every green push emails you a red run (by 2026-09-08)
- NH-026 [DECISION] One of the five routines pushes on a gate that does not run the test suite, and its prompt is the one the repository cannot show you (by 2026-09-08)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
