# WildfireGuardian autoloop · critic · 2026-09-10T2028Z

| | |
|---|---|
| when | 2026-09-10 20:28 UTC · 2026-09-11 05:28 KST |
| branch / head | `auto/dev` / `be39dea` |
| kind | critic |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 10, done: 33, dropped: 1, parked: 1, title-derivable: 1, todo: 118 |

## What happened this lap

Critic lap #61. Reviewed head `be39dea` on `auto/dev`. Window since critic #60:
`71e95ee..be39dea`, five commits, of which one is the 1817Z research lap and four are report
or archive commits. 24 h look-back to 2026-09-09T18:40Z read in full. This lap changed no
code, no test, no data, no figure and no committed artifact; it wrote only under `docs/auto/`.

⚠ The clone opened **SHALLOW at 50 commits**, shorter than the 24 h window itself, so
`git log --since` would have returned the whole clone and truncated in silence.
`git fetch --unshallow` was run before anything was counted: `--is-shallow-repository`
answers **false**, `rev-list --count HEAD` answers **730**. Every ancestry statement here is
licensed under CHARTER §4.

## Your decisions, applied

**Nothing new from you on either channel, and this is the sixth consecutive lap saying so.**
Gmail `from:siyeong0318@gmail.com subject:"WildfireGuardian autoloop" newer_than:14d` returns
25 threads on the first page and **every one holds exactly one message**, all of them the
loop's own sends, so no reply is threaded under any of them. **PR #31 has zero comments**,
read through the GitHub MCP. `docs/auto/decisions_seen.json` is unchanged: its `applied` list
still ends at **NH-031**, closed 2026-09-06 in a Claude Code session on your laptop. No
`NH-###:` line has ever reached the loop by email, and `scripts/auto/decisions.py` was
therefore not run this lap. **24 entries are open** (23 DECISION + 1 BLOCKER) plus 5 FYI;
**NH-046 is due today and is not overdue**; NH-049 and NH-051 come due tomorrow.

## Verifying the loop's own claims

- `gates.py --mode full` at `be39dea` exits **0**: `verify` PASS 18.9 s, `baseline-verify`
  **WARN** on the two git-ignored `data/raw/**` contracts (NH-029 and §3d working as decided),
  `snapshot-verify` PASS, `env-check` PASS, `pytest-full` PASS **2042 passed, 63 skipped,
  3 xfailed** in 282.0 s. `--assert-head` and `--assert-reported` both exit 0.
- **GitHub `auto-gates`, runs 294 to 343: ZERO `failure`**, with run **343** `success` at this
  exact head. **No CHARTER §4b finding #1, the tenth consecutive lap.**
- **Every dev report in the window carries `Reviewed by:`** (checked one by one over the 20
  reports from 2026-09-09T2023Z to now). The 1835Z research report carries none, **and that is
  by design, not a defect**: only the dev and paper routines have a reviewer step, and no
  research report has ever carried one.
- The pass/skip counts critic #60 published (2041/64 at the reviewed head, 2042/63 at the
  pushed head) reconcile with this lap's 2042/63; critic #60 recorded the cause in its own
  report and it is not re-filed.

## Findings, ranked

**1. (WFG-240, P0, KCF, new) The printed 관련연구 panel names this project's spatial unit and
its own differentiator in the household register `WC-013` withdrew, and no gate can see either
spelling.** `docs/auto/finals/RELATED_WORK_PANEL.md:46` reads 「공간 단위는 **가구(집)**」 as the
third row of a three-way comparison whose other rows are NIFoS's and G-DAPS's 「**읍면동**」
(`:43`); `:119-120` reads 「**조사한 범위에서 찾지 못한 것**은 그 발상을 농촌 **가구 단위의 도보
대피**에 ...」, the sentence that states what this project does that the survey did not find.
The same file already carries the correction 73 lines above the second one: `:23` says
「**지점 단위의 대피 경로와 구조 출동 순서**」 and `:27-31` is the ⚠ 2026-09-10 정정 block naming
WFG-222 and WC-013, which scopes itself to 「**이 문단**의 단위 낱말」 and leaves the rest of the
sheet in the old register. **It is on the paper, verified rather than assumed:**
`docs/auto/finals/printables/manifest_20260910T1233Z.json` lists this file among its seven
`sources` and all **seven** hash equal to the tree at this head, re-computed in this lap's own
process, so `WFG_printables_20260910T1233Z.pdf` carries both lines to the booth.
**Why no gate sees it, which is the part worth keeping.** WC-013's Korean pattern is anchored
on 「가구 단위」 **plus** one of {걸어서 나갈, 대피 판정, 구조 순서, 출동 순서, 판정과 걸어, 인명},
deliberately, so that Q20a's definition and Q16's quantity do not trip. 「공간 단위는 가구(집)」
contains no 「가구 단위」 at all, and 「가구 단위의 **도보 대피**」 matches no alternative in the
list. **And a spelling that covered it would still miss this one**, because the phrase wraps the
source line break between `:119` and `:120`, which is WFG-223's measured one-line-at-a-time
limit. `docs/creativity_card.md:721-726` predicted this instance in writing: 「a fourth,
differently-worded copy of the same claim escapes both gates」. This is that copy, and it is
printed.

**2. (WFG-241, P0, KCF, new; carved out of WFG-239) The judge question this week made
unavoidable has no card, and the row that was going to write it sat at P1/IEEE at position 233
of 233.** The question is 「비슷한 걸 만든 사람이 이미 있지 않습니까?」. Bokade, Barai, Bhogle,
Chapherkar & Kale, Zenodo preprint, **2026-09-09**, concept DOI `10.5281/zenodo.22668357`,
couple a U-Net flammability prior to a CA physics engine and then route away from actively
predicted fire. `docs/auto/JUDGE_QA.md:1246-1247` (Q29a, 없는 것 item 4) tells the student
「문헌 조사는 저희가 열어 본 자료의 범위이고, **그 범위 밖에서 같은 일을 한 연구가 있는지는
모릅니다**」, and item 3 sends the judge to Q16a and the 관련연구 패널, neither of which names it.
The posture is right; what changed is that something **inside** the range did it. Under
DIRECTION's own 「do not start a P1 row while a P0 row is `todo`」 with 15 P0 rows `todo`, a
P1/IEEE row at the bottom of the table cannot be reached before the freeze, so the card half is
now a P0 KCF row and WFG-239 keeps the manuscript and `references.bib` half.

**3. (NH-057, DECISION, HIGH, new) The object the project now says it contributes has never
been produced from a real fire, and no cloud lap can produce one.** `docs/auto/DIRECTION.md`'s
thesis, rewritten two days ago, is 「the **output object and its measured limits**」. The
committed instances are `outputs/dispatch/20260801T163042Z/**`, made in a run whose
`provenance` records **화재 위험면과 지형은 합성** and **출발지는 표본 좌표**, and every surface
ends on 「**실제 확산면으로 만든 출동 지시서는 아직 없습니다**」. Measured here rather than
assumed: `scripts/generate_dispatch_outputs.py:43` needs a per-origin artifact
(`destinations`, `dispatch_top20`, `unreachable_homes`, `four_way_counts`), and
`data/processed/real_roads_real_hazard_canonical.json` carries counts only
(`n_origins_scanned` **458**, `n_shelter_nodes` **46**, `n_nodes` **8443**, `n_edges` **21982**,
**no per-origin record at all**). Re-running the routing needs
`data/cache/osm/yeongdeok_2025`, which is git-ignored and reaches no sandbox:
`tests/test_rescue_routing_real.py:71-109` skips on exactly that path inside the green run at
this head. Four options, nothing proposed for deletion, and option C (「the sentence stays」) is
explicitly not a bad outcome. The row is **WFG-242**, `blocked(NH-057)`.

**4. (WFG-239, corrected in place) The row's headline was false against the manuscript, and it
cited no manuscript line.** As filed it read 「and **the manuscript claims the architecture as
its contribution**」. Checked in three places: `paper/manuscript.md:29-31` (Abstract, 「the
transferable contribution is the **evaluation design** ... rather than the model」), `:84-88`
(Introduction, 「**We make three claims**」, and none of the three is the architecture) and
`:821-833` (Conclusion, 「is **not a contribution to spread modelling**, and this paper does not
present it as one」 ... 「The rest of the contribution is the **instrument**」). A lap acting on
the row as filed would have gone hunting for a claim to retract that is not there, and the
nearest thing it would have found is a correctly scoped contribution statement. The real gap is
narrower and the row's own deliverables already address it: the manuscript's related work does
not mention the preprint at all.

**5. (no row; resolved on `DIRECTION.md`) The board's own P0 count is knowable, and both of the
research lap's rival parses are artifacts.** Re-counted here from column 2 of every line
beginning `| WFG-`: **236 rows**, **94 P0** (73 written `**P0**`, 21 written bare `P0`), **15
with a `todo` cell**, which is exactly critic #60's number. 「21 rows / 3 todo」 counted only the
bare spelling; 「108 / 28」 is a substring match over the whole line. WFG-191's column-shift
defect is real (four status cells still read 「법적\」, 「프라이버시」, 「' '/^\」, 「예산」),
separate, and does not move this count. **No fourth number was published. Use 94 / 15.**

**6. (no row; corrected inside WFG-239) A version DOI is called a 「duplicate」.** Re-fetched in
this lap's own process: the Zenodo record at id `22668357` returns `doi: ...22668358`, and
`22069027` returns `...22069028`. One record, two DOIs, concept and version. The knowledge note
cites the concept DOI, which is the correct one to cite; 「duplicate」 is the wrong word and it
matters because WFG-239 asks a lap to add a `verified` `references.bib` entry.

## `factchk` on the window's new prose about the world

**Both external records re-fetched here and both verify.** Bokade et al., 「A Hybrid Neural
Physical Framework for Wildfire Propagation Modeling and Dynamic Evacuation Routing」, Preprint,
publication date **2026-09-09**, five authors exactly as the note names them; the 「persistence
bias」 sentence and 「neither a recurrent nor a non-recurrent network learns to advect fire past
the persistence baseline」 are in the record's own abstract. Choi & Chae, 「Data and code for:
Deep learning prediction of wildfire burned-area extent and burn probability ...」, Dataset,
**2026-09-01**, Kangwon National University, **118** Korean events 2018-2025 with a **102**-event
operating-envelope subset, and **no metric value stated**, exactly as the note records. The
research lap also marked the 「위험 구역 / 잠재적 위험 구역」 zoning framing **UNVERIFIED** and
forbade writing it, which is the right call and is left standing.

## Root objection (`hate`), and its cheapest test

**The project has just moved its entire novelty claim onto the one artifact class whose
real-data instance does not exist.** Every judge-facing surface says so honestly, which is
exactly what makes the objection load-bearing rather than a gotcha. **Cheapest test, and it was
run in this lap:** point `generate_dispatch_outputs.py --source` at the real-hazard artifact.
It cannot be done, for the reason in finding 3, and that is NH-057.

## Direction check (§3b)

**ZERO reorders, the fifth consecutive critic lap to spend none.** WFG-233 stays the top row and
is still the right one: it is the only cheap P0 that touches the **submitted** document and it
pays no printables rebuild. One ordering note that costs no reorder is written onto
`DIRECTION.md`: **WFG-235, WFG-240 and WFG-241 should be taken by one lap**, because each alone
pays the same `make printables` rebuild and bundle re-point (NH-049).
**Readiness lines ticked in the last 24 h: ZERO, the eighteenth consecutive critic lap.** This
window could tick none by construction, and the structural cause is unchanged and is not the
product's: R3 is `blocked(NH-046)`, R12 is NH-014, R11 is downstream of R3. Appended to
**NH-046** rather than filed again.

## `fix-before-next-row`: ZERO

Measured at `be39dea`. No gate is red locally or on GitHub, and both judge-facing defects
(WFG-240, WFG-241) edit hashed printables sources, so each pays `make printables` at a new stamp
plus a re-pointed `release/kcf-finals-2026/MANIFEST.json`. That is a price tag, not minutes.
The next dev lap takes the top row directly.

## Scorecard deltas

**Track B 96 → 95** and **Track A 97 → 96**, one row on each and it is the same row:
**제출 자료 19 → 18**. Everything else HELD. The window's diff moved nothing; the measurement
changed, on critic #59's precedent in that file that the instruction is to score today's state.
The evidence is new even though the defect is not: WFG-240 is on the **printed paper**, it is
the fourth open 제출 자료 defect, and two of the four are now on paper rather than in `docs/`.
**Pre-registered for critic #62:** 제출 자료 returns to 19 when WFG-240 and WFG-241 close with
the kit rebuilt, and to 20 when WFG-233 and WFG-236 close as well.

## In plain terms

**What the judges would see today.** A strong project with one embarrassing paper cut: the
printed 관련연구 sheet in your booth kit says your system works 「가구(집) 단위」, house by house,
in two places, while the same sheet says 「지점 단위」, point by point, near the top. Point is the
honest word, the sheet already knows it, and a judge reading both will ask which is true.
Separately, an Indian group published almost your exact architecture on 2026-09-09, and your
Q&A card still tells you to say you do not know of anyone who did.

**What changed since yesterday.** Nothing was built: the 18:17 slot was the research lap, and it
did good, careful work. I checked both papers it found and both are real and described
correctly. I filed three rows and corrected one that was telling the next lap something untrue
about your own manuscript.

**What you should do.** Answer **NH-057** if you answer only one thing. Your project now says its
contribution is the dispatch documents, and the only ones that exist were made on a simulated
fire; the run with a real fire cannot produce them without the OpenStreetMap cache that lives
only on your laptop. Option B is cheap: one run that saves the per-point records, after which
the cloud can do the rest. Option C, leaving it as it is, is genuinely fine too, because you
already say so in writing. **NH-046 is due today** and it is still the single reply holding 102
backlog rows shut.



## Gates

**ALL GREEN** · mode `full` · head `be39dea` · 2026-09-10T20:04:53Z · current at `be39dea`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 18.9 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.1 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 1.8 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 282.0 s | 2042 passed, 63 skipped, 3 xfailed, 52 warnings in 281.08s (0:04:41) |

## Commits since the previous report (9a45b2e9ad9c45f9526d0d75b3ab1e10178a75a6..HEAD)

- be39dea the lap's gate-failed first report is archived, not left loose and not filed as a report of record
- fbc2273 the research report names the head it ships in, and says why its own kind still needed the fix
- ae4b1f2 the routing arm's novelty is the measured output object, because someone else published the architecture on 2026-09-09

## Needs a human (29 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09, one day past; raised to HIGH by critic #55 on a measured third instance)
- NH-036 [DECISION] One critic lap told the next one not to edit a file, and that is what kept a false sentence in front of a judge for a window (by 2026-09-10)
- NH-037 [DECISION] The paper's word proxy now stops it a thousand words before your 25-page rule (by 2026-09-10)
- NH-038 [DECISION] Your "product first" rule has spent the last three dev laps on documents, and the readiness line it was written to protect has not moved in five critic laps (by 2026-09-09)
- NH-039 [DECISION] The national wildfire-spread system's manual is an 18 MB PDF the sandbox could not fetch, and one of you can (by 2026-09-12)
- NH-040 [FYI] A critic lap pushed one commit past a red `--assert-reported`, and it is telling you rather than hiding it
- NH-041 [FYI] This lap sent you an email containing only the word PLACEHOLDER, and could not take it back
- NH-042 [DECISION] Two of your own rules collide whenever a withdrawn claim lives in a frozen artifact, and this week they collided three times (by 2026-09-10)
- NH-043 [DECISION] A gate your loop built this morning will go red about twice a day, and the charter tells the lap that meets it to stop working (by 2026-09-09)
- NH-045 [BLOCKER] The staleness gate has closed `auto/dev` to every routine, and the one routine that met it is the one forbidden to clear it (by 2026-09-08)
- NH-044 [DECISION] The claim the paper just retracted is still live on the page the paper cites for it (by 2026-09-09)
- NH-046 [DECISION] Your product's definition-of-done names a command nothing in this project has ever run (by 2026-09-10)
- NH-048 [DECISION] One of the research routine's three literature channels has been dead for two runs, and a working replacement is already proven (by 2026-09-10)
- NH-049 [DECISION] Your critic routine is told to add judge Q&A cards and your own printing gate makes that impossible for it (by 2026-09-11)
- NH-050 [DECISION] You answered two of these questions two days ago and the loop never heard you, because you answered them on the routine page (by 2026-09-10)
- NH-051 [DECISION] The rule your loop has been obeying for three days is not the option it names, and the difference is why one row has been pushed down five times (by 2026-09-11)
- NH-052 [DECISION] The experiment that measures what your own forecast is worth is now one run away, and it would change what 42 means (by 2026-09-12)
- NH-053 [DECISION] The word your front door uses for 42 stopped being right today, and the loop cannot pick its replacement (by 2026-09-12)
- NH-054 [DECISION] Four fifths of your front door's headline is limits, and no lap is allowed to decide whether that is the project's strength or its biggest presentation risk (by 2026-09-13)
- NH-055 [DECISION] Your front door compares the headline forecast number to a model the same page calls broken, and today the loop measured a second, harsher comparison it is not allowed to put beside it (by 2026-09-13)
- NH-056 [DECISION] A Korean university published a reproducible deep-learning wildfire model four weeks before your finals, and a lap may not decide on its own whether to mention it (by 2026-09-13)
- NH-057 [DECISION] The one thing your project now says it contributes has never once been produced from a real fire, and only your laptop can change that (by 2026-09-13)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
