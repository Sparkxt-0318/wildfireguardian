# WildfireGuardian autoloop · critic · 2026-09-05T0517Z

| | |
|---|---|
| when | 2026-09-05 05:17 UTC · 2026-09-05 14:17 KST |
| branch / head | `auto/dev` / `c37f27e` |
| kind | critic |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 6, done: 16, parked: 1, todo: 59 |

## What happened this lap

Critic lap #16. Window `466900b..c37f27e` on `auto/dev`, seven commits; the 24 h window is
60 files and 2,971 insertions. No code, no test, no artifact and no `docs/NUMBERS.json` entry
was touched by this lap. Everything below was measured in this sandbox, not read from a report.

## Your decisions, applied

**Nothing new, for the second lap running.** The Gmail search
(`from:siyeong0318@gmail.com subject:"WildfireGuardian autoloop" newer_than:14d`) returns
44 threads across two pages and **every one of them is a single message** — this loop's own
report, sent to you. There are no replies, so no `NH-###:` line reached the loop by email and
`docs/auto/decisions_seen.json` is unchanged. PR #31 has no comments at all, so the second
channel is empty too.

Three DECISION entries are open and waiting on you: **NH-005** (Yeongdeok building
footprints), **NH-014** (run the booth recipe once on the real laptop), **NH-025** (`Main`
cannot follow the green commit until you change one GitHub setting). This lap opens a fourth,
**NH-026**, below.

## Findings, ranked

### 1. The one `fix-before-next-row` item — WFG-100, and it is the five minutes the whole booth turns on

`docs/auto/DEMO_SCRIPT_5MIN.md` §5 says the six segment times were set from
「문장 수와 한국어 발화 속도」. Nobody has checked that arithmetic, and the tests cannot:
`tests/test_demo_script_5min.py` asserts the six numbers **sum to 300**, which is true and is a
different question. Measured this lap over the spoken blockquote lines only (Hangul syllables
plus numerals read as sino-Korean; ⚠ blocks, the mapping table and the DRAFT header excluded,
because nobody says them):

| segment | declared | spoken syllables | implied syllables/second |
|---|---:|---:|---:|
| 도입 | 25 s | 161 | 6.44 |
| 1막 · 발견 | 45 s | 235 | 5.22 |
| 2막 · 시간이 도로망을 바꿉니다 | 55 s | 279 | 5.07 |
| 3막 · 같은 출발지, 두 개의 답 | 75 s | 318 | 4.24 |
| 4막 · 예측을 판단으로 | 55 s | 319 | 5.80 |
| 마무리 · 한계 | 45 s | 318 | 7.07 |
| **total** | **300 s** | **1,630** | **5.43** |

The finding does not rest on knowing the true comfortable rate for spoken Korean, and I am not
asserting one. It rests on the spread: **4.24 to 7.07 is 1.67×, inside one document, from one
stated method.** One rate cannot satisfy both ends. If 3막's 4.24 is right the script needs
**385 s**; if 마무리's 7.07 is achievable it needs **231 s** and 3막 is 30 s too long. At least
one of the six numbers is wrong and probably five are.

Why this is the item rather than a note: the segment that must be spoken fastest is
**마무리 · 한계**, the limitations close, and it is **last**. `docs/auto/RUBRIC.md` 심사개요 says
「발표는 5분을 넘지 않는 것을 권장; 길어지면 중단될 수 있음」. So the material that gets cut when
the clock runs out is the material 창의성 and 과학적 사고 are scored on — the withdrawn claims,
the 0.138 recall, the 「20가구를 구했다가 아니라」 correction — and it gets cut five times, once
per judge. The three **[버림]** sentences buy back 84 syllables, about 17 s, and §2 of the same
document guarantees interruptions that will spend it.

Done when the six times are re-derived from a per-segment syllable count at one rate, the table
and every cumulative bracket still summing to 300 s (proportional re-budget at 5.43 syl/s is
**30 / 43 / 51 / 58 / 59 / 59 s**), any segment still over its share trimmed of text carrying
neither a number nor a caveat, and the test gains a per-segment syllables-over-seconds band.
**No caveat is deleted to buy seconds** (CHARTER §3.5) — that is exactly what WFG-095 was about,
and WFG-095 closed cleanly this window. Cheapest test if the row is doubted: read 마무리 aloud
once and time it.

### 2. WFG-101 (P0, new) — the headline number has a 「before」 value in this repository and no judge-facing document says so

`docs/dem_defect_2026-08-02.md:67-70`: re-acquiring the DEMs at `9ba83b4` — Uljin-Samcheok's
raster had filled the East Sea with a ramp to −497 m, and that region trains every other fire's
leave-one-fire-out model — moved 의성·안동's predicted envelope +38 % (95 → 131 core cells) and
its future-aware-only share **from 3.53 % to 24.73 %**. `docs/decision_shift.md:118-151` holds
the origin-level ledger of the same event (346/13/0/9 → 263/91/12/2, 90 origins changed bucket,
arithmetic reconciled). `docs/multi_region.md:8-18` puts it in a warning blockquote at the top
of the page, which is the **first thing a judge who opens that file reads**.

Drilled against the files: `docs/auto/JUDGE_QA.md` (41 questions) contains no `3.53`, no
`DEM 재취득` and no question about the stability of the routing shares.
`docs/auto/DEMO_SCRIPT_5MIN.md` says 24.73 % three times and never that it moved. The `multi_region.md` blockquote records a second
move in the same two lines: 영덕 to the canonical **9.17 %**, off a field the investigation
identified as a run reverted on 2026-07-21. The earlier share on that reverted field is a
retired value and is deliberately not restated here (`docs/auto/withdrawn_claims.json`); the
script quotes 9.17 % with none of that history either.

This is not an error in the result. It is the **best 과학적 사고 answer this project owns** — we
found a defect in our own data, corrected it, recorded both values, and quote the corrected one
— sitting in three files the student has never been given a sentence for. Distinct from WFG-096,
which is about sampling uncertainty; this is sensitivity to a corrected input, and it is
measured rather than absent.

### 3. WFG-096 was carrying the false premise a reviewer had removed from the manuscript six hours earlier

The row critic #15 filed to fix the missing uncertainty statement told the next dev lap that the
368 and 458 origins are 「a census of that region's walk-network origins, not a probability
sample」. They are not.
`data/processed/real_roads_real_hazard_canonical.json` gives `n_nodes` 8,443 against
`n_origins_scanned` 458 at `origin_scan_stride` 18, and `scripts/run_real_roads_real_hazard.py:117-131`
then drops nodes already at the hazard cutoff at t = 0 and nodes outside a band around the core:
a systematic subsample intersected with a hazard-dependent frame, **5.4 % of the graph**. The
paper lap's own reviewer caught the identical sentence in `paper/manuscript.md` §6 and it was
rewritten there at `2b7c3a0`. The backlog was the second place the false framing was living, and
it was the place a dev lap would have copied from. **Corrected in the row this lap.**

### 4. NH-026 (author) and WFG-102 (P1, parked) — a routine pushed red on a gate that does not run the tests, and its prompt is the one the repository cannot show you

`2b7c3a0`, the paper routine's 03:18Z push, broke two tests in
`tests/test_detection_ordering_is_not_claimed.py`. Its own `auto-gates` run **109 was cancelled**
by the next push, so GitHub never finished checking it, and the red surfaced two pushes later on
run **110 at `d2418c2`** — a bare `claim WFG-095` marker that changes no code. The 0439Z ci-red
lap reproduced both failures in its own sandbox, so this was **not** a clean-runner difference:
the branch was simply red for about forty-five minutes and the commit that made it red was never
named by a red run.

The cause is in the paper lap's own report: `docs/auto/reports/2026-09-05T0317Z-manual.md:115`
says it ran `scripts/auto/gates.py --mode quick`. `--mode quick` does not run `pytest-full`,
which is the step that was red. CHARTER §3 rule 9 requires `--mode full` before every push and
CHARTER §12 grants the paper loop no exemption.

And the instruction cannot be audited: `docs/auto/ROUTINE_PROMPTS.md` is titled
「Verbatim prompts of the three cloud routines」, carries four (`dev`, `critic`, `research`,
`ci-red`), and does **not** carry `wfg-autoloop-paper` — against CHARTER §9, which says every
routine prompt stays recorded there verbatim. So the one routine that pushed a red commit this
window is the one routine whose prompt no reader of this repository can see. That half is yours
(NH-026); the concurrency hole that let a cancelled run leave a commit unchecked is the loop's
own (WFG-102, P1, parked behind the readiness lines by §14b).

**This is not finding #1 under CHARTER §4b.** No lap reported a green full gate at a commit
GitHub found red: the paper lap reported a *quick* gate, and the dev lap that inherited the red
found and fixed it inside its own lap. The rule fires on a green report over a red run, and that
did not happen here.

### 5. Two bookkeeping corrections, made rather than filed

- `docs/auto/KCF_READINESS.md`: critics #14 and #15 wrote 「nothing in this window touched
  `web/`, twelfth / thirteenth consecutive window」. `git log -- web/` in this fresh clone says
  `web/finals.html` was changed at **`deeb147`, 2026-09-04T15:32Z** — one line, the commit stamp
  that closed WFG-067 — about four critic windows before #15, not thirteen. The sentence they
  meant is true and is the one to write from now on: the screen's **content** has not changed
  since `dc63a06`, 2026-09-04T07:14Z. Corrected in the file, their text left alone (§3.7).
- `docs/auto/SCORECARD.md`: the `83f49bc` row appears twice and the first of the two spills a
  paragraph into the `A·목적` column, so the combined table stops rendering as a table from there
  down. Left in place under §3.7 and recorded here for a lap that may edit it.

## The root objection (`hate`), on the headline narrative

The narrative is 「예측이 경로를 바꾼다 — 의성·안동에서 출발지 368곳 중 91곳, 24.73 %가 시간 인지
경로로만 대피 지점에 닿는다」.

**The objection:** 24.73 % is a property of *this repository's hazard field*, not of the fire.
The field is produced by a model whose recall at its own operating point is 0.138, and the
percentage measures how often two routing policies disagree on that field. The repository
already holds the evidence that the number is unstable to the field: correcting one DEM moved it
7.0×, and `docs/dilation_perturbation.md` shows that dilating the hazard footprint by about
122 m erases the future-aware route's entire hazard-free margin. The claim that survives all of
that is narrower and is the one the project should lead with: *given a forecast field, planning
against it changes which origins reach a refuge, and the size of that change is bounded by how
wrong the field is — which this project measured on itself and published.*

**The cheapest test**, and it needs no new data and no retrain (so it is inside CHARTER §3 and
DIRECTION's 「what not to do」): re-score the same committed route pairs against a hazard field
built by dilating the canonical one to the 122 m break distance `dilation_perturbation.md`
already identified, and report the FA-only share there beside 24.73 %. If the share survives
dilation, the claim is about roads and geometry and is robust. If it collapses, the honest
headline is the narrower sentence above. Either answer is worth more at a booth than the number
alone. Not filed as a row this lap — WFG-100, WFG-101 and the two booth rows come first, and
a row nobody will reach is noise.

## Direction check (CHARTER §14, five minutes)

**One row moved, and it is the same correction as last lap one line further down.** WFG-037
(P0, the booth recipe, R3's booth half) sat at table line 75, **below fourteen P1 rows**, so the
row a dev lap would take after WFG-036 was a P1 hygiene item and not the thing R9 is actually
waiting on. CHARTER §3b forbids a P0 below a non-P0 and §14b holds those P1 rows behind the
readiness lines, so this was the table contradicting the charter twice rather than a judgement
call. WFG-037 now sits directly under WFG-036; the two new P0 rows were inserted in
priority-correct positions (WFG-100 above both as the `fix-before-next-row` item, WFG-101 below
them), which is not a reorder of an existing row.

Judge-facing line census, same instrument as critics #13 to #15: **382 of 1,754 authored lines
(21.8 %)** in this window, against 27.3 %, 4.9 % and 2.6 % in the three before it. Two
consecutive windows above 20 % after eleven below 5 %. The falsifiable test for critic #17: if
the next lap produces `docs/auto/finals/BOOTH_SETUP.md`, this page is steering; if it produces
another gate or another registry, WFG-084's cap should stop being a proposal and become the rule.

## Readiness

**4 of 11 (R2, R4, R5, R6). No line ticked this window**, and that is not the direction finding
— R4 moved last window, so the 「zero for two consecutive critic laps」 rule does not fire.

**R9 stays ☐ and this lap answered the question WFG-036 v1 put to the critic** instead of
inheriting it. The v1 document asks whether R9 requires a *committed* payload. It does not: the
line's own condition is 「`make finals-bundle` rebuilds it byte-identically」, which presupposes
a build rather than a second copy in the tree. I ran it here — exit 0,
`OK — release/kcf-finals-2026/ rebuilt byte-identically, 16 files`. R9 is held by the printables
(R7 / WFG-007) and the booth recipe (WFG-037), which is why they are now rows 2 and 3 on
DIRECTION.md.

**R4 keeps its tick and takes a second defect on it**, recorded the way WFG-067 and WFG-095
were: WFG-100 above. The tick was for a document that exists and whose numbers verify, and both
still hold — I re-resolved all 33 registry keys in the §3 mapping table (35 rows) against
`docs/NUMBERS.json` and every one matches.

## What this lap verified rather than read

- `gates.py --mode full` at `c37f27e`: **ALL GREEN**, `1475 passed, 62 skipped` in 197.1 s,
  cold. `verify`, `snapshot-verify`, `env-check` PASS; `baseline-verify` WARN, expected
  off-laptop, `hard: false`, sixteenth window and still not a finding. Green at HEAD for a
  **thirteenth** consecutive critic lap. Against critic #15's cold `1464 / 62` at `43710f7`:
  **+11 passed, skips unchanged**, like for like, tenth comparable window.
- `make finals-bundle`: exit 0, 16 files, byte-identical.
- `gates.py --assert-reported --base 466900b`: exit 0.
- Every dev report in the 24 h window carries a `Reviewed by:` line — seven of seven, clean for
  a second consecutive window.
- `auto-gates` on `auto/dev`: runs **111 and 112 are `success`**; run 110 is the window's one red
  and finding 4 is why; run 109 was cancelled.
- `paper/references.bib`: 28 entries, **every one** carries a `verified` note. Spot-checked five
  including `radeloff2005` against Ecol. Appl. 15(3):799-805, doi 10.1890/04-1413. `factchk` on
  the window's new prose claims about the world found nothing to withdraw.

## Scorecard

**Track B 83, held** (연구 목적 16 · 설계와 방법론 15 · 데이터 수집·분석·해석 19 · 창의성 15 ·
제출 자료 18). The paper work this window is the best in the series and does not take the point:
the manuscript sits at 7,467 words against a 7,500 hard fail with 33 of margin, so 논리적 구성 is
now constrained by a budget the loop cannot spend, and the printables (R7) and README Round-4
section (R8) did not move again.

**Track A 76 → 77** (개발 목적 16 · 설계와 방법론 15 · **구현 및 유용성 17** · 창의성 15 ·
제출 자료 14). +1 for a bundle this lap assembled itself rather than read about. 제출 자료 held
at 14 for WFG-100: a row scored on 논리적 구성 does not rise while the document the student
speaks from has a time budget that cannot be true at both ends.

## In plain terms

**What the judges would see today.** For the first time there is something to hand them: a
folder that goes on a USB stick, with the four screens, a ten-step Korean recipe written for the
day itself, and a checksum list that names the file if the stick goes bad. I built it here from
scratch and it came out identical, so it works. What they would still not see is anything
printed on paper, and nobody has yet written what to do if the laptop dies.

**What changed since yesterday.** The five-minute script got safer — the sentences it tells you
to drop when you run long now take their own numbers with them, so you cannot accidentally drop
a caveat and leave its number behind. The paper got three real expert comments written into it
as limitations. Against that, I found one thing on the script and one thing missing from your
Q&A cards, and both are below.

**What you should do.** Two things, both short. One: **read the last 45 seconds of the script
out loud with a stopwatch.** I counted the syllables and that closing section needs about 64
seconds at a normal pace, not 45 — and it is the part where you say what the project cannot do,
which is the part the judges score you highest on. Tell the loop what it actually took. Two:
when you have a moment, answer **NH-026** — one of the five routines is pushing without running
the full test suite, and its instructions are the only ones not written down in the repository,
so I cannot check them for you.



## Gates

**ALL GREEN** · mode `full` · head `c37f27e` · 2026-09-05T05:02:04Z · current at `c37f27e`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 13.9 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.1 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 1.7 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 198.3 s | 1475 passed, 62 skipped, 52 warnings in 197.08s (0:03:17) |

### Post-commit gate runs (CHARTER §4 step 8)

The table above is `report.py`'s run at `c37f27e`, written before this report was committed,
so it does not name the pushed head. The runs after the commit, recorded here rather than by
editing the block above:

| head | result | what it was |
|---|---|---|
| `98c42f2` | **RED** | `check-forbidden` and `tests/test_screen_checks.py::test_the_tree_states_no_retired_claim_above_the_recorded_floor`: **this report's own prose** stated a retired 영덕 share with no caveat within ten lines. The gate was right. Rewritten so the retired value is not restated at all |
| `1049db4` | **RED** | `check-number-collisions`: this lap's correction to the WFG-096 row wrote 「5.4 % of the graph」 on a line whose anchors resolve to `bld_yeongdeok_fa_only_pct` (8.4). Genuinely different quantities; annotated with a `collision-ok` comment naming both, in the idiom `DEMO_SCRIPT_5MIN.md` already uses |
| this head | see `.auto/gates.json` | re-run after the fix above and asserted with `--assert-head` and `--assert-reported` immediately before the push |

Worth recording against this lap rather than for it: a critic whose whole subject this window is
a routine that pushed without running the full suite was itself caught twice by the full suite,
on prose, in the same lap. Both catches were the gates doing exactly what CHARTER §3 rule 3 and
§3.5 exist for, and neither reached `origin`.

## Commits since the previous report (76e0d80f9471387154cf6f3a9369aaa183b6bc3a..HEAD)

- c37f27e report 2026-09-05T0439Z-manual

## Needs a human (6 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-025 [DECISION] `Main` cannot follow the green commit until you change one GitHub setting, and until you do, every green push emails you a red run (by 2026-09-08)
- NH-026 [DECISION] One of the five routines pushes on a gate that does not run the test suite, and its prompt is the one the repository cannot show you (by 2026-09-08)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
