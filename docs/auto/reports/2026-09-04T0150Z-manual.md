# WildfireGuardian autoloop · paper · 2026-09-04T0225Z

| | |
|---|---|
| when | 2026-09-04 01:50 UTC · 2026-09-04 10:50 KST |
| branch / head | `auto/dev` / `b621441` |
| kind | manual |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 7, done: 10, parked: 1, todo: 28 |

## What happened this lap

Paper lap 2. Scope `e558ebd..5a0466e`: 27 commits. Two code-side documents changed what
the manuscript is allowed to say; one showed the paper repeating an error the repository
had already corrected four times over; and two more arrived *during* the lap and corrected
the correction twice, so the lap rebased twice and rewrote the same paragraph three times.
Both rebases are recorded below, because the first version of that paragraph and then the
second each very nearly shipped stale.

## What changed and why

**§1 Introduction — the opening numbers were wrong, and were wrong in the way critic #4
had just caught twice in the README.** The manuscript opened by restating the WWA rapid
study's tallies as this fire's: "32 casualties, 26 of them in Uiseong-gun, more than 48,000
hectares burned and around 5,000 buildings destroyed." Three scopes were being mixed.

⚠ **This paragraph was then written twice.** The first rewrite followed `fbe71de`'s
`docs/data_sources.md`. While the lap was still running, `d2f314d` landed and moved three
of those figures again; then `98557b9` and `5a0466e` landed and moved two of *those*. The
lap rebased onto each and rewrote the paragraph against the newest document each time. What the paper now says, and what changed between the two
versions:

- **99,289 ha** is the chain's *surveyed forest damage*, with **3,819 homes**, **3,587
  people from 2,246 households** and **1.05 trillion won** (Gyeongsangbuk-do final tally
  reported 2025-05-06; the province's recovery plan of 05-07, which this lap opened and
  verified itself, carries the same area). Unchanged between versions.
- **26 deaths** are now attributed to the *provincial* disaster headquarters' count of
  **2025-03-30**, not to 중대본 as final. And Yeongdeok's toll is given as **10** (영덕군
  notice, 04-29) *beside* the provincial **9** of 03-30, both kept, neither asserted alone.
- **104,788 ha over 347 fires is not a March total.** It is the Korea Forest Service's
  **spring fire-watch season, 2025-01-24 to 05-15**. The first version of this paragraph
  called it a March figure; `d2f314d` proved otherwise from the primary release.
- **No nationwide death toll is stated.** The first version said the widely quoted 32
  "appears in no primary source"; `5a0466e` then found it in the korea.kr copy of the same
  release (사망 32명, 부상 54명) and registered it. The false sentence is gone, and the
  paper simply does not need the figure, so it does not print it.
- **No share of any nationwide total is stated, and the paper says why.** The first version
  gave the 95 %; the second gave "95 % one way, 43 % the other". `5a0466e` establishes that
  the 43 % itself divides an initial 산불영향구역 estimate by a surveyed final — the mixed
  basis the sentence was warning against — and refers the disagreement to the author as
  NH-018. The paper now prints neither ratio and says the bases and periods differ and the
  repository's own two readings disagree. That is the only claim both laps support.

Every surviving figure carries agency, basis and date, per CHARTER §3 rule 5b. The WWA
entry in `references.bib` had its `note` rewritten to what the page verbatim says.

**§1 — G1 is closed.** The age composition of the casualties had been a `[GAP]` since the
manuscript began, because the repository sourced it only to press outlets with no stable
URL. `docs/evidence/greenpeace_2026_survey.md` and its committed artifact close it with two
openable sources: Yeongdeok-gun's notice (mean age **84** among its ten dead, maximum
**101**) and the survey's own age table (**189 of 296**, 63.9 %, aged 60–79; **53**, 17.9 %,
aged 80 or over; in Yeongdeok **34.0 %** aged 80+, **36.0 %** living alone). Both caveats
travel with the numbers in the text — the survey is a non-probability sample of survivors
from which the dead are absent by construction, and its casualty figures are re-cited, not
measured. The same survey supplied the delivery-channel evidence now in §1 and §5: village
broadcast plus a neighbour **237** mentions against **112** for the emergency text message,
and **89 to 22** in Yeongdeok, where only **48.0 %** received the text at all.

**§4.7 is new — "How early could a satellite have seen these fires?"** The GK2A detection
floor (`docs/detection_floor.md`, `data/processed/detection/gk2a_detection_floor.json`, six
registry keys) bears directly on the paper's own premise. Four of the six fires are inside
the archive; three produced a detection, at **+22**, **+34** and **+64** minutes after the
recorded occurrence time — see the reviewer section below for what that clock is and is
not. The section also reports the false-alarm control (**0 of 709** steps, written as an
upper bound of roughly 0.4 % per step at 95 % confidence, not as an absence), the size
floor (**0.19 ha** at an assumed 750 K, moving more than eightfold across 600–900 K, so
read as 0.1–1 ha), and Yeongdeok as a case **set aside rather than explained** —
Uiseong-Andong burning 66 km away inside its background annulus lifted the threshold to
**21.96 K** against a best target anomaly of **11.61 K**, but a reconstructed bbox and a
~30 km ignition-point conflict could produce the same null. The withdrawn target-zone
statistics from that document's first draft are not repeated anywhere. §3.1 gains the
method, §5 what it means for the design, §6 its limitation.

**§2 Related work** gains a short Detection paragraph so the GK2A and Dozier citations are
positioned rather than dropped into Results.

## Figures

**F1 was broken and is fixed.** Looking at all seven figures with fresh eyes — which this
routine is required to do and which had not caught this — the system-overview diagram was
rendering four of its seven box labels straight through the box border and into the
neighbouring box: "time-sliced probability" ran out of Hazard field into Routing, "routes
avoid future fire" ran out of Routing into Decisions, "script · SMS draft" ran off the
canvas, and "which homes a crew can still reach" collided with the Evidence registry text.
This is precisely the font-substitution failure `paper/README.md` documents — the labels
were sized for Source Sans and the sandbox only has DejaVu, which is wider — and it had
been shipping in the `.docx` since the diagram was drawn. The fix is a `_fit_text` helper
that measures each label's rendered extent against its own box and steps the font size
down until it fits, so the diagram is correct under either family rather than under one;
the Rescue ingress box was widened from 18 to 24.5 grid units and three labels reworded to
avoid shrinking them to illegibility. Arrow endpoints and the grid are unchanged. The
redrawn figure was looked at again and is clean.

The other six re-rendered **byte-identical**, which is worth recording against the same
README warning: within this sandbox the figures are stable. F3, F4, F5, F6 and F7 were each
looked at and their plotted values agree with the prose. §4.7's result is
a four-row table rather than an eighth figure, deliberately — table and caption text is not
counted against the body-word budget, and the result is three numbers and a confound.
Three lineage notes were moved out of body prose into the captions of Fig. 4, Fig. 6 and
Fig. 7 and the caption of Table 2, where they qualify the object they belong to. Nothing
was deleted in the move. Figure appearance numbering is unchanged (F1→1, F2→2, F4→3,
F5→4, F3→5, F6→6, F7→7) and every `Fig. N` in the text still matches.

## Gaps

- **G1 closed** (above); its row is gone from `GAPS.md` and the closure is recorded there.
- **G5 opened** — the detection reference clock, above; not after-sprint, one lookup. It
  is **NH-019** in `NEEDS_HUMAN.md`, renumbered from 018 when an upstream lap took that
  number mid-flight.
- G2, G3, G4 unchanged, all still after-sprint and all still needing the author's laptop or
  the author's consent handling. G4 now also records the 2,290-vs-2,219 manifest conflict,
  which has to be settled before the hindsight arm is run at all.
- **New note in `GAPS.md`: length pressure.** The body is **7,479 words** against a 7,500
  hard fail and a 7,000 aim — twenty-one words of headroom, which is not much of a margin. This lap added
  roughly 1,250 words of new sourced content plus another 450 of reviewer corrections, and
  paid for it by compressing existing prose and moving four lineage notes into figure and
  table captions, which `build_docx.py` does not count. The next lap must take words out
  before it adds any; the condensation candidates are named in `GAPS.md`, in order, and no
  number or caveat may be dropped to make room.

## Gates

- `paper/check_paper.py` **exit 0** — 7,479 body words, 7 figures, 3 tables, 25 references,
  4 gap markers matching 4 rows.
- `scripts/auto/gates.py --mode quick` **exit 0** — verify PASS (14.2 s), pytest-quick PASS
  (40 passed). Re-run after the reviewer's fixes, after the F1 redraw, and again after
  rebasing onto `d2f314d`, whose new `tests/test_motivating_event_figures.py` the lap also
  passes. One line-packing false collision was caught and re-wrapped on the way (the
  string "Section 4.7" landing beside the anomaly-minutes anchors), the fourth this lap.
- The collision gate caught three false collisions introduced by re-wrapping, exactly the
  failure mode `paper/README.md` documents: a Table 2 caption line that packed "896.5 km²"
  next to the depot-density anchors, a re-wrapped line that put 37.8 % next to the
  window-threshold anchors, and one that put the 66.7th percentile on a line carrying all
  three anchors of a *different* registered quantity (`redundancy_e17_pooled_percentile_of_null`
  = 56.7, the 17-column replication, against the manuscript's correct
  `arme_pooled_percentile_of_null` = 66.7). All three were re-wrapped; no value changed.

## The reviewer blocked, and it was right about the biggest thing in the lap

The independent reviewer returned **block**, and its root objection was aimed at the
section this lap was proudest of. §4.7's headline — restated in the abstract, §1, §5 and
§7 — said a satellite trigger would have fired 22 to 64 minutes *after a person had already
reported the fire*. That word "reported" was doing all the work, and nothing in the
repository supports it. The delays are measured from `fire_manifest.json`'s `start` field.
`docs/detection_floor.md` §1 asserts that field is a 신고 time; the manifest itself says
"start/end/reported_ha are provenance only", gives no source for the minute, and elsewhere
calls the same field the ignition. I checked the manifest directly rather than take the
reviewer's word: it is exactly as described. Under the ignition reading the human report
time is unmeasured and could fall *after* the satellite — and §6's safeguard, that the
bias "flatters the satellite", is true only under the reading the claim needed to prove.

So the claim was reverted rather than defended. The manuscript now measures against "the
recorded occurrence time", states in Table 3's caption and in §6 exactly why that clock is
unsourced, and makes no claim about whether the satellite beat the telephone. What it
keeps is what stands on its own: the size floor of 0.1 to 1 hectare, which means a 2 km
geostationary pixel cannot be an ignition-scale alarm whatever its cadence. §5's design
paragraph was rewritten to say the satellite is corroboration *because of the floor*, and
to say plainly that the ordering the design notes had drawn from this is an open question.
New gap **G5** and **NEEDS_HUMAN NH-018** carry what would close it: one 신고접수시각, for
one fire, from a KFS/119/중대본 record. One lookup, no rerun.

Seven further findings, all verified against the artifacts before acting, all fixed:

- **8.6 was not a real number.** It is the quotient of two *rounded* display values in
  `docs/detection_floor.md` §6; the artifact gives 0.6892 and 0.0828 ha, and the registry
  caveat says only "more than 8x". Now written "more than eightfold". Exactly the failure
  class the paper claims its registry prevents, sitting inside its sharpest section.
- **§1's chain death toll was hung on a citation that does not carry it.** p. 9 of the
  Greenpeace report carries the Yeongdeok figures only; the 26 belongs to 중대본 and the 32
  to 산림청 as of 2025-05-15. Both agencies and both dates are now in the text.
- **The @wwa2025korea note misquoted its own source.** It rendered "Uiseong was hardest
  hit, with 26 deaths, while four occurred in Sancheong" as "26 of them in Uiseong-gun",
  dropping the four Sancheong deaths. I re-fetched the page and rewrote the note verbatim.
  The manuscript now gives WWA's reading *and* the provincial one with their scopes.
- **"347 fires" was unverifiable at the time** — one appearance, in a heading, no source.
  `d2f314d` then sourced it to the KFS release and established it is a spring-season
  count, which is what the paper now says.
- **Table 3's caption claimed identical reference times** for GK2A and FIRMS; they differ
  by one minute at two fires. Now stated.
- **§4.7 had dropped a registered caveat.** It blamed Yeongdeok's non-detection on
  background contamination alone; the manifest flags that fire's bbox as *reconstructed*
  and the two committed manifests put its ignition ~30 km apart against a 15 km target
  radius, so a coordinate error alone could produce the same null. Yeongdeok is now "set
  aside rather than explained", and the 2,290-vs-2,219 divergence is written into G4.
- **§3.1 described a two-condition detector.** `contextual_flag` is a three-condition
  conjunction and the omitted term — 3.8 µm brightness temperature above its own
  background median + 4σ — is the one doing most of the selectivity. Now complete.

The reviewer's own audit is worth recording as the other half: it matched every numeric
token against the 296 registry entries, chased every survivor into the artifacts by hand,
re-derived the internal arithmetic, and found the rest clean — every Table 3 cell, every
Greenpeace figure, the provincial figures against external sources, and all 25 citations.

⚠ **The 20-page claim still rests on the word budget, not on a measured page count.**
LibreOffice is installed in the sandbox but refuses to load the built `.docx`
("source file could not be loaded"), so no PDF and no real page count could be produced
this lap. `check_paper.py` enforces words, and `paper/README.md` calibrates 7,000 words at
roughly 16 pages; a crude recount over the built document (8,909 words including captions,
tables and 25 references, plus seven full-width figures) lands nearer 21 pages, which does
not agree with that calibration. One of the two is wrong and neither was measured. Filed
here rather than smoothed over; the next lap should either get a page count out of the
sandbox or re-derive the words-per-page constant from the built document.

Reviewed by: subagent (block): §4.7's headline called the manifest's `start` field a
report time on the strength of one unsourced sentence in a design document, making
"the satellite rang after the telephone" — restated in the abstract, §1, §5 and §7 — an
unregistered re-interpretation rather than a measurement. Fixed by reverting the claim,
not by defending it; all seven secondary findings fixed as well.

## In plain terms

- The paper now opens with the right numbers for the right fire, each with an agency and a
  date you can check, and it finally says how old the people who died were.
- The reviewer caught a real mistake and I have taken it out: I had the paper saying the
  satellite would have rung *after* somebody phoned the fire in. We cannot actually show
  that — the clock we measured from is a field nobody sourced. The paper now says only what
  holds: the satellite cannot see a fire smaller than about a hectare, so it cannot be the
  first alarm whatever else is true.
- **One thing would fix it, and only you can get it.** For any one of 의성·안동, 강릉 or
  홍성, the 신고접수시각 — the minute the call came in — from a 산림청/119/중대본 record.
  One lookup. That is NH-019. Everything else still missing needs your laptop and is not
  urgent this week.



## Gates

**ALL GREEN** · mode `quick` · head `5a0466e` · 2026-09-04T01:49:01Z · ⚠ **stale: the gates read `5a0466e`, HEAD is `b621441`** — this table does not certify the pushed tree; re-run `gates.py --mode quick` and `gates.py --assert-head` before pushing

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 14.3 s | === make verify: PASSED === |
| pytest-quick | PASS | 8.3 s | 40 passed in 7.59s |

## Commits since the previous report (5a0466e65afca743c76f394ce673f2c183e9491e..HEAD)

- b621441 the reviewer blocked on the word "reported", and it was the load-bearing word

## Needs a human (14 open)

- NH-001 [DECISION] Email delivery needs three repository secrets
- NH-002 [DECISION] Optional: `@claude` on GitHub issues and PRs
- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-010 [DECISION] Expert consultations and the firefighter record (by 2026-09-20)
- NH-011 [DECISION] One real, recorded email send from a network that works in Shanghai (by 2026-09-20)
- NH-012 [DECISION] Portal downloads the loop cannot do (by 2026-10-01)
- NH-013 [FYI] Optional: a stable web address for the visual board
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-016 [DECISION] The critic routine has 30 minutes to land its findings, and needs about 40 (by 2026-09-05)
- NH-017 [DECISION] Three entries were closed on replies the repository cannot see, and one of them was wrong (by 2026-09-06)
- NH-018 [DECISION] Two laps read the same primary sources and disagree on two sentences (by 2026-09-08)
- NH-019 [DECISION] One report time, for one fire, decides what the detection result is allowed to say (by 2026-09-08)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
