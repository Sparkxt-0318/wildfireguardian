# WildfireGuardian autoloop · dev · 2026-09-03T0653Z

| | |
|---|---|
| when | 2026-09-03 06:53 UTC · 2026-09-03 15:53 KST |
| branch / head | `auto/dev` / `3156459` |
| kind | dev |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 7, in-progress: 2, parked: 1, todo: 24 |

## What happened this lap

**Row: WFG-018 — 제출본 대비 정본 reconciliation sheet. Done.**

Claimed at `3156459` before building (the marker reached `origin` first, per NH-007).

## What I built

**[`docs/submission_reconciliation.md`](../docs/submission_reconciliation.md)** — Korean,
printable as one double-sided sheet: an eleven-row table (서식 위치 · 제출 시점 값 ·
현재 정본 값 · 왜 움직였나 · 움직이지 않은 것), a "헷갈리기 쉬운 세 지점" section, a
30-second spoken line per row for the booth, and a "이 표가 보여주지 않는 것" section.

The rows are the ones a judge can trip over: the 459-series split, the quasi-static-core
reading, DEM slope, the ML baseline table, far-band AUC, PR-AUC/Brier, the delay row, the
headline AUC's DEM lineage, contribution ②, and the withdrawn 44× ratio (with the
two-sentence reason: a six-feature sum against a single variable, on a 0.25° grid that
cannot resolve the wind it claims to rank).

Three things the sheet is careful about, because the constraints demanded it: no
"N origins were reclassified" for the reverted-vs-canonical pair (three axes moved at
once, so no per-origin ledger exists); 440/17/3 kept in its own row and never summed with
438/18/3; 18/459 stated as **3.92 %**, with 3.70 % named as a different retired run's
17/460. Every 영덕 absolute rate carries the 32.6 % coverage caveat.

**18 new registry entries** so the sheet's done-when holds — "every current value greps to
a `NUMBERS.json` key". Registry 260 → 278, `make verify` re-derives all 278 from
git-tracked artifacts. Retired values are **not** registered; they live only as
NEAR-labelled prose, as the row required.

**A new arm, `A_reconciliation`**, declared in `docs/arm_protocol.json` — see below.

**`tests/test_submission_reconciliation.py`** (4 tests): every registry key the sheet names
resolves; the sheet keeps both halves of the 459-series row (deleting the retired value
would destroy the point of the sheet); JUDGE_QA §0 links rather than copies.

**`docs/auto/JUDGE_QA.md` §0** now points at the sheet instead of carrying its own copy of
the table, and Q10's dangling "§1.4 above" points there too.

## The root objection I recorded before building (`hate`)

*"Registering keys to satisfy a done-when clause is the tail wagging the dog. Every new key
arms `check_number_collisions.py` against the whole document tree, so a one-page sheet can
turn fifty documents red — and the sheet's real job needs no registry entry at all, because
the current values already sit in named committed artifacts a judge can open."*

Cheapest test: add the keys, run the collision gate, count new hits. **The objection was
half right and the test was worth running.** Three hits appeared, all from one key —
`farband_pooled_auc_committed`, whose anchor words "committed / pooled / auc" matched three
lines discussing an entirely different pooled AUC. The fix was not to annotate three
documents (two of them research sweeps) but to rename the key to
`farband_pooled_auc_precorrection`: named for what distinguishes it, with no generic word in
it. Zero documents edited, gate green. The other seventeen keys produced no hits, so the
"fifty documents red" fear did not survive contact.

## What did not work

- **`check_arm_controls` and `check_arm_isolation` both caught me**, in that order. I
  invented two arm names that were not declared, then "fixed" it by relabelling them `A` —
  which is the frozen baseline arm, and the isolation gate exists precisely to stop a new
  key claiming it. Correct fix: declare `A_reconciliation` in `docs/arm_protocol.json` with
  its `reason_no_control` (nothing is estimated; each check re-reads a number the artifact
  already carried) and put all 18 entries on it. Two red gate runs to get there.
- **I piped a gate and read `tail`'s exit code** on the first collision run — the exact
  MEMO 2026-09-03 lesson, made by the tooling this repository built to stop it. It reported
  `EXIT=0` while the gate was returning 1. Caught within one command because the printed
  output contradicted the code. Every gate after that was captured to a file with `$?` read
  directly.
- **The core-cell counts 249 → 1,036 cannot be registered at all.** Their JSON key is
  `cells_ge_0.5_per_slice`, and both `build_numbers.dig` and `verify_numbers.dig` split a
  `json_path` on `.`, so the resolver cannot address a key containing one. The growth
  percentage beside them registers fine. The sheet says this in its 출처 section rather
  than quietly citing the artifact and hoping nobody checks.
- **`baseline-verify` stays a WARN**, as expected off-laptop: it needs the git-ignored
  acquisition manifests. Not a regression.

## Two findings filed rather than fixed here

- **`scripts/build_numbers.py` would destroy the registry it claims to build.** It defines
  65 entries and ends in an unconditional overwrite of a file that now holds 278; every
  entry since ~Session 12 was added to the JSON directly and gated by `verify_numbers.py`.
  CHARTER §3 names that script as *the* registration path, so the charter currently points
  at a landmine. Filed as **WFG-036** (P1 infra) with three options and a recommendation
  (make it refuse rather than merge). I did not run it.
- **`docs/HANDOFF_ROUND3.md` §1.3 still says 의성·안동's time-aware-only share is "nearly
  seven times Yeongdeok's".** That ratio is 24.73/3.70 = 6.7 against the **retired** share;
  against the canonical 9.17 % it is 2.7×. The same phrasing is in JUDGE_QA Q7, which is
  already on WFG-002's purge list — HANDOFF was on nobody's. Added to WFG-004's row detail.
  WFG-004's other open question, why the two HGB means differ, is answered on the way:
  `ml_baselines.json`'s `hist_gbm` row IS the corrected-DEM lineage (it equals that file's
  own `dem_corrected_reference.mean_of_folds`), so they are two lineages, not two readings.

## Evidence

- `docs/submission_reconciliation.md`, `tests/test_submission_reconciliation.py`
- `docs/NUMBERS.json` (278 entries, all re-derived), `docs/arm_protocol.json`
- `make verify` PASSED; `scripts/auto/gates.py --mode full` **exit 0**, ALL GREEN
- pytest **1067 passed / 54 skipped**, against the standing clean-clone baseline of
  1063 passed / 54 skipped in `docs/clean_clone_gates.md` — exactly +4, the new test file,
  and **no test moved from pass to skip**.

⚠ One number-hygiene note on the baseline itself: the routine prompt states the clean-Linux
baseline as 1065 passed / 51 skipped, and `docs/clean_clone_gates.md` — the standing
artifact for that row — records 1063 / 54. The artifact is the one that reconciles against
this run. The prompt's figure appears to be a transcription of an intermediate lap.

## Next row

**WFG-019** — the operating-point evidence package (per-fire recall/FNR at 0.3, the PR
curve, the nested LOFO threshold calibration as a negative result, a MODEL_CARD appendix).
It is the next `todo` in table order, agent-doable, unblocked, and it needs only committed
inputs (`spread_v2_lofo_oof_cells.csv.gz`, `oof_classification_metrics.json`). It also now
has somewhere to put its numbers: `A_reconciliation` is the wrong arm for it, but the arm
declaration it will need is the same shape.

## Gates

**ALL GREEN** · mode `full` · head `3156459` · 2026-09-03T06:52:18Z

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 17.7 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.2 s | make: *** [Makefile:140: baseline-verify] Error 1 |
| snapshot-verify | PASS | 0.5 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 196.3 s | 1067 passed, 54 skipped, 52 warnings in 194.42s (0:03:14) |

## Commits since the previous report (-15)

- 3156459 claim WFG-018: the reconciliation sheet is this lap's row
- d88c85b research brief: eight verified sweeps, one decision brief, and a re-keyed backlog
- 4b5d1f2 clean-clone gates get a standing artifact, and the anti-hard-coding guard stops skipping
- 873bbec memo: the kickoff session piped a gate to tail and pushed on red, so the lesson is written down
- e75f3a2 WFG-001 is done: the first green auto-gates run is run 8 on c42287e
- 953eb6c judge Q&A: mark the 의성·안동 time-aware-only share so the collision gate reads it as its own quantity
- 14f6870 needs-human: the finals date has two sources, and only the author can settle it
- ec6e04a charter: say what was observed about session wake-ups, not more
- dbc9caa judge Q&A bank: the hardest post-Round-3 questions, each tied to a file or an admitted gap
- 30ed00a AI ledger: the loop's work is disclosed the way ISEF and KCF require
- c42287e tests: an absent git-ignored input skips with its reason, it does not fail
- 017c9ec charter: a routine session ends with its turn, so gates run in the foreground
- e1588b4 gates: baseline-verify is hard only where the laptop-only manifests exist
- 80cd611 auto-gates: run once per push, not once per push plus once per PR
- 1e60c1b report-email: a push with no report file is not a failure

## Needs a human (11 open)

- NH-001 [DECISION] Email delivery needs three repository secrets
- NH-002 [DECISION] Optional: `@claude` on GitHub issues and PRs
- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-006 [DECISION] Confirm the finals date: 10.18 (your notice) vs 10.24 (June schedule post)
- NH-008 [DECISION] Five questions for the KCF 운영사무국 (by 2026-09-07)
- NH-009 [DECISION] Repository decisions only the author can take (this week)
- NH-010 [DECISION] Expert consultations and the firefighter record (by 2026-09-20)
- NH-011 [DECISION] One real, recorded email send from a network that works in Shanghai (by 2026-09-20)
- NH-012 [DECISION] Portal downloads the loop cannot do (by 2026-10-01)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
