# WildfireGuardian autoloop · manual · 2026-09-04T1731Z

| | |
|---|---|
| when | 2026-09-04 17:31 UTC · 2026-09-05 02:31 KST |
| branch / head | `HEAD` / `21403e5` |
| kind | manual |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · macOS-26.4.1-arm64-arm-64bit |
| backlog | blocked: 7, done: 13, parked: 1, todo: 47 |

## What happened this lap

# A red GitHub run is now caught within the hour, fixed by a routine, and named in the email

The author received "Run failed: auto-gates" emails and asked that such cases be fixed and caught immediately. Facts: GitHub's clean-clone gate has been red on `auto/dev` since 201c554 (16:03 UTC) with one failing test out of about 1,400 while every lap reported green; the run's log names no test because `gates.py` writes step logs under `.auto/`, which `upload-artifact` skips as hidden files, so the artifact was empty. This laptop could not reproduce it tonight: the machine is running other sessions' jobs at a load average above 30 and the suite crawls.

What changed (CHARTER §4b):
- `.github/workflows/auto-gates.yml`: on failure the job prints the failing steps and the `FAILED` / error lines from their logs, and the artifact upload includes hidden files, so the email and the artifact carry the test name.
- New routine `wfg-autoloop-ci-red` (hourly at :05, cloud sandbox): polls the Actions API, exits if green, otherwise reproduces, fixes test-only or environmental causes within §3, pushes with a `ci-red · <sha>` report, and emails the author the cause. It runs first at 18:05 UTC tonight against the current red.
- Dev prompt step 2 checks the same API and takes a red run as its first job; critic step 2 treats a red run behind a green report as finding #1 and the one permitted fix-before-next-row item; §4 step 4 forbids tests that depend on the clock, timezone, network or files outside the repository.
- A GitHub webhook trigger was attempted through the routines API and refused for schema reasons; the hourly poll is the fallback and is recorded here so a later session can retry the webhook.

## In plain terms

Those "Run failed" emails were real: GitHub's own check has been failing since the afternoon on one test that passes everywhere else, and the email did not even say which test. From now on the email names the test, a new hourly routine fixes such failures on its own and emails you the cause, and every lap checks GitHub before it starts new work. The first automatic fix attempt runs tonight at 18:05 UTC.



## Gates

**ALL GREEN** · mode `full` · head `c65dc56` · 2026-09-04T16:51:21Z · ⚠ **stale: the gates read `c65dc56`, HEAD is `21403e5`** — this table does not certify the pushed tree; re-run `gates.py --mode full` and `gates.py --assert-head` before pushing

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 6.5 s | === make verify: PASSED === |
| baseline-verify | PASS | 0.1 s | baseline intact — 127 artifacts, config 36f42903a65f…, LOFO (151,904, 2,989) |
| snapshot-verify | PASS | 0.2 s | VERIFY: all present snapshots intact (2 DIGEST-ONLY ORIGIN DRIFTED (external data changed under us — see docs/DATA_LOSS_2026-07-24.md)) |
| env-check | PASS | 0.0 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 335.1 s | 1425 passed, 12 skipped, 1 xpassed, 114 warnings in 334.62s (0:05:34) |

## Commits since the previous report (-15)

- 21403e5 product first: DIRECTION.md names the screen, the bundle and the booth next; CHARTER §14b caps critic interruptions to judge-facing surfaces
- 3d77e01 critic #12: the county is right, and 29 letters to strangers rode in on the commit that fixed it
- c65dc56 NH-022: the 영덕 subset re-cut with 47770 (the first cut was 봉화군), verified by address and by the canonical box; WFG-073/074 unblocked
- 89730db correction report: assert-reported refused the fix, and it was right to
- 6f6a4a9 the report about a screen naming a dead commit named a dead commit
- 201c554 dashboard and latest images rebuilt on the rebased tree, so both reports appear
- 9011c56 report: the lap the reviewer stopped, and the number it had copied instead of measured
- 4745920 the reviewer blocked on a number this lap had copied instead of measured
- deeb147 the artifact labelled 영덕 is annotated as wrong where it lives, and the screen's commit id is now one a judge can resolve
- fdab7bc report: the lap ended green, and the first report says otherwise
- 72cf230 paper: the reviewer blocked this lap's own new paragraph, and three numbers got their caveats back
- d5e2562 claim WFG-075 and WFG-067: the critic's two fix-before-next-row items
- 7988769 STATE.json restored after a rebase committed conflict markers; a test now refuses unparsable state files
- ad00199 direction: one page the builder reads first, a five-minute critic check, research every second day (CHARTER §14)
- bcba946 critic #11: the suite was green, and one of its passing tests asserted the wrong county

## Needs a human (5 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-023 [DECISION] Twenty-nine outreach drafts to named strangers, and a contact list published in a public repository, with no record anywhere in the loop (by 2026-09-06)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
