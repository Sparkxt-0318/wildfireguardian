# WildfireGuardian autoloop · dev · 2026-09-06T1313Z

| | |
|---|---|
| when | 2026-09-06 13:13 UTC · 2026-09-06 22:13 KST |
| branch / head | `auto/dev` / `a866b25` (pushed; `report.py` stamped `fc05320`, the head it was generated at, and the report ships one commit later — the review-fix commit. Both resolve.) |
| kind | dev |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 7, done: 16, parked: 1, todo: 72 |

## What happened this lap

# WFG-117 — the Q&A card stops holding a number that three laps had corrected

**Row:** WFG-117 (P0, KCF), named position 1 by `docs/auto/DIRECTION.md` and critic #26's
one `fix-before-next-row` item. Claim commit `2c8ecbd`. **The row ends this lap back at `todo`
with a residue note, not `done` — my own independent reviewer returned `block`, and it was
right about something I had shipped.**

## Your decisions, applied

Nothing new. The Gmail search `from:siyeong0318@gmail.com subject:"WildfireGuardian autoloop"
newer_than:14d` returned its first page of 25 threads, newest first — every one sent by this loop
and holding exactly one message, so no thread has been replied to. Threads sort by their most
recent message, so a reply would have surfaced at the top; that page reaches back to
2026-09-05T05:35Z, and critic #26 ran the same search at 1100Z and also found none, so the two
together cover the window. `docs/auto/decisions_seen.json` is unchanged and PR #31 has no comments
at all. No `NH-###:` line reached the loop through either channel this lap, so no entry was closed
and no plan changed on your account.

## What I did, and why

Q30 of `docs/auto/JUDGE_QA.md` is **T0** — you recite it from memory, with no paper — and it is
the question a judge asks about why today's numbers should be believed. Its draft answer quoted
the size of the number registry. Three consecutive critic laps found that quote stale and each
replaced it with the then-correct pair: #21 (2026-09-05), #22 (the same evening), #26
(2026-09-06). All three went stale inside a lap. By the time #26 ran, the *warning* had outlived
the defect: WFG-113 rebuilt the screen at `1ec1d06`, so the card was drilling you to distrust the
one surface that had just become correct.

**The third correction is the finding.** A number corrected three times in two days is not a
number with a typo in it. So the recited draft now holds no count at all: it says 「대부분」 of
registered values re-derive under `make verify`, that the rest carry a 「확인됨, 재현 불가」 label
with its reason, and it names the two places to count *with* the judge — `docs/NUMBERS.json` and
the screen's 검증 레지스트리 카드. The four superseded blocks are kept as dated records
(CHARTER §3.7) behind a greppable marker `[기록 · YYYY-MM-DD · 오늘의 값이 아닙니다]`, and one
note addressed to laps rather than to you says, once, that a literal typed here is a defect with
a shorter fuse.

**The design was chosen on a measurement, not on taste** — this is the root objection I recorded
against my own first plan, below. **Four** gates in `tests/test_judge_qa_bank.py` — three by
design, the fourth added after the independent review blocked this lap:

| test | asserts | graded red by |
|---|---|---|
| `test_the_recited_registry_answer_quotes_no_count` | the draft holds no multi-digit number and still names both places to read one | typing today's count into the draft |
| `test_every_registry_count_in_the_bank_sits_in_a_dated_record` | every 「등록 N」/「재현 가능 N」/「재현 불가 N」 in the bank opens a dated record block | removing one record marker |
| `test_the_banks_qualitative_registry_claim_is_true_of_the_registry` | the word 「대부분」 you say aloud is true of `docs/NUMBERS.json`, re-derived in-process | flipping most registry entries to non-reproducible |
| `test_the_cards_account_of_the_irreproducible_covers_every_bucket` | every reason the registry gives for not re-deriving is a kind the card names | a new `reproducibility.status` in the registry; a bucket phrase deleted from the card |

Method, caveats and what it does not show: **`docs/judge_qa_gates.md`** (new).

## The root objection I recorded against my own plan (`hate`)

*If the recited answer quotes no literal, the gate has nothing live to compare against, so the
done-when's 「a mutation that moves the registry」 cannot turn it red — the gate is decoration.
But if I keep one live literal, I re-plant the very defect three critic laps have now filed, on a
shorter fuse.*

The cheapest test of it was a measurement, and I took it before designing anything. On an
**unshallowed** clone — `git rev-parse --is-shallow-repository` → `false`, 485 commits — walking
every revision of `docs/NUMBERS.json` (57 of the 485 commits touch it): the entry count **changed
44 times across 45 distinct values** between 2026-08-01 and 2026-09-05, ten of those on the four
sprint days (09-02: 1, 09-03: 3, 09-04: 2, 09-05: 4). A gated literal would go red two to four
times a day and oblige each of those laps to edit a rehearsal document for no booth benefit.

So the objection resolves against the literal, and the registry-side mutation is graded by the
**neighbouring** gate rather than by mine — verified by running it, not by reasoning about it:
adding one key to `docs/NUMBERS.json` without rebuilding the screen turns
`tests/test_finals_payload_rederives.py::test_the_registry_card_counts_the_registry_it_ships_beside`
red and leaves all three of mine green. That is stated on the row, in the doc and in the test's
own docstring, because it decides what a red one *means*.

## What did not work, and what it turned up

**The first record marker turned the full gates RED.** I chose `【기록 …】`, and
`docs/auto/JUDGE_QA.md` is one of four **printables sources**: the CJK brackets are not in the
committed IBM Plex Sans KR subset, so `test_every_source_character_can_be_drawn` failed and took
the printables build with it (3 failed, 5 errors). The edit was 「prose only, no run」 and it was
a code change. ASCII brackets, and the marker reads the same.

**Fixing that exposed a live defect nothing catches.** The committed booth PDF's manifest,
`manifest_20260906T0620Z.json`, records `docs/auto/JUDGE_QA.md` at sha256 `2c845121…`; after this
lap the file hashes `6c09095d…`. **Every printables test stays green**, because the manifest is
checked for internal consistency and never against the tree it describes. So the printed kit is
now older than the card it was built from and nothing says so — and `docs/printables.md` already
claimed that staleness 「can be checked mechanically」. 「Can be checked」 is not 「is checked」,
which is the same sentence-shape as the card this row just fixed. Annotated in place; the
new-stamp rebuild and the gate (re-hash every `SOURCES` path, fail when the newest manifest
disagrees, graded by touching one source) are filed on **WFG-130**, which is the next row and
already rebuilds at a new stamp under CHARTER §3.2. I did not rebuild here: that would have put a
third PDF beside the two WFG-130 must reconcile.

**A correction to critic #26, recorded on WFG-119.** Its withdrawal of WFG-115's premise holds —
`git merge-base --is-ancestor 41498ef 2c8ecbd` exits 0 here. Its distance does not:
`git rev-list --count 41498ef..2c8ecbd` answers **280**, not the **277** published in
`DIRECTION.md`, `CRITIC_LATEST.md` and the 1112Z report. Critic #26 deepened to 300 and 300 is
still not this graph; the rule it wrote in the same breath — no ancestry claim until
`is-shallow-repository` answers `false`, with the depth beside it — is the rule its own headline
number does not satisfy. ⚠ And the reviewer caught me making that same error one turn further in:
my first draft of this wrote `HEAD`, and `HEAD` moved during the lap — at `fc05320` the same
command answers **281** on a **486**-commit clone. A distance is a function of the head it is
measured at, so the head is now pinned everywhere the number appears: at `2c8ecbd`, distance
**280**, clone **485**, `is-shallow-repository` `false`. A dev lap does not write `DIRECTION.md`
(CHARTER §14), so the correction sits on WFG-119 for critic #27.

## Gates

`gates.py --mode full` **ALL GREEN**, read from its exit code, at the tree of `fc05320`:
`1574 passed, 56 skipped` in 341.9 s (`baseline-verify` WARN is NH-029's expected
new-artifact-is-information case). ⚠ **Not comparable to critic #26's `1565 passed, 62 skipped`
without a caveat:** +3 of the +9 are this lap's new tests; the remaining +6, matched by −6 skips,
I did **not** attribute, and the likeliest cause is that I unshallowed this clone mid-lap. I am
flagging it rather than explaining it, because a plausible attribution I have not run is exactly
what this lap is about. Critic #27 can settle it by comparing skip reasons on a depth-50 clone.

## Independent review

**Reviewed by: subagent (block): the lap applied its own thesis to one clause of a sentence and
not to the other clause of the same sentence.**

I removed the registry *counts* from Q30 and left standing, ungated, a **two-bucket** account of
*why* the rest do not re-derive — the overwritten OSM graph, and past runs kept for the
reconciliation sheet. The reviewer counted the registry in one command and found **three**
buckets. Counted again here, independently: of the 58 irreproducible entries, **16** carry the
overwritten-OSM reason, **18** are past runs not re-executed in this environment, and **24** —
the largest group — are `reproducibility.status == "external"`, agency-published figures whose
re-verification means opening the source again rather than re-running a pipeline. So the recited
T0 answer named two of three kinds and omitted the biggest, on the question about honesty, while
inviting the judge to open `docs/NUMBERS.json` alongside them. And four paragraphs below, the
same card filed the older split as *never verified* — the file marked a categorisation unverified
and recited it as fact at once.

**Taking the numbers out had removed the only handle a gate had on the claim.** That is this
row's own defect, reproduced one clause later in the same sentence.

**What I changed in response, rather than arguing:**

1. Q30's draft now names **three** kinds and no proportion, and points at the
   `reproducibility.status` field so a judge can bucket them live. The date in it went too — it
   tripped this lap's own no-numerals rule, which is the rule working.
2. A **fourth** gate, `test_the_cards_account_of_the_irreproducible_covers_every_bucket`, takes
   the bucket set **from the registry** and fails when it grows one the card does not describe.
   Graded red both ways: a new `status` value in the registry, and a bucket phrase deleted from
   the card.
3. The 없는 것 line no longer says 「no lap has ever counted this」 about a count that takes one
   command. It records what the count was and that the proportions still do not go in the card.
4. **The reviewer's secondary finding, which I had disclosed but under a `done` status:** the
   done-when asks *this row's* gate to be graded against a registry-moving mutation, and it is
   not — that mutation is caught by `test_the_registry_card_counts_the_registry_it_ships_beside`,
   which shipped with WFG-113 and is unchanged in my diff. Honest prose over a status column that
   was not. The row is back at `todo` with `(a) done / (b) outstanding`.
5. **And it caught me doing the thing I had just faulted critic #26 for.** My correction wrote
   `git rev-list --count 41498ef..HEAD` = **280**; at the head it would have shipped in the same
   command answers **281**, because `HEAD` moved during the lap. An ancestry distance is a
   function of the head it is measured at. Now pinned: at `2c8ecbd`, distance **280**, clone
   **485** commits, `is-shallow-repository` `false`.

Everything else the reviewer re-ran held: all three original mutations, the churn measurement,
the printables staleness (both halves), the ancestry withdrawal, and `gates.py --mode full` ALL
GREEN at `fc05320` (`1574 passed, 56 skipped`) — its numbers match mine exactly. It found one
escape from the gates and `docs/judge_qa_gates.md` already discloses it: a *novel* phrasing of
the count outside the draft paragraph passes. No CHARTER §3 violation, no unregistered number in
judge-facing prose, the frame untouched.

## Next row

**WFG-117 (b)** is the small outstanding half above, and a next lap can take it or amend the
done-when in writing. It does not block **WFG-130** (P0, minutes) — DIRECTION's position 2, which
this lap handed a live instance and a gate design. Then WFG-128.

## In plain terms

- **What changed for the project.** The rehearsal card for the question 「why should we believe
  today's numbers?」 no longer has any numbers in it. It tells you to open the file and count with
  the judge, which is the one answer that cannot go stale. Three earlier reviews had tried to fix
  it by writing the right number in, and all three were wrong again within hours. The checker
  that came after me then caught a second thing: my rewrite still explained the leftover values
  in two groups when there are three, and the group it left out was the biggest one. That is
  fixed, and a check now watches it.
- **Why it matters to the judges or the science.** That question is about honesty, so being
  caught with a stale figure in the answer to it is worse than in any other answer. And a check
  now runs on every lap that stops anyone — me included — from quietly typing a number back in.
- **What you should do.** Nothing, and there is nothing waiting on you. One warning if you are
  printing this week: **the booth PDF is now older than the Q&A card**, so do not print it until
  the next lap rebuilds it (that is the very next row). Nothing in it is wrong; it is just behind.
  And the row stays open by one small clause, on purpose — the card itself is fixed and shipped.



## Gates

**ALL GREEN** · mode `full` · head `fc05320` · 2026-09-06T13:07:39Z · current at `fc05320`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 21.7 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.2 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 0.5 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 561.2 s | 1574 passed, 56 skipped, 52 warnings in 559.23s (0:09:19) |

## Commits since the previous report (b2bdaf0aed5507d687420bc39281eae608988b19..HEAD)

- fc05320 WFG-117: the card stops holding a number that three laps had corrected
- 2c8ecbd claim WFG-117 (20260906T1230Z)
- fb6a0c4 the critic report names the head it ships in as well as the head it reviewed
- 828bbae critic #26: the commit five laps called unreachable is 277 back, and every lap measured inside a shorter graph

## Needs a human (8 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
