# WildfireGuardian autoloop · dev · 2026-09-03T1724Z

| | |
|---|---|
| when | 2026-09-03 17:24 UTC · 2026-09-04 02:24 KST |
| branch / head | `auto/dev` / `5290ac5` |
| kind | dev |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 7, done: 4, in-progress: 1, parked: 1, todo: 25 |

## What happened this lap

**Row: WFG-004 — SSOT sweep. Done.**

Claimed at `392fe43` and pushed to `origin/auto/dev` before any build work
(NH-007 rule). Baseline `gates.py --mode quick` was green at `42818ec` first.
WFG-002 was skipped deliberately: it was `in-progress(20260903T1536Z)`, claimed
41 minutes before this lap's baseline, so a live lap holds it.

## The root objection I recorded before building

The vendored `hate` skill is marked `disable-model-invocation: true` and cannot be
fired by an agent (see "What did not work"), so the objection is recorded here as
the charter asks, in my own words:

*"The row's deliverable is an audit **report**, which is the most self-confirming
artifact possible: I choose what counts as a 'quantity', so I can write 'every
quantity has one home' while the failure that actually bites — a number correct in
lineage A, quoted in a paragraph about lineage B — stays invisible to both the
collision gate and to my own audit. README:731 is the proof: it survived every
green gate to date."*

**Cheapest test:** run `check_number_collisions.py --report` on the untouched tree
and see whether it flags README:731. **It did not** — `UNMARKED collisions: 0`,
exit 0, while the line was wrong. The gate provably cannot see this failure class,
so the lap's real deliverable had to be a *test*, not a prose audit.

The objection was right, and it changed what this lap built.

## What I found, and how it differs from the row

**The row's two premises were both wrong, and inherited from a bad diagnosis.**

**1. `README.md:731`'s "6 → 34" is not a typo.** It is the real bracket of the
superseded pre-flip **452-series** baseline —
`data/processed/rescue_baseline_synthetic/rescue_verify.json` →
`[6, 15, 20, 25, 34]`, superseded N = 452, a git-tracked artifact. The canonical 439-series
real-OSM run is `[6, 11, 24, 51, 66]`. The defect was a **lineage mix**: the superseded 452
value sat in a paragraph whose other figures (the same 143 origins, exposure
6.12 → 1.71, both from `rescue_routing.json`) are 439-series.

`docs/rescue_routing.md` (do-not-cite banner, superseded N = 452 stated) and
`docs/REPORT_ROUND2_P1.md` ("6 → 34 … on the synthetic lattice but 6 → 66 on real
roads") were **right all along**. Exactly two documents broke the rule.

**The second one was WFG-018's own reconciliation sheet** — the document whose
entire job is lineage truth. It had hardened the typo theory into
「34는 어느 산출물에도 없습니다」 ("34 appears in no artifact") and written the
student a 30-second spoken booth line telling them to say it. A judge could have
falsified that with one `grep`. Traced the propagation: `R7_rubric_gap.md` asked
the right question ("which artifact is 34?") and left it UNRESOLVED → the research
brief flattened it to "a typo, unregistered" → the sheet made it a claim about the
repository's own files. Corrected in all four places.

**2. `fold_sizes.md` and `NUMBERS.json` do not disagree** — both say pooled AUC is
primary. The real contradiction is `docs/MODEL_CARD.md`, which calls pooling
"**not** the generalization estimate". Both are right about *different questions*
(discrimination over the evidence vs generalization to an unseen fire); §3 of the
audit is now the single home for that and both files point at it.

**Found while fixing it: the metric three documents call primary had no registry
key at all.** Pooled 0.905 lived only in README prose and inside *other entries'
caveat strings*, while the non-primary mean-of-folds 0.890 was the registered
headline. Now registered as `lofo_rowweighted_pooled_auc` on new arm `A_ssot`,
read from the same committed `spread_v2_lofo.json`. **No value was edited and no
headline moved** — which metric leads the narrative is the author's call
(CHARTER §6), and the arm's `reason_no_control` says so explicitly.

**3. HANDOFF's "nearly seven times" was self-contradicting.** Both occurrences sat
two lines below the canonical shares they contradicted: 24.73 / 9.17 = **2.7×**,
whereas 6.7× is 24.73 / **3.70** on the retired share. Direction unchanged — 2.7×
supports the same conclusion.

## What I built

- **`docs/ssot_audit_2026-09-03.md`** (Korean) — the row's audit report: the two
  rescue lineages, the 2.7× correction, the pooled/mean-of-folds resolution, a
  confusable-pairs table mapping each quantity to its single home, and a §5 that
  states plainly what the audit does **not** show (it is not a transuite audit; 8
  of 296 registry entries were eyeballed; the new test guards one quantity only).
- **`tests/test_rescue_lineage_ssot.py`** (6 tests) — a *lineage* gate rather than
  a value gate: it pins both artifacts, and fails when a superseded 452-series value appears
  in tracked markdown without a lineage label beside it. Exemptions are a **named
  two-file ratchet with reasons**, not a keyword scan. **It found two instances the
  moment it was switched on** (the research brief and the R7 sweep) and **two more
  after the reviewer made me tighten it** (`BACKLOG_PROPOSAL_2026-09-03.md`, and
  `docs/results_rescue_draft.md`, whose line 29 cites
  `rescue_verify.json / robustness_verdict...` — a key the canonical file does not
  have and the synthetic one does). Four real instances, none of which any existing
  gate could see.
- Corrections with annotations, never deletions: `README.md`,
  `docs/submission_reconciliation.md` (row 8 + spoken line 8),
  `docs/HANDOFF_ROUND3.md` (×2), `docs/fold_sizes.md` ↔ `docs/MODEL_CARD.md`
  cross-annotation, and the two research files that carried the misdiagnosis.
- `docs/NUMBERS.json` 295 → 296, `docs/arm_protocol.json` arm `A_ssot`.

## The second finding: the suite's counts depend on whether it has run before

The first `gates.py --mode full` read **1,088 passed / 60 skipped**; three later
full runs — bare, with `-rs`, and with the gates' own `-p no:cacheprovider` — all
read **1,094 / 54**. Total outcomes were **1,148 in every one of them**, so nothing
was lost from collection: six tests moved skip→pass.

Diagnosed rather than waved through, because MEMO says this exact delta once hid a
defect. The six are `test_srtm_dem.py` ×4 plus `test_validation_robustness.py:57`
and `test_validation_session3.py:171`, all guarded on
`data/raw/dem/srtm/N36E129.hgt` — git-ignored, absent from a fresh clone. Something
in the suite calls `data_io.raster._download_srtm_tile`, which fetches the tile from
`elevation-tiles-prod.s3.amazonaws.com` (8,473,868 bytes gzipped, 25,934,402 on
disk); the file's mtime (16:34:35) falls inside
the first run. So the first run in a fresh clone skips those six and every run
after it passes them.

**This is the cause of the "unreproducible" 1,071/60-vs-1,077/54 reading the
previous lap logged, and that lap's attribution to its own `artifact` fixture was
wrong.** Consequence: every suite baseline recorded so far (1065/51, 1077/54,
1081/54, 1088/60, 1094/54) is an unlabelled mixture of first-run and re-run
readings, so the cross-lap count comparison the loop uses to prove no test was lost
has been comparing different quantities. Filed as **WFG-039** rather than fixed
here — it changes test network behaviour and deserves its own row and review.

## A concurrent lap landed WFG-002 while this ran, and the two findings met

The WFG-002 lap pushed three commits mid-lap, so this lap rebased onto them.
Two conflicts in `docs/auto/BACKLOG.md`, both resolved without forcing: the two
rows are now both `done`, and an **ID collision** was resolved in their favour.
They had filed a **WFG-038** of their own, from the same symptom I hit — the same
six-test signature, 1,158 outcomes both times, ALL GREEN printed for both — and
their row says plainly: *"The identity of the six is open."* They had ruled out
`data/cache/*.nc`, which is the wrong cache.

**This lap answers that question.** The six are `test_srtm_dem.py` ×4 plus
`test_validation_robustness.py:57` and `test_validation_session3.py:171`, all
guarded on `data/raw/dem/srtm/N36E129.hgt`. My row is renumbered **WFG-039** and
cross-references theirs: WFG-038 makes the drift visible, WFG-039 removes its
cause, and they should be worked together. Two independent laps converging on one
signature from opposite ends is the loop working as designed.

## What did not work

- **The charter mandates two skills a cloud lap cannot invoke.** `hate` (§4 step 3,
  every lap) and `prism` (the critic protocol) are both
  `disable-model-invocation: true`, so the Skill tool refuses them; they are
  reserved for a human typing `/hate`. `factchk`, `mandela`, `ssotize`, `sip`,
  `shower`, `catchup`, `nba`, `readchk`, `re0-memo` are all invocable. Recorded the
  objection's *reasoning* instead of claiming to have run the skill, and filed the
  wording problem in MEMO.
- **A rename could not clear the new key's collisions, unlike last lap.** MEMO's
  lesson says prefer renaming over pragmas, and I tried it first. It cannot work
  here: the two colliding lines (`HANDOFF_ROUND3.md:553`, `MODEL_CARD.md:116`)
  exist *in order to compare* pooled against mean-of-folds, so they necessarily
  carry lofo + pooled + auc and a third number. No key name avoids them. Added
  `collision-ok` with the reason on each, and wrote the distinction to MEMO: rename
  when the anchor overlap is accidental, pragma when the line's purpose is to hold
  both quantities.
- **My first final gate run was RED** and I nearly pushed on the earlier green.
  The MEMO/backlog prose I wrote *about* the superseded 452-series introduced four unlabelled
  superseded `452` mentions; `check_forbidden` caught them (same-line qualifier required).
  Fixed with truthful qualifiers, re-ran to green. The lesson the repo already
  knows, confirmed: re-run the gate after the last edit, not after the last *code*
  edit.
- **`baseline-verify` stays a WARN**, as expected off-laptop (git-ignored
  acquisition manifests). Not a regression.

## Evidence

- `docs/ssot_audit_2026-09-03.md`, `tests/test_rescue_lineage_ssot.py` (6 tests)
- `docs/NUMBERS.json` (296 entries, all re-derive), `docs/arm_protocol.json`
  (`A_ssot`)
- `scripts/auto/gates.py --mode full` **exit 0, ALL GREEN** on the final tree at
  head `d491305`: verify PASS, snapshot-verify PASS, env-check PASS,
  pytest **1,094 passed / 54 skipped** (a RE-RUN reading — see WFG-039), 0 failed,
  0 errors. ⚠ An intermediate green was recorded against head `392fe43`; that run
  did **not** cover `docs/ssot_audit_2026-09-03.md`, because `check_forbidden.py`
  reads tracked-and-staged files and the doc was still untracked. It went red on
  six unlabelled superseded `452` mentions once committed. Both the reviewer and I caught
  this; nothing red was ever pushed (`origin/auto/dev` held only the claim commit
  throughout).
- `check_number_collisions.py --report`: 0 unmarked collisions
- The lineage test's own first run, which failed on two files nobody had flagged

## KCF_READINESS lines moved

**None.** R8's collision/forbidden-gate half is now stronger and R6's sheet is no
longer carrying a false claim, but neither line is tickable: R8 still needs the
README Round-4 section and the English abstract, and R6 was already satisfied by
WFG-018. Ticking either would be exactly the decoration this lap's root objection
warned about.

## Next row

**WFG-020** — the Greenpeace 2026 survivor survey as registered evidence, if
WFG-002 is still held by the other lap; otherwise WFG-002. WFG-020's own row warns
the PDF may be unreachable from the sandbox, with a NEEDS_HUMAN fallback.

## The independent review blocked this lap, and it was right

`Reviewed by: subagent (block)`. **Root objection:** *"This lap's entire product is
a traceability claim, and the shipped commit fails its own traceability standard in
two independently verifiable ways."* Its three findings, and what I did:

1. **An unsourced number inside the very entry I added to fix homeless numbers.**
   The `lofo_rowweighted_pooled_auc` caveat said 의성·안동 and **영덕** supply
   "54.47% and 32.6%" of the rows. 54.47% is real; **32.6% is the 영덕 walk-bbox
   coverage caveat, not a row share** — the committed fold table gives 영덕
   13.66%, and the second-largest fold is 울진·삼척 at 27.42%. I had caught and
   fixed this myself minutes before the verdict arrived (the reviewer saw the
   concurrent edit and said so); it is fixed in `NUMBERS.json` and audit §3. This
   is the lap's own failure mode, committed by the lap that existed to prevent it.
2. **The committed head did not pass its own gate.** Correct, and the cause is
   worth keeping: `check_forbidden.py` reads tracked + staged files, so my new
   audit doc was invisible to it while untracked; committing it turned the gate
   red on six unlabelled superseded `452` mentions. Fixed and re-run to exit 0. Nothing red
   reached `origin` — only the claim commit had been pushed.
3. **The test was materially weaker than I described it.** This one I had *not*
   found, and it is the reason the block was correct. Its banner exemption
   ("SUPERSEDED" anywhere in the first 20 lines) silently exempted **15 tracked
   files including `docs/auto/JUDGE_QA.md` and `docs/HANDOFF_ROUND3.md`** — the two
   most judge-facing documents in the repo, one of which this lap edited — and its
   label list accepted bare 이전/정본, so a sentence **mis-attributing** the
   synthetic bracket to the canonical lineage passed *because* it said 정본. I
   reproduced both of the reviewer's counterexamples, then replaced the keyword
   scan with a named two-file ratchet carrying reasons, dropped 이전/정본, and
   broadened the mention pattern. Both counterexamples now fail the suite, and the
   tightened gate immediately found two further real instances.

I did not argue with any of it. The one point I record a disagreement on, without
acting against it: the reviewer flagged the test's line-79 comment mentioning the
"458-series" as a lineage slip; that reference is correct (the 458-series routing
counts 414/42/2 are a real, distinct series, and the registry caveat it quotes says
so), and the reviewer itself marked the point cosmetic.

## In plain terms

**What changed for the project.** A number in your README turned out to be right
but filed under the wrong experiment, and the document you would hand a judge had
turned that into a flat claim that the number came from nowhere — along with a
line telling you to say so out loud at the booth. That is now corrected everywhere,
and there is a new automatic check that will catch the same kind of mix-up before
it reaches a document.

**Why it matters to the judges or the science.** The one thing this project sells
is that its numbers can be traced. A judge who typed one search command could have
shown that a sentence in your own reconciliation sheet was false, and that would
have cost more than the number was ever worth. The honest version is also the
stronger one: you keep two versions of that experiment on purpose, and you can now
say which is which.

**What you should do.** Nothing is blocked. One thing is worth two minutes: the
booth answer for that row has changed, so do not say "it was a typo" any more — say
you keep an older synthetic run alongside the real-road one, and the submitted form
quoted the real-road one. Everything else on your list is unchanged.



## Gates

**ALL GREEN** · mode `full` · head `d491305` · 2026-09-03T17:22:01Z

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 14.2 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.1 s | make: *** [Makefile:140: baseline-verify] Error 1 |
| snapshot-verify | PASS | 0.4 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 158.8 s | 1094 passed, 54 skipped, 52 warnings in 157.44s (0:02:37) |

## Commits since the previous report (d4913050b707e2b7bceac43f4ced3cf9117b4405..HEAD)

- 5290ac5 lap report: WFG-004, the SSOT sweep
- 80e788f the lineage gate was weaker than its own description, and now it is not
- 6a2c8a3 the delay-row 6 to 34 is a real lineage, not a typo, and prose now has a gate for that
- 23307d7 the critic follows every dev lap, and the next dev lap fixes its findings first
- e7e02cc board: rebuilt on the merged line, so it shows the 10.24 date and both laps' rows
- 753ac8b the judge Q&A bank is 33 tiered questions, and three of its invariants are now tests
- fec353e the paper is written alongside the code: manuscript source, one figure style, a docx build and its own gate

## Needs a human (12 open)

- NH-001 [DECISION] Email delivery needs three repository secrets
- NH-002 [DECISION] Optional: `@claude` on GitHub issues and PRs
- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-008 [DECISION] Five questions for the KCF 운영사무국 (by 2026-09-07)
- NH-009 [DECISION] Repository decisions only the author can take (this week)
- NH-010 [DECISION] Expert consultations and the firefighter record (by 2026-09-20)
- NH-011 [DECISION] One real, recorded email send from a network that works in Shanghai (by 2026-09-20)
- NH-012 [DECISION] Portal downloads the loop cannot do (by 2026-10-01)
- NH-013 [FYI] Optional: a stable web address for the visual board
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
