# WildfireGuardian autoloop · paper · 2026-09-06T0348Z

| | |
|---|---|
| when | 2026-09-06 03:23 UTC · 2026-09-06 12:23 KST |
| branch / head | `auto/dev` / `6145001` |
| kind | manual |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 7, done: 16, parked: 1, todo: 72 |

## What happened this lap

# Paper lap 10 — the fair opponent enters the manuscript, without its margin

**Incorporated:** `6145001` (previous: `04153b7`). The code moved: WFG-114 landed the
present-perimeter "fair opponent" arm (`scripts/run_present_perimeter_arm.py`,
`data/processed/present_perimeter_arm_uiseong_andong_2025.json`, `docs/present_perimeter_arm.md`,
57 `pp_uiseong_*` registry keys) and WFG-121 landed `docs/fair_opponent_line.md`. That arm is
the answer to the manuscript's own §4.3 gap G7, so this lap's whole job was to write it in
correctly — and, harder, to write in what the project is not yet entitled to say about it.

## What changed and why

**A new §4.5, "The fair opponent: refusing where the fire is now."** The paper's headline
contrasts (42 of 458 at Yeongdeok, 91 of 368 at Uiseong-Andong) are measured against a
*fire-blind* baseline that consults no hazard at all. WFG-114 built the stronger opponent on
Uiseong-Andong — refuse everything inside a fixed buffer of the slice-0 perimeter, drop refuges
inside it, shortest path through the rest — and it recovers most of what that region's
fire-blind contrast credits to the forecast. §4.5 reports that qualitatively, with the
reproduction gate that warrants it (the run re-derives the committed classification, bucket
counts and origin identities, before measuring anything).

**§4.5 quotes no count from that arm at all, and says why in the paper's own voice.** The row
was built twice, concurrently, by two dev laps that could not see each other, by two different
constructions of the opponent — prune-and-shortest-distance with no budget, against the
time-expanded time-minimising router on a frozen hazard, budget-capped and able to refuse
departure — and they disagree substantially on the residual. That is NH-032, open, and
CHARTER §14b lists the manuscript as a judge-facing surface. **The deadlock is worth naming:**
the shared `pp_uiseong_*` caveat opens 「Four facts travel together or none of them may be
quoted」 and fact (1) *is* the margin, while NH-032 bars the margin — so quoting any count from
the arm was unavailable, and the section quotes none. The missing number is `[GAP]` **G8** in
`paper/GAPS.md`, with both builds described and the decision that closes it. The oracle caveat
(WFG-125) travels with it: the forecast-aware arm plans on the field it is graded against, so
any such margin is what a *noiseless* forecast buys.

**The width finding is stated at the strength the run supports, in neither direction.**
WFG-127 (P0, critic #23) found that the shipped 「no operator can know the right width」 line
outruns a five-point grid whose middle width's neighbours are a factor of two away. §4.5 states
the change of *kind* — at the narrow widths routes cross ground that is alight when the walker
crosses it, at the wide widths the detour outruns the evacuation budget — which both builds of
the opponent show, and then says the grid holds a single point in the region a
「which width」 claim would be about. It asserts **neither** spike nor plateau. §4.5's last
sentence also records that two documents this paper cites still draw the stronger conclusion
from those same five points; that is WFG-127(i), outside CHARTER §12's paths for this routine.

**§4.4 gains the over-budget qualification (NH-031, closed by the author 2026-09-06 option A).**
Table 2's over-budget column is the one bucket in the series that runs against the system. Its
2 Uiseong-Andong origins have fire-blind arrival times of 624.8 and 628.2 minutes, so under one
600-minute budget applied to both arms they are origins *no* arm saves rather than origins the
forecast lost. The committed value is not moved, and the paragraph reports the qualification
rather than a correction — the author's option-A key is approved but not yet registered
(WFG-122, todo), so the manuscript does not write as though it exists. Of the other two regions
it now says Uljin-Samcheok has not been re-read and Yeongdeok *cannot* be, its walk graph of
that vintage no longer being recoverable.

**Four surfaces updated to match:** the abstract (a clause saying the separation has now been
measured on a second region, qualitative, no numbers), §1's claims paragraph, §5's "what the
coupling adds" paragraph, a new §6 limitation ("the counterfactual was weak, and the stronger
one is run on only one region"), and §7's headline sentence, which now says outright that the
part attributable to the forecast is smaller and not yet a single number.

**§4.3's gap G7 is narrowed rather than closed.** The arm ran on Uiseong-Andong, not on the
canonical Yeongdeok 458. Leaving G7 as written would have been a fabricated limitation — the
same defect `docs/fair_opponent_line.md` §2 names on the booth surfaces — so the marker now asks
for the arm over the Yeongdeok origins specifically, and the GAPS.md row records what closed.

## Figure — written, then withdrawn from the manuscript

**F9 `paper/figures/F9_present_perimeter.png` is drawn and committed but NOT referenced.**
It is the failure-mode composition across the five buffer widths — crosses live ground / no
route to any refuge / arrives after the 600-minute budget — on a categorical x axis, because
widths a factor of two apart cannot support a curve drawn between them. It was in §4.5 until
the reviewer's block; see below. It stays in `FIGURES` rather than being deleted (CHARTER §3.7),
so it keeps being rebuilt and drops straight into §4.5 the day NH-032 is answered.
`check_paper.py` checks that every referenced figure exists, not the converse, so an
unreferenced figure is not a gate failure. Looked at with the Read tool in both versions:
legend below the axes (tall bars at both ends leave no provably empty corner), values inside
the segments that fit, nothing clipped or overlapping.

**The manuscript therefore still has 8 figures and the appearance mapping is unchanged** —
F1→1, F2→2, F4→3, F5→4, F8→5, F3→6, F6→7, F7→8. The lap had renumbered to nine and then put
it back; both directions were verified by reading the captions out of the built `.docx` with
`python-docx` and comparing them to the prose, which is the only check that catches this.
The §4.5/4.6/4.7 → 4.6/4.7/4.8 section renumbering **stands**, with its five cross-references
updated, since §4.5 is a real new section.

## Gaps

- **Opened: G8** — which build of the present-perimeter opponent defines the comparison, and
  therefore the forecast's residual advantage. Closes on the author answering NH-032 and NH-034;
  both artifacts already exist and are green. Not after the sprint — it needs a decision, not a run.
- **Narrowed: G7** — from "the arm is scheduled after the sprint" to "the arm has run on the
  other region; it has not run on the Yeongdeok 458".
- G2, G3, G4, G5, G6 unchanged. Seven markers now, six before.

## Length

Body **8,735 words** (was 7,639), against the 8,500 target and the 9,000 hard fail — over the
target, under the fail. The document **measures 22 pages under Carlito**, page objects and
page-tree `/Count` agreeing, against the author's 25-page ceiling (NH-028): **three pages of
margin.** It was 21 at lap 9 and the page went to §4.5's prose. The withdrawn figure is the
useful datum here: with F9 referenced the same document measured **23**, so a figure costs a
page and no words, exactly as the words-to-pages curve says and exactly what the word budget
cannot see. Measured, not inherited — this lap installed `libreoffice-writer
fonts-crosextra-carlito fonts-nanum` in the sandbox and ran `paper/measure_pages.py`, so
`built_pages` and its `built_pages_inputs` anchor are both values a run produced.

## Gates

`gates.py --mode full` — ALL GREEN (`verify`, `snapshot-verify`, `env-check`, `pytest-full`
1,551 passed / 56 skipped). `baseline-verify` WARN is the documented NH-029 behaviour: a grown
registry is information, not a failure. `check_paper.py` exit 0.

## Reviewer

**Reviewed by: subagent (block): the section was built on the claim that it states no figure
for the forecast's residual advantage, and that claim was false on the manuscript's own pages
— the new figure's bar totals against its "of 368 scanned" axis, plus Table 2's own row,
determine the contested margin in two subtractions, and it would have reached the reader
stripped of the five caveats the registry makes mandatory.** The reviewer's `first_nail` cost
nothing and needed no repository access: read the total above the 1 km bar and the axis
denominator, read Table 2's Uiseong-Andong row, subtract twice. It also found that the draft's
two derivable residuals disagreed with each other, because the one reconciling registered
value — the already-safe origins the buffer breaks — was the one the draft did not print.

Everything it raised was fixed rather than argued:

1. **The margin leak.** §4.5 now quotes no count from the arm at all, and F9 is withdrawn from
   the manuscript. The reviewer's own framing is the one adopted: withholding a number while
   printing its determinants is the appearance of restraint with none of the protection.
2. **The registry deadlock, which the lap had walked into without reporting it.** The shared
   `pp_uiseong_*` caveat forbids quoting the recovery count without the margin; NH-032 forbids
   the margin. Quoting neither was the only available move. That is now stated in §4.5, in
   GAPS.md G8 and above.
3. **`differ only in`** — corrected: the two builds also differ in objective
   (distance-minimising prune-and-shortest-path against the time-minimising time-expanded
   router), which is arguably the largest difference and which "differ only in" recast as
   housekeeping.
4. **NH-031 is closed, not open** — §4.4 no longer implies a pending qualification, and does
   not write as though the approved option-A key exists, since WFG-122 has not registered it.
   It also now says Yeongdeok *cannot* be re-read rather than merely has not been.
5. **`roughly 800 m to 1.5 km`** — an unregistered number lifted from a backlog row's prose,
   removed under CHARTER §3 rule 3. With it went "a narrow optimum at 1 km" (which presupposes
   what two registered forbidden phrasings forbid, surviving only because the gate matches
   spellings) and "a plateau that wide is a width an operator could aim at" (an unmeasured
   affirmative claim in the opposite direction from the one WFG-127 struck).
6. **The code contradicted its own docstring** — `F9_present_perimeter` said it deliberately
   did not draw the safe totals and then printed the totals and the denominator that give
   them. Both are gone from the figure and the docstring records why.

What it passed, having tried to break it: the section renumbering and all sixteen `Section 4.x`
and nine `Fig. N` cross-references ("the one part of the lap I could not break"); all 238
registered forbidden phrasings matched case-insensitively against the manuscript, zero hits;
`check_withdrawn_claims` clean; 27 citations, 27 bib entries, every one carrying a verified note
and a URL or DOI; and every printed value — 86, 91, 368, 624.8, 628.2, 600, the five widths and
the failure counts — arithmetically correct against the registry and the artifact. Its closing
observation is the one worth keeping: **every gate was green across the defect**, because no
gate can see a number that exists only as the difference of two printed numbers, which is
exactly the surface NH-032's instruction was written to protect.

## In plain terms

Your fair-opponent experiment is now in the paper, and it makes the paper better and the
headline smaller. The paper now says, in its abstract and its conclusion, that a router which
only avoids where the fire *is* recovers most of what the headline credits to the forecast —
instead of leaving that for a reviewer to notice.

What it does not say is any number from that experiment, and that is deliberate. Two of your
laps built the opponent differently and got answers far apart, and the registry rule on those
keys is that the disputed number has to travel with every other number from the run — so the
paper can quote all of them or none, and until you answer **NH-032** it quotes none. The
Yeongdeok half of the same comparison has not been run at all.

The one thing to do: answer **NH-032** (which of the two opponents is the fair one), then
**NH-034** (what the booth says). The paper is 22 of your 25 pages and the figure for that
section is already drawn and waiting — it goes in the same day you answer.



## Gates

**ALL GREEN** · mode `full` · head `6145001` · 2026-09-06T03:22:07Z · current at `6145001`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 17.5 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.1 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 0.3 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 216.9 s | 1551 passed, 56 skipped, 52 warnings in 216.02s (0:03:36) |

## Commits since the previous report (de7bd0a23409c535e5eb44471eed76a95deb6028..HEAD)

- 6145001 the critic report names the head it ships in as well as the head it reviewed
- fd5a779 critic #23: the loop's honesty discipline outran its evidence discipline by one sentence

## Needs a human (7 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
