> ⚠ **SUPERSEDED by `2026-09-04T0725Z-dev.md`, same lap, same commit range.**
> Regenerated after `check-number-collisions` refused this file's own summary
> table and then refused the paragraph explaining that refusal. Kept, not
> deleted: the RED was real and the record of it is the point.

# WildfireGuardian autoloop · dev · 2026-09-04T0719Z

| | |
|---|---|
| when | 2026-09-04 07:19 UTC · 2026-09-04 16:19 KST |
| branch / head | `auto/dev` / `bb2fb96` |
| kind | dev |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 7, done: 12, parked: 1, todo: 40 |

## What happened this lap

## Your decisions, applied

**Nothing new this lap.** The Gmail search
(`from:siyeong0318@gmail.com subject:"WildfireGuardian autoloop" newer_than:14d`)
returns 23 threads and every one of them contains exactly one message: the loop's
own sent report. There is no reply from you in any of them. PR #31 carries no
comments at all. So `decisions.py` had nothing to parse and
`docs/auto/decisions_seen.json` still does not exist. Nineteen NEEDS_HUMAN entries
are open; eleven of them carry an `**Options:**` line and are waiting on one line
of email from you.

---

## The lap

**Row: WFG-017 (P0, KCF) — `web/finals.html` refresh v2.** Claimed at `8ab569e`,
built at `a562045`.

Critic #7 handed this lap **no** `fix-before-next-row` item, deliberately, and
named WFG-017 instead. Its root objection was that eight windows had improved the
evidence and none had touched the thing five judges look at for five minutes, and
that **the cost of the first attempt was unknown**. Its cheapest test was: take the
row and find out what it costs.

**The cost is 10 seconds.** `scripts/build_finals.py` runs end to end in this cloud
sandbox on the committed snapshots, needs nothing git-ignored, and the row's
fallback (emit JSON, file a NEEDS_HUMAN, ask the student to run `make finals` on
the laptop) was not needed. That was the first thing this lap ran, before writing a
line of card. The risk critic #7 named was real to name and imaginary in size, and
the sprint has its ten days of slack.

### The root objection I recorded against my own plan (`hate`)

> Adding four registry-bound cards is mechanical, and the number bindings will all
> pass. The risk is not a wrong number; it is a **right number under a wrong verb** —
> a card that states a claim the artifact does not carry, while every numeric test
> stays green. That is F27's shape, then F35's, and this screen would be the fifth
> judge-facing document to acquire it.

Cheapest test: write each card's sentence, then read the artifact and ask whether
the number carries that verb. It caught two things, and neither was caught by a test.

1. **The operating-point card said 「나머지 폴드의 미검출률은 0.544~1.000」.**
   1.000 is the *perfect-miss* folds' own value, so "the remaining folds" was
   printing a figure that belongs to the folds it had just excluded. Found by
   screenshotting the card and reading it, not by the suite. The payload now takes
   the range over the folds that have a true positive (0.544~0.977) and the test
   asserts `fnr_max < 1.0`.
2. **The detection card is written in WFG-063's post-fix form, before that row
   lands.** Critic #7's F35: the size floor rules the **satellite out** and says
   nothing about whether the human channel is fast, complete or primary. The card
   therefore says 「정지궤도 위성을 일차 트리거로 둘 수 없습니다」 and its warn line
   says, in as many words, that which source *should* be primary was not measured.
   The 99 % statistic does not appear.

### What shipped

Four EVIDENCE cards and one RELIABILITY card, each with a 「근거」 provenance block,
each with its own caveat line:

| card | headline | artifact |
|---|---|---|
| 운영점 | pooled OOF recall 0.138, precision 0.308 | `oof_classification_metrics.json`, `per_fire_recall.json` |
| 탐지 바닥 | 0.08~0.69 ha size floor | `detection/gk2a_detection_floor.json` |
| 240분 지평 | 79.23 % of n = 2,008 | `detection/kfs_containment_duration.json` |
| 대피 지점 배치 | 20 → 24 households | `vulnerability/refuge_placement.json` |
| 제출본과 정본 (RELIABILITY) | prose only, no retired literal | `docs/submission_reconciliation.md` |

The operating-point card prints the three perfect-miss folds' `n_positive`
(8·24·34) beside their FNR, which is what the row asked for: without it, "three
folds catch nothing" reads as a broken model instead of as prevalence.

**23** registry keys added to `build_finals.py`'s `REGISTRY_KEYS` (18 → 41). 18 of the
23 are asserted present by `test_the_v2_cards_have_every_registry_key_they_read`, which
is the subset the four cards read at render time; the other five
(`oof_pooled_precision_at_operating_threshold`, `optpoint_*_fnr_advance_cut` ×3,
`l0i_survival_check_reached_by_fire`) are carried into the payload so the 근거 blocks and
the student's own answers have them, and are not read by a card. One new payload block,
`evidence_v2()`, reads the four structural facts a single key cannot hold (how many
folds have no true positive and how few positives they contain, the failing-household
denominator, the survival-check evaluation count) straight from the committed
artifacts, the way `model_evidence()` already did.

### What did not work, and what I found on the way

- **My first ban list forbade my own caveat.** `test_..._never_reads_as_lives_...`
  banned the string 「입지 결정」 while the card's own caveat says 「입지 결정이
  아닙니다」. A substring ban on a noun forbids the sentence that does the work. The
  list now bans the assertion (「입지 결정입니다」), not the noun.
- **A bare-digit scan of the built page hung pytest for ten minutes at 100 % CPU.**
  `assert "438" not in text` against the 2 MB payload: "438" is inside a canonical
  coordinate, so the assertion failed, and pytest then tried to render a 2 MB
  operand as an assertion diff. The gate now scans the small template and bans the
  **composite** spellings (`438/18/3`, `18/459`), because in this file `460` is a
  CSS max-width.
- **Two documents called the CSV's reference time a report time.**
  `docs/horizon_grounding.md:106` and `docs/auto/JUDGE_QA.md:242` both said
  「79.23 %가 **신고 후** 240분」 while `horizon_grounding.md` §2, thirty lines
  above, says in a warning box that the repository could not confirm what
  `발생일시` means. I weakened both to 「기록된 발생일시에서」 and called it a
  correction — **and the reviewer showed that was one-sided**, because the artifact
  itself asserts the report-time reading. See the review section below: both
  documents now state the conflict rather than a side. No number moves either way.
  `SESSION20_REPORT.md` carries the same clause and was left alone: it is a record
  of what was believed then, like the README's Round-2 section.
- **I did not touch `JUDGE_QA.md:240`**, the 「신고 우선 … 99 %」 sentence. That is
  WFG-063's whole row across four documents, and fixing one clause of a two-clause
  sentence is the exact failure critic #7 named twice. It is the next row.

### The claim gate, and its measured limit

`test_the_detection_card_rules_the_satellite_out_and_nothing_in` bans the spellings
this repository has actually written. The MEMO entry of 2026-09-04 says that is
worth little, because a phrase gate written from the corpus inherits the corpus's
vocabulary. So a second gate,
`test_every_trigger_priority_sentence_on_the_screen_is_a_negation`, reads no
spellings: **any line naming both a priority word and a trigger source must carry a
negation.** Mutation-tested against three phrasings that appear nowhere in the tree:

| mutation | spelling gate | negation gate |
|---|---|---|
| 「전화 신고를 우선 소스로 둡니다」 | passed (missed it) | **blocked** |
| 「최초 인지는 주민 신고입니다」 | passed (missed it) | **blocked** |
| 「위성보다 신고가 앞섭니다」 | passed (missed it) | **blocked** |

The trigger-source condition is load-bearing: without it the rule misfires on
「최초 임계 도달」 (a route-timeline label) and 「최초 승리 창」 (the ordering-boundary
result). **What it still cannot do** is see a fifth document, because it reads one
file. That is WFG-062, open.

### Evidence

- `docs/finals_screen_v2.md` — what each card says, its artifact, and what it does
  **not** show; the gate table; the mutation table; and five things this lap did not
  demonstrate (booth laptop, demo media, automated smoke, 5-minute script).
- `scripts/build_finals.py --verify` exit 0; SYSTEM INTEGRITY panel records
  `verify-numbers`, `check-forbidden`, `check-region-literals` all PASS with real
  exit codes.
- `scripts/check_screen_assets.py web/finals.html`: PASS (offline strict, no dash).
- `tests/test_finals_screen.py`: 17 → **29** tests, all green (26 before the review's
  three fixes added scope, size-floor and artifact-header gates).
- `docs/auto/finals/screens_20260904T0630Z/` — nine screenshots: one per act, plus
  each of the five new cards on its own, with a README saying what they are and are
  not (a record of the lap looking at what it shipped, not a test).
- Screens driven in headless Chromium: all four acts render, 12 evidence cards and
  10 reliability cards present, no page errors. The two failed requests are
  `intro-forest-loop.mp4` and `ambient-documentary.mp3`, the git-ignored demo media
  the screen is designed to work without.
- `gates.py --mode full` exit **0** at the pushed commit, with `--assert-head` and
  `--assert-reported` both exit 0 immediately before the push. `baseline-verify`
  WARN, expected off-laptop, `hard: false`, eighth lap running.
- **The census, and it needs two words this time.** The first full run of this lap
  read `1270 passed, 62 skipped` **COLD** against critic #7's cold `1261 / 62`:
  **+9, skips unchanged**, and +9 is exactly this lap's first nine tests. The run at
  the pushed commit reads `1279 passed, 56 skipped` and is **WARM** — the three tests
  the review fixes added take it to 1273 cold-equivalent, and the SRTM tile the
  earlier run downloaded turns six data-gated skips into passes (`1273 + 6 = 1279`,
  `62 - 6 = 56`). That is WFG-039 producing WFG-038's symptom inside one lap again,
  and the arithmetic closes exactly, so nothing is unaccounted for. **Do not compare
  1279 with a cold reading.**
- Two process failures this lap, both caught by the machinery rather than by me. The
  first full gate run was recorded at the **claim** commit with a dirty tree, which is
  verbatim the `24751fa` / `8d1decf` case CHARTER §8 exists for; the reviewer flagged
  it and the table above is the re-run at HEAD. Then that re-run came back **RED**:

  `check-number-collisions` refused the report's own summary table. The 운영점 row
  carried a recall value on a line whose anchor words (`oof`, `pooled`, `operating`)
  belong to the sibling PRECISION key, so the gate read one metric's number under the
  other metric's name. Both values are correct where they stand; the line was not.
  Spelling out both metrics cleared it, which is better than an allowlist entry.

### The independent review blocked, and it was right three times

`Reviewed by: subagent (block): the refuge card states a verification scope the
artifact does not carry — the full-layer recomputation covers ONE site at k = 1, and
the card put the agreement verb after k = 1, k = 2 and k = 3.`

The reviewer independently reproduced and **confirmed** the mutation table (it
injected all three phrasings into both the template and the built page, which is the
honest way and not the way this lap ran it), the `+9` arithmetic, the FNR fix, the
five caveats, `make verify`, and that no artifact was modified and the frame did not
move. Then it refused the push on three counts. All three are taken, none argued.

**1. The refuge card's verification scope (the root objection).** The card closed its
k1/k2/k3 sentence with 「빠른 탐색의 예측과 전 계층 재계산이 같은 가구 집합에서
일치했습니다」. `verification.full_layer_verification` recomputes **one node**
(`6579850774`) at k = 1 only, and that node is **not even the k1 optimum** —
`marginal_curve.k1_nodes` names `437261613`. k2 and k3 were never put through the
full layer. The numbers 20 / 24 / 0 were right and the verb after them was wrong,
which is precisely the failure mode this lap's own root objection named and then
committed. Fixed: the sentence now scopes to the one verified site, shows that it
left 4 of 24 still failing, and says the two- and three-site figures were not
verified that way. `test_the_refuge_card_claims_only_the_verification_that_was_run`
pins the **scope**, not the values.

**2. The 발생일시 correction was one-sided, and the test made it permanent.** I read
`docs/horizon_grounding.md` §2's warning box ("we could not confirm what the column
means") and changed two documents to match it. But the committed artifact the card
cites says the **opposite** in its own header: `kfs_containment_duration.json` →
`⚠_reference_time` = 「`발생일시` is a REPORTED start time … Every duration is
report-to-containment」, and all four registry entries the card reads carry that
caveat. So this is not doc-vs-nothing, it is **doc-vs-artifact**, and my edit picked
a side silently. Worse, my test banned the artifact's own wording from the screen
forever, which is a presentation layer outvoting its source through a gate. Fixed:
the card, `horizon_grounding.md` and `JUDGE_QA.md` now state **both** records and
read the distribution as 기록 → 진화, which is true under either; the test now
requires that disclosure instead of banning a phrase; and WFG-061 is widened to say
the conflict is with the artifact, which is what that row must actually resolve.
`SESSION20_REPORT.md` still carries the old clause, as a record.

**3. "18 registry keys" was wrong; it is 23.** 18 is the count *before* the change.
Found and corrected in this summary before the review returned, and it is exactly
rule 3 applied to the lap's own prose, so it stands as a block either way.

Two of the reviewer's minor points were taken as well. The 탐지 바닥 card printed
**0.19 ha** as its headline while this repository's own registry caveat says 「ORDER OF
MAGNITUDE ONLY … READ THE FLOOR AS ROUGHLY 0.1-1 ha」 — a five-second read got an
interval five times narrower than the one we state. It now prints the span the
artifact actually contains across the three assumed flaming temperatures
(**0.08~0.69 ha**) with the 750 K value in the sentence. And the refuge card now
shows `failing_after` = 4, which it had hidden.

The reviewer's `mandela` finding is recorded in `docs/finals_screen_v2.md` §4.1: the
two claim gates read **different surfaces** (spelling gate → the built page, negation
gate → the template), so mutating only the template makes the "spelling gate missed
it" column vacuous. The table survives because the reviewer re-ran it properly; the
procedure is now written down so the next lap does it that way.

Not taken, and said plainly: `JUDGE_QA.md:240` still reads 「트리거 설계가 신고
우선」 two lines from the clause this lap corrected, so the Q&A bank and the booth
screen disagree on critic #7's F35 question **right now**. That is WFG-063's whole
row across four documents and it is the next row. Splitting it would repeat the
one-clause-of-two failure that created F35.

### Next row

**WFG-063 (P0)** — the trigger recommendation whose evidence was withdrawn. The
screen already speaks in its corrected form; `JUDGE_QA.md` Q10, `detection_floor.md`
§10 and the booth card do not.

## In plain terms

**What changed for the project.** The screen the judges actually watch now carries
five evidence cards it did not have: how often the model catches a real fire at the
setting it runs at, what a weather satellite structurally cannot see, why the system
plans four hours ahead, what one more evacuation point would buy, and which numbers
have moved since you submitted. Each card also says out loud what it does not prove.

**Why it matters to the judges or the science.** For three days the daily critic
warned that this screen was the sprint's biggest risk because nobody knew how long it
would take. It took one lap and rebuilds in ten seconds, so that worry is gone and you
have slack. More usefully: the independent checker refused to let this lap publish
three times, and it was right every time. Twice the numbers were correct and the
sentence around them claimed more than the data does. That is the exact mistake that
loses points at a booth, and it was caught before you ever saw it.

**What you should do.** Nothing for this row. But nineteen questions are waiting on
you and no report has ever been answered, so please reply to this email with even one
line from the block below, so we know the reply channel works before the sprint ends.



## Gates

**RED** · mode `full` · head `bb2fb96` · 2026-09-04T07:18:39Z · current at `bb2fb96`

| step | result | time | last line |
|---|---|---|---|
| verify | FAIL | 18.4 s | make: *** [Makefile:113: check-number-collisions] Error 1 |
| baseline-verify | WARN | 0.2 s | make: *** [Makefile:152: baseline-verify] Error 1 |
| snapshot-verify | PASS | 0.5 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 211.5 s | 1279 passed, 56 skipped, 52 warnings in 209.85s (0:03:29) |

## Commits since the previous report (48f7c9be17d260f7290a9f6aa64e82c6092398a6..HEAD)

- bb2fb96 report: WFG-017, the finals screen, and the three blocks that made it honest

## Needs a human (14 open)

- NH-001 [DECISION] Email delivery needs three repository secrets
- NH-002 [DECISION] Optional: `@claude` on GitHub issues and PRs
- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-010 [DECISION] Expert consultations and the firefighter record (by 2026-09-20)
- NH-011 [DECISION] One real, recorded email send from a network that works in Shanghai (by 2026-09-20)
- NH-012 [DECISION] Portal downloads the loop cannot do (by 2026-10-01)
- NH-013 [FYI] Optional: a stable web address for the visual board
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-016 [DECISION] The critic routine has 30 minutes to land its findings, and needs about 40 (by 2026-09-05)
- NH-017 [DECISION] Three entries were closed on replies the repository cannot see, and one of them was wrong (by 2026-09-06)
- NH-018 [DECISION] Two laps read the same primary sources and disagree on two sentences (by 2026-09-08)
- NH-019 [DECISION] One report time, for one fire, decides what the detection result is allowed to say (by 2026-09-08)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
