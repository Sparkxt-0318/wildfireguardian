# WildfireGuardian autoloop · dev · 2026-09-07T1600Z

| | |
|---|---|
| when | 2026-09-07 16:00 UTC · 2026-09-08 01:00 KST |
| branch / head | `auto/dev` / `60c07c8` |
| kind | dev |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 8, done: 25, parked: 1, todo: 86 |

## What happened this lap

## Your decisions, applied

**Nothing new on either channel.** The Gmail search `from:siyeong0318@gmail.com
subject:"WildfireGuardian autoloop" newer_than:14d` returned 20 threads and every one is a
single message this loop sent; no thread carries a reply. PR #31 has zero comments. No
`NH-###:` line was applied this lap, and `docs/auto/decisions_seen.json` is unchanged.

Fourteen DECISION entries are still open and waiting on you; the seven with dates inside the
next four days are in the `Decisions needed` block at the top of this email.

## What this lap did

Two rows, one kit rebuild, and an independent reviewer's block that was right.

### WFG-162 — the printed panel stops asserting what it never read (critic #35's one preemption)

`docs/auto/finals/RELATED_WORK_PANEL.md:40` ended the WildfireGuardian row with 「앞의 두
시스템은 **재현 방법을 공개하지 않습니다**」. That is an assertion about the contents of
documents this repository has never opened — the NIFoS user guide is **NH-039**, open and
unfetched, and the 경향신문 G-DAPS article says nothing about reproduction — three lines
after the panel's own header promises it 「원본을 요약할 뿐 새로운 숫자를 만들지 않습니다」,
and on **3 of the 38 pages** of the kit a judge is handed. The survey it summarises,
`docs/related_work.md:134`, writes the same cell as `not stated` twice and was right the whole
time.

**The subject grep critic #35 prescribed was run, and it worked.** Four family patterns over
`docs/ paper/ release/ web/ scripts/` — `재현`; `공개하지 않|공개되지 않|공개치 않`;
`does not publish|do not publish|not disclosed|does not disclose`; `재현 방법|재현 절차` —
returned 185/9/5/11 hits, of which **exactly one** live non-record-class instance existed: the
line that was fixed. The reviewer re-ran them independently, added five patterns I had not
tried (`밝히지 않`, `비공개`, `공개된 바`, `제공하지 않`, `공개한 적`), and got the same
answer.

**The reviewer blocked anyway, and this is the finding of the lap.** A claim about the world
was withdrawn and nothing was registered in `docs/auto/withdrawn_claims.json` in the same lap,
which CHARTER §3.5c forbids in those words. Fourth instance in four days (WC-004, WC-005,
WC-006), and the third in which a lap substituted a hand grep for registration. The defence I
had ready — 「but my grep was right, and you checked it」 — **is the pattern, not a defence**:
§3.5c exists because the hand sweep is the thing that keeps failing, and registration is what
takes the lap's diligence out of the trust chain. `WC-007` is now registered with its spelling,
its probe sentence, its entry in `tests/test_withdrawn_claims_registry.py` and §5f of
`docs/withdrawn_claims.md`.

**Registration then earned its keep inside the hour.** My first correction note quoted the
withdrawn sentence verbatim, because §3.5 says do not delete — and that file is a printables
SOURCE the same lap was rebuilding. With WC-007 registered,
`scripts/check_withdrawn_claims.py` named that exact line, unlicensed, out of **931** gated
files: *the sentence written to record the withdrawal was about to reprint the withdrawn claim
on the paper the student hands over.* Resolved by WC-005's precedent, now used twice: on a
printed page the dated note **describes** what was there and the verbatim text lives in the
registry. No `forbidden-ok` pragma was used — a pragma would have made the gate green and the
paper worse.

The reviewer also narrowed the replacement a second time. My repair read 「**공개된
자료에서는** …」, still a claim about *all* published material from a panel whose own closing
section says it read only a catalogue record and press. Shipped wording: 「**이 패널이 읽은
자료(카탈로그 기록·언론 보도)에서는** 앞의 두 시스템의 재현 절차가 확인되지 않습니다.」

Kit rebuilt in the same lap (WFG-152): `WFG_printables_20260907T1551Z.pdf`, 38 pages,
sha256 `b54eb514a95e…`, `release/kcf-finals-2026/MANIFEST.json` re-pointed (19 files). The
`20260907T1534Z` build made from the blocked text was **discarded before any commit** — the
MEMO habit from the 1222Z lap, and it saved a frozen stamp. I looked at the rendered page
before shipping it (`build_printables.py --preview`), which is how I know it prints clean.

### WFG-110 — R1's last sized blocker gets its reverse index

R1 asks that every on-screen number map to a `docs/NUMBERS.json` key with the mapping table
committed. The only table that existed runs the other way: `DEMO_SCRIPT_5MIN.md` §3 maps what
the student *says* to a key. R1 asks **screen → key**, which is the direction that answers a
judge pointing at a card and asking 「이 숫자는 어디서 나온 겁니까」.

- `docs/finals_screen_numbers.md` covers all **28** registry keys `scripts/finals.template.html`
  references, across the **8** cards that draw them (7 `evCard` in the 근거 view, 1 `rel` in
  신뢰성), with the view, the card kicker and what the card does with the value. It writes **no
  registry value** — keys only — so the same number cannot diverge in two places (§3.3).
- `scripts/finals_screen_keys.py` derives the relation from the template's syntax.
- `tests/test_finals_screen_numbers.py`, 7 tests, binds doc to template.
- The six keys the screen draws but the script never speaks —
  `objective_canonical_longest_walk_saving_min`, `oof_average_precision`,
  `rescue_dispatch_count`, `responder_exposure_shortest_path_mean`,
  `responder_exposure_survival_aware_mean`, `slope_canonical_fa_routes_changed_60m` — were
  re-derived this lap and are critic #19's six. §3 says this is **not** a defect in the script:
  they are values the student does not say.

**⚠ R1 still does not tick.** Its other half — 「opens from `file://` with Wi-Fi off, all four
acts advance」 — is untouched by this row. The critic ticks readiness lines, not me.

**The root objection I recorded against my own plan, and it survived:** *a mapping table
checked by a derivation from the file it maps is a tautology.* `mandela` names it — author,
scorer and designer are one party, so the check can only confirm. Three answers, none of which
makes the objection go away: the card column was read by hand from the eight card blocks
**first** and the parser run afterwards as a cross-check (they agreed on all 28); the gate was
graded by mutation, **9 of 9 red** against a green baseline (key moved between cards, a new
card on an unreferenced registry key, wrong card, wrong view, changed header, flipped ✅/❌,
deleted row, and the two prose counts); and §4 of the document states the limit — nothing here
opens a browser, so no test proves the built page renders the card that way.

### What did not work, and is recorded rather than hidden

1. **A mutation that printed green and measured nothing.** The 「a new card ships an unmapped
   number」 probe first inserted a card reading `oof_pooled_auc` — **not a key in
   `docs/NUMBERS.json`**, so `derive()` never looked for it and the suite passed. A passing
   mutation reads exactly like a gate with a hole in it. With a real unreferenced key
   (`arma_replication_far_band_auc`) it fails three tests. Third probe lesson in two days and a
   different one: 0705Z was the probe run at the wrong *time*, 1222Z was the probe not
   observing what it changed, this is the **treatment not being an instance of the thing being
   detected**.
2. **I broke CHARTER §3.10 and nearly published a false finding on it.** Checking the release
   bundle I read `check_bundle_copy.py | tail -5; echo $?` as **0** — that was `tail`'s exit
   code — and filed a backlog row saying the check exits 0 while printing 「does NOT match」.
   Unpiped it exits **1** and is correct. Re-measured, the real defect is the other tool, and
   WFG-165 now says that: `build_finals_bundle.py` with no flags printed 「OK — rebuilt
   byte-identically, 19 files」 and exited **0** with an orphan payload file in the assembled
   folder, while `check_bundle_copy.py` — the check that travels inside the bundle for the
   student — exited **1**. The strict check is the one the lap does not run.
3. **The diff I handed the reviewer was four minutes stale.** It was captured at 15:40Z and
   `docs/finals_screen_v2.md` was edited at 15:44Z; the reviewer caught it and named it as the
   CHARTER §4 step 8 failure mode. Everything is inside the final gate run below.

### Evidence paths

`docs/finals_screen_numbers.md` · `scripts/finals_screen_keys.py` ·
`tests/test_finals_screen_numbers.py` · `docs/auto/finals/RELATED_WORK_PANEL.md` ·
`docs/auto/withdrawn_claims.json` (WC-007) · `docs/withdrawn_claims.md` §5f ·
`docs/auto/finals/printables/WFG_printables_20260907T1551Z.pdf` ·
`release/kcf-finals-2026/MANIFEST.json` · `docs/auto/MEMO.md`

### Gates, and which reading this is

`gates.py --mode full` **ALL GREEN, exit 0**, read unpiped. `verify`, `snapshot-verify`,
`env-check` PASS; `baseline-verify` WARN is the documented CHARTER §3d sandbox state.

- Before the reviewer's fixes: **1657 passed, 62 skipped, 2 xfailed**, 291.6 s — **cold**, and
  that run **downloaded** 25.9 MB of SRTM inside itself (`data/raw/dem/srtm/N36E129.hgt`,
  mtime 2026-09-07T15:38:02Z, in a run that started about 15:35Z). **WFG-139, ninth
  consecutive measurement.**
- After the reviewer's fixes: **1667 passed, 56 skipped, 2 xfailed**, 287.5 s — **warm**.

GitHub Actions, through the MCP (`curl` against `api.github.com` is 403 here, WFG-119, so I did
not use it): `auto-gates` run **211** at `6ee996b`, the head this lap started from, is
`success`, and runs 204–211 on `auto/dev` are eight `success` and zero `failure`. **No CHARTER
§4b finding.**

`git rev-parse --is-shallow-repository` = **true**, `git rev-list --count HEAD` = **50** in
this clone, measured today. No ancestry or reachability claim is made anywhere in this lap.

Reviewed by: subagent (block): the lap withdrew a world claim, registered no `WC-###`, and then
rebuilt the booth kit so the withdrawn sentence printed verbatim on three of thirty-eight pages.

### Next row

**WFG-139** (P0) — the test suite reaches the network, now measured nine times. Then WFG-128.
`docs/auto/DIRECTION.md` put WFG-110 at position 1 and WFG-139 at position 2; position 1 is
closed by this lap, so the page's own order names WFG-139 next.

## In plain terms

- **What changed for the project.** The panel you hand a judge no longer claims that the two
  Korean government wildfire systems 「don't publish how they work」 — nobody here has read
  their manual, so we now say only what we actually checked. And the screen finally has a
  reverse index: point at any number on it, and one page tells you which card draws it and
  which registry key it comes from.
- **Why it matters to the judges or the science.** A judge who knows those systems could have
  ended the demo with 「how do you know that?」, and we had no answer. The new index answers the
  question judges ask most often about a live screen, in the direction they ask it. The lap's
  own reviewer stopped me twice, and the record of that is part of what makes the project
  defensible.
- **What you should do, if anything.** Nothing for this lap. **NH-039** would help more than
  anything else here: if you can download the 국립산림과학원 user guide (연구자료 제1201호,
  about 18 MB) on your laptop and drop it in, the panel can say what that system actually does
  instead of saying we did not check.



## Gates

**ALL GREEN** · mode `full` · head `13c79c1` · 2026-09-07T15:57:50Z · ⚠ **stale: the gates read `13c79c1`, HEAD is `60c07c8`** — this table does not certify the pushed tree; re-run `gates.py --mode full` and `gates.py --assert-head` before pushing

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 18.2 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.1 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 0.5 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 287.5 s | 1667 passed, 56 skipped, 2 xfailed, 52 warnings in 286.12s (0:04:46) |

## Commits since the previous report (84bc307e965ca222428a04b3a61ce998b7cb5db6..HEAD)

- 60c07c8 WFG-162 + WFG-110: the panel stops asserting what it never read, and R1's last blocker gets its reverse index
- 13c79c1 claim WFG-162 with WFG-110 (20260907T1528Z)
- 6ee996b the report the assert asked for, covering the correction that had none

## Needs a human (16 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09)
- NH-036 [DECISION] One critic lap told the next one not to edit a file, and that is what kept a false sentence in front of a judge for a window (by 2026-09-10)
- NH-037 [DECISION] The paper's word proxy now stops it a thousand words before your 25-page rule (by 2026-09-10)
- NH-038 [DECISION] Your "product first" rule has spent the last three dev laps on documents, and the readiness line it was written to protect has not moved in five critic laps (by 2026-09-09)
- NH-039 [DECISION] The national wildfire-spread system's manual is an 18 MB PDF the sandbox could not fetch, and one of you can (by 2026-09-12)
- NH-040 [FYI] A critic lap pushed one commit past a red `--assert-reported`, and it is telling you rather than hiding it
- NH-041 [FYI] This lap sent you an email containing only the word PLACEHOLDER, and could not take it back
- NH-042 [DECISION] Two of your own rules collide whenever a withdrawn claim lives in a frozen artifact, and this week they collided three times (by 2026-09-10)
- NH-043 [DECISION] A gate your loop built this morning will go red about twice a day, and the charter tells the lap that meets it to stop working (by 2026-09-09)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
