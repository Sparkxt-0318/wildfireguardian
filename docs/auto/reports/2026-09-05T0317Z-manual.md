# WildfireGuardian autoloop · paper · 2026-09-05T0330Z

| | |
|---|---|
| when | 2026-09-05 03:17 UTC · 2026-09-05 12:17 KST |
| branch / head | `auto/dev` / `466900b` |
| kind | manual |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 6, done: 15, parked: 1, todo: 58 |

## What happened this lap

The code moved between `3bf4dc1` and `466900b` — eight files outside `paper/` and
`docs/auto/`, of which one row matters to the manuscript — so this lap built rather than
exiting early. Every figure, number and citation already in the paper survived unchanged;
what changed is four additions the new evidence supports, and the trims that paid for them.

## What was incorporated

**WFG-062, the withdrawn-claim registry** (`docs/auto/withdrawn_claims.json`,
`scripts/check_withdrawn_claims.py`, `docs/withdrawn_claims.md`, two test files). The
repository's withdrawn claims used to be guarded by hand-written per-file lists covering
11 files; they are now one registry that every tracked document is checked against, with a
declared exemption list for the loop's own process records. This is not a project-process
detail for the paper: §3.5 and §5 argue that the *instrument* is the transferable
contribution, and "withdrawn claims stay in the tree as withdrawn" was until now an
assertion about discipline rather than about a gate. §3.5 now says what the second registry
holds and — the part that matters for an IEEE reviewer — states its measured limit: it
matches spellings, not meaning. An independent probe of sentences the pattern author had
not written was caught wherever it reused a registered spelling and missed wherever it
reworded around one. That limit is written into the manuscript rather than designed away.

**Three written expert replies** (`docs/auto/research/EXPERT_REPLIES_2026-09-04.md`). §5
gains a short "What outside readers asked first" paragraph carrying the two questions
nothing here answers — whether prioritising rescue need by age alone stays appropriate in
an ageing population, and whether the results are usable without modelling how forest-fire
suppression and residential emergency response divide roles during a fire. The third
reply's substantive point became a §6 limitation in its own right (below), and the
wildland-urban-interface reply became a clause in §2 saying that the standard WUI
definition's transfer to rural Korean settlement is untested here.

**Critic #15's finding F2 (WFG-096)**, which named `paper/` explicitly: the routing
percentages have no sampling-uncertainty statement anywhere in the repository. §6 now
carries one, and it states the sampling frame rather than inventing an interval — the 458
origins are a deterministic systematic subsample, every 18th of the walk graph's 8,443
nodes and then filtered by the hazard at time zero and by a band around the predicted core,
so no design-based standard error is defined for the shares in §4.3 and §4.4, neighbouring
origins are not independent, and reading those shares as estimates for households would
need an interval nothing here supports. ⚠ **This lap's first version of that sentence
called the origin set a census and was flatly false; the reviewer killed it. See below.**

## Sections changed

- **§2** — one clause: the WUI definition's transfer to rural Korea is untested here.
- **§3.3** — "four limitations of this router" became "this router's approximations"; the
  count was about to go stale, and a brittle count is worse than no count.
- **§3.5** — the second registry, what it holds, and the spelling-not-meaning limit.
- **§5** — new "What outside readers asked first" paragraph; GAP G2 narrowed to the
  practitioner half.
- **§6** — two new blocks. *Origins are not households, and no interval attaches to a share
  of them* (the sampling-frame statement). *A pedestrian is not confined to the graph, but this
  router is* — a walker can cross a field where a vehicle cannot, so a shortest-path
  formulation borrowed from road-bound traffic may not transfer on foot; every route here
  stays on mapped ways and the direction of that bias is unmeasured.

## Figures

None added, none redrawn. `make_figures.py` regenerated all eight from committed artifacts
and produced byte-identical PNGs, so there was nothing new to look at. The appearance-order
mapping (F1→1, F2→2, F4→3, F5→4, F8→5, F3→6, F6→7, F7→8) was re-checked by hand against
every `Fig. N` in prose after the edits and is unchanged and correct. Every result section
still has its figure or table: §4.1 F2 + Table 1, §4.2 F4, §4.3 F5 + F8, §4.4 F3 + Table 2,
§4.5 F6, §4.6 F7 + Table 3, §4.7 Table 4.

## Gaps

- **G2 narrowed** — the researcher half is closed; the practitioner consultations (119 duty
  officer, 이장, social worker) remain, and remain after-sprint. One new item was written
  into the row rather than into the paper: `EXPERT_REPLIES_2026-09-04.md` names the three
  researchers by the author's decision *for the repository*, and consent to be named there
  is not consent to be cited in a publication. **The manuscript therefore names nobody** —
  "three domain researchers", "an external researcher". Before submission the author either
  seeks permission to cite each as a personal communication, or leaves it as it stands.
- No gap opened. G3, G4, G5 and G6 stand where they were; all four GAP markers were
  shortened to what is missing, with the detail that was in them already present in
  `GAPS.md`, which is where the gate cross-checks it.

## The length budget is now the binding constraint

The body was 7,362 words when this lap started — already 362 over the 7,000 target. It is
**7,467** now. That leaves **33 words of margin** under the 7,500 hard fail, and the next
lap that adds a clause fails `check_paper.py`.

Trimmed this lap: the abstract (two sentences merged, "we argue that" removed), §1's
three-claims paragraph and a rhetorical clause, §1's damage tally (2,246 households and
1.05 trillion won went; 99,289 ha and 3,819 homes stay, being the two the paper actually
uses), the four GAP markers, §4.6's mechanism paragraph, §4.7's opening and closing, §5's
conformal paragraph and its "why the negative dispatch result is worth keeping" restatement,
and the availability section. **Two trims went too far and deleted registered caveats; the
reviewer caught both and they are restored.** The record is in `GAPS.md` and below.

**No further trim of that kind exists.** Every remaining paragraph carries a registered
number and the caveat CHARTER §3 rule 3 binds to it, and this lap will not drop a caveat to
buy space. Closing the remaining 467 words to the target is structural, and the choice is
the author's, not a lap's. `GAPS.md` records the three options: move §6's designated-site
inventory block (~200 words describing an input no result uses) to an appendix; cut §4.7
(~530 words on detection timing) to a paragraph plus Table 4 and publish it separately; or
accept ~7,470 and rely on IEEE Access measuring pages rather than words, which the built
`.docx` is comfortably inside.

## Gates

`paper/build_docx.py` rebuilt the `.docx`. `paper/check_paper.py` exits 0 — 7,467 body
words, 8 figures, 4 tables, 27 references, 5 gap markers matching 5 rows in `GAPS.md`.
`scripts/auto/gates.py --mode quick` exits 0, re-run after the reviewer's fixes: `make verify` PASS (which now includes
`check_withdrawn_claims` over the manuscript alongside the registry collision and
forbidden-string scans), 40 tests pass. No citation was added, so no URL needed opening
this lap; `check_paper.py` re-confirmed all 27 keys carry a `verified` note.

## Independent review

**Reviewed by: subagent (block): the paper's one claimed contribution is the instrument,
and this lap's edit broke that discipline in both directions while every mechanical gate
stayed green.** All three findings were correct, were fixed before the push, and none was
argued with.

1. **A false sentence, newly written by this lap.** §6 said the origin set is "a census of
   that region's walk graph rather than a probability sample", and made that the stated
   reason the shares have no standard error. It is wrong:
   `data/processed/real_roads_real_hazard_canonical.json` gives `n_nodes` 8,443 against
   `n_origins_scanned` 458 at `origin_scan_stride` 18, and
   `scripts/run_real_roads_real_hazard.py:117-131` then drops nodes whose hazard is already
   at the cutoff at t = 0 and nodes outside a band around the core. That is a systematic
   subsample intersected with a hazard-dependent frame — 5.4 % of the graph, not a census —
   and it contradicted §3.3, §4.3, the Fig. 5 caption and the registered caveat's own words.
   Verified against the artifact, then rewritten to what the artifact says.
2. **A registered caveat dropped by a trim.** §1's compression deleted "its casualty figures
   are re-cited rather than measured", which is the caveat on
   `fire2025_chain_deaths_yeongdeok`. Restored.
3. **A second registered caveat dropped by a trim.** §4.3's compression deleted "and no
   per-origin ledger exists", the caveat that stops a reader inferring a per-origin
   reclassification count between the reverted and canonical lineages. Restored.

Two lesser findings were also taken: §5 said two questions "recur" when both came from one
of the three readers, and the shortened hindsight GAP had become vaguer than the §4.7 body
it points at. Both fixed.

**The lesson is the one this lap wrote into §3.5 about the other registry, turned on
itself.** `make verify`, the collision scan, the forbidden-string scan,
`check_withdrawn_claims` and `check_paper` were *all green* across both caveat deletions and
across the false census sentence. A compression that keeps every number can still drop the
caveat bound to it, and nothing mechanical here reads for that. `GAPS.md` now records both
deletions rather than the trim ledger's original claim that every compression was made
"without dropping a number or a caveat" — which was itself false when written.

Restoring the two caveats pushed the body to 7,508 and failed `check_paper.py`. The caveats
stayed; three passages carrying neither a number nor a caveat went instead — §5's
"why the negative dispatch result is worth keeping" (a restatement of §4.6's measured
mechanism), a rhetorical clause in §1, and the detection counts inside the §6 GAP that
`GAPS.md` G4 already holds. Final: **7,467 words, 33 of margin**, both gates green.

## In plain terms

The paper now says what it said this morning, plus three things it could not say before:
that the withdrawn-claim record is a gate the whole tree is checked against and not a
promise, that the 42-of-458 and 24.73 % figures are a systematic subsample of the road
network with no standard error behind them, and that a real person on foot can leave the
mapped paths this router is stuck on.

What is still missing is unchanged and is mostly on your laptop: the hindsight run that
would show whether those 42 routes avoided where the fire actually went (G4), the leak-free
Yeongdeok refit (G3), one 신고접수시각 record (G5), and the refuge comparison a dev lap can
run here (G6). The practitioner conversations — 119, 이장, 사회복지사 — are still yours.

Two things need you, not a lap. **The paper is 467 words over its target with 33 to spare
before the gate fails**, so the next lap cannot add a sentence; pick one of the three
options in `paper/GAPS.md` (move §6's designated-site inventory to an appendix, cut §4.7 to
a paragraph plus its table, or accept ~7,470 and let IEEE Access count pages). And **the
manuscript names none of the three researchers who wrote back** — they agreed to be named
in the repository, which is not the same as agreeing to be cited in a paper, so if you want
them credited as personal communications you need to ask them.



## Gates

**ALL GREEN** · mode `quick` · head `466900b` · 2026-09-05T03:16:35Z · current at `466900b`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 16.8 s | === make verify: PASSED === |
| pytest-quick | PASS | 10.5 s | 40 passed in 9.64s |

## Commits since the previous report (43710f7724dde8a3cc041717254dab9e0cb889d1..HEAD)

- 466900b critic #15: the booth script arrived, and the two sentences that keep it honest are the two it says to drop first

## Needs a human (5 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-025 [DECISION] `Main` cannot follow the green commit until you change one GitHub setting, and until you do, every green push emails you a red run (by 2026-09-08)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
