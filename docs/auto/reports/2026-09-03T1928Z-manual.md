# WildfireGuardian autoloop · paper · 2026-09-03T1955Z

| | |
|---|---|
| when | 2026-09-03 19:28 UTC · 2026-09-04 04:28 KST |
| branch / head | `auto/dev` / `e558ebd` |
| kind | manual |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 7, done: 4, in-progress: 2, parked: 1, todo: 27 |

## What happened this lap

## What this lap did

The manuscript went from a skeleton of twelve `[GAP]` markers and 605 words to a
complete publication-register paper: **7,204 words of body text, 7 figures, 2 tables,
21 verified references, 4 gaps** (`check_paper.py` exit 0, `gates.py --mode quick`
ALL GREEN). `paper/STATE.json` now records the head it incorporated, `e558ebd`, which
is the first time that file has been true — it read `null / 0 / 0 / 0 / 0` while the
gate on the same tree printed 605 words and 12 gaps (critic finding F6, now closed).

This was the first paper lap with anything to incorporate, so `last_incorporated_commit`
was `null` and the whole repository was in scope rather than a diff.

## Sections written

Everything except the title. Abstract; Introduction; Related work; Data and methods
(§3.1 inputs, §3.2 model and LOFO protocol, §3.3 hazard field and time-expanded
routing, §3.4 rescue ingress and dispatch, §3.5 the five control types and the
registry); Results (§4.1 held-out skill and baselines, §4.2 the operating point and
the vacuous conformal bound, §4.3 the Yeongdeok decision shift, §4.4 three regions,
§4.5 sensitivity, §4.6 rescue ingress and the negative dispatch-ordering result);
Discussion; Limitations; Conclusion; Data and code availability.

The paper's frame follows RESEARCH_BRIEF §(d): it does **not** claim to be a spread
model contribution (n = 6 against benchmarks of hundreds), and leads instead on the
coupling plus the evaluation design. Three claims and one explicit non-claim are
stated in §1 and each is closed in Results.

Every number is a `docs/NUMBERS.json` value written with the caveat the registry
attaches to it. In particular: mean-of-folds 0.890 and pooled 0.905 are always
labelled as different quantities; the 32.6 % coverage caveat travels with every
absolute Yeongdeok rate (abstract, §4.3, Fig. 5 caption, §6); the withdrawn
severity-versus-direction claim appears only as withdrawn, with the two reasons; the
short-budget failure shares never appear without their budget; origins are called
sampled walk-network origins and never households; the fire-blind baseline's 44
hazard entries are labelled as belonging to the baseline, not to the proposed system;
the three regions are explicitly not ranked.

## Figures

Four new, drawn by `paper/make_figures.py` from committed artifacts, in `style.py`:

- **F4 operating point** — per-fire recall at the shipped 0.3 cut with each fold's
  igniting-cell count and the two folds where the threshold is unreachable, beside the
  pooled PR curve against the 0.0197 prevalence baseline.
- **F5 decision shift** — the same 458 canonical-Yeongdeok origins under the fire-blind
  and forecast-aware policies (414/44 → 456/2), beside the hazard core's growth over
  the five slices.
- **F6 sensitivity** — three panels: the budget sweep with its flat-timing control, the
  slope-sampling sweep with its flat control, and the dilation axis with the 125 m
  breaking distance marked.
- **F7 dispatch ordering** — rescues against team count at the committed operating cell
  for four orderings plus 200 random permutations, beside the win/tie/loss tally by
  window.

I looked at every figure with the Read tool and fixed what I saw: F4's annotation
arrow crossed the curve and its prevalence label sat on top of it; F5's first draft
had the footnote overlapping the stage-2 labels and was rebuilt as a two-row
before/after bar; F6's budget panel had colliding log-scale tick labels and its
dilation panel had the legend on top of the break annotation; F7's legend sat on top
of the "123" bar label. F5's colours were then harmonised with F3 so vermilion means
"no safe route" in both.

**F1–F3 changed without a code change, and that is worth knowing.** `style.py` asks
for Source Sans 3 and this sandbox has only DejaVu Sans, so every figure re-renders
with a different face than the environment that first committed them. The figures are
deterministic within an environment, not across environments. Recorded in
`paper/README.md`; vendoring the font would close it and was not done.

## Gaps

Twelve markers closed, four opened, all four mirrored in `paper/GAPS.md`:

- **G1 (§1, not after sprint)** — the age composition of the March 2025 casualties.
  The repository sources it to three Korean press outlets the manuscript cannot cite
  with an openable URL. Needs the author (WFG-043 / NH-015).
- **G2 (§5, after sprint)** — the expert consultations, as design feedback, pending
  the author's consent handling (NH-009).
- **G3 (§6, after sprint)** — the leak-free Yeongdeok fold (WFG-032). Needs the
  laptop-only raw bundle.
- **G4 (§6, after sprint)** — the hindsight-field routing arm, opened in response to the
  reviewer's root objection. Route the same 458 origins a third time on a field built
  from the *observed* FIRMS detections and report how many of the 42 fire-blind routes
  actually intersect observed burn. ⚠ I checked the reviewer's claim that this runs in
  the sandbox today: it does not. The manifest is committed and records 2,290 detections
  spanning 2025-03-25T12:25 to 2025-03-27T04:28, but the detections themselves sit under
  the git-ignored `data/raw/firms_data/`, which is absent from a fresh clone. It is
  cheaper than G3 — no refit, no re-simulation — but it still needs the laptop.

## Two critic findings on `paper/` closed this lap

- **F2** — `references.bib` marked `wildfirespreadts2025` verified with the
  WildfireSpreadTS title and the Gerard/Zhao/Sullivan authors against
  arXiv:2502.12003, which is a different paper by different authors. The file now
  carries **both**, correctly: `wstsplus2026` (Lahrichi, Bova, Johnson, Malof, WACV
  2026) at that arXiv URL, and `wildfirespreadts2024` (Gerard, Zhao, Sullivan) at its
  own DOI. The critic's suggested replacement id, arXiv:2406.04759, is **also wrong** —
  I opened it and it is a graph-neural-network weather paper by Oskarsson et al. The
  dataset paper is not on arXiv under that title at all; it is reachable at
  `doi.org/10.52202/075280-3258`, which is where the entry now points.
- **F3** — the manuscript attributed 27 deaths to a source reporting a different
  figure. The Introduction now states what the World Weather Attribution report
  actually says (32 casualties, 26 of them in Uiseong-gun, more than 48,000 ha, around
  5,000 buildings), and the age-composition claim, which that source does not carry,
  became gap G1 instead of an unsourced sentence.

All 21 references were opened at their URL this lap: arXiv entries through the arXiv
abstract page or API, DOI entries through the Crossref record for that DOI, web
sources at the URL given. Every `note` says what was found there. Two candidates were
**not** cited because I could not open them (a PubMed record that returned only a
cookie notice, and an OpenReview page behind a verification screen).

## What did not work

- `check_forbidden.py` carries a word rule against the surname of an author of the
  gradient-boosting library this project does not use. The Next Day Wildfire Spread
  paper's sixth author happens to share that surname — a different, real person. I first
  tried a `forbidden-ok` pragma, and the explanatory comment I wrote beside it tripped
  three further rules by naming the banned tokens. The bib entry now abbreviates the
  author list with "and others", as three other entries in the same file already do.
  ⚠ This report is itself scanned by that gate, so the rule cannot be named here either;
  the first version of this paragraph turned the gate red after the rebase.
- `check_number_collisions.py` matched anchor words per source line, so the
  one-line-per-paragraph manuscript produced nine false collisions between unrelated
  quantities. Fixed by hard-wrapping the Markdown at 88 columns — `build_docx.py`
  rejoins wrapped paragraph lines, so the `.docx` is unchanged — plus rephrasing the
  three lines that cannot be wrapped (one list item, two figure captions).
- That same gate's number pattern does not capture a leading sign, so a **negative**
  registered value written with a typographic minus can never match its registry
  entry, and one written with an ASCII minus still cannot. Eight U+2212 characters
  were normalised to ASCII so the gate at least reads the digits, and the one
  remaining case was reworded. Both gate behaviours are now documented in
  `paper/README.md` so the next lap does not rediscover them.
- `## References` is generated by `build_docx.py` from `references.bib` at build
  time, so it is a heading in the `.docx` and not in `manuscript.md`. Adding it to the
  Markdown would duplicate it. Noted in `GAPS.md` rather than changed.

## Reviewer

Reviewed by: subagent (**block**): the paper's one positive claim — 42 of 458 origins
saved by the forecast — is graded entirely against the system's own predicted hazard
field, so "saved" means "re-routed around a model-flagged cell", not "away from where the
fire went"; with pooled recall 0.138, every control in the paper perturbs the predicted
field rather than admitting external truth.

**That objection is correct and it is now the first paragraph of Limitations**, together
with gap G4 naming the arm that would settle it. I could not run that arm: it is a new
experiment outside `paper/`, and the observed detections are not in the clone.

Seven further findings, all acted on:

1. **919 km² walk box** — the retired pre-2026-08-03 planar reading of a registered
   quantity (`mr_uiseong_bbox_area_km2` = 896.5). It came from HANDOFF's prescribed
   wording, which predates the geodesic correction, and it evaded both gates: the
   region-literal gate does not scan `paper/`, and the collision gate's number pattern
   matches only decimals, so an integer is invisible to it. **Fixed** to 896.5.
2. **3,926 km²** — unregistered, same pre-correction lineage. **Removed**; the sentence
   now says "the wider acquisition box" without a number.
3. **The dispatch-ordering lineage was missing.** Four registry entries carry the same
   capitalised caveat — drift-arm-B network vintage, synthetic hazard on two of four
   arms, a travel-aware occupancy rule the shipped code does not use, never the committed
   439 series — and I had dropped it from all of them, immediately after a sentence that
   says "On the committed 439-origin responder run". Since §4.6 is the paper's advertised
   negative contribution, that lineage is load-bearing. **Fixed**: §4.6 now opens with a
   lineage paragraph before any of its numbers.
4. **Table 1's baselines are the corrected-DEM lineage**, which is why the "shipped" row
   reads 0.8943 against §4.1's 0.890 — two different lineages of the same model, eight
   lines apart, with the un-adopted one unlabelled. Verified against
   `ml_baselines.json`, whose `dem_corrected_reference` is byte-identical to the
   hist_gbm row. **Fixed** in the caption, which also now gives the pooled figures and
   notes that the 0.0073 pooled gap barely clears the 0.0064 platform-drift floor.
5. **The 32.6 % caveat was missing from §1 and §5** — the two places a reader takes the
   headline from. **Fixed** in both.
6. **The spatial-translation break is a band, not a point**: 125 m is the minimum over
   eight directions and the band runs to 530 m. Verified in
   `forecast_robustness.json::breaking_band_m`. **Fixed**, and the artifact-only values
   in that paragraph are now named as committed artifact values without registry keys.
7. **"correlation"** is a literal `forbidden_phrasings` entry for the n = 3 rank
   statistic. **Fixed** before the review returned, and a full scan now reports **zero**
   forbidden phrasings anywhere in the manuscript.

Two style points were also taken: "sixfold" described an axis that is neither, and
"homes" now gets defined once as shorthand for sampled walk-graph origins.

The reviewer's own reading was checked in one place and it was right and I was not: it
re-opened the WWA page to confirm 26 of the 32 deaths were in Uiseong-gun rather than
Yeongdeok. What it confirmed as sound: all 21 citations resolve and carry verified notes;
mean-of-folds and pooled are never substituted; both withdrawn claims appear only as
withdrawn; every short-budget failure share carries its budget; origins are never called
households; the canonical and non-canonical budget lineages are not mixed.

Absorbing the fixes pushed the manuscript to 7,508 words, past the 7,500 hard fail. I
trimmed about 300 words of elaboration from Related work, Results 4.1/4.2/4.3/4.5/4.6,
Discussion, Methods and Limitations — no number, caveat, citation or limitation was
dropped to make room.

## Numbers

Body text 7,204 words against a 7,000 target and a 7,500 hard fail. At the
`build_docx.py` layout (A4, 11 pt, 1.15 spacing, 2.3 cm margins) that is roughly 12
pages of text, plus about 3 pages of figures, under a page of tables, a title page and
a page and a half of references — call it 17-18 pages against the 20-page budget.
⚠ That is an estimate from the layout, not a measurement: nothing in this sandbox
renders the `.docx` to pages, so the page count is the one number in this report that
is not traced to an artifact.

## In plain terms

The paper now exists end to end: it says the forecast changes the walking decision for
42 of 458 sampled points at Yeongdeok, admits the prediction model is weak at the
threshold it ships, and reports that your own dispatch-ordering idea loses to
"nearest first" — all with the numbers traced to files.

The reviewer found the one hole that matters and it is now written into the paper: the
42 is scored against your own forecast, so it shows the two routes differ, not that the
fire went where you predicted. The run that would settle it needs the raw FIRMS files on
your laptop. Also missing: one citation you have to supply (who died in March 2025 and at
what ages, from a source with a link), and your expert consultations.

Read the Abstract, Discussion and Limitations and rewrite them in your own words
before this goes anywhere — that is required by the rules and by `AUTHORSHIP.md`, and
the Discussion is where a judge will hear your voice or someone else's.



## Gates

**ALL GREEN** · mode `quick` · head `e558ebd` · 2026-09-03T19:27:23Z

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 17.9 s | === make verify: PASSED === |
| pytest-quick | PASS | 11.7 s | 40 passed in 10.45s |

## Commits since the previous report (11133882cb14c1b3ed9d4ddc8ef3d200f3ff7cfe..HEAD)

- e558ebd critic report: the gates block said RED for a tree that never left the sandbox
- a278a56 critic #1: the booth Q&A still concedes an error that was disproved, and the gate reads it as fine
- 5ed83cc claim WFG-020: the survivor survey is this lap's row

## Needs a human (13 open)

- NH-001 [DECISION] Email delivery needs three repository secrets
- NH-002 [DECISION] Optional: `@claude` on GitHub issues and PRs
- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-008 [DECISION] Five questions for the KCF 운영사무국 (by 2026-09-07)
- NH-009 [DECISION] Repository decisions only the author can take (this week)
- NH-010 [DECISION] Expert consultations and the firefighter record (by 2026-09-20)
- NH-011 [DECISION] One real, recorded email send from a network that works in Shanghai (by 2026-09-20)
- NH-012 [DECISION] Portal downloads the loop cannot do (by 2026-10-01)
- NH-013 [FYI] Optional: a stable web address for the visual board
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-015 [DECISION] The three sources behind the README's opening numbers (by 2026-09-08)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
