# WildfireGuardian autoloop · dev · 2026-09-09T1250Z

| | |
|---|---|
| when | 2026-09-09 12:50 UTC · 2026-09-09 21:50 KST |
| branch / head | `auto/dev` / `f9183b0` |
| kind | dev |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 9, done: 33, dropped: 1, parked: 1, title-derivable: 1, todo: 103 |

## What happened this lap

## Your decisions, applied

**Nothing new this lap.** The Gmail search (`from:siyeong0318@gmail.com subject:"WildfireGuardian autoloop" newer_than:14d`) returns only the loop's own sent reports — every thread carries exactly one message, so no reply is waiting — and PR #31 has no comments at all. Fifteen NEEDS_HUMAN items remain open, five of them overdue; this lap adds a sixteenth (**NH-052**) and it is the one that matters most, because it is the question your own front door asks.

## What this lap did

**Row: WFG-125** — 「every margin in the fair-opponent dispute is what a PERFECT forecast is worth, not what this project's forecast is worth」. First `todo` in table order; the row both `CRITIC_LATEST.md` and `DIRECTION.md` named next, and critic #50 set no `fix-before-next-row` item so this lap would reach it. Claimed at `f9183b0`.

**The root objection I recorded against my own plan (`hate`), before building.** The row's cheap branch is "record in writing that no predicted field is committed". A document that says 「we looked and the file is not there」 moves no judge question from 「no answer」 to 「an answer」 — critic #47 had already measured the coverage percentages and written them into the row, so re-stating them in a `docs/` file would produce a filename, not a result. That objection is what sent me to open the artifact instead of re-costing it.

**What I found, and it inverts the row.** The row's first question — 「which of them is a prediction rather than the graded truth」 — has an answer, and it is not the one five laps assumed:

- `data/processed/routing_demo_canonical.npz`, the canonical 영덕 field the headline 42-of-458 is scanned on, holds **both** `haz_stack` — the **leave-one-fire-out forward simulation**, already a model output (`scripts/build_canonical_hazard.py` fits on every fire except the target) — **and** `obs_stack`, the cumulative FIRMS-observed footprint, on the same 181×156 grid at 500 m, in the same file, scored against by nothing.
- So the forecast-aware arm does **not** plan on truth. **The oracle is in the grader, not in the planner:** what makes the arm unbeatable is that the grader uses the arm's own planning array as if it were truth.
- Therefore removing the oracle needs a different **grading** field, not a new **planning** field — and a complete one is committed. **No fill rule** over the four fifths of the grid the out-of-fold sample never scores, no refit, no re-acquisition. Critic #47's arithmetic was right and was about the wrong instrument.

**Measured**, at the best time-matched pair (27 min apart; `t=0` excluded because both stacks share the seed detection): the simulation puts **952** cells in the fire where **937** burned — within 2 % on area — and only **534** are the same cells. IoU **0.394**, false alarms 418, misses 403. Right size, wrong place, and routing depends only on the place.

**Evidence paths.** `scripts/measure_oracle_gap.py` · `data/processed/oracle_gap_yeongdeok.json` · `docs/oracle_gap.md` · `scripts/register_oracle_gap.py` (25 `og_yeongdeok_*` keys, additive) · `tests/test_oracle_gap.py` (11 tests; two graded by mutation, both red on the mutant and green restored) · `docs/arm_protocol.json` declares `field_comparison` with `control_required: false` and its reason.

**What did not work, and what I did not do.** I did not run the re-grading. It would change what a committed judged headline number means (42, 91), which CHARTER §6 makes yours, and it needs a written-down-first rule for reading `obs_stack` between observation times — the two time grids do not line up (0/180/360/540/720 vs 0/333/1005/1480/1812/2403) and a rule chosen after seeing the margin is the post-hoc maximum WFG-201 was filed about. So it is **WFG-213**, filed `blocked(NH-052)`, in table order and not at position 1 (NH-051). **No judge-facing surface moved and no committed number moved.**

⚠ **The asymmetry I am not smoothing over:** `hazard_uiseong_andong_2025.npz` carries **no** `obs_stack` at all. So this check is possible on the **42** and not on the **9** or the **27** — the region the fair-opponent margins come from is the region where it cannot be done from committed data. A test pins that so it goes red if a later lap adds the array.

**Reviewed by: subagent (block)** — root objection: 「All ten registered numbers cite an artifact that is not in the repository. `data/processed/oracle_gap_yeongdeok.json` matches `.gitignore:74`, and the lap added none of the negation lines the repo uses for its other 79 committed artifacts, so `make verify` is RED in the tree as presented.」 It was right, and it was the same failure my own `gates.py --mode full` had just gone red on. I did not argue with any of it. Fixed, all of it:

1. **The block itself** — `.gitignore` negation added beside the other artifacts, the file staged by name; `make verify` now exits 0.
2. **Its second nail, which made my evidence weaker rather than stronger, and which I would have shipped:** I had written that the IoU 「independently re-derives」 the model card's ≈ 0.40 from 「a **different** artifact by a different route」. It does not. `measure_oracle_gap.py` recomputes the *same* quantity as `forward_sim.py`'s `drift_vs_observed` — same `p_cut`, same nearest-observation matching, same estimator, same leave-target-out fit, same fire — differing only in the canvas. The agreement is close to mechanical. That false sentence was also sitting in the `caveat` band on every key, where the paper routine would have inherited it as established. Rewritten in `docs/oracle_gap.md` §4b and in the registrar band: a consistency check on the canvas change, **not** corroboration.
3. **The size claim was a slice property stated as a model property.** The selection rule is clean and does *not* pick the IoU maximum (`t=540` and `t=720` both score higher), but it does land on the best area ratio; across the artifact the ratios are 0.74 / 1.02 / 1.05 / 1.05, so at 3 h the simulation is **26 % under**. §4 now says so.
4. **Two soft §3.3 items:** the per-slice IoU series and the 285-minute gap were prose numbers with no key — now 15 further registered keys, one per per-slice quantity the prose quotes. And the 「18.3 % / ~82 %」 figure inherited from critic #47 was measured against the *older* 26,607-cell grid while this document tabulates the 28,236-cell canonical one; re-measured here (4,859 distinct cells = **17.2 %** canonical, 18.3 % older) with both grids named.

`gates.py --mode full` red on the first pass (the ignore rule, plus the two `test_finals_payload_rederives.py` staleness gates the 25 new registry keys aged). The screen was rebuilt with `make finals` **while `HEAD` was still the pushed claim commit** — the ordering rule this repository's own MEMO learned two laps ago — then re-run green.

**Next row:** WFG-027 (일정, at literal zero on all four judge surfaces), unless `DIRECTION.md` says otherwise. WFG-213 does not move until you answer NH-052.

## In plain terms

- **What changed for the project.** Everyone, me included, believed your evacuation router was being compared against the real fire — which would mean your headline number flattered it, and the honest fix looked expensive. It is not being compared against the real fire: it is compared against its own prediction, and the record of what actually burned has been sitting in the very same file all along, unused. So the fair test is one short run away instead of out of reach, and I measured the piece of it that needs no decision from you: your model gets the *size* of the fire almost exactly right (952 cells predicted, 937 burned) and the *place* only about half right.
- **Why it matters to the judges or the science.** 「비교하신 예보는 정답을 미리 본 예보 아닙니까?」 is the sharpest question a science judge can ask you, and until today the only honest answer was 「맞습니다」 and silence. Now there is a real answer that points at a file, and it ends in an admission — which has been this project's strongest card all along.
- **What the author should do.** Answer **NH-052**: may the loop run the fair test on 영덕 and report whatever it says? It would change what your headline 42 means, so I will not start it without you. And NH-032 and NH-034 are now two days overdue and are about the same number — one reply with three lines clears all three.



## Gates

**ALL GREEN** · mode `full` · head `90042dd` · 2026-09-09T13:08:14Z · the tree that was pushed

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 26.1 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.2 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 0.6 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 479.2 s | 1841 passed, 63 skipped, 3 xfailed, 52 warnings in 477.19s (0:07:57) |

⚠ **`report.py` ran before this lap's commits existed, so the table it generated was
the RED first pass at `f9183b0`** — the run that caught the `.gitignore` defect the
independent reviewer blocked on, and the two `test_finals_payload_rederives.py`
staleness gates the 25 new registry keys aged. Both were fixed in `f958c3e`, and the
release-bundle manifest the rebuilt screen aged was re-pointed in `90042dd`. The
table above is the run on `90042dd`, the commit actually pushed, and
`--assert-head` and `--assert-reported` both exit 0 on it. The red run is named
rather than deleted, because a report that quietly swaps one verdict for another
teaches the next lap nothing about why the first one failed.

`baseline-verify` WARNs on the two git-ignored `data/raw/**` contracts, which is
NH-029 / CHARTER §3d working as the author chose.

**The lap's four ids, all of which resolve:** `f9183b0` the claim, `f958c3e` the
work and the reviewer's fixes, `90042dd` the bundle manifest and the pushed head,
and this correction, which rides one commit later because a commit cannot contain
its own hash.

## Commits since the previous report (9c22ff37a7790e131319ee40ce3ae7765b5cdfe3..HEAD)

- f9183b0 claim WFG-125 (20260909T1230Z)
- 16f3525 critic #50: the front door asks the judge a question this repository cannot answer

## Needs a human (24 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09)
- NH-036 [DECISION] One critic lap told the next one not to edit a file, and that is what kept a false sentence in front of a judge for a window (by 2026-09-10)
- NH-037 [DECISION] The paper's word proxy now stops it a thousand words before your 25-page rule (by 2026-09-10)
- NH-038 [DECISION] Your "product first" rule has spent the last three dev laps on documents, and the readiness line it was written to protect has not moved in five critic laps (by 2026-09-09)
- NH-039 [DECISION] The national wildfire-spread system's manual is an 18 MB PDF the sandbox could not fetch, and one of you can (by 2026-09-12)
- NH-040 [FYI] A critic lap pushed one commit past a red `--assert-reported`, and it is telling you rather than hiding it
- NH-041 [FYI] This lap sent you an email containing only the word PLACEHOLDER, and could not take it back
- NH-042 [DECISION] Two of your own rules collide whenever a withdrawn claim lives in a frozen artifact, and this week they collided three times (by 2026-09-10)
- NH-043 [DECISION] A gate your loop built this morning will go red about twice a day, and the charter tells the lap that meets it to stop working (by 2026-09-09)
- NH-045 [BLOCKER] The staleness gate has closed `auto/dev` to every routine, and the one routine that met it is the one forbidden to clear it (by 2026-09-08)
- NH-044 [DECISION] The claim the paper just retracted is still live on the page the paper cites for it (by 2026-09-09)
- NH-046 [DECISION] Your product's definition-of-done names a command nothing in this project has ever run (by 2026-09-10)
- NH-048 [DECISION] One of the research routine's three literature channels has been dead for two runs, and a working replacement is already proven (by 2026-09-10)
- NH-049 [DECISION] Your critic routine is told to add judge Q&A cards and your own printing gate makes that impossible for it (by 2026-09-11)
- NH-050 [DECISION] You answered two of these questions two days ago and the loop never heard you, because you answered them on the routine page (by 2026-09-10)
- NH-051 [DECISION] The rule your loop has been obeying for three days is not the option it names, and the difference is why one row has been pushed down five times (by 2026-09-11)
- NH-052 [DECISION] The experiment that measures what your own forecast is worth is now one run away, and it would change what 42 means (by 2026-09-12)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
