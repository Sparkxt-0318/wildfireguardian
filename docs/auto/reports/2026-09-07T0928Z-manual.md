# WildfireGuardian autoloop · paper · 2026-09-07T0947Z

| | |
|---|---|
| when | 2026-09-07 09:28 UTC · 2026-09-07 18:28 KST |
| branch / head | `auto/dev` / `e649f2d` |
| kind | manual |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 7, done: 25, parked: 1, todo: 85 |

## What happened this lap

# Paper lap 15 — §3.5 claimed a withdrawn claim cannot survive in a file nobody listed, and four tracked files are carrying one right now

**Lap:** `wfg-autoloop-paper`, 2026-09-07. **Incorporated:** `e649f2d` (was `5cca6ce`).
**Paths touched:** `paper/manuscript.md`, `paper/GAPS.md`, `paper/README.md`, `paper/STATE.json`,
`paper/WildfireGuardian_Park_2026.docx`, plus this lap's report files. Nothing else.

**Reviewed by: subagent (block)** — root objection: *§3.5 asserts a completeness the gate does
not have, and the example this lap added is one the gate structurally cannot see.* The block
was right, it changed two of the three edits, and it found the only edit that actually
mattered. What follows is the corrected account, not the drafted one.

## What moved in the code

Fourteen commits. Outside `paper/` and `docs/auto/` the diff is documents, tests and the
release bundle — no new artifact under `data/processed/`, no change to `docs/NUMBERS.json`, no
model, routing or detection result. So no result section gained a figure or a table, and none
needed one. All nine `FIGURES` re-rendered **byte-identically**; no figure was added, changed
or looked at. The substantive change for the paper is `WC-006`: the booth kit's
dispatch-exclusion reason was declared false by the lap that had just shipped it, and
registered two laps later.

## The correction that mattered, which the lap did not find and its reviewer did

§3.5 has said, since the paragraph was written: 「every gated document is read against it, so a
withdrawn claim cannot survive in a file nobody thought to list.」

**That is false, and it is false right now.** The registry's scope is
`docs/auto/withdrawn_claims.json` → `scope.extensions` = `['.md','.html']`. Re-derived here
with `git grep` rather than taken on the reviewer's word: at `e649f2d`,
`scripts/check_withdrawn_claims.py` prints 「PASSED === 6 claims over 931 gated files」 while
`WC-006`'s exact registered spellings are alive in **four tracked `.json` manifests** under
`docs/auto/finals/printables/` (`20260906T0620Z`, `20260907T0032Z`, `20260907T0059Z`,
`20260907T0630Z`). They are frozen artifacts under CHARTER §3.2, so they are permanent, and no
list would have caught them: the extension is out of scope.

The sentence now reads 「cannot survive in a **prose** file nobody thought to list; **a data
file or a generator escapes it**」. **+9 words.** It also absorbs critic #32's WFG-155 finding
against this manuscript — that §3.5's later 「the template, which the scan does read」 is true
of `scripts/finals.template.html` (`gated: True`) and hides that `scripts/build_printables.py`,
where `WC-006` also lived, is `gated: False` — so the later clause was left alone and the
point is now made once, earlier, for both escape classes instead of one.

This is the paragraph a hostile reviewer attacks first, because the registry is this paper's
headline methodological contribution. The claim had been standing in it unchallenged.

## The count went to three and came back to two

The draft took 「September 2026 retractions skipped it **twice**」 to 「**three times**」, reading
`WC-006` as a third unregistered withdrawal after `WC-004` and `WC-005`. The reviewer's
objection, checked and upheld: **`WC-005` was registered inside its own lap** — correcting
commit `590c29a` (00:43Z), registration `d0a739e` (01:08Z), both in lap `20260907T0018Z` — and
CHARTER §3.5c's unit is the lap. What `docs/withdrawn_claims.md` §5d records is a near miss
caught by that lap's own reviewer, not a violation. Two retractions reached the branch
unregistered: `WC-004` and `WC-006`.

So the manuscript keeps 「twice」 — **the word did not move but both of its referents did**,
from `WC-004` + `WC-005` to `WC-004` + `WC-006`. Lap 14's 「twice」 was an over-count at the
time and is correct today for different reasons. A number that is right for the wrong reason
survives until someone checks it, which is what happened.

「edited or printed」 became 「edited or **shipped**」, same length: `printed` belonged to
`WC-005`, which left the tally. `WC-004`'s claim stayed live in `docs/auto/JUDGE_QA.md` Q35, a
file that lap had **edited**; `WC-006`'s stayed live in
`release/kcf-finals-2026/printables/manifest_20260907T0059Z.json`, **inside the folder a judge
is handed**, which the 0355Z lap put there by changing the bundle plan (`86d1b2c`, 17 files →
19). ⚠ The triad does not cover every surviving instance — `WC-006` also survived in
`docs/printables.md`, which that lap never touched — and the sentence does not claim it does.

## Four numbers this lap's own ledger got wrong

All four were in the paragraphs about not letting unchecked claims stand. Corrected in place
in `paper/GAPS.md` and kept as the record.

1. **「thirteen hours after the second」** — about **three**. `WC-005`'s lap is `20260907T0018Z`
   (`0c862cb`, 00:30Z), `WC-006`'s is `20260907T0320Z` (`ff566bc`, 03:19Z). Thirteen hours is
   the `WC-004`→`WC-005` interval, which lap 14 measured correctly and this lap copied forward
   one step. ⚠ `docs/withdrawn_claims.md` §5e carries the same 「열세 시간 뒤에」; the error
   originates there. **Dev-lap item.**
2. **「the first observed instance」** — the **second**. §5d records the same observation for
   `WC-005` three hours earlier: registering it turned 「PASSED, 4 claims」 into 「FAILED, 1
   unlicensed mention」 naming `docs/auto/finals/BOOTH_SETUP.md:240`. The draft's NH-037
   escalation rested on this being new evidence; it is not, and the escalation is now stated
   smaller.
3. **「`86d1b2c` declared it false」** — inverted. `86d1b2c` **asserted** it; `15f3df2`, 37
   seconds later in the same lap and after that lap's own reviewer blocked it, declared it
   false. The lap attribution stands, the commit attribution did not.
4. **「corrected it in three places」** — four files, five sites, per `15f3df2`'s own message.
   The 「three」 comes from `WC-006`'s registry entry, which undercounts. **Dev-lap item.**

## Gaps

No `[GAP]` opened or closed; the count stays at **7**. Still declined for want of words, at 6
of margin: 「registering the third found one no hand sweep had named」 (10 words), and the
Uiryeong shelter-siting MIP (`10.3390/systems13121125`) that `docs/related_work.md` supplies
as a §2 entry — which is doubly blocked, since that page states plainly it is not yet in
`paper/references.bib` with a `verified` note and CHARTER §12 forbids citing a work no lap
opened. §3.5's 「scanned by both gates」 is still the undercount lap 14 recorded.

## Build, measurement and gates

- **8,994 body words** (from 8,985), 8 figures, 4 tables, 29 references, 7 gaps. All 29 cited
  keys resolve in `references.bib`, all carry a `verified` note, none orphaned.
- **23 pages under Carlito**, `metrics_ok: true`, page objects and page-tree `/Count` agreeing.
  23 at laps 11–15. **Two pages** of margin against the author's 25-page rule; **6 words**
  against the 9,000-word proxy. Measured, not inherited — this sandbox needed the one
  `apt-get install libreoffice-writer fonts-crosextra-carlito fonts-nanum` that
  `paper/README.md` records; a clean clone still cannot measure (WFG-116's open half).
- `paper/check_paper.py` exit **0**; `built_pages_inputs` refreshed to `f2ee9be6c9c7c4e0` by
  the run that measured.
- `scripts/auto/gates.py --mode full`, foreground, exit code read not piped: **ALL GREEN**,
  1654 passed / 56 skipped / 2 xfailed.

⚠ **The first full run was RED on two tests and neither was this lap's.**
`tests/test_finals_screen.py::test_the_integrity_panel_names_a_commit_this_repository_has` and
`::test_the_escape_this_gate_cannot_close_is_still_open` failed because `web/finals.html` names
commit `62b58e1`, which the routine's **shallow** clone (50 commits) does not contain. This is
exactly the condition critic #33 diagnosed in the report shipped at `HEAD`: 55 commits back,
green after `git fetch --unshallow`, nothing in the tree wrong. `git fetch --unshallow` was run
— it changes no file and touches no artifact — and the re-run is the green one above.
**Flagged rather than absorbed: a routine whose default clone is 50 commits deep hits this on
every lap until the clone is deepened or that gate learns the shallow case, and it costs a full
six-minute suite each time.** Dev-lap or infra row, not this routine's.

## What the reviewer confirmed rather than objected to

Gates green on its own cold runs at `e649f2d`; all 29 citations resolve and carry `verified`
notes; §3.5's control figures re-derive from `docs/NUMBERS.json`
(`platform_drift_pooled_auc` 0.006428 → 0.0064, `platform_drift_far_band_auc` 0.030715 →
0.0307, `column_addition_floor_pooled_p95` → +0.0093, `column_addition_floor_far_band_p95` →
+0.0425); no unregistered number and no fabricated citation in the manuscript; and no withdrawn
claim is live in judge-facing prose in the current tree — the surviving instances are the
frozen `.json` artifacts named above.

## In plain terms

- The paper had a sentence claiming your evidence gate cannot miss a withdrawn claim in a file
  nobody listed. **It can, and four files in the repository are proving it today** — the gate
  only reads `.md` and `.html`, and the claim is sitting in four `.json` files it never opens.
  The paper now says so. That was the single most attackable sentence in it.
- What is still missing is **room**, and this lap is the clearest case yet: the corrections it
  had to make took 9 of its 15 spare words, and the next lap has **6**. It also had to leave
  out a related-work entry your own new survey page supplies.
- **Answer NH-037 — it is the only thing now standing between the paper and its own backlog.**
  The document is 23 pages against your 25-page rule while the 9,000-word proxy stops it about
  a thousand words early. Reply `NH-037: A` (raise the proxy to the measured 9,461-word /
  24-page point) or another option, and the next lap writes instead of rationing.



## Gates

**ALL GREEN** · mode `quick` · head `e649f2d` · 2026-09-07T09:17:25Z · current at `e649f2d`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 22.8 s | === make verify: PASSED === |
| pytest-quick | PASS | 12.8 s | 40 passed in 11.60s |

## Commits since the previous report (2896c9c94b16b32e20eb07774b789d30824b22eb..HEAD)

- e649f2d the critic report's own gate table is the finding, and the green re-run beside it is conditional on a deepening
- d4445d6 critic #33: R7 ticks, and the gate went red on a correct tree because the screen's stamp aged out of the sandbox's clone

## Needs a human (15 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09)
- NH-036 [DECISION] One critic lap told the next one not to edit a file, and that is what kept a false sentence in front of a judge for a window (by 2026-09-10)
- NH-037 [DECISION] The paper's word proxy now stops it a thousand words before your 25-page rule (by 2026-09-10)
- NH-038 [DECISION] Your "product first" rule has spent the last three dev laps on documents, and the readiness line it was written to protect has not moved in five critic laps (by 2026-09-09)
- NH-039 [DECISION] The national wildfire-spread system's manual is an 18 MB PDF the sandbox could not fetch, and one of you can (by 2026-09-12)
- NH-040 [FYI] A critic lap pushed one commit past a red `--assert-reported`, and it is telling you rather than hiding it
- NH-041 [FYI] This lap sent you an email containing only the word PLACEHOLDER, and could not take it back
- NH-042 [DECISION] Two of your own rules collide whenever a withdrawn claim lives in a frozen artifact, and this week they collided three times (by 2026-09-10)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
