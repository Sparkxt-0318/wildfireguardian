# WildfireGuardian autoloop · paper · 2026-09-11T2200Z

| | |
|---|---|
| when | 2026-09-11 21:42 UTC · 2026-09-12 06:42 KST |
| branch / head | `auto/dev` / `b5987b7` |
| kind | manual |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 10, done: 36, dropped: 1, parked: 1, title-derivable: 1, todo: 123 |

## What happened this lap

# Paper lap 33 — a rule that landed in the window's last commit made a manuscript sentence non-compliant, and the deletion paid for the claim critic #67 said was missing

**Incorporated:** `a334a9e..b5987b7`, **13** commits, **16** files outside `paper/` and
`docs/auto/`. `docs/NUMBERS.json` **554 → 555**, compared key by key: **one** addition
(`ppy_yeongdeok_slice0_components_8conn`), **zero** removed, **zero** existing values or
caveat bodies changed. One withdrawal registered, **WC-019**.

**Length:** `body_words` **8,995 → 8,993**; margin **5 → 7**; built pages **23** of the
author's **25**, measured this lap under Carlito. Nothing litigated was compressed and no
caveat or registered number was traded. Figures: **10 drawn, all byte-identical**, none
added.

**Reviewed by: subagent (block): the lap certified its own compliance in prose instead of
running the check — three of its own files said 「nothing in the repository cites that
clause」 while an own-voice, present-tense description of §4.3 sat falsified in the same
file.** Five findings, all sustained, all repaired before the push.

---

## 1. The mandatory correction, and the rule that made it one

`docs/auto/DIRECTION.md`'s NH-059 bar read 「do not put any `ppy_yeongdeok_*` count on a
judge-facing surface」. Critic #69 extended it to 「**and do not point at the section that
prints them** either」 in **`b5987b7` — the LAST commit of the range this lap incorporated**.
⚠ The first draft of this report said `8952a5b`, the head that critic *reviewed* rather than
the head it wrote; re-derived by walking the thirteen commits until
`git show <rev>:docs/auto/DIRECTION.md` first carries the clause. **A rule that lands in a
window's last commit is one the manuscript had no window in which to obey**, so this is a
first obedience rather than a missed correction.

CHARTER §14b names this manuscript a judge-facing surface, and §4.3 ended

> …reproducing the committed partition first, **with the per-outcome counts in
> `docs/present_perimeter_yeongdeok.md`**

— naming the counts and the file whose §4 prints all three in bold. That is critic #69's
booth-card finding one file over: the card that stopped explaining a withholding started
pointing at the page that prints it. Lap 32 wrote the clause in good faith.

**Deleted, and replaced by the artifact rather than the page.** §4.3 now names
`data/processed/present_perimeter_yeongdeok_2025.json` — listed in
`docs/artifact_manifest.json`, holding the same partition in JSON, printing no count in prose
— so the quantitative claim keeps a re-derivable source. That repair is the reviewer's; see
§5.

## 2. WFG-260 — the claim that needs no model, said where the claim is made

Critic #67 filed WFG-260 against **this manuscript**: the Abstract and §4.5 claim the fair
opponent 「needs no model at all」, and §4.3's account of what that opponent plans on was 「the
nodes alight at the departure slice」, which says nothing about whether those nodes are an
observation or a model output. A reviewer reading `docs/present_perimeter_yeongdeok.md` §5
item 6 would have concluded the latter.

**Re-derived in this lap's own process from `data/processed/routing_demo_canonical.npz`**,
not taken from the critic report or the dev report: `haz_times[0]` is `0.0`; `haz_stack[0]`
holds exactly `{0.0, 1.0}` while `haz_stack[1]` holds **3,961** distinct values;
`haz_stack[0] >= 0.5` and `obs_stack[0] > 0` are the same **249** cells and are **identical
cell for cell** (`np.array_equal`). The 249 is the already-registered
`dn_yeongdeok_t0min_n_cells` and is not a number this lap introduces.

§4.3's clause now reads 「a router **that plans on no model but is graded on the forecast**,
refusing only the observed detection cells (a 500 m detection scatter, not a mapped fire
line), recovers most of them」. ⚠ **The graded-on-the-forecast half was added on the
reviewer's block and is the more important half**: the arm's input is model-free, its
**scoring** is not, and `docs/present_perimeter_yeongdeok.md` §5 item 6 — which DIRECTION
forbids weakening — puts the oracle of this comparison squarely on the scoring side.

**The half that cuts against us rides in the same clause.** Re-derived here as well: those
249 cells fall into **226** 8-connected components with a largest piece of **3** cells (236
at 4-connectivity), on a grid whose `grid_extent` gives a **500 m** cell, rasterised from
VIIRS detections whose footprint the registry entry's own caveat gives as 375 m. The object
the arm refuses is a **detection scatter**, not a mapped fire line.

⛔ **No count from this run reaches the manuscript** — none of 226, 3, 249, 26 or 16 is in it
anywhere. DIRECTION bars the family while NH-059 is open, the registry entry for the 226
repeats the bar in its own caveat, and critic #69 added 「not 226」 to the list by name. 500 m
is the manuscript's own grid resolution, at **§3.2** and **§6**.

⚠ **No direction is asserted for that coarseness, and the temptation was real.** DIRECTION
reads the two halves together as 「the fair opponent is weakened by this project's own input
coarseness and the bias runs in this project's favour」. That inference rests on
`docs/present_perimeter_yeongdeok.md` §5 item 5's dilation probe, whose numbers **DIRECTION
itself calls unregistered and re-derivable by nobody** (WFG-259, P0 at position 1), and the
same item records that the obvious monotonicity argument is false. The paper therefore says
what the object **is** and leaves what it costs the opponent to the buffer axis it already
names twice.

## 3. The arithmetic, measured at every step

Never subtracted from intermediate totals, which is the error lap 32's reviewer caught twice
in one paragraph.

| step | `body_words` |
|---|---:|
| inherited | 8,995 |
| first draft of the §4.3 clause | 8,998 |
| second draft, carrying the scatter | **9,000** |
| after the first three trims | 8,994 |
| the wording the reviewer was given | 8,995 |
| **after the reviewer's block** | **8,993** |

**The 9,000 row is the one worth keeping.** `check_paper.py` fails on `> LIMIT`, so that
draft was **inside** the proxy and would have passed the gate while breaching DIRECTION's
「do not add a word without trimming one」 by five. The gate is not the rule.

All four trims are this lap's own new prose or payload-free rhetoric: the pointer clause;
§4.5's 「measured **rather than conceded**」; 「the weakness of a fire-blind baseline」 → 「a
fire-blind baseline's weakness」; and then that same opener compressed again, to 「Section
4.3's third caveat is now measured」, to pay for the reviewer's repairs.

## 4. What was not written, costed

- **The Uiseong-Andong half of the same audit — measured, not asserted.**
  `data/processed/hazard_uiseong_andong_2025.npz` holds `grid_extent`, `haz_times`,
  `haz_stack` and `ignition_xy` and **no `obs_stack` at all**. Its slice 0 is at `t = 0` and
  strictly binary exactly like Yeongdeok's, so the structure and seeding path plainly match —
  **but the observation it would have to be compared against is not in that archive**, and
  the cell-for-cell identity is checkable there by nobody. Sharper than the registry's own
  「NOT TRANSFERABLE」. §4.5 therefore keeps 「what a county office can run with no model at
  all」 as a statement about the **policy**, which is what it has always been. Writing the
  asymmetry costs about **20 words against a margin of 5** and is a fact about which arm
  *can* be audited rather than about the fire. Filed as a dev-lap row: commit `obs_stack`
  beside that field, or state the seeding path in the artifact's provenance, and the sentence
  becomes writable. **That is this lap's NH-037 datum, milder than lap 21's declined
  limitation and milder than lap 24's.**
- **The budget caveat on 「recovers most of them」.** The opponent runs **unbudgeted**
  (`scripts/measure_present_perimeter_yeongdeok.py` calls `naive_route` with no
  `time_budget_min`), which inflates 「most」. Declined as lap 32 declined it and for the same
  reason: **the inflation runs against this project**, so the sentence is if anything weaker
  than the evidence requires. The reviewer raised it and agrees it is not a block.
- **A figure**, for the second lap running and for the same two reasons, neither of them
  cost: every bar of the obvious figure is a count NH-059 is open on, and a committed PNG is
  the one surface the withdrawal registry structurally cannot reach.

## 5. The reviewer's block, in full

⛔⛔ **It is on the ledger, not on the manuscript, and that is the uncomfortable part: the
edit survived scrutiny and this routine's account of it did not.**

1. **ROOT.** Three of this routine's files said 「Nothing in the repository cites that
   clause」 while `paper/GAPS.md`'s lap-32 §2 — an own-voice, present-tense description of
   §4.3, in `paper/`, in the same file — was falsified in all three limbs, and this lap's
   only `GAPS.md` hunk never reached it. The lap ran a grep, wrote that it had, and did not
   read what came back. ⚠ It is the rule critic #69 wrote **in the same commit that forced
   the deletion**: 「grep the file you just edited for the sentences that DESCRIBE it」.
   `GAPS.md` §1 now carries the real grep with a row per hit; lap-32 §2 and the G7 cell are
   annotated in place (CHARTER §3.7).
2. **WFG-260 was filed against the Abstract and §4.5, both byte-identical to HEAD**, and the
   lap tightened §4.3 instead. The reviewer grants §4.3's wording is correct and narrow, and
   objects that it quoted the registry entry's fact (3) onto a judge-facing sentence and left
   fact (2) off every one. Repaired in the clause.
3. **The G7 cell's 「six surfaces」 is SEVEN** — lap 32 corrected six → seven in three files
   and missed that row. Annotated.
4. **This lap's annotation was placed so that six of lap 32's corrections read as this
   lap's.** Scoped explicitly.
5. **Two ledger numbers wrong:** 「§3.2, §3.3 and §6」 (§3.3 contains no 「500」) and 「no count
   … not 2 or 44」 (**2 and 44 are in the manuscript**, at `:387`, `:396`, `:431`, `:529` —
   the committed partition's own arithmetic, registered under other keys, none from the
   영덕 run). No bar is breached; the sentence offered as proof of that was wrong anyway.

⚠ **One reviewer point is recorded rather than fully discharged.** Its proposed warrant fix
shipped (the artifact named in place of the page), but §4.5 still describes the node-pruning
**construction** and no zero-buffer run on Yeongdeok, so a reader following the
cross-reference finds a method and not a run. Next lap's first item.

## 6. What was checked and found not to be owed

- All **19** registered withdrawn claims, every spelling, against `paper/manuscript.md`
  **per line** and **flattened**: **zero hits each way**. `WC-019`'s three English spellings
  were affordable to register in this window only because **paper lap 32 had already removed
  the sentences they name**.
- `check_number_collisions.py` exits **0**; `check_withdrawn_claims.py` prints 「PASSED === 19
  claims over 941 gated files」.
- Citations: **30** entries in `references.bib`, **30** `note` fields, `missing_refs: []`, 29
  cited. No citation was added, so no URL needed re-opening.
- `[GAP:` markers **7**, `| G\d+ |` rows **7**.
- The 32.6 % coverage caveat still travels with every absolute Yeongdeok rate; mean-of-folds
  and pooled are still named as different quantities wherever either appears.

## 7. Gates

`gates.py --mode full` exits **0**: `verify` PASS, `snapshot-verify` PASS, `env-check` PASS,
`pytest-full` **2158 passed, 65 skipped, 3 xfailed**. `baseline-verify` WARNs at exit 2,
NH-029's designed behaviour for a window that added artifacts. `check_paper.py` exits 0 and
took its **measuring** branch: `pages 23, calibri_face Carlito, metrics_ok true`, printing
**`84b91dde83d63607`**. ⚠ That string is the anchor **lap 21** recorded — the digest is over
the figure list, table count, reference count and `body_words`, and lap 21 also ended at
8,993 with the same eight figures, four tables and 29 references. A cross-check that the
digest behaves, not a second measurement.

The clone is **shallow** (`--is-shallow-repository` true, `rev-list --count HEAD` = **50**,
both measured this lap). **No ancestry or reachability claim appears anywhere in this lap's
output.**

## In plain terms

- The paper now says, where it matters, that the tough opponent you compare yourself against
  really does need no model — it refuses only what the satellite actually saw — and, in the
  same sentence, that it is still *marked* against your model's forecast and that what it
  refuses is a scatter of 500 m squares rather than a drawn fire line, which makes it an
  easier opponent than a real county office's would be.
- Still missing: the same check on the 의성·안동 arm, which cannot be done at all until
  someone commits the observed footprint beside that field; and any number from the 영덕 run,
  which stays out of the paper until you answer **NH-059** — due 2026-09-13, and
  `decisions_seen.json` shows no reply applied since NH-031 on 2026-09-06.
- What to do: answer NH-059, and NH-046, which is past due and is single-handedly holding the
  readiness checklist at 8 of 11. Nothing else in the paper is waiting on you.



## Gates

**ALL GREEN** · mode `full` · head `b5987b7` · 2026-09-11T21:42:24Z · current at `b5987b7`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 23.4 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.2 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 0.5 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 391.5 s | 2158 passed, 65 skipped, 3 xfailed, 52 warnings in 390.13s (0:06:30) |

## Commits since the previous report (8952a5b90038fd119d21de18e6b3339b6a41c04f..HEAD)

- b5987b7 critic #69: the card that stopped explaining a withholding now points at the page that prints it, and its own note nine lines up says it does not

## Needs a human (31 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09, one day past; raised to HIGH by critic #55 on a measured third instance)
- NH-036 [DECISION] One critic lap told the next one not to edit a file, and that is what kept a false sentence in front of a judge for a window (by 2026-09-10)
- NH-037 [DECISION] The paper's word proxy now stops it a thousand words before your 25-page rule (by 2026-09-10)
- NH-038 [DECISION] Your "product first" rule has spent the last three dev laps on documents, and the readiness line it was written to protect has not moved in five critic laps (by 2026-09-09)
- NH-039 [DECISION] The national wildfire-spread system's manual is an 18 MB PDF the sandbox could not fetch, and one of you can (by 2026-09-12)
- NH-040 [FYI] A critic lap pushed one commit past a red `--assert-reported`, and it is telling you rather than hiding it
- NH-041 [FYI] This lap sent you an email containing only the word PLACEHOLDER, and could not take it back
- NH-042 [DECISION] Two of your own rules collide whenever a withdrawn claim lives in a frozen artifact, and this week they collided three times (by 2026-09-10)
- NH-043 [DECISION] A gate your loop built this morning will go red about twice a day, and the charter tells the lap that meets it to stop working (by 2026-09-09)
- NH-045 [BLOCKER] The staleness gate has closed `auto/dev` to every routine, and the one routine that met it is the one forbidden to clear it (by 2026-09-08)
- NH-044 [DECISION] The claim the paper just retracted is still live on the page the paper cites for it (by 2026-09-09)
- NH-046 [DECISION] Your product's definition-of-done names a command nothing in this project has ever run (by 2026-09-10)
- NH-048 [DECISION] One of the research routine's three literature channels has been dead for two runs, and a working replacement is already proven (by 2026-09-10)
- NH-049 [DECISION] Your critic routine is told to add judge Q&A cards and your own printing gate makes that impossible for it (by 2026-09-11)
- NH-050 [DECISION] You answered two of these questions two days ago and the loop never heard you, because you answered them on the routine page (by 2026-09-10)
- NH-051 [DECISION] The rule your loop has been obeying for three days is not the option it names, and the difference is why one row has been pushed down five times (by 2026-09-11)
- NH-052 [DECISION] The experiment that measures what your own forecast is worth is now one run away, and it would change what 42 means (by 2026-09-12)
- NH-053 [DECISION] The word your front door uses for 42 stopped being right today, and the loop cannot pick its replacement (by 2026-09-12)
- NH-054 [DECISION] Four fifths of your front door's headline is limits, and no lap is allowed to decide whether that is the project's strength or its biggest presentation risk (by 2026-09-13)
- NH-055 [DECISION] Your front door compares the headline forecast number to a model the same page calls broken, and today the loop measured a second, harsher comparison it is not allowed to put beside it (by 2026-09-13)
- NH-056 [DECISION] A Korean university published a reproducible deep-learning wildfire model four weeks before your finals, and a lap may not decide on its own whether to mention it (by 2026-09-13)
- NH-057 [DECISION] The one thing your project now says it contributes has never once been produced from a real fire, and only your laptop can change that (by 2026-09-13)
- NH-058 [DECISION] Two of your routines built the same row in the same window, and the rule that was supposed to stop that cannot see the paper routine (by 2026-09-13)
- NH-059 [DECISION] Your loop measured the hardest question a judge can ask you, and the answer is uncomfortable enough that no lap may decide on its own whether you say it out loud (by 2026-09-13)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
