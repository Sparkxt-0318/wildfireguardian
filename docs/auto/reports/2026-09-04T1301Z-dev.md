# WildfireGuardian autoloop · dev · 2026-09-04T1301Z

| | |
|---|---|
| when | 2026-09-04 13:01 UTC · 2026-09-04 22:01 KST |
| branch / head | `auto/dev` / `3bfa59c` |
| kind | dev |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 7, done: 12, parked: 1, todo: 42 |

## What happened this lap

Lap: WFG-070, the first of critic #10's two `fix-before-next-row` items. Both are cleared.
**The author's twelve decisions landed mid-lap and are applied and acted on below.**
Claim commit `80f5fa3`; work `008ca36`; MEMO and the row this lap filed against itself
`97253ac`.

## Your decisions, applied

**They arrived mid-lap, and they are the first replies in eight days.** When this lap began,
the Gmail query CHARTER §6 specifies returned **30 threads, every one holding exactly one
message** — the loop's own report — and PR #31 had zero comments, so step 1b reported nothing
new for an eighth lap. **That is no longer true.** While this lap was building, the author
answered **twelve open items in one sitting**, and a concurrent session applied them at
`c180a5a`. This lap rebased onto them; the only conflict was the generated
`docs/auto/dashboard.html`, which is rebuilt below rather than merged by hand.

The decisions were applied through a channel this loop had not used, which is itself the
answer to NH-020: **not email.** `docs/auto/decisions_seen.json` now records twelve entries
under `claude-code-session-7da6bf25`, and CHARTER gained **§6b**: 「The author decides in an
interactive Claude Code session on the laptop, answered with buttons… Email replies remain a
valid second channel and are still read by step 1b.」 Thirty report emails asked for a reply
in a form the author was never going to use; the fix was the author changing the channel, not
the loop asking harder. **This lap's own draft, written before the rebase, ended 「Reply to
this email with one line — any line」. It has been removed.**

| entry | the author's decision | what it changed here |
|---|---|---|
| **NH-016** | **A** — the 3-hour grid, set on the routine pages | CHARTER §11 now reads dev `17 */3`, critic `57 1-22/3`, paper `47 2-23/6`. The dev/critic collision that made NH-016 urgent is gone |
| **NH-021** | **WFG-062 to P0, top of the table** | **This is the one that changes the next row.** It also lands on this lap's own subject: see below |
| **NH-020** | decide in a Claude Code session with buttons, not by email | CHARTER §6b written; this report's plea to reply by email deleted |
| **NH-017** | yes | closed |
| **NH-018** | **B** | closed |
| **NH-019** | **B** | closed — and NH-019 is the open question every annotation this lap wrote points at |
| NH-001, NH-002, NH-010, NH-011, NH-013 | skip | closed |
| NH-012 | deferred post-finals | closed |
| NH-005, NH-014 | stay open, with the author's verbatim reply recorded | still open |

**A decision that was recorded and not delivered, corrected by this lap.** NH-021's answer was
applied by inserting the WFG-062 row into the **sprint-plan table** — the three-column
「by | rows | what the author sees」 table at the top of `BACKLOG.md` — instead of the task
table. An eight-column row in a three-column table renders as garbage, and worse, CHARTER §4
step 3 sends every dev lap to the **task** table, where WFG-062 then appeared nowhere at all.
The author's 「top of the table」 would have been invisible to the next lap that went looking
for it. The row is moved to the top of the task table, unedited, with a dated note at the foot
of `BACKLOG.md`. The decision stands; only its placement was wrong.

**Nothing was guessed at.** Every line above maps to an entry; no reply text was interpreted.

## What this lap did

**The row.** Two laps (WFG-053, WFG-063) withdrew the claim that a satellite trigger would have
fired *after* the human report, narrowing five Korean surfaces to one sentence. Critic #9 then
certified the tree clean by 「grepping every `.md` and `.html`」. Critic #10 found the claim
alive in **English** at `RESEARCH_BRIEF_2026-09-03.md:75` — under the heading 「The ten hardest
judge questions, with the answers that survive the verdicts」, which is the student's own drill
material — and at `R3_science_gaps.md:22`. Every claim gate in this repository is Korean, and
so was the grep.

**The root objection I recorded against my own plan (`hate`).** The obvious fix is to add
English strings to a Korean-shaped gate, which makes this the *fourth* hand-rolled string
family in a tree whose last critic's root objection (F53) was that the loop keeps building
gates beside the registry that already holds their answer. A fourth family graded by its own
author is worth nothing. **Cheapest test:** write the candidate rule, run it over the untouched
tree, and print the hit count *before* fixing anything — critic #10's own prescription for
WFG-071, applied here. If it is mostly noise, do not ship it; state the limit in the docstring,
which the row explicitly permits as an outcome.

**The measurement, which chose the design.** Two candidates, over both research directories,
`README.md` and `paper/manuscript.md`, on the tree before a single annotation was written:

| variant | rule | hits on the untouched tree `4ff56d6` |
|---|---|---|
| A | direct mirror of the Korean structural rule: a priority word + ONE source noun, no negation | **27, mostly noise** — 「first ISEF delegation」, 「primary category」, 「before Round 3–5」 |
| B | the same, but requiring **both sides of the comparison**: a machine-detection noun AND a human-channel noun in one sentence | **4, of which 3 are the real claim** |

**That table said 37 when the reviewer read it, and 37 was wrong.** See the block-and-fix
section below; the number is now a committed function and a test, not a comment. B ships. The asymmetry is the finding: the withdrawn claim is a *comparison*, and demanding that
both compared things appear is what separates it from ordinary English, in a way that has no
Korean analogue because the Korean priority words are not ordinary Korean.

**It found a third instance nobody had seen.** `R7_rubric_gap.md:109` is a prepared answer to a
fire-scientist judge: 「a satellite trigger would still have fired 22–64 minutes *after* the
human report **in every fire we could test**, so the trigger interface is designed report-first,
satellite-confirm」 — with an 「every fire」 the same file's preamble forbids, in a sentence the
student would say out loud. Critic #10's manual grep raised this row on two instances; the rule
found a third. That is the only evidence offered that it beats a reader.

**What did not work, and is recorded rather than fixed.** I froze the four word lists, then wrote
sixteen fresh sentences and **predicted 11 of 16 in the test file before running it. It scored
8.** Two I was confident of escaped, and one of them taught something no amount of re-reading
would have: the semicolon split — the thing that lets the rule see `R3_science_gaps.md:22`, whose
third clause carries a negation that would otherwise cover the first — is *also* why
「Residents call first; the satellite catches up 22 minutes later.」 walks through, because
splitting on the semicolon leaves one side of the comparison in each half and a rule needing both
sides then sees neither. **The design decision buys one real instance and costs one natural
sentence, and only grading after freezing showed it.** None of the eight escapes was folded back
in as a passing case: that is critic #10's F55, and it is exactly what left the previous gate
graded by nobody.

**The second `fix-before-next-row` item**, one character: `tests/test_external_figures_carry_
their_scope.py` accepted `%` and `퍼센트` but not `％` (U+FF05, what a Korean IME types by
default), so 「목격 신고 99％가 …」 walked through the gate shipped last window. Fixed, with
critic #10's own escape added as a case credited to it. That file's docstring now also carries
critic #10's grade of the **shipped** version — **14 of 20**, one better at **15** with this
character closed — beside the 12/20 it had been quoting for its *first draft*.

**Coverage, stated plainly.** Four English surfaces, not the tree. `paper/manuscript.md` is
deliberately excluded: its only hit is the `[GAP: …]` marker that *refuses* the claim, split from
its own negation by a sentence boundary, and licensing it would put an HTML pragma inside the file
`paper/build_docx.py` converts to .docx and CHARTER §12 gives to another routine. A test pins that
single hit so the exclusion cannot be inherited silently.

**Gates, with the temperature, because the two runs in this lap are not comparable.** The
lap's first full run was **COLD** (`1353 passed, 62 skipped`), the six SRTM-gated tests
skipping on a fresh sandbox — WFG-039, a sixth consecutive lap. The certification run at
`9c934d6` is **WARM**: `1370 passed, 56 skipped`, exit 0, the same six tests now cached and
passing. Neither is presented as the other. Like-for-like against critic #10's cold `1342 /
62` at `3a70e16`, this lap's cold reading is **+11 passed, skips unchanged** — exactly the
tests it added.

**Evidence paths.** `tests/test_detection_ordering_is_not_claimed.py` (rule, guard list, the graded
set and the 8/16 assertion); `tests/test_external_figures_carry_their_scope.py`;
`docs/auto/research/RESEARCH_BRIEF_2026-09-03.md`, `sweeps_2026-09-03/R3_science_gaps.md`,
`sweeps_2026-09-03/R7_rubric_gap.md` (annotated, prose unedited per CHARTER §3 rule 7).

**Filed against myself.** **WFG-072** (P1): the rule names three surfaces it does not cover and
closed none of them. MEMO 2026-09-04 gained the gate that demands it — a stated limit that does not
become a row documents the hole for the reader who already knew, which is precisely how the English
hole survived three laps inside a docstring that admitted it in two places.

**Reviewed by: subagent (block → fixed → re-run green):** *「The number that chose the shipped
design — variant A's 37 hits, mostly noise — is the one number in this diff that nobody can
re-derive, and it does not reproduce.」*

The reviewer was right on every count and nothing here was argued with.

1. **The root objection.** 「37」 lived in a committed comment and in a backlog row and in no
   runnable thing. The reviewer rebuilt variant A in good faith from the shipped constants and
   got **27**. CHARTER §3 rule 3 — a number you cannot register, you do not write — and the gate
   never saw it because the rule is broader than its enforcement. **Cause:** the throwaway script
   matched the nouns *by substring* for variant A and *by word boundary* for variant B, so the two
   arms were never run under one rule (`\bcall\b` vs `call` is 「recall at threshold」 counting as
   a human-channel noun). **Fix:** variant A is now `english_ordering_violations_variant_a`, a
   committed function, and `test_the_number_that_chose_this_design_is_re_derivable` asserts both
   counts every run. Re-measured on the untouched tree: **A = 27, B = 4** — the reviewer's number
   exactly. The design call does not change; the ratio is 27:4 rather than 37:4. **37 is withdrawn.**
2. **The row's contract was met on the weaker branch and the report read as though it were the
   stronger.** WFG-070 names three English spellings; **all three escape the structural rule
   standalone**, because it requires a machine-detection noun and none of them contains one —
   「The human report is the primary trigger source.」, the literal claim WFG-063 spent a lap
   removing, passed clean. They now ship as `BANNED_EN_SPELLINGS`, a separate family with its own
   pragma tokens, **deliberately not merged** into the structural rule so that its 8/16 remains the
   score of the thing that was actually graded (`test_the_two_english_rules_are_scored_separately`).
3. **Two of three annotations were not at the point of use** — the reviewer's most important
   finding for the student, and the one this lap should have caught by reading its own output.
   The drill brief's warning sat **six long answers above** the sentence it withdraws, and the only
   marker at answer 7 was an HTML comment, **invisible in every renderer**. A student jumping to Q7
   the night before the booth would have seen the withdrawn sentence with nothing on screen. All
   three points of use now carry a visible **⚠ WITHDRAWN 2026-09-04** marker in rendered text.
4. **It graded the gate independently: 9 caught, 9 escaped of 18 — 50 %, the same rate as the
   frozen set's 8/16.** Two independent sets agreeing is the strongest evidence in this lap that
   the number generalises off its author's corpus. **None of its nine escapes was folded in.**
5. **Three further limits, recorded and not fixed** (they are WFG-072): markdown headings are
   scanned by *nothing* — `_blocks` flushes on any line starting with `#`, so a heading carrying
   the claim escapes all three families, inherited and never noticed; the structural pragma
   licenses a **whole block, not a line**, which the file's own 「per-line, on purpose」 overstates;
   and the noun lists are the real floor — 「Report first, satellite second」 escapes with **both
   sides named**, which is worse than the one-side-only limit advertised.
6. **The pre-registration is not evidence and is no longer offered as any.** 「Predicted 11, scored
   8」 is prose committed after the run, so nothing can falsify it. Said so in the file. The next
   lap that grades a gate writes its prediction as its own committed line *before* the run.
7. **It also caught a stale census**, which is the WFG-067 shape: the first report drafted
   `1353 passed, 62 skipped` from a run made before two later commits. The table below is the run
   at the commit this report ships in.

**Next row: WFG-062, by the author's decision, not by table order** — the registry of withdrawn
claims that any document can be checked against. NH-021 asked whether a sprint lap should go to a
gate or to the booth and the author chose the gate, which supersedes the 「table order stands」
default this lap followed when it started.

**And this lap is the strongest argument yet for that decision, against its own work.** WFG-070
shipped the **third and fourth** hand-rolled claim families in one file
(`english_ordering_violations`, `BANNED_EN_SPELLINGS`) beside two Korean ones, and measured the
new structural family at 50 % twice — 8/16 on a frozen set, 9/18 on the reviewer's independent
set. Critic #9 measured the Korean pair at 2/20. Four families, four vocabularies, four guard
lists, one file named for one of the claims, and a reviewer that broke the newest one nine times
out of eighteen. **The author picked the right row.** WFG-062's own constraint already says any
replacement must publish its catch rate against a mutation set its author did not write; this lap
leaves two such sets, and the reviewer's nine escapes, as its starting corpus. Then WFG-003 and
WFG-067.

## In plain terms

**What changed for the project.** Your practice sheet — the ten hardest judge questions, the file
you would read the night before the booth — was still telling you to say a sentence the project
spent two days deleting from everywhere else, because it was written in English and every safety
check you own reads Korean. All three copies now carry a dated warning above them saying what to
say instead, and there is a new check that reads English.

**Why it matters to the judges or the science.** A judge who hears 「the satellite would have been
slower than the phone call」 can ask what time the phone call came in, and the honest answer is that
this repository has never recorded one — so the sentence cannot be defended in either direction.
The new check also found a third copy nobody had spotted, in the prepared answer for the fire
scientist. **The reviewer stopped this lap from pushing once**, for a good reason: I had justified
the design with a number I had not made checkable, and when it rebuilt the measurement it got a
different one. The number in the report is now 27 instead of 37, it is a test rather than a
sentence, and the reviewer also caught that my warnings were in the wrong place on the page — at
the top of the file rather than next to the sentence you would actually read. Both are fixed.

**What the author should do.** Nothing, and thank you — you answered twelve questions while this
was running, which is the first time the loop has been told anything. Two notes. Your 「WFG-062 to
the top of the table」 was written into the wrong table and would have been invisible to the next
run; I moved it, unedited. And your instruction that the satellite-versus-phone-call question is
decided your way (NH-019 B) is what every warning I added today points at, so those warnings are
now correct rather than provisional. The next run starts on WFG-062, as you chose.



## Gates

**ALL GREEN** · mode `full` · head `6b1cddd` · 2026-09-04T12:59:41Z · ⚠ **stale: the gates read `6b1cddd`, HEAD is `3bfa59c`** — this table does not certify the pushed tree; re-run `gates.py --mode full` and `gates.py --assert-head` before pushing

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 18.4 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.2 s | make: *** [Makefile:152: baseline-verify] Error 1 |
| snapshot-verify | PASS | 0.5 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 205.8 s | 1370 passed, 56 skipped, 52 warnings in 204.09s (0:03:24) |

## Commits since the previous report (9c934d667153002bc3476e02309a27feb5c2b304..HEAD)

- 3bfa59c the reviewer blocked on a number that lived in a comment, and it was right
- 6244e60 the lap's own stated limit becomes a row, which is the gate it just wrote into the MEMO
- 58447e0 the withdrawn claim was alive in English because every gate in the tree reads Korean
- c180a5a the author decides twelve open items in one sitting; routines move to the 3-hour grid; WFG-062 goes first

## Needs a human (4 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.

---

## Certification of the commit this report ships in

The gate table above was rendered at `6b1cddd`, the pre-rebase head this lap built on. That
commit no longer exists on this branch: the author's twelve decisions landed at `c180a5a`
mid-lap, this lap rebased onto them, and the report was rewritten to apply and act on them.
The table is therefore marked **stale** by `report.py`, correctly, and this section is what
certifies the tree that is actually pushed.

| run | head | result | census | temperature |
|---|---|---|---|---|
| pre-rebase | `6b1cddd` | ALL GREEN, exit 0 | `1370 passed, 56 skipped` | **WARM** |
| post-rebase, report commit | `ed1107b` | ALL GREEN, exit 0 | `1370 passed, 56 skipped` in 208 s | **WARM** |
| certification, this commit | see `.auto/gates.json` at push time | ALL GREEN, exit 0, `--assert-head` and `--assert-reported` both 0 | `1370 passed, 56 skipped` | **WARM** |

**Both censuses are quoted with their temperature and neither is presented as the other.**
This lap's **first** full run was `1353 passed, 62 skipped` **COLD**, on a fresh sandbox where
the six SRTM-gated tests skip; every run after it is **WARM** at `1370 / 56`. The `+6 passed /
−6 skipped` between cold and warm is **WFG-039 reproducing for a sixth consecutive lap** and is
not a change in the suite: it is the same six tests, skipped cold and passed warm.

The like-for-like comparison against critic #10's cold `1342 / 62` at `3a70e16` is cold-to-cold:
**+11 passed, skips unchanged**, which is exactly the eleven tests this lap added before its
reviewer blocked it. The warm total then rises to `1370` because the reviewer's fixes added a
further seventeen — variant A's registered comparison, the three contract spellings and their
mutations, and the separation test that keeps the two English scores from being quoted as one.

**The earlier draft of this report, `2026-09-04T1255Z-dev.md`, never reached `origin`.** It was
written before the rebase and stated that no decisions had arrived, which was true when the lap
started and false by the time it finished. It is superseded by this file rather than annotated,
because an unpublished draft is not a record.
