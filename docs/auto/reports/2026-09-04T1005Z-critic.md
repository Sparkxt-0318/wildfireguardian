# WildfireGuardian autoloop · critic · 2026-09-04T1005Z

| | |
|---|---|
| when | 2026-09-04 10:05 UTC · 2026-09-04 19:05 KST |
| branch / head | `auto/dev` / `ce31b91` |
| kind | critic |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 7, done: 12, parked: 1, todo: 41 |

## What happened this lap

Critic lap #9. Window `12bf2d9..ce31b91` on `auto/dev`. No code, tests, data, figures or
`docs/NUMBERS.json` were touched; this lap writes only under `docs/auto/`.

## Your decisions, applied

**Nothing new — the sixth lap in a row with an empty mailbox, and this lap checked it two
ways rather than one.** Searched Gmail for
`from:siyeong0318@gmail.com subject:"WildfireGuardian autoloop" newer_than:14d`: **27
threads**, every one holding exactly one message, the loop's own report. Because the search
preview only shows a thread's oldest messages, this lap also **fetched the newest thread in
full** (`1a06ba6d9d642ed2`, the 0855Z dev report) rather than trusting the preview: one
message, no reply. PR #31 has **zero** comments (GitHub MCP, `get_comments`).
`docs/auto/decisions_seen.json` does not exist, because `scripts/auto/decisions.py apply`
has still never had anything to record, so `decisions.py seen` was not run against any id.

Sixteen NEEDS_HUMAN entries are open. **NH-016's date is today.** NH-020 asks the one
question the loop cannot answer for itself and is unanswered. NH-021 is added by this lap.

## The findings, ranked

**F47 (WFG-062, raised P1 → P0) — the window's gate is 2-for-20 against a reader who did not
write it.** The 0855Z lap's headline deliverable is a second string-gate family over seven
judge-facing surfaces. This lap wrote twenty affirmative human-primacy sentences from the
withdrawn *claim* rather than from the deleted *sentences*, having not seen the patterns, and
ran them through both shipped detectors: `primacy_violations` caught **0 / 20**,
`priority_violations` caught **2 / 20**, **18 / 20 are invisible to both**. Three escape
classes, none a spelling: a source noun outside `SOURCE_NOUNS` (119, 상황실, 목격, 주민,
이장, 전화 are all absent); a priority word outside `PRIORITY_WORDS` (출발점, 기점, 머리,
맨 앞); and — the one that matters — **a negation morpheme anywhere else in the sentence**,
which exempts the whole sentence, so 「위성이 1 ha 아래를 보지 못하므로 최초 인지는 사람
신고입니다」 passes. That is the shape this repository's own honest prose already uses
everywhere. **The lap is not accused of overclaiming:** its reviewer broke the first draft 19
times in 20 and the lap wrote the weakness into Q10d, a T0 answer the student memorises,
including 「부스에서 이 검사를 근거로 들지 마십시오」. That is the best act in the window.
The finding is about what to build next.

**F48 (WFG-069, new, P0, the one `fix-before-next-row` item) — the document that spent this
window forbidding a conclusion opens by stating it.** `docs/detection_floor.md:13` is the
first sentence of the file that `JUDGE_QA.md` Q10 and Q10d both cite as their 근거, and it
reads 「한국의 산불 탐지는 사실상 전부 사람입니다」, resting on the 99 % 목격신고 statistic
that §10 of the same file forbids, in bold, from carrying a conclusion in a judge-facing
document. §0 also writes 「보도된 해에」 for a figure the source states as 「올해」 at
2023-04-28, i.e. about four months of accumulation, which §10 itself calls a 잠정치 — an
interim presented as a settled annual fact, which is the exact case CHARTER §3 rule 5b
exists for. Both new gates are blind to it: no `PRIORITY_WORDS` token, and the 99 % clause on
the next line was pragma-licensed by the same lap as 「background with its source」. Both
decisions were defensible alone; together they leave the strongest surviving primacy sentence
in the repository inside the guarded file, unflagged. The fix is one sentence and one scope
label, no number moves, and the same article carries a count (경북 152대, 최초 발견 0건, two
years) that needs no interim label.

**F49 (WFG-057, P0, fourth window) — the Q&A bank now miscounts itself by eight, and the
report that named it miscounted it too.** Header at `:17-23` says 33 questions and
T0 14 / T1 13 / T2 6; the file holds **41 / 15 / 19 / 7**. All three tier counts are now
wrong, where last window one was. The student who obeys the header memorises fourteen of
fifteen T0s and the one skipped is **Q10d**, the guard. And critic #8 wrote 「38 questions and
15 T0s」 at a head that held **40** — the previous window's total beside the current window's
tier count. This lap **deliberately added no question of its own** even though the drill
turned up two, because adding a 42nd header to a file that says 33 makes the finding worse by
the hand of the lap reporting it.

**F50 (WFG-056, second window) — the daily check this routine is asked to perform still
cannot answer its question.** Ran `--assert-reported --base` against the four commits of this
lap's own window (`12bf2d9`, `d8aec94`, `0965b15`, `ec2e813`), where push boundaries are
known and only one report exists. All four exit 0 and all four name the same report, and that
includes `--base d8aec94`, the commit that *carried* critic #8's report. On a one-report
window the check is uninformative for every base, not merely weak. What this lap **can**
verify: every dev report in the 24 h window carries a `Reviewed by:` line except
`2026-09-04T0401Z-dev.md` (critic #7's F40, a record) and `2026-09-03T1300Z-dev.md`, which
predates the review rule's introduction at `a131daf` and is therefore not a defect.

**F51 — carried, verified unchanged.** WFG-067 (P0, second window: `git cat-file -t a562045`
still fails, `web/finals.html` still carries it, on a ☑ readiness line). WFG-051 (P0, **fifth
window**: registry says 중앙재난안전대책본부, `README.md:198` says 「경상북도 최종
집계·중앙재난안전대책본부 확인」, nothing in the repository supports the confirmation — the
oldest live defect in the tree). WFG-065 (third window: 8.2 km h⁻¹ in no judge-facing file).
WFG-054, WFG-055, WFG-061, WFG-050, WFG-048, WFG-044, WFG-038 / WFG-039, WFG-068 unchanged.

## What this lap verified rather than assumed

- `gates.py --mode full` at `ce31b91`: **exit 0**. `1312 passed, 62 skipped` in 206 s,
  **COLD**, against critic #8's cold `1273 / 62` at `12bf2d9`: **+39 passed, skips
  unchanged**, like for like. `verify` / `snapshot-verify` / `env-check` PASS;
  `baseline-verify` WARN, expected off-laptop, `hard: false`, ninth window and not a finding.
  `--assert-head` and `--assert-reported` both exit 0 at HEAD. **Green at HEAD for a sixth
  consecutive critic lap.**
- The WFG-063 fix is real in **meaning**, not only in spelling: grepped every `.md` and
  `.html` in the tree for affirmative primacy, and the only survivors are the
  `SESSION19_REPORT.md` record inside its own dated withdrawal block and §10's paragraph
  explaining what its table used to be. `docs/SESSION20_REPORT.md` and the other session
  reports carry none of it.
- `factchk`: the window asserts nothing new about the world. Its one external-world claim is
  Q35's, that `a562045` does not resolve, and this lap ran the command and got the stated
  answer. No citation was added, so nothing needed opening.
- `mandela`: no model, split, metric, arm or eval moved. It fires on the gate instead, and
  the lap had already named it — a scorer graded on buckets its own author drew.

## Scorecard deltas

Track B **81 → 82**: 제출 자료 **16 → 17**, the point critic #8 named and withheld, because
the reason it withheld it (one document, two opposite T0 answers) is closed and closed the
expensive way, across five files, with a record annotated rather than edited. It stops at 17
because of F49, F48 and WFG-051. 연구 목적 16, 설계와 방법론 15, 데이터 19, 창의성 15 held:
nothing touched a model, split, metric, arm, coupling or protocol, and 39 new tests are a
string gate, not 재현 progress.

Track A **73, every row held**. These judges do not read the Q&A bank; they hear the student
for five minutes at a booth, and what changed is a document, not a rehearsal.
`DEMO_SCRIPT_5MIN.md`, `BOOTH_SETUP.md` and `release/` all still do not exist; three of
eleven `KCF_READINESS.md` lines are ticked with eleven days of sprint left; the screen still
prints a commit that does not resolve.

## The root objection

**The loop's answer to 「a gate cannot read meaning」 has twice been a bigger string gate, and
it has twice graded that gate with mutations it wrote itself.** Measured from outside, this
window's gate catches 2 of 20, and it is blindest to the sentence shape this repository's own
prose already uses. It is strongest against sentences already deleted and weakest against
sentences not yet written. Meanwhile the strongest surviving primacy claim in the tree sits at
line 13 of the guarded file, licensed by a pragma the same lap wrote.

**Cheapest test, one lap:** build WFG-062's withdrawn-claims registry, but have the reviewer
subagent write the mutation set **before** the rules exist, and print the catch rate as a
number in the report, the way this repository prints pass/skip counts with their temperature.
Above about half, the registry is the right instrument and should absorb both hand-rolled
families. Below it, that is the more valuable answer: string matching is the wrong instrument
for claims, and the right one is that every judge-facing claim sentence cites a registry key
or an artifact, which is WFG-030's shape and a different lap. **Which comes first is not this
lap's call** — it costs a booth row in a twelve-day sprint, so it is NH-021, with options,
for the author.

## What was filed

- **New:** WFG-069 (P0, F48) with a details section; **NH-021** (DECISION, by 2026-09-06,
  three options) for the gate-versus-booth ordering disagreement.
- **Updated, not duplicated:** WFG-062 (P1 → P0, with the 18/20 measurement and the
  requirement that any replacement publish its catch rate against an outside set), WFG-057
  (the 41 / 15 / 19 / 7 table and the report-level miscount), WFG-056 (the four-base
  reproduction).
- **Scorecard:** one dated row appended to each of the three tables; no row rewritten.
- **KCF_READINESS:** R3 and R2 carry this lap's independent re-verification; no line's status
  changed, and R2's ☑ is explicitly noted as resting on a lap having read the panel text
  rather than on the suite.

## In plain terms

**What the judges would see today.** A project that is green, that answers the hardest
question about its fire-detection result the same way on the screen, on the card and in your
Q&A cards, and that tells you in your own script not to lean on its newest safety check
because it measured that check and it is weak. That last part is unusual and a good judge
will like it. Two things they would still catch in the first minute: your detection document
opens by saying Korean wildfire detection is essentially all human, which is the exact claim
the rest of the document spent this week withdrawing, and your Q&A cards say there are 33
questions when there are 41.

**What changed since yesterday.** The contradiction in your Q&A cards is gone. One sentence
now stands in all five places, and where a past session report had the old version it was
labelled as withdrawn rather than rewritten, so the record stays honest. Your submission-
materials score moves 81 to 82. The booth score does not move, because nothing that a judge
watches for five minutes was touched.

**What you should do.** Reply to this email with one line per item. **NH-016 is due today**
and NH-020 has been open since yesterday, and this is now the sixth report with no reply, so
the loop still cannot tell whether these are reaching you at all. NH-021 is new and it is the
only one that changes what gets built this week: booth materials first, or the gate work
first. One line each is enough.



## Gates

**ALL GREEN** · mode `full` · head `ce31b91` · 2026-09-04T09:53:26Z · current at `ce31b91`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 20.5 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.2 s | make: *** [Makefile:152: baseline-verify] Error 1 |
| snapshot-verify | PASS | 2.4 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 205.8 s | 1312 passed, 62 skipped, 52 warnings in 203.98s (0:03:23) |

## Commits since the previous report (ec2e813a6d17b8006aa225ff1970144d34c714b1..HEAD)

- ce31b91 the bank told the student to say what the bank forbids, and the gate that fixed it was the wrong instrument twice

## Needs a human (16 open)

- NH-001 [DECISION] Email delivery needs three repository secrets
- NH-002 [DECISION] Optional: `@claude` on GitHub issues and PRs
- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-010 [DECISION] Expert consultations and the firefighter record (by 2026-09-20)
- NH-011 [DECISION] One real, recorded email send from a network that works in Shanghai (by 2026-09-20)
- NH-012 [DECISION] Portal downloads the loop cannot do (by 2026-10-01)
- NH-013 [FYI] Optional: a stable web address for the visual board
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-016 [DECISION] The critic routine has 30 minutes to land its findings, and needs about 40 (by 2026-09-05)
- NH-017 [DECISION] Three entries were closed on replies the repository cannot see, and one of them was wrong (by 2026-09-06)
- NH-018 [DECISION] Two laps read the same primary sources and disagree on two sentences (by 2026-09-08)
- NH-019 [DECISION] One report time, for one fire, decides what the detection result is allowed to say (by 2026-09-08)
- NH-020 [DECISION] Twenty-five report emails, no reply, and the decision channel has never once been exercised (by 2026-09-06)
- NH-021 [DECISION] Two laps disagree on whether to spend a sprint lap on a gate or on the booth (by 2026-09-06)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.

---

## Addendum — the commit this report travels in

The gate table above names `ce31b91`, the head this lap **read**. This report was written
after that run, so the tree it certifies is not the tree it ships in (CHARTER §4 step 8; the
rule `12bf2d9` added).

`gates.py --mode full` was therefore re-run on the commit carrying this report, `adf712d`:
**ALL GREEN**, exit 0. `1318 passed, 56 skipped` in 203 s — **WARM**, because an earlier run
in this sandbox downloaded the SRTM tile mid-suite and the six SRTM-gated tests now execute
(WFG-039, fourth consecutive lap). It is **not** comparable with the `1312 / 62` cold reading
quoted above, and both are recorded here with their temperature rather than one being chosen.

This addendum is itself a later commit, and it is certified the way every push here is:
`gates.py --assert-head` refuses the push unless `.auto/gates.json` records a `full` pass at
exactly the pushed `HEAD` with a clean tree, and `--assert-reported` refuses it unless a new
report travels with the substantive paths. Both were run immediately before the push and both
exited 0, so the commit on `origin/auto/dev` is green at itself and not only at its parent.

Nothing in this addendum changes a finding, a score or a backlog row.
