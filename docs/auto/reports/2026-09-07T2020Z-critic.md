# WildfireGuardian autoloop · critic · 2026-09-07T2020Z

| | |
|---|---|
| when | 2026-09-07 20:20 UTC · 2026-09-08 05:20 KST |
| branch / head | `auto/dev` / `64f015b` |
| kind | critic |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 8, done: 25, parked: 1, todo: 89 |

## What happened this lap

Critic lap #37 (report stamp 2026-09-07T2020Z). Reviewed head `64f015b`, window the 24 h to 2026-09-07T2020Z.
This routine changed no code, no test, no data, no figure and no
`docs/NUMBERS.json` entry; it wrote only under `docs/auto/`.

## Your decisions, applied

**Nothing new, on either channel, and the ledger says why that matters.** The Gmail
search `from:siyeong0318@gmail.com subject:"WildfireGuardian autoloop" newer_than:14d`
returns **96 threads across both pages**, and **every one of the 96 is a single-message
thread this loop itself sent** to your address (each carries the `SENT` label, each has
exactly one message, and each snippet is a report body). I paged to exhaustion rather than
reading the first page, because a reply would sit inside one of these threads and lift it
to the top only if it were recent. There is no reply, so
there is no `NH-###:` line to parse and `scripts/auto/decisions.py` was not run against
anything. PR #31 has **zero** comments. `docs/auto/decisions_seen.json` is unchanged and
records `"seen": []` — **no decision has ever reached this loop by email**; all 23 applied
decisions came from Claude Code sessions on your laptop, the newest being NH-031 on
2026-09-06.

That is not a complaint about the channel, it is finding 4 below: **NH-032 and NH-034 are
due tomorrow.**

## Verifying the loop's own claims

**Baseline GREEN, on the routine's default clone, before any deepening, read unpiped.**
`git rev-parse --is-shallow-repository` = `true`, `git rev-list --count HEAD` = **50**, and
the oldest resolvable commit `d6cb996` is itself inside the window, so this clone resolves
the **whole** 24 h. `gates.py --mode full` exits **0**, ALL GREEN at `64f015b`:
`1682 passed, 62 skipped, 2 xfailed`, pytest 289.3 s, **COLD**, and the run downloaded
25.9 MB partway through (finding 3). `verify`, `snapshot-verify`, `env-check` PASS;
`baseline-verify` WARN is the documented CHARTER §3d state (two `data/raw/firms_data/`
manifests that a fresh clone cannot have). `--assert-head` exits 0;
`--assert-reported --base 018dd78` exits 0 with 20 substantive paths.

**No CHARTER §4b finding.** Through the GitHub MCP (`curl` against `api.github.com` is 403
here, WFG-119): `auto-gates` runs **200 to 219** on `auto/dev` are **18 `success`, 2
`cancelled` (200, 218), ZERO `failure`**. Run **219** at this head is `success`, and its
`gates` job is green on a clean clone at `fetch-depth: 0`. Every push in the window carried
a report; every dev and critic report in the window carries `Reviewed by:`; the research
report of 2026-09-06T1838Z still does not (WFG-147, already filed, not duplicated).

`web/finals.html` names `7308b06`, **24** commits behind `HEAD`, inside the staleness
gate's limit of 30. NH-043, not a defect in this window.

## R1 TICKS — 7 of 11, and it is worth stating before the findings

This is the first movement on readiness line R1 since the checklist was written, and all
three of its clauses were re-derived here rather than inherited.

- **「opens from `file://` with Wi-Fi off」.** `scripts/check_finals_acts.py` records
  **10 requests, none off `file://`, zero console errors**. The two `ERR_FILE_NOT_FOUND`
  lines are the uncommitted optional booth media, which `804e5b6` made the git index
  rather than an extension list decide.
- **「all four acts advance」, the clause nothing in this repository had ever tested.** I
  ran the driver: `1막 · 발견` 1 dot, `2막 · 시간과 도로망` 2, `3막 · 경로 비교` 3,
  `4막 · 판단` 4, four screenshots, exit 0, `Chromium 141.0.7390.37`. ⚠ **And I did not
  stop at this sandbox, because a driver that only ever runs where it was written proves
  the machine and not the screen.** GitHub `auto-gates` run **219** at this exact head ran
  the `finals-acts` job on a clean `ubuntu-latest` runner, 19:12:10Z to 19:12:26Z, and its
  log prints the same four labels, the same dot counts and the same
  「10 requests, none off file://, no console error」, then uploaded 5 files (2,238,234 B).
  **That closes the 1900Z dev report's own open finding 5**, which had recorded the CI
  browser as unmeasured and warned the job might be silently skipping. It is not.
- **「every on-screen number maps to a `docs/NUMBERS.json` key」.** Re-derived rather than
  read: `scripts/finals_screen_keys.py` returns **28** keys, all 28 are in
  `docs/finals_screen_numbers.md`, all 28 are keys of `docs/NUMBERS.json` (383 entries).

⚠ What the tick does **not** cover, said here so nobody reads it as more: the four view
**tabs** (WFG-169), the **G** key the booth uses between judges (WFG-170), and your own
laptop (NH-014 / R12).

## Findings, ranked

### 1. `fix-before-next-row` — WFG-171. The rule the loop wrote covers one direction of a claim, and the other direction is live on the printed page.

`docs/auto/finals/RELATED_WORK_PANEL.md:32` (printed, 뒷면 1, in bold) and
`docs/auto/JUDGE_QA.md:652` (the recited **Q16a · T0** draft, printed on the kit's Q&A
pages) both assert, flat and present tense, that the NIFoS 산불확산예측시스템's
발화점은 「운영자가 손으로 입력」한다.

**The whole provenance of that sentence is a chapter title in a library catalogue's table
of contents** (「발화지점 생성」). The guide itself is an unopened 18 MB PDF; NH-039 is open
because of that. Every other surface in this repository states the same claim **with** its
attribution, and I read all three rather than grepping:

- `paper/references.bib:258` names the eight catalogue chapters and adds
  「Only the catalogue page was opened」.
- `paper/manuscript.md:111-112` writes 「a 2026 user guide for an AI spread-prediction
  console driven by a human-entered origin point [@nifos2026guide]」.
- `docs/auto/knowledge/KOREAN_OPERATIONAL_SYSTEMS.md` §1 shows the derivation in the open.

**The two surfaces a judge actually meets are the two that dropped the attribution**, and
the panel does it **four lines above its own ⚠ note**, whose argument is that this panel
read only 「카탈로그 기록·언론 보도」 and that 「열어 본 적 없는 문서에 무엇이 **없다**고
말하는 것은 요약이 아니라 새로운 주장입니다」. The note's rule covers negatives. A positive
assertion of the same provenance is the same new claim and fails the same way: a judge who
has driven that console and knows it can take an origin from a KFS feed hears a flat
sentence that is simply wrong, and what breaks is, in Q16a's own words, 「신뢰 전부」.

⚠ **The 읍면동 and 30분 clauses beside it are NOT this finding and must not be softened.**
Those are reported by 경향신문 2026-03-30, an article this project opened, and their
register is correct.

**Why the critic did not fix it itself.** DIRECTION bars this routine from `JUDGE_QA.md`,
`DEMO_SCRIPT_5MIN.md`, `BOOTH_SETUP.md` and, on the same `SOURCES` footing,
`RELATED_WORK_PANEL.md`: a one-line edit without a same-lap kit rebuild turns
`tests/test_printables.py::test_the_newest_printable_is_not_stale_against_the_tree` red
(probed and reverted at `3f881f6`). The row therefore carries the rebuild and the bundle
re-point (WFG-152) as part of its definition of done.

It qualifies as the one `fix-before-next-row` item under §14b as amended by NH-038 B: one
clause in each of two files plus one `make printables`, on the Q&A bank and the printables,
which are named judge-facing surfaces.

### 2. WFG-172 (new, P1) — DIRECTION wrote the cold/warm rule and the very next lap broke it, in the artifact you read.

`docs/auto/reports/2026-09-07T1900Z-dev.md` and commit `64f015b`'s own message both record
「1688 passed / 56 skipped / 2 xfailed, unpiped」 with **no** cold/warm word and **no**
statement of whether the run downloaded anything. DIRECTION's *What not to do* says, in its
own ⚠ bullet: 「Do not report a pass/skip count without saying cold or warm, and without
saying whether the run downloaded anything (WFG-139).」

It is not a harmless omission. My **cold** run at the **same tree** gives
`1682 passed, 62 skipped`; theirs is a **warm** `1688 / 56`. A reader comparing 1682 to
1688 reads a six-test regression that did not happen, and the six are exactly WFG-139's
terrain tests. `report.py` can take both facts mechanically — it already parses the pytest
summary, and the byte size of `data/raw/` before and after answers 「did it download」 with
no network call — so no lap has to remember. §14b holds this behind R3, so it is P1 and
waits.

### 3. WFG-139 — tenth consecutive measurement, on this lap's own clock.

At container start `data/raw/` held `.gitkeep` and `README.md` only. Afterwards it holds
`dem/srtm/N36E129.hgt` (**25,934,402 B**) and `N36E129.hgt.gz` (8,473,868 B) with mtime
**2026-09-07T20:02Z**, inside a `pytest-full` stage that ran 19:59:37Z to 20:04:26Z.
Unrepaired, unchanged, and it is DIRECTION position 1 for the next lap. It blocks R3, and
R3 is one of the two lines still holding §14b's infra block shut.

### 4. Twelve open decisions, two of them due tomorrow, and nothing has arrived.

NH-032 and NH-034 (**by 2026-09-08**) are the pair that decides what the project's headline
number means against a fair opponent: two laps built the same row concurrently and got
margins of **9** and **27**, and the best-buffer sweep reaches **5**. Until you choose,
`JUDGE_QA.md` Q19 forbids the student from saying any of the three at the booth, and
WFG-104 and WFG-124 stay blocked. NH-035, NH-038, NH-043 and NH-044 are due **2026-09-09**.
I annotated NH-032 and NH-034 with a dated line saying they are overdue and that nothing
was guessed. This is the one thing in this window that no lap can clear.

## The root objection (`hate`)

**The loop wrote a rule for one direction of a claim, and the other direction has been on
the printed page the whole time.** Six instances of 「what somebody else's system does」
have been caught this week — WFG-144, WFG-162, WFG-163, WC-007, WC-008, WFG-166 — every one
of them by a reader rather than a gate, and every fix, every registered spelling and every
DIRECTION bullet written about **negatives**. The sixth surface, live at `64f015b` on the
panel a judge holds, is a **positive**, in bold, four lines above the note whose own
argument would forbid it if the note said 「있다고」 as well as 「없다고」. The asymmetry is
in the rule, not in the laps.

**The cheapest test:** `git grep -nE '(합니다|입니다)' docs/auto/finals/RELATED_WORK_PANEL.md
docs/auto/JUDGE_QA.md` restricted to sentences whose subject is 국립산림과학원, G-DAPS or
경기도, then check each against `docs/auto/knowledge/KOREAN_OPERATIONAL_SYSTEMS.md` §6 for
which opened source carries it. That is minutes, and it is what WFG-168's lint should
actually check for once it exists.

## `factchk` on the window's new prose

Every new world claim in the 24 h diff traced to an opened source or was already hedged.
`paper/references.bib:258` (NIFoS guide) and `:261` (경향신문 G-DAPS) both carry `verified`
notes that state exactly what was and was not opened; `paper/manuscript.md:105-119` makes
no accuracy comparison in either direction and says why. `docs/finals_acts_smoke.md` (new,
166 lines) is unusually honest about its own limits: §1.2 states in its own words that a
driver which presses a button and writes a PNG proves only that Chromium did not crash, and
§0 corrects the lap's own initial confusion between the four **views** and the four **막**.
I found no overclaim in it. The one flat world claim that failed is finding 1.

## `mandela` on the window's evals

`_assert_advanced` is the window's only new evaluator, and its independence is real: the
oracle is `web/finals.html`'s own act labels and dot counts, and the dev lap graded it by
mutation, first at 5/7 (its reviewer's count) and then at **7/7** after two tests were
added. ⚠ One leakage note the lap already made itself and I confirm rather than repeat as a
finding: the progress-dot check is **not** an independent witness, because
`web/finals.html:1403-1410` sets label, caption and dots from one `act` argument in one
synchronous block. Both the code comment and `docs/finals_acts_smoke.md` now say so.

## Judge drill

Ten hardest questions taken against the bank. I read three of them line by line rather than
skimming — **Q16a** (differentiation), **Q19** (the fair opponent) and **Q28**
(reproducibility) — because those are where a KFS judge, a statistician and a software
professor respectively would push hardest, and all three answer from files and refuse the
comparison they cannot support. The remaining seven (Q10c/Q10d, Q17, Q30, Q35, Q38, Q39)
I checked only that the card exists and names its evidence file; I did not re-derive their
contents this lap and do not claim to have. **The one question with no file at all is
responsibility**: `git grep -niE '책임|법적|면책' docs/auto/JUDGE_QA.md` returns **zero** at
this head, while `web/finals.html:1393` prints 「최종 판단은 언제나 사람이 내립니다」 and
`DEMO_SCRIPT_5MIN.md:217` gives it as the one-line answer for the 재난대응 실무자 lens. One
of the five judges is a public-sector disaster-response official. That is **WFG-167**,
already filed by critic #36 and now DIRECTION position 2; I did not duplicate it, and I did
not write the card myself because this routine may not edit `JUDGE_QA.md`.

## Scorecard deltas

**Track B 86, unchanged** (17 / 18 / 18 / 15 / 18). 데이터 수집·분석·해석 holds at 18 and
does not rise while WFG-139 is unrepaired for a tenth measurement; 제출 자료 holds at 18 on
two moves that cancel, WFG-166's proper closure against WFG-171's discovery on the same two
files.

**Track A 83 → 85.** **구현 및 유용성 19 → 20**, which critic #36 pre-registered in those
words, earned on WFG-009 and verified on two independent machines including GitHub's own
clean runner; NH-014 is the remaining condition and it is yours, tracked as R12, and a row
cannot be held below its evidence by an item no lap can close. **제출 자료 15 → 16**, the
kit reprinted without the sentence critic #36 docked it for; 16 and not 17 because
WFG-171's sentence is on the same printed page.

## Files this lap wrote

`docs/auto/CRITIC_LATEST.md`, `docs/auto/DIRECTION.md`, `docs/auto/KCF_READINESS.md`,
`docs/auto/BACKLOG.md` (WFG-171 at position 1, WFG-172 in the infra block),
`docs/auto/NEEDS_HUMAN.md` (dated overdue notes on NH-032 and NH-034),
`docs/auto/SCORECARD.md`, and this report. No code, no test, no data, no figure,
no `docs/NUMBERS.json`.

## In plain terms

- **What the judges would see today.** For the first time, a machine other than ours
  opened your finals screen, pressed the button four times, and proved all four acts of
  your demo really advance — GitHub's own computer did it, not just our sandbox. That was
  the last open item on readiness line R1, so the checklist moves for the first time in its
  life: **7 of 11**.
- **What changed since yesterday.** Your Q&A card and your printed panel stopped saying
  something untrue about a rival system, and the paper you would hand a judge was reprinted
  without it. But four lines above that repair, the same panel still says in bold that the
  other system's fire origin 「is entered by hand by the operator」 — and all we ever read
  was a chapter list in a library catalogue. It is the same mistake, pointing the other way,
  and it is the one thing I want fixed before anything else.
- **What you should do.** Two decisions are due tomorrow and they are the same question:
  **NH-032 and NH-034**. Your headline says the forecast rescues 91 routes, but that is
  against a plan that ignores the fire completely; against a fair plan that avoids where the
  fire is *now*, the honest answer is somewhere between 5 and 27, and two of our own laps
  disagreed about which. Until you pick one, the student is forbidden to say any number at
  the booth. Reply with `NH-032: A` or `NH-032: B` and `NH-034: <your choice>` and the next
  lap acts on it.



## Gates

**ALL GREEN** · mode `full` · head `64f015b` · 2026-09-07T20:04:28Z · current at `64f015b`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 19.2 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.1 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 1.6 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 289.3 s | 1682 passed, 62 skipped, 2 xfailed, 52 warnings in 287.66s (0:04:47) |

## Commits since the previous report (6d1d730df6c3b5b058ff49537413a11ddf981381..HEAD)

- 64f015b the dev report names the head it ships in, and the re-run that certified it
- 804e5b6 the reviewer blocked on a number I could not register, and on a tolerance grounded in a false premise

## Needs a human (17 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09)
- NH-036 [DECISION] One critic lap told the next one not to edit a file, and that is what kept a false sentence in front of a judge for a window (by 2026-09-10)
- NH-037 [DECISION] The paper's word proxy now stops it a thousand words before your 25-page rule (by 2026-09-10)
- NH-038 [DECISION] Your "product first" rule has spent the last three dev laps on documents, and the readiness line it was written to protect has not moved in five critic laps (by 2026-09-09)
- NH-039 [DECISION] The national wildfire-spread system's manual is an 18 MB PDF the sandbox could not fetch, and one of you can (by 2026-09-12)
- NH-040 [FYI] A critic lap pushed one commit past a red `--assert-reported`, and it is telling you rather than hiding it
- NH-041 [FYI] This lap sent you an email containing only the word PLACEHOLDER, and could not take it back
- NH-042 [DECISION] Two of your own rules collide whenever a withdrawn claim lives in a frozen artifact, and this week they collided three times (by 2026-09-10)
- NH-043 [DECISION] A gate your loop built this morning will go red about twice a day, and the charter tells the lap that meets it to stop working (by 2026-09-09)
- NH-044 [DECISION] The claim the paper just retracted is still live on the page the paper cites for it (by 2026-09-09)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
