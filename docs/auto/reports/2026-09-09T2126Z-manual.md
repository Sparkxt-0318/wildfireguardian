# WildfireGuardian autoloop · paper · 2026-09-09T2126Z

| | |
|---|---|
| when | 2026-09-09 21:26 UTC · 2026-09-10 06:26 KST |
| branch / head | `auto/dev` / **`4f3bd85`**, the pushed head. ⚠ Generated at `a442aa0`, the head this lap *incorporated*; `report.py` stamps this row before the lap's own commit exists. Both are named rather than one silently replaced |
| kind | manual |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 9, done: 33, dropped: 1, parked: 1, title-derivable: 1, todo: 108 |

## What happened this lap

# Paper lap 25 — the pointer four laps costed as unaffordable costs one word

Incorporated `4693540..a442aa0`: fourteen commits, fifteen files outside `paper/` and
`docs/auto/`, five new reports. **No manuscript sentence went false in this window** — checked
rather than assumed. One sentence gained a citation, at a measured **+1** word.

## Sections changed, and why

**§4.5 — the only manuscript edit, and it closes WFG-214's last open conjunct.**
`docs/auto/DIRECTION.md` records that P0 row as three of four surfaces closed, with "one link
in `paper/manuscript.md`" left, blocked because "the link costs a caveat, which CHARTER §3
rule 5 forbids". That premise is falsified by measurement, and the arithmetic behind it was
this routine's own: lap 24 costed the pointer as a *sentence* ("`docs/oracle_gap.md` states
what would measure it", 8 words against a margin of 1) and every surface since quoted that
costing. A bare parenthetical citation is a different and much cheaper form and nobody had
measured it. Built with the builder's own counter: `body_words` 8,999 → **9,000**, exactly
**+1**, and `check_paper.py` fails on `> LIMIT`, so 9,000 is inside the gate.
`grep -c oracle_gap paper/manuscript.md` goes **0 → 1**. Nothing was compressed and no caveat
or registered number was traded.

That is the shape the cited document itself records one level up. `docs/oracle_gap.md` §2b
opens "Critic #47 costed WFG-125's first branch as unaffordable, **and the arithmetic was
right for the instrument it assumed**" — and the row then turned out to be cheap because a
different instrument was available. This lap is the same failure inside this routine's own
ledger: the costing was right for the *form* it assumed, and wrong for the cheaper form nobody
priced. A costing is a claim about one construction, and it goes stale the way a count does.

Placement was the whole of the care. The link attaches to the **mechanism** clause — "…a
leave-one-fire-out simulation rather than the fire, so it cannot be wrong there
(`docs/oracle_gap.md`)" — which is what that document's §2 establishes. An identical +1 word
one clause later, after "by an amount no run here measures", would have read as though
`oracle_gap.md` measured that amount. It does not; that run is WFG-213, `blocked(NH-052)`.

**The bound is untouched, word for word, and that was a decision rather than an omission.**
The incorporated diff narrowed four of the five surfaces NH-053's own table lists — its three
`README.md` rows in `3ec5737`, and its `docs/auto/JUDGE_QA.md` Q36 row two commits earlier in
`359fd15` — so the manuscript is
now the only one still asserting "this project's own model is worth less" flatly. That
asymmetry is real and is reported, not fixed: NH-053 is a DECISION with four options, one of
which is "change nothing before the finals", and a paper lap that quietly brought the
manuscript into line would be choosing option A for the author on a judged headline number.
`docs/auto/DIRECTION.md` forbids it in one line. The entry comes due **2026-09-12**.

NH-053's three-part standing instruction now stands at **two of three**, reported as two of
three: (a) the mechanism, landed lap 24; (b) the link, landed this lap; (c) the bound left
"with a pointer to this entry" — **permanently declined with a stated reason**, a published
manuscript having no way to point at an internal NEEDS_HUMAN entry. (c) is not a budget
casualty and must stop being counted as one.

**`paper/AUTHORSHIP.md` — the window's other work, at zero manuscript words.** WFG-027 shipped
`data/processed/timeline_roles/timeline_roles.json` and twenty `timeline_*` registry keys
re-derived from `git log` alone. That is not a result about wildfires and no manuscript
sentence wants it, but it is exactly what the file the manuscript's availability section
already points a reviewer at is for. CHARTER §9's "every agent commit carries the
`Co-Authored-By` trailer" is now **counted** there rather than described: at the artifact's
own stamp (`89da7d3`) it records **662** commits on `auto/dev`, **513** carrying the trailer
and **149** not, with the registry's four mandatory caveats travelling with them.
`check_number_collisions.py` exits 0 over the new prose.

## The finding: a withdrawn claim alive in two files the scanner structurally cannot read

`WC-012` was registered in `3ec5737`, the same commit that withdrew it — verified from the
commits rather than from the ledger's summary, which is the error lap 17 shipped — so §3.5's
"September 2026 retractions skipped it **three** times" stays three. What it retracts is "the
phase boundaries were not chosen by anyone; the record's empty calendar gaps chose them", and
its own `withdrawn_by` field names `scripts/build_timeline_roles.py:40-84`, the hard-coded
boundary literals, as the disproof.

**The comment block at `:42-46` — inside that range rather than above it, `PHASES` opening at
`:47` and the first boundary literal at `:51` — still says "Phase boundaries are the calendar
gaps in the record, not a narrative imposed on it".** And the identical assertion
sits in the shared caveat of **all twenty** `timeline_*` entries in `docs/NUMBERS.json`, which
CHARTER §3 rule 3 makes travel with every one of those numbers onto every surface quoting
them. Re-derived, not quoted: both `WC-012` patterns return **False** against both files;
`scripts/check_withdrawn_claims.py` reads `.md` and `.html` **only**; both registered
spellings are **Korean** and both survivals are **English**. Two of §3.5's own stated limits
firing together, inside the window that created the instance.

**No manuscript sentence is owed and none was written**: §3.5 already states both limits in
its own voice, and lap 19 retired the §3.5 illustration as a want rather than deferring it.
Both files are outside CHARTER §12, so this is a **dev-lap fix**. `paper/AUTHORSHIP.md`
quotes caveat (4) in its corrected form and says why.

## Figures

None added, none redrawn. `make_figures.py` drew F1–F9 and `git status --short paper/figures/`
is empty — all nine byte-identical — so no look-at-it pass is owed. No figure was added
because the manuscript states no new result.

## Gaps

None opened, none closed. Seven markers, unchanged. G4's supersession and G8's NH-053
annotation were both written last lap and stand.

## Length, measured on one document by one run

`body_words` **9,000** against the proxy's 9,000 — **margin zero**. What this lap spent is
exactly the ability to make a +1-word correction; a zero-net or negative-net correction still
passes. Against a margin of 1 that lap 24 already called "not a margin" that is a narrow loss,
and it bought the last open piece of a P0 row no other routine can land. It is still a loss
and the direction is still one way. **Laps 13 through 21, 24 and now 25 have all had their
writing shaped by the proxy rather than by the evidence. NH-037 is the answer and it is still
open.**

The anchor was **re-derived, not inherited**: `body_words` moved, so lap 24's
`built_pages_inputs` turned the gate red exactly as designed. After the one `apt` line
`paper/README.md` has printed since lap 9, `check_paper.py` took its measuring branch —
`pages 23, calibri_face Carlito, metrics_ok true`, page objects and page-tree `/Count`
agreeing — and printed the new anchor `50b905d584c8030c`. **Two pages against the author's 25,
zero words against the proxy's 9,000.** The `.docx` is committed because the manuscript
actually moved, which is the case lap 23's byte-non-determinism finding says a rebuild is
legitimate in.

## What was checked and found not to have moved

- `docs/NUMBERS.json` **428 → 448**, compared key by key: all twenty new keys are `timeline_*`,
  zero existing values changed, zero existing caveats changed, none removed.
- `scripts/check_withdrawn_claims.py`: PASSED === **12** claims over **938** gated files.
  §3.5 quotes neither integer.
- `references.bib` unchanged at **29** entries; no citation was added, so none was opened.
- `gates.py --mode full` exits **0** (1862 passed, 64 skipped, 3 xfailed). `baseline-verify`
  WARNs on the two git-ignored `data/raw/**` contracts, which is NH-029 and CHARTER §3d
  working as designed.
- ⚠ Clone shallow: `--is-shallow-repository` **true** at a depth of **50**, both measured this
  lap. **No ancestry or reachability claim is made anywhere in this lap's output.** The 662 is
  the artifact's registered value at its own stamp, carried with caveat (1), not a count this
  clone took.

## ⚠⚠ The independent reviewer BLOCKED this push, and it was right on both counts

Reviewed by: subagent (**block**): *"A ledger whose only product is provenance cannot be wrong
about provenance in the sentence where it claims to have checked it."* Both findings were
re-verified in the tree before the repair, and both are the failure this lap's own subject is.

1. **`GAPS.md` and `paper/README.md` both said the four narrowed surfaces were "all in
   `3ec5737`". `3ec5737` touches no `JUDGE_QA.md` at all.** Q36 was narrowed two commits
   earlier, in `359fd15`. Re-run here: `git log 4693540..a442aa0 -- docs/auto/JUDGE_QA.md`
   returns `359fd15` and nothing else, and `git show --stat 3ec5737` lists `README.md` and no
   Q&A bank. **The commit id had been inherited from `3ec5737`'s own commit message** — which
   names the README lines and never mentions Q36 — i.e. taken from a summary instead of from
   `git log -- <path>`, one paragraph away from this lap boasting that it verified `WC-012`'s
   registration commit from the commits rather than the ledger summary. That is the lap-17
   error, committed by the lap citing it.
2. **"the comment at `:42`, three lines above those literals" is wrong geometry, in the
   sentence a dev lap will use to locate the fix.** The comment block is `:42-46`, `PHASES = [`
   opens at `:47` and the first boundary literal at `:51`; and the comment sits *inside*
   `WC-012`'s own cited `:40-84` rather than above it. Re-measured with `sed -n '38,52p' | nl`.

Both are corrected in all three files and in `STATE.json`. Everything else the lap claimed the
reviewer re-derived independently and confirmed, including the +1 word, the `> LIMIT`
boundary, the untouched bound, the placement argument, 662/513/149 against both the registry
and the artifact (with the phase sums closing: 55+42+99+49+417 = 662, 0+4+93+0+416 = 513), the
`.md`/`.html` scanner scope, the 428 → 448 comparison over whole entry objects, and the shallow
-clone discipline.

⚠ **One judgment the reviewer added and it is worth carrying to the author:** the §4.5 sentence
now *contains* a citation twenty words before the clause NH-053 disputes, and to a reviewer
reading at speed a cited sentence reads as a sourced sentence. The placement is right and the
citation is accurate; the sentence is still the one an adversarial reviewer will circle, and
answering NH-053 is what removes it.

## In plain terms

The paper now points at the document that explains why the 42 is measured the way it is, and
it cost one word — the last piece of a finals to-do that nobody but this routine could do.
What is still missing is your answer on whether 42 is an "upper bound": every other surface
stopped saying it today, the paper still says it, and I deliberately did not change that
because it is your call, not mine (NH-053, due 12 Sep).
Two things for you: answer NH-053, and answer NH-037 — the paper is now at exactly 9,000
words of 9,000, so the next lap that has to fix a sentence cannot make it any longer.



## Gates

**ALL GREEN** · mode `full` · head **`4f3bd85`** — the pushed commit — re-run after the rebase onto `4b580df`: 1862 passed, 64 skipped, 3 xfailed, 475.2 s; `--assert-head` and `--assert-reported` both exit 0 there. ⚠ **The table below is the run `report.py` stamped, at `a442aa0`** — a real full pass, on a tree without this lap's manuscript edit, `AUTHORSHIP.md`, ledgers or report. It is kept rather than overwritten, because a report that quietly swaps one verdict for another teaches the next lap nothing. The two runs agree step for step. **Fifth consecutive lap to pay this by hand**; it is loop hygiene, so it stays a P1 row under CHARTER §14b and displaces no P0.

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 26.1 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.2 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 1.7 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 474.9 s | 1862 passed, 64 skipped, 3 xfailed, 52 warnings in 472.82s (0:07:52) |

## Commits since the previous report (ba06467e4898b94e675901bb97edd1ef8f316bdc..HEAD)

- a442aa0 the critic report names the head it ships in, and keeps the run it describes
- 08edf76 critic #53: the day fixed four surfaces that say what 42 means, and the fifth is the one five judges stand in front of

## Needs a human (25 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09)
- NH-036 [DECISION] One critic lap told the next one not to edit a file, and that is what kept a false sentence in front of a judge for a window (by 2026-09-10)
- NH-037 [DECISION] The paper's word proxy now stops it a thousand words before your 25-page rule (by 2026-09-10)
- NH-038 [DECISION] Your "product first" rule has spent the last three dev laps on documents, and the readiness line it was written to protect has not moved in five critic laps (by 2026-09-09)
- NH-039 [DECISION] The national wildfire-spread system's manual is an 18 MB PDF the sandbox could not fetch, and one of you can (by 2026-09-12)
- NH-040 [FYI] A critic lap pushed one commit past a red `--assert-reported`, and it is telling you rather than hiding it
- NH-041 [FYI] This lap sent you an email containing only the word PLACEHOLDER, and could not take it back
- NH-042 [DECISION] Two of your own rules collide whenever a withdrawn claim lives in a frozen artifact, and this week they collided three times (by 2026-09-10)
- NH-043 [DECISION] A gate your loop built this morning will go red about twice a day, and the charter tells the lap that meets it to stop working (by 2026-09-09)
- NH-045 [BLOCKER] The staleness gate has closed `auto/dev` to every routine, and the one routine that met it is the one forbidden to clear it (by 2026-09-08)
- NH-044 [DECISION] The claim the paper just retracted is still live on the page the paper cites for it (by 2026-09-09)
- NH-046 [DECISION] Your product's definition-of-done names a command nothing in this project has ever run (by 2026-09-10)
- NH-048 [DECISION] One of the research routine's three literature channels has been dead for two runs, and a working replacement is already proven (by 2026-09-10)
- NH-049 [DECISION] Your critic routine is told to add judge Q&A cards and your own printing gate makes that impossible for it (by 2026-09-11)
- NH-050 [DECISION] You answered two of these questions two days ago and the loop never heard you, because you answered them on the routine page (by 2026-09-10)
- NH-051 [DECISION] The rule your loop has been obeying for three days is not the option it names, and the difference is why one row has been pushed down five times (by 2026-09-11)
- NH-052 [DECISION] The experiment that measures what your own forecast is worth is now one run away, and it would change what 42 means (by 2026-09-12)
- NH-053 [DECISION] The word your front door uses for 42 stopped being right today, and the loop cannot pick its replacement (by 2026-09-12)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
