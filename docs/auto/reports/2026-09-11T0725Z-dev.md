# WildfireGuardian autoloop · dev · 2026-09-11T0725Z

| | |
|---|---|
| when | 2026-09-11 07:25 UTC · 2026-09-11 16:25 KST |
| branch / head | `auto/dev` / `c2bb9e6` |
| kind | dev |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 10, done: 34, dropped: 1, parked: 1, title-derivable: 1, todo: 119 |

## What happened this lap

Lap 2026-09-11T0620Z (dev). **WFG-249, WFG-250 and WFG-251 all done**, in one lap and one
booth-kit rebuild, exactly as `docs/auto/DIRECTION.md` item 1 named them. Claim commit
`c2bb9e6`. Baseline at `0a66c90`: `gates.py --mode quick` ALL GREEN. GitHub `auto-gates`
run **362** is `success` at that exact head, so CHARTER §4b set no finding #1 for the
fourteenth consecutive lap. `CRITIC_LATEST.md` declared **zero** `fix-before-next-row`
items, measured, so the top row was taken directly.

## Your decisions, applied

**Nothing new, and this is the sixth day.** The Gmail search
`from:siyeong0318@gmail.com subject:"WildfireGuardian autoloop" newer_than:14d` returns
fifty threads in the newest page (201 estimated across all pages) and **every one of the
fifty holds exactly one message, every one of them sent by this loop**; a reply would bump
its thread to the top of that ordering and none of the top fifty is a reply.
`docs/auto/decisions_seen.json` still reads `"seen": []` and the newest applied decision is
still **NH-031, of 2026-09-06**. `scripts/auto/decisions.py` was not invoked, because there
is nothing to parse. **PR #31, the second channel CHARTER §6 names, returned an empty
comment list** at the GitHub MCP this lap (it has been a merged pull request since
2026-09-05; that is **WFG-211**, already filed). Nothing was guessed at and nothing was
applied.

**24 decisions are open for you (23 DECISION + 1 BLOCKER), and 2 of them have no date at
all.** ⚠ **Stated precisely, because the last two laps' reports disagreed about it:
NH-046 (「by 2026-09-10」) is PAST DUE; NH-049 and NH-051 (both 「by 2026-09-11」) come due
TODAY; NH-052 and NH-053 come due tomorrow.** The sprint ends **2026-09-15**, four days
out.

## What this lap did

All three rows sat at positions 1, 2 and 3 of the table, all P0 KCF, all on the
identically worded 제출 자료 criterion, and all three edit a **hashed source of the booth
kit**. Paying `make printables` once instead of three times is the whole reason
`DIRECTION.md` named them as one lap.

### WFG-250 — the closing named the arm's population where the claim's denominator is the failing set

`data/processed/vulnerability/refuge_placement.json :: optimum_h240.baseline` holds
`n_households` **124** and `n_failing` **24** four lines apart. The result counts
households recovered out of those that **fail** the 240-minute horizon. The spoken 마무리,
as WFG-247 left it one lap earlier, said 「여기서 세는 가구는 … **OSM 건물 124동**입니다」
and nothing else, so a judge doing the arithmetic in their head hears **20 of 124** — a
sixth of the village — where the result is **20 of 24**, five sixths of the problem. The
factor is 124 / 24 = **5.17**.

⚠ **The finals screen was already right and the spoken script was not.**
`scripts/finals.template.html` renders 「240분 지평에서 도달에 실패하는 24가구 가운데 …」
and `docs/finals_screen_v2.md` §2.4 says the same; both say it at this head (*since when*
is a history question a shallow clone cannot answer, CHARTER §4, and is not claimed). So
the repair is the screen's own words, in the script:

> 240분에 도달 실패인 **24가구** 중 한 곳이면 **20가구**, 두 곳이면 **24가구** 전부가
> 도달 가능해져 남는 실패가 **0가구**이고, 세 번째는 **0가구** 를 더합니다.
> 여기서 세는 **가구**는 판정 단위인 **지점**이 아니라 **OSM 건물 124동**이고,
> 분모는 그 124동이 아니라 도달 실패인 **24가구**입니다.

WFG-247's population sentence is **kept verbatim** up to 124동, as the row's constraints
require — those counts really are buildings.

**The denominator is now a registry key, not prose.** `l0i_failing_denominator_h240`
(24, `optimum_h240.baseline.n_failing`) is added by the additive registrar
`scripts/register_refuge_population.py`, which this lap extended to carry a **per-key
caveat** rather than one shared band — because the denominator's warning is a different
warning from the population's. ⚠ The registrar's `notes` field is deliberately left
byte-identical across all three keys: `build_entries` rewrites an existing entry wholesale,
so any field that varied with the row that added the key would change the stored hash of an
already-committed key, which CHARTER §3d's freeze check reads as a MODIFIED registry entry.
The row that added each key lives in its `derivation` instead. The registry diff is **36
insertions and 0 deletions**.

**The §3 화면/구두 table** now labels both result rows the same way
(「분모는 도달 실패 24가구, 세는 단위는 OSM 건물」) and carries one row for the denominator
and one for the population, saying which is which. **금지 item 6** listed two things that
can be wrong and now lists three: the verb, the **분모**, and the unit being counted.

**The pace was re-measured, because it had to be.** 마무리 **360 → 383** syllables (+23).
Allocation **35 / 41 / 47 / 58 / 55 / 64**, total **1,799** at **6.00** syl/s, spread
1.02x; artifact `data/processed/demo_script_pace/pace_20260911T0620Z.json`, two registry
keys under tag `20260911t0620z`, `TAG` moved in `tests/test_demo_script_pace.py`, six
segment headers and their cumulative brackets rewritten. The spoken line 「마지막 61초는」
became 「마지막 64초는」 and the count was re-run **after** that edit, per
`docs/demo_script_pace.md`'s fixed-point procedure: `count_syllables(["마지막 64초는"])`
answers **8**, as at 61 and at 56, because 육십사 is three Hangul blocks like 육십일. The
allocation came back unchanged, so the fixed point did not bite.

### The root objection I recorded before building, and what it cost

`hate`, written into the claim commit `c2bb9e6`: **all three repairs are additions to
blocks that are already the densest on their page, and two of them land on documents whose
length is a hard budget.** Q20a is a **T0** card the student answers from memory, and it
was wrong *because* it was written short and absolute — so a longer, more qualified Q20a
the student cannot hold in their head is that same defect moved from the text to the
student. The test it named: require the 마무리 net to be **≤ 0**, a rewrite and not an
append, and require each card's scope to **replace** the absolute clause rather than follow
it; if a card must grow, say by how much and where the reader's attention was taken from.

**The test was run and the 마무리 half FAILED it: the net is +23, not ≤ 0.** What the
rewrite did buy back is stated rather than hidden: 「대피 지점 한 곳을 추가하면」 became
「한 곳이면」 and 「두 곳이면 240분에서 도달 실패가 0가구 남습니다」 folded into the
preceding sentence. The denominator clause itself is irreducible — a judge has to hear
which denominator — so it was paid through the documented re-measure, and the exchange is
written into the script's own §1 with who paid (도입 · 1막 · 4막, one second each; 2막 and
3막 unmoved) and the explicit note that **whether 23 syllables are worth it is the judges'
question, not this repository's**. **Q20a grew by one paragraph and the objection's
prediction stands unanswered**: the row's done-when required the other population to be
named in the same block, and there is no way to do that inside the existing sentence.
A rehearsal is the only thing that settles whether a T0 card of this length is sayable from
memory, and that is **R12 / NH-014**, still yours.

### WFG-249 — the T0 privacy card defined 「가구」 for the whole repository

Q20a said, unqualified, 「이 저장소에서 「가구」는 사람도 주소도 **건물도 아니라** OSM 보행
도로망의 노드 하나입니다」, and the 마무리 the same window wrote tells the same five judges
the 가구 it counts **are** OSM buildings. Both are printed in the same kit, and the student
answers Q20a from memory (§6 드릴 순서, 회차 1). ⚠ 금지 item 6 cited Q20a as the authority
for the definition it broke.

The definition is now scoped to 「이 저장소의 **구조 · 경로 계층**에서」 — the layer whose
`home_node` output is what the rest of that answer describes — and the card names the other
population **in the same block**, with the sentence that answers the follow-up: 한 낱말이 두
뜻으로 쓰인 것이 아니라 **두 계층이 서로 다른 것을 셉니다**. The privacy answer is
untouched; nothing was deleted (CHARTER §3.5); `WC-013`'s `say_instead` was read first, and
this is consistent with it — WC-013 withdrew the household register for the **verdict**,
which is exactly the layer the scope now names.

### WFG-251 — a negative over a record whose only file this project never opened

Q16d's spoken draft said 「기록에는 **성능 수치가 하나도 없습니다**」 while the same card's
없는 것 item 1, forty lines below, says 「초록과 기록만 읽었고 본문 PDF 는 열지 못했습니다」
and prescribes 「**공개된 기록과 초록에는** …」. Three paragraphs after the unscoped sentence
the card tells the student to open that DOI in front of the judge. The sentence now carries
item 1's own wording plus one clause saying the body PDF was not opened. Item 1 is
untouched and **no other fact on the card is touched** — `WC-007` and `WC-009` are what
this error class already cost this project once, in the other direction.

### The three gates this lap leaves behind, each graded on its own mutation

| gate | mutation | result |
|---|---|---|
| `test_the_privacy_card_scopes_its_household_definition_to_one_layer` | strip 「구조 · 경로 계층에서」 from Q20a | **RED**, naming Q20a; 32 others pass |
| `test_the_related_preprint_card_scopes_its_negative_to_what_was_read` | restore 「기록에는 성능 수치가 하나도 없습니다」 | **RED**, naming Q16d; 32 others pass |
| `test_the_spoken_closing_names_the_denominator_the_artifact_holds` | the whole pre-WFG-250 block, verbatim | **RED** |
| the same gate | 124동 first, failing set only in a later sentence | **RED** — but only after the fix below |
| `test_no_surface_offers_the_arm_population_as_the_claims_denominator` | the same | **RED** |

⚠ **The fourth row is the one worth reading, because the first grading of this file let its
own mutation through.** Putting 124 in front of the counts and the failing set in a
sentence afterwards satisfied every property the test had: the block named the denominator,
and the number matched the artifact. A denominator that arrives after its numerator is a
correction, not a statement, and the 마무리 is spoken once. The test now asserts that the
denominator's match **starts before** the first count's, and `_WRONG_DENOMINATOR` was
widened to tolerate the 「한 곳이면」 that sat between the two halves of the shipped
mislabel. The gate is graded against the **committed artifact** rather than a literal 24,
so a refit that moved the failing set turns it red — the alternative is the same remembered
number the defect was made of.

### What did not work, and what this lap did not do

- **The registry key was NAMED badly the first time, and the collision gate caught it.**
  `l0i_failing_before_any_refuge` made `check_number_collisions.py` fire on two unrelated
  lines about GitHub branch protection, because its anchor words — `any`, `before`, `refuge`
  — are three generic English words and MIN_ANCHORS is three. ⚠ **The fix was the key name,
  not a pragma**: annotating two unrelated lines with `collision-ok` about a key they have
  nothing to do with is exactly the noise that gate's own docstring warns against. The key
  is **`l0i_failing_denominator_h240`**, and the lesson is that a registry key name is an
  interface to a tree-wide grep and not only a label.
- **The booth kit was rebuilt TWICE and the bundle re-pointed THREE times**, and only the
  last of each ships. The first `make finals-bundle` reported 「rebuilt byte-identically, 19
  files」 while still naming the **0412Z** kit, because `build_finals_bundle.py` resolves the
  newest **tracked** printable and the new PDF was untracked; staging it first fixed that.
  The **0620Z** kit was then made stale by three prose corrections this lap made to
  `DEMO_SCRIPT_5MIN.md` after building it, and `tests/test_printables.py::
  test_the_newest_printable_is_not_stale_against_the_tree` said so by name. The kit that
  ships is **`20260911T0706Z`**, 59 pages; the 0620Z PDF stays in the tree because CHARTER
  §3.2 forbids overwriting it. **Two rebuilds, and the second one is the price of correcting
  prose after the kit was built rather than before** — the same mistake the 0317Z lap made
  and wrote up, made again here.
- **Three red gate runs before the green one**, each a real thing this lap had not done:
  `check-artifact-manifest` on the new pace artifact (`build_artifact_manifest.py`, now 111
  artifacts), `tests/test_finals_payload_rederives.py` on the un-rebuilt screen, and the
  collision above.
- **`SURFACES` was re-pointed, not quietly edited.** 금지 item 6's marker moved from
  「두 군데가 틀릴 수 있으므로」 to 「세 군데가」 because the list grew an item; the test's own
  message asks for exactly that, and the comment beside it says so.
- **Three sentences of this lap's own prose were corrected before the gates ran**, all the
  same class: 「처음부터」 about the screen (a history claim a shallow clone cannot make,
  CHARTER §4) became 「이 머리에서」; 「seven hours apart」 in two places became 「three
  hours」, which is what 0326Z to 0620Z is.
- **No new NEEDS_HUMAN entry.** All three rows were agent-doable and a twenty-fifth open
  question while twenty-four go unanswered would be noise. **NH-049 gained a measurement
  instead**: this lap's two kit rebuilds took **13.7 s** and **12.4 s** of wall-clock, which
  confirms critic #64's 17 s independently, twice, on a 59-page kit.

## ⚠ The reviewer PASSED this lap and was right about two things anyway

`Reviewed by: subagent (pass): "The lap's self-reported justifications are not checked
against the tree it just wrote — and where I caught that, the diff itself is the
refutation."`

Both findings are fixed rather than argued with.

1. **The load-bearing one, and it is about this lap's honesty rather than its result.**
   `docs/NUMBERS.json :: l0i_failing_denominator_h240.notes` read 「registered 2026-09-11
   by **WFG-247**」 for a key **WFG-250** added, and the registrar carried a comment
   defending that as forced: 「any field that varied with the row that added the key would
   change the stored hash of a key already committed」. **The same hunk disproves it.**
   `caveat` and `forbidden_phrasings` were converted from shared constants to per-key
   values in this very change and the two pre-existing entries' bytes did not move,
   because their VALUES did not change. What CHARTER §3d's freeze forbids is *changing* a
   committed entry, not *varying* a field across keys. So the misattribution was a choice
   dressed as a constraint, and §3.2 would then have frozen a knowingly-wrong provenance
   line into the one record this project asks a judge to trust. `notes` is now per key
   (`WFG-247`, `WFG-247`, `WFG-250`), the two existing entries are still byte-unchanged —
   the registry diff against `c2bb9e6` is **108 insertions and 0 deletions** — and the
   comment now says what actually constrains it.
2. **The 「In plain terms」 paragraph said the screen 「had it right all along」**, an
   unbounded history claim that the same document's body declines three paragraphs
   earlier and that a 51-commit shallow clone cannot make (CHARTER §4). It now says the
   screen **says it correctly today**. ⚠ The repository prose got this right and the
   author-facing paraphrase did not, which is the second time this week the plain-language
   half of a report has been the loosest part of it.

**What the reviewer verified independently and reports as TRUE**, having re-derived rather
than read: every pace figure and delta against both artifacts (1,799 / 383 / 35-41-47-58-55-64
/ 6.00 / 1.02x, and `count_syllables(["마지막 64초는"])` = 8); `n_households` 124 and
`n_failing` 24 read straight from `refuge_placement.json`; the registry diff additive with no
VALUE edited; **all four mutation gradings reproduced in its own process**, red on the
mutation and green on restore with sha256 confirmed after each restore; zero deletions in the
whole diff; the clone still shallow and not deepened; GitHub `auto-gates` run **362** green at
`0a66c90` **and run 363 green at the claim commit `c2bb9e6`**, checked at the API; and every
「What not to do」 bullet of `DIRECTION.md` respected.

**Its `mandela` pass fires one pattern and it is worth keeping in the record: the two
JUDGE_QA gates are verifier-equals-designer.** They assert the exact spellings this lap
wrote (「구조 · 경로 계층에서 「가구」는」 and two occurrences of 「공개된 기록과 초록에는」),
so they can only ever be satisfied by that wording and never test whether the scope is the
*right* one; both docstrings say so. The contrast the reviewer draws is the useful part:
**the denominator gate is the only one of the three where outside ground truth enters**,
because it re-reads `n_failing` from the committed artifact and compares. The named
independence fix, if these cards drift again, is to bind the privacy card to the layer's own
`home_node` output rather than to a phrase. Not done here; recorded.

## Next row

⚠⚠ **WFG-129 (P0, science), and `DIRECTION.md` item 2 plus critic #64's pre-registration
both say so in as many words**: 「if a dev lap ran in the next window and WFG-129 is still
`todo`, that is finding #1」. This lap ran, took the three rows DIRECTION named as item 1,
and **did not get to WFG-129** — the kit bundle cost three documents, a pace re-measure, a
registry key, three gates, two kit rebuilds and three bundle re-points. It is fully
specified in `paper/GAPS.md` G7, runs on committed inputs only, needs no refit, no
re-acquisition and **no decision from the author**, and it is the cheapest test of the exact
number the booth leads with. It is the next row and nothing in this lap's result changes
that.

## In plain terms

- **What changed for the project.** The last thing five judges hear said your refuge result
  was 20 out of 124 buildings when it is 20 out of the **24** that cannot get out in time —
  so the sentence made your own work sound about five times worse than it is. Your screen
  says it correctly today; now the script does too, and the correct denominator is a
  registered number a test re-derives. Your privacy card also said a 「가구」 here is *not* a
  building while that closing line says the ones it counts *are* — both are now one card
  that explains the two layers, so you have an answer instead of a contradiction.
- **Why it matters to the judges.** 「자료의 논리적 구성」 is worth 20 points on both scoring
  tables, and all three fixes were on the two documents printed in the folder a judge
  carries away. The honest part worth saying out loud at the booth: the error was making
  your project look *worse*, and every gate this repository has is built to catch numbers
  that flatter the work — which is why it survived a window.
- **What you should do.** Nothing for this. But **24 decisions are waiting and 2 have no
  date at all**; NH-046 is past due and NH-049 and NH-051 come due today. NH-049 now has two independent
  measurements saying a booth-kit rebuild takes under 20 seconds, so the rule that stops
  your critic fixing a printed card costs far more than it saves. And the one thing the loop
  most needs from you is **nothing**: experiment WFG-129 is next, it needs no reply, and it
  answers the hardest question a judge can ask about your headline number. The sprint ends
  **2026-09-15**.



## Gates

**ALL GREEN** · mode `full` · head `c2bb9e6` · 2026-09-11T07:17:39Z · current at `c2bb9e6`

⚠ **`c2bb9e6` is this lap's CLAIM commit, not the commit this report ships in, and no sha
typed here could be.** `report.py` reads `HEAD` at generation, necessarily before the commit
carrying the report exists; the run recorded above is the full-gate run on the working tree
at that head, and it is kept as the record of **where the run happened**. **What certifies
the pushed commit is the MECHANISM, not an id:** after committing, `gates.py --mode full`
was re-run and read in the foreground, and `gates.py --assert-head` and
`gates.py --assert-reported` were both run immediately before the push and both exited 0.
`--assert-head` runs no gate; it refuses to exit 0 unless `.auto/gates.json` records a
`full` pass at exactly the pushed `HEAD` **on a clean tree**, so its exit code is the
certification and the sha is in that file. ⚠ Two changes landed after the run above and are
covered by the re-run rather than by this table: the registrar's per-key `notes` fix (the
reviewer's root objection) and this report itself. **One subtraction that belongs beside
every green run from this sandbox:** the clone is shallow at **51** commits and was
deliberately NOT deepened (CHARTER §4 forbids writing an ancestry claim from a shallow
clone; it does not require deepening), so `tests/test_timeline_roles.py:234` **SKIPS**.
GitHub `auto-gates` at `fetch-depth: 0` is what certifies that check, and run **363** is
`success` at the claim commit. `baseline-verify` WARNs as designed in a sandbox without the
laptop's manifests (NH-029, CHARTER §3d), and this lap adds five tracked artifacts, which
§3d calls information rather than failure.

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 22.6 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.2 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 0.4 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 713.3 s | 2097 passed, 65 skipped, 3 xfailed, 52 warnings in 711.89s (0:11:51) |

## Commits since the previous report (5482ba81e6c9dca3270681dd6aa98608e911a340..HEAD)

- c2bb9e6 claim WFG-249 + WFG-250 + WFG-251 (20260911T0620Z)
- 0a66c90 critic #64: the privacy card says a 가구 here is not a building and the closing sentence the same lap wrote says the 가구 it counts are buildings

## Needs a human (29 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09, one day past; raised to HIGH by critic #55 on a measured third instance)
- NH-036 [DECISION] One critic lap told the next one not to edit a file, and that is what kept a false sentence in front of a judge for a window (by 2026-09-10)
- NH-037 [DECISION] The paper's word proxy now stops it a thousand words before your 25-page rule (by 2026-09-10)
- NH-038 [DECISION] Your "product first" rule has spent the last three dev laps on documents, and the readiness line it was written to protect has not moved in five critic laps (by 2026-09-09)
- NH-039 [DECISION] The national wildfire-spread system's manual is an 18 MB PDF the sandbox could not fetch, and one of you can (by 2026-09-12)
- NH-040 [FYI] A critic lap pushed one commit past a red `--assert-reported`, and it is telling you rather than hiding it
- NH-041 [FYI] This lap sent you an email containing only the word PLACEHOLDER, and could not take it back
- NH-042 [DECISION] Two of your own rules collide whenever a withdrawn claim lives in a frozen artifact, and this week they collided three times (by 2026-09-10)
- NH-043 [DECISION] A gate your loop built this morning will go red about twice a day, and the charter tells the lap that meets it to stop working (by 2026-09-09)
- NH-045 [BLOCKER] The staleness gate has closed `auto/dev` to every routine, and the one routine that met it is the one forbidden to clear it (by 2026-09-08)
- NH-044 [DECISION] The claim the paper just retracted is still live on the page the paper cites for it (by 2026-09-09)
- NH-046 [DECISION] Your product's definition-of-done names a command nothing in this project has ever run (by 2026-09-10)
- NH-048 [DECISION] One of the research routine's three literature channels has been dead for two runs, and a working replacement is already proven (by 2026-09-10)
- NH-049 [DECISION] Your critic routine is told to add judge Q&A cards and your own printing gate makes that impossible for it (by 2026-09-11)
- NH-050 [DECISION] You answered two of these questions two days ago and the loop never heard you, because you answered them on the routine page (by 2026-09-10)
- NH-051 [DECISION] The rule your loop has been obeying for three days is not the option it names, and the difference is why one row has been pushed down five times (by 2026-09-11)
- NH-052 [DECISION] The experiment that measures what your own forecast is worth is now one run away, and it would change what 42 means (by 2026-09-12)
- NH-053 [DECISION] The word your front door uses for 42 stopped being right today, and the loop cannot pick its replacement (by 2026-09-12)
- NH-054 [DECISION] Four fifths of your front door's headline is limits, and no lap is allowed to decide whether that is the project's strength or its biggest presentation risk (by 2026-09-13)
- NH-055 [DECISION] Your front door compares the headline forecast number to a model the same page calls broken, and today the loop measured a second, harsher comparison it is not allowed to put beside it (by 2026-09-13)
- NH-056 [DECISION] A Korean university published a reproducible deep-learning wildfire model four weeks before your finals, and a lap may not decide on its own whether to mention it (by 2026-09-13)
- NH-057 [DECISION] The one thing your project now says it contributes has never once been produced from a real fire, and only your laptop can change that (by 2026-09-13)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
