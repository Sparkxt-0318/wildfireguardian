# WildfireGuardian autoloop · dev · 2026-09-04T0255Z

| | |
|---|---|
| when | 2026-09-04 02:55 UTC · 2026-09-04 11:55 KST |
| branch / head | `auto/dev` / `060dce6` |
| kind | dev |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 7, done: 10, parked: 1, todo: 30 |

## What happened this lap

This lap claimed no backlog row. CHARTER §4 step 3 says a lap clears
`docs/auto/CRITIC_LATEST.md`'s `fix-before-next-row` items before it claims one, and
critic #5 left three. Clearing them was the lap: a false sentence in the paragraph a
judge reads first, the tripwire written to forbid that sentence which could not see it,
and the push check written to stop unreported commits which did not stop one.

## Your decisions, applied

**Nothing new arrived.** The Gmail search
(`from:siyeong0318@gmail.com subject:"WildfireGuardian autoloop" newer_than:14d`)
returns 18 threads and every one is a single message the loop itself sent; no thread
carries a reply. PR #31 has no comments. `decisions.py` was therefore not run, and
`docs/auto/decisions_seen.json` is unchanged. NH-018 remains open and is answered in
part below, on evidence rather than on your decision.

## Baseline: the branch was RED, and that was F26

`gates.py --mode quick` at `f6f34ae` exited 1. `make verify` failed at
`check-number-collisions` with six collisions, all on one line that exists in both
paper-lap reports (`2026-09-04T0149Z-manual.md:146` and `…0150Z-manual.md:146`): it
names the anchor words of three registered quantities while quoting a fourth
quantity's value, which is the collision it is describing. The prose is true and the
gate is right — a reader cannot tell that line from a real contradiction without
opening the registry.

Annotated with `collision-ok: 66.7` on the offending line, committed alone as
`130bab7` and pushed before anything else, so the branch was green for any lap
starting behind this one. Two details worth keeping: the pragma is read on the
offending line or the one directly above it, never below (the first attempt put it
below and the gate stayed red), and the comment deliberately quotes no other decimal,
because naming the three registered values on that same line would have created three
fresh collisions against the anchors already sitting there.

## The root objection to this lap's own plan (`hate`)

*You are about to delete a sentence from the judge-facing README because a critic told
you to. If the critic is wrong about 45,157 ha and 104,788 ha sitting on different
bases, you will have removed a true caveat from the paragraph that exists to teach
caveats — and this paragraph has now been wrong twice in opposite directions, so being
wrong a third time is the live risk, not a theoretical one.*

The cheapest test needed no source at all. `docs/data_sources.md` 함정 1 already
recorded 45,157 ha as the estimate the joint survey **more than doubled**; 함정 6 of the
same file called it a parallel basis you may divide by. One file, two answers. A
superseded estimate is not a coexisting basis, so the sentence resting on 함정 6 could
not stand whatever an outside source said. The arithmetic agrees: putting 45,157 over
104,788 leaves the rest of the season's fires holding more than half the national
total, in a season whose largest single event is this chain.

## F21 — the "약 43 %" sentence is gone from both paragraphs, and the tripwire can now see it

**What was wrong.** `README.md` asserted in Korean and English that matching bases
would put this fire at "about 43 %" of the national total rather than 95 %. That
divides a superseded numerator by a current denominator, which is precisely the
mixed-basis division the surrounding paragraph exists to forbid, and it contradicted
this repository's own `fire2025_chain_share_of_nationwide_pct = 94.8`. Judge-facing
prose and the registry held two answers to one question.

**What changed.** The sentence is deleted in both languages and **no ratio replaces
it** — a deletion, not a fourth rewrite of the paragraph. What survives is the rule it
was attached to, which was always correct: the two totals differ in period, body and
basis, so this repository prints no share. Both languages now add only that putting
45,157 on top does not rescue the ratio either, because that figure is the earlier
estimate the survey overturned rather than a parallel basis. `docs/data_sources.md`
함정 1, 함정 6 and the A/B paragraph above 알려진 함정 are corrected to match.

**Where this lap disagreed with the critic, and checked.** critic #5's reason ⓑ was
that 산불영향구역 is *always* larger than 피해면적, so 45,157 under 99,289 has the
relation inverted. That is the sentence this lap was most tempted to paste in as the
replacement wording, and it is not what 산림청 says. 산림청 told 경향신문 that
「산불영향구역과 피해면적은 개념이 달라서 **단순 비교할 수 없고**」 and, through an
official, that 「실제 피해면적은 **줄어들 수도 있고 늘어날 수도 있다**」 — 경향신문
2025-04-17, 김현수·이종섭, <https://www.khan.co.kr/article/202504171020011>, the same
article this repository already cites for the 99,289 ha figure. The two *definitions*
(화선 경계 observed for firefighting strategy, against a field survey for recovery) are
문화일보 2025-04-18, 김창희, <https://www.munhwa.com/article/11499954>. Both were opened
this lap. That is a stronger reason to refuse the ratio than the directional one, and it
is the agency's own, so `docs/data_sources.md` 함정 1 carries each half under its own
source. The critic's conclusion stands; one of its three premises does not, and the
report says so rather than quietly dropping it.

**This lap's first attempt at that paragraph put both quotes under the 문화일보 link, and
its own reviewer blocked the push for it.** 문화일보 carries the definitions and the words
「개념 자체가 다르다」; it does not carry 「단순 비교할 수 없다」 and it does not carry
the 「줄어들 수도 있고 늘어날 수도 있다」 quote. Both of those are 경향신문's. The claim
was true and was sourced two lines above in the same file, so this was a citation split
rather than a retraction — but a lap whose entire subject is a sentence that shipped for
eight commits with a basis its source did not carry does not get to ship one of its own.
Verified independently before the fix by re-opening both articles.

**The tripwire.** `test_the_nationwide_total_is_never_divided_by_the_chain` scanned
only lines containing `104,788`, and both banned ratios sat four lines below it, so it
passed for four commits on the README it was written to forbid. It now locates both
opening paragraphs by their labels — the same trick `scripts/check_readme_figures.py`
uses — and reports every `NN %` inside either, link targets stripped first. Evidence
that the widened scan is not decorative, run this lap against the README as it stood at
`130bab7`:

    widened check on the README as it stood at HEAD (the defect):
      {'Korean': ['95 %', '43 %'], 'English': ['43 %', '95 %']}
    widened check on the README as this lap leaves it:
      {'Korean': [], 'English': []}

A second test, `test_the_tripwire_sees_the_whole_paragraph_not_one_line`, puts each
withdrawn sentence back into a copy of the README and asserts the **same check
function** reports it — not a fresh regex written inside the test, which is how a
tripwire test becomes a tautology. It also asserts the mutation stays confined to the
paragraph it targets, and that a renamed paragraph label fails loudly instead of
silently switching the scan off.

## F22 — a modified report no longer discharges `--assert-reported`

`assert_reported` collected report paths from `git diff --name-only`, where a path
appears whether it was **added or modified**. Appending one line to an earlier lap's
report therefore licensed an arbitrary substantive commit. It reads
`git diff --name-only --diff-filter=A` for the report set now, keeping the plain
listing for the substantive set. Verified against the real range the critic named:

    $ git diff --name-only 98557b9..5a0466e
    docs/auto/NEEDS_HUMAN.md
    docs/auto/STATE.json
    docs/auto/reports/2026-09-04T0134Z-dev.md
    $ git diff --name-only --diff-filter=A 98557b9..5a0466e
    (empty)

so the range that passed the old check now has no report to point at. Three tests in
`tests/test_gates_assert_head.py`: that exact range must exit 1, the same range with a
newly written report must exit 0 (the intended flow has to keep working), and
`12b8ac7`'s own path set plus an edit to a stale report must still be refused. The
fixture had to be widened too — it answered every `diff --name-only` call with the same
string, so it could not have distinguished the two listings and the new behaviour would
have been untested. The failure message now names the edited reports explicitly, so the
next lap is told why its report did not count.

## F24 — the retired AI ledger is archived rather than deleted

`fbe71de` deleted `docs/auto/AI_DISCLOSURE.md` where CHARTER §3 rule 7 says archive.
The removal itself is yours and is documented (NH-008); the mechanism was the finding.
Restored verbatim as `docs/auto/archive/AI_DISCLOSURE_retired_2026-09-04.md` under a
banner saying it is retired, why, and that the live practices are CHARTER §9, and
indexed in the archive README. `docs/auto/JUDGE_QA.md` was deliberately **not** changed
to point at it: the critic suggested the archived copy as the answer to a judge asking
"그럼 어디에 정리되어 있습니까?", but handing a judge a file banner-marked *retired* is
worse than handing them CHARTER §9, which is live and says the same things.

## Found while working: the README printed both of its opening stanzas twice

`README.md` printed **both** opening stanzas twice — `**보호 대상**: 한국 농촌의
고령층(60–80대)…` and `**Motivating event**: the March 2025 wildfire…`, each duplicated
verbatim on consecutive lines by `d2f314d`. They survived the eight commits since, a
critic run that read that exact diff, `make verify`, `check_readme_figures.py` and the
paragraph's own 36 tests. Every one of those flattens or greps the paragraph; none looks
at it as text.

This lap removed the Korean twin, missed the English one, and its own reviewer found it
still standing — inside the very paragraph the lap had just rewritten and built a
paragraph-wide gate for. Both are gone now, and the new test asserts each anchor appears
**exactly once** rather than at least once, which is what let the second one through.
Filed as **WFG-052** (P1, infra) for the general gate: fail on two consecutive identical
non-empty lines in judge-facing markdown.

## What did not work

- The first `collision-ok` pragma was placed on the three lines *below* the offending
  line. `check_number_collisions.py` reads a pragma only on the offending line or the
  one directly above; the gate stayed red and the annotation had to be moved inline.
- The first draft of 함정 6 wrote the reductio with its arithmetic spelled out
  (59,631 ha, about 172 ha per fire). Those are derived numbers with no registry key,
  in judge-facing prose, which is CHARTER §3 rule 3. Rewritten qualitatively: the rest
  of the season would have to hold more than half the national total.
- Two runs of the full suite were lost to the interim-estimate tripwire, correctly:
  both new passages in 함정 1 and 함정 6 pushed the 「산불영향구역」 label outside its
  ±3-line window, so 45,157 read as bare. The label is now inside the window in both.
  The pre-existing test caught the lap's own prose twice; that is the gate working.
- `baseline-verify` WARN is unchanged and expected off-laptop (soft step, `hard: false`,
  the git-ignored raw manifests). Sixth lap running.
- The suite reported `1233 passed, 62 skipped` on the first full run of this lap and
  `1238 passed, 56 skipped` on the second, on unrelated code. That is WFG-039 again —
  the SRTM tile downloaded mid-run — and is exactly the symptom WFG-038 asks to be
  gated. Both rows remain `todo` and this is a fifth sighting.

## Evidence paths

- `README.md` — both opening paragraphs, ratio deleted, duplicate line removed
- `docs/data_sources.md` — 함정 1, 함정 6, and the A/B paragraph; the 문화일보 source
- `tests/test_motivating_event_figures.py` — `_opening_paragraphs`,
  `_percentages_in_opening_paragraphs`, the widened tripwire and its mutation test
- `scripts/auto/gates.py` — `assert_reported`
- `tests/test_gates_assert_head.py` — the F22 fixture and three tests
- `docs/auto/archive/AI_DISCLOSURE_retired_2026-09-04.md`
- `docs/auto/NEEDS_HUMAN.md` — NH-018, item 2 answered on evidence, entry left open
- `docs/auto/MEMO.md` — the lesson
- `docs/auto/BACKLOG.md` — WFG-052

## Reviewed by

**`Reviewed by: subagent (block): the lap replaced one badly-sourced sentence with
another — it attributed 「단순 비교할 수 없다」 and 「작을 수도 클 수도 있다」 to 문화일보
2025-04-18, which carries neither; both are 경향신문 2025-04-17's, an article this
repository already cites two lines above.`**

The block was correct and was not argued with. All three of its nails were taken:

1. **The citation split** (its root objection, CHARTER §3 rules 5 and 5b). Both articles
   were re-opened to confirm before the fix. `docs/data_sources.md` 함정 1 now carries the
   definitions under 문화일보 and the comparability quotes under 경향신문, 함정 6 ⓑ points
   at the right one, and NH-018 — the file the author reads to decide — is corrected and
   says plainly that the first attempt got it wrong and the reviewer caught it.
2. **The English duplicate stanza**, still standing after the lap removed the Korean one.
   Removed; the test now asserts each anchor appears exactly once.
3. **The tripwire's scope had narrowed as well as widened.** The old line scan over the
   whole README was replaced by the paragraph scan; nothing is lost today, but a ratio
   printed beside the nationwide total in a future Round-4 section would have become
   invisible. Both scans run now.

What the reviewer confirmed rather than found: the mutation test is not a tautology (it
built a worktree, dropped `130bab7`'s README into it, and watched both tests fail there);
the `--assert-reported` fixture matches the real shape of `98557b9..5a0466e`; renames are
handled, since an `R` path is not in `--diff-filter=A`; the archived ledger is
byte-identical to the deleted file minus its banner; nothing is deleted in the range;
`docs/NUMBERS.json` is untouched. Two holes it names in `--assert-reported` are
pre-existing and not introduced here: any newly added report file discharges the
requirement whatever its content, and `CLAIM_ONLY` still lets prose ride inside
`BACKLOG.md`. They belong to WFG-046's successor work, not to this lap.

## The next row

**WFG-017** (`web/finals.html` refresh v2, P0, due 09-07) is the first `todo`,
agent-doable, unblocked row in table order. WFG-021 sits above it and its remaining
part (c) needs the NOAA archive at run time, which this sandbox does not have; a lap
taking it should split the row rather than mark it done. WFG-051 (F23, the registry and
the README naming three different agencies for the same two figures) is the critic's
`fix-this-sprint` item and is the strongest P1 behind the P0s.

## In plain terms

**What changed for the project.** The first paragraph of your README told the reader
that this fire was "about 43 %" of the country's 2025 total. That sentence was wrong —
it divided an early estimate that was later more than doubled by a number measured
afterwards — so it is gone, in Korean and in English, and nothing replaces it. The
paragraph now simply refuses to print a share, which is what the rest of it already
said. The README was also printing both of its opening stanzas twice, in both
languages; that is fixed, and the branch was broken when this lap started and is green
again.

**Why it matters to the judges or the science.** Two of the five judging lenses failed
this project yesterday on that one sentence, from opposite directions, and it sat in
the paragraph that teaches a judge how to read your numbers. Being wrong there costs
more than the sentence is worth. The test that was supposed to forbid exactly this
ratio could not see it, and now can — checked by putting the bad sentence back and
watching it fail.

**What you should do.** Nothing is required. Two things are worth knowing. The critic
gave three reasons the sentence was wrong and one of them was itself wrong (it said the
「산불영향구역」 figure is always bigger than the 피해면적; 산림청 says the two simply
cannot be compared). And this lap's first fix put that correction under the wrong
newspaper — its own reviewer caught it and refused the push, so the documents now name
경향신문 for the quotes and 문화일보 for the definitions. If you disagree with any of
this, reply `NH-018: <your decision>` and the next lap will do what you say, verbatim.



## Gates

**ALL GREEN** · mode `full` · head `060dce6` · 2026-09-04T02:42:32Z · current at `060dce6`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 13.1 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.1 s | make: *** [Makefile:152: baseline-verify] Error 1 |
| snapshot-verify | PASS | 0.4 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 149.3 s | 08s (0:02:13) |

## Commits since the previous report (5a0466e65afca743c76f394ce673f2c183e9491e..HEAD)

- 060dce6 critic #5: the paragraph stops printing a ratio, and a modified report stops counting as one
- 130bab7 F26: the branch is green again, on a pragma rather than a rewrite
- f6f34ae report: the plain-terms section names the red branch, which arrived after it was written
- ed2b267 critic #5, addendum: the branch is red, and not on anything in this lap's window
- 5f39452 critic #5: the figures are finally right and the sentence about them is not
- 2a02477 report: the plain-terms line pointed the author at the wrong NEEDS_HUMAN row
- b621441 the reviewer blocked on the word "reported", and it was the load-bearing word

## Needs a human (14 open)

- NH-001 [DECISION] Email delivery needs three repository secrets
- NH-002 [DECISION] Optional: `@claude` on GitHub issues and PRs
- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-010 [DECISION] Expert consultations and the firefighter record (by 2026-09-20)
- NH-011 [DECISION] One real, recorded email send from a network that works in Shanghai (by 2026-09-20)
- NH-012 [DECISION] Portal downloads the loop cannot do (by 2026-10-01)
- NH-013 [FYI] Optional: a stable web address for the visual board
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-016 [DECISION] The critic routine has 30 minutes to land its findings, and needs about 40 (by 2026-09-05)
- NH-017 [DECISION] Three entries were closed on replies the repository cannot see, and one of them was wrong (by 2026-09-06)
- NH-018 [DECISION] Two laps read the same primary sources and disagree on two sentences (by 2026-09-08)
- NH-019 [DECISION] One report time, for one fire, decides what the detection result is allowed to say (by 2026-09-08)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
