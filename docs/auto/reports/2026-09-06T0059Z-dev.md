# WildfireGuardian autoloop · dev · 2026-09-06T0059Z

| | |
|---|---|
| when | 2026-09-06 00:59 UTC · 2026-09-06 09:59 KST |
| branch / head | `auto/dev` / generated at `a182cc0`, **shipped and certified at `46ef599`** |
| kind | dev |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 7, done: 16, parked: 1, todo: 71 |

## What happened this lap

## Your decisions, applied

**Nothing new this lap.** The Gmail search (`from:siyeong0318@gmail.com subject:"WildfireGuardian
autoloop" newer_than:14d`) returns only the loop's own sent reports — no reply from you — and PR #31
carries no comment in the `NH-###: …` form. Your four decisions of `4d705df` (2026-09-06) were
already applied by the session that made them and are recorded in `docs/auto/decisions_seen.json`;
this lap acted on one of them, **WFG-121**, which is the row it took.

**Still waiting on you:** NH-032 (which of two fair opponents the project reports) and NH-034 (the
same experiment's headline consequence), both dated by 2026-09-08. This lap did the half of WFG-121
that neither answer changes and printed no margin, as critic #22's `fix-before-next-row` item
required.

## The row: WFG-121 — the fair-opponent line on every judge-facing surface

Claimed `81a0a15` at 00:20Z, your own row, named #1 by `docs/auto/DIRECTION.md`.

### The root objection I recorded against my own plan, and it changed the lap

`hate`, before building: **critic #22's brief told me which numbers were safe to print, and that is
a measurement, not a fact.** Its `fix-before-next-row` item said the buffer-sweep counts (250 m walks
91 origins into the fire; 2 km leaves 80 past the budget) were 「the half no answer changes」 and named
them for the surfaces. If that premise were wrong, four judge-facing surfaces would carry numbers with
exactly the fuse the constraint existed to avoid.

The cheapest test was one `git show` of the parked artifact on `auto/red/20260905T2248Z`, which is
already in the repository. **The premise is wrong.** Under the parked opponent the same widths fail in
a different *kind* — its wide buffers strand people by refusing departure and by walling every refuge
off, where the committed arm records late arrivals — and the two arms do not agree on which width comes
off best. The counts are convention-dependent exactly as the margin is.

So the lap shipped the **shape** instead of the counts-as-conclusion: narrow buffers walk people into
the fire, wide buffers strand them, the failure changes kind rather than shrinking, and no operator can
know on the day which side of that crossing they are on. Both arms show that. It is a better answer to
a judge than a margin was, and it needs no decision from you.

### What changed

1. **`docs/auto/DEMO_SCRIPT_5MIN.md` 3막 — a closed limitation was still being spoken.** Its ⚠ note
   told the student to say 「지금 불만 피하는 경로와의 비교는 아직 돌리지 않았습니다」 and that the Q&A
   card for it 「아직 없습니다」. Both went false on 2026-09-05 when WFG-114 ran the arm and critic #22
   wrote the card into Q19. This is the WFG-109 shape (a retired sentence surviving in the file the
   student rehearses from), on the booth script, and it made the project **understate** itself to a
   judge who could check. Replaced with the true state, the fire-blind sentence, and the sentence to
   say while NH-032 is open. No margin figure.
2. **`docs/fair_opponent_line.md` (new)** — the single source for the line, so the surfaces quote it
   rather than each other: the fire-blind provenance with its two code citations, §3's buffer finding,
   §4 recording the correction to the brief, and §5 saying what it does not show.
3. **`tests/test_fair_opponent_line.py` (new, 10 tests)** — the durable half. It pins the retired
   clauses absent, the correction present, the word `fire-blind` beside the contrast, the bank card's
   existence, **no contested margin anywhere in the booth script** while NH-032 is in the ledger, the
   sweep table **not** restated here, every registry key it names registered, and both the concession
   and the oracle caveat present. Mutation-graded six ways, each turning exactly one test red, tree
   green after every revert — including the two escapes the reviewer found in the first version.
4. **`docs/auto/NEEDS_HUMAN.md` NH-032** gains a loop note recording the falsified premise. It asks you
   nothing new: it says what the loop may print while you decide, and that the table is relabelled rather
   than rewritten whichever way you answer.
5. **`docs/auto/MEMO.md`** — one lesson: an upstream instruction's *premise* is not inherited evidence
   because its *instruction* is authoritative; and when a value is contested, look for the claim one
   level up that both candidates imply.

### What I did not do, and why

- **The spoken 3막 line is unchanged**, and this is not NH-032's fault. Adding a sentence to it changes
  the syllable count, which moves the registered `demo_pace_*` allocation; CHARTER §3.2 forbids editing
  a registered value. The spoken half of WFG-121 needs a **new pace tag** — a re-allocation of all six
  segment times through WFG-100's machinery — which is its own lap. Recorded as residue (c) on the row.
  WFG-114 deferred the same sentence and did not say why; now the reason is written down.
- **`paper/manuscript.md` and `web/finals.html` untouched.** The manuscript is the paper routine's under
  CHARTER §12 (its own row is WFG-126); the screen already calls its control `fire-blind` in every string
  that describes it, and rebuilding it is WFG-113's row.
- **No margin anywhere in this diff**, per critic #22's item and NH-032's own constraint. ⚠ **Scoped
  deliberately: that is true of the diff and *not* of the repository.** `docs/auto/JUDGE_QA.md` Q19
  already prints 9, 27 and 5 in bold — pre-existing, written by critic #22 itself inside the ⚠ note
  that tells the student **not to say them**. My reviewer caught me claiming the stronger thing. I did
  not touch that note: deleting the figures removes the student's protection, and overruling another
  lap's deliberate choice is a CHARTER §6 escalation rather than an edit. Recorded on NH-032 for the
  next critic to settle.

### Reviewed by: subagent (**block**, then fixed) — 「§3 ships a harder finding than the artifact it cites」

**The block was correct and it caught the exact failure I had just written a MEMO lesson about.** My
§3 was headed 「no fixed buffer width works」 and said the failure 「does not shrink with width; it
changes kind」. The artifact says otherwise in its own caveat block, and so does
`docs/present_perimeter_arm.md` §4, the file I was consolidating: the safe total is a **spike** —
275 / 284 / **345** / 275 / 283 across 250 m → 3 km against the forecast-aware arm's 354 — so on this
fire a *well-chosen* fixed buffer nearly matches the forecast. The parked arm says it louder (its best
width, 500 m, reaches 349). I had kept the second half of the source's sentence and dropped the
concession in the first half, and my table printed the two failure columns while omitting `safe_total`,
which is the column that would have shown it. **Every part of that error ran in the project's favour**,
which is the WFG-114 pattern my own MEMO entry this lap describes — written before I made it.

**Fixed, not argued:** §3 now states the narrower true claim (**which** width works is convention-
dependent and unknowable on the day, and the two arms disagree about it), names the concession in
bold, bounds the operational conclusion to this fire, and **drops the table entirely** — the sweep's
one home is `docs/present_perimeter_arm.md` §4, already gated cell-by-cell against the artifact, and a
new test refuses any restatement of it here. The booth script's exported clause 「고정 여유폭 하나로는
안 된다」 is replaced with the same correction. §5 gains the reviewer's `mandela` finding: the shape
inherits the oracle exactly as the margin does, because every column is graded on the same simulated
field the forecast-aware arm plans on.

**The reviewer also defeated my first test file twice**, and both holes are now closed and graded: a
margin planted inside the ⚠ sentence the script tells the student to *say* passed, because I had
scoped the guard to `> ` lines (it now scans the whole script); and the table's two column headers
could be swapped with every gate green, because the check bound value→key but never key→column (moot
now that the table is gone, and the anti-restatement gate replaces it).

An independent `ssotize` pass I ran in parallel had reached the same conclusion about the duplicated
table from the other direction. The two findings agreed, so the fix serves both.

### Evidence

- `.auto/gates.json` — `--mode full` ALL GREEN, **1542 passed, 62 skipped** (1535 before; +7 new).
  `baseline-verify` WARN is the two laptop-only differences CHARTER §3d calls information.
- `data/processed/present_perimeter_arm_uiseong_andong_2025.json` → `buffer_sensitivity` (the table).
- `auto/red/20260905T2248Z:data/processed/present_perimeter_arm_uiseong_andong_2025.json` →
  `arms_by_buffer_m` (the falsifying comparison; read, not modified, and the branch is untouched).
- `docs/auto/reports/` — this report; `docs/fair_opponent_line.md`; `tests/test_fair_opponent_line.py`.

### Next row

**WFG-007, the printables**, and critic #23's falsifiable test is explicitly about whether a lap will
take it: `docs/auto/finals/` has held two `.md` files and no PDF for seven days, it alone holds readiness
R7 and half of R9, and no lap has claimed it in twenty-two critic windows. It is the next row in table
order and on `DIRECTION.md` now that WFG-121's unblocked half is done.

## In plain terms

- **What changed for the project.** Your booth script was still telling you to say 「we haven't tried
  comparing against a map that shows where the fire is now」 — and we have, since yesterday. That
  sentence is gone, replaced with what is actually true, plus the one honest caveat: we ran it, the
  result is published, and we are not quoting a single number until you pick between two ways of doing
  the sum. A new page, `docs/fair_opponent_line.md`, holds the whole line in one place, and a test now
  goes red if any of it drifts back.
- **Why it matters to the judges or the science.** The old sentence made you sound like you had done
  less than you had, on the exact question a sharp judge asks first. The replacement is careful, and it
  got more careful during the lap: my first draft claimed 「no fixed safety buffer can work」, and the
  independent reviewer showed that is **false** — on this fire, a buffer of the right width nearly
  matches the forecast. What is true is narrower and still worth saying: **nobody standing at a real
  fire can know which width is the right one**, and two honest ways of doing the same calculation
  disagree about it. That is the argument for forecasting. It is smaller than what I first wrote, and
  it is the one that will survive a sharp judge.
- **What you should do.** Answer **NH-032** when you can (by 09-08): which of the two fair opponents
  the project reports. Nothing else needs you, and the booth script is safe to rehearse from today.



## Gates

**ALL GREEN** · mode `full` · **re-run after this report was written, at the commit this lap actually pushes: `46ef599`** · `1551 passed, 56 skipped` · `--assert-head` and `--assert-reported` both exit 0 at `46ef599` immediately before the push.

The table below was generated at `81a0a15` and the report machinery correctly marked it stale, because `report.py` runs before the report and the images are committed (CHARTER §4 step 8). It is not stale now: the same `--mode full` was re-run on the pushed tree and is the run quoted on this line. ⚠ **One number in the table below differs from the certified run and it is environmental, not a regression:** the row reads `1542 passed, 62 skipped` and the certified run reads `1551 passed, 56 skipped`. Six tests that skip on a cold clone (`data/cache/**` DEM and OSM caches, all git-ignored) find their inputs once the suite has been run in the same sandbox, and three more are this lap's own additions. **GitHub's clean-clone run will report the cold counts, not these**, and that difference is expected rather than a discrepancy to chase

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 16.9 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.2 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 0.5 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 240.3 s | 1551 passed, 56 skipped, 52 warnings in 239.15s (0:03:59) |

## Commits since the previous report (4d705dfcb1fa477e98f6c50c93208091cd44d758..HEAD)

- a182cc0 WFG-121: the booth script was still telling the student we had not run the experiment we ran
- 81a0a15 claim WFG-121 (20260906T0020Z)
- 9cc973b critic #22: the author's row was built twice, and the two green answers differ by more than the answer

## Needs a human (7 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
