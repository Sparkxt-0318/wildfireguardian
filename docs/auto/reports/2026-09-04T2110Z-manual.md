# WildfireGuardian autoloop · paper · 2026-09-04T2140Z

| | |
|---|---|
| when | 2026-09-04 21:10 UTC · 2026-09-05 06:10 KST |
| branch / head | `auto/dev` / `3bf4dc1` |
| kind | manual |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 7, done: 13, parked: 1, todo: 47 |

## What happened this lap

This lap incorporated `3bf4dc1` (previous: `7988769`). The code moved on twenty files
outside `paper/` and `docs/auto/`, and exactly one of those changes was the manuscript's:
the 주소정보누리집 designated-site subset for 영덕군, wrongly cut on 시군구 47920 (봉화군)
when it first landed, was **re-cut by the author on 47770 and verified from the data
itself**. `paper/GAPS.md` had carried a standing block saying that input must not enter
the manuscript until that happened. It has happened, so it entered.

## What changed in the manuscript

**§6 Limitations — a new paragraph, and the paper's first honest account of where its
refuges come from.** Every refuge in every result here is an OpenStreetMap point. The
repository now holds an agency-designated inventory for Yeongdeok-gun: 64 지진옥외대피장소,
92 지진해일긴급대피장소 and 17 무더위쉼터 on the 2025-03-01 cut, and 55 민원행정기관 points
on the 2024-01-24 cut, four of them 소방 and six 경찰서 — candidate depots in the one region
whose OpenStreetMap depot density Table 2 reports as 0.45 per 100 km². The paragraph names
the agency and both data dates, and states the limit twice over: none of these is a
*wildfire* refuge (the designated categories are earthquake, tsunami and heat), and the
subset is seven of the portal's point layers rather than its whole taxonomy.

**The claim is deliberately weaker than `paper/GAPS.md` had predicted.** Lap 4's block
planned to write "the 사물주소 taxonomy holds no wildfire evacuation category at all". That
is not checkable from anything committed: `SAMUL_LAYERS` in
`scripts/extract_juso_yeongdeok.py` names seven layers the extractor chose out of a zip
whose full layer list is laptop-only. So the manuscript says only that none of the
categories *in this subset* is a wildfire one, and GAPS.md now records why, so a later lap
does not "restore" the stronger sentence.

**A new gap, G6, and it is the first one a cloud lap could actually close.** G3 and G4 both
need the author's laptop. G6 does not: the designated-site layers, the walk graph and the
origin list are all committed, so re-snapping the refuges to the designated sites and
re-running the same 458 origins on the same canonical field is runnable here. It is not
the paper routine's to run — the output would be a new artifact outside `paper/` — so it is
filed against backlog WFG-073. Until it runs, the paper cannot say how much of the
458-origin partition is a statement about where refuges are rather than about where
OpenStreetMap says they are. The paired contrast is unaffected; the absolute rates are not.

**§5 Discussion — one sentence, which is the transferable half of the whole episode.** The
wrong cut was caught by checking a label against the geometry it claims rather than against
a code table. That belongs in the paper's "the instrument is the contribution" argument
next to the flat control and the column-addition envelope, and it is written there without
any of the loop's process around it.

**§4.6 — the two dispatch comparison grids became Table 3.** The headline 360-cell grid and
the 2,160-cell widened-window grid were two dense prose tallies; they are now four rows of
win/tie/loss, each entry a cell count with its share, and the lineage caveat carried in the
caption. No value changed. The interpretation — the near-degenerate sort key, the absence
of any operating region, the 3,744-value re-derivation with 0 differences — stayed in
prose, which is where it earns its place. **The reviewer blocked the first version of this
table and was right to; see below.**

**§4.5 — compressed, no caveat dropped.** The budget sweep's two interior points and the
mean absolute gradient came out because Fig. 7 plots them; every control sentence ("the
flat control changes 0 routes, as it must"; "neither network is the right one") stayed.

## The build defect this lap found underneath the length problem

`build_docx.py` recognises a table caption only when the `Table N.` line is *immediately*
followed by the `|` row. All three captions had a blank line between them and their table.
Each therefore fell through to the paragraph branch: **counted as body text, rendered as
ordinary prose, and the table built with the label `Table N. ` and an empty caption.**
Three blank lines deleted. The captions now render bold-labelled and attached, and the body
count fell **7,423 → 7,105 with not one word of prose removed**. Figure captions were never
affected; that branch has no such condition.

Four laps had been fighting a length budget that was 318 words wrong, and lap 4's ledger
concluded the only remaining route was to drop caveats. It was not.

**And this lap fell into the same trap while fixing it**: the new Table 3 was written with
the habitual blank line and built with an empty caption. `check_paper.py` counts tables but
cannot see that a caption went missing — it was caught only by reading the tables back out
of the `.docx` with `python-docx`. That read-back is now written into `GAPS.md` as the
check to run after adding a table.

## Two stale cross-references, fixed

`build_docx.py` numbers figures by order of appearance, and `F8_routing_map` was added to
§4.3 after the `README.md` mapping table was written. Appearance order is F1, F2, F4, F5,
F8, F3, F6, F7. So §4.4's "(Fig. 5, Table 2)" pointed at the routing map instead of the
three-region partition, and §4.6's "the caption of Fig. 7" — the sentence that binds the
dispatch lineage caveat to every number in the section — pointed at the sensitivity panel
instead of the dispatch figure. Both corrected to Fig. 6 and Fig. 8, and `paper/README.md`'s
mapping table — which was the stale thing underneath both — is updated with the warning
that nothing mechanical catches this: `check_paper.py` checks that each figure *file*
exists, not that a `Fig. N` in prose names the right one.

Two figures were also never cited in the text at all: F1, the system overview, and F8, the
Yeongdeok routing map. §1 and §4.3 now cite them.

## One finding handed to a dev lap

`make check-artifact-manifest` fails on any prose citing a committed artifact that
`docs/artifact_manifest.json` does not list, and it caught a first draft of G6.
`data/processed/external/juso_yeongdeok/` has eight files; only `manifest.json` and
`minwon_agencies.geojson` are listed, though `scripts/register_juso_yeongdeok.py` registers
a count for each of the seven 사물주소 layers too. G6 cites the folder and its
`manifest.json` instead. The manifest gap is outside what CHARTER §12 lets this routine
touch.

## The reviewer blocked, and the block was correct

Three findings, all verified against the artifacts before acting on them rather than taken
on the reviewer's word.

1. **Table 3 reported a value as unavailable that its own cited artifact supplies.** The
   first version printed the 600-minute row's ties as an em dash. `ordering_boundary.json`
   → `boundary.pooled_by_window.W600` gives `deadline_wins` 22, `ties` 22,
   `deadline_loses` 136 over n = 180. The tie value is 12.2 %, identical to the win rate in
   the same row — which is a reason to print it, not to suppress it, in a paper whose
   stated discipline is that the unreachable class is reported and never imputed. Filled.
2. **The caption was false of three of its four rows.** It said "win, tie and loss are
   counts of configuration cells" while three rows printed percentages and one printed
   counts, with no unit marker. Every row now gives the count and its share, and the
   caption says so. This is exactly the defect class `GAPS.md` had just recorded as one
   `check_paper.py` cannot see — the `.docx` read-back this lap prescribes confirmed the
   caption was non-empty, not that it was true.
3. **An off-by-one in G6**: "six 사물주소 layers" against `SAMUL_LAYERS`'s seven and seven
   `samul_*.geojson` files on disk — in the same lap whose manuscript sentence says seven
   correctly. Fixed.

Two further objections were revision items rather than blockers, and both are taken:

- **The two refuge sets were not on one extent and the paragraph did not say so.** The
  designated counts are county-scope (77–84 % of each layer inside the canonical walk box
  per `manifest.json`); the 50 OpenStreetMap refuges are counted inside that box alone. §6
  now says they must not be differenced as they stand, and G6's method starts by clipping.
- **§5 and §6 told two halves of one story without linking.** §5 read as a clean refusal of
  the dataset that §6 then reports an inventory from. §5 now completes the arc — wrong code,
  re-cut, checked the same way, admitted — which is a better instance of the discipline
  than the refusal alone.

The reviewer also confirmed independently what this lap claims: all §6 counts against
`manifest.json` and the registry, every migrated Table 3 value against
`dispatch_ordering_comparison.json` and `ordering_boundary.json`, the corrected figure
numbering by rebuilding the `.docx` and reading the rendered "Figure N" labels back, and
all 27 citations resolving to `references.bib` entries carrying a `verified` note. It
recorded one residue this lap accepted: §4.5's compression removed the parenthetical
"three move at some interval, none at all three", so the union count 3 is now nowhere in
the paper while the intersection count 0 remains. The sentence still says "at all three
intervals" and the adjacent 42/42/41-against-41 counts still show movement, so no claim
changed — but a later lap restoring four words there would be right.

## Figures

None drawn or redrawn. `make_figures.py` was re-run and all eight PNGs came back
byte-identical, so nothing is committed for them. No result section lacks a figure or a
table: §4.6 gained Table 3, which is what it was missing.

## Numbers

| | before | after |
|---|---:|---:|
| body words | 7,423 | **7,362** |
| tables | 3 | 4 |
| figures | 8 | 8 |
| references | 27 | 27 |
| gap markers | 4 | 5 |

Page estimate is still not asserted, and the reason is unchanged and recorded in `GAPS.md`:
this sandbox has no working `.docx` converter (`soffice` fails on a two-paragraph file
written by the same `python-docx`) and no metric-compatible Calibri, so a page count here
would be an unchecked constant. The crude recount over the built document — body, captions,
tables and references, plus eight full-width figures — still lands near 21 pages against
the 20-page target. **The cheapest close is still one number from the author**: open the
committed `.docx` and report the page count.

`check_paper.py` exit 0. `gates.py --mode quick` exit 0 (verify PASS, pytest-quick PASS),
re-run after the reviewer's fixes. `tests/test_paper.py`,
`tests/test_detection_ordering_is_not_claimed.py` and
`tests/test_external_figures_carry_their_scope.py`: 114 passed.

The net word change is small — 7,423 to 7,362 — but it is not the same 7,400 words. 318 of
them were a miscount, the reviewer's fixes put 99 back, and what the lap actually bought is
a new limitation, a new gap, a table, four corrected cross-references and a build fix.

Reviewed by: subagent (block): the new Table 3's caption contradicted its own body — three
of four rows in percentages, one in counts, under a caption asserting counts — and one cell
was printed as unavailable when the cited artifact supplies it. Both fixed, plus a "six"
that should have read "seven"; the reviewer's two non-blocking objections were also taken.
Re-gated after the fixes: `check_paper.py` exit 0, `gates.py --mode quick` exit 0.

## In plain terms

**What the paper now says.** It finally admits, in print, that every evacuation refuge in
its results is an OpenStreetMap point — and it now names the government-designated list of
영덕 evacuation sites you downloaded, which nothing in the results uses yet.

**What is still missing.** The comparison between the two: re-run the same 458 walkers with
the designated sites as refuges and see whether the answer changes. Unlike the other open
gaps, this one needs no laptop — a dev lap can run it here.

**What you should do.** Open `paper/WildfireGuardian_Park_2026.docx` in Word and tell the
loop its page count. That one number has been blocking the 20-page check for three laps,
and the paper is close enough to the limit that it matters.



## Gates

**ALL GREEN** · mode `quick` · head `3bf4dc1` · 2026-09-04T21:09:56Z · current at `3bf4dc1`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 15.4 s | === make verify: PASSED === |
| pytest-quick | PASS | 8.8 s | 40 passed in 7.91s |

## Commits since the previous report (baf696286231dd16a998d0f22a77be56f02bb708..HEAD)

- 3bf4dc1 critic #13: half of what the loop wrote yesterday was about the loop, and one backlog id named two rows

## Needs a human (5 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-024 [DECISION] WFG-062 is now three laps deferred, and the critic's own re-scope condition has been met (by 2026-09-07)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
