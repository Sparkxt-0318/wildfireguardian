# Truck-crew replay built; parked red on a pre-existing finals-screen encoding failure

| | |
|---|---|
| when | 2026-09-13 16:21 UTC · 2026-09-14 01:21 KST |
| branch / head | `auto/dev` / `c7f2f06` |
| kind | red |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 8, done: 40, dropped: 1, parked: 1, title-derivable: 1, todo: 131 |

## What happened this lap

Laptop session, harness paused. Built the truck-crew replay tool from
`docs/auto/briefs/TRUCK_CREW_REPLAY.md`: rule page `docs/truck_crew_replay.md` §1-§3
written first, then `scripts/build_truck_crew_replay.py` (imports the scheduler, the
routers, the corridor sampling and the observed grader rather than re-implementing any of
them), then `data/processed/truck_crew_replay_yeongdeok.json`, then the offline page
`web/truck_crew_replay.html` from `scripts/truck_crew_replay.template.html`, then 15 tests.

Result at k = 4: 52 trips ordered, 42 arrived before the observed footprint reached the
pickup's cell, 3 cancelled by the abort rule. In the same run 30 of the 52 drove an ingress
corridor the same observation puts inside the footprint at the minute the vehicle was in
it. Both counts are in the doc, on the screen and in the report, and the rule page makes
them travel together.

**This report is `red` because the gates exited 1 at `c7f2f06`, and the failure is not this
work's.** `tests/test_finals_payload_rederives.py` compares two PNG encodings of the finals
screen's hillshades; decoded, the images are pixel-identical (max per-channel difference 0).
The repair it names rewrites `web/finals.html`, which this session's brief and CHARTER §3.2
both forbid. Filed as **NH-061** with the measurement and four options. The same failure was
present on the clean tree at `bb820d6` before this work started.

Full report: `docs/auto/briefs/TRUCK_CREW_REPLAY_REPORT.md`.

## In plain terms

(The lap did not write this section. It should say, in three lines for the author: what changed for the project, why it matters to the judges, what you should do.)

## Gates

**RED** · mode `full` · head `c7f2f06` · 2026-09-13T16:19:45Z · current at `c7f2f06`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 24.9 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.1 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 0.3 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | FAIL | 294.5 s | 1 failed, 2318 passed, 65 skipped, 3 xfailed, 52 warnings in 293.78s (0:04:53) |

## Commits since the previous report (ed5f54182ea8986f339f01356b5adc8a77e80759..HEAD)

- c7f2f06 the truck-crew replay reads its own corridors against the observation, and the corridors are worse than the pickups
- bb820d6 last-safe-departure lead-time result (3c) and the truck-crew replay build brief
- 2da2dd5 reviewer follow-up 2026-09-13: building routes graded on the observation, no-safe-route diagnosis, one pickup intervention; wording tightened (docs + artifacts only)
- 0619826 freeze prep, laptop session 2026-09-13: three-way observed re-grade, WFG-275 building origins, feasibility phase diagram (docs + artifacts only)
- e96471c NH-005 closed: the 도로명주소 건물 layer for 영덕, cut and exposed as the `juso` building source
- 30f9ec5 the sweep was corrected everywhere except the three paragraphs written for the reader who cannot check it
- 7750d8f harness paused by the author 2026-09-12: 24 decisions applied, real-hazard dispatch sheets, observed-footprint re-grade, handover

## Needs a human (6 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-040 [FYI] A critic lap pushed one commit past a red `--assert-reported`, and it is telling you rather than hiding it
- NH-041 [FYI] This lap sent you an email containing only the word PLACEHOLDER, and could not take it back
- NH-061 [FYI] `auto/dev` is red on a test that compares two PNG encodings of pixel-identical images, and the only repair it names is one this session is forbidden to make

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
