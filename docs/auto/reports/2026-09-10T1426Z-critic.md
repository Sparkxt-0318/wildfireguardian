# WildfireGuardian autoloop · critic · 2026-09-10T1426Z

| | |
|---|---|
| when | 2026-09-10 14:26 UTC · 2026-09-10 23:26 KST |
| branch / head | `auto/dev` / `3867860` |
| kind | critic |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 9, done: 33, dropped: 1, parked: 1, title-derivable: 1, todo: 111 |

## What happened this lap

Critic lap #59. Reviewed head `3867860`, window 2026-09-09T13:09Z to 2026-09-10T14:00Z (25 h, 61 commits,
45 pushed heads). This lap changed no code, no test, no data, no figure and no committed artifact; it wrote
only under `docs/auto/`.

⚠ The clone arrived **SHALLOW at exactly 50 commits** for the third lap running, which is almost exactly the
24-hour window, so `git log --since` would have returned the whole clone and truncated silently.
`git fetch --unshallow` was run before anything was counted: `--is-shallow-repository` answers `false` and
`git rev-list --count HEAD` answers **717**, so every ancestry statement below is licensed under CHARTER §4.

## Your decisions, applied

**Nothing new reached me, on either channel, and it is the fifth consecutive lap to say so.** The Gmail
search `from:siyeong0318@gmail.com subject:"WildfireGuardian autoloop" newer_than:14d` returns 25 threads on
the first page, every one a report this loop sent, and **every one holds exactly one message**, so no reply
is threaded under any of them. **PR #31 has zero comments.** Nothing was applied with
`scripts/auto/decisions.py` and `docs/auto/decisions_seen.json` is unchanged: its `applied` list still ends
at **NH-031**, closed 2026-09-06 in a Claude Code session on your laptop. No `NH-###:` line has reached the
loop through email at any point.

**What is waiting on you, counted at this head rather than inherited.** **21 open entries (20 DECISION + 1
BLOCKER)**, plus 5 open FYI. **13 are at or past their stated date**: NH-032, 034, 045 (09-08); NH-035, 038,
043, 044 (09-09); NH-036, 037, 042, 046, 048, 050 (09-10). **NH-049 comes due tomorrow.** ⚠ Two open
entries (**NH-005, NH-014**) carry no date at all and fall out of any date-based count, so the honest
phrasing is 「13 of 21, and 2 undated」. The twenty-first is **NH-054**, filed today.

**One of them is now costing a readiness line every single lap.** **NH-046 is two days past due.** Its line
**R3** is the only unticked one of the six that CHARTER §14b needs before the P1 block opens, and R11's row
**WFG-024** is held shut behind it. So **one open decision of yours is holding two of the three remaining
readiness lines**, with five days of sprint left.

## Findings, ranked

**1. WFG-233 (P0, KCF, minutes plus one registration). The one sentence in this repository written to be
pasted into the SUBMITTED 작품설명서 explains the weak fold with a number the rest of the same file does not
use, and cites nothing for it.**

`docs/MODEL_CARD.md` §「작품설명서 (Korean writeup) correction mapping」 ends with a 「Suggested replacement
sentence」. That is not a note about the repository: it is text you are told to put into a document five
judges receive. At `:524-525` it reads 「평균 ROC-AUC는 0.89입니다(6개 산불, 범위 0.68–0.97; 0.68은 **탐지
약 17건**의 소규모 산불 폴드입니다)」. Every other place the same file explains the same fold uses a
different quantity: `:52-53` and `:86` both say 「**~8 positives**」, and `docs/fold_sizes.md:10` gives 396
rows / 8 positive cells / 0.26 % of the evidence.

Both numbers are true and they measure different things (whole-fire FIRMS detections against positive 500 m
cells in the fold), which is exactly why it is a defect: the pasted sentence distinguishes them only through
the single word 「탐지」. `README.md:489` is the one surface that writes both and writes them correctly
(「양성 약 8건(탐지 약 17건)」), so the repair already exists in the repository and has simply not reached
the sentence that leaves it.

**The sourcing half is sharper, and it is 「사용된 자료에 대한 출처 명기」, a named criterion of a 20-point
row on both tables.** 「17」 is registered under **no key** of the 453 in `docs/NUMBERS.json`, so
`make verify` never re-derives it, and neither `README.md:489` nor `docs/MODEL_CARD.md:524` names a source.
It does trace, to `data/processed/detection/firms_first_detection.json :: gangneung_2023.n` = 17, reaching
`gk2a_detection_floor.json` through `scripts/gk2a_detection.py:366`. ⚠⚠ **And there is a second 17, in the
directory that must not be cited:** `data/processed/spread_v2/audit.json` gives `detections_csv` 17 and
`detections_in_grid` 17 for the same fire over a different window, and that directory carries
`LEGACY_DO_NOT_CITE.md` with the instruction 「if you are writing a document, do not open these files」. The
two 17s are differently scoped and agree by coincidence, so a lap that sources this number by grepping lands
in the legacy file first.

**Not a `fix-before-next-row`:** `docs/MODEL_CARD.md` is on none of CHARTER §14b's listed surfaces, so it is
a row even though the prose half is minutes. Filed **in table order**, not at position 1 (NH-051).

**2. NH-054 (DECISION, due 2026-09-13). Four fifths of your front door's headline is limits, and no lap is
allowed to decide whether that is the project's strength or its biggest presentation risk.**

Measured on `README.md`'s TL;DR: the bullet headed 「Headline result」 is **2,286 characters**. **433 of them
(19 %)** come before the first ⚠ and state the result, including 「42 of 458 scanned origins reach a refuge
only when the router accounts for where the fire will be, and 2 have no safe walking route at all」. **The
remaining 1,853 (81 %)** are four qualifications: the fire-blind baseline does not separate
knowing-where-it-will-be from knowing-where-it-is; the fair opponent has never been run on 영덕; the arm is
graded on the field it planned on; and whether 42 is an upper bound is open (NH-053). The next bullet exists
only to record a withdrawal.

Nothing there is false and every qualification was added by a lap that was right to add it. What no lap can
settle is the **proportion**, and the two judging lenses split on it by construction: the fire scientist
reads the caveats as the best thing in the repository, and the disaster-response official reaches the end of
the bullet without learning what the tool outputs. `docs/auto/DIRECTION.md`'s standing rule sends a
disagreement about the README lead to you with options rather than to an edit. **Four options, none of which
deletes, softens, hedges or withdraws anything.** Until you answer, no lap edits that lead in either
direction, and the loop's reading is offered as C and is not applied.

**3. WFG-107's sixth recorded instance, and it corrects this routine rather than the product.** Critic #58
published 「P1 is 9 done against **102** `todo`, up one this lap, and mine is the 102nd」 on
`docs/auto/DIRECTION.md`, in `CRITIC_LATEST.md` and in its report. Counted here in one process over every
`| WFG-` row, reading the status as the fourth field from the end so a `|` inside a title cell cannot shift
it: at `d3ca754` (the head it reviewed) P1 `todo` is **106**; at `f4ef66e`, **the commit critic #58 itself
pushed**, **107**; at this head, **108**. `done` was right at 9 both times, so only the `todo` half is
wrong, by five, and 「mine is the 102nd」 names a position WFG-231 never occupied. The number is
load-bearing: it is the evidence NH-038 uses to call the P1 queue a write-only ledger, and the error runs in
the direction that makes that entry's case **stronger**. Corrected on DIRECTION.md, appended to NH-038 and
to WFG-107.

**4. Two stale figures inside an open decision of yours (NH-049).** It says `docs/auto/JUDGE_QA.md` is one
of **six** hashed sources of the printed kit; it is one of **seven** today. It lists three queued judge
questions; **WFG-027 is `done(20260909T1517Z)` and WFG-194 is `done(20260909T0321Z)`**, so the queue is two,
WFG-197 and WFG-228. The entry is a record and was annotated rather than edited (CHARTER §3.7). The
mechanism it describes is unchanged and I re-proved it rather than quoting it.

**5. WFG-232 sized, so the row can be planned rather than guessed.** **289 of 453** registry keys declare
`forbidden_phrasings`, over **27** prefixes. The write side is bound for **two documents and two prefixes**,
so 25 of the 27 have no document scanned for their spellings at all. I re-ran the reader enumeration
**without a `head`**, which is that row's own lesson: five test files and eight scripts; the eight scripts
write the field rather than enforce it.

## The root objection I put to the headline narrative, and why it did not survive as a finding

*「The front door spends four fifths of its headline retracting, and the one affirmative claim left, the
output object, is the only claim in the repository with no measurement attached to it.」*

The cheapest test was to count, and the count is real: 19 % result against 81 % qualification, on the one
bullet that states what the system does. But the second half of the objection is **false**, and I checked it
rather than asserting it: the output object **is** measured, by 42 of 458, by the four registered per-slice
IoUs, and by `docs/oracle_gap.md` §4 cell by cell. So what the count shows is a presentation question and
not an evidence question, which is why it became **NH-054** rather than a finding, and why none of NH-054's
options removes a word.

## Judge drill, and the constraint it hit

Ten hardest cards taken against the files. Nine were answerable from a file, including the two most exposed
to a statistician: **Q5** (six fires, one fold with eight positives) resolves through `docs/fold_sizes.md`
and `lofo_fold_auc_sd`, and **Q7** (random forest beats the shipped model at 0.9142 against 0.8943) resolves
through `data/processed/ml_baselines.json` and admits in its own 없는 것 line that six fires cannot power the
comparison. The tenth, **「IoU 0.394는 무엇에 견준 값입니까?」**, has no answer in any file. It is **WFG-228**,
filed by critic #58 and now the top row on DIRECTION.md.

⚠ **It could not become a Q&A card, for the fifth consecutive lap, and that is NH-049.** The bank is a
hashed source of the printed kit; the first byte written into it turns
`tests/test_printables.py::test_the_newest_printable_is_not_stale_against_the_tree` red until
`make printables` runs, and this routine builds no artifact. Re-proved here: seven sources, **7 of 7**
hashing against the tree.

## What I verified and found clean

- **`gates.py --mode full` exits 0 at `3867860`**: **2024 passed**, 63 skipped, 3 xfailed, pytest 321.1 s
  (up 8 tests and 22 seconds on critic #58's run). `baseline-verify` WARNs on the two git-ignored
  `data/raw/**` contracts, which is NH-029 and CHARTER §3d working as designed.
- **GitHub `auto-gates`, runs 287 to 331: 45 runs, 38 `success`, 7 `cancelled`, ZERO `failure`**, with run
  **331 green at this exact head**. CHARTER §4b sets **no finding #1** for the eighth consecutive lap.
- **All eight dev reports in the window record `Reviewed by:`**, all eight name `subagent`, six of the eight
  record `block`. **Every push carried a report**: `--assert-reported` over the 45 consecutive pushed heads
  from `f9183b0` to `3867860` returns **44 OK, 0 FAIL**.
- **Critic #58's one preemption was cleared and the dev lap was right to refuse the sentence it suggested.**
  Writing 「this measures what the model buys」 into `docs/oracle_gap.md` turns
  `tests/test_oracle_gap.py::test_the_forbidden_phrasings_are_registered_and_absent_from_the_doc` red.
  Re-measured here: 30 `og_yeongdeok_*` keys, all 30 carrying the field with the same seven spellings, and
  「what the forecast buys」 registered on **zero** of them.
- **The window's two closed rows shipped, checked on the built objects rather than the tree.** The kit was
  rebuilt at `20260910T1233Z` and its seven printed sources hash **7 of 7**; the release bundle's 19 entries
  hash **19 of 19**; `timeline_agent_trailer_commits` 513 and `timeline_total_commits` 662 are registered at
  `89da7d3`, `git rev-list --count 89da7d3` answers **662** exactly, and the five phase counts
  (55 + 42 + 99 + 49 + 417) sum to 662 with `commits_outside_phases` = 0.
- **The finals screen's registry payload reads 453 / 395 against 453 / 395 in the tree**, so the 57-entry
  drift critic #24 found is not back, and the screen is 11 commits behind a 30-commit staleness limit.
- **`factchk` on the window's new prose about the world: nothing to correct.** No external citation and no
  URL reached a judge-facing surface; the new URLs are all in `docs/auto/knowledge/` and
  `docs/auto/research/`, each carrying an `[opened]` marker.
- ⚠ **`curl` against `api.github.com` returned HTTP 200 again**, the second lap running, where CHARTER §4
  and WFG-119 both record 403. Recorded, not built on.

## Scorecard deltas

**Track B 95 → 94. Track A 95 → 96. The divergence is a convergence:** 제출 자료 moves on both tracks in
opposite directions and lands on **19 on both**, for a criterion the two tables word identically, ending
three windows in which the two carried different values for track-specific reasons that are now discharged.

- **A 제출 자료 18 → 19**, paying critic #58's own stated condition. That lap gave +1 and not +2 naming two
  reasons (Q38 stale on paper, Q29 carrying none of the 513-of-662 arithmetic); both closed at `ba76b8c`,
  and I verified the object rather than the report.
- **B 제출 자료 20 → 19** on WFG-233. The defect predates this window and is scored anyway, because the
  instruction is to score today's state and a 20 on a 20-point row is a claim that nothing was found.
- **B 데이터 수집·분석·해석 19, HELD.** Critic #58's pre-registration stands unchanged: it returns to 20 when
  WFG-228 lands a null figure with its caveat, whatever the figure says.
- Everything else held: B 연구 목적 18, B 설계와 방법론 20, B 창의성 18; A 개발 목적 19, A 설계와 방법론 20,
  A 구현 및 유용성 20, A 창의성 18.
- **Pre-registered for critic #60:** 제출 자료 reaches **20 on both tracks** when WFG-233 closes with the
  count registered, and falls back to 18 on either track if a kit source drifts against its manifest.

## Direction

**ZERO §3b reorders, the third consecutive critic lap to spend none.** WFG-226 and WFG-229 closed, so
DIRECTION.md's own list advanced by itself: **WFG-228** is the top row, **WFG-233** second, **WFG-230**
third. **ZERO `fix-before-next-row` items**, measured rather than assumed: no gate is red, the kit and
bundle hash clean, and the one minutes-sized defect found is off CHARTER §14b's list of surfaces.
**Readiness 8 of 11, ZERO ticked, the sixteenth consecutive critic lap.**

⚠ **One thing this lap got wrong and fixed before pushing, because it belongs in the record.** My first
draft of the verification table appended to `docs/auto/KCF_READINESS.md` used `| R7 |` and `| R9 |` as its
first cells, and that turned the suite **red**:
`tests/test_printables.py::test_r7_still_enumerates_the_five_printables_this_list_resolves` and
`tests/test_finals_bundle.py::test_r9_still_enumerates_the_contents_this_list_resolves` each assert that
**exactly one** line of that file starts with the prefix, so a report table wearing the same cell shape
counts as a second readiness row. Caught by re-running `gates.py --mode full` **on the commit** before the
push, which is CHARTER §4 step 8 doing exactly the job it was written for, and it is the second lap in two
days where step 8 caught something step 5 could not have seen. The table now reads `| line R7 |`, and the
reason is written into `KCF_READINESS.md` and `CRITIC_LATEST.md` so the next lap does not rediscover it.
⚠ This paragraph was added to the report file after `report.py` ran; the report's own stamp and head are
unchanged and the gate table below was re-run after it.

## In plain terms

**What the judges would see today.** A booth kit whose paper matches the repository exactly (I re-checked
all seven printed documents and all nineteen files on the USB, byte for byte) and a front door that is
honest to a fault: the one paragraph saying what your system does spends four fifths of itself on the
limits. That is not a mistake, but it is a choice, and it is yours to make rather than the loop's, so it is
waiting for you as NH-054.

**What changed since yesterday.** Two things landed that judges will actually touch: the Q&A card about
the 예산 초과 2가구 stopped telling you to volunteer a contradiction the repository fixed four days ago, and
the card about 「what did you build yourself」 now carries the real number (커밋 513개 중 662개, as of
2026-09-09) instead of leaving you to meet it for the first time in front of a judge. Both are on the
reprinted paper. I found one new thing to fix and it is small: the sentence in `docs/MODEL_CARD.md` you are
meant to copy into your 작품설명서 describes the weak 강릉 fold with 「17건」 while the rest of that same file
describes it with 「8건」. Both are true, they count different things, and your README already says it right.

**What you should do.** Answer NH-046 first, and it is one line. It is two days late, it is the only thing
holding two of the three remaining readiness boxes shut, and no amount of work by the loop can move it.
Then NH-054, which is the one real judgement left about how your project introduces itself. Twenty-one
decisions are open, thirteen are past their date, and the sprint ends 2026-09-15.



## Gates

**ALL GREEN** · mode `full` · head `3867860` · 2026-09-10T14:05:16Z · current at `3867860`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 19.4 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.1 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 2.1 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 321.1 s | 2024 passed, 63 skipped, 3 xfailed, 52 warnings in 319.77s (0:05:19) |

## Commits since the previous report (ba76b8c71ad633b2261cfd00c913b664fda34d07..HEAD)

- 3867860 the dev report names the head it ships in, and says which of its two gate runs certifies it
- 60b8975 the reviewer's block: a negative proved by a truncated grep, and the lap that had just caught the same error

## Needs a human (26 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09, one day past; raised to HIGH by critic #55 on a measured third instance)
- NH-036 [DECISION] One critic lap told the next one not to edit a file, and that is what kept a false sentence in front of a judge for a window (by 2026-09-10)
- NH-037 [DECISION] The paper's word proxy now stops it a thousand words before your 25-page rule (by 2026-09-10)
- NH-038 [DECISION] Your "product first" rule has spent the last three dev laps on documents, and the readiness line it was written to protect has not moved in five critic laps (by 2026-09-09)
- NH-039 [DECISION] The national wildfire-spread system's manual is an 18 MB PDF the sandbox could not fetch, and one of you can (by 2026-09-12)
- NH-040 [FYI] A critic lap pushed one commit past a red `--assert-reported`, and it is telling you rather than hiding it
- NH-041 [FYI] This lap sent you an email containing only the word PLACEHOLDER, and could not take it back
- NH-042 [DECISION] Two of your own rules collide whenever a withdrawn claim lives in a frozen artifact, and this week they collided three times (by 2026-09-10)
- NH-043 [DECISION] A gate your loop built this morning will go red about twice a day, and the charter tells the lap that meets it to stop working (by 2026-09-09)
- NH-045 [BLOCKER] The staleness gate has closed `auto/dev` to every routine, and the one routine that met it is the one forbidden to clear it (by 2026-09-08)
- NH-044 [DECISION] The claim the paper just retracted is still live on the page the paper cites for it (by 2026-09-09)
- NH-046 [DECISION] Your product's definition-of-done names a command nothing in this project has ever run (by 2026-09-10)
- NH-048 [DECISION] One of the research routine's three literature channels has been dead for two runs, and a working replacement is already proven (by 2026-09-10)
- NH-049 [DECISION] Your critic routine is told to add judge Q&A cards and your own printing gate makes that impossible for it (by 2026-09-11)
- NH-050 [DECISION] You answered two of these questions two days ago and the loop never heard you, because you answered them on the routine page (by 2026-09-10)
- NH-051 [DECISION] The rule your loop has been obeying for three days is not the option it names, and the difference is why one row has been pushed down five times (by 2026-09-11)
- NH-052 [DECISION] The experiment that measures what your own forecast is worth is now one run away, and it would change what 42 means (by 2026-09-12)
- NH-053 [DECISION] The word your front door uses for 42 stopped being right today, and the loop cannot pick its replacement (by 2026-09-12)
- NH-054 [DECISION] Four fifths of your front door's headline is limits, and no lap is allowed to decide whether that is the project's strength or its biggest presentation risk (by 2026-09-13)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
