# WildfireGuardian autoloop · dev · 2026-09-09T2206Z

| | |
|---|---|
| when | 2026-09-09 22:06 UTC · 2026-09-10 07:06 KST |
| branch / head | `auto/dev` / `ada022a` (pushed; `4b580df` is the claim commit report.py stamped this header from, the work is `fef4c71` and the rebased board `ada022a` — all three resolve) |
| kind | dev |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 9, done: 33, dropped: 1, parked: 1, title-derivable: 1, todo: 108 |

## What happened this lap

Row: **WFG-212** (P0, KCF) — "The front door's newest and largest section states no result
in this project's favour, and has no lead sentence a judge can carry away."

## Your decisions, applied

**Nothing new on either channel this lap.** The Gmail search
(`from:siyeong0318@gmail.com subject:"WildfireGuardian autoloop" newer_than:14d`) returns
only this loop's own outgoing reports — every matching thread holds exactly one message
and it is the report itself — and PR #31 carries no comments at all. **25 NEEDS_HUMAN
entries remain open**, several now past their stated due date; the block at the top of
this email lists them with their reply lines.

## What I did, and why

`docs/auto/DIRECTION.md` named WFG-212 as the next row after critic #53's one §3b reorder,
and `CRITIC_LATEST.md` set **no** `fix-before-next-row` item this lap, so the table
decided. Claimed at `4b580df`, pushed before building. No stale `in-progress` claim
existed to release.

The defect the row names is 「자료의 논리적 구성」, a criterion worth 20 points on both KCF
tables. Re-measured at this head before building, and the row's figures still hold: the
Round-4 section carries **11** ⚠ markers and **28** negative-framing tokens against **one**
affirmative token, and that one is the sentence saying the model-free opponent scored
better than the repository had recorded. A judge reading the front door for sixty seconds
left with the caveats and not the contribution.

**The fix.** The section now opens with a lead block that states, in the repository's own
caveats-first register, the one claim that survives Round 4 — the **output object**: a
per-point 「walk out or be rescued」 verdict with its remaining time, printed as real
dispatch documents that are committed to this repository — followed by the bounds on that
claim in the same block. It is written as an **existence claim, not a performance claim**,
so that opening a file settles it. It adds material and removes no ⚠ line.

**The root objection I recorded against my own plan (`hate`).** *Every headline claim this
repository has written on this front door has been narrowed or withdrawn within days* —
the fire-blind baseline, the buffer 「봉우리」, 「어느 심사용 자료에도」, the meaning of the
42. A new affirmative lead paragraph is a fresh surface for exactly that failure, written
by a lap that cannot measure whether the claim is true; it can only measure that files
exist. The discipline I took from it: the lead may assert only what a committed file
settles, and the gate enforces that *inside the lead block*.

**And the objection was right, twice, in this very lap.** Both the cold reader (`shower`)
and the independent reviewer found the same overclaim in my first draft, independently:

> The lead ended one sentence with two links — `outputs/dispatch/README.md` and
> `docs/real_roads_real_hazard.md` — and **two artifacts side by side are a claim about
> their conjunction**. The committed dispatch sheets come from the rescue-routing
> pipeline, whose `provenance.sources` says in as many words `hazard: synthetic,
> terrain: synthetic, origins: sampled candidates`. The run where the walking graph and
> the spread surface are *both* real is a different execution that produces four-way
> verdicts and **no dispatch documents**. So the sentence read as 「real roads + real fire
> → these A4 sheets」, which does not exist in this repository, and 「공개 데이터만으로
> 만들어져」 was false of the object being linked.

Corrected in the shipped block: the claim sentence no longer says 「공개 데이터만으로」, the
first bound now names exactly what was real (OSM 보행·차량 도로망, 대피소, 차고지) and what
was synthetic (화재 위험면, 지형) in the run that made the sheets, and states plainly that
**실제 확산면으로 만든 출동 지시서는 아직 없습니다**. 「가구 단위」 became 「지점 단위」 with a
bound saying the origins are sampled coordinates, because real elderly-household locations
are not public.

## The reviewer blocked, and it was right about the gate too

**`Reviewed by: subagent (block): the lead asserts a conjunction no run produced, and the
new gate cannot catch that or any other softening because it only pins literal substrings
the same lap wrote after writing them.`**

The second half is the more useful half. Asked to prove the gate could be beaten, the
reviewer beat it **three times with the whole suite green (1867 passed)**:

1. it inverted the section's 「읽는 법」 note from an audit into a pitch (「유리한 항목은
   다섯 항목 전부입니다 … 신뢰도를 **높여 주는** 결과」) while keeping the pinned prefix
   「이 절에서 이 프로젝트에」;
2. it appended an unsourced operational-readiness and precedence sentence to the lead
   (「현장 담당자가 그대로 사용할 수 있는 수준으로 검증돼 있으며, 국내에서 이만한 산출물을
   내놓은 사례는 확인되지 않았습니다」) — no ban listed it, because nobody had written it;
3. it hedged the measured dispatch-ordering zero into 「확실하게 이긴 적이 **아직 없을 뿐**,
   실무에서는 **더 나은 순서일 수 있습니다**」, with the pinned fragment 「이긴 적이」 sitting
   intact inside it.

**A pinned quotation constrains the letters, not the sentence** — the same anti-pattern
`docs/auto/MEMO.md` named yesterday about `_UPPER_BOUND`, committed again by the lap that
had just read it. Each is now a **property** assertion instead: the note must keep its
**direction** (「깎는」, and 「유리한 결과는 … 없습니다」); the claim half has a **ceiling** of
five sentences, because no wording ban catches a sentence nobody has written yet; and the
bullet carrying the adverse result must state its count and carry **no hedge**. All three
exploits are kept verbatim in `_REVIEWER_MUTATIONS` as regression data and each was re-run
against the shipped README: **each goes red, and the restored tree is green.**

## What did not work, and one thing I changed my mind about

- The first gate's three mutation tests only deleted the four literal tokens the gate
  banned. That is `mandela` pattern #4 — the scorer grading buckets it drew itself — and
  `.auto/claims.md`'s claim that the gate "binds both directions" was an overstatement
  until the reviewer's exploits were folded in.
- I first **filed** the stale 「읽는 법」 note as a new row (WFG-221) rather than fixing it,
  on the reasoning that every available correction widened this project's own favourable
  count on the front door and so needed the author. The reviewer called that indefensible
  while I was adding twenty-four lines of lead prose, and it was right — but the resolution
  is not the one it implied. There **is** a correction that runs the other way: the only
  **results** in the section are items 1 and 3, and both cut against this project; items 4
  and 5 are inventories and item 2 is a device. The note now says exactly that and ends
  **「이 절에 이 프로젝트에 유리한 결과는 없습니다」**, which is stricter than the 「4번뿐」 it
  replaces. WFG-221 is closed in the same lap with that reasoning recorded.
- One mechanical miscount fixed beside it: 「아래 **네** 항목」 → 「**다섯**」, five numbered
  items since WFG-207.
- A loop mechanic, learned the hard way: the reviewer restored `README.md` with
  `git checkout --`, which reverts to `HEAD` and briefly deleted this lap's entire
  uncommitted lead block. Nothing was lost only because the work had been copied to the
  session scratchpad first. Its `--mode quick` run also overwrote `.auto/gates.json`, so
  the full gates were re-run afterwards. Both are in MEMO.

## Evidence

- `README.md` — the Round-4 lead block (claim + bounds) and the re-stated 「읽는 법」 note.
- `tests/test_readme_round4_lead.py` — nine assertions; `_REVIEWER_MUTATIONS` carries the
  three exploits verbatim; the module docstring records what the gate still cannot catch
  (it reads `README.md` only, and an arbitrary rewording still escapes, as
  `tests/test_future_aware_attribution.py::test_a_reworded_overclaim_still_escapes`
  already declares as an xfail).
- `data/processed/rescue_routing.json` → `provenance.sources` — the source of the
  synthetic-hazard correction. Nothing in `data/` was touched.
- `docs/auto/MEMO.md` — two lessons: the conjunction claim, and the pinned quotation.
- `gates.py --mode full` exits **0** on the corrected tree: **1871 passed, 64 skipped,
  3 xfailed**. `baseline-verify` WARNs only on the two git-ignored `data/raw/**`
  contracts, which is NH-029 and CHARTER §3d working as the author chose.

## Next row

**WFG-218 and WFG-220** (both P0, KCF), and a lap that takes one should take both:
`DIRECTION.md` notes they share `make finals` on a commit already on `origin` plus a
re-pointed `release/kcf-finals-2026/MANIFEST.json`, so the mechanic is paid once if they
ship together. WFG-214's residue is one link in `paper/manuscript.md`, which is the paper
routine's file and is budget-blocked on NH-037.

## In plain terms

- **What changed for the project.** Your README's biggest section used to list only the
  things Round 4 knocked down. It now opens by saying what your project actually claims —
  that the *output* is the contribution, and the real dispatch sheets are sitting in the
  repository for a judge to open — with the limits of that claim right underneath it, and
  a test that stops any future version from quietly dropping one of those limits.
- **Why it matters to the judges or the science.** 「자료의 논리적 구성」 is 20 points on
  both scoring tables, and a judge who reads your front door for one minute now leaves
  with a sentence they can carry away instead of only a list of caveats. It also caught a
  real mistake of mine before it reached them: I had implied the printed dispatch sheets
  were made with the *real* fire, and they were not — that run used a synthetic fire, and
  the page now says so out loud.
- **What the author should do.** Nothing for this row. But 25 decisions are still open and
  several are past their due date — NH-032 and NH-034 in particular still forbid the
  student from saying any margin number out loud to a judge. One reply with a few
  `NH-###: <answer>` lines clears them.



## Gates

**ALL GREEN** · mode `full` · head `4b580df` · 2026-09-09T22:05:19Z — a real pass, but on a tree holding neither this report, nor the reviewer's corrections that followed it, nor the rebased board. **The pushed head is `ada022a`**: `gates.py --mode full` exits 0 there (1871 passed, 64 skipped, 3 xfailed, 466.6 s) and `--assert-head` and `--assert-reported` both exit 0 on it. Both runs are named because a report that quietly swaps one verdict for another teaches the next lap nothing.

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 25.7 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.2 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 0.5 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 465.5 s | 1871 passed, 64 skipped, 3 xfailed, 52 warnings in 463.87s (0:07:43) |

## Commits since the previous report (ba06467e4898b94e675901bb97edd1ef8f316bdc..HEAD)

- 4b580df claim WFG-212 (20260909T2118Z)
- a442aa0 the critic report names the head it ships in, and keeps the run it describes
- 08edf76 critic #53: the day fixed four surfaces that say what 42 means, and the fifth is the one five judges stand in front of

## Needs a human (25 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09)
- NH-036 [DECISION] One critic lap told the next one not to edit a file, and that is what kept a false sentence in front of a judge for a window (by 2026-09-10)
- NH-037 [DECISION] The paper's word proxy now stops it a thousand words before your 25-page rule (by 2026-09-10)
- NH-038 [DECISION] Your "product first" rule has spent the last three dev laps on documents, and the readiness line it was written to protect has not moved in five critic laps (by 2026-09-09)
- NH-039 [DECISION] The national wildfire-spread system's manual is an 18 MB PDF the sandbox could not fetch, and one of you can (by 2026-09-12)
- NH-040 [FYI] A critic lap pushed one commit past a red `--assert-reported`, and it is telling you rather than hiding it
- NH-041 [FYI] This lap sent you an email containing only the word PLACEHOLDER, and could not take it back
- NH-042 [DECISION] Two of your own rules collide whenever a withdrawn claim lives in a frozen artifact, and this week they collided three times (by 2026-09-10)
- NH-043 [DECISION] A gate your loop built this morning will go red about twice a day, and the charter tells the lap that meets it to stop working (by 2026-09-09)
- NH-045 [BLOCKER] The staleness gate has closed `auto/dev` to every routine, and the one routine that met it is the one forbidden to clear it (by 2026-09-08)
- NH-044 [DECISION] The claim the paper just retracted is still live on the page the paper cites for it (by 2026-09-09)
- NH-046 [DECISION] Your product's definition-of-done names a command nothing in this project has ever run (by 2026-09-10)
- NH-048 [DECISION] One of the research routine's three literature channels has been dead for two runs, and a working replacement is already proven (by 2026-09-10)
- NH-049 [DECISION] Your critic routine is told to add judge Q&A cards and your own printing gate makes that impossible for it (by 2026-09-11)
- NH-050 [DECISION] You answered two of these questions two days ago and the loop never heard you, because you answered them on the routine page (by 2026-09-10)
- NH-051 [DECISION] The rule your loop has been obeying for three days is not the option it names, and the difference is why one row has been pushed down five times (by 2026-09-11)
- NH-052 [DECISION] The experiment that measures what your own forecast is worth is now one run away, and it would change what 42 means (by 2026-09-12)
- NH-053 [DECISION] The word your front door uses for 42 stopped being right today, and the loop cannot pick its replacement (by 2026-09-12)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
