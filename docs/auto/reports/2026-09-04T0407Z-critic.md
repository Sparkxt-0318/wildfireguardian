# WildfireGuardian autoloop · critic · 2026-09-04T0407Z

| | |
|---|---|
| when | 2026-09-04 04:07 UTC · 2026-09-04 13:07 KST |
| branch / head | `auto/dev` / `b855943` |
| kind | critic |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 7, done: 10, parked: 1, todo: 35 |

## What happened this lap

Critic lap #6. Scope `5a0466e..b855943`, ten commits: the two manual paper laps critic #5
explicitly left unreviewed, critic #5's own report, the F26 repair, the new decisions
machinery, and the 0255Z dev lap that cleared F21, F22 and F24. No code, no artifact and no
number changed in this lap; writes are confined to `docs/auto/`.

**Gates, re-run independently at `b855943`: `gates.py --mode full` exits 0.** `1240 passed,
62 skipped` in 205 s; verify PASS, snapshot-verify PASS, env-check PASS, `baseline-verify`
WARN as expected off-laptop. `check_paper.py` exits 0. Third consecutive critic lap opening
green, and the first that stayed green for the whole run.

## Your decisions, applied

**Nothing new.** The Gmail search
`from:siyeong0318@gmail.com subject:"WildfireGuardian autoloop" newer_than:14d` returns 19
threads, every one of them a report this loop sent to you, each a single message with no
reply on it. PR #31 has no comments. `docs/auto/decisions_seen.json` does not exist yet, so
no decision has ever been applied through the new machinery. Eleven entries are waiting for
you, and the reply lines are at the foot of this report.

That machinery is also this lap's second finding, before it has been used once: see F28.

## Findings, ranked

### F27 (fix-before-next-row, WFG-053, P0) — the booth card says the satellite was slower than the telephone; this project's own paper says the measurement cannot say that

Asserted, all judge-facing: `docs/auto/finals/DETECTION_FLOOR_CARD.md:17-19` (the card's
front, in bold, 「위성은 사람보다 느렸습니다 … 신고보다 +22분 · +34분 · +64분」) and `:11-13`;
`docs/detection_floor.md:29-38` (§1), `:240-252` (§9 평결), `:262-275` (§10's trigger-priority
table, which ranks 사람 신고 first *because of* the ordering); and
`docs/auto/JUDGE_QA.md:234`, inside **Q10 — a T0 answer the drill table tells the student to
know without paper**.

Withdrawn, in the same repository, in this window: `paper/manuscript.md:492-497` and `:533`
— 「no artifact supports that and this paper does not claim it」, 「Whether that is ahead of
or behind the emergency call, this measurement cannot say」 — plus `paper/GAPS.md` G5 and
NH-019.

The paper is right, and this needs no external source. `docs/data_provenance/fire_manifest.json`
carries `start/end/reported_ha are provenance only` for all four detection fires, and every
one of their notes describes the event start as the **ignition** (`first hit … may lag
ignition`). Not one entry contains 신고 or any report-time language. For `yeongdeok_2025`
`start` is `2025-03-22T12:15:00+09:00` and the same note reads `first hit (2025-03-25) lags
the 2025-03-22 ignition by days` — the manifest names that date the ignition, in the field
the delays are measured from. So §1's reading is not merely unsourced, as NH-019 put it: the
one artifact it cites labels the field the other way. The caveat inherits the error too — the
card tells a judge the figures flatter the satellite *because* the clock is a report time,
and under the manifest's reading there is nothing to flatter.

The fix is a narrowing and the paper has written the words: delays against the recorded
occurrence time, no ordering claim, the size floor carrying the booth answer because it holds
either way. No number moves, so `tests/test_detection_floor_card.py`'s 17 bindings must still
pass untouched. NH-019 stays open for the stronger claim.

### F28 (fix-before-next-row, WFG-054, P0) — a reply the loop cannot map is discarded, and the message is marked read

`scripts/auto/decisions.py:100-104` appends the Gmail message id to `decisions_seen.json`
unconditionally, outside any check that `apply_one` recorded anything; `:69-71` returns the
text unchanged with `no such entry; recorded nowhere`; `:115-119` then reports the whole
message `seen`. Reproduced in process against the live file without writing it:

    apply_one(text, "NH-020", "yes, do it", …)
      -> "NH-020: no such entry; recorded nowhere"
      -> text changed?  False        the author's words appear anywhere?  No

A reply naming an id with a typo, or an entry a later lap has not written yet, is written
nowhere and never read again; a reply with one good line and one bad one marks the whole
message read after applying only the good one. The module's own docstring at `:15-16`
promises the opposite. Nothing has been lost yet only because no reply has arrived — the
first one is what would find out. This is NH-017's failure class rebuilt in code.

### F29 (fix-this-sprint, WFG-055) — the paper's page limit is a word proxy nobody calibrated, and the loop's own recount says it is already breached

`check_paper.py` reports `body_words: 7479` against `LIMIT = 7500` — 21 words of headroom, so
the next lap that adds a sentence fails the gate. The paper lap declared that itself. The
layer under it is the finding: CHARTER §12 states the constraint in **pages**, the gate
measures **words**, and the conversion has never been checked. The paper lap's own recount of
the built `.docx` lands nearer **21 pages**. `check_paper.py` also checks no section against
§12's list, which absorbs WFG-045.

### F30 (fix-this-sprint, WFG-051 raised P1 → P0) — one figure, three agencies, and it has now reached the paper

사망 26명 is a 중앙재난안전대책본부 count in the registry, a 경상북도 재난안전대책본부 count
in `docs/data_sources.md:190`, and 「the provincial disaster headquarters' count」 in
`paper/manuscript.md:36-38` — new this window. `README.md:193-199` still puts the figure under
the 아시아경제 link that critic #5 opened and found carries no death figure at all. Critic #5
filed the gate half at P1; three attributions in three judge-facing documents makes it P0.

### F31, F32, F33, F34 (fix-this-sprint)

- **F31** — `KCF_READINESS.md` R8 still named the 「43 %」 sentence as blocking, eight commits
  after it was deleted. Fixed in this lap's commit; recorded because a stale ☐ is
  indistinguishable from a real one. **The number that matters more: 2 of 11 lines ticked,
  eleven days of sprint left.**
- **F32** — `gates.py --assert-reported` reads `origin/auto/dev` at push time and writes
  nothing, so this lap could not verify its own prompt's step 2 (「every push carried a
  report」). The one check meant to make pushes auditable is the only thing here that cannot
  be audited. WFG-056: append a `{utc, base, head, verdict}` line to a committed ledger.
- **F33** — critic #5's two recommended Q&A questions were never added; `JUDGE_QA.md` had not
  changed since `fbe71de`. Added this lap as Q30a and Q30b. A critic recommendation that
  names no backlog row is one the next lap does not see.
- **F34** — a question numbered `Q10a` is invisible to every test in
  `tests/test_judge_qa_bank.py` (`QUESTION_RE` matches `Q(\d+)`), including the stated
  anti-padding guard. Five such questions now exist. WFG-057.

**Verified closed from critic #5, in place rather than on the report's word:** F21 (the
「43 %」 sentence is gone from both languages and the tripwire now scans both whole
paragraphs), F22 (`--diff-filter=A` is in and is load-bearing), F24 (the retired disclosure
document is archived), F26 (the collisions are annotated and the branch is green). F11 is
absorbed into F29; F12 (`report.py` has no `paper` kind) is open and unchanged.

## The root objection

Critic #5 said the failure class had moved: numbers are bound, and the reasoning *about* the
numbers is not. The loop fixed that instance better than it was asked to — the lap checked
its reviewer's argument, found one premise wrong, and wrote 산림청's own words into the
record instead of the critic's. And in the same twenty-four hours it shipped the identical
class on a bigger surface: the sentence 「위성은 사람보다 느렸습니다」 is the front of the one
finished evidence card, the verdict of the design document, the basis of its trigger-priority
table, and the body of a T0 answer. Every gate in this repository passes on it, because every
gate reads a *value*. Not one reads what the sentence around the value asserts. Two windows
running, the one wrong thing a judge would catch is a sentence rather than a number — and in
both cases the loop had already written the correction down somewhere else and left the
judge-facing copy standing.

**Cheapest test, ten seconds:**

    grep -n "신고" docs/auto/finals/DETECTION_FLOOR_CARD.md docs/auto/JUDGE_QA.md docs/detection_floor.md
    grep -n "recorded occurrence" paper/manuscript.md

## Scorecard deltas

Track B **78 → 79**; Track A **69 → 70**. Both moves are the same one: 연구 목적 / 개발 목적
15 → 16, the first 16 this table has recorded, for the deleted ratio sentence and for how the
lap checked the critic before deleting it.

Held, with reasons in `docs/auto/SCORECARD.md`: 데이터 18 (the paper's §4.7 is a real new
result and the same window leaves two answers to the question it asks — the two cancel);
제출 자료 15/13 (five excellent new references against one figure with three agencies and a
length gate measuring the wrong quantity); 설계 15 and 창의성 15 (no model, split, metric,
arm or coupling moved — said explicitly so a 938-line manuscript rewrite is not read as
scientific progress).

**구현 및 유용성 11, seventh consecutive window.** This is now the report's second finding
rather than a note.

## What this lap wrote

`docs/auto/` only: five new backlog rows (WFG-053 to WFG-057) with details, WFG-051 raised to
P0, WFG-021's unrunnable part parked so it stops reading as a `todo` above WFG-017, a note on
NH-019 with the manifest evidence and an `**Options:**` line, three JUDGE_QA additions (Q10c
marked 「근거 없음」, Q30a, Q30b), two scorecard rows and their window note, the R8 and R2
corrections in `KCF_READINESS.md`, `CRITIC_LATEST.md`, and this report.

## In plain terms

**What the judges would see today.** A project whose paper is careful and whose booth card is
not. The card you would hand a judge says the satellite noticed the fire after somebody
phoned it in. Your own paper, written last night, says we cannot actually know that, because
nobody wrote down what time the phone call was. Both sentences are in your repository right
now, and the card is the one a judge reads.

**What changed since yesterday.** The opening paragraph of the README is finally finished:
the wrong 「43 %」 sentence is gone, every figure has a source you can open, and one lap even
double-checked the critic and found the critic wrong about part of it. That is worth a point
on both scorecards and it is the first time this project has scored 16 on anything.

**What you should do.** Nothing urgent, and nothing on this needs you — the loop can fix the
card by itself and it is the next lap's first job. But eleven questions are still waiting for
your one-line answers, and the one that would help most today is NH-016 (the schedule). The
biggest risk is not in this report at all: the finals screen has not been touched in seven
laps, and 2 of the 11 readiness lines are ticked with eleven days left.



## Gates

**ALL GREEN** · mode `full` · head `b855943` · 2026-09-04T03:53:01Z · current at `b855943`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 19.0 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.2 s | make: *** [Makefile:152: baseline-verify] Error 1 |
| snapshot-verify | PASS | 2.0 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 206.6 s | 1240 passed, 62 skipped, 52 warnings in 204.66s (0:03:24) |

## Commits since the previous report (-15)

- b855943 after the rebase: the board is rebuilt over both laps, and NH-018's option B says it is done
- 28b4c38 review block taken: the correction gets the newspaper that actually printed it
- e5aaaa7 critic #5: the paragraph stops printing a ratio, and a modified report stops counting as one
- 5ffacd0 decisions by email reply: the loop reads the author's answers and records them with the message id
- 130bab7 F26: the branch is green again, on a pragma rather than a rewrite
- f6f34ae report: the plain-terms section names the red branch, which arrived after it was written
- ed2b267 critic #5, addendum: the branch is red, and not on anything in this lap's window
- 5f39452 critic #5: the figures are finally right and the sentence about them is not
- 2a02477 report: the plain-terms line pointed the author at the wrong NEEDS_HUMAN row
- b621441 the reviewer blocked on the word "reported", and it was the load-bearing word
- 5a0466e after the rebase: the registry gate fits the paragraph as the cloud lap left it, and the two disagreements go to the author
- 98557b9 the README's opening figures have keys, a gate, and a push check that sees prose-only commits
- d2f314d the share was a framing, the nationwide total was a different season, and the figures finally have a gate
- fbe71de critic #4 was right: the fix understated the fire by 54,000 ha, and the ledger is gone
- 58d4fa7 claim WFG-043: the opening paragraph is the row the critic sent back twice

## Needs a human (14 open)

- NH-001 [DECISION] Email delivery needs three repository secrets
- NH-002 [DECISION] Optional: `@claude` on GitHub issues and PRs
- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-010 [DECISION] Expert consultations and the firefighter record (by 2026-09-20)
- NH-011 [DECISION] One real, recorded email send from a network that works in Shanghai (by 2026-09-20)
- NH-012 [DECISION] Portal downloads the loop cannot do (by 2026-10-01)
- NH-013 [FYI] Optional: a stable web address for the visual board
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-016 [DECISION] The critic routine has 30 minutes to land its findings, and needs about 40 (by 2026-09-05)
- NH-017 [DECISION] Three entries were closed on replies the repository cannot see, and one of them was wrong (by 2026-09-06)
- NH-018 [DECISION] Two laps read the same primary sources and disagree on two sentences (by 2026-09-08)
- NH-019 [DECISION] One report time, for one fire, decides what the detection result is allowed to say (by 2026-09-08)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
