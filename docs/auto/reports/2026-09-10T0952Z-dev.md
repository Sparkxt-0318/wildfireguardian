# WildfireGuardian autoloop · dev · 2026-09-10T0952Z

| | |
|---|---|
| when | 2026-09-10 09:52 UTC · 2026-09-10 18:52 KST |
| branch / head | `auto/dev` / `e043c1f` |
| kind | dev |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 9, done: 33, dropped: 1, parked: 1, title-derivable: 1, todo: 107 |

## What happened this lap

Row **WFG-225** (P0, KCF), claimed at `53d1a4e`, built at `e043c1f`. Plus critic #57's one
`fix-before-next-row` item, which travelled in the same commit so the independent reviewer saw it.

## Your decisions, applied

**Nothing new reached me this lap, on either channel.** The Gmail search
`from:siyeong0318@gmail.com subject:"WildfireGuardian autoloop" newer_than:14d` returns 25 threads and every
one of them is a report this loop sent; each thread holds exactly one message, so no reply is threaded under
any of them. A second search for `subject:Re WildfireGuardian autoloop newer_than:14d` returns nothing. PR #31
has zero comments. Nothing was applied with `decisions.py` because there was nothing to apply, and
`docs/auto/decisions_seen.json` is unchanged.

**What is still waiting on you, counted here rather than inherited.** `docs/auto/NEEDS_HUMAN.md` holds
**20 open entries for you** (19 DECISION + 1 BLOCKER; 5 FYI besides), and **13 of them are at or past the
date they name**: NH-032, NH-034, NH-045 (due 09-08), NH-035, NH-038, NH-043, NH-044 (due 09-09), and
NH-036, NH-037, NH-042, NH-046, NH-048, NH-050 (due **today**). ⚠ A note on that 13, because this loop has
now published three different numbers for it: my first count returned **12**, and it was wrong.
`^## NH-\d+ · … \(by (\d{4}-\d{2}-\d{2})\)` misses **NH-035**, whose header reads
`(by 2026-09-09, one day past; raised to HIGH by critic #55 …)` — the date is followed by prose inside the
same parenthesis. Critic #57's 13 is the right number; a strict parser over hand-written headers is not a
census, and this is the second time in two laps that a generated count beside hand-written prose has been the
defect (WFG-107's shape).

## What I did, and why

**The row.** `web/finals.html` is the screen five judges stand in front of. `renderPanel()` loops
`DATA.buckets` and writes one row per bucket as mark + Korean label + `r.counts[b.key]`, so the region panel
prints **◆ 예산 초과 2** for `uiseong_andong_2025` and **3** for `uljin_samcheok_2022`; `DATA.default_region`
is `uiseong_andong_2025`, so the 2 is on the first panel a judge sees, and the bucket is a map legend entry
besides. Both counts were qualified six hours before this lap on `README.md:147-150` (re-measured in this clone; the
row inherited `:145-149`, and the block moved by two lines) and
`docs/multi_region.md` §3.1 — the two arms are scored under different time rules, `classify()` in
`scripts/run_real_roads_real_hazard_slope.py` passes `time_budget_min` to the future-aware router and no
budget at all to `naive_route`, and under one rule the Uiseong-Andong bucket is empty. The screen carried
none of it, and `tests/test_budget_rule_asymmetry_is_stated.py:63-64` graded those two documents and nothing
else, so the gate written to keep the correction honest could not see the screen.

**The objection I recorded against my own plan, before building** (`hate`). *The row's `Done when` asks for a
card in the 알려진 한계 panel, and a card there would have satisfied the row and left the defect standing.*
The 알려진 한계 cards are in the **신뢰성 view**; the count is printed in the **지역 패널**. A judge renders
one view at a time, so a card in the other one is a correction on a page the reader of the number is never on
— `WC-004`'s exact shape (a fix that reached one card and left the claim standing eight sections away) with a
view boundary in place of eight sections. The cheapest test of it was to ask where the reader is looking at
the moment the wrong impression forms, and to put a pointer **there**.

So the caveat landed in two places, not one:

1. **`renderPanel()`** prints a pointer inside the same block as the bucket rows, between the bucket loop and
   the panel's hairline rule, guarded on a non-zero count (Yeongdeok's is 0 and a caveat on an empty bucket
   is noise). One sentence: the counts were measured with different time rules on the two arms, the
   fire-blind arm carries no budget, see the card of the same title.
2. **A `rel(...)` card** in the 알려진 한계 panel, second on the panel behind WFG-220's oracle card, because
   it is the only other card there that qualifies a number the map and the panel actually **print**. It gives
   the mechanism (`classify()`), says out loud that this is a count running against the project measured with
   a rule that inflates it, states that under one rule the Uiseong-Andong bucket is empty and why, and refuses
   to extend that to Uljin-Samcheok, which has not been re-scored.

Both went into `scripts/finals.template.html` and reached `web/finals.html` through `make finals` (WFG-109's
lesson), and `release/kcf-finals-2026/MANIFEST.json` was re-pointed with `build_finals_bundle.py --update`.

**What the card does not do.** It writes **no number at all**. `mr_uiseong_fa_exceeds_budget` stays 2 and is
untouched (NH-031 option A; the registry half is WFG-122, still `todo`). No margin value reaches the screen
while NH-032, NH-034 and NH-052 are open. The word 상한 is not settled (NH-053).

**One thing I did differently from the row's own instruction, deliberately.** The card names Uiseong-Andong
and Uljin-Samcheok by reading `DATA.regions[<key>].label` rather than by typing the Korean names.
`scripts/check_region_literals.py` is right that a region name typed into a screen string is a defect class
this repository has already shipped four times, and the labels genuinely belong to the payload.

**The gate.** `tests/test_budget_rule_asymmetry_is_stated.py` goes from **20 to 51** assertions and now grades
four surfaces: the two documents it already had, plus `scripts/finals.template.html` and `web/finals.html`,
each read as a `{ … }` block rather than as a file. Adjacency is graded **separately** from the card's
content, because they fail independently — which the mutations then demonstrated.

**Mutation grading, reported as it came out** (in the test's docstring too):

| mutation | red |
|---|---|
| M5 · card deleted from the template only | **15** |
| M6 · card deleted from both files | **29** |
| M7 · card lead flipped 「서로 다른」 → 「같은 시간 규칙」, both files | **4** |
| M8 · the `renderPanel()` pointer deleted, both files | **2** |
| M9 · that pointer flipped to one rule and a budgeted fire-blind arm | **2** |
| M10 · payload-read region label replaced by a typed 「의성·안동」 | **0** |

Restored tree: 51 passed.

**What did not work, and what it cost.** Two things.

*First, my denial pattern went red on the correct tree.* The obvious inverse of this card is 「두 팔에 같은
규칙」 — but the card's own required counterfactual sentence is 「두 팔에 **같은 규칙을 적용해 다시 재면**
… 버킷은 비어 있습니다」. Anchoring the denial on those words refuses the sentence the card must contain. It
is anchored on the **lead** instead. This is the previous lap's MEMO rule (cut the denial from the document,
not from the claim in your head) arriving one lap later in a new costume, and again it was running the
mutation that found it, not reading the regex.

*Second, M10 scores zero and I did not close it here.* Replacing the payload-read label with a typed
「의성·안동」 leaves the region **key** on the `const uaKey` line, so `SCREEN_REQUIRES` stays satisfied. The
gate that owns a typed region name is `scripts/check_region_literals.py`, and I ran the same mutation through
it: it exits **1** on `scripts/finals.template.html:2101`. Recorded in the docstring rather than fixed,
because a second ratchet over the same string is how two ratchets come to disagree. ⚠ Honest limit: that
check's `SCOPE` covers the template and **not** `web/finals.html`, which is derived from it.

**The preemption** (critic #57's one `fix-before-next-row` item, minutes-sized, so honoured here under
CHARTER §14b as amended by NH-038 B). `docs/oracle_gap.md` §2 credited `scripts/run_forward_sim_region.py`
with writing `data/processed/routing_demo_canonical.npz`. Measured in this clone rather than read from the
report: `grep -c routing_demo_canonical scripts/run_forward_sim_region.py` → **0**;
`scripts/build_canonical_hazard.py:109` carries the `--npz-out` default and `:177` the
`np.savez_compressed`. The leave-one-fire-out claim the row makes is **true** at that script —
`:130` reads `IgnitionModelV2(seed=args.seed).fit(ds[ds["fire_id"] != args.fire])` and `:129` prints
「fitting leave-the-target-fire-out」. So the pointer was corrected and the claim was **not** softened and
**not** withdrawn: no `WC-###` is opened, because nothing was withdrawn. NH-053 quotes the old sentence
verbatim and is record class, so it gained a dated bracketed annotation beside the quote instead of an edit.

## Evidence

- `scripts/finals.template.html` — the `renderPanel()` pointer and the `rel(...)` card
- `web/finals.html` — rebuilt by `make finals`; `check_screen_assets.py web/finals.html` PASS
- `release/kcf-finals-2026/MANIFEST.json` — re-pointed, 19 files
- `tests/test_budget_rule_asymmetry_is_stated.py` — 51 assertions, mutations M5–M10 in the docstring
- `docs/oracle_gap.md` §2, `docs/auto/NEEDS_HUMAN.md` NH-053 — the preemption and its record annotation
- `docs/auto/MEMO.md` — this lap's lesson

**What this change did NOT have to touch, checked rather than assumed.** `web/finals.html` is **not** one of
the seven hashed `SOURCES` of `docs/auto/finals/printables/manifest_20260910T0140Z.json` (they are
`BOOTH_SETUP.md`, `DEMO_SCRIPT_5MIN.md`, `JUDGE_QA.md`, `submission_reconciliation.md`,
`DETECTION_FLOOR_CARD.md`, `creativity_card.md`, `RELATED_WORK_PANEL.md`), so this lap needed no
`make printables` and no new kit stamp, and
`tests/test_printables.py::test_the_newest_printable_is_not_stale_against_the_tree` is green (NH-049 was not
touched). **WFG-226, the next row, does touch one of those seven**, and its lap will need the rebuild.

## Gates

`.auto/venv/bin/python scripts/auto/gates.py --mode full` exits **0**: verify PASS, snapshot-verify PASS,
env-check PASS, pytest **2016 passed**, 64 skipped, 3 xfailed (386.5 s), up **31** on the 1985 critic #57
measured — exactly the 31 assertions this lap added. `baseline-verify` WARNs on the two git-ignored
`data/raw/**` contracts and on two NEW tracked artifacts, which is NH-029 and CHARTER §3d working as designed.
GitHub `auto-gates` run **323** is `success` at `1983ff1`, the head this lap started from, so CHARTER §4b set
no finding #1.

## The independent review

**Reviewed by: subagent (pass): the new gate is a string-presence ratchet over prose the same lap authored,
so it can prove the caveat is ON the screen but never that the caveat is TRUE.**

The reviewer re-measured all eight of my claims in its own process and re-ran all six mutations from scratch,
getting the same numbers (51 / 15 / 29 / 4 / 2 / 2 / 0). It found no CHARTER §3 rule broken and passed. Its
root objection is worth more than the pass, and I am not arguing with it:

> The load-bearing assertion the card actually puts in front of five judges — that under one rule the
> `uiseong_andong_2025` bucket is EMPTY — is a zero asserted in words (「비어 있습니다」), and it is the one
> figure on the card with no `docs/NUMBERS.json` key of its own. … `mr_uiseong_fa_exceeds_budget` = 2 is
> pinned to `data/processed/multi_region_comparison.json` by `make verify`, while the counter-claim standing
> beside it on the same screen is pinned to nothing. … The 624.8 / 628.2-minute origins that make it true
> could stop being those origins and every one of the 51 assertions would still be green.

That is a `mandela` finding (verifier-equals-designer) and it is correct. **Both of its nails were acted on
in this lap, one closed and one filed:**

- **Second nail, closed here, one line.** `scripts/check_region_literals.py`'s `SCOPE` held
  `scripts/finals.template.html` and **not** `web/finals.html`, so the one file a judge opens sat outside the
  only gate that catches a typed region name. `web/finals.html` is now in `SCOPE`. It costs nothing — the
  embedded JSON payload is one line carrying every region key, which `_REGION_KEY` already exempts as the
  correct pattern — and it is graded: hand-editing a typed 「의성·안동」 into the **built screen only**, the
  WFG-109 shape the template could not see, now exits **1** at `web/finals.html:2101` where it previously
  exited **0**. `tests/test_check_region_literals.py` and `tests/test_screen_checks.py`: 87 passed, 1 xfailed.
- **First nail, filed not built, and the row already existed.** Registering the symmetric-rule count as its
  own key is **WFG-122** (P1, `todo`, NH-031 option A). I appended the reviewer's objection to that row
  verbatim, with the addition it implies: the row's `Done when` should also re-point this gate's 「버킷은
  비어 있」 assertion at the new key on all four surfaces, which is the difference between a gate that fails
  when the claim becomes false and one that fails when somebody rewords it. I did **not** reorder it —
  reorders are the critic's, one per lap (CHARTER §14) — and the critic is asked on the row to weigh it.

**What the reviewer explicitly did not check, in its own words:** it did not re-run `make finals` to confirm
`web/finals.html` is byte-reproducible from the template; it verified only that the two files' card blocks
are identical and that `MANIFEST.json`'s sha and byte count match the committed file.

## Next row

**WFG-226** (P0, KCF) — `docs/auto/JUDGE_QA.md:1421` card Q38 still tells the student to say the repository
answers this question two ways and that this is a defect; that stopped being true at `5bcfe11`. It is on the
paper in the 0140Z booth kit, so it needs `make printables` at a new stamp and a re-pointed bundle manifest
in the same lap or `tests/test_printables.py` goes red (NH-049). `docs/auto/DIRECTION.md` names it second.

## In plain terms

- **What changed for the project.** The big screen at your booth printed a number that counts *against* us
  (「예산 초과 2」) without saying that the number was measured in a way that inflates it. Your README already
  said so; the screen did not. Now it says so twice: one line right under the number, and a fuller card in the
  한계 panel. No number changed.
- **Why it matters to the judges or the science.** 제출 자료 is a 20-point row and 「사용된 자료에 대한 출처
  명기」 is one of its named criteria. A judge who reads a count on your screen and then finds the caveat only
  in a document has found a gap; a judge who reads the caveat beside the count has found a project that
  volunteers its own weak spot. Four surfaces now hold that caveat and a test fails if any of them loses it.
- **What you should do, if anything.** Nothing for this change. One thing to know: the reviewer that checks
  my work passed it but pointed out that the new card says 「비어 있습니다」 (it is empty) in words, and words
  are not something the machine can re-check the way it re-checks a number. The row that fixes that already
  existed (WFG-122) and now carries the argument. But **13 of the 20 decisions waiting for you
  are now at or past their date**, six of them due today, and two of them (NH-046, NH-037) are holding up the
  readiness checklist and the manuscript's word budget. Reply to this email with one line per item in the form
  `NH-046: A`.



## Gates

**ALL GREEN** · mode `full` · head `e043c1f` · 2026-09-10T09:50:33Z · current at `e043c1f`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 21.3 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.1 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 0.5 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 584.9 s | 2016 passed, 64 skipped, 3 xfailed, 52 warnings in 583.26s (0:09:43) |

## Commits since the previous report (16e6824d026bbce9c88fb3bc812fe61e18548da1..HEAD)

- e043c1f WFG-225: the screen that prints 예산 초과 now says what rule it was measured under
- 53d1a4e claim WFG-225 (20260910T0920Z)
- 1983ff1 the critic report names the head it ships in, and keeps the head it reviewed
- 90d3ea7 critic #57: the page a judge is sent to for the oracle names a script that never wrote the array

## Needs a human (25 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09, one day past; raised to HIGH by critic #55 on a measured third instance)
- NH-036 [DECISION] One critic lap told the next one not to edit a file, and that is what kept a false sentence in front of a judge for a window (by 2026-09-10)
- NH-037 [DECISION] The paper's word proxy now stops it a thousand words before your 25-page rule (by 2026-09-10)
- NH-038 [DECISION] Your "product first" rule has spent the last three dev laps on documents, and the readiness line it was written to protect has not moved in five critic laps (by 2026-09-09)
- NH-039 [DECISION] The national wildfire-spread system's manual is an 18 MB PDF the sandbox could not fetch, and one of you can (by 2026-09-12)
- NH-040 [FYI] A critic lap pushed one commit past a red `--assert-reported`, and it is telling you rather than hiding it
- NH-041 [FYI] This lap sent you an email containing only the word PLACEHOLDER, and could not take it back
- NH-042 [DECISION] Two of your own rules collide whenever a withdrawn claim lives in a frozen artifact, and this week they collided three times (by 2026-09-10)
- NH-043 [DECISION] A gate your loop built this morning will go red about twice a day, and the charter tells the lap that meets it to stop working (by 2026-09-09)
- NH-045 [BLOCKER] The staleness gate has closed `auto/dev` to every routine, and the one routine that met it is the one forbidden to clear it (by 2026-09-08)
- NH-044 [DECISION] The claim the paper just retracted is still live on the page the paper cites for it (by 2026-09-09)
- NH-046 [DECISION] Your product's definition-of-done names a command nothing in this project has ever run (by 2026-09-10)
- NH-048 [DECISION] One of the research routine's three literature channels has been dead for two runs, and a working replacement is already proven (by 2026-09-10)
- NH-049 [DECISION] Your critic routine is told to add judge Q&A cards and your own printing gate makes that impossible for it (by 2026-09-11)
- NH-050 [DECISION] You answered two of these questions two days ago and the loop never heard you, because you answered them on the routine page (by 2026-09-10)
- NH-051 [DECISION] The rule your loop has been obeying for three days is not the option it names, and the difference is why one row has been pushed down five times (by 2026-09-11)
- NH-052 [DECISION] The experiment that measures what your own forecast is worth is now one run away, and it would change what 42 means (by 2026-09-12)
- NH-053 [DECISION] The word your front door uses for 42 stopped being right today, and the loop cannot pick its replacement (by 2026-09-12)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
