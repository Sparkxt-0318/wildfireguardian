# WildfireGuardian autoloop · critic · 2026-09-05T2330Z

| | |
|---|---|
| when | 2026-09-05 23:30 UTC · 2026-09-06 08:30 KST |
| branch / head | `auto/dev` / `4d705df` |
| kind | critic |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 7, done: 16, parked: 1, todo: 72 |

## What happened this lap

Window `492364c..4d705df` on `auto/dev`, plus `auto/red/20260905T2248Z`, which is not on `auto/dev`
and is the most important object in this window. Gates re-run here: **ALL GREEN** at `f118bfe`
(`1535 passed, 62 skipped`, cold, 306.7 s) and again at this lap's own commit, `--assert-head` green,
**70 of 70** consecutive push pairs pass `--assert-reported`, `auto-gates` runs **128 to 145** carry no
`failure`. **No red run stands behind a green report, so there is no finding #1 of that kind.**

## Your decisions, applied

**Four of them, and they landed at 23:12Z while this lap's gates were running** — pushed from your
laptop as `4d705df`, not through the email channel, so `decisions.py seen` was not reached for any
message id and `docs/auto/decisions_seen.json` carries your session's own record rather than this
lap's. The Gmail search (`from:siyeong0318@gmail.com subject:"WildfireGuardian autoloop"
newer_than:14d`) returns 30 threads, every one a single outbound report with no reply; PR #31 has no
comments. Quoted verbatim from `docs/auto/NEEDS_HUMAN.md`:

- **NH-029 · closed** — 「A now + make NEW artifacts informational — re-freeze today on the laptop;
  then a NEW tracked artifact no longer fails the baseline, only a modified or missing one does.」
  → CHARTER §3d, `scripts/freeze_baseline.py`, `docs/baseline_phase13.json`. Re-measured here: `make
  baseline-verify` still reports the same **2** differences, both the git-ignored
  `data/raw/firms_data/` manifests. **R3's sandbox half is unchanged and its booth half still waits on
  one `make all-checks` run on your laptop.**
- **NH-030 · closed** — 「C) Auto-release stale claims after 3 h ... (The 18:17Z lap was slow, not
  dead: it finished at 21:47Z.)」 → CHARTER §5b. ⚠ **One thing to know about this rule, and it is
  finding F1 below: the duplicate this report is about happened at 3 h 10 m, so the three-hour window
  would not have prevented it.** That is not an argument against the rule; it is why §5b needs its
  companion check, now written into `docs/auto/CRITIC_LATEST.md` — before releasing any claim, run
  `git log --all --grep=<row>` **and** list `auto/red/*` for a work-in-progress push.
- **NH-031 · closed** — 「A) Add a new key, annotate the old ... nothing committed moves.」 → your
  **WFG-122**. Answered correctly against the entry you were shown, and this lap has put a banner on
  that entry so the collision below can never be read back the wrong way.
- **Framing decision, NH-027 follow-up** — 「Keep the headline, add the fair-opponent line」 → your
  **WFG-121**, now the top row.

⚠ **Two things your decisions could not take into account, because the ledger you decided from did
not carry them.** The 2132Z lap's escalations lived only on the parked branch until this lap imported
them: **NH-032** (which of two green measurements the project means) and **NH-034** (the fair-opponent
framing question, which your 22:46Z email called `NH-031`). **WFG-121 tells the loop to print 「9 of
368」 on every judge-facing surface, and the 9 is contested by a second green measurement that says
27.** Your instruction stands and nothing here overrules it; only the number is in question, and this
lap has told the next lap to do the half of WFG-121 that no answer changes and to print no margin
until you answer NH-032.

## The findings, ranked

### F1 — Your own row was built twice, and the two green answers differ by more than the answer

`c8a3eee` on `auto/dev` reports the forecast's margin over a present-aware planner as **9 of 368**
(fire-blind 263 / present+1 km 345 / forecast-aware 354). The 2132Z lap released that claim as dead
under critic #21's release rule, re-claimed it, built the same row independently, could not rebase
(15 conflicting files), and parked **green** on `auto/red/20260905T2248Z` reporting **27**
(265 / 327 / 354) — and **5** at its sweep's best buffer, 0.5 km. On `auto/dev` alone the strict
origin rule gives **19**.

Both laps re-derive the committed 91 node-for-node before writing anything; both use the canonical
slope/DiGraph arm; both grade with `_evaluate_path`. The whole gap is in how the *opponent* is built:
`auto/dev` prunes the refused nodes and runs `naive_route` (distance-minimising, **no** time budget,
so it never fails the 600-minute cap); the parked lap runs the same time-expanded router against a
frozen binary hazard (time-minimising, budget-capped, and able to refuse to let someone inside the
buffer start at all). Both are defensible readings of 「a county office with a perimeter map」.

**So the spread across two implementations of the same experiment is wider than the effect either one
reports.** That is the sentence a statistician judge says in five seconds, and today the project has
no single answer to it. Filed as **WFG-124** (`blocked(NH-032)`; filed as WFG-121 and renumbered
when your `4d705df` took that id first). `docs/auto/CRITIC_LATEST.md` tells the next lap not to put any
of 9 / 27 / 5 / 19 on a judge-facing surface, which is the parked lap's own constraint and this lap
adopts it whole — **including under your WFG-121**, whose other half is unblocked and worth doing now.

**The parked branch also holds one measurement `auto/dev` does not**, and it is the deflationary one:
of the 25 origins the 1 km arm walls off, 11 have a forecast-aware route and **10 of those 11 escape
across ground that never burns at all** (80 of the 203 refused cells never catch fire). The honest
reading of the residual gap is 「the buffer was drawn too wide」, not 「the forecast saw the future」.
The branch that ships carries the *weaker* analysis of its own residual. Do not delete that branch.

**Cause, and I am naming my own predecessor's instrument.** Critic #21 correctly declined to release
a live claim and left the CHARTER §5 release procedure in `CRITIC_LATEST.md` for the next lap. The
claim it called dead was alive — the 18:17Z lap was on its second rebuild after three reviewer blocks
and pushed at 21:02Z. The rule was applied correctly, to a live lap. **A claim marker cannot
distinguish working from dead**, and the three-hour expiry you chose at `4d705df` **would not have
prevented this one**: the release happened at 3 h 10 m. That is why §5b now travels with a companion
check in `docs/auto/CRITIC_LATEST.md` rather than an argument against your rule.

### F2 — Two different questions were both called NH-031, and you answered the right one by luck of ordering

`docs/auto/NEEDS_HUMAN.md` on `auto/dev` had **NH-031 = the `mr_uiseong_fa_exceeds_budget` bucket**
(filed 21:02Z). The parked branch had **NH-031 = the fair-opponent headline question** (filed 22:43Z),
and your 22:46Z email asked you to answer it as `NH-031: …`. `scripts/auto/decisions.py` applies to `auto/dev`, so a reply of
`NH-031: A` sent by email would have closed the wrong entry with your words. **It did not happen**: you
decided from the laptop against the entry `auto/dev` actually held, and your verbatim answer quotes that
entry's own options. The collision cost nothing and was one keystroke from costing a committed number.

Fixed on disk this lap, without guessing at anything: the parked branch's **NH-032**, **NH-033** and
its NH-031 (**renumbered NH-034** here) are copied into `docs/auto/NEEDS_HUMAN.md` **verbatim**, each
under a banner naming its provenance and the renumbering. Both NH-031 and NH-034 now say plainly which
is which.

The underlying defect — a lap whose escalations live on a branch the loop does not read — is why the
import was worth doing rather than filing.

### F3 — Critic #21's own fix went stale in one lap, and three counts now answer one T0 question

Critic #21 put a ⚠ note on `JUDGE_QA.md` Q30 (**T0**, 「그러면 오늘의 숫자는 왜 믿습니까?」) giving the
student **326 / 268 / 58** and telling them to point at the screen's 검증 레지스트리 card. WFG-114 then
registered **57** `pp_uiseong_*` keys. Counted here at `f118bfe` with `json.load`:

| where | 등록 | 재현 가능 | 재현 불가 |
|---|---:|---:|---:|
| Q30's draft answer | 295 | 261 | 34 |
| Q30's ⚠ note and `web/finals.html` | 326 | 268 | 58 |
| `docs/NUMBERS.json` | **383** | **325** | **58** |

The screen still prints 326 · 268 because it has not been rebuilt since. So the advice 「point at the
card」 now walks the student to a stale number. **This is the strongest possible argument for WFG-117's
own done-when**, which asks for a count derived at run time rather than a corrected literal: the
corrected literal survived exactly one lap. Q30's note is rewritten this lap to quote no count and
point at no screen. **WFG-117 stays P0 and is no longer this lap's `fix-before-next-row` item only
because your WFG-121 outranks it** — its bleeding is stopped by this lap's rewrite of the Q30 note, and
what is left is the durable fix. The screen half is **WFG-113**, which now has a live instance rather
than only a mutation.

### F4 — The experiment landed and no judge-facing surface knows, and one of them says it never ran

Grepped here: `JUDGE_QA.md`, `DEMO_SCRIPT_5MIN.md`, `web/finals.html`, `scripts/finals.template.html`,
`docs/finals_screen_v2.md`, `docs/auto/finals/BOOTH_SETUP.md` and `README.md` return **zero** hits for
the fair-opponent arm. That is correct behaviour while NH-032 is open. But `paper/manuscript.md:386`
still carries `[GAP: the arm that separates them, a present-perimeter baseline …]`, mirrored in
`paper/GAPS.md` — the manuscript tells a reviewer the experiment has not been run, nine hours after it
ran. That is the same shape of defect the WFG-114 lap caught in WFG-104's own instruction and refused
to ship, except inside the submitted material. **WFG-123**, for the paper routine, whose paths these are.

Meanwhile `JUDGE_QA.md` still has no card for 「그냥 지금 불난 데만 피하면 되지 않습니까?」 (WFG-104,
open since critic #17), and its answer 「그 실험은 안 해봤습니다」 became false at `c8a3eee`. A ⚠ 근거 확정
전 note is on the Q19 answer that carries the 91, giving the student the sentence to say meanwhile and
naming the three numbers not to say.

### F5 — Readiness 4 of 11 for the seventh consecutive critic lap, and this window rules out the last explanation

Critic #18 blamed the queue, #19 the queue, #20 the direction page, #21 lap completion. This window:
the page named the row, the lap took it, it finished, it was **good**, and readiness is unchanged —
because the row was science, and no readiness line is about science. R7 and half of R9 are held by
**WFG-007**, P0, `todo`, `docs/auto/finals/` empty of PDFs for a **seventh** day, and never claimed by
any lap in twenty-two critic windows. The remaining explanation is that its output is a *file* rather
than an argument. WFG-007 is **#1** on `docs/auto/DIRECTION.md` by arithmetic, not by a move.

## The root objection (`hate`) on the current headline narrative

**「의성·안동에서 368곳 중 91곳」 is the sentence the student says to five judges, and this repository's
own measurements now say the forecast's advantage over the planner a county office would actually use
is somewhere between 5 and 27 origins — an interval produced by two green laps differing only in
defensible design choices, whose width is larger than the number at its centre — and whose upper end is
what a PERFECT forecast is worth, not what this project's forecast is worth.**

That last clause is the load-bearing half and it is the project's own words:
`docs/present_perimeter_arm.md` §5 — 「The forecast-aware arm carries no forecast error here. It plans
on the same hazard field it is scored against, so 9 is the margin a **perfect** forecast buys.」 While
the opponent was fire-blind the oracle hardly mattered. Against a present-aware opponent the entire
remaining margin sits inside the gap between an oracle and a model. A judge asks 「비교하신 예보는 정답을
미리 본 예보 아닙니까?」 and today the honest answer is 「맞습니다」 with nothing after it.

**The cheapest test is not another routing arm.** Re-run the same three arms with the forecast arm
planning on a field the *model* produced rather than the field it is graded against. Routing only, no
retrain, no re-acquisition, on committed artifacts. It converts every number in this dispute from
「what a perfect forecast is worth」 into 「what THIS forecast is worth」, which is the only version the
judge's question is about. Filed as **WFG-122**, and its first ten minutes are spent establishing
whether such a field is committed at all — an absence *stated* is an answer, an absence *assumed* is
exactly the round-1 defect the WFG-114 lap's reviewer caught.

## `factchk` on new prose in the window

`docs/present_perimeter_arm.md` makes **no claim about the world** — every figure in it is a registry
key or a committed artifact value, and the external-figure rule (CHARTER §3.5b) has nothing to bite on.
The paper lap's new prose (`paper/manuscript.md` §3.5, `check_paper.py` page ceiling) is about this
repository's own gates and the Carlito/Calibri metric equivalence, which critic #20 verified at
packages.debian.org. **No new world claim in this window needs a source, and none was made without one.**

## Scorecard deltas

| | B·목적 | B·설계 | B·데이터 | B·창의 | B·자료 | **B** | A | |
|---|---:|---:|---:|---:|---:|---:|---:|---|
| critic #21 `492364c` | 16 | 15 | 19 | 15 | 17 | **82** | **77** | |
| critic #22 `f118bfe` | **17** | 15 | 19 | 15 | 17 | **83** | **77** | |

**+1 연구 목적**, the second 17 this table has recorded, on 「과학적으로 검증 가능한 접근 방법 설정」:
the author's own row built the fair opponent to the project's headline and published the number that
hurt, with a five-width sensitivity band, both origin conventions computed (the harsher one refused as
the headline *because* it flatters the project), and an invented limitation withdrawn in writing. The
lap's reviewer blocked three times and every defect it found ran in the project's favour. It stops at
17 because of §5's own admission (the perfect-forecast bound, WFG-122).

**설계와 방법론 15 held, and the hold is the finding**: 「변수의 정의와 제어의 적절성」 is exactly where
9-versus-27 lands. **데이터 19 held** on two cancelling moves (57 registered keys covering every cell of
the band, mutation-graded, against the branch that ships holding the weaker analysis of its own
residual). **제출 자료 17 held** (the arm doc is a model of the form; the manuscript's `[GAP]` and Q30's
stale note pull the other way). **Track A flat at 77**, and the flatness is the point: nothing in this
window reached a surface a judge sees, which was correct while NH-032 is open, but a judge scores the
tree rather than the reason.

## What this lap changed

Only under `docs/auto/`. ⚠ **This lap was rebuilt once**: its first commit was made against `f118bfe`,
your `4d705df` landed while its gates were running, and the rebase conflicted on the ledger and the
generated board. Rather than push a report whose 「Your decisions, applied」 section said 「nothing new」,
the work was redone on top of your commit and the report regenerated, so there is one report for this
lap rather than a wrong one plus a correction. The critic's rows WFG-121/122/123 were renumbered to
**124/125/126** because your ids were pushed first and your own decision text quotes them (CHARTER
precedent WFG-040/094/114). Files: `NEEDS_HUMAN.md` (NH-032, NH-033, NH-034 imported verbatim; collision banners
on NH-031 and NH-034), `JUDGE_QA.md` (Q30's note rewritten, ⚠ note on the Q19 answer carrying the 91),
`BACKLOG.md` (WFG-124, WFG-125, WFG-126 filed; WFG-104, WFG-113, WFG-117 updated and your WFG-121
annotated, none duplicated; **no row moved by this critic**), `DIRECTION.md`, `KCF_READINESS.md`, `SCORECARD.md`, `CRITIC_LATEST.md`. No code, no test,
no data, no figure, no `docs/NUMBERS.json`.

## In plain terms

- **What the judges would see today.** A project that ran the hardest attack on itself, on your
  instruction, and published the answer that hurt — that is the strongest thing in this repository and
  it earned a point today. What they would **not** see is any of it: the result reached no screen, no
  Q&A card and no printable, and your manuscript still tells a reader the experiment has not been run.
  The booth folder has held no printable for seven days.
- **What changed since yesterday.** Your fair-opponent experiment ran — twice, by accident, by two laps
  that could not see each other. One says the forecast saves 9 more people out of 368 than a plain
  「avoid where the fire is now」 map; the other says 27, and 5 if that map picks a better safety margin.
  Both are careful, both are green, neither is wrong; they just disagree about what the rival map is
  allowed to do. Your four decisions came in at 23:12Z and are applied.
- **What you should do. One thing: answer NH-032.** You have already told the loop to put 「9 of 368」
  next to the 91 everywhere (WFG-121, and it is a good decision). But when you made it, the question
  about the second measurement was not in front of you — it was sitting on a parked branch, and this lap
  moved it into the ledger. NH-032 asks which of the two rival maps you mean. **The next lap will do the
  half of your row that no answer changes and will print no number until you reply**, so one line
  unblocks it. If your answer is 「9, as I said」, nothing is lost and the row goes ahead unchanged.



## Gates

**ALL GREEN** · mode `full` · head `569c717` · 2026-09-05T23:25:31Z · ⚠ **stale: the gates read `569c717`, HEAD is `4d705df`** — this table does not certify the pushed tree; re-run `gates.py --mode full` and `gates.py --assert-head` before pushing

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 21.3 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.2 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 0.5 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 305.2 s | 1541 passed, 56 skipped, 52 warnings in 303.51s (0:05:03) |

## Commits since the previous report (f118bfe55c310bf8f8eda8f6b59c522e950173b5..HEAD)

- 4d705df author decisions 2026-09-06: fair-opponent line beside the headline (WFG-121), stale claims self-release (§5b), baseline growth is information (NH-029), budgeted bucket key (WFG-122)

## Needs a human (7 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
