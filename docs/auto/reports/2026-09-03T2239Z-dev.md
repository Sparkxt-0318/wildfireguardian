# WildfireGuardian autoloop · dev · 2026-09-03T2239Z

| | |
|---|---|
| when | 2026-09-03 22:39 UTC · 2026-09-04 07:39 KST |
| branch / head | `auto/dev` / `c7b8a66` |
| kind | dev |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 7, done: 8, parked: 1, todo: 28 |

## What happened this lap

Sprint lap 2026-09-03T2217Z. Branch `auto/dev`, claim commit `cd4e670`, head `c7b8a66`.
`gates.py --mode full` ALL GREEN at `c7b8a66`: `1191 passed, 56 skipped`, verify PASS,
snapshot-verify PASS, env-check PASS, `baseline-verify` WARN as expected off-laptop
(soft step, `hard: false`, the two `data/raw/firms_data/*.json` manifests cannot exist
in a fresh clone).

## Critic #3, cleared before a row was claimed

**F13 · `done(509819d)`.** `auto/dev` was RED at `9bf15fb` on one line of prose: the
2053Z report's plain-terms paragraph quoted the 폐기된 452계열 bracket with no lineage
label inside the gate's ±2-line window. Repaired by annotation, the way a record is
repaired — `(폐기된 452계열 합성 baseline; 정본은 6 → 66)` — using the critic's own
replacement text. `tests/test_rescue_lineage_ssot.py` 6 passed and `make check-forbidden`
clean, as the first act of the lap, before anything else was touched.

**F14 · `done(509819d)`, and it is WFG-046 widened.** The critic's diagnosis was that
the unchecked thing was never "the report" but everything committed after step 5's gate
run. Three pieces:

- `scripts/auto/gates.py --assert-head` runs no gate. It reads `.auto/gates.json` and
  exits non-zero unless it records a pass, **in mode `full`**, at exactly this `HEAD`,
  with a clean working tree. The mode clause is this lap's addition to the critic's
  design: a 20-second `--mode quick` run at the right head would otherwise have
  satisfied the check that guards a push.
- `scripts/auto/report.py` runs `make check-forbidden` and `pytest
  tests/test_rescue_lineage_ssot.py` over the report it just wrote and returns their
  exit code (WFG-046 a). The report is written first and checked second on purpose: a
  lap that trips this still has the file to repair.
- The report's own `## Gates` block now says **stale** in words when it certifies a
  commit that is no longer `HEAD`, so the failure is legible in the artifact the critic
  reads rather than only in two short hashes printed in different sections.

`tests/test_gates_assert_head.py`, 15 tests, pins all three including the exact
`f5f8498`/`8d1decf` mismatch that produced the finding. CHARTER §4 step 8 is now one
command, and one of those tests asserts the charter still names it.

**F15 · `done(509819d)`**, though it was only `fix-this-sprint`, because it decided
which row this lap could take. CHARTER §5 gains the release rule: `in-progress` is only
ever held by a lap that is still running. WFG-021 and WFG-016 are back to `todo` with
their residue notes verbatim. WFG-046 and WFG-047 are `done(509819d)`.

Nothing was disputed.

## The row: WFG-021 (a) · `done(b557e9d)`

WFG-021 (a) was the first `todo` row in table order once F15 released it, and
`KCF_READINESS` R2 names it. Session 19 measured that the satellite was slower than the
person in every one of the three fires the archive could verify; that result had a
document and no booth artifact.

`docs/auto/finals/DETECTION_FLOOR_CARD.md` states it as recorded: +22 / +34 / +64 min
after the report; 영덕 2025 classified 교란 and counted in neither direction, with the
four numbers that justify the classification; the false-alarm result as the **upper
bound** a zero numerator permits, not a rate; the size floor as an order of magnitude;
the 신고 > GK2A > FIRMS priority; and an explicit "not claimed: GK2A buys time".
`docs/auto/JUDGE_QA.md` gains Q10a (why 영덕 is excluded rather than counted as a miss)
and Q10b (the false-alarm rate is a bound, not 0 %).

**The root objection to my own plan** (`hate`), recorded before building: a card is the
fourth place these figures live, and this repository's history is that a retyped number
drifts — WFG-004 was a whole lap spent reconciling one AUC. The cheapest test was not to
proof-read the card but to make it falsifiable: `tests/test_detection_floor_card.py`
reads every cited figure back out of `docs/NUMBERS.json`, binds each delay inside its own
table row, and keeps a tripwire over new bare digits.

**What that test found, which a careful reading would not.** The FIRMS delays the row
asked for (+117 / +151 / +17 min, printed in `docs/detection_floor.md` §4 and §8 beside
the registered GK2A ones) have **no registry key**. They are readable by hand from
`data/processed/detection/firms_first_detection.json` → `delay_h` = 1.95 / 2.52 / 0.28,
and not re-derivable by `make verify`. Under CHARTER §3 rule 3 a number you cannot
register you do not write, so they were left off the card, which points at §8 instead,
and the gap is filed as **WFG-048** (P1). Citing that artifact then made
`check-artifact-manifest` fire, correctly; the manifest was rebuilt at `710d5b0`, purely
additive.

## Reviewed by: subagent (block): the lap that built `--assert-head` wrote an unearned green claim into the finals definition-of-done

The reviewer blocked, and it was right. `docs/auto/KCF_READINESS.md` R3 said
"`gates.py --mode full` green in this sandbox at the head this lap pushed" while
`.auto/gates.json` recorded `passed: false` at that same head and nothing had been
pushed. That is the F14 failure class reproduced inside the lap that closed it, written
into the artifact a KCF judge is meant to read as evidence. R3 now states what was
observed: RED at `633c3db`, the reason, ALL GREEN at `710d5b0`.

Its four secondary findings were fixed rather than argued with, at `c7b8a66`:

1. The card printed `0.1939 ha` while its own registry entry says ORDER OF MAGNITUDE
   ONLY and §6 says do not read the decimals. Now `약 0.2 ha` with the warning repeated.
2. The 217-step counterfactual dropped its denominator and the two riders that stop a
   judge reading it as "a clean background would have detected 영덕". Card and Q10a now
   carry 283 스텝 중 217, the 3.09 K threshold, the steps hours before the report, and the
   +6…+8 K daytime solar contrast.
3. The delay tests were substring-only: swapping 강릉's +34 with 홍성's +64 kept them
   green. Each delay is now matched inside its own table row.
4. "The last test refuses any bare number with no registry key" oversold a
   hand-maintained escape list. The test's docstring, the MEMO entry and the backlog
   residue now say what it actually enforces — a tripwire over digits a later lap adds,
   not a proof about the ones already there.

The reviewer confirmed independently that all nine card figures match `docs/NUMBERS.json`
and re-derive from their artifacts (296/296 in `verify.log`), that no committed artifact
was edited, and that the card does not drift from the submitted frame.

## What did not work

**WFG-021 (c) is still not attempted, and the reason is time, not reachability.**
SESSION19 items 10 (FD-vs-LA cadence) and 12 (cross-fire contamination for 강릉/홍성)
need granules fetched from the NOAA GK2A archive at run time; the intermediates are not
committed. The 2017Z reviewer established the archive is reachable anonymously, so this
is agent-doable by a lap that budgets the download. The row is `todo` with that written
in it, under the release rule this lap installed.

**The FIRMS numbers were not registered this lap.** Registering them means either
hand-editing `docs/NUMBERS.json` or touching `scripts/build_numbers.py`, which WFG-040
says would drop 65 of 278 entries on its next run. That is a second defect and it
belongs in its own row, not folded into a card lap.

## KCF_READINESS

R2 stays ☐ and its evidence changes: the card exists and is gated, but nothing in `web/`
has changed, so only WFG-017's rebuild ticks it. R3 stays ☐ with the F13 red repaired
and `docs/auto/finals/BOOTH_SETUP.md` (WFG-037) still unwritten.

## Next row

WFG-017 — `web/finals.html` refresh v2 — is the first `todo` row in table order, and it
is what turns this lap's card and the other four into the screen R1, R2 and R9 depend on.

## In plain terms

- The branch was red again when this lap started, and the fix is now mechanical: the loop
  can no longer push a commit its own tests have not read. Two lines of prose caused the
  red; a five-line check and 15 tests mean that class of mistake ends here.
- Your detection-floor result is now a card you can hold at the booth: the satellite was
  22, 34 and 64 minutes slower than the person who phoned it in, 영덕 is explained as
  contaminated rather than missed, and the false-alarm figure is stated as an upper bound
  instead of "0 %". Every number on it is checked against the registry by a test, and one
  claim the task asked for was deliberately left off because it has no registry entry yet.
- Nothing needs you tonight. The two open items that do are the README's burned-area
  figures (NH-015) and the routine schedule (NH-016). Next lap puts these cards onto the
  finals screen.



## Gates

**ALL GREEN** · mode `full` · head `c7b8a66` · 2026-09-03T22:38:17Z · current at `c7b8a66`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 11.3 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.1 s | make: *** [Makefile:140: baseline-verify] Error 1 |
| snapshot-verify | PASS | 0.3 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 108.8 s | 1191 passed, 56 skipped, 52 warnings in 107.82s (0:01:47) |

## Commits since the previous report (8d1decfe1a00633d74808eb961707c9d3a7ed053..HEAD)

- c7b8a66 the reviewer blocked on an unearned green, and it was right
- 710d5b0 manifest: the FIRMS artifact WFG-048 names becomes a citable one
- 633c3db backlog: WFG-021 (a) resolves to its own commit
- b557e9d WFG-021 (a): the detection floor becomes a card a judge can hold
- cd4e670 claim WFG-021: the row the release rule just made pickable again
- 509819d the push is refused unless the gates read the commit being pushed
- 9bf15fb critic #3: the commit you push is not the commit the gates read

## Needs a human (14 open)

- NH-001 [DECISION] Email delivery needs three repository secrets
- NH-002 [DECISION] Optional: `@claude` on GitHub issues and PRs
- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-008 [DECISION] Five questions for the KCF 운영사무국 (by 2026-09-07)
- NH-009 [DECISION] Repository decisions only the author can take (this week)
- NH-010 [DECISION] Expert consultations and the firefighter record (by 2026-09-20)
- NH-011 [DECISION] One real, recorded email send from a network that works in Shanghai (by 2026-09-20)
- NH-012 [DECISION] Portal downloads the loop cannot do (by 2026-10-01)
- NH-013 [FYI] Optional: a stable web address for the visual board
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-015 [DECISION] The three sources behind the README's opening numbers (by 2026-09-08)
- NH-016 [DECISION] The critic routine has 30 minutes to land its findings, and needs about 40 (by 2026-09-05)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
