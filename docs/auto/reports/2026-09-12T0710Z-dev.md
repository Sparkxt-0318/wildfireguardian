# WildfireGuardian autoloop · dev · 2026-09-12T0710Z

| | |
|---|---|
| when | 2026-09-12 07:10 UTC · 2026-09-12 16:10 KST |
| branch / head | `auto/dev` / `a9bc4ab` |
| kind | dev |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 10, done: 37, dropped: 1, parked: 1, title-derivable: 1, todo: 128 |

## What happened this lap

## Your decisions, applied

**Nothing new on either channel.** Gmail's newest 25 threads matching
「WildfireGuardian autoloop」 in the last 14 days each carry exactly one message and
every one is the loop's own send; PR #31's comment list is empty. `decisions_seen.json`
still records `"seen": []`, and the newest applied decision is still NH-031, received
2026-09-06. That is WFG-211, already filed, now twelve days — confirmed here, not re-filed.

## The lap

**Row: WFG-266 (P0, KCF), table position 1, and `DIRECTION.md` names it at position 1
with its reason, so page and table agreed.** Claimed at `a9bc4ab`, pushed before
building. Baseline `gates.py --mode quick` ALL GREEN at `ac86c21`; GitHub `auto-gates`
run **400** is `success` at exactly that head, so CHARTER §4b set no finding #1;
`CRITIC_LATEST.md` listed zero `fix-before-next-row` items; no live claim stood on the board.

**The defect.** Commit `63e9d20` narrowed F8's caption in `paper/manuscript.md` to
「2 reaching no refuge」 and left that same figure's legend in `paper/make_figures.py`
reading 「origin: no safe walking route」. A caption and its own legend asserted
different things inside one commit, on the rubric clause 「그래픽 및 범례의 명확성」
that `docs/auto/RUBRIC.md:34` and `:47` name in both tables. F3's legend carried the
third instance.

**The root objection I pre-registered, before editing anything** (full text in
`a9bc4ab`): the row hands me a finished remedy to copy — F5b did this one lap earlier —
and the cheap lap pastes F5b's phrase onto two more legends and closes. *That is repair
by analogy.* Two figures sharing a bucket NAME do not necessarily share a bucket
CONDITION, and the paste is not earned until the producer behind each artifact has been
read. Second pre-registered objection, which survived: F3's legend has a neighbour F5b's
does not, `fa_exceeds_budget` = `not nv.enters_hazard and not fa.reached`, which SHARES
「the forecast-aware search reached no refuge」 with this bucket. Any phrase built on
reaching-no-refuge alone describes two of that figure's four bands and discriminates
neither; 「no safe walking route **found**」 does, because in `fa_exceeds_budget` the
fire-blind route is hazard-free and reaches.

**Built.** `F3b_regions.png` and `F8b_routing_map.png`, new filenames, both legends
reading 「no safe walking route found」, supersession in each drawing function's
docstring, predecessors byte-unchanged (`git diff a9bc4ab --name-status -- paper/figures/`
is two `A` lines, no `M` and no `D`), `paper/manuscript.md` repointed, docx rebuilt. Both figures were opened and looked
at before shipping: legends fit, nothing clipped, F8b's counts match its caption.
F8's caption was deliberately **not** edited — it is already correct, NH-037 prices
every manuscript word, and two true statements about one bucket do not contradict.

**Evidence paths.** `docs/figure_legend_claims.md` (method, result, caveats, what it
does NOT show); `tests/test_figure_legend_claims.py`; `paper/figures/F3b_regions.png`,
`paper/figures/F8b_routing_map.png`.

**The done-when I could not meet, predicted in the claim commit and amended in writing.**
Clause (d) asked that one grep of `paper/make_figures.py` for 「no safe walking route」
return only supersession docstrings. **It cannot be satisfied while the repository is
correct:** the repaired legends contain that substring, so the clause's own exemplar F5b
fails it. A substring cannot tell a claim from a report of a search, which is the entire
distinction being repaired. I did not break a correct legend to turn a grep green. The
predicate moved onto the claim instead: every non-docstring string in that file naming
this bucket must carry 「found」 and must not carry 「exists」 or 「at all」, plus checks
that the labels still exist, that each repaired figure writes its own new filename, that
the predecessors stay on disk, and that the manuscript points at the repaired files.
Graded **red against five mutations** (reverted legend; `exists` legend; legend deleted
rather than repaired; a figure regenerating its committed predecessor in place;
manuscript pointing back at a superseded file) and green once each was undone.

## What did not work, what I got wrong, and what the reviewer stopped

**The independent reviewer returned `block`, it was right, and the block is the most
useful thing in this lap.**

I pre-registered that I would not paste F5b's phrase across figures until I had read
the producer behind each artifact. I then counted those producers wrong **three
times**, and the third was inside the correction:

- **Draft 1** read three producers and wrote 「repeated verbatim in three scripts」,
  with three line numbers each off by one. Caught by my own `sip` pass against
  `paper/STATE.json`'s lap-34 record, which names a *different* three.
- **Draft 2**, written as the correction with the anti-pattern freshly named in the
  MEMO, said **five** — and pasted `grep -rn 'enters_hazard and not fa.reached'`
  beside that answer *without re-reading what the grep returns*.
- **Draft 3**, after the reviewer ran that one line: the command returns **19**
  lines, and there are **seven** producers of this bucket.

**The seventh is the one that matters, and it is in the file I had open.**
`paper/make_figures.py:649` sits inside `F8b_routing_map` itself, about a hundred
lines above the legend I rewrote; the set it builds is exactly what `:710` plots and
`:755` labels. So **F8b does not read this bucket from a committed artifact at all —
it recomputes the partition with the repository's router and then asserts equality
with the committed one** (`counts_ok`, true at the render that shipped, which is why
the legend prints its counts). My document's sentence 「every figure that draws this
bucket is drawing the same predicate」 was false for the figure the document existed
to justify. I enumerated across `scripts/` and `src/` and never grepped the file I
was editing.

Corrected in all four places that carried the wrong count; the enumeration now ships
beside the command **and its raw output**, with the 19 lines broken down (7
`no_safe_route` producers, 3 `fa_exceeds_budget`, 9 docstrings quoting the
condition); WFG-271's done-when was rewritten to require a test that DERIVES the site
list by AST and to forbid one written against a fixed list of paths or a fixed count,
because a gate born from today's enumeration inherits today's blind spot — which is
precisely how `:649` was missed twice.

**The reviewer's supporting objections, all acted on:** the done-when (d) amendment
now gives the count the claim commit promised (`grep -c` returns **9** in that file;
six are docstrings, and the other three are the three repaired legends themselves);
the byte-unchanged claim now cites `git diff a9bc4ab --name-status -- paper/figures/`
(two `A` lines, no `M`, no `D`) instead of a bare `git diff`, which cannot see staged
additions and so was the wrong instrument for a true fact; the test's hardcoded
`len(labels) == 3` became a per-figure check, since a fixed total would go spuriously
red the first time a fourth correctly-worded legend is added and teach the next lap
to weaken the file; the test's own grading docstring now lists all five mutations;
and both the test and `docs/figure_legend_claims.md` now state what the gate does NOT
reach — it reads source string literals, so nothing binds a committed PNG to the
legend that drew it, and it keys on a literal substring, so a reworded over-claim
("no safe route", "cannot reach safety") escapes it entirely, the same limit
`docs/withdrawn_claims.md` §4 records for registered spellings.

**What the reviewer verified and I am not re-litigating:** the five identical
classifier sites are byte-for-byte identical including indentation; all five mutation
gradings are real and each fires a distinct test; the test does **not** pass on the
defective tree (3 failed / 2 passed against `a9bc4ab`'s files); CHARTER §3 rule 2
holds with no committed figure modified and nothing deleted anywhere in the lap; and
both new PNGs render correctly with F8b's counts matching its caption.

**Two more found by DIRECTION's 「grep the judge-facing set for the NEGATION」 rule,
and deliberately not fixed here.** `README.md:25` and `:798` say, in the project's
own voice, that two origins have no safe walking route **at all** — the strongest
spelling of this claim anywhere in the repository, on its most-read surface.
`paper/GAPS.md:116` has had it flagged ⛔ for a day, dispositioned 「Filed, not fixed.
Outside CHARTER §12: this routine cannot edit README.md」 — true of the paper routine,
which is confined to `paper/`, and not true of a dev lap. So it sat unowned because
the only routine that measured it is the one forbidden to act on it. **Filed as
WFG-270, P0, not fixed**, because CHARTER §3.5c makes a withdrawal unapplied until it
is registered in the same lap and the spelling is live in five non-record documents:
registering it green is the work, and the two README lines are the easy half.
`paper/GAPS.md` now names its owner. The `fa_exceeds_budget` band's own label is the
same family of over-claim — filed as WFG-269, P1, read and left standing rather than
silently widened into this row.

**Also updated because my own commit falsified it:** `docs/auto/JUDGE_QA_PENDING.md`
P-003's draft answer told the student 「F8과 F3 범례는 아직입니다」. It now carries the
answer this row earned, with a source that exists, the first draft kept as a dated
record, and an explicit line that the repair does **not** mean a safe route exists.

## Gates

`gates.py --mode full` RED on its first run, and staging is what exposed it — the MEMO's
own rule (`git add` first, gates second) firing on the lap that wrote nothing new about
it. Two causes, both with sanctioned remedies, both fixed: `check-forbidden` read the
bare token in a backlog row naming another row's id (per-line pragma, same class four
laps have now paid); and `check_paper.py` found `paper/STATE.json`'s `built_pages` anchor
measured on a different document, because repointing two figures changes the inputs
digest. **Nulled, not re-anchored**, per that check's own instruction — this sandbox has
no renderer, and hand-writing an anchor would keep a page count no run produced. ⚠ **The
page count is therefore UNKNOWN at this head and NH-028's 25-page limit is unverified
until a lap with LibreOffice runs `paper/measure_pages.py`.** Re-run ALL GREEN
(2221 passed, 65 skipped, 3 xfailed); `baseline-verify` is the usual sandbox WARN for two
absent `data/raw/` manifests (CHARTER §3d).

Reviewed by: **subagent (block)** — root objection: 「the lap's centrepiece is a corrected enumeration, and the corrected enumeration is still wrong, in the file the lap was editing」. `paper/make_figures.py:649`, inside `F8b_routing_map`, is a seventh producer of the bucket, feeding the very markers the repaired legend labels, and it appeared in none of the four places the lap wrote the enumeration down; re-running the grep the lap had pasted returns 19 lines, not five. First nail: `:649-650`. **Every finding, blocking and supporting, is repaired above rather than argued with**, and the reviewer's own verifications (identical spellings, five real mutation gradings, the test failing on the defective tree, §3 rule 2 intact, both renders correct) are recorded as verified.

**Next row:** WFG-267 (P0, KCF), table position 2, the dispatch sheet a judge is
physically handed — unless the critic's next direction note moves WFG-270 above it,
which is its call and not mine.

## In plain terms

- **What changed for the project.** Two pictures in your paper had a label that claimed
  more than your code can show — that two homes have *no* safe walking route, when what
  your program actually found is that its search did not reach a shelter. Both pictures
  are redrawn under new names saying 「none found」, the old ones are kept untouched as
  the record, and a new test now fails if any future label overclaims this again.
- **Why it matters to the judges or the science.** One of the scoring lines is about
  whether your figures and their legends are clear, and a caption that said one thing
  while its own legend said the opposite was sitting on that line. It is gone. The
  stronger habit is the one to talk about at the booth: this lap found the same
  overclaim in a worse place, your README's front page, and *filed it instead of quietly
  fixing it*, because correcting it properly means registering the withdrawal everywhere
  the phrase appears.
- **What you should do.** Nothing today, but two things are waiting on you and both are
  three days from the sprint end: **NH-046 is still unanswered and it alone holds the
  ninth readiness tick and the whole P1 block**, and the paper's page count is now
  unknown until someone runs it on a machine with Word or LibreOffice — worth a minute
  on the laptop, since the author's own 25-page limit is unverified until then.



## Gates

**ALL GREEN** · mode `full` · head `a9bc4ab` · 2026-09-12T07:10:40Z · current at `a9bc4ab`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 21.6 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.2 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 0.4 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 300.8 s | 2221 passed, 65 skipped, 3 xfailed, 52 warnings in 300.08s (0:05:00) |

## Commits since the previous report (cd1878297860bb487fc9c4a68cb66bdf18cf9e0c..HEAD)

- a9bc4ab claim WFG-266 (20260912T0617Z)
- ac86c21 a row cannot name the commit that adds it, so it stops trying and names origin instead
- 93402d1 the critic report names the head it ships in, because F4 of that report is laps that do not
- f0d3d7b the report naming a backlog row id carries the pragma, fourth lap running
- 155aa07 critic #72: a figure's caption and its own legend disagree inside one commit, on the row that scores legends

## Needs a human (32 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09, one day past; raised to HIGH by critic #55 on a measured third instance)
- NH-036 [DECISION] One critic lap told the next one not to edit a file, and that is what kept a false sentence in front of a judge for a window (by 2026-09-10)
- NH-037 [DECISION] The paper's word proxy now stops it a thousand words before your 25-page rule (by 2026-09-10)
- NH-038 [DECISION] Your "product first" rule has spent the last three dev laps on documents, and the readiness line it was written to protect has not moved in five critic laps (by 2026-09-09)
- NH-039 [DECISION] The national wildfire-spread system's manual is an 18 MB PDF the sandbox could not fetch, and one of you can (by 2026-09-12)
- NH-040 [FYI] A critic lap pushed one commit past a red `--assert-reported`, and it is telling you rather than hiding it
- NH-041 [FYI] This lap sent you an email containing only the word PLACEHOLDER, and could not take it back
- NH-042 [DECISION] Two of your own rules collide whenever a withdrawn claim lives in a frozen artifact, and this week they collided three times (by 2026-09-10)
- NH-043 [DECISION] A gate your loop built this morning will go red about twice a day, and the charter tells the lap that meets it to stop working (by 2026-09-09)
- NH-045 [BLOCKER] The staleness gate has closed `auto/dev` to every routine, and the one routine that met it is the one forbidden to clear it (by 2026-09-08)
- NH-044 [DECISION] The claim the paper just retracted is still live on the page the paper cites for it (by 2026-09-09)
- NH-046 [DECISION] Your product's definition-of-done names a command nothing in this project has ever run (by 2026-09-10)
- NH-048 [DECISION] One of the research routine's three literature channels has been dead for two runs, and a working replacement is already proven (by 2026-09-10)
- NH-049 [DECISION] Your critic routine is told to add judge Q&A cards and your own printing gate makes that impossible for it (by 2026-09-11)
- NH-050 [DECISION] You answered two of these questions two days ago and the loop never heard you, because you answered them on the routine page (by 2026-09-10)
- NH-051 [DECISION] The rule your loop has been obeying for three days is not the option it names, and the difference is why one row has been pushed down five times (by 2026-09-11)
- NH-052 [DECISION] The experiment that measures what your own forecast is worth is now one run away, and it would change what 42 means (by 2026-09-12)
- NH-053 [DECISION] The word your front door uses for 42 stopped being right today, and the loop cannot pick its replacement (by 2026-09-12)
- NH-054 [DECISION] Four fifths of your front door's headline is limits, and no lap is allowed to decide whether that is the project's strength or its biggest presentation risk (by 2026-09-13)
- NH-055 [DECISION] Your front door compares the headline forecast number to a model the same page calls broken, and today the loop measured a second, harsher comparison it is not allowed to put beside it (by 2026-09-13)
- NH-056 [DECISION] A Korean university published a reproducible deep-learning wildfire model four weeks before your finals, and a lap may not decide on its own whether to mention it (by 2026-09-13)
- NH-057 [DECISION] The one thing your project now says it contributes has never once been produced from a real fire, and only your laptop can change that (by 2026-09-13)
- NH-058 [DECISION] Two of your routines built the same row in the same window, and the rule that was supposed to stop that cannot see the paper routine (by 2026-09-13)
- NH-059 [DECISION] Your loop measured the hardest question a judge can ask you, and the answer is uncomfortable enough that no lap may decide on its own whether you say it out loud (by 2026-09-13)
- NH-060 [DECISION] Your "product first" rule sent a booth-printable defect to the bottom of the board, and two of your routines read the same sentence of it differently (by 2026-09-14)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
