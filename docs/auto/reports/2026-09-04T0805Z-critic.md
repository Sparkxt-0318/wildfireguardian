# WildfireGuardian autoloop · critic · 2026-09-04T0805Z

| | |
|---|---|
| when | 2026-09-04 08:05 UTC · 2026-09-04 17:05 KST |
| branch / head | `auto/dev` / `12bf2d9` |
| kind | critic |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 7, done: 12, parked: 1, todo: 41 |

## What happened this lap

**Critic lap #8, window `8e0a6ad..12bf2d9`.** Independent verification, five judging
lenses, `hate` on the headline narrative, `factchk` on the window's new world claims, and
a ten-question judge drill. No code, test, artifact, figure or `docs/NUMBERS.json` entry
was touched; this lap writes only under `docs/auto/`.

## Your decisions, applied

**Nothing new, and that is now itself the finding.** Searched the mailbox this lap with
`from:siyeong0318@gmail.com subject:"WildfireGuardian autoloop" newer_than:14d`: 25 threads,
every one fetched, and **every thread holds exactly one message** — the loop's own report.
PR #31 has zero comments. `docs/auto/decisions_seen.json` does not exist, because
`scripts/auto/decisions.py apply` has never recorded anything. The decision channel built on
2026-09-04 has never been exercised in either direction, twelve NEEDS_HUMAN entries are open,
and NH-016's date is tomorrow. Filed as **NH-020** with three one-line reply options.

## Verified independently

`gates.py --mode full` exits **0** at `12bf2d9` in a fresh cloud sandbox:
`1273 passed, 62 skipped` in 204 s, **COLD**. Against critic #7's cold `1261 / 62` at
`8e0a6ad` that is **+12 passed, skips unchanged**. `verify`, `snapshot-verify`, `env-check`
PASS; `baseline-verify` WARN, expected off-laptop, `hard: false`. `--assert-head` and
`--assert-reported` both exit 0. Green at HEAD for a fifth consecutive critic lap.

Every dev report in the window carries a `Reviewed by:` line except
`docs/auto/reports/2026-09-04T0401Z-dev.md`, which is critic #7's F40 unchanged.

## What the window did

`web/finals.html` had not been opened by this loop since 2026-09-02. It now carries five
evidence cards (운영점, 탐지 바닥, 240분 지평, 대피 지점 배치, 제출본과 정본), 26 tests that
compare the built HTML to the committed artifacts, nine committed screenshots, and
`docs/finals_screen_v2.md`, 219 lines organised around 「무엇을 말하지 않는가」.
`KCF_READINESS.md` **R2 ticks today**. The paper lap closed critic #7's F36 and found two
more figure defects nobody had named. Critic #7's cheapest test was "take WFG-017 and find
out what it costs". It cost one lap and three review blocks, and the risk is retired.

## Findings, ranked

1. **F41 · The panel that exists to prove the build prints a commit that does not exist.**
   `web/finals.html` stamps `"git":"a562045"` and the RELIABILITY tab renders
   「SYSTEM INTEGRITY · commit a562045」. `git cat-file -t a562045` →
   `fatal: Not a valid object name`. `scripts/build_finals.py:815` reads `HEAD` at build
   time; the lap then rebased and the object died. No test reads that line. The same dead id
   is in the screenshots' provenance README, WFG-017's `done(...)` marker and three reports.
   **Row WFG-067 (P0).** Gate that the stamp *resolves*, never that it equals `HEAD` — the
   commit carrying a build is always one later, so an equality gate is unsatisfiable.

2. **F42 · The Q&A bank tells the student to say, at T0, a sentence it forbids at T0.**
   `JUDGE_QA.md:240` (Q10, **T0**) still grounds 신고 우선 on 「신고의 99 %가 목격 신고」;
   `:353` (Q10d, **T0**, critic #7's guard) lists that exact sentence under
   「말하면 안 되는 것」. Both are among the fourteen the student is told to memorise. And the
   split now includes the screen: `web/finals.html` ships the corrected sentence, so the
   student reciting Q10 would contradict the screen behind them. **Row WFG-063 (P0), second
   window.**

3. **F43 · Six questions are invisible to every gate, and one of them is the guard.**
   `tests/test_judge_qa_bank.py:45` `QUESTION_RE` is defeated by the parenthetical between
   the tier tag and the period, not only by the letter suffix — so `Q34`, which has no
   letter, escapes too, and WFG-057's stated fix would close the row with the defect
   standing. The header says 「33개 · T0 14개」; the file holds **38 questions and 15 T0s**,
   and the fifteenth T0 is Q10d. A student obeying the bank's own drill plan memorises the
   fourteen and never reaches the guard. **WFG-057 raised P1 → P0, its fix corrected.**

4. **F44 · A routine that can do a row's work but cannot mark it done.** CHARTER §12
   confines the paper routine to `paper/`, so a row it finishes stays `todo`. WFG-064 is the
   first instance and is fully done — this lap opened both PNGs to check. **WFG-064 closed;
   mechanism filed as WFG-068 (P1).**

5. **F45 · The daily check this routine is asked to perform cannot answer its question.**
   `gates.py --assert-reported --base X` returned OK for eight bases spanning the whole 24
   hours, naming the **same** report every time, because any one new report in the range
   satisfies it. The per-push verification the critic prompt asks for has never been
   performed and cannot be until WFG-056's ledger exists. Said plainly rather than reported
   as a pass.

6. **F46 · One death toll, two spellings, and the wrong one is the registry.** The paper lap
   corrected the manuscript; this lap opened <https://dgmbc.com/article/bLdh4s3M4pgcSdYI0MZPc>
   independently and confirms 「경상북도 재난안전대책본부 … 26명」 with **no** 중대본 or
   중앙재난안전대책본부 anywhere on the page. `docs/NUMBERS.json` → `fire2025_chain_deaths`
   still says 중앙재난안전대책본부, and `README.md:198`/`:510` assert a
   「중앙재난안전대책본부 확인」 no source supports. **WFG-051 (P0), fourth window.**

## The root objection

> **The screen is now the most correct document in this repository, and every gate this
> project owns points the other way.** It states the withdrawn claim correctly while three
> markdown documents do not; it carries a build stamp no gate checks and that no longer
> resolves; and the Q&A bank cannot count its own questions, so the entry that stops the
> student saying a forbidden sentence is invisible to both the test suite and the drill
> plan. Eight windows of gates that read **values**, and every defect in this window is a
> defect of **agreement between surfaces**, which no gate here can see.

**Cheapest test, one lap:** take WFG-063 and WFG-067 together and look at what the fix
required. If three files and one `git cat-file -e` close both, WFG-062 (a registry of
withdrawn claims any document can be checked against) is the right generalisation and should
move to the front of the P0 block, because it is the only row here that would have caught
F42 on its own.

## Scorecard deltas (`8e0a6ad` → `12bf2d9`)

| track | row | was | now |
|---|---|---:|---:|
| A | 구현 및 유용성 | 11 | **14** |
| A | total | 70 | **73** |
| B | every row | 16/15/19/15/16 | held, **81** |

**Track A 구현 및 유용성 11 → 14** is the first move on that row in nine windows: the screen
exists, R2 ticks, 26 tests bind it to its artifacts. Not higher because R1, R4, R7, R9 are
still ☐, nobody has opened it on the booth laptop, and the integrity panel prints a dead
commit. **Track B held at 81 on two pairs that cancel:** 데이터 gains the screen-to-artifact
tests and loses F41; 제출 자료 gains the closed figure row and the registry-correcting
citation and loses it to F42, because a 제출 자료 row does not rise while the document the
student recites from contradicts itself.

## In plain terms

**What the judges would see today.** For the first time there is something to look at. Your
finals screen now has five evidence cards on it, and each one says in plain Korean what it
does not prove. Four of my five judge lenses passed it, including the disaster official who
failed you the last two days. One line on that screen is wrong: it prints a build code
(`a562045`) that no longer exists, so if a judge tries to check it they get nothing.

**What changed since yesterday.** The screen went from untouched-since-September-2 to done,
your two crooked paper figures were fixed properly, and your paper now credits the right
agency for the 26 deaths. Your score on the "does it actually work" row moved 11 to 14 out
of 20, the first movement there in nine of my laps.

**What you should do.** Reply to this email with just two lines: `NH-016: move it` and
`NH-020: email works, I read them`. You have not answered any of the 25 reports the loop has
sent, so it cannot tell whether they are reaching you at all, and NH-016 is a one-field
change on the routines page that is due tomorrow. Everything else on the list can wait; that
one cannot.



## Gates

**ALL GREEN** · mode `full` · head `12bf2d9` · 2026-09-04T07:53:03Z · current at `12bf2d9`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 19.7 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.2 s | make: *** [Makefile:152: baseline-verify] Error 1 |
| snapshot-verify | PASS | 1.8 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 203.9 s | 1273 passed, 62 skipped, 52 warnings in 202.25s (0:03:22) |

## Commits since the previous report (-15)

- 12bf2d9 the report now certifies the commit it travels in, not the red one it was born in
- 44a535b the paragraph explaining the collision collided, and a pragma was the wrong fix twice
- 7601734 the re-run at the push commit went red on the report itself, and the fix names both metrics
- a8dab5c report: WFG-017, the finals screen, and the three blocks that made it honest
- dc63a06 review block taken three times: a scope, a side and a count
- d0d64fb the finals screen gets its evidence cards, and the one that could not be measured now has a number
- e28377c paper: the reviewer blocked twice, and the first block was this lap making a P0 worse
- 8ab569e claim WFG-017: the finals screen, which critic #7 says is the one P0 whose cost nobody has measured
- 77576bc critic #7 addendum: the report certifies the commit it travels in, not only the one it read
- bfc5a5e critic #7: the card stopped saying who was late and started saying who should be first
- 8e0a6ad renumber this lap's two new rows: the paper lap took 058-060 in the same window
- b6caa9b reviewer blocked twice, and the second block was the row repeating its own defect
- fe480eb the reviewer escaped twice: the second time with the number in the middle
- 16bab3a the reviewer broke the gate with a synonym, and the gate now bans both directions
- 6c2840a the card stops saying the satellite was late, and a gate now says it cannot

## Needs a human (15 open)

- NH-001 [DECISION] Email delivery needs three repository secrets
- NH-002 [DECISION] Optional: `@claude` on GitHub issues and PRs
- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-010 [DECISION] Expert consultations and the firefighter record (by 2026-09-20)
- NH-011 [DECISION] One real, recorded email send from a network that works in Shanghai (by 2026-09-20)
- NH-012 [DECISION] Portal downloads the loop cannot do (by 2026-10-01)
- NH-013 [FYI] Optional: a stable web address for the visual board
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-016 [DECISION] The critic routine has 30 minutes to land its findings, and needs about 40 (by 2026-09-05)
- NH-017 [DECISION] Three entries were closed on replies the repository cannot see, and one of them was wrong (by 2026-09-06)
- NH-018 [DECISION] Two laps read the same primary sources and disagree on two sentences (by 2026-09-08)
- NH-019 [DECISION] One report time, for one fire, decides what the detection result is allowed to say (by 2026-09-08)
- NH-020 [DECISION] Twenty-five report emails, no reply, and the decision channel has never once been exercised (by 2026-09-06)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.


---

## Addendum, written after the push-commit gate run (WFG-038/039, third reproduction)

CHARTER §4 step 8 says the commit you push is the commit the gates read, so this lap re-ran
`gates.py --mode full` at `d8aec94` after committing. It is ALL GREEN, and it reports
**`1279 passed, 56 skipped`** where the run half an hour earlier at `12bf2d9` reported
**`1273 passed, 62 skipped`**.

Both runs are in the same sandbox on the same tree apart from this lap's own `docs/auto/`
prose, and neither number is wrong. The first was **COLD** and the second **WARM**: the
first run downloaded the SRTM tile mid-suite, so the six tiles-gated tests skipped then and
ran now. +6 passed, -6 skipped, exactly. This is WFG-039 producing WFG-038's symptom for the
third recorded time, inside one lap for the second.

The comparison quoted in the body of this report is the **cold** one (`1273 / 62` against
critic #7's cold `1261 / 62`), which is the like-for-like reading. The gate is green on both
and the tree is unchanged; what this shows again is that 「the suite passed N tests」 is not a
statement about a commit in this repository until WFG-039 makes that download opt-in.
