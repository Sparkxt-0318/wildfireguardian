# WildfireGuardian autoloop · dev · 2026-09-09T0950Z

| | |
|---|---|
| when | 2026-09-09 09:50 UTC · 2026-09-09 18:50 KST |
| branch / head | `auto/dev` / `f0917a5` (the work; the board and STATE ride in `e095e2f`, which is the pushed head and the commit `--assert-head` read. ⚠ report.py stamped this line `098db0c`, a commit the lap then un-made: the independent reviewer's block forced a finals-template change, and `make finals` may only stamp the screen with a commit that is already on `origin`, so the lap's commits were re-made after rebuilding at `b497a0a`. `098db0c` is orphaned and resolves on no clone) |
| kind | dev |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 8, done: 32, dropped: 1, parked: 1, title-derivable: 1, todo: 104 |

## What happened this lap

# WFG-210 — the answer's own gate kept the other systems out of the mouth, and its anchor handed them to the judge

**Row:** WFG-210 (P0, KCF), table position 1, filed by critic #49 at 2026-09-09T0820Z.
Claimed at `b497a0a` (`20260909T0917Z`), work at `098db0c`.
**Clone:** `git rev-parse --is-shallow-repository` answers **true**, `git rev-list --count HEAD`
answers **50**, measured in this clone on 2026-09-09. **No ancestry or reachability claim is
made anywhere in this report** (CHARTER §4).

## Your decisions, applied

**Nothing new to apply this lap.** The Gmail search
`from:siyeong0318@gmail.com subject:"WildfireGuardian autoloop" newer_than:14d` returns only
the loop's own sent reports — every thread holds exactly one message, and a search for
`subject:"Re: WildfireGuardian autoloop"` returns nothing. PR #31 carries no comments at all,
so the second channel is empty too. `docs/auto/decisions_seen.json` is unchanged.

Twenty-two NEEDS_HUMAN entries are open. **NH-038 came due today** and it is the one this lap
would most like answered, because this lap is itself the evidence in it: a `fix-before-next-row`
item displaced the top row again, and it was minutes, and it was worth it — which is option B
in your own words. **NH-032 and NH-034 are two days overdue** and still forbid the student from
saying any margin value out loud.

## What was wrong

Item ① of the 창의성 answer claims the contribution is the **output object**: a per-household
「걸어서 나갈 수 있는가 / 구조를 보내야 하는가」 verdict, the walking route that goes with it,
and the village dispatch list. On all four surfaces that answer it, its only anchor was
`docs/auto/knowledge/KOREAN_OPERATIONAL_SYSTEMS.md` — the landscape note about the other
systems. Two consequences, and the second is the one a judge feels:

1. `tests/test_creativity_card.py` spends four graded mutations keeping those names out of what
   the student **says**, in either direction, because this card carries no source line and a
   sentence about another system here is unsourced by construction (`WC-007/8/9`). The anchor
   then handed the judge a document full of them. A principle cited, and its opposite
   implemented one line below it.
2. A judge who reads item ① and says 「그 산출물을 하나 보여 주십시오」 had **no path from any
   surface**, while `outputs/dispatch/` has carried committed instances of exactly that object
   the whole time.

It was **inherited, not chosen four times**: written in `docs/creativity_card.md` §2 (WFG-182),
taken by the finals screen (WFG-194), by the README's front door (WFG-207), and standing
unchanged in Q29a's own 근거 line. Each of those laps did its own row correctly. That is why
this is a gate and not four edits — the next surface would have inherited it a fifth time.

**The fourth surface is this lap's own finding.** Critic #49 named three (README, screen, card
§2) and could not touch the fourth: `docs/auto/JUDGE_QA.md` is a hashed `SOURCES` entry of the
printed kit, so a critic lap editing it turns `tests/test_printables.py` red and a critic lap
may not rebuild the kit (that is NH-049, open). A dev lap can, so the bank is fixed here too —
and it is the sharpest form of the defect, because the spoken draft says 「셋 다 파일로 열어
보여 드릴 수 있습니다」 and item ①'s file was the competitors' landscape note.

## The root objection I recorded against my own plan (`hate`)

**「Adding a second link does not remove the first. A judge presses one of two links, and half
the time it is still the competitor note. An addition is not a fix.」**

Cheapest test: read the block as a judge and ask which link is first and whether either says
what it is *for*. It did not. So the instance goes **first** on every surface and each anchor
now carries its role in the visible text — 「실물과 그 한계」 for the sheets, 「정확도로 겨루지
않기로 한 판단의 근거」 for the landscape note, which is what critic #49 correctly said it
honestly is. And the gate binds the **anchor's content**, not the presence of a path, so a
later lap that drops the instance and keeps the note is red rather than merely different.

## What changed

| surface | item ①'s anchor now |
|---|---|
| `README.md:326-355` | `outputs/dispatch/README.md` (the committed index), then the landscape note |
| `web/finals.html` `CREATIVE[0].doc` (via `scripts/finals.template.html`) | a named sheet, then the index, then the note; `ko_b`/`en_b` say the instances are committed and that nothing was sent |
| `docs/creativity_card.md` §2 row 1 | a named sheet **and** the index, then the note with its role in parentheses |
| `docs/auto/JUDGE_QA.md` Q29a | the spoken draft now offers to open the file; 근거 names the index and a named sheet first |
| `docs/auto/DEMO_SCRIPT_5MIN.md` | one clause in the Q&A paragraph (outside the 300-second budget): if a judge asks to see one, open the file, not the tab |

Nothing was deleted, no comparison sentence was added anywhere, and no number entered prose.

**One asymmetry, stated rather than hidden — and the version below is the second one, because
the first was false and the reviewer blocked on it.** `README.md`'s block is the **only**
surface carrying a `\d{2,}` count assertion, and every committed instance lives under a run
stamp (`outputs/dispatch/{stamp}/…`), which that assertion reads as a count. So the front door
names the committed **index** — one click above the sheets, and the page that states the
clusters are not 행정리 and that nothing was transmitted — while the other three name a sheet
and the index. Weakening the assertion that caught WFG-117 and WFG-178, on the page a judge
reads first, to fit a path in, was the alternative and was declined.

## The gate

Four assertions in `tests/test_creativity_card.py`: an instance exists in the tree at all;
every one of the four surfaces hands a judge one; item ①'s anchors are never *all* documents
about the other systems, decided by **reading the anchors' contents** rather than by matching
their names; and the classifier that decides that still separates the two anchors.

Nine mutations, all red, each on the assertion it was aimed at — the four anchor reversions,
the directory named as prose with no resolvable file, a second document about the other systems
swapped in, the premise check pointed at a directory holding no sheets, and the two threshold
mutations below.

## What did not work, and it is the lap's most useful half

**Mutation M8 was GREEN.** Raising the classifier's threshold to a number no document in the
tree reaches makes it answer False for everything, which turns 「item ①'s anchors are not all
documents about other systems」 into 「they are not all members of the empty set」 — true of any
surface, on any tree. The whole suite stayed green. That is the **third** check this one file
has shipped that could not fail: the count assertion that was digit-only and could not see
「여섯 개」, the Korean-numeral half with a trailing `\b` that after a Hangul syllable can never
match, and now a free constant. The floor is asserted in both directions now, and M8 and its
mirror M9 are both red.

**And the harness itself was lying for twenty minutes.** M9 wrote `_ABOUT_OTHERS_MIN = 0` and
restored `= 5` in a `finally`. Same byte length, same second — and CPython invalidates a `.pyc`
on `(mtime, size)`, so every later import carried the **mutant** while the file on disk read
`5`. Two tests went red on a correct tree and their messages named the wrong cause. The tell
was that calling the function directly answered `False` while pytest answered `True`, on the
same file in the same second. The whole set was re-graded with `-B`,
`PYTHONDONTWRITEBYTECODE=1` and `tests/__pycache__` cleared between mutations; the results above
are that run. This is the MEMO entry, and the class is wider than pytest: any
write/run/restore loop over a Python file has it, and it is worst for exactly the mutation a
careful reviewer reaches for first — a single character inside a constant.

## Evidence

- `.auto/venv/bin/python scripts/auto/gates.py --mode full` → **exit 0, ALL GREEN**:
  `verify` PASS, `snapshot-verify` PASS, `env-check` PASS, `pytest-full` **1830 passed, 63
  skipped, 3 xfailed** in 262.2 s. `baseline-verify` WARNs on the two git-ignored `data/raw/**`
  contracts, which is NH-029 and §3d working as you decided.
- GitHub `auto-gates` run **280** at `65edfa3` is `success`; no `failure` in the window, so
  CHARTER §4b had no first job for this lap.
- `make finals` rebuilt `web/finals.html`; `make printables` rebuilt the kit at
  **20260909T0931Z** (53 pages, seven `SOURCES`) because three of them changed;
  `release/kcf-finals-2026/MANIFEST.json` was re-pointed **after** `git add` and
  `make finals-bundle` then verified byte-identical, 19 files. (That ordering is the
  2026-09-09T0630Z MEMO lesson: `build_finals_bundle.py` reads `git ls-files`, so a check run
  before staging answers about the previous kit.)
- Mutation harness: **`scripts/mutate_creativity_gates.py`**, committed and re-runnable by a
  stranger; the nine mutations and their verdicts are in `docs/creativity_card.md` §8.
- **One more thing your loop cannot reach you through, filed rather than guessed at.** CHARTER §6
  names a PR comment on **#31** as your second decision channel and every dev prompt tells the
  lap to poll it. PR #31 was **closed and merged on 2026-09-05T14:25Z** by the `promote` job —
  §4c working exactly as you decided — and carries zero comments. The measurement is appended to
  **NH-050**, which already asks this question in your own words, and the repair is **WFG-211**
  (P1, infra). No lap will rewrite §6 on its own; where you are told to reply is your call.

## Reviewed by

**Reviewed by: subagent (block): 「the lap closed WFG-210's anchor hole and reopened its shape
one level up, inside the document whose whole premise is that no claim stands without a file
behind it.」** Every nail fixed, none argued (CHARTER §4 step 5).

`docs/creativity_card.md` §8 — a hashed `SOURCES` entry of the printed booth kit — and the
header comment of `tests/test_creativity_card.py`, which had inherited the sentence verbatim,
both said 「the surfaces with no count assertion name a sheet directly」. **It was false.** The
finals screen has no count assertion and named the index, exactly as the README did. So the
asymmetry was presented in the *measured* register while being one real constraint (the
README's) plus an unexplained choice (the screen's), on a page printed for judges — which is
the defect class this card exists to prevent, one level up from the anchor it was fixing.

The reviewer proved it in one command instead of arguing: it put a stamped sheet path into
`CREATIVE[0].doc` and ran the eight suites that read the screen — **262 passed**, and
`check_forbidden.py` exit 0 — so nothing had ever constrained that surface.

Four fixes, all in the shipped tree:
1. **The screen names a sheet**, so the claim is now true rather than reworded away; the kit was
   rebuilt again at **20260909T0949Z** and the bundle re-pointed after staging.
2. **The false sentence is recorded, not quietly replaced**, in `docs/creativity_card.md` §8 and
   in the test header — including that the mechanism which spread it (a sentence inherited
   verbatim from the card into a test comment) is the same one that carried the original anchor
   onto four surfaces.
3. **§8's mutation accounting no longer contradicts itself.** 「Nine, all red」 sat two sentences
   from 「the eighth was green」, and the ninth was never named. It now says the numbering is the
   final set and not the order of discovery.
4. **`mandela` #5, verifier = designer, raised against the lap and not the code.** The lap
   designed, ran and scored its own nine mutations by a recipe that lived in `/tmp` — and the
   MEMO entry it wrote in the same lap says that recipe silently ran a mutant for twenty
   minutes. A claim about this repository's own state with no re-runnable artifact behind it is
   CHARTER §3.3. The harness ships as **`scripts/mutate_creativity_gates.py`**, re-run here
   against the fixed tree: nine mutations, all red, each naming the assertion that caught it.

The reviewer's own ten-mutation set, independent of the lap's by `docs/creativity_card.md` §6,
found every one of the four new assertions to have a reachable and distinct failing case — its
R5 mutated the classifier's *function body* rather than the constant and the floor still held,
and its R8 confirmed the anchor assertion is not subsumed by the surface assertion. It also
verified 33 of 33 committed sheets carry the 행정리 warnbox that Q29a now promises a judge.

Two things it flagged and did not block on, both fixed: the working tree was not empty during
the review because this lap wrote its MEMO entry while the reviewer ran (its own restores are
`git checkout --`, so a concurrent write could have been discarded silently — that is the
second MEMO paragraph), and `--assert-head` was red mid-lap, which is the pre-condition of the
`24751fa` / `8d1decf` failures CHARTER §8 names. Both assertions are green on the pushed commit.

## Next row

**WFG-027** (P0, KCF) — 일정 및 팀원 역할 배분, a named sub-item of 설계와 방법론, 20 points on
both tables, at a raw count of zero on every judge-facing surface. It is what
`docs/auto/DIRECTION.md` and `docs/auto/CRITIC_LATEST.md` both name next, and this lap does not
touch it.

## In plain terms

- **What changed for the project.** Your answer to 「무엇이 새롭습니까」 said the important thing
  you built is the *output* — the per-house 「걸어 나가시라 / 구조를 보내겠다」 sheet — and then
  the only file it offered a judge was a page about other people's systems. Every place that
  answer appears now hands over one of your own dispatch sheets first, and those sheets have been
  sitting in the repository all along.
- **Why it matters to the judges or the science.** 창의성 is 20 marks on both score sheets, and
  the question that follows it is 「그럼 그 산출물을 하나 보여 주십시오」. Until today you had
  nothing to open. Now the front door, the finals screen, the printed card and the booth script
  all point at a real sheet — and a new test refuses to let the next change quietly point back
  at the competitors.
- **What you should do.** Nothing for this row. **NH-038 came due today and this lap is the
  evidence for it**: a minutes-long fix jumped the queue again and was worth it, which is your
  own option B. NH-032 and NH-034 are two days overdue and until you answer them the student
  still may not say any margin number out loud. One reply with a few `NH-###:` lines clears them.



## Gates

**ALL GREEN** · mode `full` · head `b497a0a` · 2026-09-09T09:36:25Z · ⚠ **stale: the gates read `b497a0a`, HEAD is `098db0c`** — this table does not certify the pushed tree; re-run `gates.py --mode full` and `gates.py --assert-head` before pushing

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 16.1 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.1 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 0.6 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 262.2 s | 1830 passed, 63 skipped, 3 xfailed, 52 warnings in 261.22s (0:04:21) |

## Commits since the previous report (7f914fde59040a810e14b77ae5e5cd48899c58a7..HEAD)

- 098db0c WFG-210: the answer's own gate keeps the other systems out of the mouth and its anchor handed them to the judge
- b497a0a claim WFG-210 (20260909T0917Z)
- 65edfa3 critic #49: the gate keeps the competitors out of the spoken answer, and the answer's own link hands them over

## Needs a human (22 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09)
- NH-036 [DECISION] One critic lap told the next one not to edit a file, and that is what kept a false sentence in front of a judge for a window (by 2026-09-10)
- NH-037 [DECISION] The paper's word proxy now stops it a thousand words before your 25-page rule (by 2026-09-10)
- NH-038 [DECISION] Your "product first" rule has spent the last three dev laps on documents, and the readiness line it was written to protect has not moved in five critic laps (by 2026-09-09)
- NH-039 [DECISION] The national wildfire-spread system's manual is an 18 MB PDF the sandbox could not fetch, and one of you can (by 2026-09-12)
- NH-040 [FYI] A critic lap pushed one commit past a red `--assert-reported`, and it is telling you rather than hiding it
- NH-041 [FYI] This lap sent you an email containing only the word PLACEHOLDER, and could not take it back
- NH-042 [DECISION] Two of your own rules collide whenever a withdrawn claim lives in a frozen artifact, and this week they collided three times (by 2026-09-10)
- NH-043 [DECISION] A gate your loop built this morning will go red about twice a day, and the charter tells the lap that meets it to stop working (by 2026-09-09)
- NH-045 [BLOCKER] The staleness gate has closed `auto/dev` to every routine, and the one routine that met it is the one forbidden to clear it (by 2026-09-08)
- NH-044 [DECISION] The claim the paper just retracted is still live on the page the paper cites for it (by 2026-09-09)
- NH-046 [DECISION] Your product's definition-of-done names a command nothing in this project has ever run (by 2026-09-10)
- NH-048 [DECISION] One of the research routine's three literature channels has been dead for two runs, and a working replacement is already proven (by 2026-09-10)
- NH-049 [DECISION] Your critic routine is told to add judge Q&A cards and your own printing gate makes that impossible for it (by 2026-09-11)
- NH-050 [DECISION] You answered two of these questions two days ago and the loop never heard you, because you answered them on the routine page (by 2026-09-10)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
