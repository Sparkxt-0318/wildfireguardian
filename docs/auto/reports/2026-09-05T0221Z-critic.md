# WildfireGuardian autoloop · critic · 2026-09-05T0221Z

| | |
|---|---|
| when | 2026-09-05 02:21 UTC · 2026-09-05 11:21 KST |
| branch / head | `auto/dev` / `43710f7` |
| kind | critic |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 6, done: 15, parked: 1, todo: 58 |

## What happened this lap

# Critic #15 — the booth script arrived, and the two sentences that keep it honest are the two it says to drop first

Window `a234a1c..43710f7` on `auto/dev`, 5 commits; the 24 h window is 101 commits and
22,032 authored lines. Gates re-run independently at HEAD; GitHub's own runs read through
the MCP. This lap changed no code, no test, no data, no figure and no registry value.

## Your decisions, applied

**No new author message reached the loop by email or on PR #31 this window.** The Gmail
search `from:siyeong0318@gmail.com subject:"WildfireGuardian autoloop" newer_than:14d`
returns 41 threads and every one of them is a report the loop sent to you; none has a
reply. `mcp__github__pull_request_read` on #31 returns zero comments. So there is nothing
for `decisions.py parse` to read, and `docs/auto/decisions_seen.json` is unchanged by this
lap.

One decision **did** reach the loop in this window, through the laptop channel (CHARTER
§6b), and the 0022Z session applied it before I ran. Quoting it because it is the only
author input in the window and because how it landed matters:

> **NH-024**, closed 2026-09-05 · channel: Claude Code session (AskUserQuestion on the
> laptop) · ref `claude-code-session-7da6bf25#NH-024` · verbatim:
> **"A) Hold: full WFG-062 next lap (the very next dev lap does the whole row and nothing
> else)"**

**What it implied, and what the loop did with it.** Option A was already satisfied when you
picked it: WFG-062 closed at `e350571` at 2026-09-04T2154Z, and NH-024 had been annotated
「resolved by events」 by critic #14 an hour before your session. So your answer confirmed a
row that was done rather than redirecting a lap, and the 0022Z session recorded it as
「NH-024 held」 without spending a lap re-doing anything. That is the right handling and I
verified it from the entry and from the commit rather than from the report. The same
session turned your three expert replies into WFG-090 through WFG-093 and wrote CHARTER
§4c (`Main` follows the last green commit) — and **that last one needs one more action from
you, which nobody had filed. It is now NH-025 with three options.**

## Findings, ranked

**F1 (fix-before-next-row, WFG-095, P0, minutes). The booth script marks its two
load-bearing caveats as the sentences to cut first.** `docs/auto/DEMO_SCRIPT_5MIN.md` gives
each segment one droppable sentence marked **[버림]**. In 1막 (`:52-53`) that marker sits on
a sentence carrying its claim and its caveat together, so dropping it drops both — correct.
In **2막 (`:70-71`)** and **3막 (`:85-86`)** the marker sits on a **caveat-only** sentence
while the claim it guards stays in the non-droppable text.

- Drop 2막's and the student has just spoken two different 기록된 발생일시 clocks in
  consecutive segments — 1막's 22 / 34 / 64분 detection delays and 2막's 「2,008건 중
  79.23 %가 240분 안에」 — with nothing left saying they are different samples. That is the
  conflation `WFG-053` withdrew, NH-018 and NH-019 were opened over, and the paper lap's own
  reviewer blocked a §4.6 paragraph for at `72cf230`.
- Drop 3막's and the student has just recited 24.73 %, 9.17 %, 15.14 % and 23.67 % across
  three regions with nothing left saying the differences carry OSM mapping density — while
  **§4 item 4 of the same document** lists 「지역 간 순위」 among the sentences this script
  never says. The script's own never-say list is enforced by its most disposable sentence.

Measured, not asserted: I deleted each caveat in the working tree and ran
`scripts/check_forbidden.py`, `tests/test_withdrawn_claims_registry.py`,
`tests/test_detection_ordering_is_not_claimed.py` and `tests/test_demo_script_5min.py`.
**All four exit 0 on both deletions.** Tree restored; nothing committed. Cheapest fix: move
each marker onto the claim-plus-caveat pair, or move the caveat into that segment's ⚠ block
where the non-droppable warnings already live, keeping the six times summing to 300 s.

**F2 (WFG-096, P1, hours). The number the script calls 「이 작품의 전부」 has no uncertainty
statement anywhere in the repository, and the Q&A bank has no card for the question.** 3막
has the student say 「368곳 중 91곳, 24.73 %」 and 「458곳 중 42곳, 9.17 %」. Both keys resolve
and both value-match. Nothing in `docs/`, `paper/` or `JUDGE_QA.md` says what their sampling
uncertainty is or whether neighbouring origins are independent; Q8 answers that question for
the spread model's cell-level DeLong intervals only. A statistician judge — one of the five
lenses this project scores itself against — asks it of the routing percentages, and there is
no file to answer from. The honest answer probably exists already in the repository's own
style: 368 and 458 are a **census** of that region's walk-network origins, not a probability
sample, which is the reasoning `JUDGE_QA.md:579` applies to the survivor survey. Nobody has
written it down. Filed as a row rather than as a new Q&A card because the bank asserts its
own count of 41 in two places and its drill table is gated on covering all 41.

**F3 (WFG-097, P1, minutes, parked by §14b). The script's number half is gated well; its
prose half is not gated at all.** Sixteen mutations written from the document before reading
the test body: `tests/test_demo_script_5min.py` **caught 9 of 16**, and **10 of 16** counting
the tree's other claim gates. Every numeric mutation was caught — a spoken number drifting
from the table, a table value drifting from the registry, an unresolvable key, a 구두 row
relabelled 화면, a new spoken number missing from the table, times that stop summing to 300,
a cumulative bracket contradicting its segment, a 화면 row citing a real key the template
never renders, a count contradicting its own percentage. That is a much better instrument
than the string tripwires this repository has measured before (2/20, 1/20), and it deserves
saying. Uncaught, all on the prose half: the CHARTER §9 DRAFT label can be deleted (the Q&A
bank has a test for this, the script does not), a 화면 row can be downgraded to 구두 freely,
the ⚠ block at `:88-93` separating 26.6 % (경사 보정) from 15.14 % (시간 인지 우회) can be
deleted, and the caveats of F1. **P1 and not a fix-before-next-row item**: this is gate work
and CHARTER §14b holds it behind R1, R3, R7, R8 and R9.

**F4 (WFG-098, P1, minutes). The re-cut document Q18 now sends a judge to does not list the
layer Q18 tells the student to name.** The repaired Q18 teaches 「지진해일긴급대피장소 92개」
and cites `docs/juso_yeongdeok.md`; that file's contents table at `:58` lists the 사물주소도형
layers as 「지진옥외대피장소, 무더위쉼터, 인명구조함, 소화전, 비상급수시설, 버스정류장」 and
**does not include 지진해일긴급대피장소**, whose count appears nowhere in the file's prose.
It survives only in the header sentence at `:3` and in the registry. This is the layer whose
**0 rows** were the evidence that the first cut was the wrong county (`:20-21`), so it is the
one layer the document cannot afford to omit from its own contents. The Q18 figures
themselves are all correct: I resolved `juso_yeongdeok_samul_eqout_point_count` = 64,
`..._eqwav_...` = 92, `..._coolingcen_...` = 17 against the registry.

**F5 (no row; recorded on R3 and as NH-025). Two red `auto-gates` runs in the window, and
they are a new kind.** Runs **103 (`a2a2994`) and 104 (`c8124a8`) are `failure` with the
`gates` job PASSED and only the new `promote` job failed** — CHARTER §4c's fast-forward of
`Main` cannot run while `Main` requires a pull-request review. No lap reported green over a
red gate, so this is **not** finding #1 and it does not take the fix-before-next-row slot.
Fixed at `b3244f8` twenty minutes after the first red, inside §4b's hour, and runs 105–107
are `success`. What it changes is durable and belongs in every lap's reading from now on:
**a red `auto-gates` run can now be a promotion refusal with green gates behind it, so read
the failing job, not the run.** The author action it needs is NH-025.

**F6 (no row, said once). The 0022Z session pushed with a gate table its own report marked
stale** (`docs/auto/reports/2026-09-05T0022Z-manual.md`: 「the gates read `21b8740`, HEAD is
`1f69c10`」), and `a2a2994` is the commit that carried it. That is your own laptop session
rather than a routine, so it is yours to weigh, not a row against the loop; recording it
because CHARTER §4 step 8 is the rule the loop has broken most often and the two red runs
above rode in on the same push.

## Root objection (`hate`) on the current headline narrative

The headline the project now tells is: *「불이 지금 어디 있는가」가 아니라 「불이 다음에 어디로
갈 것인가」를 풀었고, 그래서 368곳 중 91곳이 시간 인지 경로에서만 대피 지점에 닿습니다.*

**The one load-bearing objection.** The 24.73 % is a comparison between two routers on
**one synthetic hazard surface produced by a model whose own operating-point recall is
0.138**, and the script says both numbers out loud four minutes apart without ever saying
they are the same object. 마무리 admits the recall honestly. 3막 presents the 24.73 % as the
project's entire point. A judge who joins them asks: *if the forecast finds 13.8 % of the
cells that burn, what is the 24.73 % a measurement of?* The defensible answer exists —
the router consumes a **cumulative** hazard field at a different cutoff from the forward
simulation's per-step threshold, which is exactly what `JUDGE_QA.md` Q8/Q17 and the ML-lens
interruption sentence in §2 say — but **the script never says it in the five minutes**, and
the interruption sentence only fires if a judge asks. If nobody asks, the strongest claim
and its strongest limitation pass each other without meeting.

**Cheapest test, and it is one sentence, not a lap.** Add to 3막 or to the 마무리 handoff:
「이 24.73 %는 예측이 맞았는지의 측정이 아니라, 같은 위험면 위에서 두 라우터가 서로 다른 답을
내놓는 비율입니다.」 Then read 3막 and 마무리 aloud back to back and see whether a listener
still has to be told. If one sentence closes it, the objection was a presentation defect; if
it does not, the objection is about the coupling and belongs in the manuscript, not the
booth. **I did not file this as a row on its own** — it lands inside WFG-095's fifteen
minutes on the same document, and CHARTER §14b caps me at one item.

## `factchk` on the new prose claims about the world

The window's new world-facing prose is `DEMO_SCRIPT_5MIN.md` and the Q18 rewrite.

- 「GK2A 2 km 화소」, 「750 K 화염 온도 가정 → 0.1939 ha」 — method assumption, registered as
  `det_size_floor_ha_tf750`, and the script itself says the value must be read 자릿수로만
  because the flame temperature is assumed. **Holds.**
- 「산림청 산불통계 2,008건 중 79.23 %가 240분 이내, 중앙값 120분, 100 ha 이상은 4,025분」 —
  all four resolve in `docs/NUMBERS.json` at the quoted values. **Holds**, with F1 against
  how its caveat is marked.
- 「트리거에서 출동 목록까지 약 25초」 — `docs/live_pipeline.md` §6 measures trigger→list warm
  at 24.93 and 26.87 s, cold at 27.92 and 29.86 s, and instructs in its own words 「quote
  "about 25 seconds" and treat 30 s as the safe upper bound」. The script takes the first
  half and drops the second. **Holds as cited**; the booth laptop is cold-started for the
  first judge, so the 30 s upper bound is the half worth carrying. Folded into WFG-095's
  same-document pass rather than filed separately.
- Q18's 「지진옥외대피장소 64 / 지진해일긴급대피장소 92 / 무더위쉼터 17」 and 「그중 지정
  「산불」 대피소는 하나도 없습니다」 — all three counts resolve at those exact values, and the
  layer list in `docs/juso_yeongdeok.md` contains no fire category, so the scoped negative is
  supported. **Holds**, with F4 against the document it cites.
- 「2025년 3월 경북 산불」 in the opening — unchanged, untouched, and per the routine's own
  rule not rewritten by this lap.

**No fabricated citation, no unregistered figure and no rounded-away limitation found in the
window's new prose.** That is worth stating plainly after a window whose main output was 231
lines of new judge-facing Korean.

## `prism` — the five lenses, one line each

- **KCF judge, software professor.** For the first time the demo has a shape: 20-second
  reset, six segments, five interruption answers. The A* objection has a good one-sentence
  answer (time-expanded graph, edges disappear) and it is in §2. Would still ask where the
  0.50 closure threshold came from; §1 says `config/default.yaml` and the script does not
  defend it.
- **KCF judge, disaster-response official.** 「출동 목록이 마을 단위로 나오고 인터넷 없이
  돌지만 최종 판단은 사람이고 문자는 모사」 is exactly the right sentence and it is in §2. The
  gap this lens still sees is **which agency acts** — filed as WFG-091 from your expert
  reply, not yet written.
- **Fire scientist.** 240분 being 「행동의 창」 rather than 불의 수명, with the 100 ha median at
  4,025분 beside it, is the single most credible thing the script says. Would ask the spread
  rate of 의성 itself; the repository still cannot answer (WFG-065, 8.2 km h⁻¹ lives only in
  a knowledge note).
- **ML reviewer, hunting leakage and weak baselines.** The 0.138 is stated in the student's
  own closing 45 seconds, unprompted, with the three empty folds and the 1.97 % prevalence —
  that is the right posture. See the root objection for the one thing missing.
- **Statistician.** 폴드 평균 AUC 0.89 with SD 0.107 quoted as a spread rather than a point is
  correct practice. The routing percentages have no equivalent — F2.

## Judge drill against `docs/auto/JUDGE_QA.md`

Mechanical half, run this lap: **all 77 file paths the bank cites resolve** (five are cited
as bare basenames — `check_forbidden.py`, `check_number_collisions.py`, `delivery/sms.py`,
`routing_demo.npz`, `routing_demo_canonical.npz` — and all five exist under `scripts/`,
`src/` and `data/processed/`), and every registry key it names resolves; the six remaining
snake-case tokens are config field names, not keys. Hardest ten attempted from files only:
nine answerable, including the two the bank labels 「그건 측정하지 않았습니다」 (Q34 spread
rate, Q8 cell independence) where declining **is** the answer and the file says so. **The one
I could not answer from any file is F2**, filed as WFG-096.

## Direction check (CHARTER §3b) and readiness

**One row moved, and it is the first reorder in four critic laps that is not a deferral.**
`WFG-036` (P0, the release bundle, R9, due 09-10) was at table line 69 — **below** `WFG-088`
and `WFG-089`, two P1 hygiene rows critic #14 filed on 09-04 — so the next `todo` a dev lap
would take in table order was a minutes-long prose repair on `docs/withdrawn_claims.md`
rather than the bundle. CHARTER §14b holds exactly those rows behind R1, R3, R7, R8 and R9
and §3b forbids a P0 below a non-P0, so this was the table contradicting the charter, not a
judgement call. WFG-036 now sits directly under WFG-095. I did not move WFG-037; one move is
the limit, and `DIRECTION.md` names it instead.

**Readiness: R4 ticked, 4 of 11, and it is the first line to move in SIX critic laps** (last
was R2 by critic #8 at `12bf2d9`, 0750Z on 09-04). Ticked on my own reading, not the lap's
claim: six segments summing to exactly 300 s with consistent cumulative brackets, five
judge-lens interruption sentences, **all 33 registry keys** in the §3 mapping table resolved
and value-matched against `docs/NUMBERS.json`, the two quoted screen sentences found in the
built `web/finals.html`, and `default_region` confirmed as `uiseong_andong_2025` at
`scripts/build_finals.py:846`. The half a test cannot reach — five minutes of Korean fitting
in five minutes — is R12/NH-014 and the tick does not claim it. `web/` was untouched for a
thirteenth window, so R1 could not move; `BOOTH_SETUP.md`, `release/kcf-finals-2026/` and the
printables still do not exist.

**Census.** Judge-facing lines this window: **257 of 943 authored (27.3 %)**, of which 231
are the script itself, against critic #14's 4.9 % and critic #13's 2.6 %. Over 24 h it is
1,275 of 22,032 (5.8 %) with `docs/auto/reports/` at 8,513. The window is the first evidence
that the product-first steer is reaching the tree; the 24 h figure is why WFG-084's cap is
still worth running.

## Gates and certification

`gates.py --mode full` exits **0** at `43710f7` in a fresh cloud sandbox: **1464 passed, 62
skipped** in 317.3 s, COLD (six SRTM-gated tests skip, WFG-039). Against critic #14's cold
`1453 / 62` at `ed35f0d`: **+11 passed, skips unchanged**, ninth comparable window. `verify`,
`snapshot-verify`, `env-check` PASS; `baseline-verify` WARN, expected off-laptop, `hard:
false`, fifteenth window and still not a finding. Green at HEAD for a **twelfth** consecutive
critic lap. `--assert-head` and `--assert-reported --base a234a1c` both exit 0. Every dev
report of the last 24 h carries a `Reviewed by:` line, seven of seven, clean for a second
window.

## Scorecard deltas

**Track B 82 → 83.** 제출 자료 **17 → 18** on the demo script, held below 19 by F1. 데이터
수집·분석·해석 **19 held** — no result registered or withdrawn; F2 is a gap, not an error.
연구 목적 16, 설계와 방법론 15, 창의성 15 all held: a demo script is not methodology.

**Track A 74 → 76.** 구현 및 유용성 **15 → 16**, the first move on that row about the product
being usable rather than a defect being closed, and notably **not** a commit to `web/` — the
screen has not changed in thirteen windows; what changed is that there is now a written way
to run it five times. 제출 자료 **13 → 14**, returning the point critic #14 took off for the
stale Q18 answer, which this window fixed.

## In plain terms

**What the judges would see today.** For the first time, a booth visit that works: a
20-second reset between judges, a five-minute Korean script whose every spoken number traces
to a committed file, and one prepared sentence for each of the five kinds of judge who will
interrupt you. I checked all 33 numbers in it myself and they are all correct. Four of the
eleven readiness boxes are now ticked, the first to move in about fifteen hours.

**What changed since yesterday.** The script exists (that is box R4, which I ticked), and
your Q&A card about shelters stopped telling you a portal was blocked when it no longer is.
Two GitHub checks went red overnight, but the tests were fine both times — only the new
「update Main automatically」 step failed, and it was fixed twenty minutes later.

**What you should do.** One thing needs you: **NH-025**. The automatic `Main` update you
asked for cannot run until you allow it on GitHub, so reply with A, B or C. Everything else
is the loop's: the next lap has one fifteen-minute fix on the script (the two caution
sentences are marked as the first to cut, which is backwards), then it builds the release
bundle. And when you first read the script aloud with a timer, write the real seconds in the
margin — that number is the one thing in it no test can check.



## Gates

**ALL GREEN** · mode `full` · head `43710f7` · 2026-09-05T02:06:23Z · current at `43710f7`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 21.0 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.2 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 2.4 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 317.3 s | 1464 passed, 62 skipped, 52 warnings in 315.57s (0:05:15) |

**Post-commit run, at the commit this report is pushed in (CHARTER §4 step 8).** The table
above was written by `report.py` before this lap's commit existed, so it names `43710f7`, the
head it read. Re-ran the full gates on the commit itself rather than editing that line:

**ALL GREEN** · mode `full` · 2026-09-05 · `verify` PASS 20.2 s · `baseline-verify` WARN 0.2 s
(expected off-laptop, `hard: false`) · `snapshot-verify` PASS 0.5 s · `env-check` PASS 0.1 s ·
`pytest-full` PASS 312.6 s, **1470 passed, 56 skipped**.

**This block names no commit id on purpose, and that is the point of writing it this way.**
The first draft of it named the id the gates had just certified, `8c19f07`; appending the
block changed the tree, the amend that carried it rewrote the commit, and the id in the
sentence was dead the moment it was written. That is `a562045` and WFG-067 again, in a report
about not doing it. A block that certifies the commit it travels in cannot spell that commit's
hash, so this one does not try. The mechanical certification is
`gates.py --assert-head`, which exits non-zero unless `.auto/gates.json` records a `full` pass
at exactly the pushed `HEAD` with a clean tree; it and `--assert-reported` were run
immediately before the push and both exit 0. `report.py`'s own table above is left exactly as
it was written, naming `43710f7`, the head it read.

The suite counts differ from the run above in the expected way and it is worth saying which:
`1464 passed, 62 skipped` was the COLD first run in this sandbox, where six SRTM-gated tests
skip (WFG-039); `1470 passed, 56 skipped` is the WARM re-run, where those six execute. Total
collected is unchanged at 1526. The like-for-like comparison against critic #14 in the body of
this report uses the cold number, as every critic lap's comparison has.

## Commits since the previous report (c8124a801607c001aaa88c5daa318fd4aac43a97..HEAD)

- 43710f7 the report named the head the rebase had rewritten, and this lap checked instead of being caught
- 4b44cc4 the row this lap filed as WFG-090 is WFG-094: a research lap had already published 090-093
- 96f8ff5 the reviewer was right: a key in the page is not a value on the screen, and an untracked file is invisible to the gate
- 0aac8fc the five minutes a judge actually gets now has a script, and the script is read by a test
- b3244f8 promote job: a refused fast-forward of Main is a warning, not a red run

## Needs a human (5 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-025 [DECISION] `Main` cannot follow the green commit until you change one GitHub setting, and until you do, every green push emails you a red run (by 2026-09-08)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
