# WildfireGuardian autoloop · critic · 2026-09-06T1414Z

| | |
|---|---|
| when | 2026-09-06 14:14 UTC · 2026-09-06 23:14 KST |
| branch / head | `auto/dev` / `dd500e6` |
| kind | critic |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 7, done: 16, parked: 1, todo: 74 |

## What happened this lap

Critic lap #27. Reviewed head `dd500e6`; window `b2bdaf0..dd500e6` (six commits: critic #26's
own push, the WFG-117 claim, the WFG-117 repair, its reviewer's second pass, and two report
header corrections), plus the 24 h to 2026-09-05T14:00Z for the gate, CI and report checks.
Changed no code, no test, no data, no figure and no `docs/NUMBERS.json`; wrote only under
`docs/auto/`.

## Your decisions, applied

**Nothing new is waiting.** The Gmail search
`from:siyeong0318@gmail.com subject:"WildfireGuardian autoloop" newer_than:14d` returns 25
threads. Every one of them was sent by this loop, and every one holds exactly one message
with no reply on it. `docs/auto/decisions_seen.json` is unchanged and
`scripts/auto/decisions.py` was not invoked. PR #31 carries no comment in the
`NH-###: <decision>` form. Your open items are still NH-032, NH-033, NH-034 and NH-035, and
this lap adds NH-036 below.

## What this lap verified rather than read

- `gates.py --mode full` **ALL GREEN** at `dd500e6`: `1569 passed, 62 skipped`, cold, 304.3 s.
  Against critic #26's cold `1565 / 62` at `b2bdaf0`: **+4 passed, skips unchanged**, like for
  like. `verify`, `snapshot-verify` and `env-check` PASS; `baseline-verify` WARN is CHARTER §3d
  information, not a finding.
- **No red CI run behind a green report, so there is no gate finding and no CHARTER §4b finding
  this lap.** Read through the GitHub MCP (the routine's `curl` returns 403 through the proxy,
  WFG-119): `auto-gates` runs **145 to 173** on `auto/dev` are **22 `success` and 3
  `cancelled`** (`828bbae`, `ef61e9b`, `9ebf5a5`, each superseded within minutes by a green
  push). Run 173, at this exact head, is `success`. **Zero `failure` in the window.**
- `--assert-head` and `--assert-reported` both exit 0. Every **dev** report in the window
  carries `Reviewed by:`; the 1313Z lap's reads `subagent (block)` and the row went back to
  `todo` because of it, which is CHARTER §5 working as written.
- The clone was **fully unshallowed** before any ancestry sentence in this report was written:
  `git rev-parse --is-shallow-repository` answers `false`, 488 commits. That is the control
  the last seven laps on this subject did not have.

## Critic #26's falsifiable test, both branches, answered

**(1) The withdrawal stands, and now it is measured with the instrument rather than around
it.** On the unshallowed clone: `git merge-base --is-ancestor 41498ef HEAD` exits **0**;
`git rev-list --count 41498ef..HEAD` answers **283**, which is critic #26's 277 at `b2bdaf0`
plus this window's six commits, so the two measurements agree exactly; `git rev-list HEAD |
grep -c 41498efbf0679276c140b3cbfc0819e5265e7733` answers **1**; `git branch -a --contains`
names `auto/dev`, `origin/auto/dev` and `origin/Main`. WFG-115 stays withdrawn at P1.

**(2) The item mechanism DOES carry prose. Do not escalate it.** Q30's ⚠⚠ block no longer
says 「326 · 268」. And the dev lap carried it further than the item asked: the card now names
**three** reason-buckets for irreproducible values instead of two (the third, `external`, is
the largest and the draft had omitted it entirely), holds **no** live count at all, and ships
`tests/test_judge_qa_bank.py::test_the_cards_account_of_the_irreproducible_covers_every_bucket`,
which goes red when the registry grows a reason the card does not describe. That is the right
direction for a gate: it fails on the failure that actually happened, not on a phrase somebody
chose. Its own reviewer returned `block` on the first draft and was right both times.

## Findings, ranked

**F1 · `docs/auto/JUDGE_QA.md` Q35 scripts the student to say a false sentence to a judge, and
critic #26 forbade editing it. WFG-133. THE ONE `fix-before-next-row` ITEM.**

Q35 is **T1**, the reproducibility question: 「화면 아래 적힌 커밋을 받아서 이 화면을 그대로
다시 만들 수 있습니까?」. Its draft answer says the stamp is 「현재 브랜치에서 닿는
커밋입니다」, which is **true**. The ⚠ block underneath tells the student not to say that and
to say 「레지스트리 카드의 각인은 … **지금 브랜치에서 닿지 않습니다**」 instead.

| the block says | the truth at `dd500e6`, measured here on an unshallowed clone |
|---|---|
| `merge-base --is-ancestor 41498ef HEAD` exits non-zero | it exits **0** |
| `branch -a --contains` finds it on two superseded branches only | it names **`auto/dev`, `origin/auto/dev`, `origin/Main`** |
| the card beside it prints **326** | it prints **383** (WFG-113, `1ec1d06`) |

Critic #26 withdrew exactly this measurement at 1100Z, into `CRITIC_LATEST.md`,
`KCF_READINESS.md` R1 and `DIRECTION.md`, and in the same lap wrote **「`JUDGE_QA.md` Q35 is
correct as written and must not be edited」** into all three, plus 「Q35 needs no change」 into
WFG-115's backlog cell. That is true of Q35's draft answer and false of the ⚠ block that
overrides it. The effect of writing it was to protect the false half from repair for a full
window. I have lifted the instruction for the ⚠ block only, in writing, and left a dated ⚠⚠
correction note with the table above on Q35 at this head so nobody rehearses the false block
tonight.

**F2 · The booth printables PDF is stale against the Q&A bank it prints, by exactly the text
this window withdrew, and no gate reads for it. WFG-134 (P0).**

`docs/auto/finals/printables/manifest_20260906T0620Z.json` records `docs/auto/JUDGE_QA.md` at
sha256 `2c8451211e5f97eb…`; the file at `dd500e6` hashes `af955a30fa500391…`. I re-hashed all
four sources: `BOOTH_SETUP.md`, `DEMO_SCRIPT_5MIN.md` and `DETECTION_FLOOR_CARD.md` all match,
so the drift is one file and it is the 17-page one. The PDF was built at `3e92b69` (06:20Z);
WFG-117 rewrote Q30 at `fc05320`. **So the paper the student physically carries to the booth
still holds the pre-WFG-117 Q30 with the 「326 · 268」 warning this window removed, and Q35's
false block.** `tests/test_printables.py` has 15 tests:
`test_the_manifest_records_what_the_pdf_was_built_from` checks the manifest *has* a sha256 per
source, and `test_every_pdf_has_its_manifest_beside_it` checks the PDF matches its own hash.
**Nothing compares a recorded source hash against the working tree**, which is the one
comparison that detects a stale printable, so the suite is green on an out-of-date kit. Pair
this with WFG-130 in one lap: the reconciliation sheet R7 names and the manifest calls
nonexistent goes into the same rebuild. CHARTER §3.2 means a **new stamp beside**
`WFG_printables_20260906T0620Z.pdf`, never over it.

**F3 · Q30's repaired card invites a judge to count by a field that 18 of the 58 entries do
not have. WFG-135 (P1).**

The card says 「어느 부류가 몇 개인지는 `docs/NUMBERS.json` 의 `reproducibility.status` 로
지금 함께 세어 보실 수 있습니다」. Counted here in one process: of the 58 entries with
`reproducible: false`, **24** carry `status = "external"`, **16** carry `"not_reproducible"`,
and **18 carry no `reproducibility` block at all** (`mlbase_*`, `farband_*` and that family).
A judge who accepts the invitation finds 31 % of the class unlabelled, and the card's third
bucket is an *interpretation of an absent field* rather than a recorded reason. The gate is
right — it maps `None` deliberately and its docstring says why. What over-reaches is the
promise. Two repairs close it and either is fine: write a `reproducibility.status` on the 18
(a registry **addition**, permitted by CHARTER §3.2), or say in the card that the absence of
the field is itself the third label. Do not close it by dropping the invitation; the
invitation is the best sentence on the card.

**F4 · not a finding, a correction to the record.** Critic #26 wrote that every *critic*
report of the last 24 h carries `Reviewed by:`. `docs/auto/reports/2026-09-05T2330Z-critic.md`
does not. Critic laps have no subagent reviewer by design, so this is a wrong sentence rather
than a missing practice, and the routine's actual requirement — every **dev** report — holds.

## The root objection (hate)

**This loop measures whether a correction was made. It never measures whether the correction
arrived.**

Twice in one window, on the same file, a repair landed in the source of truth and not in the
surface a human meets. Critic #26 withdrew the `41498ef` finding into the three pages the
*loop* reads and left it standing in the card the *student* reads aloud, then wrote a sentence
that protected it. WFG-117 repaired Q30 in `docs/auto/JUDGE_QA.md` and the PDF built from that
file was not rebuilt, so the printed kit still carries the withdrawn text. Both are the same
failure, and neither is a lapse of care: both laps did careful, verified work and wrote
「withdrawn」 truthfully about the object they were holding.

The gap is that this repository's honesty discipline is built on **re-deriving values** —
`make verify`, the collision scan, the payload re-derivation gate — and every one of those
reads a *number*. A withdrawn *sentence* has exactly one gate behind it,
`tests/test_withdrawn_claims_registry.py`, and neither of this window's two withdrawals was
entered into it. So a correction propagates by whoever remembers to grep.

**The cheapest test is a grep**, and it is now a standing rule on `docs/auto/DIRECTION.md`:
before writing 「withdrawn」 anywhere, grep the judge-facing surfaces (`README.md`, `web/`,
`docs/auto/JUDGE_QA.md`, `docs/auto/DEMO_SCRIPT_5MIN.md`, `paper/manuscript.md`, the newest
printables manifest) for the withdrawn string. Both of this lap's P0 rows would have been
caught by it, in seconds, by the lap that created them.

## Direction check (CHARTER §14, five minutes)

**No existing row moved.** Two new P0 rows enter at positions 1 and 2 — WFG-133 and WFG-134 —
and every existing row keeps its order behind them, so CHARTER §14's ordering rule is not
touched and no P0 sits below a non-P0. Critic #26's position-1 row, WFG-117, is not demoted so
much as spent: its judge-facing half shipped at `fc05320` and what remains is grading the
row's own gate against a registry-moving mutation, which is hygiene and belongs behind two
judge-facing falsehoods under §14b.

**Readiness lines ticked in the last 24 h: zero, for a fourth consecutive critic lap, and for
the second lap running the count is not about direction.** The window cleared the one item it
was given, cleared it well, and shipped a gate that did not exist. What the zero exposes is
narrower and cheaper than critic #26's version of it: the loop's laps are correcting each
other faster than corrections reach the booth. 4 of 11 (R2, R4, R5, R6). R1 waits on WFG-110's
six unmapped registry keys alone. R7 now waits on two things instead of one: WFG-130's missing
reconciliation sheet, and WFG-134's staleness.

## Scorecard

Appended at `dd500e6`: Track B **84** (17 / 15 / 19 / 15 / 18), Track A **78**
(16 / 15 / 18 / 15 / 14). **Every row held**, and the hold on Track A 제출 자료 is F1 and F2
rather than a note. The Q30 repair is worth +1 on its own — three buckets instead of two, no
live count, a gate shaped at the failure that happened. It is spent exactly, on a T1 card in
the same file telling the student to say something false, and on the printed version of that
file being out of date. This table's own precedent from critic #8 (F42) is the rule I applied:
a 제출 자료 row does not rise while the document the student recites from tells them to say
something wrong. Track B 데이터 held at 19 on two moves that cancel: the new registry-side
bucket gate (+) against F3's 18 unlabelled entries (-). 연구 목적, 설계와 방법론 and 창의성
held on both tracks — nothing in this window touched a model, split, metric, arm, coupling or
protocol, and the manuscript did not move.

## Escalation

**NH-036 (DECISION, MEDIUM, open, by 2026-09-10)** — one critic lap told the next not to edit
a file, and that is what kept a false sentence in front of a judge for a window. Four options
on the entry, from 「an instruction expires at the next critic lap and must name the exact
lines it covers」 to 「only the author may lift one」. Until you answer, the loop runs option B,
which is what this lap already did: a later lap may lift an instruction by publishing a
contradicting measurement, in writing.

## In plain terms

**What the judges would see today.** A project whose gates are all green (1,569 tests) and
whose booth screen is correct. But the card you would answer from has one question, about
whether a judge can rebuild your screen from the commit printed on it, where the card tells
you to say something that is not true about your own repository. And the booth PDF that was
printed this morning still carries yesterday's version of another answer. Both are minutes of
work for the next lap and both are already at the top of the list.

**What changed since yesterday.** The lap before this one fixed the biggest thing on that
card, and fixed it properly: it stopped writing today's numbers into the answer at all, so it
cannot go stale again, and it added a test that goes red if the project ever grows a new kind
of number the card does not explain. That is the best repair this loop has made to the Q&A
bank. Nothing else moved: no model, no result, no figure.

**What you should do.** Nothing is blocking you and nothing needs your laptop today. If you
have five minutes, reply to NH-036 (should one day's reviewer be able to tell the next day's
reviewer 「do not touch this file」?) and to NH-032 and NH-034, which have been open since
09-05 and are the ones actually holding a number back. And do not rehearse from the printed
PDF until the next lap rebuilds it; read the Q&A bank on screen instead.



## Gates

**ALL GREEN** · mode `full` · head `dd500e6` · 2026-09-06T14:05:13Z · current at `dd500e6`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 18.4 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.1 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 2.2 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 283.4 s | 1569 passed, 62 skipped, 52 warnings in 281.67s (0:04:41) |

## Commits since the previous report (fc05320bca53acea1e1c9afe6afa31114319f86b..HEAD)

- dd500e6 the dev report names the head it ships in as well as the head it was generated at
- a866b25 the reviewer found the same defect one clause later in the sentence I had just fixed

## Needs a human (9 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09)
- NH-036 [DECISION] One critic lap told the next one not to edit a file, and that is what kept a false sentence in front of a judge for a window (by 2026-09-10)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
