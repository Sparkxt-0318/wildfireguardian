# WildfireGuardian autoloop · critic · 2026-09-07T1717Z

| | |
|---|---|
| when | 2026-09-07 17:17 UTC · 2026-09-08 02:17 KST |
| branch / head | `auto/dev` / `7cc4eb7` |
| kind | critic |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 8, done: 25, parked: 1, todo: 87 |

## What happened this lap

Critic lap #36. Window: `719c420` (2026-09-06T21:20Z) to `7cc4eb7` (2026-09-07T16:12Z), which in this
clone is the whole resolvable history: `git rev-parse --is-shallow-repository` = **true**,
`git rev-list --count HEAD` = **50**, measured here today. It holds two dev laps (1256Z, 1600Z), two
critic laps (#34, #35) and three paper laps. No code, no artifact and no `docs/NUMBERS.json` entry was
touched by this lap; everything written is under `docs/auto/`.

## Your decisions, applied

**Nothing new on either channel, and this is the third consecutive lap that can say so in one line.**
The Gmail search `from:siyeong0318@gmail.com subject:"WildfireGuardian autoloop" newer_than:14d`
returned 25 threads on the first page (estimate 201 in total) and **every one is a single message this
loop sent to you**; not one thread carries a reply. A second search for the reply syntax itself
(`to:siyeong0318@gmail.com "NH-" newer_than:14d -subject:"WildfireGuardian autoloop"`) returned two
threads, both `ci-red` reports the loop sent. PR #31 carries no comment in the `NH-###:` form.
`scripts/auto/decisions.py seen` was therefore never called on a new id, no `apply` ran, and
`docs/auto/decisions_seen.json` is byte-identical to what the last lap pushed.

Sixteen entries in `docs/auto/NEEDS_HUMAN.md` are open and waiting on you. **This lap adds none**, and
that is deliberate: the one thing it found that could have been an escalation (nothing in the
repository detects a newly written flat negative about somebody else's system) is agent-doable, so it
is a backlog row (**WFG-168**) instead. Seven open entries have dates inside the next four days and
they are in the `Decisions needed` block of this email.

## Verification of the loop's own claims

Everything below was re-run here rather than read out of a report, on the routine's **default** clone
before any deepening.

| check | result |
|---|---|
| `gates.py --mode full` at `7cc4eb7` | **ALL GREEN, exit 0** — `1665 passed, 62 skipped, 2 xfailed`, 260.5 s, **cold** |
| `verify` / `snapshot-verify` / `env-check` | PASS |
| `baseline-verify` | WARN, the documented CHARTER §3d sandbox state |
| `gates.py --assert-head` | exit **0** at `7cc4eb7` |
| `gates.py --assert-reported --base 719c420` | exit **0**, 58 substantive paths travelling with `docs/auto/reports/2026-09-07T1600Z-dev.md` |
| GitHub `auto-gates` on `auto/dev`, runs **186 to 215** | **28 `success`, 2 `cancelled` (197, 200), ZERO `failure`**; run **215** at this head is `success` |
| `Reviewed by:` in every report in the window | present in all 14 dev and critic reports; **absent** in `2026-09-06T1838Z-research.md` (WFG-147, unchanged) |
| `web/finals.html` build stamp | `7308b06`, **19** commits behind `HEAD`, inside the gate's limit of 30 |

**No CHARTER §4b finding this lap.** No dev lap reported green where GitHub was red, and no push in the
window travelled without a report. `curl` against `api.github.com` returned 403 here again, so the runs
were read through the GitHub MCP (WFG-119).

⚠ One forward-looking number, taken rather than guessed: the stamp is 19 commits behind and this branch
put those 19 commits down in 7 h 40 m, about 2.5 per hour. At that rate the 30-commit staleness gate
fires in roughly **four and a half hours**, that is, inside the next dev lap but one. That is NH-043
exactly, and it is not a finding against this window.

## Findings, ranked

### 1. WFG-166 — the T0 card that won a scorecard point four hours ago contains the sentence its own do-not-say list forbids, and it is printed

`docs/auto/JUDGE_QA.md:650-652` has the student say aloud, in **Q16a · T0**, that G-DAPS
「사이렌을 어디에 울릴지는 정해 주지만 **특정한 집의 어느 길이 위험에 들어가고 어느 길이 들어가지
않는지는 말해 주지 않습니다**」. Lines **671-672** of the same card say:
❌ 「저쪽은 가구 단위로는 못 합니다」라고 **단정하지 마십시오**; the permitted form is
⭕ 「**공개된 자료에서는** 가구 단위 산출물이 확인되지 않습니다」; and the reason given is
「심사위원이 그 시스템을 직접 써 본 분일 수 있고, 그때 무너지는 것은 이 답변 하나가 아니라 신뢰
전부입니다.」 Twenty lines apart, written by the same lap.

Three things make it worse than a slip:

- **It is the Korean half of `WC-008`**, registered at 16:12Z on the English spelling
  `walk out, and along which path`. A registered spelling is a copy-paste ratchet, not a claim
  detector (`docs/withdrawn_claims.md` §4), so it structurally cannot reach a Korean rendering. This is
  the exact mirror of what happened to `WC-007` ninety minutes earlier, inside the same lap, in the
  other direction: registered in Korean, English half found later in `KOREAN_OPERATIONAL_SYSTEMS.md`
  §3.3. The dev lap wrote that lesson down and then shipped its mirror image.
- **`JUDGE_QA.md:625` asserts that this card is clean**: 「운영 시스템에 대해서는 Q16a가
  「공개된 자료에서는 확인되지 않습니다」라고**만** 말합니다.」 That is false at `:651`, and it is the
  sentence a later lap would read to decide the card needs no work.
- **It is on paper.** `JUDGE_QA.md` is a `SOURCES` file of kit `20260907T1551Z`, 17 of its 38 pages,
  and the kit is inside the release bundle a judge is handed.

This is **this lap's one `fix-before-next-row` item** under CHARTER §14b: one sentence on a
judge-facing surface, plus the one `make printables` that WFG-152 requires. ⚠ A critic lap may not do
it: `docs/auto/DIRECTION.md` forbids critic and research laps from editing `JUDGE_QA.md` at all,
because a one-line edit turns `test_the_newest_printable_is_not_stale_against_the_tree` red unless the
same lap rebuilds the kit.

### 2. WFG-009 raised P1 to P0 — closing WFG-110 left R1 blocked by a row parked behind R1

`WFG-110` closed well at `60c07c8` and I re-derived it rather than reading it:
`scripts/finals_screen_keys.py` returns **28** keys, **all 28** appear in
`docs/finals_screen_numbers.md`, and **all 28** are keys of `docs/NUMBERS.json` (383 entries). That is
R1's second clause met.

R1's **first** clause is 「opens from `file://` with Wi-Fi off, **all four acts advance**」 and nothing
in this repository has ever exercised the word *advance*. I took the cheapest honest reading rather
than inheriting the 2026-09-05 hand-run:
`/opt/pw-browsers/chromium-1194/chrome-linux/chrome --headless=new --disable-gpu --no-sandbox --virtual-time-budget=8000 --dump-dom file://$PWD/web/finals.html`
exits **0** and returns **3,449,612 bytes** of post-JS DOM holding `view-live`, `view-evidence`,
`view-reliability`, `view-system` and the labels 라이브 · 근거 · 신뢰성 · 시스템; the built file has
**zero** `http(s)` `src`/`href` references. ⚠ What it does **not** show: at initial load `view-live` is
1,157,688 B and `view-reliability` 2,063,451 B, while **`view-evidence` is 225 B and `view-system` 235 B
with no Korean text at all**. Those two views build on switch, and `--dump-dom` presses no key. So the
two evidence acts a judge is walked through are exactly the ones no measurement covers.

`WFG-009` is the row that covers it, it has sat at P1 since the kickoff **inside the block CHARTER
§14b holds behind R1**, and eleven infra rows are held behind R1. A row that unblocks a readiness line
cannot be parked behind that readiness line. That is critic #31's WFG-026 finding and critic #33's
WFG-119 finding recurring a third time; the rule that produces it is yours, and **NH-038** already asks
you about it. **P1 → P0 is this lap's one row move** (CHARTER §14 direction check, one per lap).

### 3. WFG-167 — the judge drill's own finding: 42 cards, and none about responsibility

`git grep -niE '책임|법적|면책|사람이 다치' docs/auto/JUDGE_QA.md` returns **zero** at this head. The
claim exists on exactly two surfaces and neither is one the student rehearses from:
`web/finals.html:1393` renders 「**최종 판단은 언제나 사람이 내립니다.**」 and
`docs/auto/DEMO_SCRIPT_5MIN.md:217` gives it as a one-line interruption answer for the 재난대응 실무자
lens. One of the five KCF finals judges is a public-sector disaster-response official, the interview is
1:1 and about five minutes, and the natural follow-up to a screen printing a rescue order is 「이 순서
대로 갔다가 사람이 못 나오면 누가 책임집니까」. This is **WFG-144's asymmetry in the other direction**,
the fourth instance of the shape: the claim is on the screen and on the printed script, and the bank
the student recites from has no card.

### 4. WFG-168 — five withdrawals of one claim family in one day, none of them caught by a gate

The 1600Z dev report hands this here in its own words: 「the gates read prose for registered spellings;
nothing in this repository detects a **newly written** flat negative about somebody else's system. That
is a finding, not a fix, and it is left for the critic.」 It is right, and the count is five, not four,
because the Korean half of WC-008 was still live when I looked (finding 1). What is missing is a lint
rather than a claim detector: a committed list of third-party subjects, a list of negative predicates,
a list of licensed hedges, and a red build for any sentence pairing the first two without the third.
Filed **P1**, deliberately, because CHARTER §14b holds loop hygiene behind R1, R3, R4, R7, R8 and R9.

### 5. Unchanged and untaken, re-checked rather than re-filed

- **WFG-139**, ninth consecutive measurement, taken on this lap's own clock:
  `data/raw/dem/srtm/N36E129.hgt` (**25,934,402 B**) and its `.gz` have mtime **17:02:33Z**, inside a
  `pytest-full` run that began about 17:00Z, in a container whose `data/raw/` held only `.gitkeep` and
  `README.md` at start.
- **WFG-163** is still `todo` and `docs/auto/knowledge/PYROGEOGRAPHY.md` §2 still states that G-DAPS
  「entered trial operation in April 2026」 as accomplished fact. I did not re-fetch the 경향신문 article
  and I do not claim its wording as my own measurement; critic #35 did, four hours ago, and recorded
  「이르면 다음 달부터」.
- **WFG-147**: the research report is still the one report in the window with no `Reviewed by:` line.
- The **1600Z dev report's own gate table** ships marked `stale: the gates read 13c79c1, HEAD is
  60c07c8`, and the commit it was pushed in is `7cc4eb7`, later still. The prose beside it reports a
  green unpiped run, GitHub run 215 at `7cc4eb7` is `success`, and my own run here is green, so the
  **tree is right and the report's table is what is wrong**. Loop hygiene, held behind R1 by §14b,
  covered by the existing report-certification rows; recorded here rather than filed again.

### 6. What I checked and found sound, because restraint is a result

- `docs/related_work.md` names four `docs/*.md` paths that do not exist, and that is **correct**: they
  are the record of paths the 0711Z reviewer found dead, each licensed with `<!-- dead-path-ok -->` and
  held true by `tests/test_related_work_paths.py::test_the_paths_named_as_dead_are_still_dead`. This is
  the right shape for CHARTER §3.7 and I want it on the record as such.
- The `42` and `91` caveat trace holds at this head, re-run rather than inherited: both numbers appear
  only in `README.md`, `web/finals.html`, `docs/auto/JUDGE_QA.md`, `docs/auto/DEMO_SCRIPT_5MIN.md` and
  `paper/manuscript.md`, and every one of those five carries the noiseless-forecast wording.
- Every file path cited in `docs/auto/finals/RELATED_WORK_PANEL.md`, `docs/auto/DEMO_SCRIPT_5MIN.md`
  and `docs/finals_screen_numbers.md` resolves. `JUDGE_QA.md`'s ten unresolved strings are all bare
  basenames used in prose (`build_numbers.py`, `DIRECTION.md`), not broken paths.
- `WFG-162` is genuinely closed on the printed panel, and the dated note beside it **describes** the
  withdrawn sentence instead of reprinting it. That is `WC-005`'s precedent used correctly on a page
  that prints, and it is the single best thing in the window.

## Root objection (`hate`)

**A registered withdrawal reaches one language, and nobody has ever registered both halves in the same
lap.** `WC-007` went in in Korean and its English half surfaced ninety minutes later in the same
knowledge note. `WC-008` went in in English and its Korean half is on the T0 card the student says out
loud. The loop's response to the WC-004 lesson was to register harder, and registration is the right
tool, but the ratchet is per-spelling by construction and the project ships in two languages. **The
cheapest test:** for the next claim any lap withdraws, before it calls the withdrawal done, run the
subject grep once in Korean and once in English and write down both results. If the second language
returns nothing on three consecutive withdrawals, this objection is wrong and I will have said so.
It has returned something on two out of two so far, both inside one day.

## Scorecard deltas

**Track B 86 (17 / 18 / 18 / 15 / 18), Track A 83 (16 / 18 / 19 / 15 / 15). Every row holds on both
tracks, and the flat window is the finding rather than the absence of one.** Each of the window's three
real gains bought a point that is spent, in the same window, on a defect measured here:

- the screen-to-key index (+1 제출 자료, both tracks) against the Korean half of `WC-008` printed on 17
  of the kit's 38 pages;
- WFG-110 closing R1's second clause (+1 A 구현 및 유용성, pre-registered by critic #34) against
  **NH-014**, which was on critic #32's original three-part list at `0fc6130` and is unchanged and is
  yours;
- the withdrawn-claims registry work against the fact that five withdrawals in one day were found by
  readers and none by a gate.

The pre-registration for the next critic, so this is falsifiable: **when a driver advances all four
acts of `web/finals.html` and uploads four screenshots (WFG-009), Track A 구현 및 유용성 goes to 20.**

## In plain terms

- **What the judges would see today.** A strong booth. The screen opens with no internet and now has a
  one-page index that answers 「이 숫자는 어디서 나온 겁니까」 for all 28 numbers on it, and the printed
  panel no longer claims anything about the two government systems that nobody here has actually
  checked. One sentence is still wrong, and it is on the paper you would hand over: the Q&A card about
  those two systems tells you to say they cannot work house by house, and the same card, one page
  later, tells you never to say exactly that.
- **What changed since yesterday.** Two rows shipped and both were real. The last piece of the
  「every number traces to a source」 checklist item is done. What that revealed is that the other half
  of the same checklist item, 「the screen's four sections actually advance」, has never been tested by
  anything, so the row that would test it moved to the top of the list.
- **What you should do.** Nothing for this lap; the loop can do all four items itself. If you have ten
  minutes, **NH-039** is still worth more than anything else here: downloading the 국립산림과학원 user
  guide (about 18 MB) on your laptop would let three documents say what that system actually does
  instead of saying we did not check, and it is the root of four of the five sentences the loop had to
  withdraw today.



## Gates

**ALL GREEN** · mode `full` · head `7cc4eb7` · 2026-09-07T17:04:33Z · current at `7cc4eb7`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 20.1 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.1 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 2.4 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 260.5 s | 1665 passed, 62 skipped, 2 xfailed, 52 warnings in 259.38s (0:04:19) |

## Commits since the previous report (-15)

- 7cc4eb7 WC-008: the paper lap's reviewer named two live copies, and registering them found two more
- a737198 report 2026-09-07T1600Z-dev, WFG-165, and the pipe that nearly became somebody else's bug
- b076b23 WFG-162 + WFG-110: the panel stops asserting what it never read, and R1's last blocker gets its reverse index
- 27b76f1 NH-043 was renamed by my own string replace, and the report it generated asked one question twice
- 8ff1b40 paper §2 retracted a claim about a manual nobody opened, and its own bibliography had said so
- 13c79c1 claim WFG-162 with WFG-110 (20260907T1528Z)
- 6ee996b the report the assert asked for, covering the correction that had none
- 84bc307 the critic's third finding was an accusation it had not checked, and NH-041 had already said it
- 40cac91 critic #35: the narrowing reached two files and missed the one that gets printed
- b54ca28 the dev report names the head it ships in, and the re-run that certified it
- 1f024ed WFG-144 + WFG-146: the card a judge's likeliest question needed, and the negative claim it made me find
- 0713c7e claim WFG-144 with WFG-146 (20260907T1222Z)
- 1a5caa2 critic #34: the caveat reached every surface a judge meets and missed the page every lap reads first
- 2720840 the board names the rebased head and both laps that landed in this window
- b8ee618 report 2026-09-07T1002Z-dev: WFG-119, and the reviewer's block on a number I interpolated

## Needs a human (17 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09)
- NH-036 [DECISION] One critic lap told the next one not to edit a file, and that is what kept a false sentence in front of a judge for a window (by 2026-09-10)
- NH-037 [DECISION] The paper's word proxy now stops it a thousand words before your 25-page rule (by 2026-09-10)
- NH-038 [DECISION] Your "product first" rule has spent the last three dev laps on documents, and the readiness line it was written to protect has not moved in five critic laps (by 2026-09-09)
- NH-039 [DECISION] The national wildfire-spread system's manual is an 18 MB PDF the sandbox could not fetch, and one of you can (by 2026-09-12)
- NH-040 [FYI] A critic lap pushed one commit past a red `--assert-reported`, and it is telling you rather than hiding it
- NH-041 [FYI] This lap sent you an email containing only the word PLACEHOLDER, and could not take it back
- NH-042 [DECISION] Two of your own rules collide whenever a withdrawn claim lives in a frozen artifact, and this week they collided three times (by 2026-09-10)
- NH-043 [DECISION] A gate your loop built this morning will go red about twice a day, and the charter tells the lap that meets it to stop working (by 2026-09-09)
- NH-044 [DECISION] The claim the paper just retracted is still live on the page the paper cites for it (by 2026-09-09)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
