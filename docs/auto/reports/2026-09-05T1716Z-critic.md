# WildfireGuardian autoloop · critic · 2026-09-05T1716Z

| | |
|---|---|
| when | 2026-09-05 17:16 UTC · 2026-09-06 02:16 KST |
| branch / head | `auto/dev` / `ce262fe` |
| kind | critic |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 6, done: 16, parked: 1, todo: 68 |

## What happened this lap

Critic #20. Window `8a8a940..ce262fe` on `auto/dev`: 7 commits, two of them the author's own
laptop sessions, 1,670 authored insertions with images, the generated board and the `.docx`
excluded. Gates run here at the pushed head; findings verified on disk or by running the thing,
never read from the lap that claimed it.

## Your decisions, applied

**No new decision arrived by email, and that is now a two-day pattern rather than a gap.** The
Gmail search `from:siyeong0318@gmail.com subject:"WildfireGuardian autoloop" newer_than:14d`
returns 30 threads and **every one of them is a message the loop sent you**; not one carries a
reply. `docs/auto/decisions_seen.json` agrees: its `seen` list is empty and all 20 applied
decisions carry a `claude-code-session-*` reference. `scripts/auto/decisions.py seen` therefore
had nothing to run against, and no `NH-###:` line reached this lap through the mail channel or
through PR #31 (its comment list is empty). The laptop channel (NH-020) is the one you actually
use and it is working.

Two decisions that arrived through that channel **before** this window shaped this lap, so they
are quoted here rather than treated as history:

- **NH-027, closed by you 2026-09-05**, verbatim: *"A) Run it in the sprint now, P0 — the
  present-perimeter + buffer arm of WFG-033(b) on 의성·안동, one buffer, one lap, report the number
  whatever it says (new row at the top of the table after WFG-062). On the second question: the four
  P0 infra rows below P1 rows are loop hygiene and are demoted to P1 under CHARTER §14b."*
  **Applied and checked:** the row is at the top of the table (BACKLOG line 38) and WFG-051,
  WFG-076, WFG-078 and WFG-082 all read `P1`. **What had NOT been applied is the part that decides
  what the next lap builds:** `docs/auto/DIRECTION.md`, which CHARTER §14b says the dev lap follows
  in preference to the table, never named the row. See F3. It is now first on that page.
- **NH-029, executed by you at `38620f2`** (option A, the baseline re-freeze). Re-run here rather
  than quoted, and it worked: see F5.

## Findings, ranked

**F1 · WFG-115 (new, P0, judge-facing) · the judged screen prints a second commit id and it is not
on this branch.** The 검증 레지스트리 evidence card renders
`'built at commit ' + DATA.registry.built_at_commit` (`web/finals.html:1924`,
`scripts/finals.template.html:1924`), fed by `scripts/build_finals.py:628` from `docs/NUMBERS.json`
`built_at_git_commit`. The value is `41498ef`. Run here:

```
git cat-file -t 41498ef                     -> commit
git merge-base --is-ancestor 41498ef HEAD   -> exits non-zero
git branch -a --contains 41498ef            -> origin/auto/lap-b1989d5-superseded
                                               origin/ordering-boundary
```

That is the **exact** WFG-067 failure shape (a stamp that exists but is not reachable), in the same
panel WFG-067 was about, and the gate WFG-067 added never looks at this field: both ancestry
assertions in `tests/test_finals_screen.py` (`:544`, `:550`, `:649`) read `_payload()["git"]`, which
is `5f9a3b8` and is fine. Two commit stamps on one screen, one gated, one not.

It is also **stale by construction**: `built_at_git_commit` was written when the registry held 153
entries (`41498ef` is "session 8 closes: 1085 green, 153 registered"), and the card beside it prints
**326**. `build_numbers.py` is deliberately never re-run (WFG-040), so it will keep drifting.

And it falsifies half of a judge-facing answer. `docs/auto/JUDGE_QA.md` Q35 (T1, drill round 3) has
the student say the panel's hash 「이 저장소에 실제로 있고 **현재 브랜치에서 닿는** 커밋입니다」.
True of one stamp, false of this one; the answer's 「없는 것」 line disclaims only byte-identity of the
HTML. The whole 「이 질문의 내력」 paragraph holds WFG-067 up as this project's best example of how it
handles reproducibility, which is what makes a second, ungated stamp worse than an ordinary stale
string. A ⚠ 「근거 없음」 note is added to Q35 in this lap with the sentence to say instead, so the
student does not rehearse the wrong one; rewriting the answer is WFG-115's job, after the gate proves
what it may say.

**F2 · a duplicate backlog id, and one of the two rows is yours.** Two live rows carried `WFG-108`:
the `make finals-bundle` row filed by the 1302Z dev lap at 13:02Z, and the fair-opponent arm your
laptop added at 14:24Z (`9442430`). The dev lap picks its row by id, and a report saying "WFG-108
done" would be ambiguous between a P1 infra row and a P0 science row. Repaired the way CHARTER
precedent sets (WFG-040, WFG-094: an id others quote wins): the bundle row keeps `WFG-108`, because
it is quoted in `docs/auto/finals/BOOTH_SETUP.md:86` (judge-facing, Korean), `docs/auto/MEMO.md:1012`,
critic #19's `CRITIC_LATEST.md` and `docs/auto/reports/2026-09-05T1302Z-dev.md`; the fair-opponent row
becomes **WFG-114**, with the reason written into both the table row and its details section. **Your
decision text names no id**, so nothing you wrote moves. Reports are records and are not edited, so
the 1424Z and 1449Z manual reports still say `WFG-108`.

**F3 · the page that outranks the backlog table did not know you had moved a row.** `9442430` landed
at 14:24Z and put the fair-opponent arm at the top of the table, exactly where you asked. Critic #19
rewrote `docs/auto/DIRECTION.md` at 14:12Z, twelve minutes earlier, naming WFG-109, WFG-106, WFG-104,
WFG-007 and a tail of eleven more rows, and nothing since added yours. CHARTER §14b says the dev lap
takes **the page's** order over the table when the two differ. So for one full lap the page steered the
loop away from the one row you personally promoted into the sprint, and it is the row that answers
this routine's own standing root objection. Fixed on the page. The transferable rule, written into the
direction note: **the page is re-read after an author push, not only after a dev lap.** This finding
is about the loop, so under §14b it is a note and not a row.

**F4 · WFG-116 (new, P1) · the paper's page ceiling is enforced by a gate that cannot run anywhere the
loop runs.** `paper/check_paper.py` `page_check()` gates your 25-page rule (NH-028) only when
LibreOffice **Writer** is present, and the lap that measured 21 pages installed Writer inside a sandbox
that no longer exists. Verified at `ce262fe`: `/usr/bin/soffice` is there,
`.auto/venv/bin/python paper/measure_pages.py --why` prints "No LibreOffice Writer on this machine",
and `libreoffice-writer` and `fonts-crosextra-carlito` grep to **zero** hits across
`.github/workflows/`, `scripts/auto/bootstrap.sh`, `requirements.txt` and `Makefile`. So the only
branch that can fail is dead in every cloud lap and every `auto-gates` run, and `paper/STATE.json`
`built_pages: 21` is the one field in that file nothing re-derives (the drift check skips it unless
`metrics_ok`). The file's own docstring names the risk the word proxy cannot see: a **figure**, which
costs a page and no words. Not urgent (21 of 25 pages, 7,461 of 9,000 words), filed so the number does
not quietly rot. Fix is one apt line in the workflow, or a gate that fails when `built_pages` is
recorded and unverified since the last figure changed.

**F5 · NH-029 executed, and it is the best thing in this window.** You ran option A at `38620f2`.
Re-run here rather than read from the commit message: `make baseline-verify` now reports **2**
differences against `944243054a59`, not six, and both are the git-ignored `data/raw/firms_data/`
manifests that exist only on your machine. The four in-every-clone differences are gone. And the
re-freeze **preserved every protection**, which is the thing CHARTER §3.2 exists for and which a
sandbox re-freeze would have destroyed: diffing `38620f2^` against `38620f2`, both
`untracked_contracts` hashes and all four `protected` artifact hashes are byte-identical, and
`tracked_processed` went 127 to 130 (the three `pace_*.json` files). R3 still does not tick, because
its own command needs one run on your machine, which is NH-029's remaining half.

**F6 · checked and NOT findings, recorded so no later lap re-derives them.**
(a) **WFG-109's gate is real and I graded it myself.** I edited the corrected STATIC VIEW caption in
`scripts/finals.template.html` back to the withdrawn wording and re-ran the suite: 2 failed, 7 passed.
The invariant it asserts (the built screen is the template line-for-line outside the one payload line)
is stronger than the caption comparison the row asked for, and it covers `web/console.html` too.
(b) **WFG-113 reproduces exactly as filed.** I changed `"n_entries": 326` to `999` on
`web/finals.html:434` and ran the named guards: `test_finals_template_sync.py` and
`test_finals_screen.py` 41 passed, `check_forbidden.py` OK, `verify_numbers.py` OK. A false number
stays on the judged screen with everything green.
(c) **factchk on the window's one new claim about the world.** `paper/measure_pages.py` and
`paper/check_paper.py` call Carlito "the metric-compatible stand-in for Calibri". Debian's own package
page for `fonts-crosextra-carlito` reads "Sans-serif font metric-compatible with Calibri font"
(packages.debian.org/sid/fonts-crosextra-carlito, opened 2026-09-05). Holds. No other new prose in the
window asserts anything about the world; the fire-science and Korean-source surfaces were untouched.
(d) The payload's `git` stamp is `5f9a3b8`, an ancestor of `HEAD`, and its gate passes. F1 is about the
**other** stamp only.

**F7 · loop hygiene, clean, one line.** `auto-gates` runs 117 to 136 on `auto/dev` carry **no
`failure`**; 136 at this head is `success`; 131 and 125 were `cancelled` by a superseding push
(WFG-102, already filed). Every consecutive pair of commits in the window passes `--assert-reported`.
Every dev, paper and critic report in the last 24 h carries `Reviewed by:`; the four without it are
`manual`, which is your own laptop and is not a lap. `gates.py --mode full` exits 0 here at `ce262fe`
(`1515 passed, 62 skipped`, 202.0 s, cold; +9 like for like over critic #19's cold run).

## fix-before-next-row: none, and the reason is a rule

`docs/auto/DIRECTION.md` now names two rows above anything I found: **WFG-114**, your NH-027 row, and
**WFG-007**, the printables, raised P1 to P0 on critic #19's falsifiable test. An item would displace
one of them. Readiness has been 4 of 11 for **five** consecutive critic laps while four of the last
five dev slots went to a critic's item; critic #18 proved the empty block moves the product, critic #19
set an item anyway and readiness did not move. WFG-115 would qualify under CHARTER §14b and is filed as
a P0 row, third on the page, instead.

## Direction check (CHARTER §3b)

**One row moved, and it is the one critic #19's test named.** WFG-007 raised P1 to P0 and placed
directly under your WFG-114 at the top of the table; its details section carries the reason. Critic
#19 wrote: 「if WFG-109 is closed and the printables still do not exist, then WFG-007's priority is the
defect, not its position」. WFG-109 is `done(20260905T1520Z)` and `docs/auto/finals/` holds two `.md`
files and no PDF. R7 and half of R9 wait on this row alone.

**KCF_READINESS lines ticked in the last 24 h: zero.** Still 4 of 11 (R2, R4, R5, R6), unmoved for five
critic laps. The 「zero for two consecutive laps」 rule fires and is answered by setting no item, not by
another direction note.

## Root objection (hate)

**The headline credits the forecast with what merely seeing the present fire would have bought.** Every
comparison the project ships is against `naive`, which is fire-blind
(`src/wildfireguardian/routing/evacuation.py:270`, `docs/real_roads_real_hazard.md:50`). Three
consecutive critics have written this and the cheapest test has been costed twice. What changed is that
it is no longer a critic's objection: you closed NH-027 with "Run it in the sprint now, P0", so it is
answered by running WFG-114 and reporting whatever the number says. The only thing that had gone wrong
is that the page ordering the work did not carry it (F3).

## Scorecard

B 84 to **83**, A 79 to **78**, both on F1 and both scored once: a Track B judge takes an unreachable
commit id in the reproducibility panel out of 제출 자료 (출처 명기); a Track A judge takes it out of
구현 및 유용성 (작품 완성도), because something on the judged screen is wrong today and was not a lap
ago. Track B 데이터 수집·분석·해석 holds at 19 on two moves that cancel: the re-freeze up, the
unverifiable `built_pages` down. Everything else holds. The window's real gains are holes closed rather
than capability added, and the identity gate's own disclosure paragraph had to be corrected inside the
same commit. Evidence per row in `docs/auto/SCORECARD.md`.

Reviewed by: critic lap (this is the independent review; CHARTER §10).

## In plain terms

**What the judges would see today.** A finals screen that works, and one line on it that is wrong: an
evidence card says the screen was "built at commit 41498ef", and that commit is not on your branch, so
a judge who takes it and tries to rebuild cannot. Your Q&A card for that exact question currently tells
you to say it is reachable. I put a warning on the card with the sentence to say instead, and filed the
fix. Still no printed paper for the booth, on the sixth day.

**What changed since yesterday.** Your baseline re-freeze worked and it was safe: I re-ran the check
here and six problems became two, and both of those only exist because the raw data lives on your
laptop. The screen-versus-template bug from yesterday is genuinely fixed, and I broke it again on
purpose to prove the new test catches it.

**What you should do.** Nothing is waiting on you today. Three decisions are still open (NH-005,
NH-014, NH-029's last half, which is one `make all-checks` run on your own laptop). The next two laps
should build your fair-opponent experiment and then the printables, in that order, and I have written
both onto the page the loop follows.



## Gates

**ALL GREEN** · mode `full` · head `ce262fe` · 2026-09-05T17:02:23Z · current at `ce262fe`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 14.9 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.1 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 1.9 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 202.0 s | 1515 passed, 62 skipped, 52 warnings in 200.68s (0:03:20) |

## Commits since the previous report (-15)

- ce262fe board and lap images rebuilt on the rebased head, so they show the paper lap's report too
- ab429af report 2026-09-05T1542Z-dev, and the disclosure paragraph that named two gates without running either
- 18c724a WFG-109: the withdrawn sentence is out of the source, and the gate is the whole file rather than the two lines
- ada8175 the paper's page ceiling is measured and checked, not converted from a word count
- 5f9a3b8 claim WFG-109 (20260905T1520Z)
- 38620f2 bundle plan = git-tracked files only (a local demo video broke the manifest on the laptop); baseline re-frozen after three cloud-lap artifacts
- 9442430 four author decisions: Main promotes (NH-025), paper lap on the full gate (NH-026), fair-opponent arm WFG-108 into the sprint (NH-027), paper limit 25 pages (NH-028)
- 8a8a940 the report names the head it reviewed, and now also the head it ships in
- 6361373 critic #19: the file the judged screen is generated from still says what the screen withdrew
- 92bfc4f the plain-terms line told the author every command had been run, in the report whose reviewer proved one had not
- d43033b report 2026-09-05T1302Z-dev, and the one line of the booth recipe that was copied instead of run
- 5aecc5f WFG-037: the booth day is written down, and running its own commands falsified three of them
- b108043 claim WFG-037 (20260905T1217Z)
- e2628f3 the assertion said no and the push went anyway, because both were on one line joined by a semicolon
- c8765e5 NH-005 fell out of the count for the third consecutive lap, and this time a gate should catch it

## Needs a human (5 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-029 [DECISION] The baseline freeze is stale, so `make all-checks` cannot pass — on this machine or on yours (by 2026-09-10)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
