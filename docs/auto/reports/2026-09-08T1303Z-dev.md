# WildfireGuardian autoloop · dev · 2026-09-08T1303Z

| | |
|---|---|
| when | 2026-09-08 13:03 UTC · 2026-09-08 22:03 KST |
| branch / head | `auto/dev` / `ea04478` |
| kind | dev |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 8, done: 28, parked: 1, title-derivable: 1, todo: 94 |

## Head annotation — which commit this report names, and which one it ships in

`report.py` stamps the header table and the gate table at the head it **runs on**, which
is the head this lap's work ended at: **`ea04478`**. Committing the report is itself a
commit, so this file actually ships in **`0d884c6`**, and that is the commit pushed to
`auto/dev`.

Both ids resolve, checked rather than asserted (`git cat-file -t` answers `commit` for
each, and `git rev-parse origin/auto/dev` answers `0d884c6`). This is **not** the WFG-183
failure, which was a report certifying ids that existed nowhere after a rebase; there was
no rebase here (`git pull --rebase` reported `Current branch auto/dev is up to date`), and
the two ids differ only because one is the work and the other is the work plus this
report.

⚠ **The gate table below therefore names `ea04478`, and the commit that was pushed was
certified separately:** `gates.py --mode full` was re-run after the report was committed
and exits **0** at `0d884c6` (`1749 passed, 63 skipped, 2 xfailed`, pytest 473.4 s, read
unpiped), with `--assert-head` and `--assert-reported` both exiting 0 immediately before
the push. Refreshing the table instead would only move the same staleness one commit
along, so it is labelled rather than chased.

The report email's header line carries `@ ea04478` for the same reason.

## What happened this lap

# Dev lap 2026-09-08T1240Z — WFG-187 (the screen) then WFG-010 (the front door)

## Your decisions, applied

**Nothing new to apply, and here is what was actually checked rather than assumed.**
The Gmail search `from:siyeong0318@gmail.com subject:"WildfireGuardian autoloop"
newer_than:14d` returns 25 threads and **every one is a single message the loop itself
sent** (each carries the `SENT` label and each thread holds exactly one message, so
none is a reply). PR #31's comment list is **empty**. So there is no `NH-###:` line
from you in either channel, `decisions_seen.json` is unchanged, and no entry was
closed this lap.

⚠ **Fourteen entries are open and none has been answered since NH-031 on 2026-09-06.**
NH-032 and NH-034 went overdue on 09-08; **NH-035, NH-038 and NH-043 come due
tomorrow, 09-09**. This lap's own work ran into two of them and is shaped by them,
which is said in the technical account rather than filed as a fifteenth question.

---

## What this lap did

Two rows, in the order CHARTER §4 and `CRITIC_LATEST.md` set: the one
`fix-before-next-row` item first and pushed on its own, then the table's first `todo`
row.

### WFG-187 — the judged screen, cleared before anything else (pushed at `b2c494f`)

`web/finals.html` named `1bca8ed` and `git rev-list --count 1bca8ed..HEAD` answered
**29** against `tests/test_finals_screen.py`'s `STAMP_MAX_COMMITS_BEHIND = 30`. **One
commit of headroom.** This lap's own claim commit would have spent it and the next
commit would have turned `auto/dev` red for every routine — dev, critic and paper —
before any work was done. `make finals` took the count **29 → 0**; measured at the
head this report ships in, the screen is **5** commits behind, so the next lap meets
the full thirty.

`release/kcf-finals-2026/MANIFEST.json` was re-pointed in the **same commit**: the
bundle hashes `web/finals.html`, so the rebuild alone turned two assertions in
`tests/test_finals_bundle.py` red, each naming its own fix (`make finals-bundle
UPDATE=1`). A lap that ran `make finals` and stopped would have pushed a red tree.
That is WFG-152's rule applied to the screen instead of the printables kit.

**The thing worth keeping from it, for NH-043:** critic #42 measured this drift four
times and *each measurement was itself a commit*, so the headroom it was reporting
went 5 → 1 while it was reporting it. The critic may not run `make finals`. **The only
routine that can see this gate coming is the one routine forbidden to clear it.** That
is no longer a prediction; it is in the record.

### WFG-010 — README Round-4 section + English abstract draft (`692497a`, `ea04478`)

Both of readiness line **R8**'s conditions existed nowhere. `grep -nE '^## Round'
README.md` returned `:59` and `:75` and nothing for Round 4, every day this week, and
the README carried no English abstract at all. It now returns `:59`, `:75` and
**`:200` Round 4 (2026-09 — 본선 준비)** — below Round 3, with the Round-2 record
untouched (CHARTER §3.12; the README diff is a pure addition, no deleted lines).

**The section is deliberately not a feature list, and that was the root objection to my
own plan** (`hate`, recorded before building): *a Round-4 section is the most tempting
place in this repository to publish a changelog of the loop's own activity — a bundle,
a Q&A bank, a kit — which is loop hygiene wearing a judge-facing costume, adds no
measured result, and opens a fresh surface of hand-typed self-counts, the exact class
DIRECTION names (「여섯 개」, `1708/63`, 「933 gated files」).* The cheapest test was to
count the numbers the section needs: if it cannot be written without hand-typing counts
of the repository's own artifacts, it is a changelog. So it points at files and
commands instead of at counts, and where it does print a number the number is
registered. The printables bullet says in its own text why no page count is written
there.

What it says: Round 4 added **no new model, no retrain, no new region**, and its own
reading note states that **three of its four items argue against the project** —

1. the **fair opponent** (present perimeter + buffer, no model at all) that recovers
   most of what the fire-blind contrast credited to the forecast, with the admission
   that it has **never been run on 영덕**, where the 42 comes from;
2. the **withdrawn-claim registry**, printed beside its own two measured limits — a
   reworded assertion escapes, and the scan reads `.md` and `.html` only, so `make
   verify` green is **not** evidence a claim is gone;
3. the **two negative results** (deadline-first dispatch never out-rescues
   nearest-first at the committed window — 0; the geostationary detection floor);
4. and only then what a judge can open.

⚠ **The fair-opponent margins are deliberately absent and the absence is stated in the
section's own voice**, because NH-032 and NH-034 are open and DIRECTION bars those
numbers from every judge-facing surface until they are answered. That is your decision
showing up as a hole in the product, which is the honest way for it to show up.

The **abstract draft** is labelled a draft in its own opening lines and points at
`paper/AUTHORSHIP.md` for who rewrites it — CHARTER §9 asks that the label sit where
the draft is *read*, not only in a file the reader never opens.

`tests/test_readme_round4.py` is the part that outlives the lap. **Nothing in `tests/`
read R8 before**: it was graded four windows running by a critic typing a `grep` into a
report, which is the hand-typed-fact failure class DIRECTION names. Eight tests now
bind R8's three conditions, §3.12's ordering, §9's draft label, that every link in the
section resolves, and DIRECTION's both-caveats rule on 42.

### What did not work, and it is the most useful thing here

**The independent reviewer blocked the push, and it was right.** `692497a` added a
Korean bullet stating **42곳** with *neither* binding caveat in its own block — and the
same commit's test docstring named that exact sentence as the one mutation the gate
could not catch. **Naming a mutation you cannot catch is not a substitute for not
shipping it.** Critic #41's rule asks a lap to publish the denominator of its own
coverage claim; publishing it and then writing an instance into the same commit turns
an honesty practice into an alibi.

The reviewer also found the two mechanisms, neither of which I had seen:

- `tests/test_future_aware_attribution.py` classifies a claim block with a regex that
  **requires the denominator** (`458 … 42`). The new sentence dropped it, so the block
  was never classified as a claim at all and that gate could not have fired.
- My own guard asserted a hand-listed count of 「four known sites」 — a list written
  **after** looking at the artifact, which whitelisted the offending line. The artifact
  passed by construction. That is the `mandela` leakage pattern, and the tell is that
  the scope is a count.

Fixed in `ea04478`: the Korean bullet carries both caveats in its own block (including
that the real value is lower and that **no run in this repository measures how much
lower**); the gate is keyed on the **bare** number in **either** language across every
block, so dropping the denominator buys no escape, with exactly one site exempted **by
name and with a reason** (the Round-3 correction table, a §3.7 record) plus a second
test asserting that exemption still matches exactly one block; and the R8 note and the
module docstring, which both overstated the coverage, now say what was wrong instead.

**Mutation grading, with the denominator published** (WFG-186's rule, honoured before
WFG-186 is taken). Nine mutations on the first version, six red; the seventh — written
to attack the *assertion style* rather than the feature — came back **GREEN**: deleting
the fire-blind caveat from the 42's own sentence passed, because a *different* claim
four lines later also says `fire-blind`. That is critic #41's WFG-185 defect
reproducing inside a test written after it. After the fix, the reviewer's own nail and
five further mutations all go red, including a fresh uncaveated Korean sentence written
elsewhere in the file and a widened exemption.

⚠ **The mutation this gate still cannot catch, and it is live:** the file reads
`README.md` only. The same uncaveated sentence on `web/finals.html`, `docs/auto/JUDGE_QA.md`
or the printed panel passes everything in it, and `test_future_aware_attribution.py`'s
denominator regex leaves the same hole on those surfaces. **WFG-168** is the bilingual
lint that closes it and it does not exist yet.

**R8 is annotated and NOT ticked — the critic ticks (CHARTER §10).**

## Evidence

| what | where |
|---|---|
| the Round-4 section | `README.md:200` |
| the English abstract draft | `README.md` §`Abstract (draft)` |
| the new gate | `tests/test_readme_round4.py` (8 tests) |
| the screen rebuild | `web/finals.html`, `release/kcf-finals-2026/MANIFEST.json` |
| readiness note | `docs/auto/KCF_READINESS.md` R8 (annotated, ☐) |
| lessons | `docs/auto/MEMO.md` |

**Gates.** `gates.py --mode full` exit **0** at `ea04478`: `1749 passed, 63 skipped, 2
xfailed`, pytest 462.4 s, read unpiped. `verify`, `snapshot-verify`, `env-check` PASS;
the `baseline-verify` WARN is the documented CHARTER §3d sandbox state. **The run
downloaded nothing** — `du -sb data/raw` answers **201,187** bytes before and after,
and `data/raw/dem/srtm/` is empty. **Cold on the tile, warm on `data/cache`.** This
clone is **shallow** (`--is-shallow-repository` = `true`, depth **55**), so this report
makes no ancestry or reachability claim.

**GitHub's own run (CHARTER §4b): no finding.** Through the GitHub MCP. `auto-gates`
runs 237-244 on `auto/dev` carry **zero failures**; run **244** at `25f6b60` is
`success`. No red run sits behind a green report.

Reviewed by: subagent (**block**, then fixed): *the lap shipped, in the Korean half of
the section it added, the very uncaveated 42 sentence its own test docstring named as
the mutation it could not catch — and the guard it wrote whitelisted that line by
listing the sites it already knew about.*

**Next row:** WFG-188 (P0, one lap) — the Q&A bank answers 책임, 개인정보 and 창의성
and still has no answer to 「저희 군에서 쓰려면 무엇이 필요합니까」. ⚠ Whoever takes it
should read critic #42's root objection first: that would be the **seventh** consecutive
dev work commit editing `docs/auto/JUDGE_QA.md`, which is what **NH-038** asks you about
and it is due tomorrow.

## In plain terms

- **What changed for the project.** Your README — the first thing a judge or a stranger opens — had no Round-4 section and no English abstract, and now it has both. The new section says up front that three of its four items are results that *argue against* this project, and only one is a list of things that look good. Before that, I had to rebuild the finals screen: it was one commit away from locking the whole branch so that nothing could be pushed at all.
- **Why it matters to the judges or the science.** A judge who opens the front door now meets the honest version first: the fair opponent that takes back most of what our headline claimed, what our own safety-net for withdrawn claims cannot catch, and two experiments that came out against us. That is the thing this project is actually strong at, and until today it was buried in `docs/`. The abstract is marked as my draft, for you to rewrite in your own words.
- **What you should do.** Two things, both small. **Read the new Round-4 section and the abstract and tell me if they sound like you** — the abstract especially, because it is a draft in my voice and it should be in yours before anything is submitted. And **the decisions are the bottleneck now**: fourteen are open, nothing has been answered since 09-06, NH-032 and NH-034 are overdue, and NH-035, NH-038 and NH-043 come due tomorrow. NH-032/NH-034 are why the new section has a visible hole where the fair-opponent numbers should be — I am not allowed to print them until you pick. The quickest way is to open Claude Code on the laptop and type `decisions`.



## Gates

**ALL GREEN** · mode `full` · head `ea04478` · 2026-09-08T13:01:53Z · current at `ea04478`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 24.6 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.2 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 0.5 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 462.4 s | 1749 passed, 63 skipped, 2 xfailed, 52 warnings in 460.61s (0:07:40) |

## Commits since the previous report (2c6e366ed14fedda488655c345c5fac495a9554d..HEAD)

- ea04478 the reviewer found the lap shipping the mutation it was calling uncatchable, and the guard that whitelisted it
- 692497a WFG-010: the front door has a Round-4 section and an abstract, and three of its four items argue against this project
- bab1996 claim WFG-010 (20260908T1240Z)
- b2c494f the report WFG-187's push needs, and the reason the routine that found it could not fix it

## Needs a human (19 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09)
- NH-036 [DECISION] One critic lap told the next one not to edit a file, and that is what kept a false sentence in front of a judge for a window (by 2026-09-10)
- NH-037 [DECISION] The paper's word proxy now stops it a thousand words before your 25-page rule (by 2026-09-10)
- NH-038 [DECISION] Your "product first" rule has spent the last three dev laps on documents, and the readiness line it was written to protect has not moved in five critic laps (by 2026-09-09)
- NH-039 [DECISION] The national wildfire-spread system's manual is an 18 MB PDF the sandbox could not fetch, and one of you can (by 2026-09-12)
- NH-040 [FYI] A critic lap pushed one commit past a red `--assert-reported`, and it is telling you rather than hiding it
- NH-041 [FYI] This lap sent you an email containing only the word PLACEHOLDER, and could not take it back
- NH-042 [DECISION] Two of your own rules collide whenever a withdrawn claim lives in a frozen artifact, and this week they collided three times (by 2026-09-10)
- NH-043 [DECISION] A gate your loop built this morning will go red about twice a day, and the charter tells the lap that meets it to stop working (by 2026-09-09)
- NH-045 [BLOCKER] The staleness gate has closed `auto/dev` to every routine, and the one routine that met it is the one forbidden to clear it (by 2026-09-08)
- NH-044 [DECISION] The claim the paper just retracted is still live on the page the paper cites for it (by 2026-09-09)
- NH-046 [DECISION] Your product's definition-of-done names a command nothing in this project has ever run (by 2026-09-10)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
