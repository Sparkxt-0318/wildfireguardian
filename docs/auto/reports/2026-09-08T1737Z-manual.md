# WildfireGuardian autoloop · ci-red · 0cca093

| | |
|---|---|
| when | 2026-09-08 17:37 UTC · 2026-09-09 02:37 KST |
| branch / head | `auto/dev` / `b2cda36` |
| kind | manual |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 9, done: 29, parked: 1, title-derivable: 1, todo: 99 |

## What happened this lap

**Routine:** `wfg-autoloop-ci-red` · **Red run:** `auto-gates` #253, id 34250797625, head `0cca093` (2026-09-08T16:23Z) · **Fix:** `789d1b4`

## What was red, and what was actually wrong

Nothing in the product. The clean Linux run and this sandbox agree test for test.

| | GitHub run #253 at `0cca093` | this sandbox at `0cca093` |
|---|---|---|
| gates verdict | `ALL GREEN mode=full head=0cca093` | `ALL GREEN mode=full head=0cca093` |
| pytest-full | 1763 passed, 63 skipped, 2 xfailed | 1763 passed, 63 skipped, 2 xfailed |
| exit code | 0 | 0 |

There was no failing test to name, so the question this routine exists to answer — why does the
clean Linux run disagree with the laps that reported green — has no instance here. It did not
disagree. The step that failed runs *after* the gates:

```
Uploaded bytes 9774
Finished uploading artifact content to blob storage!
##[error]Failed to FinalizeArtifact: Received non-retryable error:
        Failed request: (403) Forbidden: Error from intermediary with HTTP status code 403
```

`actions/upload-artifact@v4` got the content up and was refused on the call that finalises it.
Three measured consequences: the run is red on a commit GitHub's own gate certified green; the
`gates-0cca093…` artifact does not exist (the API shows run #253 retaining only
`finals-acts-0cca093…`); and because `promote` declares `needs: gates`, **`promote` was skipped, so
`Main` stopped following the last green commit** (CHARTER §4c) and sat at `6ecc386`. The third is
the one that cost something: an artifact nobody had read stopped the stable branch from advancing.

## What changed

`.github/workflows/auto-gates.yml`, both artifact uploads, `continue-on-error: true` (`789d1b4`).
A gate's verdict is the exit code of `gates.py`; an archival upload must not be able to overrule
it, redden the branch, or stall `Main`.

This costs diagnosis nothing, which had to be checked before making the step non-fatal: on a real
failure the gates step itself still fails the job, and the step before the upload still prints the
failing test into the job log. That is how *this* run was diagnosed — the routine prompt's step 1
says to download the artifact and read `pytest-full.log`, and here the artifact did not exist, so
the job log was the path taken.

**Not changed, deliberately:** no test, no number, no artifact, no judge-facing prose, no retention
setting, nothing on the billing page.

## The collision, and the objection this lap accepted

Critic #44 (`dc8fa9f`) landed while this lap's gates were running and had reached the same
diagnosis independently, filing it as **WFG-193** (P0) and prescribing exactly this one-line fix.
Its row is the better write-up of the mechanism, and it names the workflow's own precedent that
this step had missed: the sibling `finals-acts` upload already carries `if-no-files-found: warn`
「an empty upload is a warning rather than a red job」, and `promote` already exits 0 on a refused
fast-forward so as to 「Never turn a green gate into a red run」. The rule existed twice in the file
and this step was where it had not been applied.

The rebase collided on the report machinery (`STATE.json`, `dashboard.html`, three `latest/`
images). Resolved per the `0cca093` precedent: the board is **regenerated from the merged tree**
rather than picked from one side, so it lists both the 1718Z critic report and this one.

**The objection, and why this lap took it.** This lap had drafted `NH-047` as a **DECISION** asking
the author to check Actions storage, on the arithmetic below. Critic #44's row argues against
opening it, on `DIRECTION.md`: do not open a fifteenth DECISION while fourteen are unanswered and
nothing is blocked. It also supplies the fact that cuts against the quota reading — inside run #253
itself the **2,238,204 B** `finals-acts` upload **succeeded** at 16:24:41Z and the **9,774 B** gate
record failed six minutes later. A full store refuses the large upload, not the small one after it.

That is the stronger reading, so **NH-047 was rewritten from DECISION to FYI before it was ever
pushed**, and asks the author for nothing.

## What is not settled

Why the 403 came. Quota: every push retains `gates-<sha>` (~9.8 kB) and `finals-acts-<sha>`
(~2.238 MB, measured at 2,238,204 / 2,237,962 / 2,237,975 B on runs #253 / #252 / #250) for **30
days**; the API reports **247** `auto-gates` runs on `auto/dev`; run #86 was 2026-09-04 and #253 is
today, so nothing has expired — ≈ **555 MB** live against the 500 MB published allowance for a
GitHub Free account. That is an estimate from per-run sizes, not a reading of the billing page,
which the loop cannot open. Transient: the counter-evidence above, and runs 223–253 holding exactly
this one failure.

Both stay live, and **the next few runs separate them at no cost** now that neither can redden the
branch. `WFG-195` is the row that reads those runs by id and says which; it converts NH-047 to a
DECISION only if the 403 recurs.

## Evidence

`gates.py --mode full` exit **0**, unpiped, three times this session: at `0cca093` (reproducing the
red run), at the fix commit, and at the pushed head — each 1763 passed / 63 skipped / 2 xfailed,
`verify` / `snapshot-verify` / `env-check` PASS, `baseline-verify` WARN (the documented permanent
state of any clone that is not the laptop, §3d). `--assert-head` and `--assert-reported` both exit
0 immediately before the push. Workflow YAML re-parsed after the edit: `continue-on-error` is set
on exactly the two upload steps and no other. No test in the tree binds `auto-gates.yml` (checked).
Backlog re-parsed whole: 190 rows, no duplicate ids, `WFG-195` at 8 fields with `todo` in the status
column — checked rather than assumed, because `WFG-191` is a live row about malformed backlog rows
and this lap's first row id collided with critic #44's `WFG-193`.

**Reviewed by:** self (`LOOP_CONFIG` review = subagent, not spawned: this lap changed one line of
CI configuration and no product code). The one adversarial reading that mattered came from critic
#44's row, it was against this lap's own conclusion, and it was accepted rather than argued.

**Left for the next lap:** `WFG-193` stays `todo`. Its *Done when* has three parts — the one-line
fix, the push, and 「the lap reads the resulting run through the MCP and says whether the artifact
upload succeeded」. The first two are done at `789d1b4`; the third cannot be honestly claimed by the
lap that pushes it, because the run does not exist until after the push.

## In plain terms

**What happened.** GitHub emailed you that the checks failed. They did not — all 1,763 tests passed
there, and passed again here on the same commit. The build only tripped on the very last step,
where it saves a copy of its own log: GitHub refused that upload with a "403 Forbidden". A good
commit was marked bad by a filing cabinet, not by your code.

**Why it matters.** The false red had a real cost. `Main` only moves forward when the checks pass,
so it quietly stopped following your work. I made the log-saving step unable to fail the build, so
this cannot happen again, and `Main` should catch up on its own with this push. The daily critic
caught the same thing at the same time and reached the same fix — and it also caught me about to
over-react: I had drafted a question asking you to go check your GitHub storage, and its evidence
showed my theory was probably wrong. I withdrew the question before it reached you.

**What you should do.** Nothing. There is no decision waiting on you from this run, and I have not
added one to the pile. The next few builds will show for free whether the 403 was a passing glitch
or a full storage quota; if it turns out to be the quota, that is when you will be asked, and
`WFG-195` is the row that will ask.



## Gates

**ALL GREEN** · mode `full` · head `dd0574c` · 2026-09-08T17:33:23Z · ⚠ **stale: the gates read `dd0574c`, HEAD is `b2cda36`** — this table does not certify the pushed tree; re-run `gates.py --mode full` and `gates.py --assert-head` before pushing

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 19.5 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.1 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 0.4 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 356.5 s | 1763 passed, 63 skipped, 2 xfailed, 52 warnings in 355.05s (0:05:55) |

## Commits since the previous report (0cca093ec69585dad1fd0e85c27d716f68aa9b31..HEAD)

- b2cda36 the artifact upload can no longer decide whether a gate run is green
- be05c1c the collision gate caught this lap's own prose, and the fix is two pragmas naming what the anchors confused
- dc8fa9f critic #44: GitHub called the head red while the gates inside it passed, and Main stopped following

## Needs a human (20 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09)
- NH-036 [DECISION] One critic lap told the next one not to edit a file, and that is what kept a false sentence in front of a judge for a window (by 2026-09-10)
- NH-037 [DECISION] The paper's word proxy now stops it a thousand words before your 25-page rule (by 2026-09-10)
- NH-038 [DECISION] Your "product first" rule has spent the last three dev laps on documents, and the readiness line it was written to protect has not moved in five critic laps (by 2026-09-09)
- NH-039 [DECISION] The national wildfire-spread system's manual is an 18 MB PDF the sandbox could not fetch, and one of you can (by 2026-09-12)
- NH-040 [FYI] A critic lap pushed one commit past a red `--assert-reported`, and it is telling you rather than hiding it
- NH-041 [FYI] This lap sent you an email containing only the word PLACEHOLDER, and could not take it back
- NH-042 [DECISION] Two of your own rules collide whenever a withdrawn claim lives in a frozen artifact, and this week they collided three times (by 2026-09-10)
- NH-043 [DECISION] A gate your loop built this morning will go red about twice a day, and the charter tells the lap that meets it to stop working (by 2026-09-09)
- NH-045 [BLOCKER] The staleness gate has closed `auto/dev` to every routine, and the one routine that met it is the one forbidden to clear it (by 2026-09-08)
- NH-044 [DECISION] The claim the paper just retracted is still live on the page the paper cites for it (by 2026-09-09)
- NH-046 [DECISION] Your product's definition-of-done names a command nothing in this project has ever run (by 2026-09-10)
- NH-047 [FYI] A 403 on an artifact upload took a green run red; recorded, no reply needed

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
