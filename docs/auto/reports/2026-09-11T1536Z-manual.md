# WildfireGuardian autoloop · paper · 2026-09-11T1600Z

| | |
|---|---|
| when | 2026-09-11 15:36 UTC · 2026-09-12 00:36 KST |
| branch / head | `auto/dev` / `a334a9e` |
| kind | manual |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 10, done: 35, dropped: 1, parked: 1, title-derivable: 1, todo: 123 |

## What happened this lap

# Paper lap 32 — the fair opponent reached the headline region, and seven sentences of the manuscript still said it never had

**Range incorporated:** `748e89f..a334a9e`, 20 commits. **Reviewed by: subagent (block)** —
*"the diff deletes 'plus a fixed buffer' from the Abstract and in the same sentence widens the
claim from one region to 'each region', so it now asserts that a zero-buffer opponent recovers
most on BOTH regions, when the Uiseong-Andong result exists only at a 1 km buffer."* Sustained,
verified against the committed width table, and repaired before the push, along with its three
other findings.

**Length:** `body_words` **8,995 → 8,995** — net **zero**. Margin **5 → 5**. Built document
**23 pages** under Carlito against the author's 25, measured this lap rather than inherited.
`check_paper.py` exit 0; `gates.py --mode full` exit 0 (2127 passed, 65 skipped, 3 xfailed).

## What moved in the code, and why it reached the paper

The dev lap of 2026-09-11T1219Z ran **WFG-129**: the *present-perimeter* opponent — a router
that sees only where the fire is **now**, needs no model, and is the actual status quo a county
office has — over the canonical Yeongdeok 458 origins, which is the origin set the headline
**42** comes from. It reproduced the committed 414 / 42 / 2 partition in its own process before
writing anything (`scripts/measure_present_perimeter_yeongdeok.py`,
`data/processed/present_perimeter_yeongdeok_2025.json`, `docs/present_perimeter_yeongdeok.md`,
six registered `ppy_yeongdeok_*` keys).

Critic #67's root objection is that the repository then **stopped**: judge-facing lines still
said the comparison had never been run. It filed `paper/manuscript.md:414-418` and `:718-724`
as **the paper routine's half** of WFG-258 under CHARTER §12. That is this lap's job.

## Sections changed

**Seven sentences, not the two the critic named.** DIRECTION's rule is *grep for the subject of
the claim, never for the sentence you just wrote*. One grep on *which regions this opponent has
run on* returns seven — four flatly false (§4.3's "would recover an **unmeasured share**" plus a
`[GAP:` marker its own ledger calls closed; §4.5's "on one region"; §5's "on the one region with
a stronger opponent"; §6's "The same substitution **has not been made** on the canonical
Yeongdeok field") and three understated (**Abstract**, §1, §7). **Two of the four false ones sit
in sections the critic did not name**, so fixing only the two cited line ranges would have
shipped a document asserting "one region" twice over.

**No count from the run is in the manuscript, and that is a standing bar rather than a length
decision.** `docs/auto/DIRECTION.md` bars every `ppy_yeongdeok_*` count from every judge-facing
surface while **NH-059** is open; CHARTER §14b names this manuscript as one; critic #67's own
item says *"Put NO count on any surface in this item."* **NH-059 is the author's and comes due
2026-09-13.** The sections carry the qualitative "recovers **most** of them" — not a count,
verified true on both denominators (26/42 = 61.9 %, 26/44 = 59.1 %) — with the partition left to
`docs/present_perimeter_yeongdeok.md`.

**The `[GAP:` marker was narrowed, not deleted.** The zero-buffer arm ran; the **swept** arm
(WFG-033(b), NH-027) has not, and §4.5 already shows the buffer is the axis governing how strong
this opponent can be made. No direction is asserted for it: `docs/present_perimeter_yeongdeok.md`
§5 item 5 records that the obvious monotonicity argument is **false**, and **WFG-259** is open
because the numbers that replaced it are unregistered. Markers stay at 7, rows at 7 — deleting
the marker would also have forced the G7 row out of the table against CHARTER §3 rule 7.

## Gaps opened or closed

**G7 narrowed** (second time): from "the present-perimeter baseline over the canonical Yeongdeok
origins" to "the same opponent at a **swept buffer** on these origins". No gap opened, none
closed; the count is unchanged at 7.

## Figures

**None added or redrawn.** All ten redrew byte-identical. The obvious figure — the target
origins partitioned into three outcomes — is barred by its own content while NH-059 is open, and
a committed PNG is the one surface the withdrawal registry structurally cannot reach: `WC-017`'s
known-stale exception (a) is the worked instance **in this very window**, where
`F10_disc_null.png` baked a retracted locative into a binary CHARTER §3 rule 2 then forbids
regenerating. If NH-059 returns A or C, the figure is drawn then.

## The limitation lap 21 declined for length is now in the paper

§6's Operational status ends: *"The hazard field cannot be current either: its weather
reanalysis publishes days late [@era5]."* The paper had nowhere said its hazard field **cannot**
be current, while `docs/live_pipeline.md` §0 makes it the project's own lead line on every
screen, sheet, broadcast script, SMS draft and JSON record. The wording is not lap 21's and
could not be — that lap's "ERA5 publishes on a ~5-day lag" writes an external figure **bare**,
which CHARTER §3.5b forbids. The shipped form carries no number and hands provenance to the
`era5` note, opened at the Copernicus page; the reviewer checked it both ways and found "days
late" **weaker** than the source, which is the right direction.

⛔ **The arithmetic is the sharpest NH-037 datum this routine has, and it runs the wrong way.**
Measured in isolation by deleting it and rebuilding: **8,981 without, 8,995 with — +14.**
Against the **5** words this lap inherited it was unaffordable in every form measured (+12 bare,
+14 as shipped, +16, +28). **It became affordable only because seven sentences had gone false
and deleting them freed six words.** A limitation missing since lap 21 was paid for by a batch
of falsehoods arriving — the only reliable way this document buys space is for something in it
to stop being true. Had this been a quiet window, it would have been declined a seventh time, on
a document measuring 23 pages against the author's 25.

## The reviewer's block, and three more findings

1. ⛔ **The Abstract.** Deleting "plus a fixed buffer" (correct — the Yeongdeok arm has none)
   while widening "that region's" to "each region's" asserted a **zero-buffer** opponent
   recovering most on **both** regions. It does not:
   `docs/present_perimeter_arm.md` §4's committed five-width table gives **12 of 91 at 250 m**
   and **23 of 91 at 500 m** against **86 of 91 at 1 km**, and no zero-buffer run exists for
   Uiseong-Andong at all. The two halves of the edit were word-neutral against each other, so
   **the budget did not cause this one — a careless repair did.** Now: "…what is burning now
   *(buffered there, not here)* and needs no model at all…".
2. ⚠ **"Six sentences" was seven**, the missing one being §7, the Conclusion. Caught here
   independently and confirmed by the reviewer the harder way, from `git show HEAD:` and hunk
   matching. Corrected in all three of this routine's files.
3. ⚠ **The word arithmetic was wrong twice** — "+12" for a sentence that costs +14, and "four
   words (→ 8,983)" for four edits that total six. **A lap whose subject is a count going stale
   published two wrong counts in the paragraph reporting it.** Every integer was then re-derived
   by a rebuild.
4. ⚠ **A DIRECTION breach, recorded rather than argued:** "do not add a word without trimming
   one" stood at **net +2** in the draft the reviewer read. Now net zero, paid for by compressing
   this lap's own new prose and nothing litigated. Lap 31's lesson is why it was fixed rather
   than defended.

**What the reviewer checked and passed**, in its own process: all four gates green by exit code;
no `ppy_yeongdeok_*` count in the manuscript (every numeral in the diff's added lines extracted —
4.3, 4.4, 4.5 and one `42`); "most" true on both denominators; the "node-pruning build"
attribution correct, verified **in code** at `scripts/measure_present_perimeter_yeongdeok.py:246`,
where `naive_route` is called with no `time_budget_min`; §6's repaired inference leaving no
stranded connective; all 29 citations resolving with `verified` notes; the 32.6 % coverage caveat
travelling with every absolute Yeongdeok rate; `WC-017` / `WC-018` reaching the manuscript **0**
times per line and **0** flattened.

## Checked and found not to be owed

- **§3.5's "retractions skipped registration four times" stays four**, checked from the commits
  and not from the ledger's summary. `WC-018` registered in the *same* commit as its correction.
  `WC-017` looks like a fifth and is not: the retracting commit is **this routine's own lap 31**,
  which cannot write the registry at all under CHARTER §12, the dev lap had already claimed
  WFG-254 twenty minutes earlier, and every surviving spelling carries a dated "superseded"
  annotation written in that same commit.
- **`docs/NUMBERS.json` moved purely additively**, 546 → 554, compared key by key: zero removed,
  zero values changed, zero caveat bodies changed.
- **§6's floor-comparison sentence survives `WC-018` unchanged** — it claims *overlap*, not the
  retracted attribution to shape.

## For other routines (not this one's paths)

- **Dev lap:** the Yeongdeok present-perimeter arm runs **unbudgeted**, the same asymmetry §4.4
  flags for the committed classification, except that here it runs in the **opponent's** favour
  and therefore inflates "most". `docs/present_perimeter_yeongdeok.md` §5's eight items do not
  say so. No manuscript change is owed — the inflation runs *against* this project, which is the
  safe direction — but the artifact page should carry the clause.
- **WFG-259** remains the thing that would let this paper say more: until the buffered direction
  is re-derivable, no lap may write one.

## In plain terms

Your loop measured the hardest question a judge can ask — what does the forecast add over just
looking out of the window — and the paper now says so on the region your headline comes from,
in words, with no number, because **NH-059 is your call and it is due 2026-09-13**.

What is still missing: the buffered version of that opponent has never been run on 영덕, so
nobody can say how much stronger it gets; and the three counts stay off every judge-facing page
until you answer.

What to do: read `docs/present_perimeter_yeongdeok.md` §4 and §5, then answer **NH-059** (A say
it, B say only that it ran, C Q&A card only, D wait for WFG-259). **NH-037** — the 9,000-word
proxy — is still open and cost this lap real work again.



## Gates

**ALL GREEN** · mode `full` · head `a334a9e` · 2026-09-11T15:35:48Z · current at `a334a9e`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 28.9 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.2 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 0.5 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 497.6 s | 2127 passed, 65 skipped, 3 xfailed, 52 warnings in 495.86s (0:08:15) |

## Commits since the previous report (dec00f425d8864bacca640d2d85db3d20daab021..HEAD)

- a334a9e the spoken rate of the whole script and the ratio between its fastest and slowest segment are two numbers, annotated rather than deleted
- c7de4c0 critic #67: the repository measured the fair opponent for its headline this morning, and four judge-facing lines still say it has never been run

## Needs a human (31 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09, one day past; raised to HIGH by critic #55 on a measured third instance)
- NH-036 [DECISION] One critic lap told the next one not to edit a file, and that is what kept a false sentence in front of a judge for a window (by 2026-09-10)
- NH-037 [DECISION] The paper's word proxy now stops it a thousand words before your 25-page rule (by 2026-09-10)
- NH-038 [DECISION] Your "product first" rule has spent the last three dev laps on documents, and the readiness line it was written to protect has not moved in five critic laps (by 2026-09-09)
- NH-039 [DECISION] The national wildfire-spread system's manual is an 18 MB PDF the sandbox could not fetch, and one of you can (by 2026-09-12)
- NH-040 [FYI] A critic lap pushed one commit past a red `--assert-reported`, and it is telling you rather than hiding it
- NH-041 [FYI] This lap sent you an email containing only the word PLACEHOLDER, and could not take it back
- NH-042 [DECISION] Two of your own rules collide whenever a withdrawn claim lives in a frozen artifact, and this week they collided three times (by 2026-09-10)
- NH-043 [DECISION] A gate your loop built this morning will go red about twice a day, and the charter tells the lap that meets it to stop working (by 2026-09-09)
- NH-045 [BLOCKER] The staleness gate has closed `auto/dev` to every routine, and the one routine that met it is the one forbidden to clear it (by 2026-09-08)
- NH-044 [DECISION] The claim the paper just retracted is still live on the page the paper cites for it (by 2026-09-09)
- NH-046 [DECISION] Your product's definition-of-done names a command nothing in this project has ever run (by 2026-09-10)
- NH-048 [DECISION] One of the research routine's three literature channels has been dead for two runs, and a working replacement is already proven (by 2026-09-10)
- NH-049 [DECISION] Your critic routine is told to add judge Q&A cards and your own printing gate makes that impossible for it (by 2026-09-11)
- NH-050 [DECISION] You answered two of these questions two days ago and the loop never heard you, because you answered them on the routine page (by 2026-09-10)
- NH-051 [DECISION] The rule your loop has been obeying for three days is not the option it names, and the difference is why one row has been pushed down five times (by 2026-09-11)
- NH-052 [DECISION] The experiment that measures what your own forecast is worth is now one run away, and it would change what 42 means (by 2026-09-12)
- NH-053 [DECISION] The word your front door uses for 42 stopped being right today, and the loop cannot pick its replacement (by 2026-09-12)
- NH-054 [DECISION] Four fifths of your front door's headline is limits, and no lap is allowed to decide whether that is the project's strength or its biggest presentation risk (by 2026-09-13)
- NH-055 [DECISION] Your front door compares the headline forecast number to a model the same page calls broken, and today the loop measured a second, harsher comparison it is not allowed to put beside it (by 2026-09-13)
- NH-056 [DECISION] A Korean university published a reproducible deep-learning wildfire model four weeks before your finals, and a lap may not decide on its own whether to mention it (by 2026-09-13)
- NH-057 [DECISION] The one thing your project now says it contributes has never once been produced from a real fire, and only your laptop can change that (by 2026-09-13)
- NH-058 [DECISION] Two of your routines built the same row in the same window, and the rule that was supposed to stop that cannot see the paper routine (by 2026-09-13)
- NH-059 [DECISION] Your loop measured the hardest question a judge can ask you, and the answer is uncomfortable enough that no lap may decide on its own whether you say it out loud (by 2026-09-13)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
