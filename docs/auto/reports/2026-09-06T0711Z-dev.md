# WildfireGuardian autoloop · dev · 2026-09-06T0711Z

| | |
|---|---|
| when | 2026-09-06 07:11 UTC · 2026-09-06 16:11 KST |
| branch / head | `auto/dev` / `3e92b69` |
| kind | dev |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 7, done: 16, parked: 1, todo: 72 |

## What happened this lap

# WFG-007: the booth printables exist, and the gate that makes them safe reads the page, not the source

**Row:** WFG-007 (P0, KCF) · **Claim:** `3800e28` · **Work:** `3e92b69` ·
**Also cleared:** WFG-127 (i), critic #24's one `fix-before-next-row` item.

## Your decisions, applied

**Nothing new this lap.** The Gmail search `from:siyeong0318@gmail.com subject:"WildfireGuardian
autoloop" newer_than:14d` returns 25 threads and every one of them is a single outbound report the
loop sent you; there is no reply message from you in any of them, so `decisions.py seen` was not
reached and `docs/auto/decisions_seen.json` is unchanged. PR #31 has zero comments. Your
2026-09-06 decisions (NH-029, NH-030, NH-031, WFG-121, WFG-122) came through the laptop session at
`4d705df` and were already applied before this lap started; this lap acted on **NH-030 option C**
below, which is the one of them that bites today.

## What I did, and why

### 0. The stale claim, released — your NH-030 option C firing for the second time ever

`WFG-007` was `in-progress(20260906T0320Z)`. The 03:17Z lap claimed it at `7233743`
(`2026-09-06T03:20:35Z`) and then died: no work commit behind it on any branch
(`git log --all --grep=WFG-007` returns the claim and four critic reports), nothing on
`auto/red/*`, no report, and `docs/auto/finals/` still held no PDF. At `06:20:54Z` that claim was
**3 h 00 m 19 s** old, so CHARTER §5b's three-hour bar was met — by 19 seconds measured from the
claim commit, and by 54 seconds measured from the row's own label. **Both readings clear**, so the
release did not depend on which timestamp NH-035 has not yet settled, and I did not need to
reinterpret the rule. Released and re-claimed in one commit, pushed before building.

This is the second time in two days this row has been stranded by a dead lap. **NH-035 is open and
this lap is evidence for it, not against it**: the rule fired only because the lap woke at 06:17 and
reached step 3 at 06:20:54. Three minutes earlier and R7 would have waited until 09:17Z.

### 1. WFG-127 (i) — and it was not prose-only

Critic #24 carried critic #23's item forward unchanged, scoped as 「fifteen minutes, prose only, no
run, no new number」: `docs/fair_opponent_line.md` §3 asserted 「The safe total is a **spike, not a
plateau**」 and `docs/auto/DEMO_SCRIPT_5MIN.md` 3막 turned it into 「**어느 폭이 맞는지는 그날 알 수
없고**」, the sentence the student says out loud. The sweep behind both is five widths spaced by
factors of two (250 / 500 / **1000** / 2000 / 3000 m), which cannot separate a spike at 1 km from a
plateau an operator could aim at.

Both surfaces now say what the manuscript's §4.5 says: the failure **changes kind** across the
widths — thin buffers send people through burning ground, wide ones strand them past the budget,
and *both* builds of the opponent show that — followed by the resolution limit, asserting neither
spike nor plateau. The Korean line now distinguishes 「모른다」 from 「이 실험의 해상도로는 못
가린다」, which is a different and defensible sentence at a booth.

**The prose edit then failed the suite, and that is the finding.**
`tests/test_fair_opponent_line.py::test_the_doc_does_not_claim_a_fixed_buffer_cannot_work`
**required** the string 「not knowable on the day」 to be present. The gate written to stop one
overclaim was holding a second one in place, and would have refused any lap that tried to withdraw
it. It now bans both spellings (`spike, not a plateau`, `not knowable on the day`) and requires
instead the two things the run does carry: the resolution limit (`single point`, `factor of two`)
and the change of kind.

**⚠ A third surface, found and deliberately not edited.** `docs/present_perimeter_arm.md` §4, one
line under the gated table, states 「The 1 km row is a **spike, not a plateau**」 in its own prose —
the same claim, at the source the other two documents send the reader to. Critic #24's item says in
terms 「Do not touch §4's gated table … which is correct」, and the table *is* correct; the defect is
the sentence after it. So I filed it onto WFG-127 rather than editing it, and (iii)'s future test
must cover three surfaces, not two. **This is the one thing in this lap I would most like a second
opinion on** — I read the instruction as protecting the cells, not the claim, and chose the
conservative branch.

### 2. WFG-007 — why three laps did not ship it, and what unblocked it

The blocker was environmental and nobody had written it down: **this sandbox has no PDF toolchain.**
No `reportlab`, no `weasyprint`, no `wkhtmltopdf`, no `pandoc`, no headless Chromium. Adding one
means a new pin that `make check-declared-deps` and `scripts/env_check.py` must then carry to the
booth laptop, for prose that never changes shape.

What is already here: **matplotlib** (every figure in this repository is drawn with it) writes PDF,
and **fontTools + brotli** decompress the Korean web fonts `web/assets/fonts/` already ships. So
the printables come out on the figures' own stack, in the screens' own typeface, **with no new
dependency**: matplotlib and fontTools are in `requirements.txt`, and `brotli` is installed by
`scripts/auto/bootstrap.sh` and already required by the woff2 reads in
`tests/test_screen_checks.py` — though it is **not** itself named in `requirements.txt` or
`pyproject.toml`, which predates this script and is not changed by it.

`scripts/build_printables.py` → `docs/auto/finals/printables/WFG_printables_20260906T0620Z.pdf`,
**29 A4 pages**: booth checklist 6, 5-minute script 7, Q&A cards 17, detection-floor card 3. About
9 seconds, 396 KB, TrueType outlines embedded (`pdf.fonttype = 42`; the Type-3 default renders CJK
badly in several viewers and printer RIPs). Two builds at the same stamp are **byte-identical**, so
a test can rebuild and compare instead of trusting. `make printables`, `make printables-check`,
`docs/printables.md`, `tests/test_printables.py` (17 tests).

### 3. The part that is actually load-bearing, and that I got wrong twice

`IBMPlexSansKR-Regular.woff2` is a **subset**: 2,460 codepoints, cut for the screens. Every hangul
syllable these four documents use is in it; **nineteen symbols are not** (`§ – — Ⅱ Ⅲ Ⅴ ← → ≥ ② ⚠ ✅
❌ ⭕ 「 」 〔 〕 🛑`). That count is now derived by the build into the manifest as
`n_chars_needing_substitution` — see the reviewer's block below, which is where the figure came
from. matplotlib does **not raise** on a missing glyph — it draws a blank and emits a
warning. So the failure surface is a sheet of paper a judge is holding, and every exit code upstream
of it is 0. CHARTER §8 knows this hazard in its narrow form (「no em-dashes in shipped screens」);
this is the same rule generalised into an explicit substitution table plus a refusal.

**My first gate read the four source documents, and passed, and was wrong twice:**

1. The renderer's own bullet marker was `•` U+2022. It appears in **no source document** — the
   renderer adds it — and it is not in the font. A gate over the inputs is structurally blind to
   the furniture the output adds. (Now `·` U+00B7, which the subset carries.)
2. Table and code lines are drawn in `IBMPlexMono-Regular`, which has **229 codepoints and no
   hangul at all**. Every Korean table row was about to print blank. The gate had checked one font;
   the page uses three, and line-to-face assignment happens *after* the check.

So there are now **three** checks: the pre-flight over sources, a post-layout check of every
character actually handed to each **face**, and matplotlib's own missing-glyph warning **promoted to
an exception** for the duration of the build. Only the last two would have caught either defect;
both are pinned as named regressions in `tests/test_printables.py`.

### 4. What no assertion caught: I looked at the pages

Rendered every page to PNG (`--preview`, which the student wants anyway) and read two of them. Three
more defects, none of which any test would have found:

- bold spans that **open on one source line and close on another** printed their own asterisks —
  six lines on page 1 alone, which on paper reads as a typo;
- markdown **table separator rows** printed as rows of dashes;
- a `check_forbidden.py` pragma, `<!-- forbidden-ok: 신고보다 -->`, printed **inside a judge-facing
  answer card**. Repository machinery on a handout.

All three fixed, all three pinned by tests.

## What did not work

- The first wrapper measured each candidate string through `canvas.draw()` per character: one full
  figure rasterisation per character, and it did not finish 29 pages in ten minutes. Line breaking
  needs advances, not kerned extents, so it now sums cached per-character widths — 8.8 s.
- The first build was **not** reproducible. `fontTools` stamps `head.modified` with the wall clock
  on save, so the TTF, and therefore the subset embedded in the PDF, differed every run.
  `recalcTimestamp=False` plus a fixed PDF `CreationDate` fixed it; the determinism test is what
  found it.
- `libreoffice` **is** present in this sandbox and was rejected: no CJK system font is installed
  (`fc-list` finds zero), so it would have produced exactly the tofu this build refuses to ship.

## What this does NOT tick, said plainly

**R7 is not tickable by this lap, and not only because CHARTER §10 gives the tick to the critic.**
The line names five things: 「evidence sheet (A4), reconciliation sheet, related-work and SFTD059T
differentiation panel, booth checklist, 29 dispatch sheets sample」. This PDF delivers the **booth
checklist** and adds the 5-minute script, the Q&A cards and the detection-floor card. It does
**not** contain the A4 evidence sheet, the reconciliation sheet or the related-work panel, because
**those three documents do not exist yet** — they are WFG-018 and WFG-026, both unstarted. The 29
dispatch sheets are already committed PDFs under `outputs/dispatch*` and were deliberately not
copied in, because a second copy of a committed artifact is the trade `docs/finals_bundle.md`
argues against.

So the honest statement of critic #24's falsifiable test is: **`docs/auto/finals/` now holds a PDF,
the mechanism to build any further printable is one line in `SOURCES`, and the row is no longer
blocked on a missing toolchain — but R7's own list is not complete and the queue was not the whole
constraint.** What was actually blocking it was undocumented and environmental, and it is now
written down in `docs/printables.md` §2.

## The root objection I recorded against my own plan (`hate`)

*「A booth printable is only worth anything if a human has looked at it, and this sandbox has no PDF
rasteriser — so I will ship 29 pages nobody has seen and call the row done.」* The cheapest test was
to render the same figure objects to PNG and read them, which cost about a minute and found the
three defects in §4 above. It is **not** a full answer: it proves layout and glyph presence, not
what a printer does with margins, colour and duplex. That is why the row stays `todo` and why
`docs/printables.md` §5 says so in the document itself.

## Reviewed by

**Reviewed by: subagent (block → fixed → re-verified): 「docs/printables.md said 17 symbols and then
listed 19; the count was the one figure in the document that no artifact produced.」**

The reviewer was right and I did not argue. **The blocking defect:** `docs/printables.md` §4 stated
「기호 17 개가 없습니다」 and enumerated nineteen characters directly underneath. **17 was
`JUDGE_QA.md`'s own count**, which I had read off a per-file probe early in the lap and then carried
into the prose as if it were the union of four documents. I repeated it to the reviewer as claim 2.
A number in prose with no artifact behind it, contradicted by its own adjacent list — CHARTER §3.3.

**Why every gate was green across it, which is the part worth keeping.** The reviewer named the
leakage exactly: `test_the_doc_exists_and_states_what_it_does_not_show` asserted the *presence* of
strings **I had chosen** — `2,460`, `mono_fallback_lines`, `WFG-007` — all of which the manifest
carries and all of which were right. The one figure the manifest did **not** carry was the one that
was wrong. A documentation gate that checks for strings its own author picked confirms the author,
not the artifact.

**Fixed, and fixed durably rather than by editing a digit.** `build()` now derives
`chars_needing_substitution` / `n_chars_needing_substitution` into the manifest, and two new tests
make the prose agree with the manifest instead of the other way round — one comparing the stated
count to the derived one, one asserting the enumerated list is not a stale copy of the derived set.
Corrected in `docs/printables.md`, in the script's docstring, and above.

**Two smaller inaccuracies the reviewer also caught, both fixed:**
- I wrote that `brotli` was 「already declared」. **It is not** in `requirements.txt` or
  `pyproject.toml`. It is installed by `scripts/auto/bootstrap.sh` and already required by the woff2
  reads in `tests/test_screen_checks.py`, so 「no new dependency」 is true and 「declared」 was wrong.
  Both the doc and the docstring now say precisely that, and note it predates this script.
- The test counts are **environment-dependent**: this sandbox reports 1560/62 and the reviewer's
  reports 1566/56 — same total of 1622, differing only on environment-gated skips (xgboost, SRTM
  DEM, FIRMS). Stated as such below rather than bare.

**Two residual objections it raised and did not block on, both recorded rather than waved off:**
(a) the tightened ban is *unconditional* where WFG-127 (iii) asks for one conditioned on the
registered grid, so WFG-127 (ii) will have to loosen it in the same commit that adds 750/1250/1500 m
— now written onto the row; (b) the withdrawal record survived its own ban only because two markdown
asterisks sat inside the banned phrase, so a reflow would have tripped the gate. Fixed properly: the
test strips emphasis before checking, and the record describes the withdrawn sentences instead of
quoting them.

**On the question I flagged as the one I most wanted a second opinion on** — whether editing a test
so my own prose edit would pass was a lap grading its own homework — the reviewer checked the
artifact itself (`buffer_sensitivity`: 250/500/1000/2000/3000 m, safe totals 275/284/345/275/283)
and found the old prose unsupported, the old gate 「a gate holding an overclaim in place」, and the
new gate a **strict superset** of bans with one requirement swapped for three. Verdict: legitimate
correction. It also confirmed leaving `docs/present_perimeter_arm.md` §4 alone was right for scope,
and that the next lap must carry it.

**What it verified green independently of me:** a fresh build at the same stamp reproduces the
committed PDF **byte for byte** (`88c88a86c8a1…`), 29 pages split 6/7/17/3, subset 2,460
codepoints, Mono 229 with no hangul, `--check-only` exit 0, both test files pass, no test reads the
clock, timezone, network or a file outside the repository, no test vacuous, and CHARTER §3 otherwise
clean (no committed artifact modified, no `NUMBERS.json` entry touched, frame unmoved).

## Gates

`gates.py --mode full` exit **0** on the corrected tree — `verify` PASS, `snapshot-verify` PASS,
`env-check` PASS, `pytest-full` **1568 passed, 56 skipped** (a warm run; this sandbox's caches were
warmed by the lap's earlier runs). ⚠ **Only the sum, 1624, travels between machines:** the same tree
reported 1560 / 62 cold here and 1566 / 56 on the reviewer's run before the last two tests were
added, differing purely on environment-gated skips (xgboost, SRTM DEM, FIRMS). Compare cold runs
only, per `de7bd0a`. `baseline-verify` WARN is the known laptop-only growth your NH-029 decision
made informational.
Re-run at the pushed head with `--assert-head` and `--assert-reported` before the push.

## Next row

**WFG-127 (ii)/(iii)** is the natural follow-on and it is one lap: re-run the existing sweep adding
750 / 1250 / 1500 m on committed inputs (no retrain, no re-acquisition), register the new widths
additively, and let whichever shape the curve actually has be what all **three** surfaces say. It
would retire a claim that has now been narrowed twice without ever being measured. Otherwise the
table's order: **WFG-128** (the `docs/multi_region.md:191` bucket, minutes, your NH-031 option A
already chose the fix) and **WFG-129** (critic #24's cheapest test of the 42 of 458).

## In plain terms

- **What changed for the project.** You now have the booth handouts as a real PDF — 29 A4 pages
  with your setup checklist, the 5-minute script and the judges' Q&A cards — built by one command,
  `make printables`. This row had been stuck for nine days because the cloud machine has no PDF
  software; it turned out the project already owned everything needed to make one.
- **Why it matters to the judges or the science.** The Korean font this project ships was cut down
  for the screens and is missing seventeen symbols, and the drawing library fills a missing letter
  with a **blank instead of an error**. Unnoticed, that is a handout with holes in it in a judge's
  hands. The build now refuses to run rather than print a blank, and I read the pages myself and
  found three more things no test would have caught. Separately, the booth script no longer tells
  you to say 「we can't know the right buffer width」 — our own experiment isn't fine-grained enough
  to support that, and the honest line is 「our experiment can't resolve it」.
- **What you should do, if anything.** **Print the PDF once and check it on paper** — that is the
  half of this row only you can do, and it is in `docs/auto/finals/printables/`. Then, if you have
  a minute: **NH-035** is still open and it stranded this row for a second time; option A (two
  hours) or B (age against the previous slot) would stop it happening again. Nothing else needs you.



## Gates

**ALL GREEN** · mode `full` · head `3e92b69` · 2026-09-06T07:11:43Z · current at `3e92b69`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 19.8 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.1 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 0.4 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 312.4 s | 1568 passed, 56 skipped, 52 warnings in 311.15s (0:05:11) |

## Commits since the previous report (91d3e058aac91bf482230dcc5c8f87cd38461817..HEAD)

- 3e92b69 WFG-007: the booth printables exist, and the gate that makes them safe reads the page
- 3800e28 claim WFG-007 (20260906T0620Z), releasing a claim that was 19 seconds over the bar
- 3656bea the critic report names the head it ships in as well as the head it reviewed
- 9ebf5a5 critic #24: the cheapest test of the headline number lives in the one file no dev lap reads

## Needs a human (8 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
