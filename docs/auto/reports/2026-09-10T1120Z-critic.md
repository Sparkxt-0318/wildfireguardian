# WildfireGuardian autoloop · critic · 2026-09-10T1120Z

| | |
|---|---|
| when | 2026-09-10 11:20 UTC · 2026-09-10 20:20 KST |
| branch / head | `auto/dev` / `d3ca754` |
| kind | critic |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 9, done: 33, dropped: 1, parked: 1, title-derivable: 1, todo: 111 |

## What happened this lap

Critic lap #58. Reviewed head `d3ca754`; window 2026-09-09T11:00Z to 2026-09-10T11:00Z, which is
eight dev laps, four paper laps and three critic laps. This lap changed no code, no test, no data,
no figure and no registry value; it wrote only under `docs/auto/`.

⚠ **The clone arrived SHALLOW at exactly 50 commits, which is almost exactly the 24-hour window** —
the same coincidence critic #57 hit, so `git log --since='26 hours ago'` would have returned the
whole clone and truncated every count silently. `git fetch --unshallow` was run before anything was
counted: `--is-shallow-repository` answers `false`, `git rev-list --count HEAD` answers **712**.

## Your decisions, applied

**Nothing new reached me on either channel, and this is the sixth consecutive lap that has been
true.** The Gmail search `from:siyeong0318@gmail.com subject:"WildfireGuardian autoloop"
newer_than:14d` returns 25 threads and every one of them is a report this loop sent to you; each
thread holds exactly one message, so no reply is threaded under any of them. A second search,
`subject:(Re WildfireGuardian) newer_than:14d`, returns exactly one thread and it is GitHub's own
「Merged #31 into Main」 notification of 2026-09-05, not a decision. PR #31's comment list is empty
(`pull_request_read` / `get_comments` returns `[]`). Nothing was applied with
`scripts/auto/decisions.py`; `docs/auto/decisions_seen.json` is unchanged and still records only the
23 decisions you gave in Claude Code sessions on the laptop.

**What is waiting on you, parsed here rather than inherited.** `docs/auto/NEEDS_HUMAN.md` holds **20
open entries for you** (19 DECISION + 1 BLOCKER; 5 FYI besides) and **13 are at or past the date they
name**: NH-032, 034, 045 (due 09-08), NH-035, 038, 043, 044 (09-09), NH-036, 037, 042, 046, 048, 050
(09-10). ⚠ My own first pass said **14** and was wrong — it counted NH-049, which is due **09-11**.
And **two open entries carry no date at all** (NH-005, NH-014), so they fall out of any date-based
count silently. That is WFG-107's shape a sixth time, this time mine, and the row now says to write
「13 of 20 dated, 2 undated」 rather than a bare number.

## Findings, ranked

**F1 · the one `fix-before-next-row` item · `docs/oracle_gap.md:233` claims a registry protection
that does not exist for the string it quotes.** The sentence reads 「Anyone quoting 0.394 as "what the
forecast buys" has misread it, and that phrasing is registered as forbidden on all ten keys」. Measured
at `d3ca754` in one process: the `og_yeongdeok_*` prefix holds **30** keys, not ten; all 30 do carry a
`forbidden_phrasings` list, so 「all of them」 is right and only the number is wrong; and the quoted
string 「what the forecast buys」 is a registered forbidden phrasing on **zero** keys — the nearest
registered spelling is 「this measures what the model buys」. The words 「forecast buys」 do occur in the
registry, 75 times in `caveat` fields and once in a `derivation`, all on `pp_uiseong_*` keys and never
as a forbidden phrasing. ⚠⚠ The paragraph's **argument** is correct and must not be softened; only the
appeal to the registry is wrong, and no `WC-###` is opened because nothing is withdrawn. ⚠ The repair
must carry **no count**: this page's own §Registry line at `:6-9` says 「a key count written into prose
goes stale the next time the registrar grows」, and `:233` is that mistake made 226 lines below the
warning by the same document. It is a preemption and not a row because it is genuinely minutes:
`docs/oracle_gap.md` is in no `SOURCES` list of the printed kit, is on no build path, and no test pins
any sentence of §7 (the four tests that mention the file mention 「forecast buys」 only in two comment
lines). It is 「사용된 자료에 대한 출처 명기」, a named criterion of a 20-point row on **both** tables,
failing on the page four judge-facing surfaces send a judge to.

**F2 · WFG-228 (P0) · the headline statistic of that same document has no null model, and this
repository already ships the machinery for one.** §4 concludes 「the model gets the size nearly exactly
right and the **place substantially wrong**」 from **IoU 0.394**, and that reading now stands on the
README TL;DR, the finals screen's first 알려진 한계 card, JUDGE_QA **Q36 at tier T0** and the
manuscript. Nothing says what 0.394 should be compared with.
`src/wildfireguardian/validation/baselines.py` has implemented `run_persistence_baseline` (`:44`) and
`run_isotropic_baseline` (`:68`) since Session 4 and neither has been scored on this metric;
`docs/MODEL_CARD.md` gives ~0.07 for the new-ring-only metric and nothing for the forward-sim envelope
on any canvas. So five judges are offered 「자리는 절반쯤 맞힙니다」 with no way to tell whether it is a
criticism of the model or a compliment to it. The row specifies an **area-matched disc** null with
**zero free parameters** — same centre as the shared `t = 0` seed, cell count equal to that slice's
predicted core, same observation and same time-matching rule — and requires the interpretation to be
written down **before** the run and published whichever way it comes out.

**F3 · WFG-229 (P0) · the schedule document now publishes the number that answers 「이거 학생이 만든 게
맞습니까?」, and the card the student drills from carries no number at all.**
`docs/auto/finals/TIMELINE_ROLES.md:81` states 「`Co-Authored-By: Claude` 트레일러 **513개** (전체
662개 중)」, both halves registered (`timeline_agent_trailer_commits`, `timeline_total_commits`). That is
**77.5 %** of the tree, published on the document that answers 「일정 및 팀원 역할 배분의 타당성」, a
named sub-item of a **20-point row on both tables**. The document is right to publish it. But
`grep -c 513 docs/auto/JUDGE_QA.md` answers **0**, and Q29 is a **T0** card, said from memory — so the
judge has arithmetic the student has never drilled. Rides with WFG-226 so the printables rebuild is
paid once (NH-049).

**F4 · WFG-230 (P0) · §4 says three of its four slices share one observation; all four share it to
within 5 %.** Counted from the npz rather than from the artifact summary: the observed cumulative
footprint is **249 / 937 / 987 / 988 / 1021 / 1023** cells at **0 / 333 / 1005 / 1480 / 1812 / 2403**
minutes, so `t = 720`'s denominator (987) is **5.3 %** larger than the 937 the other three use, and
**91.6 %** of this fire's final FIRMS footprint is present at the first post-ignition observation. The
fire gives the whole comparison **one informative frame**. This sharpens the document's existing
caveat rather than contradicting it, and no `WC-###` is opened.

**F5 · WFG-231 (P1) · the ruling that keeps three correct 「household-level」 lines from being "fixed"
lives in the one file a lap sweeping for them will not open.** `README.md:3`, `CITATION.cff:5`,
`src/wildfireguardian/__init__.py:1` and `paper/manuscript.md:1` all say 「household-level」 and **none
is a WC-013 breach**: `paper/GAPS.md:389` records that 「routing **for** household-level wildfire
evacuation」 names the *application*, not a per-household *result*, and `:375` records that no
registered spelling covers it — deliberately, as WC-013's own `spellings` block argues at length. The
defect is location, not content.

## Root objection (`hate` on the current headline narrative)

**The narrative now leads with 「the oracle is in the grading, not in the planning」, and that
relocation is genuinely good work — but the document it rests on has spent two consecutive windows
being corrected by critics on its own pointers, and it has never been compared with anything.**
Critic #57 fixed a script name on `:39`; I fixed a registry claim on `:233`; and the number the whole
page exists to produce, 0.394, has no null. The cheapest test of the objection is F2's disc, one script
over one committed npz, and it is cheap precisely because the objection is not about honesty — the page
is scrupulous — but about the absence of a comparator. ⚠ **The one objection I expected to sustain did
NOT survive.** `scripts/build_canonical_hazard.py:137-145` picks the canonical canvas by sweeping
westward extensions until the *simulated* envelope clears the in-grid guard, so if the *observation*
ran past that canvas the 403 missed cells would be a floor and 0.394 optimistic. Measured directly:
**neither stack touches any grid border in any of the eleven slices** (obs rows 71-121, cols 6-96; haz
core rows 70-120, cols 7-96; border count 0 on a 181 × 156 grid). Nothing is clipped, and that answer
is now in a file rather than in a judge's doubt.

## Verified and clean

- **`gates.py --mode full` exits 0 at `d3ca754`** — 2016 passed, 64 skipped, 3 xfailed, pytest
  **299.1 s**: up 31 tests and **183 seconds faster** than critic #57's run. `baseline-verify` WARNs on
  the two git-ignored `data/raw/**` contracts, which is NH-029 and CHARTER §3d working.
- **GitHub `auto-gates`, runs 301 to 327: 27 runs, 20 `success`, 7 `cancelled` (306, 310, 312, 316,
  317, 320, 326, each superseded within minutes) and ZERO `failure`**, run 327 green at this exact
  head. CHARTER §4b sets **no finding #1** for the seventh consecutive lap.
- **All eight** dev reports record `Reviewed by: subagent`; **seven of eight record `block`** and spend
  commits acting on it.
- **Every push carried a report**: `gates.py --assert-reported` over the twelve consecutive pushed
  heads `16f3525` → `d3ca754` returns twelve OK.
- **WFG-225 shipped as reported, checked in the built file and not the template**: the caveat string
  appears twice in `web/finals.html` and twice in `scripts/finals.template.html`, and the bundle
  manifest is re-pointed.
- **§4's result table re-derives from the artifact exactly** (952 / 937 / 534 / 418 / 403 / 0.3941 /
  1.016 and all four per-slice rows).
- **`factchk` on the window's new prose about the world: nothing to correct.** The window adds no new
  citation and no new external URL to any judge-facing surface; the world-facing edits are all
  WFG-222 / WC-013 unit-word corrections carrying dated annotations.
- ⚠ **`curl` against `api.github.com` returned HTTP 200 in this sandbox.** WFG-119 records it as 403.
  Noted on that row, **not** acted on: a capability that flips between laps is not something a routine
  step may depend on, so the GitHub MCP stays the documented path.

## Direction and scorecard

**ZERO §3b reorders, second consecutive critic lap.** DIRECTION.md names WFG-226 + WFG-229 as one
piece of work, then WFG-228. All four new rows are filed **in table order, not at position 1** — the
same deliberate departure critic #57 made, for the same reason: **NH-051** shows 「position 1」 is
NH-038 option **D**'s mechanic while this routine's prompt cites option **B**, whose own words put the
row 「in the table like any other」. NH-051 is open; if you answer D, moving them up is one edit.

**Readiness 8 of 11, ZERO ticked, fifteenth consecutive critic lap** (re-counted from the checklist
table, not inherited). R3 is `blocked(NH-046)`, **NH-046 is one day past due**, R11's WFG-024 waits
behind R3, R12 is yours. The P1 block those six lines release is **9 done against 102 todo**. Appended
to NH-046 and NH-038 rather than filed as a new entry.

**Scorecard: Track B 95 HELD on two moves that offset; Track A 94 → 95.** 제출 자료 UP to 20 (B) and
18 (A) on the finals-screen caveat and the repaired oracle-page pointer; 데이터 수집·분석·해석 DOWN to
19 on Track B only, on F2, because Track A has no such row. **Pre-registered: it returns to 20 when
WFG-228 lands a null figure with its caveat, whatever the figure says.**

## In plain terms

**What the judges would see today.** A project that keeps getting harder to knock over. The screen the
five judges stand in front of now explains the one number on it that was making your own project look
worse than it is, and the page that explains your biggest weakness is honest and checkable. The one
thing a picky judge could still catch: that page says a phrase is "banned by the machine" and it is
not. It is one sentence and the next lap fixes it first.

**What changed since yesterday.** Eight build laps ran and closed one of the two rows I flagged
yesterday. All the automatic checks are green, including GitHub's own, and nothing in the last 24 hours
was pushed without a report. I found one small wrong sentence and three real gaps, and I checked one
worry of my own that turned out to be fine.

**What you should do.** Please answer **NH-046** — it is one day late, and it is the single thing
holding back 102 items of the loop's own to-do list with five days of the sprint left. Then NH-038,
which asks whether that queue should be released a different way. Everything else can wait for you.



## Gates

**ALL GREEN** · mode `full` · head `d3ca754` · 2026-09-10T11:04:15Z · current at `d3ca754`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 16.6 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.1 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 1.8 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 299.1 s | 2016 passed, 64 skipped, 3 xfailed, 52 warnings in 297.61s (0:04:57) |

## Commits since the previous report (-15)

- d3ca754 the dev report names the head it ships in, and keeps the head it was stamped from
- 5b6d716 the board is rebuilt on the rebased tree, so it covers both laps
- ab5a616 report: 2026-09-10T0952Z dev
- 6d4a60b WFG-225: the screen that prints 예산 초과 now says what rule it was measured under
- 9f3c6f3 the paper report names the head it ships in, and says why its own hash is not the one named
- e4321e7 the paper said a walk graph is lost, counts its nodes two sections later, and routes on it in a figure caption
- 53d1a4e claim WFG-225 (20260910T0920Z)
- 1983ff1 the critic report names the head it ships in, and keeps the head it reviewed
- 90d3ea7 critic #57: the page a judge is sent to for the oracle names a script that never wrote the array
- 16e6824 the dev report names the head it ships in, and keeps the head it was stamped from
- 3601c5e the report's own prose collided with the keys the lap had just registered
- ee3ec87 report: 2026-09-10T0703Z dev
- 5bcfe11 WFG-128 and critic #56's preemption: the two counts that run against this project were measured with a rule that binds one arm, and the page that says so withheld its own evidence
- 97978fe claim WFG-128 (20260910T0625Z)
- ddb567c the critic report names the head it ships in, and keeps the head it describes

## Needs a human (25 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09, one day past; raised to HIGH by critic #55 on a measured third instance)
- NH-036 [DECISION] One critic lap told the next one not to edit a file, and that is what kept a false sentence in front of a judge for a window (by 2026-09-10)
- NH-037 [DECISION] The paper's word proxy now stops it a thousand words before your 25-page rule (by 2026-09-10)
- NH-038 [DECISION] Your "product first" rule has spent the last three dev laps on documents, and the readiness line it was written to protect has not moved in five critic laps (by 2026-09-09)
- NH-039 [DECISION] The national wildfire-spread system's manual is an 18 MB PDF the sandbox could not fetch, and one of you can (by 2026-09-12)
- NH-040 [FYI] A critic lap pushed one commit past a red `--assert-reported`, and it is telling you rather than hiding it
- NH-041 [FYI] This lap sent you an email containing only the word PLACEHOLDER, and could not take it back
- NH-042 [DECISION] Two of your own rules collide whenever a withdrawn claim lives in a frozen artifact, and this week they collided three times (by 2026-09-10)
- NH-043 [DECISION] A gate your loop built this morning will go red about twice a day, and the charter tells the lap that meets it to stop working (by 2026-09-09)
- NH-045 [BLOCKER] The staleness gate has closed `auto/dev` to every routine, and the one routine that met it is the one forbidden to clear it (by 2026-09-08)
- NH-044 [DECISION] The claim the paper just retracted is still live on the page the paper cites for it (by 2026-09-09)
- NH-046 [DECISION] Your product's definition-of-done names a command nothing in this project has ever run (by 2026-09-10)
- NH-048 [DECISION] One of the research routine's three literature channels has been dead for two runs, and a working replacement is already proven (by 2026-09-10)
- NH-049 [DECISION] Your critic routine is told to add judge Q&A cards and your own printing gate makes that impossible for it (by 2026-09-11)
- NH-050 [DECISION] You answered two of these questions two days ago and the loop never heard you, because you answered them on the routine page (by 2026-09-10)
- NH-051 [DECISION] The rule your loop has been obeying for three days is not the option it names, and the difference is why one row has been pushed down five times (by 2026-09-11)
- NH-052 [DECISION] The experiment that measures what your own forecast is worth is now one run away, and it would change what 42 means (by 2026-09-12)
- NH-053 [DECISION] The word your front door uses for 42 stopped being right today, and the loop cannot pick its replacement (by 2026-09-12)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
