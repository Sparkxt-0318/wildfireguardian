# WildfireGuardian autoloop · critic · 2026-09-08T2023Z

| | |
|---|---|
| when | 2026-09-08 20:23 UTC · 2026-09-09 05:23 KST |
| branch / head | `auto/dev` / `9c24a8b` |
| kind | critic |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 9, done: 31, parked: 1, title-derivable: 1, todo: 101 |

## What happened this lap

Critic lap #45. Reviewed head `9c24a8b` on `auto/dev`. Window: the ~19.6 hours this clone can
actually read, 2026-09-08T00:22Z to 2026-09-08T20:00Z.

⚠ **Clone honesty first.** `git rev-parse --is-shallow-repository` = **true**,
`git rev-list --count HEAD` = **50**, oldest resolvable commit `088203c` at **00:22Z on 09-08**.
So this clone reads about **19.6 hours**, not 24, and this report makes **no ancestry or
reachability claim at all**. One consequence recorded so the next lap does not repeat it:
`git show 088203c` presents the whole tree as additions because its parent lies outside the clone,
so a naive "what changed in 24 h" read of that commit reports 2,669 files including
`outputs/dispatch/**` and a `data/raw/**` CSV. Nothing was added or modified there, and this lap
nearly filed it as a finding before checking.

## Your decisions, applied

**Nothing new, and both channels were checked.** Gmail,
`from:siyeong0318@gmail.com subject:"WildfireGuardian autoloop" newer_than:14d`: every matching
thread holds exactly **one** message and every one of them is labelled `SENT` — the loop's own
reports arriving back at the same address. `pull_request_read` on PR **#31** returns `[]`. So
`scripts/auto/decisions.py apply` was not called and `docs/auto/decisions_seen.json` is unchanged.

**Twenty entries are open** (14 DECISION, 6 FYI or BLOCKER). NH-032 and NH-034 are due **today**,
2026-09-08 — critic #44 wrote that they were "overdue by a day" and that is corrected here rather
than repeated. NH-035, NH-038 and NH-043 come due tomorrow. **NH-046 (due 09-10) is still the
single reply holding eight P1 infra rows shut**, because it is R3's only blocker and R3 is the last
of CHARTER §14b's six readiness lines.

## The verification of the loop's own claims

✅ **ALL GREEN, and for the first time this week GitHub agrees.**

- `.auto/venv/bin/python scripts/auto/gates.py --mode full` exits **0** at `9c24a8b`, read unpiped:
  `verify` PASS (20.8 s), `baseline-verify` WARN (the documented CHARTER §3d sandbox state),
  `snapshot-verify` PASS, `env-check` PASS, `pytest-full` PASS — **1763 passed, 63 skipped,
  2 xfailed** in 322.5 s.
- `--assert-head` exits 0. `--assert-reported --base 088203c` exits 0 over **56** substantive paths.
- **GitHub's own runs (CHARTER §4b): NO open finding.** Through the GitHub MCP (`curl` against
  `api.github.com` is still refused here, WFG-119): `auto-gates` runs **240 to 257** on `auto/dev`
  are **16 `success`, 2 `failure`**, and both failures are closed inside the window. Run **257** at
  this exact head is `success`.
- **`Main` follows again.** `git ls-remote origin Main` answers **`9c24a8b`**, the same commit as
  `auto/dev`. Critic #44 found it stranded at `6ecc386` because `promote` needs the `gates` job and
  an artifact upload 403 had failed it.
- **The bootstrap succeeded on the FIRST attempt**, `pins_ok: true`, `stack_ok: true`, ending the
  two-lap `pip` `ReadTimeoutError` streak critics #43 and #44 both recorded. Nothing in the
  repository was implicated then and nothing is now.

**Both red runs in the window were closed, and I checked the second one's fix rather than reading
its commit message.** Run **253** (`0cca093`) was `actions/upload-artifact@v4` answering 403 at
`FinalizeArtifact`; `b2cda36` put `continue-on-error: true` on the 「Keep the gate record」 step, and
run 254 then uploaded 2,238,011 B and 9,792 B an hour later — which settles that 403 as **transient**
and retires the ~555 MB quota theory the 1737Z lap had built and then withdrew itself. Run **255**
(`b7c1837`) went red on
`tests/test_finals_acts.py::test_the_four_acts_advance_in_a_real_browser` with
`CDPError: no page target on port 51449 within 30.0s (last: Connection refused)`, and `298a09c`
converted that one error into a `pytest.skip` while re-raising every other `CDPError`. **The
discrimination holds:** `page_target()` has exactly one call site (`scripts/check_finals_acts.py:313`),
against `about:blank`, **before** `Page.navigate` reaches `web/finals.html`, so 「no page target on
port」 is unreachable from anything the screen does; a JS throw raises `CDPError("JS threw: …")`
from `evaluate()` and still fails. The residual risk — a permanently broken Chromium now skips in
silence while `finals-acts` carries `if-no-files-found: warn` — was filed by that lap itself as
**WFG-196**. I am not filing against it.

✅ **Report certification.** Every **dev**, **critic** and **paper** report in the window carries
`Reviewed by:`. Two do not and neither is a finding: `2026-09-08T1132Z-manual.md`, which critics
#42, #43 and #44 each declined to file and I decline for the same reason and not a new one; and
`2026-09-08T1836Z-research.md` — checked rather than assumed, the only other research report in the
tree (`2026-09-06T1838Z-research.md`) has none either, so this is the research routine's standing
convention rather than a regression. Recorded so a later lap need not re-derive it.

✅ **Every push in the window carried a report**, read run by run: run 254 carried
`2026-09-08T1718Z-critic.md`, run 255 carried `2026-09-08T1737Z-manual.md` with the workflow change
`b2cda36` in the same push, run 256 carried `2026-09-08T1758Z-manual.md`, run 257 carried
`2026-09-08T1836Z-research.md`.

## Findings, ranked

### F1 (the root objection) · The project's strongest claim is labelled on its own front door as a bound its model does not reach, the experiment that would replace the bound with a number is P1, and the material that experiment needs has been committed here the whole time

`docs/auto/JUDGE_QA.md:1393` files the question as **Q36 · T0** — the bank's own top tier, the one
whose header says a card you cannot answer costs you that judge — and the answer it gives the
student is 「맞습니다」 with nothing measured after it. `README.md:637-645` says the same in English:
42 is 「an **upper bound** — what a *noiseless* forecast would buy, not what this project's own model
buys」, and 「실제 값은 그보다 적으며, **얼마나 적은지는 이 저장소의 어떤 실행도 측정하지
않았습니다**」. `docs/present_perimeter_arm.md` §5 says it in the document's own words. The row is
**WFG-125** and it has sat at **P1**, below twelve P0 rows, since critic #23 filed it.

**The cheapest test, and it is the finding.** `data/processed/spread_v2_lofo_oof_cells.csv.gz` holds
the shipped model's **leave-one-fire-out out-of-fold** per-cell probabilities: 151,904 rows, columns
`fire_id, op_from, row, col, label, dist_band, dist_to_fire_m, far_band, prob`, with **20,749**
cells for `yeongdeok_2025` and **82,736** for `uiseong_andong_2025` — the two regions the routing
arms use. Those are predictions the model made on a fire it did **not** train on, which is exactly
「a field the model produced rather than the field it is graded on」. Shape check, measured:
`hazard_uiseong_andong_2025.npz`'s `haz_stack` is `(5, 135, 128)`, and the uiseong OOF cells span
rows 23-123 and cols 0-126 — inside that grid — with 18 `op_from` slices against 5 committed hazard
times.

⚠ **What this lap did NOT verify, and the dev lap must, first:** that the OOF `row`/`col` indexing
shares an origin and CRS with the hazard stack the router consumes, and how 18 `op_from` slices map
onto 5 committed hazard times. If they do not align, WFG-125 falls back to its written branch — and
that record is then worth **more**, because it can name the file and say precisely why it cannot be
used, instead of recording that no such field exists.

**Routing only. No retrain, no re-acquisition, no committed artifact modified** (CHARTER §3 rule 2
and §3.11 both hold). **WFG-125 is this lap's ONE row move: P1 → P0, table line 97 → 49, position 3.**
It makes thirteen P0 rows against seven sprint days and that is stated rather than hidden.

### F2 · 창의성 has a written answer and no judge can reach it

At `9c24a8b`, a `grep -cE` over the alternation of 창의 and 독창 answers **2** on
`docs/auto/JUDGE_QA.md`, **0** on `web/finals.html`, **0** on `docs/auto/DEMO_SCRIPT_5MIN.md` and
**0** on `release/kcf-finals-2026/README_KO.md`. Meanwhile `docs/creativity_card.md` exists — 149
lines, 10 hits, bound by `tests/test_creativity_card.py` — and **is not in the printed kit**:
`docs/auto/finals/printables/manifest_20260908T1529Z.json` lists six source documents
(`BOOTH_SETUP`, `DEMO_SCRIPT_5MIN`, `JUDGE_QA`, `submission_reconciliation`,
`DETECTION_FLOOR_CARD`, `RELATED_WORK_PANEL`) and this is not one of them. The row is worth **20
points on both rubric tables** and the 심사기준 names it first. Written into **WFG-194** as a
strengthened *Done when*; no new row.

### F3 · The judge drill found a named rubric bullet at zero on every judge-facing surface

설계와 방법론 (20 points, both tracks) lists four bullets and one is 「일정 및 팀원(개인의 경우
제외) 역할 배분의 타당성」. For an individual entry the 팀원 half is excluded by the criterion's own
text; the **일정** half is not. Measured at `9c24a8b`: `grep -n 일정` answers **0** matches in
`docs/auto/JUDGE_QA.md`, **0** in `release/kcf-finals-2026/README_KO.md`, **0** in
`web/finals.html`, and its single hit in `docs/auto/DEMO_SCRIPT_5MIN.md:70` is 「일정 크기」 —
「a certain size」, a false positive. Forty-six cards, no 일정 card. **WFG-027** exists, is P1, and
estimates **hours**; the material is `git log`, the sprint plan and `docs/auto/reports/`.

⚠ **I did not move it.** The one reorder went to WFG-125, and raising a priority without moving the
position is the exact defect critic #42 recorded against critic #41. It is written into the row and
onto DIRECTION as **the next critic lap's reorder candidate**, so that lap need not re-derive it.

### F4 · The head of the `todo` block is a P1 infra row again, and this lap can name the mechanism instead of moving a fourth row

Critics #40, #42 and #44 each spent their one reorder on this shape and each recorded that it
recurred. The reason is not that the moves were wrong. It is that **new rows are inserted near the
TOP of the table, so a new P1 row is born ABOVE the P0 block.** Measured at `9c24a8b` before this
lap's edits: WFG-189, WFG-191, WFG-192 at table lines 49-51 and WFG-196, WFG-197, WFG-198 — all
three filed in the last 24 h, all P1 — at lines 53-55, with six P0 `todo` rows below them at lines
85, 86, 87, 88, 114 and 117 and one at 184. So the gate WFG-183 and WFG-191 ask for must check
**order** (no non-P0 `todo` row above a P0 `todo` row), not only **shape**, or it will pass a table
that still points a fresh lap at work §14b forbids it to take. Written into WFG-191. **No fourth
row filed**, for the same reason critic #44 declined one.

## The one `fix-before-next-row` item: WFG-199, and it is one command

**Run `make finals` before you claim a row**, and re-point `release/kcf-finals-2026/MANIFEST.json`
in the same commit (WFG-152). CHARTER §14b names the finals screen as a judge-facing surface and
this is minutes.

Measured at `9c24a8b`, unpiped: `web/finals.html`'s embedded payload carries `"git":"25f6b60"` and
`git rev-list --count 25f6b60..HEAD` answers **22** against `tests/test_finals_screen.py:540`'s
`STAMP_MAX_COMMITS_BEHIND = 30`. **Critic #44 measured 16 at `0cca093` three hours earlier** — six
commits in three hours, about two an hour — and this lap's own report commits take it to roughly 24.
A dev lap costs 3 to 5 commits, so the next lap lands near 28 and **the lap after it trips the
assert on its claim commit alone**. That is NH-045 (BLOCKER, open) replaying for the third time, and
for the third time the routine that can see it coming is the one routine forbidden to clear it.
⚠ Stamp a commit reachable from `origin/auto/dev`, not your own unpushed HEAD
(`tests/test_finals_screen.py::test_the_escape_this_gate_cannot_close_is_still_open`).

**WFG-127 stays position 1 of the table.** It was position 1 at critic #44 and no dev lap has run
since — the 1817Z slot was ceded to research — so `README.md:232` still reads
「고원이 아니라 뾰족한 봉우리」, unchanged, contradicting `docs/auto/JUDGE_QA.md:1394`,
`docs/auto/DEMO_SCRIPT_5MIN.md:151` and `docs/present_perimeter_arm.md:129`.

## `factchk` — the window's claims about the world

✅ **The research lap's Ready-Set-Go item is exactly right, verified independently.** I opened
<https://biz.heraldcorp.com/article/10675702> myself. It is dated **2026-02-12** and carries,
verbatim, 「'준비(Ready)-실행 대기(Set)-즉시 실행(Go)'으로 이어지는 단계별 체계」 and 「화선 도달
8시간 전 산불확산 예측 정보를 바탕으로 고령자 등 안전 취약계층의 선제적 대피를 돕고, 5시간
전에는 대상 주민이 안전한 곳으로 지체 없이 이동하도록 유도할 방침」. Source, date, wording and
both figures are as recorded, and the note's own framing — a **press restatement of an agency
plan**, 「방침」, no measured sufficiency — is the correct one.

✅ **And the figures stayed where CHARTER §13 puts them.** 8시간 / 5시간 / Ready-Set-Go appear in
**no** README, screen, script, manuscript or bundle file. The single hit for 76 % → 88 % outside the
knowledge note is `docs/auto/JUDGE_QA.md:688`, where it appears inside a **prohibition** — 「기관의
계획 발표를 언론이 옮긴 것」, do not compare accuracy in either direction — which is the licensed
use, not a leak.

✅ **The new English abstract's one world claim checks out, and its shape is why.**
`README.md:623` calls the March 2025 Gyeongbuk fires 「the largest on Korea's record by burned area」
and gives **no hectare figure**. Public sources give 104,788 ha (2025 national), 99,289 ha (the
Gyeongbuk event) and 45,157 ha (a provincial interim tally) for overlapping scopes — which is
exactly the `12b8ac7` / NH-015 failure mode. A claim without a number cannot repeat it.

✅ **The manuscript moved in the right direction and it is the window's quiet gain.**
`paper/manuscript.md` narrowed six related-work statements to what the cited papers actually say:
@wstsplus2026 「reports」 → 「finds」, @cova2005 「a spatial line」 → 「a buffer edge」, @li2017
「name the road segments that matter」 → 「identify the prominent ones」, @li2019 「traffic simulation
so it accounts for the time evacuation takes」 → 「fire and traffic simulation models」, @kim2021gk2a
「the Korean local area」 → 「its sectored regions」, @dozier1981 「standard since Dozier」 → 「after
Dozier」, @studenski2011 「an established predictor of survival」 → 「predicts survival」. That is
「기존의 연구와의 차별점」 made accurate rather than merely present.

## Scorecard

**Track B 90 (18 · 18 · 19 · 16 · 19), Track A 90 (19 · 18 · 20 · 16 · 17). No row moves on either
track** — the first all-hold window since `2720840` on 2026-09-07, and it is the honest reading of
three hours that contained two CI-repair laps and one research lap and touched no judge-facing
surface. Pre-registered so the next lap can hold me to it: **B 데이터 수집·분석·해석 goes to 20**
when WFG-125 reports a number from the out-of-fold field, or discharges its written branch by naming
that file and saying why it cannot be used; **창의성 goes to 17** when 창의성 reaches
`web/finals.html` or `DEMO_SCRIPT_5MIN.md`. Full per-row reasoning is in `docs/auto/SCORECARD.md`.

## Direction check (CHARTER §14, five minutes)

`docs/auto/DIRECTION.md` named WFG-193 as row 1 and that row is now `done(b2cda36)`, so the page was
one row stale. Updated: **WFG-199 (the item), then WFG-127, WFG-194, WFG-125.** One reorder spent,
upward, on WFG-125; no P0 row moved below a non-P0 row. **KCF_READINESS: 8 of 11, held (R1, R2, R4,
R5, R6, R7, R8, R9); R3, R11, R12 open, R10 withdrawn.** One line was ticked in the 24 h window — R8,
by critic #43 at `dee1bc1`, re-derived here rather than read — and none in the last three hours, so
the routine prompt's "zero for two consecutive critic laps" direction finding does **not** fire.

## What this lap did NOT find, said plainly

- **No red gate on either machine, no test failure, no fabricated number.** `make verify` PASS at
  this head; every figure in the window's new prose is registered, licensed by a pragma, or absent
  by design.
- **No leakage-style defect introduced this window.** Nothing touched a model, a split, a metric, an
  arm, a coupling or a protocol. The standing leakage issue is the one the repository documents
  about itself, and it is F1.
- **The backlog's malformed-row count did not grow: 10 before this lap and 10 after** — the same ten
  (WFG-167, WFG-181, WFG-182, WFG-188, WFG-175, WFG-168, WFG-133, WFG-115, WFG-112, WFG-149).
  ⚠ **I wrote three more while filing WFG-191's own finding** — an unescaped pipe inside a `grep -cE`
  alternation in WFG-194, another inside a `^| WFG-` pattern in WFG-191, and a WFG-199 row with the
  status cell missing entirely. All three were caught by re-parsing the table before the commit and
  rewritten without the pipe. That is the twelfth, thirteenth and fourteenth instance of WFG-191's
  defect, two of them committed by the lap reading WFG-191 at the time, and it is written into the
  row rather than hidden. Critic #44 did the same thing on WFG-194 three hours ago; a convention
  three consecutive laps cannot follow is a gate, not a convention.
- **I nearly filed a finding from the shallow boundary and did not.** See the clone note above.
- **No JUDGE_QA card is added by this lap, and none may be:** the critic routine must not edit a
  printables `SOURCES` file (DIRECTION, WFG-152). The drill's findings are F2 and F3, both rows.
- **No new NEEDS_HUMAN entry.** Nothing this lap found is blocked on the author; twenty are already
  open and a twenty-first would be noise.
- **The statistician lens found nothing to file.** The headline separates mean-of-folds 0.890 from
  pooled 0.905 「a different quantity」, gives the fold range 0.682-0.974, reports the weak operating
  point (recall 0.138, three of six held-out fires with no true positive), carries a bootstrap 95 %
  CI on pooled and DeLong per-fold CIs, and Q8 answers the cell-independence objection directly.

## The one thing worth copying forward

**Two laps in this window each closed a red CI run by first ruling out the explanation that would
have been convenient.** The 1737Z lap had built an "Actions artifact store is full" theory and a
NEEDS_HUMAN entry to go with it, then downgraded both to an FYI because critic #44's measurement — a
2.24 MB upload succeeding six minutes before a 9.7 kB one failed — runs backwards for that theory.
The 1758Z lap turned a browser failure into a skip only after raising each kind of `CDPError`
through the test body in a throwaway harness, confirming that the launch error skips and `JS threw`
still propagates. Neither argued from its commit message. That habit is why this lap's third finding
is a thing nobody had looked at rather than a thing somebody had got wrong.

## In plain terms

- **What the judges would see today.** A front door that is honest to the point of being hard on
  itself, and one sentence on it that is still wrong: `README.md` line 232 tells a judge the buffer
  width is a sharp peak, while your own booth script says out loud that the experiment cannot tell
  a peak from a plateau. Two other things a judge would ask about have no answer anywhere they can
  reach: 창의성 — 20 of the 100 marks, and the first thing the rubric names — appears nowhere on the
  finals screen or the demo script even though you have a good page written about it, and there is
  no answer at all to 「어떤 일정으로 만들었습니까?」, which the rubric also asks for by name.
- **What changed since yesterday.** The machinery got healthy: both broken GitHub checks are fixed,
  `Main` is following your work again, and the paper stopped overstating what six papers it cites
  actually say. But nothing a judge sees changed in the last three hours, so today's score does not
  move: 90 out of 100 on both tracks.
- **What you should do.** Two things, and one is a five-minute reply. First, answer **NH-032** and
  **NH-034** — they were due today, and until you do, the student is forbidden to say any of the
  three headline margin numbers to a judge. Second, know that I found something good: the file that
  would let this project finally measure **what your own model buys, rather than what a perfect
  forecast would buy**, is already sitting in the repository
  (`data/processed/spread_v2_lofo_oof_cells.csv.gz`). No retraining, no new data. I raised that row
  to top priority; the next build lap should try it, and if the grids do not line up it will say so
  plainly instead of guessing.



## Gates

**ALL GREEN** · mode `full` · head `9c24a8b` · 2026-09-08T20:06:28Z · current at `9c24a8b`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 20.8 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.1 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 1.9 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 322.5 s | 1763 passed, 63 skipped, 2 xfailed, 52 warnings in 321.28s (0:05:21) |

## Commits since the previous report (298a09c2674b487045cd31fdb51c7db100fff5cb..HEAD)

- 9c24a8b research 2026-09-08: the forecast horizon has a policy anchor, and the dead scan channel has a replacement

## Needs a human (20 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09)
- NH-036 [DECISION] One critic lap told the next one not to edit a file, and that is what kept a false sentence in front of a judge for a window (by 2026-09-10)
- NH-037 [DECISION] The paper's word proxy now stops it a thousand words before your 25-page rule (by 2026-09-10)
- NH-038 [DECISION] Your "product first" rule has spent the last three dev laps on documents, and the readiness line it was written to protect has not moved in five critic laps (by 2026-09-09)
- NH-039 [DECISION] The national wildfire-spread system's manual is an 18 MB PDF the sandbox could not fetch, and one of you can (by 2026-09-12)
- NH-040 [FYI] A critic lap pushed one commit past a red `--assert-reported`, and it is telling you rather than hiding it
- NH-041 [FYI] This lap sent you an email containing only the word PLACEHOLDER, and could not take it back
- NH-042 [DECISION] Two of your own rules collide whenever a withdrawn claim lives in a frozen artifact, and this week they collided three times (by 2026-09-10)
- NH-043 [DECISION] A gate your loop built this morning will go red about twice a day, and the charter tells the lap that meets it to stop working (by 2026-09-09)
- NH-045 [BLOCKER] The staleness gate has closed `auto/dev` to every routine, and the one routine that met it is the one forbidden to clear it (by 2026-09-08)
- NH-044 [DECISION] The claim the paper just retracted is still live on the page the paper cites for it (by 2026-09-09)
- NH-046 [DECISION] Your product's definition-of-done names a command nothing in this project has ever run (by 2026-09-10)
- NH-048 [DECISION] One of the research routine's three literature channels has been dead for two runs, and a working replacement is already proven (by 2026-09-10)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
