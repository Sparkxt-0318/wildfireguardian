# WildfireGuardian autoloop · critic · 2026-09-06T0516Z

| | |
|---|---|
| when | 2026-09-06 05:16 UTC · 2026-09-06 14:16 KST |
| branch / head | `auto/dev` / `91d3e05` (the head this lap reviewed) |
| ships in | `9ebf5a5`, pushed to `auto/dev`. `gates.py --mode full` was re-run **on that commit** and is ALL GREEN (`1551 passed, 56 skipped`, 2026-09-06T05:20:08Z), with `--assert-head` and `--assert-reported` green before the push; the gate table below was generated at `91d3e05` and is the review run, not the push run |
| kind | critic |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 7, done: 16, in-progress: 1, parked: 1, todo: 72 |

## What happened this lap

# Critic #24 — the best manuscript lap this loop has run, and the finding is in what it did not touch

Window `de7bd0a..91d3e05` on `auto/dev`: three commits, one substantive (the 0323Z paper lap), one a bare
claim, one critic #23's own report tail. Reviewed at `91d3e05`.

## Your decisions, applied

**Nothing new this lap, and I checked both channels.** The Gmail search
`from:siyeong0318@gmail.com subject:"WildfireGuardian autoloop" newer_than:14d` returns twenty-five threads
and every one of them is a report this loop sent to you; no thread carries a reply message, so
`scripts/auto/decisions.py` had nothing to parse and `docs/auto/decisions_seen.json` is unchanged. Your
2026-09-06 decisions — NH-029 (baseline growth is information), NH-030 option C (stale claims self-release),
NH-031 option A (budgeted bucket key), and the WFG-121/122 rows — came through the laptop session at
`4d705df` and were already applied before this lap started. PR #31 has no new `NH-###:` comment.

One of those decisions is the subject of this lap's escalation: **NH-030 option C, the three-hour release
rule, cannot fire on the three-hour dev grid.** See F4.

## Verification of the loop's own claims

- `gates.py --mode full` at `91d3e05`: **ALL GREEN**, exit 0 (`verify`, `snapshot-verify`, `env-check`,
  `pytest-full` **1545 passed, 62 skipped**, cold, 179.9 s). `baseline-verify` WARN is the documented
  NH-029 behaviour.
- **GitHub's own runs (CHARTER §4b):** all fifteen `auto-gates` runs on `auto/dev` in the window are
  `success`; two are `cancelled`, both superseded by a later push. Read through the GitHub MCP, not `curl`,
  which WFG-119 records as 403 in this sandbox. **No red run sits behind a green report, so there is no
  finding #1 and no `fix-before-next-row` item is owed to a gate.**
- Every dev report of the last 24 h carries a `Reviewed by:` line (1542Z, 2132Z, 0059Z, and the 0323Z paper
  lap), and each records a `block` that was fixed rather than argued.
- `factchk`: the window's diff contains **no new claim about the world**. Every new sentence in
  `paper/manuscript.md` §4.4/§4.5 is about this repository's own runs, and the lap's own reviewer already
  removed the one unregistered quantity it had lifted from a backlog row's prose (「roughly 800 m to 1.5 km」).
  Nothing to check outward, and I am reporting that rather than manufacturing a check.

## Findings, ranked

**F1 — the root objection, and it is the oldest one in this project. `WFG-129`, P0, new.**
The number the student says out loud and the judged screen prints is **42 of 458 on 영덕**, and it has never
met a fair opponent. On the one region where the fair opponent was built (WFG-114, 의성·안동), most of that
region's contrast did not survive it — and the manuscript said so in its own abstract this window, which is
the loop at its best. **The cheapest test of the 42 exists, is fully specified, is runnable in this sandbox
in minutes, and lives in the one file no dev lap reads.** `paper/GAPS.md` G7, rewritten by this window's
paper lap: mask slice 0 of the committed canonical field (p ≥ 0.5, 249 cells,
`data/processed/routing_demo_canonical.npz`) as a node filter and re-run the existing `naive_route` over
**only the 44 origins whose fire-blind route enters the hazard**, counting how many a present-perimeter-only
router already saves. Zero buffer, one region, every input committed, no refit, no re-acquisition;
`F8(a)` in `paper/make_figures.py` already loads that same mask. G7's own words: 「A dev lap should run this
before the finals whatever the author decides on NH-027.」 CHARTER §4 step 1 does not list `paper/GAPS.md`,
and CHARTER §12 forbids the paper routine from writing outside `paper/`, so the instruction sits where
neither routine can act on it. Checked: `44 origins` and the slice-0 recipe appear **zero** times in
`docs/auto/BACKLOG.md`; WFG-033(b)`:888` holds only the full two-lap P2 version.

**F2 — the same defect shape one document over, on a page the README links. `WFG-128`, P0, new, minutes.**
`docs/multi_region.md:191` states the only bucket in this repository that runs against the project as
「`fa_exceeds_budget` — fire-blind route safe, future-aware router cannot finish in time — ... **2** for
Uiseong-Andong and **3** for Uljin-Samcheok」. `docs/present_perimeter_arm.md:46-63` measured the opposite:
the committed classification scores the fire-blind route under **no** time budget while the forecast-aware
router enforces the 600-minute one internally, those two origins' fire-blind routes arrive at **624.8** and
**628.2** minutes, and under one rule applied to both arms the bucket is **empty** — origins no arm saves,
not origins the forecast lost. So the sentence's stated meaning does not hold for either of its own two
members. `README.md:113` names the same bucket and sends the reader to that file for 「완전한 분할」.
`paper/manuscript.md` §4.4 gained exactly this qualification in the 0323Z lap and its twin did not: **the
WFG-126 shape, one window later, in the other direction.** You already chose the fix (NH-031 option A,
2026-09-06); `WFG-122` carries the registry half at P1 and the prose half needs neither the key nor NH-032.
The booth answer is now `JUDGE_QA.md` **Q38**, with the two sentences not to say.

**F3 — the judged screen is 57 registry entries stale and the repair is two commands this loop has already
run. `WFG-113` / `WFG-115` / `WFG-117`, updated, not duplicated.**
Counted at this head by parsing `web/finals.html:434` and `docs/NUMBERS.json` in one process: the 검증
레지스트리 card prints `n_entries` **326** / `n_reproducible` **268**; the registry holds **383** / **325**.
`scripts/build_finals.py:629-630` derives both from the registry, so `make finals` fixes them and needs no
decision from anyone. **The standing reason not to rebuild is gone and no row said so:** WFG-109 was that
reason and is `done(20260905T1520Z)`, and its own closure records the lap re-running `make finals` with
「the ONLY line it changed is the payload (434)」, then `make finals-bundle UPDATE=1`. It does **not** fix
「built at commit 41498ef」, which comes from the registry's `built_at_git_commit` and is still **not** an
ancestor of `HEAD` — re-tested on a clone deepened to **250** commits so the shallow boundary is not the
confounder. ⚠ **WFG-119's predicted sandbox-only red now has a date:** the screen's stamp `5f9a3b8` is 23
commits behind `HEAD`, the branch took **53** commits in 24 h, and the sandbox clones at depth 50, so within
roughly twelve hours `test_the_integrity_panel_names_a_commit_this_repository_has` goes RED in every sandbox
while CI at `fetch-depth: 0` stays green — and CHARTER §9 sends the first lap that hits it to `auto/red/`
instead of building. `make finals` resets that clock as a side effect.

**F4 — the rule you chose to un-stick a stranded row cannot fire on the grid it runs on. `NH-035`, DECISION,
MEDIUM, with options.**
CHARTER §5b (your NH-030 option C) releases an `in-progress` claim 「more than three hours old」 at the next
dev lap. The dev cron is `17 */3`, and laps claim between **3 m 26 s** and **3 m 59 s** after waking — read
off the commit timestamps of the last five claims (`7233743` 03:20:35, `81a0a15` 00:20:26, `d14b29a`
21:20:53, `492364c` 18:20:59, `5f9a3b8` 15:20:40). So at the next wake a stranded claim reads **2 h 56–57 m**
old, under the bar every time, and the rule can only fire **two** slots later — six hours, two dev slots.
The only release it has ever performed cleared by seconds: `492364c` claimed at `18:20:59Z`, `785ba13`
released at `21:20:46Z`, which is 2 h 59 m 47 s from the claim commit and 3 h 00 m 46 s from the label
`20260905T1820Z`, and no document says which timestamp to read. It is live now: **WFG-007 is
`in-progress(20260906T0320Z)`** and, if that lap died, the 06:17Z lap must skip it. Options A) two hours,
B) age against the previous dev slot rather than a clock, C) keep three hours but fix which timestamp is
measured, D) something else.

**F5 — direction: readiness 4 of 11, unchanged for nine critic laps, and the explanation has changed.**
R2, R4, R5, R6 ticked; zero moved in the window, which by the routine's own rule is a direction finding.
But it is no longer the queue: **`7233743` claimed WFG-007 three minutes after the 03:17Z lap woke**, the
first window in which it was unambiguously first by both routes. Critic #23's falsifiable test is running
and the early half of the answer is that position was at least part of the constraint. What the window
exposes instead is the *release* rule (F4). Nine days of R7 has been queue position and, twice now, a lock
with no key.

**Bookkeeping.** `WFG-126` is `done(91d3e05)` — the paper lap closed it inside the window that filed it,
verified at this head (§4.3's `[GAP]` no longer says the arm is 「scheduled after this sprint」, and
`paper/GAPS.md` G7 records the narrowing). No row was moved: the reorder budget is deliberately unspent,
following critic #22's precedent, because the top row is claimed and moving anything would only disturb a
test that is finally running.

## The one `fix-before-next-row` item

**Critic #23's, carried forward unchanged: WFG-127 (i).** Both surfaces still assert it at `91d3e05` —
`docs/fair_opponent_line.md` §3 「spike, not a plateau」 and `DEMO_SCRIPT_5MIN.md` 3막 「어느 폭이 맞는지는
그날 알 수 없고」. Fifteen minutes, prose only, no run, no new number, and **the correction is already
written**: the paper lap made exactly this fix in §4.5 this window, which now asserts neither spike nor
plateau. I did not spend the budget on F1 or F2, and the reason is method rather than modesty: an item that
survives a lap unspent is the only way to learn whether the item or the mechanism is at fault, and
substituting a fresh one would lose both that and the finding.

## What the window did well, said plainly

The 0323Z paper lap is the best submission-material work in this series. It wrote the fair opponent into the
manuscript, cut the abstract and the conclusion down to what the arm supports, and then **quoted no count
from the arm at all** because the registry's own all-or-none caveat and NH-032 left no legal number — and
said that in the paper's voice rather than hiding it. Its reviewer blocked a draft that withheld the margin
while printing a figure whose bar totals and axis denominator determine it in two subtractions, and the lap
withdrew the figure rather than arguing. Its closing observation is the one worth keeping: **every gate was
green across that defect**, because no gate can see a number that exists only as the difference of two
printed ones.

## Scorecard

| | 연구/개발 목적 | 설계와 방법론 | 데이터 / 구현 | 창의성 | 제출 자료 | total |
|---|---|---|---|---|---|---|
| **Track B** | 17 | **15** (+1) | 19 | 15 | **18** (+1) | **84** (+2) |
| **Track A** | 16 | 15 | 17 | 15 | 14 | **77** (=) |

**B 설계와 방법론 14 → 15:** critic #23 docked this row for a sentence asserting a shape a five-point grid
cannot resolve; §4.5 now states the change of *kind* across the widths and refuses both spike and plateau,
and the lap's reviewer removed three unregistered or unmeasured claims in both directions. Not 16, because
WFG-127 (i) is still live on the two booth-side documents. **B 제출 자료 17 → 18:** for Track B the
manuscript *is* the 제출 자료 — WFG-126 closed, the deadlock named, the margin-determining figure withdrawn,
22 of 25 pages measured under Carlito rather than converted from a word count. **B 데이터 holds at 19** on
two moves that cancel: the best interpretation discipline this loop has produced, against F2. **Track A is
flat because the only substantive commit in the window is a manuscript lap and these five judges do not read
the manuscript** — the split critics #2, #8 and #16 applied. A 구현 및 유용성 17 is re-earned, not
inherited: gates green in this fresh sandbox, and it does not rise for the ninth window because no PDF, a
57-entry-stale card and an unreachable stamp are all still on the judged surface.

## In plain terms

**What the judges would see today.** The same booth as yesterday: four screens that work offline, no printed
sheet in your hand for the ninth day, and a screen whose 검증 레지스트리 card says you have registered 326
numbers when you have registered 383. Your paper is genuinely strong and got stronger today. Your booth did
not change.

**What changed since yesterday.** One real lap ran, on the paper, and it did the hardest thing well: it
wrote your fair-opponent experiment into the manuscript, told the reader outright that your headline is
smaller than it looked, and then refused to print any number from that experiment because two of your own
laps disagree about it and you have not decided which is right. I found two things it left behind — a
sentence in `docs/multi_region.md` that describes your one bad result in a way your own measurement says is
wrong, and the cheapest possible test of your headline number sitting in a file none of the routines read.

**What you should do.** Three things, in this order. (1) Answer **NH-032** — which of the two fair opponents
is the fair one. It is now blocking your paper, your booth script and two backlog rows. (2) Answer
**NH-034** — what the booth says about it. (3) Answer **NH-035**, one line: the three-hour rule you set for
releasing a stuck row can never actually fire, because laps claim their row three minutes after they start
and the grid is three hours. Two hours would fix it.



## Gates

**ALL GREEN** · mode `full` · head `91d3e05` · 2026-09-06T05:01:58Z · current at `91d3e05`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 15.2 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.1 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 1.3 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 179.9 s | 1545 passed, 62 skipped, 52 warnings in 179.17s (0:02:59) |

## Commits since the previous report (614500125f14e325f122c46ff66180a26e37fa5b..HEAD)

- 91d3e05 the paper reports the fair opponent and, by the registry's own rule, none of its numbers
- 7233743 claim WFG-007 (20260906T0320Z)

## Needs a human (8 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
