# WildfireGuardian autoloop · dev · 2026-09-10T0142Z

| | |
|---|---|
| when | 2026-09-10 01:42 UTC · 2026-09-10 10:42 KST |
| branch / head | `auto/dev` / `17499a8` |
| kind | dev |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 9, done: 33, dropped: 1, parked: 1, title-derivable: 1, todo: 108 |

## What happened this lap

## Your decisions, applied

**Nothing new on either channel.** The Gmail search `from:siyeong0318@gmail.com subject:"WildfireGuardian autoloop" newer_than:14d` returns only this loop's own sent reports — every thread in the window holds exactly one message and none is a reply — and a second search for `subject:"Re: WildfireGuardian"` in the same window returns nothing at all. PR #31 has zero comments. So no `NH-###:` line reached the loop this lap, no decision was applied, and no reply line was left unmapped.

⚠ **Twenty-five entries are open; six come due today and seven are already past.** Counted at this head: NH-036, NH-037, NH-042, NH-046, NH-048 and NH-050 are stated due **2026-09-10**, and NH-032, NH-034, NH-035, NH-038, NH-043, NH-044 and NH-045 are past their stated dates. The readiness count has not moved in eleven critic laps and both remaining agent-reachable lines are downstream of one of them (NH-046).

---

## The lap

**Row: WFG-222 (P0, KCF), named by `DIRECTION.md` over table order with a reason** — it is the only one of the WFG-218 / WFG-220 / WFG-222 group that is a *correction* rather than an addition, and adding cards to a screen that states the wrong thing is work done twice. Claim commit `49ac16e`, pushed before building. No stale `in-progress` claim to release: no status cell in the table held one.

**Critic #54's one `fix-before-next-row` item was cleared first**, as NH-038 option B requires for a minutes-long item, and it lives in the same file as this row's third surface.

### The root objection I recorded against my own plan, before building

*Replacing 「가구 단위」 with 「지점 단위」 on five surfaces corrects a noun and leaves the defect. A judge does not object to the word. A judge objects when item ① says the committed instances exist and item ② says both axes were real, and nothing on the surface says the committed instances came from a synthetic fire.*

The cheapest test of it, run at `49ac16e` before any edit: take the block on each of the five surfaces that makes the existence claim and ask whether any token naming the synthetic hazard or the sampled origins appears **inside that block**. The answer was **zero of five**. That measurement decided the shape of the whole lap — the bound in the same block is the fix, the word is the smaller half — and it is now the gate.

### What shipped

**The row named five surfaces. Eleven were corrected, and the row's list found the fewest of them.** The five: `README.md` §5 item ①; `scripts/finals.template.html` CREATIVE item ① in Korean **and** English, with `web/finals.html` rebuilt from it; `docs/auto/JUDGE_QA.md` Q29a (tier **T0**, spoken from memory); `docs/auto/DEMO_SCRIPT_5MIN.md`'s 도입 segment; `docs/creativity_card.md`'s method table row 1. My own sweep added a sixth, **`docs/auto/finals/RELATED_WORK_PANEL.md`**, a *printed* panel saying the same thing about this project's own output. Registering `WC-013` found **three** more. The independent reviewer found **two** the registry's first pattern still missed. Where each came from is below.

Each block now carries, in the same block: 지점 단위, that the run which produced the committed dispatch documents had `hazard: synthetic`, `terrain: synthetic` and `origins: sampled candidates`, and that **실제 확산면으로 만든 출동 지시서는 아직 없습니다**.

**Two things were added rather than swapped, because a word swap alone would have left the conjunction standing.**

- `docs/auto/JUDGE_QA.md` Q29a gains a 없는 것 item, placed **first**, that forbids reading items ① and ② as one sentence and gives the student the answer to 「이 출동 지시서, 진짜 불로 만든 겁니까?」 — the one question in critic #54's own judge drill that had an answer nowhere but the README. The answer is 「아직 아닙니다」.
- `docs/auto/DEMO_SCRIPT_5MIN.md` takes its bound in a ⚠ block rather than in the spoken line. §1's syllable budget is measured (`tests/test_demo_script_pace.py`), one added sentence moves the segment off the document's single rate, and 가구 → 지점 is **two syllables for two** — so the student says the corrected word inside the same 37 seconds and the pace gate never moved.

### The gate, and why it is a different shape from the one it replaces

`tests/test_output_object_claim_bounds.py` holds one **structural** property over six declared blocks: *a block that asserts committed instances of the output object exist must carry the synthetic-hazard and sampled-origin bounds itself.*

- It **cannot be satisfied by deleting the claim** — a separate assertion fails if a surface goes quiet, which is the state WFG-194 found `web/finals.html` in for six days.
- Its families are not strings I invented: 합성 / synthetic and 표본 / sampled are the words `data/processed/rescue_routing.json` → `provenance.sources` uses about itself.
- It is **graded, not asserted** (MEMO 2026-09-09, the `_UPPER_BOUND` anti-pattern): the mutation test strips every bound-carrying sentence from each shipped block and requires the check to fail. The same mechanical mutation runs on all six, so no wording was hand-picked to fail. The five pre-fix wordings are kept as regression data.

`tests/test_readme_round4_lead.py` now runs that assertion over the whole Round-4 **section** rather than over its `lead` fixture. That fixture boundary is exactly what let one file answer its own headline claim two ways 180 lines apart, and it was written by the lap that made the claim. Both new assertions there were graded against the pre-fix text by calling the test functions with the pre-fix section string: both go red on it, and both pass on what shipped.

### WC-013, registered in the same lap — and it found three copies no list named

CHARTER §3.5c. The claim had already been withdrawn **once, on one surface, without being registered**: WFG-212's reviewer broke the lead block on the conjunction at 2206Z yesterday and that lap fixed `README.md` alone. That is the third repeat of the shape §3.5c exists for.

Registering it read every gated file instead of the five a critic listed, and immediately returned three live copies in files nobody had reason to open: `docs/auto/knowledge/KOREAN_OPERATIONAL_SYSTEMS.md` (which *instructs the student what to say at the booth*), `docs/auto/knowledge/PYROGEOGRAPHY.md`, and `docs/auto/research/WEEKLY_2026-W36.md`. The two knowledge notes are corrected with dated 정정 notes (CHARTER §13: append and date, do not restructure); the dated research record is kept verbatim under a pragma, because editing it would rewrite what that run concluded.

The patterns are anchored on 「가구 단위」 **plus the output-object phrase**, never on the bare term, so the three legitimate uses in the same Q&A bank are untouched: Q20a *defines* 「가구」 as one node of the OSM walking graph, Q16 names the quantity `ingress_survival_time_min`, and Q16a says what other systems' published material does not show.

### What did not work, and one limit measured rather than papered over

- **My first version of the Round-4 section splitter cut the lead blockquote per bullet**, which demanded every bound bullet repeat every other bound and went red on the shipped text. A `>` run is now one block: the assertion is about what a judge reads, not about typography.
- **My first §5 bound sentence contained the literal 「**②**」**, and `tests/test_creativity_card.py` slices item ① at the ② marker — so the anchors fell outside the chunk and two gates went red. The sentence now names item ② by its title instead. Worth keeping: a correction written *inside* a block can move that block's boundary.
- ⚠ **`scripts/check_withdrawn_claims.py` scans one line at a time, so a registered spelling that wraps a source line break is invisible to it.** `docs/creativity_card.md` §8 is the worked example: 「a per-household / walk-or-be-rescued verdict」 straddles two lines there and the gate reported it **not at all**. The pragma sitting on that line is one a human put there. `WC-012` had already hit this and registered one sentence as two patterns for the reason, but it was recorded only in a test comment. It is now published as `docs/withdrawn_claims.md` §4 item 7, beside the two limits already there — a reworded claim escapes, and a claim in a `.py` or `.bib` file is out of scope. **Three known holes, all measured, none closed.** That is the honest state and it is worth more at a booth than a gate described as complete.

### The `fix-before-next-row` item, cleared

`docs/auto/JUDGE_QA.md` told the student in three places that an answer from the 운영사무국 was still awaited. `:1159` (inside Q29's T0 「없는 것」) now records what NH-008 actually resolved — the organisers addressed the disclosure question directly, no artifact is required, and the practices in CHARTER §9 are kept voluntarily. `:609` held a spoken sentence back until 「WFG-022가 오기 전에는」, a release condition that cannot occur now that the author has decided not to contact the 운영사무국; it is now stated as a settled instruction rather than a conditional one, and the safe sentence is the sentence. `:1377` was not stale but **false** — it told a judge a query *was* submitted — and now says plainly that none was, and why.

### The independent review blocked this lap, and it was right

`LOOP_CONFIG.json` → `review: subagent`. **Reviewed by: subagent (block): the gate this lap
printed as「graded rather than asserted」graded itself — its mutation was built by deleting
whatever matched its own two patterns and then asserted those same two patterns reported them
missing, which cannot fail — and the surface list was short by two.** I fixed it; I did not
argue with it. Three defects and four smaller corrections, all accepted:

1. **The mutation could not fail** (`mandela` #4, a scorer grading buckets it drew itself), in
   a file whose own docstring cites the MEMO lesson about exactly that. It is replaced by a
   corpus of **the six blocks the repository actually shipped at `3eec471`** — text written by
   five earlier laps over six days, none of which had seen these patterns. Every one is now
   refused, and a second test re-derives the corpus from git and refuses a copy that has
   drifted (it **passed** here rather than skipping).
2. **The checks were polarity-blind.** The reviewer wrote a block carrying 지점 단위, 합성,
   표본 좌표 and the existence claim that asserted the **opposite** of all of it, and the gate
   returned green. Two assertions were added — the bound must carry the consequence (no
   dispatch document has yet been made on a real spread surface, object and absence in one
   sentence), and a block asserting the run *was* real is refused outright. Its sentence is
   kept verbatim as a regression and now fails on both counts. ⚠ My first draft of the
   polarity pattern excluded the newline, so the printed panel's hard-wrapped sentence escaped
   it — **the same line-break hole I had just published about the registry**, reproduced in the
   gate written in the same lap. The gap class is now `[^.。]`.
3. **`RELATED_WORK_PANEL.md` was not in `SURFACES`** — the panel my own commit, MEMO and
   backlog row all used as the *proof* that fixing the surfaces on the list fixes the list.

**And the reviewer found two surfaces the registry still missed**, both this project's own
output in the household register: `docs/evidence/greenpeace_2026_survey.md` (「가구 단위 출동
순서」) and `docs/firefighter_consultation.md` (「가구 단위 인명」). WC-013's Korean anchor
list is widened with 출동 순서 and 인명, after measuring zero other hits in any tracked `.md`
or `.html`.

**The count is the honest finding of this lap.** A hand list found five surfaces, my own sweep
found a sixth, registration found three, and a hostile reader found two more.

Four smaller corrections it raised, all made: `docs/creativity_card.md` §8 printed 「the pragma
above is here because a human put it here」 about a pragma this lap's own commit added — an
agent's action printed for judges as a human's, which is the one thing CHARTER §9 exists to
keep straight; §9 printed 「the first **37** seconds」, a figure tracing only to the demo
script's own segment header and to no `docs/NUMBERS.json` entry, so it is gone; the printed
panel's correction note reprinted the withdrawn wording, breaking the WC-005 / WC-007
precedent the same section cites, so it now describes it; and Q29's 없는 것 now says in the
same breath that the disclosure answer did **not** come from a query to the 운영사무국, which
is what Q33 says three sections away.

⚠ **A correction to this lap's own earlier claim.** 「No committed artifact was regenerated」
is wrong as written: `web/finals.html` and `release/kcf-finals-2026/MANIFEST.json` were
rewritten in place by their documented builders. What is true is that **no CHARTER §3.2
protected path moved** — nothing under `data/processed/` or `docs/figures/`, no snapshot, no
`fire_manifest.json`, and no registered value in `docs/NUMBERS.json`. The kit was rebuilt a
second time at a new stamp after the reviewer's fixes; the 0017Z build stays committed as the
record (CHARTER §3 rule 7).

### Gates

`gates.py --mode full` exits **0** at `17499a8` — **1941 passed, 64 skipped, 3 xfailed**, against 1871 passed at `3eec471`; the 70 new cases are this lap's two gate files and their parametrisations. `verify` passes (20.3 s), `snapshot-verify` passes, `env-check` passes, and `baseline-verify` WARNs on the two git-ignored `data/raw/**` contracts, which is NH-029 and CHARTER §3d working as the author chose. `check_withdrawn_claims.py` passes with **13 claims over 938 gated files**.

⚠ **One operational number worth the next lap's attention: `pytest` took 1,751 s here — 29 minutes.** Every critic lap this week recorded 303-333 s for the same suite. The 70 cases this lap adds run in 0.12 s, so they are not the cause; the machine is. A full-gate step that costs half an hour changes the arithmetic of CHARTER §4's two-hour box, and this lap paid it twice (once on the work head, once on the pushed head). If the next lap sees the same, `--durations=15` is already in the invocation and its output is in the run log.

⚠ **On the head the gate table below names.** `report.py` stamps its header and gate table from `.auto/gates.json` at the moment it runs, which here was the green full pass at `17499a8` — a real pass, on a tree without this report or the backlog and status edits that ride with it. Those travel one commit later, `gates.py --mode full` was re-run on that commit before the push, and `--assert-head` and `--assert-reported` were run immediately before pushing. The pushed head is in this report's own header line and in the push output. Seventh consecutive lap to pay this by hand; it is loop hygiene, so it stays a P1 row under CHARTER §14b and displaces no P0.

### Next row

**WFG-218 and WFG-220**, together, and a lap that takes one should take the other: 일정 counts 0 on `README.md` and 0 on `web/finals.html` (WFG-218), and `web/finals.html` carries no word of the oracle-grading objection this repository corrected everywhere else (WFG-220). Both need `make finals` on a commit already on `origin` and a re-pointed bundle manifest, and that mechanic is paid once if they ship together. **Thirteen** P0 rows remain `todo` with five sprint days left, counted at this head.

## In plain terms

- **What changed for the project.** Yesterday the front door started saying honestly that the printed rescue orders in this repository were made on a *simulated* fire, not a real one. Every other page still said the old thing — including the screen the five judges will stand in front of and a card you say out loud from memory. Today all six pages say the same honest sentence, and a new test refuses to let any of them make that claim again without the limit attached.
- **Why it matters to the judges or the science.** A judge who opens one file and finds it contradicts another page stops trusting both. The dangerous version was worse than a contradiction: two true sentences sitting next to each other read as one false one — 「real roads plus real fire made these sheets」 — and that run does not exist here. You can now answer 「이 출동 지시서, 진짜 불로 만든 겁니까?」 with 「아직 아닙니다」 and point at the page, which is the move that has been this project's strongest card all along.
- **What the author should do.** Nothing for this row. But **six decisions came due today** (NH-036, NH-037, NH-042, NH-046, NH-048, NH-050) and two more are two days past (NH-032, NH-034) — and NH-046 is the single question both remaining readiness lines are waiting on. One reply, one line each, clears all of them.



## Gates

**ALL GREEN** · mode `full` · head `17499a8` · 2026-09-10T01:41:53Z · current at `17499a8`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 20.3 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.1 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 0.3 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 1752.1 s | 1941 passed, 64 skipped, 3 xfailed, 52 warnings in 1751.22s (0:29:11) |

## Commits since the previous report (3eec471dbf774739e9763b4b24ee66950c093a69..HEAD)

- 17499a8 the independent reviewer's block: the gate graded itself, and the list was short by two
- 8bd4b2d WFG-222: every page that offers the dispatch sheets now says what fire they were made on
- 49ac16e claim WFG-222 (20260910T0017Z)
- 02715eb the critic report names the head it ships in, and keeps the run it describes
- 88cb2e7 critic #54: the front door corrected its one affirmative claim and the four surfaces that carry it by design did not

## Needs a human (25 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09)
- NH-036 [DECISION] One critic lap told the next one not to edit a file, and that is what kept a false sentence in front of a judge for a window (by 2026-09-10)
- NH-037 [DECISION] The paper's word proxy now stops it a thousand words before your 25-page rule (by 2026-09-10)
- NH-038 [DECISION] Your "product first" rule has spent the last three dev laps on documents, and the readiness line it was written to protect has not moved in five critic laps (by 2026-09-09)
- NH-039 [DECISION] The national wildfire-spread system's manual is an 18 MB PDF the sandbox could not fetch, and one of you can (by 2026-09-12)
- NH-040 [FYI] A critic lap pushed one commit past a red `--assert-reported`, and it is telling you rather than hiding it
- NH-041 [FYI] This lap sent you an email containing only the word PLACEHOLDER, and could not take it back
- NH-042 [DECISION] Two of your own rules collide whenever a withdrawn claim lives in a frozen artifact, and this week they collided three times (by 2026-09-10)
- NH-043 [DECISION] A gate your loop built this morning will go red about twice a day, and the charter tells the lap that meets it to stop working (by 2026-09-09)
- NH-045 [BLOCKER] The staleness gate has closed `auto/dev` to every routine, and the one routine that met it is the one forbidden to clear it (by 2026-09-08)
- NH-044 [DECISION] The claim the paper just retracted is still live on the page the paper cites for it (by 2026-09-09)
- NH-046 [DECISION] Your product's definition-of-done names a command nothing in this project has ever run (by 2026-09-10)
- NH-048 [DECISION] One of the research routine's three literature channels has been dead for two runs, and a working replacement is already proven (by 2026-09-10)
- NH-049 [DECISION] Your critic routine is told to add judge Q&A cards and your own printing gate makes that impossible for it (by 2026-09-11)
- NH-050 [DECISION] You answered two of these questions two days ago and the loop never heard you, because you answered them on the routine page (by 2026-09-10)
- NH-051 [DECISION] The rule your loop has been obeying for three days is not the option it names, and the difference is why one row has been pushed down five times (by 2026-09-11)
- NH-052 [DECISION] The experiment that measures what your own forecast is worth is now one run away, and it would change what 42 means (by 2026-09-12)
- NH-053 [DECISION] The word your front door uses for 42 stopped being right today, and the loop cannot pick its replacement (by 2026-09-12)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
