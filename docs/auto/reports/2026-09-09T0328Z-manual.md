# WildfireGuardian autoloop · paper · 2026-09-09T0247Z

| | |
|---|---|
| when | 2026-09-09 03:28 UTC · 2026-09-09 12:28 KST |
| branch / head | `auto/dev` / **`1ac2468`** (pushed) — `report.py` stamped this header at `a9e0430`, the commit this lap INCORPORATED, because the report is written before the commit that carries it. The lap's work is `4b0010a` (rebased onto `9a97e53`) and this header correction is `1ac2468`; all four ids resolve. `gates.py --mode full` exit 0 was re-run on `4b0010a` and again on the head that ships, and `--assert-head` exited 0 against each immediately before its push. |
| kind | manual |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 7, done: 32, dropped: 1, parked: 1, title-derivable: 1, todo: 104 |

## What happened this lap

# Paper lap 22 — the grid the paper said could not answer the question answered it, and the answer came with a property no `paper/` page stated

Incorporated **`eff2183..a9e0430`**, seventeen commits, **seventeen** files outside `paper/`
and `docs/auto/`. Unlike the last four laps, **`docs/NUMBERS.json` is in this diff and so is a
new artifact under `data/processed/`**, so a result section could gain a number for the first
time since lap 17. Re-derived key by key against `eff2183` rather than read off the diffstat:
the registry gained **18** keys, all `ppshape_uiseong_*`, and **no existing key's value or
caveat changed**.

`body_words` **8,993 → 8,998**; margin against the 9,000-word proxy **7 → 2**. Built pages
**23** against the author's 25 (NH-028), **re-measured this lap, not inherited**. Gaps **7**,
unchanged. Figures **8** referenced, 9 drawn, all nine byte-identical.

## What changed in the manuscript, and why

### §4.5 — the mandatory correction, and both halves of it were false before the lap began

§4.5 ended: 「The run supports no statement about whether a workable width could be chosen in
advance: the five widths differ by factors of two, so the grid holds a single point in the
region such a claim would be about. **Two** repository documents still draw the stronger
conclusion from those same five points … others were narrowed to this reading during
revision, and the rest is an open item there.」

1. **The grid is no longer five points.** `9170a37` (WFG-127/WFG-199) added **750, 1250 and
   1500 m** on the same runner and the same committed inputs, into a *separate* artifact, all
   five original widths reproducing cell for cell — checked mechanically by
   `tests/test_buffer_shape.py::test_the_five_shared_widths_reproduce_cell_for_cell`, which is
   the control that makes the two grids comparable. The top is a **shoulder two sampled widths
   wide**; the earlier reading of the five points as a peak is withdrawn as `WC-011`.
2. **The count of documents that disagree is retired, not corrected.** The same commit rewrote
   `docs/present_perimeter_arm.md` §4 and `README.md`'s Round-4 bullet, so the answer today is
   **zero**. The manuscript now counts nobody: a tally of who currently disagrees with the
   paper goes stale on every push, has been one, then two, then zero inside four laps, and is
   a fact about this repository rather than about the fire. The clause about the frozen
   `pp_uiseong_*` caveats went with it — §3.5 already tells the reader that superseded values
   are annotated in place rather than deleted.

### §4.5 — the addition (WFG-202), and why it is a limitation rather than a note

**The score put forward as the opponent's is the one at the best width measured, read off the
table after the run** — nothing in the problem chooses a width, and giving the opponent its
best shot is the right and conservative design. What it costs is that a width **added** to the
grid can only tie or beat the incumbent, so the opponent's best score is non-decreasing and
**whatever advantage the forecast is reported to hold over it is non-increasing in how finely
anyone searches**. Not hypothetical here: this repository's one refinement moved the best
width off the committed one and took part of that advantage with it. ⚠ The manuscript says
「reported at」 rather than 「handed」 for a reason the reviewer forced; see the block below.

This is a property of the estimator, not of the loop's filing. §4.5, §5 and §7 all lean on
「a present-perimeter policy recovers most of what the fire-blind contrast credits to the
forecast」, every one of those readings is taken at a post-hoc-selected width, and until this
lap the paper had no answer anywhere to 「what is it on a finer grid?」.

⚠ **Why the qualifier is in §4.5 only, and not also in the Abstract, §5 and §7 — asked and
answered rather than assumed, because this is the shape of the defect that blocked lap 20.**
The property bites where a **margin** is claimed. Those three surfaces claim no margin; they
claim that the opponent 「recovers most」 of the fire-blind contrast and that the share
attributable to the forecast is 「smaller」 and 「not yet a single number」. A better width
recovers **more**, so refining the grid makes every one of those sentences **more** true, not
less. The qualifier is therefore owed where the residual is discussed — §4.5 — and adding it
to a sentence it can only strengthen would spend words to weaken nothing. Each of those three
surfaces already points the reader at §4.5 for the residual.

**Cost, measured with the builder's own counter and reconciled with the document's move:** 80
false words removed, **84** true ones added, plus one turning §4.5's closing 「Two caveats bind
the whole comparison」 into 「Two **further** caveats」 — net **+5**. **Nothing was compressed
and no caveat or registered number was traded** (CHARTER §12). The correction paid for itself
because what it removed had gone false, which is the first lap since 18 where that was true.

**No count from the dense grid is quoted.** The `ppshape_uiseong_*` caveat opens 「Four facts
travel together or none of them may be quoted」 and its fact (3) is that the opponent is
stronger than the committed artifact reports — the margin — which **NH-032** bars from every
judge-facing surface, and CHARTER §14b names the manuscript as one. The asymmetry the denser
grid supports — which side of the shoulder is the costlier one to miss — is likewise not
written: it is scoped to one grid step and is operator advice rather than a result.

## Figures

**No new figure; all nine PNGs byte-identical.** `make_figures.py` printed
`drawn [F1 … F9]; skipped []` and `git status paper/figures/` is empty afterwards. The only
change to the module is a docstring note on `F9_present_perimeter`, recording the decision so
the next lap does not re-argue it: F9 reads the **committed** five-width artifact, which is
where NH-032's options are computed, so it stays there and stays the figure that drops into
§4.5 when that decision lands; a dense-grid companion was costed and **declined**, because its
failure classes plus the safe total sum to the scanned-origin count that **Table 2 of this
manuscript prints**, so the stack would recover the safe series and — against Table 2's
forecast-aware total — the very margin NH-032 bars. That is the same reasoning that already
keeps F9's own totals and denominator off its axes. F9 was looked at this lap and renders
clean: no clipped label, no overlap, legend below the axes.

## ⚠⚠ The reviewer BLOCKED this lap, and both of its nails were counts in this routine's own ledgers

**Neither was in the manuscript, and both are the defect the lap exists to fix, committed by
the lap in the act of reporting it.**

1. **The lap ran the gate, fixed what it found, and then wrote the entire lap-22 ledger
   without re-running it.** `unqualified_post_hoc_claims` returned **five** hits, one real
   (`paper/GAPS.md`'s G8 row); the lap fixed it and wrote 「one real, four false」 into three
   files. The reviewer re-ran it: **six**. The new hit was the lap's *own* new
   mandatory-correction section, stating the shape of the top bare, with the qualifier sitting
   one heading away in the next unit. **That is `WC-004`'s shape exactly** — correct one unit,
   leave the same claim standing one unit later in the same green file — and
   `test_post_hoc_maximum.py` ships
   `test_a_qualifier_in_another_section_does_not_license_the_claim` against precisely it.
2. **「seven matches」 was `paper/GAPS.md`'s count alone presented as a two-file total** (13 at
   that moment).

A third reviewer point was taken and **changed the manuscript**: the draft's 「The opponent is
**therefore handed** the best width the sweep found, by scanning outcomes after the run」
overstated the mechanism. `scripts/run_present_perimeter_arm.py` sets
`DEFAULT_BUFFER_M = 1000.0` and sweeps **around** that default, so the committed width is a
value a person chose — `README.md`'s Korean bullet says so — that the sweep then ranked first
among five; no code scans outcomes and hands the opponent a width. And 「therefore」 was a
non-sequitur. What is true is about **reporting**, and the monotonicity goes through whoever
picks the width: §4.5 now reads 「The opponent's score is **reported at** the best width
measured, a width picked out after the run by scanning outcomes, so it is a maximum over that
grid …」. The property is unchanged; the overclaim is gone.

## ⚠ After the repair the gate still cannot be satisfied honestly in `paper/`, and that is the finding

Re-measured after every edit that followed: `unqualified_post_hoc_claims` returns **six** units
across `paper/` — five in `paper/GAPS.md`, one in `paper/README.md` — and **zero** of them are
real. Every one is the trigger `margin\s+(?:of|is|was)\s+\d` matching the **word-budget**
margin: 「a margin of 7」, 「a margin of 6」, 「the margin is 6」. Raw trigger matches over the two
files: **11** (8 and 3), three of them the ledger's own quotations of the offending phrases.
**Not one of the eleven is about the fair opponent.** In `paper/` the dominant sense of
「margin」 is NH-037's, not NH-032's — and **one of the six units is the paragraph describing
the false positives.**

**They were not silenced.** Writing a post-hoc-maximum qualifier into a paragraph about a word
counter would be a sentence using the right words wrongly — precisely the leakage that
module's own docstring says it cannot detect (「it grades a form, not a meaning」) — and a green
gate over it would become evidence for the thing it was built to catch. **The fix is a
narrowed trigger in `tests/`**, which CHARTER §12 keeps this routine out of; adding the two
`paper/` paths to `SURFACES` today would turn the suite red on six sentences about a word
count. **Until that lands, WFG-202's stated done-when cannot be met by any honest paper lap.**

⚠ Writing that section moved its own numbers three times: reporting the defect reproduces the
trigger, and the first repair *quoted* the offending sentence and created two fresh hits. Those
two are now **described rather than quoted** — the idiom `docs/fair_opponent_line.md` already
uses for its own retired sentences — and this is said out loud rather than done quietly,
because rewording to clear a gate sits a hair's breadth from the caption bypass lap 19 banned.
The difference is the one CHARTER §3.5c's own per-line licence draws: a sentence that records a
claim in order to say it is wrong is not the claim, and this gate has no way to be told.

## ⛔ One clause the reviewer asked for, declined on a measured cost

The Abstract's 「recovers most」 is argmax-conditioned and, unlike §5, §6 and §7, does **not**
cross-reference §4.5 for the residual. The clause 「… credits to the forecast, **at the best of
the buffer widths tried**」 was **inserted and measured, not estimated**: `body_words`
**8,998 → 9,006** — **8 words against a margin of 2**, six over the proxy's hard fail. It was
reverted and the document rebuilt at 8,998; nothing was compressed to fund it.

⚠ **The honest other half, and it is why this is a want rather than a defect.** Omitting it
makes the Abstract **under**-claim: refining the grid strengthens the opponent, so 「recovers
most」 becomes *more* true and the missing clause hides nothing in this project's favour. That
is the opposite direction from lap 20's blocked Abstract edit, which inverted a bound on the
headline number, and milder than lap 21's declined *limitation*. Recorded verbatim and costed
in `GAPS.md` so the next lap with room, or NH-037's answer, lands it in one edit.

## Checked and deliberately not changed

- **§3.5's 「September 2026 retractions skipped it three times」 stays three**, re-derived from
  the commits and not from a ledger summary. `WC-011` was registered in `9170a37`, the same
  commit that withdrew the claim and rewrote the surfaces — a compliance success under CHARTER
  §3.5c, not a fourth skip. ⚠ The near miss is real and is a different animal: at `9170a37`
  four `WC-011` spellings sat **unlicensed** in `paper/GAPS.md` (lines 35, 47, 48 and 74 of
  that revision; line 15 was licensed), in a file that commit had itself edited, and `d82b18b`
  licensed them 40 minutes later. Every one of those four **records** the claim in order to say
  it was withdrawn, and a missing per-line licence is not a surviving assertion, which is what
  §3.5's sentence is about.
- **§3.5's three instrument sentences were re-run, not reasoned about.**
  `check_withdrawn_claims.py` prints 「PASSED === **11** claims over **936** gated files」 and
  `check_number_collisions.py` 「no registered quantity appears elsewhere with a different,
  unmarked value (22 quantities carry marked historical values)」 — both re-run on this tree
  after every edit above, both exit 0. And 「a withdrawn
  claim cannot survive in a prose file nobody thought to list」 gained a supporting instance:
  registering `WC-011` is what found `paper/GAPS.md` G8, a prose file no surface list named.
- **No `[GAP]` opened or closed.** G8 stays open — NH-032 and NH-034 are unanswered and one day
  overdue — and this lap makes it **harder**: whichever build the author picks, the margin it
  yields is a ceiling on the grid that happened to be searched, and exactly one refinement of
  that grid exists.
- **Nothing outside `paper/`**, `docs/auto/reports/`, `images/`, `dashboard.html` and
  `STATE.json` was touched. No NEEDS_HUMAN entry was needed: NH-037 is already open and this
  lap adds no new decision.

## Gates

`paper/check_paper.py` exit **0** — `body_words 8998, figures 8, tables 4, references 29,
gaps 7`; `pages {"pages": 23, "calibri_face": "Carlito", "metrics_ok": true}`; anchor
`6b0702d747ed5beb` **printed by the run that measured it**. The page count was **re-derived,
not inherited**: `body_words` moved, which moves `built_pages_inputs`, so lap 21's anchor
turned the gate red exactly as designed until a run had produced a new count. This lap ran the
one `apt` line `paper/README.md` has printed since lap 9. ⚠ It does **not** close **WFG-116** —
the fix that makes a *clean clone* measure is the same line in
`.github/workflows/auto-gates.yml`, outside `paper/`, still open.

`scripts/auto/gates.py --mode full` exit **0**, ALL GREEN, `1806 passed, 63 skipped,
2 xfailed`. `baseline-verify` WARNs on the two `data/raw/**` contract files that are
git-ignored and cannot exist in any sandbox — NH-029 and CHARTER §3d working as decided.

Reviewed by: subagent (**block**): 「Claim 7 is false on the tree it ships, and the way it is
false is the exact defect the lap says it is defending against」 — the lap reported its gate
residue as a count it had not re-measured, and its own new ledger prose contained the sixth
hit. Both nails verified in the tree and repaired before the push; the reviewer's third point
rewrote §4.5's mechanism clause. Everything the reviewer checked in the **manuscript**
verified true and independently: both removed sentences false at lap start, no dense-grid
count quoted, the post-hoc property numerically accurate (best score 345 → 349, advantage
9 → 5, forecast-aware arm unmoved at 354 on both artifacts), §3.5's 「three」 correct, 18 keys
added and none changed, net +5 with nothing compressed, all 29 citations resolving both ways,
and every `Fig. N` matching appearance order in the built `.docx`.

**Both margins, measured on one document by one run: 23 pages against the author's 25 is two
pages; 8,998 words against the proxy is two words** — the tightest yet, and funded by a
sentence going false rather than by compression, which is not a repeatable source. **NH-037
remains open**, and laps 13 through 21 all had their writing shaped by the proxy, lap 21's
casualty being a limitation.

## In plain terms

The paper used to say "our test grid is too coarse to tell what shape the answer is" — yesterday's dev lap filled the grid in, so that sentence was wrong and is now replaced by what the grid actually showed.

What is still missing is your decision on NH-032 and NH-034 (a day overdue): until you pick which build of the "fair opponent" counts, the paper cannot print the one number a reviewer will ask for — and it now has to add that whatever that number is, it is a ceiling, because we hand the opponent its best setting after seeing the results.

Two things for you: answer NH-032/NH-034, and answer NH-037 — the word budget is down to two words of slack, and the only reason nothing was cut this time is that a false sentence happened to free the space.



## Gates

**ALL GREEN** · mode `full` · head `a9e0430` · 2026-09-09T03:28:37Z · current at `a9e0430`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 16.5 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.1 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 0.3 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 263.3 s | 1806 passed, 63 skipped, 2 xfailed, 52 warnings in 262.33s (0:04:22) |

## Commits since the previous report (86f8929bf134052adfa12bf662388247994bf9c3..HEAD)

- a9e0430 the critic report names the head it ships in, and the second run that certified the pushed tree
- 55dedbd critic #47: the measurement three laps called cheap covers a fifth of the map, and three P0 rows were frozen on a question already answered

## Needs a human (21 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09)
- NH-036 [DECISION] One critic lap told the next one not to edit a file, and that is what kept a false sentence in front of a judge for a window (by 2026-09-10)
- NH-037 [DECISION] The paper's word proxy now stops it a thousand words before your 25-page rule (by 2026-09-10)
- NH-038 [DECISION] Your "product first" rule has spent the last three dev laps on documents, and the readiness line it was written to protect has not moved in five critic laps (by 2026-09-09)
- NH-039 [DECISION] The national wildfire-spread system's manual is an 18 MB PDF the sandbox could not fetch, and one of you can (by 2026-09-12)
- NH-040 [FYI] A critic lap pushed one commit past a red `--assert-reported`, and it is telling you rather than hiding it
- NH-041 [FYI] This lap sent you an email containing only the word PLACEHOLDER, and could not take it back
- NH-042 [DECISION] Two of your own rules collide whenever a withdrawn claim lives in a frozen artifact, and this week they collided three times (by 2026-09-10)
- NH-043 [DECISION] A gate your loop built this morning will go red about twice a day, and the charter tells the lap that meets it to stop working (by 2026-09-09)
- NH-045 [BLOCKER] The staleness gate has closed `auto/dev` to every routine, and the one routine that met it is the one forbidden to clear it (by 2026-09-08)
- NH-044 [DECISION] The claim the paper just retracted is still live on the page the paper cites for it (by 2026-09-09)
- NH-046 [DECISION] Your product's definition-of-done names a command nothing in this project has ever run (by 2026-09-10)
- NH-048 [DECISION] One of the research routine's three literature channels has been dead for two runs, and a working replacement is already proven (by 2026-09-10)
- NH-049 [DECISION] Your critic routine is told to add judge Q&A cards and your own printing gate makes that impossible for it (by 2026-09-11)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
