# WildfireGuardian autoloop · paper · 2026-09-08T1448Z

| | |
|---|---|
| when | 2026-09-08 15:33 UTC · 2026-09-09 00:33 KST |
| branch / head | `auto/dev` / `dee1bc1` |
| kind | manual |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 8, done: 28, parked: 1, title-derivable: 1, todo: 95 |

## What happened this lap

# Paper lap 20 — a real defect found, the fix written, blocked by the reviewer, and reverted

**The manuscript did not move.** `paper/manuscript.md` is byte-identical to the one lap 18
pushed: `body_words` **8,994**, margin against the 9,000-word proxy **6**, figures 8, tables 4,
references 29, gaps **7**. This lap wrote a change, its independent reviewer **blocked** it, and
the change was **reverted rather than argued or shipped** (CHARTER §4 step 5). What ships is the
finding, the re-measurement, and the datum NH-037 has been waiting for.

**Incorporated** `d36ad21..dee1bc1` — seven files outside `paper/` and `docs/auto/`: `README.md`
(new Round-4 section), the new `docs/creativity_card.md`, `web/finals.html`'s embedded stamp
line, `release/kcf-finals-2026/MANIFEST.json`, and three test files. **`docs/NUMBERS.json` is not
in the diff and no artifact under `data/processed/` is either**, so no result section could gain
a number, a table or a figure, and none did. All nine figures re-rendered byte-identically
(`make_figures.py` printed `drawn [F1 … F9]; skipped []`; `git status paper/figures/` empty
after). `F9_present_perimeter` still drawn, committed and unreferenced (G8, NH-032 open).

## The finding, which stands

`docs/auto/DIRECTION.md:59` requires **every judge-facing surface stating 42 or 91 to carry both
binding caveats** — fire-blind opponent, *and* upper bound for a noiseless forecast. CHARTER §14b
names the manuscript as such a surface. The manuscript carries the fire-blind caveat in the
Abstract, §4.3, §4.4, §6 and §7 and carries the upper-bound caveat **only in §4.5**, scoped there
to the present-perimeter comparison rather than to the 42.

⚠ The repository already specified this defect more precisely than the lap did, and the lap did
not run it: `tests/test_future_aware_attribution.py` enumerates exactly **two** claim blocks in
the manuscript — the Abstract and §7 — and is `xfail(strict=True)` because neither names the
oracle bound.

## The fix, and the four reasons the reviewer blocked it

The lap wrote the caveat into the Abstract and §4.3's third caveat and paid for it by replacing
§4.5's 43-word statement with a 9-word back-reference. **Every defect below was verified in the
tree before the revert, and none is reachable by any gate in this repository.**

1. ⚠⚠ **A load-bearing line number was wrong and went into three files unverified.** The lap
   claimed `README.md:626` cites `paper/manuscript.md` §4.5 for the caveat. `:626` carries **no
   citation at all**; the citing line is **`README.md:38`**, the TL;DR bullet — CHARTER §14b's
   first-named judge-facing surface — which the lap never identified. `sed -n '38p;626p'
   README.md` settles it in a second. DIRECTION's subject-grep rule exists for this and was not
   run.
2. ⚠⚠ **The fix deleted the text `README.md:38` cites.** §4.5 matched `noiseless` at `:512`
   before the diff and nothing after. That would have left a judge-facing surface citing a
   section silent on the claim it quotes — **strictly worse than the mismatch being fixed** — and
   CHARTER §12 bars this routine from repairing `README.md`.
3. ⚠⚠ **The Abstract clause inverted the bound.** It read 「bounds a noiseless forecast rather
   than this model」, where every other surface has 42 as an upper bound **on this model**,
   *equal* to what a noiseless forecast buys. A reader can take the lap's wording as 「this model
   may do better」 — the inverse of the safety-relevant meaning, on the headline number, in the
   most-read block, inside the diff whose purpose was to make two surfaces agree.
4. ⚠ **The §4.5 back-reference was false of §4.5**, whose opponent is expressly not fire-blind;
   and the merged lead's second clause had **no premise anywhere in §4.3**, that premise being
   the sentence deleted from §4.5.

⚠ The reviewer also found one inside this routine's own file: `paper/GAPS.md` row **G8** asserts
「§4.5 states both …」, which the diff would have falsified — a live gap row, in the file the lap
was editing in the same diff, left standing. True again on the reverted text.

## Why it was reverted rather than repaired

The correct version needs the mechanism stated once with its premise (~48 words as a fourth §4.3
caveat), §4.5's original 43 words **kept** so the README's citation survives, a corrected Abstract
clause (~+23), and §7. That is **~+50 words against a margin of 6**. Every cheaper shape was
costed and each inverts the bound, strands the premise, or breaks the citation.

**Nothing was compressed and no caveat was trimmed to close the gap** — CHARTER §12 forbids
buying space that way, and the only compressible prose left is the litigated stock that produced
wrong sentences at laps 13 and 15.

## What the lap did land: the page count, re-derived

It ran the `apt` line `paper/README.md` has printed since lap 9, so `check_paper.py` took its
**measuring** branch for the first time since lap 18:

    [check_paper] pages {"pages": 23, "calibri_face": "Carlito", "metrics_ok": true}
    [check_paper] built_pages_inputs f2ee9be6c9c7c4e0  (measured this run)

The anchor was **re-derived and found matching**, not accepted. ⚠ It does **not** close WFG-116 —
that is the same line in `.github/workflows/auto-gates.yml`, outside `paper/`, still open.

## Word count and page estimate

`body_words` **8,994** (unchanged); margin **6**. Built pages **23**, measured under Carlito with
`metrics_ok` true. Figures 8, tables 4, references 29, gaps 7 (none opened, none closed).

## Gaps

**7, unchanged.** Nothing in the diff supplies evidence for any: G3, G4, G5 need the laptop
bundle or the author; G6, G7 need a dev lap's new artifact; G8 needs NH-032; G2 needs the
practitioner consultations.

## Gates

`scripts/auto/gates.py --mode full` — ALL GREEN, exit 0 (`verify`, `snapshot-verify`, `env-check`
pass; `pytest-full` 1,749 passed / 63 skipped / 2 xfailed; `baseline-verify` WARN exit 2 as on
every cloud lap, the sandbox having no raw bundle to re-freeze). `check_number_collisions` OK.
`check_withdrawn_claims` PASSED, 10 claims over 934 gated files (933 last lap;
`docs/creativity_card.md` is the new one). `check_paper.py` exit 0.

Reviewed by: subagent (block): the lap acted on a citation it never opened — it named
`README.md:626` as the surface citing `paper/manuscript.md` §4.5 when the citing line is
`README.md:38` — and its fix then deleted the cited text, leaving a judge-facing surface citing a
section silent on the claim it quotes, which is worse than the defect it set out to fix. Block
accepted in full and the manuscript reverted; no finding was argued.

## What this lap deliberately did not do

Ship the blocked text; argue with the reviewer; compress anything; use the caption bypass lap 19
measured and banned; edit `README.md`, `tests/` or `docs/NUMBERS.json` (all outside CHARTER §12);
or write any margin from the fair-opponent arm, which NH-032 still bars from every judge-facing
surface.

## In plain terms

- The paper is unchanged. I found a real hole — where it states its headline **42**, it does not
  say the number is a **ceiling** on what your model buys, though your own README does — and then
  my fix for it was wrong in a way my reviewer caught, so I threw it away rather than ship it.
- Still missing: that caveat, in the Abstract and the Conclusion. It needs about 50 words and the
  paper has 6 left. `tests/test_future_aware_attribution.py` already fails-on-purpose over
  exactly this, so the fix also needs a small change in `tests/`, which this routine may not touch.
- **What to do: answer NH-037, due 09-10.** The paper is **23 pages against your 25-page rule**
  but **8,994 words against a 9,000-word stand-in**. Today a correction your own project rules
  require did not fit. That is the situation the entry asked you to decide about, and it has now
  actually happened.



## Gates

**ALL GREEN** · mode `full` · head `dee1bc1` · 2026-09-08T15:33:43Z · current at `dee1bc1`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 20.9 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.1 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 0.4 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 326.2 s | 1749 passed, 63 skipped, 2 xfailed, 52 warnings in 325.17s (0:05:25) |

## Commits since the previous report (7a966b2c75e470fef1228913517fc5394b0bf898..HEAD)

- dee1bc1 the report the assert asked for: NH-041's third instance and the row that replaces the habit with a test

## Needs a human (19 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09)
- NH-036 [DECISION] One critic lap told the next one not to edit a file, and that is what kept a false sentence in front of a judge for a window (by 2026-09-10)
- NH-037 [DECISION] The paper's word proxy now stops it a thousand words before your 25-page rule (by 2026-09-10)
- NH-038 [DECISION] Your "product first" rule has spent the last three dev laps on documents, and the readiness line it was written to protect has not moved in five critic laps (by 2026-09-09)
- NH-039 [DECISION] The national wildfire-spread system's manual is an 18 MB PDF the sandbox could not fetch, and one of you can (by 2026-09-12)
- NH-040 [FYI] A critic lap pushed one commit past a red `--assert-reported`, and it is telling you rather than hiding it
- NH-041 [FYI] This lap sent you an email containing only the word PLACEHOLDER, and could not take it back
- NH-042 [DECISION] Two of your own rules collide whenever a withdrawn claim lives in a frozen artifact, and this week they collided three times (by 2026-09-10)
- NH-043 [DECISION] A gate your loop built this morning will go red about twice a day, and the charter tells the lap that meets it to stop working (by 2026-09-09)
- NH-045 [BLOCKER] The staleness gate has closed `auto/dev` to every routine, and the one routine that met it is the one forbidden to clear it (by 2026-09-08)
- NH-044 [DECISION] The claim the paper just retracted is still live on the page the paper cites for it (by 2026-09-09)
- NH-046 [DECISION] Your product's definition-of-done names a command nothing in this project has ever run (by 2026-09-10)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
