# WildfireGuardian autoloop · paper · 2026-09-10T0942Z

| | |
|---|---|
| when | 2026-09-10 09:42 UTC · 2026-09-10 18:42 KST |
| branch / head | `auto/dev` / `1983ff1` |
| kind | manual |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 9, done: 33, dropped: 1, parked: 1, title-derivable: 1, todo: 108 |

## What happened this lap

# WildfireGuardian autoloop · paper · lap 27

**Range incorporated:** `55466eb..1983ff1`, **17** commits (measured, `git rev-list --count`).
Two dev laps in it: `c4eb8d2` (WFG-218 / WFG-220) and `5bcfe11` (WFG-128 + critic #56's
preemption). **Gates:** `gates.py --mode full` **ALL GREEN** on the commit pushed
(`verify`, `snapshot-verify`, `env-check`, `pytest-full` 1,985 passed / 64 skipped / 3 xfailed;
`baseline-verify` WARN — the standing sandbox WARN for the two git-ignored
`data/raw/firms_data/*.json` contracts that never reach a fresh clone, CHARTER §4).

**Reviewed by: subagent (block)** — 「The one sentence lap 27 wrote contains a flatly false
factual claim, and the falsity was manufactured by the edit itself.」 It was right. Fixed before
push; the record is below and in `paper/GAPS.md`.

## Sections changed

**§4.4, one sentence — and the lap ends with a different sentence than it set out to write.**
The paragraph qualifying Table 2's over-budget column ended:

> Of the other two regions, Uljin-Samcheok has not been re-read under the uniform rule and
> Yeongdeok cannot be, its walk graph of that vintage no longer being recoverable.

`docs/multi_region.md` §3.1, new in `5bcfe11`, had stopped resting Yeongdeok's `0` on the data
loss and argued it from the bucket's own membership test instead. WFG-128's row records the
demotion in as many words: 「The data loss is still true and is now the second reason, not the
first.」 So the paper was still giving the second reason as the reason.

**The mechanism is correct and was derived from the code, not from the page.** `classify()` in
`scripts/run_real_roads_real_hazard_slope.py:118-144` passes `time_budget_min=budget` to
`future_aware_route` and **no budget at all** to `naive_route`; membership of `fa_exceeds_budget`
is the sixth branch, `nv.reached and not nv.enters_hazard and not fa.reached`. Budgeting the
fire-blind arm can only flip `nv.reached` true → false, landing the origin in `naive_unreachable`
at the **first** branch. Members can leave; none can enter. The reviewer re-derived this
independently and confirmed it.

## ⚠⚠ The clause the lap wrote around that mechanism was false, and the lap manufactured the falsehood

The first draft read 「… and that walk graph is lost anyway.」 **The three dropped words —
「of that vintage」 — were the entire scope of the claim.** Re-derived in the tree, not read off
the reviewer:

- `docs/DATA_LOSS_2026-07-24.md` records that what was overwritten is the **2026-07-23,
  8,439-node** graph behind the committed 459-origin run `data/processed/real_roads_real_hazard.json`.
- Table 2's Yeongdeok row is **not that run**. `mr_yeongdeok_fa_exceeds_budget` names
  `source_file` `data/processed/real_roads_real_hazard_canonical.json`, whose `network_source` is
  `osm-walk_yeongdeok-2025_20260724_2bff8d85.graphml.gz` — **present in the tree**, uncompressed
  sha256 `2bff8d851296dedf86d7543684a837897c3e6d188a3192fe16398e857305012f`, byte-identical to
  `data/snapshots/MANIFEST.json` — and whose registry entry carries `reproducibility.status`
  **「reproducible」**, evidence 「built 2026-08-02 from the hash-verified 2026-07-24 snapshot walk
  graph」.
- DATA_LOSS's own evidence table lists **8,443** nodes in the **surviving** column — the number
  this manuscript prints at §6.

So the draft asserted in §4.4 that a graph is lost, counted its nodes in §6, and recomputed routes
from it in Fig. 8's caption. ⚠ **And the superseded sentence was not innocent either:**
「Yeongdeok cannot be re-read」 attached an irrecoverable vintage to a count measured on a graph
that survives. The old sentence was wrong in the **same direction**, the lap had the evidence open
in the file it was editing, and its first draft re-asserted the error more baldly instead of
fixing it. That is this lap's finding and it is about this routine.

## After the repair

> Yeongdeok's 0 cannot rise, since budgeting the fire-blind arm only takes members out of this
> bucket. Uljin-Samcheok's 3 has not been re-read, and nothing is inferred from it.

**Measured with the builder's own counter: 8,997 → 8,998 → 8,997. Net ZERO words against the
pre-lap document; margin stays 3.** ⚠ **The data-loss clause is gone rather than rescoped, and
that is not a caveat trade:** it is not true of the graph this `0` was measured on, so it was an
**error**, and deleting an error is the only source of headroom CHARTER §12 permits. The counts
`3` and `0` are named where the old sentence named neither, and both are registry values
(`mr_yeongdeok_fa_exceeds_budget` = 0, `mr_uljin_fa_exceeds_budget` = 3, matching Table 2).

**The reviewer's second objection was taken rather than argued.** The draft welded the two regions
with a semicolon and stated the mechanism as a general property of the bucket, so a reader
conjoins them in one step and gets 「at most 3」 — the wording **NH-053** reserves to the author.
`docs/multi_region.md` §3.1 keeps them in separate bullets and adds that it 「does not name 3 as a
bound of any kind」; the draft imported the mechanism and left the disclaimer behind. The repair
splits the sentence, attaches the mechanism to Yeongdeok's `0`, and adds 「and nothing is inferred
from it」 to the Uljin clause.

## Figures

**None added, none redrawn.** `make_figures.py` drew F1..F9 and `git status paper/figures/` is
empty — all nine byte-identical, so no look-at-it pass is owed. `data/processed/**` and
`docs/figures/` are byte-unchanged across the range, so no figure input moved. F9 stays drawn and
unreferenced by the standing NH-032 decision (GAPS G8).

## Gaps and registry

**GAPS 7 → 7**, none opened, none closed — the window added no artifact. Five registry keys added
additively (448 → **453**); **no registered value moved anywhere**, checked field by field over
the whole registry, not just the key set. ⚠ **25 caveat bodies did move**, all `og_yeongdeok_*`,
carrying WC-014's time-gap warning; the manuscript quotes none of those keys. `references.bib`
unchanged at 29 entries against 29 cited keys — **no citation added, so no URL needed opening.**

## The result the paper still does not state — declined for the seventh lap

`docs/oracle_gap.md` is now registered at **thirty** `og_yeongdeok_*` keys (25 before this window
plus five in it; an earlier draft of this report wrote 25 as the post-lap total and the reviewer
caught it). It measures what §6 calls 「the objection we would raise first」: at the one
well-matched pair the predicted core holds 952 cells against 937 observed, sharing 534. Lap 24
measured the shortest honest prose form at **+41 words**; this lap's margin is **3**.

⚠ **A second route to it was measured and also declined, and the refusal is written where the next
lap will find it before the temptation.** `build_docx.py` counts `body_words` over paragraphs and
list items only (`:177`, `:187`) — figure captions, table captions and table cells are counted
nowhere. So a figure carrying this result in its caption costs **zero** body words and one page
(23 → 24, inside the author's measured 25) and would pass `check_paper.py` in both branches. It
was not drawn: routing the exact sentences the proxy refuses through the one channel the proxy
cannot see is a lap raising its own ceiling, which **NH-037** says a lap must not do, and it would
put a six-fact mandatory caveat band into a caption. The reviewer verified the hatch is real and
was correctly declined.

## Checked and found still true — re-derived, and confirmed independently

1. §3.5's 「September 2026 retractions skipped it four times」 **stays four**: `git log -S'WC-014'`
   on `docs/auto/withdrawn_claims.json` and on `docs/oracle_gap.md` both return exactly `5bcfe11`,
   so correction and registration are one commit and one lap (CHARTER §5c met).
2. **No withdrawn spelling fires on the manuscript** — `check_withdrawn_claims.py` prints
   「PASSED === 14 claims over 938 gated files」.
3. §6's 「Every control here perturbs the predicted field, so none admits external truth」 is
   **still true**: all five controls of §3.5 perturb the predicted field; a field-to-field
   comparison is not one of them.

⚠ **The withdrawal ratchet fired on this routine, in the lap that was checking it.** The first
draft of finding (1) pinned its second command on the retired wording itself, and the gate failed
with one unlicensed mention at `paper/GAPS.md:74` — in the bullet whose point is that the
registration worked. Re-pinned on `WC-014`; **no pragma was added**, so WC-014's record of adding
none anywhere stays true.

## Two repository defects the manuscript is now on the safe side of (for a dev lap)

**(a) `docs/multi_region.md` §3.1 makes the same conflation this lap was blocked for**, on a page
`README.md:146` sends a judge to. Its second reason reads 「the OSM walk network behind the
459-series was overwritten on 2026-07-24」 — but the Yeongdeok `0` in that page's own table comes
from the canonical run on the **surviving** Jul-24 snapshot, which the registry marks
`reproducible`. **The re-read that page calls impossible is runnable on committed inputs in the
sandbox**: add the same post-hoc arrival-time rejection `docs/present_perimeter_arm.md` §2 used
for Uiseong-Andong and read `fa_exceeds_budget`. Minutes.

**(b) `docs/multi_region.md` §3.1 prints 624.8 and 628.2**, both registered `pp_uiseong_*` values
whose shared caveat band opens 「Four facts travel together or none of them may be quoted」. Fact
(5) travels with them there; facts (1)–(4) do not. ⚠ This is the **NH-032 bind** rather than
carelessness — fact (1) *is* the margin, and NH-032 bars the margin from every judge-facing
surface. The manuscript quotes neither number and says why (§4.4).

## Build and the anchor

`check_paper.py`: `{"body_words": 8997, "figures": 8, "tables": 4, "references": 29, "gaps": 7}`,
**OK**. **Anchor re-derived twice, not inherited:** the blocked draft measured `6b0702d747ed5beb`
at 8,998 words; the repair moved `body_words` again, so it was re-measured on the **final**
document. `check_paper.py` took its **measuring** branch — **pages 23**, `calibri_face` Carlito,
`metrics_ok` true — and printed **d866b7b725675ebf**, which is lap 26's string because the
document returned to lap 26's four digest inputs.

**Two pages against the author's 25; three words against the proxy's 9,000**, measured on one
document by one run. ⚠ 8,997 is **497 words over CHARTER §12's target of 8,500**: 「margin 3」 is
headroom against the hard fail, not against the target. The `.docx` is committed because the
manuscript actually moved. **Clone shallow:** `--is-shallow-repository` true, `rev-list --count
HEAD` = 51, both measured this lap; **no ancestry or reachability claim is made anywhere in this
lap's output.**

⚠ **One review step did not happen and is not glossed:** the reviewer reports that
`.claude/skills/hate/SKILL.md` carries `disable-model-invocation: true` and the Skill tool refused
it, with an instruction not to replicate its workflow by other means. It ran `factchk` — the
source of the root objection — and did not replicate `hate`. `LOOP_CONFIG.json` names `hate` as
part of the `subagent` review, so this is a gap in the review machinery for a dev lap.

## In plain terms

- The paper was about to say a map file is lost that is actually sitting in the repository, hash-checked, and used elsewhere in the same paper. My reviewer caught it, I verified it myself, and the sentence is now shorter, true, and costs nothing.
- The same mistake is still live on `docs/multi_region.md`, which is a page the README sends judges to — and it means a check that page calls impossible is actually about ten minutes of work for a dev lap.
- Please answer **NH-037** (the paper's length rule — it is the only thing keeping the paper's strongest evidence against itself out, seven laps running) and **NH-032 / NH-034**, now four days overdue.



## Gates

**ALL GREEN** · mode `full` · head `1983ff1` · 2026-09-10T09:41:12Z · current at `1983ff1`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 18.4 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.1 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 0.4 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 308.9 s | 1985 passed, 64 skipped, 3 xfailed, 52 warnings in 307.63s (0:05:07) |

## Commits since the previous report (16e6824d026bbce9c88fb3bc812fe61e18548da1..HEAD)

- 1983ff1 the critic report names the head it ships in, and keeps the head it reviewed
- 90d3ea7 critic #57: the page a judge is sent to for the oracle names a script that never wrote the array

## Needs a human (25 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09, one day past; raised to HIGH by critic #55 on a measured third instance)
- NH-036 [DECISION] One critic lap told the next one not to edit a file, and that is what kept a false sentence in front of a judge for a window (by 2026-09-10)
- NH-037 [DECISION] The paper's word proxy now stops it a thousand words before your 25-page rule (by 2026-09-10)
- NH-038 [DECISION] Your "product first" rule has spent the last three dev laps on documents, and the readiness line it was written to protect has not moved in five critic laps (by 2026-09-09)
- NH-039 [DECISION] The national wildfire-spread system's manual is an 18 MB PDF the sandbox could not fetch, and one of you can (by 2026-09-12)
- NH-040 [FYI] A critic lap pushed one commit past a red `--assert-reported`, and it is telling you rather than hiding it
- NH-041 [FYI] This lap sent you an email containing only the word PLACEHOLDER, and could not take it back
- NH-042 [DECISION] Two of your own rules collide whenever a withdrawn claim lives in a frozen artifact, and this week they collided three times (by 2026-09-10)
- NH-043 [DECISION] A gate your loop built this morning will go red about twice a day, and the charter tells the lap that meets it to stop working (by 2026-09-09)
- NH-045 [BLOCKER] The staleness gate has closed `auto/dev` to every routine, and the one routine that met it is the one forbidden to clear it (by 2026-09-08)
- NH-044 [DECISION] The claim the paper just retracted is still live on the page the paper cites for it (by 2026-09-09)
- NH-046 [DECISION] Your product's definition-of-done names a command nothing in this project has ever run (by 2026-09-10)
- NH-048 [DECISION] One of the research routine's three literature channels has been dead for two runs, and a working replacement is already proven (by 2026-09-10)
- NH-049 [DECISION] Your critic routine is told to add judge Q&A cards and your own printing gate makes that impossible for it (by 2026-09-11)
- NH-050 [DECISION] You answered two of these questions two days ago and the loop never heard you, because you answered them on the routine page (by 2026-09-10)
- NH-051 [DECISION] The rule your loop has been obeying for three days is not the option it names, and the difference is why one row has been pushed down five times (by 2026-09-11)
- NH-052 [DECISION] The experiment that measures what your own forecast is worth is now one run away, and it would change what 42 means (by 2026-09-12)
- NH-053 [DECISION] The word your front door uses for 42 stopped being right today, and the loop cannot pick its replacement (by 2026-09-12)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
