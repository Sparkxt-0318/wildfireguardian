# WildfireGuardian autoloop · dev · 2026-09-09T0452Z

| | |
|---|---|
| when | 2026-09-09 04:52 UTC · 2026-09-09 13:52 KST |
| branch / head | `auto/dev` / `9a97e53` |
| kind | dev |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 7, done: 32, dropped: 1, parked: 1, title-derivable: 1, todo: 104 |

## What happened this lap

Row: **WFG-194 (P0, KCF)** — 창의성 reaches the two surfaces a judge stands in front of.
Claim commit `9a97e53` (pushed). The work is one commit on top of it; the finals screen is
stamped at the claim commit, because `tests/test_finals_screen.py::test_the_escape_this_gate_cannot_close_is_still_open`
requires the stamp to resolve for a stranger who clones, not only on this machine.

## Your decisions, applied

**Nothing new this lap.** The Gmail search (`from:siyeong0318@gmail.com subject:"WildfireGuardian autoloop" newer_than:14d`, plus a second pass on `subject:Re`) returns only the loop's own sent reports — every thread in the window has exactly one message, and none of the nine `Re:` threads in the mailbox is an autoloop thread. PR #31 has no comments at all. `docs/auto/decisions_seen.json` is unchanged. **Fifteen decisions are open and five are now overdue** (NH-032 and NH-034 by a day, NH-035, NH-038 and NH-043 as of today); one reply with five `NH-###: <letter>` lines clears them.

## What this lap did

### The defect

창의성 is **20 points on both KCF scoring tables** and the 심사기준 names it **first**. The answer has existed since WFG-182 as `docs/auto/JUDGE_QA.md` Q29a plus `docs/creativity_card.md`. Measured at the claim commit `9a97e53`, unpiped, a count of 창의 or 독창 answered:

| surface | at `9a97e53` | after this lap |
|---|---:|---:|
| `web/finals.html` | 0 | 3 |
| `docs/auto/DEMO_SCRIPT_5MIN.md` | 0 | 8 |
| `docs/auto/JUDGE_QA.md` | 2 | 2 |

and `docs/creativity_card.md` was in none of the printed kit's six `SOURCES`. Critics #41, #45 and #47 each re-measured the same zero on four consecutive days. **The material was written and a judge could not reach it.**

### What shipped

- **`web/finals.html`** — a 「창의성 · 이 작품이 직접 만든 것」 block at the foot of the 시스템 구조 tab: the three items as three cards, each carrying the repository path behind it, plus a closing line saying the verdict is the judge's. Written in the descriptive register the card's whole design rests on (`docs/creativity_card.md`): no rating, no other system, no count, no novelty word. Built with `make finals`, not by hand.
- **`docs/auto/DEMO_SCRIPT_5MIN.md`** — **one spoken sentence** in 도입 naming 창의성 and saying what the output object is, plus two ⚠ blocks (Q&A prose, outside the 300 s by the pace script's own counting rule) that send the judge to the screen block, to Q29a and to the card, and that forbid the evaluative register out loud.
- **The printed kit** — `docs/creativity_card.md` is now the sixth of seven printables `SOURCES`, printing immediately before the related-work panel. Kit rebuilt at `20260909T0321Z` (48 pages) and `release/kcf-finals-2026/MANIFEST.json` re-pointed in the same lap (WFG-152); `make finals-bundle` then rebuilds byte-identically.
- **`docs/creativity_card.md` §6** — method, the surfaces, the cost, and five 「what this does NOT show」 lines, plus a section recording what the reviewer blocked. The old 「the other two surfaces are still silent」 bullet is kept as a quoted retraction rather than deleted (§3.7).

### What the spoken sentence cost, said plainly

300 seconds is fixed by the 운영요강, so 도입 is paid for by the other five segments. Re-measured per `docs/demo_script_pace.md`'s own procedure: new artifact `data/processed/demo_script_pace/pace_20260909T0321Z.json`, two new registry keys under tag `20260909t0321z` (additive registrar; `build_numbers.py` was not run), `TAG` moved in `tests/test_demo_script_pace.py`.

| 구간 | before | after | delta |
|---|---:|---:|---:|
| 도입 | 28 | 37 | +9 |
| 1막 | 44 | 42 | -2 |
| 2막 | 50 | 48 | -2 |
| 3막 | 61 | 60 | -1 |
| 4막 | 59 | 57 | -2 |
| 마무리 | 58 | 56 | -2 |

Total spoken syllables **1,744** (was 1,692); rate spread **1.02x**, tighter than the 1.03x it replaced; 마무리 · 한계 is still not the fastest segment, which is the defect WFG-100 was filed for. The fixed point was checked rather than assumed: 마무리's own line says 「마지막 NN초는」, and 58 → 56 costs no syllables (오십팔, 오십육), so re-running the count after rewriting the header returned the same allocation. **Whether nine seconds is worth the 창의성 row is the judge's arithmetic, not this repository's**, and the trade is written into §1 of the script where the student reads it.

### The root objection I recorded before building (`hate`)

**Naming 창의성 on two more surfaces does not make the project more creative; it makes the word appear. The row's own 「done when」 is a grep count, and a grep count is satisfied by typing the word — a keyword, not a mark.**

Cheapest test of it: ask what a judge can *do* after the change that they could not before. The answer is only real if the surfaces carry the **근거 paths**, so the gates were aimed at the anchor paths and the register rather than at the word. `tests/test_creativity_card.py` gains nine assertions, graded against eight mutations of my own and eight more written independently by the reviewer, **sixteen of sixteen red**. Mine:

| mutation | result |
|---|---|
| M1 the spoken 창의성 sentence deleted | RED |
| M2 the screen block retired in the template | RED |
| M3 a novelty claim planted in a screen item | RED |
| M4 an anchor path that does not resolve | RED |
| M5 the closing 「심사위원의 판단입니다」 struck from the built screen | RED |
| M6 the built screen left stale against the template | RED |
| M7 the evaluative-register prohibition deleted from the script | RED |
| **M8 the word 창의성 kept, the claim replaced by 「저희 프로젝트는 매우 독창적입니다」** | **RED, on seven tests** (the lap first claimed two; the reviewer counted) |

M8 is the answer to the objection: the row's criterion passes it and the gate does not.

### What did not work, and what a gate found

- **`scripts/check_region_literals.py` refused the screen block, correctly by its own rule and wrongly in fact: 의성 (Uiseong) is the tail of 창의성.** The gate that stops a per-region value being typed into a shared screen read the card title as a claim about a region. The one-line fix was the ratchet — `KNOWN_REGION_LITERALS["scripts/finals.template.html"] = 1` — and it would have bought silence on *any* future region literal in the one template that renders all three regions from one payload. Instead the predicate was narrowed: a place name counts where it **starts** a word. `tests/test_check_region_literals.py` grades five cases. The reviewer measured what the narrowing actually suppresses across the whole scope — **exactly one finding in the tree, the card title** — and separately objected that the hole it accepts (`경북의성군` escapes) was recorded as a *passing* assertion, which is a ratchet pointing the wrong way: the lap that closes the hole would have to delete a green test to ship the fix. It is an `xfail` now, so closing it produces an XPASS.
- **A typographic apostrophe fixed to a plain one inside a single-quoted JS string killed the whole finals screen, and `make finals` shipped it green.** `check_screen_assets.py` reads assets, `check_forbidden.py` reads prose, and neither parses JavaScript. The only thing that caught it was `tests/test_finals_acts.py::test_the_four_acts_advance_in_a_real_browser`, timing out on 「web/finals.html to finish building the intro」 — a message that names the symptom. The five-second diagnostic is in MEMO for the next lap: split the built page's `<script>` blocks and run `node --check` on the one that is not the JSON payload. **A dead finals screen is the worst artifact this project could take to a booth and one apostrophe is all it takes.**
- **The build order matters and cost one full-gate run.** `make finals` was run before the two pace keys were registered, so the screen's 검증 레지스트리 card counted 401 against a registry of 403 and `tests/test_finals_payload_rederives.py` went red. Correct order: register keys → `make finals` → `make printables` → `make finals-bundle UPDATE=1`.

### What is still not done

- **WFG-206** is on the board: the anchors are bound card-level on both surfaces, so transposing two real paths between items leaves every assertion green. It is the one hole that survived both mutation sets.
- `release/kcf-finals-2026/README_KO.md` still answers **0** on the same count. It is the bundle's own front page and it is not in the row's done-when; noted rather than widened.
- The screen block is **card-level, not item-level**, the same limit the Q&A card has: swapping one item's path for another real path stays green while all four anchors are named somewhere in the block.
- **The evaluative-register mutation is still uncaught by any assertion** — nothing here reads tone. What changed is that the prohibition is now on the page the student reads immediately before speaking, and a test asserts that sentence is still there. That is a control on the human, not on the prose.
- Whether a judge opens the 시스템 구조 tab in a five-minute visit is not measured. R12 (`NH-014`, the booth rehearsal on the real laptop) is the only thing that would find out.

### Gates

`gates.py --mode full` exits **0** on the tree that is pushed: verify PASS, snapshot-verify PASS, env-check PASS, pytest **1821 passed, 63 skipped, 3 xfailed** (446 s). ⚠ Two intermediate runs were red and both are worth naming rather than hiding: one because `make finals` ran before the two pace keys were registered, so the screen's 검증 레지스트리 card counted 401 against 403; one because the reviewer's own new gates caught this lap's fixes. `baseline-verify` WARNs on the two `data/raw/**` contracts that are git-ignored and cannot exist in any sandbox — NH-029/§3d working as chosen. GitHub `auto-gates` run **269** is `success` at `a9e0430`, the head this lap started from, so there was no red run to take as a first job (CHARTER §4b). Critic #47 set **no** `fix-before-next-row` item.

⚠ This clone opened **shallow**, `git rev-parse --is-shallow-repository` answering `true` at **50** commits, so **no ancestry or reachability claim appears anywhere in this report** (CHARTER §4).

### The independent reviewer returned **block**, and it was right four times

`docs/auto/LOOP_CONFIG.json` → `review: subagent`. Its root objection, which is the finding:
**every gate this lap wrote reads a source file, and nothing read what a judge is actually
handed or shown.** Three defects lived in that gap, all in the finals kit, and all are fixed:

1. **A number in prose that did not re-derive (CHARTER §3.3).** The lap wrote 「가장 많이 낸 구간은 2막」 into the booth script and 「2막 most」 into the card doc. The deltas from its own artifact are +9 / −2 / −2 / −1 / −2 / −2: **four segments tie at two seconds and 3막 gave the least.** Both wrong sentences print in the kit. The doc-table gate read the syllable and seconds columns and could not see a claim about their differences. Fixed in both documents; `test_the_deltas_the_pages_claim_are_the_deltas_the_artifacts_show` now asserts the property from the artifacts rather than matching a string, and the table gate also reads the syl/s column. Both graded: restoring the false claim goes red, and a wrong rate in the table goes red.
2. **The print renderer had no strikethrough rule.** The lap struck a claim through and added the same file to the printables `SOURCES` **in the same commit**; `strip_inline()` handled images, links, code, bold and italic and not that one, so on paper the retraction vanished and a judge would have read 「그 두 화면은 창의성에 대해 아직 침묵합니다」 as a live claim — contradicted by the screen in front of them. The renderer now prints a struck span as 「[철회] …」; the retraction in the card doc is written in **words** instead, because this parser works a line at a time and a span that wraps is not matched at all; and `test_no_residual_inline_markup_prints_in_the_real_kit` scans the seven real `SOURCES` instead of a two-line literal the author typed. That new gate then caught **my own fix**, twice — once for the wrapped span and once for prose that named the token.
3. **The screen dropped the limit that makes its third claim honest.** The register gates were ported to the screen; `test_the_card_keeps_the_limit_that_makes_its_third_item_honest` was not, on the one item whose content is 「we are honest about being wrong」. The screen said the checker reads the registry back across 「추적 문서 전체」, full stop. It now names the file types, the exempt record class and the reworded-claim escape, and a screen-side gate binds all three (graded).
4. **The 시스템 구조 tab stacks its own blocks on a language toggle** — one visit gives one `#syscreative`, one toggle two, two toggles three, with duplicate element ids. The reviewer found it by **driving the real page**, which no amount of reading the template would have produced. The class is pre-existing (`#syssrc` and the operator note do the same), but this lap added the largest element yet to that block and told the student to open that exact tab. Fixed by removing the three nodes by id before rebuilding; `scripts/check_finals_acts.py` now opens the tab, toggles twice and counts, and removing the fix turns it red.

It also **corrected the lap in the project's favour twice**: the self-assessment mutation goes red on **seven** tests, not the two claimed, and it re-derived the pace measurement independently (1,744 syllables, spread 1.02, the same six rates) including the fixed-point numerals. And it raised `mandela` #5 against its own method — the lap wrote both the gates and the mutations that grade them, in one session — which is why the two mutation sets came from two sessions. **The hole that survived both sets is now WFG-206** (the anchors are bound card-level, so transposing two real paths stays green).

⚠ **The reviewer left one edit in the working tree it had been told to restore**, adding a trailing `(?![가-힣])` to the region-name regex. Measured before reverting: it blinds the gate to 「영덕에서는」, 「의성군」 and 「울진의」, because Korean particles attach directly to the noun — the same trap `tests/test_creativity_card.py` records for a trailing `\b`. It passed every test in the file, so `test_a_particle_attached_to_the_name_still_fires` now exists and the one-sidedness is a comment on the pattern.

**One more fix this lap owes the review:** the booth script said 창의성 is 「맨 앞에」 on both tables. It is **fourth of five rows** on both; what names it first is the 심사개요's 심사기준 line. A judge holding the 채점표 can see that, so the script now says which is which and tells the student not to call it the first item.

Reviewed by: subagent (block, then fixed): every gate this lap wrote reads a source file and nothing read what a judge is handed or shown - three defects lived in that gap, all in the finals kit

### Next row

**WFG-125 (P0, science)** — the oracle gap. DIRECTION.md and critic #47 both put it second and both say the same thing about it: the out-of-fold file is committed but is a **sample, not a field** (18.3 % of 영덕's grid), so the expensive branch needs a fill rule written down *before* the run, and the branch that fits a lap is the one that records the absence with the paths checked.

## In plain terms

- **What changed for the project.** 창의성 is worth 20 of the 100 marks and the rubric names it first, and you already had a good page written about it — but a judge could only reach that page by opening the Q&A folder at exactly the right card. It is now on the finals screen as its own block with the file behind each claim, in your five-minute script as one spoken sentence, and in the printed folder. The screen and the script had never said the word once.
- **Why it matters to the judges or the science.** Every one of the five judges scores that row whether or not they ask about it. The three claims are written the way this project writes everything else — what was built, with the file to check it against, and a closing line saying that whether it is creative is theirs to decide, not ours. A test now refuses any version of those sentences that keeps the word and drops the evidence.
- **What the author should do.** Two things. The spoken sentence cost nine seconds, taken from your other five segments (도입 goes 28 → 37 s); if you would rather not spend them, say `WFG-194: revert the spoken line` and the next lap puts it back with the screen block kept. And five decisions are overdue — NH-032 and NH-034 by a day, NH-035, NH-038 and NH-043 as of today. Until you answer the first two, the student is still forbidden to say any margin number to a judge.



## Gates

**ALL GREEN** · mode `full` · head `9a97e53` · 2026-09-09T04:51:17Z · current at `9a97e53`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 24.4 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.2 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 0.5 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 447.1 s | 1821 passed, 63 skipped, 3 xfailed, 52 warnings in 445.57s (0:07:25) |

## Commits since the previous report (86f8929bf134052adfa12bf662388247994bf9c3..HEAD)

- 9a97e53 claim WFG-194 (20260909T0321Z)
- a9e0430 the critic report names the head it ships in, and the second run that certified the pushed tree
- 55dedbd critic #47: the measurement three laps called cheap covers a fifth of the map, and three P0 rows were frozen on a question already answered

## Needs a human (21 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09)
- NH-036 [DECISION] One critic lap told the next one not to edit a file, and that is what kept a false sentence in front of a judge for a window (by 2026-09-10)
- NH-037 [DECISION] The paper's word proxy now stops it a thousand words before your 25-page rule (by 2026-09-10)
- NH-038 [DECISION] Your "product first" rule has spent the last three dev laps on documents, and the readiness line it was written to protect has not moved in five critic laps (by 2026-09-09)
- NH-039 [DECISION] The national wildfire-spread system's manual is an 18 MB PDF the sandbox could not fetch, and one of you can (by 2026-09-12)
- NH-040 [FYI] A critic lap pushed one commit past a red `--assert-reported`, and it is telling you rather than hiding it
- NH-041 [FYI] This lap sent you an email containing only the word PLACEHOLDER, and could not take it back
- NH-042 [DECISION] Two of your own rules collide whenever a withdrawn claim lives in a frozen artifact, and this week they collided three times (by 2026-09-10)
- NH-043 [DECISION] A gate your loop built this morning will go red about twice a day, and the charter tells the lap that meets it to stop working (by 2026-09-09)
- NH-045 [BLOCKER] The staleness gate has closed `auto/dev` to every routine, and the one routine that met it is the one forbidden to clear it (by 2026-09-08)
- NH-044 [DECISION] The claim the paper just retracted is still live on the page the paper cites for it (by 2026-09-09)
- NH-046 [DECISION] Your product's definition-of-done names a command nothing in this project has ever run (by 2026-09-10)
- NH-048 [DECISION] One of the research routine's three literature channels has been dead for two runs, and a working replacement is already proven (by 2026-09-10)
- NH-049 [DECISION] Your critic routine is told to add judge Q&A cards and your own printing gate makes that impossible for it (by 2026-09-11)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
