# WildfireGuardian autoloop · paper · 2026-09-09T0847Z

| | |
|---|---|
| when | 2026-09-09 09:24 UTC · 2026-09-09 18:24 KST |
| branch / head | `auto/dev` / **`4155f32`** — the commit this report ships in. ⚠ **The generated cells below name `65edfa3`, which is the head this lap INCORPORATED and is never the head it pushes**: `report.py` stamps the header and the gate table before the report commit exists. Read the Gates section with this note, which rides one commit later so every id resolves. The pushed tree was certified by its own `gates.py --mode full` run at **`4155f32`** (`2026-09-09T09:43:31Z`, ALL GREEN, 1826 passed / 63 skipped / 3 xfailed), followed by `--assert-head` and `--assert-reported`, both exit 0. The `65edfa3` run in the table is the earlier one and describes the **previous** tree. Between the two, `git pull --rebase` brought in `b497a0a` (a dev lap's one-line `BACKLOG.md` claim of WFG-210), which moved this lap's commit from `37b9a86` to `4155f32` and is why the suite was re-run rather than the first green reused. |
| kind | manual |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 8, done: 32, dropped: 1, parked: 1, title-derivable: 1, todo: 104 |

## What happened this lap

# Paper lap 23 — nothing in the manuscript went false, and the page anchor was confirmed by measurement for the first time

**Incorporated `a9e0430..65edfa3`** — sixteen commits, seventeen files outside `paper/` and
`docs/auto/`, five new reports. **This lap changed no sentence in the manuscript.** It is the
**third** lap since 12 to change none — laps 19 and 20 were the first two — and the reason is
not that the lap declined to spend its headroom: nothing in the manuscript had gone false.

⚠⚠ **That count read 「second … lap 19 was the first」 until the independent reviewer blocked
the push over it, and the block was right.** It was the one figure in this lap's ledger
**inherited rather than re-derived**, in a section whose stated principle is re-derivation.
The nail is one grep in the same file — `byte-identical to the one lap 18 pushed` returns
**lap 20's** own opening as well as lap 19's — corroborated by `git log -- paper/manuscript.md`
returning three commits in this depth-50 clone, none of them lap 19's or lap 20's. That is the
same failure class lap 22 was blocked for: a wrong count in these ledger files, written by a
lap whose subject is counts going stale.

⚠ **The correction is worth more than the arithmetic.** Lap 20 is the closest precedent and
the two laps are **not** the same. Lap 20's draft was proved by its reviewer to be *worse than
the defect* — it inverted the bound on the headline number — and would not have shipped at any
length, so its no-change outcome has a **correctness** cause beside the budget one. **Lap 23's
does not.** Nothing is wrong with either sentence measured below; no one objected to their
content. **Of the three no-change laps since 12, this is the first where the budget is the sole
cause**, which is a sharper form of the NH-037 escalation than the count first written.

⚠ **Not a claim about the range.** `git diff a9e0430..65edfa3 -- paper/manuscript.md` is
**not** empty — it carries §4.5's rewrite from `4b0010a`, which is **this routine's own
lap-22 push**. A paper lap's commits land inside the range the next lap sees, which is why
the file count above is stated **outside `paper/`**.

## What the incorporated window contains

The 창의성 (creativity) answer and the booth kit: `docs/creativity_card.md` and its test suite
(WFG-194), the same answer onto `web/finals.html`, the printed kit and `README.md` §5
(WFG-207), the repository's address onto the USB (WFG-208), a spoken-pace measurement of the
demo script (WFG-100), a narrowed region-literal gate, and a duplicate-block assertion on the
finals screen's 시스템 구조 tab. None of it is a research result, and none of it is owed a
sentence in an IEEE manuscript.

Re-derived key by key against `a9e0430` rather than read off the diffstat:

- `docs/NUMBERS.json` went **401 → 403** entries. The two new keys are
  `demo_pace_20260909t0321z_total_spoken_syllables` and `_rate_spread`, both booth-pace
  quantities about how many syllables the demo script asks the student to pronounce.
  **No existing key's value or caveat changed** (0 changed values, 0 changed caveats).
- **No claim was withdrawn.** `docs/auto/withdrawn_claims.json` holds **11** entries before
  and after, and `check_withdrawn_claims.py` prints `PASSED === 11 claims over 936 gated
  files`. So §3.5's 「September 2026 retractions skipped it **three** times」 stays three —
  because there was no fourth retraction, which is a different thing from a fourth that was
  registered correctly.
- **No gap opened or closed.** Seven, as at lap 22.
- **All nine figures redrawn, all nine byte-identical** (`drawn [F1 … F9]; skipped []`;
  `git status paper/figures/` empty afterwards). No figure was added or changed, so none was
  looked at as changed.

## Sections changed, and why the manuscript was not one of them

A diff that adds gates is the kind that falsifies §3.5, the section describing the
instrument. Four of its sentences were checked **against the tree at `65edfa3`**, not
inherited:

| §3.5 sentence | how checked | result |
|---|---|---|
| 「every gated document is read against it」 | ran `check_withdrawn_claims.py`'s own `gated_files()` | 936 files, `paper/manuscript.md` among them |
| 「scanned by both gates like any other document」 | ran `check_number_collisions.py`'s `_git_files()` too | 831 files; manuscript in **both** sets — still both, still true |
| 「the scan would have found it in the template, which it does read」 | probed `scripts/finals.template.html` against the gated set | **True** — it is `.html` and is read |
| 「their oracle is the builder … never that what the builder emits is right」 | see below | true, and this window produced its worked instance |

The `Fig. N` mapping was re-counted rather than trusted (F1→1, F2→2, F4→3, F5→4, F8→5, F3→6,
F6→7, F7→8; eleven mentions, each naming the figure that renders at that position). It was
stale for two laps once before.

## ⛔ The one true sentence this lap wanted, measured and declined

§3.5 already says the printables tests' oracle is the builder, so they certify what the
builder emits and **never that what the builder emits is right**. This window produced the
first worked instance, and it is sharper than the stale-provenance-field example the sentence
currently carries: the kit's renderer had no strikethrough rule, so `~~…~~` markup was
dropped and **a retraction would have printed as a live claim** on a sheet a judge holds.
Every other inline rule there loses only weight; that one **inverts** the sentence. Both
existing tests pass a builder that does it, because the builder is their oracle. Sharper
still, the gate that already knew the class grades a **two-line string literal**
(`tests/test_printables.py:170-179`); the repair runs over `SOURCES` itself. The transferable
form generalises this paper's own §3.5 theme: *a gate whose subject is a fixture knows the
failure class and cannot see an instance of it.*

⚠⚠ **This lap's own draft overstated that instance and corrected it before the push.** The
first version said the live instance 「sat in the seven documents that print」. **No commit
ever carried it:** `git show <rev>:docs/creativity_card.md` across all three revisions in the
range returns **zero** lines with `~~`, and the seven `SOURCES` documents are clean at
`65edfa3`. The struck span was in that lap's **working draft**; the account is the
repository's own record (the `_STRIKE` comment in `scripts/build_printables.py`, the new
test's docstring), not a measurement made here. **Nothing reached paper**, which weakens it
to a near miss — and what caught it was a **reviewer with every gate green**, which is
verbatim the shape §3.5 already reports for the defect that prompted those two tests, now a
second time in the same generator. The declined sentence below is costed on the weaker,
truer reading.

**Both forms were inserted and measured with the builder's own counter, not estimated:**

| form | `body_words` | verdict |
|---|---|---|
| minimal — the instance as a clause on the existing sentence | 8,998 → **9,018** | **+20** against 2 words of headroom, 18 over the hard fail |
| full — instance plus the gate-design lesson | 8,998 → **9,040** | **+42**, 40 over |

Both reverted and rebuilt at **8,998**. **Nothing was compressed to fund either** — CHARTER
§12 forbids buying space with a caveat or a registered number.

⚠⚠ **The honest other half, which is the half that matters for NH-037.** The manuscript is
**not false without either sentence and is not missing a limitation.** §3.5 states the
general limit in its own voice; this window supplied an *illustration* of a limit the paper
already carries. That is the **mildest** thing the budget has shaped away in eleven laps —
milder than lap 14's declined illustration and much milder than lap 21's, which was a
limitation the paper nowhere stated. Stated at that size and not inflated. **NH-037 is still
the answer and it is still open.**

## ✅ What the lap did land

**The page anchor confirmed by measurement, for the first time.** `body_words` did not move,
so `check_paper.py` would have passed with no renderer at all. The lap ran the `apt` line
`paper/README.md` has printed since lap 9 anyway and took the measuring branch **on the
committed `.docx`**: `pages 23, calibri_face Carlito, metrics_ok true`, printing
`6b0702d747ed5beb` — **the same string `STATE.json` already carried**. Every earlier
confirmation either carried an unmoved input or replaced a moved one. **Two pages against the
author's 25 and two words against the proxy's 9,000, measured on one document by one run.**

⚠⚠ **New, and it changes what a diff on the built `.docx` means: the document is
content-deterministic and byte-non-deterministic.** `build_docx.py` run three times on an
identical manuscript produced three different files. Decomposed rather than asserted: all
**25** zip members are **byte-identical in content** across the three, all 25 differ only in
their **zip modification timestamp** (the wall clock at build time), and `docProps/core.xml`
is itself identical — nothing is stamped into the document, only into the archive. Two
operational consequences: **a byte diff on `WildfireGuardian_Park_2026.docx` is not evidence
that the document moved** (the figure-font paragraph one layer up, now measured for the
builder too), and **a lap whose manuscript did not move must not commit a rebuilt `.docx`**,
because the diff is 2.9 MB of binary churn that says nothing. This lap restored the committed
file and measured its 23 pages on **that** file.

⚠ It gates nothing — no push re-derives it, the standing weakness `measure_pages.py` and
`measure_render_gap.py` share. The fix that would make it checkable (a fixed `date_time` in
the builder, or a content-only digest) is one line inside this routine's own paths and was
**not** taken: a build change wants its own lap and its own reviewer, and the manuscript is
at two words of headroom.

## The post-hoc-maximum scan, re-run AFTER writing

Re-running it after writing rather than before is the exact failure lap 22's reviewer blocked
on. `unqualified_post_hoc_claims` returns **six units** across `paper/`, **zero of them
real**, **14 raw matches** (1 manuscript, 10 `GAPS.md`, 3 `README.md`) — enumerated line by
line this lap. The manuscript's single match is §4.5's shoulder sentence and its unit count
is **zero**, because the post-hoc qualifier sits in that same unit: the gate working. Every
other match is the trigger firing on a **word-budget** margin, NH-037's sense of the word
rather than NH-032's. **None of this lap's new sections is among the six.** This lap's prose
deliberately avoids the colliding phrasing and says so where it does; the real fix is still a
narrowed trigger in `tests/`, outside CHARTER §12.

## What did not work / what was not attempted

- The sentence §3.5 wants does not fit and could not be made to fit honestly. Two forms,
  both measured, both reverted.
- The `.docx` build fix is inside this routine's paths and was deliberately deferred.
- No gap is closeable by this routine: G6 and G7's cheap version need a new artifact outside
  `paper/`; G3, G4 and G5 need the author's laptop or the author.

## Gates

`gates.py --mode full` exit **0** at `65edfa3` (verify PASS, snapshot-verify PASS, env-check
PASS, pytest 1826 passed / 63 skipped / 3 xfailed in 480 s; `baseline-verify` WARNs on the
two git-ignored `data/raw/**` contracts, which is NH-029/§3d working as designed).
`check_paper.py` exit 0. Clone shallow, depth **50**, measured this lap — **no ancestry or
reachability claim appears anywhere in this lap's output.**

Reviewed by: subagent (**block**): the ledger's one inherited count — 「the second lap since 12
to change no sentence」 — is false; lap 20 also changed none, so it is the third, and a section
whose organising principle is 「re-derived rather than read off the diffstat」 implemented the
opposite on the one count about its own history.

**Fixed before the push, in `paper/GAPS.md`, `paper/README.md` and `paper/STATE.json`**, and
the reviewer's own suggestion was taken and sharpened: lap 20 is the closest precedent, but
its draft was *worse than the defect* and would not have shipped at any length, so **lap 23 is
the first no-change lap since 12 whose sole cause is the budget.** That strengthens NH-037
rather than weakening it.

**Everything else the reviewer was asked to check reproduced exactly, re-run rather than
trusted**: the 401 → 403 registry with 0 changed fields of any kind over the 401 shared keys;
11 withdrawn-claim entries at both commits; 936 gated and 831 collision-gate files with the
manuscript in both; all four §3.5 sentences; the `.docx` determinism decomposition rebuilt
independently (three md5s, 25 members, 0 differing in content, all 25 differing in zip
`date_time`, `docProps/core.xml` identical); 16 commits / 17 files / 5 reports / shallow at
depth 50; the figure mapping and all eleven `Fig. N` line numbers; the post-hoc scan still 6
units and 14 raw after the mid-lap edits. The manuscript audit found **no CHARTER §3
violation**: 29 citations all present and all carrying a `verified` note, the five
catalogue-only works each held to their title's own words, the 7 `[GAP:]` markers bijective
with G2–G8, no withdrawn claim asserted, and the mean-of-folds / pooled and 32.6 % coverage
caveats travelling with every number that needs them. `factchk` confirmed the 99,289 ha
provincial tally and GK2A's 2 km / two-minute sector externally, in both directions.

⚠ **One non-blocking item the reviewer raised, for a dev lap rather than this one.** §4.6's
caption already self-declares that the Monte-Carlo (86 % of 2,000 draws), dilation (125 m) and
translation (125–530 m) values 「do not yet carry registry keys of their own」. Both artifacts
are git-tracked and carry those values, so they meet the committed-artifact standard, and the
disclosure is in place where the numbers are — but under a strict reading of CHARTER §3.3 they
are unregistered prose numbers. Pre-existing, not introduced this lap, and outside CHARTER
§12's paths: it wants a backlog row (`scripts/build_numbers.py` entries for
`forecast_robustness.json` and `dilation_perturbation.json`), not a manuscript edit.

## In plain terms

The paper says the same thing it said six hours ago, and that is the right answer this time: the code that moved was your booth material — the 창의성 card, the demo script's pace, the USB — and none of it changes a research claim, so I checked the four sentences it could have made false, found them all still true, and wrote nothing into the manuscript.

What is still missing is unchanged and it is still waiting on you: NH-032 and NH-034 (which fair opponent, and what the surfaces then say) are a day overdue and are what keeps §4.5 from stating a number, and NH-037 is the length question — the paper is at 8,998 words against a 9,000 limit, and today that stopped a true sentence about how one of your own gates can be fooled.

Answer NH-037 with a number, or say "keep 25 pages and drop the word limit" — the document measures 23 pages against your 25, so there is room the word counter cannot see, and every lap for eleven laps has now been shaped by the counter rather than by the evidence.



## Gates

**ALL GREEN** · mode `full` · head `65edfa3` · 2026-09-09T09:09:09Z · current at `65edfa3`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 25.5 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.2 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 1.6 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 482.2 s | 1826 passed, 63 skipped, 3 xfailed, 52 warnings in 480.19s (0:08:00) |

## Commits since the previous report (7f914fde59040a810e14b77ae5e5cd48899c58a7..HEAD)

- 65edfa3 critic #49: the gate keeps the competitors out of the spoken answer, and the answer's own link hands them over

## Needs a human (22 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09)
- NH-036 [DECISION] One critic lap told the next one not to edit a file, and that is what kept a false sentence in front of a judge for a window (by 2026-09-10)
- NH-037 [DECISION] The paper's word proxy now stops it a thousand words before your 25-page rule (by 2026-09-10)
- NH-038 [DECISION] Your "product first" rule has spent the last three dev laps on documents, and the readiness line it was written to protect has not moved in five critic laps (by 2026-09-09)
- NH-039 [DECISION] The national wildfire-spread system's manual is an 18 MB PDF the sandbox could not fetch, and one of you can (by 2026-09-12)
- NH-040 [FYI] A critic lap pushed one commit past a red `--assert-reported`, and it is telling you rather than hiding it
- NH-041 [FYI] This lap sent you an email containing only the word PLACEHOLDER, and could not take it back
- NH-042 [DECISION] Two of your own rules collide whenever a withdrawn claim lives in a frozen artifact, and this week they collided three times (by 2026-09-10)
- NH-043 [DECISION] A gate your loop built this morning will go red about twice a day, and the charter tells the lap that meets it to stop working (by 2026-09-09)
- NH-045 [BLOCKER] The staleness gate has closed `auto/dev` to every routine, and the one routine that met it is the one forbidden to clear it (by 2026-09-08)
- NH-044 [DECISION] The claim the paper just retracted is still live on the page the paper cites for it (by 2026-09-09)
- NH-046 [DECISION] Your product's definition-of-done names a command nothing in this project has ever run (by 2026-09-10)
- NH-048 [DECISION] One of the research routine's three literature channels has been dead for two runs, and a working replacement is already proven (by 2026-09-10)
- NH-049 [DECISION] Your critic routine is told to add judge Q&A cards and your own printing gate makes that impossible for it (by 2026-09-11)
- NH-050 [DECISION] You answered two of these questions two days ago and the loop never heard you, because you answered them on the routine page (by 2026-09-10)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
