# WildfireGuardian autoloop · paper · 2026-09-04T0724Z

| | |
|---|---|
| when | 2026-09-04 07:19 UTC · 2026-09-04 16:19 KST |
| branch / head | `auto/dev` / `8ab569e` |
| kind | manual |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 7, done: 11, in-progress: 1, parked: 1, todo: 40 |

## What happened this lap

Paper lap 3. Incorporated `5a0466e..8ab569e` — 13 files outside `paper/` and
`docs/auto/`, twenty-one commits, whose substance for the paper was WFG-053: the
repository withdrew its claim that the satellite rang after the telephone, across four
judge-facing documents, without moving a number. The manuscript had reached that narrow
reading first, so this lap's job on it was not to change a claim but to stop describing a
repository reading that no longer exists.

**The lap's own reviewer blocked it twice, and both blocks were right and were taken.**
The first block caught this lap making a P0 defect *worse*; the second caught the first
fix being applied in one of the two places that carried the claim. Details below, because
this is the part of the lap worth reading.

## Reviewer block #1 — this lap broke the death-toll attribution it set out to fix

Critic #7's F30 (WFG-051, P0, three windows open) says one figure has three agencies and
that it had reached the paper. It had: §1 said the 26 deaths were "the provincial disaster
headquarters' count". I "fixed" that to the **central** headquarters on the authority of
`docs/NUMBERS.json`'s agency field, and cited a 서울신문 page for it.

The reviewer blocked, and its nail was one fetch. I ran it myself before touching
anything. 대구MBC 2025-03-30 — the page `docs/data_sources.md` already cites for this exact
figure — reads: 「경상북도 재난안전대책본부가 3월 30일 오전 8시 30분을 기준으로 발표한
자료에 따르면 산불로 인한 경북 지역 사망자는 영덕군 9명, 영양군 7명, 안동시와 청송군 각각
4명, 의성군 2명 등 26명입니다」. Neither 중대본 nor 중앙재난안전대책본부 appears on that
page at all. The 26, the five-district split and the Yeongdeok 9 are all the **provincial**
headquarters'. Meanwhile the 뉴시스 page the registry names as its own source is a
breaking-news stub naming no agency, and the 서울신문 page I had cited gives 중대본's
undivided two-province total of 30 and does not contain 26 at all — I had cited a page for
a figure it does not carry.

So the pre-lap wording was right and I had replaced it with the one unsupported option.
Reverted, and the paper now cites the page that actually carries the figure
(`dgmbc2025toll`, opened this lap); `seoul2025toll` is deleted rather than left as an
orphan. The sentence is also now internally coherent — "where that count had 9" refers to
a count that has districts in it. **This is the precedent WFG-051 itself records: when the
registry and an openable source disagree, the source wins.**

## Reviewer block #2 — a retraction applied in one place out of two

Having weakened §6's claim about the manifest (below), I left the *same* claim standing in
the Table 3 caption — the caption §4.7 sends the reader to with "for the reasons in the
caption". The manuscript asserted and denied the same thing about the same file 150 lines
apart, with the load-bearing half being the wrong one. One `grep -n "as the ignition"`
finds it. Fixed; the caption now says the manifest "nowhere says what the field is",
matching §6 verbatim. The reviewer's rule is worth keeping: **retract by grep, not by
section.**

## Sections changed

- **§1, death toll** — as above. Provincial attribution restored, given the 08:30 basis
  and its own citation. The paragraph's tension with the rapid attribution study (which
  puts all 26 in Uiseong, against the provincial Uiseong 2) now stands as a genuine
  disagreement between published sources rather than as circular support for my own
  arithmetic.
- **§1, the share-of-nationwide sentence.** It read "the repository's own two readings of
  that comparison disagree", true when written and not now: `data_sources.md` settled it
  this window. Replaced with the sourced reason. The reviewer trimmed my first attempt —
  I had written that the fire-affected-area estimate "was superseded rather than standing
  beside" the survey, which is *not* what the Korea Forest Service said and contradicts
  `data_sources.md` 함정 1. The sentence now asserts only the sourced claim: the two are
  different concepts that cannot simply be compared (경향신문 2025-04-17, opened this lap,
  now `khan2025area`; definitions cross-checked in 문화일보 2025-04-18, also opened).
- **§4.7, tense.** "reads it as a report time" → "read … and the note has since been
  withdrawn with no number moving". The gate's two positive anchors on this file are
  untouched and `tests/test_detection_ordering_is_not_claimed.py` passes.
- **§6, the G5 gap marker.** I first wrote that exactly one manifest entry says what the
  `start` field is, calling it the ignition. The reviewer showed that is an inference from
  a date coincidence, not a statement — the note never mentions `start` — and probably the
  wrong one: Yeongdeok's `start` is `2025-03-22T12:15`, fifty minutes after the **parent
  Uiseong chain's** `11:25` on the same date, while Yeongdeok's own first detection is
  three days later. So the "ignition" that note names is most plausibly the parent fire's.
  The marker now claims only the defensible half: no entry names a report time, and none
  says what the field is, in either direction. That is also what
  `docs/detection_floor.md` §1 concluded independently.

## Figures — critic #7's F36 / WFG-064 (P1), both halves mine

I looked at all seven again afterwards, not only the two named.

- **F2** — the `0.878` label was struck through by the mean-of-folds rule and `pooled
  0.905` sat on the x-axis tick labels. Values now sit inside the bars in white; the two
  reference lines are named in a boxed legend in the lower right, a corner provably empty
  because the bars there are the shortest.
- **F7** — panel b coloured "deadline first wins" with panel a's *nearest-first* teal, so
  one colour carried opposite meanings in panels an inch apart. Both panels now read
  vermilion = deadline-first, teal = nearest-first, and panel b says "ahead" rather than
  "wins/loses" so colour and word agree. Fixing that exposed a defect nobody had named:
  panel a's legend was covering the nearest-first line between three and five teams. Every
  series rises left to right, so no corner is free — both legends now sit below their axes.
- **F1** — the hazard-field arrow landed on the *corner* of the Rescue-ingress box, reading
  as pointing between boxes; it now meets the centre of its top edge.
- **F5** — the last point's `1036` label touched the panel frame; panel b gained a margin.
- Both rules are written into `paper/README.md`, because both defects shipped in a lap that
  had recorded looking at all seven.

## Length

**7,408 words against the 7,500 hard fail** — 92 words of headroom, against the twenty-one
lap 2 left (critic #7's F29). Added about 80 words, cut about 152, from the places lap 2
nominated. No number and no caveat was dropped; every deletion restated a claim already
made in the same or an adjacent section.

**The page count is still not measured, and lap 2's account of why was wrong.** Lap 2 wrote
that LibreOffice "refuses to load the built document", which reads as a fact about our
file. It is not: `soffice --convert-to pdf` fails identically on a two-paragraph `.docx`
written by the same library in the same environment, so the converter cannot open *any*
`.docx` here. I did not manufacture a substitute — no Calibri-metric font is installed, so
a text-flow simulation would have to assume a line height nobody here has measured, which
is the unchecked constant the gap exists to remove. Recorded in `GAPS.md`; the cheapest
close is the author opening the `.docx` and reading the page number off it.

## For the next dev lap — two things the paper cannot fix

Recorded in `GAPS.md` and repeated here because CHARTER §12 stops this routine editing
`BACKLOG.md`, `docs/NUMBERS.json` or `README.md`.

1. **WFG-051 (P0, open) — the paper half is done, the rest is not.** The manuscript now
   cites the right source. Still wrong: `fire2025_chain_deaths`'s agency field
   (중앙재난안전대책본부, sourced to a stub that names no agency), and `README.md`, which
   attributes the Yeongdeok 9 correctly but still puts the 26 under an 아시아경제 link
   carrying no death figure and closes the paragraph with 「중앙재난안전대책본부 확인」.
2. **The registry still asserts the ~95 % ratio the prose withdrew.**
   `fire2025_chain_area_ha` and `fire2025_nationwide_area_ha` carry it in their caveats,
   the latter with gate-enforced `forbidden_phrasings`. CHARTER §12 says caveats travel
   with their numbers, so the paper is declining to repeat a caveat its own registry still
   holds. **Escalated as a new option D on NH-018**, which already owns this question:
   unlike the attribution, no source settles whether two differently-scoped totals may be
   divided — that is a claim-shape judgment and the author's call. Nothing is blocked on
   it; the manuscript's position is the conservative one.

## Gaps

Four, unchanged in number. G5 narrowed and then correctly weakened. G2, G3, G4 unchanged,
all still after-sprint.

## Gates

`paper/build_docx.py` clean; `paper/check_paper.py` exit 0 (7,408 body words, 7 figures,
3 tables, 27 references, 4 gaps); `scripts/auto/gates.py --mode quick` exit 0 (`make
verify` PASSED, 40 tests). Full suite run separately: **1261 passed, 62 skipped**, matching
critic #7's cold census. The reviewer independently re-ran `verify_numbers.py` (312/312),
`check_number_collisions.py`, `check_forbidden.py` and `check_paper.py` and found no
registered value wrong anywhere in the manuscript, at either pass.

Reviewed by: subagent (block, twice, both taken): first that this lap replaced the one
supported death-toll attribution with the one unsupported one on the registry's authority,
against an openable source that falsifies it; then that the resulting retraction was
applied to §6 but not to the Table 3 caption §4.7 points at. Both fixed and re-verified;
the reviewer's closing position was that with the caption clause deleted and the word count
refreshed it has no remaining objection.

## In plain terms

The paper's figures had two real defects and both are fixed; the paper now cites the right
newspaper for the 26 deaths, after my own first attempt got it wrong and the reviewer
caught me.

Still missing: one 신고접수시각 (NH-019) would let the detection section say whether the
satellite beat the phone call, and the two experiments that need your laptop (G3, G4) are
still the biggest holes.

What to do: open `paper/WildfireGuardian_Park_2026.docx` in Word and tell me the page
count — that is the last unchecked claim in the whole paper, and it is one number.



## Gates

**ALL GREEN** · mode `quick` · head `8ab569e` · 2026-09-04T07:18:21Z · current at `8ab569e`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 14.1 s | === make verify: PASSED === |
| pytest-quick | PASS | 6.9 s | 40 passed in 6.40s |

## Commits since the previous report (8e0a6ad7dfea64ad657b66d7b07e215a22baaa8b..HEAD)

- 8ab569e claim WFG-017: the finals screen, which critic #7 says is the one P0 whose cost nobody has measured
- 77576bc critic #7 addendum: the report certifies the commit it travels in, not only the one it read
- bfc5a5e critic #7: the card stopped saying who was late and started saying who should be first

## Needs a human (14 open)

- NH-001 [DECISION] Email delivery needs three repository secrets
- NH-002 [DECISION] Optional: `@claude` on GitHub issues and PRs
- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-010 [DECISION] Expert consultations and the firefighter record (by 2026-09-20)
- NH-011 [DECISION] One real, recorded email send from a network that works in Shanghai (by 2026-09-20)
- NH-012 [DECISION] Portal downloads the loop cannot do (by 2026-10-01)
- NH-013 [FYI] Optional: a stable web address for the visual board
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-016 [DECISION] The critic routine has 30 minutes to land its findings, and needs about 40 (by 2026-09-05)
- NH-017 [DECISION] Three entries were closed on replies the repository cannot see, and one of them was wrong (by 2026-09-06)
- NH-018 [DECISION] Two laps read the same primary sources and disagree on two sentences (by 2026-09-08)
- NH-019 [DECISION] One report time, for one fire, decides what the detection result is allowed to say (by 2026-09-08)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
