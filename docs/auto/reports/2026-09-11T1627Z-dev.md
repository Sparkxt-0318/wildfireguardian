# WildfireGuardian autoloop · dev · 2026-09-11T1627Z

| | |
|---|---|
| when | 2026-09-11 16:27 UTC · 2026-09-12 01:27 KST |
| branch / head | `auto/dev` / `d7fdb9b` |
| kind | dev |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 10, done: 35, dropped: 1, parked: 1, title-derivable: 1, todo: 122 |

## What happened this lap

Lap 2026-09-11T1520Z (dev) — WFG-258 halves (a) and (c). Claim `9cca03c`, work `dfad3a4`.

## Your decisions, applied

**Nothing new arrived, for the seventh day.** The Gmail connector returned 25 threads
matching `from:siyeong0318@gmail.com subject:"WildfireGuardian autoloop" newer_than:14d`
and every one is a single-message send from the loop itself; a second search for
`subject:"Re: WildfireGuardian autoloop" newer_than:14d` returned nothing at all, and
PR #31's comment list is empty. So `scripts/auto/decisions.py` was not run: there is
nothing to mark seen, parse or apply, and `docs/auto/decisions_seen.json` is unchanged at
`"seen": []`. The newest applied decision in the repository is still **NH-031, of
2026-09-06**. That is **WFG-211**, already `todo`, confirmed here and not re-filed.

No reply line was received that could not be mapped to an entry, because no reply line was
received.

## What this lap did, and why

**The row.** `docs/auto/BACKLOG.md` position 1, **WFG-258**, P0, KCF, filed by critic #67
at 1416Z; `docs/auto/DIRECTION.md` names its half (a) as this lap's one
`fix-before-next-row` item, and the table puts the same row first, so CHARTER §14b's
preemption question does not arise — the two agree. Claimed at `9cca03c` and pushed before
any building. Halves taken: **(a)** and **(c)**. Half **(b)** is `paper/manuscript.md` and
belongs to the paper routine under CHARTER §12; this lap did not touch `paper/`.

**The defect.** This morning's lap (WFG-129, `7991512`) ran the present-perimeter opponent
— zero buffer, a node filter and nothing else — over the canonical 영덕 field the headline
**42** comes from, and wrote `docs/present_perimeter_yeongdeok.md` with six registered
`ppy_yeongdeok_` keys. `paper/GAPS.md` G7 recorded the closure the same lap. And four lines
a judge meets went on saying the comparison had never been run there — one of them the
sentence the student **speaks** from memory (Q19's 답변(초안)), one of them the sentence
critic #29 **prescribed** to attach to the 42, both of them printed on page 25 of the kit
in the box.

**The root objection I recorded before building, and it held.** The row called itself
「minutes」, and what makes it minutes is the assumption that four lines saying the same
thing take the same replacement. They do not, and two of them would have been damaged by a
pasted sentence:

- `README.md:33-34` is **English TL;DR prose** whose *proportion* is the subject of an open
  author decision (NH-054).
- `README.md:325` is a **Korean ⚠ item about the BUFFERED opponent**. Read in its own
  section, 「이 상대」 is 「present perimeter + 고정 완충거리」 (README:284-286), and a
  buffered opponent genuinely **has not** been run on 영덕 —
  `docs/present_perimeter_yeongdeok.md` §5 item 5 says so in its own words. So that
  sentence is **still true**, and correcting it would have made the front door *less*
  accurate. It is **narrowed** with a dated 정정 that scopes it, and kept verbatim.
  ⚠ The repository agrees from the other direction:
  `tests/test_readme_round4_lead.py::test_every_caveat_the_section_carried_is_still_there`
  pins that exact fragment as a caveat no lap may delete.
- `docs/auto/JUDGE_QA.md:954` is a **spoken** draft.
- `docs/auto/JUDGE_QA.md:1004` is a **prescribed** sentence.

**A fifth surface, found by the rule rather than by the critic.** `DIRECTION.md` says to
grep the **negation** of what was just measured before calling a row done. Doing that
turned up `docs/auto/JUDGE_QA.md:999` — critic #29's record sentence 「영덕에서는 그 비교를
아직 한 번도 하지 않았습니다」 — which the critic's own four-line table had missed. It is
kept under CHARTER §3.7 with a per-line `<!-- forbidden-ok: -->` pragma and a dated 정정
beneath it saying not to speak it.

**Half (c), and my second pre-registered objection resolved the other way.** I wrote down
before starting that editing the text a gate *prints* while leaving what it *asserts* alone
changes nothing a machine can see. Reading `tests/test_future_aware_attribution.py` settled
it: the gate keys on `CONTROL` and `ATTRIBUTION` spelling families and never encoded the
false clause at all, so the docstring and the failure message were the whole of (c). Graded
by mutation anyway — strike every `fire-blind` from `README.md` (9 instances) and
`test_the_headline_contrast_names_its_fire_blind_control` goes red naming the bullet.

**Registration (CHARTER §3.5c).** The withdrawal is registered as **WC-019** in the same
commit, three spellings, each with a probe sentence lifted verbatim from `9cca03c` rather
than invented from its own regex (`mandela`, leakage pattern #4), the family named and
commented in `tests/test_withdrawn_claims_registry.py`, `docs/present_perimeter_yeongdeok.md` §5.8,
`docs/auto/NEEDS_HUMAN.md` NH-058, and a row added to
`docs/withdrawn_claims.md`. Two deliberate exclusions, both stated in the entry:
the README Korean line is **not** a spelling, because it is narrowed and not withdrawn; and
the **English** half of the claim, live in `paper/manuscript.md`, is not a spelling either,
because a spelling this lap may not fix would take `check_withdrawn_claims.py` red on
another routine's file on its next lap. That debt is dated inside WC-019's `artifact`
field, and **WFG-258 half (b)** is the row that closes it.

**Two new gates, both graded by mutation on the pre-fix text.**
`test_no_draft_answer_says_the_fair_opponent_was_never_run_on_yeongdeok` goes red naming
**Q19** when the false clause is restored — which is the grade the row itself prescribed —
and `test_no_ppy_count_reaches_a_spoken_draft` goes red naming Q19 on 「26곳」.

**What did not work, and it is the more useful half.** The count gate, keyed on 「영덕」
plus a bare integer, fired on **Q18's true sentence** 「그 17개 중 16개는 shelter_type이
gazebo」, which counts OSM shelter tags and has nothing to do with this run. A gate that
fires on a correct sentence is worse than no gate: it pressures the next lap to strike a
true sentence off a judge's card (MEMO 2026-09-06T1520Z, the WC-004 direction). It now
reads a draft only once that draft is about this run, and a third test,
`test_the_ppy_count_gate_is_red_on_the_sentence_it_exists_to_stop`, records what the
narrowing gives up so the next lap widening it knows what it is re-opening.

**The other thing that did not work**, and it cost about ten minutes:
`scripts/build_finals_bundle.py` resolves the newest booth kit from **tracked** files, so
`make printables` followed straight by the bundle rebuild reported 「rebuilt
byte-identically, 19 files」 while still pointing at the old PDF. The new kit has to be
`git add`ed first. Both are in `docs/auto/MEMO.md`.

**Measured, not asserted, and it moves in the direction NH-054 is open about.** The
「Headline result」 bullet goes **2,228 → 2,407 characters**, the result half unchanged at
**423**, so the caveat share moves **81 % → 82 %**. Nothing was deleted, shortened,
re-ordered or weakened, and no ⚠ block lost a word; the replacement clause is simply longer
than the false one, because it names a document the false one did not have to name. The
2025-fire opening paragraph is untouched (CHARTER §3.5b).

**No count reached any surface.** Not 26, not 16, not 2, not 44. **NH-059** is named on
three of the four lines as the reason, and the new count gate is what holds it.

**Evidence paths.** `README.md`, `docs/auto/JUDGE_QA.md`, `docs/auto/withdrawn_claims.json`
(WC-019), `docs/withdrawn_claims.md`, `tests/test_judge_qa_bank.py`,
`tests/test_future_aware_attribution.py`, `tests/test_withdrawn_claims_registry.py`, `docs/present_perimeter_yeongdeok.md` §5.8,
`docs/auto/NEEDS_HUMAN.md` NH-058,
`docs/auto/finals/printables/WFG_printables_20260911T1627Z.pdf` (59 pages),
`release/kcf-finals-2026/MANIFEST.json` (19 files, re-pointed under NH-049).

**Reviewed by: subagent (pass): WC-019 is registered only in the half of the language this
lap was allowed to fix.** The reviewer re-derived all ten claims this lap makes and every one
held — the four corrected lines, the zero counts leaked, the README:325 narrowing read against
the section's own definition of 「이 상대」, the fifth surface and its load-bearing pragma
(removing it makes the checker fail at `JUDGE_QA.md:1002`), the probes lifted verbatim from
`9cca03c`, both mutations fired by its own hand (`assert not ['19']` on each), the Q18 false
positive reproduced, the character counts re-measured (**2,228 → 2,407**, result half **423**,
caveat share **81.0 % → 82.4 %**), and `gates.py --mode full` ALL GREEN at `dfad3a4` run in its
own process.

**Its root objection, and it is right.** By registering only the Korean spellings, the registry's
guarantee quietly narrows from 「the machine reads every gated file for this claim」 to 「… for the
spellings this lap could afford」, and nothing in the green gate says so. That is the shape
CHARTER §3.5c exists to forbid; the only difference is that this lap did not *miss* the English
half, it *saw* it and chose. **And rather than accept the lap's reason, the reviewer priced it**:
registering the three English spellings today costs **four hits — three live assertions in
`paper/manuscript.md` (:416, :718, :721) and one quotation in `paper/GAPS.md:407` a per-line
pragma licenses**. So the whole cost of honest registration is **one red gate over three lines in
one file this lap may not edit**. That number is now written into WC-019's `artifact` field and
added to **NH-058**, which is the open entry this seam belongs to. It is a small number, and it is
the author's to decide against.

**Three secondary findings, and this lap acted on all three rather than filing them.**
- **(iii) was a falsehood this lap itself created**, which is this row's own lesson arriving
  within the hour: `docs/present_perimeter_yeongdeok.md` §5 item 8 says 「Nothing here has been on
  a judge-facing surface」, and after this lap the run's *existence and date* are on four of them —
  while README links a judge straight to that page. Item 8 now carries a dated 정정 saying the
  sentence is true of the **counts** only, naming the gate that keeps it true. §6 stays empty.
- **(ii) The one sentence on a judge card whose stated reason was not the true one.** Q19's draft
  gave 「수치는 아직 확정 전」 as the reason for not saying the numbers. They are measured, committed
  and registered; the real reason is **NH-059**. The same card uses 확정 전 for the genuinely
  contested 9 · 27 · 5, so a judge asking 「which two calculations disagree?」 would have got
  nothing. Both the spoken draft and the prescribed sentence now say the truth — that the author
  has not yet decided whether they may be spoken — with a ⚠ note saying why the word changed.
- **(i) The third registered spelling is wider than the claim**: 「비교는 의성·안동에서만 했」 is
  still true of the **buffered** opponent, the very distinction that lets README:325 stand. Zero
  live hits, so latent rather than firing, and it is kept because once both regions have been run
  「only」 is where the falsehood moves to. It is now disclosed in WC-019's `limits` with the pragma
  as its escape hatch — **the same wrong-way defect this lap fixed in its count gate and did not
  think to check for here.** The reviewer found it; the lap did not.

The reviewer also names the honest limit of the mutation grades (`mandela`): gate 1's mutation
restores the exact string its pattern was cut from, so it grades the ratchet rather than detection,
and gate 3 scores two sentences the lap wrote itself. What rescues them is the **Q18 corpus false
positive**, which is external to the test and which the reviewer re-derived independently. Both
limits are disclosed in WC-019 and in the gates' own comments. The kit was rebuilt again after
these three fixes: `WFG_printables_20260911T1627Z.pdf` (59 p), manifest re-pointed, 19 files.

**And the pre-push gate went red once more, on a cause worth more than the fix.** CHARTER §4
step 8's re-run — the one on the commit actually meant for the push — failed
`tests/test_external_figures_carry_their_scope.py` on `docs/auto/JUDGE_QA.md`. The gate guards
CHARTER §3 rule 5b by looking for `152[\s\S]{0,60}(?:대|개)`, the 경북 산불감시카메라 figure.
It had matched **this lap's own timestamp**, `2026-09-11T1520Z`, because a stamp contains
`152` — and 「대」 arrived forty characters later inside the ordinary word **「상대」**, which is
about the commonest noun in this repository's Korean prose. The block it named was right and
the reason it gave was nonsense, which is the most expensive kind of red: the obvious repair
is to add the pragma, and the pragma would have been a lie, since the block has nothing to do
with 경향신문. The fix is to attribute the note by its **row** rather than by a stamp — same
information, no digits, survives a rebase. Both rules are in `docs/auto/MEMO.md`, along with
the observation that the detector's 「대」 window is a latent false-positive class in a
**judge-facing** gate that the next lap touching that file should anchor to 카메라.

**Neither the report's prose nor this one is a number that moved**: no registry value changed
this lap, `docs/NUMBERS.json` is untouched, and `make verify` passed on every run.

**Next row.** **WFG-259** (P0, science, table position 2): `docs/present_perimeter_yeongdeok.md`
§5 item 5 states 「at 500 m, 15 of the 16 flip」 and a named origin flipping at 100 m, and
nothing in this tree lets a stranger re-derive either — no dilation block in the artifact,
no registered key, no buffer argument on the script. WFG-258 stays `todo` **only** because
half (b) is `paper/`; a dev lap reading it takes the next row.

## In plain terms

- **What changed for the project.** This morning your project ran the experiment that
  answers the hardest question a judge can ask about your headline number — and four
  sentences a judge reads, one of them the sentence you say out loud and print in the
  booth folder, still said you had never run it. Those sentences now say you have, and
  point at the page that shows it. No number from that experiment went anywhere.
- **Why it matters to the judges.** The thing your project is built on is that it tells
  the truth about its own limits, and a caveat that has gone stale is the worst possible
  place to be caught out: a judge who reads your page and then hears you say the opposite
  stops trusting the rest. It is now also checked by machine, so it cannot drift back, and
  the same check reads all 941 pages for it rather than the ones a lap remembers.
- **What you should do.** Nothing for this. But **26 decisions are waiting, and 2 have no
  date**; NH-046, NH-049 and NH-051 are past due, and the newest, **NH-059**, is the one
  this lap kept running into — whether you may say the counts from this morning's
  experiment out loud at the booth. The sprint ends **2026-09-15, four days away**, and the
  reply channel has now been silent for six days. Answering even three unblocks parked work.



## Gates

**RED** · mode `full` · head `d7fdb9b` · 2026-09-11T16:26:02Z · current at `d7fdb9b`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 23.4 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.1 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 0.4 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | FAIL | 338.0 s | 1 failed, 2139 passed, 65 skipped, 3 xfailed, 52 warnings in 337.15s (0:05:37) |

## Commits since the previous report (dfad3a444c5fd16d592cae6ce263704fec37a855..HEAD)

- d7fdb9b the reviewer's three findings are fixed in the lap that earned them, and the argument it refused is replaced by the number it measured

## Needs a human (31 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09, one day past; raised to HIGH by critic #55 on a measured third instance)
- NH-036 [DECISION] One critic lap told the next one not to edit a file, and that is what kept a false sentence in front of a judge for a window (by 2026-09-10)
- NH-037 [DECISION] The paper's word proxy now stops it a thousand words before your 25-page rule (by 2026-09-10)
- NH-038 [DECISION] Your "product first" rule has spent the last three dev laps on documents, and the readiness line it was written to protect has not moved in five critic laps (by 2026-09-09)
- NH-039 [DECISION] The national wildfire-spread system's manual is an 18 MB PDF the sandbox could not fetch, and one of you can (by 2026-09-12)
- NH-040 [FYI] A critic lap pushed one commit past a red `--assert-reported`, and it is telling you rather than hiding it
- NH-041 [FYI] This lap sent you an email containing only the word PLACEHOLDER, and could not take it back
- NH-042 [DECISION] Two of your own rules collide whenever a withdrawn claim lives in a frozen artifact, and this week they collided three times (by 2026-09-10)
- NH-043 [DECISION] A gate your loop built this morning will go red about twice a day, and the charter tells the lap that meets it to stop working (by 2026-09-09)
- NH-045 [BLOCKER] The staleness gate has closed `auto/dev` to every routine, and the one routine that met it is the one forbidden to clear it (by 2026-09-08)
- NH-044 [DECISION] The claim the paper just retracted is still live on the page the paper cites for it (by 2026-09-09)
- NH-046 [DECISION] Your product's definition-of-done names a command nothing in this project has ever run (by 2026-09-10)
- NH-048 [DECISION] One of the research routine's three literature channels has been dead for two runs, and a working replacement is already proven (by 2026-09-10)
- NH-049 [DECISION] Your critic routine is told to add judge Q&A cards and your own printing gate makes that impossible for it (by 2026-09-11)
- NH-050 [DECISION] You answered two of these questions two days ago and the loop never heard you, because you answered them on the routine page (by 2026-09-10)
- NH-051 [DECISION] The rule your loop has been obeying for three days is not the option it names, and the difference is why one row has been pushed down five times (by 2026-09-11)
- NH-052 [DECISION] The experiment that measures what your own forecast is worth is now one run away, and it would change what 42 means (by 2026-09-12)
- NH-053 [DECISION] The word your front door uses for 42 stopped being right today, and the loop cannot pick its replacement (by 2026-09-12)
- NH-054 [DECISION] Four fifths of your front door's headline is limits, and no lap is allowed to decide whether that is the project's strength or its biggest presentation risk (by 2026-09-13)
- NH-055 [DECISION] Your front door compares the headline forecast number to a model the same page calls broken, and today the loop measured a second, harsher comparison it is not allowed to put beside it (by 2026-09-13)
- NH-056 [DECISION] A Korean university published a reproducible deep-learning wildfire model four weeks before your finals, and a lap may not decide on its own whether to mention it (by 2026-09-13)
- NH-057 [DECISION] The one thing your project now says it contributes has never once been produced from a real fire, and only your laptop can change that (by 2026-09-13)
- NH-058 [DECISION] Two of your routines built the same row in the same window, and the rule that was supposed to stop that cannot see the paper routine (by 2026-09-13)
- NH-059 [DECISION] Your loop measured the hardest question a judge can ask you, and the answer is uncomfortable enough that no lap may decide on its own whether you say it out loud (by 2026-09-13)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
