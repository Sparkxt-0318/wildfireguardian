# WildfireGuardian autoloop · critic · 2026-09-06T1736Z

| | |
|---|---|
| when | 2026-09-06 17:36 UTC · 2026-09-07 02:36 KST |
| branch / head | `auto/dev` / `e95fe28` |
| kind | critic |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 7, done: 16, parked: 1, todo: 79 |

## What happened this lap

# Critic #28 — the honesty machinery is built for claims we retracted, and the two worst sentences here were only narrowed

**Reviewed head:** `e95fe28`. **Window:** `dd500e6..e95fe28` (critic #27's own two commits, the WFG-133 dev
lap, and the paper lap's §3.5 repair), plus the 24 h to 2026-09-05T17:41Z for the gate, CI and report checks.
**I changed no code, no test, no data, no figure and no `docs/NUMBERS.json`.**

## Your decisions, applied

**Nothing new this lap.** The Gmail search (`from:siyeong0318@gmail.com subject:"WildfireGuardian autoloop"
newer_than:14d`) returns a first page of 50 threads, newest first; every one was **sent by this loop** and
holds exactly one message, so no thread carries a reply — a reply would have bumped its thread to the top of
that ordering. `docs/auto/decisions_seen.json` is unchanged (`seen: []`; 23 applied, every one from a laptop
session). PR #31 has no comments at all. No `NH-###:` line reached the loop through either channel, so no
decision was applied and none is quoted here.

Still open and waiting on you: **NH-032**, **NH-034** (both dated 09-08), **NH-035** (09-09), **NH-036**,
**NH-037** (09-10), **NH-038** (new, 09-09), plus the older NH-005 and NH-014. NH-033 is FYI.

## What I verified rather than read

- `gates.py --mode full` **ALL GREEN** at `e95fe28`: `1599 passed, 62 skipped`, **cold**, 288.4 s. Against
  critic #27's cold `1569 / 62`: **+30 passed**, skips unchanged. `verify`, `snapshot-verify`, `env-check`
  PASS; `baseline-verify` WARN is CHARTER §3d information.
- **No red CI run behind a green report, so no CHARTER §4b finding.** Read through the GitHub MCP (`curl`
  returns 403 through the proxy, WFG-119): `auto-gates` runs **#154 to #178** on `auto/dev` are **21 <!-- forbidden-ok: 154 -->
  `success` and 4 `cancelled`** (`dfdf480`, `828bbae`, `ef61e9b`, `9ebf5a5`, each superseded by a later
  green push), **no `failure` at all**. Run 178 at `e95fe28`, this head, is `success`.
- `--assert-head` OK; `--assert-reported --base dd500e6` OK. Every **dev** report in the window carries
  `Reviewed by:`.
- The clone was **fully unshallowed** before anything about the commit graph was written:
  `git rev-parse --is-shallow-repository` answers `false`, 496 commits.

## Findings, ranked

### 1. The test suite downloads 25 MB from Amazon S3 mid-run, and the clean-clone claim is false (WFG-139, P0, new)

On a clone created at 16:57Z with no `data/raw/` at all, `gates.py --mode full` produced
`data/raw/dem/srtm/N36E129.hgt` (**25,934,402 bytes**) and its `.gz` at **17:02Z**, during `pytest-full`.
The caller is `tests/test_spread_warmup.py:156`, which passes `dem_source="srtm"` with **no skip guard**;
`load_dem` reaches `elevation-tiles-prod.s3.amazonaws.com` through
`src/wildfireguardian/data_io/raster.py:521` → `:282` → `:252`.

- **CHARTER §4b forbids exactly this**, in these words: 「No test may depend on the local clock, the
  timezone, the network, or files outside the repository.」 If S3 is unreachable the loader **raises**
  (`raster.py:306`) rather than skipping, so `auto-gates` goes red on an outage and the loop reads it as a
  regression.
- **`docs/clean_clone_gates.md:27` reads 「No network, no keys, no `.env`.」** That is false, and it is one of
  the three files `docs/auto/JUDGE_QA.md` **Q28 · T2** cites to a judge. Q28's card also tells the student
  「그 입력을 필요로 하는 테스트는 이유를 적은 채로 건너뜁니다」, true of the FIRMS/ERA5 tests and false of
  this one. **That is the same shape as WFG-133**, the item this window just cleared: a card that scripts
  the student to say something untrue about their own repository.
- **Six tests have never run in CI.** `tests/test_srtm_dem.py:81/:94/:109/:170`,
  `tests/test_validation_robustness.py:57` and `tests/test_validation_session3.py:171` are `skipif`-guarded
  on that tile; `skipif` is evaluated at **collection** and every CI job is a fresh clone, so the download
  always lands after the decision to skip. Those six are the terrain-plausibility checks on the DEM the
  router walks over: ocean clipped to 0 m, maximum elevation at least 400 m, east strip lower than west.
- **This is the whole of the cold/warm skip gap the loop has diagnosed twice and never chased.** Cold
  `1599 passed, 62 skipped`; warm `1605 passed, 56 skipped`; same commit `e95fe28`, both measured here, and
  the six-test delta is exactly this tile. The 0059Z dev lap wrote 「Six tests that skip on a cold clone
  (`data/cache/**` DEM and OSM caches) ... expected rather than a discrepancy to chase」; the 1638Z lap wrote
  「the two disagree by 6 and I did not chase it」. **Both named the wrong directory.** Critic laps quote cold
  counts and dev laps warm ones, so every 「+N passed」 across the two kinds is contaminated until this closes.

`docs/auto/JUDGE_QA.md` gains **Q40 · T1** for it, with the do-not-say lines.

### 2. The README asserts what the manuscript disclaims, about the number the project leads with (WFG-138, P0, new — my one `fix-before-next-row` item)

`README.md:22-26`, the TL;DR's 「Headline result」 bullet: 42 of 458 origins reach a refuge 「**only** when the
router accounts for where the fire **will be**」, with the 32.6 % coverage caveat and nothing else.

`paper/manuscript.md`'s Abstract carries the same two numbers and then says: 「That contrast is measured
against a fire-blind baseline, so it does not separate knowing where the fire will be from knowing where it
is.」 The baseline is fire-blind in this repository's own words
(`src/wildfireguardian/routing/evacuation.py:270`, `docs/real_roads_real_hazard.md:50`).

`paper/GAPS.md` G7 records that the abstract 「had the same overclaim ... and it was corrected this lap」, and
names the two surfaces the loop had already repaired for the identical claim: the booth script's 3막 sentence
(**WFG-103**, `92366cb`) and the finals template (**WFG-109**). **The README is the fourth surface and no row
has ever named it.** It is the first page a KCF judge, an ISEF reviewer or an IEEE reader opens, it is what
`CITATION.cff` points at, and its TL;DR advertises a withdrawn-claim record three bullets below the
unqualified sentence.

The fix is one additive clause, in the manuscript's own words. No number moves; no registered value changes
meaning. It is the 「Headline result」 bullet and **not** the opening paragraph about the 2025 fire, which
stays untouched.

### 3. The booth kit has no freshness gate, and it is staler than last lap (WFG-140, P0, new)

Critic #27 pre-registered this: 「If any is still stale, then R7's kit is a snapshot with no freshness gate
and WFG-134 is understated: file the gate as P0 rather than as part of the rebuild.」 I re-hashed all four
`SOURCES` in `manifest_20260906T0620Z.json` against the tree at `e95fe28`:

| source | manifest | tree | |
|---|---|---|---|
| `docs/auto/finals/BOOTH_SETUP.md` | `ef7342dacf…` | `ef7342dacf…` | match |
| `docs/auto/DEMO_SCRIPT_5MIN.md` | `b1aae78f35…` | `b1aae78f35…` | match |
| `docs/auto/JUDGE_QA.md` | `2c8451211e…` | **`7d5ac4c9c5…`** | **STALE** |
| `docs/auto/finals/DETECTION_FLOOR_CARD.md` | `84648d4d6e…` | `84648d4d6e…` | match |

The drift is **larger** than critic #27 measured (`af955a30fa…`), because the WFG-133 lap edited Q35 again at
`923ffbd`/`32de531`. So the printed 17 Q&A pages carry the pre-WFG-117 Q30 **and** Q35's ⚠ block with **no**
retraction note on it, that note having landed at `a64b904`, after the `3e92b69` build. The PDF's own
self-hash still matches, so `tests/test_printables.py` is fully green over a stale kit.

The gate has been a sub-clause of two rebuild rows for two windows and both rebuilds were displaced. **A gate
that ships only if a rebuild ships is not a gate**, so it is now its own row. Grade it red on today's tree,
before the rebuild makes it green.

### 4. The mechanism that finds these has taken the last three dev laps (NH-038, DECISION, MEDIUM, new)

| dev lap | row built | where it came from |
|---|---|---|
| 0711Z | WFG-007 (booth printables) | table order |
| 1000Z | WFG-113 | critic #25's one item |
| 1313Z | WFG-117 | critic #26's one item |
| 1638Z | WFG-133 | critic #27's one item |

Three consecutive dev laps, three critic items, all three defects in documents the loop itself wrote, every
one worth fixing. Meanwhile nothing has finished the booth kit since `3e92b69` at 06:20Z, and
`docs/auto/KCF_READINESS.md` has read **4 of 11 for five consecutive critic laps**. The sprint plan names
09-11 for the printables and 09-10 for the bundle.

The cap of one item per critic lap is also a **floor** of one, and it caps the number of items rather than
their cost or how many laps in a row may carry one. I kept my item to one clause on purpose, and I am asking
rather than widening the rule myself, because §14b is your steer. Four options on the entry.

### 5. Three routines file their reports under one `kind` (WFG-141, P1, and it waits)

`scripts/auto/report.py:227` offers `--kind` a closed list with no `paper` and no `ci-red`, so the paper
routine, the ci-red routine and your own laptop sessions all land as `manual`. Of 33 `*-manual.md` reports,
**12** are titled 「autoloop · paper」 and **2** 「autoloop · ci-red」. Critics #26 and #27 each wrote that the
window's reports lacking `Reviewed by:` are 「`manual`, the author's own laptop」 and treated that as a reason
it is not a finding; twelve of them are the paper routine, which does run a subagent reviewer and is held to
the same discipline (CHARTER §12, NH-026). No report has actually escaped, so this is a **wrong reason**
rather than a missed defect — filed because the reason is what the next critic reuses. Second defect on the
same files: the stamp in the title disagrees with the stamp in the filename by up to 40 minutes
(`2026-09-06T1507Z-manual.md` is titled `1547Z`), and the **email subject uses the title stamp**, so you
cannot find in the repository the report you were sent. CHARTER §14b holds this behind R1, R3, R4, R7, R8, R9.

## The root objection

**The loop's honesty machinery is built for claims it has retracted, and the two worst sentences in this
repository today were never retracted — they were narrowed in one file and left standing in another.**

`WC-004`, `check_withdrawn_claims.py`'s 925-file sweep and critic #27's new grep rule all key on a
**withdrawn string**. `README.md:22-26` has no withdrawn string: the manuscript qualified the claim and
`paper/GAPS.md` G7 wrote down that it had, and nothing carried it across. `docs/clean_clone_gates.md:27` has
no withdrawn string either, because nobody ever measured it. Last window's mechanism, which is a good
mechanism, cannot see either one.

**The cheapest test**, and it is now a standing rule on `DIRECTION.md`: when a lap narrows a claim in one
file, it names every other file that states the unnarrowed version, in that lap, in writing. For
WFG-138 that costs one clause.

**Critic #27's falsifiable test, both branches, answered.** (1) Q35's ⚠ block no longer tells the student
「지금 브랜치에서 닿지 않습니다」; it is retracted in place with a dated note and the measured table, and
`WC-004` now turns `make verify` red if the sentence reappears anywhere in 925 files. So a
`fix-before-next-row` item **can** survive a previous critic's prohibition, and the way it survived was to
stop depending on a lap choosing the right document. (2) The manifest is still stale and staler, so the
pre-registered branch fires and the gate is filed separately as WFG-140.

**The falsifiable test for critic #29.** (1) Delete `data/raw/dem/srtm/` and run the full suite with no
network: green at `1599 / 62` means WFG-139 closed properly; red means the fix guarded the wrong test.
(2) If `README.md:22-26` still carries no fire-blind caveat at the next critic head, then a
`fix-before-next-row` item costing one clause is not cheap enough to survive a window, and the finding is
about the dev lap's step 3 rather than about the row.

## `factchk` on the window's new prose

**No new claim about the world was made in this window**, so there is nothing external to check. Every
assertion added is about this tree, and I re-derived the ones that matter: `check_withdrawn_claims.py` reports
`4 claims over 925 gated files, PASSED`; `41498ef` is an ancestor of `HEAD` on the unshallowed clone; the four
manifest hashes above; the cold and warm pass counts. The one claim about the outside world in the dev
report — that the Korean object particle `문장을` marks the object of any verb, permission included, so a gate
keyed on it fires on true and false sentences alike — is correct, and it is the reason the reviewer's block
was right.

⚠ One small correction to my own predecessors, offered as a note and not filed as a row: critic #27 appended
its `dd500e6` row to the combined series table in `docs/auto/SCORECARD.md` but not to the two track tables.
I appended to all three.

## Scorecard, `e95fe28`

**Track B 84 → 82** (연구 목적 17, 설계와 방법론 15, 데이터 19→**18**, 창의성 15, 제출 자료 18→**17**).
**Track A 78 → 77** (개발 목적 16, 설계와 방법론 15, 구현 및 유용성 18, 창의성 15, 제출 자료 14→**13**).

Four rows fall and **none of them is a regression.** The clean-clone reproducibility claim and the README's
headline attribution were both wrong before this window and were scored without them by every earlier lap,
mine included. The tree did not get worse; the estimate was wrong. The credit in the same window is real and
is why 데이터 falls one point and not two: WFG-133 registered the withdrawn claim as `WC-004` over 925 files,
the independent reviewer caught the first gate pointing backwards (3/3 true sentences wrongly flagged, 1/6
reworded false ones caught), and the fix is graded against the reviewer's own nine sentences, committed so
the score re-derives every lap — 0/5 and 6/6. That is the best instrument-grading in this repository.

## In plain terms

- **What the judges would see today.** A strong project with one sentence on its front page that its own
  research paper says is too strong. The README says 42 households can only reach safety if the router knows
  where the fire *will be*; the paper says the comparison was against a router that does not look at the fire
  *at all*, so the claim cannot separate 「will be」 from 「is」. The paper is right and the README was simply
  never updated. Separately, the printed booth pack in your hands is now two corrections out of date.
- **What changed since yesterday.** The loop fixed the false sentence on your Q&A card and, better, built the
  reason it had been missed: your repository now has a registry of retracted claims that is checked against
  925 documents on every push. My finding is that the same registry cannot catch today's two worst sentences,
  because those were softened rather than retracted, so there is no banned wording to search for. I also
  found that your test suite quietly downloads a 25 MB map tile from Amazon every time it runs on a fresh
  machine, which means six terrain checks have never once run in your automatic testing.
- **What you should do.** Nothing urgent tonight. One new question is yours: **NH-038**. The rule you set on
  09-04, that a critic finding on a judge-facing surface jumps the queue, has now taken three dev laps in a
  row, and your booth pack has not been finished since it was built at 06:20 UTC this morning. I think the rule is right and the
  *cost* of an item is what needs a limit, but that is your call, and I have kept my own item to a single
  clause so it costs you almost nothing either way. NH-032 and NH-034 are still the two that hold the most
  work.



## Gates

**ALL GREEN** · mode `full` · head `e95fe28` · 2026-09-06T17:04:15Z · current at `e95fe28`

⚠ **Annotation added after the report was generated (CHARTER §4 step 8).** This report reviews `e95fe28` and the table below is that run. It **ships in** `050731a`, on which `gates.py --mode full` was re-run and is **ALL GREEN**, with `--assert-head` and `--assert-reported` green immediately before the push; all three ids resolve. ⚠⚠ **The re-run reports `1605 passed, 56 skipped` where the table below reports `1599 passed, 62 skipped`, on trees that differ only in this lap's prose.** That is finding 1 of this report happening to this report: the first run of the suite in a sandbox downloads the SRTM tile, and every run after it in the same sandbox therefore runs six more tests. The **cold** counts are the ones GitHub's clean clone will report and the ones this lap's prose quotes; the warm counts belong to the second run. **WFG-139.**

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 18.6 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.1 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 1.6 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 288.4 s | 1599 passed, 62 skipped, 52 warnings in 286.91s (0:04:46) |

## Commits since the previous report (e95fe286ed975d53eca5019b2a9bcb010f33a890..HEAD)

(no new commits)

## Needs a human (11 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09)
- NH-036 [DECISION] One critic lap told the next one not to edit a file, and that is what kept a false sentence in front of a judge for a window (by 2026-09-10)
- NH-037 [DECISION] The paper's word proxy now stops it a thousand words before your 25-page rule (by 2026-09-10)
- NH-038 [DECISION] Your "product first" rule has spent the last three dev laps on documents, and the readiness line it was written to protect has not moved in five critic laps (by 2026-09-09)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
