# WildfireGuardian autoloop · paper · 2026-09-05T1533Z

| | |
|---|---|
| when | 2026-09-05 15:33 UTC · 2026-09-06 00:33 KST |
| branch / head | `auto/dev` / `38620f2` |
| kind | manual |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 6, done: 16, parked: 1, todo: 66 |

## What happened this lap

# Paper lap 8 — the page count exists, and the gate now checks the rule the author actually set

**Head incorporated:** `38620f2` (the lap started at `8a8a940` and rebased mid-run).
**Manuscript prose: unchanged this lap, deliberately.** Body 7,461 words, 8 figures,
4 tables, 27 references, 6 open gaps — identical to lap 7. What changed is the length
rule's enforcement, and the reason is below.

## What the code window contained, and why none of it reached the manuscript

Since `4d1a596` the code moved on sixteen paths: the finals USB bundle
(`scripts/check_bundle_copy.py`, `tests/test_check_bundle_copy.py`,
`tests/test_booth_setup.py`, `release/kcf-finals-2026/*`, `docs/finals_bundle.md`,
`Makefile`), the demo-script pace re-measure (`data/processed/demo_script_pace/
pace_20260905T0947Z.json`, two new registry keys, `docs/demo_script_pace.md`), the
baseline re-freeze (`docs/baseline_phase13.json`), and one sentence on the judged
screen (`web/finals.html`).

**None of it changes a claim in the paper, and this lap wrote no prose because of it.**
The two new registry entries are syllable counts for the Korean booth script — booth
material, not results. The bundle checker and the booth recipe are competition logistics;
§6's Operational status and the Data and code availability section are already correct
about them and would only be padded by naming them. The one item that *is* scientifically
adjacent — `web/finals.html` replacing 「지금 이 순간만 보는 지도」 with a sentence saying
the STATIC VIEW arm is fire-blind — is the same correction the manuscript's abstract and
§4.3 already shipped in lap 7 (G7). The screen has now caught up with the paper, not the
other way round. Reporting that as "nothing to incorporate" is the honest read (CHARTER §7).

## What this lap did instead: measured the thing three laps could only estimate

`paper/GAPS.md` has carried, since lap 2, a block saying no page count had ever been
produced for the built document, and NH-028 told the author that one open-and-look on
their laptop was the cheapest way to settle it. **It was not: it was one apt install.**

**The diagnosis that was one step short.** Lap 2 recorded that LibreOffice "refuses to load
the built document"; lap 3 correctly narrowed that to "it refuses a two-paragraph `.docx`
written by the same `python-docx` in the same environment, so it says nothing about our
file". Both true. The cause is that the image ships `libreoffice-core` **without**
`libreoffice-writer`, so there is no text-document import filter at all and every
word-processor format fails identically with `source file could not be loaded`. Confirmed
by listing `/usr/lib/libreoffice/program/` (no `swriter`, no `libswlo.so`), then by
installing the package, after which the same command converted the same file in 3.9 s.

**The measurement: 21 pages, under Carlito.** 21 page objects against a page-tree `/Count`
of 21. `pypdf` 6.17.0 was pip-installed once as a third check, agreed, and was removed —
it is in neither the pins nor the venv, so that cross-check is a note, not something a
fresh clone re-derives.

⚠ **The face is part of the number, and the first version of this lap's gate ignored that.**
Same file, same renderer, varying only which faces fontconfig may see: **Carlito 21 pages,
DejaVu Sans 23.** `build_docx.py` asks for Calibri, which is not redistributable; only a
metric-compatible stand-in makes the count a statement about the document. The gate now
gates only on Carlito or real Calibri and otherwise reports and falls back to the word
budget — failing on a DejaVu render would reject a document that is inside the author's
rule in Word.

**New: `paper/measure_pages.py`** (stdlib only, so no declared dependency; runs on the
author's laptop). Renders, counts two independent ways, **refuses to print a number when
the two disagree**, exits 2 rather than 1 where the renderer is missing, and names the face
Calibri resolved to instead of printing a bare integer. `--why` prints the one-line install.
**And `paper/calibrate_pages.py`**, which regenerates the curve below, so it is re-derivable
rather than seven numbers a lap once printed.

**The words-to-pages curve — and the control this lap first got wrong.** Filler is the
manuscript's own paragraphs recycled; figures, tables and references held fixed; the only
variable is where the words land:

| body words | 7,461 | 7,961 | 8,561 | 8,961 | 9,461 | 9,961 | 10,461 |
|---|---|---|---|---|---|---|---|
| appended after the last figure | 21 | 21 | 22 | 23 | 24 | 24 | **25** |
| spliced into §4, among the figures | 21 | **22** | **23** | 23 | 24 | **25** | **25** |

The lap first measured only the tail row, controlled it with two fillers of different
vocabulary (mean word length 4.87 and 5.13), found the counts identical at all seven points,
and wrote that the conversion was "a property of the template, not of the words poured into
it". **The reviewer answered that this varied the thing that cannot matter and held fixed
the thing that does**, and the re-run agreed: same words, up to a page more. That sentence
is withdrawn in `GAPS.md` and recorded there as what it was — a control on the wrong
variable, which is precisely the failure mode §3.5 of the manuscript exists to warn about,
committed by the file that describes it.

What the sampling supports: at the proxy's own 9,000-word limit the document is 23 pages by
either route, two of margin, so the proxy is sound; the ceiling arrives between 9,961 words
(among the figures) and 10,461 (at the end). Step 500 words, and **no count above 25 was
ever measured**, so the ceiling is bracketed rather than located. `paper/README.md`'s old
gloss of "≈ 16 pages at 7,000 words" was simply wrong; lap 3's crude recount of "nearer 21"
was right on the integer and wrong on the method.

## The author answered NH-028 mid-lap, and the gate did not yet enforce the answer

The lap started at `8a8a940`; `git pull --rebase` brought in `9442430`, four author
decisions from the laptop channel. On NH-028, verbatim: **「Don't worry about the word
count for now. Just make sure it doesn't exceed. 25 pages for. now」**. That session wrote
25 pages into CHARTER §12, the 8,500 / 9,000-word proxy into `LOOP_CONFIG.json`, and
raised `paper/check_paper.py`'s `LIMIT` to 9,000 with the note 「7,479 words built to about
21 pages」 — an estimate, which this lap's measurement puts at exactly 21.

**But the gate still checked only words, and the author's rule is pages.** Those are not
the same test, and the gap is not academic: §4 is eight of the 21 pages because it carries
six of the eight figures, so **a new figure costs a page and not one word**. A
word-only gate would pass a document that broke the author's rule.

So `check_paper.py` now renders the document it has just built and fails above 25 pages,
keeping 9,000 words as the proxy everywhere else. Five branches, each exercised by hand:

- `measure_pages` missing → reported, passes. The import is **lazy** for this reason: a
  module-scope import of a sibling a lap forgot to stage would make every push
  ImportError-red through `tests/test_paper.py`. The reviewer called this the
  near-certain red and it was;
- renderer absent → reported, passes; the word budget covers those machines;
- renderer present but broken → reported, **passes** — deliberate, so a flaky converter
  cannot turn a push red. An earlier draft of the docstring claimed the opposite of code
  that failed on nothing; the docstring now says what the code does;
- face not metric-compatible → count reported with the face named, **not gated**;
- metric-compatible and over 25 → fails, naming the rule and why the words missed it.

⚠ **None of the five has a committed test**, because `tests/` is outside CHARTER §12. A
fixture-driven test is a dev-lap item and until it lands the branch that can fail a push is
untested. Also fixed after review: `render_pdf`'s timeout was 600 s inside a test whose own
subprocess timeout is 300, which would have turned "the renderer hung" into a test ERROR
instead of the reported-and-passed path the design promises; it is now 90.

This is the one judgement call in the lap and it is recorded as such: a lap changing its
own gate's threshold would be the move CHARTER §3 exists to stop. What it is instead is
the paper routine implementing a decision the author had already made and a laptop session
had already written into CHARTER and `LOOP_CONFIG.json`, in the one file (`paper/`) the
routine owns, where the gate had been left contradicting it. No threshold was invented:
25 is the author's, 9,000 is theirs too. The reviewer examined this and did not block on
it, for those reasons plus the observation that the change only ever tightens.

## Also found, not the paper's to fix

**Two test failures that were an artifact of the shallow clone, not of the tree.** The
first `gates.py --mode full` run failed `tests/test_finals_screen.py::
test_the_integrity_panel_names_a_commit_this_repository_has` and
`::test_the_escape_this_gate_cannot_close_is_still_open`: `web/finals.html` names commit
`d5e2562`, and `git cat-file -e` could not find it. The cloud clone arrives **shallow** (50
commits), and the stamp is from 2026-09-04 15:27, outside that depth. After
`git fetch --unshallow` the object resolves and both tests pass — the run after it was
`1512 passed, 56 skipped` against `1504 passed, 62 skipped` before. The test's
`_needs_git_history()` guard does not detect a clone deep enough to look complete but too
shallow to hold the stamp, so **any** cloud lap can hit this and read it as a real orphaned
stamp. That is a `tests/` fix and outside CHARTER §12, so it is reported here rather than
made.

## Figures

`make_figures.py` re-ran; all eight redrew **byte-identically** (no PNG appears in
`git status`), which also says the Carlito and Nanum installs did not perturb matplotlib's
font resolution. No figure was added or changed, so nothing needed re-inspecting; F5 and
F8, the two that carry the headline §4.3 contrast, were read anyway and are clean —
legends outside the data, no clipped labels, the 414/44 and 456/2 splits agreeing with the
text.

## Gaps

No gap opened or closed. G2, G3, G4, G5, G6, G7 stand as lap 7 left them; the six
`[GAP]` markers and six `GAPS.md` rows still match. What did change in `GAPS.md` is that
the four-lap length-pressure ledger is now marked as history rather than instruction: the
trimming regime it describes ended with the author's answer, and the block that said "no
page number is asserted here" carries the measurement inline against each clause it got
wrong.

## Reviewer

**Reviewed by: subagent (block): the lap treated the page count as a property of the
document when it is a property of (this .docx, this renderer, this font substitution, this
insertion point) — and wrote seven unregistered pairs into five committed files on the
strength of it.** The block was upheld on both empirical halves after this lap reproduced
them independently rather than taking them on trust:

- **Font.** Carlito 21 pages, DejaVu Sans 23, identical file and renderer. `check_paper.py`
  computed the substituted face, printed it, and failed hard on the number anyway.
  Fixed: it gates only on a metric-compatible face.
- **Insertion point.** The curve was built by appending filler after every figure. Spliced
  into §4 instead: 7,961 words → 22 where the tail row says 21, 8,561 → 23 where it says
  22. The two-filler control varied vocabulary and held the insertion point fixed — a
  control on the wrong variable. Fixed: both rows published, tail labelled a lower bound,
  the "property of the template" sentence withdrawn in writing.
- **Re-derivability.** The calibration lived in a scratch heredoc and nothing was committed.
  Fixed: `paper/calibrate_pages.py` regenerates both rows and refuses to report on a
  non-metric-compatible face.
- **Fabricated machine fact.** Five files asserted that the Actions runner "ships
  `libreoffice-core` without `libreoffice-writer`" — a machine this lap never inspected.
  Removed; the text now claims only what was measured here and says the gate skips wherever
  the renderer is absent, whatever the reason. This one was a CHARTER §3 rule 5 violation
  and the reviewer was right to name it as such.
- **Stale-STATE regression.** `built_pages` was written into `STATE.json` where no gate
  compared it — recreating the exact staleness class the previous lap's drift check was
  added to kill, on the one number a new figure changes. Fixed: it is drift-checked
  whenever a trustworthy count was produced.
- **Docstring/behaviour mismatch and a nested timeout**, both above.

**Where the reviewer was itself wrong, checked rather than accepted:** it argued the
9,000-word proxy has "one page of margin or less, not two", from a single in-§4 point at
8,549 words reaching 23. Reaching 23 sooner is not the same quantity as the margin at the
proxy's limit; measured at 9,000 words both insertion points give 23 against a 25-page
ceiling, so two pages of margin is correct and stands. Its remaining unaddressed items —
a committed test for the five branches, and registering the page count as a registry key —
are outside `paper/` and are named in this report as dev-lap work rather than done here.

## Gates

`gates.py --mode full` exits 0 at the pushed head: `verify` PASS, `snapshot-verify` PASS,
`env-check` PASS, `pytest-full` PASS (1512 passed, 56 skipped, 206 s).
`baseline-verify` WARN is the pre-existing stale freeze, open as **NH-029** and not this
lap's. `check_paper.py` exits 0 and now prints `pages {"pages": 21, "calibri_substitute":
"Carlito"}`.

## In plain terms

**What the paper now says.** Exactly what it said this morning. Not one word of the
manuscript changed: the six hours of code since the last paper lap were the booth USB
bundle, the demo-script timing and one screen sentence, and none of it changes a claim in
the paper. What did change is underneath it — your 25-page rule is now measured (the
document is 21 pages) and actually checked by the gate, instead of being stood in for by a
word count that cannot see figures.

**What is still missing.** The same six gaps, unchanged, and the biggest is still that the
paper grades its headline routing result against the field the model predicted rather than
against where the fire actually went. Your NH-027 decision to run the fair-opponent arm in
the sprint is the thing that moves it. Smaller and new: the page check has no test yet, and
that needs a dev lap because the paper routine may not touch `tests/`.

**What you should do.** One minute if you want it: open the `.docx` in Word and confirm it
says 21 pages, because the loop measured it with a metric-compatible stand-in for Calibri
rather than with Word itself — and the font genuinely moves the answer (23 with the wrong
one). Otherwise nothing. The length pressure that squeezed the last four laps is over: the
next lap can add a caveat instead of trading one away.



## Gates

**ALL GREEN** · mode `full` · head `38620f2` · 2026-09-05T15:33:48Z · current at `38620f2`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 13.6 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.1 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 0.3 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 197.8 s | 1512 passed, 56 skipped, 52 warnings in 196.74s (0:03:16) |

## Commits since the previous report (944243054a590dd80ca08af4d89716a1fb4ba6fd..HEAD)

- 38620f2 bundle plan = git-tracked files only (a local demo video broke the manifest on the laptop); baseline re-frozen after three cloud-lap artifacts

## Needs a human (5 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-029 [DECISION] The baseline freeze is stale, so `make all-checks` cannot pass — on this machine or on yours (by 2026-09-10)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
