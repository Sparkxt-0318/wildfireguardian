# WildfireGuardian autoloop · critic · 2026-09-09T2319Z

| | |
|---|---|
| when | 2026-09-09 23:19 UTC · 2026-09-10 08:19 KST |
| branch / head | `auto/dev` / `3eec471` |
| kind | critic |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 9, done: 33, dropped: 1, parked: 1, title-derivable: 1, todo: 109 |

## What happened this lap

# Critic #54 — 2026-09-09T2319Z, reviewed head `3eec471`

Window: `ba06467..3eec471` (the last 24 h on `auto/dev`) — one critic lap's own findings, one paper lap, one
dev lap (WFG-212), and three report-machinery commits. This clone is **shallow at 50 commits**
(`--is-shallow-repository` = `true`, measured here); no ancestry or reachability claim appears anywhere in
this report, per CHARTER §4.

## Your decisions, applied

**Nothing new on either channel.** The Gmail search
`from:siyeong0318@gmail.com subject:"WildfireGuardian autoloop" newer_than:14d` was read over **two pages, 75
threads**, covering every report from 2026-09-06T0725Z to this evening's 2206Z dev report. **Every one of the
75 holds exactly one message** — the loop's own outbound report — so no thread carries a reply, and a reply
would appear as a second message. `scripts/auto/decisions.py parse` therefore had nothing to read and
`docs/auto/decisions_seen.json` is unchanged (`seen: []`, 23 applied, all from the 2026-09-04 Claude Code
sessions). ⚠ What I did **not** re-check: the ~126 older threads inside the 14-day window, which every critic
lap since #22 has read and which `decisions_seen.json`'s empty `seen` list independently confirms carry no
applied email decision. PR **#31** has **zero** issue comments, read through the GitHub MCP. **25 NEEDS_HUMAN entries are open**, of which **NH-032 and NH-034 are
two days past their stated date**, and **NH-036, NH-037, NH-042, NH-046 and NH-050 come due tomorrow,
2026-09-10**.

## Findings, ranked

### F1 · ROOT OBJECTION · new row WFG-222, P0, end of the P0 block

**The lap that corrected the front door's one affirmative claim left the identical claim, in the identical
words, on the four surfaces that carry it by design — including the screen five judges stand in front of and a
T0 card the student says out loud from memory — and the gate it wrote to prevent that reads only the paragraph
it fixed.**

WFG-212's independent reviewer broke the lead block's first draft on a **conjunction**: two artifacts named
side by side are a claim about their conjunction, and the committed dispatch sheets came from a run whose
hazard surface is **synthetic**, while the run where the walk graph and the spread surface are both real
produces no dispatch documents. The lap accepted it. `README.md:212-251` now says 「지점 단위」, names what was
real and what was synthetic, and states 「실제 확산면으로 만든 출동 지시서는 아직 없습니다」.

I re-derived the premise rather than reading it. `data/processed/rescue_routing.json` → `provenance.sources`:
`walk_network: osm`, `drive_network: osm`, `shelters: osm`, `depots: osm`, **`hazard: synthetic`**,
**`terrain: synthetic`**, **`origins: sampled candidates`**. `outputs/dispatch/README.md`: the clusters are
DBSCAN groupings at eps = 500 m rather than 행정리, and the committed sheets cover 44 of the artifact's 143
dispatch points. Both hold.

The same claim, **uncorrected**, at `3eec471`:

| surface | what it says | bound |
|---|---|---|
| `README.md:391-395` (§5 item ①) | 「**가구 단위**의 …판정과 그 도보 경로」 + 「그 산출물의 실물이 저장소에 커밋돼 있습니다」 | **none**, 180 lines below the block that denies it, in the same file |
| `web/finals.html:1580` + `scripts/finals.template.html` | 「가구 단위의 대피 판정」 / "a **per-household** evacuation verdict" + 「그 실물이 저장소에 커밋돼 있습니다」 | **none** |
| `docs/auto/JUDGE_QA.md:1172` (Q29a, **T0**) | 「**가구 단위** 구조 순서와 보행 경로가 붙습니다」 + 「실물을 열어 드릴 수 있습니다」 | 행정리 only |
| `docs/auto/DEMO_SCRIPT_5MIN.md:67` | 「지도가 아니라 **가구 단위 판정과 걸어 나갈 길**입니다」 | **none** |
| `docs/creativity_card.md` | "per-household", printed in `WFG_printables_20260909T1908Z.pdf` | **none** |

Counts, unpiped: 「가구 단위」 is 1 / 1 / 1 / 5 / 1 across `README.md`, `web/finals.html`,
`scripts/finals.template.html`, `docs/auto/JUDGE_QA.md`, `docs/auto/DEMO_SCRIPT_5MIN.md`. 「지점 단위」 is
**1 in the whole repository** and it is the line added at 2206Z.

Three things make this more than stale wording.

1. **On three of the five, item ① sits immediately beside item ②** — 「두 축이 동시에 실제인 실행」,
   `docs/real_roads_real_hazard.md` — the exact adjacency the reviewer named. Read together they say
   「real roads + real fire → these committed sheets」, and that execution does not exist here.
2. **The loop built a consistency mechanism and the correction landed outside it.**
   `web/finals.html:1574-1576` carries the source comment 「the Q29a card, the booth script and this screen must
   make the same three claims against the same three files」. Three surfaces were deliberately bound to each
   other; the README was bound to none of them, and the README is the one that was fixed.
3. **The gate written this lap to stop exactly this cannot see it.** `tests/test_readme_round4_lead.py` scopes
   `test_the_lead_block_carries_its_own_bounds` to its `lead` fixture (Round-4 heading → 「### 1.」), while its
   `items` fixture (「### 1.」 → section end, which **contains** §5) asserts only caveat-fragment presence, a ⚠
   floor of 10, and the dispatch-ordering hedge. **No assertion binds §5 item ① to the lead's bounds.** The
   section can contradict itself on its own headline claim with the whole suite green. No mutation was needed
   to establish this; it is the file's own fixture boundaries. That is the `_UPPER_BOUND` anti-pattern
   `docs/auto/MEMO.md` named yesterday, committed again by the lap that wrote the MEMO paragraph about it.

⚠ **The claim itself is not withdrawn and this row does not withdraw it.** The output object is real and it is
this project's strongest card. What is missing is its bound. The row **adds** bounds and removes no caveat and
no link. Filed at the **end of the P0 block**, not position 1, per NH-051 and NH-038 option B as
`NEEDS_HUMAN.md:2016-2019` words it.

### F2 · the one `fix-before-next-row` item · three lines in the Q&A bank, minutes

**`docs/auto/JUDGE_QA.md:609`, `:1159` and `:1377` tell the student an answer from the 운영사무국 is still
awaited. The author closed that on 2026-09-04, five days ago, and one of the three is not merely stale but
false.**

- **:1159**, inside Q29's 「없는 것」 block — Q29 is **T0**, `JUDGE_QA.md:1385` lists it among the nineteen
  spoken 「자기 문장으로, 종이 없이」 — reads 「…어떤 형식으로 공시하기를 요구하는지는 **아직 사무국 답을 못
  받았습니다** (NH-008, 백로그 WFG-022)」. `NEEDS_HUMAN.md:135` carries NH-008 as **closed 2026-09-04**, its
  resolution records that the organisers addressed the disclosure question and no artifact is required, CHARTER
  §9 says the same, and `docs/auto/finals/TIMELINE_ROLES.md:85` says the same **in the same printed kit**.
- **:609** holds a spoken sentence back until 「**WFG-022(사무국 답변)가 오기 전에는**」. WFG-022 is
  `dropped(NH-008 closed 2026-09-04 by the author: no contact with the 운영사무국 will be made)`. The release
  condition **cannot occur**, so the student is permanently held to a fallback sentence by an instruction whose
  premise the author retired.
- **:1377** is the sharpest: 「사무국에 **질의한** 항목이 NH-008입니다」 tells a judge a query was submitted.
  NH-008's own resolution says 「No contact with the 운영사무국 will be made」. That is a false statement about
  what this team did, on a surface printed in the booth kit, and CHARTER §3 rule 5 is the rule it breaks.

Three prose passages, no artifact, no number, no screen rebuild. The Q&A bank is named in CHARTER §14b's own
list. The printables rebuild any lap touching a printed source pays rides with it as usual; if the same lap
also takes WFG-222 — same file — do both and rebuild the kit **once**.

### F3 · NH-037 is now zero-margin, measured · existing entry updated, no new question

`paper/check_paper.py` at this head prints `body_words: 9000` against `LIMIT = 9000` (`check_paper.py:75`),
enforced as `> LIMIT` (`:194`). Exit 0, headroom **zero words** — not the 55 the entry was filed with, and not
the 1 the 2126Z paper lap reported: that lap spent its last word on WFG-214's `docs/oracle_gap.md` link, which
was the right call. **The next mandatory correction the paper routine meets cannot be made at all** — not by
tightening, because nothing is left, and not by trading a caveat, because CHARTER §3 rule 5 forbids it. NH-037
is open and due **2026-09-10**. Recorded on the entry; no new entry, because the question is already right.

### F4 · WFG-214 was finished and still read `todo` at table position 1 · closed on the measurement

Critic #53 re-measured WFG-214 down to one item: 「one link in `paper/manuscript.md`」. Paper lap 25
(`4f3bd85`, 2126Z) added it at `manuscript.md:511`; `grep -c oracle_gap paper/manuscript.md` answers **1**
here. The paper routine may not touch `docs/auto/` beyond its own report (CHARTER §12), so no lap could close
the row, and the next dev lap would have claimed the top of the table and found nothing in it, six sprint days
out. Closed as `done(4f3bd85)`. ⚠ A **status update on a measurement, not a §3b reorder**: no row moved
position, and the bound clause NH-053 owns is untouched word for word.

### F5 · readiness: ZERO ticked in 24 h, for the ELEVENTH consecutive critic lap

8 of 11, unchanged since critic #43 ticked R8 at 2026-09-08T1429Z. Re-read rather than restated: R12 is the
author's (NH-014); R3 is `blocked(NH-046)`; R11's row WFG-024 is held by CHARTER §14b as loop hygiene until R1,
R3, R4, R7, R8 and R9 all tick, of which **R3 is the only one unticked**. Both agent-reachable lines are
downstream of **one unanswered question, NH-046, due 2026-09-10 — tomorrow**. No fourteenth question is filed;
filing one would be the loop asking itself. ⚠ F1 is **not** a readiness line and I checked each line's own
criterion before saying so — R1 asks about numbers mapping to registry keys, R5 asks that T0 answers cite files
and carry no **purged** phrasing, and 「가구 단위」 is an **unregistered** overclaim, which is exactly why
CHARTER §5c's ratchet could not catch it. No tick is removed.

### F6 · no red gate, and no finding #1 under CHARTER §4b

`gates.py --mode full` exits **0** at `3eec471` (1871 passed, 64 skipped, 3 xfailed, pytest 333.2 s).
`baseline-verify` WARNs on the two git-ignored `data/raw/**` contracts, which is NH-029 and §3d working as the
author chose. `--assert-head` exits 0; `--assert-reported --base ba06467` exits 0. GitHub `auto-gates`, the
full window (runs **262 to 305**, read through the GitHub MCP because `curl` against `api.github.com` is 403 in
this sandbox, WFG-119): **no `failure` anywhere**; three `cancelled` (275, 278, 284), each superseded by the
next push; run **305** is `success` at this exact head. Run 260 (`failure`, `7eeccab`, 2026-09-08T21:47Z) is
**outside** this window and was closed the same evening by `wfg-autoloop-ci-red`. Every dev report of the last
24 h records `Reviewed by:`.

### F7 · `factchk` on the window's new prose: nothing to correct

The window adds **no new external URL and no new citation**. Every URL in an added line is either this
repository's own clone address or a quotation inside a record page. The only new claims about the world are
claims about this repository's own committed artifacts, and the two load-bearing ones are re-derived in F1.
`paper/manuscript.md` moved by one clause and one link, and the bound clause 「this project's own model is worth
less」 is unchanged word for word, which is correct while NH-053 is open.

### The judge drill · ten questions, one with no evidence

Answered from a file, each opened: 「어떤 일정으로 만들었습니까?」 (`docs/auto/finals/TIMELINE_ROLES.md`),
「42가 무슨 뜻입니까?」 (`docs/oracle_gap.md`), 「가구 위치는 어디서 얻었습니까?」 (`README.md:235-238` +
`outputs/dispatch/README.md`), 「개인정보는?」 (Q20a), 「누가 책임집니까?」 (Q16b), 「저희 군이 도입하려면?」
(Q16c), 「완충거리 폭은 어떻게 골랐습니까?」 (README §1), 「탐지 바닥은?」
(`docs/auto/finals/DETECTION_FLOOR_CARD.md`), 「다른 지역에도 됩니까?」 (README lead, which declines the claim).

**No evidence on the surfaces a judge reaches:** 「이 출동 지시서, 진짜 불로 만든 겁니까?」 — answered on
`README.md` since 2206Z and nowhere else. That is F1, and it is why WFG-222 exists.

⚠ **No card was added to the bank and that is not an oversight.** WFG-216 (P1) records that the bank's answered
cards and its open questions share one id namespace and the open questions have consumed the next five ids, and
NH-049 is the author's open question about it. The drill's one gap is filed as a backlog row instead.

## Scorecard deltas

**Track B 94 → 93. Track A 93 → 92. One row moves on each and it is the same row, 제출 자료, DOWN**
(19 → 18 and 17 → 16). Full reasoning in `docs/auto/SCORECARD.md` at `3eec471`.

The raise is real and is not enough: `README.md:212-251` opens the section with what the project claims before
what Round 4 attacked, which is precisely the defect critic #50 measured, written as an existence claim with
its bounds in the same block, removing no caveat, with a **stricter** reading note than the one it replaced.
The deduction is critic #51's own, re-applied: the repository again answers its headline claim two ways, this
time inside one file, and the printed kit and the finals screen carry **only** the unbounded answer while the
bundle contains no `README.md` at all. **Pre-registered for critic #55:** 제출 자료 returns to 19 / 17 when
WFG-222 closes on all five surfaces **with the kit rebuilt and the manifest re-pointed** — not on the README
alone, and not on a report that says it was.

**연구 목적 18, 설계와 방법론 19, 데이터 20, 창의성 18** held on Track B; **개발 목적 19, 설계와 방법론 19,
구현 및 유용성 20, 창의성 18** held on Track A. Nothing in the window touched a model, a split, a metric, an
arm, a coupling or a protocol. 창의성 is held deliberately at 18 although the defect sits on the 창의성 answer:
what is wrong is the claim's bound, not the creativity of the output object, and one deduction on one row per
window is this table's discipline.

## What this lap changed

`docs/auto/` only, staged by explicit path. One row closed (WFG-214), one row filed (WFG-222), one existing
NEEDS_HUMAN entry updated (NH-037), three scorecard rows appended, `CRITIC_LATEST.md`, `DIRECTION.md` and
`KCF_READINESS.md` rewritten. **Zero §3b reorders. Zero new NEEDS_HUMAN entries. No code, test, data, figure
or `docs/NUMBERS.json` entry touched.**

## In plain terms

- **What the judges would see today.** A front door that finally says what this project claims — a per-point
  「walk out or wait for rescue」 decision with the real dispatch sheets committed in the repository — and says
  it honestly, with its limits in the same paragraph. That went up three hours ago and it is the best thing the
  loop has written this week. But the *same sentence*, without any of those limits, is still on the finals
  screen the five judges actually stand in front of, in the answer the student says out loud from memory, on
  the spoken script, and in the printed booklet on the USB. Those pages call the sheets 「가구 단위」 (per real
  household) and the corrected page calls them 「지점 단위」 (per sampled point), because the real addresses of
  elderly households are not public. Judges only see the uncorrected version, so the project currently looks
  like it is claiming a little more than it can open a file to prove.
- **What changed since yesterday.** The README opening got its lead paragraph (good, and it is a 20-point
  rubric row). The paper added the last link it owed. And I found one more thing worth telling you: three lines
  in the Q&A booklet still tell the student that the competition office has not answered about AI-assisted
  development — you settled that on 2026-09-04 — and one of the three tells a judge that a question **was
  submitted** to the office, which your own record says was never sent. That is the next lap's first job; it is
  three lines.
- **What you should do.** One reply clears the jam. **NH-037 is the urgent one and it is due tomorrow:** the
  paper is now at exactly 9,000 words against a 9,000-word limit, so the paper routine cannot add a single word
  — the next correction it has to make, it cannot make. **NH-046 is the other one due tomorrow**, and it is the
  single thing holding your readiness checklist at 8 of 11 for the eleventh day in a row. NH-032 and NH-034
  are now two days past, and until you answer them the student still may not say any margin number to a judge.



## Gates

**ALL GREEN** · mode `full` · head `3eec471` · 2026-09-09T23:05:33Z · current at `3eec471`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 21.4 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.1 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 1.6 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 333.2 s | 1871 passed, 64 skipped, 3 xfailed, 52 warnings in 331.85s (0:05:31) |

## Commits since the previous report (4b580df5a13aafd391187e9d06ab1ca710ebf950..HEAD)

- 3eec471 the dev report names the head it was pushed on, and keeps the run it replaces
- ada022a the board is rebuilt on the rebased tree
- fef4c71 WFG-212: the front door now says what the project claims, and the gate that keeps it honest was written by the reviewer that broke the first one
- bbda6f8 the paper report names the head it ships in, and keeps the run it was stamped from
- 4f3bd85 the paper points at the document that explains its own oracle, and it cost one word

## Needs a human (25 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09)
- NH-036 [DECISION] One critic lap told the next one not to edit a file, and that is what kept a false sentence in front of a judge for a window (by 2026-09-10)
- NH-037 [DECISION] The paper's word proxy now stops it a thousand words before your 25-page rule (by 2026-09-10)
- NH-038 [DECISION] Your "product first" rule has spent the last three dev laps on documents, and the readiness line it was written to protect has not moved in five critic laps (by 2026-09-09)
- NH-039 [DECISION] The national wildfire-spread system's manual is an 18 MB PDF the sandbox could not fetch, and one of you can (by 2026-09-12)
- NH-040 [FYI] A critic lap pushed one commit past a red `--assert-reported`, and it is telling you rather than hiding it
- NH-041 [FYI] This lap sent you an email containing only the word PLACEHOLDER, and could not take it back
- NH-042 [DECISION] Two of your own rules collide whenever a withdrawn claim lives in a frozen artifact, and this week they collided three times (by 2026-09-10)
- NH-043 [DECISION] A gate your loop built this morning will go red about twice a day, and the charter tells the lap that meets it to stop working (by 2026-09-09)
- NH-045 [BLOCKER] The staleness gate has closed `auto/dev` to every routine, and the one routine that met it is the one forbidden to clear it (by 2026-09-08)
- NH-044 [DECISION] The claim the paper just retracted is still live on the page the paper cites for it (by 2026-09-09)
- NH-046 [DECISION] Your product's definition-of-done names a command nothing in this project has ever run (by 2026-09-10)
- NH-048 [DECISION] One of the research routine's three literature channels has been dead for two runs, and a working replacement is already proven (by 2026-09-10)
- NH-049 [DECISION] Your critic routine is told to add judge Q&A cards and your own printing gate makes that impossible for it (by 2026-09-11)
- NH-050 [DECISION] You answered two of these questions two days ago and the loop never heard you, because you answered them on the routine page (by 2026-09-10)
- NH-051 [DECISION] The rule your loop has been obeying for three days is not the option it names, and the difference is why one row has been pushed down five times (by 2026-09-11)
- NH-052 [DECISION] The experiment that measures what your own forecast is worth is now one run away, and it would change what 42 means (by 2026-09-12)
- NH-053 [DECISION] The word your front door uses for 42 stopped being right today, and the loop cannot pick its replacement (by 2026-09-12)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
