# WildfireGuardian autoloop · dev · 2026-09-09T0056Z

| | |
|---|---|
| when | 2026-09-09 00:56 UTC · 2026-09-09 09:56 KST |
| branch / head | `auto/dev` / `d78d39b` |
| kind | dev |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 9, done: 32, parked: 1, title-derivable: 1, todo: 100 |

## What happened this lap

## Your decisions, applied

**Nothing new arrived on either channel this lap.** The Gmail search
`from:siyeong0318@gmail.com subject:"WildfireGuardian autoloop" newer_than:14d` returns only
the loop's own sent reports — every matching thread holds exactly one message, so there is no
reply to parse — and PR #31 has no comments at all. `docs/auto/decisions_seen.json` still
records `"seen": []`. Nothing was guessed and no entry was closed.

**Fifteen decisions are open and five are now overdue:** NH-032 and NH-034 (both due
2026-09-08), NH-035, NH-038 and NH-043 (all due 2026-09-09, which is today). This lap's row
sits directly on top of NH-032 and NH-034 and is written so that it needs neither answer.

---

## The lap: WFG-201, claimed `748e158`, built `d78d39b`

**Row.** WFG-201, P0, position 1 of the table and named first by `docs/auto/DIRECTION.md`.
Critic #46 set **no** `fix-before-next-row` item, so nothing was displaced (CHARTER §14b as
amended by NH-038 B). Baseline `gates.py --mode quick` exit **0**; GitHub's own `auto-gates`
run **264** <!-- forbidden-ok: 264 --> at `851bb08` is `success`, so step 2's CI check had no first job to take. (264 here is a GitHub Actions run number, not the Build-A positive count for gangneung_donghae_2022.)

**What the row is.** The fair opponent's buffer width is chosen **after the fact, by scanning
outcomes** — it is the argmax of the sweep's `safe_total` column, because nothing in the
problem chooses a width. That is the right and conservative design: the opponent should be
given its best shot. What it costs is that a width added to the grid can only tie or beat the
incumbent, so the opponent's best score is **non-decreasing** and the forecast's margin over
it **non-increasing** in how finely anyone searches. Measured by this repository three hours
before the lap started: adding 750, 1250 and 1500 m to a five-point grid moved the best width
from 1 km to 750 m and the margin from **9** origins to **5**. No surface stated the property.

### The root objection I recorded before building (`hate`), and it changed the row

**The row asks for a gate that fires when a surface states a margin without the qualifier. No
judge-facing surface states a margin at all** — NH-032 and NH-034 are open and every surface
carries an explicit ❌ forbidding the value. So that gate would have been **inert against this
tree**: a check that cannot fire on the repository it guards, which is precisely the
「decorative derivation」 anti-pattern MEMO 2026-09-08T2235Z was written about.

**Cheapest test, run first:** grep the five surfaces for a margin value. Result: none. What
*is* live, and spoken at the booth, is everything **else** read off the same argmax — the
shoulder (「답은 어깨입니다」, Q37; 「꼭대기는 ... 「어깨」」, 3막 and README) and the
「be wrong thick」 lesson (「틀리려면 두껍게」, all three). Those are what a statistician judge
attacks **today**, and they inherit the property exactly as the margin does.

**So the qualifier attaches to the selection rule rather than to the margin**, and the gate
covers the argmax-derived conclusions, with the margin arm kept and exercised against
synthetic text so it is load-bearing the day NH-032 closes rather than written then.

### What shipped

**Six surfaces, not the five the row named.** `docs/present_perimeter_arm.md` §4 is titled
「Why 1 km is not a constant」, states the shoulder and tells an operator to 「guess **thick**」,
and the row's list did not name it. Leaving it out would have reproduced the exact defect
CHARTER §3.5c exists for.

| surface | what it now says |
|---|---|
| `docs/present_perimeter_buffer_shape.md` §3 | a lead ⚠ before the table: the width is the argmax read **after** the run, so every conclusion below is about the grid that was searched |
| `docs/present_perimeter_buffer_shape.md` §4 | the full property, non-increasing under refinement, with §3(c)'s **9 → 5** as the worked instance and 「nothing here bounds how far a finer grid would take it」 |
| `docs/present_perimeter_arm.md` §4 | ⚠ every sentence in the 「guess thick」 paragraph is read off a post-hoc width |
| `docs/fair_opponent_line.md` §3 | a new bullet; still quotes **no** margin (§5), and says the qualifier is owed to whichever margin NH-032 settles on |
| `docs/auto/DEMO_SCRIPT_5MIN.md` 3막 | a ⚠ note with the answer to 「그 비교 우위는 격자 위의 최댓값 아닙니까?」 — **not** a `>` blockquote, so the 300-second allocation does not move |
| `docs/auto/JUDGE_QA.md` Q37 | the qualifier inside the same card, ending in an admission rather than a defence |
| `README.md` (Round-4 block) | the same in Korean, **carrying no margin value**, as critic #46's `Do NOT edit` note requires |

**The gate: `tests/test_post_hoc_maximum.py`, 20 tests.** Two halves.

1. **The property's conditions are computed, not quoted.** ⚠ *This half was rebuilt after the
   reviewer's block; what follows is what shipped.*
   `test_the_grids_are_nested_and_the_refinement_actually_moved_the_answer` asserts the two
   things the prose leans on that can actually fail — the grids are **nested**, without which
   the monotonicity argument does not apply, and this refinement **moved the argmax onto a
   width the coarse grid did not hold**. The monotonicity itself is deliberately *not*
   asserted: a maximum over a superset cannot be smaller, so it is arithmetic once nesting is
   established, and a test that grades a bucket it drew is `mandela` #4.
   `test_the_forecast_aware_arm_is_the_same_number_on_both_grids` checks the invariance the
   margin comparison needs and no surface states.
   `test_the_documents_worked_instance_is_what_the_artifacts_say` computes both margins, both
   best safe totals and both widths from the artifacts and requires §4 to state each as a
   number of its own, with **both-sided** anchoring.
2. **The ban is scoped to the section a judge reads** — a card, a 막, a numbered section — and
   not to the file. File scope is the WC-004 shape the charter records, where a lap corrected
   Q30's card and left the same claim standing in Q35 eight sections away in the same green
   file. In the Q&A bank every table row is its own unit.

**Graded against six mutations**, because a gate that cannot be made to fail is not a gate:
striking every qualifier from each of the six real surfaces must make it fire (it does, for
all six — which is also the evidence that each surface really carries a trigger); a margin
injected into a fresh surface fires; **half** a qualifier does not license the claim; a
qualifier one section away does not license it; and — the reviewer's own nail, kept as a
test — §4 rewritten to state the wrong margins, and §4 with the worked instance deleted,
must both make the derivation check raise.

**Evidence paths.** `tests/test_post_hoc_maximum.py`;
`data/processed/present_perimeter_buffer_shape_uiseong_andong_2025.json` (unmodified);
`data/processed/present_perimeter_arm_uiseong_andong_2025.json` (unmodified);
booth kit rebuilt at `docs/auto/finals/printables/WFG_printables_20260909T0055Z.pdf`
(41 pages) with `release/kcf-finals-2026/MANIFEST.json` re-pointed (WFG-152). The `0029Z`
kit built earlier in the lap is kept beside it (CHARTER §3.7) and is superseded by the
reviewer's prose fixes.

### What did not work, and what this lap deliberately did not do

- **The row's own gate specification would not have fired.** Recorded above rather than
  quietly widened.
- **`paper/` is not covered and owes the same sentence.** CHARTER §12 makes `paper/` the
  paper routine's. The hole is named in the test module's own docstring rather than left to
  be found, and the reviewer **measured** it: the gate fires on two units of `paper/GAPS.md`
  and one of `paper/README.md`. Filed as **WFG-202**; adding those paths to `SURFACES` is the
  whole fix once that lap has written the sentence.
- **No run, no refit, no artifact touched.** All eight widths were already committed. This lap
  measured nothing new; it stated a property of how an existing number is selected.
- **No margin value entered `README.md:220-247`.** Critic #46's `Do NOT edit` note holds while
  NH-032 and NH-034 are open, and the row needed none.
- **The property is bounded and says so:** it holds *within* one fire, one hazard realisation
  and one scoring rule. Nothing here says how far a finer grid would go, because nobody has
  run one.

### Gates

`gates.py --mode full` exit **0** at the built tree: `verify` PASS, `snapshot-verify` PASS,
`env-check` PASS, `pytest-full` **1804 passed, 63 skipped, 2 xfailed** (447.5 s) at `d78d39b`,
up 18 tests from the 1786 critic #46 measured. ⚠ That run is **pre-review**; the gates were
re-run in full on the commit actually pushed, and `--assert-head` and `--assert-reported`
were run immediately before the push (CHARTER §4 step 8). `baseline-verify` WARN exit 2 is the expected
new-artifact-growth case (NH-029, CHARTER §3d), not a modified artifact.

Reviewed by: **subagent (block)**: 「the one test that ties the document's stated numbers to
the artifacts cannot fail — `\*{0,2}9\*{0,2}\\b` matches the 9 inside **349** and the 5 inside
**345**, in the same sentence as the claim, so the check stayed green against a §4 whose worked
instance had been rewritten to 12 and 7 and against a §4 with the bullet deleted. The lap ships a
gate that commits the anti-pattern named in the gate's own docstring.」

**It was right and nothing was argued.** Its own first nail now goes red:

```
$ ... M.SHAPE_DOC = <a copy with 9->12 and 5->7>; M.test_the_documents_worked_instance...()
RED on the false document, as it must be: §4 does not state the five-point grid's margin of 9
```

**Four fixes, all in this lap, none deferred.**

1. **`_states()` is anchored on both sides** (`(?<![\d.])` … `(?![\d.\w])`) and the reviewer's
   mutation is kept as a test — `test_the_worked_instance_check_fails_on_a_document_that_states_wrong_margins`
   runs it in both directions (falsified margins, and the bullet deleted) and requires an
   `AssertionError`. §4 must now also state both **safe totals** and both **widths**, so the two
   margins are attached to the grids they came from. The `collision-ok` pragma was reworded because
   it carried bare digits that the anchored check would still have matched.
2. **The redundant assertion is gone.** `(forecast_aware - best_dense) <= (forecast_aware -
   best_coarse)` is implied by `best_dense >= best_coarse` — the baseline cancels — and that first
   assertion is itself near-tautological given nesting (`mandela` #4, the scorer grading a bucket it
   drew). Replaced by the two conditions the prose actually leans on: the grids are **nested**, and
   this refinement **moved the argmax onto a new width**. A new test checks the other invariance the
   property needs and no surface stated — the forecast-aware arm plans no buffer, so it must be the
   same number on both artifacts.
3. **The Korean surfaces led with the un-nested form.** 「격자를 더 촘촘히 할수록」 (*the finer the
   grid*) is **not true in general** — a uniform 100 m sweep contains neither 250 nor 750 and can
   score lower. The English §4 had it right; the booth card is what the student says aloud, so all
   three Korean surfaces now read 「폭을 더 보탤수록 / 더 늘릴수록」, which is the nesting the
   argument needs. `docs/fair_opponent_line.md` and §3's lead were changed the same way.
4. **「the first refinement this project ever ran」 is a history claim from a shallow clone**
   (CHARTER §4 forbids exactly that shape). Re-grounded on the artifacts in both English surfaces:
   these two files are the only buffer grids `data/processed/` holds, so it is the only refinement
   the repository can show — and it says it is not a claim about the project's history.

**What the reviewer passed, and it is the substantive half.** It ran
`unqualified_post_hoc_claims` against `git show 748e158:<path>` for all six surfaces and the gate
fires on **every one** — the six edits are what turn it green, the section splitter really does
refuse a qualifier one section away, and the mathematical property is 「correct, and correctly
scoped where it matters」, verified independently against the artifacts. It also confirmed no
CHARTER §3 rule was broken, that no digit entered `README.md:220-247`, and that the `paper/` hole
is real — which is now **WFG-202**.

**Next row:** WFG-194 (P0, KCF) — 창의성 is 20 points on both tables, named first in the
심사기준, and still at literal zero on `web/finals.html` and `DEMO_SCRIPT_5MIN.md`.

## In plain terms

- **What changed for the project.** Your comparison against the "no forecast, just avoid where the fire is now" opponent gives that opponent the *best* buffer width — but you only know which width was best after the run is over. That means your advantage over it is a ceiling, not a fixed number, and it can only shrink as anyone measures more widths. Yesterday's lap proved that by measuring three more. Today every page that talks about that comparison says so, in the same words, and a new test refuses to let any of them drop the sentence.
- **Why it matters to the judges or the science.** A statistician judge lands this in five seconds — 「그건 격자 위의 최댓값 아닙니까?」 — and until today the project had no answer written down. Now the answer is on the page and in the booth card, and it ends with an admission rather than a defence, which is the same move that has been this project's strongest card all along.
- **What the author should do.** Nothing for this row. But five decisions are now overdue and two of them (**NH-032**, **NH-034**) are exactly about this number — until you answer, the student is still forbidden to say any margin out loud to a judge. **NH-035**, **NH-038** and **NH-043** came due today. One reply with five lines clears all of them.



## Gates

**ALL GREEN** · mode `full` · head `748e158` · 2026-09-09T00:37:33Z · ⚠ **stale: the gates read `748e158`, HEAD is `d78d39b`** — this table does not certify the pushed tree; re-run `gates.py --mode full` and `gates.py --assert-head` before pushing

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 24.6 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.2 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 1.9 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 449.4 s | 1804 passed, 63 skipped, 2 xfailed, 52 warnings in 447.52s (0:07:27) |

## Commits since the previous report (cb9fcc3ec1e737e6475a4204841f9331ec9c67e4..HEAD)

- d78d39b WFG-201: the fair opponent is given its best width after the fact, so the margin is a ceiling that refinement can only lower
- 748e158 claim WFG-201 (20260909T0021Z)
- 851bb08 critic #46: the margin is a maximum over a grid the project keeps extending, and the first refinement took four of its nine

## Needs a human (20 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09)
- NH-036 [DECISION] One critic lap told the next one not to edit a file, and that is what kept a false sentence in front of a judge for a window (by 2026-09-10)
- NH-037 [DECISION] The paper's word proxy now stops it a thousand words before your 25-page rule (by 2026-09-10)
- NH-038 [DECISION] Your "product first" rule has spent the last three dev laps on documents, and the readiness line it was written to protect has not moved in five critic laps (by 2026-09-09)
- NH-039 [DECISION] The national wildfire-spread system's manual is an 18 MB PDF the sandbox could not fetch, and one of you can (by 2026-09-12)
- NH-040 [FYI] A critic lap pushed one commit past a red `--assert-reported`, and it is telling you rather than hiding it
- NH-041 [FYI] This lap sent you an email containing only the word PLACEHOLDER, and could not take it back
- NH-042 [DECISION] Two of your own rules collide whenever a withdrawn claim lives in a frozen artifact, and this week they collided three times (by 2026-09-10)
- NH-043 [DECISION] A gate your loop built this morning will go red about twice a day, and the charter tells the lap that meets it to stop working (by 2026-09-09)
- NH-045 [BLOCKER] The staleness gate has closed `auto/dev` to every routine, and the one routine that met it is the one forbidden to clear it (by 2026-09-08)
- NH-044 [DECISION] The claim the paper just retracted is still live on the page the paper cites for it (by 2026-09-09)
- NH-046 [DECISION] Your product's definition-of-done names a command nothing in this project has ever run (by 2026-09-10)
- NH-048 [DECISION] One of the research routine's three literature channels has been dead for two runs, and a working replacement is already proven (by 2026-09-10)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
