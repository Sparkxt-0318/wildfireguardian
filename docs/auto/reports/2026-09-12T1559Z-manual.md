# WildfireGuardian autoloop · paper · 2026-09-12T1559Z

| | |
|---|---|
| when | 2026-09-12 15:59 UTC · 2026-09-13 00:59 KST |
| branch / head | `auto/dev` / `925d1a3` |
| kind | manual |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 10, done: 37, dropped: 1, parked: 1, title-derivable: 1, todo: 132 |

## What happened this lap

# Paper lap 36 — the window answered §6's first objection, and the answer does not fit

**Incorporated** `f448bdd..925d1a3`, 17 commits, 22 files outside `paper/` and `docs/auto/`.
**`paper/manuscript.md` is UNCHANGED this lap.** `body_words` **9,000 → 9,000**, margin
**0 → 0**. Built document **23 pages** against the author's 25, measured this lap under
Carlito. Nothing was compressed; no caveat and no registered number was traded.

## What the window built, and why it is this paper's business

`WFG-256` ran a **rotation null** on the forward-simulated hazard core
(`docs/rotation_null.md`, `data/processed/rotation_null_yeongdeok.json`, 70 new `rn_` keys).
It holds the model's own shape and cell count fixed, rotates that mask rigidly about the
centroid of the `t = 0` seed — the same centre the disc null uses — through every 15 degrees
with zero excluded, and scores every orientation against the same observation under the same
matching and the same `p_cut`, with the scorer **imported** from the disc null's script rather
than copied. The rule and both readings were pre-registered in the claim commit before the
script existed.

The true orientation ranks **1 of 24** with **0** ties at all four off-seed slices; and the
second, pre-registered branch came out against the tidy story — **at the headline slice** only
**3** of 23 rotations beat the disc, so `docs/disc_null.md` §5.1 now records the disc as **a
stronger opponent than 20 of 23 rotations of the model's own core**, 「a direction this page did
not expect」. ⚠ That scope is the reviewer's and it is load-bearing: the count is **4, 3, 3, 4**
across the four slices, so 「20 of 23」 holds at two of four. The draft carried the bare form.

**That is the answer to the first objection a reviewer makes to §6's disc clause** — *an
irregular blob out-overlaps a circle whatever it does, so the gap measures shape complexity
and not the model* — and until this window no file in the repository had it.

## Sections changed: none, and that was checked rather than assumed

No sentence of the manuscript went false. §6's clause survives limb by limb: the overlap claim
is unchanged, the centroid finding is explicitly re-affirmed by `docs/rotation_null.md` §5.2,
and 「a **floor** comparison」 survives because `docs/disc_null.md`'s own amendment says
「Nothing in this item is withdrawn: necessary-and-not-sufficient stands」.

Three checks re-derived in this clone, not quoted:

- All **21** withdrawal entries and all **68** spellings return **zero** hits against the
  manuscript, per source line and flattened. `WC-021` is the window's new entry and the
  manuscript was **already on the right side of it** — its Abstract has said 「and 2 reach no
  refuge under it」 since lap 34, and the window narrowed `README.md:25` and `:798` **to this
  paper's own wording**. First retraction in this ledger that moved another surface *onto* the
  manuscript's register rather than off it.
- §3.5's 「skipped it **four** times」 stays four: `WC-021` was registered in `f1da9da`, the
  same commit that narrowed both README surfaces, verified from that commit's own file list
  rather than from the registry's summary.
- §3.5's registry sentence survives the 76 new keys, with lap 29's corrected predicate:
  `source_file` / `derivation` / `caveat` **658 / 658** each, `json_path` **654 / 658**,
  `forbidden_phrasings` **494 / 658 (75.1 %)** — up from 418 / 582. Reconciled a second way:
  418 + 76 = 494 exactly, all 76 new entries carrying a non-empty list.

## What the budget refused, measured and reverted

Margin inherited: **zero**. `docs/auto/DIRECTION.md` states the consequence as arithmetic.
Three forms of the §6 clause were inserted, built, read off `build_docx.py`'s own counter and
reverted:

| form | `body_words` |
|---|---|
| tightest — 「an advantage rotating that same core away destroys」 | 9,000 → **9,008** |
| decisive — 「which most rotations of that same core do not beat」 | 9,000 → **9,010** |
| both facts, with the disc's own standing | 9,000 → **9,020** |

A **number-free** shape was chosen deliberately and is not a stripped one: the `rn_` entries
carry a 「six facts travel together or none may be quoted」 band (seven, as written), so any
*quantity* costs the band too, while a number-free clause needs only the caveats that **scope**
the claim — and §6 already carries every one of them. ⚠ What that band costs was **not
measured**, and the draft said it was, attributing lap 29's **+198** — which is the `dn_`
six-fact band's figure from a different sentence. Named as unmeasured rather than guessed;
reviewer's finding.

⚠ Both directions, because `WC-009` forbids the one-directional form. **The manuscript is not
false without it and is not missing a limitation**; what is declined runs *in this paper's
favour*. What it leaves is a reviewer's first objection to a comparison the paper itself cites,
unanswered on a page where the answer is committed, registered and drawn.

⚠⚠ **The novelty claim was checked against the ledger, then narrowed by the reviewer.** The
ledger's budget refusals are evidence **against** the paper (lap 24's IoU at +41, lap 29's disc
null at +143, lap 35's `vus_` split at +32) or illustrations of a limit §3.5 already states in
its own voice (laps 14, 18, 23, 26); laps 32 and 34's favourable declines were for **bars**
(NH-059), not length. ⛔ The draft wrote that as **exhaustive** and it is not — laps 15, 21, 28,
30 and 33 declined things too, and **lap 30 refused the disc null's quantities, `2.2044` among
them, which run in this paper's favour**. The claim survives on a distinction the draft never
stated: the `dn_` band forces the centroid metres to travel with that ratio, so lap 30 refused a
**package** its own record calls 「the strongest evidence against itself」, and the favourable
half's number-free form **did land** in §6 that lap. Accurate form: **the first lap where the
budget alone stopped an empirical result supporting the paper *and nothing of it landed in any
form*.** Smaller than lap 21's declined *limitation*, and stated smaller.

## Figures

`F11_rotation_null.png` is **new, drawn, looked at with the Read tool, and unreferenced** —
the `F9` / `F10b` precedent a third time. Panel (a) is all 23 rotations at the headline slice
sorted by score, with the disc and the as-oriented value as reference lines, so the 「3 above,
20 below」 split is something a reader counts rather than something the figure asserts; panel
(b) is the same three quantities at every off-seed slice; **panel (c) is the half that points
the other way**, the two centre-of-mass distances from the observed footprint, where the core
is the worse of the two. Every value is read from the artifact
at draw time — there is no numeric literal in the function, so nothing in its source can go
stale against the registry.

Two defects were found **by looking** and repaired before the commit: panel (b)'s neighbouring
「gap NNN min」 tick labels collided at the 540- and 720-minute slices, and its grey bar is the
**best** rotation where panel (a)'s grey is **any** rotation — which the shared legend cannot
say, and leaving a reader to infer it is verbatim the inference-by-negation defect lap 29's
reviewer found on `F10b`. The axis now names it.

⛔⛔ **The look-at-it pass then PASSED TWO MORE, both found by the reviewer, both repairable
only because the PNG was still uncommitted.** (i) The figure carried **only the flattering
half**: `F10b` puts both halves in one image, `docs/rotation_null.md` §6 licenses this result
on a judge-facing surface only 「beside the centroid displacements that keep it honest」, and a
300 dpi binary carrying the win without the overshoot is **`WC-017`'s known-stale exception
(a)** — whose worked instance is `F10_disc_null.png`, committed wrong one lap cycle ago and
then unregenerable under CHARTER §3 rule 2. **`F11` now has a third panel**, the two
centre-of-mass distances from the observed footprint, where the core is the **worse** of the
two. (ii) Panel (a) did not name the slice it is drawn at, while the ledger invites a count off
its bars that is not the same at every slice; its axis now names it. **Three defects of one
class, two past a pass this routine records as done.**

⚠⚠ Kept rather than softened: **`F11` is free precisely because nothing in `paper/` reads it.**
It reaches neither the page count, nor `figure_fingerprint`, nor the built document, and takes
no appearance number (mapping unmoved: F1→1, F2→2, F4→3, F5b→4, F8b→5, F3b→6, F6→7, F7→8).
Lap 19's rule stands and was not used against it — a caption carrying body argument is 「a false
measurement of the document, not a short document」 — so the figure draws numbers and does not
carry §6's argument. The other **ten** figures redrew **byte-identical**.

## The mandatory repair (critic #75, WFG-275)

`paper/README.md`'s lap-35 block opened 「8,999 → **8,999**; margin **1 → 1**」 and its heading
said 「both repairs came in at NET ZERO」, while lap 35 ended at **9,000** with a margin of
**0**: its reviewer's `responder` repair to §4.6 came in at +1 and landed after the headline was
written. The corrected pair was already further down the same block, so that file has been
contradicting itself since the push, and `STATE.json`'s note header carried the same dead pair.
**Annotated in place rather than edited** (CHARTER §3.7), with the live figure re-derived here
(`check_paper.py` prints `body_words` 9000) rather than copied from the critic report.

⛔⛔ **And the first pass at that repair reproduced the error it was repairing: it fixed two
surfaces, certified 「Both are annotated in place」, and left FOUR live copies — two of them in
`paper/GAPS.md`, which this same diff was rewriting.** `GAPS.md`'s lap-35 Range paragraph and
both ledgers' lap-35 closing formulae (「ONE word against the proxy's 9,000」 where the formula
is 9,000 minus final `body_words`, so ZERO), plus `GAPS.md`'s lap-35 anchor paragraph still
naming `bdb470ea0108d633` as what `STATE.json` carries. All four now annotated. ⚠ **The cost of
finding them was one command** — DIRECTION's own 「grep for the SUBJECT of the claim, never for
the sentence you just wrote」 — which this routine invoked by name at laps 32, 34 and 35 and did
not run here, taking the critic's filed list as the diff. That is verbatim CHARTER §3.5c's 「a
lap chooses which documents to correct and will miss one」. The draft's 「second consecutive
window」 was itself a stale hand-typed count in the sentence about stale hand-typed counts; **no
count replaces it** — what is recorded is that the error was committed again *inside its own
repair*, with every gate in `paper/` green over all four copies.

## Gaps

**None opened and none closed.** Seven `[GAP:` markers, seven `G` rows, unchanged. The rotation
null deliberately gets **no** `G` row: those rows are one-to-one with the markers and
`check_paper.py` counts both, so a row with no marker turns the gate red — and a marker would be
false, because nothing is missing from the artifacts. What is missing is room.

## Gates, anchor and review

`scripts/auto/gates.py --mode full` exits **0** (2,256 passed, 65 skipped, 3 xfailed;
`baseline-verify` is the usual sandbox WARN, CHARTER §3d). `check_withdrawn_claims` prints
「PASSED === 21 claims over 945 gated files」; `check_number_collisions` is OK. The `.docx` is
**not** rebuilt into the commit — the manuscript did not move, and lap 23's byte-non-determinism
finding makes a rebuild 2.9 MB of diff that says nothing.

✅ The anchor was **measured** and is recorded as the **weaker** of the two kinds of
confirmation: neither `body_words` nor the referenced figure list moved, so `check_paper.py`
would have passed with no renderer at all; the one `apt` line was run anyway and the gate took
its measuring branch — `pages 23, calibri_face Carlito, metrics_ok true`, page objects and
page-tree `/Count` agreeing — printing `7ed51c97137c9b2c`, the string `STATE.json` already
carried.

**Two pages against the author's 25, ZERO words against the proxy's 9,000, measured on one
document by one run.** 9,000 is also **500 over** CHARTER §12's target of 8,500. Laps 13
through 21, 24, 25, 30, 33, 35 and now 36 have all had their writing shaped by the proxy rather
than by the evidence. **NH-037 is the answer and it is still open.**

Clone **SHALLOW**: `--is-shallow-repository` true, `rev-list --count HEAD` = 53, both measured
this lap; **no ancestry or reachability claim is made anywhere in this lap's output**.

Reviewed by: **subagent (BLOCK)** — root objection: *「the lap's one mandatory item, repairing a
stale hand-typed word count inside the document whose subject is stale hand-typed word counts,
is certified complete and is not — there is a third live copy, in a file this diff rewrites with
176 new lines, 38 lines below the new section that declares the repair closed」*. Sustained in
full. Five wrong numbers named; **all five verified in the tree and repaired before the push**
(four stale lap-35 figures annotated per CHARTER §3.7, the unscoped 3/20 count scoped in three
files, the `+198` band attribution withdrawn as unmeasured), plus two further `F11` defects and
the exhaustiveness narrowing above. The reviewer also confirmed, by re-running rather than
reading: the 17-commit range and its file count, `manuscript.md` byte-unchanged, §6 surviving
limb by limb, 21 entries / 68 spellings at zero hits, the registry's 582→658 with zero existing
entries changed and the 418+76=494 reconciliation, the three declined clause costs (9,008 /
9,010 / 9,020), the figure mapping, and `pages 23 / Carlito / metrics_ok true`. It judged the
decision **not** to put the rotation null in the manuscript **defensible** under CHARTER §3
rule 5, the omission running in the conservative direction.

## In plain terms

- **The paper now says exactly what it said six hours ago** — nothing in it went wrong — but
  the loop proved something new in its favour: your predicted fire shape beats a circle *and*
  beats every rotation of itself, so the win is not just "an irregular blob beats a circle".
  The figure is drawn and committed; the one sentence that would say it does not fit.
- **What is still missing is one sentence, and the only thing blocking it is your own word
  limit.** The paper is 23 pages against your 25-page rule, but the word proxy is at 9,000 of
  9,000 — zero spare — so a 10-word true sentence is illegal while two blank pages sit there.
- **What to do: answer NH-037**, with one of the four options the entry itself lists — **A**
  raise the proxy to the measured 9,461-word point (24 pages, one under your rule), **B** land
  the one `apt` line in the CI workflow so a clean clone measures the pages and the proxy stops
  being load-bearing, **C** both, **D** leave it. Reply `NH-037: <letter>`. It is the oldest
  overdue decision and it now costs the paper a result that makes it *look better*, not only
  ones that make it look worse.



## Gates

**ALL GREEN** · mode `full` · head `925d1a3` · 2026-09-12T15:44:42Z · current at `925d1a3`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 30.7 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.2 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 0.5 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 705.9 s | 2256 passed, 65 skipped, 3 xfailed, 52 warnings in 703.98s (0:11:43) |

## Commits since the previous report (98fe21d58a71b9e18acc2b7dedf40808574f9b60..HEAD)

- 925d1a3 explaining a collision collides, so the explanation carries the pragma too
- 299d0ca the card quoting a ratio beside an IoU trips the gate that reads both, fourth window running
- 1e4f35b critic #75: the card carrying the new numbers does not carry the sentence that reconciles them

## Needs a human (32 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09, one day past; raised to HIGH by critic #55 on a measured third instance)
- NH-036 [DECISION] One critic lap told the next one not to edit a file, and that is what kept a false sentence in front of a judge for a window (by 2026-09-10)
- NH-037 [DECISION] The paper's word proxy now stops it a thousand words before your 25-page rule (by 2026-09-10)
- NH-038 [DECISION] Your "product first" rule has spent the last three dev laps on documents, and the readiness line it was written to protect has not moved in five critic laps (by 2026-09-09)
- NH-039 [DECISION] The national wildfire-spread system's manual is an 18 MB PDF the sandbox could not fetch, and one of you can (by 2026-09-12)
- NH-040 [FYI] A critic lap pushed one commit past a red `--assert-reported`, and it is telling you rather than hiding it
- NH-041 [FYI] This lap sent you an email containing only the word PLACEHOLDER, and could not take it back
- NH-042 [DECISION] Two of your own rules collide whenever a withdrawn claim lives in a frozen artifact, and this week they collided three times (by 2026-09-10)
- NH-043 [DECISION] A gate your loop built this morning will go red about twice a day, and the charter tells the lap that meets it to stop working (by 2026-09-09)
- NH-045 [BLOCKER] The staleness gate has closed `auto/dev` to every routine, and the one routine that met it is the one forbidden to clear it (by 2026-09-08)
- NH-044 [DECISION] The claim the paper just retracted is still live on the page the paper cites for it (by 2026-09-09)
- NH-046 [DECISION] Your product's definition-of-done names a command nothing in this project has ever run (by 2026-09-10)
- NH-048 [DECISION] One of the research routine's three literature channels has been dead for two runs, and a working replacement is already proven (by 2026-09-10)
- NH-049 [DECISION] Your critic routine is told to add judge Q&A cards and your own printing gate makes that impossible for it (by 2026-09-11)
- NH-050 [DECISION] You answered two of these questions two days ago and the loop never heard you, because you answered them on the routine page (by 2026-09-10)
- NH-051 [DECISION] The rule your loop has been obeying for three days is not the option it names, and the difference is why one row has been pushed down five times (by 2026-09-11)
- NH-052 [DECISION] The experiment that measures what your own forecast is worth is now one run away, and it would change what 42 means (by 2026-09-12)
- NH-053 [DECISION] The word your front door uses for 42 stopped being right today, and the loop cannot pick its replacement (by 2026-09-12)
- NH-054 [DECISION] Four fifths of your front door's headline is limits, and no lap is allowed to decide whether that is the project's strength or its biggest presentation risk (by 2026-09-13)
- NH-055 [DECISION] Your front door compares the headline forecast number to a model the same page calls broken, and today the loop measured a second, harsher comparison it is not allowed to put beside it (by 2026-09-13)
- NH-056 [DECISION] A Korean university published a reproducible deep-learning wildfire model four weeks before your finals, and a lap may not decide on its own whether to mention it (by 2026-09-13)
- NH-057 [DECISION] The one thing your project now says it contributes has never once been produced from a real fire, and only your laptop can change that (by 2026-09-13)
- NH-058 [DECISION] Two of your routines built the same row in the same window, and the rule that was supposed to stop that cannot see the paper routine (by 2026-09-13)
- NH-059 [DECISION] Your loop measured the hardest question a judge can ask you, and the answer is uncomfortable enough that no lap may decide on its own whether you say it out loud (by 2026-09-13)
- NH-060 [DECISION] Your "product first" rule sent a booth-printable defect to the bottom of the board, and two of your routines read the same sentence of it differently (by 2026-09-14)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
