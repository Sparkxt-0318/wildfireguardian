# WildfireGuardian autoloop · critic · 2026-09-12T1200Z

| | |
|---|---|
| when | 2026-09-12 12:00 UTC · 2026-09-12 21:00 KST |
| branch / head | `auto/dev` / `d8d2c5d` |
| kind | critic |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 10, done: 37, dropped: 1, parked: 1, title-derivable: 1, todo: 130 |

## What happened this lap

## Your decisions, applied

**Nothing new on either channel, and that is now seven days.** The Gmail search
`from:siyeong0318@gmail.com subject:"WildfireGuardian autoloop" newer_than:14d` returns 30
threads in the newest page, and the four newest were opened with `get_thread` rather than read
from the search preview (the preview only shows a thread's oldest messages, so a reply could
hide behind it). Each carries **exactly one** message and the `SENT` label: the loop's own
reports. PR #31's comment list is **empty**, checked through the GitHub MCP.
`docs/auto/decisions_seen.json` still records `"seen": []`, and the newest applied decision
anywhere is **NH-031, received 2026-09-06 on the laptop**. **No decision has ever arrived on
the email channel.** Nothing was applied this lap and nothing was guessed at.

⚠ **One new measurement about the channel itself, with its limit stated in the same sentence.**
**29 of the 30 newest report threads also carry the `UNREAD` label** (the exception is the
2026-09-12T0410Z paper report). A message the API sends to your own address lands in `INBOX`
unread, and reading it in the Sent view need not clear that label — **so this is evidence the
inbox copy was not opened, and not proof the report was unread.** It is still the only signal
the loop has, and it points the same way `"seen": []` does. Appended to **WFG-211**, not
re-filed.

---

## Findings, ranked

**Verification first (step 2), because it sets whether there is a finding #1.** There is not.
`gates.py --mode full` exits **0** on its **first** run at `092c907` (2233 passed, 65 skipped,
3 xfailed, pytest 357.8 s; `baseline-verify` is the usual sandbox WARN for two absent
`data/raw/` manifests, CHARTER §3d). `--assert-head` and `--assert-reported` both exit 0, and
`--assert-reported --base <push>` exits 0 for **all ten** pushes in the window. GitHub's own
runs, read with `curl` (WFG-268: CHARTER §4's 403 claim is false here, confirmed a third
time): `auto-gates` **399 to 409**, every one `success` except **403** and **404**, both
`cancelled` by a newer push. Run **409** is `success` at `092c907`, this exact head. All **39**
reports dated 09-11 and 09-12 record `Reviewed by:`. **So no dev lap reported green over a red
run, and CHARTER §4b sets no finding #1.**

### F1 · The card that tells the student which sheets to print is the one thing in the booth box that is wrong, and the correction was written and then not merged for a reason that does not apply

This is the lap's one **`fix-before-next-row`** item (CHARTER §14b: minutes, judge-facing).

`docs/auto/JUDGE_QA.md` Q39 (`:1554`) instructs: print the three pre-built `dispatch_a4.pdf`
and regenerate the other thirty. Measured at `092c907` in this lap's own process:

| | |
|---|---|
| pre-built PDFs in `outputs/dispatch/20260801T163042Z/` | **3** |
| of those, printing the superseded 사유 | **1** (`02-천전공원-일대`) |
| stale committed PDFs across all of `outputs/` | **3** — the other two in `outputs/dispatch_full/20260801T183522Z/03-영덕해맞이공원-일대/`, a directory Q39 never names |

So the instruction assembles a stack in which one sheet says
「예산 내 차량 진입로가 화재로 차단됨(우회 포함)」 and thirty say
「어느 거점에서도 생존 인지 차량 진입 경로가 확인되지 않음」 for **one** code condition. Q39 is
source **3 of the 7** in `manifest_20260911T2137Z.json` (kit `WFG_printables_20260911T2137Z.pdf`,
**60** pages, re-hashed **7 of 7** here), and that kit is named by
`release/kcf-finals-2026/MANIFEST.json` (re-hashed **19 of 19** here). **It is on paper in the
box, not only in the tree.**

The correction exists: **P-005** in `docs/auto/JUDGE_QA_PENDING.md`, written by the 0920Z dev
lap, paste-ready, one paragraph. That lap did not merge it 「because NH-049 is open」.
**NH-049 constrains the critic, not a dev lap** — its own text reads 「the critic has exactly
two legal moves and neither is a card」, and its recommended **option A** reads 「have the next
dev lap that rebuilds the kit merge and empty it (this is WFG-205)」. NH-049's own appended
measurement prices `make printables` at about a minute batched. This critic did not do it
itself because re-pointing the release manifest is outside `docs/auto/`.

⚠ **One correction to the draft, measured here:** P-005 reads 「미리 만들어 둔 장은 … 사유가
옛 문장이고」, which reads as all three. It is **one of three**. The instruction itself —
*regenerate all 33, hand over nothing pre-built* — is right and stays as drafted.

Filed on **WFG-205** and on **NH-049** (severity raised to HIGH), with the full recipe in
`docs/auto/CRITIC_LATEST.md`.

### F2 · The paper's word margin is ZERO for the first time, and both pages that narrate this window open by saying it is 1

New row **WFG-275** (P1), and the author's half is **NH-037**, severity raised.

`paper/STATE.json` records `body_words` **9000**; `check_paper.py` re-derives **9000** here and
exits 0 only because the test is `> LIMIT` and `LIMIT` is 9000. **The next paper lap that adds
one word is red.** But `paper/README.md:1514`, the opening sentence of the lap-35 paragraph,
reads 「8,999 to **8,999**; margin **1 to 1**. Nothing was compressed」, and the header of
`STATE.json`'s own `built_pages_note` carries the same dead pair. The corrected figures are in
the same paragraph at `paper/README.md:1560` — 「8,999 to 9,000; margin 1 to 0」 — because the
reviewer's `responder` repair landed *after* the headline was written and the headline was
never re-read. **That is the exact class critic #73's new rule names, one window after it was
written, in the document family that narrates the first three instances.**

Why it matters beyond bookkeeping: the window's strongest new evidence **runs against this
paper's own number** — of the 24 origins it reports with no confirmed survival-aware ingress,
the run's own corridor screening recorded **4** with a corridor surviving to or past the
responder's ETA and **1** reachable with the full 12-minute margin — and the paper lap measured
that sentence at **+32 words** and reverted it, and the bare register clause at **+10** and
reverted that too. Both were refused for want of margin against a **proxy**, on a document the
**real** rule (25 pages) says has two pages spare. A limitation the project found against itself
is being withheld by the wrong instrument.

✅ **The good news on the same file, and it closes a gap critic #73 opened:** `built_pages` is
back at **23** with a re-derived anchor (`built_pages_inputs` `7ed51c97137c9b2c`), because the
paper lap installed LibreOffice Writer in its own sandbox and took `check_paper.py`'s measuring
branch rather than hand-writing a number. **Your 25-page rule is verified again, with two pages
to spare.**

### F3 · WFG-272 re-verified, it reproduces exactly, and this critic nearly published a rebuttal of a correct finding

`grep -n "no safe walking route" paper/make_figures.py` at `092c907` returns **133, 146, 172,
251, 282, 573, 580, 585, 754**. `docs/figure_legend_claims.md:127` names **133, 146, 173, 252,
283, 574, 581, 586, 755** — first two right, other seven each **+1**. The prose offsets
reproduce too (`:649` should be `:648`; `:755` should be `:754`). Critic #73's exculpating half
also reproduces: I re-resolved all seven producer citations in the table at `:39-45` line by
line and **six of the seven land exactly**, the only miss being the `make_figures.py` entry.

⚠ **What I got wrong and caught.** I read 「nine line numbers into `paper/make_figures.py`」 as
nine *citations*, counted six, and had the rebuttal half-written before re-reading `:127` and
finding the sentence is about nine **grep hits**. The row is right as filed. **Before
contradicting a finding, run the command the finding names, not the command you think it
names.** The corrected pointers for every site are now on the row so the fix is two minutes.

### F4 · Zero readiness lines ticked for the thirty-first consecutive critic lap, and not one of the three is reachable by a lap

`docs/auto/KCF_READINESS.md` is **8 of 11**; `git diff` over the 24 h window on that path is
**empty**. ⚠ A trap worth recording: `git log --since='26 hours ago'` on that path answers
`679c187`, and `git show` renders the file as `A` there — that is the shallow clone's boundary
at depth **50**, not a change (WFG-217).

The three unticked lines are **R3** (`blocked(NH-046)`, one unanswered email), **R11** (held
behind R3) and **R12** (your hands on the laptop). **None of them is reachable by a lap.** So
the flat count is not evidence of idle laps — this window closed two P0 rows and built an
independent second measurement route — it is evidence that the last three lines of the
definition of done sit on the other side of a channel that has delivered nothing in seven days.
**R3 is the only one of CHARTER §14b's six named lines outstanding, so one email releases the
whole P1 block.** The sprint ends **2026-09-15**.

### F5 · WFG-252 annotated: two more scorecard defects, reported and deliberately not fixed

Both track tables now end with a `2026-09-11 b6778e7` row sitting **below** the 2026-09-12
rows, so a reader taking the last row as 「today」 gets the wrong day; and the Track B row for
`cd18782` spells the criterion 「제울 자료」. Neither changes a score. CHARTER §3.7 and
DIRECTION both forbid editing a scorecard row another lap wrote, so they are reported and the
row's done-when grows a clause: the gate must also require the per-track tables to be
non-decreasing in date and to draw criterion names from a fixed list.

---

## The root objection (`hate`), and it is about the loop rather than the product

**「This loop has spent thirty hours getting very good at one bug.」**

Of the **fifteen** rows closed in the last thirty hours, **nine** are a single defect family —
an outcome-class name that asserts a cause its code condition does not establish — chased from
`fa_exceeds_budget` to `no_safe_route` to `no_surviving_vehicle_ingress`, across a figure
legend, an operational sheet, an abstract, a README bullet and a card. **Three** are
experiments. The repairs are good and the family is real; **WFG-271** says why it keeps
recurring, which is that the classifier lives in **seven hand-written copies** and nothing
fails when one drifts. But the marginal lap is now buying the ninth instance of one wording bug
while **WFG-256** — the rotation null, the only row in the backlog that would let the student
answer 「모양 때문입니까?」 with a measurement instead of 「아직 재지 않았습니다」 — has been
`todo` since 2026-09-11. Three days remain.

**The cheapest test, pre-registered so it cannot be argued after the fact:** if 데이터
수집·분석·해석 is still 20 and 창의성 still 19 at critic #76, the loop has spent two more days
at the ceiling of one row and the floor of another, and the reading is right.

**What the objection does NOT say.** It is not a criticism of the lap under review, which was
the best-run lap of the week. It was handed a row that pre-committed its answer to 「which of
the three committed PDFs」; its `hate` pass broke the row before it started (**38** committed
PDFs across **12** run directories, two of the three stale sheets in a directory the row never
names, so answering as written would have missed them); it caught its own 「there is no `pypdf`
here, **so** we class by sibling HTML」 as a non-sequitur and built a second route from the
standard library that reads each PDF's own embedded font subset; and it struck the word
「대조군」 off its own zero because that zero came from the same scan as the count it was said to
control. **I re-ran that second route here: 38 probed, 3 stale, 0 current — identical.** That is
설계와 방법론 and 데이터 수집·분석·해석 at 20, re-earned rather than inherited.

## `factchk` and the judge drill

**`factchk`: no new claim about the world entered the tree this window.** The window's prose is
about this repository. The two world-facing assertions inside it were checked: Chrome embeds a
**subset** of a Korean face per printed PDF (borne out structurally — the probe recovers a
different `/ToUnicode` coverage per file), and Carlito is the metric-compatible stand-in for
Calibri (used only to make the page count comparable with Word). No citation was added, so no
URL needed opening.

**Judge drill: ten questions, and it produced no new 「no evidence yet」 entry.** The three that
the window newly answers are 「이 세 장 중 어느 것이 옛 문장입니까?」 (`outputs/dispatch/README.md`
names all three paths), 「PDF를 읽을 수는 있습니까?」 (`docs/dispatch_sheet_staleness.md` §4.1, the
font-subset probe) and 「논문은 몇 쪽입니까?」 (`paper/STATE.json` `built_pages` 23, re-anchored).
The hardest one — 「실제 산불의 관측 확산면으로 만든 출동 지시서가 있습니까?」 — is answerable and
the answer is **no**, carried in the bank at `docs/auto/JUDGE_QA.md:662`, `:1311` and `:1346`
and on `README.md`. The one I could not answer from a file, 「모양 때문에 겹친 것입니까?」, is
already **WFG-256** and is confirmed here rather than re-filed. **No new card is added**, which
is the honest result and not an omission.

## Scorecard deltas

**Track A UP to 97; Track B HOLDS at 96.** Full rows appended to all three tables in
`docs/auto/SCORECARD.md`.

- **구현 및 유용성 19 to 20** (Track A). Critic #73 docked this row with the words 「returns to
  20 when WFG-267 (i) names the stale page in a committed file」. It does, and it names
  **three** rather than one, in `outputs/dispatch/README.md`, with a gate that re-derives the
  list from the tree **in both directions**. The condition is met past its own wording.
- **제출 자료 19, HELD on both tracks.** Critic #73 made 20 conditional on the README spelling
  **and** WFG-272. The README half is paid in full and re-measured here (`README.md:25` and
  `:798` read 「reach no refuge under that policy」, `grep` for the old spelling returns zero,
  `WC-021` registered, 21 entries). **WFG-272 is still `todo`**, so the row does not move.
- ⚠ **The fall condition was tested and did NOT fire, which is worth saying because it was
  close.** Critic #73 set 18 for 「a third document in this family shipping `file:line`
  citations that do not resolve」. The window's one such document,
  `docs/dispatch_sheet_staleness.md`, cites section numbers and registry keys and **no
  `file:line` at all** — deliberately, and its §5.5 says why. That is the defect class being
  designed out rather than avoided by luck.
- **연구 목적 18, 설계와 방법론 20, 데이터 수집·분석·해석 20, 창의성 19** hold. ⚠ 창의성 had a
  real case (recovering a page's printable glyph set from a font subset to grade a claim the
  same lap made is 뛰어난 창의성 on the rubric's own wording) and it is **declined with the
  condition written down**: 20 is a claim that nothing is missing, and WFG-256 is missing.

**Pre-registered for critic #75.** 제출 자료 reaches **20** on both tracks when the Q39 merge
lands with the kit reprinted and the bundle re-pointed **and** WFG-272 closes. 창의성 reaches
**20** when WFG-256 runs and reports either way. **구현 및 유용성 falls back to 19 if the Q39
instruction is still unmerged at the next critic lap** — a defect named twice and left standing
is no longer a window's lag.

## Direction check (§3b)

**No row move spent, and that is a measurement rather than an omission.** WFG-267 and WFG-270
both closed, leaving table order **WFG-256** then **WFG-255** — an experiment first, which is
exactly what the root objection above says the next lap owes. The one thing this lap wants done
before it is **minutes**, which CHARTER §14b routes to the preemption slot and not to a reorder.
`docs/auto/DIRECTION.md` is rewritten at one screen with the date, the reasoning and the
standing prohibitions. Board at this head: **269 rows, 13 P0 `todo`**, with the counting rule
written beside it and reconciling exactly from 267/15 at `4bf34ab` (two P0 closures, two new P1
rows). The rule's own 12 broken rows (WFG-191) were re-counted after this lap's four
annotations: still 12, none added.

## In plain terms

- **What the judges would see today.** A strong project with one wrong instruction in the paper
  box. The front page no longer claims two homes have **no safe walking route at all** — it now
  says what the search actually returned. But the Q&A card you study, and that is printed in the
  60-page booklet, still tells you to print three ready-made dispatch sheets plus thirty freshly
  made ones, and one of those three prints an old sentence the project has since corrected. A
  judge holding two sheets side by side would see the project contradict itself.
- **What changed since yesterday.** The loop proved, twice and by two unrelated methods, exactly
  which three printed sheets carry the old sentence, and wrote the answer into a file you can
  open at the booth. Your paper's page count is measured again: **23 pages against your 25-page
  limit**. Everything is green — the gates here and GitHub's own runs.
- **What you should do.** Two lines in one reply, and the first is the same as yesterday.
  **`NH-046:`** — it is two days past due and is the single item holding your readiness
  checklist at 8 of 11 with three days left in the sprint. **`NH-037:`** — your paper is now at
  exactly 9,000 words against a 9,000-word limit, zero margin, and the first thing that limit
  has actually blocked is a caveat the project found **against its own number**. Its options are
  the ones already listed on that entry, unchanged; today's measurement points at **B**, because
  the thing B asks for — a machine that measures real pages instead of guessing from words — was
  done by hand this morning and gave **23 pages against your 25**.



## Gates

**ALL GREEN** · mode `full` · head `d8d2c5d` · 2026-09-12T11:59:05Z · current at `d8d2c5d`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 24.8 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.1 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 0.3 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 350.5 s | 2233 passed, 65 skipped, 3 xfailed, 52 warnings in 349.31s (0:05:49) |

## Commits since the previous report (086d23d117f450a839f991b133d15da60429a8c1..HEAD)

- d8d2c5d the critic report names the head it ships in, and keeps the head it reviewed as the record

## Needs a human (32 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09, one day past; raised to HIGH by critic #55 on a measured third instance)
- NH-036 [DECISION] One critic lap told the next one not to edit a file, and that is what kept a false sentence in front of a judge for a window (by 2026-09-10)
- NH-037 [DECISION] The paper's word proxy now stops it a thousand words before your 25-page rule (by 2026-09-10)
- NH-038 [DECISION] Your "product first" rule has spent the last three dev laps on documents, and the readiness line it was written to protect has not moved in five critic laps (by 2026-09-09)
- NH-039 [DECISION] The national wildfire-spread system's manual is an 18 MB PDF the sandbox could not fetch, and one of you can (by 2026-09-12)
- NH-040 [FYI] A critic lap pushed one commit past a red `--assert-reported`, and it is telling you rather than hiding it
- NH-041 [FYI] This lap sent you an email containing only the word PLACEHOLDER, and could not take it back
- NH-042 [DECISION] Two of your own rules collide whenever a withdrawn claim lives in a frozen artifact, and this week they collided three times (by 2026-09-10)
- NH-043 [DECISION] A gate your loop built this morning will go red about twice a day, and the charter tells the lap that meets it to stop working (by 2026-09-09)
- NH-045 [BLOCKER] The staleness gate has closed `auto/dev` to every routine, and the one routine that met it is the one forbidden to clear it (by 2026-09-08)
- NH-044 [DECISION] The claim the paper just retracted is still live on the page the paper cites for it (by 2026-09-09)
- NH-046 [DECISION] Your product's definition-of-done names a command nothing in this project has ever run (by 2026-09-10)
- NH-048 [DECISION] One of the research routine's three literature channels has been dead for two runs, and a working replacement is already proven (by 2026-09-10)
- NH-049 [DECISION] Your critic routine is told to add judge Q&A cards and your own printing gate makes that impossible for it (by 2026-09-11)
- NH-050 [DECISION] You answered two of these questions two days ago and the loop never heard you, because you answered them on the routine page (by 2026-09-10)
- NH-051 [DECISION] The rule your loop has been obeying for three days is not the option it names, and the difference is why one row has been pushed down five times (by 2026-09-11)
- NH-052 [DECISION] The experiment that measures what your own forecast is worth is now one run away, and it would change what 42 means (by 2026-09-12)
- NH-053 [DECISION] The word your front door uses for 42 stopped being right today, and the loop cannot pick its replacement (by 2026-09-12)
- NH-054 [DECISION] Four fifths of your front door's headline is limits, and no lap is allowed to decide whether that is the project's strength or its biggest presentation risk (by 2026-09-13)
- NH-055 [DECISION] Your front door compares the headline forecast number to a model the same page calls broken, and today the loop measured a second, harsher comparison it is not allowed to put beside it (by 2026-09-13)
- NH-056 [DECISION] A Korean university published a reproducible deep-learning wildfire model four weeks before your finals, and a lap may not decide on its own whether to mention it (by 2026-09-13)
- NH-057 [DECISION] The one thing your project now says it contributes has never once been produced from a real fire, and only your laptop can change that (by 2026-09-13)
- NH-058 [DECISION] Two of your routines built the same row in the same window, and the rule that was supposed to stop that cannot see the paper routine (by 2026-09-13)
- NH-059 [DECISION] Your loop measured the hardest question a judge can ask you, and the answer is uncomfortable enough that no lap may decide on its own whether you say it out loud (by 2026-09-13)
- NH-060 [DECISION] Your "product first" rule sent a booth-printable defect to the bottom of the board, and two of your routines read the same sentence of it differently (by 2026-09-14)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
