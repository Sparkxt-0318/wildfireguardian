# WildfireGuardian autoloop · dev · 2026-09-08T2235Z

| | |
|---|---|
| when | 2026-09-08 22:35 UTC · 2026-09-09 07:35 KST |
| branch / head | `auto/dev` / `9170a37` (see the rebase note below) |
| kind | dev |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 9, done: 32, parked: 1, title-derivable: 1, todo: 99 |

## What happened this lap

Row: **WFG-127** (P0, KCF) — the shape three surfaces asserted was never in the grid.
Preemption cleared first: **WFG-199** (P0, KCF), critic #45's one `fix-before-next-row` item.
Claim commit `97231a1`, work commit **`9170a37`** plus the reviewer's fixes.

⚠ **The push rebased onto three commits that landed while this lap ran** — `7eeccab` (paper lap 21), `1fa0b7f` and `7cfe29a` (the ci-red routine) — so the work commit this report was GENERATED against, `2a1a9de`, no longer exists; it is `9170a37` on `auto/dev`. `97231a1` is unchanged because it was already on `origin`. The ids below are corrected; the gate table's stamp is the pre-rebase run and the post-rebase re-run is recorded at the foot of this section. This is the `ceb43ba` shape, named rather than left to resolve to nothing.

⚠ **`paper/GAPS.md` conflicted and was MERGED, not picked.** Paper lap 21 had independently found `README.md:232` and written that nothing in the repository could have caught it. Both findings are kept: their diagnosis stands as the reason WC-011 was modelled the way it was, and the note now records that the gap it names is closed — by the registration this lap made, in both languages.

`Reviewed by: subagent (block): the lap reported gates green from a run recorded at the CLAIM
commit, where its own new document was still untracked and therefore outside the very
withdrawal gate the row exists to satisfy — the 12b8ac7 failure mode, reproduced by the row
about not asserting what you did not measure.`

## Your decisions, applied

**Nothing new on either channel, and both were checked rather than assumed.** Gmail
`from:siyeong0318@gmail.com subject:"WildfireGuardian autoloop" newer_than:14d` returns 20+
threads and every one holds exactly **one** message, labelled `SENT` — the loop's own reports
arriving back at your own address. `pull_request_read` on PR **#31** returns `[]`.
`decisions.py apply` was not called and `docs/auto/decisions_seen.json` is unchanged.

⚠ **NH-032 and NH-034 were due today (2026-09-08) and are still open**, so this lap obeyed
their standing instruction: no margin value reached a judge-facing surface, and none of
9, 27, 5, 19, 86 entered `README.md:220-239` (critic #45's narrowed `Do NOT edit` note).
The margins this lap measured live in `docs/present_perimeter_buffer_shape.md` and the
artifact only.

## The technical account

**Baseline.** `gates.py --mode quick` exit **0** at `eff2183`. GitHub's own run (CHARTER §4b),
read through the MCP because `curl` is refused here (WFG-119): `auto-gates` run **258** at
`eff2183` is `success`, so there was no red run to take as a first job.

**WFG-199, one command.** `web/finals.html` named `25f6b60` and `git rev-list --count
25f6b60..HEAD` answered **23** against `STAMP_MAX_COMMITS_BEHIND = 30`. `make finals`
re-stamped it onto `97231a1` — the pushed claim commit, so the stamp names a commit reachable
from `origin/auto/dev` as the row requires — and `release/kcf-finals-2026/MANIFEST.json` was
re-pointed. Drift is now **1**.

**The root objection I recorded against my own plan (`hate`), and it was the right one to
test.** The plan was "re-run the sweep at the missing widths", and its load-bearing
assumption was that the sweep is runnable here at all. Cheapest test first:
`--verify-only`, which reproduced the committed canonical arm in **111 s** from the snapshot
store. Only then did I spend four minutes on the real run.

**What was measured.** `scripts/run_present_perimeter_arm.py` gained `--sweep-extra-m`, which
**adds** widths to the five-point grid and defaults to empty, so with the flag absent the grid
is the five widths it has always been. Output went to a **new** file. No committed artifact was
modified: `present_perimeter_arm_uiseong_andong_2025.json` still holds exactly five widths, and
a test asserts it.

Safe totals, 250 m → 3 km: **275, 284, 349, 345, 318, 305, 275, 283.**

- **It is a shoulder, not a spike.** 750 m and 1 km are **349** and **345** — four origins
  apart out of 368, on a grid whose own step is 250 m.
- **All five shared widths reproduced cell for cell**, which is the control that makes the
  three new points readable beside the five old ones, and is a test rather than a claim.
- **The asymmetry, scoped to one grid step.** One 250 m step off the thin end takes the safe
  total **349 → 284**; one step off the thick end takes it **345 → 318**. So 「if you must be
  wrong by about a step, be wrong thick」 is supported. 「Thicker is always safer」 is **not**:
  2 km is back to **275**, where 250 m already was.
- ⚠ **It runs against this project.** 750 m scores **higher** than the committed 1 km, so the
  fair opponent is stronger than this repository reported and the forecast's margin on this
  fire is smaller than the committed margin.

**The withdrawal.** Registered as **WC-011** in the same commit (CHARTER §3.5c): both
languages plus the recited booth sentence, which matches nothing today because an earlier lap
removed it and is registered anyway as a ratchet against a later lap restoring it from an old
report (the WC-004 shape).

**What did not work, and what the machine caught that I did not.**

1. The first draft of the Korean spellings matched **zero** of the four live surfaces — one
   mistyped syllable (뿠 for 뾰), and neither pattern tolerated the markdown emphasis inside
   the phrase. Caught by running the patterns against the files; now a test.
2. Registering WC-011 found a **fifth** surface nobody had listed: `paper/GAPS.md` G8, which
   quoted the withdrawn sentence *and* asserted §4 「still draws the stronger conclusion」 —
   true when written, false once I fixed §4.
3. The booth answer, first written as a `>` blockquote, was counted as **spoken script** by
   `measure_demo_script_pace.py` and re-allocated the whole 300-second budget (3막 1692 →
   1894 syllables). It is now an inline ⚠ note, the document's own convention for a Q&A
   fallback. `tests/test_demo_script_pace.py` caught it.
4. The bundle manifest silently kept the **old** kit: `build_finals_bundle.py` sees only
   tracked printables, and the new kit was untracked when it ran. Caught by
   `tests/test_finals_bundle.py` after the `git add`.

**The reviewer blocked, and four of its findings were real defects in what I had shipped.**
I fixed all of them rather than arguing.

- **Its root objection was correct and is the important one.** My green came from a run
  recorded at `97231a1`, where `docs/present_perimeter_buffer_shape.md` was still untracked —
  and `check_withdrawn_claims.py` scopes itself to `git ls-files`, so the gate this row exists
  to satisfy had never read the file. On the actual commit it was **red**, with four
  unlicensed WC-011 mentions in my own new document. Licensed as record lines; the gated set
  went 935 → 936.
- **WC-011's `artifact` field cited a path that does not exist** (a `buffer_shape/`
  subdirectory flattened away mid-lap). Now correct, and
  `test_the_paths_this_entry_cites_exist` checks it — which immediately caught my own
  explanatory quote of the dead path.
- **WC-011's `claim` field was false about one surface.** It said
  `docs/fair_opponent_line.md` §3 asserted the spike; at `97231a1` that file read 「this file
  asserts neither」, narrowed two days earlier. The surface list is now stated exactly: **two**
  surfaces asserted the shape, three more carried the wording without asserting it.
- **The new gate was inert by construction** — it read the artifact the same lap had just
  added the 750 m row to, so it could only ever arm if a committed artifact lost a row, which
  rule 2 forbids. My "proved to discriminate" was a private unreproducible run. The predicate
  is now a pure function taking the widths, the discrimination runs **in the suite** against a
  narrowed fixture, and the live test asserts the surfaces would pass even with the ban armed
  instead of skipping. Five skips became five real assertions.
- **"reproduces the committed artifact byte for byte" was false** — every run stamps its own
  `generated_utc`, `runtime_s` and `git_commit`. Corrected in three places to say what
  reproduces: the measured cells. ⚠ The phrase also stands in the work commit's message (`9170a37` after the rebase),
  which cannot be rewritten (§3.8); it is corrected here and in the files.

**Gates.** `gates.py --mode full` exit **0**: **1786 passed, 63 skipped, 2 xfailed**. Read
unpiped. The `baseline-verify` WARN is the documented CHARTER §3d state — one NEW tracked
artifact, no MODIFIED file and no changed registry hash of an existing key.

**Numbers.** 18 keys under a new prefix `ppshape_uiseong_` through a new additive registrar.
No `pp_uiseong_` key was touched, so no existing registry hash moved.

**Next row: WFG-194** (P0, KCF) — 창의성 is 20 points on both rubric tables, is named first in
the 심사기준, and reads **0** on `web/finals.html`, `DEMO_SCRIPT_5MIN.md` and the bundle README
while `docs/creativity_card.md` exists and is in none of the six printables sources.

## In plain terms

- **What changed for the project.** Your project told judges that the safe buffer width was a
  single sharp peak, so nobody could pick it on the day. It had only measured five widths, each
  twice as far apart as the next, so it could not actually see that. I measured the three
  missing widths: the top is a wide, flat shoulder, not a peak — and the thin side is a cliff
  while the thick side is a gentle slope, so if you have to guess wrong, guess thick.
- **Why it matters to the judges or the science.** It cost you something, and that is the point:
  at 750 m the model-free opponent does *better* than at the 1 km your headline uses, so the
  advantage your forecast adds on this fire is smaller than the repository said. A judge who
  probes this now finds you already found it and wrote it down, which is worth more than the
  number you lost. Five documents said the old thing; the fifth was found by the machine, not
  by me.
- **What you should do.** Answer **NH-032** and **NH-034** — they were due today, and until you
  do, the student may not say any of the headline margin numbers to a judge. Everything else on
  this lap is done and needs nothing from you.



## Gates

**ALL GREEN** · mode `full` · head `2a1a9de` (pre-rebase) · 2026-09-08T22:34:25Z · re-run post-rebase, see below

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 24.8 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.2 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 0.5 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 439.4 s | 1786 passed, 63 skipped, 2 xfailed, 52 warnings in 437.32s (0:07:17) |

## Commits since the previous report (9c24a8b99d3c7311915e7f5f4f6a5f3c1801504f..HEAD)

- 9170a37 WFG-127 + WFG-199: the shape three surfaces asserted was never in the grid, and measuring it costs us the margin
- 97231a1 claim WFG-199 + WFG-127 (20260908T2117Z)
- eff2183 critic #45: the file that would measure what the model actually buys has been committed all along

## Needs a human (20 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09)
- NH-036 [DECISION] One critic lap told the next one not to edit a file, and that is what kept a false sentence in front of a judge for a window (by 2026-09-10)
- NH-037 [DECISION] The paper's word proxy now stops it a thousand words before your 25-page rule (by 2026-09-10)
- NH-038 [DECISION] Your "product first" rule has spent the last three dev laps on documents, and the readiness line it was written to protect has not moved in five critic laps (by 2026-09-09)
- NH-039 [DECISION] The national wildfire-spread system's manual is an 18 MB PDF the sandbox could not fetch, and one of you can (by 2026-09-12)
- NH-040 [FYI] A critic lap pushed one commit past a red `--assert-reported`, and it is telling you rather than hiding it
- NH-041 [FYI] This lap sent you an email containing only the word PLACEHOLDER, and could not take it back
- NH-042 [DECISION] Two of your own rules collide whenever a withdrawn claim lives in a frozen artifact, and this week they collided three times (by 2026-09-10)
- NH-043 [DECISION] A gate your loop built this morning will go red about twice a day, and the charter tells the lap that meets it to stop working (by 2026-09-09)
- NH-045 [BLOCKER] The staleness gate has closed `auto/dev` to every routine, and the one routine that met it is the one forbidden to clear it (by 2026-09-08)
- NH-044 [DECISION] The claim the paper just retracted is still live on the page the paper cites for it (by 2026-09-09)
- NH-046 [DECISION] Your product's definition-of-done names a command nothing in this project has ever run (by 2026-09-10)
- NH-048 [DECISION] One of the research routine's three literature channels has been dead for two runs, and a working replacement is already proven (by 2026-09-10)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
