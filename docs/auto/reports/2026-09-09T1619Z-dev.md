# WildfireGuardian autoloop · dev · 2026-09-09T1619Z

| | |
|---|---|
| when | 2026-09-09 16:19 UTC · 2026-09-10 01:19 KST |
| branch / head | `auto/dev` / `89da7d3` |
| kind | dev |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 9, done: 33, dropped: 1, parked: 1, title-derivable: 1, todo: 105 |

## What happened this lap

## Your decisions, applied

**Nothing new to apply this lap.** The Gmail search
(`from:siyeong0318@gmail.com subject:"WildfireGuardian autoloop" newer_than:14d`)
returns only the loop's own sent reports — every thread it found holds exactly one
message, none is a reply, and a search for `subject:"Re: WildfireGuardian autoloop"`
over the same window returns nothing. `docs/auto/decisions_seen.json` has an empty
`seen` list, so no message id needed checking. PR #31 carries no comments at all,
so the second channel is empty too. **Twenty-five entries are still open in
`docs/auto/NEEDS_HUMAN.md`** — 19 DECISION, 5 FYI and 1 BLOCKER, counted from the
file's own headings — and five of them (NH-032, NH-034, NH-035, NH-036, NH-043)
are now past their stated dates. ⚠ This paragraph first said 「fifteen」, which was
a number I did not count; the correction is written here rather than substituted,
because a report that quietly swaps one figure for another teaches the next lap
nothing.

---

## The technical account

**Row: WFG-027 (P0, KCF) — 「일정 및 팀원(개인의 경우 제외) 역할 배분의 타당성」, a
named sub-item of 설계와 방법론, worth 20 points on both scoring tables and at
literal zero on every judge-facing surface for five consecutive critic laps.**
Claimed at `89da7d3` (pushed before building, CHARTER §4 step 3). It was the first
`todo` row in table order and the row both `CRITIC_LATEST.md` and `DIRECTION.md`
name next. No stale `in-progress` claim existed to release.

### First, the one `fix-before-next-row` item (critic #51)

`docs/auto/JUDGE_QA.md` Q36 told the student to say to a judge that after
「맞습니다」 this repository has nothing. `docs/oracle_gap.md` had made that false
two hours before the critic read it. The card is rewritten inside the critic's
stated scope and no wider:

- the false clause is marked as a dated correction rather than deleted, in this
  repository's own annotate-never-delete discipline;
- the card now says what `docs/oracle_gap.md` establishes — the arm plans on
  `haz_stack`, a **leave-one-fire-out forward simulation** (a model output on a
  fire the model never saw), the observation `obs_stack` sits unused in the same
  file, and **the oracle is therefore in the grader, not in the planner**;
- **no margin value** (9, 27, 5, 19, 86) appears on it and no re-graded margin is
  asserted; WFG-213 is named as `blocked(NH-052)`;
- **the word 「상한」 is not resolved.** The card says the question is open and
  names **NH-053**. The old bare assertion (「그러므로 91 도 … 완벽한 예보의
  상한이지」) is kept as a dated record and labelled as the thing now in question.

### Then the row

- `scripts/build_timeline_roles.py` walks `git log` on HEAD and writes
  `data/processed/timeline_roles/timeline_roles.json` (newly un-ignored and
  tracked). Five phases whose boundaries are **the calendar gaps in the commit
  record** — 06-15→07-18, 07-26→08-01, 08-12→08-28, 09-01→09-03 — not a narrative
  imposed afterwards. `commits_outside_phases` is **0**, which is what makes the
  table a partition of the record rather than a selection from it.
- `scripts/register_timeline_roles.py` (additive, never `build_numbers.py`
  wholesale) registers **20** `timeline_*` keys. The registry now holds 448
  entries and `verify_numbers.py` re-derives all of them.
- `docs/auto/finals/TIMELINE_ROLES.md` (Korean, DRAFT-labelled per CHARTER §9):
  the phase table, a roles statement, **§3 what these counts do not show**, the
  booth lines with their ❌ list, and §5's regeneration recipe.
- `tests/test_timeline_roles.py`, 10 tests, **9 graded by mutation**: a wrong
  phase day-count, a wrong phase commit count, a wrong trailer count, a wrong
  total, a moved phase start, a broken anchor commit, a dropped 「커밋 수는
  작업량이 아닙니다」 caveat, a removed DRAFT label and a single-site typo in one
  of the two 「662개」 each take the suite red — the last of those is the hole the
  independent reviewer found in my own grading and it is now closed.
- Judge-facing: 일정 answered **0** on `docs/auto/JUDGE_QA.md` and **1** on
  `docs/auto/DEMO_SCRIPT_5MIN.md` (the adjective 일정한). Re-counted at this tree
  against `git show 89da7d3:<file>`, it now answers **4** and **4**. ⚠ It still
  answers **0** on `README.md` and **0** on `web/finals.html`; the screen half is
  named as residue on the row rather than claimed, because `make finals` may only
  stamp a commit already on `origin` (MEMO 2026-09-09) and rebuilding it after this
  lap's commits would have un-made them. `docs/arm_protocol.json` declares the new
  `process_record` arm, additively.

### The root objection I recorded before building (`hate`), and what it changed

**「A schedule an agent assembles is a claim about who did what. The dates are
cheap to get right; the ROLES half is where a fabricated claim would live, and a
judge who catches one invented attribution stops believing the rest — which is
worse than the zero it replaces.」** Cheapest test: every actor claim must reduce
to something the tree can count, or be written as an absence. Three consequences
that are in the shipped file because of it:

1. The AI row cites **the `Co-Authored-By: Claude` trailer count (513 of 662)**
   and the registry caveat says in as many words that this is a **string test and
   not authorship** — the trailer convention only begins in July 2026, so the
   **0** in phases 1 and 4 means 「not marked then」, not 「no tool used」.
2. The **지도교사 row is empty on purpose**: 「이 저장소에는 지도교사 역할에 대한
   기록이 없습니다」. An absence stated, not an absence assumed.
3. **커밋 수는 작업량이 아닙니다** is a caveat the test refuses to let the document
   drop, because phase 5's 417 against phase 1's 55 invites exactly that misreading
   and the difference is a commit habit (the loop commits a claim, the work and a
   report per lap), not seven times the work.

### What did not work, and what I changed because of it

- **A test that re-reads `git log` would have been green on GitHub and red here.**
  This clone opened **shallow at 51 commits**, oldest 2026-09-08, and the document
  is about 2026-05-27. That is the CHARTER §4 class where a gate fires at the clone
  depth. The build therefore reads git **once**, into a committed artifact, and
  every prose check reads the **artifact**; the single test that must read git
  carries a `skipif` on `--is-shallow-repository` with the reason written into it.
  I deepened with `--unshallow` rather than a guessed depth, because the honest
  predicate here is 「this document is about the whole history」;
  `--is-shallow-repository` answers **false** and the clone holds **662** commits,
  which is what licenses any dated claim in this report.
- **The Q&A bank would not accept a new card, and this is a finding, not a
  shortcut.** Card ids must be contiguous **1..35** and **Q36–Q40 are already used
  by the 「아직 답이 없는 질문」 table**, which the contiguity gate cannot see. So
  Q36 is taken and Q41 breaks the gate: **no lap can add a card today.** The 일정
  answer shipped as a labelled block instead, which works and is worse — a judge
  drilling by card will not meet it. Filed as **WFG-216** (P1, infra, in table
  order, not at position 1) and written into `docs/auto/MEMO.md`.
- **The first `make finals-bundle` re-point was wrong and I caught it in the
  gates:** the bundle still named the 09:49Z kit, so the USB a judge takes would
  have carried the false Q36 clause. Re-pointed after staging, twice, because the
  demo-script edit landed after the first kit build.
- **My first placement of the 일정 answer broke a gate that was right.** I put it
  in `DEMO_SCRIPT_5MIN.md`'s 「심사위원 유형별」 table, which
  `tests/test_demo_script_5min.py` pins at exactly five rows — the five critic
  lenses. 일정 is a rubric item, not a judge type. Moved to a paragraph below the
  table; the gate was not touched.
- **`sip`/`factchk` caught a false clause in my own new prose, and then I shipped
  the correction badly enough that the independent reviewer blocked the push over
  it.** Both new blocks said 설계와 방법론 lists 「네 소항목」;
  `docs/auto/RUBRIC.md:31` (Track A) lists four and `:44` (Track B) lists five, so
  the claim is false on one table. I corrected both surfaces — but the
  `JUDGE_QA.md` half landed **after** the 1548Z kit was rendered, so the 53-page
  PDF the bundle had just been re-pointed at still carried the false sentence.
  **The paper a judge picks up off the USB is the thing this row is for**, and for
  the third time in one lap I got the order of 「edit, render, re-point」 wrong.
  Rebuilt at **1611Z** against the corrected text and re-pointed after staging;
  `tests/test_printables.py::test_the_newest_printable_is_not_stale_against_the_tree`
  is the gate that says so and it is green.

### Verification

`gates.py --mode full` — see the gate table in this report for the run at the
pushed head. `baseline-verify` WARNs on the two git-ignored `data/raw/**`
contracts, which is NH-029 and CHARTER §3d working as the author chose. The kit
was rebuilt (`WFG_printables_20260909T1611Z.pdf`, 53 pages) and
`release/kcf-finals-2026/MANIFEST.json` re-pointed after staging, 19 files, **no
payload added or removed** (`DIRECTION.md`'s standing instruction). Two earlier
kits (1532Z, 1548Z) were built and superseded inside this lap; neither entered a
commit, so CHARTER §3.7 is not in play — but three renders for one lap is the
lesson, and it is in `docs/auto/MEMO.md`.

**`web/finals.html` was rebuilt** (`built_utc` 12:49Z → 15:48Z, git stamp
`f9183b0` → `89da7d3`, embedded registry counters 428 → 448 and 370 → 390),
because the 20 new keys aged `tests/test_finals_payload_rederives.py`. It was
built while `HEAD` was the **pushed** claim commit, which is the only moment
MEMO 2026-09-09 allows. Its 일정 count is unchanged at **0** — the screen half of
this row is residue, not delivery.

Reviewed by: **subagent (block)** — 「the lap says it corrected a false rubric
claim on both surfaces before the kit was rebuilt; it did not, and the printed
kit the bundle now points at still carries it」. **First nail:**
`pytest tests/test_printables.py` → `test_the_newest_printable_is_not_stale_against_the_tree`,
`docs/auto/JUDGE_QA.md: manifest 9fabfd89d7… tree 3741a2a3c5…`. It filed a second
independent block (the summary said 일정 answers 「4 and 2」; the measured count on
`DEMO_SCRIPT_5MIN.md` is **4**) and three secondary defects. **All six are fixed,
none argued:** kit rebuilt at 1611Z and re-pointed; the count corrected here and
on the row; the clone-depth contradiction reconciled to **51** (MEMO was right,
`TIMELINE_ROLES.md` §5 said 50); the unsourced 「서식2 Ⅱ-5」 removed, because that
section number appears nowhere else in the tree and the 서식 itself lives outside
it (CHARTER §3.12); and the `web/finals.html` rebuild disclosed above. It also
found a hole in my own test that I had graded as covered — the totals check was
substring containment, so changing **one** of the two 「662개」 escaped it. It now
counts occurrences, and that mutation is caught.

⚠ **What the reviewer checked and could not break, recorded because it is the
half that matters:** it recounted the git history independently (662 / 42 / 513,
and every per-phase triple) and found no fabricated or overstated attribution; it
confirmed the four calendar gaps are real, all five anchor commits resolve inside
their phases, and the 지도교사 absence is genuine; it re-ran all eight of my
mutations and added four of its own; it verified the Q&A-bank id finding is true
rather than a dodge; and it confirmed the Q36 card breaks none of
`CRITIC_LATEST.md`'s four scope limits — no margin value, no re-graded margin,
「상한」 left open and pointed at NH-053.

**Next row:** WFG-212 (P0, KCF) — the README Round-4 section's missing lead
paragraph — unless `DIRECTION.md` is rewritten first. WFG-214 sits above it in
table order and is the mechanism half of the same oracle correction this lap
began on the Q&A card.

## In plain terms

- **What changed for the project.** The judges' scoring sheet asks, by name, how
  you scheduled the work — it is part of a 20-point row on both tables — and until
  today the word 「일정」 appeared **zero** times anywhere a judge could look. There
  is now a Korean page that lays out the five stretches you actually worked in,
  rebuilt from the repository's own history so nobody has to take it on trust, and
  it says plainly what the numbers do *not* mean.
- **Why it matters to the judges or the science.** It turns a blank into an answer
  you can give in one breath, and it does it in this project's own style: the page
  admits there was never a plan document, admits four gaps where school got in the
  way, and refuses to let a big commit count look like a lot of work. I also fixed
  a card in the printed booth kit that was telling you to say the project has no
  answer to its hardest question — it has had one since yesterday lunchtime.
- **What the author should do.** Read the new page once and correct the one row I
  could not fill: **there is no record in the repository of a 지도교사's role**, so
  I left it blank rather than invent it. And five of your decisions are now
  overdue — NH-032, NH-034, NH-035, NH-036 and NH-043. Until NH-032 and NH-034 are
  answered the student still may not say any margin number out loud to a judge.



## Gates

**ALL GREEN** · mode `full` · head **`dc32aad`** · 2026-09-09T16:33:55Z · current at `dc32aad`

⚠ **As generated this line named `89da7d3`, the claim commit `report.py` ran on**, which is never
the head a lap pushes. The pushed head is **`dc32aad`** and it was certified there: `gates.py --mode
full` exits **0** at `dc32aad` (1851 passed, 63 skipped, 3 xfailed, pytest 356.3 s), then
`--assert-head` and `--assert-reported` both exit 0 over 17 substantive paths. Three green full runs
were needed, not one, and the reason is recorded rather than smoothed over: the first was at
`89da7d3` before the report existed, the second at `820a7e6` after the independent reviewer's block
was fixed, and the third at `dc32aad` after a paper lap (`bd0da54`) landed and the push rebase
conflicted on the report-machinery paths. The lap's ids: claim `89da7d3`, work `359fd15` (was
`820a7e6` before the rebase), board `dc32aad`. All four resolve.

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 27.3 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.1 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 0.6 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 365.0 s | 1851 passed, 63 skipped, 3 xfailed, 52 warnings in 363.94s (0:06:03) |

## Commits since the previous report (8506a2d5668f4a91fb0abbdf7ff944c82e9524ad..HEAD)

- 89da7d3 claim WFG-027 (20260909T1517Z)
- 4693540 critic #51: the repository gives two answers to what its headline number means, and the right one is unreachable

## Needs a human (25 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09)
- NH-036 [DECISION] One critic lap told the next one not to edit a file, and that is what kept a false sentence in front of a judge for a window (by 2026-09-10)
- NH-037 [DECISION] The paper's word proxy now stops it a thousand words before your 25-page rule (by 2026-09-10)
- NH-038 [DECISION] Your "product first" rule has spent the last three dev laps on documents, and the readiness line it was written to protect has not moved in five critic laps (by 2026-09-09)
- NH-039 [DECISION] The national wildfire-spread system's manual is an 18 MB PDF the sandbox could not fetch, and one of you can (by 2026-09-12)
- NH-040 [FYI] A critic lap pushed one commit past a red `--assert-reported`, and it is telling you rather than hiding it
- NH-041 [FYI] This lap sent you an email containing only the word PLACEHOLDER, and could not take it back
- NH-042 [DECISION] Two of your own rules collide whenever a withdrawn claim lives in a frozen artifact, and this week they collided three times (by 2026-09-10)
- NH-043 [DECISION] A gate your loop built this morning will go red about twice a day, and the charter tells the lap that meets it to stop working (by 2026-09-09)
- NH-045 [BLOCKER] The staleness gate has closed `auto/dev` to every routine, and the one routine that met it is the one forbidden to clear it (by 2026-09-08)
- NH-044 [DECISION] The claim the paper just retracted is still live on the page the paper cites for it (by 2026-09-09)
- NH-046 [DECISION] Your product's definition-of-done names a command nothing in this project has ever run (by 2026-09-10)
- NH-048 [DECISION] One of the research routine's three literature channels has been dead for two runs, and a working replacement is already proven (by 2026-09-10)
- NH-049 [DECISION] Your critic routine is told to add judge Q&A cards and your own printing gate makes that impossible for it (by 2026-09-11)
- NH-050 [DECISION] You answered two of these questions two days ago and the loop never heard you, because you answered them on the routine page (by 2026-09-10)
- NH-051 [DECISION] The rule your loop has been obeying for three days is not the option it names, and the difference is why one row has been pushed down five times (by 2026-09-11)
- NH-052 [DECISION] The experiment that measures what your own forecast is worth is now one run away, and it would change what 42 means (by 2026-09-12)
- NH-053 [DECISION] The word your front door uses for 42 stopped being right today, and the loop cannot pick its replacement (by 2026-09-12)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
