# WildfireGuardian autoloop · dev · 2026-09-10T1620Z

| | |
|---|---|
| when | 2026-09-10 16:20 UTC · 2026-09-11 01:20 KST |
| branch / head | `auto/dev` / **`3a38395`** (pushed) · `4ab2e07` is the claim commit `report.py` and `web/finals.html` were stamped at, and every measurement in the body was taken at `b8fd6a8`, its work commit |
| kind | dev |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 9, done: 33, dropped: 1, parked: 1, title-derivable: 1, todo: 112 |

## What happened this lap

## Your decisions, applied

**Nothing new this lap.** The Gmail search `from:siyeong0318@gmail.com
subject:"WildfireGuardian autoloop" newer_than:14d` returned 25 threads, all of
them this loop's own sent reports and every one holding exactly one message, so
no reply from you is waiting; `scripts/auto/decisions.py seen` was therefore not
reached for any new id. PR #31 carries zero comments, so the second channel
(CHARTER §6) is empty too. No line went unmapped, because there was no line.

**21 decisions remain open and 13 are at or past their date** (NH-032, 034, 045,
035, 038, 043, 044, 036, 037, 042, 046, 048, 050), with NH-005 and NH-014 carrying
no date at all. NH-046 is now three days past due and holds two of the three
unticked readiness lines; the sprint ends 2026-09-15.

---

## What this lap did

**Row: WFG-228 (P0, science)**, which `docs/auto/DIRECTION.md` named as the next
row to claim. Claimed at `4ab2e07`; the work is **`b8fd6a8`** and the rebuilt board **`3a38395`**. ⚠ Two ids this report quoted in an earlier draft, `5b78ffd` and `163df7f`, no longer resolve: the lap moved `HEAD` back to the pushed claim commit so the finals screen could be stamped at a commit already on `origin` (see the gates note below), and the paper routine's `eff1bc0` then rebased the result. Both ids are named here rather than quietly dropped, because a report that cites an unresolvable commit is the defect this loop has corrected four times.

`docs/oracle_gap.md` §4 has concluded 「the place substantially wrong」 from **IoU
0.394** since it was written, and that reading now stands on the README TL;DR, the
finals screen's first 알려진 한계 card, `docs/auto/JUDGE_QA.md` Q36 at tier **T0**
and `paper/manuscript.md`. Nothing in the repository said what 0.394 should be
compared with, so 「0.394는 무엇에 견준 값입니까?」 — a question the ML-reviewer and
statistician lenses both ask in the first minute — was answered with a number that
had no scale.

**The null.** An area-matched disc: centred on the centroid of the `t = 0` seed the
two stacks agree on cell-for-cell (249 cells, asserted at run time, not assumed),
holding exactly as many cells as that slice's **own** predicted core (692 / 952 /
981 / 1036), ties broken by `(row, col)`, scored against the same observed
footprint under the same nearest-observation matching, the same `p_cut` and the
same cumulative masks `scripts/measure_oracle_gap.py` already applies. **Zero free
parameters**, and the rule *and* the interpretation were both written into the
claim commit `4ab2e07` before the script was run (the WFG-201 discipline).

**Result — the model clears it, by less than the first run said.** At the
27-minute pair the document already quotes: model **0.3941**, disc **0.1554**,
ratio **2.5360**. ⚠ **But that ratio is inflated by an initial condition, and the
independent reviewer caught it — the row did not, and neither did I.** `obs_stack`
is cumulative, so the 249-cell `t = 0` seed is a *subset* of the 937-cell
observation being scored; the model's core contains all 249 by construction
(they are its initial condition, not a prediction) while the disc recovers 92.
Removing the shared seed from all three masks gives the fair comparison:

| headline slice | model | disc | ratio |
|---|---:|---:|---:|
| as first scored | 0.3941 | 0.1554 | 2.5360 |
| **shared seed removed** | **0.2577** | **0.1169** | **2.2044** |

The finding survives in sign at all four slices (1.9538 / 2.2044 / 2.2335 /
2.2286) and loses about a fifth of its size. **2.2044 is the number the project
quotes**, both figures are on both pages, and the caveat band on all 84 keys
refuses one without the other.

**The root objection (`hate`), recorded before the run and sustained by it.** The
row asserts the disc 「destroys the one thing routing depends on (direction), so
the difference between the two IoUs is the model's directional skill and nothing
else」. **I did not accept that and the artifact refutes it.** A disc differs from
the model's core in *two* ways — where its mass sits *and* that it is a circle
rather than an irregular terrain- and wind-shaped blob — so the gap is joint
placement-and-shape skill. The cheapest test was the centroid displacements, and
they say something sharper than the objection predicted:

| centre-of-mass distance, headline slice | cells | metres |
|---|---:|---:|
| seed → observed footprint | 2.250 | 1,124.8 |
| seed → model core | 7.292 | 3,646.1 |
| model → observed | 5.340 | 2,670.2 |
| disc → observed | 2.266 | 1,133.0 |

**By centre of mass the disc is closer to the truth than the model is.** The fire's
mass moved about 1.1 km; the model sent it about 3.6 km. On this fire, at this
slice, the model points in a *worse* direction than doing nothing. What it does
better than a circle is reproduce the **shape and extent** of an elongated
footprint. Both halves are in the caveat band on all 52 keys, because 「2.5× the
null」 quoted alone tells a judge the model has directional skill it does not have.

**What did not work / what I refused to ship.** The row says the persistence and
isotropic baselines have 「never been scored on this metric」. I nearly wrote that
and it is false: `validation/harness.py:703-704` runs both and
`compute_horizon_metrics` (`:496`) scores them with `perimeter_iou` (`:514`). The
true statement is narrower and is what shipped — that is a **polygon** IoU against
a series the harness itself labels 「APPROXIMATE, reconstructed from public
reporting」, not the FIRMS-derived `obs_stack` raster that produced 0.394, so the
two IoU families are not comparable and no persistence null has been scored
against *this* truth. Filed as **WFG-234**.

**Evidence paths.** `scripts/measure_disc_null.py` · `scripts/register_disc_null.py`
· `data/processed/disc_null_yeongdeok.json` · `docs/disc_null.md` ·
`docs/oracle_gap.md` §4c · `tests/test_disc_null.py` · 52 keys under the
`dn_yeongdeok_` prefix of `docs/NUMBERS.json`.

**Tests: 18** (15 before the review, 3 added by it), shown red on their own
mutations. Reversing the centroid inequality fails
`test_the_centroid_overshoot_is_what_the_artifact_says`; raising a disc IoU above
the model's fails `test_the_model_clears_the_null_at_every_slice`. The artifact was
restored and re-hashed against its original sha256 after both. One test I wrote was
too broad and was narrowed rather than deleted: it demanded every centroid value
appear in prose, including `seed_to_disc_cells` (0.057), which is ~0 by construction
and would have forced the page to print a tautology.

**Two rows filed, neither above the P0 block** (DIRECTION's rule): **WFG-235** (P0
KCF, at the end of the P0 block) puts both halves on Q36, and pays a
`make printables` rebuild plus a re-pointed release manifest (NH-049), so it should
ride with another JUDGE_QA row; **WFG-234** (P1 science) is the persistence null.

**Reviewed by: subagent (block).** Root objection: *「the lap certified the wrong
commit and the commit it actually wants to push is RED」* — `.auto/gates.json`
recorded the only full pass at the **claim** commit `4ab2e07`, written before any
of the work existed, and `make verify` at the real head failed
`check-number-collisions` because `docs/disc_null.md` shipped the **largest** disc
radius (18.162) while the only registered radius key held the **headline** slice's
17.355. My own gate run, started before the verdict arrived, hit the identical
failure independently. I fixed rather than argued:

1. **The collision** — every slice's `disc_radius_cells` and
   `disc_intersection_cells` is now registered, and the sentence carries a
   `collision-ok` pragma naming which slice it means, with a new test binding that
   line to `max()`. The reviewer noted the suite did not gate 18.162; it does now.
2. **The seed leak** (the reviewer's second nail, and the substantive one) — the
   `seed_removed` block above, on every slice, registered, on both pages, in the
   caveat band, in the Korean draft, and pinned by two new tests. Its numbers
   reproduce the reviewer's exactly.
3. **A degenerate key I had created** — `dn_yeongdeok_t0min_bare_*`: removing the
   seed from the *seed* slice empties both masks, so the IoU is undefined and the
   registered 0.0 collided with the correct registered value of 1.0 for the seed
   slice's own IoU in `paper/GAPS.md`.
   <!-- collision-ok: 0.0 — this line REPORTS a collision the lap removed; the registry's og_yeongdeok_t0min_iou is 1.0 and this sentence says so. The 0.0 is the withdrawn dn_yeongdeok_t0min_bare_disc_iou, which is no longer registered at all. -->
   The registrar now skips `bare_*` at the seed slice. The three keys existed
   only in the working tree — checked against `HEAD` before removing them, because
   a committed key is annotated, never deleted (§3 rule 7).
4. **A misdescription in this report** — it said 「three gate timings」 changed on
   the finals screen. It was two timings plus one gate's *output line* (the
   region-literal gate now counts 12 operator-facing files, not 11, because
   `docs/disc_null.md` is one of them). Corrected below.

The reviewer verified the overshoot finding, the 2.536 ratio, the
persistence/isotropic correction and the tests independently and confirmed all
four; it found no deletion, no modified artifact, no moved headline and no
forbidden phrasing. The scientific finding survived; the certification and one
undisclosed inflation did not.

**Screen rebuild.** The new keys made the finals screen's 검증 레지스트리 card
stale twice (**453 → 537** entries, **395 → 479** reproducible across the lap) and
two tests said so by name. `make finals` + `make finals-bundle UPDATE=1` fixed it
each time. Measured structurally against the lap's starting head `5f321dc`, **7 leaf
values changed out of 340,892**: the build stamp, the commit, **two** gate timings,
**one** gate output line (11 → 12 operator-facing files scanned) and those two
counts. **No judge-facing figure moved** — no hazard, observation, route or margin
value changed. `timeline_roles.json` was NOT rebuilt — `build_finals.py` only reads
it — so DIRECTION's guardrail is intact, and this clone stayed shallow at **51**
commits, so this report makes no ancestry claim.

**Moves no committed number.** 42, 91, 9 and 27 untouched; no artifact regenerated;
no route run; the README's opening paragraph untouched.

**⚠ Which head this report is stamped from, and why it is the claim commit.**
`report.py` and `web/finals.html` are both stamped at **`4ab2e07`**, this lap's
*pushed* claim commit, and that is deliberate rather than stale.
`tests/test_finals_screen.py::test_the_escape_this_gate_cannot_close_is_still_open`
requires the screen to name a commit that is already an ancestor of
`origin/auto/dev`, because a stamp naming an unpushed commit resolves for the
building machine and for nobody who clones the repository. My first build stamped
the unpushed work commit and that test failed — correctly. The screen is therefore
rebuilt with the working tree fully updated while `HEAD` still sits on the pushed
claim, which is the same operation the `6d4a60b` and `c4eb8d2` laps used (their
screens name `53d1a4e` and `4cb7cf7`, both claim commits). **The commit carrying
this report is the head that was actually pushed**, and the gate table below,
stamped before that commit existed, names `4ab2e07`.

**Gates.** `gates.py --mode full` exits **0** on the commit that was pushed:
**2041 passed**, 64 skipped, 3 xfailed. `baseline-verify` WARNs on exactly the two
git-ignored `data/raw/**` contracts, which is NH-029 and §3d working; the new
artifact is reported as growth by design. `--assert-head` and `--assert-reported`
both exit 0 immediately before the push. GitHub `auto-gates` run **333** was green
at `5f321dc`, the head this lap started from, so CHARTER §4b set no finding #1.

**Next row:** WFG-233 (P0, KCF, minutes plus one registration), which DIRECTION
already names second.

## In plain terms

- **What changed for the project.** Your headline accuracy number, 0.394, had
  nothing to compare it against, so nobody could tell if it was good or bad. Now it
  does: a plain circle of the same size, placed where the fire started, scores
  0.117 on a fair footing. Your model scores **about 2.2 times better than that**.
- **Why it matters to the judges or the science.** 「0.394가 좋은 값입니까?」 was a
  question you could not answer and now can. Two honest catches came with it. The
  first run said 2.5×, but both the model and the circle were being given the fire's
  starting patch for free, and your model swallows all of it while a circle only
  catches part — take that away from both and the real answer is 2.2×; that is the
  number now on the page. The second is more interesting: measured by centre of
  mass, your model pushes the fire 3,646 m when it actually moved 1,125 m. It
  **overshoots**, and by that one measure the plain circle is *closer* to the truth.
  What your model is genuinely good at is the *shape* of the burn, not its
  direction. Saying all of that at the booth is far stronger than saying only the
  good half — a judge who works it out after hearing only the good half stops
  trusting the rest.
- **What you should do.** Nothing for this row. But **21 decisions are waiting for
  you and 13 are overdue** — NH-046 especially, which is three days late and is
  holding two of the three remaining readiness boxes shut, with the sprint ending
  on the 15th.



## Gates

⚠⚠ **THE TABLE BELOW IS THE RUN THAT FAILED, AND IT IS KEPT.** `report.py` stamps the gate table from `.auto/gates.json` at the moment it runs, which here was the RED run at `163df7f` — the run whose `check-number-collisions` failure the independent reviewer had just named. It is left in place because deleting the failing run and printing only the passing one is how a report starts lying about its own lap.

**The run that certifies the pushed tree is a later one: ALL GREEN · mode `full` · head `3a38395` · 2026-09-10T16:33:57Z · 2041 passed, 64 skipped, 3 xfailed** (`verify` PASS 21.6 s, `baseline-verify` WARN on the two git-ignored `data/raw/**` contracts per NH-029 and §3d, `snapshot-verify` PASS, `env-check` PASS, `pytest-full` PASS 329.4 s). `gates.py --assert-head` and `gates.py --assert-reported` both exited 0 at `3a38395` immediately before the push, and the push landed at `eff1bc0..3a38395`.

| step | result | time | last line |
|---|---|---|---|
| verify | FAIL | 21.6 s | make: *** [Makefile:120: check-number-collisions] Error 1 |
| baseline-verify | WARN | 0.1 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 0.3 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | FAIL | 333.0 s | 1 failed, 2040 passed, 64 skipped, 3 xfailed, 52 warnings in 332.06s (0:05:32) |

## Commits since the previous report (5b78ffdad265ea796b8a077bd8046a93ca744de0..HEAD)

(no new commits)

## Needs a human (26 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09, one day past; raised to HIGH by critic #55 on a measured third instance)
- NH-036 [DECISION] One critic lap told the next one not to edit a file, and that is what kept a false sentence in front of a judge for a window (by 2026-09-10)
- NH-037 [DECISION] The paper's word proxy now stops it a thousand words before your 25-page rule (by 2026-09-10)
- NH-038 [DECISION] Your "product first" rule has spent the last three dev laps on documents, and the readiness line it was written to protect has not moved in five critic laps (by 2026-09-09)
- NH-039 [DECISION] The national wildfire-spread system's manual is an 18 MB PDF the sandbox could not fetch, and one of you can (by 2026-09-12)
- NH-040 [FYI] A critic lap pushed one commit past a red `--assert-reported`, and it is telling you rather than hiding it
- NH-041 [FYI] This lap sent you an email containing only the word PLACEHOLDER, and could not take it back
- NH-042 [DECISION] Two of your own rules collide whenever a withdrawn claim lives in a frozen artifact, and this week they collided three times (by 2026-09-10)
- NH-043 [DECISION] A gate your loop built this morning will go red about twice a day, and the charter tells the lap that meets it to stop working (by 2026-09-09)
- NH-045 [BLOCKER] The staleness gate has closed `auto/dev` to every routine, and the one routine that met it is the one forbidden to clear it (by 2026-09-08)
- NH-044 [DECISION] The claim the paper just retracted is still live on the page the paper cites for it (by 2026-09-09)
- NH-046 [DECISION] Your product's definition-of-done names a command nothing in this project has ever run (by 2026-09-10)
- NH-048 [DECISION] One of the research routine's three literature channels has been dead for two runs, and a working replacement is already proven (by 2026-09-10)
- NH-049 [DECISION] Your critic routine is told to add judge Q&A cards and your own printing gate makes that impossible for it (by 2026-09-11)
- NH-050 [DECISION] You answered two of these questions two days ago and the loop never heard you, because you answered them on the routine page (by 2026-09-10)
- NH-051 [DECISION] The rule your loop has been obeying for three days is not the option it names, and the difference is why one row has been pushed down five times (by 2026-09-11)
- NH-052 [DECISION] The experiment that measures what your own forecast is worth is now one run away, and it would change what 42 means (by 2026-09-12)
- NH-053 [DECISION] The word your front door uses for 42 stopped being right today, and the loop cannot pick its replacement (by 2026-09-12)
- NH-054 [DECISION] Four fifths of your front door's headline is limits, and no lap is allowed to decide whether that is the project's strength or its biggest presentation risk (by 2026-09-13)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
