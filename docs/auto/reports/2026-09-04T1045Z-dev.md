# WildfireGuardian autoloop · dev · 2026-09-04T1045Z

| | |
|---|---|
| when | 2026-09-04 10:45 UTC · 2026-09-04 19:45 KST |
| branch / head | `auto/dev` / `613ff3c` (the report was rendered at `b047146`, a review WIP commit that was amended away and does not exist on the branch — see the certification block at the end) |
| kind | dev |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 7, done: 12, parked: 1, todo: 40 |

## What happened this lap

## Your decisions, applied

**Nothing new.** Searched Gmail for `from:siyeong0318@gmail.com subject:"WildfireGuardian
autoloop" newer_than:14d`: 28 threads, every one holding exactly one message — the loop's
own report. PR #31 has zero comments. `docs/auto/decisions_seen.json` still does not exist.
Sixteen NEEDS_HUMAN entries are open; **NH-016's date was yesterday and NH-020 asks whether
these emails are arriving at all.**

---

## Row: WFG-069 (P0, KCF) — the file that forbids a conclusion opened by stating it

Critic #9's single `fix-before-next-row` item, taken ahead of WFG-003 for that reason.

**What was wrong.** `docs/detection_floor.md` §0 opened: 「한국의 산불 탐지는 사실상 전부
사람입니다. 보도된 해에 … 산불 신고의 **99 %가 목격 신고**였고 …」. Two defects in two
sentences: the claim is a stronger human-primacy assertion than anything WFG-063 deleted,
and its only ground is the 99 % clause that §10 of the same file forbids in bold from
carrying any conclusion in a judge-facing document; and 「보도된 해에」 presents as a settled
annual fact a figure the source states as 「올해」 on 2023-04-28 — about four months of
accumulation, which CHARTER §3 rule 5b exists to stop.

**The source was opened.** The article had no URL anywhere in this repository, in a project
whose rule is that a citation is opened before it is used. It is 경향신문 2023-04-28,
「'산불 못 보는' 산불감시카메라···경북에 6000만원짜리 152대」,
<https://www.khan.co.kr/article/202304281446001>, read this lap. It says, verbatim:
「경북에는 152개의 산불감시카메라가 설치돼 있다 … 대당 6000만원 정도다」;
「올해 산림청과 119로 접수된 산불 발생 신고 99%는 목격자 신고로 집계됐다. 감시카메라로
산불을 먼저 발견한 건수는 '0'건 이었다. 지난해에도 감시카메라가 산불을 최초 감지한
사례는 없는 것으로 확인됐다」. So the 99 % is 2023 year-to-date, and the camera zero covers
2022 in full plus 2023 to 04-28. Both scopes are now written where the figures are.

**What was done.**

* **§0 opens on a count instead of a share.** 「조기 발견을 위해 설치된 경북의 산불감시카메라
  152대(2023-04-28 기준, 대당 약 6천만 원)가 산불을 최초로 발견한 건수는 0건입니다 —
  2022년 한 해와 2023년 4월 28일까지 …」 with the URL and the read date, then the sentence
  that does the actual work: **「그 자리에 무엇이 오고 있는지는 이 저장소가 재지
  않았습니다.」** The camera figure rules a source **out**; it elects nothing, which is the
  whole of WFG-063 applied to the sentence WFG-063 left standing.
* **The 99 % clause stays, demoted and labelled.** §10 explicitly permits §0 to keep it as
  background with its source, so it is kept in a ⚠ paragraph that states the agency
  (산림청·119 접수), the as-of date and the scope (「2023년 4월 28일 시점의 연중 누계, 즉
  잠정치」, 원문 표현은 「올해」), says it supports no conclusion in the file, and records the
  withdrawal of the sentence that stood on it.
* **§10's own first line was the same defect and is fixed with it.** 「측정이 우선순위를
  정해줍니다」 was the opening sentence of the section whose two ⚠ blocks say the table is
  **not** a priority table. It now reads 「이 측정이 정하는 것은 위성의 자리 하나입니다」,
  with the change dated and named. Not in the row; one sentence; disclosed here because it
  is WFG-069's shape eight lines from WFG-069's fix.
* **§10's camera row and its ban paragraph** now carry the same scope and source rather
  than 「경북, 2년」 and 「§0 은 … 그대로 둡니다」 (which was stale the moment §0 changed).
* **`docs/auto/JUDGE_QA.md`**: the Q10 block that names the 99 % as 「미등록 잠정치」 now says
  which tally, taken when; the ❌-list occurrence, which is a prohibition rather than a use,
  carries the new `scope-ok` pragma with its reason.

**The gate, and the number critic #9 asked for: 12 of 20.**
`tests/test_external_figures_carry_their_scope.py` makes CHARTER §3 rule 5b mechanical for
the first time: a block printing one of a registered list of **external** figures must also
name that figure's **agency, as-of date and scope**.

The first draft defended itself in its own docstring with an argument this report made too,
and the argument was wrong. It said: critic #9's 2-of-20 result lands on gates whose subject
is an *open class* of sentences, whereas this one's subject is a *closed, enumerated list of
literal figures*, so 「escaping it requires not writing the figure」. **The reviewer measured
it and it is 12/20** (false positives 0/5). A closed registry closes the set of *figures*; it
does not close the set of *spellings*, and the spellings were always the problem. Eight
plausible Korean blocks printed a registered figure and walked through: a line wrap between
the number and its noun — in a repository that hard-wraps every Korean paragraph — 「99
퍼센트」, 「99.0 %」, twenty-six characters of padding between the halves, a markdown table row
licensed by a label in a **different row of the same table** (the shape §10's own trigger
table has), 「백오십이 대」 in Korean numerals, and, worst, the entire 「최초 발견 0건」 half of
the camera figure, which the draft could not see because it looked only for `152` — the very
sentence §0 now opens on.

**Six of the eight are fixed** (wraps, 퍼센트, decimals, a 60-character window, the 0건 half;
and `_blocks` now cuts a markdown table into rows so a neighbouring row cannot license this
one). **Two are not, and are not fixable by this instrument**: a Korean-numeral spelling and
a paraphrase carrying no digits. Both are in the file as executable documentation —
`test_the_escapes_this_gate_cannot_close_are_still_open` fails if either silently starts
being caught, so the docstring and the report have to be corrected together.

**Coverage is two files, not seven.** `GUARDED` lists seven surfaces; only
`docs/detection_floor.md` and `docs/auto/JUDGE_QA.md` contain either figure today. The other
five are there so that moving a figure onto the screen, the booth card or the manuscript
fails here. Counting list entries as coverage is how a report overstates a gate, and the
reviewer caught this report doing it. Outside `GUARDED`, the camera figure appears
unlabelled in about twenty blocks across the loop's own records (BACKLOG, MEMO, NEEDS_HUMAN,
SCORECARD, KCF_READINESS, the dashboard and five reports); those are records of a moment, a
judge is handed none of them, and they are out of scope deliberately — which is a different
thing from being clean. `docs/SESSION19_REPORT.md` is out for the stronger reason that
CHARTER §3 rule 7 says a record is annotated, never edited.

The pragma takes a reason (`<!-- scope-ok: <key> — <why> -->`, refused under twelve
characters) because F48 happened underneath a bare `forbidden-ok` that licensed the figure
while the conclusion standing on it went unread. It is a weak mechanism — a lazy author
writes twelve lazy characters — and it costs one line.

**Nothing in a judge-facing document cites this check**, and Q10d's standing sentence
「부스에서 이 검사를 근거로 들지 마십시오」 covers it too.

### The root objection I recorded before building (`hate`)

> *I am about to make an unregistered newspaper figure the load-bearing opening sentence of
> a judge-facing document, in a repository whose rule 3 is that numbers in prose trace to a
> committed artifact. Swapping a bad unregistered figure for a better unregistered figure
> is not obviously progress.*

**Cheapest test: grep the file for anything downstream that uses §0's figures as ground.**
It found one — §10's trigger table concludes 「트리거 소스가 아님」 for 감시카메라 on §0's
0건. So the camera figure *is* load-bearing for one judge-facing table row, which is what
the objection feared. Two things came out of running it rather than assuming the answer:

1. the conclusion survives, because it rules a source **out** on a count with a stated
   period, which is the same direction and the same strength as the size floor; and
2. that table row was carrying the vaguer scope 「경북, 2년」, which is now
   「2022년 및 2023년 4월 28일까지; 경향신문 2023-04-28」. The objection did not stop the
   change; it improved a line nobody had named.

On rule 3 versus rule 5b: rule 3 governs this repository's own measurements (register or do
not write), rule 5b governs figures from outside (agency + as-of + scope). §10 already read
them that way when it left the 99 % in §0 「배경 설명으로 출처와 함께」. This lap makes that
reading mechanical instead of a sentence in one file.

### What did not work / what was left alone

* **The two 경향신문 figures were not added to `docs/data_sources.md`.** That table is the
  provenance of figures this project stands behind, and these two are explicitly values it
  does **not** register and does not let carry conclusions. Putting them in a source table
  beside registered values would blur exactly the line §10 spent a paragraph drawing. The
  URL and the read date are in §0 instead, where the judge reads the figure.
* **No paraphrase rule was written.** It would be the third hand-rolled family in three
  laps, which is what critic #9's root objection says to stop doing.
* **`docs/SESSION19_REPORT.md` was not touched.** It is a record with a dated withdrawal
  block that already names the 99 % as 「미등록 연중 누계 잠정치」.
* No number moved, no artifact was regenerated, no registry key was added or edited,
  nothing was deleted, the frame did not move.

### Evidence

* `gates.py --mode full` — `verify`, `snapshot-verify`, `env-check`, `pytest-full` all
  **PASS**; `baseline-verify` **WARN**, expected off-laptop, `hard: false`.
* Suite census: **1334 passed, 62 skipped**, **COLD** (first full run in this sandbox, so
  the six SRTM-gated tests skip — WFG-039, fifth consecutive lap). Against critic #9's cold
  reading at `ce31b91` (`1312 / 62`) that is **+22 passed, skips unchanged**, like for like.
* `tests/test_external_figures_carry_their_scope.py` — **30 tests** (counted with
  `pytest --collect-only -q`, after the reviewer caught this line saying 23 for a file that
  collected 22): 7 guarded surfaces, 13 unlabelled blocks that must fail — 7 written by this
  lap and **6 by the reviewer, who had not seen the patterns** — 5 fully labelled blocks
  that must pass, 2 escapes recorded as still open, the two pragma-hygiene rules, and the
  §0 regression.
* Independent catch rate, per critic #9's standing instruction: **12/20** on the first
  draft, false positives **0/5**. Six of the eight escapes are closed; the rate is quoted as
  the reviewer measured it and is deliberately **not** re-measured against its own escapes,
  because a set folded into the suite has stopped being external ground truth.
* Source opened this lap: <https://www.khan.co.kr/article/202304281446001>.

### Reviewed by

**Reviewed by: subagent (block, then fixed): *the lap's defence of its new gate — that a
closed registry of literal figures makes critic #9's 2-of-20 inapplicable, so 「escaping it
requires not writing the figure」 — is false as measured: 12 of 20, and the whole 「최초 발견
0건」 half of the camera figure was undetectable by the gate guarding the sentence that
prints it.***

The reviewer's brief was critic #9's standing instruction, given to it before it saw the
code: write twenty blocks a future author might plausibly write, run them through the gate's
own helpers, and print the rate. It wrote them, ran them, and returned **12/20 caught,
0/5 false positives**, with the eight escapes named. Its first nail is the sentence quoted
above, which was in the test file's docstring *and* in the draft of this report — the lap
asserted its own instrument was strong in the one place a later reader would take it on
trust.

**What the block cost, and what was done about it.** Every fix is above; none of them is a
third spelling family:

* the overstatement is gone from both places and replaced by the measured number, the eight
  escapes, and which six were closed;
* six escapes became rules (wraps, 퍼센트, decimals, a 60-character window, the 0건 half,
  and per-row table blocks) and are now parametrised cases attributed to the reviewer;
* the two that cannot be closed are parametrised as **still open**, so a later widening
  cannot quietly claim them;
* 「23 tests」 was wrong for a file that then collected 22 — it collects 30 now and the count
  was taken from `pytest --collect-only`;
* 「7 guarded surfaces」 was list length, not coverage; the file and this report both say
  **two files** and name the twenty-odd unlabelled blocks in the loop's own records that are
  out of scope on purpose.

**What the reviewer verified and found honest**, recorded so it is not re-litigated: the §0
rewrite replaces an unsourced human-primacy assertion with a count the opened source
supports and carries agency, URL, read date and period; the 152대 / 6천만 원 / 99 % / 0건
quotations, the 2023-04-28 date and the 「올해」→연중 누계 reading are consistent between the
source, §0, §10 and the JUDGE_QA edit; the §10 first-line fix removes a real
self-contradiction; `mandela` finds no eval, metric, split or arm touched; no CHARTER §3
rule is broken by the documents — no artifact regenerated, no registry key moved, nothing
deleted, the frame unmoved. **Its last finding was procedural and is why the gate table
below is not the one it read:** `.auto/gates.json` recorded the run at the diff's base
rather than at HEAD, so `gates.py --mode full` was re-run at the commit actually pushed,
per CHARTER §4 step 8.

### Next row

**WFG-003** (P0) — the finals screen audit and the 5-minute demo script; first `todo` row
in table order, deferred twice now for stated reasons that are both spent. **WFG-067**
after it.

---

## In plain terms

**What changed for the project.** The design document your Q&A cards send a judge to used
to open with 「탐지는 사실상 전부 사람입니다」 — and 300 lines later the same file said in
bold that the statistic behind that sentence may not be used to conclude anything. The
opening now states only what the newspaper actually counted (경북의 감시카메라 152대가
2022년과 2023년 4월까지 산불을 최초로 발견한 건수 0건), with the article's link and the
date it was read, and then says plainly that we did not measure what fills that gap.

**Why it matters to the judges or the science.** A 재난 대응 공무원 judge is the person who
would know whether Korean wildfire detection is "essentially all human" — and we had no
right to tell them it is. Ruling out the camera network is something the source supports;
electing a replacement is not, and now the file says so in its first paragraph instead of
in a footnote. A new check also enforces, for the first time, the rule that any figure
taken from outside this project is written with its agency, its date and its scope.

**What you should do.** Reply to this email with one line per decision below — **NH-020
first**: this is the twenty-ninth report and nothing has ever come back, and the loop cannot
tell whether that means "carry on" or "these are not reaching me". NH-016's cadence change
was due yesterday.



## Gates

**ALL GREEN** · mode `full` · head `c57ef90` · 2026-09-04T10:30:30Z · ⚠ **stale: the gates read `c57ef90`, HEAD is `b047146`** — this table does not certify the pushed tree; re-run `gates.py --mode full` and `gates.py --assert-head` before pushing

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 14.9 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.1 s | make: *** [Makefile:152: baseline-verify] Error 1 |
| snapshot-verify | PASS | 1.8 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 160.2 s | 1334 passed, 62 skipped, 52 warnings in 159.17s (0:02:39) |

## Commits since the previous report (ce31b919e89896da7438908852add7d4c19fda9b..HEAD)

- ~~b047146 WIP for review~~ — amended into `05691d6`; this line names a commit `git cat-file -t` cannot resolve, kept here as the record of what the renderer saw (cf. WFG-067)
- c57ef90 claim WFG-069: the file that forbids the conclusion opens by stating it
- 197dae3 the report certifies the commit it ships in, and says which census was warm
- adf712d critic #9: the gate that stopped one sentence is 2-for-20 against a reader who did not write it

## Needs a human (16 open)

- NH-001 [DECISION] Email delivery needs three repository secrets
- NH-002 [DECISION] Optional: `@claude` on GitHub issues and PRs
- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-010 [DECISION] Expert consultations and the firefighter record (by 2026-09-20)
- NH-011 [DECISION] One real, recorded email send from a network that works in Shanghai (by 2026-09-20)
- NH-012 [DECISION] Portal downloads the loop cannot do (by 2026-10-01)
- NH-013 [FYI] Optional: a stable web address for the visual board
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-016 [DECISION] The critic routine has 30 minutes to land its findings, and needs about 40 (by 2026-09-05)
- NH-017 [DECISION] Three entries were closed on replies the repository cannot see, and one of them was wrong (by 2026-09-06)
- NH-018 [DECISION] Two laps read the same primary sources and disagree on two sentences (by 2026-09-08)
- NH-019 [DECISION] One report time, for one fire, decides what the detection result is allowed to say (by 2026-09-08)
- NH-020 [DECISION] Twenty-five report emails, no reply, and the decision channel has never once been exercised (by 2026-09-06)
- NH-021 [DECISION] Two laps disagree on whether to spend a sprint lap on a gate or on the booth (by 2026-09-06)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.

---

## Certification of the commit this report ships in

The `## Gates` table above was written from the run at the head this lap read. The lap then
re-ran `gates.py --mode full` at `05691d6`, the commit that carries this report and every
change it describes, and it is **ALL GREEN** there too: `verify`, `snapshot-verify`,
`env-check`, `pytest-full` PASS, `baseline-verify` WARN (`hard: false`, expected off-laptop).

**Both censuses, with their temperature, rather than one replacing the other:**

| run | commit | census | temperature |
|---|---|---|---|
| first full run this lap | `c57ef90` (working tree) | **1334 passed, 62 skipped** | **COLD** — first full run in this sandbox, the six SRTM-gated tests skip (WFG-039) |
| re-run at the pushed commit | `05691d6` | **1348 passed, 56 skipped** | **WARM** — an earlier run in this sandbox downloaded the SRTM tile, so those six now run |

The comparable figure is the cold one: against critic #9's cold reading at `ce31b91`
(`1312 passed, 62 skipped`) that is **+22 passed, skips unchanged**, like for like. The warm
run is +14 on top of that because it stops skipping six tests and gains the eight this
lap's reviewer contributed. WFG-039 has now reproduced in five consecutive laps and is still
`todo`; five laps in a row have been careful by hand instead.

The only commit after `05691d6` is the one carrying this block, which is prose. `--assert-head`
and `--assert-reported` were run immediately before the push and both exited 0.
