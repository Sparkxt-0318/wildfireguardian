# WildfireGuardian autoloop · dev · 2026-09-08T0121Z

| | |
|---|---|
| when | 2026-09-08 01:21 UTC · 2026-09-08 10:21 KST |
| branch / head | `auto/dev` / `088203c` |
| kind | dev |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 8, done: 25, parked: 1, title-derivable: 1, todo: 90 |

## What happened this lap

⚠ **Head annotation (CHARTER §4 step 8).** The table above and the gate table below were
stamped at `088203c`, the head `report.py` read — the commit that recovered critic #38's
lap and reopened the branch. **The head this report ships in is `ab4e71e`**, which carries
the WFG-139 work, this report and its images. Both ids resolve. `gates.py --mode full` was
re-run at `ab4e71e` after the commit and is **ALL GREEN, exit 0** — `1708 passed,
63 skipped, 2 xfailed`, **COLD**, read unpiped, `data/raw/` 201,187 B before and after —
with `--assert-head` and `--assert-reported` both exiting 0 immediately before the push,
and the push needed no rebase. Two commits ship this lap: `088203c` (WFG-173, the recovery
and the screen rebuild) and `ab4e71e` (WFG-139).


## Your decisions, applied

**Nothing new arrived on either channel, and this is the fifth lap in a row that says so.**
The Gmail search (`from:siyeong0318@gmail.com subject:"WildfireGuardian autoloop"
newer_than:14d`) returns 201 threads and every one of them is a single message this loop
sent to itself; there is no reply from you in any of them. PR #31 has no comments.
`docs/auto/decisions_seen.json` still records `"seen": []`.

⚠ **NH-032 and NH-034 were due today (2026-09-08) and are now overdue.** They decide what
your headline margin means against a fair opponent, and until they are answered the student
is forbidden to say *any* of the three fair-opponent margins that exist. **NH-035, NH-038,
NH-043, NH-044 and NH-045 are due tomorrow.** NH-045 is new, filed by critic #38 last night,
and this lap cleared the thing it is about — but the recurrence it asks you to decide on is
still open. Reply to this mail with one line per item, `NH-###: <your decision>`.

## The lap in one line

`auto/dev` was **closed to every routine** when this lap arrived, and is open again;
**WFG-139 is closed after eleven laps of measuring it**; and critic #38's whole lap, which
was parked on a red branch, is back on `auto/dev`.

## First: the branch was shut, and the routine that found it could not open it

Critic #38 (2026-09-07T2319Z) measured a green baseline at `1bca8ed`, then committed its own
report — `docs/auto/` only — and turned the gates red. `web/finals.html`'s build stamp sat at
exactly `STAMP_MAX_COMMITS_BEHIND = 30` commits behind `HEAD`, and `behind` counts commits,
not changes. So the **next commit from any routine** was going to be red, including the claim
commit CHARTER §4 step 3 tells a dev lap to push before it does any work. That lap parked
itself on `auto/red/2026-09-07T2319Z` per §3.9 and filed **NH-045**, and it could not fix it:
`make finals` regenerates an artifact outside `docs/auto/`, which the critic routine's
standing prompt forbids in those words.

This lap did what that addendum and NH-045 ask the next lap to do:

- **Recovered the parked lap** onto `auto/dev` by fast-forward. Nothing in it was red on its
  merits — its single failure was the staleness assertion its own report commit tripped. So
  **WFG-173, WFG-174, WFG-175, critic #38's `CRITIC_LATEST.md`, its `DIRECTION.md` note, its
  `KCF_READINESS` and `SCORECARD` rows, NH-045 and its 401-line report are on `auto/dev`**
  rather than on a branch nobody reads.
- **Rebuilt the screen and re-pointed the bundle** in the same commit (WFG-152). `behind`
  went 31 → 2.
- ⚠ **The screen is stamped `1bca8ed`, not this lap's own head, and that was forced by a
  second gate in the same file.** My first build stamped `3ec7bbb` — the recovered critic
  commit — and `test_the_escape_this_gate_cannot_close_is_still_open` failed: a stamp naming
  an unpushed commit resolves for the builder and for nobody who clones. So the screen was
  built in a detached checkout of `1bca8ed`, the newest commit already on the remote. Critic
  #38 touched only `docs/auto/`, so the payload is the one this tree generates.
- `STAMP_MAX_COMMITS_BEHIND` was **not** raised. `tests/test_finals_screen.py:754` grades the
  threshold with two literals deliberately not derived from it, so raising it fails rather
  than slides past.

## The row: WFG-139 — done, and it was not the row's own diagnosis that closed it

DIRECTION position 1, the blocker of readiness line **R3**, and the reason `JUDGE_QA.md`
Q28 told a judge something the loop had measured to be false eleven times.

**The root objection I recorded before building (`hate`).** *Editing the one test the row
names to `dem_source="synthetic"` closes the caller ten laps identified, and my evidence that
it is the ONLY caller would be a grep I designed, scoped and graded myself — the exact
failure this repository logged twice this week. The cheapest test that settles it is to make
the download physically impossible and let the run name the offenders.*

That objection is why this lap shipped a mechanism instead of a one-line edit, and it was
right.

**What was built.** `tests/conftest.py` — the repository's first `conftest.py` — installs a
session-wide guard refusing `connect`, `connect_ex` and `create_connection` to any
non-loopback address, and to any address named in a `*_proxy` variable even on loopback. It
raises a `RuntimeError` subclass on purpose: `_download_srtm_tile` catches `OSError` and
re-raises `FileNotFoundError`, which callers read as 「no tile, fall back」, so an `OSError`
would have been swallowed and the test would have passed while silently measuring something
else.

**What did not work, and it is the whole lesson.** My **first draft exempted loopback**. That
is the obviously safe rule, it is what every version of this pattern does, and here it blocked
**nothing**: this sandbox routes egress through `HTTPS_PROXY=http://127.0.0.1:38639`, so the
S3 fetch the guard exists to stop goes to 127.0.0.1. I did not reason my way to that — I ran
the guard, watched the offending test pass, watched `data/raw/` grow by 34 MB anyway, and went
looking for why. **A guard is not installed until you have watched it refuse the thing it was
written to refuse.** That is WFG-156's vacuous-binding class landing inside the mechanism
written to end a measurement problem, and it is in MEMO.

**What the machine then found, which no lap had.** A cold full run flagged **three** tests:

| test | verdict | what happened |
|---|---|---|
| `test_spread_warmup.py::test_model_config_ignition_radius_increases_initial_burn` | real network use | the row's named caller; now `dem_source="synthetic"` — both assertions are about the ignition disc, `fuel_source` was already synthetic |
| `test_raster_ingestion.py::test_auto_dem_prefers_srtm_when_tile_available_else_synthetic` | real network use | **a second caller no lap had ever named.** It asserted 「either outcome is acceptable」, so it passed under both; split into three offline tests |
| `test_finals_acts.py::test_the_four_acts_advance_in_a_real_browser` | **not** a network use | talking to a Chromium this repository launched, on a loopback port. The guard's first draft was wrong; that is what the proxy clause fixed |

`data/raw/` was byte-for-byte unchanged across that run, and across the final gate run.

**The second half of the mechanism, because the guard alone is not enough.**
`conftest.pytest_sessionfinish` fails any run in which `data/raw/` grew — the measurement
eleven laps took by hand, taken automatically, and it sees paths the socket guard cannot. It
was graded before it was trusted: a throwaway test writing a few kilobytes turned a passing
run's exit status to 1. ⚠ **A test could not have done this job.** pytest runs files in
collection order and `test_no_network_in_tests.py` sorts before both offenders, so a test
there reads the disk before they run. The first draft of that file did exactly that and would
have passed for the wrong reason on the tree that motivated it.

**A finding that fell out of it, asserted rather than fixed.** `load_dem(source="auto")`
documents 「try ngii → srtm → synthetic in order」 and catches only `NotImplementedError`. A
missing tile raises `FileNotFoundError`, which propagates — so **on a clean clone with no
network, `source="auto"` raises instead of falling back**. Invisible for as long as the
download always succeeded. Filed as **WFG-176** and pinned by a test whose docstring says it
contradicts the contract, so a later change to it fails rather than passes silently.

**Judge-facing surfaces.** `docs/clean_clone_gates.md:27` read 「No network, no keys, no
`.env`.」 and the first three words were false from 2026-09-03 to 2026-09-08; the section now
carries the method, the result, the second mechanism and an **enumerated** list of what the
guard does not see. `JUDGE_QA.md` Q28 gains a dated correction and Q40 — which scripted the
student to answer 「아니오」 — is rewritten to today's answer, with the six terrain tests that
still skip on a clean clone kept as the honest remainder. Kit rebuilt at `20260908T0114Z`,
bundle re-pointed in the same lap (WFG-152).

## The independent reviewer returned `block`, and four of its five findings were right

Its first nail was already stale by the time it landed — the bundle re-point had not yet
happened when it read the tree, and the gates are green at that head now. The rest were not
stale, and all four are fixed rather than argued (CHARTER §4 step 5):

1. **Q28 said three tests reached the network. Two did.** The third was the guard's own false
   positive, and Q40 — edited by me twelve minutes later in the same file — said so correctly.
   **A lap contradicted itself inside one file**, which is the Q30/Q35 shape CHARTER §3.5c
   exists for. Q28 now says two; Q40 states the three-flagged / two-real / one-our-mistake
   split in the student's own voice; the kit was rebuilt at a new stamp.
2. **§3.5c registration was missing.** Registered as **`WC-010`**, in both languages, in this
   lap, with probe sentences taken verbatim from the shipped text, the family in
   `tests/test_withdrawn_claims_registry.py` and §5i of `docs/withdrawn_claims.md`. The sweep
   read **933 gated files** and passed. ⚠ **WC-010 is the first entry here that withdraws a
   claim for being too WEAK.** Q40's 「아니오」 was an honest confession of a real defect, and
   the lap that closed the defect left the confession on the printed card. A stale
   self-criticism costs a judge's trust exactly as a stale boast does, and nothing in this
   project was watching that direction — hence the family name `stale_self_criticism`.
   ⚠ **What I deliberately did NOT register, with the reason:** 「No network, no keys, no
   `.env`.」 is now **true**, because this lap made it true. Registering a spelling that is
   true turns the gate red on every correct sentence forever. A statement made true gets a
   dated correction, not a forbidden pattern.
3. **Unregistered numbers on a judge card.** `201,187 B` was the size of `data/raw/`, which is
   git-ignored — a different number on every machine and **zero** on the clean CI clone the
   card promises reproduces. Removed from `JUDGE_QA.md` entirely and replaced in
   `clean_clone_gates.md` by the difference, which is what the hook actually measures and what
   a stranger can check. `12,345` is gone the same way.
4. **A vacuous test inside the file written to prove the guard is not vacuous.**
   `test_the_escape_hatch_is_named_and_unused` searched for the literal
   `WFG_TESTS_ALLOW_NETWORK=1` — the **shell** spelling only. A workflow writes
   `WFG_TESTS_ALLOW_NETWORK: 1` and Python writes `os.environ[...] = "1"`, so it could not
   fire on two of the three file classes it scanned, and it never scanned `.sh` at all. It now
   searches the variable NAME across `.yml/.yaml/.sh/.py/.toml/.cfg/.ini` plus the `Makefile`,
   and a second test **grades the detector** on a planted tree carrying all three spellings
   plus a decoy.

The reviewer also named five things the guard does not close — import/collection time, UDP,
`getaddrinfo`, an undeclared loopback forwarder, and `pytest` run outside `tests/`. None was
in my docstring. All five are now written into the conftest docstring and
`docs/clean_clone_gates.md`, and filed as **WFG-177** with the two that matter most named. The
`localhost`-spelled-proxy hole it found is closed in code.

**Reviewed by: subagent (block → fixed): a lap that shipped a real mechanism and then wrote
a judge-facing sentence its own record contradicted, registered none of what it withdrew, and
put a byte count that reads zero on a clean clone onto a card promising reproducibility.**

⚠ One reviewer note I could **not** reproduce and am recording rather than repeating: it
reported that a full suite run rewrote the tracked `release/kcf-finals-2026/MANIFEST.json` as
a side effect. My final gate run left the working tree identical to the index. It may have
been an artefact of the half-finished state it read. Unconfirmed, and not filed as a row.

## Gates, and what ships

- `gates.py --mode quick` at baseline: **exit 0**, ALL GREEN at `1bca8ed`, read unpiped.
- `gates.py --mode full` before the claim push: **exit 0**, ALL GREEN, `1695 passed,
  56 skipped, 2 xfailed`, **warm**, no download.
- `gates.py --mode full` on the work: **exit 0**, ALL GREEN, `1708 passed, 63 skipped,
  2 xfailed`, **COLD**, and **the run downloaded nothing** — `data/raw/` 201,187 B before and
  after, which is the first cold gate run in this repository's recorded history that can say
  that. `baseline-verify` WARN is the documented CHARTER §3d state. `check_withdrawn_claims`
  PASSED, **10 claims over 933 gated files**.
- The 63rd skip is new and is mine: `test_auto_dem_prefers_srtm_when_the_tile_is_cached`,
  which is the honest form of a branch that used to download its own precondition.
- GitHub's own run (CHARTER §4b, read through the MCP; `curl` is 403 here, WFG-119):
  `auto-gates` run **223** at `1bca8ed` is `success`; runs 216–223 are 7 `success`,
  1 `cancelled`, **zero `failure`**. No §4b finding, so no CI job preceded the row.
- Clone: `git rev-parse --is-shallow-repository` = **true**, `git rev-list --count HEAD` =
  **50**. No ancestry or reachability claim is made anywhere in this report.

**Next row:** **WFG-167** (P0) — DIRECTION position 2, and the judge drill's finding that 42
cards later the bank has no answer to 「누가 책임집니까」, while one of the five judges is a
public-sector disaster-response official. WFG-128 follows it.

## In plain terms

- **What changed for the project.** Two things. Your branch was **stuck** — a check about the
  demo screen's build date had reached its limit, so the next commit from any of your
  robots would have failed it, and the one that found this was the one routine not allowed to
  fix it. It is unstuck, and last night's critic lap, which had been parked on a side branch,
  is back where you can read it. Then the long-running one: for five weeks your test suite
  quietly downloaded a 25 MB terrain file from the internet every time it ran clean, which is
  against your own rules and which your booth card admitted to. It cannot any more — not
  because I found the line, but because I made the tests unable to reach the internet at all
  and let the machine find the lines. It found **two**, and only one of them was the one your
  backlog had been naming for eleven laps.
- **Why it matters to the judges or the science.** 「A stranger can re-run this」 is a claim you
  make on the printed card, and it was not fully true. It is now, and the card says exactly how
  far it goes — six terrain tests still skip on a fresh clone, and that is written down rather
  than smoothed over. The part I would tell a judge about is this: my first version of the
  block looked obviously correct and blocked nothing, because this machine sends its internet
  traffic through its own address. I only found that out by running it and watching the file
  download anyway. That is the difference between a project that checks and a project that
  believes.
- **What you should do.** **NH-032 and NH-034 were due today and are unanswered** — they decide
  what your headline number means, and until you answer, the student may not say any of the
  fair-opponent margins out loud. **NH-045 is new and due tomorrow**: your loop got itself
  wedged last night and needs a one-word rule from you so it cannot happen again (option A or
  B). Reply to this mail with one line per item, like `NH-032: A`.



## Gates

**ALL GREEN** · mode `full` · head `088203c` · 2026-09-08T01:19:32Z · current at `088203c`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 19.3 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.1 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 0.5 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 294.0 s | 1708 passed, 63 skipped, 2 xfailed, 52 warnings in 292.40s (0:04:52) |

## Commits since the previous report (1bca8eda39fb14475917ee23ef293400906442ba..HEAD)

- 088203c WFG-173: the branch was closed to every routine, and the routine that found it could not open it
- 3ec7bbb critic #38: the screen's stamp reached the gate's exact limit, and the report that said so tripped it

## Needs a human (18 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09)
- NH-036 [DECISION] One critic lap told the next one not to edit a file, and that is what kept a false sentence in front of a judge for a window (by 2026-09-10)
- NH-037 [DECISION] The paper's word proxy now stops it a thousand words before your 25-page rule (by 2026-09-10)
- NH-038 [DECISION] Your "product first" rule has spent the last three dev laps on documents, and the readiness line it was written to protect has not moved in five critic laps (by 2026-09-09)
- NH-039 [DECISION] The national wildfire-spread system's manual is an 18 MB PDF the sandbox could not fetch, and one of you can (by 2026-09-12)
- NH-040 [FYI] A critic lap pushed one commit past a red `--assert-reported`, and it is telling you rather than hiding it
- NH-041 [FYI] This lap sent you an email containing only the word PLACEHOLDER, and could not take it back
- NH-042 [DECISION] Two of your own rules collide whenever a withdrawn claim lives in a frozen artifact, and this week they collided three times (by 2026-09-10)
- NH-043 [DECISION] A gate your loop built this morning will go red about twice a day, and the charter tells the lap that meets it to stop working (by 2026-09-09)
- NH-045 [BLOCKER] The staleness gate has closed `auto/dev` to every routine, and the one routine that met it is the one forbidden to clear it (by 2026-09-08)
- NH-044 [DECISION] The claim the paper just retracted is still live on the page the paper cites for it (by 2026-09-09)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
