# WildfireGuardian autoloop · critic · 2026-09-06T1112Z

| | |
|---|---|
| when | 2026-09-06 11:12 UTC · 2026-09-06 20:12 KST |
| branch / head | `auto/dev` · **reviewed head `b2bdaf0`** · this report ships in `828bbae` plus the one-line header correction that follows it. Every id in this row resolves with `git cat-file -e`, which is the whole of **WFG-132**, filed below about a dev report where two did not. |
| kind | critic |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 7, done: 16, parked: 1, todo: 73 |

## What happened this lap

Critic lap #26. Reviewed head `b2bdaf0`; window `b70e464..b2bdaf0` plus the 24 h to
2026-09-05T10:00Z for the gate, CI, report and decision checks. No code, test, artifact,
figure or `docs/NUMBERS.json` entry was touched; writes are confined to `docs/auto/`.

Reviewed by: this routine is the independent reviewer. Every measurement below was taken in this
sandbox at the head named beside it, and the ancestry measurements were taken **only after**
recording the clone's depth, which is the point of this report.

## Your decisions, applied

**Nothing new this lap, and this is the eighth consecutive lap that sentence is true.** The Gmail
search `from:siyeong0318@gmail.com subject:"WildfireGuardian autoloop" newer_than:14d` returns 25
threads and every one of them is a message this loop sent to you (`sender` and `toRecipients` are
both your address, label `SENT`), each holding exactly one message with no reply on it. No thread
count is quoted as a figure here: Gmail's `resultCountEstimate` answered 201 to me and 66 to the
1000Z dev lap hours earlier, so it is not a number this report can stand behind, and the 0917Z lap
already recorded that. `docs/auto/decisions_seen.json` is unchanged, so `scripts/auto/decisions.py`
had nothing to parse or apply. PR #31 carries no comment in the `NH-###:` form.

Five decisions of yours are still open: **NH-032** and **NH-034** (which fair opponent, and what the
surfaces then say), **NH-035** (the three-hour release rule against the three-hour dev grid),
**NH-014** (run the booth recipe once on the real laptop) and **NH-005**. Nothing this lap adds to
that list. **I filed no new NEEDS_HUMAN entry**: every finding below is agent-doable and none of them
moves a committed number.

## Findings, ranked

### F1 — WFG-115's premise is false, and five critic laps published it as measured fact

`41498ef`, the commit the judged screen's provenance line names and which critics #20, #21, #23, #24
and #25 all recorded as unreachable from this branch, **is an ancestor of `HEAD`**. Verified three
independent ways here, on a clone deepened to **300** commits:

    git merge-base --is-ancestor 41498ef HEAD                       -> exit 0
    git rev-list HEAD | grep -c 41498efbf0679276c140b3cbfc0819e5265e7733 -> 1
    git rev-list --count 41498ef..HEAD                              -> 277
    git branch -a --contains 41498ef  -> auto/dev, origin/auto/dev, origin/Main, and four others

The object sits **277** commits back. Every earlier test was taken inside a shorter graph than that:
critic #20 at the sandbox's default **depth 50** (`docs/auto/BACKLOG.md`, WFG-115's original text);
critic #21 after `git fetch --deepen=120`, 170 commits, which is where WFG-119 was filed;
critic #24 at **250**, writing 「re-measured on a clone deepened to 250 commits so the shallow
boundary is not the confounder」. 250 is less than 277, so it still was.

Where the false finding reached: `docs/auto/KCF_READINESS.md` R1's cell and five tick-count blocks;
`docs/auto/DIRECTION.md`; `docs/auto/SCORECARD.md`, where critic #20 **docked Track A 구현 및 유용성
from 18 to 17 naming it**, a deduction that is withdrawn in this lap's row; and
`docs/finals_screen_v2.md` §4.3, the screen's own explainer, which now cites the row.

What survives is real and much smaller: `registry.built_at_commit` is a commit at which
`docs/NUMBERS.json` held **153** entries, while the card beside it prints **383**. The line is
**stale by construction and mislabelled**, not unreachable, because `build_numbers.py` is
deliberately not re-run (WFG-040). WFG-115 drops **P0 -> P1** and is re-scoped to that.
`docs/auto/JUDGE_QA.md` Q35 — 「이 저장소에 실제로 있고 현재 브랜치에서 닿는 커밋입니다」 — is
**correct as written** and must not be edited, which the old row instructed a lap to do.

Cited: `docs/auto/BACKLOG.md` WFG-115, WFG-119; `docs/auto/KCF_READINESS.md` R1;
`docs/finals_screen_v2.md` §4.3; `docs/auto/JUDGE_QA.md` Q35; `web/finals.html:434`.

### F2 — the Q&A bank now warns the student against a screen that is correct (fix-before-next-row)

`docs/auto/JUDGE_QA.md` Q30 is **T0**: answered from memory, no paper, and it is the question about
why today's numbers should be believed. WFG-113 rebuilt the screen at `1ec1d06`. The warning about it
did not move. At this head, all three of these sentences in the bank are false:

| the bank says | measured here at `b2bdaf0` |
|---|---|
| 「화면은 아직 **326 · 268** 을 인쇄합니다」 | `web/finals.html` prints **383** / **325** |
| 「화면을 가리키라는 조언은 학생을 낡은 숫자로 데려갑니다」 | screen and registry agree exactly |
| 「서로 다른 수가 셋 있습니다」 | two: the draft's 295 / 261 / 34, and 383 / 325 |

`docs/NUMBERS.json`, counted in one process: **383** entries, **325** reproducible, **58** not.
A wrong warning outliving a correct repair is more expensive than the drift it was written about,
because nothing looks broken and the student is drilled to disown the one surface that agrees with
them. A dated correction note is on Q30 as of this lap so it is not rehearsed; **WFG-117** is
re-scoped, moved to position 2 (this lap's one row move) and is the one `fix-before-next-row` item.

### F3 — a dev report and its email name two commits that do not exist (WFG-132, P1)

`docs/auto/reports/2026-09-06T1000Z-dev.md` header: `branch / head` = `auto/dev` / `a464d1a`; body:
「work `a68c2d0`」; the 10:15:47Z email to you: 「auto/dev @ a464d1a」. On a 300-deep clone,
`git cat-file -t` answers `fatal: Not a valid object name` for **both**, and `origin/auto/dev` is
`b2bdaf0`. Two other dev reports in the window name rewritten heads (`9184a72`, `bf4cb31`) but both
**say so in the same cell**; this one does not. Three commits in the same window are titled 「the
report names the head it ships in as well as the head it reviewed」, so the loop has been repairing
this by hand once a lap for three days and it recurred on the fourth. Loop hygiene, so under
CHARTER §14b it is filed as **P1** and waits behind R1, R3, R4, R7, R8 and R9. It is not scheduled.

### F4 — no gate finding and no CHARTER §4b finding

`gates.py --mode full` is **ALL GREEN** at `b2bdaf0` in this fresh cloud sandbox: `1565 passed, 62
skipped`, cold, 199.1 s, against critic #25's cold `1562 / 62` — **+3 passed, skips unchanged**, like
for like. `verify`, `snapshot-verify`, `env-check` PASS; `baseline-verify` WARN is the documented
CHARTER §3d behaviour. Read through the GitHub MCP because this routine's `curl` returns 403
(WFG-119): `auto-gates` runs **140 to 168** on `auto/dev`, spanning 2026-09-05T20:21Z to
2026-09-06T10:14Z, are **22 `success` and 3 `cancelled`** — `ef61e9b`, `9ebf5a5` and `785ba13`, each
superseded within minutes by a push that is green. **Zero `failure`.** Run 168 at this head is
`success`. `--assert-reported` exits 0 across the window and every dev, paper and critic report of
the last 24 h carries `Reviewed by:`; the four without it are `manual`, your own laptop.

**Critic #25's falsifiable test, branch (1), answered.** The screen prints 383 / 325, its stamp moved
`5f9a3b8` -> `62b58e1` (**6** commits behind `HEAD`, was 29), and WFG-119's ten-hour clock is reset.
Branch (2) does not fire: the `fix-before-next-row` mechanism worked on a two-command repair.

### F5 — factchk: the window introduces no new claim about the world

Every new prose line in `b70e464..b2bdaf0` outside `docs/auto/` is about this repository's own
artifacts: `docs/finals_screen_v2.md` §4.3 (the new payload gate, its three tolerated fields and its
four grading mutations), `paper/GAPS.md` G5 and G8, `paper/README.md`, and 26 changed lines of
`paper/manuscript.md`. No agency, no external figure, no new citation, no date about the 2025 fire.
There is nothing for `factchk` to verify against a source this lap, and I say so rather than inventing
a check. The manuscript's changes all move in the honest direction: §4.5 stopped printing two arrival
times that are values of the withheld arm, and its closing sentence now says **one** repository
document still draws the stronger conclusion from the five-point sweep rather than two.

### F6 — the judge drill, and one question that has no answer in a file

Ten hardest questions run against files only. Nine resolve to a committed file. The one that does not
is **Q36 (T0, critic #23)**: 「비교하신 예보 경로는 정답을 미리 본 예보 아닙니까?」 — the bank's own
answer says the honest reply is 「맞습니다」 and that what follows it is empty, because the value this
project's *own* model buys has never been measured (WFG-125, open). That is the same gap critic #24
named as its root objection and it is unchanged; **WFG-129**, the cheapest test of the headline, is
still `todo` and still in `paper/GAPS.md` G7, a file CHARTER §4 step 1 does not tell a dev lap to
read. No new JUDGE_QA entry is needed for it: Q36 already carries it, marked.

## Root objection (`hate`)

**「Re-run rather than read」 is this loop's central honesty claim, and it is doing less work than
every report that says it implies.** It defends against a *stale* reading. It does nothing against a
*systematically wrong instrument*. Five consecutive critic laps re-ran the same ancestry command
inside the same truncated commit graph and got the same wrong answer; the two laps that suspected the
instrument deepened by an amount they picked — 120, then 250 — rather than by the amount that would
settle the question, and reported the same wrong answer with more confidence than the lap before.
The finding then propagated into the readiness record, the direction page, the scorecard's own
deductions and the screen's explainer, where later laps read it as established.

**The cheapest test, and it is one line:** write no reachability or ancestry claim until
`git rev-parse --is-shallow-repository` answers `false`, and record the depth beside the claim. It
would have caught this on 2026-09-05T1700Z. It is now a rule on `docs/auto/DIRECTION.md` and a clause
in WFG-119's done-when.

The corollary for the next critic, which I am putting in writing against myself: a finding that three
laps have repeated is not thereby confirmed. It has been *copied*. The measurement that matters is
the one that could have come out the other way.

## Scorecard deltas (`b70e464` -> `b2bdaf0`)

**Track B: 84, flat.** 연구 목적 17, 설계와 방법론 15, 데이터 19, 창의성 15, 제출 자료 18 — all
held. The window is one booth-screen repair and one manuscript lap; neither changes what a research
judge scores. 데이터 holds at 19 rather than rising, and the reason is F1: the same window that added
a genuinely good reproducibility gate (`tests/test_finals_payload_rederives.py`, structural
re-derivation, three tolerated build-provenance fields, integrity block checked against the builder's
own `GATES` constant, graded on four mutations) also showed the reviewer's own instrument was never
validated.

**Track A: 78, flat, and two rows move in opposite directions.**

- **구현 및 유용성 17 -> 18.** The judged screen is correct for the first time in sixteen windows and
  now has a gate that re-derives its payload instead of reading strings; and critic #20's deduction
  on this row for the `41498ef` stamp is withdrawn. Why not 19: R7's kit holds one of the five
  printables it names (WFG-130), and the provenance line, while reachable, is stale and mislabelled.
- **제출 자료 15 -> 14.** F2. These five judges never open the registry; they hear the student and
  they look at the screen, and the bank now drills the student against it.

**KCF readiness: 4 of 11 (R2, R4, R5, R6), unchanged — zero ticks for a THIRD consecutive critic
lap.** Direction was right again: the window cleared critic #25's one item and reset a clock. **R1 is
now one row from tickable** — two of its three recorded defects are gone (the counts, fixed; the
ancestry, never real) and it waits on WFG-110's six unmapped keys alone, which I did not re-measure
this lap and therefore do not tick on.

## In plain terms

**What the judges would see today.** The screen you demo from is right for the first time in two
days: it prints 383 registered numbers and 325 that re-derive, and those are exactly the numbers in
the file behind it. The booth kit, the bundle and every gate are green. The one thing that is wrong is
on paper: your Q&A bank still tells you the screen is out of date, and it is not, so if you rehearse
Q30 today you would talk yourself out of trusting your own demo.

**What changed since yesterday.** Two things. The loop ran the two-command screen repair I asked for
yesterday, and it worked. And I found that a fault this loop has been reporting to you for five days
— that the screen names a commit which is not on this branch — **was never true.** The commit is
there; it sits 277 commits back, and every lap that checked was looking at a shortened copy of the
history that only went back 50, then 170, then 250. Nothing on your screen was ever wrong about it.
I have withdrawn the finding, taken it off P0, and written the rule that stops it recurring.

**What you should do.** Nothing, and no decision is waiting on you from this lap. Your five open
questions are unchanged, and NH-032 is still the one that unblocks the most. If you open one file
today, make it `docs/auto/JUDGE_QA.md` Q30: read the new note at the bottom of that card, because it
is the sentence I would not want you rehearsing wrong at the booth.



## Gates

**ALL GREEN** · mode `full` · head `b2bdaf0` · 2026-09-06T11:02:13Z · current at `b2bdaf0`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 15.2 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.1 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 1.4 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 199.1 s | 1565 passed, 62 skipped, 52 warnings in 198.26s (0:03:18) |

## Commits since the previous report (-15)

- b2bdaf0 board and lap images rebuilt on the rebased head, so they show the paper lap too
- f0670d0 report 2026-09-06T1000Z-dev
- 507c183 the reviewer found the anti-pattern I had just congratulated myself for avoiding
- 1ec1d06 WFG-113: the judged screen's numbers, and a gate that re-derives them instead of reading them
- 32e5310 the report's own gate-table duration collided with a retired count
- 4dac720 the manuscript was quoting two numbers under a rule it cites elsewhere as its reason for silence
- 62b58e1 claim WFG-113 (20260906T0920Z)
- 9c2b903 the critic report names the head it ships in as well as the head it reviewed
- ef61e9b critic #25: the booth kit shipped, and it was measured against its own sources rather than its own definition of done
- b70e464 the corrected count had not reached the one section the author reads
- 5f927b0 the reviewer blocked on the one number in the doc that no artifact had produced
- 3e92b69 WFG-007: the booth printables exist, and the gate that makes them safe reads the page
- 3800e28 claim WFG-007 (20260906T0620Z), releasing a claim that was 19 seconds over the bar
- 3656bea the critic report names the head it ships in as well as the head it reviewed
- 9ebf5a5 critic #24: the cheapest test of the headline number lives in the one file no dev lap reads

## Needs a human (8 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
