# WildfireGuardian autoloop · critic · 2026-09-10T0523Z

| | |
|---|---|
| when | 2026-09-10 05:23 UTC · 2026-09-10 14:23 KST |
| branch / head | `auto/dev` / `9b7d21c` |
| kind | critic |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 9, done: 33, dropped: 1, parked: 1, title-derivable: 1, todo: 107 |

## What happened this lap

Critic lap #56. Reviewed head `9b7d21c`, window `435c54d..9b7d21c` (about 22 hours; this
clone is shallow at 50 commits and I did not deepen it, so no ancestry claim appears
anywhere in this report). I changed no code, no test, no data, no figure and no
`docs/NUMBERS.json`; everything I wrote is under `docs/auto/`.

## Your decisions, applied

**Nothing new on either channel, and both were checked directly rather than by paging
through sent mail.** Gmail: `from:siyeong0318@gmail.com subject:"WildfireGuardian autoloop"
newer_than:14d` returns 25 threads and **every one of them is a single message the loop
itself sent** (all carry `SENT` and `INBOX`, all are report emails; the search preview
returns the five oldest messages of each thread, so a reply would show as a second message
and none does). PR #31: **zero** comments. So no `NH-###:` line was parsed, none was
applied, `scripts/auto/decisions.py` was not invoked with `apply`, and
`docs/auto/decisions_seen.json` is unchanged from the 23 entries it already held.

⚠ **Six decisions are open and four of them are now past their own stated date.** NH-032 and
NH-034 were due **2026-09-08** (three days past). NH-037 and NH-046 were due **2026-09-10**,
which is today. NH-035 was due 2026-09-09. NH-053 is due 2026-09-12. **NH-046 is the one that
matters most to the product**: `docs/auto/KCF_READINESS.md` has stood at 8 of 11 for thirteen
consecutive critic laps and both remaining agent-reachable lines are downstream of that single
unanswered question. No lap can move it.

## Findings, ranked

**Finding #1 is NOT a red gate, because nothing is red.** `gates.py --mode full` exits **0**
at this head (`verify` PASS 21.2 s, `snapshot-verify` PASS, `env-check` PASS, `pytest-full`
**1958 passed**, 64 skipped, 3 xfailed in 373.8 s; `baseline-verify` WARNs on the two
git-ignored `data/raw/**` contracts, which is NH-029 and CHARTER §3d working). GitHub
`auto-gates` runs **283 to 315** hold **no `failure`**: four `cancelled` (284, 306, 310, 312),
each superseded by the next push within minutes, and run **315** is `success` at this exact
head. CHARTER §4b therefore sets no finding #1, for the fifth consecutive lap. Every dev
report in the window records `Reviewed by:` (all eight checked by grep; seven record
`subagent (block)` and spend commits acting on it, the eighth records `pass`), and
`gates.py --assert-reported --base 435c54d` exits **0** over 66 substantive paths.

**1. `docs/oracle_gap.md` §4 reads a matching gap as forecast bias, and this window made that
document judge-facing.** This is the lap's one `fix-before-next-row` item and the repair is
minutes. Confirmed against `data/processed/oracle_gap_yeongdeok.json` in one process rather
than inherited from WFG-215: the four non-seed slices pair `t=180` with the observation at
333 min (gap 153), `t=360` with **the same** 333 (gap 27), `t=540` with **the same** 333 again
(gap 207), and only `t=720` with 1005 (gap 285); `observed_cells` reads 937, 937, 937, 987
while `predicted_cells` rises 692, 952, 981, 1036. So three of the four terms of the series
0.74 / 1.02 / 1.05 / 1.05 are a growing numerator over a constant denominator, and §4's
sentence 「at 3 h the simulation is **26 % under**」 compares a forecast with an observation
153 minutes later than the forecast time. **WFG-215 filed this on the ground that it was
「not a judge-facing surface」. That ground is false at this head:** `docs/oracle_gap.md` is
the cited evidence anchor from **seven places on four judge-facing surfaces** (`README.md:43`,
`:256`, `:331`, `:784`; `docs/auto/JUDGE_QA.md:1419`, which is card **Q36 at tier T0** inside
the printed booth kit; `paper/manuscript.md:512`; and, **new at `c4eb8d2` this window**,
`web/finals.html:2069`, where it anchors the **first** card of the 알려진 한계 panel). Six of
the seven send the judge with the same phrase, 「셀 단위로 재어 두었고」. ⚠ The row's own 「Do」
is wrong in one clause and I corrected it on the row: per-slice `obs_time_min` is **not**
registered (only the headline key is), so printing 333 and 1005 beside each ratio would write
numbers CHARTER §3 rule 3 forbids. The preemption is the registered half only. ⚠ The
misreading is **contained**: `26 %` and `0.74` count 0 on all six judge-facing surfaces.

**2. `docs/auto/DIRECTION.md` named two `done` rows, and the row a dev lap would have fallen
back to is a trap.** DIRECTION's 「next three rows」 named WFG-218 and WFG-220; both closed at
`c4eb8d2`. Table order after them reaches **WFG-007**, whose own status cell reads 「the agent
half is done; the student half is not」 and whose remaining clauses are a printing task that
belongs to the student and WFG-130. **This lap's one CHARTER §3b reorder moves WFG-128 to the
head of the P0 block** (P0 above P0, so §14's ordering rule is untouched). I re-read WFG-128's
defect at the file rather than at the row: `docs/multi_region.md:187-192` now carries a
600-minute budget clause for the **future-aware** arm that was not there when the row was
filed, and still does not say that the **fire-blind** arm is scored under **no** budget, still
does not say the Uiseong-Andong bucket is empty under one rule, and still carries no pointer to
`docs/present_perimeter_arm.md`. `README.md:146` still sends a judge to that page for
「완전한 분할」. P0, `minutes`, judge-facing, and the author decided it eight days ago (NH-031 A).

**3. The `Do NOT edit` bounds went stale inside this window, exactly as CHARTER §14c predicts.**
Critic #55 wrote `README.md:263-342` for the Round-4 fair-opponent block and held that it was
unchanged. Re-measured here with `grep -n '^### ' README.md`: the block is now **`README.md:270-349`**.
WFG-218 inserted **seven** lines at line 65 (the TL;DR schedule bullet) and six at line 389,
and the seven above pushed the block down by exactly seven. The note is re-stated at the new
bounds, and a scan of 270-349 for each of 9, 27, 5, 19, 86 as a standalone integer finds
**zero**. This is the third time in four laps that these bounds have moved, and it is the whole
argument for the expiry rule.

**4. WFG-217 acquired a judge-facing edge, recorded on the row rather than filed as a new one.**
`web/finals.html`'s new 개발 일정과 역할 card renders its as-of stamp from
`data/processed/timeline_roles/timeline_roles.json` → `last_commit_date`, which is **2026-09-09**,
so the screen prints 「2026-09-09 커밋까지 세었습니다」 and phase 5 renders 「2026-09-03~진행 중」.
That is the artifact WFG-217 forbids a lap to rebuild from a deepened clone, and the finals are
**2026-10-24**. The guardrail now freezes a date a judge reads. Still not urgent: a rebuild from
a **fresh full clone** writes seven-character anchors and passes (critic #53 measured that), so
the row's deadline is the **2026-10-16 freeze** rather than the paper.

**5. No new backlog row and no new NEEDS_HUMAN entry, deliberately.** Every finding above
belongs to a row that already exists (WFG-215, WFG-128, WFG-217, WFG-204). CHARTER §7 counts
that as a lap doing its job, and the root objection below is why filing more would have been
the wrong move.

## The root objection (`hate`)

**The loop's P1 queue is a write-only ledger, and the critic is its main producer.**

Counted at this head across the whole backlog table in one process, not read from a report:
**P0 is 64 done, 11 todo, 4 blocked. P1 is 6 done and 100 todo.** Six P1 rows have ever closed.
The trend inside the window is not noise: every critic lap in the last 24 h added one or two
rows and every dev lap removed one or two, so the `todo` count went **104 to 107** while `done`
went 32 to 33, in a window that closed **three P0 rows**. CHARTER §14b releases the P1 infra
block only when R1, R3, R4, R7, R8 and R9 all tick; **R3 is the only unticked one and it cannot
tick without the author** (NH-046, due today, open); the sprint ends **2026-09-15**.

**Cheapest test, and it is already run:** count P1 rows closed during the sprint. Six. If the
rule were serving the product the queue would be draining; instead it grew by three in the
window that closed three P0 rows.

**Two things this objection is not saying.** It is not saying §14b is wrong: the P0 half is
working, visibly, and this window is the evidence. And it is not saying the P1 rows are
worthless: this lap's own preemption **is** a P1 row, filed two days ago on the explicit ground
that it was not judge-facing, and that ground has since become false. The queue is not dead
weight; it is a queue whose items become urgent unpredictably and which nothing is allowed to
drain. **Appended as a measurement to NH-038 and raised MEDIUM to HIGH**, rather than filed as
a fourteenth question, because NH-038 is the author's own open question about this rule.

## Probes that found nothing, recorded so the next lap does not spend them again

**1. ⚠ I nearly filed a false finding and am recording the near miss rather than the
conclusion.** Q16b and Q20a, both **T0** and both printed, tell a judge 「커밋된 실행 기록
**28**개가 전부 `nothing_was_sent: true`」. My first count globbed `MANIFEST.json` and answered
**16**, which looks exactly like a wrong number on a printed card. It is not:
`tests/test_responsibility_and_privacy_cards.py::_manifests` counts `MANIFEST.json` **and**
`RUN.json` (the PHASE-6 replay writes the second spelling), 16 + 12 = **28**, and
`test_nothing_was_ever_sent_and_the_cards_count_the_manifests` asserts the card's literal equals
that count in-process. Re-counted across both spellings: **28**. The gate's own docstring
records that its first draft made my mistake. The card is right and the gate binds the number,
which is the pattern the rest of this repository should copy.

**2. Every T0 card cites a file.** All **19** T0 cards carry at least one file path and every
path resolves, except four pieces of module-relative shorthand the surrounding prose establishes
and one that is **deliberately** absent: `email_sent.json` is Q16b's negative evidence, and
`scripts/send_dispatch_email.py:270` confirms it is what a real send would write. WFG-111 would
have found nothing today.

**3. The release bundle is current with this window's work.** `release/kcf-finals-2026/MANIFEST.json`
was re-pointed in the same commit as the screen rebuild, and **nineteen of nineteen** declared
hashes match their sources, including the rebuilt `web/finals.html`. The printed kit's **seven of
seven** sources still hash equal, because `README.md` and `web/finals.html` are not printed
sources. R7 and R9 hold on content, not on existence.

## `factchk` on the window's new prose

The window adds **three** external URLs and **none is on a judge-facing surface**: two arXiv
identifiers and one Frontiers DOI, all inside `docs/auto/reports/2026-09-09T1122Z-critic.md` and
`docs/auto/BACKLOG.md`, which are record class. No card, no README block, no manuscript sentence
and no screen string acquired a new claim about the world. The window's added judge-facing prose
is claims about this repository's own artifacts, and the load-bearing ones re-derive: the screen's
`timeline` payload equals the committed artifact field for field, and the nineteen bundle hashes
and seven printed-source hashes match. The paper routine applied `WC-013` across routines
(`manuscript.md:676`, `:825`), which is CHARTER §5c's ratchet working outside the lap that
registered it.

## Readiness and the manuscript budget

**8 of 11, ZERO lines ticked in 24 h, for the THIRTEENTH consecutive critic lap.** R12 is the
author's (NH-014); R3 is `blocked(NH-046)`; R11's row WFG-024 is held by §14b until R3 ticks.
**R1 was re-measured rather than restated**: the screen's build stamp names `4cb7cf7`, **5**
commits behind this head against a 30-commit limit.

**NH-037 has three words of margin, re-measured:** `paper/check_paper.py` prints
`{"body_words": 8997, ...}` against a 9,000 hard fail, up from **zero** at `7dabdef`, and it is
up because the paper lap **spent** words rather than gained room, rewriting three sentences
shorter while applying `WC-013`. NH-037 came due today and is open.

## Scorecard deltas

**Track B 94, HELD, on two moves that offset. Track A 93 to 94.**

- **제출 자료 UP on both tracks (B 19 to 20, A 17 to 18).** Critic #55's pre-registration, paid on
  exactly the condition it stated, verified at the payload, the gate assertions and the nineteen
  bundle hashes rather than at the lap's report.
- **데이터 수집·분석·해석 DOWN on Track B only, 20 to 19**, on finding #1. Track A has no such row,
  which is why the two tracks part company this window.
- **설계와 방법론 19, HELD, and I decline critic #52's pre-registered raise to 20 with a reason.**
  Both of its conditions are met, but 「변수의 정의와 제어의 적절성」 is one of this row's named
  sub-items and WFG-128 is open against exactly it. Re-pre-registered on WFG-128 closing, which is
  why that row was moved to the head of the P0 block.
- **연구 목적 18, 개발 목적 19, 구현 및 유용성 20, 창의성 18, all HELD.**

## In plain terms

- **What the judges would see today.** Better than yesterday on the two surfaces they actually
  stand in front of. The finals screen now shows how the project was built, in five phases read
  straight from the repository's own history, and it now says out loud that the dots on the map
  are graded by the same forecast that drew them. Both are the kind of admission that has been
  this project's strongest card all along. The one thing I would not want a statistics judge to
  click on is the analysis page the screen sends them to: one paragraph in it compares a
  three-hour forecast against a five-and-a-half-hour observation and calls the difference a
  forecast error. It is not; it is a timing mismatch. That is a few minutes of writing and the
  next lap does it first.
- **What changed since yesterday.** Three of your biggest remaining to-do items closed in one
  window, which is the best day of the sprint so far. The gates are green here and on GitHub, the
  USB bundle and the printed booklet both still match the repository exactly, and the score went
  up on one of the two scoring tables and stayed level on the other.
- **What the author should do.** Answer the questions. **NH-046 and NH-037 came due today**, and
  NH-032 and NH-034 are three days past. NH-046 is the single reason your readiness checklist has
  not moved for thirteen straight days: no amount of work by the loop can move it, only you can.
  One reply with six lines clears all of them. The easiest way is to open Claude Code on your
  laptop in this repository and say `decisions`.



## Gates

**ALL GREEN** · mode `full` · head `9b7d21c` · 2026-09-10T05:06:08Z · current at `9b7d21c`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 21.2 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.2 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 1.8 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 373.8 s | 1958 passed, 64 skipped, 3 xfailed, 52 warnings in 372.04s (0:06:12) |

## Commits since the previous report (-15)

- 9b7d21c the board is rebuilt over both laps, not just this one
- 6c4b86e report: 2026-09-10T0415Z dev
- c4eb8d2 WFG-218 and WFG-220: the schedule reaches the front door and the screen, and the screen says what grades its own dots
- 739172c the paper report names the head it ships in, and keeps the run it was stamped from
- 0d2e56c the paper's account of its own retirement gate was wrong in a way the gate cannot see, and the reviewer found a fifth surface in this routine's own file
- 4cb7cf7 claim WFG-218 and WFG-220 (20260910T0317Z)
- 4af246d the critic report names the head it ships in, and keeps the run it describes
- 55466eb critic #55: the lap that looked dead had finished, and this lap withdraws its own first draft to say so
- 7dabdef report: 2026-09-10T0142Z dev
- 17499a8 the independent reviewer's block: the gate graded itself, and the list was short by two
- 8bd4b2d WFG-222: every page that offers the dispatch sheets now says what fire they were made on
- 49ac16e claim WFG-222 (20260910T0017Z)
- 02715eb the critic report names the head it ships in, and keeps the run it describes
- 88cb2e7 critic #54: the front door corrected its one affirmative claim and the four surfaces that carry it by design did not
- 3eec471 the dev report names the head it was pushed on, and keeps the run it replaces

## Needs a human (25 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09, one day past; raised to HIGH by critic #55 on a measured third instance)
- NH-036 [DECISION] One critic lap told the next one not to edit a file, and that is what kept a false sentence in front of a judge for a window (by 2026-09-10)
- NH-037 [DECISION] The paper's word proxy now stops it a thousand words before your 25-page rule (by 2026-09-10)
- NH-038 [DECISION] Your "product first" rule has spent the last three dev laps on documents, and the readiness line it was written to protect has not moved in five critic laps (by 2026-09-09)
- NH-039 [DECISION] The national wildfire-spread system's manual is an 18 MB PDF the sandbox could not fetch, and one of you can (by 2026-09-12)
- NH-040 [FYI] A critic lap pushed one commit past a red `--assert-reported`, and it is telling you rather than hiding it
- NH-041 [FYI] This lap sent you an email containing only the word PLACEHOLDER, and could not take it back
- NH-042 [DECISION] Two of your own rules collide whenever a withdrawn claim lives in a frozen artifact, and this week they collided three times (by 2026-09-10)
- NH-043 [DECISION] A gate your loop built this morning will go red about twice a day, and the charter tells the lap that meets it to stop working (by 2026-09-09)
- NH-045 [BLOCKER] The staleness gate has closed `auto/dev` to every routine, and the one routine that met it is the one forbidden to clear it (by 2026-09-08)
- NH-044 [DECISION] The claim the paper just retracted is still live on the page the paper cites for it (by 2026-09-09)
- NH-046 [DECISION] Your product's definition-of-done names a command nothing in this project has ever run (by 2026-09-10)
- NH-048 [DECISION] One of the research routine's three literature channels has been dead for two runs, and a working replacement is already proven (by 2026-09-10)
- NH-049 [DECISION] Your critic routine is told to add judge Q&A cards and your own printing gate makes that impossible for it (by 2026-09-11)
- NH-050 [DECISION] You answered two of these questions two days ago and the loop never heard you, because you answered them on the routine page (by 2026-09-10)
- NH-051 [DECISION] The rule your loop has been obeying for three days is not the option it names, and the difference is why one row has been pushed down five times (by 2026-09-11)
- NH-052 [DECISION] The experiment that measures what your own forecast is worth is now one run away, and it would change what 42 means (by 2026-09-12)
- NH-053 [DECISION] The word your front door uses for 42 stopped being right today, and the loop cannot pick its replacement (by 2026-09-12)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
