# WildfireGuardian autoloop · paper · 2026-09-08T0847Z

| | |
|---|---|
| when | 2026-09-08 09:18 UTC · 2026-09-08 18:18 KST |
| branch / head | `auto/dev` / `d36ad21` |
| kind | manual |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 8, done: 26, parked: 1, title-derivable: 1, todo: 96 |

## What happened this lap

## What happened this lap

Incorporated **`c456da7..d36ad21`** — thirteen commits, **four** files outside `paper/` and
`docs/auto/`: `docs/clean_clone_gates.md`, `release/kcf-finals-2026/MANIFEST.json` and two new
test files. **`docs/NUMBERS.json` is not in the diff and neither is any artifact under
`data/processed/`**, so no result section could gain a number, a table or a figure.

**No sentence in `paper/manuscript.md` changed, and none needed to.** The file is byte-identical
to lap 18's. `body_words` 8,994, figures 8, tables 4, references 29, gaps 7 — all unchanged, so
`built_pages_inputs` still matches and the recorded **23 pages** still anchors to the document it
was measured on. No re-measurement was owed and none is claimed.

## Sections changed, and why

None in the manuscript. The lap's output is `paper/measure_render_gap.py` (new),
`paper/GAPS.md`'s lap-19 section, `paper/README.md`'s lap-19 blocks and `paper/STATE.json`.

**The manuscript makes four assertions about this repository's own instrument and the diff
touches the machinery behind all four**, so each was re-run rather than reasoned about:
`check_number_collisions` OK; `check_withdrawn_claims` PASSED, 10 claims over 933 gated files —
the same 10 and 933 lap 18 recorded; both prose gates glob tracked `.md`, so 「this manuscript is
scanned by both gates like any other document」 holds; the registry's scope is still `.md`/`.html`
only, so 「a data file or a generator escapes it」 holds. §3.5's 「three times」 survives and was
re-derived **from the commits** rather than from the ledger's summary (lap 17's error): the diff
registers no new `WC-###` at all, touching only two fields of the existing `WC-010`.

## Figures

**None added, none redrawn.** `make_figures.py` was run and reported `drawn [F1 … F9]; skipped
(artifact absent) []`; `git status paper/figures/` is empty afterwards, so all nine were redrawn
and all nine are byte-identical. `F9_present_perimeter` is still drawn, committed and
**unreferenced** (gap **G8**, NH-032 open).

## Gaps

**None opened, none closed. Still 7.** One was considered and deliberately not opened: 「Data and
code availability」 points at a moving GitHub branch with no tag and no DOI, which an IEEE venue
will ask about. It is not an *evidence* gap — nothing in the paper is unsupported by it — and it
is already tracked as **WFG-031** and **WFG-015**, both author-only after the finals and needing a
Zenodo login this sandbox does not have. Recorded in `GAPS.md`'s prose rather than as a marker, so
the gap table keeps meaning 「evidence the artifacts cannot supply」.

## The finding: the word proxy does not count 2,403 of the words the document renders

Laps 13 to 18 all had their writing shaped by the 9,000-word proxy and five reports described that
pressure without anyone measuring what the proxy counts. `paper/measure_render_gap.py` now
re-derives it from the shipped `.docx`:

    rendered in the built document :  11397
    counted by build_docx.py       :   8994  (body_words)
    rendered and NOT counted       :   2403  (21.1% of the document)
      figure captions 789 · table captions 439 · table cells 213 · headings 119
      references (29 entries) 802 · title page 39 · citation markers 2
    of that, prose a lap could move a sentence INTO: 1441  (16.0% of body_words)

`body_words` is incremented in `build_docx.py`'s paragraph branch (`:187`) and list-item branch
(`:177`) and nowhere else. Three consequences:

1. **The proxy is bypassable in one keystroke** over those 1,441 words — move a sentence into the
   caption above it and the gate reports a shorter document. That is the `built_pages_inputs`
   bypass shape lap 9's reviewer killed, except nothing stands against this one. **This lap did
   not use it and no lap may.**
2. **F5's caption already carries a methodological justification §4.3 does not restate.** No claim
   is made about how it got there: this clone is shallow at depth **50** (measured today), so
   `git log -S` answers the clone boundary, not the history (CHARTER §4). Moving it into §4.3
   costs ~30 words against 6.
3. **It sharpens NH-037.** The question is not 「the proxy stops us a thousand words early」 but
   「the proxy is tight on prose and blind on captions and tables, and the words-to-pages curve
   holds only at this caption density」. The remedy that removes the class is **WFG-116's one
   `apt` line in `.github/workflows/auto-gates.yml`**, not a bigger number.

## And it shrinks NH-037's case by one line, which is owed

Laps 13–18 each recorded declining the same optional §3.5 illustration — *that registration keeps
finding live copies a hand sweep missed* — and cited the decline as a cost of the budget. **It is
retired as a want, not deferred a seventh time.** This diff carries the other direction:
`WC-010`'s `say_instead` field was wrong from `ab4e71e` to `82ec346` (2 h 16 min on author dates),
and the scan enforcing `WC-010` over 933 files reads `.md`/`.html`, so **it does not read the file
`WC-010` lives in**; a hand grep of the subject found it and registration structurally could not.
Both directions hold — what dies is the one-directional form all six drafts had, which is the
register `WC-009` itself forbids. **The rest of NH-037 stands: laps 13, 15 and 16 each funded a
mandatory correction by compression, and lap 13 shipped two wrong sentences doing it.**

## What did not work

**Nothing was compressed to bank margin.** It was considered — 6 words of margin, no mandatory
correction to fund, the cheapest lap in which to realise compressible stock — and refused, because
every compression is an edit to litigated prose for no gain in truth and two of the three
sentences laps 13 and 15 got wrong were compressions. The margin is 6, reported as 6, and never
bare: it is a margin against a proxy that does not measure the rule it stands in for.

## Reviewed by

**Reviewed by: subagent (block)** — root objection: *「the one lap whose whole product is the
finding that a word counter mis-counts words shipped a word miscount of its own, in the same
table, replicated into three files — and nothing in the repository can catch it, because the lap
committed no script that re-derives any of these six integers.」*

**It was right and I had reached the same reconciliation failure independently; it found more than
I had, and I fixed all of it rather than arguing.** The first draft decomposed the Markdown
*source* instead of the rendered document — 773 / 431 / 153 / 40 — which sums to **2,412** against
the 2,403 it claimed to decompose. Two distinct errors: heading lines counted with their `#`
markers attached (153 against the 119 that render), and the builder's rendered `Figure N. ` /
`Table N. ` labels, which the source does not contain. Its first nail was one command:
`153 130`, and 130 is still not 119.

**The remedy is the script, not a corrected integer.** `paper/measure_render_gap.py` prints the
table and exits **1** when its parts do not reconcile with its total. Four further findings, all
applied: `/usr/bin/soffice` does exist here (24.2.7.2) and only Writer is missing, so the
phrasing now says so; the 2 h 16 min is on *author* dates and `82ec346` was rebased to a committer
date giving 3 h 03 min, now named; 「re-rendered byte-identically」 cited `git status`, which cannot
distinguish that from never having run the script, so the run's own output is quoted beside it;
and 「79 % of the page」 is now 「79 % of the words that render」, words not being page area in the
one section whose point is that the two differ.

Everything else the reviewer tried to falsify held: the headline 11,397 / 8,994 / 2,403, the
`:177`/`:187` branch claim, 「three times」, the `WC-010` window and `ab4e71e^` holding no `WC-010`,
the depth-50 refusal of the F5 ancestry claim, both gates' scope over the manuscript, and NH-041
being about this loop's report email rather than the delivery layer §6 describes. It explicitly
did **not** block the margin-at-6 decision.

## Counts

`body_words` **8,994** of 9,000 (margin **6**, unchanged) · figures 8 · tables 4 · references 29 ·
gaps 7 · built pages **23** under Carlito, inherited on an unchanged digest rather than
re-measured · rendered words 11,397.

## Gates

`gates.py --mode full` exit **0**, read unpiped: verify PASS, snapshot-verify PASS, env-check
PASS, **pytest 1735 passed, 63 skipped, 2 xfailed**; the `baseline-verify` WARN is the documented
CHARTER §3d sandbox state. `check_paper.py` exit 0. `--assert-head` and `--assert-reported` run
immediately before the push.

## In plain terms

The paper says exactly what it said this morning — I checked every sentence the new code could
have made false and none of them moved, so I changed none.

What I did find is that the 9,000-word limit your paper is being squeezed by only counts 79 % of
the words on the page: figure captions, tables, headings and references are invisible to it, 2,403
words' worth. So the squeeze five reports have complained to you about is partly an artifact of a
bad ruler — and one of the reasons I gave you for NH-037 turns out not to have been a good one,
which I have withdrawn rather than repeated.

**NH-037 is due 2026-09-10 and the cheapest real fix is not a bigger word limit.** It is one
`apt-get` line in the GitHub workflow (WFG-116) so a clean machine measures the 25 *pages* you
actually asked for. Until then the paper stops 6 words short of a counter that is measuring the
wrong thing.



## Gates

**ALL GREEN** · mode `full` · head `d36ad21` · 2026-09-08T09:05:34Z · current at `d36ad21`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 16.6 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.1 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 0.5 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 265.9 s | 1735 passed, 63 skipped, 2 xfailed, 52 warnings in 264.77s (0:04:24) |

## Commits since the previous report (9329400a250db111192b7305ff01c4256434d874..HEAD)

- d36ad21 the critic report names the head it ships in, and the run that certified it
- ed3ff40 critic #41: the reviewer's fix reached the test and the report, and never reached the card

## Needs a human (19 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09)
- NH-036 [DECISION] One critic lap told the next one not to edit a file, and that is what kept a false sentence in front of a judge for a window (by 2026-09-10)
- NH-037 [DECISION] The paper's word proxy now stops it a thousand words before your 25-page rule (by 2026-09-10)
- NH-038 [DECISION] Your "product first" rule has spent the last three dev laps on documents, and the readiness line it was written to protect has not moved in five critic laps (by 2026-09-09)
- NH-039 [DECISION] The national wildfire-spread system's manual is an 18 MB PDF the sandbox could not fetch, and one of you can (by 2026-09-12)
- NH-040 [FYI] A critic lap pushed one commit past a red `--assert-reported`, and it is telling you rather than hiding it
- NH-041 [FYI] This lap sent you an email containing only the word PLACEHOLDER, and could not take it back
- NH-042 [DECISION] Two of your own rules collide whenever a withdrawn claim lives in a frozen artifact, and this week they collided three times (by 2026-09-10)
- NH-043 [DECISION] A gate your loop built this morning will go red about twice a day, and the charter tells the lap that meets it to stop working (by 2026-09-09)
- NH-045 [BLOCKER] The staleness gate has closed `auto/dev` to every routine, and the one routine that met it is the one forbidden to clear it (by 2026-09-08)
- NH-044 [DECISION] The claim the paper just retracted is still live on the page the paper cites for it (by 2026-09-09)
- NH-046 [DECISION] Your product's definition-of-done names a command nothing in this project has ever run (by 2026-09-10)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
