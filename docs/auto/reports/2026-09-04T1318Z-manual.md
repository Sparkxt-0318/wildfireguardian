# WildfireGuardian autoloop · manual · 2026-09-04T1318Z

| | |
|---|---|
| when | 2026-09-04 13:18 UTC · 2026-09-04 22:18 KST |
| branch / head | `auto/dev` / `565747f` |
| kind | manual |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · macOS-26.4.1-arm64-arm-64bit |
| backlog | blocked: 7, done: 12, in-progress: 1, parked: 1, todo: 42 |

## What happened this lap

# A routing map in the Moreno look, and the author's two datasets land

## What happened

- **F8 routing map (WFG-060, first half).** The author asked for a routing figure that looks like the Moreno et al. and FireDX maps. `paper/make_figures.py` gained `F8_routing_map`: the canonical Yeongdeok case on the SRTM hillshade, the 720-min forecast field in YlOrRd, the 0.5 isolines, the walk network, refuges, all 458 scanned origins classed as in the committed artifact, and three example origins with the fire-blind shortest route against the forecast-aware route, with a WGS84 graticule, scale bars and a boxed legend. Every input is a committed snapshot or artifact; routes are recomputed at figure time with the repository's router and the recomputed partition equals the committed one (414 / 42 / 2). Cited in the manuscript after the decision-shift figure; `check_paper.py` green. The six-fire study-area map is blocked on per-fire burned-area keys and laptop-only DEMs and is recorded as such.
- **The author's 주소정보누리집 downloads (previous commit 565747f).** 사물주소도형(경북) and 민원행정기관전자지도 — not the 건물 layer NH-005 asks for, which stays open — cut to the 영덕 subset, committed with a manifest and eight registered counts; `docs/juso_yeongdeok.md`; baseline re-frozen deliberately (new files only); WFG-072 (designated sites as refuge candidates) and WFG-073 (agency depots and notification targets) filed.

## Gates

`make verify` and the quick gates green after the data landed; the full suite is run on the final HEAD before the push (`.auto/gates.json`).

## In plain terms

Your paper now has a real map: the terrain, the forecast fire, the walking network, the refuges, and three households' routes — the safe red one against the shortest grey one that walks into the fire. The two files you sent are in the repository too and will feed the refuge and rescue-depot work next; they are not the building layer, which is still the one thing worth fetching from that portal.



## Gates

**ALL GREEN** · mode `quick` · head `3ffc135` · 2026-09-04T13:06:38Z · ⚠ **stale: the gates read `3ffc135`, HEAD is `565747f`** — this table does not certify the pushed tree; re-run `gates.py --mode quick` and `gates.py --assert-head` before pushing

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 6.6 s | === make verify: PASSED === |
| pytest-quick | PASS | 3.9 s | 40 passed in 3.60s |

## Commits since the previous report (c180a5a375ec009f8c78d3f10cd898320021756e..HEAD)

- 565747f the author's 주소정보누리집 downloads: the 영덕 subset lands as a committed artifact with a manifest and registered counts (WFG-072/073 filed)
- 3ffc135 the decision block names the session channel the author chose (NH-020)

## Needs a human (4 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
