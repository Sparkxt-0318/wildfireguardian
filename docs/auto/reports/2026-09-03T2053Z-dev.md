# WildfireGuardian autoloop · dev · 2026-09-03T2053Z

| | |
|---|---|
| when | 2026-09-03 20:53 UTC · 2026-09-04 05:53 KST |
| branch / head | `auto/dev` / `f5f8498` |
| kind | dev |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 7, done: 6, in-progress: 1, parked: 1, todo: 28 |

## What happened this lap

Lap 2026-09-03T2017Z · dev · branch `auto/dev` · claim `282993a` · build `f5f8498`

## 0. The branch was red and nobody knew

Before anything else: `auto/dev` was **RED at `24751fa`**, the critic's own commit.
`tests/test_rescue_lineage_ssot.py` failed on
`docs/auto/reports/2026-09-03T2002Z-critic.md:47`, where finding F1's text quotes the
폐기된 452계열 bracket "6 → 34" with no lineage label inside the gate's ±2-line window.

Critic #2 did nothing wrong. It ran `gates.py --mode full` at `0ff1b36` and reported
exit 0 truthfully. The gap is structural: CHARTER §4 runs the gates at **step 5** and
writes the report at **step 7**, so **every lap's report is the one tracked file that no
gate has ever read** — and the prose gates (`check_forbidden.py`, the lineage gate,
WFG-030's future report-number gate) read exactly that kind of file. Worse, a lap writing
*about* a retired number is the likeliest prose in the repository to trip a
retired-number gate. This lap's own `CRITIC_LATEST.md` response paragraph tripped the
same gate while describing it.

Repaired three ways: the critic's report is **annotated, not rewritten** (its words are
unchanged; a bracketed lineage note is appended inside the gate's window); CHARTER §4
step 8 now orders a post-report gate run; **WFG-046** (P0, infra) makes it mechanical
inside `report.py`.

## 1. Critic items

| item | state |
|---|---|
| **F1** JUDGE_QA §0 tells the student to concede an error the audit disproved | **done(`1c1561e`)** |
| **F9** the critic cannot reach `origin` before the next dev lap | **done(`1c1561e`)**, repo-side half; cadence half stays NH-016 |
| **F7** three live lines carry the retired 10-18 finals date | **done(`1c1561e`)** — taken with F1 because it is three lines and it was failing R11 |
| **F5** README's 116,000 ha vs the national 104,788 ha | open — WFG-043 / NH-015 (needs the author's sources) |
| **F11** manuscript has 21 citations and no `## References` | open — WFG-045; `paper/` belongs to the paper routine |
| **F12** `report.py` has no `paper` kind | open — WFG-044 |
| **N5** WFG-014 still `todo` against a 7,204-word manuscript | applied — `done(0ff1b36)` |
| **N9** do not "fix" `wildfirespreadts` back to 2024 | noted; the file is under `paper/`, untouched |

Nothing was disputed. F1's rewrite is the substantive one: the bullet now says both
brackets are real committed runs (439계열 real-OSM `[6, 11, 24, 51, 66]`; 폐기된 452계열
pre-flip synthetic `[6, 15, 20, 25, 34]`), that the submitted 서식 quoted the canonical
lineage, and that the defect was one README paragraph mixing two lineages — and it tells
the student in as many words **not to say "오타입니다" at the booth**, because
`grep -rn "34" data/processed/` falsifies it in one command.

## 2. The row: WFG-021

Claimed at `282993a` and pushed before building (CHARTER §4.3). Per the F9 sentence this
lap had just written, `origin/auto/dev` was re-fetched after the claim and
`CRITIC_LATEST.md` re-read: unchanged, so no second round of items.

**Built: part (b), the tests.** SESSION19 item 11 is one sentence — 「새 코드에 테스트가
하나도 없습니다」 — against code that produces four numbers the finals screen and the Q&A
bank quote. `tests/test_gk2a_detector.py`, **33 tests in five groups** (the fifth
opt-in; see §3a):

1. **Units.** Forward Planck → DN → `brightness_temperature` round-trips within 0.75 K
   (integer-count quantisation; a wrong unit is worth tens to hundreds of kelvin). The
   module's own docstring says the radiance unit was settled *empirically*, so this is
   the test that fails if anyone tidies the `×1e-3/100` factor away.
2. **Bit width.** A synthetic scene reproduces the module's recorded failure — sw038 read
   as 13 bits instead of 14 lands the whole scene near 376 K — and confirms it is the
   scene-**median** guard, not the pixel-range guard, that catches it.
3. **The K = 4 rule**, as a conjunction: a hot 3.8 μm pixel without MIR–TIR contrast is
   not a detection; a pixel outside the 15 km disc is never flagged; a background with
   vanishing spread cannot manufacture one (the 3 K absolute floor is the only term that
   refuses it); and a **contaminated** background raises the bar rather than lowering it,
   reproducing the threshold `21.964 K` that the committed 영덕 diagnostic records for its
   strongest step — the one assertion in the file that an outside fact decides. Geometry: the 15 km disc, the 30–80 km annulus,
   and the deliberate 15–30 km gap that keeps target and background disjoint.
4. **Regression pin** binding the committed artifact to six registry keys
   (`det_gk2a_delay_uiseong_andong_min` 22, `..._gangneung_2023_min` 34,
   `..._hongseong_2023_min` 64, `det_gk2a_yeongdeok_best_delta_k` 11.611,
   `det_control_steps` 709, `det_false_alarm_steps` 0), the detector settings in the file
   to the constants in the script, and the shape: three fires carry a delay, and 영덕
   carries none and is 「교란」 — counted neither as a detection nor as a fire the
   satellite missed (§4), a distinction a test now pins in the document itself.

One refactor, semantics unchanged: `analyse_step`'s inner `_flag` closure became a
module-level `contextual_flag()`, so the rule is exercisable on arrays without an S3
fetch. `docs/detection_floor.md` §12 records method, what is pinned, and what is not.

**The root objection (`hate`), and what it changed.** *The tests can only confirm what
the code already does; nothing outside the repository is consulted, so a bug baked in
during Session 19 gets pinned as truth.* That was the right objection and I answered it
with a disclaimer instead of a test. The independent reviewer found the disclaimer was
false. See §3a.

**What did not work (1) — the negative result that survived.** The first bit-width fixture
was built assuming a wrong mask always trips a guard. It does not. Which way a dropped
top bit moves the temperature is **the sign of the gain**: decreasing (the shape the
module's own 376 K observation implies) overshoots 330 K and is caught; increasing lands
a 13-bit read of a 290 K scene near 266 K and passes **both** windows.
`test_a_mask_error_small_enough_to_pass_both_guards_exists` pins that. The reviewer then
narrowed it correctly: the real sw038 granule ships a **decreasing** gain, so that hole
cannot open on the calibration GK2A actually distributes. §12 now says the conditional and
the narrowing together. What actually protects the numbers is unchanged and now checked on
a real file: `read_granule` reads the bit count out of the granule.

## 3a. The reviewer blocked, and it was right

`Reviewed by: subagent (block)`. Root objection, in its words: *the lap's headline virtue
is its honesty caveat, and that caveat is false.* The test file and `docs/detection_floor.md`
§12 both asserted that the NOAA GK2A archive is unreachable from this sandbox. **It is
not.** The reviewer fetched the granule; this lap reproduced it independently before
acting: `gk2a_ami_le1b_sw038_la020ge_202503220224.nc`, **HTTP 200, 458,172 bytes**, and
`read_granule` decodes it — `number_of_valid_bits_per_pixel` **14**, gain
**-0.00108296517282724**, offset **17.699987411499**, BT median **286.69 K** over
259.73-319.46 K.

That single unchecked sentence was load-bearing three times: it excused every synthetic
array, it excused the fixture inventing a calibration, and it was the reason the one test
that could validate the reader against something outside this repository was never
written. It cost one HTTP request to disprove. Nothing was argued; four things changed:

1. **The false claim is corrected in place, not deleted**, in both the test docstring and
   §12, saying what it said and that it was never checked.
2. **Group 5 exists now**: `test_the_reader_decodes_a_real_granule_and_takes_its_bit_count_from_it`
   reads the real granule and asserts 14 valid bits (≠ the module's default 13), a
   negative gain, and a scene median inside the window the module defends. It is opt-in
   (`WFG_GK2A_NETWORK_TESTS=1`) for WFG-039's reason — a suite that downloads mid-run has
   two different skip counts and this project's gates read them — not for a reachability
   reason. Verified both ways: 32 passed + 1 skipped by default, **33 passed** with the
   switch on.
3. **The mandela finding is fixed.** `test_a_contaminated_background...` had used a
   rounded `d_sd = 4.0` where the committed diagnostic records **3.409**, so its outcome
   followed from monotonicity and nothing external entered. It now reads 8.328 and 3.409
   out of `yeongdeok_background_contamination.json` and asserts the K = 4 rule reproduces
   the artifact's recorded threshold **21.964 K** to three decimals. That is the one
   assertion in the file an outside fact decides.
4. **The 영덕 overreach is withdrawn.** §12 had written that the annulus geometry is
   「영덕 비탐지의 기전이 **정확히** 그것」. §4 forbids exactly that: 영덕 is 「교란」,
   counted neither as a detection nor as a fire the satellite missed, and whether the
   fire, the coordinates or the timestamp is at fault **was not separated**. §12 now says
   교란, and a test pins §4's refusing sentence in the document so it cannot quietly leave.

The reviewer's fifth point — that the unregistered "약 266 K" in §12 is a number with no
artifact behind it (§3.3) — is accepted: the figure is gone from the prose and the
mechanism is stated without it.

**And it changes the row.** WFG-021 (c) was filed as conditional because "NOAA S3 is not
reachable from the sandbox proxy". That premise is now disproved in writing on the row.

**Not done.** Part (a) — the finals evidence card in `docs/auto/finals/` and the
JUDGE_QA block — and part (c) are outstanding; the row stays `in-progress` with that
written on it rather than being marked done. Part (c) needs NOAA S3, which the sandbox
proxy does not reach.

## 3. Evidence

- `tests/test_gk2a_detector.py` — 32 passed.
- `scripts/auto/gates.py --mode full` at the build: **exit 0**, `1159 passed, 55 skipped`
  (`baseline-verify` WARN is the expected off-laptop state). Baseline at lap start:
  quick green, full red (see §0).
- `docs/detection_floor.md` §12; `docs/ssot_audit_2026-09-03.md` §1 (cited, not changed).
- No committed artifact modified, no figure regenerated, nothing under `paper/` touched,
  no new number registered — this lap added no number to `docs/NUMBERS.json`.

Reviewed by: subagent (block): the honesty caveat was itself an unchecked claim about the world — the NOAA GK2A archive IS reachable, and saying it was not is what kept the only external check out of the file. Fixed, not argued (§3a). Second finding (§12 promoting 영덕's 교란 to a settled mechanism) and the mandela finding (a rounded 4.0 where the artifact records 3.409) fixed in the same pass.

## 4. KCF_READINESS

- **R5 → ☑.** Its only stated blocker was JUDGE_QA §0's false concession; F1 cleared it.
  `tests/test_judge_qa_bank.py` 18 passed, forbidden and lineage gates green on the
  rewritten text.
- **R11 → still ☐, but for one reason instead of four.** The three date lines are fixed;
  what remains is `docs/HANDOFF_ROUND3.md:898` ("All work stays on `round3-dev`"), which
  is WFG-024 and blocked on the author's WFG-023 decisions.
- R2 (detection-floor card on the finals screen) did **not** move: that is WFG-021 (a).

## 5. Next

WFG-021 (a) — the detection-floor evidence card and JUDGE_QA block — finishing the row
this lap left half-done. Then WFG-017 (finals screen v2), which consumes that card.
WFG-046 is P0 and cheap; a lap that has 20 spare minutes should take it, because until it
lands every lap can push a red branch the same way critic #2 did.

## In plain terms

- The branch was quietly broken and this lap found and fixed it: the checker that stops
  the project quoting an old number caught the *critic's own report*, because reports are
  written after the checks run. That hole is now closed twice over.
- Your booth script no longer tells you to admit a mistake you did not make. If a judge
  asks about the "6 → 34" number, the honest answer is that both versions are real runs,
  the paper you submitted quoted the right one, and one paragraph of the README mixed
  them up. Do not call it a typo — a judge can disprove that in one command.
- The satellite detection code that produces your "+22 / +34 / +64 minutes" numbers had
  no tests at all; it now has 32, and they are honest about what they cannot prove. Two
  things need you: the README's burned-area figures (NH-015) and the routine's schedule
  (NH-016).



## Gates

**ALL GREEN** · mode `full` · head `f5f8498` · 2026-09-03T20:52:29Z

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 14.3 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.1 s | make: *** [Makefile:140: baseline-verify] Error 1 |
| snapshot-verify | PASS | 0.4 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 158.9 s | 1159 passed, 56 skipped, 52 warnings in 157.54s (0:02:37) |

## Commits since the previous report (0ff1b362a84808bb84769f2620585ba0d82e0d96..HEAD)

- f5f8498 detection: the Session-19 GK2A reader gets the tests its own record asked for
- 282993a claim: WFG-021, the detection floor gets its tests before its card
- 1e3ff46 critic #2 answered in place: two items done, three filed, one the critic could not see
- 1c1561e critic #2 cleared: the booth script stops conceding a defect the audit disproved
- 24751fa critic #2: the findings arrive thirteen seconds after the lap that needed them

## Needs a human (14 open)

- NH-001 [DECISION] Email delivery needs three repository secrets
- NH-002 [DECISION] Optional: `@claude` on GitHub issues and PRs
- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-008 [DECISION] Five questions for the KCF 운영사무국 (by 2026-09-07)
- NH-009 [DECISION] Repository decisions only the author can take (this week)
- NH-010 [DECISION] Expert consultations and the firefighter record (by 2026-09-20)
- NH-011 [DECISION] One real, recorded email send from a network that works in Shanghai (by 2026-09-20)
- NH-012 [DECISION] Portal downloads the loop cannot do (by 2026-10-01)
- NH-013 [FYI] Optional: a stable web address for the visual board
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-015 [DECISION] The three sources behind the README's opening numbers (by 2026-09-08)
- NH-016 [DECISION] The critic routine has 30 minutes to land its findings, and needs about 40 (by 2026-09-05)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
