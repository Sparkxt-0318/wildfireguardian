# WildfireGuardian autoloop · critic · 2026-09-07T1118Z

| | |
|---|---|
| when | 2026-09-07 11:18 UTC · 2026-09-07 20:18 KST |
| branch / head | `auto/dev` / `2720840` |
| kind | critic |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 8, done: 25, parked: 1, todo: 86 |

## What happened this lap

Reviewed head `2720840`. Window `b2bdaf0..2720840`, the 24 h to 2026-09-07T11:00Z, 59 commits.
Clone: the first measurement was taken on the routine's **default** clone (`is-shallow-repository`
= `true`, `rev-list --count HEAD` = **50**) *before* any deepening, which is the reading critic
#33's finding was about; ancestry work afterwards on an unshallowed clone (`false`, **540**).

## Your decisions, applied

**Nothing new on either channel, and that is now twelve laps in a row.** The Gmail search
`from:siyeong0318@gmail.com subject:"WildfireGuardian autoloop" newer_than:14d` returns threads
that are every one a single message this loop sent you; none has a reply. PR #31 has no comments.
No `NH-###:` line reached this lap, so no entry was closed and `docs/auto/decisions_seen.json` is
unchanged.

Eleven DECISION entries are open and waiting on you: **NH-005, NH-014, NH-032, NH-034, NH-035,
NH-036, NH-037, NH-038, NH-039, NH-042, and NH-043 (new, below).**

## Critic #33's two falsifiable tests, answered first

1. **「If a lap runs `make finals`, pushes, and files nothing about recurrence, WFG-119 has been
   reset and not closed.」** **It was not reset.** The 09:18Z lap ran `make finals` *and* built a
   depth-independent carrier gate, `test_the_screen_is_rebuilt_before_its_stamp_ages_out_of_this_clone`,
   which asks 「is the judged screen a recent build」 — answerable at any clone depth, so the
   sandbox and CI reach the same verdict for the same reason. Its grader pins the policy with two
   hard-coded distances (29, 31) instead of deriving them from the constant, which is the correct
   repair of a first version that could not have failed for any threshold. The lap's **own
   reviewer** caught that, and the lap fixed rather than argued. This is the best lap I have
   reviewed on this dimension and I say so before taking it apart.
2. **「If WFG-146 is fixed in `docs/related_work.md` and not in `RELATED_WORK_PANEL.md`…」**
   **Not triggered — it was fixed in neither.** Third critic lap; it is this lap's one item.

## Verified rather than read, all at `2720840`

- **`gates.py --mode full` ALL GREEN, exit 0**, on the default shallow clone: `1650 passed,
  62 skipped, 2 xfailed`, pytest 260.6 s. `verify`, `snapshot-verify`, `env-check` PASS;
  `baseline-verify` WARN is the documented CHARTER §3d state. **Critic #33's red is cleared.**
- `web/finals.html` names `7308b06`, **6** commits behind `HEAD`, inside the new limit of 30, and
  on `origin/auto/dev`.
- `gates.py --assert-reported --base b2bdaf0` over the whole 59-commit window exits **0**, 59
  substantive paths.
- GitHub Actions via the MCP (`curl` is 403 here): `auto-gates` runs **181 to 205** on `auto/dev`
  are **22 `success`, 3 `cancelled`, no `failure`**; run **205** at this head is `success`.
  **No CHARTER §4b finding.**
- Every **dev** report in the window carries `Reviewed by:`; the research report does not (WFG-147).
- **KCF_READINESS 6 of 11**, unchanged, with R9 (05:00Z) and R7 (08:00Z) both ticked inside this
  window — so the "zero ticks for two consecutive critic laps" direction finding does **not** fire.

## The root objection

**The page that steers this loop asserted the one thing the repository spent this week retracting.**
`docs/auto/DIRECTION.md`'s thesis read 「A forecast of where the fire will be, **not where it is**,
changes which walking route and which rescue order are safe, and the repository **proves that**」.
In the same window `README.md` gained 「that contrast is measured against a fire-blind baseline, so
it does not separate knowing where the fire will be from knowing where it is」 and 「42 is an upper
bound … what a *noiseless* forecast would buy」, and `JUDGE_QA.md` Q36 tells the student to say
「저희 모델이 실제로 사는 값은 아직 재지 않았습니다」 before a judge digs it out.

**The cheapest test is one grep and it was run.** Every judge-facing surface stating 42 or 91
carries both binding caveats — `README.md`, `web/finals.html`, `docs/auto/JUDGE_QA.md`,
`docs/auto/DEMO_SCRIPT_5MIN.md`, `paper/manuscript.md`. The apparent miss in
`release/kcf-finals-2026/README_KO.md` is the directory stamp `20260801T163042Z`, not the claim; I
opened the line rather than trusting the grep. **WFG-138's propagation was complete on every
surface except the one every lap reads first.** Corrected this lap, and added to DIRECTION's
*What not to do*, since CHARTER §14 makes that page the critic's.

## Findings, ranked

**F1 — `fix-before-next-row` (exactly one, CHARTER §14b): WFG-146.** Minutes, on a printable.
Re-verified from the source rather than inherited: I fetched the 사이언스타임즈 article in this
sandbox. It carries `2026-02-13` twice — byline `연합뉴스 2026-02-13`, copyright line
`저작권자 2026-02-13 ⓒ ScienceTimes` — and no `2026-02-12` as a date; the three `2026/02/12`
strings are 연합뉴스 image CDN paths carrying the wire id `AKR20260212072300063`. The repository
pairs one publisher with another's date at `docs/related_work.md:104`, `:187` and
`docs/auto/finals/RELATED_WORK_PANEL.md:43` — the last being 3 of the 36 printed pages of kit
`20260907T0953Z`, on the stick and in the release bundle. 출처 명기 is a named criterion on the
제출 자료 row of both rubric tables. The citation's substance is characterised correctly (the 5ｍ
and ~30% figures are agency plan statements in the future tense); only the date is wrong.
**Fold it into WFG-144's kit rebuild — one `make printables` pays for both.**

**F2 — the one row move: WFG-144, P1 → P0, position 1 of the table.** §14b: a judge-facing finding
larger than minutes becomes a P0 row at position 1 and is never a preemption, so the top of the
table is displaced by F1's minutes and nothing else. Re-measured here: the only `산림청` hits in
`docs/auto/JUDGE_QA.md` are the burned-area lines at `:399` and `:1003`; there is no card for
「산림청·경기도가 이미 산불확산예측을 하고 있는데 무엇이 다릅니까?」 while the kit prints three
pages that invite it. **Seventh consecutive critic lap measuring this and the sixth to decline to
promote it**, each time because the single allowed item went elsewhere. That is exactly the pattern
NH-038 is about, and this lap ends it. I may not write the card myself: JUDGE_QA is a printables
`SOURCES` document and a one-line edit turns the freshness gate red, so only a kit-rebuilding lap
may touch it (WFG-152).

**F3 — WFG-110 is R1's sole blocker, and it is what holds Track A 구현 및 유용성 at 19 rather than
20.** DIRECTION position 2. Critic #33 wrote that a stale build stamp was what stopped a 20 on that
row. That is cleared and verified. I am not paying the point, and the reason is not a moved
goalpost: critic #32 named WFG-110 as a 19-blocker at `0fc6130`, before the stale stamp existed.
`scripts/finals.template.html` references **28** registry keys, `DEMO_SCRIPT_5MIN.md` §3 maps 22 of
them and maps them script → key rather than screen → key, and **6** are in no committed table. A 20
on 구현 및 유용성 while six numbers on the demo screen answer to nothing is not defensible. Nine
infra rows sit behind R1 under §14b, so this row unblocks the whole tail.

**F4 — WFG-139 unrepaired, seventh consecutive measurement, and the first taken on the lap's own
clock rather than by a pass/skip delta.** At container start `data/raw/` held exactly `.gitkeep`
and `README.md`, both mtime **2026-09-04T16:57:27Z**. `bash scripts/auto/bootstrap.sh` finished
**10:59:40Z**; `gates.py --mode full` ran from about that moment and its `pytest-full` stage took
**260.6 s**. Afterwards the directory holds `kfs_fire_statistics/산림청_산불통계데이터_20250911.csv`
(195,891 B, mtime 10:57:59Z) and `dem/srtm/N36E129.hgt` (**25,934,402 B**) with its `.gz`
(8,473,868 B), **both mtime 11:02:33Z — inside the pytest window**. The suite fetched 25.9 MB over
the network in the middle of a gate run. CHARTER §4b forbids a test depending on the network in
those words; `JUDGE_QA.md` Q28 tells a judge that tests needing raw input skip with a reason. My
own counts are therefore neither cleanly cold nor warm and I say so: `1650 / 62` on a run that
downloaded the DEM partway through.

**F5 — WFG-160 (new, P1): the threshold shipped with a frequency estimate this branch's own rate
contradicts, one paragraph from the number its reviewer blocked.** `tests/test_finals_screen.py:534-539`
argues `STAMP_MAX_COMMITS_BEHIND = 30` 「against the branch's measured rate of ~40-55 commits/day: a
rebuild buys about 30 commits, i.e. roughly 18 hours, or six dev laps at the sprint's 3-hour
cadence」, and `docs/finals_screen_v2.md` §4.4 repeats it. Measured on an unshallowed clone (540
commits): **194** commits in the last 72 h = **64.7/day**; per day **94** (09-04), **64** (09-05),
**52** (09-06), **29** in the first 11.1 h of 09-07. **No full sprint day falls inside the quoted
band**; the three-day mean is 70/day. At 64.7/day a 30-commit budget is **11.1 hours**, about
**3.7** dev laps — not 18 hours and six. The number is the argument for the threshold, and it was
made by picking the low end of a band the data does not contain. Do not fix it by moving the
constant; the grader pins it at 29/31 on purpose.

**F6 — NH-043 (new escalation) + WFG-161 (new, P1, blocked): the recurrence is legible but not
routed.** When the new gate fires — on F5's measured rate, roughly every 11 hours, or one dev lap in
four — CHARTER §4 step 2 still reads 「Red baseline → do not build … write a NEEDS_HUMAN BLOCKER and
a `red` report, and stop」, and §3.9 sends the work to `auto/red/<stamp>`. This red is neither
environmental nor an escalation: the remedy is one command printed in the failure text. On
2026-09-07 the only thing that stopped a lap obeying the standing rule was critic #33 writing an
explicit override at the top of `CRITIC_LATEST.md`, **and that file is rewritten by every critic
lap, so the override expires today.** The WFG-119 lap amended CHARTER §4's sandbox-facts paragraph
for the shallow-clone facts and did not amend step 2 for this class. Four options in NH-043; my
recommendation is B (fold `make finals` into the push path so the stamp cannot age — WFG-119's own
done-when #2, second shape) **with A written down as well** so a lap meeting the red before B lands
has a rule instead of a report to follow.

**F7 — the restraint entry, and it is a genuine positive.** The WFG-138/WFG-148 caveat propagation
is complete on every judge-facing surface, traced file by file. Nothing else in the window needed a
finding: no red GitHub run, no missing report, no missing `Reviewed by:` on a dev report, no
un-actioned author reply.

## Direction check (CHARTER §14, five minutes)

DIRECTION position 1 was WFG-119, whose (a), (b), (c) and critic #26's (ii), (iii) all shipped and
whose only remainder is clause (i) — a decision, not work, and the row itself says so. So position 1
was vacant and the page needed a move. **One row moved: WFG-144 P1 → P0 to table position 1** (F2),
reason written on the page. No P0 row went below a row of higher priority. DIRECTION's next three are
now WFG-144, WFG-110, WFG-139, with WFG-146 named as this lap's item immediately after. The page is
one screen and its thesis is corrected (root objection above). **Readiness lines ticked in the last
24 h: R9 and R7 — two, not zero — so no direction finding fires on that axis.**

## Scorecard deltas

**Both tracks flat, and the flat window is the honest read.** B **85** (17 / 17 / 18 / 15 / 18),
A **82** (16 / 17 / 19 / 15 / 15), both unchanged from critic #33 at `2896c9c`.

- **Track B, all five held.** Nothing in the window touched a model, a split, a metric, an arm, a
  protocol or the manuscript's results; the substantive commit is a test and a docstring.
  데이터 수집·분석·해석 holds at 18 on F4 re-measured rather than inherited — it cannot rise while
  the suite reaches the network, and it does not fall because that is the defect already priced in
  at `e95fe28`. 제출 자료 holds at 18: the window's judge-facing gain is a current build stamp,
  which is a 완성도 matter and belongs to Track A's 구현 row; what stops 19 here is F1's 출처 명기
  error.
- **Track A, all five held.** 구현 및 유용성 19 is where the arguing is: critic #33's stated blocker
  is cleared and verified the way that lap asked, and the point it buys is spent on critic #32's
  older and still-standing one (F3).

## Anti-pattern for the MEMO, if a dev lap wants it

*A number does not become measured by sitting next to measurements.* The WFG-119 lap was blocked by
its reviewer for interpolating 「24 commits earlier」 from two probe points, conceded cleanly, and in
the same file wrote 「~40-55 commits/day … roughly 18 hours, or six dev laps」 — a band the branch's
own `git log` does not contain, quoted from its low end. The fix in both cases is the same one line:
`git log --since=` at the head you are pushing, with the head and the window named beside the answer.

## In plain terms

**What the judges would see today.** A booth kit of 36 printed pages, a demo screen that opens with
no internet and now reports a build from this morning instead of yesterday, and a front page that
says out loud what the project has and has not proved. Two things would still catch a sharp judge:
one printed page cites a news article with the wrong date by one day, and if a judge asks 「산림청이
이미 산불확산예측을 하고 있는데 무엇이 다릅니까?」 you have three excellent printed pages beside you
and no rehearsed answer.

**What changed since yesterday.** The gate that went red yesterday morning is green, and it was
fixed properly rather than papered over — the loop built a new alarm that rings earlier and says
plainly 「rebuild the screen」 instead of looking like a broken repository. Nothing else that a judge
meets changed, so today's scores are the same as yesterday's, which is the honest answer rather than
a disappointing one.

**What you should do.** Answer **NH-043** if you can — it is the one that stops the loop wasting a
lap every eleven hours or so, and my recommendation is option B. If you only read one line of this
report, read this: the next lap should write the Korean answer card for 「무엇이 다릅니까」 and fix
the one-day date error in the same rebuild, and that is now what the backlog tells it to do.



## Gates

**ALL GREEN** · mode `full` · head `2720840` · 2026-09-07T11:04:30Z · current at `2720840`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 19.8 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.1 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 2.2 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 260.6 s | 1650 passed, 62 skipped, 2 xfailed, 52 warnings in 259.54s (0:04:19) |

## Commits since the previous report (-15)

- 2720840 the board names the rebased head and both laps that landed in this window
- b8ee618 report 2026-09-07T1002Z-dev: WFG-119, and the reviewer's block on a number I interpolated
- 1995581 the bundle a judge is handed carries the kit this lap built, not the one before it
- 9958d2a the reviewer's block: "24 commits earlier" was one observation generalised into a property of the gate
- 3baa564 WFG-119: the gate that reports the clone gets fixed by fixing the clone, so a second gate now reports the screen
- 15d81f6 the paragraph that says a withdrawn claim cannot hide is wrong, and four tracked files are proving it
- 7308b06 claim WFG-119 (20260907T0918Z)
- e649f2d the critic report's own gate table is the finding, and the green re-run beside it is conditional on a deepening
- d4445d6 critic #33: R7 ticks, and the gate went red on a correct tree because the screen's stamp aged out of the sandbox's clone
- 2896c9c the dev report names the head it ships in, and which of this lap's two kit stamps to print
- d75d2ba the reviewer's block: the page that refuses to compare accuracy pointed "the measurements are over there" at four files that do not exist
- d2640ca WFG-026 + WFG-153 + WFG-156: R7's last unwritten document, and the registration that found a fourth live instance
- ff63907 claim WFG-026 (20260907T0630Z)
- 9f78bfa the critic report names the head it ships in, and its own gate table measures WFG-139 a fifth time
- f6d8246 critic #32: a readiness line moved for the first time in nine laps, and the same window skipped the rule written to stop what it skipped

## Needs a human (16 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09)
- NH-036 [DECISION] One critic lap told the next one not to edit a file, and that is what kept a false sentence in front of a judge for a window (by 2026-09-10)
- NH-037 [DECISION] The paper's word proxy now stops it a thousand words before your 25-page rule (by 2026-09-10)
- NH-038 [DECISION] Your "product first" rule has spent the last three dev laps on documents, and the readiness line it was written to protect has not moved in five critic laps (by 2026-09-09)
- NH-039 [DECISION] The national wildfire-spread system's manual is an 18 MB PDF the sandbox could not fetch, and one of you can (by 2026-09-12)
- NH-040 [FYI] A critic lap pushed one commit past a red `--assert-reported`, and it is telling you rather than hiding it
- NH-041 [FYI] This lap sent you an email containing only the word PLACEHOLDER, and could not take it back
- NH-042 [DECISION] Two of your own rules collide whenever a withdrawn claim lives in a frozen artifact, and this week they collided three times (by 2026-09-10)
- NH-043 [DECISION] A gate your loop built this morning will go red about twice a day, and the charter tells the lap that meets it to stop working (by 2026-09-09)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
