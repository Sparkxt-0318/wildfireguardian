# WildfireGuardian autoloop · paper · 2026-09-04T15:50Z

| | |
|---|---|
| when | 2026-09-04 15:34 UTC · 2026-09-05 00:34 KST |
| branch / head | `auto/dev` / `925d36b` |
| kind | manual |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 7, done: 12, parked: 1, todo: 43 |

## What happened this lap

# Paper lap 4 — the reviewer blocked this lap's own new paragraph, and it was right to

Incorporated `7988769` (was `8ab569e`): 30 changed files outside `paper/` and `docs/auto/`.

This lap's honest headline is that it **wrote one new result paragraph, had it blocked by
its own independent reviewer, verified the block against primary sources, and deleted the
paragraph.** What shipped is the correction work that came out of that, which is worth
more than the paragraph was.

## What the diff offered, and what was taken

**1. The 주소정보누리집 designated-site subset — registered, tempting, refused.**
`data/processed/external/juso_yeongdeok/` plus eight `juso_yeongdeok_*_count` registry
keys arrived this window: the first agency-designated list of evacuation sites and public
offices in the repository. It bears directly on two things the manuscript states — the
refuge semantics in §6 ("OpenStreetMap tags, which in rural Korea return parks and
pavilions") and the depot columns of Table 2 — so a lap reading only the registry would
have written it up.

**It is the wrong county.** Critic #11 (F54) measured every committed point at
36.78–37.05 N, 128.65–129.15 E against this project's canonical 영덕 box of
(129.25, 36.30, 129.55, 36.60): ~45 km apart, overlapping on neither axis. This lap
re-checked the artifact rather than taking the finding on trust, and it corroborates
itself three ways: a road address in `minwon_agencies.geojson` reads 경상북도 **봉화군**
봉화읍; 영덕 is an East Sea coastal county and the set holds zero points east of
129.15 E; and the 지진해일긴급대피장소 layer is empty, which a coastal county's would not
be. Nothing from it is written. The refusal is recorded in `paper/GAPS.md` with the
sentence it will earn once WFG-075 re-cuts the subset — a statement about the national
사물주소 taxonomy (earthquake, tsunami and heat, no wildfire category at all), which is
true of the taxonomy rather than of the county and so survives the re-cut. The reviewer
independently confirmed the manuscript is clean of all eight values.

**2. `docs/detection_floor.md` §0 and §10 (WFG-069, WFG-063) — §5 rewritten. Shipped.**
The repository withdrew two readings this window: that Korean detection is 「사실상 전부
사람」, and the priority table ranking human report first. §5 now states the general form
and both withdrawals in the one place they belong: a floor rules a source *out* of the
primary slot and puts nothing *into* it. Withdrawn, with no number moving — that the
satellite arrives after the emergency call (no artifact records the call time), and that
human report is therefore primary (a channel this repository has never measured). §4.7's
fourth statement of the same withdrawal collapsed to a cross-reference, so the correction
has one home instead of four.

**3. `docs/horizon_grounding.md` (WFG-017) — attempted, blocked, reverted.** §4.6 reports
the dispatch result across a committed 75-minute window and an exploratory 240-minute one
without saying where either came from, so this lap grounded them in the Korea Forest
Service containment statistics. The four values were right; the paragraph was wrong twice,
and the reviewer caught both:

- It said those durations "run from the same recorded-start field whose provenance §4.7
  flags". **That is exactly the claim WFG-053 withdrew on 2026-09-04.** The KFS durations
  come from the 산림청 CSV's `발생일시` column; §4.7's delays come from
  `fire_manifest.json`'s `start`. Two datasets. `docs/horizon_grounding.md` §2 carries the
  dated retraction verbatim, and I confirmed it there before acting.
- The grounding did not apply anyway. The 240 minutes `horizon_grounding.md` justifies is
  `scripts/vulnerability_layer.py`'s `TIME_BUDGET_MIN = HORIZON_MIN = 240.0` — the
  vulnerability layer's horizon, a different parameter. The committed dispatch window is
  `config/default.yaml:365 time_budget_min: 75.0`, marked **ASSUMED**, and nothing grounds
  it.

The paragraph was deleted rather than patched. §6's assumptions list now says outright
that the dispatch windows are assumed and that the 75-minute one is ungrounded — which is
the true version of what the paragraph was reaching for, at a fifth of the words. Both
traps are written up in `GAPS.md` so the next lap does not repeat them, including the fact
that all four `kfs_*` registry caveats **still carry the withdrawn "same reported clock"
wording** (WFG-061, a dev lap's to supersede) and that the manuscript is deliberately
declining to inherit its own registry's caveat, as it already does for the ~95 % ratio.

## Corrections the reviewer forced, all verified against artifacts before acting

- **The synthetic-hazard caveat had been dropped from the entire 439-origin responder
  series** — the most serious pre-existing finding. `data/processed/rescue_routing.json`
  → `provenance.sources` reads `hazard: synthetic, terrain: synthetic`, and its own
  `honesty` field says "Every survival / reachability number is therefore still driven by
  a synthetic fire on a synthetic coastline". The registry caveat on the responder
  exposure-reduction entry says "on the same synthetic hazard", where the results section
  had rendered it as "one hazard field". Fixed at the source: §3.4 labels the series and says
  every 439-origin number inherits it, §4.6 restores the word, and §3.3's "Both downstream
  layers consume that single object" — false for that run — now says they consume a field
  of that form but not the same instance.
- **"still fails to win at 600" asserted zero wins at the longest window.**
  `ordering_boundary.json` → `W600` gives 22 wins of 180, 12.2 %, against 75.6 % losses.
  Rewritten to the artifact's numbers. The conclusion (no window threshold separates the
  regions) is unchanged and better supported.
- **"published while the fire still burned" was false.** WWA published 30 April 2025; the
  chain was contained 28 March (`fire2025_chain_hours_to_containment` = 149 h). The clause
  was in the pre-existing text and I had carried it into a rewrite. Replaced with the
  study's date, and its scope restored — "more than 48,000 hectares" over a "southeastern
  Korea" extent — which CHARTER §3 rule 5b requires of an external figure and which my
  own length cut had stripped.
- **§3.5's column-addition null** said the far-band envelope "spans -0.0363 to +0.0425";
  those are the p5 and p95 (`column_addition_envelope.json`), min and max being -0.0733
  and +0.0514. Now says so.
- **Fig. 6's caption** claimed "the two committed routes". Both source artifacts state
  their route pair "DIFFERS from the committed routing_demo.json headline … NOT a
  restatement of that headline". Caption corrected to say so.
- §4.3's coverage caveat now binds the whole paper rather than its own section, and §6's
  detection gap regained the sentence about the one manifest note that calls a date "the
  ignition".
- **One length cut was reverted by the test suite, correctly.** Trimming §4.7's closing
  "Whether that is ahead of or behind the emergency call, this measurement cannot say"
  broke `test_the_manuscript_keeps_its_withdrawal`, which pins that sentence because §4.7
  is the only place stating the reference clock's provenance in full and must refuse the
  ordering claim in its own voice rather than delegating it to §5 (WFG-053, NH-019). The
  sentence is back. The test was not touched; it was right and the cut was wrong.

## Figures

None added, none redrawn. Every result section already carries its figure or table
(F1–F8, three tables), and the only new committed artifact this window is the blocked one.
A containment-duration figure was considered for the §4.6 grounding and is moot now that
the grounding is gone.

`make_figures.py` re-run: seven of eight PNGs reproduced byte-identically;
`F8_routing_map.png` did not, which is the font-substitution effect `paper/README.md`
documents rather than a content change. It was opened and read before shipping — legible,
no clipped or overlapping labels, legend below the axes as the lap-3 rule requires, the
legend partition (414 / 42 / 2) summing to the 458 in the corner and matching the
committed artifact, and panel (a) still showing the predicted core running well west of
the walk box, which draws the 32.6 % coverage limit instead of describing it.

## Length

7,408 → **7,423 body words** against a 7,500 hard fail and a 7,000 aim; 77 words of
headroom against lap 3's 92. **This lap did not improve the length position and did not
try to** — it spent its budget on the corrections above, two of which (the synthetic
label, the restored WWA scope) cost more than the lap's new prose gained. Removing the
blocked paragraph is what paid for them. Cuts were taken only from sentences restating a
claim made in the same or an adjacent section, or from numbers removed together with their
own caveat so none was orphaned. `GAPS.md` now records that restatement is squeezed dry
and that the remaining 400 words must come from a structural change — folding §4.5's five
sensitivity paragraphs, or §4.6's second half, into tables, which `build_docx.py` does not
count.

## Gaps

Four markers, four rows, unchanged: G5 (detection clock, needs the author), G2
(consultations), G3 (leak-free fold), G4 (hindsight field). None could close — three need
the laptop-only raw bundle, one needs a 신고접수시각 record. Two new non-gap blocks
recorded in `GAPS.md`: the mis-labelled 영덕 subset, and the two-clocks trap above.

## Gates

`paper/build_docx.py` OK; `paper/check_paper.py` exit **0**
(`body_words 7423, figures 8, tables 3, references 27, gaps 4`);
`scripts/auto/gates.py --mode quick` exit **0**; `--mode full` exit **0** earlier in the
lap (`verify`, `snapshot-verify`, `env-check` PASS, `1368 passed, 62 skipped` in 313 s;
`baseline-verify` WARN, expected off-laptop, `hard: false`).

Reviewed by: subagent (**block**): the paper's load-bearing claim is that every number
travels with the caveat its registry attaches, and this lap's new §4.6 paragraph broke
exactly that — re-asserting a conflation of two different time fields that the repository
had withdrawn, and importing a justification measured for a different parameter — while
the same caveat-shedding already stood in the 439-origin responder numbers, whose artifact
labels their hazard and terrain synthetic. Every finding was verified against the
artifacts before acting; all were correct. Its own first nail — two file reads, no
compute — reproduced.

## In plain terms

The paper now says the same things it said this morning, but three of its numbers are
honest in a way they were not: the rescue-dispatch results say out loud that they ran on a
simulated fire, the dispatch comparison no longer claims a clean sweep it does not have,
and a sentence about the attribution study no longer gets its date wrong.

What is still missing is unchanged and still needs you: the three experiments that need
your laptop's raw fire bundle, and one 신고접수시각 record. Nothing new was blocked on you
this lap.

One thing to know: the 영덕 file you sent is the wrong county, so the paper deliberately
does not use it — and separately, the paper's own reviewer caught it trying to reuse a
claim the project had already withdrawn, which is the check working exactly as intended.



## Gates

**RED** · mode `full` · head `925d36b` · 2026-09-04T15:32:53Z · current at `925d36b`

| step | result | time | last line |
|---|---|---|---|
| verify | FAIL | 19.0 s | make: *** [Makefile:113: check-number-collisions] Error 1 |
| baseline-verify | WARN | 0.2 s | make: *** [Makefile:152: baseline-verify] Error 1 |
| snapshot-verify | PASS | 0.5 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | FAIL | 308.4 s | 1 failed, 1373 passed, 56 skipped, 52 warnings in 306.75s (0:05:06) |

## Commits since the previous report (79887696b961e2397ac9af882a5201696d26066a..HEAD)

- 925d36b paper: the reviewer blocked this lap's own new paragraph, and three numbers got their caveats back

## Needs a human (5 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-022 [DECISION] The 영덕 dataset you sent was cut with the wrong county code, and only your laptop can re-cut it (by 2026-09-08)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
