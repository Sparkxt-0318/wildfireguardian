# WildfireGuardian autoloop · critic · 2026-09-03T2002Z

| | |
|---|---|
| when | 2026-09-03 20:02 UTC · 2026-09-04 05:02 KST |
| branch / head | `auto/dev` / `0ff1b36` |
| kind | critic |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 7, done: 5, in-progress: 1, parked: 1, todo: 29 |

## What happened this lap

Critic lap #2. Window `a278a56..0ff1b36`: the WFG-020 dev lap (survivor survey
evidence) and the first real paper lap (605 words to 7,204). No code, no artifact
and no `paper/` file was changed by this lap; the writes are `docs/auto/` only.

## The loop's own claims, verified independently

| check | result |
|---|---|
| `gates.py --mode full` at `0ff1b36` | **exit 0**, ALL GREEN (verify, snapshot-verify, env-check, pytest; `baseline-verify` WARN as expected off-laptop) |
| pytest, COLD first run | 1121 passed / 61 skipped, **1182 collected** |
| pytest, WARM re-run, same tree | 1127 passed / 55 skipped, **1182 collected** |
| `paper/check_paper.py` | **exit 0** (`body_words 7204, figures 7, tables 2, references 21, gaps 4`) |
| `Reviewed by:` in both window reports | present (1851Z `subagent (pass)`, 1928Z `subagent (block)`, acted on rather than argued) |

Both laps' green claims are true. Critic #1's F2, F3, F4 and F6 are closed, and so are
notes N2 and N4. Nothing found this window requires an experiment to be redone.

## Root objection

**The loop's correction channel is open-loop, and the cause is the clock, not the work.**
Critic #1's largest finding, F1, is still on disk untouched. The dev lap claimed its row
at 18:21:07; critic #1 pushed its verdict at 18:21:20, thirteen seconds later. At the
configured cadence that is arithmetic, not bad luck: `dev` at even hours `:17` and
`critic` at odd hours `:47` give the critic 30 minutes to read a window, run the full
gates and push before the next dev lap starts. Critic #1 took 34. This lap started 19:47
against a 20:17 dev lap, and its own prompt budgets 40 minutes, which cannot fit
(19:47 + 40 = 20:27). CHARTER §11 promises a wrong turn is caught within one lap; the
loop has now produced one lap's worth of evidence that the promise is not being kept.

## Findings, ranked

**fix-before-next-row**

1. **F1 (carried, untouched)** `docs/auto/JUDGE_QA.md:50-52` still tells the student the
   repository was wrong about "6 → 34" and still hands the fix to WFG-004, which is
   `done(20260903T1622Z)`. This is the first section of the file spoken at the booth. Not
   the 1851Z lap's fault. Smallest fix in `CRITIC_LATEST.md`.
2. **F9 (new)** The critic cannot reach `origin` before the next dev lap. Repo-side fix
   available to the next dev lap: one sentence in CHARTER §4 step 3 telling it to re-fetch
   and re-read `CRITIC_LATEST.md` after pushing its claim and before building. The cadence
   half is author-only: **NH-016** asks for `47 1-23/2` to become `17 1-23/2`.

**fix-this-sprint**

3. **F11 (new, was note N3)** `paper/manuscript.md` cites 21 works and has no
   `## References` section. CHARTER §12 requires it; `check_paper.py` asserts nothing about
   sections at all, so the gate written to hold §12 true has never looked at the one
   structural thing §12 states. Filed **WFG-045**.
4. **F7 (carried, unfixed)** `CHARTER.md:11`, `RUBRIC.md:20`, `NEEDS_HUMAN.md:72` still
   carry 10.18 / 10-10. R11 fails on the two documents every lap reads first.
5. **F5 (carried, unfixed)** `README.md:191-194` and `:486-491` still claim ~116,000 ha for
   one fire chain against a nationwide total near 104,788 ha. Correctly filed (WFG-043,
   NH-015); restated because it is the first paragraph a judge reads.
6. **F12 (new)** `scripts/auto/report.py:123` has no `paper` kind, so the paper routine
   filed its report as `manual`, `STATE.json` → `last_report_kind` now reads `manual` for a
   paper lap, and that report carries three timestamps for one lap (title `1955Z`, `when`
   19:28 UTC, filename `1928Z`). Filed **WFG-044**.

**notes** (full text in `docs/auto/CRITIC_LATEST.md`): N7 the cold/warm census and what it
makes cheap for WFG-038; **N9 corrects critic #1**, whose F2 remedy text was itself wrong;
N2 and N4 closed; N5 WFG-014 now plainly stale; N10 a `paper/STATE.json` value that looks
like a bug and is not; N11 the survey card's arithmetic re-derived cell by cell.

**N9 matters most among the notes.** Critic #1 wrote that WildfireSpreadTS is
"arXiv:2406.04759 (NeurIPS 2024 Datasets & Benchmarks)". Verified at the NeurIPS
proceedings this lap: it is **NeurIPS 2023** Datasets & Benchmarks, *Advances in Neural
Information Processing Systems* 36:74515-74529. The paper lap's committed entry
(`year = {2023}`, Crossref DOI `10.52202/075280-3258`) is correct. The note exists so no
later lap "fixes" a right entry back to a wrong one. Residual nit: the key is
`wildfirespreadts2024` while its own year is 2023.

## What did not move

`docs/auto/KCF_READINESS.md`: **no line changed status.** R5 is still blocked by F1 and
R11 still fails on the three date lines; both evidence cells now record that they were
re-checked at `0ff1b36` and are unchanged. Nothing in this window touched `web/`, the
printables or the release bundle, so R1, R2, R7, R8, R9 are untouched by construction.

`docs/auto/SCORECARD.md`: one dated row appended per table, because the window plausibly
moved two rows. Track B 72 → **75** (데이터 16 → 17 for the digest-gated survey extractor
and its doc-to-artifact tests; 제출 자료 11 → 13 for 21 verified references with F2 and N4
closed). Track A 67 → **68** (제출 자료 only; 구현 및 유용성 held at 11, because a manuscript
is not what the booth judges watch).

## Filed

- Backlog: **WFG-044** (report.py has no `paper` kind), **WFG-045** (no bibliography, no
  section gate). Both with a details section and a seeded-mutation definition of done.
- NEEDS_HUMAN: **NH-016** (move the critic routine to odd hours `:17`).
- No row status was changed by this lap; N5 asks the next dev lap to mark WFG-014
  `done(0ff1b36)`.

## The judge drill

`JUDGE_QA.md` changed, so the drill ran. It produced one question with a real but
awkward answer: 영덕's 80세 이상 **34.0 %** is quoted in Q17 and in the evidence card §4
but is not in the card's own §3 citation table, so the student would have to open the
JSON to find it. It is in the artifact (표 1-1, 영덕 34/100) and pinned by the bare-percent
test, so this is a completeness nit in §3, not a traceability hole, and it was not added
to the bank. The 116,000 ha question critic #1 withheld is still unanswerable and still
correctly withheld.

## In plain terms

Today's judges would see a project whose evidence keeps getting better and whose two
worst sentences have not changed in three hours: the booth Q&A still admits to an error
that never happened, and the README still opens with a burned area bigger than the whole
country's.

The last two laps did good work, and the survey evidence card is the best artifact this
repository has: it refuses to read a number until the source file's fingerprint matches,
and its own test says out loud where it stops working. The problem is not the work, it is
the timing: the critic finishes 13 seconds too late to be read, every single lap.

Reply "move it" to NH-016 and the critic runs at odd hours :17 instead of :47, which
gives it two hours of clear air instead of thirty minutes. That one field is the highest
leverage thing you can change today. Everything else is already filed.



## Gates

**ALL GREEN** · mode `full` · head `0ff1b36` · 2026-09-03T19:52:34Z

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 13.9 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.1 s | make: *** [Makefile:140: baseline-verify] Error 1 |
| snapshot-verify | PASS | 2.0 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 120.1 s | 1121 passed, 61 skipped, 52 warnings in 119.25s (0:01:59) |

## Commits since the previous report (-15)

- 0ff1b36 the manuscript is a paper now, and the reviewer found the hole in its one claim
- fc09db7 merged with critic #1: regenerated board, images and the needs-a-human count
- a991b96 lap report: WFG-020, and the two survey errors the repo already had
- f2eecf9 the survey figures are now read out of the report, not retyped from notes
- e558ebd critic report: the gates block said RED for a tree that never left the sandbox
- a278a56 critic #1: the booth Q&A still concedes an error that was disproved, and the gate reads it as fine
- 5ed83cc claim WFG-020: the survivor survey is this lap's row
- 1113388 lap report: WFG-004, the SSOT sweep
- 80e788f the lineage gate was weaker than its own description, and now it is not
- 6a2c8a3 the delay-row 6 to 34 is a real lineage, not a typo, and prose now has a gate for that
- 23307d7 the critic follows every dev lap, and the next dev lap fixes its findings first
- e7e02cc board: rebuilt on the merged line, so it shows the 10.24 date and both laps' rows
- 753ac8b the judge Q&A bank is 33 tiered questions, and three of its invariants are now tests
- fec353e the paper is written alongside the code: manuscript source, one figure style, a docx build and its own gate
- 392fe43 claim WFG-004: the SSOT sweep is this lap's row

## Needs a human (14 open)

- NH-001 [DECISION] Email delivery needs three repository secrets
- NH-002 [DECISION] Optional: `@claude` on GitHub issues and PRs
- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-008 [DECISION] Five questions for the KCF 운영사무국 (by 2026-09-07)
- NH-009 [DECISION] Repository decisions only the author can take (this week)
- NH-010 [DECISION] Expert consultations and the firefighter record (by 2026-09-20)
- NH-011 [DECISION] One real, recorded email send from a network that works in Shanghai (by 2026-09-20)
- NH-012 [DECISION] Portal downloads the loop cannot do (by 2026-10-01)
- NH-013 [FYI] Optional: a stable web address for the visual board
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-015 [DECISION] The three sources behind the README's opening numbers (by 2026-09-08)
- NH-016 [DECISION] The critic routine has 30 minutes to land its findings, and needs about 40 (by 2026-09-05)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
