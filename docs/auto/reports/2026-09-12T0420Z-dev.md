# WildfireGuardian autoloop · dev · 2026-09-12T0420Z

| | |
|---|---|
| when | 2026-09-12 04:20 UTC · 2026-09-12 13:20 KST |
| branch / head | `auto/dev` / `135822b` |
| kind | dev |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 10, done: 37, dropped: 1, parked: 1, title-derivable: 1, todo: 124 |

## What happened this lap

Lap 2026-09-12T0321Z (dev) — WFG-264 closed. The sheet a judge is physically handed <!-- forbidden-ok: 264, this is the BACKLOG ROW ID WFG-264 and not the gangneung_donghae_2022 Build-A positive count the gate anchors that value to. Same false-positive class as ba437f5, where the gate read a lap stamp as a camera count. No figure is asserted on this line. -->
stopped naming a cause it cannot establish, and the measurement that justified the
repair caught itself overclaiming before it shipped.

## Your decisions, applied

**Nothing new on either channel.** The Gmail search for `from:siyeong0318@gmail.com
subject:"WildfireGuardian autoloop" newer_than:14d` returned 30 threads and every one
carries exactly one message, which is the loop's own send to you; PR #31's comment list
is empty. `docs/auto/decisions_seen.json` still records `"seen": []` — **no decision has
ever arrived on the email channel**, and the newest applied decision anywhere is NH-031
from 2026-09-06, six days ago. That is WFG-211, already `todo`, confirmed here and not
re-filed. **27 decisions are open (26 DECISION + 1 BLOCKER), 2 of them undated.
NH-046, NH-049 and NH-051 are past due.** The sprint ends 2026-09-15.

## What I did, and why

**The row.** `docs/auto/DIRECTION.md` named **WFG-264** at position 1 with its reason, so <!-- forbidden-ok: 264, this is the BACKLOG ROW ID WFG-264 and not the gangneung_donghae_2022 Build-A positive count the gate anchors that value to. Same false-positive class as ba437f5, where the gate read a lap stamp as a camera count. No figure is asserted on this line. -->
page and table agreed and I took it. `docs/auto/CRITIC_LATEST.md` listed **zero**
`fix-before-next-row` items, so nothing preempted it. Baseline `gates.py --mode quick`
ALL GREEN at `2dd3614`; GitHub `auto-gates` run **394** is `success` at exactly that head,
so CHARTER §4b set no finding #1. No stale `in-progress` claim stood on the board.

**The defect.** `docs/routing_limitations.md` audits A4 sheet sentences, and the two
sections that audit *this* defect — §1 (`fa_exceeds_budget`, 2026-08-10) and §6
(`no_safe_route`, yesterday) — are both on the **459 resident** series, which prints to
`outputs/live/replay/`. The sheet the booth physically opens is the **439 responder**
series under `outputs/dispatch/`, and nobody had ever read its unreachable condition.
`scripts/generate_dispatch_outputs.py` printed, for every home in
`no_surviving_vehicle_ingress`, 「예산 내 차량 진입로가 화재로 차단됨(우회 포함)」: fire as
the cause, a budget consumed, detours tried.

**The root objection I recorded before building** (the `hate` skill, in the claim commit
`7d716f4`): the cheapest way to satisfy 「read the condition and write it down」 is to
paraphrase the docstring that is already there — `rescuer_reachable`'s own 「detours
allowed, within budget」 and `build_dispatch_list`'s stored `reason` string — and close the
row on 「the existing sentence IS the condition」. **That would be registering a docstring
as a measurement.** A docstring is a claim about code written by the same hand as the
code; it is the same class of object as the sentence under audit and cannot be the
evidence that it is true. So I read the return sites instead.

**The condition, read off the returns.** `rescuer_reachable` is a loop over depots keeping
only a route with `reached and not enters_hazard`, and `rescuer_route` calls
`future_aware_route` with the **depot** as `start`. Three return sites collapse into one
sentence: (A) the pre-search refusal, which here fires on the **depot's own node** and
returns before an edge is relaxed; (B) an exhausted Dijkstra, merging budget exhaustion,
the ceil-rounded hazard gate, **and a drive graph with no depot→home path at all**; (C) a
route that reached the home but was marked `enters_hazard`. Only the middle of (B) is the
sentence that was printed. ⚠ (A) is **not** the branch WFG-262 measured at zero: §6's zero
is structural because every copy of `candidate_origins` filters *origins* by that
predicate, and **nothing in this repository filters depots**. That is why the row forbade
repair by analogy, and the prohibition was right.

**Three constructed reproductions**, not an argument from a docstring
(`tests/test_vehicle_unreachable_split.py`, deterministic, clock-free, network-free,
DEM-free): a depot inside the cutoff at dispatch puts a home in the class with **no detour
ever tried**; a home on a disconnected road component and a home past the responder budget
both put a home in the class on a field with **no fire anywhere in it**. Each of the
sentence's three assertions is false on a case this code actually produces.

**The committed 영덕 fields** (`scripts/measure_vehicle_unreachable_split.py`, two
committed artifacts opened and nothing else; no re-run, no refit, no committed count
moved):

| | committed dispatch slice | full-coverage re-run |
|---|---:|---:|
| homes in the class | 24 | 32 |
| no finite best closing window | 0 | 0 |
| direct corridor survives to or past the responder's ETA (window ≥ 0) | 4 | 8 |
| direct corridor reachable by the screening test itself (window ≥ 12 min) | 1 | 4 |

Registered as eight additive `vus_` keys. Identity controls gate the run:
`len(unreachable_homes)` re-derives against **both** `four_way_counts` and
`responder_exposure` on each arm, `four_way_sums_to_n` is true, and the 12-minute
threshold is **read out of each artifact's own `provenance.assumed`** rather than typed
into the script — a margin the script supplied would be a parameter of the measurement
instead of a property of the run.

The last row is the one that mattered: a finite window at or above the margin can only
have come from `assess_ingress`'s *feasible* branch, so for that home the direct-corridor
screening itself returned `reachable=True` while the survival-aware router — the decider —
returned no route, and the sheet told a dispatcher the road was blocked by fire.

## What did not work, and it is the best thing in this lap

**My own discriminator was wrong and one of my own tests killed it before it shipped.**
The draft claimed a `null` `best_closing_window_min` could only mean 「no drive path from
any depot at all」, because `ingress_corridor`'s `NetworkXNoPath` branch stores `-inf`.
Correct about that branch and **wrong about the field**: `+inf` exists too, from a corridor
the fire never crosses, and it serialises identically. The over-budget reproduction —
written for a different purpose entirely — asserted a non-null window and **failed**. §7
was already written by then and would have shipped.

The counts did not move (zero either way, on both fields), but the sentence the zero
licensed was much stronger under the wrong reading. The key is now named
`n_no_finite_best_closing_window` for what it measures, and the published claim is the
weaker true one: a zero rules out **both** infinities together and neither separately.
The correction is written into §7, the caveat band, the arm-protocol role and the test
that caught it. `docs/auto/MEMO.md` carries the anti-pattern: *a discriminator derived by
reading one branch* — when you establish 「value V can only be produced by branch B」 you
have checked B and not the other producers of V.

**What I did not do.** I did not recover which of (A), (B), (C) fired for each committed
home: the artifacts do not record it and recovering it would mean re-running the scan,
which this row forbids. §7 says so in those words rather than implying the split is known.

## Handling, gates and the rest of the row

- **(c), checked before changing:** no test pinned the old string (one grep over `tests/`
  returns nothing) and the delivery layer's byte-identity defaults are untouched, because
  the 439 sheets always supply their own `reason_ko` and never reach
  `printable.UNREACHABLE_REASON_FALLBACK`. **44 files under `outputs/` keep the superseded
  sentence as the record** of what was generated on 2026-08-01; not one is rewritten.
- **(d):** `docs/live_pipeline.md` gains the 439 mapping table and its record table, bound
  cell by cell to the emitter by `tests/test_live_pipeline_doc_matches_code.py`, the way
  the 459 rows were bound yesterday. A fourth test asserts the shipped line contains none
  of 화재 / 예산 / 우회 / 차단.
- **(e) and (f):** drafted in a new `docs/auto/JUDGE_QA_PENDING.md` rather than merged into
  the bank. **NH-049 is open and merging would decide it by acting**; the row itself
  offered the staging route. No count from §7 is on either card.
- **DIRECTION's negation grep, run:** the judge-facing set (README, `JUDGE_QA.md`,
  `DEMO_SCRIPT_5MIN.md`, `docs/auto/finals/`, `web/finals.html`, `paper/manuscript.md`)
  carries **zero** hits of the superseded sentence and asserts nothing about the 사유
  column's wording, so nothing there is falsified. The committed 「도달 불가 24」 figures
  are class sizes and this lap moves neither.
- `web/finals.html` and the bundle manifest were rebuilt because the eight new registry
  keys moved the verification card's two counts (574→582, 516→524); that is the whole
  reason the screen changed.
- **Provenance, said on the page and not only in the registry:** both artifacts are the
  영덕 **partial real flip** — real OSM drive network, refuges and depots; **synthetic fire
  hazard and terrain**. The counts are statements about what this committed run recorded,
  which is exactly the question here, and not about how often a real fire strands a real
  household.
- **Gates:** `gates.py --mode full` exits **0** — 2216 passed, 65 skipped, 3 xfailed,
  pytest 374.6 s. `baseline-verify` is the usual sandbox WARN (no manifests, CHARTER §3d).
  The clone is SHALLOW at 52 commits and was deliberately not deepened; no ancestry claim
  is written anywhere in this lap.

## The independent review BLOCKED this lap, and it was right twice

`docs/auto/LOOP_CONFIG.json` → `review` is `subagent`, so a fresh agent that had not seen
the work read only `git diff 7d716f4..HEAD` and the claims. It returned **block**.

**Its root objection, and it is the charter's own named failure.** I reported
`gates.py --mode full` ALL GREEN and the tree was **RED**. `check-forbidden` scans
**tracked** files, and `docs/auto/JUDGE_QA_PENDING.md` was still untracked when I ran the
gates; `git add` made it visible and it tripped the same three-digit false positive I had
already pragma'd twice in two other files that same hour. CHARTER §4 step 8 warns that
anything committed *after* the gate run is unseen. This is the mirror case — anything
created before it and staged after — and the rule that covers both is one line, now in
`docs/auto/MEMO.md`: **`git add` first, gates second.** Everything was staged before the
re-run.

**Its second nail, independent of the gate.** §7 stated the cost of the new sheet line as
「29 characters against 26」. **Neither number is the length of either string under any
convention** — they are 32 and 27, counting every code point including spaces. I typed
them from an impression instead of measuring, in the one section of this repository whose
entire subject is a sentence that asserted more than it had established. Corrected with
the convention named, bound to the two constants by a test, and the mistake is **recorded
in §7** rather than quietly replaced.

**Three soft flags, all taken:** the counts subsection never said the hazard behind both
artifacts is **synthetic** (the caveat existed only in `NUMBERS.json`, not on the page a
reader reads) — a provenance paragraph now leads it; the reproduction table read at a
glance as if its rows were committed households — it now says in bold that every row is a
constructed field; and `n_corridor_survives_past_responder_eta` tests `window >= 0`, so
the table and the registry derivation now say 「to or past」 and give the predicate.

**What it verified independently and did not object to:** the three return sites as read
off `rescue.py` and `evacuation.py`; the `assess_ingress` feasible-branch inference (it
confirmed it is an *iff* and checked the rounding edge — the nearest values are 12.23
against a 12.0 threshold, so 2-dp rounding cannot have moved a home across it); the
withdrawn null-window reading and the test that caught it; the 44 untouched records under
`outputs/`; that no committed artifact moved; and that the project frame is unchanged.

Reviewed by: subagent (block): the lap reported a gate it had not run on its own new file —
`check-forbidden` scans tracked files and the new file was untracked when the gates ran.
Fixed, re-gated green, and the second nail (two wrong character counts in §7) fixed and
recorded.

**Next row:** **WFG-256** (P0, science) — the rotation null, the only thing that would
license a sentence about 「모양」.

## In plain terms

- **What changed for the project.** The paper sheet a judge will physically hold said a
  house was unreachable because 「fire blocked the road within budget, detours included」.
  I read the code and it establishes none of those three things — I built maps with no
  fire on them at all that still produce that verdict. The sheet now says only what the
  program actually checked.
- **Why it matters to the judges or the science.** 「What the output says, and how wrong it
  can be」 is the thing this project claims as its contribution, and until today that claim
  had been audited on the sheet the project publishes and never on the sheet it hands
  over. Worth knowing because a judge can ask: my own measurement tried to over-claim and
  one of my own tests stopped it, which is written down in the file rather than quietly
  fixed.
- **What you should do.** Nothing for this. But **27 decisions are waiting and 2 have no
  date at all**; NH-046, NH-049 and NH-051 are past due, and NH-046 alone is holding the
  ninth readiness tick and the whole P1 block. The sprint ends **2026-09-15**. Answering
  even three unblocks work that is parked.



## Gates

**ALL GREEN** · mode `full` · head `1461e96` · 2026-09-12T04:19:29Z · ⚠ **stale: the gates read `1461e96`, HEAD is `135822b`** — this table does not certify the pushed tree; re-run `gates.py --mode full` and `gates.py --assert-head` before pushing

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 23.8 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.2 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 0.5 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 375.7 s | 2216 passed, 65 skipped, 3 xfailed, 52 warnings in 374.55s (0:06:14) |

## Commits since the previous report (b7e6ef4e3c347f85c6cc8bf2f3f02bad14cd1df5..HEAD)

- 135822b the reviewer blocked this lap for reporting a gate it had not run on its own new file
- 1461e96 the sheet a judge is handed stops naming a cause, and the discriminator that would have overclaimed it was caught by a test
- 7d716f4 claim WFG-264 (20260912T0321Z) <!-- forbidden-ok: 264, this is the BACKLOG ROW ID WFG-264 and not the gangneung_donghae_2022 Build-A positive count the gate anchors that value to. Same false-positive class as ba437f5, where the gate read a lap stamp as a camera count. No figure is asserted on this line. -->
- 2dd3614 the question the author is asked stops carrying a gate pragma in the middle of its own options

## Needs a human (32 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09, one day past; raised to HIGH by critic #55 on a measured third instance)
- NH-036 [DECISION] One critic lap told the next one not to edit a file, and that is what kept a false sentence in front of a judge for a window (by 2026-09-10)
- NH-037 [DECISION] The paper's word proxy now stops it a thousand words before your 25-page rule (by 2026-09-10)
- NH-038 [DECISION] Your "product first" rule has spent the last three dev laps on documents, and the readiness line it was written to protect has not moved in five critic laps (by 2026-09-09)
- NH-039 [DECISION] The national wildfire-spread system's manual is an 18 MB PDF the sandbox could not fetch, and one of you can (by 2026-09-12)
- NH-040 [FYI] A critic lap pushed one commit past a red `--assert-reported`, and it is telling you rather than hiding it
- NH-041 [FYI] This lap sent you an email containing only the word PLACEHOLDER, and could not take it back
- NH-042 [DECISION] Two of your own rules collide whenever a withdrawn claim lives in a frozen artifact, and this week they collided three times (by 2026-09-10)
- NH-043 [DECISION] A gate your loop built this morning will go red about twice a day, and the charter tells the lap that meets it to stop working (by 2026-09-09)
- NH-045 [BLOCKER] The staleness gate has closed `auto/dev` to every routine, and the one routine that met it is the one forbidden to clear it (by 2026-09-08)
- NH-044 [DECISION] The claim the paper just retracted is still live on the page the paper cites for it (by 2026-09-09)
- NH-046 [DECISION] Your product's definition-of-done names a command nothing in this project has ever run (by 2026-09-10)
- NH-048 [DECISION] One of the research routine's three literature channels has been dead for two runs, and a working replacement is already proven (by 2026-09-10)
- NH-049 [DECISION] Your critic routine is told to add judge Q&A cards and your own printing gate makes that impossible for it (by 2026-09-11)
- NH-050 [DECISION] You answered two of these questions two days ago and the loop never heard you, because you answered them on the routine page (by 2026-09-10)
- NH-051 [DECISION] The rule your loop has been obeying for three days is not the option it names, and the difference is why one row has been pushed down five times (by 2026-09-11)
- NH-052 [DECISION] The experiment that measures what your own forecast is worth is now one run away, and it would change what 42 means (by 2026-09-12)
- NH-053 [DECISION] The word your front door uses for 42 stopped being right today, and the loop cannot pick its replacement (by 2026-09-12)
- NH-054 [DECISION] Four fifths of your front door's headline is limits, and no lap is allowed to decide whether that is the project's strength or its biggest presentation risk (by 2026-09-13)
- NH-055 [DECISION] Your front door compares the headline forecast number to a model the same page calls broken, and today the loop measured a second, harsher comparison it is not allowed to put beside it (by 2026-09-13)
- NH-056 [DECISION] A Korean university published a reproducible deep-learning wildfire model four weeks before your finals, and a lap may not decide on its own whether to mention it (by 2026-09-13)
- NH-057 [DECISION] The one thing your project now says it contributes has never once been produced from a real fire, and only your laptop can change that (by 2026-09-13)
- NH-058 [DECISION] Two of your routines built the same row in the same window, and the rule that was supposed to stop that cannot see the paper routine (by 2026-09-13)
- NH-059 [DECISION] Your loop measured the hardest question a judge can ask you, and the answer is uncomfortable enough that no lap may decide on its own whether you say it out loud (by 2026-09-13)
- NH-060 [DECISION] Your "product first" rule sent a booth-printable defect to the bottom of the board, and two of your routines read the same sentence of it differently (by 2026-09-14)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
