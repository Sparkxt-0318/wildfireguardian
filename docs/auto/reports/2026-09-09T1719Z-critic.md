# WildfireGuardian autoloop · critic · 2026-09-09T1719Z

| | |
|---|---|
| when | 2026-09-09 17:19 UTC · 2026-09-10 02:19 KST |
| branch / head | `auto/dev` / `375be251` |
| kind | critic |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 9, done: 33, dropped: 1, parked: 1, title-derivable: 1, todo: 107 |

## What happened this lap

Critic lap #52. Reviewed head `375be25` on `auto/dev`. No code, test, data, figure or
`docs/NUMBERS.json` entry was touched; this lap wrote only under `docs/auto/`.

## Your decisions, applied

**Nothing new to apply this lap, and I checked both channels.** The Gmail search
`from:siyeong0318@gmail.com subject:"WildfireGuardian autoloop" newer_than:14d` returns
**50 threads and every one of them holds exactly one message** - they are the loop's own
sent reports, not replies. `docs/auto/decisions_seen.json` has an empty `seen` list, so no
message id needed checking against it, and its `applied` list still ends at NH-031, all
twenty-three of which came from Claude Code sessions on your laptop. **PR #31 carries zero
comments**, so the second channel is empty too.

**What is waiting on you, counted from the file's own headings: 25 entries open, and seven
are at or past their stated date.** NH-032 and NH-034 were due 2026-09-08 (one day past;
critic #51 wrote 「two days」, which the calendar does not support, and that is corrected
here rather than repeated). NH-035, NH-038, NH-043 and NH-044 are due today. **NH-045 is a
BLOCKER and is one day past.**

---

## The findings, ranked

### 1. The one gate binding the schedule document to the history it reports measures the clone, not the tree

This is the root objection and it is stated in full below. `scripts/build_timeline_roles.py`
gives three different verdicts on three clones of `375be25`: exit 2 (`shallow`) in the
sandbox as it opens, exit **1** (`STALE timeline artifact: phase starts or anchor commits`)
in the same sandbox after `--unshallow`, and exit **0** on GitHub `auto-gates` run 294 at
`fetch-depth: 0`. Filed as **WFG-217** (P1, infra, in the P1 band per CHARTER §14b).

### 2. The document that closed a five-lap zero says two things about itself that are not true

**This is the one `fix-before-next-row` item, and it is prose only.**
`docs/auto/finals/TIMELINE_ROLES.md` is the answer to 「일정 및 팀원 역할 배분의 타당성」,
worth 20 points on both tables, and it is reachable from the two printed judge surfaces.

- §0 says the phase boundaries 「제가 정한 것이 아니라 커밋 기록에 실제로 비어 있는 달력
  간격입니다」. Measured on the builder's own clock at `375be25`, the record's empty-day gaps
  are 32, 15, **6**, 5, 2 and four of 1 day; the document splits on the 32, the 15, the 5 and
  **one of the one-day gaps**, and skips the **six-day** one. `scripts/build_timeline_roles.py:40-84`
  hard-codes all five boundaries. The weaker claim in the same paragraph IS verified:
  `commits_outside_phases` is 0, so the four gaps it names are genuinely empty.
- §2 states 「트레일러 **513개** (전체 **662개** 중)」 as a flat present-tense fact. Re-derived
  at this head with the repository's own builder: **517 of 666**. Two of three totals were
  wrong within six hours of being written; 활동일 42 is still 42.

The item's scope forbids rebuilding the artifact, because that is finding 1's trap.

### 3. WFG-027's residue was written into a `done` cell, where no lap will ever read it

일정 counts **4** on `docs/auto/JUDGE_QA.md` and **4** on `docs/auto/DEMO_SCRIPT_5MIN.md`,
and **0** on `README.md` and **0** on `web/finals.html`. The 1619Z dev lap disclosed this
honestly and called it residue, then marked the row `done(20260909T1517Z)`. CHARTER §5 puts
residue on a row that goes back to `todo`; residue in a `done` cell is invisible to CHARTER
§4 step 3, which reads `todo` rows only. Filed as **WFG-218** (P0, KCF, end of the P0 block).

### 4. WFG-214's inventory is stale by two, and the row is smaller than it was filed

`docs/auto/JUDGE_QA.md` Q36 was corrected in full by the 1619Z dev lap and now links
`docs/oracle_gap.md`; `paper/manuscript.md:509-512` had its mechanism corrected by paper lap
24 (`bd0da54`), which deliberately left the bound clause word for word because settling it is
NH-053. **Three README lines and one missing link are what remain.** The row was updated in
place rather than duplicated.

### 5. DIRECTION.md and the backlog table named different next rows, and a dev report inherited the error

`DIRECTION.md` listed WFG-214 second and WFG-212 third while the table had WFG-212 at row 19
and WFG-214 at row 21. The 1619Z dev report repeated it verbatim (「WFG-214 sits above it in
table order」). Resolved by this lap's one §3b reorder rather than left to be discovered again.

### 6. Critic #51 did not touch `docs/auto/KCF_READINESS.md`

Its tick header still named critic #50 at `9c22ff3`. CHARTER §11 calls that page the final
product's definition of done and says the critic ticks it with evidence every day. Repaired
here, with the omission recorded on the page rather than filed as a row.

---

## The root objection (`hate`)

**The one gate that binds the schedule document to the history it reports gives two different
verdicts on two clean clones of the same commit, and the one it gives here accuses a correct
document of drift.**

`scripts/build_timeline_roles.py:110-115` resolves each phase's anchor with `%h` /
`--abbrev-commit`. Git derives that width from the **clone's object count**, not from the
history. In this clone after `--unshallow` (666 commits) the anchors re-derive as `a88700c8`,
`4e9dfe39`, `66abf92e`, `25f1e142`, `522f7a72`; the committed artifact holds the
seven-character forms. `--check` therefore reports drift in the one class its own comment says
must never move, while GitHub run 294 ran the same check on the same commit and exited 0.

The dangerous half is the obvious repair: a lap meeting the false STALE rebuilds
`data/processed/timeline_roles/timeline_roles.json`, which commits eight-character anchors and
turns **GitHub** red instead, on a tracked artifact CHARTER §3 rule 2 protects.

**Cheapest test, ten seconds, already run:** `build_timeline_roles.py --check` in a full clone
at `375be25` prints `STALE`; shallow it prints `shallow` and exits 2; GitHub 294 is green.
Three clones, three answers, one commit.

**Credit, because it changes the reading.** The file this objection is about is the
best-instrumented document the loop has shipped: 20 registry keys, 10 tests with 9 graded by
mutation, a partition proof rather than a selection, an honest empty 지도교사 row, and a §3
naming four things its own numbers do not show. The defect is narrow: it is CHARTER §4's
clone-shape failure class arriving through abbreviation **width** instead of clone **depth**,
which is exactly why the test's own docstring, written about depth, did not see it.

## Verification, and what I checked rather than inherited

`gates.py --mode full` exits **0** at `375be25` (1850 passed, 64 skipped, 3 xfailed, pytest
263.3 s). `baseline-verify` WARNs on the two git-ignored `data/raw/**` contracts, which is
NH-029 and CHARTER §3d working as you chose. `--assert-head` exits 0.

⚠ My pytest counts differ from the 1619Z dev lap's (1851 passed, 63 skipped) by exactly one
test, and that difference **is** finding 1: the timeline test skips shallow and fails full.

**GitHub `auto-gates`, runs 255 to 294, the full 24 h window.** Two `failure` runs, **255**
(`b7c1837`, upload-artifact 403) and **260** (`7eeccab`, a debug-port race), both inside
critic #50's window and both closed by `wfg-autoloop-ci-red` inside the hour (runs 256, 261).
Three `cancelled` (275, 278, 284), each superseded by the next push. **The five runs since
critic #51 (290 to 294) are all `success` and 294 is green at this exact head. No new red run,
so CHARTER §4b sets no finding #1.** Every dev report in the window carries `Reviewed by:`;
the 1619Z lap records `subagent (block)` and lists all six nails fixed, none argued.

**`factchk`:** the window's added markdown contains **no new external URL and no new
citation**. Every new claim is about this repository's own git history or its own committed
arrays. Two cross-references I verified by opening the file: 「트랙 A 는 네 개, 트랙 B 는
다섯 개」 is right (`docs/auto/RUBRIC.md:31` and `:44`, and 일정 is in both), and the NH-008
AI-disclosure sentence matches CHARTER §9.

**Clone discipline:** opened SHALLOW at 50, deepened by the window predicate to 101, then
`--unshallow` to **666**, so `--is-shallow-repository` answers **false** and every
re-derivation claim above is licensed.

## Scorecard

**No row moves on either track. Track B holds 93, Track A holds 92.**

The honest reading is that one row is now correctly scored at a number it was over-scored at
yesterday. **설계와 방법론 19 held and re-justified:** critic #51 scored it 19 while a named
sub-item read literal 0 on all four judge surfaces; this window closed that zero with a
document, an artifact, a registry and a mutation-graded gate, so the same 19 is now earned. It
is not 20 because of the boundary overclaim and because 일정 still reads 0 on the README and
the finals screen. **The raise to 20 is pre-registered on both tracks** for the lap that clears
the `fix-before-next-row` item AND lands WFG-218. **제출 자료 held on two cancelling moves:**
+1 for the oracle split going from five surfaces to three in one window with neither repair
ordered by a row, -1 for a new judge-facing document contradicting its own cited source.

## Decisions needed

Five that actually block something, in order. The other twenty are in
`docs/auto/NEEDS_HUMAN.md`; I am not listing them all again, because critic #48 measured that
the long list is where NH-032 and NH-034 have been getting lost.

- **NH-046** - the readiness line R3 names a command nothing has ever run, and R3 is the
  single point of failure behind the ninth consecutive zero-tick lap. The loop's
  recommendation (option A) has been unchanged for five laps.
- **NH-053** - what the word 「상한」 should be on the front door, now that the repository's
  own document says the arm plans on a model output. Loop's recommendation: option A.
- **NH-052** - the re-grading run that would measure what your own model is actually worth.
- **NH-032** and **NH-034** - which margin value is canonical. Both one day past, and both
  are why no margin value may appear on any judge-facing surface.

## In plain terms

- **What the judges would see today:** the project can finally answer 「어떤 일정으로
  만드셨습니까?」, a question worth points on both scoring tables that had no answer anywhere
  for five days. The new page is good, but it claims the schedule split itself out of the
  commit history, and it did not, you did; and two of its three totals were already out of
  date six hours after it was written. That is a fifteen-minute wording fix and I have set it
  as the next lap's first job.
- **What changed since yesterday:** the front door's biggest wrong sentence got fixed in two
  of the five places it appears, without anyone being told to. Three of them are on the README
  and that is the next row. Everything is green, and GitHub agrees.
- **What you should do:** answer **NH-046** first, in one line. It is the single question
  holding the readiness checklist at 8 of 11 for the ninth day running, and nothing the loop
  builds can move that count until you do. Then NH-053.



## Gates

**ALL GREEN** · mode `full` · head `375be25` · 2026-09-09T17:06:01Z · ⚠ **stale: the gates read `375be25`, HEAD is `375be251`** — this table does not certify the pushed tree; re-run `gates.py --mode full` and `gates.py --assert-head` before pushing

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 15.9 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.1 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 2.2 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 263.3 s | 1850 passed, 64 skipped, 3 xfailed, 52 warnings in 262.13s (0:04:22) |

## Commits since the previous report (359fd15eb0f6b3c44b5eb076163e7f087e4a6a1c..HEAD)

- 375be251 the dev report names the pushed head, and the count in its first paragraph is counted
- dc32aad5 the board is rebuilt on the rebased tree, and STATE names the report it ships beside

## Needs a human (25 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09)
- NH-036 [DECISION] One critic lap told the next one not to edit a file, and that is what kept a false sentence in front of a judge for a window (by 2026-09-10)
- NH-037 [DECISION] The paper's word proxy now stops it a thousand words before your 25-page rule (by 2026-09-10)
- NH-038 [DECISION] Your "product first" rule has spent the last three dev laps on documents, and the readiness line it was written to protect has not moved in five critic laps (by 2026-09-09)
- NH-039 [DECISION] The national wildfire-spread system's manual is an 18 MB PDF the sandbox could not fetch, and one of you can (by 2026-09-12)
- NH-040 [FYI] A critic lap pushed one commit past a red `--assert-reported`, and it is telling you rather than hiding it
- NH-041 [FYI] This lap sent you an email containing only the word PLACEHOLDER, and could not take it back
- NH-042 [DECISION] Two of your own rules collide whenever a withdrawn claim lives in a frozen artifact, and this week they collided three times (by 2026-09-10)
- NH-043 [DECISION] A gate your loop built this morning will go red about twice a day, and the charter tells the lap that meets it to stop working (by 2026-09-09)
- NH-045 [BLOCKER] The staleness gate has closed `auto/dev` to every routine, and the one routine that met it is the one forbidden to clear it (by 2026-09-08)
- NH-044 [DECISION] The claim the paper just retracted is still live on the page the paper cites for it (by 2026-09-09)
- NH-046 [DECISION] Your product's definition-of-done names a command nothing in this project has ever run (by 2026-09-10)
- NH-048 [DECISION] One of the research routine's three literature channels has been dead for two runs, and a working replacement is already proven (by 2026-09-10)
- NH-049 [DECISION] Your critic routine is told to add judge Q&A cards and your own printing gate makes that impossible for it (by 2026-09-11)
- NH-050 [DECISION] You answered two of these questions two days ago and the loop never heard you, because you answered them on the routine page (by 2026-09-10)
- NH-051 [DECISION] The rule your loop has been obeying for three days is not the option it names, and the difference is why one row has been pushed down five times (by 2026-09-11)
- NH-052 [DECISION] The experiment that measures what your own forecast is worth is now one run away, and it would change what 42 means (by 2026-09-12)
- NH-053 [DECISION] The word your front door uses for 42 stopped being right today, and the loop cannot pick its replacement (by 2026-09-12)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.

---

## Correction, written by this same lap after it committed (2026-09-09, `a53912d0`)

**The finding above named the wrong discriminator, and the gate suite went red proving it.** Everything else
in this report stands; this section replaces one clause and keeps the clause it replaces.

Above I wrote that `scripts/build_timeline_roles.py --check` gives one answer in a shallow clone and another
in a full one, as though **depth** were the variable. It is not. The variable is the clone's **object count**,
which unrelated remote-tracking branches move. What happened, in order, all on one tree:

1. I committed this report as `a53912d0` and re-ran `gates.py --mode full`, as CHARTER §4 step 8 requires.
2. It came back **RED**: `1 failed, 1850 passed, 63 skipped`, the failure being
   `tests/test_timeline_roles.py::test_the_artifact_still_agrees_with_the_history_when_the_clone_has_one`,
   with `AssertionError: STALE timeline artifact: phase starts or anchor commits`. **A critic lap does not
   push past a red gate** (CHARTER §3.9), so nothing was pushed at that point.
3. I pulled GitHub's own job log for `auto-gates` run **294** at `375be25`. It reads
   `[gates] PASS pytest-full exit=0 345.0s 1851 passed, 63 skipped, 3 xfailed`. **63 skipped is the same
   count as the full-clone dev lap**, so that test ran on GitHub and passed. The tree was therefore not the
   cause.
4. My clone held five origin branches, fetched shallow when the session opened, plus the full `auto/dev`
   history I had unshallowed: **20,641** packed objects, at which git's automatic abbreviation is **eight**
   characters. I deleted the four remote-tracking refs that are not `auto/dev` and ran `git gc --prune=now`:
   **12,052** packed objects, **not one commit reachable from `HEAD` changed**, `%h` back to **seven**
   characters, `--check` exit **0**, full gates green.

**So the honest statement of the root objection is stronger than the one I first wrote.** The gate is not
sensitive to how deep the clone is. It is sensitive to **which other branches happen to be in it**, which is
a property no lap controls and no reviewer would think to check. That is why **WFG-217** asks for `%H` with a
prefix comparison rather than for a deeper clone, and why the `fix-before-next-row` item forbids rebuilding
`data/processed/timeline_roles/timeline_roles.json`: a lap that meets the false STALE and rebuilds would
commit eight-character anchors and turn **GitHub** red on a tracked artifact.

⚠ **What I did NOT do, and would not:** set `core.abbrev`, edit the test, or push past the red. The gate
was answered by repairing the clone it was measuring, and both the red run and the reason are on this page
rather than replaced by a green one.
