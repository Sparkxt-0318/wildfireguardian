# WildfireGuardian autoloop · dev · 2026-09-09T0654Z

| | |
|---|---|
| when | 2026-09-09 06:54 UTC · 2026-09-09 15:54 KST |
| branch / head | `auto/dev` / `435c54d` |
| kind | dev |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 8, done: 32, dropped: 1, parked: 1, title-derivable: 1, todo: 104 |

## What happened this lap

## Your decisions, applied

**Nothing new reached the loop this lap.** The Gmail search for
`from:siyeong0318@gmail.com subject:"WildfireGuardian autoloop" newer_than:14d` returns 20 threads and
**every one of them is a single message — the loop's own report, sent from your address to your address.**
No thread carries a reply, so `decisions.py seen` was not called and `decisions.py apply` had nothing to
apply. PR #31's comment list is **empty**. `docs/auto/decisions_seen.json` still stops at NH-031.

⚠ That means **NH-032 and NH-034 are now three days overdue**, and NH-035, NH-038 and NH-043 came due
yesterday. Until NH-032 and NH-034 are answered the student is still forbidden to say any of the margin
numbers out loud, and this lap obeyed that: no margin value was written anywhere in `README.md:210-282`.

---

## What this lap did

**Two rows: WFG-208 (critic #48's one `fix-before-next-row` item, minutes) then WFG-207 (P0, table
position 1).** Baseline `gates.py --mode quick` exited 0 at `fd5fabe`. GitHub's own `auto-gates` was
checked through the GitHub MCP rather than `curl` (CHARTER §4 sandbox facts, WFG-119): run **276** at
`fd5fabe` is `success`, so there was no red run to take as a first job. Run 275 shows `cancelled` — it was
superseded by 276 on the next push, not a failure. The clone opened **shallow at 50 commits**, measured in
this clone today; **no ancestry or reachability claim appears anywhere in this lap's output.**

### WFG-208 — the USB a judge carries away had no address on it

`release/kcf-finals-2026/README_KO.md` cites repository paths in backticks throughout. **Critic #48
measured 15 such paths, 7 of them existing in the tree and outside the bundle's 19 files**; this lap did
not re-derive that pair, because a naive backtick scan of this file mis-pairs on its fenced code block and
the number is not load-bearing for the fix. What this lap did re-derive is the fact the fix is actually
about: a count of `github.com` in that file answered **0** before and answers **1** now. On the booth
laptop every path resolves because BOOTH_SETUP §1 puts a full clone there. On the USB they do not, and
there was no address at which to look.

The file now opens with one block: backticked `docs/…` / `scripts/…` / `outputs/…` paths are paths **in
the repository, not in this folder**; the repository URL once; `auto/dev` named as the branch the bundle
was built from; and the booth laptop's clone named as where they do resolve. ⚠ **No document was added to
the bundle** — DIRECTION.md forbids widening the payload, and `make finals-bundle` still rebuilds
byte-identically at 19 files.

### WFG-207 — the front door was silent on the row it is scored on first

창의성 is 20 points on both KCF tables and the 심사기준 names it first. WFG-182 shipped the Q29a card;
WFG-194 shipped the finals screen, the spoken script and the printed kit. **Both closed correctly on their
own scope.** The surface in nobody's scope was `README.md`, where a raw count of 창의 or 독창 answered 0 —
the page a judge opens before the booth, and the page this loop had just spent a window writing a Round-4
section into. That is the useful part of the finding: *a surface in nobody's scope is not caught by any lap
doing its row properly.* It took four critic laps.

`README.md` now carries **§5 창의성 — 이 작품이 직접 만든 것** inside Round 4, below §4 and above the
abstract pointer: the same three items as `docs/creativity_card.md` §2, in the same descriptive register,
each on its own line with its own repository path as a link, plus the 없는 것 limit for item ③, the closing
line that the verdict is the judge's, and pointers to the card and to Q29a. The opening paragraph about the
2025 fire and the Round-2 section are untouched (CHARTER §3.12), and no margin value entered
`README.md:210-282`.

### The root objection, recorded because it changed the work

**`hate`, before building: the row's own 「done when」 cannot be satisfied as written, and satisfying it
literally would ship a gate with the defect it was meant to prevent.** The row asks for a gate reading
「the rendered README and not a source file」. That criterion is inherited from WFG-194, where the defect
was real: the gates read `scripts/finals.template.html` and not the `web/finals.html` that `make finals`
builds from it. **Nothing in this tree builds `README.md`** — `grep -rn 'README\.md' scripts/` returns seven text hits
and every one of them reads or names the file, none writes it; neither the `Makefile` nor
`.github/workflows/` generates it. So `README.md` is at once the source and the artifact a judge opens,
and any test of it reads "a source file" by construction. ⚠ The first version of this sentence said the
only `README.md` string in `scripts/` was a printables body paragraph. **That was false and the reviewer
measured it**; the conclusion survives, the measurement did not, and both the summary and
`docs/creativity_card.md` §7 now say what was actually checked.

So the criterion was replaced rather than declared met, and the report says which:

- **(a) the link.** What a judge does on a front door is press on a path, and the only thing that makes the
  claim real to them is that the file opens. `test_every_link_in_the_readme_block_opens` resolves every
  markdown link target in the block against the tree.
- **(b) item-level pairing.** `WFG-206` exists because the screen's block passes every assertion with two
  anchors transposed — the checks ask whether the paths appear *somewhere*. Here the block is cut at its
  ①②③ markers and each chunk must name its own anchors and none of another item's, so a transposition is
  red. ⚠ **WFG-206 stays open**: this closes the hole on the newest surface only, not on the screen or the
  card.

### Evidence

| what | where |
|---|---|
| the block a judge reads | `README.md` §5, inside Round 4 |
| method, mutation table, limits | `docs/creativity_card.md` §7 |
| the six new gates | `tests/test_creativity_card.py` (19 assertions in the file, 6 new; the sixth is the reviewer's) |
| the rebuilt kit | `docs/auto/finals/printables/WFG_printables_20260909T0700Z.pdf` + its manifest (7 sources) |
| the bundle | `release/kcf-finals-2026/README_KO.md`, `MANIFEST.json` re-pointed, 19 files, byte-identical |

**Ten mutations, nine red:** delete the block heading; transpose items ① and ② anchors (the WFG-206
shape); point item ② at a path not in the tree; a bare novelty claim; a sentence about another operational
system; a digit count (「12건」); the Korean-numeral form (「여섯 건」); delete the reworded-claim limit;
delete the 「심사위원의 판단입니다」 line. The tenth is **M8**, the evaluative-register rewrite, and it
stays green here as on all three other surfaces — nothing in this suite reads tone.

**The reviewer wrote eight more without seeing those ten**, which is the two-source rule the card's §6
stated after WFG-194 and this lap had not followed. Its **mutation D** is what a single-session set
could not have found: rewriting an item's backticked **label** to another real repository path while
leaving the link **target** correct left all eighteen assertions green. On a rendered README the label is
the whole of what a reader sees — they read one path and open another — so the pairing was bound where
the markup is and not where a judge reads. A sixth assertion,
`test_the_label_a_judge_reads_is_the_path_the_link_opens`, now requires the two strings to be equal, and
D is red.

### What did not work, and it is this lap's MEMO entry

**M8 first went red, and for the wrong reason.** The mutation opened with 「아래 세 **항목**은」, and 항목
is on this file's own `_OBJECT_COUNTER` list, so the *count* gate fired on structural prose about the
block's own shape. Nothing had read the register. Re-run without a counter word, M8 stayed green — the
honest result. That is the exact mirror of the failure `docs/creativity_card.md` §5 records from the other
side: two mutations that stayed green because a `\b` after a Hangul syllable can never match. Both
directions produce the same lie, a coverage claim the suite does not hold. **Grade the mutation, not just
its exit code**; the check is one line of output naming which test failed.

**And one defect this lap nearly shipped, caught by re-running a claim instead of quoting it.**
`scripts/build_finals_bundle.py` builds the bundle from the files **git tracks**. The lap ran
`--update` right after `make printables` but before `git add`, so the new kit was still untracked, the
builder could not see it, and both `--update` and the byte-identical check returned **green about the
previous kit** — the manifest still named the `20260909T0321Z` PDF. `make finals-bundle`, re-run after the
work commit, is what said so. Fixed in `435c54d`; the manifest now names the `0630Z` pair and the bundle is
OK at 19 files. **A check whose input is `git ls-files` answers about the index, not the working tree.**

One measured false positive was excluded rather than the check weakened: the block sends a reader to card
**Q29a**, and 29 is a card ID. `Q\d+[a-z]?` is stripped before the count scan, so 「265」 or 「33」 in the
block is still red — the multi-digit half is kept here, unlike the screen's version of the same check,
because the front door is the one page where a stale headline number would be read first.

### Gates

`gates.py --mode full` exits **0** (`1825 passed, 63 skipped, 3 xfailed`, pytest 373.3 s).
`baseline-verify` WARNs on the two `data/raw/**` contracts that are git-ignored and cannot exist in any
sandbox, which is NH-029/§3d working as designed. `make verify` PASSED. `--assert-head` and
`--assert-reported` were run immediately before the push and both exit 0.

### Reviewed by: subagent (block) — and every nail was fixed, none argued

**Root objection:** *the claimed kit rebuild was only half-applied, and three of the ten claims were
false against the commit they described.* At `da86cc0` the bundle manifest still named the
`20260909T0321Z` kit, so `make finals-bundle` exited 2, three `tests/test_finals_bundle.py` tests failed,
and `.auto/gates.json` still recorded `7c897f7` — the full run had never read the work. **The reviewer is
right and its first nail is two seconds long:** `make finals-bundle`, or
`pytest tests/test_finals_bundle.py`, either one, run after staging. The lap had found and fixed the
manifest itself in `435c54d` while the review was running; the block stands on the claims as submitted,
and it is lifted by re-running the gates at the pushed head with a clean tree, which step 8 below does.

**What the block changed in the work, beyond the manifest:**

1. **A false measurement in a printed document** — the `scripts/` grep sentence above. Corrected in the
   summary and in `docs/creativity_card.md` §7, conclusion kept, measurement replaced.
2. **An unregistered count on a judge-facing page** — §7 said the README is a 「1,100-line page」. It is
   not registered anywhere, it is printed in the booth kit, and it is the exact shape the block's own
   count gate forbids one surface over. Removed.
3. **Mutation D**, above: the sixth assertion exists because of it.
4. **An undisclosed cost of the `Q\d+[a-z]?` exclusion** — a count written *adjacent to a Q*
   (「근거 문서 Q33종」) escapes. Narrow, and now written on the page instead of hidden behind the word
   「excluded」.

It also verified clean, independently: the four link targets resolve; the README diff is a single hunk
with the opening paragraph, the Round-2 section and lines 210-282 untouched and no margin value added;
`make verify` exits 0; the malformed-row histogram of the backlog is identical before and after; and the
critic's two measurements re-derive (창의/독창 = 0 on `README.md`, `github.com` = 0 in `README_KO.md`,
both at `7e222c2`).

### Next row

**WFG-027 (P0, KCF)** — 일정 및 팀원 역할 배분, a named sub-item of 설계와 방법론 and 20 points on both
tables, at literal zero on every surface a judge meets. DIRECTION.md and the table agree on it. Then
WFG-125 (P0, science).

## In plain terms

- **What changed for the project.** 창의성 is worth 20 marks and the judging notes name it first. Your
  booth screen, your five-minute script and your printed kit all answer it now — but your README, the page
  a judge opens before they ever reach the booth, did not use the word once. It does now: three short lines
  saying what this project actually built, each linking to the file behind it, with the honest limit
  attached and the last word left to the judge. Separately, the USB kit now carries your repository address,
  because it was pointing at files a judge could not open from the stick.
- **Why it matters to the judges or the science.** Twenty marks on both scoring tables were resting on a
  page a judge might never open. A new test refuses to let the README's claims and their files come apart —
  swap two of the links and the test suite goes red — which is a stronger promise than "the word is on the
  page".
- **What the author should do.** Nothing for these two rows. But **NH-032 and NH-034 are three days
  overdue**, and until you answer them the student still may not say any margin number out loud to a judge.
  NH-035, NH-038 and NH-043 came due yesterday. One reply with five lines clears all of them — and note
  that the loop has received **no reply at all** through either channel, so if you have been answering on
  the routine page, the loop cannot hear it (that is NH-050).



## Gates

**ALL GREEN** · mode `full` · head `435c54d` · 2026-09-09T06:51:05Z · current at `435c54d`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 20.4 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.2 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 0.5 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 395.3 s | 1825 passed, 63 skipped, 3 xfailed, 52 warnings in 393.53s (0:06:33) |

## Commits since the previous report (7e222c2885e87d1bd56790aab23647021e5556f5..HEAD)

- 435c54d the bundle manifest was re-pointed before the kit was tracked, so it pointed at the old kit
- da86cc0 WFG-207 + WFG-208: the front door said nothing about the row it is scored on first, and the USB had no address
- 7c897f7 claim WFG-207 (20260909T0630Z)
- fd5fabe the critic report names the head it ships in, and the run that certified the pushed tree
- dfa79c2 critic #48: the author answered two of these two days ago on the routine page, and the loop has been asking ever since

## Needs a human (22 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09)
- NH-036 [DECISION] One critic lap told the next one not to edit a file, and that is what kept a false sentence in front of a judge for a window (by 2026-09-10)
- NH-037 [DECISION] The paper's word proxy now stops it a thousand words before your 25-page rule (by 2026-09-10)
- NH-038 [DECISION] Your "product first" rule has spent the last three dev laps on documents, and the readiness line it was written to protect has not moved in five critic laps (by 2026-09-09)
- NH-039 [DECISION] The national wildfire-spread system's manual is an 18 MB PDF the sandbox could not fetch, and one of you can (by 2026-09-12)
- NH-040 [FYI] A critic lap pushed one commit past a red `--assert-reported`, and it is telling you rather than hiding it
- NH-041 [FYI] This lap sent you an email containing only the word PLACEHOLDER, and could not take it back
- NH-042 [DECISION] Two of your own rules collide whenever a withdrawn claim lives in a frozen artifact, and this week they collided three times (by 2026-09-10)
- NH-043 [DECISION] A gate your loop built this morning will go red about twice a day, and the charter tells the lap that meets it to stop working (by 2026-09-09)
- NH-045 [BLOCKER] The staleness gate has closed `auto/dev` to every routine, and the one routine that met it is the one forbidden to clear it (by 2026-09-08)
- NH-044 [DECISION] The claim the paper just retracted is still live on the page the paper cites for it (by 2026-09-09)
- NH-046 [DECISION] Your product's definition-of-done names a command nothing in this project has ever run (by 2026-09-10)
- NH-048 [DECISION] One of the research routine's three literature channels has been dead for two runs, and a working replacement is already proven (by 2026-09-10)
- NH-049 [DECISION] Your critic routine is told to add judge Q&A cards and your own printing gate makes that impossible for it (by 2026-09-11)
- NH-050 [DECISION] You answered two of these questions two days ago and the loop never heard you, because you answered them on the routine page (by 2026-09-10)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
