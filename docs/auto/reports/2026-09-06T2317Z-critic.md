# WildfireGuardian autoloop · critic · 2026-09-06T2317Z

| | |
|---|---|
| when | 2026-09-06 23:17 UTC · 2026-09-07 08:17 KST |
| branch / head | `auto/dev` / `524f13c` |
| kind | critic |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 7, done: 17, oracle: 1, parked: 1, todo: 84 |

## What happened this lap

Critic lap #30. Reviewed head `524f13c`; window `1b26c3a..524f13c`, plus the 24 h to
2026-09-05T23:38Z for the gate, CI and report checks. I changed no code and no artifact;
everything I wrote is under `docs/auto/`.

## Your decisions, applied

**Nothing new on either channel, and this is the one line the charter asks for.** The Gmail
search `from:siyeong0318@gmail.com subject:"WildfireGuardian autoloop" newer_than:14d` returns
30 threads, every one of them a single message this loop sent, all labelled `SENT`; not one
thread carries a reply. PR #31 has no comments at all. So no `NH-###:` line existed to parse,
`scripts/auto/decisions.py` was not run, and `docs/auto/decisions_seen.json` is unchanged.
**Twelve NEEDS_HUMAN entries are open and nine of them are DECISION entries waiting on you.**
Two have deadlines that have now passed their comfort margin: **NH-032** (which fair-opponent
number counts) is dated 09-08 and is what keeps every margin off every judge-facing surface, and
**NH-038** (your product-first rule) is dated 09-09 and gained its seventh data point tonight.

## Critic #29's two falsifiable tests, answered first

Both were about the dev lap rather than about the row, and both came back in the lap's favour.

1. **「If `README.md:22-26` still says 「only when the router accounts for where the fire will be」
   with no fire-blind caveat at the next critic head, the finding is about the dev lap's step 3.」**
   It does not. `README.md:22-33` carries the caveat inside the same bullet and Q19's spoken draft
   carries it inside the sentence. The 20:17Z slot ran, claimed **WFG-138** at `8cc1e89` and closed
   both halves at `0ed15aa`/`20b0b7e`. **No finding.**
2. **「If that lap clears the README half and leaves Q19's 42 standing, then a widened row does not
   travel and the correct unit is one row per surface.」** It did not leave it standing. **A widened
   row travels when both halves are one clause long**, so the unit stays the claim, not the surface.
   The record now says so rather than the hypothesis.

## Findings, ranked

### F1 (the one `fix-before-next-row` item, CHARTER §14b) — WFG-148, new, P0, KCF

**The README's headline bullet gained the first of the two caveats the project's own manuscript
says bind that comparison, and not the second, and the gate written in the same lap certifies the
bullet as caveated.**

`paper/manuscript.md:506`, this loop's own sentence: 「Two caveats bind the whole comparison. The
forecast-aware arm plans on the same hazard field it is graded against, so whatever it is worth
against a present-perimeter policy is what a *noiseless* forecast is worth; this project's own
model is worth less, by an amount no run here measures.」

WFG-138 put caveat one into `README.md:22-33`. Caveat two is absent from the README entirely:
`grep -niE "perfect|oracle|upper bound|상한|noiseless" README.md` returns **nothing** at `524f13c`.

**It binds the 42 for the same reason it binds the 91**, checked rather than assumed:
`docs/present_perimeter_arm.md` §5 states it and calls it 「a property the 91 has always had,
inherited not introduced」; `docs/real_roads_real_hazard.md` hands both arms the same
`HazardSequence`; `src/wildfireguardian/routing/evacuation.py:270` says the fire-blind arm is
「then scored against the hazard」. So on 영덕 too the forecast-aware arm plans on the field it is
graded against, and the 42 is an upper bound on what a **noiseless** forecast buys rather than a
measurement of this project's model.

**The gate makes it harder to notice, not easier.** `tests/test_future_aware_attribution.py:135`,
`_is_caveated(block)`, returns True on any one spelling of the CONTROL family and asks nothing
else, so the README bullet is green under the gate built for it while half of what the manuscript
calls binding is missing.

**The judge already has the question and the bank already has the answer.** `docs/auto/JUDGE_QA.md`
**Q36** (「비교하신 예보 경로는 정답을 미리 본 예보 아닙니까?」, critic #23) sits in the 근거 없음
table and tells the student to say 「맞습니다. 그건 완벽한 예보가 사는 값의 상한이고, 저희 모델이
실제로 사는 값은 아직 재지 않았습니다」 before the judge digs. The README is the one surface that
invites the question and does not answer it.

⚠ The row explicitly does **not** touch `docs/auto/JUDGE_QA.md`: Q36 already carries the sentence
and the printables are already stale against that file for a fourth time. Budget twenty minutes
and one clause; extend the module WFG-138 shipped rather than writing a new one; then take
**WFG-134 with WFG-140 and WFG-130 in the same lap** and do not idle.

### F2 — the booth kit drifted a FOURTH time, and this drift is the first that makes the printed pages worse (existing rows WFG-134 and WFG-140, updated, not duplicated)

Re-hashed in one process at this head: `BOOTH_SETUP.md` `ef7342dacf…`, `DEMO_SCRIPT_5MIN.md`
`b1aae78f35…` and `DETECTION_FLOOR_CARD.md` `84648d4d6e…` all **match**; `docs/auto/JUDGE_QA.md`
manifest `2c8451211e5f97…` against tree **`5ac45ea8103f11…`**. The PDF still matches its own
recorded sha256, so `tests/test_printables.py` stays fully green over a stale kit. The series is
`af955a30fa…` (#27) → `7d5ac4c9c5…` (#28) → `175da9e50c…` (#29) → **`5ac45ea810…`** (here).

Critic #29's root objection was that all three earlier drifts were the loop's own correction notes
rather than judge-facing improvements. **This one is a judge-facing improvement, which is exactly
why it is worse than the three before it:** the 17 printed Q&A pages now carry Q19 **without** the
fire-blind caveat that the repository decided in the same window is mandatory and now enforces in
two test modules. The paper in the student's hand and the files the gates read disagree about what
the student may say aloud. R7 and R9 are blocked on nothing else.

### F3 — zero readiness lines for a SEVENTH consecutive critic lap, and this time the direction rule fires (NH-038, seventh data point)

`docs/auto/KCF_READINESS.md` reads **4 of 11** (R2, R4, R5, R6), unchanged since 2026-09-05.
Critic #29 recorded the zero without firing the rule, correctly, because that window contained no
dev lap. This one did. Five of the last six dev laps built a critic's one item; four of those five
were corrections to documents this loop wrote. Each was worth doing. The sum is that the checklist
defining 「the product is ready」 has not moved while the sprint is past halfway and R7 and R9 wait
on the same trio, displaced a fifth window against a sprint-plan date of 09-11.

⚠ **And this lap does it again.** My one item is another document correction. I filed it anyway,
because it is the README opening and it is twenty minutes, and I wrote into the item that the same
lap must then take WFG-134. That is the most a critic can do inside the rule as written, and it is
critic #31's first falsifiable test.

### F4 — no gate finding, no CHARTER §4b finding, no report-certification finding

- `gates.py --mode full` **ALL GREEN** at `524f13c`, **exit 0**: `1616 passed, 62 skipped,
  1 xfailed`, **cold**, 350.4 s. `verify`, `snapshot-verify`, `env-check` PASS; `baseline-verify`
  WARN is CHARTER §3d information. `--assert-head` exits 0.
- **CI read through the GitHub MCP** (`curl` is 403 through this proxy, WFG-119): `auto-gates`
  runs **165 to 184** on `auto/dev` are **18 `success` and 2 `cancelled`** (`dfdf480`, `8cc1e89`,
  each superseded by a green push) with **no `failure` at all**. Run **184** at `524f13c`, this
  head, is `success`. No red run sits behind a green report.
- **Every push in the window carried a report**, checked pair by pair with
  `gates.py --assert-reported --base <previous push>`. The one non-zero exit is the range spanning
  the concurrent paper lap and the WFG-138 claim commit, where the paper work is covered by
  `docs/auto/reports/2026-09-06T2119Z-manual.md` in the neighbouring range. Every **dev** report
  carries `Reviewed by:`; the research report does not (**WFG-147**, already filed, not duplicated).
- The clone was unshallowed before any measurement: `is-shallow-repository` answers **`false`**,
  508 commits.

### F5 — the cold/warm gap is now a controlled demonstration of WFG-139 (no new row)

My **cold** `1616 / 62` at `524f13c` against the 2154Z lap's **warm** `1622 / 56` at `8e902dd`,
trees that differ only in prose and generated board files: **exactly six tests in each direction**.
That is WFG-139 reproduced across two sandboxes one lap apart, and it is the cleanest evidence for
that row anyone has produced.

### Not filed, deliberately

`scripts/auto/report.py` has no `paper` kind, so the paper lap files as `manual`. Already
**WFG-044** and **WFG-141**; I did not file a third. **WFG-146** (the knowledge note's
one-character date on the 사이언스타임즈 article) is unchanged and did not propagate into the
manuscript, which does not cite that article. Both new `references.bib` entries added at `719c420`
carry `verified 2026-09-06` notes with agency, date and scope and state in writing that no accuracy
figure exists on either page; the `khan2026gdaps` URL slug `202603301116001` agrees with the
`30 March 2026` the entry records.

## The root objection (`hate`)

**A gate that certifies 「this sentence is caveated」 is only as wide as its list of caveats, and
the loop wrote that list from the one defect in front of it.** The window's work is good: a real
overclaim on the front page was repaired, the correction reached the sentence rather than a note
beside it, and the gate was graded by a **move** mutation, which is the axis the claim is about.
And the result is a README bullet that is now green under a gate while the second of the
manuscript's own two binding caveats is missing from it. Certifying a sentence is not the same as
reading it.

**The cheapest test is one line**, and it is WFG-148's half (b): add an ORACLE spelling family, make
`_is_caveated` require both families on `README.md` and `paper/manuscript.md`, and watch it go
**red** at this head. If it goes green, my finding is wrong and the row closes as no-change.

I checked the obvious generalisation and it does **not** hold, so I am not making it: the gates
from WFG-113, WFG-117 and WFG-133 all read a class from a registry rather than a list the lap
typed. This is one gate, not a pattern, and saying otherwise would be the kind of tidy story this
report exists to catch.

## Direction check (CHARTER §14, five minutes)

**No row moved and the reorder budget is unspent.** WFG-138 closed, so the first `todo` row in
table order is **WFG-134**, which is `DIRECTION.md`'s row 2 and the 2154Z lap's own stated next
row. The page and the table already agree; a reorder would be motion without a reason. WFG-148
enters at position 1 as this lap's one item, which is not a reorder of an existing row.
`DIRECTION.md` is updated in place and still one screen.

## Scorecard

**Every row held on both tracks, and the flat window is the finding rather than the absence of
one.** Track B `17 · 16 · 18 · 15 · 17 = 83`; Track A `16 · 16 · 18 · 15 · 13 = 78`, both
unchanged from `1b26c3a`.

- **제출 자료 held on two moves that cancel.** **+1** for the repair of the README's headline
  attribution, which is the exact defect critic #28 docked both tracks for and which is now gated.
  **-1** for the two things measured in the same window: the second binding caveat missing from the
  same bullet with the new gate green on it (F1), and the fourth printables drift, the first that
  makes the printed pages worse rather than older (F2). Critic #27's precedent in its own words: a
  제출 자료 row does not rise while the document the student recites from disagrees with the files
  the gates read.
- **데이터 수집·분석·해석 18 held:** no model, split, metric, arm, coupling or protocol moved.
- **연구 목적, 설계와 방법론, 창의성 held on both tracks:** the manuscript's new Related-work
  paragraph is the same evidence critic #29 already scored on both tracks; a row does not move
  twice for one fact, and the surface these five judges meet still has no card for it (WFG-144).
- **A 구현 및 유용성 18 held, re-earned rather than inherited:** gates green at this head and
  `auto-gates` run 184 `success`. Nothing was built and nothing broke.

## The falsifiable test for critic #31

1. If the next dev lap clears **WFG-148** and does **not** also claim **WFG-134** in the same lap,
   then 「clear the item, then take the next row」 does not fit in one lap and the fix belongs to the
   cadence, not to the row.
2. If **WFG-134** ships without **WFG-140** going red on the pre-rebuild tree first, the freshness
   gate is green by construction and the drift series will reach five.

## In plain terms

- **What the judges would see today.** A strong project whose front page finally agrees with its
  own paper about the number it leads with. The one thing left on that sentence is that your paper
  gives it **two** warnings and the front page now carries one: the forecast route is planned on
  the very fire map it is then graded against, so 42 is what a *perfect* forecast would buy, not
  what your model buys. Your Q&A card already says this well. The front page does not say it yet.
- **What changed since yesterday.** The 21:54 lap fixed the front page and the answer you read
  aloud, and put two tests behind both so they cannot slip back. Nothing broke: all gates green,
  GitHub's own runs all green. The one thing that got worse is the booth folder you print: the
  17 Q&A pages in it are now out of date against exactly the sentence that was just improved.
- **What you should do.** Two five-minute replies, both overdue. **NH-032** decides which
  fair-opponent number counts, and until you answer it three sentences you say at the booth stay
  hedged. **NH-038** asks whether the loop should keep spending every dev lap on my findings; the
  readiness checklist has now sat at 4 of 11 for seven of my laps in a row, and the printed booth
  folder is what is waiting behind them.



## Gates

**ALL GREEN** · mode `full` · head `524f13c` · 2026-09-06T23:05:23Z · current at `524f13c`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 22.1 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.2 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 1.6 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 350.4 s | 1616 passed, 62 skipped, 1 xfailed, 52 warnings in 348.03s (0:05:48) |

## Commits since the previous report (20b0b7ea85a3f9b2b78ead257dd10c5e52df93ff..HEAD)

- 524f13c the board and STATE name this lap after the rebase onto the concurrent paper lap
- 0983983 report 2026-09-06T2154Z-dev

## Needs a human (12 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09)
- NH-036 [DECISION] One critic lap told the next one not to edit a file, and that is what kept a false sentence in front of a judge for a window (by 2026-09-10)
- NH-037 [DECISION] The paper's word proxy now stops it a thousand words before your 25-page rule (by 2026-09-10)
- NH-038 [DECISION] Your "product first" rule has spent the last three dev laps on documents, and the readiness line it was written to protect has not moved in five critic laps (by 2026-09-09)
- NH-039 [DECISION] The national wildfire-spread system's manual is an 18 MB PDF the sandbox could not fetch, and one of you can (by 2026-09-12)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.

---

## Annotation, written after the push (2026-09-06T2330Z)

A report is a record, so this is an annotation rather than a rewrite, following every critic
lap since 0215Z.

1. **The head.** `report.py` stamps the gate table at the head this report *reviewed*,
   `524f13c`. The report ships in **`d6cb996`**, and the fix-up commit that carries this
   annotation ships after it. All ids resolve.
2. **The re-run at the pushed commit is WARM and the table is COLD, and the gap is WFG-139
   for the third consecutive critic lap.** `gates.py --mode full` at `d6cb996` is **ALL
   GREEN**, exit 0: `1622 passed, 56 skipped, 1 xfailed`, 342.1 s, against the table's cold
   `1616 / 62` on a tree that differs only in this lap's prose. Six tests, both directions,
   because the first suite run in this sandbox downloaded the SRTM tile. `--assert-head` and
   `--assert-reported` both exit 0 immediately before the push. The cold counts are what
   GitHub's clean clone reports and what this report's prose quotes.
3. **The header's backlog line reads `oracle: 1`, and that is a defect this lap caused and
   then filed.** `scripts/auto/report.py` `backlog_counts()` requires a backlog row's
   description cell to contain no pipe character. `WFG-148` as first written quoted a regex
   alternation, so the parser split the row at the wrong cell and invented a status bucket
   named after one of the alternatives. The row was rewritten pipe-free before the fix-up
   commit and `todo` went from 84 to 85. Pulling that thread found the general case: the
   table holds **144** `WFG-` rows and the parser matches **110**, so **34** rows, every one
   whose prose quotes a shell pipeline or a regex, have been invisible to the header line of
   every report and to `03_backlog.png`, which the report email calls 「The plan」, since the
   loop started. Filed as **WFG-149** (P1, infra, held behind the readiness lines by CHARTER
   §14b). The correct reading of this report's header is
   `blocked: 7, done: 17, parked: 1, todo: 85`, and even that is 110 of 144.
