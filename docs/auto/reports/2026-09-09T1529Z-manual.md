# WildfireGuardian autoloop · paper · 2026-09-09T1448Z

| | |
|---|---|
| when | 2026-09-09 15:29 UTC · 2026-09-10 00:29 KST |
| branch / head | `auto/dev` / `4693540` |
| kind | manual |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 9, done: 33, dropped: 1, parked: 1, title-derivable: 1, todo: 105 |

## What happened this lap

Paper lap 24. Incorporated `65edfa3..4693540` — **twelve** commits, **sixteen** files
outside `paper/` and `docs/auto/`, five new reports. **Two sentences of the manuscript went
false in this window and both were corrected**, at a measured **net +1** word: `body_words`
8,998 → **8,999**, margin **2 → 1**, the tightest this document has ever been. Nothing was
compressed and no caveat or registered number was traded.

**Reviewed by: subagent (block)** — see the block section below. Every one of its objections
was verified in the tree and every one was right; all were fixed before this push.

One document did all of it: **`docs/oracle_gap.md`** (WFG-125, the 1250Z dev lap), which
opened `data/processed/routing_demo_canonical.npz` — **the file the canonical 458-origin
routing already runs on** — and found two stacks inside it. Re-derived here with `np.load`
rather than quoted: `haz_stack float32 (5, 181, 156)`, the leave-one-fire-out forward
simulation the router **plans** on, and **`obs_stack uint8 (6, 181, 156)`, the cumulative
FIRMS-observed footprint**, on the same 500 m grid. `haz_times` 0/180/360/540/720 min;
`obs_times` 0/333/1005/1480/1812/2403 min.

## Sections changed, and why

**§4.5 — the oracle is in the grader, not in the planner (mandatory; WFG-214).** The section
had said the forecast-aware arm's margin is 「what a **noiseless** forecast is worth」. The
premise is right and the mechanism is wrong: the arm does not plan on truth, it plans on a
model output on a fire the model never trained on, and what makes it an oracle is that the
**grader** treats that same array as truth. Critic #51 filed this as its **root objection**,
naming `paper/manuscript.md:512` **by line number**. §4.5 now reads 「…plans on the same
hazard field it is graded against, a leave-one-fire-out simulation rather than the fire, **so
it cannot be wrong there**」 — **+2** words, measured.

⛔ **The bound in the same sentence is untouched, word for word.** 「this project's own model
is worth less, by an amount no run here measures」 is the 「상한」 half; nothing in this
repository derives it (the margin is a difference of two scores, and changing the grading
field moves the fire-blind arm's score too). That is **NH-053**, an open author decision, and
no lap may settle it in either direction.

**§6 — the paper said the data does not exist, and it has been committed since Round 3
(mandatory).** The first limitation, the one the paper calls 「the objection we would raise
first」, closed with a gap marker asserting the hindsight-field routing pass cannot run because
「**Those detections are not distributed with the repository**」. `obs_stack` falsifies it. Six
days of gap **G4** were blocked on the wrong input — the git-ignored per-detection CSV, which
this arm never needed. The marker now reads 「…on the observed FIRMS footprint … **That field
is committed on the same grid, on its own clock, an observation and not the fire]**」, and
G4's 「after sprint?」 goes **yes → no**: the run is a dev lap's, blocked on **NH-052** rather
than on data.

⚠ **The clause deleted with it is not lost and is not a caveat trade.** 「the two committed
manifests place its ignition point about 30 km apart」 was a *second* blocker that does not
bind a re-grade on an already-rasterised field, and the fact **survives verbatim at
`manuscript.md:636`** inside §4.8 — verified by grep before the deletion, and re-verified
independently by the reviewer.

## ⚠⚠ The reviewer blocked, and the block was right on every count

**Root objection — the sharpest this routine has had.** The draft §4.5 fix **deleted the
bound's stated mechanism and left the bound standing.** Pre-edit the section gave a chain:
graded on the field it plans on → 「noiseless」 → therefore this project's model is worth less.
The premise was wrong, but the conclusion had *a* stated mechanism. The draft removed it and
put nothing in its place, leaving 「this project's own model is worth less」 with **no
antecedent whatever**, in a judge-facing section, on precisely the claim NH-053 says 「nothing
in this repository proves」. **Deleting a false justification while keeping the conclusion
strengthens an unproven claim rather than repairing one.** The draft also never once said
**grader** — the whole content of WFG-214, and the dev commit's own subject line (`f958c3e`,
「the oracle is in the grader, not in the planner」). **Fixed**: the mechanism is now stated.

**First nail — two commands, both under a second.** `grep -c oracle_gap paper/manuscript.md`
→ **0**; `git status --short` → ` M docs/auto/NEEDS_HUMAN.md`.

Everything it required, done:

1. ⚠⚠ **CHARTER §12 breach, reverted in full.** The lap had written fifteen lines into
   `docs/auto/NEEDS_HUMAN.md` — outside `paper/` — in an annotation whose own text read
   「none is inside CHARTER §12, so no paper lap can reach them」, **citing the boundary in the
   act of crossing it**. Worse, the insertion landed **between two rows of NH-053's surface
   table**, so the `docs/auto/JUDGE_QA.md:1399` row — the sentence the student is told to say
   at the booth — stopped rendering as a table row in the one document the author reads to
   decide. `git checkout --`; the information is in this report and `paper/GAPS.md` instead,
   and updating NH-053's table is a critic or dev lap's job.
2. ⛔ **Half of NH-053's instruction is declined for the budget, and the draft ledger reported
   partial compliance as complete.** The entry asks for the mechanism 「plus a link to
   `docs/oracle_gap.md` from every surface that discusses the margin」 and the bound left
   「with a pointer to this entry」. `grep -c oracle_gap paper/manuscript.md` returns **0** and
   still does: the shortest pointer costs **8 words against a margin of 1**, and a manuscript
   cannot carry a pointer to an internal NEEDS_HUMAN entry at all. The phrase 「exactly as that
   entry instructs」 stood in `GAPS.md`, `paper/README.md` and `STATE.json` and is struck from
   all three. **One conjunct of two, reported as one of two.**
3. ⚠ **The grep bullet was itself a miscount** — the exact failure class the previous lap's
   reviewer blocked on, inside the bullet whose point is that the sweep was run. The draft said
   `grep "noiseless\|upper bound"` returns 「three hits besides `:512`」 and listed as the third
   a line that pattern does not match. Re-run: **three total** pre-edit (`:512`, `:623`,
   `:768`), i.e. **two** besides; **two** after the edit. Only the widened
   `grep -nE "noiseless|upper bound|\bbounds?\b"` reaches §5's `:652`, returning **six**, and
   it also surfaces `:375` (conformal coverage), `:746` (ERA5 resolution) and `:774`
   (「road-**bound**」, a false positive) — none mentioned by the draft, none an NH-053 instance.
4. ⚠ **§6's marker was less honest than the ledger, which is the wrong way round.** G4 names
   three surviving obstacles and the draft marker kept only 「not truth」. Within the 720-minute
   horizon `obs_stack` has exactly **two** slices, 0 and 333 min, and the 0 slice is the
   simulation's own seed (`og_yeongdeok_t0min_iou` = **1.0** by construction) — so reading the
   observation between its own times is a free parameter that must be fixed **before** the run
   (WFG-201). 「on its own clock」 now says so, **+2** words.

**What the reviewer cleared, in its own words:** the manuscript's numbers and citations are
clean and this is not a numbers failure; the §6 correction is substantively right; the
「committed since Round 3」 claim is supported not by git (shallow, depth 50) but by
`data/processed/canonical_hazard.json`, generated 2026-08-02, listing `obs_stack` with a
sha256 matching the file on disk. It independently re-derived the npz contents, the commit
range (12 / 16 / 5), the registry delta (403 → 428, all 25 `og_yeongdeok_*`, 0 values and 0
caveats changed), all ten oracle-gap figures, `check_paper.py`'s every field, and 29 bib keys
against 29 citations with zero orphans either way.

## Gaps opened and closed

**None opened; none closed; still seven.** `G4` is **superseded in place** — the run has not
happened, so the gap stands — and its 「after sprint?」 goes **yes → no**. `G8`'s own
「noiseless」 clause is annotated as superseded the same way, with the 「buys less」 half
explicitly left standing under NH-053.

## Figures

**None added, none changed.** `make_figures.py` printed `drawn [F1…F9]; skipped []` and
`git status paper/figures/` was empty afterwards — all nine redrew **byte-identical** — so no
figure was new or changed and none needed the look-at-it pass. No figure was added: the only
result that would want one is the section declined below, and a figure costs a page. No
citation was added, so none was opened this lap; `references.bib` is unchanged at 29 entries.

## ⛔ The one true sentence the lap wanted, measured and declined — the largest NH-037 casualty yet

`docs/oracle_gap.md` measured the thing §6's first limitation says nothing here admits: how far
the predicted field sits from what burned. At the best time-matched pair (simulation at 360 min
against the observation at 333 min, **27 minutes** apart) the predicted core holds **952** cells
against **937** observed, **534** shared — **IoU 0.394**, size ratio **1.016** — with **25**
registered `og_yeongdeok_*` keys. The paper is free to write them.

It cannot. The shortest honest form was inserted and **measured with the builder's own counter,
not estimated**: `body_words` 8,996 → **9,037** at the pre-reviewer baseline, i.e. **41 words**,
37 over the proxy's hard fail — **against this lap's final margin of 1**. Reverted. **Nothing
was compressed to fund it** (CHARTER §12 forbids buying space with a caveat or a number).

⚠ **The honest other half.** §6's 「Every control here perturbs the predicted field, so none
admits external truth」 **is still true and was checked rather than assumed** — all five controls
of §3.5 perturb the predicted field, and a field-to-field comparison is not one of them. So the
manuscript is **not false** without the number. It is **under-reporting its own strongest new
evidence against itself**, on the limitation it names first. That is sharper than lap 21's
casualty, which was a limitation the paper nowhere stated: this is a *registered measured
result* on the canonical field the headline number comes from. **Laps 13 through 21, and now 24,
have all had their writing shaped by the proxy rather than by the evidence. NH-037 is the answer
and it is still open.**

## Handoff to a dev lap (outside CHARTER §12, so this routine cannot do it)

**WFG-214 is only one-fifth done.** Four surfaces still carry the corrected-away mechanism:
`README.md:36`, `:266-267`, `:702` and `docs/auto/JUDGE_QA.md:1399` (the last also a hashed
`SOURCES` entry of the printed kit, NH-049). ⚠ **`README.md:38` cites `paper/manuscript.md`
§4.5** and was checked before the edit, because deleting the text it cites is what killed lap
20's fix: §4.5 **still carries both** things it is cited for — the same-field mechanism, and
「less by an amount no run here measures」 verbatim — so the citation is **incomplete rather
than broken**, the one word it now quotes that §4.5 lacks being 「noiseless」. **NH-053's own
surface table still lists `paper/manuscript.md:512` as uncorrected**, and a critic or dev lap
should annotate that row rather than leave the author reading a stale table.

## Verification

- `docs/NUMBERS.json` re-derived key by key against `65edfa3`: 403 → **428**, **all 25 new
  keys `og_yeongdeok_*`**, **0** existing values changed, **0** caveats changed, 0 removed.
- **No claim was withdrawn:** eleven entries before and after; `check_withdrawn_claims.py`
  prints 「PASSED === 11 claims over **937** gated files」 (935 at lap 21, 936 at lap 23; run
  this lap). §3.5's 「skipped it **three** times」 stays three, because there was no fourth
  retraction — a different thing from a fourth registered correctly.
- **Anchor re-derived twice, because `body_words` moved twice.** After the one `apt` line
  `paper/README.md` has printed since lap 9, `check_paper.py` took its measuring branch —
  **`pages 23, calibri_face Carlito, metrics_ok true`**, page objects and page-tree `/Count`
  agreeing. The pre-repair run printed `b7c713c21f56a2d2` and **that is not what `STATE.json`
  records**; it records `61b8ee7cf0f331f8`, the value the final run derived.
- ⚠ **Clone shallow:** `--is-shallow-repository` = `true`, `git rev-list --count HEAD` = **50**,
  both measured this lap. **No ancestry or reachability claim is made anywhere in this lap's
  output** (CHARTER §4).
- `check_paper.py` exits **0**. `gates.py --mode full` exits **0** (verify PASS,
  baseline-verify WARN — the two git-ignored `data/raw/**` contracts, NH-029 and CHARTER §3d
  working as the author chose — snapshot-verify PASS, env-check PASS, pytest-full **1841
  passed, 63 skipped, 3 xfailed**).

**Word count: 8,999 body words** (proxy 9,000, margin **1**). **Built pages: 23** under
Carlito, measured this lap (author's ceiling 25, margin **2 pages**). Figures 8 referenced
(9 drawn), tables 4, references 29, gaps 7.

## In plain terms

- **What the paper now says.** It used to explain your headline 42 with the wrong reason and
  now gives the right one: the forecast was never graded against the real fire, it was graded
  against its own prediction, so it could not be wrong. It also used to say the record of the
  real fire is not in the repository — and it is, sitting in the same file as the routing all
  along.
- **What is still missing.** Nobody has re-scored the routes against what actually burned,
  which is the one run that would make 42 a statement about the fire instead of about the
  model. The data is in hand; the run is waiting on you. And the paper still cannot report how
  far its prediction sits from the burn — that sentence needs 41 words and the paper has 1.
- **What you should do.** Answer **NH-053** (the loop recommends option A: stop calling 42 an
  「상한」 until something measures it) and **NH-052** (may the loop run that re-scoring on
  영덕). Then **NH-037**: the 9,000-word limit is now down to a single word of slack and it is
  blocking the paper from reporting the very measurement that answers its own first limitation.



## Gates

**ALL GREEN** · mode `full` · head `4693540` · 2026-09-09T15:21:44Z · current at `4693540`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 20.8 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.2 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 0.5 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 398.4 s | 1841 passed, 63 skipped, 3 xfailed, 52 warnings in 397.00s (0:06:36) |

## Commits since the previous report (8506a2d5668f4a91fb0abbdf7ff944c82e9524ad..HEAD)

- 4693540 critic #51: the repository gives two answers to what its headline number means, and the right one is unreachable

## Needs a human (25 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09)
- NH-036 [DECISION] One critic lap told the next one not to edit a file, and that is what kept a false sentence in front of a judge for a window (by 2026-09-10)
- NH-037 [DECISION] The paper's word proxy now stops it a thousand words before your 25-page rule (by 2026-09-10)
- NH-038 [DECISION] Your "product first" rule has spent the last three dev laps on documents, and the readiness line it was written to protect has not moved in five critic laps (by 2026-09-09)
- NH-039 [DECISION] The national wildfire-spread system's manual is an 18 MB PDF the sandbox could not fetch, and one of you can (by 2026-09-12)
- NH-040 [FYI] A critic lap pushed one commit past a red `--assert-reported`, and it is telling you rather than hiding it
- NH-041 [FYI] This lap sent you an email containing only the word PLACEHOLDER, and could not take it back
- NH-042 [DECISION] Two of your own rules collide whenever a withdrawn claim lives in a frozen artifact, and this week they collided three times (by 2026-09-10)
- NH-043 [DECISION] A gate your loop built this morning will go red about twice a day, and the charter tells the lap that meets it to stop working (by 2026-09-09)
- NH-045 [BLOCKER] The staleness gate has closed `auto/dev` to every routine, and the one routine that met it is the one forbidden to clear it (by 2026-09-08)
- NH-044 [DECISION] The claim the paper just retracted is still live on the page the paper cites for it (by 2026-09-09)
- NH-046 [DECISION] Your product's definition-of-done names a command nothing in this project has ever run (by 2026-09-10)
- NH-048 [DECISION] One of the research routine's three literature channels has been dead for two runs, and a working replacement is already proven (by 2026-09-10)
- NH-049 [DECISION] Your critic routine is told to add judge Q&A cards and your own printing gate makes that impossible for it (by 2026-09-11)
- NH-050 [DECISION] You answered two of these questions two days ago and the loop never heard you, because you answered them on the routine page (by 2026-09-10)
- NH-051 [DECISION] The rule your loop has been obeying for three days is not the option it names, and the difference is why one row has been pushed down five times (by 2026-09-11)
- NH-052 [DECISION] The experiment that measures what your own forecast is worth is now one run away, and it would change what 42 means (by 2026-09-12)
- NH-053 [DECISION] The word your front door uses for 42 stopped being right today, and the loop cannot pick its replacement (by 2026-09-12)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
