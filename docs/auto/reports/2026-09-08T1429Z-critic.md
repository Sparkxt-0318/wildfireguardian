# WildfireGuardian autoloop · critic · 2026-09-08T1429Z

| | |
|---|---|
| when | 2026-09-08 14:29 UTC · 2026-09-08 23:29 KST |
| branch / head | `auto/dev` / `dee1bc1` |
| kind | critic |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 9, done: 28, parked: 1, title-derivable: 1, todo: 97 |

## What happened this lap

Critic lap #43. Reviewed head `dee1bc1`, window the 24 h to 2026-09-08T14:29Z.
No code, test, data, figure or `docs/NUMBERS.json` entry was touched; this lap writes
only under `docs/auto/`.

⚠ **This clone is shallow.** `git rev-parse --is-shallow-repository` = **true**,
`git rev-list --count HEAD` = **50**, oldest resolvable commit `7cc4eb7` at 16:12Z on
09-07. So this clone reads about 22 hours, I claim nothing about anything older, and
**no ancestry or reachability claim appears anywhere in this report** (DIRECTION's
standing rule after five consecutive critic laps published false ancestry findings).

## Your decisions, applied

**Nothing new to apply, and I checked both channels.** Gmail returned
`The service is currently unavailable` on the first call and answered on the second.
Every thread matching `from:siyeong0318@gmail.com subject:"WildfireGuardian autoloop"
newer_than:14d` holds exactly **one** message, and every one of those messages is
labelled `SENT`, that is, written by the loop itself. `pull_request_read` on PR **#31**
returns `[]`. **There is no author reply in either channel**, so
`scripts/auto/decisions.py` was not run and `docs/auto/decisions_seen.json` is
unchanged.

Fourteen entries remain open. **NH-032 and NH-034 are two days overdue**; NH-035,
NH-038 and NH-043 come due tomorrow; NH-036, NH-037 and **NH-046** on 09-10.

## The headline: a readiness line ticked

**`docs/auto/KCF_READINESS.md` moves 7 of 11 to 8 of 11. R8 is green, and the
zero-tick streak ends at three consecutive critic laps.** Measured at `dee1bc1`,
unpiped, not read from a report:

- `grep -nE '^## Round' README.md` answers `:59`, `:75` and **`:200`**
  「Round 4 (2026-09 — 본선 준비)」, where all week it answered only `:59` and `:75`.
- `grep -n '^### Abstract' README.md` answers **`:596`** 「Abstract (draft)」, carrying
  the draft label on the file that CHARTER §9 requires.
- `make check-forbidden` exits **0**; `make verify` PASS inside the full gate run.

That is all three halves of R8's condition. The row is WFG-010, done at `692497a` with
its reviewer's fixes at `ea04478`, bound by `tests/test_readme_round4.py` (316 lines).

**WFG-187 also closed, and the branch is no longer near the limit that shut it on
09-07.** `web/finals.html` carries `"git":"25f6b60"`, `git rev-list --count
25f6b60..HEAD` answers **9**, and `tests/test_finals_screen.py:540` sets
`STAMP_MAX_COMMITS_BEHIND = 30`. Critic #42 measured **29** at the head its own
bookkeeping produced.

Two different judge-facing surfaces moved in one window. That has not been true for
six windows and it is the first thing in this report because the last three critic
laps could not say it.

## Findings, ranked

### F1 (the one `fix-before-next-row` item, filed as WFG-190) — the section that ticked R8 is the weakest of four surfaces carrying the same fact, and one of its sentences is a flat universal this repository falsifies

Two defects, one paragraph block, `README.md:220-239`. Neither is bound by a test:
a `grep -nE` over `tests/test_readme_round4.py` for 상당 부분, 회수, `recovers most`
and 완충 returns nothing. The fix is prose alone, which is why it qualifies under
CHARTER §14b as amended by NH-038 B (minutes, README opening).

**(a) `README.md:235-239` claims something `docs/auto/JUDGE_QA.md` contradicts.** The
line reads 「이 실험이 내놓은 구체적인 margin 값들은 아직 어느 심사용 자료에도 싣지
않습니다」. `docs/auto/JUDGE_QA.md:849-852` carries **9**, **27** and **5** in one
paragraph, and `docs/auto/finals/printables/manifest_20260908T0939Z.json` lists that
file, under the title 「심사위원 질의응답 카드」, among the six documents bound into the
**41-page** printed booth kit. A document whose own title contains 심사위원 carries all
three values, on paper, today. 「심사용 자료」 is defined nowhere in this repository.
⚠ **The Q&A card is correct and must not be changed:** its block ends with an explicit
❌ forbidding the student from speaking 9, 27 or 5 at the booth, which is precisely the
discipline NH-032 and NH-034 are waiting on. **The README sentence is the wrong one**,
and it is the WC-009 class this loop registered on 09-07: never a flat sentence about
what a document contains.

**(b) `README.md:220-225` states a buffer-conditional result unconditionally, and the
project's own manuscript does not.** Four surfaces carry the same fact and the front
door is the only one that drops the condition:

| surface | what it says about the buffer |
|---|---|
| `paper/manuscript.md:493` | 「The buffer width is a second free parameter and nothing in the data chooses it.」 |
| `docs/present_perimeter_arm.md:116` | section heading: 「Why 1 km is not a constant, and why that matters more than the 9」 |
| `docs/auto/JUDGE_QA.md:1346` (Q37 · T1) | five widths swept; 「1 km 의 양옆 이웃이 각각 두 배 거리에 있어서 … 이 sweep 으로는 구분할 수 없습니다」 |
| **`README.md:220-225`** | **the width is not named and the sweep is not mentioned** |

And the strength is understated in both languages. `docs/present_perimeter_arm.md:103-104`
measures the opponent recovering **86 of the 91**; the table at `:121` gives **12 of 91**
at 250 m and **23 of 91** at 500 m, with **91** origins walking into the fire at 250 m.
「상당 부분」 and the abstract's 「recovers most」 (`README.md:629`, `paper/manuscript.md:23`)
are both true and both weaker than 86 of 91, on a result the section's own reading note
at `:207-208` promises reads **against** this project.

⚠ **Constraint on the fix:** qualitative only. No margin value and no new result figure
goes into `README.md` while NH-032 and NH-034 are open.

### F2 (WFG-191) — the backlog table does not parse as the table CHARTER §5 defines, in 11 rows of 187, and the worst is at the head of the `todo` block

Splitting each backlog row on the pipe character, **11 of the 187** `WFG-` rows yield
something other than the 9 columns §5 names: WFG-112, WFG-115, WFG-133, WFG-149,
WFG-167, WFG-168, WFG-175, WFG-181, WFG-182, WFG-188 and (as first written by this lap)
WFG-191, from 11 fields to 19. The cause every time is an unescaped pipe inside a quoted
`grep` alternation or a regex in the title cell.

**WFG-188 is the one that matters now**, because it is the row the next dev lap takes:
its column 5, which §5 defines as `status`, reads **「예산」**, its real `todo` sits in
column 9, and `goal`, `agent_doable`, `effort` and `rubric rows` are each shifted by
four. These rows also render with shifted columns wherever BACKLOG.md is read as
Markdown, which includes the board image attached to your report emails.

⚠ **I filed this row and then committed the same defect three times inside five
minutes.** Both of my new rows first went in malformed, at 13 and 17 fields, and my
first repair appended text into the middle of a sentence because the row was already
split, so WFG-191 had to be rewritten whole. That is not an aside and it is written
into the row: it is the evidence that a convention cannot hold this invariant and a
gate must. **The ten pre-existing rows are left as they are**; this lap repaired only
what it broke, and the repair of the rest is the row's own work.

Second, smaller half of the same class: §5 defines the status as `done(<commit>)` and
**32 of the 46** `done()` cells carry a claim stamp such as `done(20260908T1240Z)`
rather than a commit id, against 13 that carry a commit.

### F3 — WFG-179 was a `todo` with `agent_doable: true` that no lap is permitted to finish

The row's own *Done when* cell ends 「whichever the author picks in NH-046」, and NH-046
states that a lap editing a readiness line to match what the loop already does is a lap
grading its own homework. That is CHARTER §5b's lock-with-no-key, one column over.
**Status changed to `blocked(NH-046)` by this lap** as a step-5 update to an existing
row; nothing was reordered to do it.

### F4 — with R8 ticked, R3 is the last of CHARTER §14b's six lines, and it is yours

§14b holds the P1 infra block until R1, R3, R4, R7, R8 and R9 tick. R1, R4, R7 and R9
were ticked before this window, R8 ticks today, and **R3 is the only one left**. R3's
row is WFG-179, which F3 has just established is blocked on **NH-046, due 2026-09-10**.
**Eight P1 infra rows now wait on one email reply**, and nothing in the loop's power
shortens that queue. Written into NH-046 rather than opened as a fourteenth entry.

### F5 (WFG-192) — nothing in this loop measures a standing claim getting weaker on its way to the front door

`docs/auto/withdrawn_claims.json` and `scripts/check_withdrawn_claims.py` catch a
**withdrawn** claim being copied forward. There is no equivalent for a **standing**
unfavourable result being restated more softly for a wider audience, and F1 is two
instances of exactly that, found by hand, in one paragraph.

## The root objection

**The loop keeps writing the weakest version of its own honesty on the surface a judge
meets first, and the strongest version in the file nobody opens at a booth.**

Measured, not felt. On the buffer's freedom: `paper/manuscript.md:493` states it
outright, `docs/present_perimeter_arm.md:116` gives it a section heading saying it
matters more than the headline number, `docs/auto/JUDGE_QA.md:1346` sweeps five widths
and names what the sweep cannot resolve, and `README.md:220-225`, written this window
and last of the four, says none of it. On what the opponent recovers: the artifact says
**86 of 91**; the manuscript and the README both say 「most」 / 「상당 부분」. On where
the margin values live: the README says 「어느 심사용 자료에도」 and the printed 41-page
kit carries all three.

This is not critic #42's objection. Critic #42 said the loop kept fixing the same file;
this window it fixed two different judge-facing surfaces and ticked a readiness line,
so that objection is answered and I have written the counter-measurement into NH-038.
The new one is about **direction of travel inside a document**: every time this
repository restates a hard fact for a wider audience, the restatement comes out softer,
and nothing measures that.

**The cheapest test, and it fits in one lap:** for each of the three unfavourable
results the README's Round-4 section names (present-perimeter recovery, dispatch
ordering, detection floor), diff the README's sentence against the source document's
own strongest sentence and say which is weaker. Two of the three were checked here and
both were weaker on the README. WFG-192 files that as a gate rather than as a claim.

## Baseline and the two independent channels

✅ **`gates.py --mode full` exits 0 at `dee1bc1`**, ALL GREEN on the routine's default
clone before any deepening: `1749 passed, 63 skipped, 2 xfailed`, pytest **312.3 s**.
`verify`, `snapshot-verify`, `env-check` PASS; the `baseline-verify` WARN is the
documented CHARTER §3d sandbox state. **The run downloaded nothing:** `du -sb data/raw`
answers **201,187** bytes and `data/raw/dem/srtm/` holds **0** files afterwards. Cold on
the tile, warm on `data/cache`. `--assert-head` exits 0; `--assert-reported --base
7cc4eb7` exits 0 over 61 substantive paths.

✅ **GitHub's own runs (CHARTER §4b): no finding.** Through the GitHub MCP; `curl`
against `api.github.com` is still refused here (WFG-119). `auto-gates` runs **230 to
249** on `auto/dev` are **16 `success`, 4 `cancelled` (232, 235, 242, 245), ZERO
`failure`**, and run **249** at this exact head is `success`. No red run sits behind a
green report.

✅ **Report certification.** Every dev and critic report in the window carries
`Reviewed by:`. The one without it is `2026-09-08T1132Z-manual.md`, written to satisfy
`--assert-reported` for a `CRITIC_LATEST` correction that carried no build; critic #42
declined to file that case and this lap declines it too.

⚠ **The bootstrap needed three attempts, and it is worth a line for the next lap.**
`scripts/auto/bootstrap.sh` died twice on
`pip._vendor.urllib3.exceptions.ReadTimeoutError` against `files.pythonhosted.org`
before `PIP_DEFAULT_TIMEOUT=300 PIP_RETRIES=15` carried it through.
`.auto/bootstrap.json` records `pins_ok: true`, `stack_ok: true`. Nothing in the
repository is implicated; those two variables are the whole remedy.

## Direction check (CHARTER §3b)

**No reorder spent, on the page or on the table.** Positions 1 and 2 of the last
DIRECTION page both closed inside this window (WFG-187 at `2c6e366`, WFG-010 at
`692497a`), so **WFG-188 reaches position 1 by fall-through**, and it is the right row:
one of the five finals judges is a public-sector disaster-response official and the bank
still has no answer to what a county would need to run this. I confirmed that rather
than inheriting it: a `grep -nE` over `docs/auto/JUDGE_QA.md` for 군청, 도입 비용,
운영 비용 and 유지보수 returns **2** lines and neither answers the question (`:703` is
legal status, `:851` a passing clause).

**Readiness lines ticked in the last 24 h: ONE (R8).** The zero-for-two-laps direction
finding the routine prompt defines does **not** fire this lap.

⚠ **`docs/auto/DIRECTION.md` is 226 lines and CHARTER §14 says one screen.** It was 210
before this lap; the growth is entirely the nested 「Superseded lead:」 chain in the
header, now five deep in one paragraph, because every critic adds one and §3.7 forbids
deleting. The fix is an archive file, not a deletion, and it is recorded on the page
rather than filed as a fourth row from one critic lap.

## Scorecard

Appended to all three tables at `dee1bc1`. **Track B 89 to 90; Track A 88 to 89.** One
row moves on each track and it is the purpose row on both, 연구 목적 / 개발 목적 17 to
18, on the same evidence: the front door now states the research purpose, the honest
ladder and an English abstract, and R8 ticked.

**Two rows are held deliberately and the hold is the interesting half.** Track B
제출 자료 stays at 19 and Track A 제출 자료 at 17, and 데이터 수집·분석·해석 stays at
19, because WFG-190 is a live defect on the exact page the window's work produced. A row
scored on 자료의 논리적 구성 does not rise on a page that contradicts another page in the
same printed kit, and a row scored on 논리적 해석 does not rise on a paragraph that drops
a free parameter its own manuscript names. I will not raise a row on the page I filed a
finding against in the same hour.

## What this lap did NOT find, said plainly

- **No red gate, on this machine or on GitHub.** Both channels checked, unpiped.
- **No fabricated number and no false world claim in the window's new prose.** The
  abstract's `0.890`, `0.905`, `0.138`, `42 of 458`, `2` and `32.6 %` all survive
  `make verify` at this head.
- **No JUDGE_QA card added.** The judge drill's one real gap is already WFG-188 at
  position 1; I confirmed it rather than duplicating it, and adding a card here would
  make the 41-page kit stale on a lap that cannot rebuild it.
- **R11 re-checked, still correctly unticked, already filed.**
  `docs/HANDOFF_ROUND3.md:898` still reads 「All work stays on `round3-dev`」 inside §5,
  contradicting CHARTER §3.1. That is WFG-024, blocked on WFG-023, and it is named in
  R11's own evidence cell. No new row.
- **No `Do NOT edit` note** (CHARTER §14c, NH-036 A). WFG-190 must edit
  `README.md:220-239` and WFG-188 must edit `docs/auto/JUDGE_QA.md`; freezing either
  would block the work. The single scoped prohibition is in `CRITIC_LATEST.md`, covers
  those lines only, forbids one thing, and expires at critic #44 unless that lap
  re-states it after re-reading NH-032 and NH-034.

## The one thing worth copying forward

The 1303Z lap wrote the section that ticked R8 and made three of its four items
arguments **against** this project, in Korean, on the front door, and said so out loud
at `README.md:207-208`: 「이 절에서 이 프로젝트에 유리한 항목은 4번뿐입니다.」 A README
that tells the reader which of its own paragraphs cut against it is the rarest thing in
this repository. **Fix the scope, keep the register.**

Reviewed by: critic #43 (independent daily review; no code changed)

## In plain terms

- **What the judges would see today.** For the first time your front page tells the
  whole story: it now has a Round 4 section and an English summary, and three of its
  four items argue against your own project on purpose. That is worth more to a judge
  than a good result. Your booth screen was also rebuilt, so it is no longer close to
  the check that shut the branch down yesterday. Your product checklist went from 7 of
  11 to 8 of 11, the first time it has moved in four of my laps.
- **What changed since yesterday.** Two real things landed instead of one more Q&A
  card, and everything is green here and on GitHub. What I found is small but it is on
  that same new page: it says your fair-opponent numbers are in none of your judging
  material, and they are in fact printed inside your 41-page Q&A booklet; and it
  describes that unfavourable result more gently than your own research paper does. The
  paper says the safety margin used in that test is a number nobody chose; the front
  page does not mention it. Say the harder version on the front page, not the softer
  one, because a judge who opens the file will find the harder one anyway.
- **What you should do.** One reply, and it unblocks the most: **NH-046**, due
  Thursday. Eight of my queued jobs are now waiting on that single answer and nothing
  else. Then **NH-032 and NH-034**, which are two days overdue and are still stopping
  your student from saying any of the three headline margins out loud to a judge.
  **NH-038** comes due tomorrow and I have written today's evidence into it, which
  points toward your option B.



## Gates

**ALL GREEN** · mode `full` · head `dee1bc1` · 2026-09-08T14:09:56Z · current at `dee1bc1`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 20.9 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.1 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 1.9 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 312.3 s | 1749 passed, 63 skipped, 2 xfailed, 52 warnings in 311.40s (0:05:11) |

## Commits since the previous report (7a966b2c75e470fef1228913517fc5394b0bf898..HEAD)

- dee1bc1 the report the assert asked for: NH-041's third instance and the row that replaces the habit with a test

## Needs a human (19 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09)
- NH-036 [DECISION] One critic lap told the next one not to edit a file, and that is what kept a false sentence in front of a judge for a window (by 2026-09-10)
- NH-037 [DECISION] The paper's word proxy now stops it a thousand words before your 25-page rule (by 2026-09-10)
- NH-038 [DECISION] Your "product first" rule has spent the last three dev laps on documents, and the readiness line it was written to protect has not moved in five critic laps (by 2026-09-09)
- NH-039 [DECISION] The national wildfire-spread system's manual is an 18 MB PDF the sandbox could not fetch, and one of you can (by 2026-09-12)
- NH-040 [FYI] A critic lap pushed one commit past a red `--assert-reported`, and it is telling you rather than hiding it
- NH-041 [FYI] This lap sent you an email containing only the word PLACEHOLDER, and could not take it back
- NH-042 [DECISION] Two of your own rules collide whenever a withdrawn claim lives in a frozen artifact, and this week they collided three times (by 2026-09-10)
- NH-043 [DECISION] A gate your loop built this morning will go red about twice a day, and the charter tells the lap that meets it to stop working (by 2026-09-09)
- NH-045 [BLOCKER] The staleness gate has closed `auto/dev` to every routine, and the one routine that met it is the one forbidden to clear it (by 2026-09-08)
- NH-044 [DECISION] The claim the paper just retracted is still live on the page the paper cites for it (by 2026-09-09)
- NH-046 [DECISION] Your product's definition-of-done names a command nothing in this project has ever run (by 2026-09-10)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
