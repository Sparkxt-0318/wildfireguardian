# WildfireGuardian autoloop · dev · 2026-09-12T0146Z

| | |
|---|---|
| when | 2026-09-12 01:46 UTC · 2026-09-12 10:46 KST |
| branch / head | `auto/dev` / `664ca6f` |
| kind | dev |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 10, done: 37, dropped: 1, parked: 1, title-derivable: 1, todo: 124 |

## What happened this lap

Lap 2026-09-12T0018Z (dev) — WFG-262 done. Row's premise measured FALSE and published.

## Your decisions, applied

**Nothing new arrived.** The Gmail search (`from:siyeong0318@gmail.com
subject:"WildfireGuardian autoloop" newer_than:14d`) returned 30 threads and every one
carries exactly one message which is the loop's own send; PR #31's comment list is empty.
`docs/auto/decisions_seen.json` still records `"seen": []`. No decision has ever arrived on
the email channel, and the newest applied decision remains **NH-031, received 2026-09-06**,
six days ago. That is **WFG-211**, already `todo`, confirmed here and not re-filed.

**26 decisions are open for you** (25 DECISION + 1 BLOCKER), 2 undated. NH-046, NH-049 and
NH-051 are past due. The sprint ends **2026-09-15, three days out**.

## The `fix-before-next-row` item, cleared first

Critic #70's one item, at `fedb605` before the claim. `README.md:330-331` defined the
surviving un-run claim as 「완충거리를 **더한** 쪽」 — the side WFG-259 actually ran, eleven
lines above its own addendum saying so. The scope clause now names what `:338` names
(「완충거리를 폭까지 훑어 정식 상대로 세운 쪽」), and the bullet's duplicated 「거기서 나온
수치는 이 README 에 옮겨 적지 않습니다」 loses the bare copy at `:341`, keeping the one that
carries the reason. **Graded before and after** on the judge-facing set (README, JUDGE_QA,
DEMO_SCRIPT_5MIN, finals/, web/finals.html, manuscript): one hit before, in README alone;
none after. `docs/auto/JUDGE_QA.md` deliberately untouched — its Q19 block uses a positional
pointer and is correct, which also kept this off NH-049's reprint requirement.

## WFG-262 — what I did, and the finding

`docs/routing_limitations.md` §1 is an audit of `fa_exceeds_budget`'s A4 sheet line, which
asserted a **cause** the classification condition does not establish. §1 was exemplary and
**was never run on the bucket defined one line above it in the same dict**. Run here, as §6.

**The repair (c, d, e).** `no_safe_route`'s condition is only 「the naive route enters the
hazard AND the future-aware search reached no refuge」. It printed 「예산 내 안전한 보행
경로가 없음(우회 포함)」, asserting both that a budget was consumed and that detours were
tried and exhausted. Neither is established — the ceil-rounded hazard gate closes every
alternative with the budget nowhere near binding, §1's mechanism unchanged. It now prints
「직행 경로는 화재를 지나고 안전한 우회 도달은 확인되지 않음」, the condition and no more.

**The premise (a, b) — and this is the lap's finding.** The row was filed on 「at least one
member of the bucket did neither」. Critic #70's four measured facts establish that the
pre-search guard exists, that its note is read nowhere, that the classifier cannot see the
note, and that the sheet asserts a cause. **None of the four is a count of the guard
firing**, and nobody had counted it. It is **ZERO on all three regions**, and structurally
so: every copy of `candidate_origins` skips a node with `hazard.prob_at(x, y, 0.0) >= p_cut`,
and `build_time_expanded_field` fills `table[:, 0]` with `prob_at_points` at the same time,
so at `departure_min = 0` the origin rule and the guard are **the same predicate**. Every
origin that could trigger it was removed before the scan began.

| region | committed `no_safe_route` | refused before any search |
|---|---|---|
| 영덕 2025 (canonical) | 2 | **0** |
| 의성·안동 2025 | 12 | **0** |
| 울진·삼척 2022 | 10 | **0** |

**Pre-registered both ways in the claim commit `c83e8d2`, before the numbers existed**, and
published as it came out. Identity controls gate every count: `n_nodes` and
`n_origins_scanned` re-derive exactly against each committed artifact or the script refuses
to exit 0, and for the two regions that committed their bucket membership every listed
member was tested individually rather than inferred. No scan re-run, no arm refit, no
committed count moved — 2 / 12 / 10 stand.

**What the zero does not buy, and what it changes.** The repair above needed no contaminated
member, so the wording changed anyway; what moved is the *reason* the bucket is clean. And
the protection is unnamed, duplicated three ways, and **thin**: 영덕's largest departure-time
probability over scanned origins is `nsr_max_departure_prob_yeongdeok`, clearing the cutoff
by under half a hundredth. So the guard's note becomes `ORIGIN_REFUSED_NOTE`,
`live/pipeline.py` reads it and gives such an origin its own sheet line inside the same
bucket, and `tests/test_no_safe_route_origin_split.py` ties the two predicates together.

**Root objection I recorded before building** (`hate`, in `c83e8d2`): the row hands me its
own answer, and the code said the answer was zero. The failure mode was confirmation —
finding zero, then relabelling the premise as the finding while shipping (c)(d)(e) as though
the audit had found what it went to find. The cheapest test was the identity controls first,
the count second, and a written commitment to publish zero.

**Evidence:** `scripts/measure_no_safe_route_origin_split.py`,
`data/processed/no_safe_route_origin_split/split_20260912T0026Z.json`,
`scripts/register_no_safe_route_split.py` (four additive `nsr_` keys),
`docs/routing_limitations.md` §6, `tests/test_no_safe_route_origin_split.py` (15 tests),
`docs/arm_protocol.json`.

## What did not work, and what the reviewer caught

**The Yeongdeok slope arm cannot be re-run in this sandbox** — it needs
`data/raw/firms_data/yeongdeok_2025_dem.tif`, which is git-ignored. I did not re-run any
scan. Instead the predicate is measured directly, which is sound *because* it reads node
coordinates and the hazard only, not edges. That is an argument, so the run states it as one
and pins it with the `n_nodes` identity control rather than assuming it.

**The independent reviewer's root objection was right and I acted on it.** It found the
repair was instance-scoped **again** — the exact failure §6 names as its own lesson — in two
more places:

- `live/pipeline.py:81`, `WALK_UNREACHABLE_FALLBACK = "예산 내 안전한 보행 경로 없음"`, 29
  lines above the dict I had just audited, and the line printed when an unreachable point
  carries **no** `reason_ko` — so it stands in for a cause nobody computed, which makes the
  budget claim worse there, not better. **Fixed in this lap** and pinned; only the constant
  is ever referenced, so no committed sheet re-renders.
- `scripts/generate_dispatch_outputs.py:102,134`, the same two-clause overclaim on the
  **vehicle-side 439 series**, on a booth printable. **Deliberately not repaired here**: it
  is a different arm whose unreachable condition no lap has read out of the code, and
  rewriting the sentence to match a guess is the trap this row pre-registered against. Its
  defaults are also byte-identity-pinned to committed sheets.
  <!-- forbidden-ok: 264, the id below is a BACKLOG ROW ID and not the gangneung_donghae_2022 Build-A positive count the gate anchors that value to. Same false-positive class as ba437f5, where the gate read a lap stamp as a camera count. No figure is asserted on that line. -->
  Filed as **WFG-264 (P1)**, below the P0 block.

**And my own cold read caught a third instance before the reviewer did**:
`docs/live_pipeline.md`'s mapping table — the page whose job is to show what the operator
sees — was showing `fa_exceeds_budget`'s **pre-§1** wording, stale since 2026-08-10, a month.
Both rows now carry the shipped strings, both superseded wordings are kept in a record table
below, and `tests/test_live_pipeline_doc_matches_code.py` parses the live table and binds
every cell to `BUCKET_TEXT` so the next one cannot drift silently.

Reviewed by: subagent (pass): the repair is instance-scoped again — the same overclaim still
ships at `pipeline.py:81` and on the vehicle-side printable, and the page that documents the
wording was not in the commit. All three are addressed above; the reviewer independently
re-ran the measurement and reproduced every field of the artifact exactly.

The reviewer also logged a `mandela` pattern-4 (tautology) hit worth repeating: the
measurement filters X out and then counts X, so it could never have returned non-zero. That
is true, it is the point, and it is disclosed in the claim commit, the artifact, the registry
caveat and §6 — the independent part is the identity controls and the per-member checks.

**Next row:** WFG-256 (P0, science) — the rotation null, the only thing that would license a
sentence about 「모양」.

## Gate table, and the report this supersedes

The certifying run is **ALL GREEN at `664ca6f`** — `make verify` PASS, 2202 passed, 65
skipped, 3 xfailed; `baseline-verify` is the known WARN (NH-029, CHARTER §3d) — with
`--assert-head` and `--assert-reported` both exiting 0 immediately before the push, and the
push landed at `664ca6f` with **no rebase**, so the finals screen's stamp is not orphaned
(the failure class of WFG-263 and run 386).

`docs/auto/reports/2026-09-12T0135Z-dev.md` is the same lap's same work and is **kept, not
deleted** (CHARTER §3.7), with a header saying what supersedes it. Its gate table read RED
<!-- forbidden-ok: 264, the value on the next line is check-forbidden's own anchor, quoted to explain a false positive it raised against this lap's new backlog row id. No figure is asserted. -->
at `09eea5b` because `check-forbidden` anchors the string `264` to the
gangneung_donghae_2022 Build-A positive count and this lap had just filed a backlog row
whose id ends in it — the false-positive class of `ba437f5`, where the gate read a lap stamp
as a camera count. A `forbidden-ok` pragma settled it. A stale table naming an earlier head
would not have been worth a second report; an **email header telling the author `gates: RED`
about a green branch** is.

## In plain terms

- **What changed for the project.** Your dispatch sheet used to tell a rescuer 「there is no
  safe walking route within the time budget, detours included」 for a household, when the
  program had only established something weaker and had not necessarily checked either of
  those things. It now says exactly what the program actually worked out, and nothing more.
  I also went looking for the case where the sheet would have been badly wrong — a house the
  fire has already reached, which the program refuses to plan for without saying so — and
  measured that it happens **zero times** in all three of your regions, for a reason built
  into how origins are chosen. None of your published numbers moved.
- **Why it matters to the judges or the science.** A judge who asks 「how do you know your
  output means what it says?」 now gets a page that answers it for two of your buckets
  instead of one, with the measurement behind it and a test that will catch it drifting
  back. The honest part is the part worth showing: the task I was given assumed the problem
  was already happening, I checked instead of agreeing, and I published the answer that made
  my own task less dramatic. Two more places in the project still carry the old
  overclaim — one I fixed, one I wrote down as a job rather than guessing at it, because it
  belongs to a different part of the system I have not read carefully enough yet.
- **What the author should do.** Nothing for this. But **26 decisions are waiting** and no
  reply has ever reached the loop by email — every answer so far came from a Claude Code
  session on your laptop. NH-046 is the one that matters most: it alone holds the ninth
  readiness tick **and** the whole parked P1 block, and it has been overdue since 09-10.
  The sprint ends **2026-09-15**. Opening the repository and typing `decisions` is the
  fastest path.



## Gates

**ALL GREEN** · mode `full` · head `664ca6f` · 2026-09-12T01:43:25Z · current at `664ca6f`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 23.4 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.1 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 0.4 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 379.1 s | 2202 passed, 65 skipped, 3 xfailed, 52 warnings in 377.90s (0:06:17) |

## Commits since the previous report (664ca6f17cf207c177572ea4a331f4e16a391351..HEAD)

(no new commits)

## Needs a human (31 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09, one day past; raised to HIGH by critic #55 on a measured third instance)
- NH-036 [DECISION] One critic lap told the next one not to edit a file, and that is what kept a false sentence in front of a judge for a window (by 2026-09-10)
- NH-037 [DECISION] The paper's word proxy now stops it a thousand words before your 25-page rule (by 2026-09-10)
- NH-038 [DECISION] Your "product first" rule has spent the last three dev laps on documents, and the readiness line it was written to protect has not moved in five critic laps (by 2026-09-09)
- NH-039 [DECISION] The national wildfire-spread system's manual is an 18 MB PDF the sandbox could not fetch, and one of you can (by 2026-09-12)
- NH-040 [FYI] A critic lap pushed one commit past a red `--assert-reported`, and it is telling you rather than hiding it
- NH-041 [FYI] This lap sent you an email containing only the word PLACEHOLDER, and could not take it back
- NH-042 [DECISION] Two of your own rules collide whenever a withdrawn claim lives in a frozen artifact, and this week they collided three times (by 2026-09-10)
- NH-043 [DECISION] A gate your loop built this morning will go red about twice a day, and the charter tells the lap that meets it to stop working (by 2026-09-09)
- NH-045 [BLOCKER] The staleness gate has closed `auto/dev` to every routine, and the one routine that met it is the one forbidden to clear it (by 2026-09-08)
- NH-044 [DECISION] The claim the paper just retracted is still live on the page the paper cites for it (by 2026-09-09)
- NH-046 [DECISION] Your product's definition-of-done names a command nothing in this project has ever run (by 2026-09-10)
- NH-048 [DECISION] One of the research routine's three literature channels has been dead for two runs, and a working replacement is already proven (by 2026-09-10)
- NH-049 [DECISION] Your critic routine is told to add judge Q&A cards and your own printing gate makes that impossible for it (by 2026-09-11)
- NH-050 [DECISION] You answered two of these questions two days ago and the loop never heard you, because you answered them on the routine page (by 2026-09-10)
- NH-051 [DECISION] The rule your loop has been obeying for three days is not the option it names, and the difference is why one row has been pushed down five times (by 2026-09-11)
- NH-052 [DECISION] The experiment that measures what your own forecast is worth is now one run away, and it would change what 42 means (by 2026-09-12)
- NH-053 [DECISION] The word your front door uses for 42 stopped being right today, and the loop cannot pick its replacement (by 2026-09-12)
- NH-054 [DECISION] Four fifths of your front door's headline is limits, and no lap is allowed to decide whether that is the project's strength or its biggest presentation risk (by 2026-09-13)
- NH-055 [DECISION] Your front door compares the headline forecast number to a model the same page calls broken, and today the loop measured a second, harsher comparison it is not allowed to put beside it (by 2026-09-13)
- NH-056 [DECISION] A Korean university published a reproducible deep-learning wildfire model four weeks before your finals, and a lap may not decide on its own whether to mention it (by 2026-09-13)
- NH-057 [DECISION] The one thing your project now says it contributes has never once been produced from a real fire, and only your laptop can change that (by 2026-09-13)
- NH-058 [DECISION] Two of your routines built the same row in the same window, and the rule that was supposed to stop that cannot see the paper routine (by 2026-09-13)
- NH-059 [DECISION] Your loop measured the hardest question a judge can ask you, and the answer is uncomfortable enough that no lap may decide on its own whether you say it out loud (by 2026-09-13)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
