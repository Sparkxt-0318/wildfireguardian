# WildfireGuardian autoloop · critic · 2026-09-09T2023Z

| | |
|---|---|
| when | 2026-09-09 20:23 UTC · 2026-09-10 05:23 KST |
| branch / head | `auto/dev` / `ba06467` |
| kind | critic |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 9, done: 33, dropped: 1, parked: 1, title-derivable: 1, todo: 108 |

## What happened this lap

Critic lap #53, reviewed head `ba06467`, window `375be25..ba06467` plus the full 24 h on `auto/dev`.
No code, no test, no data, no figure and no `docs/NUMBERS.json` entry was touched. Writes are confined to
`docs/auto/`.

## Your decisions, applied

**Nothing new arrived, on either channel.** I searched Gmail for
`from:siyeong0318@gmail.com subject:"WildfireGuardian autoloop" newer_than:14d` and got 201 threads, every one
of them a report this loop sent **from** your address (label `SENT`), each a single-message thread with no
reply on it. A second search restricted to replies (`subject:Re` or a body containing `NH-0`) returned an empty
result. `docs/auto/decisions_seen.json` is unchanged: its `applied` list still holds the 23 laptop-session
decisions and its `seen` list is still empty. PR #31 carries no comment in the `NH-###: …` form. **So there is
nothing to apply, nothing to quote and nothing unmappable this lap.**

⚠ What that means in practice, said once and not repeated: **25 NEEDS_HUMAN entries are open**, counted with
`decisions.py list` rather than by hand. NH-032, NH-034 and **NH-045, which is a BLOCKER**, are one day past
their stated 2026-09-08 date. NH-035, NH-038, NH-043 and NH-044 are due **today**. NH-036, NH-037, NH-042,
NH-046 and NH-050 come due **tomorrow**. Two of them are load-bearing on work the loop is doing right now:
**NH-037** is the only thing standing between WFG-214 and `done`, and **NH-046** is the single point of failure
under a readiness count that has not moved for ten critic laps.

## Findings, ranked

### F1 (root objection, new row WFG-220, P0) — the one surface five judges actually look at is the only judge-facing surface carrying no word of the objection this repository spent the whole day correcting everywhere else

`web/finals.html` ACT 3 tells the judge, in as many words, that 「모든 출발지에서 화재 무시 직행 경로와 시간 인지
경로를 비교해 네 가지로 분류합니다. **이 판정이 화면의 점 하나하나입니다**」. Every dot on the screen is a
verdict of that comparison. `docs/oracle_gap.md` §2 establishes that those verdicts are graded on `haz_stack`,
the very array the time-aware arm planned on, while `obs_stack` sits in the same npz and scores nothing.

I opened the artifact rather than reading that off the prose. `data/processed/routing_demo_canonical.npz` holds
`haz_stack` float32 `(5, 181, 156)` and `obs_stack` uint8 `(6, 181, 156)` on one grid, and a scan of `scripts/`
and `src/` finds **no grader that reads `obs_stack`**: only `scripts/measure_oracle_gap.py`, which exists to
measure the gap, and `scripts/make_routing_figures.py`, which draws it as a contour.

Counts on the built screen at `ba06467`, and on its template so this cannot be dismissed as a stale build:

| token | `web/finals.html` | `scripts/finals.template.html` |
|---|---:|---:|
| 채점 | **0** | **0** |
| 오라클 | **0** | **0** |
| `oracle_gap` | **0** | **0** |
| `haz_stack` / `obs_stack` | **0** / **0** | **0** / **0** |
| 정답을 미리 / 정답이 아니 | **0** / **0** | **0** / **0** |
| 「LOFO」 | **1** | **1** |

⚠ **The sharpest form of it:** that single 「LOFO」 is the evidence card 「확산 모델 · LOFO 교차검증」, which uses
the leave-one-fire-out design to show the model is **good** (AUC 0.89 ± 0.107). Nothing on the screen tells the
judge that the routing comparison the whole demo is built on is scored against that same leave-one-fire-out
output rather than against the observation committed beside it. Meanwhile the screen has a dedicated 「알려진
한계」 panel with **ten** `rel()` cards, spent on walking-network coverage, the ERA5 publication lag, OSM 정자
tagging in the refuge layer and the dispatch-ordering negative result. Every one of those is a smaller caveat.

**Read the difference from WFG-214 exactly.** Those five surfaces said the **wrong** thing and were corrected
today. The screen never said the wrong thing; it says **nothing**. That is an omission, not a contradiction,
which is why the scorecard restores 제출 자료 this lap rather than docking it again — and it is an omission on
the surface with by far the longest exposure: five judges, about ten minutes each, offline, with no README in
front of them.

Filed as **WFG-220** (P0, KCF) at the end of the P0 block in table order, not at position 1 (NH-038 option B as
`NEEDS_HUMAN.md` words it, and NH-051). It inherits WFG-214's four constraints verbatim: it may not settle
「상한」 (NH-053), no margin value reaches the screen (NH-032, NH-034, NH-052), no committed number moves, and
the screen may only be stamped with a commit already on `origin`. It shares `make finals` and a re-pointed
`release/kcf-finals-2026/MANIFEST.json` with **WFG-218**, so a lap taking both pays that mechanic once.

**Cheapest test, ten seconds, already run:** the seven counts above, on the built file and on the template.

### F2 — yesterday's root objection is corrected, and the correction makes the loop freer rather than more careful

Critic #52 found that `build_timeline_roles.py --check` is decided by the clone's object count, and
`DIRECTION.md` grew a guardrail from it forbidding any rebuild of
`data/processed/timeline_roles/timeline_roles.json` because 「the rebuild writes eight-character anchors and
turns GitHub red」.

**The mechanism is right and the blast radius was wrong.** I did not deepen this clone. I cloned the repository
fresh into the session scratchpad, checked out `auto/dev` at `ba06467`, and measured there:

| clone state | commits | packed objects | `%h` | `--check` |
|---|---:|---:|---:|---|
| this sandbox, as it opened (shallow at 51) | 51 | 12,975 | - | exit **2**, `shallow` (test skips) |
| **fresh full clone, default single-branch fetch** | **673** | **12,393** | **7** | exit **0** |
| **same clone, all 13 origin branches fetched** | **673** | **12,393** | **7** | exit **0** |
| critic #52's clone, `--unshallow`ed from shallow | 666 | 20,641 | 8 | exit 1, `STALE` |
| critic #52's clone, after `git gc --prune=now` | 666 | 12,052 | 7 | exit 0 |

In the fresh clone `--check` printed `OK — timeline artifact agrees with git log (673 commits, 42 active days)`.
So the red state is **not** produced by a full clone and **not** produced by extra branches. It is produced by
`git fetch --unshallow` on an already-shallow clone, which leaves a bloated object store; `git gc --prune=now`
clears it. **Blast radius, corrected: an agent lap that deepens its own sandbox clone.** Not a stranger cloning
the repository, not the student's laptop, not GitHub at `fetch-depth: 0`, and **not `TIMELINE_ROLES.md` §5's
reproduce recipe, which I ran end to end in the fresh clone and which passes.**

**WFG-217** is updated in place rather than duplicated: it stays P1 and stays the right fix (`--abbrev=7`, or
resolve anchors with `%H` and compare on a prefix), but it blocks nothing judge-facing and needs no urgency.
`DIRECTION.md`'s guardrail now names the discriminator and the one-command cure. **The transferable lesson, and
it is cheap: when a lap needs full history, clone fresh into the scratchpad instead of unshallowing the working
clone.** It is faster, it leaves this clone's gates alone, and it is the clone shape the student and the
stranger actually have.

### F3 (this lap's one §3b reorder) — the top row of the table was a row the next dev lap could claim and not move

**WFG-214 is `todo` at table position 1 and its only remaining work is blocked in another routine's file.**
Three of its four surfaces closed at the 1817Z lap: `README.md:36`, `:266-284` and `:715-723` all state the
mechanism and all three link `docs/oracle_gap.md`. What is left is one link in `paper/manuscript.md`, and
`paper/check_paper.py` reads **8,999 body words against a 9,000 hard fail** (re-run here at this head, exit 0),
so the link costs a caveat, which CHARTER §3 rule 5 forbids. That is **NH-037**, open and due tomorrow, and
`paper/` belongs to the paper routine under CHARTER §12.

Critic #52's reason for putting WFG-214 above WFG-212 was that WFG-214 rewrites `README.md:266-267`, which sits
inside WFG-212's block, so the mechanism had to be right before a lead paragraph was written over it. **That
reason is discharged.** So the reorder puts **WFG-212 back above WFG-214**. Both are P0, so no P0 row falls
below a non-P0 row.

### F4 — the `Do NOT edit` note's own line numbers had gone stale, which is evidence for NH-036 arriving through a different door

The note has covered 「`README.md:210-282`」 through critics #49, #50, #51 and #52. **At this head that block is
`README.md:220-299`**: WFG-214 added ten lines inside it, so `## Round 4` moved 200 → 210, 「### 1.」 210 → 220
and 「### 2.」 283 → 300. Re-measured here rather than inherited. A note written as absolute line numbers stops
naming what it means the first time anyone edits the file — which is the failure mode NH-036 option A was
written to prevent, arriving through width rather than through staleness. Recorded in `CRITIC_LATEST.md` and
`DIRECTION.md` with corrected bounds; **not** filed as a fourteenth question, because NH-036 is already open and
comes due tomorrow. The note is re-stated after re-reading its premise: NH-032 and NH-034 are both still `open`,
both one day past, and a scan of 220-299 finds **zero** occurrences of the five margin values.

### F5 — recorded so the next lap does not re-find it and think it is new, and deliberately NOT filed

`docs/auto/finals/TIMELINE_ROLES.md` is **not** among the printed kit's seven `SOURCES`, while
`docs/auto/JUDGE_QA.md:1414` and `docs/auto/DEMO_SCRIPT_5MIN.md:287` both send the reader to it. It is reachable
on the judged laptop, which `docs/auto/finals/BOOTH_SETUP.md` puts in front of the student, and `DIRECTION.md`
says in as many words not to widen the freeze with new payload. So it is an observation, not a row.

## Verification, and no finding #1

`gates.py --mode full` exits **0** at `ba06467`: 1862 passed, 64 skipped, 3 xfailed, pytest 383.2 s.
`baseline-verify` WARNs on the two git-ignored `data/raw/**` contracts, which is NH-029 and CHARTER §3d working
as you chose. `--assert-head` exits 0.

**GitHub `auto-gates`, runs 279 to 299: no `failure` anywhere in the window.** Two runs are `cancelled` (278,
284), each superseded by the next push and both predating this window's dev laps; run **299** is `success` at
this exact head. **CHARTER §4b sets no finding #1**, for the second consecutive lap. Every dev report in the
window records `Reviewed by:` (0950Z block, 1250Z block, 1619Z block, 1928Z pass), and every push in the window
carried a report.

`factchk`: the window's added prose contains **no new external URL and no new citation**. Every claim in it is
about this repository's own git history or its own committed arrays. I re-derived the two load-bearing ones
independently in the fresh clone: the npz key shapes above, and **673 commits / 524 `Co-Authored-By: Claude`
trailers / 42 active days** at `ba06467` against the artifact's **662 / 513 / 42** at `89da7d3` — which is the
as-of stamp the 1817Z lap added, working exactly as intended.

`prism`, five lenses, on the 24 h diff, the README, the MODEL_CARD and the finals narrative: the
software-engineering judge and the disaster-response judge both land on F1 (the screen), the ML reviewer lands
on the same place from the leakage side and finds `docs/oracle_gap.md` §4b has already disarmed the one
self-confirming replication in it, the fire-behaviour scientist finds the place/size decomposition (IoU 0.394,
areas within 2 %) stated correctly and its slice-selection caveat stated beside it, and the statistician finds
the post-hoc-maximum qualifier intact on all six surfaces. `mandela` on the oracle measurement returns nothing
the document has not already written down itself.

## Scorecard

**Track B 93 → 94. Track A 92 → 93. One row moves on each track and it is the same row: 제출 자료, back up.**

Critic #51 docked it with a measurement: five judge-facing surfaces said 42 is what a noiseless forecast would
buy, `docs/oracle_gap.md` said the oracle is in the grader, and the right answer was unreachable from any of
them. Re-measured at this head, **all five surfaces state the mechanism and four of the five link the file**;
the fifth is the manuscript, whose mechanism paper lap 24 corrected and which lacks only the link, at 8,999 of
9,000 words. The deduction's own premise is gone, so the point comes back. B 18 → 19, A 16 → 17.

**Why not 20 and 18, both halves pre-registered for critic #54:** 「자료의 논리적 구성」 on the same front door
is still WFG-212, and the finals screen is still silent, which is WFG-220. Either landing moves the row again.

**Everything else holds, and two holds are deliberate refusals rather than defaults.** 설계와 방법론 holds at
19 on both tracks because critic #52 pre-registered its raise on **two** conditions and exactly one is paid: the
boundary overclaim is closed and closed well, and 일정 is still 0 on `README.md` and 0 on `web/finals.html`
(WFG-218). Track A 개발 목적 holds at 19 because critic #51 wrote 「it moves to 20 when WFG-214 lands」 and
**WFG-214 has not landed** — it is `todo` with residue. A ceiling score on a 20-point row for a row that is not
done, on a project whose front door still has WFG-212 open, is not something I could defend at a booth.

## Root objection, in one sentence

**The repository spent its best day of the sprint making four judge-facing surfaces tell the truth about its
headline number, and the one surface five judges will actually stand in front of was not one of them.**

## In plain terms

- **What the judges would see today.** Your front page is now honest about the one thing that most threatened
  it: it explains, in Korean and English, that the 42 households come from a comparison scored on the same
  forecast the route was planned with, and it links the file that measures how far that forecast sits from what
  actually burned. That is the best correction this project has made. But the **booth screen** — the thing five
  judges look at for ten minutes each, offline, with no README in front of them — says nothing about it at all,
  and the one place it mentions the leave-one-fire-out design is the card that uses it to show the model is
  good. I filed that as the top new job.
- **What changed since yesterday.** Three README lines fixed, the schedule document's two false sentences
  withdrawn and recorded, and the booth script now tells you to answer 「제가 정했습니다」 when a judge asks who
  chose the phases. I also re-checked yesterday's alarm about the schedule script and it was **overstated**: I
  cloned your repository fresh and the script passes, so a stranger or you on your laptop can re-run it and get
  673 commits and 42 active days. Score: 93 → 94 out of 100 on the research track, 92 → 93 on the tool track.
- **What you should do.** One reply. Twenty-five questions are open; three are already past their date and one
  of those is a BLOCKER, four more came due today, and five come due tomorrow. Two of them are holding up work
  right now: **NH-037** is the only thing between the oracle fix and 「finished」, and **NH-046** is the single
  reason your readiness checklist has not moved for ten laps. You can answer them with one line each.



## Gates

**ALL GREEN** · mode `full` · head `ba06467` · 2026-09-09T20:06:22Z · current at `ba06467`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 21.3 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.2 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 2.1 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 383.2 s | 1862 passed, 64 skipped, 3 xfailed, 52 warnings in 381.58s (0:06:21) |

## Commits since the previous report (3ec5737da24832e70e7f7e180e3243b667cb5b59..HEAD)

- ba06467 the report's own open question was already asked, and it is NH-050
- a45d3d4 the dev report names the head it was pushed on, and keeps the run it replaces
- dbb5350 the reviewer's own exploit is now a red test, and the printed kit carries the corrections

## Needs a human (25 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09)
- NH-036 [DECISION] One critic lap told the next one not to edit a file, and that is what kept a false sentence in front of a judge for a window (by 2026-09-10)
- NH-037 [DECISION] The paper's word proxy now stops it a thousand words before your 25-page rule (by 2026-09-10)
- NH-038 [DECISION] Your "product first" rule has spent the last three dev laps on documents, and the readiness line it was written to protect has not moved in five critic laps (by 2026-09-09)
- NH-039 [DECISION] The national wildfire-spread system's manual is an 18 MB PDF the sandbox could not fetch, and one of you can (by 2026-09-12)
- NH-040 [FYI] A critic lap pushed one commit past a red `--assert-reported`, and it is telling you rather than hiding it
- NH-041 [FYI] This lap sent you an email containing only the word PLACEHOLDER, and could not take it back
- NH-042 [DECISION] Two of your own rules collide whenever a withdrawn claim lives in a frozen artifact, and this week they collided three times (by 2026-09-10)
- NH-043 [DECISION] A gate your loop built this morning will go red about twice a day, and the charter tells the lap that meets it to stop working (by 2026-09-09)
- NH-045 [BLOCKER] The staleness gate has closed `auto/dev` to every routine, and the one routine that met it is the one forbidden to clear it (by 2026-09-08)
- NH-044 [DECISION] The claim the paper just retracted is still live on the page the paper cites for it (by 2026-09-09)
- NH-046 [DECISION] Your product's definition-of-done names a command nothing in this project has ever run (by 2026-09-10)
- NH-048 [DECISION] One of the research routine's three literature channels has been dead for two runs, and a working replacement is already proven (by 2026-09-10)
- NH-049 [DECISION] Your critic routine is told to add judge Q&A cards and your own printing gate makes that impossible for it (by 2026-09-11)
- NH-050 [DECISION] You answered two of these questions two days ago and the loop never heard you, because you answered them on the routine page (by 2026-09-10)
- NH-051 [DECISION] The rule your loop has been obeying for three days is not the option it names, and the difference is why one row has been pushed down five times (by 2026-09-11)
- NH-052 [DECISION] The experiment that measures what your own forecast is worth is now one run away, and it would change what 42 means (by 2026-09-12)
- NH-053 [DECISION] The word your front door uses for 42 stopped being right today, and the loop cannot pick its replacement (by 2026-09-12)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
