# WildfireGuardian autoloop · critic · 2026-09-04T1205Z

| | |
|---|---|
| when | 2026-09-04 12:05 UTC · 2026-09-04 21:05 KST |
| branch / head | `auto/dev` / `3a70e16` |
| kind | critic |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 7, done: 12, parked: 1, todo: 42 |

## What happened this lap

Critic lap #10. Window `ce31b91..3a70e16` on `auto/dev` — one dev lap (WFG-069) and
critic #9's own report commit. **No code, no test, no artifact and no number was changed
by this lap.** Writes: `docs/auto/` only.

## Your decisions, applied

**Nothing new, for a seventh lap.** Searched the mailbox with the query CHARTER §6
specifies (`from:siyeong0318@gmail.com subject:"WildfireGuardian autoloop" newer_than:14d`):
**29 threads**, every one holding exactly **one** message, the loop's own report. Confirmed
by fetching the newest thread and the 0255Z thread — the one whose 「Decisions needed」 block
listed eleven items — in full, rather than trusting the search preview. **PR #31 has zero
comments.** `docs/auto/decisions_seen.json` still does not exist, so `decisions.py apply`
has never recorded anything and the decision channel is untested in both directions for a
fifth day. Nothing to parse, nothing to apply, nothing quoted here because nothing was said.

Sixteen entries are open; **NH-016's date was yesterday**, NH-017 and NH-021 are due 09-06,
NH-018 and NH-019 09-08. This lap added **no** new NEEDS_HUMAN entry on purpose: both
findings need work and no decision, and a seventeenth unanswered question would make
NH-020's own finding worse by the hand of the lap reporting it. NH-020 carries a dated note
with today's counts instead.

## Verifying the loop's claims

`gates.py --mode full` exits **0** at `3a70e16` in a fresh cloud sandbox: `1342 passed,
62 skipped` in 152 s, **COLD**. Against critic #9's cold `1312 / 62` at `ce31b91` that is
**+30 passed, skips unchanged**, like for like, fourth comparable window. `verify`,
`snapshot-verify`, `env-check` PASS; `baseline-verify` WARN, expected off-laptop,
`hard: false`, tenth window and still not a finding. `--assert-head` and `--assert-reported`
both exit 0 at HEAD. **Green at HEAD for a seventh consecutive critic lap.** Every dev
report in the 24-hour window carries a `Reviewed by:` line except
`docs/auto/reports/2026-09-04T0401Z-dev.md`, which is critic #7's F40 and unchanged.

**One reported claim did not survive.** Critic #9 certified its window with 「grepped every
`.md` and `.html` in the tree this lap: the only surviving affirmative-primacy strings are
that dated withdrawal block and §10's own paragraph」. That is false, and it is finding #1.

## Findings, ranked

**F52 · WFG-070 (P0, new) — the withdrawn claim is alive, in English, in the drill brief.**
`docs/auto/research/RESEARCH_BRIEF_2026-09-03.md:75`, under the heading 「the ten hardest
judge questions, with the answers that **survive the verdicts**」, answers question 7 with
「a satellite trigger would have fired +22/+34/+64 min **after the human report** … the design
is **report-first, satellite-confirm**」. `docs/auto/research/sweeps_2026-09-03/R3_science_gaps.md:22`
says the same. That is the claim WFG-053 and WFG-063 spent two laps withdrawing from five
surfaces, and neither file carries the dated annotation `docs/SESSION19_REPORT.md` was given.
`docs/auto/ROUTINE_PROMPTS.md` tells this routine to read that file's section (c) on **every**
lap, so it is live drill material, not an archived record. Why no gate saw it: **every token
in both claim families of `tests/test_detection_ordering_is_not_claimed.py` is Korean**, and
neither `PRIMACY_GUARDED` (5 files) nor the new `GUARDED` (7 files) contains `README.md` or
anything under `docs/auto/research/`; `paper/manuscript.md` is in the second list and not the
first, so the manuscript is guarded for a figure's labels and unguarded for the claim. Same
file, question 10, cites `docs/auto/AI_DISCLOSURE.md`, retired to `docs/auto/archive/` on
2026-09-04. **First `fix-before-next-row` item.**

**F53 · WFG-071 (P1, new) — the third claim gate was built beside the registry that already
holds its answer.** This is the root objection; see below.

**F54 — a critic report says three times that it changed a priority, and it did not.**
`docs/auto/reports/2026-09-04T1005Z-critic.md:33, 134, 148` all say WFG-062 was raised
P1 → P0, including in the section headed 「Updated, not duplicated」. `docs/auto/BACKLOG.md:95`
still reads `| WFG-062 | P1 |`, and `git show adf712d -- docs/auto/BACKLOG.md` shows that lap
edited the row's text and not its priority column; its own `CRITIC_LATEST.md` says 「the table
order stands and this lap does not reorder it」. It matters mechanically: CHARTER §4 step 3
sends the next dev lap to the table, not to the report. **Critic #10 leaves WFG-062 at P1
deliberately** — NH-021 is the author's open decision and its stated default is table order.
The record is corrected at the foot of `BACKLOG.md`; no priority was changed by this lap.

**F55 — the measurement the routine asks for, on the gate that shipped.** The 1045Z lap
graded its **first draft** against a reviewer's mutation set (12 / 20, published in the
docstring — good), then folded six of the eight escapes in as parametrised cases, which
converts external ground truth into internal ground truth. So the **shipped** version was
ungraded by anyone outside it. Twenty fresh blocks written this lap, none of them in
`INCOMPLETE_BLOCKS`: **14 / 20**, against 2 / 20 for the previous family. That is a large,
real improvement and should be read as one. Six escapes: **`99％`** (U+FF05 fullwidth, which
a Korean IME produces by default, and which the class `(?:%|퍼센트)` does not accept — one
character, and the second `fix-before-next-row` item); the figure restated as a count
(「100건 가운데 99건」); 목격 paraphrased (「눈으로 본 사람의 신고」); and three restatements of
the camera figure with no digit beside 최초. Only the first is cheap; the rest are the class
the docstring already refuses to claim, which is why WFG-071 asks for a registry and not a
seventh regex.

**F56 — carried, verified unchanged.** WFG-051 P0 **sixth window**, now in four documents
with three attributions (registry 중앙재난안전대책본부 · README 「경상북도 최종 집계·중대본
확인」 · `data_sources.md` 경상북도 재난안전대책본부 · `paper/manuscript.md:37` 「the
**provincial** disaster headquarters' count」) — the booth is not exposed, because Q30b names
the disagreement and forbids asserting one. WFG-067 P0 **third window**: `git cat-file -t
a562045` still `fatal: Not a valid object name`, `web/finals.html` still `"git":"a562045"`.
WFG-057 P0 **fifth window**: header 33 / 14 / 13 / 6, file **41 / 15 / 19 / 7** — and it is
now blocking this routine's own step 3 for a second lap, because a 42nd question under a
header that says 33 makes the defect worse by the hand of the lap reporting it. WFG-065
**fourth window**. WFG-056 **third window**: `--assert-reported` run against six bases in the
window, all six exit 0 naming one report, including `--base adf712d`, the commit that carried
critic #9's own report.

## The window's own headline, which is good

WFG-069 is done and it was done well. `docs/detection_floor.md` §0 no longer asserts
「한국의 산불 탐지는 사실상 전부 사람입니다」 on a statistic §10 of the same file forbids in
bold. It opens on a **count** — 경북 산불감시카메라 152대, 대당 약 6천만 원, **최초 발견
0건**, over **2022년 한 해와 2023년 4월 28일까지** — with agency, article date and URL in the
same paragraph, and says what the measurement supports: what comes in the satellite's place
「이 저장소가 재지 않았습니다」. **`factchk`: this lap opened
<https://www.khan.co.kr/article/202304281446001> rather than trusting the citation, and every
element holds** — 「경북에는 152개의 산불감시카메라가 설치돼 있다 … 대당 6000만원 정도다」,
「감시카메라로 산불을 먼저 발견한 건수는 '0'건 이었다」, and the 99 % figure carrying the
article's own 「올해」, which is the interim scope §0 now prints beside it. Nothing was
overstated. That is the strongest 출처 명기 work yet done in a judge-facing Korean document.

And `tests/test_external_figures_carry_their_scope.py` contains
`test_the_escapes_this_gate_cannot_close_are_still_open`, which **fails when the gate starts
catching something its own docstring says it cannot**: executable documentation of a limit,
and the first instrument here whose job is to keep its author honest rather than its documents
consistent. The lap also wrote its own refuted argument into the file — 「That argument is
false and the reviewer measured it」. Both are credited in words in the scorecard, and neither
is credited in a point, for the reason below.

## The root objection, and its cheapest test

**The loop keeps building the gate beside the registry instead of on it.** Three hand-rolled
string families now guard prose. The newest asks every guarded block to carry a figure's
**agency**, **as-of date** and **scope** — the exact three fields `docs/NUMBERS.json` already
stores, and stores for **all sixteen** of its `provenance: external` keys. It reads none of
them; it hand-writes a second registry holding **two** figures that are in no registry at all.
Five of the sixteen are printed today inside files the gate itself lists as `GUARDED`
(`JUDGE_QA.md` 99,289 · 26명; `web/finals.html` 3,819 · 3,587; `paper/manuscript.md` 99,289 ·
3,819 · 3,587 · 2,246 · 104,788), where it does not look — and one of those five is
**WFG-051**. A gate that compared a printed value against its own key's `agency` field would
have failed on `paper/manuscript.md:37` the day it was written, and would have been
**language-neutral**, which is exactly what F52 cost.

**Cheapest test, one function inside one lap:** iterate `docs/NUMBERS.json` for
`provenance == "external"`; for each guarded file and each block printing that key's value in
the spelling the document uses, require a token from that key's own `agency` / `as_of` /
`scope` in the same block, or a `scope-ok:` pragma naming the key. **Print the number of
failing blocks on the current tree before fixing any of them.** If WFG-051's manuscript line
is among them, the instrument is right and both hand-rolled families should migrate onto it.
If the output is mostly noise, that is the more valuable answer and WFG-030's shape — every
judge-facing claim sentence cites a registry key or an artifact — is the right instrument.

**It is not this lap's call which comes first.** WFG-071 is one lap and the rows it displaces
are the booth ones. That trade is **NH-021**, unanswered. Table order stands; no priority was
changed.

## Scorecard deltas

**Every row held, both tracks: Track B 82 / 100, Track A 73 / 100 at `3a70e16`.**

Track B 제출 자료 is the row this lap argued itself out of raising. §0's rewrite is a clear
+1 on 「사용된 자료에 대한 출처 명기」, verified against the primary source. It is spent on
「자료의 논리적 구성」 by F52: the previous window certified the tree clean of a claim that is
alive in English in the file this routine reads every lap. Two of three sub-criteria moved in
opposite directions, so the row holds at 17. It does not reach 18 on WFG-057 (fifth window),
WFG-051 (sixth) and WFG-065 (fourth). 창의성 15 held and it was the closest call: the
self-falsifying test is real novelty, but it is novelty in the loop's **process**, not in the
작품 a judge scores, and the same window's evidence is that the process produced its **third**
parallel string family rather than the general one. Track A held on every row: nothing touched
`web/` for an **eleventh** window, R4 / R7 / R9 are still ☐, three of eleven readiness lines
are ticked, and WFG-067's dead commit is unchanged in its third window.

## Filed

New rows **WFG-070** (P0) and **WFG-071** (P1) with details; a correction to the record about
WFG-062's priority; `NEEDS_HUMAN.md` NH-020 updated with today's counts; `KCF_READINESS.md`
R2 and R3 annotated with this lap's independent verification; `SCORECARD.md` appended on all
three tables. **No question was added to `docs/auto/JUDGE_QA.md`**, for the reason under
WFG-057. **No NEEDS_HUMAN entry was added**, for the reason under NH-020. No code, test,
artifact, figure or number was touched.

## In plain terms

**What the judges would see today.** A project whose design document now opens with a fact
instead of an opinion: 152 fire-watch cameras in 경북, zero fires spotted by them first, over
two years, with the newspaper, the date and the link right there. I opened that article
myself and every word of it checks out. That is the best-sourced sentence in the whole
repository. What they would still catch in thirty seconds: your Q&A card says it holds 33
questions and it holds 41, and the number of people who died in the fire is attributed to
three different agencies in four different files.

**What changed since yesterday.** One thing got fixed and one thing got found. Fixed: the
first sentence of the detection document, which used to claim something the same document
forbade. Found: the old withdrawn claim — 「the satellite would have been slower than the
phone call」 — is still sitting in your research brief, in English, under a heading that says
these are the answers that survived. Your safety checks are all written in Korean, so they
never looked at it.

**What you should do.** Reply to this email with one line. Any line. Twenty-nine reports have
gone out and not one has come back, and four decisions are past their date — the cheapest is
`NH-016: move it`, which fixes the routines colliding with each other. If you only have time
for one thing, that is the one.



## Gates

**ALL GREEN** · mode `full` · head `3a70e16` · 2026-09-04T11:51:57Z · current at `3a70e16`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 16.3 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.1 s | make: *** [Makefile:152: baseline-verify] Error 1 |
| snapshot-verify | PASS | 1.7 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 152.0 s | 1342 passed, 62 skipped, 52 warnings in 150.64s (0:02:30) |

## Commits since the previous report (613ff3c..HEAD)

- 3a70e16 the report named a commit that does not exist, which is WFG-067 by this lap's own hand

## Needs a human (16 open)

- NH-001 [DECISION] Email delivery needs three repository secrets
- NH-002 [DECISION] Optional: `@claude` on GitHub issues and PRs
- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-010 [DECISION] Expert consultations and the firefighter record (by 2026-09-20)
- NH-011 [DECISION] One real, recorded email send from a network that works in Shanghai (by 2026-09-20)
- NH-012 [DECISION] Portal downloads the loop cannot do (by 2026-10-01)
- NH-013 [FYI] Optional: a stable web address for the visual board
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-016 [DECISION] The critic routine has 30 minutes to land its findings, and needs about 40 (by 2026-09-05)
- NH-017 [DECISION] Three entries were closed on replies the repository cannot see, and one of them was wrong (by 2026-09-06)
- NH-018 [DECISION] Two laps read the same primary sources and disagree on two sentences (by 2026-09-08)
- NH-019 [DECISION] One report time, for one fire, decides what the detection result is allowed to say (by 2026-09-08)
- NH-020 [DECISION] Twenty-five report emails, no reply, and the decision channel has never once been exercised (by 2026-09-06)
- NH-021 [DECISION] Two laps disagree on whether to spend a sprint lap on a gate or on the booth (by 2026-09-06)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
