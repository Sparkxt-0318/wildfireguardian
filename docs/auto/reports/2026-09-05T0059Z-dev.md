# WildfireGuardian autoloop · dev · 2026-09-05T0059Z

| | |
|---|---|
| when | 2026-09-05 00:59 UTC · 2026-09-05 09:59 KST |
| branch / head | `auto/dev` / `e5ab329` |
| kind | dev |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 7, done: 15, parked: 1, todo: 50 |

## What happened this lap

## Your decisions, applied

**Nothing new this lap.** The Gmail search `from:siyeong0318@gmail.com subject:"WildfireGuardian autoloop" newer_than:14d` returned 40 threads, every one of them a single `SENT` message — the loop's own reports, no reply. PR #31 carries no comments at all. No `NH-###:` line reached the loop through either channel, so no entry was closed and no `decisions_seen.json` write was needed.

---

## What this lap did

Two rows: critic #14's one `fix-before-next-row` item first, then the row DIRECTION.md and table order both named.

### WFG-087 (critic #14's item) — a T0 booth answer that taught a wall the tree had taken down

`docs/auto/JUDGE_QA.md` Q18 is a **T0** question, one of the fifteen the student must be able to answer cold, and it is the one a disaster-response judge is most likely to ask: 「그중 지정 대피소가 있습니까?」 Its 「없는 것」 paragraph explained the missing comparison with 「포털 다운로드가 로그인·CAPTCHA로 막혀 있고 NH-012에 학생 작업으로 올라가 있습니다」. Both clauses were stale at HEAD, and the tree contradicted them in three places.

Rewritten to four numbered lines the student can say at a booth:

1. The list **is** in the repository. The subset was re-cut on 시군구 **47770** and verified from the data itself (`docs/juso_yeongdeok.md`, NH-022 closed): 지진옥외대피장소 **64**, 지진해일긴급대피장소 **92**, 무더위쉼터 **17** — keys `juso_yeongdeok_samul_eqout_point_count`, `..._eqwav_point_count`, `..._coolingcen_point_count`, all carrying `scope_status: corrected`, all added to Q18's 키 line.
2. **None of them is a designated *wildfire* shelter.** The categories in hand are earthquake, tsunami and heat, so the honest answer is not 「목록이 없다」 but 「목록은 있는데 산불 범주가 아니다」.
3. The comparison itself has not been run; **WFG-073** is the row that would, and it is cloud-doable.
4. The national shelter file is **deferred post-finals by the author** (NH-012 closed), so it is not pending student work.

The withdrawn sentence is quoted above the new text, marked withdrawn, with 「부스에서 이 문장을 말하지 마십시오」 — the record stays (CHARTER §3.7) where the student reads it.

**The row's second half needed nothing.** It asked that NH-012's WFG-075 amendment be corrected. Checked at HEAD: critic #14 had already appended a **SECOND AMENDMENT** doing exactly that, correctly, as an appended record rather than an edit. Re-correcting it would have been a second correction of a corrected record. Said so rather than manufacturing a diff.

### WFG-003 — the five minutes a judge actually gets

`docs/auto/DEMO_SCRIPT_5MIN.md`, Korean, DRAFT-labelled per CHARTER §9 (the student rewrites it in their own voice; the 심사기준 explicitly disfavours 암기 발표):

- **§0** — the 20 seconds between judges. Five judges each visit the booth, so this script runs **five times in a day**: `G`, language `KO`, region back to 의성·안동.
- **§1** — six timed segments summing to exactly **300 s**: 도입 25 / 1막 발견 45 / 2막 시간이 도로망을 바꿉니다 55 / 3막 같은 출발지 두 개의 답 **75** / 4막 판단 55 / 마무리 한계 45. Each segment marks a **[버림]** sentence — the one to drop when the clock runs over, decided in advance rather than in front of a judge.
- **§2** — one interruption sentence for each of the five judge lenses (software professor, disaster-response official, fire scientist, ML reviewer, statistician), each pointing at the JUDGE_QA question that answers it in depth.
- **§3** — the mapping table: **35 rows** (23 화면 / 12 구두), each with a registry key or a named artifact, each marked **화면** (a card in `scripts/finals.template.html` actually renders it) or **구두** (spoken only, but still sourced). The 구두 rows are the honest half: the fire-blind-vs-time-aware origin counts (368/91, 458/42), the two time-aware walk-time costs, the committed-window 0-of-180, and the ~25 s pipeline latency are all real and sourced but are **not drawn on the screen**, so the script never tells the student to point at them.
- **§4** — seven sentences the script must never say, including the detection-ordering claim this repository withdrew and 「20가구를 구했다」.
- **§5** — what the document does not do, first line being the rehearsal it does not replace.

**The script keeps the result that loses.** Act 4 has the student read the screen's own card — 「이길 수 있는 조건은 존재하지 않는 것으로 판정했습니다 · 전체 셀의 **3.6 %** 에서만 우세 · 최초 승리 창 **W ≥ 120분**」 — and then add the sharper figure the screen does not print: at the committed window it wins **0 of 180 cells** (`dispatch_order_deadline_wins_at_committed_window`, whose registry caveat opens `⚠ THE SHIPPED ORDERING LOSES`). The document tells the student not to cut that paragraph, and pins the order so the two figures land as a range and a point inside it.

### The audit half: no screen change was warranted, and that is the finding

Critic #14's falsifiable test was 「if the next dev lap produces `DEMO_SCRIPT_5MIN.md` **and a `web/` commit**」. The script exists; the `web/` commit does not, and the audit is why:

- **40 of the 41 keys `build_finals.py` declares in `REGISTRY_KEYS` are present in the built `web/finals.html`.** The one absent, `mr_yeongdeok_walk_time_increase_pct`, has never existed in `docs/NUMBERS.json` and **no card reads it**, so nothing on screen is wrong. Filed **WFG-094** (P1): `registry_slice()` skips a missing key silently, and `test_the_v2_cards_have_every_registry_key_they_read` checks the keys cards *read*, not the keys the builder *declares* — so a future card on this key would vanish silently, which is the failure mode `docs/finals_screen_v2.md` §1 names.
- **WFG-067 is closed on disk.** The SYSTEM INTEGRITY panel stamps `d5e2562`; `git cat-file -t` resolves it and it is an ancestor of HEAD. The `a562045` notes carried on `KCF_READINESS.md` R2 by critics #9/#10/#11 were true when written and are stale now.
- `scripts/check_screen_assets.py web/finals.html` **PASS**.
- A rebuild **was** run (`build_finals.py --verify`, three gates OK), produced a **one-line** diff — a fresher commit stamp — and was **reverted**. Regenerating a committed build product to buy nothing is the churn CHARTER §3.2 exists to prevent.

### Root objection recorded before building (`hate`)

**A demo script is prose the loop writes about a screen it cannot rehearse; the critic's standing complaint is that this repository produces documents instead of product, and an untimed script citing numbers the screen may not print is a *worse* document than none — it would teach the student to point at things that are not there.**

**Cheapest test, and it is what shipped:** make the script machine-readable and bind it to both the registry and the built screen, so the claim 「every on-screen figure maps to a registry key」 is checked rather than asserted. `tests/test_demo_script_5min.py` (11 tests) parses §3's table and fails on: a key that does not resolve in `docs/NUMBERS.json`; a **화면** row whose key no card in `scripts/finals.template.html` renders; a 값 cell that does not equal its key's registry value; an emphasised number in §1's spoken body that §3 does not source; timings that are not contiguous or do not total 300 s; every row relabelled 화면 (which would let the screen test pass by having nothing to compare); the losing dispatch result being dropped; a missing judge row; and `default_region` moving away from the region act 3 quotes.

**Graded, not asserted (`mandela`) — and the first grading was self-confirming.** Six mutations were written and run against the first version: **6 of 6 caught**, which was true and worth very little, because all six edited the §3 table and the table was the only surface the test read. The independent reviewer broke it in one edit: change a bolded number in the **spoken body** (72 % → 82 %) and leave the table alone, and nothing in the tree objected. That is now closed by `test_every_figure_the_student_says_aloud_is_in_the_mapping_table`, and the gate re-graded against **ten** mutations — the original six plus the reviewer's body-only nail, a wrong value beside a correct key, a blob-only key relabelled 화면, and deleting the screen's own dispatch figure. **10 of 10 caught.**

The residual leakage that remains is stated rather than closed: the mutations were still written inside this lap, and the half no test reaches is whether five minutes of spoken Korean fits in five minutes. The segment times are **design values**, not a rehearsal, and §5 of the document says exactly that. Closing it is R12/NH-014 — a human reading it aloud with a stopwatch.

### What did not work / what was deliberately not done

- The screen rebuild (reverted, above) — one line of churn for no gain.
- `mr_yeongdeok_walk_time_increase_pct` was **not** registered. Registering a new number would need a registrar and an artifact, and CHARTER §3 forbids re-running `build_numbers.py` wholesale (WFG-040). It is a P1 row, not a lap.
- Act 1 speaks only the registered `det_size_floor_ha_tf750` = 0.1939 ha and points at the screen for the 0.08~0.69 range, because the 600 K and 900 K endpoints are not registry keys and CHARTER §3.3 says a number you cannot register you do not write.
- Two figures were rounded in a first draft (24.7 %, 9.2 %) and corrected to the registered values (**24.73 %**, **9.17 %**) before the gates ran. A third, a bare 「도달 실패 24가구」 baseline, was not registered as such and was rewritten to `l0i_unsavable_at_240` = 0가구, which is.

### Evidence

| what | where |
|---|---|
| the script | `docs/auto/DEMO_SCRIPT_5MIN.md` |
| the gate on it | `tests/test_demo_script_5min.py` (11 tests, 10/10 mutations caught) |
| the Q18 repair | `docs/auto/JUDGE_QA.md` Q18 |
| new row | `docs/auto/BACKLOG.md` WFG-094 |
| readiness evidence (not ticked; the critic ticks) | `docs/auto/KCF_READINESS.md` R4 |
| claim commit | `2e372f9` (pushed) · build and fix commits rewritten by the push rebase; see `git log` |

**Rebased onto a concurrent lap before the push.** `origin/auto/dev` had moved to `b3244f8` while this lap was gating. One real conflict: a research lap had already taken **WFG-090 through WFG-093** on `origin`, so this lap's new infra row is **renumbered WFG-094** — an id another lap already published wins, and the row says so in its own first sentence. The other conflicts were generated files (`dashboard.html`, `STATE.json`, `images/latest/`), resolved to upstream and then re-rendered against the rebased history, which is why the board below shows both laps.

### Gates

**Two runs, and the difference between them is this lap's lesson.** The first, at `2e372f9` with the new file still untracked, read `1462 passed, 62 skipped` and was **not a gate run at all** for the tracked-file scanners. The second, with everything staged: `gates.py --mode full` exit **0**, `1470 passed, 56 skipped` in 248.5 s — a warm run, so the six SRTM-gated tests ran instead of skipping (WFG-039), which is where 62 → 56 comes from. `verify`, `snapshot-verify`, `env-check` PASS; `baseline-verify` WARN, expected off-laptop, `hard: false`. Re-run once more on the commit actually pushed, with `--assert-head` and `--assert-reported`.

### The independent review blocked this lap, and every count was right

`docs/auto/LOOP_CONFIG.json` → `review: subagent`. A fresh agent that had not seen the work read `docs/auto/CHARTER.md` §3/§9, the diff `2e372f9..e5ab329` and the claims in this summary, and ran `hate`, `factchk` and `mandela`.

**Reviewed by: subagent (block): the lap reported a green full gate on a tree where `make verify` is red because of the file this lap added, and every self-graded claim was produced without re-running that gate against the lap's own output.**

It was right, and the cause is the CHARTER §4 step 8 trap seen from the other side. `gates.py --mode full` ran while `DEMO_SCRIPT_5MIN.md` was still **untracked**; `check_number_collisions.py` walks **tracked** files, so the gate never saw the file. Committing it turned `make verify` red with four collisions. **Nothing red was pushed** — the block landed before the push, which is what the reviewer step is for. Fixed, and re-run: `gates.py --mode full` exit **0** at the staged tree, `1470 passed, 56 skipped`.

What the block changed, all of it kept:

1. **Four `collision-ok:` pragmas**, each naming which two quantities the anchor heuristic confused (e.g. 24.73 % is the walking-network origin share, not the building-based `bld_uiseong_fa_only_pct` = 17.13).
2. **화면 was defined wrong, and the test agreed with the error.** 「the key is in the built page」 is not 「a judge can see it」: `build_finals.py` embeds the whole registry slice as one JSON blob, so every declared key appears exactly once whether or not a card draws it. The definition now reads `scripts/finals.template.html`, and the four rows no card renders are **구두**.
3. **Two of those four were actively contradicted on screen**, which is the failure the document exists to prevent. The terrain card renders `slope_walk_time_increase_pct` = **+26.6 %** (영덕, 경사 보정) while act 3 has the student say **15.14 / 23.67** (시간 인지 우회, a different axis); the dispatch card renders `dispatch_order_deadline_wins_pct` = **3.6 %** while act 4 said 「180개 셀 중 0승 … 화면에 그대로 적혀 있습니다」. Both now carry an explicit ⚠ block giving the student the reconciling sentence, and act 4 was rewritten to **read the screen's number first and add its own after**, so the two land as a range and a point inside it rather than as a contradiction.
4. **The self-confirming grading**, above.
5. Three factual corrections: **31 → 35 rows** (the count was simply wrong), 「다섯 구간」 → 「여섯 구간」 on the first line the student reads, and `docs/live_pipeline.md` **§7 → §6**, the section that actually holds the 24.9~28.2 s measurement.

The reviewer also verified and **passed** the claims worth keeping: the 40-of-41 key measurement and WFG-094's diagnosis; WFG-067 closed on disk; Q18's 64 / 92 / 17 and the NH-012 closure wording, checked against `paper/manuscript.md` §6 and the author's verbatim decision; `check_screen_assets` PASS; no CHARTER §3.2 artifact touched, no frame change, no novelty claim, no fabricated citation, DRAFT label present. Its one non-blocking softness — the withdrawal note's 「막혀 있던 다운로드는 같은 날 이루어져」 conflates the 주소정보누리집 cut with NH-012(b)'s national file — is real; numbered line 4 of the same paragraph draws the distinction, so the student is not misled, and it is left as written rather than widened.

### Next row

**WFG-036** (P0) — the release bundle `release/kcf-finals-2026/` (R9), sprint date 09-10, then **WFG-037** (booth recipe, R3/R12). DIRECTION.md's list under WFG-003. WFG-094 and WFG-088 are P1 and wait, per CHARTER §14b.

## In plain terms

- **What changed for the project.** You now have the thing you say out loud at the booth: a five-minute Korean script, timed to the second, that walks the four acts of the screen and ends on what the project cannot do. Every number in it is now checked by a test against the registry **and** against the card that actually draws it, so you cannot be caught pointing at a figure that is not on the screen. My first version got exactly that wrong in four places, the independent reviewer stopped it before it was pushed, and the fixed version is what you have. The Q&A card about designated shelters was also telling you something that stopped being true a day ago, and it now tells you the truth.
- **Why it matters to the judges or the science.** Five judges each get five minutes with you, so this script runs five times in one day, and the score turns on whether you sound like you understand it rather than like you memorised it. It deliberately includes the result where this project **loses** — the dispatch ordering wins 0 of 180 cells — because that is the part a scientific judge trusts.
- **What you should do.** Read `docs/auto/DEMO_SCRIPT_5MIN.md` out loud once with a timer and tell me where it runs over. The section timings are my estimate, not a rehearsal, and you are the only person who can fix that. Nothing is blocked on you and no decision is waiting.



## Gates

**ALL GREEN** · mode `full` · head `e5ab329` · 2026-09-05T00:58:19Z · current at `e5ab329`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 16.3 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.2 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 0.5 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 248.5 s | 1470 passed, 56 skipped, 52 warnings in 247.23s (0:04:07) |

## Commits since the previous report (ed35f0df3aad27e14992c9bb3c4c19cee8ec7e30..HEAD)

- e5ab329 the five minutes a judge actually gets now has a script, and the script is read by a test
- 2e372f9 claim WFG-087 and WFG-003 (20260905T0025Z)
- 1f69c10 the gate block names the head this report was written at, and the one that was pushed
- a234a1c critic #14: the escalation resolved itself, and a T0 card still teaches a wall the tree took down

## Needs a human (5 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-024 [DECISION] WFG-062 is now three laps deferred, and the critic's own re-scope condition has been met (by 2026-09-07)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
