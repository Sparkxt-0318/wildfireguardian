# WildfireGuardian autoloop · dev · 2026-09-11T0119Z

| | |
|---|---|
| when | 2026-09-11 01:19 UTC · 2026-09-11 10:19 KST |
| branch / head | `auto/dev` / **`39d071c`**, the pushed head. ⚠ `report.py` stamped this table at `beae52d`, necessarily before the commit carrying the report existed; that commit was then amended to `39d071c` because the 0112Z report's own prose turned `check-number-collisions` RED and the fix belongs in the report file. `beae52d` resolves nowhere on this branch. |
| kind | dev |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 10, done: 34, dropped: 1, parked: 1, title-derivable: 1, todo: 117 |

## What happened this lap

Lap 2026-09-11T0025Z (dev). Four rows closed in one lap — WFG-243, WFG-240, WFG-241,
WFG-235 — which is what DIRECTION.md named the bundle for: four separate laps would have paid
four `make printables` rebuilds and four re-pointed release manifests. ⚠ **This lap paid that
rebuild TWICE, not once, and one build was discarded rather than committed** — the honest count,
corrected by the independent reviewer before the push; see 「The rebuild」 below. Two instead of
four is still the saving the bundle was for, and it is not the one the first draft of this
sentence claimed.

## Your decisions, applied

**Nothing new from you, on either channel. Eighth consecutive lap saying so.** Gmail
`from:siyeong0318@gmail.com subject:"WildfireGuardian autoloop" newer_than:14d` returns 25
threads on the first page (201 estimated in the window) and every one of them is the loop's
own send, with no reply threaded under any. A wider search (`"NH-0" newer_than:14d -in:sent
-in:draft`) returns **zero threads**, so nothing addressed to the loop sits outside the report
threads either. PR #31 has **zero comments**, read through the GitHub MCP. `docs/auto/decisions_seen.json`
is unchanged: its `seen` list is empty and its `applied` list still ends at **NH-031**, closed
2026-09-06 in a Claude Code session on your laptop. **No `NH-###:` line has ever reached the
loop by email.** No decision was applied, none was guessed at, and no reply line went unmapped.

**24 entries are open** (23 DECISION + 1 BLOCKER) and 2 of them (NH-005, NH-014) carry no date
at all. **NH-046 is now a day overdue.** NH-049 and NH-051 come due today. The easiest channel
is still the one you chose in NH-020: open Claude Code in the repository and say `decisions`.

## What this lap did, and why

**The row.** DIRECTION.md at `a823c33` named a bundle of four with a reason — all four edit
`docs/auto/JUDGE_QA.md` or `docs/auto/finals/RELATED_WORK_PANEL.md`, the two hashed printables
sources, so each alone costs `make printables` at a new stamp plus a re-pointed
`release/kcf-finals-2026/MANIFEST.json` (NH-049). CHARTER §14 lets the page name the next row
when it gives a reason, so the bundle was taken. All four are claimed in one commit, `de0bcd3`,
pushed before any building started.

⚠ **One correction to DIRECTION.md, and it matters for the critic's own bookkeeping.** That page
says the bundle 「is already table order, so no row moved」 and that critic #62 therefore spent
**ZERO** reorders. Re-derived here by parsing the board's 238 rows in table order: **nine P0
`todo` rows sit ABOVE WFG-235** — WFG-007 (:102), WFG-117 (:103), WFG-129 (:104), WFG-121 (:110),
WFG-106 (:129), WFG-036 (:131), WFG-101 (:132), WFG-119 (:135), WFG-230 (:140), every one of them
`agent_doable: true` and none `blocked`. So naming the bundle first WAS a reorder in effect, and
it is a legitimate one under §14 (it is named with a reason) — what is not right is the claim that
it cost nothing. **This lap took the bundle anyway**, because §14's authority is the naming and not
the bookkeeping, and because two of the four are a withdrawn claim standing on printed paper. The
ledger correction belongs to the critic, not to a dev lap rewriting DIRECTION (§14: 「Dev never
writes it」), so it is reported here rather than edited there.

**Root objection (`hate`), recorded in the claim commit before building.** The whole plan was to
replace 「집」 / 「가구」 with 「지점」 on the two sheets whose front face exists to make the
읍면동-versus-something-smaller comparison — and 「지점」 is the colder word. A judge feels 「이 집
사람이 걸어 나갈 수 있는가」 and does not feel 「이 지점」. The cheapest way for this lap to fail was
to buy a true register at the price of the sentence that carries the differentiator, and then score
itself green on a gate that cannot read either. **Cheapest test, applied to every line before it
shipped:** does the corrected sentence still answer 「무엇이 다릅니까?」 in one breath, and does it
carry the sampled-origin bound in the same block? Where a bare word-swap failed that test the line
was rewritten instead: 「공간 단위는 가구(집)」 became 「공간 단위는 **보행 그래프의 한 지점**」,
which is both narrower and more concrete than what it replaced, and Q16a's block now states what a
지점 actually is in this repository (a sampled walk-graph coordinate, not an address) in the same
breath as the claim.

### WFG-243 + WFG-240 — the withdrawn household register, on paper and spoken

`WC-013` withdrew this project's own output object stated in the household register on 2026-09-10.
At `a823c33` it was still standing on **eight lines of two files**, both of them hashed sources of
the printed booth kit, three of them on **T0** cards the student says from memory. All eight are
corrected:

| file | line | what it said | surface |
|---|---|---|---|
| `docs/auto/JUDGE_QA.md` | :657-659 | 「가구 하나하나에 대한 …, 공간 단위가 읍면동이 아니라 집」 | Q16a, **T0**, printed |
| `docs/auto/JUDGE_QA.md` | :667 | 「이 집 사람이 걸어 나갈 수 있는가」 | Q16a, **T0**, printed |
| `docs/auto/JUDGE_QA.md` | :1189 | 「어느 집을 먼저 가고 어느 길로 걸어 나오는가」 | Q29a, **T0**, printed — **named by neither row** |
| `docs/auto/JUDGE_QA.md` | :1192 | 「어느 집을 먼저 구해야 하는가가 달라지는가」 | Q29a, **T0**, printed — **named by neither row** |
| `RELATED_WORK_PANEL.md` | :18-19 | 「그래서 이 집 / 사람이 지금 걸어 나갈 수 있는가」 | **앞면 한 문장**, printed — **named by neither row** |
| `RELATED_WORK_PANEL.md` | :45-46 | 「어느 집을 먼저, 어느 길로 … 공간 단위는 가구(집)」 | 뒷면 1, printed |
| `RELATED_WORK_PANEL.md` | :73-74 | 「이 집 사람이 걸어 나갈 수 있는가」 | 뒷면 1, printed |
| `RELATED_WORK_PANEL.md` | :119-120 | 「농촌 가구 단위의 도보 대피」 | 뒷면 2, printed |

Plus the two off-kit copies the row named: `docs/global_portability.md:147` (a whole comparison
table whose three 본 층 cells all read 가구; corrected with a dated 정정 note saying the values and
resolutions did not move, only the word) and `docs/SESSION14_REPORT.md:178`.

**Three of the eight were named by neither WFG-243 nor WFG-240, and one of those three is the
biggest sentence in the printed kit.** `RELATED_WORK_PANEL.md:18-19` is the panel's 앞면 「한
문장」 — bold, first thing a judge reads — and it held the withdrawn register with the phrase split
across the two source lines. Critic #62 swept every tracked `.md` and `.html` for 「이 집 사람」,
counted **six** instances, wrote up the line-wrap limit one paragraph earlier as WFG-223's 「measured
limit」, and still did not see this one, **because its own grep was line-based too**. The other two
are both halves of Q29a item 1 — in the card WFG-222 corrected the previous day, two lines below
that card's own 「지점 단위 구조 순서」, so one sentence of a spoken card named the project's unit
both ways.

The panel's 2026-09-10 정정 block scoped itself to 「이 문단」, which is what left the other four
lines of the same sheet carrying the old word for a day. It now names the sheet, says which four
lines moved, and records that **scoping a correction to one paragraph is itself the defect**. A
second block was added saying which two 「가구」·「집」 usages on the sheet are deliberately KEPT and
why (G-DAPS's 읍면동 explanation, and WC-008's what-other-systems'-published-material-does-not-show
sentence) — so the next lap does not "fix" a protected line.

**Registered, in the same lap (CHARTER §3.5c).** Six new spellings on `WC-013`, blast radius of
each measured over all tracked `.md` and `.html` **before** registering. Five came back with zero
hits outside the record class; one (`어느 집을 먼저`) had a single gated hit at
`docs/withdrawn_claims.md:260` — the very sentence that had **predicted in writing** that this
wording would escape — now licensed by a line pragma, with a dated note saying the prediction came
true. One candidate pattern was **narrowed after measurement**: `가구별\s*(?:대피|진입)` fired on
`docs/submission_reconciliation.md:35`, which is Q16's legitimate quantity 「가구별 진입로 폐쇄 시각
`ingress_survival_time_min`」 and is itself on the printed kit, so the registered pattern is
`가구별\s*대피` only.

### The gate this lap owes the next one

`tests/test_withdrawn_claims_registry.py::test_the_household_register_is_absent_even_where_a_line_break_hides_it`.
`scripts/check_withdrawn_claims.py` scans **one line at a time**, so no pattern in the registry —
however loose — can see a phrase that straddles a source line break. That is a property of the
scanner, not a flaw in a pattern, and it is exactly how the panel's front face survived. The new
test re-scans every gated file with paragraph line breaks collapsed and reports **only** what the
line-based gate cannot see. Scoped deliberately to the six output-object spellings rather than the
whole registry, because collapsing newlines loosens every pattern and a loose pattern over fourteen
claims is how a gate starts teaching laps to widen the record class.

**Shown red on its own mutation before being trusted.** The front-face line was reverted to its
`a823c33` wording in the working tree: `scripts/check_withdrawn_claims.py` reported **PASSED, 14
claims over 940 gated files** — the bug, reproduced — while the new test failed naming that exact
file and token. The mutation was then reverted and `tests/test_printables.py` re-run to confirm the
kit hash still matches the manifest built from the corrected text.

### WFG-241 — the card for the question this week made unavoidable

New card **Q16d** (T1), 「비슷한 걸 만든 사람이 이미 있지 않습니까?」. It says yes, names Bokade,
Barai, Bhogle, Chapherkar & Kale (2026-09-09, Zenodo preprint, concept DOI
`10.5281/zenodo.22668357`), and states the delta as **the output object and its measured limits,
never the architecture** — they ship an interactive simulation environment and report no evaluation
metric of any kind; this project ships a per-point verdict plus `docs/disc_null.md` and
`docs/oracle_gap.md` measuring how wrong it can be. The replanning half is in the card too (D* Lite
repairs a path when the cost surface changes; this project prices an edge by *when* a walker would
traverse it on a time-expanded graph). All four boundaries are **in the card, not a footnote**:
Uttarakhand India, a road-and-vehicle routing stack, **no reported routing metric**, an unrefereed
Zenodo preprint whose attached file is an IEEE conference template. Its 없는 것 block refuses the
overclaim 「저쪽은 평가를 안 했습니다」 in favour of ⭕「**공개된 기록과 초록에는** 경로 성능 수치가
없습니다」 (the WC-007 / WC-009 lesson), refuses accuracy comparison in either direction, refuses to
imitate the preprint's 「guaranteed safe evacuation」, and keeps the panel's 「받아 적으십시오.
반박하지 마십시오」 as its closing line.

`Q29a`'s 없는 것 item 4 — which told the student 「그 범위 밖에서 같은 일을 한 연구가 있는지는
모릅니다」 — now carries a dated clause saying one such study was found **inside** the range, points
at Q16d, and keeps 「모릅니다」 true of everything outside that one paper. Header counts moved 46 → 47
and T1 20 → 21, and the §6 drill table lists Q16d after Q16c; the bank's two independent parsers
agree.

**Every fact in the card was re-verified live at its DOI in this lap's own process** (`factchk`,
and CHARTER §5b's agency / date / scope), rather than inherited from the two repository records:
the Zenodo API returns `conceptdoi: 10.5281/zenodo.22668357` for a record whose own `doi` is
`10.5281/zenodo.22668358` — **confirming the version relation and refuting the 「duplicate record」
wording** — with `publication_date: 2026-09-09`, resource type **Preprint**, the five creators in
the order the card names them, and exactly one attached file, `IEEE_Conference_Template.pdf`. The
record metadata only; the PDF was not opened this lap either, and the card's 없는 것 item 1 scopes
its negative to 「공개된 기록과 초록에는」 for that reason. The verification, its agency and its date
are written into the card's own 근거 block, so a judge asking 「그 논문 진짜 있습니까」 gets a DOI to
open at the booth.

**No number from the knowledge note reached the card.** One dated paragraph was appended to
`docs/auto/knowledge/ROUTING_FUNDAMENTALS.md` §Update 2026-09-10 (CHARTER §13 permits appending, not
restructuring) correcting its 「duplicate record」 wording: `10.5281/zenodo.22668358` is the **version**
DOI of the same record, not a second deposit, which critic #61 re-derived and `paper/references.bib`
→ `bokade2026` already says.

### WFG-235 — what 0.394 should be compared with, on the card a judge hears it from

Q36 now carries both IoU pairs with their `dn_yeongdeok_` keys, never separated: model **0.3941** / <!-- collision-ok: 0.3941 0.1554 2.536 0.2577 0.1169 2.2044 — these are the HEADLINE slice (`dn_yeongdeok_model_iou` and its bare twin), not the per-slice `dn_yeongdeok_t###min_*` keys the anchors iou/model/yeongdeok also match; different quantities, same three words -->
disc **0.1554** / ratio **2.536**, and the seed-removed pair model **0.2577** / disc **0.1169** /
ratio **2.2044**, with the card saying in bold that the second pair is the one to quote at the booth
and the first only alongside 「초기조건이 거들어 준 값」. The overshoot travels in the same block —
observed centre of mass **2.25** cells from the seed against the model's **7.292**, and the model's <!-- collision-ok: 2.25 7.292 5.34 2.266 — four DIFFERENT displacements on one line (seed-to-observed, seed-to-model, model-to-observed, disc-to-observed); the anchors cells/model/observed match all four -->
error to the observation **5.34** against the stationary disc's **2.266** — with the explicit ruling
that the gap is **shape and extent, not direction**. Also in the block: the disc is a **floor and not
a competitor** (a circle scored against an elongated fire, so clearing it is necessary and not
sufficient), the 27-minute figure is labelled a **time gap and not a margin** (the card's standing
「어떤 여유값도 말하지 마십시오」 is untouched, and NH-032 / NH-034 / NH-052 stay closed to this lap),
and the measurement **moved no committed number**. A shortened spoken form went into the card's
부스에서 할 말 quote, derived from `docs/disc_null.md` §6's draft; the bank's own first line carries
the 「Status: DRAFT」 label CHARTER §9 asks for.

⚠ **WFG-235's premise is wrong in one detail and the row is corrected in place.** It says Q36
「carries `docs/oracle_gap.md` §4's 「자리는 절반쯤 맞힙니다」 with nothing after it」. That phrase is
not in `docs/auto/JUDGE_QA.md` at all — grepped here, zero hits in the whole bank; it lives at
`docs/oracle_gap.md:226` as 「자리는 절반 정도만 맞힙니다」. What Q36 actually did was send a judge to
`docs/oracle_gap.md` and give them no null baseline to read 0.394 against, which is the same defect
one level of indirection out, so the row's deliverable was right and its quoted premise was not.
Two figures of §6's draft were deliberately **not** carried onto the card: the 249-cell seed count
(not a registered key) and the metre forms 3,646 m / 1,125 m, because putting that magnitude on the
document the front door points at is **WFG-237**, a separate row.

### The rebuild — paid twice, and it should have been once

`make printables` → `WFG_printables_20260911T0102Z.pdf`, **58 pages**, seven sources, and
`release/kcf-finals-2026/MANIFEST.json` re-pointed to it by `make finals-bundle UPDATE=1` (19 files).
The previous committed kit (`20260910T1233Z`) is kept, not overwritten (CHARTER §3.2, §3.7).

⚠ **It was built twice, and the first build was discarded rather than committed.** A `20260911T0034Z`
build was made, and then the live DOI verification above added six lines to Q16d's 근거 block — a kit
source — which made that build stale by its own gate. Since it had never been committed, it was
removed and the kit rebuilt at `20260911T0102Z` instead of shipping a second stamp nothing points at;
no committed artifact was touched and no record was lost. The lesson is ordering: **verify the world
claims BEFORE the rebuild, not after**, which is the same shape as the critic's sweep-before-rebuild
note and is now in `docs/auto/MEMO.md`.

⚠ **The critic's second-cheapest test was run BEFORE the rebuild, not after, exactly as it asked.**
A wrap-aware re-grep of all seven hashed kit sources for the six household-register patterns returned
**0** line hits and **0** wrap-only hits, so the PDF certifies a corrected tree rather than recording
an uncorrected one.

## What did not work, and what this lap did not do

- **The first draft of the `가구별` pattern was wrong and the measurement caught it**, not review:
  it fired on a protected line that is itself on the printed kit. Had it been registered as drafted,
  the gate would have gone red on `docs/submission_reconciliation.md` and the cheapest way out would
  have been to widen the record class.
- **The first two placements of the `docs/withdrawn_claims.md` pragma did not license anything.** The
  gate reads a pragma on the offending line or the line directly above; both drafts put it below.
  Two red runs, no harm, but it is the second time this file has cost a lap that.
- **Q16a's `:659-661` sentence was left exactly as it is**, per DIRECTION and `WC-013`'s `say_instead`
  — and it now reads a little oddly beside the corrected register, because it says the other systems'
  published material shows no 「집 단위」 output while this project's unit is 지점. That is a real
  residual and it is left for the critic rather than widened into by this lap: the sentence is
  protected by name, and `:680-681`'s ⭕/❌ pair already gives the student the honest form.
- **No P0 row above the bundle was touched**, and nine of them are `todo`. That is the direction
  page's call, not this lap's.

## Gates and review

`gates.py --mode full` exits **0** on the FINAL tree, re-run after the `20260911T0102Z` rebuild
and the Zenodo verification block rather than before them (CHARTER §4 step 8: the commit you push is
the commit the gates read): **2071 passed**, 65 skipped, 3 xfailed, pytest 419.0 s. An earlier full
run on the pre-rebuild tree was also green with the same counts at 287.4 s; the run this report's
table certifies is the later one. `baseline-verify` WARNs on the two git-ignored `data/raw/**` contracts, which is NH-029 and
CHARTER §3d working as designed. Baseline at lap start was green (`quick`, exit 0) and GitHub's own
`auto-gates` run **353** is `success` at `a823c33`, the exact head this lap started from, so CHARTER
§4b set no finding #1 for the twelfth consecutive lap.

Reviewed by: **subagent (pass)**: 「The lap's justification for taking a four-row bundle ahead of
nine todo P0 rows is a cost argument — `make printables` paid ONCE instead of four times — and the
lap then paid it twice.」

The reviewer passed the work and named that as a **fix-before-push rather than a park**: the double
rebuild was already disclosed in this summary while the review was running, but the report's opening
sentence and a section heading still said 「paid once」, so the two lines an author reads first
contradicted the paragraph three lines below them. Both are corrected above, and the gate table now
certifies the post-rebuild run. Its first nail was exact: `ls docs/auto/finals/printables/ | grep
20260911` returns two stamps and the earlier one is gone from the tree.

**What the reviewer re-derived and confirmed independently**, without editing a file: all eight
withdrawn-register lines corrected, hunk by hunk; three of them named by neither row (it checked that
neither row's text contains `:18`, `:1189`, `:1192`, `Q29a`, 앞면 or 한 문장); the mutation claim
reproduced in memory — the line-based checker returns `[]` while the wrap-collapsed scan returns
exactly one hit; the scoping claim reproduced exactly (940 gated files, 14 claims, 6 spellings, zero
wrap-only hits); every protected sentence intact; Q16d's four boundaries all in the card body; all
thirteen `dn_yeongdeok_*` values on Q36 registered with exactly the printed values and
`docs/NUMBERS.json` untouched; nine P0 `todo` rows above WFG-235 with every id and line number
matching. It re-fetched the Zenodo record itself and confirmed title, five authors with Kale marked
(Supervisor), Preprint, 9 September 2026, the version-DOI relation, no quantitative metric in the
abstract, Uttarakhand, OpenRouteService, D* Lite, 「guaranteed safe evacuation」, and the single
attachment named `IEEE_Conference_Template.pdf`. On `mandela` it named the one leakage pattern that
could fire on a lap writing its own pattern, probe and gate — #4 Tautology — and ruled it defused
because the probes are lifted verbatim from the `a823c33` tree and the gate was shown red on a real
reversion of a real shipped line rather than on a synthetic string.

**Three residuals it raised that this lap deliberately did NOT act on**, each with the reason:

1. **Q36's spoken 부스에서 할 말 script quotes the IoU figures and gives the overshoot only
   qualitatively** (「실제보다 멀리 보냈고, 가만히 있는 원보다 더 많이 빗나갔습니다」), so read alone
   it carries the direction of the overshoot but none of its four numbers. Not fixed, and not for
   cost: the script is a **T0 card said from memory**, the analytic block in the same table cell
   carries all four figures with their keys two paragraphs above it, and `docs/disc_null.md` §6's own
   draft made the same choice for the same reason. Putting four decimals into a memorised sentence
   makes the card worse, not more honest. **For the critic to rule on, not for a dev lap to decide.**
2. **The panel's new 「deliberately kept」 block names 「집 한 채가 아닙니다」 as kept**, while Q16a's
   version of that same clause was rewritten to 「행정구역이고, 저희 판정은 그 안의 점 하나하나마다
   따로 나옵니다」. The block is scoped to 「이 종이」 and is true of the panel, and the reviewer
   graded it 「a note for the critic, not a defect」, so the two kit sources state one contrast two
   ways. Reported rather than papered over, and fixing it would cost a **third** rebuild.
3. **`CHARTER.md` §1's mission sentence still says 「household-level」.** That is the protected
   application-naming DIRECTION rules on by name (「Do not 'fix' 「household-level」 … they name the
   **application**」, `paper/GAPS.md:389`, WFG-231), not stale wording, so it is left alone.

One pre-existing staleness the reviewer found WAS fixed, because it costs nothing and misleads the
next lap: `test_no_gated_document_carries_an_unlicensed_withdrawn_claim`'s docstring said 「925 files,
four claims」 against a measured 940 and 14. It now names no literal and says where to read the
current counts.

## Next row

**WFG-236 (P0, KCF)** — `docs/disc_null.md:20-21` and `docs/oracle_gap.md:180-181` both assert in
bold that nothing said what 0.394 should be compared with, while `README.md:520-522` and `:888-891`
have compared it to Rothermel's ~0.09 for months. It pays no rebuild, and after this lap those two
sentences are false in a second way: Q36 now carries the comparison too. Then **WFG-244** (minutes,
`docs/MODEL_CARD.md`) and **WFG-237**.

## In plain terms

- **What changed for the project.** Your project describes what it produces as a decision for one
  small place at a time, and the printed pack you hand judges was still calling that place a
  「house」 in eight sentences — including the single biggest sentence on the sheet and three lines the
  student says from memory. All eight now say 「지점」, the pack was reprinted once from the corrected
  text, and a new check will catch the same slip even when it is split across two lines, which is how
  the biggest one hid.
- **Why it matters to the judges.** A project whose whole answer to 「무엇이 다릅니까?」 is 「our unit
  is smaller than theirs」 cannot name that unit two different ways on the paper making the comparison.
  Two other things landed on the same reprint: judges who know the field can now ask 「hasn't someone
  built this already?」 and get a straight yes with the paper named and the real difference stated, and
  the headline forecast number finally has something to be compared against on the card itself.
- **What you should do.** Nothing for this. But 24 decisions are still waiting and 2 have no date at
  all; NH-046 is a day overdue and NH-049 and NH-051 come due today. The sprint ends 2026-09-15 —
  four days. Opening Claude Code and saying `decisions` answers them as button questions.



## Gates

⚠⚠ **THIS TABLE IS THE RED RUN, AND IT IS KEPT RATHER THAN REPLACED.** **RED** · mode `full` · head `beae52d` · 2026-09-11T01:18:19Z. It is the record of the failure that CHARTER §8 exists to catch and that this lap actually met: `report.py` writes tracked prose that no gate has yet read, and the 0112Z report's own two lines — `0.3941` against the per-slice `dn_yeongdeok_t###min_model_iou` keys, and four different centroid displacements sharing the anchors cells/model/observed on one line — turned `check-number-collisions` red on the commit. Deleting this table would delete the evidence for the paragraph that explains it. <!-- collision-ok: 0.3941 — the HEADLINE dn_yeongdeok_model_iou, named on this line beside the per-slice dn_yeongdeok_t###min_* keys precisely in order to explain why the two disagree; this is the third instance in one lap of the same class, and it is the sentence explaining the first two -->

**THE RUN THAT GATED THE PUSH, on the commit that was pushed:** `gates.py --mode full` → **ALL GREEN** · mode `full` · head **`39d071c`** · written 2026-09-11T01:25:31Z · **2071 passed**, 65 skipped, 3 xfailed, pytest 290.7 s · `verify` PASS · `baseline-verify` WARN (the two git-ignored `data/raw/**` contracts, NH-029 and CHARTER §3d working as designed) · `snapshot-verify` PASS · `env-check` PASS. Immediately before the push, on that same commit with a clean tree: `gates.py --assert-head` **OK** (`head=39d071c written_at=2026-09-11T01:25:31Z`) and `gates.py --assert-reported` **OK** (14 substantive paths travel with this new report). The collision annotations that cleared it are inline `collision-ok:` pragmas carrying their own reason, written into `.auto/summary.md` so they survive regeneration — the fix is annotation, never deletion (CHARTER §3.7).

| step | result | time | last line |
|---|---|---|---|
| verify | FAIL | 17.9 s | make: *** [Makefile:120: check-number-collisions] Error 1 |
| baseline-verify | WARN | 0.1 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 0.4 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 289.0 s | 2071 passed, 65 skipped, 3 xfailed, 52 warnings in 287.87s (0:04:47) |

## Commits since the previous report (de0bcd37fac84ed8905c53be302710d461af5809..HEAD)

- **`39d071c`** the printed kit names this project's output object one way, on all eight lines that named it two — the pushed commit. (`report.py` listed it as `beae52d`, the pre-amend id, which resolves nowhere on this branch.)
- `de0bcd3` claim WFG-235 + WFG-240 + WFG-241 + WFG-243 (20260911T0025Z) — the claim commit, pushed before any building started, carrying this lap's root objection.

## Needs a human (29 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09, one day past; raised to HIGH by critic #55 on a measured third instance)
- NH-036 [DECISION] One critic lap told the next one not to edit a file, and that is what kept a false sentence in front of a judge for a window (by 2026-09-10)
- NH-037 [DECISION] The paper's word proxy now stops it a thousand words before your 25-page rule (by 2026-09-10)
- NH-038 [DECISION] Your "product first" rule has spent the last three dev laps on documents, and the readiness line it was written to protect has not moved in five critic laps (by 2026-09-09)
- NH-039 [DECISION] The national wildfire-spread system's manual is an 18 MB PDF the sandbox could not fetch, and one of you can (by 2026-09-12)
- NH-040 [FYI] A critic lap pushed one commit past a red `--assert-reported`, and it is telling you rather than hiding it
- NH-041 [FYI] This lap sent you an email containing only the word PLACEHOLDER, and could not take it back
- NH-042 [DECISION] Two of your own rules collide whenever a withdrawn claim lives in a frozen artifact, and this week they collided three times (by 2026-09-10)
- NH-043 [DECISION] A gate your loop built this morning will go red about twice a day, and the charter tells the lap that meets it to stop working (by 2026-09-09)
- NH-045 [BLOCKER] The staleness gate has closed `auto/dev` to every routine, and the one routine that met it is the one forbidden to clear it (by 2026-09-08)
- NH-044 [DECISION] The claim the paper just retracted is still live on the page the paper cites for it (by 2026-09-09)
- NH-046 [DECISION] Your product's definition-of-done names a command nothing in this project has ever run (by 2026-09-10)
- NH-048 [DECISION] One of the research routine's three literature channels has been dead for two runs, and a working replacement is already proven (by 2026-09-10)
- NH-049 [DECISION] Your critic routine is told to add judge Q&A cards and your own printing gate makes that impossible for it (by 2026-09-11)
- NH-050 [DECISION] You answered two of these questions two days ago and the loop never heard you, because you answered them on the routine page (by 2026-09-10)
- NH-051 [DECISION] The rule your loop has been obeying for three days is not the option it names, and the difference is why one row has been pushed down five times (by 2026-09-11)
- NH-052 [DECISION] The experiment that measures what your own forecast is worth is now one run away, and it would change what 42 means (by 2026-09-12)
- NH-053 [DECISION] The word your front door uses for 42 stopped being right today, and the loop cannot pick its replacement (by 2026-09-12)
- NH-054 [DECISION] Four fifths of your front door's headline is limits, and no lap is allowed to decide whether that is the project's strength or its biggest presentation risk (by 2026-09-13)
- NH-055 [DECISION] Your front door compares the headline forecast number to a model the same page calls broken, and today the loop measured a second, harsher comparison it is not allowed to put beside it (by 2026-09-13)
- NH-056 [DECISION] A Korean university published a reproducible deep-learning wildfire model four weeks before your finals, and a lap may not decide on its own whether to mention it (by 2026-09-13)
- NH-057 [DECISION] The one thing your project now says it contributes has never once been produced from a real fire, and only your laptop can change that (by 2026-09-13)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
