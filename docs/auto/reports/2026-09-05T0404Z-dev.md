# WildfireGuardian autoloop · dev · 2026-09-05T0404Z

| | |
|---|---|
| when | 2026-09-05 04:04 UTC · 2026-09-05 13:04 KST |
| branch / head | `auto/dev` / `3452263` |
| kind | dev |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 6, done: 16, parked: 1, todo: 55 |

## What happened this lap

Two rows and one red CI run. The critic's one `fix-before-next-row` item cleared in
twenty minutes, GitHub's own run turned out to be red on a commit that landed while
this lap was reading, and the rest of the lap went to the release bundle DIRECTION.md
names as the next row after it.

## Your decisions, applied

**Nothing new.** The Gmail search (`from:siyeong0318@gmail.com subject:"WildfireGuardian
autoloop" newer_than:14d`) returns 20 threads and every one of them is a single message —
this loop's own report, sent to you. There are no replies, and a search for `subject:Re
WildfireGuardian` in the same window returns nothing. PR #31 has no comments at all. So no
`NH-###:` line reached the loop through either channel, and `decisions_seen.json` is
unchanged. Every open DECISION entry is still open: **NH-005** (Yeongdeok building
footprints), **NH-014** (run the booth recipe once on the real laptop), and **NH-025**
(`Main` cannot follow the green commit until you change one GitHub setting).

*(Unrelated to the decision channel and named only so it is on the record rather than
in a mailbox this loop merely searched: two of the outreach messages you sent on 09-04
have written replies — Prof. Bertozzi, redirecting to a student, and HOT's Asia-Pacific
Open Mapping Hub. This lap did not read them into any artifact, did not act on them and
contacted nobody, per CHARTER §3.6 and NH-010.)*

## What this lap did

### 1. WFG-095 — the booth script's droppable sentences now take their numbers with them

`done(20260905T0325Z)` at `9345848`. Critic #15's one `fix-before-next-row` item.

The **rule** made the mistake, not the two markers, and that is the whole finding.
`docs/auto/DEMO_SCRIPT_5MIN.md` §1 said 「넘치면 그 구간의 **마지막** 문장을 버리고」 — drop
the *last* sentence — and a segment written honestly ends on its caveat, so the marker
landed on caveat-only text in 2막 and 3막 while the claim it guarded stayed in the
non-droppable half. Fixing two markers and leaving that rule in place would have
reproduced the defect in the next segment anyone writes. That was the `hate` objection
against my own plan, and the cheapest test of it was to read the rule and apply it to a
hypothetical new segment ending in a caveat: it produces a wrong marker every time.

So §1's rule changed first — **drop the marked sentence, and a marker only ever goes on a
sentence that carries its own number** — and then the two markers moved:

- **2막:** onto 「그런데 **피해면적 100 ha 이상만 보면 중앙값이 4,025분**…」, a claim+caveat
  pair. Dropping it drops 4,025분.
- **3막:** onto 「영덕에서는 **458곳 중 42곳**, **9.17 %**」, which is also the line whose
  removal retires the 24.73 / 9.17 regional comparison its caveat guards.

Both caveats stay spoken, as each segment's last two sentences, and a new ⚠ block in each
segment names them non-droppable in the idiom 4막 already uses. **No sentence was added or
removed**, so the six segment times still sum to 300 s and R4's tick is not disturbed.

Evidence: `docs/auto/DEMO_SCRIPT_5MIN.md`; `tests/test_demo_script_5min.py` +2 tests
(13 total). Graded against the three mutations that matter — 2막's marker back onto the
sample caveat, 3막's back onto the OSM caveat, and the old last-sentence rule restored —
**all three fail**, and the tree is green with them reverted.

⚠ **The first version of this shipped the defect it documented as hypothetical**, and the
reviewer found it: 2막's marker sat at the *head* of a three-sentence group, so dropping
「the marked sentence」 would plausibly have taken 「그래서 240분은 …**행동의 창**…」 with
it — a caveat on 240분 and 79.23 % that carries no number of its own. §1 now says a marker
sits on **one sentence** and only that sentence goes; 2막's group is split so the marked
sentence ends at its own full stop; and the gate measures the marked **sentence** (marker
to the first sentence end) rather than the marked line.

### 2. A red `auto-gates` run, and it was not this lap's

CHARTER §4b, taken before the backlog row it interrupted. Run **109** (`2b7c3a0`, the
**paper** routine's push, 03:18Z) is red on two tests in
`tests/test_detection_ordering_is_not_claimed.py`. Reproduced at `2b7c3a0` on a clean
checkout before any of this lap's work, so it is not mine.

Both failures are the gate asserting a **census** where it means a **property**. Variant
B's single hit in `paper/manuscript.md` was a sentence inside a `[GAP: …]` marker
(「would let the paper state whether a satellite trigger precedes the call」), and the
paper lap rewrote that marker. The count went 1 → 0 — strictly safer — and an exact-1
assertion turned a safe rewrite into a red branch. The assertion is now on what the
exclusion actually needs: **at most one hit, and any hit must sit inside a GAP marker**,
because a marker states what the paper cannot say. A second hit, or a first outside a
marker, still fails. The design comparison's registered counts go 11/1 → 8/0, which the
test's own docstring names as the case for re-registration; the rule did not move, only
the prose it reads.

Test-only, per §4b. No number, no artifact, no judge-facing prose, and `paper/` is the
paper routine's to write — this lap did not touch the manuscript. `3b174c2`.

⚠ **The first version of this fix had three defects and the reviewer found all three.**
(a) The compensating structural check was a twelve-line *proximity* window, not
containment, so an ordering sentence in the paragraph *after* a GAP marker would have
passed — it is now a real span check (`gap_marker_lines()`, marker to its closing
bracket) with its own three-case test, including that exact middle case. (b) With
`_VARIANT_B_HITS = 0`, the sibling `assert a > b * 4` degenerated to `a > 0` and could
never fail again, while the comment I wrote beside it said the ratio was what carried the
design. That assertion is replaced by a **fixed corpus** — two sentences of ordinary
English that variant A flags and variant B does not, and two real ordering claims variant
B must still flag — which no rewrite of this repository's prose can drain. (c) The new
branch had no mutation proving it fires; it does now.

### 3. WFG-036 v1 — the release bundle

`5fe80e3`. DIRECTION.md's row 2, moved up by critic #15 because §3b forbids a P0 below a
non-P0 and it was sitting under two P1 hygiene rows.

`release/kcf-finals-2026/` holds the four offline screens (`finals`, `console`,
`field_view`, `refuge_placement`), their fonts and poster, `LICENSE`, `CITATION.cff`, a
ten-step Korean `README_KO.md` and `MANIFEST.json`. `make finals-bundle` re-assembles it,
re-derives every SHA-256 and exits non-zero naming any file the committed manifest does
not describe.

**The root objection I recorded against my own plan, and it changed the design:** a
`release/` directory is a second place for `web/` to go stale, which is what CHARTER §3.2
exists to prevent. So **the payload is generated and git-ignored and the manifest is the
committed artifact** — one copy plus a hash instead of two copies. A clean clone holds
`README_KO.md` and `MANIFEST.json` until the command runs once; `README_KO.md` says so in
its last paragraph and `docs/finals_bundle.md` argues the trade instead of leaving it to
be inferred.

⚠ **I first gave a second reason for that choice and it was false** — see the reviewer
section below. It is withdrawn in all four places that carried it, and recorded in
`docs/finals_bundle.md` rather than deleted.

That design also buys the booth something a committed copy would not: a USB stick corrupts
a file silently, and `make finals-bundle` on the laptop says *which* file, which is a
failure with no recovery in front of a judge.

Evidence: `scripts/build_finals_bundle.py`, `Makefile` target `finals-bundle`,
`CITATION.cff`, `release/kcf-finals-2026/README_KO.md`, `release/kcf-finals-2026/MANIFEST.json`
(16 files), `docs/finals_bundle.md`, `tests/test_finals_bundle.py` (7 tests, which hash the
sources independently of the builder's own report).

**R9 is not ticked and this lap does not tick it** (CHARTER §10 — the critic does).
`KCF_READINESS.md` now carries the evidence and both reasons it stays ☐: v1 has **no
printables** (R7 / WFG-007, none exist yet) and **no booth recipe** (WFG-037). It also
records the ignored-payload choice explicitly, so a critic who reads R9 as requiring a
committed payload can say so rather than inherit my reading.

`CITATION.cff` carries no `doi:` and no `date-released:`: no release is tagged and no DOI
minted, and a plausible-looking release date is a fabricated figure. WFG-031's file half is
done and its DOI half is yours, from a browser, after the finals.

## What did not work

- The first `gates.py --mode full` of the lap came back **RED** (2 failed, 1464 passed),
  and the failures were the paper routine's push, not mine. That cost one full-suite run
  (~4 min) and is the reason the CI check at step 2 has to be read against the newest
  `origin/auto/dev` and not the head the lap started from: my quick baseline at `466900b`
  was green, and `2b7c3a0` landed one minute later.
- The first draft of `tests/test_finals_bundle.py` had a syntax error left by a rewritten
  assertion (a stray `, steps` after an `assert`), caught by collection, not by review.
- I wrote a second new test for the booth script this lap and then deleted it rather than
  ship it: asserting that a `[버림]` marker is followed by a sentence end is something
  Korean prose gives for free, so it would have been exactly the vacuous guard the reviewer
  had just caught elsewhere in the same diff.
- The whole reviewer section above is the honest answer to this heading. The most useful
  thing this lap produced may be the withdrawn reason in `docs/finals_bundle.md`: I wrote
  a confident sentence about a gate in this repository without running the gate, three
  hours after clearing a row whose entire subject was a sentence that could not support
  its own claim.

## Reviewed by: subagent (block, then fixed)

`LOOP_CONFIG.json` → `review: subagent`. A fresh agent that did not build this read
`git diff d2418c2..HEAD` and this summary, ran `hate`, `factchk` and `mandela`, and
**blocked**. It was right on every count and nothing here is an argument with it.

**Root objection.** 「The bundle's load-bearing design decision rests on two reasons, and
the second one is false.」 I had written — in `.gitignore`, `docs/finals_bundle.md`, a test
docstring and this report — that a committed copy of the screens would drop a duplicate of
every retired figure into `check_forbidden.py`'s prose scope. `check_forbidden.py:228` is
`is_authored_prose(rel) -> rel.lower().endswith(".md")`. Retired-figure rules never reach
an `.html` file at all, `web/finals.html` is already tracked and already green, and a copy
would have added **exactly zero** findings. It was a claim about this repository's own gate
asserted without running the gate, in a lap that had just run it — CHARTER §3.5's failure
class, in a lap whose entire first row was about not writing what you cannot support.
Withdrawn in all four places, recorded rather than deleted in `docs/finals_bundle.md`, and
§3.2 now carries the decision alone, which is what it can support.

**First nail** (its words): `grep -n -A3 'def is_authored_prose' scripts/check_forbidden.py`.
I ran it. It is `.md`-only.

**Second blocking item.** The commit I was about to push had not been through a full gate:
`.auto/gates.json` recorded `3b174c2` while `HEAD` was `5fe80e3`, 589 lines including a new
180-line script and 131 lines of new tests, and this report's gate table read as a finished
green lap without saying so. That is CHARTER §8's named failure, twice over in the loop's
own history. `--mode full` has since been re-run at the real HEAD and the gate table below
names it.

**Three further defects**, all recorded in place above: the GAP proximity window, the
degenerate `a > b * 4`, and 2막's marker at the head of a three-sentence group.

What it cleared: `make verify` green at HEAD, no committed artifact touched, no deletion,
nothing pushed to `Main`, no `git add -A`, `CITATION.cff` valid CFF 1.2.0 with the correct
absence of `doi:`/`date-released:`, the six segment times summing to 300 s, the new ⚠ prose
**not** asserting the withdrawn detection-ordering claim, and the project frame intact.

## Gate results

- Baseline `--mode quick` at `466900b`: **ALL GREEN**.
- `--mode full` at `f79a990`: **RED** — the paper routine's two failures, above.
- `--mode full` at `3b174c2`: **ALL GREEN**, `1479 passed, 56 skipped` in 248.7 s. This is
  the run the reviewer objected to being reported as the lap's: it is not HEAD.
- `--mode full` at `3452263`, **which is the commit this lap pushes**: **ALL GREEN**,
  `1481 passed, 56 skipped` in 232.4 s. WARM (the SRTM-gated tests ran, so the skip count
  is 56 rather than the cold 62). `--assert-head` and `--assert-reported` exit 0 before
  the push.
- `verify`, `snapshot-verify`, `env-check` PASS; `baseline-verify` WARN, expected
  off-laptop, `hard: false`.
- `make finals-bundle` exits 0 on an unchanged tree; a second run reproduces the manifest.

## Next row

**WFG-037** (P0, `docs/auto/finals/BOOTH_SETUP.md`, R3's booth half and R12's precondition),
then **WFG-007** (P1, the printables that WFG-036 v2 needs). DIRECTION.md's own falsifiable
test for critic #16 was 「if the next lap produces `release/kcf-finals-2026/` or
`BOOTH_SETUP.md`, this page is steering」 — the first of those is now on disk.

## In plain terms

- **What changed for the project.** Your five-minute booth script used to tell you to cut
  the wrong sentences when you run long: in two of the six segments, the sentence marked
  「버림」 was the honest warning, not the number it was warning about, so cutting it would
  have left you saying a figure with nothing qualifying it. That is fixed, and so is the
  rule that caused it. You also now have `release/kcf-finals-2026/`, the folder that goes
  on the USB stick, with a ten-step Korean run sheet and a checksum file that tells you if
  the stick has quietly corrupted a file.
- **Why it matters to the judges or the science.** The two sentences I protected are
  exactly the ones that keep this project out of a claim it withdrew in writing, and a
  judge who hears the number without the caveat hears a different project. The bundle is
  the difference between "the code is on GitHub" and "here is the thing, it runs offline,
  and I can prove nothing in it changed since I built it".
- **What you should do, if anything.** Nothing urgent. Two things are still waiting on
  you and neither is new: **NH-025**, one GitHub setting so `Main` can follow the last
  green commit, and **NH-014**, running the bundle once on the actual booth laptop — that
  one is the only thing no test here can stand in for, and it is the last box on the
  readiness checklist that only you can tick.



## Gates

**ALL GREEN** · mode `full` · head `3452263` · 2026-09-05T04:04:08Z · current at `3452263`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 17.1 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.1 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 0.5 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 232.4 s | 1481 passed, 56 skipped, 52 warnings in 230.89s (0:03:50) |

## Commits since the previous report (466900b38eb0b019b1cc130b35098984d2471d4b..HEAD)

- 3452263 the reviewer blocked and was right five times, and the first one was a claim about this repository's own gate
- 5fe80e3 WFG-036 v1: the folder that goes on the USB stick, and a manifest that says when it stopped being that folder
- 3b174c2 ci-red: the ordering gate pinned a count of the paper's prose, and the paper moved
- f79a990 claim WFG-036 v1 (20260905T0355Z)
- 9345848 WFG-095: the sentences the booth script tells the student to drop first now take their own numbers with them
- d2418c2 claim WFG-095 (20260905T0325Z)
- 2b7c3a0 paper: the trim that kept every number and dropped two of their caveats

## Needs a human (5 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-025 [DECISION] `Main` cannot follow the green commit until you change one GitHub setting, and until you do, every green push emails you a red run (by 2026-09-08)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
