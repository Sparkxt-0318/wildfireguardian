# WildfireGuardian autoloop · paper · 2026-09-06T1547Z

| | |
|---|---|
| when | 2026-09-06 15:07 UTC · 2026-09-07 00:07 KST |
| branch / head | `auto/dev` / `39269e3` |
| kind | manual |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 7, done: 16, parked: 1, todo: 74 |

## What happened this lap

# Paper lap 12 — a §3.5 sentence had gone false; the repair overclaimed and the reviewer blocked it

**Incorporated:** `9c2b903..39269e3` (5 dev/critic laps, 5 new reports). **Body 8,825 →
8,969 words. 23 pages, measured. 7 gaps, unchanged. 8 referenced figures, 4 tables, 27
references, all unchanged.**

**Reviewed by: subagent (block → fixed): the rewritten §3.5 stated, one sentence after
stating the opposite standard, three things about the new gate that the tree does not
support and in one case contradicts.** All three were verified against the code and fixed
before this push; no argument was made and nothing was parked.

## What changed in the manuscript, and why it was not optional

One paragraph: the tail of **§3.5** (Controls, sweeps and the evidence registry). It ended

> The injected line itself stays outside that test, and a wrong value inside it still
> passes every gate named here; that hole is open and recorded rather than repaired.

and inside this lap's window **WFG-113 narrowed it** (`1ec1d06`). The manuscript's own rule
runs in this direction too: a limitation that no longer holds is a fabricated limitation
(CHARTER §3.5), and a paper still claiming an open hole after it is closed is wrong in the
flattering direction, which is the worse one here. So the correction was mandatory.

§3.5 now says: a second test re-runs the builder and compares the payload it emits against
the payload embedded in the shipped file, field by field, **exempting the build's own
stamps and the integrity panel, which a companion test checks against the builder's list of
gates**; that checking instead against a string the test's author typed would check the
author rather than the artifact; that **its value is prospective**, the defect that prompted
it having been found by hand and the test shown to catch it only by replaying the pre-repair
payload; and that **its oracle is the builder**, so it certifies that the shipped file is
what the builder emits today and never that what the builder emits is right — one provenance
field inside the same payload being stale by construction and passing it (WFG-115, open).

## The reviewer's block, in full, because the lap earned it

The first draft claimed 「exempting only stamps of the build itself」, 「It caught a live
defect at once」, and carried none of the four limits `docs/finals_screen_v2.md` §4.3
discloses. Every gate in this repository was green while those sentences stood. Verified
here before rewriting, not taken on the reviewer's word:

1. `tests/test_finals_payload_rederives.py:117` — `if not path and key == "integrity":
   continue`. The comparator skips the **whole** integrity object, so `verified`, each
   gate's `ok` and each gate's `line` are invisible to it, and the `PROVENANCE_GATE`/
   `seconds` branch one line below cannot execute for it. The companion test checks the
   gate names against the builder's `GATES`, `ok is True` and a non-empty `line` — a
   fabricated non-empty line passes.
2. `docs/auto/reports/2026-09-06T1000Z-dev.md:119` — that lap's own reviewer: 「it was green
   at HEAD by construction, because the same lap rebuilt web/finals.html 13 minutes before
   writing the test」. Critic #25 found the stale screen by hand; the repair shipped first
   and the gate after, in one commit.
3. The deleted sentence is **still true** of `registry.built_at_commit`, which
   `build_finals.py` re-reads from the registry on every rebuild, so the new gate passes it
   by construction. §4.3 says so outright. That is WFG-115, open.

**What paid for the repair.** At 31 words of margin there was no room, and CHARTER §12
fixes the ordering: a caveat outranks a new rule. So the draft's closing sentence — the
WFG-117 rule about gating a claim rather than a moving count — **was deleted from the
manuscript** and kept in `paper/GAPS.md` for a later lap with words to spare. No caveat was
traded.

**And the lap broke that same rule one file away.** The first `GAPS.md` note printed the
current sha256 of `docs/auto/JUDGE_QA.md` beside the stale one in the printables manifest;
the reviewer found that digest had **already moved inside this lap** (critic #27's WFG-133
edit). The mismatch is what is load-bearing and it survives; the moving digest is gone and
`sha256sum` is named instead.

## Numbers: none added to the manuscript, and that is now the position

`docs/NUMBERS.json` did not move between `9c2b903` and `39269e3`, so nothing registered
changed under the manuscript. The manuscript quotes **no** registry count anywhere, on
purpose: the registry's entry count changed 44 times across 45 distinct values between
2026-08-01 and 2026-09-05 (a measurement of this repository's git history by the WFG-117
dev lap, not a registry value — which is why it stays in `GAPS.md`).

The reviewer also corrected one number **outside** the paper: NH-037's option A had read
9,800 words, an interpolation on a 500-word sampling that `paper/README.md` explicitly
forbids reading as a rate. It now names the measured sample point instead — 9,461 words,
which measured 24 pages — and says `calibrate_pages.py` is what would measure anything else.

## Figures: none added, none redrawn, and the reason

All nine `FIGURES` entries re-rendered **byte-identically** from the committed artifacts
(`git status --short paper/figures/` empty after a full `make_figures.py` run), so this
sandbox reproduced the committed set exactly. Nothing was added: the new material is
methodological and sits in §3, which carries no figure of its own, and no result section
gained a result needing one. `F9_present_perimeter` stays drawn, committed and deliberately
unreferenced (G8 / NH-032), so it still costs no page.

## Gaps

Count unchanged at **7**; none opened, none closed. `paper/GAPS.md` gained the lap-12 record
above, the WFG-117 rule as recorded-not-shipped, and one live defect kept **out** of the
manuscript: `docs/printables.md` claimed the booth print kit's staleness 「can be checked
mechanically」 because its manifest records its four sources' sha256, but nothing compares
those digests to the tree — `tests/test_printables.py` checks the manifest against itself.
The committed booth PDF is now older than the Q&A card it was built from and every
printables test is green. Same shape as the hole §3.5 records as narrowed; outside CHARTER
§12's paths and not a claim this manuscript makes, so it stays on **WFG-130**.

## The one escalation: NH-037

The author's rule is 25 pages; the document measures **23**. The proxy standing in for it is
9,000 body words; the document is at **8,969**. Two pages of margin against **31 words**,
and `paper/README.md`'s curve says the document is 23 pages at 9,000 words by either route —
so the proxy stops a lap roughly a thousand words before the author's own rule.

This lap is the evidence. It was squeezed twice: once tightening its own new prose by 27
words (adjectives only), and once paying for a reviewer-required caveat by deleting a rule
from the manuscript. No caveat was traded, and it will not be — but content is now being
lost to a limit that is not the rule the author set. Options: (A) raise the proxy to a
measured sample point, 9,461 words = 24 pages; (B) land WFG-116's open half — one `apt` line
in `.github/workflows/auto-gates.yml` — so a clean clone measures and the proxy stops being
load-bearing; (C) both; (D) leave it. Until answered the loop keeps the proxy: it is the
author's number and a lap does not raise its own ceiling.

## Measurement, taken rather than inherited

23 pages under Carlito, page objects and page-tree `/Count` agreeing, on this lap's own
build, after the one `apt-get install libreoffice-writer fonts-crosextra-carlito
fonts-nanum` that `paper/README.md` records. The 144 words this lap added bought no page.
`STATE.json` carries the digest `2c7d471ed7a006ec`, refreshed by a run that actually
measured. ⚠ **WFG-116's open half is still the half that re-derives**: a clean clone still
cannot measure, and that is option B above.

## Gates

`scripts/auto/gates.py --mode full` **ALL GREEN**, read from its exit code, never piped —
re-run on the exact commit pushed, with `--assert-head` and `--assert-reported` after it.
`paper/check_paper.py` exits **0** with the page branch actually gating (`metrics_ok: true`,
Carlito) rather than falling back to the proxy.

## In plain terms

- **What the paper now says:** the same three claims as yesterday, with one paragraph
  corrected. §3.5 had said a gap in your gates was still open; your dev lap closed most of
  it on 2026-09-06, so the paper now says what the new check does, what it does **not** do
  (it trusts the builder, and one field on the judged screen is stale by construction and
  passes it), and that its value is for next time rather than a catch it already made. No
  number, figure or claim moved.
- **What is still missing:** the same seven gaps — the big two still need your laptop's raw
  data (the leak-free Yeongdeok fold and the hindsight-field check), and §4.5 still quotes
  no count because **NH-032** is unanswered.
- **What you should do:** answer **NH-037**. The paper is 23 pages against your 25-page
  rule, but the word limit standing in for that rule is a thousand words tighter, and today
  it cost the manuscript a real sentence. Option B (one line in the CI workflow so every
  machine counts pages) is the fix that lasts; A is the quick one. **NH-032** is still the
  thing holding a real result out of §4.5.



## Gates

**ALL GREEN** · mode `full` · head `39269e3` · 2026-09-06T15:00:50Z · current at `39269e3`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 17.6 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.2 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 1.3 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 286.0 s | 1569 passed, 62 skipped, 52 warnings in 284.59s (0:04:44) |

## Commits since the previous report (dd500e61df17982b29351eb395bc88005e5eb744..HEAD)

- 39269e3 the font gate caught this lap's own correction note before the printer would have
- a64b904 critic #27: the withdrawal reached every page the loop reads and stopped at the card the student reads aloud

## Needs a human (10 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09)
- NH-036 [DECISION] One critic lap told the next one not to edit a file, and that is what kept a false sentence in front of a judge for a window (by 2026-09-10)
- NH-037 [DECISION] The paper's word proxy now stops it a thousand words before your 25-page rule (by 2026-09-10)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
