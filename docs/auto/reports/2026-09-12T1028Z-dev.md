# WildfireGuardian autoloop · dev · 2026-09-12T1028Z

| | |
|---|---|
| when | 2026-09-12 10:28 UTC · 2026-09-12 19:28 KST |
| branch / head | `auto/dev` / `04132ca`, **rebased onto `b972924` before the push** |
| rebase | ⚠ The paper routine pushed `c9ffedb` and `b972924` while this lap was running. The four work commits replayed cleanly; only the report commit conflicted, and only on regenerated report machinery (`STATE.json`, `dashboard.html`, `images/latest/*`). Resolved by taking this lap's freshly built copies — nothing was forced, no history was rewritten on a shared branch, and the paper lap's two commits are untouched beneath these four. **`gates.py --mode full` was then re-run on the REBASED head and `--assert-head` and `--assert-reported` both re-run green immediately before the push**, so the gate table below, which names `04132ca`, describes the same tree at its pre-rebase hash. |
| kind | dev |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 10, done: 37, dropped: 1, parked: 1, title-derivable: 1, todo: 129 |

## What happened this lap

## Your decisions, applied

**Nothing new on either channel, and that is now seven days.** The Gmail search
`from:siyeong0318@gmail.com subject:"WildfireGuardian autoloop" newer_than:14d` returns 25
threads, each carrying exactly one message, and every one of them is labelled `SENT` — the
loop's own reports. PR #31's comment list is empty. `docs/auto/decisions_seen.json` still
records `"seen": []`, and the newest applied decision anywhere is **NH-031, received
2026-09-06**. No decision has ever arrived on the email channel. That is **WFG-211**,
already `todo`, confirmed here and not re-filed.

⚠ The consequence has not changed and is worth one line, because the sprint ends
**2026-09-15**: `docs/auto/KCF_READINESS.md` stands at **8 of 11**, and CHARTER §14b
releases the whole P1 block when R1, R3, R4, R7, R8 and R9 tick. Five of those six tick.
**R3 is `blocked(NH-046)`**, NH-046 came due 2026-09-10, and R11's WFG-024 waits behind it.
One unanswered question holds the ninth tick and the P1 block.

---

## What this lap did

Baseline `gates.py --mode quick` exit **0**. GitHub's own run is green: `auto-gates` run
**405** concluded `success` at `f448bdd`, so CHARTER §4b set no finding #1. (Runs 403 and
404 show `cancelled`, which is the queue superseding them on a newer push, not a failure.)

### The preemption first — WFG-270, and its own price was measured wrong

Critic #73's one `fix-before-next-row` item was `README.md:25` and `:798`, both reading
「**2** have no safe walking route **at all**」. The bucket's condition is
`nv.enters_hazard and not fa.reached`: the fire-blind route reached a refuge through the
predicted hazard, and the forecast-aware search **terminated without reaching one**. A
search that fails to find a route establishes nothing about whether one is there. Repaired
to 「reach no refuge under that policy」 and 「reach no refuge under it」, the register the
Abstract and all three repaired legends already use. The count **2** did not move, the
bullet was not re-ordered, the 2025-fire opening paragraph and the Round-2 section were not
touched. **NH-054 compliance is measured, not asserted**: splitting the bullet at its first ⚠, the result half is **435 characters at both heads** and the whole bullet **2,479 at both**. `:25` is **54 characters before and after — it loses nothing**; `:798` loses 9 and is not in the lead. The proportion NH-054 froze did not move by one character.

⚠⚠ **The critic certified this as 「exactly one gated file」 and 「green with no per-line
pragma anywhere」. Both are false at `4bf34ab`, the head they were measured at.** The
certifying grep was contiguous. The README shipped the phrase unbolded; every document that
**quoted** it bolded the emphasis, as `route **at all**`, and a contiguous grep cannot see
that. Three further gated files carried it — `docs/auto/JUDGE_QA_PENDING.md`,
`paper/GAPS.md`, `paper/README.md` — and all three needed the pragma the certification said
nothing would need. All three are record lines in non-record files and are licensed per
CHARTER §3.5c. Registered as **`WC-021`** with a `\*{0,2}` tolerance in the pattern.

**The tolerance was already written down, in the file being edited.** `WC-020`, six laps
earlier, states it twice in its own `why` fields: 「a pattern without it catches only half
the sites — which is the WFG-138 shape this registry exists to stop.」 `check_withdrawn_claims.py`
is green over **944** gated files of **1,195** tracked at `4dd7c64`. The class name 「no safe route」 was
**not** registered, as the critic directed; that question is now **WFG-274**.

### The row — WFG-267, dispatch-sheet staleness

**Root objection, recorded before building (the `hate` pass).** The row hands over a
finished remedy and asks 「**which of the three** committed PDFs carries it」. That phrasing
pre-commits the answer to a three-item universe scoped to one run directory. The cheapest
test was one command — list every tracked PDF under `outputs/` — and it does not agree:

| | |
|---|---|
| `dss_committed_dispatch_pdfs` | **38** committed dispatch PDFs |
| `dss_run_dirs_with_a_committed_pdf` | across **12** run directories |
| `dss_stale_committed_pdfs` | of which **3** carry the superseded 사유 |

The three are `outputs/dispatch/20260801T163042Z/02-천전공원-일대/dispatch_a4.pdf` — the one
the row found — **plus two more** under
`outputs/dispatch_full/20260801T183522Z/03-영덕해맞이공원-일대`, in a directory the row never
mentions. Answering the row as written would have missed them.

**(i)** `outputs/dispatch/README.md` gains a section naming the 2026-09-12 repair, both
constants with their sentences, and all three paths. **(ii)** `docs/auto/JUDGE_QA_PENDING.md`
**P-005** drafts Q39's replacement — one instruction, *regenerate every sheet you print* —
plus what to say to a judge already holding a mixed stack; drafted and **not merged**,
because NH-049 is open and `JUDGE_QA.md` is one of the seven printable sources.
`JUDGE_QA_PENDING.md` is not, so nothing needed reprinting. P-004's ⚠ 「no committed
document says this」 note is discharged and its 「한 장」 narrowed. **(iii)**
`tests/test_dispatch_sheet_staleness.py`, **eight** tests, graded RED against **seven**
mutations and green once each was undone; it carries **no** enumeration and re-derives from
the tree by three independent routes, failing in both directions. **(iv)** No committed sheet was rewritten and nothing was
regenerated.

⚠⚠ **The caveat as first written was a non-sequitur, and the reviewer broke it.** It read
「there is no `pypdf`, `pdfminer` or `pdftotext` here, **so** each PDF is classed by its
sibling HTML」. The premise is true — ten libraries and binaries checked, all absent — and
the **so** is false. These sheets embed a Korean font **subset**, and
`scripts/probe_dispatch_pdf_fonts.py` (standard library only) recovers it from each PDF's
`/ToUnicode` CMaps with `zlib`. Across all **38** committed PDFs, exactly the **3** classed
stale can spell the superseded 사유 and **none** can spell the current one. Two routes
sharing only the repository, same answer. It **corroborates** rather than replaces: a
subset says what a page *can* spell, not in what order — that last step stays WFG-116-shaped
debt.

### What the `sip` pass caught, in my own work

Two things, and the first is the exact defect critic #73 filed F1 about — introduced by the
lap that was reading about it.

1. **The first draft gated only one of the two pages that carry the list.** The same
   enumeration sits in `docs/dispatch_sheet_staleness.md` §3, and nothing bound it, so it
   could have gone stale in silence. Both pages are bound now; mutations 6 and 7 are the
   proof.
2. **§3 called `dss_html_carrying_current` a 「대조군」.** It is not one: that zero comes
   from the **same scan** as the count it was said to control, so it rules out the tree
   holding two live sentences and rules out nothing about the scan itself. The word is
   struck and the limit is stated.

### What did not work

My own ad-hoc `git ls-files` enumeration, written to measure the registry's scope, returned
**550** of the real **1,194** tracked `.md`/`.html` at `2f05e12`. I would have published that number if I
had not re-run the count through `check_withdrawn_claims.py`'s own `tracked_files()`. And
the first draft of **WFG-274** listed eight `no safe route` lines copied from
`paper/GAPS.md`; re-running the grep before committing showed one of them
(`docs/routing_limitations.md:163`) **does not contain the string**, and the real count over
the gated set is **17**, three of which assert existence in so many words. The row now
carries the derived list. Both were caught by DIRECTION's newest rule — re-run every
measurement you wrote down, against the tree you are about to commit — and both were caught
at the end, not at the start.

### Gates and review

`gates.py --mode full` exits **0** at `04132ca` (2233 passed, 65 skipped, 3 xfailed); `baseline-verify`
is the usual sandbox WARN for two absent `data/raw/` manifests (CHARTER §3d). Adding six
registry keys required `make finals` and `make finals-bundle UPDATE=1`; only
`web/finals.html` drifted in the bundle, the printables kit is untouched, so NH-049 is not
engaged. `docs/artifact_manifest.json` was rebuilt by `scripts/build_artifact_manifest.py`
and `docs/arm_protocol.json` declares the census as a control-free arm.

### Reviewed by: subagent (**block**, fixed, not argued)

**Root objection:** 「The lap's central act was convicting critic #73 of certifying a count
that was true when grepped and false when shipped — and it then shipped its own
certification with exactly that defect, in the same commit, about the same repair.」 It was
right, and the first nail was one command: `git ls-files` at HEAD returns **1,195**, not the
**1,194** I had written, because *my own new gated file* moved it. Neither number was
registered, so §3.3 was broken and no gate could see it. It also measured my NH-054
compliance clause false in all four parts.

**Three fixes, all applied, none argued with.** (1) Both counts corrected and **stamped with
the commit they were measured at**, in both rows the wrong pair had already propagated to.
(2) The NH-054 clause replaced with the measurement above. (3) `mandela`'s tautology
pattern: every test scored the note with the same `measure()` that wrote it, so a wrong glob
would have made artifact, pages, registry and gate wrong *together*. Two independent routes
added — one importing none of the measurement code, one never reading an HTML at all.

⚠ **What survived, in the reviewer's own words:** every registered number is independently
correct, the three named stale paths are 「the correct AND complete set」, all four clauses of
WFG-267 verified true, the WC-021 registration satisfies §3.5c, and 「the critic was indeed
wrong」 — reproduced at `4bf34ab`. It also re-graded my gate by adding a fabricated stale
PDF/HTML pair to the tree and confirmed it goes red, a mutation I had not run.

**Next row:** **WFG-256** (P0, science) — the rotation null, still the only thing that would
license a sentence about 「모양」 — then WFG-255. WFG-274 is filed P1 and waits.

## In plain terms

- **What changed for the project.** Your booth stack now has a page that says exactly which
  printed sheets carry the old wording — and it turned out to be three sheets, not the one
  the task assumed. The front page of the project also stopped claiming that two homes have
  no safe walking route *at all*, which is more than our own search can show.
- **Why it matters to the judges or the science.** Both are the same promise: we say what we
  measured, never more. A judge can now hand you two sheets that disagree and you have a
  file with the answer, and a test that keeps that file honest as the project changes.
- **What you should do.** One thing, and it is the same as yesterday: **NH-046 is two days
  past due and is the last item holding the readiness checklist at 8 of 11, three days
  before the sprint ends.** Reply to this email with a line beginning `NH-046:` and your
  choice.



## Gates

**ALL GREEN** · mode `full` · head `04132ca` · 2026-09-12T10:28:00Z · current at `04132ca`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 23.9 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.2 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 0.4 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 381.6 s | 2233 passed, 65 skipped, 3 xfailed, 52 warnings in 380.52s (0:06:20) |

## Commits since the previous report (43bbfd83d64bc2886d8c38913c0099fd7b52ef6f..HEAD)

- 04132ca "no extractor, so we did not read the PDFs" was an assumption wearing a measurement's clothes
- 4dd7c64 the second page carrying the same list is gated too, and a count from one scan is not a control
- 9370428 the booth stack's stale page has a name, and there are three of them rather than one of three
- 2f05e12 claim WFG-267 (20260912T0920Z)
- f448bdd the lap that pushed past a red --assert-reported reports it, and the re-stamped report is the remedy the gate itself printed

## Needs a human (32 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09, one day past; raised to HIGH by critic #55 on a measured third instance)
- NH-036 [DECISION] One critic lap told the next one not to edit a file, and that is what kept a false sentence in front of a judge for a window (by 2026-09-10)
- NH-037 [DECISION] The paper's word proxy now stops it a thousand words before your 25-page rule (by 2026-09-10)
- NH-038 [DECISION] Your "product first" rule has spent the last three dev laps on documents, and the readiness line it was written to protect has not moved in five critic laps (by 2026-09-09)
- NH-039 [DECISION] The national wildfire-spread system's manual is an 18 MB PDF the sandbox could not fetch, and one of you can (by 2026-09-12)
- NH-040 [FYI] A critic lap pushed one commit past a red `--assert-reported`, and it is telling you rather than hiding it
- NH-041 [FYI] This lap sent you an email containing only the word PLACEHOLDER, and could not take it back
- NH-042 [DECISION] Two of your own rules collide whenever a withdrawn claim lives in a frozen artifact, and this week they collided three times (by 2026-09-10)
- NH-043 [DECISION] A gate your loop built this morning will go red about twice a day, and the charter tells the lap that meets it to stop working (by 2026-09-09)
- NH-045 [BLOCKER] The staleness gate has closed `auto/dev` to every routine, and the one routine that met it is the one forbidden to clear it (by 2026-09-08)
- NH-044 [DECISION] The claim the paper just retracted is still live on the page the paper cites for it (by 2026-09-09)
- NH-046 [DECISION] Your product's definition-of-done names a command nothing in this project has ever run (by 2026-09-10)
- NH-048 [DECISION] One of the research routine's three literature channels has been dead for two runs, and a working replacement is already proven (by 2026-09-10)
- NH-049 [DECISION] Your critic routine is told to add judge Q&A cards and your own printing gate makes that impossible for it (by 2026-09-11)
- NH-050 [DECISION] You answered two of these questions two days ago and the loop never heard you, because you answered them on the routine page (by 2026-09-10)
- NH-051 [DECISION] The rule your loop has been obeying for three days is not the option it names, and the difference is why one row has been pushed down five times (by 2026-09-11)
- NH-052 [DECISION] The experiment that measures what your own forecast is worth is now one run away, and it would change what 42 means (by 2026-09-12)
- NH-053 [DECISION] The word your front door uses for 42 stopped being right today, and the loop cannot pick its replacement (by 2026-09-12)
- NH-054 [DECISION] Four fifths of your front door's headline is limits, and no lap is allowed to decide whether that is the project's strength or its biggest presentation risk (by 2026-09-13)
- NH-055 [DECISION] Your front door compares the headline forecast number to a model the same page calls broken, and today the loop measured a second, harsher comparison it is not allowed to put beside it (by 2026-09-13)
- NH-056 [DECISION] A Korean university published a reproducible deep-learning wildfire model four weeks before your finals, and a lap may not decide on its own whether to mention it (by 2026-09-13)
- NH-057 [DECISION] The one thing your project now says it contributes has never once been produced from a real fire, and only your laptop can change that (by 2026-09-13)
- NH-058 [DECISION] Two of your routines built the same row in the same window, and the rule that was supposed to stop that cannot see the paper routine (by 2026-09-13)
- NH-059 [DECISION] Your loop measured the hardest question a judge can ask you, and the answer is uncomfortable enough that no lap may decide on its own whether you say it out loud (by 2026-09-13)
- NH-060 [DECISION] Your "product first" rule sent a booth-printable defect to the bottom of the board, and two of your routines read the same sentence of it differently (by 2026-09-14)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
