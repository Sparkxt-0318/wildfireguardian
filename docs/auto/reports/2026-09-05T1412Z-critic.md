# WildfireGuardian autoloop · critic · 2026-09-05T1412Z

| | |
|---|---|
| when | 2026-09-05 14:12 UTC · 2026-09-05 23:12 KST |
| branch / head | `auto/dev` / `92bfc4f` |
| kind | critic |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 6, done: 16, parked: 1, todo: 65 |

## What happened this lap

Critic lap #19. Window `e2628f3..92bfc4f` on `auto/dev`, 5 commits, 1,141 authored
insertions (images, the `.docx` and the generated board excluded). No code, data,
figure or `docs/NUMBERS.json` value was changed by this lap; it writes only under
`docs/auto/`.

## Your decisions, applied

**Nothing new this lap.** The Gmail search `from:siyeong0318@gmail.com
subject:"WildfireGuardian autoloop" newer_than:14d` returns 25 threads and every one of
them is a report the loop itself sent; each thread holds exactly one message, so there is
no reply to parse. `docs/auto/decisions_seen.json` is unchanged (16 applied, all from the
two Claude Code sessions on the laptop, none by email). PR #31 has no comments at all.
The email decision channel has still never been exercised; the laptop-session channel
(NH-020) has, twice, and it works.

**Seven decisions are open and waiting:** NH-005, NH-014, NH-025, NH-026, NH-027, NH-028,
NH-029. Two of them are now on the critical path rather than in the queue, and they are
NH-029 (new this window) and NH-027.

## fix-before-next-row

**WFG-109.** `scripts/finals.template.html:1378` (KO) and `:1381` (EN) still carry the
STATIC VIEW sentence WFG-103 withdrew, while the built `web/finals.html:1378`/`:1381`
carry the correction. I diffed both files at this head. Nothing is wrong on the judged
screen today; the next `make finals` reverts it in front of five judges, no gate reads the
template, and the only thing in the way is a paragraph of `BOOTH_SETUP.md` §3.4 asking a
student not to run a command. Minutes of work.

One lap ago critic #18 set no item and proved that the empty block moves the product: the
file it was withholding the lever for, `docs/auto/finals/BOOTH_SETUP.md`, was written the
very next lap. That rule now stands in `DIRECTION.md`. I set an item anyway, because
readiness did not stall for want of direction this window, and because this is the first
critic item in nine that is not another sentence of `docs/auto/DEMO_SCRIPT_5MIN.md`.

## Findings, ranked

Full text in `docs/auto/CRITIC_LATEST.md`; the short form:

1. **F1 · WFG-109** (P0, judge-facing) — above. Filed by the WFG-037 lap, verified here on
   disk rather than read from its report.
2. **F2 · WFG-106 corrected before a lap writes a card on it.** Critic #18 filed it as
   「the number that answers the strongest objection outright」. It names the wrong
   objection. There are two: **dilution** (91 of 368 includes 265 who were never at risk)
   and **the opponent** (you compared against a router that never looks at the fire).
   `mr_uiseong_fa_rescue_rate` answers the first and *cannot* answer the second, because
   its denominator is defined by the arm in dispute — the derivation string says 「of the
   origins whose FIRE-BLIND route is unsafe」. A card offering it as the answer to the
   second would overclaim worse than the sentence WFG-103 withdrew yesterday. Row updated,
   not duplicated. ⚠ And its caveat is wrong on two keys, not one: all three
   `mr_*_fa_rescue_rate` entries carry a byte-identical 「SMALL denominator (13-20
   origins)」 against denominators of 13, 44 and 103.
3. **F3 · WFG-110** (P0, new) — **R1 has been unticked for nineteen critic laps on a
   condition nobody had ever sized, and it is six table rows wide.** `DEMO_SCRIPT_5MIN.md`
   §3 is a mapping table but runs script → key; R1 asks screen → key. Measured with the
   repository's own criterion: `scripts/finals.template.html` references 28 registry keys,
   §3 names 22, six are in no committed table.
4. **F4 · the direction rule fires a fourth time and is measuring the wrong thing.**
   Readiness 4 of 11, unmoved since critic #16 — but this window the loop built exactly the
   artifact the last three critics named. R7 and R9 wait on **the printables** (WFG-007,
   P1, never claimed); R3 waits on **you** (NH-029). `DIRECTION.md` carries a falsifiable
   test for critic #20: if WFG-109 closes and the printables still do not exist, WFG-007's
   priority is the defect and it should be raised to P0.
5. **F5 · NH-029 is the best thing this window produced and the lap claimed no credit for
   it.** Writing the recipe forced someone to actually run `make all-checks`, the command
   R3 has named for nineteen laps. It fails, and only two of six differences are the
   sandbox; the other four are in every clone. I ran `make baseline-verify` here rather
   than quoting the lap and got the same six lines. Eighteen critic laps, mine among them,
   wrote 「WARN, expected off-laptop」 and read past four differences that are not.
6. **F6 · WFG-111** (P1, new) — R5 promises every T0 answer cites a file and nothing checks
   the file is there. All 75 path-shaped citations in `JUDGE_QA.md` resolved from the root:
   three do not, all bare names whose full path is given elsewhere in the same file.
   Nothing is wrong today; what is missing is the gate.
7. **F7 · checked and NOT findings** (so no later lap re-derives them): both code citations
   in `BOOTH_SETUP.md` resolve exactly; the Korean fonts really are in the bundle;
   `make finals-bundle` and `check_bundle_copy.py` both green at 17 files; the window's one
   demo-script change is correct; the struck docstring sentence in
   `scripts/build_finals_bundle.py` is withdrawn **in writing** with the measurement that
   falsified it.
8. **F8 · loop hygiene, clean.** `auto-gates` 111-130 on `auto/dev`: **no `failure` in the
   window**, 130 at this head `success`, three runs cancelled by superseding pushes.
   Fifteen of fifteen pushes pass `--assert-reported`. Every dev, paper and manual report
   carries `Reviewed by:`. `gates.py --mode full` exits 0 here at `92bfc4f`
   (`1506 passed, 62 skipped`, 197.4 s, cold; +22 like for like over critic #17's cold run).

## Direction check (CHARTER §14, five minutes)

**One row moved: WFG-106 now sits above WFG-104.** Both are P0, so no §3b rule was bent
either way. The reason is the one critic #17 used for the same kind of move: this page has
said since critic #18 that WFG-106 is the cheaper and stronger of the two, and the table
said the opposite, so a dev lap reading table order and a dev lap reading `DIRECTION.md`
would have claimed different rows.

**Readiness lines ticked in the last 24 h: zero, for the fourth consecutive critic lap.**
That fires the rule, and F4 is the reading.

## Root objection (hate), unchanged and now sharper

**The headline credits the forecast with what merely seeing the present fire would have
bought.** Every comparison the project ships is against `naive`, which is fire-blind
(`src/wildfireguardian/routing/evacuation.py:270`). Critic #18's proposed answer does not
close it, for the reason in F2. The cheapest test is still `paper/GAPS.md` G7: slice-0
hazard mask as a node filter, the existing `naive_route`, only the origins that enter the
hazard, every input already committed, about one lap and no new data. It is your call
(NH-027), open since 09-05 with four options.

## Scorecard deltas

**Track B 84, unchanged** (16 / 15 / 19 / 15 / 19). 설계와 방법론 has not moved in nineteen
laps and what caps it is the fire-blind opponent above, not a document. 데이터
수집·분석·해석 is the row I most wanted to move on NH-029, and did not: the score is the
state, not the loop's self-knowledge, and the tree is no more reproducible today than
yesterday, only more honestly described.

**Track A 78 to 79**, on 구현 및 유용성 17 to 18 — the first row to move in three laps, and
it is the booth. Why +1 and not +2: the failure ladder's third step is paper and there is
no paper, `make all-checks` cannot be run anywhere, and `make finals` would revert a
withdrawn sentence onto the judged screen.

## In plain terms

**What the judges would see today.** For the first time, a booth that a person could
actually run. There is now a written setup procedure in Korean for the morning of 10-24:
what to do the night before, the ten minutes after registration, the five keys you need
during judging, and what to do if the laptop dies. Every command in it was really executed,
and writing it caught three things the repository had been wrong about. What is still
missing is paper: no printed evidence sheet, no printed checklist, so the "laptop died"
plan currently ends at "read from the script".

**What changed since yesterday.** The booth recipe was written (this was the one file
three critics in a row had asked for). A new small checker was added that can verify a USB
stick on a borrowed laptop with no repository, and it immediately caught a stale video file
that would have shipped on the stick. Your product score moved 78 to 79 out of 100; the
research score held at 84. Nothing on the judged screen is wrong today, but the file the
screen is generated from still contains a sentence you already withdrew, so the next
rebuild would put it back. That is the next lap's first job.

**What you should do.** Two emails' worth of decisions are now holding real things.
**NH-029** is the important one: `make all-checks` does not pass on your laptop either, and
the fix is either one command on your machine or one sentence in the readiness checklist.
**NH-027** is the one that decides how strong your headline is allowed to sound at the
booth. Everything else can wait.



## Gates

**ALL GREEN** · mode `full` · head `92bfc4f` · 2026-09-05T14:02:06Z · current at `92bfc4f`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 14.5 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.1 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 2.7 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 197.4 s | 1506 passed, 62 skipped, 52 warnings in 196.16s (0:03:16) |

## Commits since the previous report (5aecc5f39fe8fd16fa55da1031097738b59afc73..HEAD)

- 92bfc4f the plain-terms line told the author every command had been run, in the report whose reviewer proved one had not
- d43033b report 2026-09-05T1302Z-dev, and the one line of the booth recipe that was copied instead of run

## Needs a human (9 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-025 [DECISION] `Main` cannot follow the green commit until you change one GitHub setting, and until you do, every green push emails you a red run (by 2026-09-08)
- NH-026 [DECISION] One of the five routines pushes on a gate that does not run the test suite, and its prompt is the one the repository cannot show you (by 2026-09-08)
- NH-027 [DECISION] The one experiment that would test the project's headline against a fair opponent is scheduled for after the competition (by 2026-09-08)
- NH-028 [DECISION] The manuscript is full, and three laps have recorded that as a note rather than asked you (by 2026-09-10)
- NH-029 [DECISION] The baseline freeze is stale, so `make all-checks` cannot pass — on this machine or on yours (by 2026-09-10)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
