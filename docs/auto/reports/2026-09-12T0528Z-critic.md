# WildfireGuardian autoloop · critic · 2026-09-12T0528Z

| | |
|---|---|
| when | 2026-09-12 05:28 UTC · 2026-09-12 14:28 KST |
| branch / head | `auto/dev` / `cd18782` |
| kind | critic |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 10, done: 37, dropped: 1, parked: 1, title-derivable: 1, todo: 127 |

## What happened this lap

Critic #72 reviewed `cd18782`, the window `e7ba085..cd18782` (eleven commits, one dev lap
closing WFG-264, one paper lap, one critic lap's own ship). **ONE `fix-before-next-row`
item: none, and that is a measurement.** TWO new P0 rows at positions 1 and 2
(**WFG-266**, **WFG-267**), ONE new P1 row (**WFG-268**), ZERO new NEEDS_HUMAN entries.
ONE §3b row move spent, on **WFG-263**'s table position, which is the move critic #71
measured and explicitly left for the next lap with a spare one.

## Your decisions, applied

**Nothing new on either channel, and this is the seventh consecutive lap to say so.** The
Gmail search `from:siyeong0318@gmail.com subject:"WildfireGuardian autoloop" newer_than:14d`
returned its newest 25 threads and **every one carries exactly one message, which is the
loop's own send to you**. PR #31's comment list is **empty**. `docs/auto/decisions_seen.json`
still records `"seen": []`: **no decision has ever arrived on the email channel.** Every
applied decision came through a Claude Code session on your laptop, and the newest is
**NH-031, received 2026-09-06**, six days ago. Nothing was applied, so nothing is quoted
here. That is WFG-211, already `todo`, confirmed and not re-filed.

**27 decisions are open** (26 DECISION + 1 BLOCKER), **2 of them undated** (NH-005,
NH-014), plus 5 open FYI. **NH-046, NH-049 and NH-051 are past due.** NH-046 alone holds
readiness line R3, which is the only one of CHARTER §14b's six still missing, which holds
the whole P1 block, which holds R11's WFG-024. The sprint ends **2026-09-15**.

## Verifying the loop's own claims

Everything below was measured in this lap's own process at `cd18782`.

- `gates.py --mode full` exits **0** on its **first** run: verify PASS, snapshot-verify
  PASS, env-check PASS, pytest-full PASS (**2216 passed**, 65 skipped, 3 xfailed, 493.9 s).
  `baseline-verify` is the usual sandbox WARN: its only two differences are MISSING
  `data/raw/firms_data/fire_manifest.json` and `data_layers_manifest.json`, which are
  git-ignored and never reach a fresh clone (CHARTER §3d, §4 sandbox facts). Fourteen
  growth notes, all NEW artifacts, no MODIFIED or MISSING tracked artifact.
- `--assert-head` and `--assert-reported` both exit **0**.
- **GitHub's own runs: no red in the window.** `auto-gates` runs **377 to 398** on
  `auto/dev` all concluded `success`; run **398** is green at `cd18782`. The last `failure`
  is **386** at `edd0ec0`, critic #70's, outside this window. **CHARTER §4b sets no finding
  #1 this lap.**
- All **twenty** build-lap reports in the window record `Reviewed by:`.
- Printed kit **7 of 7** sources (`WFG_printables_20260911T2137Z.pdf`, **60** pages),
  release bundle **19 of 19**, the bundle names that kit, `tests/test_printables.py` 24/24.
- The clone is SHALLOW at **51** commits and was deliberately **not** deepened; no ancestry
  claim is written anywhere in this lap.

## The root objection

**A figure's caption and that figure's own legend now assert different things, inside one
commit, on the one rubric criterion that names legends in its own text.**

`docs/auto/RUBRIC.md:34` and `:47` word 제출 자료 as 「자료의 논리적 구성 · **그래픽 및
범례의 명확성** · 사용된 자료에 대한 출처 명기」, and the clause is identical in **both**
tables. The paper lap at `63e9d20` rewrote F8's caption in `paper/manuscript.md` from
「2 with no safe walking route」 to 「**2 reaching no refuge**」, correctly, because the bucket's
code condition establishes that the search reached no refuge and not that no route exists.
It left `paper/make_figures.py:689`, **the legend of that same figure**, reading
「origin: no safe walking route」.

One grep of `paper/make_figures.py` for `no safe walking route` returns **three** hits. The
lap changed **one**: `:244`, F5, fixed from 「exists」 to 「found」 and shipped under a new
filename `F5b_decision_shift.png` with a full docstring explaining the supersession, which
is exactly right and is the F10b remedy applied a second time. The other two are `:689`
(F8_routing_map) and `:134` (F3_regions), and neither PNG was regenerated:
`git diff e7ba085..cd18782 -- paper/figures/` lists `F5b_decision_shift.png` **alone**, and
both files still carry `a37cfb0` as their newest commit.

**The cheapest test, and it is one second long:** `grep -n 'no safe walking route'
paper/make_figures.py`. That is the whole of it. This is the **third consecutive lap** of
instance-scoped repair, and `docs/auto/DIRECTION.md` has already named the class twice,
each time one abstraction too high: critic #69 wrote 「grep the file you just edited for the
sentences that DESCRIBE it」, critic #70 wrote 「grep for the sentences that DEFINE the terms
your edit uses」. Both are semantic searches. The concrete one, now on the page: **grep the
file you just edited for the string you just changed.** Not a describing sentence, not a
defining sentence: the same words, elsewhere in the same file.

Filed as **WFG-266**, P0, position 1, with `F8b`/`F3b` filenames in its `done when` because
CHARTER §3 rule 2 forbids regenerating the committed PNGs and NH-042 is still open.

## Findings, ranked

**F1 — WFG-266 (P0, position 1).** The root objection above. Rubric rows: 제출 자료, both
tracks. Evidence: `paper/make_figures.py:134`, `:244`, `:689`; `paper/manuscript.md` F8
caption; `git diff e7ba085..cd18782 -- paper/figures/`; `docs/auto/RUBRIC.md:34`, `:47`.

**F2 — WFG-267 (P0, position 2). WFG-264's repair has not reached the paper a judge is
physically handed, and the card the student studies still gives the one instruction that
guarantees both sentences in a single stack.**

WFG-264 was filed, re-prioritised to P0 by critic #71 and closed on the argument that the
sheet a judge **physically holds** had never been audited. The emitter repair is real and
well made. But the handout itself is unchanged, correctly so, and nothing tells the student
which page is which. Measured here:

- `outputs/dispatch/20260801T163042Z/` holds **33** committed cluster directories. **17 of
  the 33** `dispatch_a4.html` print 「예산 내 차량 진입로가 화재로 차단됨(우회 포함)」; **44**
  files under `outputs/` carry it in total.
- Only **3** clusters ship a pre-built `dispatch_a4.pdf` (01, 02, 03), and
  **`02-천전공원-일대` is one of the 17**. `scripts/generate_dispatch_outputs.py:259` renders
  that PDF from that HTML through `printable.html_to_pdf`. ⚠ Stated as the inference it is:
  this sandbox has no PDF text extractor, so the PDF's bytes were not searched; the claim
  rests on the build path plus the HTML measurement, and critic #71 measured the same 1-of-3
  independently.
- **The two instructions disagree and only one is safe.** Q39 (`docs/auto/JUDGE_QA.md:1554`)
  says 「`dispatch_a4.pdf` 는 가장 큰 3개뿐입니다 ... 나머지는
  `python scripts/generate_dispatch_outputs.py` 로 다시 만들어 인쇄합니다」, which hands a judge 3
  old pages beside 30 new ones. `outputs/dispatch/README.md` says 「Rebuild them all with:
  `python scripts/generate_dispatch_outputs.py`」, which yields 33 new pages and no
  inconsistency. Neither file mentions the repair. The generator writes to a fresh UTC stamp
  (`:187`), so regenerating never overwrites the committed record.
- **This stack is the one booth handout with no staleness gate.**
  `scripts/build_printables.py`'s `SOURCES` are seven markdown documents and not one is a
  dispatch sheet, so the 7-of-7 hash that protects the bound kit never sees these 33 pages.

⚠ **Credit, because this is not 「nobody wrote it down」:** `docs/live_pipeline.md:193-201`
carries the supersession table and the 44-file statement, `tests/test_live_pipeline_doc_matches_code.py`
binds those cells to the code, `docs/routing_limitations.md` §7 is the audit, and
`docs/auto/JUDGE_QA_PENDING.md` **P-002** already drafts a good booth answer. Three things
are missing: **which** page, **one** instruction, and a gate. The row's text says so.

**F3 — WFG-268 (P1). CHARTER §4 tells every lap a tool is forbidden, and the tool works.**
§4's sandbox-facts paragraph says `curl` against `api.github.com` 「returns 403 here and must
not be used (WFG-119)」. At `cd18782` the call returns a full JSON body and exit 0, and this
lap read all of runs 377 to 398 from it. The session routes outbound HTTPS through a
configured agent proxy now, which is what changed; WFG-119's measurement was true when taken
and carries no date in the charter. Date it, do not delete it (CHARTER §3.7).

**F4 — WFG-263, evidence added, still P1.** The 0420Z report's machine-written gate table
(`docs/auto/reports/2026-09-12T0420Z-dev.md:220`) is stamped at `1461e96` and marked
**stale** against `135822b`; **neither resolves on `origin/auto/dev`** and the head it ships
in is `cd18782`. The lap handled it correctly in prose, and `WFG-264`'s status cell recorded
`done(135822b)` for the same reason. Annotated rather than rewritten; the work landed at
`fcc9391` + `4d3a16d`. Third lap running that `report.py`-before-gates has cost a cycle.
Loop hygiene, so §14b holds it until R3 ticks.

## The five lenses

- **KCF judge, software professor.** The window's engineering is the best in the record and
  I would say so at the booth. Then I would open F8, read its caption, read its legend, and
  ask which is true. That is the single cheapest hostile question available today and the
  student has no file to point at.
- **KCF judge, disaster-response official.** I am handed a stack of A4 dispatch sheets. Two
  of them explain an unreachable house differently. I do not care which is the record; I
  care that the paper in my hand disagrees with itself about why a vehicle cannot get to a
  house. Q39 is what puts both pages in my hand.
- **Fire-behaviour scientist.** §7's reading of `rescuer_reachable` is honest about
  something most tools hide: 「no route was confirmed」 is not 「the road is blocked by fire」,
  and a rescue tool that errs conservatively errs the right way. The provenance paragraph
  that now leads the counts, naming the hazard as synthetic, is what makes the section
  citable rather than suggestive.
- **ML reviewer hunting leakage and weak baselines.** Nothing new to attack in the window.
  The `vus_` quantity is read off two committed artifacts, the 12-minute threshold is read
  from each artifact's own `provenance.assumed` rather than typed in, and the measurement
  explicitly declines to second-guess the classifier that produced the class. The standing
  weak-baseline exposure is unchanged and already filed (the disc null is a floor, WFG-256
  is the rotation control that would say whether the overlap is 「모양」).
- **Statistician.** The best thing in the window is a retraction. The lap's draft claimed a
  `null` `best_closing_window_min` could only mean 「no drive path from any depot」; `+inf`
  serialises identically, and a reproduction written for another purpose falsified the
  assertion before §7 shipped. The published claim is the weaker true one, the key was
  renamed `n_no_finite_best_closing_window` for what it measures, and the error is written
  into §7. A zero published as ruling out both infinities together and neither separately is
  exactly the right strength.

## `factchk` and `mandela`

**`factchk`: nothing to verify.** The window's added markdown contains three mentions of OSM
and one of SRTM, all describing this repository's own data provenance. No new claim about the
world is asserted, so nothing was checked against an external source. Stated rather than
left implicit, because a clean `factchk` and an unrun one look identical in a report.

**`mandela`: no pattern fires** on the `vus_` measurement, for the three reasons under the
statistician lens above.

## Judge drill, ten hardest questions, answered only from files

Eight of ten answer from a committed file: why the 사유 wording changed
(`docs/routing_limitations.md` §7), how many of the 24 were really blocked by fire (§7, which
says it is not measurable and why), forecast accuracy (Q36 with both IoU pairs), whether the
disc gap is 「모양」 (Q36, not measured, WFG-256), offline tests (Q40), the schedule
(`TIMELINE_ROLES.md`), submitted-versus-current figures (`submission_reconciliation.md`, in
the printed kit), and the Bokade overlap (Q16d, narrowed to 「공개된 기록과 초록」).

**Two do not, and both are this lap's findings.** Added to `docs/auto/JUDGE_QA_PENDING.md`
rather than to the bank, because NH-049 is open and merging a card without `make printables`
at a new stamp turns the tree red:

- **P-003 · WFG-266 ·** 「본문에는 2 reaching no refuge 라고 쓰셨는데 범례는 no safe walking
  route 입니다. 어느 쪽이 맞습니까?」 Marked **근거 없음**: no file answers it today.
- **P-004 · WFG-267 ·** 「이 세 장 중 어느 것이 옛 문장입니까?」 The residual of the existing
  P-002, which answers *why* the committed sheets keep the old line but not *which page*.

## Scorecard deltas

**ONE row moves on each track, the same row, DOWN. Track B 96 to 95, Track A 97 to 96**,
both on 제출 자료 (「그래픽 및 범례의 명확성」), both on F1, which this window **created**.

- **설계와 방법론 20, HELD and re-earned rather than inherited**, on 「변수의 정의와 제어의
  적절성」: the refusal to register a docstring as a measurement, the three return sites read
  off the code, the dual identity control, the threshold read from provenance.
- **데이터 수집·분석·해석 20, HELD**, and what earns it is the published retraction.
- **구현 및 유용성 20, HELD.** Critic #71's pre-registered fall does **not** fire: its
  condition was WFG-264 still being `todo`, and the row is `done`. Held rather than raised,
  because of F2.
- **연구 목적 18, 개발 목적 19, 창의성 19, HELD.**

⚠ **Pre-registered for critic #73:** 제출 자료 returns to **19** if WFG-266 closes with F8
and F3 redrawn under new filenames and the manuscript pointing at them, and falls to **17**
if another window ships a third instance of the same grep. 구현 및 유용성 falls to **19** if
WFG-267 (i) is still `todo`.

## Readiness

**8 of 11, unchanged. ZERO lines ticked for the twenty-ninth consecutive critic lap.**
`git diff e7ba085..cd18782 -- docs/auto/KCF_READINESS.md` is empty and the file's newest
commit is `a37cfb0`, 2026-09-11. R3 is the only one of §14b's six outstanding, it is
`blocked(NH-046)`, NH-046 came due 2026-09-10, and the sprint ends 2026-09-15.

## Two numbers this lap nearly got wrong, and caught in its own process

Recorded because a critic that only publishes its hits is not measurable.

1. The first re-hash of `release/kcf-finals-2026/MANIFEST.json` read each entry's `path`
   field and reported **16 of 19**. The entries carry a separate **`source`** field naming
   the repository path; read correctly it is **19 of 19**.
2. The first pass over the window's reports read three of them as uncertified, because their
   line is `Reviewed by: **subagent (block)**` and the pattern did not allow the bold
   markers. All **twenty** record it.

Both are the same class as the WFG-264 lap's null-window discriminator, which is the point:
a field that looks like the one you want is not the one you want, and a count is not a
measurement until you have named the field it came from.

## In plain terms

- **What the judges would see today.** A project whose engineering I would praise out loud,
  with one thing a judge can break in ten seconds: in your paper, the picture's own label and
  the sentence under the picture now say different things about the same two houses, and the
  scoring sheet has a line for exactly that ("graphics and legend clarity"). Separately, the
  stack of village rescue sheets you hand over has two different explanations printed on it
  for why a fire truck cannot reach a house, because three of the 33 pages were made before
  yesterday's fix and nothing tells you which three.
- **What changed since yesterday.** The loop fixed a sentence on your rescue sheets that
  claimed more than the program checks, and it did it unusually well: it refused the lazy
  shortcut, built test cases with no fire on them at all that still produced the old verdict,
  and when its own measurement tried to overclaim, one of its own tests caught it and it
  published the weaker true version. All gates are green, and GitHub agrees at every commit.
- **What you should do.** Nothing about today's two findings, the loop will fix both. But
  **27 decisions are waiting and 2 have no date at all**; NH-046, NH-049 and NH-051 are past
  due. NH-046 is one question, and it alone is holding the ninth of eleven readiness boxes
  and a whole block of parked work. The sprint ends **2026-09-15, three days from now.**
  Answering even three of them unblocks work that cannot start otherwise.



## Gates

**ALL GREEN** · mode `full` · head `cd18782` · 2026-09-12T05:08:01Z · current at `cd18782`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 32.4 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.2 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 1.7 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 493.9 s | 2216 passed, 65 skipped, 3 xfailed, 52 warnings in 492.13s (0:08:12) |

## Commits since the previous report (cd1878297860bb487fc9c4a68cb66bdf18cf9e0c..HEAD)

(no new commits)

## Needs a human (32 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09, one day past; raised to HIGH by critic #55 on a measured third instance)
- NH-036 [DECISION] One critic lap told the next one not to edit a file, and that is what kept a false sentence in front of a judge for a window (by 2026-09-10)
- NH-037 [DECISION] The paper's word proxy now stops it a thousand words before your 25-page rule (by 2026-09-10)
- NH-038 [DECISION] Your "product first" rule has spent the last three dev laps on documents, and the readiness line it was written to protect has not moved in five critic laps (by 2026-09-09)
- NH-039 [DECISION] The national wildfire-spread system's manual is an 18 MB PDF the sandbox could not fetch, and one of you can (by 2026-09-12)
- NH-040 [FYI] A critic lap pushed one commit past a red `--assert-reported`, and it is telling you rather than hiding it
- NH-041 [FYI] This lap sent you an email containing only the word PLACEHOLDER, and could not take it back
- NH-042 [DECISION] Two of your own rules collide whenever a withdrawn claim lives in a frozen artifact, and this week they collided three times (by 2026-09-10)
- NH-043 [DECISION] A gate your loop built this morning will go red about twice a day, and the charter tells the lap that meets it to stop working (by 2026-09-09)
- NH-045 [BLOCKER] The staleness gate has closed `auto/dev` to every routine, and the one routine that met it is the one forbidden to clear it (by 2026-09-08)
- NH-044 [DECISION] The claim the paper just retracted is still live on the page the paper cites for it (by 2026-09-09)
- NH-046 [DECISION] Your product's definition-of-done names a command nothing in this project has ever run (by 2026-09-10)
- NH-048 [DECISION] One of the research routine's three literature channels has been dead for two runs, and a working replacement is already proven (by 2026-09-10)
- NH-049 [DECISION] Your critic routine is told to add judge Q&A cards and your own printing gate makes that impossible for it (by 2026-09-11)
- NH-050 [DECISION] You answered two of these questions two days ago and the loop never heard you, because you answered them on the routine page (by 2026-09-10)
- NH-051 [DECISION] The rule your loop has been obeying for three days is not the option it names, and the difference is why one row has been pushed down five times (by 2026-09-11)
- NH-052 [DECISION] The experiment that measures what your own forecast is worth is now one run away, and it would change what 42 means (by 2026-09-12)
- NH-053 [DECISION] The word your front door uses for 42 stopped being right today, and the loop cannot pick its replacement (by 2026-09-12)
- NH-054 [DECISION] Four fifths of your front door's headline is limits, and no lap is allowed to decide whether that is the project's strength or its biggest presentation risk (by 2026-09-13)
- NH-055 [DECISION] Your front door compares the headline forecast number to a model the same page calls broken, and today the loop measured a second, harsher comparison it is not allowed to put beside it (by 2026-09-13)
- NH-056 [DECISION] A Korean university published a reproducible deep-learning wildfire model four weeks before your finals, and a lap may not decide on its own whether to mention it (by 2026-09-13)
- NH-057 [DECISION] The one thing your project now says it contributes has never once been produced from a real fire, and only your laptop can change that (by 2026-09-13)
- NH-058 [DECISION] Two of your routines built the same row in the same window, and the rule that was supposed to stop that cannot see the paper routine (by 2026-09-13)
- NH-059 [DECISION] Your loop measured the hardest question a judge can ask you, and the answer is uncomfortable enough that no lap may decide on its own whether you say it out loud (by 2026-09-13)
- NH-060 [DECISION] Your "product first" rule sent a booth-printable defect to the bottom of the board, and two of your routines read the same sentence of it differently (by 2026-09-14)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
