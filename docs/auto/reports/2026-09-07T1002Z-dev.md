# WildfireGuardian autoloop · dev · 2026-09-07T1002Z

| | |
|---|---|
| when | 2026-09-07 10:02 UTC · 2026-09-07 19:02 KST |
| branch / head | `auto/dev` / `ebe78dc` |
| kind | dev |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 7, done: 25, parked: 1, todo: 85 |

## What happened this lap

## Your decisions, applied

**Nothing new on either channel, and that is now eleven laps in a row.** The Gmail search
`from:siyeong0318@gmail.com subject:"WildfireGuardian autoloop" newer_than:14d` returns 30
threads and every one is a single message this loop sent you; none has a reply. PR #31 has
no comments. No `NH-###:` line reached this lap, so no entry was closed and
`docs/auto/decisions_seen.json` is unchanged.

Ten DECISION entries are open and waiting on you: **NH-005, NH-014, NH-032, NH-034,
NH-035, NH-036, NH-037, NH-038, NH-039, NH-042.**

## The row

**WFG-119 (P0, infra)** — `docs/auto/DIRECTION.md` position 1 with a reason, and critic
#33's single `fix-before-next-row` item. It was `todo`, not stale-claimed. Claimed at
`7308b06` and pushed before any work started.

### The defect, and why two laps walked past it

Two gates in `tests/test_finals_screen.py` ask whether the commit stamped into the judged
screen `web/finals.html` is reachable. They ask that **of the clone**, and the routine's
checkout is shallow. The stamp `62b58e1` aged past the horizon, both went red with
`fatal: Not a valid object name`, and GitHub at `fetch-depth: 0` stayed green on the same
commit. The red was a **true positive** — the screen a judge opens reported a 23-hour-old
build — but the sentence it printed described the *clone*, not the *screen*. Two laps read
it that way, ran `--unshallow`, saw green, wrote 「nothing in the tree was wrong」 and filed
nothing.

### The root objection I recorded against my own plan (`hate`)

*「A threshold gate does not close the recurrence — it makes the alarm ring more often. The
row's word is 'closed rather than reset', and by that standard this is a reset with extra
steps.」* Conceded on frequency, answered on cost: the failure text now names the staleness
and carries the remedy, where the old one read as repository corruption. I did **not**
claim a cure, in the report, the doc or the MEMO.

### Built

**(a)** Reproduced RED first in the routine's own default clone: `2 failed, 30 passed`.
`make finals` rebuilt at `7308b06` — a commit already on `origin/auto/dev`, which the
escape test requires — changing **exactly one line, 434, the payload**. Then `34 passed`.

**(b)** A **new** gate; neither existing test touched (§3.9 and the row both forbid it).
`test_the_screen_is_rebuilt_before_its_stamp_ages_out_of_this_clone` asks how many commits
behind `HEAD` the build is — answerable at any clone depth, so sandbox and CI reach the
same verdict for the same reason. Limit 30. An unresolvable stamp counts as already aged.
The message prints the **measured** clone size, the remedy and the row id.

**(c)** `_needs_git_history`'s 「294 commits deep」 replaced by the measurement and by the
point it hid: **the depth is not a constant** (50 here on two laps, 294 earlier, 531
unshallowed).

Documented in `docs/finals_screen_v2.md` §4.4. Learned in `docs/auto/MEMO.md`.

## Reviewed by: subagent (block): "the lap's headline grading number is an artifact of the one stale stamp it happened to observe, not a property of the gate it ships — and it was published in that stronger form into a judge-facing document"

**The reviewer blocked this lap and it was right. I fixed rather than argued.** Its own
verification confirmed claims 1, 2, 4, 5, 6, 7 and 8 by re-running the mutations itself.
What it nailed:

1. **The number was wrong.** I wrote that the new gate fires 「24 commits earlier」. The old
   gates do not fail at a fixed distance: a clone holding D commits resolves a stamp at
   most D−1 behind `HEAD`, so they fail at **D**, the clone's own depth. Re-measured here:
   `HEAD~51` resolves, `HEAD~52` is `fatal: Needed a single revision`, in a 52-commit
   clone. **24 came from subtracting 31 from 55, and 55 was merely where the stamp sat
   when critic #33 looked.** The lead is **(clone depth − 30)** — about **20** at the
   measured depth-50 checkout, and **unbounded** in CI where those gates never fire from
   staleness at all. I probed two points and let the interpolation between them become a
   stated property of the gate: the exact error this row faults critic #26 for
   (「deepening by a guess is not a control」), committed by the lap writing that sentence
   down. Corrected in the backlog, the MEMO and §4.4, with the head and date beside it.
2. **The grading test was vacuous.** It built its 「over the limit」 case from
   `STAMP_MAX_COMMITS_BEHIND + 1` and then judged with the same constant, so it could not
   fail for any threshold — it passed at 5, 20 and 45. It graded the helper while claiming
   to grade the policy: the WFG-156 class, one function below where this row records it.
   Rewritten with **two hard-coded distances (29, 31)** not derived from the constant; the
   grid is now red at 5, 20, 28, 31, 32, 45 and green only at 29 and 30. Its skip bound is
   a literal too, so raising the threshold cannot silently skip its own grader.
3. **The row was closed on its easier half.** Two clauses of critic #26's own done-when
   were unnamed. Both are now **closed**: CHARTER §4's sandbox-facts paragraph records the
   shallow checkout (it had **zero** occurrences of 「shallow」 or 「depth」 before), and
   `ROUTINE_PROMPTS.md` step 2 names the GitHub MCP instead of the `curl` that returns 403
   here. Clause (i) is outstanding, so the row goes back to **`todo` with a residue note**
   (CHARTER §5) rather than to `done`. My earlier residue wording was itself wrong — it
   said 「`bootstrap.sh` still clones shallow」, and that script **clones nothing**; the
   shallow checkout is made by the routine harness before any repository script runs,
   which is why (i) may not be doable from inside the repository at all.
4. **A judge-facing citation my own edit broke.** `docs/auto/JUDGE_QA.md` cited 「the two
   lineage checks」 by **line number** (`:544`, `:550`, `:649`), and adding a gate to that
   file made all three point elsewhere — the WFG-133 class, in the file the student reads
   aloud. ⚠ **My first fix was also wrong**: it rewrote those numbers into test names, and
   the sentence sits inside a block marked 「폐기된 기록입니다」, a record CHARTER §3.7
   keeps. Reverted and **annotated** instead, dated, telling the student to cite the names.
5. The live gate-inventory count in `docs/finals_screen_v2.md` went 32 → 34; the 32 inside
   the WFG-109 **record** paragraph was left alone (§3.5).
6. Filed **WFG-159** for the consequence I had disclosed only as a frequency.

## What did not work

- **The mutation grid lied, and it took a second look to notice.** Its first run reported
  **N=45 passing**, which would have meant the rewritten grader was *still* vacuous; N=45
  alone reported failing. `30`, `31` and `45` are all two characters, so the mutated file
  keeps the same **size**, and CPython keys its bytecode cache on (mtime, size) at
  one-second resolution — inside a fast loop the interpreter re-used the previous
  iteration's `.pyc`. **A probe with a caching layer between treatment and measurement is
  not controlled.** Clearing `__pycache__` between mutations gives the clean grid above.
  This is now a MEMO entry beside the 0705Z one about re-running probes.
- **The first independent reviewer stalled** and had to be re-run: it launched the full
  gates in the background and blocked in an `until grep` loop waiting for them — the exact
  session-mechanics failure CHARTER §4 warns laps about. Its verdict did arrive; the
  replacement was stopped once it had. The replacement left a probe mutation in
  `web/finals.html` when stopped, which **the new gate caught** — an unplanned live test.
- **`baseline-verify` reports `BASELINE MOVED`** with two `MISSING` entries, the
  git-ignored acquisition manifests that never reach a fresh clone (CHARTER §4). Not mine.
  Worth stating because this lap regenerated a tracked artifact: `web/finals.html` is
  **not** listed MODIFIED, so §3d's freeze was not tripped.
- **WFG-139 fired a seventh consecutive time, inside this lap's own gate run.**
  `data/raw/` held no DEM at bootstrap (09:19:31Z); `data/raw/dem/srtm/N36E129.hgt`
  (25,934,402 B) has mtime **09:29:39Z**, during `pytest-full`. Not this row; recorded.
- **The kit was built twice** (0951Z, then 0953Z after the JUDGE_QA revert). The 0951Z
  build was **never committed**, so §3.2 had not frozen it, and it was replaced rather
  than shipped as a second stamp — the ordering the 0705Z lap wished it had. Rendered
  pages were read before committing. `make finals-bundle UPDATE=1` selects the newest
  **tracked** kit, so the manifest could only catch up after the kit was committed; the
  two 0705Z files then left in the git-ignored bundle folder were reported `EXTRA` by
  `check_bundle_copy.py`, pruned, and re-checked `OK` at 19 files.

## Gates

`gates.py --mode full` → **ALL GREEN, exit 0** at `ebe78dc`: `1656 passed, 56 skipped,
2 xfailed`, **warm** (the DEM landed during this lap's first run; the cold count earlier
was `1650 / 62`). Baseline `--mode quick` at lap start: ALL GREEN, exit 0 at `e649f2d`.
GitHub `auto-gates` run **202** at that head is `success` — no CHARTER §4b finding.

## Next row

`docs/auto/DIRECTION.md` position 2, **WFG-146**: the 사이언스타임즈 date is wrong on
`docs/related_work.md` and on `docs/auto/finals/RELATED_WORK_PANEL.md:43`, which is printed
in the kit and on the stick, so it needs a lap that rebuilds the kit (WFG-152). Then
**WFG-139** (seven consecutive measurements) and the new **WFG-159**.

## In plain terms

- **What changed for the project.** The screen judges will look at was quietly reporting a
  build from the day before, and the only alarm about it was worded so badly that two
  earlier sessions "fixed" the alarm instead of the screen. The screen is rebuilt, and
  there is now a second alarm that goes off earlier and says plainly "the screen is stale,
  run this one command".
- **Why it matters to the judges or the science.** A reviewer I ran against my own work
  caught me stating a number — "24" — that I had guessed from two measurements rather than
  measured, and I had already written it into the Korean explainer a judge can open. It is
  corrected everywhere, with the correction left visible rather than tidied away. That
  habit, not the gate, is what this project is really claiming.
- **What you should do, if anything.** Nothing is blocked on you today, but ten decisions
  have now waited eleven laps with no reply on either channel — if only one gets an answer,
  make it **NH-032** (the fair-opponent margin), because it is holding numbers off the
  booth surfaces. Print kit **`20260907T0953Z`**, not the earlier stamps.



## Gates

**ALL GREEN** · mode `full` · head `ebe78dc` · 2026-09-07T10:01:24Z · current at `ebe78dc`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 19.7 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.2 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 0.5 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 285.5 s | 1656 passed, 56 skipped, 2 xfailed, 52 warnings in 283.93s (0:04:43) |

## Commits since the previous report (2896c9c94b16b32e20eb07774b789d30824b22eb..HEAD)

- ebe78dc the bundle a judge is handed carries the kit this lap built, not the one before it
- 044c64f the reviewer's block: "24 commits earlier" was one observation generalised into a property of the gate
- e60d1fd WFG-119: the gate that reports the clone gets fixed by fixing the clone, so a second gate now reports the screen
- 7308b06 claim WFG-119 (20260907T0918Z)
- e649f2d the critic report's own gate table is the finding, and the green re-run beside it is conditional on a deepening
- d4445d6 critic #33: R7 ticks, and the gate went red on a correct tree because the screen's stamp aged out of the sandbox's clone

## Needs a human (15 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09)
- NH-036 [DECISION] One critic lap told the next one not to edit a file, and that is what kept a false sentence in front of a judge for a window (by 2026-09-10)
- NH-037 [DECISION] The paper's word proxy now stops it a thousand words before your 25-page rule (by 2026-09-10)
- NH-038 [DECISION] Your "product first" rule has spent the last three dev laps on documents, and the readiness line it was written to protect has not moved in five critic laps (by 2026-09-09)
- NH-039 [DECISION] The national wildfire-spread system's manual is an 18 MB PDF the sandbox could not fetch, and one of you can (by 2026-09-12)
- NH-040 [FYI] A critic lap pushed one commit past a red `--assert-reported`, and it is telling you rather than hiding it
- NH-041 [FYI] This lap sent you an email containing only the word PLACEHOLDER, and could not take it back
- NH-042 [DECISION] Two of your own rules collide whenever a withdrawn claim lives in a frozen artifact, and this week they collided three times (by 2026-09-10)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
