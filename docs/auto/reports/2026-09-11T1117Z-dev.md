# WildfireGuardian autoloop · dev · 2026-09-11T1117Z

| | |
|---|---|
| when | 2026-09-11 11:17 UTC · 2026-09-11 20:17 KST |
| branch / head | `auto/dev` / `5590b3d` |
| kind | dev |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 10, done: 34, dropped: 1, parked: 1, title-derivable: 1, todo: 120 |

## What happened this lap

Lap 2026-09-11T0921Z (dev). **WFG-254 done — the judge-facing half by this lap, the paper
half by the paper routine, in the same window and without either knowing.** Claim commit
`d4b7bef`.

⚠⚠ **READ THIS FIRST: two routines repaired the same row inside one window.** The paper
routine's lap 31 (`c3bd2bb`, 0940Z) landed on `origin` while this lap was building, and it
had corrected `paper/manuscript.md` §6, `paper/GAPS.md`, `paper/README.md` and
`make_figures.py`, reissuing the figure as **`F10b_disc_null.png`**. It never touched the
Q36 card, `docs/disc_null.md`, `docs/oracle_gap.md`, the booth kit or the registry, and it
left the backlog row reading this lap's own `in-progress(20260911T0921Z)` — so the row was
still this lap's, and only the `paper/` surfaces were done twice.

**This lap pushed second, so it rebased and deferred on every `paper/` file** (CHARTER §4:
the one that pushes second rebases; §3.8: never force). Their §6 repair is **word-neutral**
(body_words unmoved at 8,995) where this lap's was −2 and would have nulled `built_pages`;
one corrected figure is better than two; and `paper/` is theirs under CHARTER §12. **This
lap's duplicate figure and manuscript wording were dropped, not merged.** What it kept in
`paper/` is exactly two things their lap did not have: the one prose instance whose
emphasis marker hid it from **both** laps' greps, and four per-line pragmas on record lines
that this lap's own new registration is what gates. Baseline at `748e89f`: `gates.py --mode quick` ALL GREEN.
GitHub `auto-gates` run **366** is `success` at exactly that head, so CHARTER §4b sets no
finding #1 for the **fifteenth** consecutive lap. `CRITIC_LATEST.md`'s one
`fix-before-next-row` item was this row's own Q36 half, so it was cleared inside the row.

## Your decisions, applied

**Nothing new, and this is the sixth day.** The Gmail search
`from:siyeong0318@gmail.com subject:"WildfireGuardian autoloop" newer_than:14d` returns
201 estimated threads; the newest **25** were read and **every one holds exactly one
message, every one of them sent by this loop**. A reply bumps its thread to the top of
that ordering and none of the top 25 is one. `scripts/auto/decisions.py` was not invoked,
because there is nothing to parse, and `docs/auto/decisions_seen.json` still reads
`"seen": []` — the newest applied decision is still **NH-031, of 2026-09-06**. **PR #31,
the second channel CHARTER §6 names, returned an empty comment list** at the GitHub MCP
this lap; that is **WFG-211**, already filed, confirmed and not re-filed. Nothing was
guessed at and nothing was applied.

**25 decisions are open for you (24 DECISION + 1 BLOCKER), and 2 of them have no date at
all** (NH-005, NH-014) — this lap filed **NH-058**, the first new entry in four laps, because
the collision below is a mechanism question no lap may settle for you. **NH-046, NH-049 and NH-051 are past due; NH-052 and NH-053 come
due tomorrow.** The sprint ends **2026-09-15**, four days out.

## What this lap did

`docs/auto/DIRECTION.md` item 1 and backlog position 1 named the same row, and
`docs/auto/CRITIC_LATEST.md`'s single `fix-before-next-row` item was its Q36 half. So the
row was taken whole rather than split.

### The defect, and why it was not a wording quibble

**Eight sentences across seven files** said the area-matched disc null is placed **at the
ignition point** — `docs/disc_null.md` §2b now enumerates all eight in a table rather than
asserting a number, which is the independent reviewer's finding and not this lap's: the
first draft said 「seven」 on that page, 「six」 in the registry, and a later sweep found an
eighth. The worst of them is `docs/auto/JUDGE_QA.md` Q36 — tier **T0**, answered from
memory to all five judges and printed on page 25 of the booth kit. The most durable is
`paper/make_figures.py` rendering 「distance moved from the ignition」 into the committed
300 dpi `paper/figures/F10_disc_null.png`.

It is not there. `data/processed/disc_null_yeongdeok.json :: null_rule.centre_from` states
the rule correctly as 「centroid of the t=0 seed」 and `scripts/measure_disc_null.py` never
reads `ign_xy` at all. **The same card holds the right rule two paragraphs above the wrong
one, and the wrong one is the half said out loud.**

### The root objection this lap recorded before building, and what it cost

`hate`, in the claim commit: *the row asks for one word to change on seven surfaces plus a
new 300 dpi figure, and every one of those edits rests on a measurement this lap did not
take.* If the row-origin convention, the axis order or the extent edge were read wrong,
this lap would have propagated a **false** locative to every one of them and a new committed
figure — and a wrong repair of a right sentence is worse than the defect, because the
defect is at least recorded and the repair would not be.

The cheapest test was to re-derive the whole measurement here before a word moved. It
reproduces exactly: centre `(97.775, 55.120)`, `ign_xy` → cell `(74, 25)` under
`row0_at_ymin` and `(107, 25)` under `row0_at_ymax`, only `(74, 25)` burning at `t = 0`
and `(107, 25)` never observed at any slice, gap **38.3986** cells.

**And then the objection paid a second time, which is the half worth keeping.** Having
re-derived it, the lap found the number lived in no file — critic #65 took it in its own
process and this repository had reasoned about that disc's centre across six direction
pages without once opening `ign_xy`. So the measurement became an artifact:

- `scripts/measure_disc_centre_vs_ignition.py` — reads two committed artifacts, writes one,
  refits nothing, and **does not assume a row convention**: it computes both candidates and
  lets the observation settle which is right.
- `data/processed/disc_null_centre_vs_ignition.json` — un-ignored and tracked, since
  `make verify` fails on a registry key whose `source_file` is untracked.
- `dnc_yeongdeok_seedcentre_to_ignition_cells` **38.3986** and `_m` **19199.3**, registered
  additively (**74 insertions, 0 deletions** in `docs/NUMBERS.json`). Against
  <!-- collision-ok: 18.162 — the key is named in the sentence: dn_yeongdeok_t720min_disc_radius_cells, the 720-minute slice's radius and the largest of the five. The other registered radii (8.913, 14.881, 17.355, 17.681) are different slices of the same run. -->
  `dn_yeongdeok_t720min_disc_radius_cells` **18.162**, the largest disc drawn at any slice:
  **no disc at any slice contains the recorded ignition point**, and under the losing row
  convention the gap is **31.6008**, still larger. The finding survives the choice.

⚠ The keys are named `seedcentre` rather than `centre_to_ignition` deliberately.
`docs/auto/MEMO.md` records what a key name made of ordinary English words costs: the
collision gate anchors on three of them, and 「centre」, 「ignition」, 「cells」 co-occur in
this repository's own prose about the null.

### The second clause: disambiguated, not deleted

「모델이 원판을 이긴 것은 **모양과 범위**」 / 「shape and **extent**」. Area is matched **by
construction** — §2 sizes the disc from the model's own core count, so both masks hold
`dn_yeongdeok_n_cells` **952** cells at the headline slice. On the area reading the claim
is *impossible*, not merely unproven; on the reach reading it is true. Every surface now
says 뻗은 거리 / **reach** and carries the clause saying why area cannot be the axis.

⚠⚠ **`tests/test_disc_null.py` had pinned the impossible reading in place.** It asserted
「shape and extent」 was in the page, so any lap that noticed the ambiguity and fixed it
would turn the suite red and, reading a red gate as its own error, would put the word back.
The gate now holds the disambiguated wording **and** requires the page to say why area is
not an axis, so the word cannot return by deleting an explanation.

### What did not work, and what was refused

- **`make finals` was reverted once, wrongly.** Rebuilding the screen changed only the
  build stamp in the diff, so it looked like no-op churn. It was not:
  `web/finals.html` carries a 검증 레지스트리 card stating `n_entries`, so **any registrar
  that adds a key moves the screen**, and the payload is one long JSON line that hides it.
  `tests/test_finals_payload_rederives.py` caught the stale card (546 against 548). Rebuilt
  and the bundle re-pointed.
- **The first `collision-ok:` annotation did not license anything**, twice: the pragma must
  sit on the offending line or the **one** line directly above it, and a four-line comment
  block whose first line carries the value does not. In Markdown it also has to go *inline*,
  because a standalone comment between two table rows splits the table.
- **`arm: "L2_forecast"` was invented and `make verify` refused it.** The keys now carry
  `field_comparison`, the arm the rest of the disc-null and oracle-gap families use.
- ⚠ **「Nothing was regenerated」 would be false and is not claimed.** CHARTER §3 rule 2
  protects `data/processed/**`, `docs/figures/**` and the snapshots, and **none of those
  moved** — `paper/figures/F10_disc_null.png` is byte-identical before and after, and
  `data/processed/disc_null_yeongdeok.json` is untouched with `verify_numbers` still
  reporting 548/548. What **was** rebuilt, deliberately and under its own rule: the `.docx`
  (CHARTER §12, because the manuscript moved), `web/finals.html` and the bundle manifest
  (their own builders, because a registry key was added), and the booth kit at a new stamp
  (NH-049). The registry moved additively.
- **No number the row's finding rests on was written that this lap could not register**, and
  the geometry numbers critic #65 measured in its own process (55 components, the 44.5 km
  box) were deliberately **left out** of every page: that is row **WFG-255**, and
  `docs/disc_null.md` §2b says so rather than borrowing them.

### `sip` found an eighth surface after the row's own sweep had closed

`paper/GAPS.md` carried 「a floor comparison of that field against an **area-matched disc**
at the ignition」 — the closing `**` sits between the two words the English pattern anchors
on, so neither the row's grep nor the first registration saw it. Corrected with a dated
note. **The registry half is the part worth keeping:** WC-017's three English patterns now
carry the `(?:\*\*)?` tolerance its own Korean patterns already had — a lesson written into
WC-011's and WC-013's `why`, paid for twice, and not applied to this lap's English
spellings until the sweep found the miss. Widening them immediately caught this lap's own
§2b correction note, which is the proof the ratchet works.

### The figure, the registry and the paper

- **`paper/figures/F10_disc_null.png` is kept byte for byte**, and the corrected figure
  that ships is the paper routine's **`F10b_disc_null.png`**, not this lap's. Both laps
  reached the same conclusion independently — CHARTER §3 rule 2 forbids regenerating a
  committed artifact, so the correction takes a new filename — and this lap dropped its own
  copy in the rebase rather than shipping two. ⚠ Worth recording: the paper lap's reviewer
  had to stop it from redrawing `F10_disc_null.png` in place, on the argument that
  `paper/figures/` sits outside CHARTER §3.2's enumerated set. That argument is correct in
  every clause and was still not that routine's call to act on.
- **`WC-017`** registers four spellings, blast radius measured before registering.
  ⚠ **The instance the registry cannot reach is the PNG**, because a pattern cannot read an
  image; it is written into the entry's `artifact` field as a **dated known-stale
  exception** (CHARTER §3.5c as amended, NH-042 A) rather than left to a regex that would
  never fire. `docs/withdrawn_claims.md` carries the row.
- **A dev lap moved `paper/manuscript.md` — and then gave the change back.** This lap wrote
  §6 as 「an equal-area **seed-centred** disc」 for `body_words` 8995 → 8993, and nulled
  `built_pages` because this machine has no renderer. The paper routine's own repair, which
  landed first, says 「an equal-area disc **centred on the first detections' centroid**」 at
  **18 words out, 18 words in**, leaving the count and the page anchor untouched. That is
  the better outcome under NH-037 and it is that routine's file, so **this lap's manuscript
  and `STATE.json` edits were dropped in the rebase.** The `.docx` on `origin` is theirs.
- **Booth kit** rebuilt at `WFG_printables_20260911T0939Z.pdf`, **59 pages**, staged, and
  `release/kcf-finals-2026/MANIFEST.json` re-pointed (19 files).

### Two new gates, each graded by mutation

`test_q36_does_not_place_the_null_at_the_ignition_point` and
`test_q36_does_not_claim_the_model_beats_the_disc_on_area`. Putting 발화점 back reddens the
first and nothing else; putting 「모양과 범위」 back reddens the second and nothing else;
restoring the tree greens both. ⚠ They needed a new `_open_card()` helper, and the reason is
its own finding: `QUESTION_RE` matches only the numbered spine, so **every card in the
「아직 답이 없는 질문」 table — Q34 through Q40, including Q36 at tier T0 — had no gate of its
own**, and `_card()` raised 「Q36 is gone from the bank」 on a card plainly in the bank.

## Gates

`.auto/venv/bin/python scripts/auto/gates.py --mode full` exits **0** on the commit this
report ships in. ⚠ **It did not, twice, before it did, and both reds were this lap's own
work**: the first was 13 tests the new registration and the corrected wording turned red
(the withdrawn-claims registry's own family and probe gates, `test_disc_null.py`'s pinned
phrase, and a stale screen card), the second was the single collision the independent
reviewer found on the backlog line. **Neither was discovered by the gate table in this
report** — the table is stamped before the report is committed, which is why what certifies
this lap is the re-run on the pushed commit plus `--assert-head`, and not the sha the table
names. `baseline-verify` WARNs on the two git-ignored
`data/raw/**` contracts, which is NH-029 and CHARTER §3d working as decided in a sandbox
with no bundle. `--assert-head` and `--assert-reported` both exit 0 immediately before the
push; the gate table below names the head it was stamped at, and what certifies this report
is that re-run on the commit it ships in, not the sha in the table.

Reviewed by: **subagent (block)** — 「`gates.py --mode full` is RED on the tree this lap
holds, and the red is caused by the lap's own unpushed work.」 **Every one of its findings
was fixed; none was argued with.** It re-derived the whole measurement in its own process
before trusting any file and confirms claims 2, 3, 5, 6 and 7 to the digit, and confirms
`paper/figures/F10_disc_null.png` is byte-identical at `d4b7bef` and at HEAD. What it
blocked on:

1. ⚠⚠ **The push blocker, and it was the lap's own line.** `docs/auto/BACKLOG.md:37` — the
   uncommitted `done(…)` note — says **38.3986** on a row whose anchors are
   `disc`/`model`/`yeongdeok`, so the collision gate reads it against
   <!-- collision-ok: 0.2453 — this sentence QUOTES the registry value of dn_yeongdeok_disc_vs_model_iou in order to explain why the gate fired against it; 38.3986 on the same line is dnc_yeongdeok_seedcentre_to_ignition_cells, a distance in cells. -->
   `dn_yeongdeok_disc_vs_model_iou` **0.2453**. **The same lap had added exactly this pragma
   twice in `docs/disc_null.md` and did not add it to its own backlog line.** Annotated;
   the checker now exits 0.
2. **Claim 1's count was wrong three different ways in one diff.** `docs/disc_null.md` §2b
   was headed 「seven surfaces」 and enumerated **six**; `WC-017` said 「six」; the follow-up
   commit found an **eighth**. That is the CHARTER §5c failure the row exists to repair,
   reproduced inside the repair. §2b now **enumerates all eight in a table** instead of
   asserting a number, and the registrar and `WC-017` say the same thing.
3. **`WC-017` was not honest about what it cannot reach.** It declared one unreachable
   instance, singular — the PNG. But **36 committed booth-kit PDFs** bake the pre-fix card
   into a binary, and `manifest_20260911T0706Z.json` records its `JUDGE_QA.md` source at a
   hash byte-identical to the pre-fix file. Both classes are now declared as dated
   known-stale exceptions, with the note that only the `…0939Z` build goes to the booth.
4. **Claim 4 was false as worded.** 「no committed artifact was regenerated」 is not true:
   the `.docx` was rebuilt (the reviewer unzipped both), and `web/finals.html` and the
   bundle `MANIFEST.json` were re-stamped. All three are sanctioned, and the claim now says
   which artifacts moved and under which rule instead of asserting a blanket a reviewer can
   falsify in one command. **The protected half is clean**, independently confirmed.
5. ⚠⚠ **One of this lap's own new asserts was theatre.** `assert "area" in doc and
   "by construction" in doc` **already passed on the pre-lap document** (6 and 4
   occurrences at `d4b7bef`), so it could not catch a revert and the comment claiming it
   held the explanation was false. It is now anchored to the paragraph that carries the
   corrected word and must find `952` there. Graded: the old assert passes on the pre-lap
   document, the new one **fails** on it.
6. **`measure_disc_centre_vs_ignition.py` stated a property it did not test** — it kept the
   *last* convention whose cell burns at `t = 0` while the artifact's `resolved_by` claimed
   exactly one does. It now refuses to write the artifact unless exactly one does.

Its `mandela` finding, recorded because it is the honest one: the spelling patterns, the
blast-radius counts and the probe sentences are all authored by the lap being audited, and
`f90a7fe` is the proof they can be self-confirming — three English patterns silently missed
a live instance for the length of a commit because they were never run against the shipped
sentence **with its markup**.

## The escalation this lap is filing

**NH-058**: two routines built the same backlog row in one window, and the loop has no
mechanism that would have stopped it. This lap claimed WFG-254 and pushed the claim to
`origin` **before** building, exactly as CHARTER §4 step 3 requires after NH-007. The paper
routine took the `paper/` half of the same row anyway, because **nothing tells the paper
routine to read the backlog**: CHARTER §12 scopes it by PATH (「touches nothing outside
`paper/`」), and a row whose surfaces span `docs/` and `paper/` is invisible to that scoping.
The cost this time was small and the tie broke cleanly. It will not always.

## Next row

**WFG-129**, which `docs/auto/DIRECTION.md` item 2 names and critic #64 pre-registered —
「if TWO dev laps have run and WFG-129 is still `todo`, that is finding #1」. The table's
next `todo` is WFG-255 at position 2, and CHARTER §14b says DIRECTION.md's order wins when
the two differ. **This lap did not get to either**; it spent its budget on the eight
surfaces, the artifact and the two gates.

## What an independent reviewer would still say about this lap

That the **process** worked and the **self-checks** did not, and the two are not the same
thing. The measurement was re-derived before a word moved, and it held to the digit. But
the lap then shipped a count that contradicted its own follow-up commit, an assert that
passed on the document it was written to reject, a registry note that declared one
unreachable instance where there are two classes, and a collision on the one line it wrote
last — and **every one of those was caught by the reviewer rather than by this lap.** The
gates it added are real; the checks it performed on itself were not adversarial. That is
the finding the next lap inherits.

## In plain terms

- **What changed for the project.** The sentence you say out loud to all five judges about
  the fairness test told them the comparison circle sits at the fire's starting point. Your
  own data file says that point is 19 km away from where the circle actually is — outside
  every circle the test drew. That sentence is fixed everywhere it appears, and the distance
  is now measured by a script and stored in a file instead of living in one report.
- **Why it matters to the judges.** 「Citing where a number came from」 is a named criterion
  worth 20 points on both scoring tables, and a judge who checks one locative and finds it
  wrong stops trusting the other forty. Nothing measured was wrong and nothing was weakened
  — only the words. A second sentence, that your model beat the circle on 「모양과 범위」, was
  claiming something the method makes impossible (both shapes are given the same area on
  purpose), so it now says what it actually means. And a test in your own repository had
  been holding that impossible wording in place; that is fixed too.
- **What you should do.** One new thing: **NH-058**. Two of your routines fixed the same
  problem in the same three hours without either knowing, and the rule meant to prevent that
  cannot see the paper routine at all. Nothing broke this time — the two halves fitted
  together and the loop resolved it without forcing — but it is a mechanism question and
  only you can pick the fix. Otherwise: **25 decisions are waiting and 2 have no
  date at all**; NH-046, NH-049 and NH-051 are now past due and NH-052 and NH-053 come due
  tomorrow. The sprint ends **2026-09-15**. Answering even three unblocks work that is
  currently parked — the easiest way is to open Claude Code in the repository and say
  `decisions`.



## Gates

**ALL GREEN** · mode `full` · head `1580dd8` · 2026-09-11T11:12:17Z · ⚠ **stale: the gates read `1580dd8`, HEAD is `5590b3d`** — this table does not certify the pushed tree; re-run `gates.py --mode full` and `gates.py --assert-head` before pushing

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 29.0 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.2 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 0.5 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 503.5 s | 2112 passed, 65 skipped, 3 xfailed, 52 warnings in 501.31s (0:08:21) |

## Commits since the previous report (748e89f49823b00a1d693819e0fede3216fe1a35..HEAD)

- 5590b3d the report a gate refused is annotated rather than deleted, and the lap ships the regenerated stamp
- 78647c5 report: 2026-09-11T1052Z dev
- 1688df1 the reviewer blocked this lap on its own last line, and on an assert that passed the text it was written to reject
- 7b1d818 the sweep that declared seven surfaces complete missed an eighth, and two asterisks are why
- 5a01968 the null circle is placed where the measurement put it, on all seven surfaces that said otherwise
- d67de57 the paper report names the head it ships in, and names the assertion rather than a sha its gate table cannot know
- 5d2d603 the lap report annotates the per-slice radius it quotes, so the gate reads the quotation as one
- c3bd2bb the null circle is named where the measurement put it, and the figure that said otherwise is reissued rather than overwritten
- d4b7bef claim WFG-254 (20260911T0921Z)

## Needs a human (30 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09, one day past; raised to HIGH by critic #55 on a measured third instance)
- NH-036 [DECISION] One critic lap told the next one not to edit a file, and that is what kept a false sentence in front of a judge for a window (by 2026-09-10)
- NH-037 [DECISION] The paper's word proxy now stops it a thousand words before your 25-page rule (by 2026-09-10)
- NH-038 [DECISION] Your "product first" rule has spent the last three dev laps on documents, and the readiness line it was written to protect has not moved in five critic laps (by 2026-09-09)
- NH-039 [DECISION] The national wildfire-spread system's manual is an 18 MB PDF the sandbox could not fetch, and one of you can (by 2026-09-12)
- NH-040 [FYI] A critic lap pushed one commit past a red `--assert-reported`, and it is telling you rather than hiding it
- NH-041 [FYI] This lap sent you an email containing only the word PLACEHOLDER, and could not take it back
- NH-042 [DECISION] Two of your own rules collide whenever a withdrawn claim lives in a frozen artifact, and this week they collided three times (by 2026-09-10)
- NH-043 [DECISION] A gate your loop built this morning will go red about twice a day, and the charter tells the lap that meets it to stop working (by 2026-09-09)
- NH-045 [BLOCKER] The staleness gate has closed `auto/dev` to every routine, and the one routine that met it is the one forbidden to clear it (by 2026-09-08)
- NH-044 [DECISION] The claim the paper just retracted is still live on the page the paper cites for it (by 2026-09-09)
- NH-046 [DECISION] Your product's definition-of-done names a command nothing in this project has ever run (by 2026-09-10)
- NH-048 [DECISION] One of the research routine's three literature channels has been dead for two runs, and a working replacement is already proven (by 2026-09-10)
- NH-049 [DECISION] Your critic routine is told to add judge Q&A cards and your own printing gate makes that impossible for it (by 2026-09-11)
- NH-050 [DECISION] You answered two of these questions two days ago and the loop never heard you, because you answered them on the routine page (by 2026-09-10)
- NH-051 [DECISION] The rule your loop has been obeying for three days is not the option it names, and the difference is why one row has been pushed down five times (by 2026-09-11)
- NH-052 [DECISION] The experiment that measures what your own forecast is worth is now one run away, and it would change what 42 means (by 2026-09-12)
- NH-053 [DECISION] The word your front door uses for 42 stopped being right today, and the loop cannot pick its replacement (by 2026-09-12)
- NH-054 [DECISION] Four fifths of your front door's headline is limits, and no lap is allowed to decide whether that is the project's strength or its biggest presentation risk (by 2026-09-13)
- NH-055 [DECISION] Your front door compares the headline forecast number to a model the same page calls broken, and today the loop measured a second, harsher comparison it is not allowed to put beside it (by 2026-09-13)
- NH-056 [DECISION] A Korean university published a reproducible deep-learning wildfire model four weeks before your finals, and a lap may not decide on its own whether to mention it (by 2026-09-13)
- NH-057 [DECISION] The one thing your project now says it contributes has never once been produced from a real fire, and only your laptop can change that (by 2026-09-13)
- NH-058 [DECISION] Two of your routines built the same row in the same window, and the rule that was supposed to stop that cannot see the paper routine (by 2026-09-13)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
