# WildfireGuardian autoloop · dev · 2026-09-07T1256Z

| | |
|---|---|
| when | 2026-09-07 12:56 UTC · 2026-09-07 21:56 KST |
| branch / head | `auto/dev` / `0713c7e` |
| kind | dev |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 8, done: 25, parked: 1, todo: 84 |

**Head annotation.** `report.py` stamps the table above at the head it read, and the report ships
in the commit after it. This report was **generated at `0713c7e`** (the claim commit) and **ships
in `1f024ed`**, which is the work commit; both ids resolve on `origin/auto/dev`. The gate table
below is therefore the run at `0713c7e`. **`gates.py --mode full` was re-run on `1f024ed`, the
exact commit pushed, and is ALL GREEN, exit 0** (`1656 passed, 56 skipped, 2 xfailed`, 377.1 s,
warm). `--assert-head` (`head=1f024ed`, written 13:04:30Z) and `--assert-reported` (12 substantive
paths travelling with this report) both exited **0** immediately before the push, and the push
needed no rebase.

## What happened this lap

# dev lap 2026-09-07T1222Z — WFG-144 (the card) and WFG-146 (the date on the printed page)

Claim commit `0713c7e`. Rows: **WFG-144** (P0, table position 1 and DIRECTION position 1) and
**WFG-146** (critic #34's one `fix-before-next-row` item). Both needed the same single kit
rebuild (WFG-152), so one lap paid for both. Both are `done`.

**Reviewed by: subagent (block): the lap narrowed an over-scoped claim in one file, graded the
narrowing by grepping for the sentence it had just written, and asserted completeness in the
student's card — while the same claim stood in a wider wording in `docs/dispatch_ordering.md`
§8, which is the page that card's own 근거 line sends the judge to.** Fixed rather than argued;
what the block cost and what it bought is the honest centre of this lap and is set out below.

## Your decisions, applied

**Nothing new on either channel, and one line is all it deserves.** The Gmail search
(`from:siyeong0318@gmail.com subject:"WildfireGuardian autoloop" newer_than:14d`) returns 20
threads and every one is a single message this loop sent — there is no reply from you. PR #31
has no comments. `docs/auto/decisions_seen.json` is unchanged; no `NH-###:` line was parsed or
applied. **NH-039** (the NIFoS user-guide PDF) and **NH-043** are open and unanswered, and both
now cost something real rather than being paperwork: NH-039 is the reason three separate
sentences in this lap had to be weakened to 「공개된 자료에서는 확인되지 않습니다」.

## What shipped

**WFG-144 — the card.** `docs/auto/JUDGE_QA.md` gained **Q16a · T0** in §3 (재난대응 실무자),
answering 「산림청·경기도가 이미 산불확산예측을 하고 있는데, 무엇이 다릅니까?」. Seven
consecutive critic laps measured that gap; six declined to promote it because §14b allows one
item and it always went elsewhere. The answer is the **output object** and nothing else: NIFoS's
console is for a suppression commander and takes a hand-entered origin point; G-DAPS is for a
civil-defence officer and resolves damage to the 읍면동 in 30-minute steps; this project's unit
is the **house**, and its output is 「지금 걸어 나갈 수 있는가, 없다면 누구를 먼저 데리러
가는가」. Its 없는 것 block carries three refusals, each paired with the sentence the student
says instead — ❌ 「저쪽은 가구 단위로는 못 합니다」 against ⭕ 「**공개된 자료에서는** 가구
단위 산출물이 확인되지 않습니다」; no accuracy comparison in either direction; and no claim
about whether either system covers 경상북도·영덕, which is not established.

**WFG-146 — the date.** The repository paired 사이언스타임즈 with 연합뉴스's date. Corrected to
**「사이언스타임즈 2026-02-13 (연합뉴스 2026-02-12 기사 전재)」** in all seven live sites, each
with a dated note and the superseded value annotated rather than deleted (CHARTER §3.5):
`KOREAN_OPERATIONAL_SYSTEMS.md` §1 and §6 (§6 holds the one canonical explanation; the rest
point at it), `docs/related_work.md` §2 and §5, `RELATED_WORK_PANEL.md`, `WEEKLY_2026-W36.md`,
`NEEDS_HUMAN.md` NH-039, and WFG-144's own cell in `BACKLOG.md`. Record-class files that quote
the error on purpose (reports, `KCF_READINESS.md`, `CRITIC_LATEST.md`, `SCORECARD.md`) are left
alone by design. I did not inherit the reading: I re-fetched the page in this sandbox — byline
`연합뉴스 2026-02-13`, copyright `저작권자 2026-02-13`, and `2026-02-12` present only inside
the Yonhap image-CDN path `AKR20260212072300063_02_i_P4.jpg`. That is the **third** independent
fetch (critic #29, critic #34, this lap); the reviewer made it a fourth and also re-fetched the
경향신문 G-DAPS article. All four agree.

**The kit and the bundle.** `WFG_printables_20260907T1248Z.pdf`, **38 pages** (was 36), sha256
`4504f5984cb9`. `build_finals_bundle.py --update` re-pointed `release/kcf-finals-2026/MANIFEST.json`
at it (19 files, no residual `0953Z`) — without that the stick would still carry the kit with
the wrong date, which is WFG-151's whole point.

## The root objection, the block, and what they cost

**My own `hate` objection was right in kind and I then failed its test.** The objection I
recorded before building was that the card's differentiator is built out of **negative claims
about someone else's operational system**, sourced from a catalogue page and two newspapers —
the WFG-049 shape. The cheapest test was to look for negative claims of that form already
standing in the repository. I ran it, found one in Q16 immediately above the card I was writing
(「비교한 어느 **시스템**도 이 값을 계산하지 않습니다」 — true while the compared set was
academic papers, an assertion about an unopened manual once `d2640ca` widened it), narrowed it
to 연구 scope, and then **certified my own narrowing by grepping for the sentence I had just
written.** It returned one file. I wrote 「이 문장은 `docs/auto/JUDGE_QA.md`에만 있었습니다」
into the student's card and 「and nowhere else」 into this summary.

**The reviewer found the claim alive one word wider.** `docs/dispatch_ordering.md` §8:
「같은 규칙, 같은 파라미터에서 나온 이 대비가 결과이고, **다른 어떤 체계도** 이 값을 계산하지
않습니다」 — not 「비교한 어느…」 but an unrestricted universal negative, in that document's
확정된 서술 section for contribution ②, and **`docs/auto/JUDGE_QA.md:615` sends the judge to
exactly that page as Q16's 근거.** The card would have said the careful thing out loud while its
cited evidence said the reckless thing on paper: the critic-#26 / WC-004 shape, committed by the
lap writing the paragraph that faults it.

`mandela` names why the check could not have worked: the search string and the searched-for
claim were written by the same party in the same edit, so no external ground truth ever entered
and the grep could only confirm. CHARTER §3.5c had already written the mechanism down —
「a reworded assertion escapes」 — and I had filed that as a fact about the registry rather than
about any spelling-based check, my own included.

**Cleared, in the shape the reviewer specified.** `docs/dispatch_ordering.md` §8 narrowed to
「`docs/related_work.md`가 조사한 어느 **연구**도 이 값을 내지 않습니다」 with a dated §3.5
annotation keeping the old wording; Q16's 없는 것 note and this summary now **name that file**
instead of claiming there was none, and say plainly that the reviewer found it, not the lap.

**Three secondary findings, all judge-facing, all taken — and two of them caught me repeating
the very failure I had just conceded.**

1. Q16a said 「실제 격차이고 **저쪽이 낫습니다**」 about NIFoS's 5 m — a present-tense
   superiority verdict resting on a **future-tense plan announcement**, three paragraphs before
   the same card bars comparing that exact figure 「어느 방향으로도」. Reworded to the
   announcement (「올리겠다고 밝혔고 … 아직 기관의 계획 발표입니다」). ⚠ The same sentence
   stood in `RELATED_WORK_PANEL.md` (printed, on the stick), `docs/related_work.md` §2 and the
   knowledge note §1 — **fixed in all four**, with the superseded wording kept. The reviewer
   flagged only the card; had I fixed only the card I would have completed the propagation
   shape a second time inside one lap.
2. 「읍면동은 보통 **수천 명**이 사는 행정구역」 is an unsourced population claim and is wrong
   for two of the three unit types (읍 carries a statutory 인구 2만 이상 기준; urban 동 run to
   tens of thousands; 수천 fits only a rural 면) — in front of the disaster-response judge this
   card is written for. Replaced in the card, the panel and the note with what the argument
   actually needs: 읍면동 is an administrative area holding many villages, not one house.
3. Q16's narrowed sentence pointed at the wrong document (`JUDGE_QA.md` compares nothing; the
   survey is `docs/related_work.md`). Repointed.

## Gates

`gates.py --mode full` **ALL GREEN, exit 0**, twice — before the block and again on the tree
that became this commit. `verify`, `snapshot-verify`, `env-check` PASS; `baseline-verify` WARN
is the documented CHARTER §3d state. `tests/test_judge_qa_bank.py` 24 passed.

⚠ **Cold and warm, and the cold one downloaded (WFG-139, eighth consecutive measurement).** The
**cold** run — container start had `data/raw/` holding only `.gitkeep` and `README.md`, mtime
2026-09-04T16:57Z — reported **1650 passed, 62 skipped, 2 xfailed** in 375.5 s, and afterwards
`data/raw/dem/srtm/N36E129.hgt` (**25,934,402 B**) and its `.gz` carried mtime **12:34:35Z**,
inside that run's own window: the suite fetched 25.9 MB over the network during a gate. The
**warm** run, with the DEM already on disk, reported **1656 passed, 56 skipped, 2 xfailed** in
363.9 s. The six tests that move between the two columns are the terrain tests that have never
run in CI, which is the whole of WFG-139.

⚠ **One observation I could not explain, filed rather than claimed.**
`data/raw/kfs_fire_statistics/산림청_산불통계데이터_20250911.csv` (195,891 B) carries mtime
**12:19:23Z** — before `bootstrap.sh` finished (12:21:20Z) and before any gate ran. I did not
establish what wrote it and I am **not** asserting a second network reach; the mtime is recorded
for whoever settles it.

## What did not work, and the residue

**Q16a is a letter suffix because a new number was structurally impossible.** Card ids run
Q1..Q35 and `test_question_numbers_are_unique_and_contiguous` requires the distinct bases to be
exactly 1..N. Q36–Q40 are already spoken for by the 「아직 답이 없는 질문」 table (Q36 is the
forecast-aware-arm question, WFG-125), so the next free number is **41**, and a card at 41
leaves a gap at 36–40 and turns the gate red. The file's suffix convention exists precisely so a
bank the student is memorising and four documents cite by number is never renumbered — and Q16
turned out to be the right parent for a real reason rather than a convenient one, since Q16 is
also the card whose over-scoped sentence this work had to narrow. **The residue: the bank cannot
accept a genuinely new question at a new number until Q36–Q40 have cards.** Not this row's to
fix; the next critic should file it.

**Three kit stamps were generated and one committed, deliberately.** `1228Z` (37 pp) was built
from the text the reviewer blocked and `1246Z` from a fix that was still incomplete; neither was
ever committed, so CHARTER §3.2 never attached and both were discarded rather than frozen. The
0705Z lap paid for two permanent stamps because it committed before it looked at the pages. Only
`1248Z` is in this commit.

**One thread I deliberately did not pull, so it is visible rather than silent.**
`KOREAN_OPERATIONAL_SYSTEMS.md` §3.3 still reads 「a 500 m grid on ERA5 is coarser than a 5 m
terrain analysis on operational weather」 — the same present-tense-verdict-on-a-plan-statement
shape as finding (1), pointed *against* this project rather than for it, and the reviewer did
not flag it. Widening the sweep on my own judgement after a block is how a lap invents new
errors; it is named here for the critic instead.

I did not touch `KCF_READINESS.md`. The critic ticks it with evidence (CHARTER §10), and a dev
lap ticking its own work is the closed loop that file exists to avoid.

## Next row

**WFG-110** (P0, one lap) — DIRECTION position 2 and the sole remaining blocker of readiness
line R1: `scripts/finals.template.html` references 28 registry keys, `DEMO_SCRIPT_5MIN.md` §3
maps 22 of them the wrong way round, and 6 sit in no committed mapping table. Then **WFG-139**.

## In plain terms

- **What changed for the project.** Your booth kit now has an answer to the question a
  disaster-response judge is most likely to ask — 「산림청이랑 경기도가 이미 이걸 하고 있는데
  뭐가 다르냐」 — and the answer is that they tell a commander where the fire goes while you tell
  one household whether it can walk out. A wrong newspaper date that was printed on the kit is
  fixed everywhere, and the USB bundle now carries the new 38-page kit.
- **Why it matters to the judges or the science.** 「기존 연구와의 차별점」 is a scored line on
  both judging tables, and you now have a rehearsed answer that never claims to be more accurate
  than a government system — because we cannot show that, and the card tells you to say so. An
  independent reviewer stopped this lap for asserting something about a manual nobody has read;
  three of those over-broad sentences were removed from pages judges will hold.
- **What you should do, if anything.** Nothing is required. If you have five minutes, **NH-039**
  is still the one ask that would change this card: download the NIFoS user guide PDF and drop
  it in the repo folder, and the card can describe the national system from its own manual
  instead of from a newspaper. Reply to this email with `NH-039: A` or `NH-039: skip`.



## Gates

**ALL GREEN** · mode `full` · head `0713c7e` · 2026-09-07T12:55:12Z · current at `0713c7e`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 23.6 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.2 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 0.5 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 363.9 s | 1656 passed, 56 skipped, 2 xfailed, 52 warnings in 361.80s (0:06:01) |

## Commits since the previous report (2720840274ad41a52a5bbc4c0a819011ca8cb23f..HEAD)

- 0713c7e claim WFG-144 with WFG-146 (20260907T1222Z)
- 1a5caa2 critic #34: the caveat reached every surface a judge meets and missed the page every lap reads first

## Needs a human (16 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09)
- NH-036 [DECISION] One critic lap told the next one not to edit a file, and that is what kept a false sentence in front of a judge for a window (by 2026-09-10)
- NH-037 [DECISION] The paper's word proxy now stops it a thousand words before your 25-page rule (by 2026-09-10)
- NH-038 [DECISION] Your "product first" rule has spent the last three dev laps on documents, and the readiness line it was written to protect has not moved in five critic laps (by 2026-09-09)
- NH-039 [DECISION] The national wildfire-spread system's manual is an 18 MB PDF the sandbox could not fetch, and one of you can (by 2026-09-12)
- NH-040 [FYI] A critic lap pushed one commit past a red `--assert-reported`, and it is telling you rather than hiding it
- NH-041 [FYI] This lap sent you an email containing only the word PLACEHOLDER, and could not take it back
- NH-042 [DECISION] Two of your own rules collide whenever a withdrawn claim lives in a frozen artifact, and this week they collided three times (by 2026-09-10)
- NH-043 [DECISION] A gate your loop built this morning will go red about twice a day, and the charter tells the lap that meets it to stop working (by 2026-09-09)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
