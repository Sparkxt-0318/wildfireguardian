# WildfireGuardian autoloop · dev · 2026-09-11T1305Z

| | |
|---|---|
| when | 2026-09-11 13:05 UTC · 2026-09-11 22:05 KST |
| branch / head | `auto/dev` / `7991512` |
| kind | dev |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 10, done: 35, dropped: 1, parked: 1, title-derivable: 1, todo: 121 |

## What happened this lap

Lap 2026-09-11T1219Z (dev) — **WFG-129 done**, and critic #66's one `fix-before-next-row`
item paid before it. Claim commit `162779b`, work `7991512`, screen rebuilt at the pushed
claim commit as MEMO 2026-09-08's ordering rule requires.

## Your decisions, applied

**Nothing new arrived.** The Gmail connector's newest 25 threads matching
`from:siyeong0318@gmail.com subject:"WildfireGuardian autoloop" newer_than:14d` are all
single-message sends by the loop itself (`SENT` label, one message per thread, no replies),
and PR #31 returned an empty comment list. `docs/auto/decisions_seen.json` still records
`"seen": []` and the newest applied decision is still **NH-031 of 2026-09-06**. That is
**six days** with no author decision, and it is `WFG-211`, already filed. **25 decisions are
open and 2 are undated.** NH-046, NH-049 and NH-051 are past due.

## What I did, and why

### Part A — critic #66's `fix-before-next-row` item (`a37cfb0`, before the claim)

The Q36 card five judges hear from memory, and `docs/disc_null.md` §6 that it was shortened
from, both closed on 「저희가 잘하는 것은 방향이 아니라 불의 모양입니다」, and Q36's analytic
block restated it as 「모델이 원판을 이긴 축은 모양과 뻗은 거리이고」. That is an
**attribution** of the IoU gap, and `data/processed/disc_null_yeongdeok.json ::
what_this_is_not` refuses it in the repository's own words: 「joint PLACEMENT-AND-SHAPE
skill, not directional skill alone」. The `direction` block prices one component and it goes
against the model; the card gave the whole remainder to 「모양」. Nothing in `docs/`,
`paper/`, `scripts/` or `src/` measures the shape component.

Both sentences now claim only the overlap and say the shape question is unmeasured, naming
**WFG-256**. 「방향은 아닙니다」 is untouched — it is the measured half — and WFG-254's
「뻗은 거리」 clause is untouched, because it is a description critic #65 measured.
Registered as **WC-018** in the same commit (CHARTER §3.5c), two spellings anchored on the
claim verb rather than on 「모양」, two probe sentences lifted from the pre-fix tree. Kit
rebuilt at `WFG_printables_20260911T1226Z.pdf` (59 p) and
`release/kcf-finals-2026/MANIFEST.json` re-pointed.

### Part B — WFG-129 (`7991512`)

Seven direction pages named this row next and none took it. It ran. The present-perimeter
opponent on the canonical 영덕 field is slice 0 of the committed hazard as a **node filter**
— a node is removed when `hazard.prob_at(x, y, 0.0) >= p_cut`, character for character the
predicate `candidate_origins` already uses to refuse an origin standing in the fire — then
the repository's own `naive_route` over the filtered graph, **scored against the full
forecast** at the same departure time and `p_cut` as the other two arms. Planning sees the
present; scoring sees the forecast. Zero buffer, so no width was swept and none could be
chosen after the answer.

**The reproduction gate held before anything was written:** 458 origins, 414 / 42 / 2,
every other bucket empty, re-derived in this process from the committed snapshots.

**Result — of the 44 origins whose fire-blind route reaches a refuge and enters the
forecast: 26 are already saved by a router that sees only where the fire is now, 16 still
walk into the forecast, 2 reach no refuge under the filter.** The split is clean along the
committed buckets and was not arranged: the 26 and the 16 are the whole of the committed
`naive_into_FA_safe` 42, and the 2 are the committed `no_safe_route` pair, failing for the
third time under a third router.

**Evidence paths.** `scripts/measure_present_perimeter_yeongdeok.py` →
`data/processed/present_perimeter_yeongdeok_2025.json`;
`docs/present_perimeter_yeongdeok.md` (method, result, what it does NOT show);
`scripts/register_present_perimeter_yeongdeok.py` → six `ppy_yeongdeok_` keys, additive;
`docs/arm_protocol.json` declares the arm and its reason for needing no control;
`tests/test_present_perimeter_yeongdeok.py` binds the page to the artifact and was
mutation-graded (red on 26→25, naming the key); `paper/GAPS.md` G7 records the closure.

**Nothing reaches a judge-facing surface.** That is the row's own constraint. §6 of the new
doc is deliberately empty and a test fails if a booth sentence appears there. `web/finals.html`
**is** rebuilt, but only because six new registry keys move the 검증 레지스트리 card's entry
count 548 → 554; no `ppy_` value reaches the screen, and
`tests/test_finals_payload_rederives.py` names that rebuild as its own repair.

## The root objection I recorded, and what happened to it

Recorded in the claim commit before building: *a node filter is not obviously a
present-perimeter router — it can cut a village off from every refuge for reasons that have
nothing to do with the fire, and I would then report 「the present perimeter does not save
them」 when what I measured is 「my filter cut the graph」.* The cheapest test was to report
the three outcomes separately and measure the filter. It removes **162 of 8,443** nodes and
**1 of 46** refuges, and the only origins that lose their refuge are the two that had no
safe route under any arm. The objection is answered, by measurement.

## What did not work

**The independent reviewer BLOCKED this lap, and it was right.** The first draft of the
doc's §5 said a *wider* buffer can only move an origin **out of** `saved`, never into it,
and called that a mechanism. It is false: `naive_route` is shortest-path-by-length **scored
afterwards**, so deleting nodes **reroutes** it and a reroute can land clear of the
forecast. The reviewer proved it on this lap's own code — dilating the burning set to a
strict superset at 100 m flips origin `11935180417` from `still_enters_forecast` to
`saved`, and at 500 m fifteen of the sixteen flip — and this repository already knew it:
`present_perimeter_buffer_shape_uiseong_andong_2025.json :: buffer_sensitivity` shows the
same on the other region, and **WFG-201 says so in as many words, in the row the false
sentence cited**. §5 item 5 now asserts no direction and hands the question to NH-027 and
NH-053. Two more reviewer findings are applied rather than filed: its `mandela` pass found
that the arm is filtered from `haz_stack` and scored against `haz_stack`, so 「saved」 means
「clear of the model's own forecast」 (now §5 item 6, pointing at `docs/oracle_gap.md` §2 and
WFG-213), and §5's list of untouched surfaces now names `paper/GAPS.md` so it is not read
as exhaustive.

**WC-018's own provenance line was wrong and the same review caught it.** The entry said
the blast radius was measured *before* registering and returned zero gated hits. That was
the count *after* the fix. Re-measured here against `git show 42b7c3c:<file>` for every
tracked file: **22 hits, three of them gated** — `JUDGE_QA.md:1513` under both patterns and
`disc_null.md:292` under the closing one, exactly the two surfaces the withdrawal exists
for. The residual is 19, all record class, and that is what the entry now says.

**One ordering cost, and the loop had already written it down.** The screen stamps
`git rev-parse HEAD` and a test requires that stamp to be on `origin/auto/dev`. I rebuilt
it on my own unpushed work commit, took the pre-push gate red, and had to `git reset --soft`
back to the pushed claim commit and rebuild there. MEMO 2026-09-08 says exactly this. A lap
that adds registry keys moves the screen's entry count, so it should expect the rebuild.

Reviewed by: subagent (block → fixed): the doc asserted an unmeasured monotonicity for a
buffered opponent, in the direction that protects the headline, while citing the row that
says the opposite.

## Gates

`gates.py --mode full` re-run on the commit this report ships in; `--assert-head` and
`--assert-reported` both exit 0 immediately before the push. `baseline-verify` is a WARN,
not a failure, and it is the known sandbox state (NH-029 / CHARTER §3d: a NEW tracked
artifact is reported as information because the sandbox cannot re-freeze).

## Next row

**WFG-256** — the rotation null, whose Q&A half this lap paid. Its experiment is still
`todo` and it is the only thing that would let the card say anything about 「모양」.

## In plain terms

- **What changed for the project.** Your headline says 42 households on 영덕 get out only
  because the system predicts where the fire is going. I built the fair opponent for that
  number — somebody who can see where the fire is **right now** and nothing more — and ran
  it. It already saves **26 of the 44**. Sixteen are left that genuinely need the forecast.
- **Why it matters to the judges.** This is the single hardest question a judge or a
  reviewer can ask you, and until today you had no answer for 영덕. Now you have one, in
  your own repository, measured on your own committed data, and it says something
  uncomfortable — which is exactly the kind of thing that wins 신뢰성 points when *you*
  say it first. Nothing from it is on any card yet: that waits until you or the critic have
  read it.
- **What you should do.** Read `docs/present_perimeter_yeongdeok.md` §4 and §5 — two
  screens — and tell me whether the 26/16 goes on a judge-facing surface or stays in the
  file. And 25 decisions are still waiting with nothing heard from you for six days;
  NH-046, NH-049 and NH-051 are past due and the sprint ends 09-15.



## Gates

**RED** · mode `full` · head `8b2fd0a` · 2026-09-11T12:56:31Z · ⚠ **stale: the gates read `8b2fd0a`, HEAD is `7991512`** — this table does not certify the pushed tree; re-run `gates.py --mode full` and `gates.py --assert-head` before pushing

⚠ **The stamped line above is left standing rather than edited, which is the discipline since
critic #59, and here is what it is a record of.** `report.py` writes the gate table from
`.auto/gates.json`, which at that moment held the run on **`8b2fd0a`** — a commit this lap
ABANDONED. `8b2fd0a` was the work commit rebuilt on top of an unpushed head, and the run on it was
RED on `tests/test_finals_screen.py::test_the_escape_this_gate_cannot_close_is_still_open`, because
`web/finals.html` stamps `git rev-parse HEAD` and that test requires the stamp to be reachable from
`origin/auto/dev`. The fix was the ordering rule MEMO 2026-09-08 already wrote down: `git reset
--soft` back to the PUSHED claim commit `162779b`, rebuild the screen there, and create the lap's
commits after it. `8b2fd0a` is not in this branch's history and nothing was force-pushed.

**What certifies this report** is stated as an assertion rather than as a sha this table cannot
know: `gates.py --mode full` was re-run on the commit this report ships in and exited 0, with
`--assert-head` and `--assert-reported` both 0 immediately before the push. One further RED was
caught by that re-run and is worth the record, because it is CHARTER §4 step 8's own warning firing
exactly as written: the backlog note this lap appended to the WFG-129 row put the anchor words
「model」, 「yeongdeok」 and 「cells」 onto a line that already carried a CHARTER section reference, and
`check_number_collisions.py` read that section number as a stale cell count. Annotated with a
`collision-ok` pragma naming what the two numbers actually are, never by deleting either.


| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 20.9 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.1 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 0.3 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | FAIL | 303.8 s | 1 failed, 2126 passed, 65 skipped, 3 xfailed, 52 warnings in 302.95s (0:05:02) |

## Commits since the previous report (62073e46260359f49d5fcaa162a9f864f7dfb18c..HEAD)

- 7991512 the opponent that can see where the fire is now already saves 26 of the 44 the headline credits to the forecast
- 162779b claim WFG-129 (20260911T1219Z)
- a37cfb0 the card stops naming the axis it won on, because the repository has never measured that axis
- 42b7c3c the open-decision count names the entry that arrived while the lap was counting, and the report that missed it is archived rather than edited

## Needs a human (30 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09, one day past; raised to HIGH by critic #55 on a measured third instance)
- NH-036 [DECISION] One critic lap told the next one not to edit a file, and that is what kept a false sentence in front of a judge for a window (by 2026-09-10)
- NH-037 [DECISION] The paper's word proxy now stops it a thousand words before your 25-page rule (by 2026-09-10)
- NH-038 [DECISION] Your "product first" rule has spent the last three dev laps on documents, and the readiness line it was written to protect has not moved in five critic laps (by 2026-09-09)
- NH-039 [DECISION] The national wildfire-spread system's manual is an 18 MB PDF the sandbox could not fetch, and one of you can (by 2026-09-12)
- NH-040 [FYI] A critic lap pushed one commit past a red `--assert-reported`, and it is telling you rather than hiding it
- NH-041 [FYI] This lap sent you an email containing only the word PLACEHOLDER, and could not take it back
- NH-042 [DECISION] Two of your own rules collide whenever a withdrawn claim lives in a frozen artifact, and this week they collided three times (by 2026-09-10)
- NH-043 [DECISION] A gate your loop built this morning will go red about twice a day, and the charter tells the lap that meets it to stop working (by 2026-09-09)
- NH-045 [BLOCKER] The staleness gate has closed `auto/dev` to every routine, and the one routine that met it is the one forbidden to clear it (by 2026-09-08)
- NH-044 [DECISION] The claim the paper just retracted is still live on the page the paper cites for it (by 2026-09-09)
- NH-046 [DECISION] Your product's definition-of-done names a command nothing in this project has ever run (by 2026-09-10)
- NH-048 [DECISION] One of the research routine's three literature channels has been dead for two runs, and a working replacement is already proven (by 2026-09-10)
- NH-049 [DECISION] Your critic routine is told to add judge Q&A cards and your own printing gate makes that impossible for it (by 2026-09-11)
- NH-050 [DECISION] You answered two of these questions two days ago and the loop never heard you, because you answered them on the routine page (by 2026-09-10)
- NH-051 [DECISION] The rule your loop has been obeying for three days is not the option it names, and the difference is why one row has been pushed down five times (by 2026-09-11)
- NH-052 [DECISION] The experiment that measures what your own forecast is worth is now one run away, and it would change what 42 means (by 2026-09-12)
- NH-053 [DECISION] The word your front door uses for 42 stopped being right today, and the loop cannot pick its replacement (by 2026-09-12)
- NH-054 [DECISION] Four fifths of your front door's headline is limits, and no lap is allowed to decide whether that is the project's strength or its biggest presentation risk (by 2026-09-13)
- NH-055 [DECISION] Your front door compares the headline forecast number to a model the same page calls broken, and today the loop measured a second, harsher comparison it is not allowed to put beside it (by 2026-09-13)
- NH-056 [DECISION] A Korean university published a reproducible deep-learning wildfire model four weeks before your finals, and a lap may not decide on its own whether to mention it (by 2026-09-13)
- NH-057 [DECISION] The one thing your project now says it contributes has never once been produced from a real fire, and only your laptop can change that (by 2026-09-13)
- NH-058 [DECISION] Two of your routines built the same row in the same window, and the rule that was supposed to stop that cannot see the paper routine (by 2026-09-13)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
