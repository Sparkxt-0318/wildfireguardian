# WildfireGuardian autoloop · manual · 2026-09-08T1224Z

| | |
|---|---|
| when | 2026-09-08 12:24 UTC · 2026-09-08 21:24 KST |
| branch / head | `auto/dev` / `2c6e366` |
| kind | manual |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 8, done: 26, parked: 1, title-derivable: 1, todo: 96 |

## What happened this lap

# WFG-187 — the judged screen, rebuilt before the branch shut

This is the `fix-before-next-row` item critic #42 set, pushed on its own and
before this lap claims a row, which is what the item asked for. The dev lap's
own row (WFG-010) follows in a separate push with its own report.

## What was wrong

`web/finals.html:434` carried `"git":"1bca8ed"`, built 2026-09-07T22:21Z.
`git rev-list --count 1bca8ed..HEAD` answered **29** at `25f6b60`, against
`tests/test_finals_screen.py:540` `STAMP_MAX_COMMITS_BEHIND = 30`. **One commit
of headroom.** This lap's own claim commit would have spent it, and the commit
after that would have turned `auto/dev` red for every routine — the dev lap, the
critic lap at 13:57Z and the paper lap — before any work was done. That is
NH-045 arriving for the third time.

Critic #42 filed this with five commits of headroom rather than at the limit,
and then recorded that its own four bookkeeping commits took the headroom from
five to one. That is the sharpest evidence NH-043 has had: **the only routine
that can measure this drift is one that is forbidden to fix it**, because the
critic may not run `make finals` (DIRECTION: `web/finals.html` is not a
printables `SOURCES` file, but the critic changes no code), so every measurement
it publishes makes the number it published worse.

## What was done

`make finals PYTHON=.auto/venv/bin/python` — one command, no decision. The
screen is rebuilt at `25f6b60` and the count goes **29 → 0**; the commit
carrying it sits at **1**, so the next lap meets the full 30 again.

`release/kcf-finals-2026/MANIFEST.json` is re-pointed in the **same commit**.
The bundle hashes `web/finals.html`, so the rebuild alone turned
`tests/test_finals_bundle.py` red on two assertions
(`test_every_hash_in_the_manifest_is_the_hash_of_the_source_file` and
`test_rebuilding_the_manifest_reproduces_the_committed_one`), each naming the
fix in its own failure message: `make finals-bundle UPDATE=1`. That is WFG-152's
rule — a payload rebuild and its manifest travel together — applied to the
screen rather than to the printables kit. A lap that ran `make finals` and
stopped there would have pushed a red tree.

Evidence: `make finals` reported `verify-numbers OK`, `check-forbidden OK`,
`check-region-literals OK`, `web/finals.html: PASS`;
`pytest tests/test_finals_screen.py tests/test_finals_bundle.py` is **46 passed**
where it was 44 passed / 2 failed before the manifest was re-pointed.

## What this is NOT

**It is not the structural fix, and it must not be read as one.** WFG-159,
WFG-160 and WFG-161 carry the recurrence, and **NH-043 (due 2026-09-09,
unanswered)** is the author's decision on what a lap may do when this gate
fires. This commit buys the time for that answer and nothing more. The class
recurs about every 18 hours at the branch's measured rate.

Reviewed by: n/a (single command with no judgement in it; the independent
subagent review runs on this lap's actual row, WFG-010, per LOOP_CONFIG
`review: subagent`).

## In plain terms

- **What changed for the project.** The screen the judges stand in front of was one commit away from locking the whole project's branch — nothing could have been pushed until someone rebuilt it. I rebuilt it, and the alarm is back from "one commit left" to "thirty".
- **Why it matters to the judges or the science.** Nothing a judge sees changed; the screen shows the same numbers. What changed is that the project can keep being worked on today, and the screen now honestly reports the build it actually is.
- **What you should do.** Nothing for this one. But this is the third time this alarm has nearly stopped the work, and **NH-043 — due tomorrow, 09-09 — is the question that ends it.** It is worth answering before the others.



## Gates

**ALL GREEN** · mode `quick` · head `25f6b60` · 2026-09-08T12:22:20Z · ⚠ **stale: the gates read `25f6b60`, HEAD is `2c6e366`** — this table does not certify the pushed tree; re-run `gates.py --mode quick` and `gates.py --assert-head` before pushing

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 24.9 s | === make verify: PASSED === |
| pytest-quick | PASS | 12.7 s | 40 passed in 11.30s |

## Commits since the previous report (26ff1d45b81c80d5260e7d3ffe00e554273e0a9e..HEAD)

- 2c6e366 WFG-187: the judged screen was one commit from closing the branch, and it is now rebuilt with 29 of headroom
- 25f6b60 the report the assert asked for, and the number it exists to state moved while it was being stated

## Needs a human (19 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09)
- NH-036 [DECISION] One critic lap told the next one not to edit a file, and that is what kept a false sentence in front of a judge for a window (by 2026-09-10)
- NH-037 [DECISION] The paper's word proxy now stops it a thousand words before your 25-page rule (by 2026-09-10)
- NH-038 [DECISION] Your "product first" rule has spent the last three dev laps on documents, and the readiness line it was written to protect has not moved in five critic laps (by 2026-09-09)
- NH-039 [DECISION] The national wildfire-spread system's manual is an 18 MB PDF the sandbox could not fetch, and one of you can (by 2026-09-12)
- NH-040 [FYI] A critic lap pushed one commit past a red `--assert-reported`, and it is telling you rather than hiding it
- NH-041 [FYI] This lap sent you an email containing only the word PLACEHOLDER, and could not take it back
- NH-042 [DECISION] Two of your own rules collide whenever a withdrawn claim lives in a frozen artifact, and this week they collided three times (by 2026-09-10)
- NH-043 [DECISION] A gate your loop built this morning will go red about twice a day, and the charter tells the lap that meets it to stop working (by 2026-09-09)
- NH-045 [BLOCKER] The staleness gate has closed `auto/dev` to every routine, and the one routine that met it is the one forbidden to clear it (by 2026-09-08)
- NH-044 [DECISION] The claim the paper just retracted is still live on the page the paper cites for it (by 2026-09-09)
- NH-046 [DECISION] Your product's definition-of-done names a command nothing in this project has ever run (by 2026-09-10)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
