# WildfireGuardian autoloop · dev · 2026-09-07T0109Z

| | |
|---|---|
| when | 2026-09-07 01:09 UTC · 2026-09-07 10:09 KST |
| branch / head | `auto/dev` / `d0a739e` |
| kind | dev |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 7, done: 21, parked: 1, todo: 82 |

## What happened this lap

Four rows in one lap, in the order critic #30 wrote into WFG-148's cell: clear the
`fix-before-next-row` item, then take the booth kit and do not idle. `WFG-148`
`done(20260907T0018Z)`, then `WFG-140` + `WFG-134` + `WFG-130` `done(20260907T0018Z)`.

## Your decisions, applied

**Nothing new on either channel, and this is the eighth consecutive lap that says so.**
The Gmail search `from:siyeong0318@gmail.com subject:"WildfireGuardian autoloop"
newer_than:14d` returns 50 threads, every one a single message this loop sent, labelled
`SENT`. PR #31 has no comments. `docs/auto/decisions_seen.json` is unchanged and
`decisions.py` was not called, because there was nothing to call it on. Thirteen
NEEDS_HUMAN entries are open, nine of them DECISION entries waiting on you. **NH-032**
(due 09-08) and **NH-038** (due 09-09) are the two whose dates are closest, and
**NH-037** became load-bearing this lap: it is the reason half of WFG-148's done-when
was amended rather than met.

## What this lap did

### WFG-148 — the second binding caveat, and the predicate it could not go into

Critic #30's item. `paper/manuscript.md` §4.5 says **two** caveats bind the headline
routing comparison. WFG-138 put the first (the control is fire-blind) into `README.md`'s
Headline-result bullet. The second was absent from the README entirely.

**(a) shipped.** The bullet now says, inside the same bullet as the number, that the
forecast-aware arm plans on the same hazard field it is scored against, so **42 is an
upper bound** — what a noiseless forecast would buy, not what this project's own model
buys, which is less by an amount no run here measures. It cites `paper/manuscript.md`
§4.5 and `docs/present_perimeter_arm.md` §5. Additive prose; no number moved; no
contested fair-opponent margin reached the surface (NH-032 is open).

**(b) shipped for the README, and the interesting half is what was NOT done.** The
obvious repair to `tests/test_future_aware_attribution.py` is to widen `_is_caveated` to
require both families. **That repair is wrong.** `_is_caveated` is the predicate
`test_the_gate_never_fires_on_a_correctly_caveated_sentence` scores over four sentences
the previous lap's reviewer wrote; all four name the fire-blind control, none mentions a
noiseless forecast, and **all four are correct**. A predicate requiring both fires on
every one of them — the WC-004 direction, where the gate pressures the next lap to strike
a true sentence off a judge-facing surface (MEMO 2026-09-06T1520Z). So the second caveat
is a second predicate (`ORACLE`, `_names_the_oracle_bound`) and a **per-surface**
requirement (`ORACLE_SURFACES`) instead.

Graded red both ways on today's tree, which is the mutation pair the row asked for:

| mutation | result |
|---|---|
| restore the pre-repair README bullet | **red**, naming `README.md` |
| **move** the new clause into the neighbouring bullet | **red** — the mutation the first gate survived (MEMO 2026-09-06T2117Z) |

**(b) amended for the manuscript, on a number rather than a judgement.**
`paper/check_paper.py` reports `body_words` **8,983** against a hard fail at **9,000**.
The two clauses the Abstract and Conclusion blocks would need do not fit in 17 words, and
that budget is exactly what **NH-037** asks you about. The manuscript is not silent — §4.5
states the caveat in the words every other surface quotes — so what is missing there is
**locality**, not the claim. Recorded as a `strict=True` xfail,
`test_the_manuscript_claim_blocks_do_not_yet_name_it`, so the gap is re-measured on every
run and turns red the day it is closed. A report can be skimmed past; a strict xfail
cannot.

**(c) measured, and deliberately not acted on.** `docs/auto/DEMO_SCRIPT_5MIN.md:107` DOES
state the attribution and does carry the fire-blind control inside the spoken block. It
was left alone because it is a 61-second spoken script whose companion answer already
exists as `docs/auto/JUDGE_QA.md` Q36. Both leftovers are the new row **WFG-150**.

### WFG-140 → WFG-134 → WFG-130 — the booth kit, gate first

`docs/auto/JUDGE_QA.md` drifted against the printables manifest **four** times
(`2c8451211e` recorded, against `af955a30fa` → `7d5ac4c9c5` → `175da9e50c` →
`5ac45ea810`) and `tests/test_printables.py` was fully green through all four, because
every test there read the manifest **against itself** and none compared a recorded source
hash against the tree the sources live in.

**Order matters and was followed.** The gate was written **first**, on the pre-rebuild
tree, and went red printing the exact pair critic #30 measured:

```
docs/auto/JUDGE_QA.md: manifest 2c8451211e... tree 5ac45ea810...
```

Then the rebuild made it green. Reversed, it would have been a gate authored beside its
own fix — green by construction, proving nothing (`paper/GAPS.md` G8 point 2). A second
test covers the other direction, where a lap adds a `SOURCES` entry and does not rebuild.
Older stamps stay valid records of what they were built from; only the newest manifest is
held to the tree.

**WFG-130.** `docs/submission_reconciliation.md` is a `SOURCES` entry now. It is what
**both** of R7's first two items resolve to: WFG-018 is `done(20260903T0653Z)` and the
file was 13,702 bytes while four manifests in a row said it did not exist. R7's wording is
**not** rewritten to match what the build happens to contain — that would be grading the
kit against its own output. Instead `R7_ITEMS` resolves R7's five names to paths with a
**written reason** for each of the two exclusions (WFG-026's panel is not written; the 29
dispatch sheets are already committed PDFs and a re-render would be a second, worse copy),
one test fails when an R7 item that exists in the tree is not printed, and another binds
the mapping to R7's own line so a reword here cannot outlive it.

The coverage gate earned its keep: `U+2212 MINUS SIGN` is in the reconciliation sheet's
signed deltas and is not in the committed font subset, so it would have printed as a blank
on the one paper a judge opens to check whether values moved.

**WFG-134.** Rebuilt at `20260907T0059Z`: **33 A4 pages** (checklist 5, demo script 6,
Q&A 17, reconciliation sheet 3, detection card 2 — summing to 33), sha256 `a4970b12cdd1…`.
The `20260906T0620Z` PDF and its manifest are untouched beside it (CHARTER §3.2).
`docs/printables.md` §1 and §3 are re-derived from the new manifest, and the superseded
paragraphs are kept as dated 〔기록〕 blocks.

**The rebuild uncovered the same defect one surface over, and it is fixed here rather than
filed.** `JUDGE_QA.md` **Q39** told the student that the reconciliation sheet is *not* in
the kit, scripted 「대조표는 화면에서 열어 드리겠습니다」, listed a page count from the old
build, and marked 「대조표도 인쇄해서 가지고 왔습니다」 as a forbidden sentence — which is
**true** from this build onward. Q39 now quotes **no page count at all**: the count lives
in the manifest, and this bank has staled three hand-typed counts already (WFG-117). The
superseded answer is kept as a dated record. The drill table's 「종이 없이」 now says why it
still reads that way when the paper exists, which is WFG-130's reconciliation clause.

## Reviewed by: subagent (**block**) — and it was right three times

`docs/auto/LOOP_CONFIG.json` → `review: subagent`. A fresh agent that had not seen the
work read only `git diff a52a3b8..HEAD` and the claims above, and ran `hate`, `factchk`
and `mandela`. **It returned `block`.** Everything the lap asked it to reproduce
reproduced — both README mutations, the pre-rebuild redness of the freshness gate, the
`SOURCES` removal (it found **three** tests going red, not the two claimed), the 8,983
word count, the four-way flip of `REVIEWER_SET_CAVEATED` under a both-families predicate,
the PDF hash and the untouched earlier build. Then it drove the nail anyway.

**Root objection: this lap withdrew a claim and did not register it, and the unregistered
claim was still standing inside the artifact the lap shipped.** WFG-130 declared 「the A4
evidence sheet does not exist yet」 false in three places; `docs/auto/withdrawn_claims.json`
was never touched, so nothing read the other 929 files. The claim was alive at four places
in `docs/auto/finals/BOOTH_SETUP.md` — which is `SOURCES` **entry #1** of the PDF the same
commit built. **Pages 1–5 of the printed booth kit told the student the kit did not exist,
and pages 21–23 were the sheet those pages said was missing.** This is CHARTER §3.5c in
exactly the WC-004 shape the rule quotes, one hour after this lap copied that rule into its
own MEMO. The reviewer settled it the way §3.5c says: it registered the claim as a probe
and the gate went from `PASSED, 4 claims over 929 files` to `FAILED, 1 unlicensed mention`,
naming `BOOTH_SETUP.md:240` by itself — the file the hand sweep had missed.

**Second blocking finding: the page decomposition never summed.** 6+7+18+4+3 = **38**
against a `pages` of 33. `scripts/build_printables.py` counted `r.page - start + 1` while
the h1 block had already turned the page, so every value was one too large and the error
telescoped — invisible in the breakdown, visible only in the sum. The `20260906T0620Z`
manifest has it too (6+7+17+3 = 33 against 29). This lap re-derived that wrong
decomposition onto **four** surfaces under a sentence saying it was re-derived from the
manifest rather than retyped. That sentence was true, and it is exactly how the wrong
numbers travelled. **Re-derived is not checked.**

**Third, minor:** 「약 439 KB」 in `docs/printables.md` traced to no field of any artifact
and was wrong.

**All three fixed before the push, in the lap** (`d0a739e`): WC-005 registered with three
spellings the repository actually shipped and graded red by restoring the sentence,
`BOOTH_SETUP.md` swept at four places, the off-by-one fixed at its source with
`test_the_per_source_page_counts_sum_to_the_page_count`, the kit rebuilt at
`20260907T0059Z`, the four surfaces corrected, the file size dropped. The `0032Z` build
stays beside it as a record and `docs/printables.md` says in one line why it must not be
printed.

⚠ **One honest note the reviewer asked for and I agree with.** 「R7's wording is not
rewritten to match the build」 is true of the wording and **not** of the mapping:
`R7_ITEMS` is my reading of R7's five names, two of them resolved onto the one file this
lap added and two excused by `why` strings this lap wrote, so that test can only ever fail
on two of five paths. It is in the open, it did grade red, and it is not the whole gate —
but it is a mapping written beside the build and the report should say so.

⚠ **A second note, on the record lines.** `BOOTH_SETUP.md`'s 〔기록〕 lines **paraphrase**
the withdrawn sentence instead of quoting it under a pragma, because that file is paper in
a student's hand. The verbatim text lives in WC-005, `docs/withdrawn_claims.md` §5d and
this report, all record class. That uses the registry's known blind spot — a reworded
assertion escapes — and §5d says so in those words rather than leaving it to be found.

## The root objection I recorded before building

*On WFG-148:* **a gate that turned out to be too narrow invites you to widen the one
predicate that carries its safety score, and that is how a correctness gate becomes a
pressure to delete true sentences.** The cheapest test was to run the four reviewer
sentences against a both-families predicate before writing it — all four flip. That is why
the fix is a second predicate and a per-surface requirement, and it is the lap's one real
design decision.

*On the booth kit:* **the freshness gate cannot be graded by the lap that fixes the
staleness**, so the whole row hinges on writing it before the rebuild and recording that
it was red. Done, with the failure text quoted above.

## What did not work

- The done-when's manuscript half. 17 words of headroom, ~40 words of clause. Amended in
  the row on the measurement, carried by WFG-150, and marked by a strict xfail rather than
  by a sentence in this report.
- `baseline-verify` still WARNs (exit 2) as it has all sprint: a **grown** registry and
  **new** tracked artifacts, which NH-029 (your option) defines as information rather than
  failure. This lap added artifacts by design, so it is the expected shape.
- Cold and warm pass counts still disagree by six, which is **WFG-139** and is still
  `todo`. Cold at `786318c`: **1617 passed, 62 skipped, 2 xfailed**. Warm at `0c862cb`:
  **1627 passed, 56 skipped, 2 xfailed**. The six are WFG-139's terrain tests; the other
  four are this lap's new printables tests.

## Gate evidence

`gates.py --mode full` exit **0**, ALL GREEN, **three times** this lap, and the reviewer
was right that the first two ran at the commit *before* the one they were describing:
`.auto/gates.json` recorded `0c862cb` while `HEAD` was `590c29a`, so `--assert-head` would
have refused that push — the process held, but the prose was ahead of it. The runs:

| when | mode | head | result |
|---|---|---|---|
| after WFG-148 | full | `a52a3b8` (claim commit, dirty tree) | **cold** 1617 passed, 62 skipped, 2 xfailed |
| after the booth kit | full | `0c862cb` (dirty tree) | **warm** 1627 passed, 56 skipped, 2 xfailed |
| after the reviewer's fixes | full | `590c29a` (dirty tree) | **warm** 1638 passed, 56 skipped, 2 xfailed |
| the pushed commit | full | see the report's own gate table | re-run at `HEAD` before the push |

`--assert-head` and `--assert-reported` are chained to the push with `&&`, which is
NH-040's remedy and the one habit that lap asked for.

## The next row

**WFG-139** — the test suite downloads a 25 MB SRTM tile mid-run, `docs/clean_clone_gates.md:27`
says 「No network」, and `JUDGE_QA.md` Q28 cites that line to a judge. It is P0, it is the
third consecutive report to quote its cold/warm gap as evidence of itself, and with the
booth kit closed it is the first `todo` row in table order that no other row waits behind.

## In plain terms

**What changed for the project.** The paper folder you take to the booth was four
corrections out of date, and the biggest paper in it — the Q&A card — told you to say
something the repository had already decided you must not say. It is rebuilt, it is 33
pages, and the reconciliation sheet you would open when a judge says 「제출한 숫자와 지금
숫자가 다릅니다」 is finally printed in it instead of being listed as a document that does
not exist. A new test now goes red the moment the paper and the files disagree again, and
it was proved by watching it go red on today's stale kit before the rebuild fixed it.
⚠ Print the **20260907T0059Z** file, not the one before it.

**Why it matters to the judges or the science.** The front page also stopped overstating
its own headline. It already said the 42 is measured against a fire-blind baseline; it now
also says the forecast used in that comparison is a perfect one, so 42 is the ceiling of
what a flawless forecast would buy and not what your model buys. That is the sentence a
fire scientist asks for, and saying it first is worth more than being caught not saying it.

**What you should do.** Nothing is blocking, and the one thing worth knowing is that the
lap's own reviewer caught it printing a booth kit whose first page said the kit did not
exist. It was fixed before anything was pushed, and that check is why it runs. When you have ten minutes, **NH-037** is
worth answering: the manuscript is 17 words from its own word limit, and that limit is the
only reason the same honest sentence did not go into the paper this lap. **NH-032**
(due 09-08) and **NH-038** (due 09-09) are still waiting too.



## Gates

**ALL GREEN** · mode `full` · head `590c29a` · 2026-09-07T01:08:19Z · ⚠ **stale: the gates read `590c29a`, HEAD is `d0a739e`** — this table does not certify the pushed tree; re-run `gates.py --mode full` and `gates.py --assert-head` before pushing

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 17.8 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.2 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 0.5 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 274.6 s | 1638 passed, 56 skipped, 2 xfailed, 52 warnings in 273.47s (0:04:33) |

## Commits since the previous report (f79c1425435b803c427b789f75363ffd04a9f5ef..HEAD)

- d0a739e the kit's own first page told the student the kit does not exist, and the lap that fixed that never registered it
- 590c29a WFG-140 + WFG-134 + WFG-130: the gate went red on the stale kit first, then the rebuild made it green
- 0c862cb claim WFG-134 with WFG-140 and WFG-130 (20260907T0018Z)
- 786318c WFG-148: the second binding caveat reaches the bullet, and not the predicate that scores the first
- a52a3b8 claim WFG-148 (20260907T0018Z)
- 4572828 the assert that went red is the one that says write a report, so this is the report

## Needs a human (13 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09)
- NH-036 [DECISION] One critic lap told the next one not to edit a file, and that is what kept a false sentence in front of a judge for a window (by 2026-09-10)
- NH-037 [DECISION] The paper's word proxy now stops it a thousand words before your 25-page rule (by 2026-09-10)
- NH-038 [DECISION] Your "product first" rule has spent the last three dev laps on documents, and the readiness line it was written to protect has not moved in five critic laps (by 2026-09-09)
- NH-039 [DECISION] The national wildfire-spread system's manual is an 18 MB PDF the sandbox could not fetch, and one of you can (by 2026-09-12)
- NH-040 [FYI] A critic lap pushed one commit past a red `--assert-reported`, and it is telling you rather than hiding it

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
