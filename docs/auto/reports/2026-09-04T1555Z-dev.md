# WildfireGuardian autoloop · dev · 2026-09-04T1555Z

| | |
|---|---|
| when | 2026-09-04 15:55 UTC · 2026-09-05 00:55 KST |
| branch / head | `auto/dev` / `e560d39` |
| kind | dev |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 7, done: 12, parked: 1, todo: 41 |

## What happened this lap

## Your decisions, applied

Nothing new. The Gmail search (`from:siyeong0318@gmail.com subject:"WildfireGuardian autoloop" newer_than:14d`) returned 32 threads and every one of them is a single message — the loop's own outgoing report. No reply from you, and no `NH-###:` comment on PR #31. The twelve decisions you took on 2026-09-04 in the laptop session are already recorded in `docs/auto/decisions_seen.json` and were applied by earlier laps.

Four NEEDS_HUMAN entries stay open; **NH-022** is the one this lap depends on and it is in the block at the top of the report email.

## What this lap did

The critic's `fix-before-next-row` block named two items and this lap cleared both. It claimed them at `d5e2562`, built at `55d3ef2`, and rebuilt at `e560d39` after the independent reviewer blocked the push.

### WFG-075 — the artifact labelled 영덕 is annotated as wrong, where it lives

Critic #11 found that `scripts/extract_juso_yeongdeok.py:32` cuts on 시군구 code `47920` under the comment `# 경상북도 영덕군`, and that the resulting points are about 45 km from 영덕. **This lap re-measured the containment from the committed files:** all 239 points across the eight layers fall at 128.649–129.148 E / 36.785–37.064 N, and **0** of them are inside `regions.lookup('yeongdeok_2025').bbox_wgs84` = `(129.25, 36.30, 129.55, 36.60)`. Confirmed, and that is the whole of the claim.

**It did not re-measure the critic's 「about 45 km」, and wrote it down anyway — the reviewer blocked on exactly that.** The figure had reached eight registry `caveat` fields, both NEEDS_HUMAN amendments, the document, the plain-terms section, and a `assert "45 km" in head` string pin. Computed over those 239 points against the box: the nearest is **30.5 km** and the farthest **65.6 km**; no construction yields 45. Every kilometre figure is now out of the registry, the document, the amendments and the test, replaced by the statement that carries the argument and needs no number — no overlap on either axis. NH-022 and the WFG-075 backlog row carry a dated withdrawal of the 45, because the author reads both. Lesson in `MEMO.md`.

What shipped:

- **The annotation went into the registrar, not into `docs/NUMBERS.json`.** `scripts/register_juso_yeongdeok.py` deletes and rebuilds every `juso_yeongdeok_*` key on each run, so a hand edit to the registry would have survived exactly until the next registration and then vanished silently. `SCOPE_CORRECTION` now opens each entry's `caveat` with 「SCOPE WRONG, DO NOT USE AS 영덕 DATA (2026-09-04, WFG-075 / NH-022)」, a new `scope_status: "wrong (…)"` field carries it machine-readably, and `forbidden_phrasings` names the seven sentences that must not be written. Registrar re-run reports `8 keys, stale before: 0` — **no value changed**.
- `scope`, `sample` and `derivation` **keep the wrong 영덕 label on purpose**, as the record of what was claimed (CHARTER §3.2/§3.3: annotate, never edit) and as the thing WFG-076's geometry gate has to catch. `tests/…::test_the_wrong_label_is_kept_as_the_record` pins that, so a later lap cannot tidy the evidence away.
- `docs/juso_yeongdeok.md` opens on a 정정 block before any other prose; the original text is left intact below it under a line saying why.
- NH-005 and NH-012's 2026-09-04 annotations are corrected as dated **AMENDMENT** paragraphs. NH-012's said part (b) was 「narrowed」 by the download; that is withdrawn in as many words — the repository holds **no** agency-designated 대피장소 list for 영덕. NH-005's own ask (the 도로명주소 **건물** layer) was never in either zip and is unaffected.
- `tests/test_juso_yeongdeok.py:11`'s `sigungu_cd == "47920"` assertion is **kept, not deleted**, as the row required. The first draft bundled it with a new geometry assertion under one `xfail`, which the reviewer showed did two wrong things: it silently stopped enforcing a property that was never broken (that the manifest records the code the extractor used), and its stated mechanism was false — a correct re-cut changes `sigungu_cd` off `47920`, the first assertion fails, and a strict xfail stays green and silent. They are now two tests. The manifest/extractor agreement is written against the script's `SIGUNGU` constant and passes today and after a re-cut. The geometry property is its own `xfail(strict=True)`, so a correct re-cut turns it green and the **suite** red, which is the signal to remove the marker — the mechanism the summary claimed before it was true. Four further tests hold the containment: the 239/0 measurement, the registry annotation, the preserved label, and the document's opening.
- WFG-073 and WFG-074 move from `blocked(WFG-075)` to `blocked(NH-022)`, which is where the block actually lives now.

**The county is not named anywhere this lap wrote.** WFG-066 is the standing rule that an identifier not read off a record is not written down, and this lap did not open 행정표준코드. NH-022 carries the critic's 봉화읍 inference, labelled there as an inference. It is **not** the only place the name occurs, and the summary said so wrongly before the review: the WFG-075 backlog row repeats it, and **74 of the committed `minwon_agencies` road-address fields contain 봉화군**. That last is a field in the data rather than a code read off a register, so it is a lead and not an identification, and NH-022 now says so where the author will read it.

### WFG-067 — the integrity panel names a commit that resolves

`web/finals.html` carried `"git":"a562045"`, which `git cat-file -t` rejects in a fresh clone; four critic windows, on the one RELIABILITY line a judge is invited to verify the build with. Rebuilt with `scripts/build_finals.py --verify` — the three gates really ran and really passed — and the diff to the 2 MB file is **one line**: `built_utc` and `git`. The screen now stamps **`d5e2562`**, chosen deliberately as a commit that was already on `origin/auto/dev` before the build, so no later rebase in this lap can orphan it.

The screenshots in `docs/auto/finals/screens_20260904T0630Z/` are **left exactly as taken** — their job is to record what the screen showed, and what it showed was `a562045`. Their README gains a dated note saying so.

### The root objection (`hate`), and it changed the build

> *The row's own done-when proposes `git cat-file -e <stamp>`, one line. That predicate asks the object database whether the object exists — and a commit orphaned by a rebase is still in the object database until `gc` runs. So the gate WFG-067 asks for stays **green on the very machine that creates this defect**, and only goes red in the fresh clone, which is where nobody is looking. It is a gate whose failure mode is invisible from where it runs.*

Cheapest test, offline, seconds: make an unreachable commit with `git commit-tree` and grade both predicates on it.

| stamp | should pass | `cat-file -e` | `merge-base --is-ancestor` |
|---|---|---|---|
| built at HEAD | yes | ✓ | ✓ |
| built at HEAD's parent | yes | ✓ | ✓ |
| **orphaned by a rebase** | **no** | **✗ passes it** | ✓ rejects it |
| `a562045`, the shipped bug | no | ✓ rejects | ✓ rejects |
| `unknown` (git unavailable) | no | ✓ rejects | ✓ rejects |

Existence scores **4 of 5**; reachability scores **5 of 5**. So the gate asserts reachability from HEAD, not existence — and not equality with HEAD, which the row rightly says is unsatisfiable because a build's commit is always one earlier than the commit carrying it. The whole table is committed as a test (`test_the_stamp_gate_is_graded_against_the_ways_a_stamp_goes_wrong`), so the claim re-measures itself every suite instead of sitting in prose.

**This lap wrote its prediction down before measuring and was wrong:** it predicted existence would score 3 of 5, having forgotten that the literal string `"unknown"` is not an object either. The measurement is 4. That is recorded in the test's docstring rather than edited away, per the MEMO rule; the objection survives and is sharpened, because a gate that is right about almost everything and wrong about the one case it was filed for is precisely how a defect ships green for four windows.

### What did not work, and was thrown away

The first draft of the gate skipped on `git rev-parse --is-shallow-repository`. Running it in the sandbox showed the skip firing here — the cloud clone is flagged shallow (294 commits deep, ample for an ancestor check on a same-lap stamp). That draft would have switched the gate off in the exact environment where the defect is made, which is the hole it was written to close. Removed; only a missing work tree skips now. Both this and the grading lesson are in `docs/auto/MEMO.md`.

## Evidence

| claim | where to check |
|---|---|
| 239 points, 0 inside the 영덕 box | `tests/test_juso_yeongdeok.py::test_not_one_point_is_inside_the_yeongdeok_box` |
| no registry value changed | registrar output `8 keys, stale before: 0`. Diffed key by key against `d5e2562`: 320 keys before and after, and the only fields that differ **anywhere in the file** are `caveat`, `scope_status` and `forbidden_phrasings`. The first draft also moved `git_commit` on the eight keys (`3ffc135`→`d5e2562`), which would have stamped a production commit at which those GeoJSONs did not exist; the reviewer caught it, the registrar now carries `git_commit` forward whenever the value is unchanged, and the field is back to `3ffc135` |
| annotation cannot be lost | `scripts/register_juso_yeongdeok.py` (`SCOPE_CORRECTION`), `…::test_every_registry_entry_carries_the_scope_correction` |
| the wrong label is preserved | `…::test_the_wrong_label_is_kept_as_the_record` |
| the stamp resolves and is reachable | `tests/test_finals_screen.py::test_the_integrity_panel_names_a_commit_this_repository_has` |
| the catch-rate table | `…::test_the_stamp_gate_is_graded_against_the_ways_a_stamp_goes_wrong` |
| gates | `gates.py --mode full` exit **0** at `e560d39`, clean tree: **1382 passed, 56 skipped, 1 xfailed** in 193 s. The mid-lap run quoted in the first draft was made at `d5e2562` on a dirty tree and proved nothing about the pushed commit — the reviewer's second nail. `--assert-head` and `--assert-reported` run immediately before the push; the gate table below names the commit it certifies. `baseline-verify` WARN, expected off-laptop, `hard: false`, twelfth window |

Against critic #11's reading at `83f49bc` (1367 passed, 62 skipped, **cold**) this run is **warm** — the six SRTM-gated tests ran here and skipped there — so the honest comparison is 1382 passed / 56 skipped against 1367 / 62 with that difference stated, not a like-for-like delta. The nine tests this lap added are the substance of it: **six for WFG-075, three for WFG-067, and none removed** (`git diff d5e2562..HEAD -- tests/` shows 9 added `def test_`, 0 removed).

## Independent review

`Reviewed by: subagent (block): every gate here is about re-deriving a record where it will be consumed, and the lap broke that discipline on three of its own numbers — an unregistered "45 km" propagated into the registry and into two notes addressed to the author, a `git_commit` re-stamp the report's own evidence row denied, and a catch-rate table graded against a mutation set its own author wrote.`

The reviewer's `first_nail` was one command: haversine the committed points against the box. Nearest 30.5 km, farthest 65.6 km, centroid-to-corner 49.5 km — **no construction yields 45**. Its second was `gates.py --assert-head`, which exits 1 at `55d3ef2` because the full run was made at `d5e2562` on a dirty tree, and the evidence table implied otherwise.

Every block item was fixed rather than argued:

| the reviewer's finding | what changed |
|---|---|
| 「about 45 km」 unregistered, in 8 registry entries, 2 author-facing amendments, the document, plain terms, and pinned by a string assertion | measured (30.5 / 65.6 km), then removed from all of them; the claim is restated with no distance; NH-022 and the WFG-075 row carry a dated withdrawal; the test now asserts the *absence* of the unregistered figure |
| `git_commit` moved `3ffc135`→`d5e2562` on 8 entries, asserting a production commit at which the artifacts did not exist | registrar carries `git_commit` forward when the value is unchanged; the field is restored; the evidence row now names it |
| catch-rate table graded against the builder's own five cases, while citing the opposite standard for the next row | the reviewer supplied the missing case — a stamp reachable from local HEAD but not on `origin`, which a judge's fresh clone cannot resolve. It is now `test_the_escape_this_gate_cannot_close_is_still_open`, which names why the predicate cannot close it and asserts the operational mitigation instead |
| the xfail bundled a never-broken assertion with a failing one, and its stated mechanism was false | split into a passing test written against the extractor's `SIGUNGU` constant and a strict-xfail geometry test whose mechanism is now the one described |
| 「NH-022 is the only place 봉화읍 appears」 | false; corrected here, and NH-022 now records that 74 committed road-address fields contain 봉화군 — a lead, not an identification |
| the gate table implied a full pass at the pushed commit | the evidence row now points at the report's own gate table, and `--assert-head` / `--assert-reported` run immediately before the push |

What the reviewer verified and left standing: the 239/0 measurement (re-derived independently), the 4/5 vs 5/5 grading (it built its own orphan), that `web/finals.html` is a legitimate rebuild and not a §3.2 breach, that keeping `scope: 영덕군` beside its correction is defensible, that nothing judge-facing prints these values, and that nothing was deleted or re-framed.

## Next row

**WFG-076** — assert that an artifact is where its label says it is. It is critic #11's root objection, the general form of what this lap contained by hand, and DIRECTION.md's rows 1–3 (WFG-062, WFG-003, WFG-067) are now one done and two of a kind with it. The measurement standard is already set: publish the catch rate against a mutation set the builder did not write.

## In plain terms

- **What changed for the project.** The 영덕 site list you sent on Thursday is not 영덕's — it was cut with the wrong county code and lands well outside 영덕 on both axes — so every place the repository repeated that claim now says so in writing, and the two jobs that would have fed those points into the rescue router are on hold. Separately, the "check our build" panel on the finals screen was printing a commit number that does not exist; it now prints one that does, and a test stops it happening again.
- **Why it matters to the judges or the science.** Nothing a judge sees was ever wrong — the screen, the README, the paper and the Q&A cards never printed these numbers, and that was checked. What was at risk was the answer to 「이 대피 지점들은 어디서 나온 겁니까?」: for one day the project had a better-sounding answer that was about the wrong county. The old answer (OSM plus a documented fallback) is intact and is the one to give.
- **What you should do.** One thing, on the laptop, when you have ten minutes: **NH-022**. Look up 영덕군's 시군구 code on code.go.kr, read it off the page rather than typing one from memory, put it in `scripts/extract_juso_yeongdeok.py`, re-run the extractor and the register script, and check the new points land between 129.25 and 129.55 east. If they do not, the filter field is wrong and not just the number, and that is worth saying rather than adjusting until something passes.



## Gates

**ALL GREEN** · mode `full` · head `e560d39` · 2026-09-04T15:54:46Z · current at `e560d39`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 13.9 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.1 s | make: *** [Makefile:152: baseline-verify] Error 1 |
| snapshot-verify | PASS | 0.4 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 193.9 s | 1382 passed, 56 skipped, 1 xfailed, 52 warnings in 193.02s (0:03:13) |

## Commits since the previous report (ad0019942c394f18d582c89b455686b88537b790..HEAD)

- e560d39 the reviewer blocked on a number this lap had copied instead of measured
- 55d3ef2 the artifact labelled 영덕 is annotated as wrong where it lives, and the screen's commit id is now one a judge can resolve
- d5e2562 claim WFG-075 and WFG-067: the critic's two fix-before-next-row items
- 7988769 STATE.json restored after a rebase committed conflict markers; a test now refuses unparsable state files

## Needs a human (5 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-022 [DECISION] The 영덕 dataset you sent was cut with the wrong county code, and only your laptop can re-cut it (by 2026-09-08)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
