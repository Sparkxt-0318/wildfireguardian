# WildfireGuardian autoloop · critic · 2026-09-10T0825Z

| | |
|---|---|
| when | 2026-09-10 08:25 UTC · 2026-09-10 17:25 KST |
| branch / head | `auto/dev` / `90d3ea7` (pushed; **`16e6824` is the head this lap REVIEWED and the head `report.py` stamped**, and every measurement in this report was taken there. The findings and this correction ship in `90d3ea7`, on which `gates.py --mode full` was re-run before the push — ALL GREEN, **1986 passed** — with `--assert-head` and `--assert-reported` both exiting 0.) |
| kind | critic |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 9, done: 33, dropped: 1, parked: 1, title-derivable: 1, todo: 108 |

## What happened this lap

Critic #57. Reviewed head `16e6824`. Window `f0917a5..16e6824`, a real 24 hours.

⚠ **First, the measurement that made every other measurement possible.** This clone arrived
**shallow at exactly 50 commits**, and `git log --since='26 hours ago'` returned exactly 50 —
the window and the clone boundary coincided, so every count on this page would have been
silently truncated at 2026-09-09T10:04Z and reported as a day. I ran `git fetch --unshallow`
before counting anything: `--is-shallow-repository` now answers **`false`**, `rev-list --count
HEAD` answers **703**. CHARTER §4's warning says the depth is not a constant and a lap that
quotes one measures it in its own clone; this lap adds the corollary that **the depth can equal
the window, and when it does, nothing looks wrong.** That is WFG-119's territory and the row is
still `todo`.

## Your decisions, applied

**Nothing new in either channel, and one line is what that deserves.** The Gmail search
`from:siyeong0318@gmail.com subject:"WildfireGuardian autoloop" newer_than:14d` returns only
this loop's own sent reports — every thread on the first page holds exactly one message, so no
thread has a reply in it; `subject:"WildfireGuardian autoloop" newer_than:14d NH-0` returns
**zero** threads. PR #31 has **zero** comments. No `NH-###:` line reached the loop,
`scripts/auto/decisions.py` was not run, and `docs/auto/decisions_seen.json` is unchanged.

⚠ **But the count of what is waiting on you was wrong, and this lap counted it.** Every recent
report says 「twenty decisions open, **six** of them at or past their stated date」. The twenty is
right (19 DECISION + 1 BLOCKER, plus 5 FYI). **The six is wrong: it is 13.** Overdue at
`16e6824`: **NH-032, NH-034, NH-045** (due 09-08); **NH-035, NH-038, NH-043, NH-044** (due
09-09); **NH-036, NH-037, NH-042, NH-046, NH-048, NH-050** (due **today**). That is the WFG-107
failure mode — a hand-written number beside a generated block — landing on the one number that
tells you how late your own queue is. Measurement appended to WFG-107; the row is P1 infra and
§14b holds it behind R3, so it cannot be worked, which is itself NH-038's question.

---

## Findings, ranked

### #1 (the preemption) — `docs/oracle_gap.md:39` names the wrong script for the array the whole document is about

The §2 table row for `haz_stack` says 「the **leave-one-fire-out forward simulation**.
`scripts/run_forward_sim_region.py` fits the spread_v2 model on every fire EXCEPT the target」.
That script never touches that file. Measured in one process, not read from any report:

| check | answer |
|---|---|
| `grep -c routing_demo_canonical scripts/run_forward_sim_region.py` | **0** |
| what it does write | `hazard_{fid}.npz` (`:283-285`), `forward_sim_regions.json` (`:338`) |
| what writes `data/processed/routing_demo_canonical.npz` | **`scripts/build_canonical_hazard.py`** (`:109`, `:177`) |
| the shape in the row itself | `(5, 181, 156)` — the canonical npz, and the named script's own `grid_note` says Yeongdeok's field used a different bbox |

⚠⚠ **The claim is TRUE and must not be softened.** `build_canonical_hazard.py:130` reads
`fit(ds[ds["fire_id"] != args.fire])` and its step print at `:129` says 「fitting
leave-the-target-fire-out」. This is a wrong pointer under a correct claim, so the repair is the
pointer — no withdrawal, no `WC-###`.

**Why this is the one preemption.** It is the only one of three findings that is genuinely
minutes: the page is in no `SOURCES` list and on no build path, so the edit cascades into no
gate. And it is 「사용된 자료에 대한 출처 명기」, a named criterion of a 20-point row on **both**
rubric tables, failing on the page **seven places on four judge-facing surfaces** send a judge
to for exactly this question. A professor judge who opens the named script to check the LOFO
claim finds a script that does not make the array.

### #2 — WFG-225 (P0): the screen five judges stand in front of prints the two counts this window corrected, with none of the correction

`web/finals.html`'s `renderPanel()` writes one row per bucket as mark + label + count, so the
region panel shows **◆ 예산 초과 2** for `uiseong_andong_2025` — which is `DATA.default_region`,
the first panel a judge sees — and **3** for Uljin-Samcheok. `5bcfe11` had qualified exactly
those two counts six hours earlier on `README.md:145-149` and `docs/multi_region.md` §3.1: the
two arms are scored under different time rules, `classify()` passes `time_budget_min` to the
future-aware router and **no budget at all** to `naive_route`, and under one rule the
Uiseong-Andong bucket is **empty**. The screen says none of it — 예산 appears on **one** line of
the file, inside the data payload, as the label, and the 알려진 한계 panel's **ten** `rel(...)`
cards contain no card about the budget rule. **The gate written to keep the correction honest
cannot see the screen:** `tests/test_budget_rule_asymmetry_is_stated.py:63-64` grades
`docs/multi_region.md` and `README.md` and nothing else. This is critic #53's fifth-surface
shape, one window later, on the same surface. The number is correct and registered and must not
move (NH-031 A; the registry half is WFG-122, `todo`) — what is missing is the caveat, which is
why it is a 제출 자료 deduction and not a 구현 one.

### #3 — WFG-226 (P0): card Q38 is stale, and it is on the paper in the booth kit

`docs/auto/JUDGE_QA.md:1421` still opens 「아닙니다. 그리고 오늘 저장소는 이 질문에 두 가지로
답합니다 — 그게 결함입니다.」 and attributes the uncaveated reading to `docs/multi_region.md:191`.
Both halves are false at this head. **The proof that it is also on paper is the hash gate
itself:** I re-hashed all **seven** declared sources of `WFG_printables_20260910T0140Z.pdf`
against the tree and got **seven of seven matching**, which means the bank has not moved since
the print — so the print carries the same stale card. A hash gate proves the paper matches the
repository; it cannot prove the repository was right when the paper was made. The card's
substantive answer is still correct, so this is a re-framing that should now credit the fix.

### #4 — WFG-227 (P1), from the judge drill: two of six buckets are always exactly zero and no judge-facing surface says whether they can occur

`both_enter` and `naive_unreachable` are **0** in all three regions and all three Yeongdeok
variants. `docs/multi_region.md:46-47` states the fact and gives no reason; the two names appear
**0** times in `README.md`, `docs/auto/JUDGE_QA.md`, `DEMO_SCRIPT_5MIN.md` and
`docs/creativity_card.md`. An always-empty partition class is the standard degenerate-partition
objection, and the repository's own half-answer already exists at `docs/multi_region.md:227-235`.
Filed as a row and not a card because **NH-049** makes a critic card impossible — fifth
consecutive lap unable to execute that half of its own prompt.

### #5 — the loop finding: a critic's `Do NOT` note was false and cost a judge-facing page its evidence for one lap

Critic #56 wrote, on DIRECTION and in the WFG-215 row in capitals, 「Do not print a per-slice
`obs_time_min` … only the headline key is registered … the row's own 「Do」 is wrong and a lap
must not follow it literally」. **It was false.** The `0703Z` dev lap obeyed it and shipped a
first draft of `docs/oracle_gap.md` §4 that **withheld its own evidence, in prose, on a
judge-facing page**, on that stated ground. Its independent reviewer blocked; the lap registered
the five keys additively rather than arguing. Re-checked here: the five
`og_yeongdeok_t###min_obs_time_min` keys exist at `16e6824`. CHARTER §14c worked exactly as
designed — I re-checked, it is false, it is **deleted rather than re-stated** — but §14c does not
stop the damage inside the window. **The thing that caught this was `review: subagent`, not any
gate.** Appended to NH-036, which is your question about exactly this mechanism.

## Root objection (`hate`) on the current headline narrative

**「Your repository now explains its own weaknesses better than it presents its own result, and
the gap is widening on the one surface you cannot explain away in person.」** Round 4's own
reading guide says it: 「이 절에 이 프로젝트에 유리한 결과는 없습니다」. That is the right
character for a research entry, and Track B rewards it — Track B rose to 95 this window. But
five judges spend ten minutes each in front of `web/finals.html`, and this lap found that screen
one window behind the README on the budget rule and holding a ten-card limits panel. The cheapest
test is not another caveat: it is to open the screen cold and ask **what a judge could point at
and call a result**. WFG-225 fixes an omission; it does not answer that.

## Scorecard deltas

**Track B 94 → 95. Track A 94 HELD (two moves that offset).**

| row | was | now | why |
|---|---|---|---|
| B 설계와 방법론 | 19 | **20** | critic #56's pre-registration paid on its own stated condition: WFG-128 closed at `5bcfe11`, verified on the page and not in the report |
| B 데이터 수집·분석·해석 | 19 | **20** | critic #56's own deduction discharged in one lap: WFG-215 closed BOTH halves, five keys registered additively, `WC-014` in the same commit |
| B 제출 자료 | 20 | **19** | three defects on one row: findings #1, #2 and #3 above |
| A 설계와 방법론 | 19 | **20** | same raise; on this track 일정 is one of **four** sub-items and both halves of the pre-registration (WFG-218, WFG-220, WFG-128) are now paid |
| A 제출 자료 | 18 | **17** | same deduction, and it bites harder here because the objects handed over are the bundle and the printed kit |

I considered declining the 설계와 방법론 raise on a sharper condition — the control is still
mis-specified in the **artifact** and corrected only in prose, with WFG-122 `todo` — and declined
to. Inventing a new condition in the lap the old one is met is the goalpost move critic #53 named,
and the deduction it expresses belongs on 제출 자료, where I put it.

## What I verified and found clean

- `gates.py --mode full` exits **0** at `16e6824`: **1985 passed**, 64 skipped, 3 xfailed, pytest
  482.0 s, **up 27 tests in one window**. `baseline-verify` WARNs on the two git-ignored
  `data/raw/**` contracts, which is NH-029 and §3d working.
- **GitHub `auto-gates`, runs 278 to 321: 44 runs, 36 `success`, 8 `cancelled` (each superseded
  by the next push within minutes), ZERO `failure`**, run **321 green at this exact head**.
  CHARTER §4b sets **no finding #1** for the sixth consecutive lap.
- **All eight** dev reports in the window record `Reviewed by:`; all eight name `subagent`;
  **seven of eight record `block`** and spend commits acting on it (1928Z records `pass`).
- The printed kit's seven sources hash equal to the tree, seven of seven.
- `factchk`: the window adds no new citation and no new external URL to any judge-facing surface.
- **The window's strongest new claim survives attack.** 「`haz_stack` is leave-one-fire-out」 now
  stands on the README TL;DR, the finals screen's first limits card, JUDGE_QA Q36 at T0 and the
  manuscript. I traced it to the code rather than the prose and it holds. Only the pointer is
  wrong, which is finding #1.

## Direction and restraint

**ZERO §3b reorders — the first critic lap of the sprint to spend none.** The two rows a dev lap
should meet first are rows this lap filed, and a newly filed row is not a move; DIRECTION names
them, which is CHARTER §14's own mechanism and is undone by deleting two paragraphs.

⚠ **The rows are filed in the table like any other P0 row, NOT at position 1, and that is a
deliberate departure from this routine's stored prompt.** NH-051 established that 「position 1」
is NH-038 option **D**'s mechanic while the prompt cites option **B**, whose own words put the row
「in the table like any other」. Three critic laps have already filed at position 1 on that
authority. This one did not, and says so in NH-051 rather than doing it quietly. If your answer to
NH-038 is D, moving the two rows up is one edit.

**Readiness: 8 of 11, ZERO ticked, fourteenth consecutive critic lap.** R3 is `blocked(NH-046)`,
NH-046 came due today, and R3 is now the only unticked line of the six gating the P1 infra block.
P1 went 100 → 101 `todo` this window with P1 `done` unmoved, and this lap added the 101st row.
Appended to NH-038 with the method stated, because critic #56's P1 `done` count and mine differ
by parser and not by fact.

**No new NEEDS_HUMAN entries.** Every finding this lap made belongs to an entry or a row that
already exists: measurements went to NH-036, NH-038, NH-049, NH-051 and WFG-107, and three new
rows carry the rest.

## In plain terms

**What the judges would see today.** Your project is in good shape and got better overnight: the
two pages a judge is sent to for its hardest question were both fixed, and the whole test suite
went from 1958 to 1985 tests, all green. The weak spot is the screen itself. It shows a small
number — 예산 초과 2 — on the first panel a judge sees, and this morning the repository learned
that number was measured unfairly and wrote that down in two places. The screen did not get the
memo. Same story for one printed Q&A card, which still tells you to say 「our repository
contradicts itself here」 when as of six hours ago it does not.

**What changed since yesterday.** Two real fixes landed and I paid for both in the score: Track B
went 94 to 95, its highest yet. I took one point back on 제출 자료 for the screen, the stale card,
and one wrong file name. I also found that a warning yesterday's critic left for today's builder
was simply wrong, and it made today's builder hide good evidence for an hour before its reviewer
caught it — I deleted the warning and wrote the story down for you.

**What you should do.** Answer the overdue decisions if you can — **13** are past their date, not
six as the last few emails told you, and I fixed that count. The two that block the most are
**NH-046** (the readiness line R3 names a command nobody can run, and it has frozen your progress
bar at 8 of 11 for fourteen laps) and **NH-037** (your paper is three words from its hard limit).
Nothing needs you today for the project to keep moving; those two are what would unstick it.



## Gates

**ALL GREEN** · mode `full` · head `16e6824` · 2026-09-10T08:07:45Z — **this is the REVIEWED head, and the table below is that run.**

⚠ **The commit this report ships in is `90d3ea7`, and `gates.py --mode full` was re-run there before the push** (CHARTER §4 step 8: the commit you push is the commit the gates read). That second run is **ALL GREEN** at `90d3ea7`: **1986 passed, 63 skipped, 3 xfailed**, pytest 497.4 s, `verify` 26.9 s, `baseline-verify` WARN as below. `--assert-head` exits 0 (`mode=full head=90d3ea7`) and `--assert-reported` exits 0 over 6 substantive paths. ⚠ **The delta from the reviewed head is +1 passed / -1 skipped, and I did not isolate which test moved, so I am not naming a cause.** What is known: the diff between the two heads touches only `docs/auto/`, so no code, test or artifact changed between the runs, and the total (1985 + 64 = 1986 + 63 = 2049) is unchanged — one test that skipped at `16e6824` ran at `90d3ea7`. A skip that flips on a `docs/auto/`-only diff is most likely conditioned on a file this lap created, but I did not confirm that and a guess in a gate table is the thing this repository keeps having to withdraw. Both counts are printed rather than one, because a lap that prints only the newer one is claiming a measurement it did not take at the head it describes.

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 27.6 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.2 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 2.0 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 482.0 s | 1985 passed, 64 skipped, 3 xfailed, 52 warnings in 480.18s (0:08:00) |

## Commits since the previous report (5bcfe11f0de290c741d998e53abcedb7ebd81f34..HEAD)

- 16e6824 the dev report names the head it ships in, and keeps the head it was stamped from
- 3601c5e the report's own prose collided with the keys the lap had just registered
- ee3ec87 report: 2026-09-10T0703Z dev

## Needs a human (25 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09, one day past; raised to HIGH by critic #55 on a measured third instance)
- NH-036 [DECISION] One critic lap told the next one not to edit a file, and that is what kept a false sentence in front of a judge for a window (by 2026-09-10)
- NH-037 [DECISION] The paper's word proxy now stops it a thousand words before your 25-page rule (by 2026-09-10)
- NH-038 [DECISION] Your "product first" rule has spent the last three dev laps on documents, and the readiness line it was written to protect has not moved in five critic laps (by 2026-09-09)
- NH-039 [DECISION] The national wildfire-spread system's manual is an 18 MB PDF the sandbox could not fetch, and one of you can (by 2026-09-12)
- NH-040 [FYI] A critic lap pushed one commit past a red `--assert-reported`, and it is telling you rather than hiding it
- NH-041 [FYI] This lap sent you an email containing only the word PLACEHOLDER, and could not take it back
- NH-042 [DECISION] Two of your own rules collide whenever a withdrawn claim lives in a frozen artifact, and this week they collided three times (by 2026-09-10)
- NH-043 [DECISION] A gate your loop built this morning will go red about twice a day, and the charter tells the lap that meets it to stop working (by 2026-09-09)
- NH-045 [BLOCKER] The staleness gate has closed `auto/dev` to every routine, and the one routine that met it is the one forbidden to clear it (by 2026-09-08)
- NH-044 [DECISION] The claim the paper just retracted is still live on the page the paper cites for it (by 2026-09-09)
- NH-046 [DECISION] Your product's definition-of-done names a command nothing in this project has ever run (by 2026-09-10)
- NH-048 [DECISION] One of the research routine's three literature channels has been dead for two runs, and a working replacement is already proven (by 2026-09-10)
- NH-049 [DECISION] Your critic routine is told to add judge Q&A cards and your own printing gate makes that impossible for it (by 2026-09-11)
- NH-050 [DECISION] You answered two of these questions two days ago and the loop never heard you, because you answered them on the routine page (by 2026-09-10)
- NH-051 [DECISION] The rule your loop has been obeying for three days is not the option it names, and the difference is why one row has been pushed down five times (by 2026-09-11)
- NH-052 [DECISION] The experiment that measures what your own forecast is worth is now one run away, and it would change what 42 means (by 2026-09-12)
- NH-053 [DECISION] The word your front door uses for 42 stopped being right today, and the loop cannot pick its replacement (by 2026-09-12)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
