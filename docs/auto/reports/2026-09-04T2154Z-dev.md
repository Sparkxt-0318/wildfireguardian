# WildfireGuardian autoloop · dev · 2026-09-04T2154Z

| | |
|---|---|
| when | 2026-09-04 21:54 UTC · 2026-09-05 06:54 KST |
| branch / head | `auto/dev` / `9570dde` |
| kind | dev |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 7, done: 13, parked: 1, todo: 48 |

## What happened this lap

Row: **WFG-062** (P0, infra) — the registry of withdrawn claims. Claim `c1ad39e`, build `e350571`.
Written against WFG-084's 120-line proposal (critic #13) and landing at **182** — the cap was not
met and I will not pretend it was. Sixty-one of those lines are the reviewer's block and what it
cost; the rest is "what this does not do" and the escape found while checking my own licences.
Those are the last three sections I would cut. The long evidence is in the commit messages.

## Your decisions, applied

**Nothing new in either channel.** Gmail, `from:siyeong0318@gmail.com subject:"WildfireGuardian
autoloop" newer_than:14d`: 38 threads, every one of them a single message this loop sent itself,
no reply from you. PR #31: zero comments. `docs/auto/decisions_seen.json` is unchanged, so
`decisions.py` had nothing to parse or apply. **NH-024 is still open and this lap did not answer
it for you** — it took the row NH-021 chose and `DIRECTION.md` names as row 1, which is what the
open escalation says to do until you say otherwise.

## What I did, and why this row and not a booth row

Critic #13 set no `fix-before-next-row` item; `gates.py --mode quick` was green at `d1b1520` and
GitHub's `auto-gates` run 97 at the same head is `success`, so §4b had no first job either.
`DIRECTION.md` row 1 is WFG-062, and NH-024 is open on exactly whether it should be — a lap that
reorders under that escalation makes it theatre, which is critic #13's reasoning and I followed it.

**The root objection I recorded against my own plan, before building (`hate`).** *A registry that
only re-expresses the four hand-rolled families in JSON changes nothing measurable — the escapes
critic #9 found are in the detectors, not in the lists.* That objection is correct about
sensitivity and it decided the shape of the work: I built no detector and claim no improvement to
one. Its cheapest test was to ask what the three findings the loop actually paid for had in common
— WFG-063's `docs/SESSION19_REPORT.md` ("a session report nobody had listed"), WFG-070's
`RESEARCH_BRIEF_2026-09-03.md` and `R3_science_gaps.md`, and the `R7_rubric_gap.md` that lap found
and did not know about. Not one is a spelling that escaped a pattern; all three are a **file that
was not on a list**. The union of the five guard lists is **11 files**; the tree has **988**
tracked `.md`/`.html`. So the row's own sentence is a scope problem, and that is what I built.

## What shipped

- `docs/auto/withdrawn_claims.json` — three claims: **WC-001** the satellite/telephone ordering
  (WFG-053 / NH-019), **WC-002** human-report primacy (WFG-063 / WFG-069), **WC-003** both in
  English (WFG-070). Fifteen spellings copied **byte for byte** from `BANNED`, `BANNED_PRIMACY`
  and `BANNED_EN_SPELLINGS`; each claim carries what withdrew it, the artifact a reader can open,
  the pragma tokens, and **what to say instead**.
- `scripts/check_withdrawn_claims.py` — reads **every** tracked `.md`/`.html` minus one exception
  class: the loop's own record surfaces (reports, MEMO, NEEDS_HUMAN, BACKLOG, SCORECARD,
  CRITIC_LATEST, KCF_READINESS, DIRECTION, CHARTER, ROUTINE_PROMPTS, `archive/`, the dashboard) —
  twelve declared paths, **each with a written reason**. In `make verify`, so it runs on every lap
  and on GitHub's clean clone. **11 gated files → 915.**
- `tests/test_withdrawn_claims_registry.py` — 73 tests. Load-bearing: the whole-tree scan. The one
  answering the row: `test_a_document_nobody_has_listed_is_gated_by_construction`, testing the
  *classifier* not the tree, because no such file exists yet and that is the point. Plus a
  **bidirectional drift test**, so a pattern widened in one place and not the other fails here.
- `docs/withdrawn_claims.md` — method, result, caveats, what it does **not** show.

**Nothing was deleted** (§3.7): `tests/test_detection_ordering_is_not_claimed.py` keeps all four
families and all of its argument; the registry is where the strings now live.

## Evidence

`python scripts/check_withdrawn_claims.py --coverage` → `claims 3 · spellings 15 · tracked in
scope 988 · gated 915 · record class 73`; the scan itself → `PASSED === 3 claims over 915 gated
files`. `gates.py --mode full` exit **0**: `1450 passed, 62 skipped` in 315 s (+73 from this row
against critic #13's 1377 at `baf6962`); `verify`, `snapshot-verify`, `env-check` PASS;
`baseline-verify` WARN, expected off-laptop, `hard: false`.

## What the first whole-tree run actually found, which is the part worth your attention

Running a rule on everything for the first time, when it has only ever run on a list of eleven, is
a measurement and not a formality. It found **11 unlicensed mentions in two files the spelling
families had never read**: `docs/SESSION19_REPORT.md` (10) and `docs/finals_screen_v2.md` (1).
Both already carried the withdrawal **in prose**; neither carried a licence a machine could read.
**No existing prose was changed** — ten lines gained invisible `<!-- forbidden-ok: … -->` pragmas,
the discipline `scripts/check_forbidden.py` uses and those files already used elsewhere. Then the
registry's own document failed the registry's own gate on three lines and gained three more.

**And then reading those ten lines turned up what this lap would otherwise have missed.**
`docs/SESSION19_REPORT.md:3` — the file's own **한 문장 결론**, the first sentence anyone reads —
states the withdrawn ordering claim outright: 「GK2A 기반 트리거는 언제나 사람의 **신고 뒤에**
울렸을 것입니다 — +22분, +34분, +64분」. Every WC-001 pattern keys on 사람보다 / 신고 대비 /
신고보다 / 기준 시각은 신고; 「사람의 신고 뒤에」 is none of them, so that sentence survived
WFG-053, WFG-063, WFG-070 **and** the whole-tree run that caught ten other lines in the same file.
A withdrawal for it existed — 76 lines lower, inside Phase 1. I added a **visible withdrawal
marker directly under the sentence** (prose added, none changed) and **deliberately did not add
the pattern**: a pattern I write and then grade with my own sentence is the leakage `mandela`
names and the objection this whole lap was built on. That half is filed as **WFG-085** (P1),
naming the escaped spelling so the next lap starts from the sentence and not a blank page.

## The row's catch-rate clause, answered by declining to invent a number

The row says: *"Any replacement must publish its catch rate against a mutation set its author did
not write."* The honest answer is that **this change does not move that number and I am not
claiming it does.** The patterns are the same patterns and the drift test keeps them so. The
externally measured scores stand as they were, restated in the test docstring and in
`docs/withdrawn_claims.md` §4 rather than quietly dropped. The reviewer's own 27-sentence set
adds a sixth row and is the first external measurement of the dimension this row *did* move:

| set, written by | rule | score |
|---|---|---|
| critic #9 (F47) | `primacy_violations` | **0 / 20** |
| critic #9 (F47) | `priority_violations` | **2 / 20** |
| the 0855Z lap's reviewer | `primacy_violations` | **1 / 20** (19 escaped) |
| the 0855Z lap's reviewer | the screen's negation rule | **8 / 20** |
| WFG-070's reviewer | `english_ordering_violations` | **9 / 18** |
| **this lap's reviewer, planted in an unlisted file** | **the registry (coverage)** | **17 / 17** |
| this lap's reviewer, rewordings | the registry (spellings) | 0 / 10 |

The dimension that *did* move is measured mechanically instead, and needs no mutation set at all:
**11 → 915 files**, re-derived by `test_the_coverage_this_row_bought_is_recorded_and_re_derived`
rather than restated.

## What did not work, and what this does not do

- **Widening scope costs false positives — one, measured not estimated.** `finals_screen_v2.md:75`
  says 「사람 신고를 일차로 **끌어올리지 않습니다**」, an honest negated sentence: the structural
  rule reads the negation and passes it, the spelling rule cannot. One in 915, licensed by pragma.
- **The two structural rules are NOT absorbed** — they reconstruct sentences from wrapped markdown
  per language and do not fit a registry row. WFG-072 keeps that half; the reason is written down.
- **The record class is the whole risk here.** If it grows quietly the gate shrinks quietly, so the
  **twelve declared paths** and the **ten single files** among them are pinned in a test that fails
  until someone edits the number and this document in the same commit; and no path in it may also
  be a guarded judge-facing file.
- **A committed artifact tripping this gate is a NEEDS_HUMAN entry, not an edit** — 652 generated
  dispatch sheets under `outputs/` are in scope and §3 rule 2 forbids touching them. Clean today;
  said out loud because the failure mode is a lap that "fixes" it.
- **My first draft of the coverage test was a time bomb and I caught it before the gate run, not
  after.** It pinned the record class at its 73 files; `docs/auto/reports/` gains a file every
  lap, so it would have gone red on the push carrying it and on every push after. The pin is now
  on the twelve declared paths and the ten single files among them — on what a human edits, not
  on what the class happens to hold.
- **The 「사람의 신고 뒤에」 escape is the honest measure of what a spelling gate is worth.** Four
  rows and one whole-tree pass did not see the headline sentence of a document the Q&A bank cites,
  because it uses a possessive. WFG-085 carries it. Nothing this lap did makes that class of miss
  less likely — it only makes the *file* class impossible.
- It reads spellings, not meaning: a copy-paste ratchet. Do not cite it at the booth as proof that
  a claim cannot come back.

## Reviewed by

**Reviewed by: subagent (block, then fixed): *the lap's number counts files READ, not claims
CAUGHT — and the same commit proves the two differ in the very file it opened.*** The reviewer
found `docs/SESSION19_REPORT.md:3` itself, independently of my finding it, and was right that I
had reported that file clean. What it broke, and what was done:

1. **A live escape three lines from a line I had just licensed.** 「기준**이** 신고 시각이므로」
   at `:229` and `:293` — the registered heading in the other word order. **Registered as WC-001's
   seventh spelling**, which is legitimate precisely because a reviewer who did not write the
   patterns found it *in the tree*; a pattern I invent and grade with my own sentence is leakage.
2. **A licence that did not reach.** `:213` sits in `## 주장 규율 점검`, which no withdrawal
   notice reaches — the nearest is scoped 「이 절」 to Phase 1, 130 lines up. Point-of-use notices
   added at the head of that section and of the limitations section.
3. **Two numbers that traced to nothing.** The checker docstring said the guard lists covered 11
   of **343** files — a number no command in the tree yields — and a test docstring said 914 where
   everything else said 915. Both fixed. §3 rule 3 is absolute and I broke it twice.
4. **"11 → 915" is generously composed.** Of the 915: **652** generated `outputs/**` sheets,
   **105** vendored `.claude/skills/**` docs, **158** hand-written project documents. The pair to
   quote is **11 → 158 for prose a person writes, 915 scanned.** Now in the doc and the test.
5. Filed **WFG-086**: `BACKLOG.md` is exempt and `dashboard.html` is generated from it and also
   exempt, so a withdrawn claim could ride from a row into a rendered page that leaves by email.

**What it could not break, and this is the measurement the row asked for.** The reviewer wrote
**27 sentences I did not write** and planted them at `docs/auto/finals/HOSTILE_REVIEW_PROBE.md`, a
path no guard list names. The 17 that use the registered spellings: **17/17 caught**, exit 1 —
while on the identical tree the old per-file gates returned **81 passed, green**, never reading
the file. The 10 that reword around the spellings: **0/10**, which is exactly the class §4.2 of
the doc says is unchanged. Combined **17/27**. The coverage claim held; the sensitivity claim was
never made.

## Next row

`DIRECTION.md` rows 2 and 3: **WFG-003** (finals screen audit + 5-minute demo script, due 09-07,
ticks R4 and half of R1), then **WFG-036** (release bundle v1, due 09-10). NH-021's gate is built,
so nothing now stands between the next lap and the booth rows.

## In plain terms

- **What changed for the project.** The check that stops a retracted claim creeping back used to
  look at eleven files, and every time it missed something it was because someone wrote a *new*
  file nobody had added to the list. Now there is one place listing what this project has
  retracted, and all 915 documents are checked against it automatically.
- **Why it matters to the judges.** Three times this week a sentence the project had publicly
  withdrawn was still sitting in a file nobody was watching; a judge finding one would be the
  worst five seconds of the demo. Two more such lines surfaced the first time the new check ran.
- **What you should do.** Nothing for this — but **NH-024 is still waiting**, and its gate half is
  now finished. Its options C and D are about handing the booth rows their place back, and the
  next two rows are already the booth ones, so a one-line reply just confirms it.



## Gates

**ALL GREEN** · mode `full` · head `e3ac1e4` · 2026-09-04T21:51:34Z · ⚠ **stale: the gates read `e3ac1e4`, HEAD is `9570dde`** — this table does not certify the pushed tree; re-run `gates.py --mode full` and `gates.py --assert-head` before pushing

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 20.1 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.2 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 0.5 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 308.2 s | 1456 passed, 56 skipped, 52 warnings in 306.58s (0:05:06) |

## Commits since the previous report (3bf4dc11c0ae3210f1713db72ae66b6d474b4272..HEAD)

- 9570dde the reviewer blocked this row and it was right: a live escape three lines from a licence, and two numbers that traced to nothing
- e3ac1e4 the coverage pin was a time bomb, and the sentence the file opens with had escaped four rows
- e350571 WFG-062: withdrawn claims move from five hand-written file lists to one registry the whole tree is read against
- c1ad39e claim WFG-062 (20260904T2119Z)
- d1b1520 paper: the designated-refuge list the manuscript could not use, and 318 words that were never prose

## Needs a human (5 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-024 [DECISION] WFG-062 is now three laps deferred, and the critic's own re-scope condition has been met (by 2026-09-07)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
