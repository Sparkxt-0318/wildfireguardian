# WildfireGuardian autoloop · critic · 2026-09-12T0230Z

| | |
|---|---|
| when | 2026-09-12 02:30 UTC · 2026-09-12 11:30 KST |
| branch / head | `auto/dev` / `b7e6ef4` |
| kind | critic |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 10, done: 37, dropped: 1, parked: 1, title-derivable: 1, todo: 125 |

## What happened this lap

Critic #71 reviewed `e7ba085` (window `c7de4c0..e7ba085`, 24 h). ONE §3b row move spent,
on WFG-264. ZERO `fix-before-next-row` items. One new P1 row, one new NEEDS_HUMAN entry. <!-- forbidden-ok: 264, this is the BACKLOG ROW ID WFG-264 and not the gangneung_donghae_2022 Build-A positive count the gate anchors that value to. Same false-positive class as ba437f5, where the gate read a lap stamp as a camera count. No figure is asserted on this line. -->
No scorecard row moves: Track B 96, Track A 97, both held.

## Your decisions, applied

**Nothing new arrived.** The Gmail search
(`from:siyeong0318@gmail.com subject:"WildfireGuardian autoloop" newer_than:14d`) returned
**30** threads and every one carries **exactly one** message, which is the loop's own send.
PR #31's comment list is **empty**, checked here through the GitHub MCP.
`docs/auto/decisions_seen.json` still records `"seen": []`. **No decision has ever arrived
on the email channel**; every applied decision came through a Claude Code session on the
laptop, and the newest is **NH-031, received 2026-09-06**, six days ago. That is
**WFG-211**, already `todo`, confirmed here and not re-filed.

**27 decisions are now open for you** (26 DECISION + 1 BLOCKER), 2 undated. **NH-046,
NH-049 and NH-051 are past due.** NH-060 is new this lap. The sprint ends **2026-09-15**,
three days out.

## The root objection: the project audits the sheet it publishes, not the sheet it hands over

`docs/routing_limitations.md` now holds **six** numbered audits of A4 dispatch-sheet
sentences, and **all six are on the 459 walk series**. The sheet the booth physically opens
is the **439 vehicle** series. `docs/auto/JUDGE_QA.md:1308` tells the student
「실물을 열어 드릴 수 있습니다 — `outputs/dispatch/` 에 마을별 A4 출동 지시서」, and Q39 at
`:1554` says **30 of its 33** sheets are reprinted for the booth with
`python scripts/generate_dispatch_outputs.py`. That script, at `:102` and `:134`, emits
「예산 내 차량 진입로가 화재로 차단됨(우회 포함)」: a budget was consumed **and** detours were
tried. That is the identical two-clause form §1 removed from `fa_exceeds_budget` on
2026-08-10 and §6 removed from `no_safe_route` on 2026-09-12.

**Measured in this lap's own process.** `outputs/dispatch/20260801T163042Z/` holds **33**
committed cluster directories and **33** `dispatch_a4.html`; **17 of the 33** print the
sentence and the other 16 carry no unreachable point; **3** are committed as
`dispatch_a4.pdf` and **1 of those 3** (`02-천전공원-일대`) carries it, so it is on paper in
the booth and not only in the tree. One grep for the sentence across `docs/`, `paper/` and
`README.md` returns **nothing**: no file states what its code condition is.

⚠ **It is not established false and must not be repaired by analogy.** Nobody has read the
439 arm's unreachable condition out of the code. The WFG-262 lap refused to rewrite it on a
guess, and that refusal was right: it is the trap that row pre-registered against. The job
is to read the condition and write it down, as §1 and §6 did.

**The cheapest test** (`hate`): one lap, no refit, no committed count moves. Read the 439
unreachable rule out of the code that sets `four_way_class` — `scripts/run_rescue_routing_full.py`
and `src/wildfireguardian/routing/rescue_demo.py` are where the term is defined and consumed —
and write it into `docs/routing_limitations.md` as §7, and either state the sentence IS that condition
with evidence, or replace it with the condition and nothing more. Committed sheets keep the
wording they were generated with, as records, exactly as §1 and §6 left theirs.

## Findings, ranked

1. **WFG-264 was filed at the wrong priority and the wrong place, and that is finding #1.** <!-- forbidden-ok: 264, this is the BACKLOG ROW ID WFG-264 and not the gangneung_donghae_2022 Build-A positive count the gate anchors that value to. Same false-positive class as ba437f5, where the gate read a lap stamp as a camera count. No figure is asserted on this line. -->
   The lap that found it filed it **P1, at table row 259 of 258**, citing §14b's readiness
   hold in the row's own text. §14b names what waits behind those ticks and it is a list:
   「**loop hygiene** (report certification, gate-on-gate, commit-id bookkeeping, mechanics
   of the routines)」. A booth printable is on none of them, and the **same sentence** of
   §14b says 「anything larger, however judge-facing, is filed as a **P0 row at position 1**
   of the table」. The consequence is arithmetic: **R3 is the only one of the six ticks
   outstanding**, it is `blocked(NH-046)`, NH-046 came due **2026-09-10**, no reply has ever
   arrived by email, and the sprint ends **2026-09-15**. Filed as it was, the row could not
   have been started at all. **Re-filed P0 at position 1**, my one §3b move, with every
   constraint the dev lap wrote kept intact. Your half is **NH-060**, with both readings as
   options; pick B and it goes back to P1 and WFG-256 returns to position 1.

2. **The judge drill found two questions no file in the repository answers**, both on that
   surface, now halves (e) and (f) of WFG-264: 「이 A4 시트가 지금 심사위원 손에 있습니다. <!-- forbidden-ok: 264, this is the BACKLOG ROW ID WFG-264 and not the gangneung_donghae_2022 Build-A positive count the gate anchors that value to. Same false-positive class as ba437f5, where the gate read a lap stamp as a camera count. No figure is asserted on this line. -->
   이 문장은 프로그램이 실제로 확인한 것입니까?」 and 「보행 쪽은 오늘 고쳤다고 하셨는데
   커밋된 시트에는 옛 문장이 그대로입니다」. The second is answered today **only** on
   `docs/live_pipeline.md`, a developer page. The other eight of the ten hardest questions
   were answerable from files.

3. **A life-safety sheet line has never been executed.** `route_region`'s `origin_refused`
   branch in `src/wildfireguardian/live/pipeline.py` prints
   「출발 지점이 이미 통행 불가 기준 이상, 경로 탐색 없음」 and sets
   `origin_refused_before_search`. `docs/routing_limitations.md` §6 discloses it in its own
   words: 「the branch is reasoned and reviewed, not executed: if it ever fires in a real
   run, that run is the first execution of those three lines」. Filed as **WFG-265** (P1,
   product). The disclosure is exemplary; the gap is that the one line whose purpose is to
   fire when a thin invariant stops holding is the line no run has produced.

4. **`WFG-263` (P1) sits above the first `todo` P0 row**, against DIRECTION's own rule.
   Flagged, not moved: the move budget was spent, and CHARTER §4 step 3 takes the
   highest-**priority** `todo` row rather than the highest table row.

5. **Zero `KCF_READINESS.md` lines ticked for the twenty-eighth consecutive critic lap.**
   `git diff 8952a5b..e7ba085 -- docs/auto/KCF_READINESS.md` is empty; the file's newest
   commit is still `c241904`, critic #66. It stands at **8 of 11**. Reported, not re-filed:
   it is NH-046 and it is yours.

**`factchk`: zero new claims about the world in the window's prose.** Everything new in
`docs/routing_limitations.md` §6 and `docs/live_pipeline.md` is a claim about this
repository's own code, checkable in the tree and checked above. No new external figure, no
new citation, nothing to verify outward.

## What the lap under review got right, stated because it is the record

The WFG-262 lap is the best-controlled lap in this repository this week.

- Handed a premise, it read code that contradicted it and wrote **both** interpretations
  into the claim commit `c83e8d2` **before any number existed**, including 「if the count is
  ZERO ... that is the lap's finding and goes on the page in those words」.
- It **imported** the committed origin rule rather than restating it.
- Its identity controls are **gating, not reported**: the script refuses to exit 0 unless
  `n_nodes` and `n_origins_scanned` re-derive exactly against each committed artifact.
- The answer was **0** on all three regions against committed buckets of 2, 12 and 10, and
  it published that, which made its own assignment less dramatic.
- It disclosed the tautology (the origin filter removes the population the count then looks
  for) in the claim commit, the artifact, the registry caveat and §6, and its own reviewer
  logged the same thing as a `mandela` pattern-4 hit. **A disclosed tautology is not a
  defect; an undisclosed one would have been.**

Verified here rather than taken on trust: **no caller in `src/` or `scripts/` passes a
non-zero `departure_min`**, so §6's caveat 「it says nothing about a scan called with
`departure_min > 0`」 is a real limit correctly stated and not a hedge.

## The five lenses (`prism`)

- **KCF judge, software professor.** Would score the window high on 설계와 방법론: a
  pre-registered null with gating identity controls is what 「변수의 정의와 제어의 적절성」
  asks for. Would then ask why the audit's own stated lesson, 「instance-scoped repair」, was
  repeated in the *priority* the same lap chose.
- **KCF judge, disaster-response official.** Holds the A4 sheet and nothing else. The walk
  sheet now says exactly what the program worked out. The sheet they will be handed is the
  vehicle one, and nobody can yet tell them whether its sentence is checked.
- **Fire-behaviour scientist.** Nothing new to attack: the guard's predicate reads the
  hazard field at t = 0, not fire behaviour. Would note approvingly that §6 names the
  `departure_min > 0` case rather than waiting to be asked.
- **ML reviewer hunting leakage and weak baselines.** The measurement is tautological by
  construction and says so; the independent part is the identity controls and they are real
  and gating. Nothing is scored, so nothing leaks. The weak-baseline question is untouched
  by this window and remains WFG-256 and WFG-234.
- **Statistician.** Three counts, all zero, no variance, no inference. The one quantitative
  statement carrying risk is 「clears the 0.5 cutoff by under half a hundredth」
  (`nsr_max_departure_prob_yeongdeok`): a margin that small means the invariant holds by
  arithmetic rather than by design, and §6 says exactly that. Correctly framed.

## The gate table below names the head this lap REVIEWED, not the head it ships in

`report.py` writes the gate table before the report is committed, so the table can only ever
name the review head (`664ca6f`, ALL GREEN, first run). That is the distinction critic #70
wrote into `3bbcbe2` rather than spending a second report and a second gate cycle on a
re-stamp. **What certifies the commit this report actually ships in is CHARTER §8's two
assertions**, both run immediately before the push and both exiting 0:
`gates.py --assert-head` (a `full` pass at exactly this `HEAD`, clean tree) and
`gates.py --assert-reported`. The full suite was re-run on the commit carrying these
`docs/auto/` changes before either assertion. Nothing here is certified by a stale table.

## Verification of the loop's own claims

- `gates.py --mode full` exits **0** on its **first** run at `664ca6f`: 2202 passed, 65
  skipped, 3 xfailed, pytest 311.2 s. `baseline-verify` is WARN on the two git-ignored raw
  manifests, which is CHARTER §3d's expected sandbox result.
- `--assert-head` and `--assert-reported` both exit 0.
- GitHub `auto-gates` run **391** is `success` at `664ca6f`; run **392** was still
  `in_progress` at `e7ba085`. **No run in this lap's window concluded `failure`** (381 was
  `cancelled`; 386 at `edd0ec0` belongs to critic #70's window and was disclosed and ruled
  on there). **CHARTER §4b therefore sets no finding #1.**
- All **fifteen** reports in the window record `Reviewed by:`.
- Printed kit **7 of 7** (`WFG_printables_20260911T2137Z.pdf`, 60 pages), release bundle
  **19 of 19**, bundle names that kit. Both re-hashed here, not read from a report.
- The clone is SHALLOW at **52** commits and was deliberately not deepened. No ancestry
  claim is written anywhere in this lap.

**And one thing the loop did right.** `report.py` stamped a **RED, stale** gate table into
the 0135Z report body and email header, naming `09eea5b`, a commit no clone can resolve, on
a branch that was green. The lap caught it **before the email went out**, re-ran the gates
at the pushed head, superseded the 0135Z body under CHARTER §3.7 rather than editing it, and
re-stamped as 0146Z, ALL GREEN at `664ca6f`. Nothing wrong reached you. It cost a second
gate cycle; **WFG-263** is the row that would stop it costing one each time, and it stays
P1 because it is loop hygiene by §14b's own enumeration, which is the distinction this whole
lap turns on.

## Scorecard deltas

**NO row moves. Track B HOLDS at 96 (18 / 20 / 20 / 19 / 19), Track A HOLDS at 97
(19 / 20 / 20 / 19 / 19).** Per-row evidence is in `docs/auto/SCORECARD.md` at `e7ba085`.

- **구현 및 유용성 20, held rather than dropped, and the decision is argued not asserted.**
  The case for falling is the vehicle sentence above. The case for holding wins: it is not
  established false, and the walk sheet **strictly improved** in this window while
  `docs/live_pipeline.md`'s mapping table became bound cell by cell to the code after a
  month of silent drift. A row does not fall in the window its artifact got better, on a
  defect newly *identified* rather than newly *created*.
- ⚠ **Pre-registered DOWNWARD for critic #72: 구현 및 유용성 falls to 19 if WFG-264 is still <!-- forbidden-ok: 264, this is the BACKLOG ROW ID WFG-264 and not the gangneung_donghae_2022 Build-A positive count the gate anchors that value to. Same false-positive class as ba437f5, where the gate read a lap stamp as a camera count. No figure is asserted on this line. -->
  `todo`.**
- ⚠ **Pre-registered UPWARD for critic #72: 제출 자료 rises to 20** when WFG-264 (a) and (b) <!-- forbidden-ok: 264, this is the BACKLOG ROW ID WFG-264 and not the gangneung_donghae_2022 Build-A positive count the gate anchors that value to. Same false-positive class as ba437f5, where the gate read a lap stamp as a camera count. No figure is asserted on this line. -->
  land, and not on a report that says so.
- ⚠ Critic #70's downward condition on 구현 및 유용성 is **discharged**: it fired only 「if
  WFG-262 is still open」, and WFG-262 closed at `77d44b6`.

## In plain terms

- **What the judges would see today.** A project that is now unusually good at catching
  itself: yesterday it found that one line on your rescue sheet claimed more than the
  program had actually worked out, and it fixed that line and published the check. The
  problem is which sheet. Every one of those six checks is on the **walking** sheet. The
  sheet your booth script tells you to hand a judge is the **vehicle** one, and it still
  says 「the road in was blocked by fire within the time budget, detours included」 on 17 of
  your 33 sheets and on 1 of the 3 you have as PDF. Nobody has checked whether that sentence
  is true. It might be. Nothing in your project says.
- **What changed since yesterday.** One backlog item closed properly, and it closed with the
  loop admitting its own task had been based on a wrong guess and publishing the boring
  answer anyway. That is the best kind of lap you can get. But the leftover job, the vehicle
  sheet, was filed at the very bottom of a 259-row list behind a gate that one unanswered
  email has been holding shut since 09-10. I moved it to the top and wrote down why. Your
  scores did not move: Track B 96, Track A 97.
- **What you should do.** **Answer NH-046.** It is still the single thing holding the most:
  the ninth readiness tick, the whole parked block of work, and, until I moved it this
  morning, a defect on the paper you hand to judges. Then **NH-060**, which is one letter
  and takes ten seconds: it just asks whether you agree that a booth printable outranks loop
  housekeeping. Open the repository and say `decisions` on your laptop; the sprint ends
  **2026-09-15**.



## Gates

**ALL GREEN** · mode `full` · head `b7e6ef4` · 2026-09-12T02:27:52Z · current at `b7e6ef4`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 18.7 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.1 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 0.3 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 300.1 s | 2202 passed, 65 skipped, 3 xfailed, 52 warnings in 298.89s (0:04:58) |

## Commits since the previous report (e7ba085da0720a8443576aa4ad3785bec83d94b0..HEAD)

- b7e6ef4 critic #71: the project audits the sheet it publishes and not the sheet it hands over

## Needs a human (32 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09, one day past; raised to HIGH by critic #55 on a measured third instance)
- NH-036 [DECISION] One critic lap told the next one not to edit a file, and that is what kept a false sentence in front of a judge for a window (by 2026-09-10)
- NH-037 [DECISION] The paper's word proxy now stops it a thousand words before your 25-page rule (by 2026-09-10)
- NH-038 [DECISION] Your "product first" rule has spent the last three dev laps on documents, and the readiness line it was written to protect has not moved in five critic laps (by 2026-09-09)
- NH-039 [DECISION] The national wildfire-spread system's manual is an 18 MB PDF the sandbox could not fetch, and one of you can (by 2026-09-12)
- NH-040 [FYI] A critic lap pushed one commit past a red `--assert-reported`, and it is telling you rather than hiding it
- NH-041 [FYI] This lap sent you an email containing only the word PLACEHOLDER, and could not take it back
- NH-042 [DECISION] Two of your own rules collide whenever a withdrawn claim lives in a frozen artifact, and this week they collided three times (by 2026-09-10)
- NH-043 [DECISION] A gate your loop built this morning will go red about twice a day, and the charter tells the lap that meets it to stop working (by 2026-09-09)
- NH-045 [BLOCKER] The staleness gate has closed `auto/dev` to every routine, and the one routine that met it is the one forbidden to clear it (by 2026-09-08)
- NH-044 [DECISION] The claim the paper just retracted is still live on the page the paper cites for it (by 2026-09-09)
- NH-046 [DECISION] Your product's definition-of-done names a command nothing in this project has ever run (by 2026-09-10)
- NH-048 [DECISION] One of the research routine's three literature channels has been dead for two runs, and a working replacement is already proven (by 2026-09-10)
- NH-049 [DECISION] Your critic routine is told to add judge Q&A cards and your own printing gate makes that impossible for it (by 2026-09-11)
- NH-050 [DECISION] You answered two of these questions two days ago and the loop never heard you, because you answered them on the routine page (by 2026-09-10)
- NH-051 [DECISION] The rule your loop has been obeying for three days is not the option it names, and the difference is why one row has been pushed down five times (by 2026-09-11)
- NH-052 [DECISION] The experiment that measures what your own forecast is worth is now one run away, and it would change what 42 means (by 2026-09-12)
- NH-053 [DECISION] The word your front door uses for 42 stopped being right today, and the loop cannot pick its replacement (by 2026-09-12)
- NH-054 [DECISION] Four fifths of your front door's headline is limits, and no lap is allowed to decide whether that is the project's strength or its biggest presentation risk (by 2026-09-13)
- NH-055 [DECISION] Your front door compares the headline forecast number to a model the same page calls broken, and today the loop measured a second, harsher comparison it is not allowed to put beside it (by 2026-09-13)
- NH-056 [DECISION] A Korean university published a reproducible deep-learning wildfire model four weeks before your finals, and a lap may not decide on its own whether to mention it (by 2026-09-13)
- NH-057 [DECISION] The one thing your project now says it contributes has never once been produced from a real fire, and only your laptop can change that (by 2026-09-13)
- NH-058 [DECISION] Two of your routines built the same row in the same window, and the rule that was supposed to stop that cannot see the paper routine (by 2026-09-13)
- NH-059 [DECISION] Your loop measured the hardest question a judge can ask you, and the answer is uncomfortable enough that no lap may decide on its own whether you say it out loud (by 2026-09-13)
- NH-060 [DECISION] Your "product first" rule sent a booth-printable defect to the bottom of the board, and two of your routines read the same sentence of it differently (by 2026-09-14)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
