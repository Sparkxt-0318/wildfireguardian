# WildfireGuardian autoloop · critic · 2026-09-07T0521Z

| | |
|---|---|
| when | 2026-09-07 05:21 UTC · 2026-09-07 14:21 KST |
| branch / head | `auto/dev` / `0fc6130` |
| kind | critic |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 7, done: 22, parked: 1, todo: 86 |

## What happened this lap

# Critic #32 — a readiness line moved for the first time in nine laps, and the same window skipped the one rule written to stop the thing it skipped

Reviewed head `0fc6130`. Window `91d3e05..0fc6130`, the 24 h to 2026-09-07T05:00Z, 58 commits:
the 0711Z, 1000Z, 1313Z, 1638Z, 2154Z, 0109Z and 0355Z dev laps, the 1838Z research lap, three
paper laps and four critic laps. Clone unshallowed before any measurement
(`git rev-parse --is-shallow-repository` answers `false`, 525 commits).

## Your decisions, applied

**Nothing new on either channel, and every lap since 2026-09-04 has reported the same.** The Gmail
search `from:siyeong0318@gmail.com subject:"WildfireGuardian autoloop" newer_than:14d` returns
threads that are every one a single message this loop itself sent; there is no reply from you in
any of them. PR #31 has no comments. `scripts/auto/decisions.py` was therefore not run and
`docs/auto/decisions_seen.json` is unchanged.

⚠ One thing worth knowing, since NH-041 asked you about it four hours ago: **the Gmail connector is
working again.** The 0355Z lap reported its token expiring mid-run at 04:05Z, between a send and a
trash call. Search and read both work from this session, so whatever expired has recovered or was
transient. Nothing is required of you.

Fifteen NEEDS_HUMAN entries are open, ten DECISION and five FYI, counted here; this lap adds
**NH-042** (DECISION) and annotates **NH-038** with its ninth data point, which is the first one that
goes the other way.

## Findings, ranked

### 1. The sentence the last lap declared false is live on the USB stick, and it was never registered

`release/kcf-finals-2026/printables/manifest_20260907T0059Z.json:95`, inside the folder a judge is
handed, states that the dispatch sheets are 「already committed PDFs that print directly」. The
0355Z lap's own report says of that sentence: **「Both halves are false.」** The tree has **33**
cluster directories and **3** committed `dispatch_a4.pdf`, re-counted here. That lap corrected the
claim in `README_KO.md`, `docs/finals_bundle.md` and R9's cell — and registered it nowhere.
`docs/auto/withdrawn_claims.json` holds WC-001 to WC-005 and no sixth. **CHARTER §3.5c says a
withdrawal is not applied until it is registered, in the same lap**, and that rule was written two
days ago because a hand-applied correction will always miss a file. This is the third day running
that the registration step failed, and the first time the residue is on a judge-facing surface.

**Probed rather than argued.** I added the two spellings to the registry and ran
`scripts/check_withdrawn_claims.py`. It exits **1**, naming exactly one file:
`docs/finals_bundle.md:86` — outside `docs/auto/`, which this routine may not edit, so the critic
could not register it either. Reverted; rescan `PASSED === 5 claims over 929 gated files`, tree
clean. ⚠⚠ **And the scan named neither of the two places the claim is actually live**, because
`withdrawn_claims.json` → `scope.extensions` is `[".md", ".html"]`: the manifest is `.json`, and the
sentence is authored at `scripts/build_printables.py:648` as implicit concatenation split at
「print 」 / 「directly」, which a line-based scan would miss even with `.py` added.

**WFG-153 raised P1 → P0** (this lap's one row move, CHARTER §14) and its (a) half is this lap's one
`fix-before-next-row` item (CHARTER §14b: the release bundle is a judge-facing surface). ⚠ It rides
**WFG-026**'s kit rebuild rather than taking a lap of its own, because only a lap that rebuilds the
kit at a new stamp may move the manifest. The rule collision behind it is **NH-042**, yours.

### 2. The test that binds R7's mapping to R7's wording is vacuous for all five names

`tests/test_printables.py::test_r7_still_enumerates_the_five_printables_this_list_resolves` asserts
`name in readiness` against the whole 111 KB of `docs/auto/KCF_READINESS.md`, which is mostly
finding history. Occurrences of each name in that file, measured at `0fc6130`:
`evidence sheet (A4)` **2**, `reconciliation sheet` **8**,
`related-work and SFTD059T differentiation panel` **3**, `booth checklist` **3**,
`29 dispatch sheets sample` **4**. Every one occurs outside R7's row, on lines that predate this
lap. R7's definition cell could be rewritten in full and the test stays green — which is exactly the
state WFG-153(a) has to move through.

**The fix is already written, in the twin, by the same lap.** `tests/test_finals_bundle.py:165`
narrows the identical assertion to the definition cell and spells out why in five lines that begin
「The same binding `tests/test_printables.py` puts on R7」. The 0355Z lap wrote that comment and did
not apply it one file over. **WFG-156**, P1.

### 3. The claim registry cannot see where this loop's judge-facing prose is generated

`.md` and `.html` only. Outside it: every `manifest_*.json` (one of which ships inside the release
bundle and carries a paragraph of prose claims about this repository), `release/kcf-finals-2026/MANIFEST.json`,
`docs/NUMBERS.json`, and every generator under `scripts/`. Widening `extensions` alone does not close
it, for the line-splitting reason above. ⚠ This is also why `paper/manuscript.md` §3.5's sentence
「Had that sentence been a registered spelling the scan would have found it in the template, which
the scan does read」 is **true** of `scripts/finals.template.html` and **false** of
`scripts/build_printables.py`, and the manuscript does not distinguish them. **WFG-155**, P1.

### 4. The suite reached the public internet during this lap's own gate run

`data/raw/` held only `.gitkeep` and `README.md` at 04:57Z in this sandbox. `gates.py --mode full`
started at 04:59Z. `data/raw/dem/srtm/N36E129.hgt` (**25,934,402 bytes**) and its `.gz` have mtime
**05:02:55Z**. CHARTER §4b forbids a test that depends on the network in those words;
`docs/auto/JUDGE_QA.md` Q28 tells a judge that the tests needing raw input skip with a reason. This
is **WFG-139**, P0, position 3 on `DIRECTION.md`, confirmed for a fifth consecutive critic lap and
for the first time by the downloaded file rather than by a cold/warm pass-count delta.

### Not findings, checked and clear

- **GitHub's own runs (CHARTER §4b).** `auto-gates` runs **177 to 196** on `auto/dev`: **18
  `success`, 2 `cancelled`, zero `failure`**; run 196 at this head is `success`. No red run behind a
  green report, so no finding #1 of that kind.
- **Report certification.** `gates.py --assert-reported --base 91d3e05` exits **0**, 55 substantive
  paths. Every **dev** report in the window carries `Reviewed by:`; the research report still does
  not (WFG-147, already filed).
- **`factchk` on new prose about the world.** There is none in this window. The only new external
  claims were the two Korean operational systems, cleared by critic #31. Every new prose claim here
  is about this repository, and the two carrying numbers were re-derived: 33 clusters / 3 committed
  PDFs (correct in `README_KO.md` and `docs/finals_bundle.md`), and 19 files in the bundle.
- **`prism`, five lenses.** The software professor and the statistician find nothing new to break
  this window: no experiment ran and the two new gates are sound. The fire scientist and the
  disaster-response official are unchanged since critic #31 — the differentiation panel a
  disaster-response judge would want (WFG-026) is still unwritten. The ML reviewer's standing
  objection is unchanged and correctly disclosed: the Yeongdeok fold has no leakage-free rerun, and
  the README now carries both binding caveats on the 42.
- **Judge drill.** Q8, Q14, Q16, Q23, Q24, Q26, Q28, Q30, Q35 and Q36 all answer from a named file,
  and each carries its own 「없는 것」 line. The one card whose promise the tree contradicts is
  **Q28**, and that is finding 4 rather than a Q&A defect. ⚠ No entry was added to
  `docs/auto/JUDGE_QA.md`: since `590c29a` it is a `SOURCES` document of the printables manifest and
  a one-line edit by this routine turns `tests/test_printables.py` red (WFG-152), so drill results
  become backlog rows instead.

## The root objection (`hate`)

**Every gate this loop writes compares an artifact to its own description, and the loop's answer to
that has itself become an artifact described by its own registry.** Critic #31 named the shape three
days running. Findings 2 and 3 are the fourth and fifth instances and both sit *inside* the
machinery built to end it: a row-binding test that searches a file containing its own answer, and a
claim registry whose reach is declared in a `scope` key rather than derived from where claims live.

**The cheapest test, and it was run:** register the withdrawn spelling and read which files come
back. One did — a record line. The two that are shipping did not.

## Direction check (CHARTER §14, five minutes)

Position 1 is unchanged: **WFG-026**, R7's sole remaining blocker. One row moved, **WFG-153 P1 →
P0**, placed immediately after it — sequencing, not a priority judgement, because (a) cannot be taken
alone and WFG-026's lap has to rebuild the kit anyway. No P0 row moved below a row of higher
priority. Reason written onto `docs/auto/DIRECTION.md`, which stays at one screen.

**KCF_READINESS ticked in the last 24 h: one, R9, by this lap.** The rule about two consecutive
zero-tick laps does not fire. **5 of 11** (R2, R4, R5, R6, R9). Every one of R9's five named
conditions was re-run in this sandbox rather than read from the lap that built it:
`make finals-bundle` exits 0 with 「rebuilt byte-identically, 19 files」, the folder holds those 19
plus `MANIFEST.json` and nothing else, `python scripts/check_bundle_copy.py release/kcf-finals-2026`
exits 0, and `git status --short` is empty afterwards. The dev lap left one call to the critic —
whether R9's word 「printables」 requires R7's five — and it is answered on R9's row: R9 asks that
the kit which exists reaches the stick, R7 asks the kit to be complete, and reading them as one
would make R9 permanently untickable on its own merit. R7 stays ☐ on WFG-026.

## Scorecard deltas

| | 연구/개발 목적 | 설계와 방법론 | 데이터·해석 / 구현 | 창의성 | 제출 자료 | total |
|---|---:|---:|---:|---:|---:|---:|
| Track B | 17 → 17 | 16 → 16 | 18 → 18 | 15 → 15 | 18 → 18 | **84 → 84** |
| Track A | 16 → 16 | 16 → 16 | 18 → **19** | 15 → 15 | 14 → 14 | **79 → 80** |

**Track A 구현 및 유용성 18 → 19**, and critic #31 pre-registered exactly this: it wrote 「on this
track the bundle *is* the product handed over, so 제출 자료 takes the +1 and 구현 does not」 while
`MANIFEST.json` listed 17 files with no printable. It now lists 19, verified by running the build
rather than reading the report, and the stamp is resolved from the tracked tree rather than typed, so
the next `make printables` breaks the bundle build until the same lap rebuilds it. **Why 19 and not
20:** R1 is still held by WFG-110's unmapped keys on the judged screen, the booth recipe has never
been run on the real laptop (NH-014), and R7's kit is three of the five printables it names.

**Track A 제출 자료 14, held**, and the hold is finding 1: the kit's freshness is worth +1 on this
row too and it is spent, exactly, on the sentence riding inside the same bundle. **Track B flat at
84** on two cancelling movements — the manuscript's §3.5 registration sentence is now about two
events rather than one and the paper lap's reviewer caught four wrong numbers in that lap's own
ledger before the push, against a window in which the loop's most-advertised mechanism was shown to
have an undeclared boundary.

## In plain terms

- **What the judges would see today.** For the first time in nine of my laps, something on the
  finished-product checklist actually got ticked. The USB folder you would hand a judge is now
  complete and correct as a package: I plugged it together myself in a clean machine, and every file
  in it matches the repository, including the 33 printed pages. Five of eleven boxes done. The one
  remaining thing standing between you and a sixth is a single page that has not been written yet —
  the panel explaining how this differs from existing work.
- **What changed since yesterday.** The booth kit reached the bundle, and it now picks itself up
  automatically so it cannot go stale quietly. Against that, I found one sentence inside that bundle
  that is not true: it says 29 finished dispatch PDFs are waiting in the repository, and there are
  3. The lap before mine spotted the same sentence elsewhere and fixed it in three places but did
  not log it in the list this project keeps of retracted claims, which is the step that finds the
  places you would otherwise miss. So it survived, in the copy that travels. It is booked as the
  next lap's first job.
- **What you should do.** One new question needs you, **NH-042**: two of your own rules pull against
  each other — never overwrite a finished file, and always log a retraction the same day — and this
  week they collided three times. I would pick **A** (log it anyway, and record the frozen file as a
  known-stale exception with a date), because that keeps the list honest without making every small
  correction expensive. NH-032, NH-034, NH-035 and NH-037 are still waiting and NH-032 and NH-034
  are due tomorrow.



## Gates

**ALL GREEN** · mode `full` · head `0fc6130` · 2026-09-07T05:05:34Z · current at `0fc6130`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 22.8 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.2 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 2.6 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 351.0 s | 1637 passed, 62 skipped, 2 xfailed, 52 warnings in 349.16s (0:05:49) |

## Commits since the previous report (0fc61302ff5a3cf14c1a9978152678f32cd2d6ff..HEAD)

(no new commits)

## Needs a human (15 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09)
- NH-036 [DECISION] One critic lap told the next one not to edit a file, and that is what kept a false sentence in front of a judge for a window (by 2026-09-10)
- NH-037 [DECISION] The paper's word proxy now stops it a thousand words before your 25-page rule (by 2026-09-10)
- NH-038 [DECISION] Your "product first" rule has spent the last three dev laps on documents, and the readiness line it was written to protect has not moved in five critic laps (by 2026-09-09)
- NH-039 [DECISION] The national wildfire-spread system's manual is an 18 MB PDF the sandbox could not fetch, and one of you can (by 2026-09-12)
- NH-040 [FYI] A critic lap pushed one commit past a red `--assert-reported`, and it is telling you rather than hiding it
- NH-041 [FYI] This lap sent you an email containing only the word PLACEHOLDER, and could not take it back
- NH-042 [DECISION] Two of your own rules collide whenever a withdrawn claim lives in a frozen artifact, and this week they collided three times (by 2026-09-10)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
