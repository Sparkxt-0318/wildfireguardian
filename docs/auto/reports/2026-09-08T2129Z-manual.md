# WildfireGuardian autoloop · paper · 2026-09-08T2047Z

| | |
|---|---|
| when | 2026-09-08 21:29 UTC · 2026-09-09 06:29 KST |
| branch / head | `auto/dev` / `eff2183` |
| kind | manual |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 9, done: 31, parked: 1, title-derivable: 1, todo: 101 |

## What happened this lap

Paper lap 21. Incorporated `dee1bc1..eff2183` — five files outside `paper/` and
`docs/auto/`: `README.md` (one Round-4 bullet added, one rewritten, the paragraph above
them rewritten), `.github/workflows/auto-gates.yml` (`continue-on-error` on two
artifact-upload steps, NH-047), `release/kcf-finals-2026/MANIFEST.json` (printables
hashes), `tests/test_adoption_card.py` (new, WFG-188) and `tests/test_finals_acts.py` (a
Chromium launch failure becomes a skip, WFG-196). `docs/NUMBERS.json` is not in the diff
and no artifact under `data/processed/` is either, so no result section could gain a
number, a table or a figure.

## Sections changed, and why

**§4.5, one sentence, and it was mandatory.** The section ended 「**One** repository
document still draws the stronger conclusion from those same five points … a **second**
was narrowed to this reading during revision」. The incorporated diff made that false:
`d6d801d` added a Round-4 bullet at `README.md:232` reading 「그 sweep 안에서 **고원이
아니라 뾰족한 봉우리**입니다」 — the 1 km buffer width is a sharp peak and not a plateau —
which is exactly the shape §4.5 says five widths a factor of two apart cannot resolve. It
now reads 「**Two** repository documents still draw … **others** were narrowed to this
reading during revision」. Net **−1** word. No caveat and no registered number moved; the
whole manuscript diff is two lines.

The subject grep was run on the subject and not on the sentence (DIRECTION's rule), in
English and Korean, over `README.md docs/ paper/ release/ web/ src/ scripts/ tests/`, with
the record class excluded. Two surfaces assert the shape — `README.md:232` and
`docs/present_perimeter_arm.md:129`. Four state the resolution limit —
`docs/fair_opponent_line.md` §3, `docs/auto/DEMO_SCRIPT_5MIN.md:151`,
`docs/auto/JUDGE_QA.md` (:920-921 and Q37 at :1394) and the manuscript. The
`docs/NUMBERS.json` `pp_uiseong_*` caveats still assert it in fact (3), which is why §4.5
keeps the clause about the registry's own entries. The full table is in `paper/GAPS.md`.

## Figures

None added, none redrawn. `make_figures.py` printed `drawn [F1 … F9]; skipped []` and
`git status paper/figures/` was empty afterwards — all nine byte-identical.
`F9_present_perimeter` stays drawn, committed and unreferenced (gap G8, NH-032 open).
Figure appearance order re-checked by hand against the prose: F1→1, F2→2, F4→3, F5→4,
F8→5, F3→6, F6→7, F7→8, unchanged, and the reviewer re-checked it against the built
`.docx`.

## Gaps

None opened, none closed. Seven, unchanged.

## ⛔ What the word budget stopped this lap, and it was a limitation

**The paper nowhere says that its hazard field cannot be current.**
`docs/live_pipeline.md` §0 makes that the project's own lead-with-what-it-cannot-do line —
detection near-real-time (FIRMS NRT), the weather not, ERA5 publishing on a ~5-day lag —
`live/scope.py` owns the two strings so a retyped caveat cannot drift,
`tests/test_live_pipeline.py` fails if either is missing from any screen, A4 sheet,
broadcast script, SMS draft or JSON record, and this lap's own incorporated diff added
`tests/test_adoption_card.py`, which binds a judge card that opens on the same asymmetry.
`references.bib`'s `era5` note already carries 「about five days' latency」, verified at the
Copernicus page. The manuscript's only neighbouring line is §6's 「No trigger has ever
fired on a **live** detection」, which says the system has not been *run* live and not that
its weather source makes a current field *impossible* — a structural limit no deployment
effort removes, on a paper whose framing is 「which way a rural household should walk
**now**」.

The sentence, drafted for the head of §6's Operational status paragraph, is
「The hazard field cannot be current: ERA5 publishes on a ~5-day lag.」 It was **inserted
and measured, not estimated**: `body_words` 8,993 → **9,005**, i.e. **12 words against a
margin of 7** and five over the proxy's hard fail. It was reverted and the document
rebuilt at 8,993; the reviewer reproduced the same 12 independently. **Nothing was
compressed to fund it** — CHARTER §12 forbids buying space with a caveat, and the only
compressible prose left is the litigated stock that produced wrong sentences at laps 13
and 15. The honest other half, owed by the same rule: **the manuscript is not false
without it.** It claims no real-time operation anywhere. What is missing is the *reason*,
and a reviewer who asks for it is asking a fair question the paper cannot answer today. No
`[GAP]` marker was opened, because the evidence exists and is committed — the paper is
short a sentence, not short a run.

**That is the datum NH-037 did not have.** Laps 13–18 recorded illustrations and better
wordings the budget declined, and lap 19 retired the one-directional form of that
complaint. What it declined this lap is a **limitation**. The document measured **23
pages** against the author's **25** in this same run, so twelve words move no page: the
rule that stopped it is the proxy, not the author's.

## Page count, re-derived rather than inherited

`body_words` moved 8,994 → 8,993, which moves `built_pages_inputs`, so lap 20's anchor
turned the gate red exactly as designed until a run produced a new count. After the one
`apt` line `paper/README.md` has printed since lap 9, `check_paper.py` took its measuring
branch: `pages 23, calibri_face Carlito, metrics_ok true`, new anchor
`84b91dde83d63607`, recorded from that run. It does **not** close WFG-116; that is the
same line in `.github/workflows/auto-gates.yml`, outside `paper/`, still open.

## Reviewed by: subagent (block): this routine's own ledger shipped three hand-typed facts about the repository that it did not re-measure, one of them falsified by the very diff it was reporting on — the same wrong-count class as the §4.5 defect the lap exists to fix

Every one was verified in the tree before being fixed, and **none was in the manuscript**:

1. 「WFG-127 … position 1 of the backlog table」 (`GAPS.md`, `STATE.json`). At `eff2183`
   WFG-127 is table row **12** and the **second** `todo` row; WFG-199 is the first,
   put there by critic #45 in that same commit. True at `9c24a8b`, falsified by the last
   commit of the range being incorporated, and never re-read.
2. 「the 933-file scan」, three new places. `scripts/check_withdrawn_claims.py` prints
   **935**. 933 is lap 18's figure carried forward. Re-derived by running the script.
3. 「The mechanism is the finding, and it is a **new** one」 / 「a third failure mode
   beside §3.5's two」. **Both false.** Critic #44 filed it at 2026-09-08T1700Z
   (`dc8fa9f`, inside the incorporated diff) as 「A FOURTH SURFACE」 in WFG-127's own row —
   the row this lap cites by name — with a sharper root: the guard
   `tests/test_fair_opponent_line.py` reads **English** and `README.md:232` is **Korean**,
   using the exact vocabulary `DEMO_SCRIPT_5MIN.md:151` uses for the negation. That is
   §3.5's own 「It matches spellings, not meaning, and one language at a time」, firing on
   a guard rather than the registry and on a newly written claim rather than a surviving
   one (WFG-168, not yet filed). **So the manuscript is owed nothing here** — it already
   states the limit this instance instantiates.

Two further reviewer points taken: the `pp_uiseong_*` clause now rests on fact (3) alone,
fact (2) being the change-of-kind statement §4.5 itself endorses; and the description of
the README diff is corrected. One defect the lap caught on itself: the date claim about
when the three surfaces were narrowed is now grounded on those documents' own dated
records, because this clone is **shallow at depth 50** (oldest resolvable commit
`ab4e71e`, 2026-09-08) and `git log -S` on both Korean narrowings returns that boundary
commit, which is the clone edge and not an answer. No ancestry claim is made.

Everything the reviewer checked in the **manuscript** verified true, independently: the
two-document count, the plural 「others were narrowed」, the registry clause, the absence
of the shape claim from `withdrawn_claims.json` (10 entries, zero occurrences), the
`README.md:232` provenance and commit id, the ERA5 absence and the reproduced 12-word
cost, no traded caveat or number, all 29 citations resolving both ways with `verified`
notes, and every `Fig. N` matching appearance order in the built `.docx`.

## Counts

`body_words` **8,993** (proxy 9,000, margin **7**) · `built_pages` **23** (author's rule
25, margin **2 pages**), measured this run under Carlito with `metrics_ok true` ·
figures 8 referenced (9 drawn) · tables 4 · references 29 · gaps 7.

## In plain terms

- **What the paper now says.** One sentence changed. Your front door started saying, two
  days ago, that the 1 km safety buffer is a sharp peak — and your own booth script and
  Q&A bank tell the student that this experiment cannot tell a peak from a plateau. The
  paper had been counting how many places in your repository still disagree with it, and
  the front door made that count wrong. It now says two, and names where.
- **What is still missing.** The paper does not tell a reader that its weather data
  (ERA5) is published about five days late, so its hazard map can never be today's map.
  Every screen and sheet you show a judge says this; the paper does not, because the
  sentence costs 12 words and the word budget had 7. That is the first time the budget has
  cost you a *limitation* rather than a nicety, and the document is 23 pages against your
  25-page rule, so those 12 words would not have cost a page.
- **What you should do.** Answer **NH-037** — it is the word budget, and it now has the
  case it was written for: raise the proxy toward your real 25-page rule, or say the
  routine may exceed it when a run has measured the pages. Two other replies are overdue
  and still block the student: **NH-032** and **NH-034**. And one line of work for a build
  lap, not for you: `README.md:232` and `docs/present_perimeter_arm.md:129` should stop
  asserting the peak, and the test that guards it should read Korean as well as English
  (WFG-127 (iv) and (v)).



## Gates

**ALL GREEN** · mode `quick` · head `eff2183` · 2026-09-08T21:20:49Z · current at `eff2183`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 25.2 s | === make verify: PASSED === |
| pytest-quick | PASS | 12.5 s | 40 passed in 11.15s |

## Commits since the previous report (9c24a8b99d3c7311915e7f5f4f6a5f3c1801504f..HEAD)

- eff2183 critic #45: the file that would measure what the model actually buys has been committed all along

## Needs a human (20 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09)
- NH-036 [DECISION] One critic lap told the next one not to edit a file, and that is what kept a false sentence in front of a judge for a window (by 2026-09-10)
- NH-037 [DECISION] The paper's word proxy now stops it a thousand words before your 25-page rule (by 2026-09-10)
- NH-038 [DECISION] Your "product first" rule has spent the last three dev laps on documents, and the readiness line it was written to protect has not moved in five critic laps (by 2026-09-09)
- NH-039 [DECISION] The national wildfire-spread system's manual is an 18 MB PDF the sandbox could not fetch, and one of you can (by 2026-09-12)
- NH-040 [FYI] A critic lap pushed one commit past a red `--assert-reported`, and it is telling you rather than hiding it
- NH-041 [FYI] This lap sent you an email containing only the word PLACEHOLDER, and could not take it back
- NH-042 [DECISION] Two of your own rules collide whenever a withdrawn claim lives in a frozen artifact, and this week they collided three times (by 2026-09-10)
- NH-043 [DECISION] A gate your loop built this morning will go red about twice a day, and the charter tells the lap that meets it to stop working (by 2026-09-09)
- NH-045 [BLOCKER] The staleness gate has closed `auto/dev` to every routine, and the one routine that met it is the one forbidden to clear it (by 2026-09-08)
- NH-044 [DECISION] The claim the paper just retracted is still live on the page the paper cites for it (by 2026-09-09)
- NH-046 [DECISION] Your product's definition-of-done names a command nothing in this project has ever run (by 2026-09-10)
- NH-048 [DECISION] One of the research routine's three literature channels has been dead for two runs, and a working replacement is already proven (by 2026-09-10)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
