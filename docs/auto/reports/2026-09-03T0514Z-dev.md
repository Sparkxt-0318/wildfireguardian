# WildfireGuardian autoloop · dev · 2026-09-03T0514Z

| | |
|---|---|
| when | 2026-09-03 05:14 UTC · 2026-09-03 14:14 KST |
| branch / head | `auto/dev` / `017c9ec` |
| kind | dev |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | todo: 14 |

## What happened this lap

**Row: WFG-001 — Clean-Linux green.** First cloud dev lap. The sandbox half is
green; the CI half is now in flight on this push.

## What I did, and why

Bootstrapped the sandbox (`pins_ok=true`, py 3.11.15) and ran the quick gate as
the baseline: green, so the lap was cleared to build. The full gate then came
back **RED with 17 items** — which is the finding, because WFG-001 exists
precisely to discover whether "green on the author's laptop" survives a stranger's
machine. It did not.

The 17 resolved to **two causes**, neither a defect in the project's own code and
neither touching a committed artifact:

1. **`brotli` was declared nowhere.** `fonttools==4.63.0` is pinned but its WOFF2
   decoder needs the Brotli extension to open the vendored `.woff2` faces. The
   author's own `e1588b4` landed mid-lap and fixed this; it cleared all five
   `test_screen_checks` failures on its own (17 red → 12).
2. **Git-ignored inputs absent from a fresh clone, with guards that did not fire.**
   All 12 survivors, and my work this lap. `data/raw/**` is ignored at
   `.gitignore:60`, so `firms_data/` never reaches a clone.

The sharpest of those: `test_osm_cache_isolation` guarded with
`if not d.exists(): skip`, but `data/cache/osm/yeongdeok_2025/` is **tracked** —
it holds `vegetation.geojson` — while the four graphs inside it are ignored. The
directory therefore always exists in a fresh clone, the skip never fired, and the
asserts failed. A directory is not a proxy for its contents.

Twelve tests now skip with a stated reason instead of failing or erroring, per
this row's own instruction ("mark it `skipif` with the reason, never delete it").

## Two places I deliberately did NOT take the blunt fix

- `test_the_migrated_yeongdeok_cache_is_where_the_loader_looks` keeps its
  old-flat-path assertion running in every clone; only the graph-presence half
  skips. That assertion needs no ignored input and is the half that would catch a
  regression of the cache migration itself.
- `test_a_moved_artifact_is_actually_detected` runs its tampering assertions
  everywhere and skips only the final two. A blanket skip would mean "a guard
  nobody has seen fail" is never exercised in CI — the exact thing that test
  exists to prevent. Its third assertion also had to move behind the guard: in a
  clean clone it would have passed on the `MISSING` line rather than on the tamper
  it means to detect, which is a green nobody should trust.

## The root objection I recorded (`hate`), and how it came out

*Objection:* a green run in this sandbox proves nothing durable, because
`.auto/gates.json` is git-ignored and dies with the container — so the next lap
starts blind and "clean-Linux green" has no artifact anyone can open.

*Cheapest test:* check the ignore rules, and query the real Actions history rather
than assuming the workflow had ever run.

*Outcome — half withdrawn, half confirmed.* `.auto/` is indeed ignored
(`.gitignore:390`), but `scripts/auto/report.py` already transcribes the whole
gate table into the committed report and records `last_gates_passed` in
`STATE.json`. The durable record exists by design, so I built no machinery for it.
The other half held and became a real finding: **`auto-gates` has never once
concluded green.** Runs 1, 2, 3 and 5 were cancelled by the concurrency rule as
pushes stacked up; run 4 concluded `failure`. The "green Actions run" half of this
row's "done when" had never been true.

## What did not work / what I am not claiming

- **The CI half is unverified.** This push triggers the run that would settle it.
  Until it concludes green, WFG-001 stays `in-progress`, not `done`.
- **The sandbox collects 1116 tests; the laptop baseline is 1120** (1116 passed /
  3 skipped / 1 xpassed). Four tests are not collected here and I did not diagnose
  why. `xgboost` is absent in the sandbox but reports as a *skip*, not as an
  uncollected test, so it does not obviously account for the gap. Recorded rather
  than rounded away.
- **`baseline-verify` still exits 2 here**, recorded as WARN by the author's
  `e1588b4` because the two laptop-only manifests cannot exist in a clone. I did
  not touch that decision, and I did not re-freeze — re-freezing would rewrite a
  committed artifact whose entire purpose is to detect exactly that kind of loss.
- **I wrote no `docs/<topic>.md` and registered no numbers.** This row is scoped
  "no artifact changes; env fixes only" and produced no measurement about the
  world; the gate counts live in the report and the backlog row. Writing a topic
  doc for it would have been ceremony.

## Concurrency note

The author's own session pushed four commits during this lap
(`80cd611 → 1e60c1b → e1588b4 → 017c9ec`), two of them into the same problem I was
diagnosing. I rebased onto each rather than racing, and confined my changes to the
four test files their commits did not touch. If they are independently writing
these same guards, drop mine.

## Next row

**WFG-002 — Judge Q&A bank v2** (P0, KCF), unless the CI run this push triggers
comes back red, in which case the next lap finishes WFG-001 first.

## Gates

**ALL GREEN** · mode `full` · head `017c9ec` · 2026-09-03T05:13:08Z

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 13.3 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.1 s | make: *** [Makefile:140: baseline-verify] Error 1 |
| snapshot-verify | PASS | 0.5 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 149.5 s | 1062 passed, 54 skipped, 52 warnings in 148.31s (0:02:28) |

## Commits since the previous report (-15)

- 017c9ec charter: a routine session ends with its turn, so gates run in the foreground
- e1588b4 gates: baseline-verify is hard only where the laptop-only manifests exist
- 80cd611 auto-gates: run once per push, not once per push plus once per PR
- 1e60c1b report-email: a push with no report file is not a failure
- 522f7a7 autoloop: the repository can now develop itself while the author is away
- 25f1e14 session 22: one refuge saves 20 of 24; two save all — and Session 16 measured distance backwards
- d1d5854 session 20: the 240-minute horizon is grounded, and the default did not move
- 11ed5cd session 20: STOP-GATE — the CSV never reached this environment
- 97ba901 phase 3-4: the 영덕 explanation was untraceable and backwards; corrected
- 093bc86 phase 0-2: GK2A sees three of four fires, and always after the person did
- 4ab8fbd session 19 phase 0: the Session 18 gate runs were vacuous for their own new files
- a9ca186 phase 6: SESSION18_REPORT.md — the number set is frozen as of this commit
- 50d3fb1 phase 5: the margin clause is inert, and the cause is construction not a quiet fire
- da2c568 phase 4: recall at the operating threshold is 0.138, and three folds catch nothing
- 424e709 phase 3 fix: I committed three times on a red check_forbidden, and here is why

## Needs a human (5 open)

- NH-001 [DECISION] Email delivery needs three repository secrets
- NH-002 [DECISION] Optional: `@claude` on GitHub issues and PRs
- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
