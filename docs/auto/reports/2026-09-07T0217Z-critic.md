# WildfireGuardian autoloop · critic · 2026-09-07T0217Z

| | |
|---|---|
| when | 2026-09-07 02:17 UTC · 2026-09-07 11:17 KST |
| branch / head | `auto/dev` / `3f881f6` (the head this lap reviewed and gated; the report ships in **`900c407`**, as every critic lap's does — both ids resolve. `gates.py --mode full` was re-run at `900c407`, ALL GREEN, exit 0, **warm** `1638 passed, 56 skipped, 2 xfailed`, 351.9 s, and `--assert-head` and `--assert-reported` both exited 0 chained with `&&` before the push, which is NH-040's remedy) |
| kind | critic |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 7, done: 21, parked: 1, todo: 84 |

⚠ **Read that backlog line as WFG-149 says to read it.** `backlog_counts()` in
`scripts/auto/report.py` matches a row only when its description cell holds no pipe, so it sees
about 110 of the table's rows and not all of them; the counts above and `03_backlog.png` are drawn
from that subset. **WFG-151 and WFG-152, this lap's two new rows, both quote file paths and one
quotes a hash pair, so neither is guaranteed to be inside the 84.** The table in
`docs/auto/BACKLOG.md` is the record. WFG-149 is filed, P1, and held behind the readiness lines by
CHARTER §14b.

## What happened this lap

Critic lap #31. Reviewed head `3f881f6`, window `de7bd0a..3f881f6` (24 h to 2026-09-07T02:00Z).
Changed no code and no artifact; wrote only under `docs/auto/`.

## Your decisions, applied

**Nothing new on either channel, and this is the ninth consecutive lap that says so.** The Gmail
search `from:siyeong0318@gmail.com subject:"WildfireGuardian autoloop" newer_than:14d` returns
**80 threads**, every one a single message this loop sent, labelled `SENT`. PR #31 has **no**
comments. `docs/auto/decisions_seen.json` is unchanged and `scripts/auto/decisions.py` was not
called, because there was nothing to call it on. **Thirteen NEEDS_HUMAN entries are open, nine of
them DECISION entries waiting on you**, and three are now past or at their date: **NH-032** and
**NH-034** (both by 2026-09-08), **NH-038** (by 2026-09-09). I added an eighth data point to
NH-038 rather than opening a tenth entry.

## Findings, ranked

### F1 (the one `fix-before-next-row` item) · WFG-151 · the release bundle does not contain the booth kit

The 01:09Z lap rebuilt the booth printables — 33 A4 pages, fresh, gated. `release/kcf-finals-2026/`,
the folder a judge would be handed on a USB, still does not contain them, and no gate says so.

Measured at this head, not read:

- `release/kcf-finals-2026/MANIFEST.json` lists **17 files**; not one is a printable.
- `scripts/build_finals_bundle.py:57` `PAYLOAD` names the four screens, `web/assets`,
  `web/demo-media`, `CITATION.cff`, `LICENSE`, `check_bundle_copy.py`. No PDF.
- `docs/auto/KCF_READINESS.md` **R9** asks for 「`web/` whole, **printables**, `README_KO.md` …」,
  and R9's own cell gives 「the printables the line names (R7 / WFG-007 — none exist)」 as one of
  the two reasons it cannot tick. They exist now.
- Timing: the first kit was built at `3e92b69` (2026-09-06T06:51Z); the bundle manifest was rebuilt
  **2 h 43 m later** at `1ec1d06` (09:34Z) and gained only a new `web/finals.html` hash.

**Why nothing went red, which matters more than the omission.**
`tests/test_finals_bundle.py:41` compares the committed manifest to `bfb.plan()` — the builder's
own plan — so manifest and builder can agree forever while both omit the same file. And `:74`,
whose docstring says 「R9 names `web/` whole」, is the **only** place R9's list reaches code: it
asserts four screens, fonts, `CITATION.cff`, `LICENSE`, `README_KO.md`, and never the printables.

### F2 (the one row move) · WFG-026 P1 → P0 · R7's only blocker was filed below the work it blocks

R7 names five printables. Three are in the `20260907T0059Z` kit, the 29 dispatch sheets are excused
in writing, and the fifth — the related-work and SFTD059T differentiation panel — **is not
written**, which the kit's own `what_this_does_not_show` says in those words. That row, WFG-026,
sat at **P1**, below the five P1 infra rows CHARTER §14b explicitly holds *behind R7*. A rule that
gates infra work on a readiness line whose only blocker is filed at the same level as the gated
work can never release. Reason written to `docs/auto/DIRECTION.md`; no P0 row moved below a
non-P0 row.

### F3 · WFG-152 · the new freshness gate has made three judge-facing files un-editable by this routine, and nothing says so

`docs/auto/JUDGE_QA.md`, `docs/auto/DEMO_SCRIPT_5MIN.md` and `docs/auto/finals/BOOTH_SETUP.md` are
`SOURCES` of the printables manifest. **Probed rather than reasoned:** appending one HTML comment
line to `JUDGE_QA.md` at `3f881f6` turns
`tests/test_printables.py::test_the_newest_printable_is_not_stale_against_the_tree` red, naming the
file (`manifest ec75a1657d… tree 5c4612b1d2…`); reverted, tree clean, 21 of 22 printables tests
pass either way. All three live under `docs/auto/`, which is exactly and only what the critic and
research routines may write. So the next critic or research lap that adds a Q&A card — **WFG-144
is one, and the loop has already decided it wants it** — will push a red tree or park on
`auto/red/`, and will find out after doing the work. The gate is right; the missing thing is one
sentence where a lap reads it. Recorded in DIRECTION's 「what not to do」 immediately, and filed P1
under §14b for the rest.

### F4 · the judge drill's one standing gap is unchanged

I re-ran the drill against `docs/auto/JUDGE_QA.md`. The bank answers Q39 (what paper do you hand
over) and Q40 (do the tests run offline) honestly and from files. **There is still no card for
「산림청·경기도가 이미 산불확산예측을 하고 있는데 무엇이 다릅니까?」** — the only `산림청` hits in the
bank are about burned-area figures. That is **WFG-144**, already filed by the research lap; its
sequencing reason (「wait for the printables rebuild」) is now spent, and I recorded that in the row
rather than opening a duplicate. WFG-026 absorbs its substance; WFG-144 keeps the spoken card.

### Not findings, checked and clean

- **Gates.** `gates.py --mode full` **ALL GREEN** at `3f881f6`, exit 0 (`1632 passed, 62 skipped,
  2 xfailed`, **cold**, 349.5 s). `--assert-head` exits 0. `--assert-reported --base de7bd0a`
  exits 0 over the whole window: 52 substantive paths, all carried by reports.
- **GitHub's own runs (CHARTER §4b).** Through the MCP: `auto-gates` runs **171 to 190** on
  `auto/dev` are **18 `success`, 2 `cancelled`, zero `failure`**; run **190** at this head is
  `success`. No red run behind a green report, so no finding #1 of that kind this lap.
- **Report certification.** Every **dev** report in the window carries `Reviewed by:`; the research
  report still does not (WFG-147, unchanged and untaken).
- **The booth kit itself.** Re-hashed here in one process: all five `SOURCES` match the tree, the
  PDF's own sha256 matches, `pages_per_source` sums 5+6+17+3+2 = **33** = `pages`, and the PDF
  carries **33** `/Type /Page` objects. The four-drift series is over and the reviewer's
  off-by-one is genuinely fixed.
- **Sourcing (`factchk` on the window's new world claims).** The only new external prose is the
  manuscript's Related-work paragraph on the two Korean operational systems and its two
  `references.bib` entries. Both carry `verified 2026-09-06` notes with agency, series or
  publication date, scope, and an explicit statement that no accuracy or validation figure exists
  on either page; the manuscript compares accuracy in neither direction. That is CHARTER §3 rule 5b
  satisfied in the form the rule asks for. WFG-146's one-day date error is in the knowledge note
  only and the manuscript does not cite that article.
- Clone unshallowed before any measurement (`is-shallow-repository` = `false`, 517 commits).

## The root objection (`hate`)

**Every gate this loop writes compares the artifact to its own description, and the loop keeps
discovering that one directory at a time instead of once.**

Three instances, three consecutive days, one shape:

1. `tests/test_printables.py` read the manifest against itself, through four drifts, until WFG-140
   hashed the sources against the tree. Fixed yesterday.
2. The reviewer's `sum(pages_per_source) == pages` found four surfaces carrying wrong page numbers
   under the true sentence 「re-derived from the manifest rather than retyped」. Fixed yesterday.
3. `tests/test_finals_bundle.py:41` compares the committed manifest to `bfb.plan()`. **Today**, and
   it is why a bundle omitting a file R9 names has been green through two rebuilds since that file
   existed.

Each fix was correct and each stopped at the directory it was found in. **The cheapest test is one
grep**: every test that compares a committed manifest to a builder's plan, or to itself, rather
than to the tree. Its first hit is WFG-151.

## Direction check (CHARTER §14, five minutes)

`docs/auto/DIRECTION.md` updated. **One row moved: WFG-026 P1 → P0** (F2), reason on the page, no
P0 below a non-P0. The page's rows 1 and 2 were both spent by the 01:09Z lap, so the new top three
are **WFG-151**, **WFG-026**, **WFG-139**. A new 「what not to do」 line carries F3 so the next
critic or research lap reads it before, not after.

**KCF_READINESS: 4 of 11 (R2, R4, R5, R6), zero ticked for an EIGHTH consecutive critic lap, and
the rule fires — with a changed diagnosis.** This window's dev lap was product work, not a
document correction, and it still ticked nothing, because **R7 and R9 are each one small unclaimed
piece short**: R7 needs WFG-026, R9 needs WFG-151. Neither had ever been anyone's item. Both are
now P0. Eighth data point recorded in NH-038, because the ordering rule that produced it is yours.

## Scorecard deltas

| | 연구/개발 목적 | 설계와 방법론 | 데이터 / 구현 | 창의성 | 제출 자료 | total |
|---|---:|---:|---:|---:|---:|---:|
| Track B `524f13c` → `3f881f6` | 17 → 17 | 16 → 16 | 18 → 18 | 15 → 15 | 17 → **18** | 83 → **84** |
| Track A `524f13c` → `3f881f6` | 16 → 16 | 16 → 16 | 18 → 18 | 15 → 15 | 13 → **14** | 78 → **79** |

**One row moves on each track, 제출 자료, on the booth kit verified here rather than read.** It
stops one short on both (19 and 15) for the same reason: the bundle a judge carries away does not
contain that kit. **데이터 does not move for WFG-148's ORACLE caveat** — a caveat added is honesty
about interpretation, not a measurement made, and what the 42 buys against a present-perimeter
opponent on 영덕 is still unmeasured and still waiting on NH-032/NH-034. Track A's 구현 및 유용성
holds on two movements that cancel, and this report says so rather than calling the window flat.

## In plain terms

**What the judges would see today.** A strong, unusually honest project with a genuine paper kit:
33 printed A4 pages, and a new check that goes red the moment the paper and the files disagree —
that check was proved by watching it fail on yesterday's stale kit before the rebuild fixed it.
The front page now states both of the limits its own paper states, which is the sentence a fire
scientist asks for. The gap a judge could still find in one minute: the USB folder you would hand
them has the four screens and no paper in it.

**What changed since yesterday.** The last lap closed four items and did real product work, and
the readiness checklist still reads 4 of 11 — for the eighth day. I found out why, and it is
smaller than it looked: two of those eleven lines are each **one small thing** short, and neither
of those things had ever been on anyone's list. Both are now at the top of the list.

**What you should do.** Nothing is broken and nothing is blocking. When you have ten minutes,
three decisions are now at or past their date and the loop has been guessing around them for days:
**NH-032** and **NH-034** (both due 09-08, about the fair-opponent margin, and they are the reason
the headline number cannot yet say what your own model buys) and **NH-038** (due 09-09, about the
「product first」 rule — my note there today argues your rule is working better than the last two
laps suggested, and asks one narrow question about it). **NH-037** is still worth ten minutes too.
⚠ If you print anything this week, print `WFG_printables_20260907T0059Z.pdf` and not the two files
beside it.



## Gates

**ALL GREEN** · mode `full` · head `3f881f6` · 2026-09-07T02:05:42Z · current at `3f881f6`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 22.6 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.2 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 2.2 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 349.5 s | 1632 passed, 62 skipped, 2 xfailed, 52 warnings in 347.84s (0:05:47) |

## Commits since the previous report (d0a739e7d7b04ad0586ec67dc77bea90a7ae0ea9..HEAD)

- 3f881f6 report 2026-09-07T0109Z-dev, and the two lessons the reviewer's block bought

## Needs a human (13 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09)
- NH-036 [DECISION] One critic lap told the next one not to edit a file, and that is what kept a false sentence in front of a judge for a window (by 2026-09-10)
- NH-037 [DECISION] The paper's word proxy now stops it a thousand words before your 25-page rule (by 2026-09-10)
- NH-038 [DECISION] Your "product first" rule has spent the last three dev laps on documents, and the readiness line it was written to protect has not moved in five critic laps (by 2026-09-09)
- NH-039 [DECISION] The national wildfire-spread system's manual is an 18 MB PDF the sandbox could not fetch, and one of you can (by 2026-09-12)
- NH-040 [FYI] A critic lap pushed one commit past a red `--assert-reported`, and it is telling you rather than hiding it

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
