# WildfireGuardian autoloop · dev · 2026-09-04T0855Z

| | |
|---|---|
| when | 2026-09-04 08:55 UTC · 2026-09-04 17:55 KST |
| branch / head | `auto/dev` / `ec2e813` |
| kind | dev |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 7, done: 12, parked: 1, todo: 40 |

## What happened this lap

## Your decisions, applied

**Nothing new — the fifth lap in a row with an empty mailbox.** Searched Gmail for
`from:siyeong0318@gmail.com subject:"WildfireGuardian autoloop" newer_than:14d`: 26 threads,
every one holding exactly one message, the loop's own report. PR #31 has zero comments.
`docs/auto/decisions_seen.json` still does not exist because `decisions.py apply` has never
had anything to record. Fifteen NEEDS_HUMAN entries are open and **NH-016's recommended
cadence change was dated for today.** NH-020 (filed by critic #8) asks you the one question
the loop cannot answer for itself: are the reports arriving and you have not needed to
answer, or are they not arriving at all? Those need opposite fixes.

---

## Row: WFG-063 (P0, KCF) — the T0 answer that told the student to say what the bank forbids

**Status: done. `docs/auto/JUDGE_QA.md` Q10 · Q10d, `docs/detection_floor.md` §10,
`docs/auto/finals/DETECTION_FLOOR_CARD.md`, `docs/SESSION19_REPORT.md` Phase 3, and a new
gate over five surfaces including the built screen.**

### Why this row and not the one above it in the table

Table order puts **WFG-003** (finals screen audit + 5-minute demo script) first, and it is
`todo` and agent-doable. It was not taken, on purpose, and this is the lap's one deliberate
deviation from the mechanical rule:

* critic #8 names WFG-063 and WFG-067 as the next two rows and says the next dev lap should
  take them in that order (its claim that they are the top two `todo` rows is factually
  wrong — WFG-003 sits above both — and the report says so rather than repeating it);
* the 0725Z dev lap's own report says `JUDGE_QA.md:240` **「was deliberately not touched …
  It is the next row」**;
* and the substantive reason, which is the one that decided it: WFG-003's deliverable is
  `docs/auto/DEMO_SCRIPT_5MIN.md`, a **fifth judge-facing document that recites T0
  answers**. Writing it while the bank held two opposite T0 answers to one question would
  have copied the contradiction into a new file — which is exactly how the screen came to
  disagree with the card (critic #8's N49). WFG-003 is unchanged, still the next row, and
  now has a coherent bank to recite from.

### What was wrong

WFG-053 withdrew the ordering claim (「위성은 사람보다 느렸습니다」). Withdrawing it removed
the ground under a *different* sentence riding on it — 「사람 신고가 일차」 — so §10 reached
for 「신고의 99 %가 목격 신고」 instead. That statistic is an unregistered year-to-date interim
(경향신문, 2023-04-28); a reviewer struck it from the booth card and `detection_floor.md` §10
forbade it as support. What was left is the size floor alone, and **the size floor rules the
satellite out; it does not rule the human in.**

The result, for one window: `JUDGE_QA.md` Q10 (tier **T0**, one of the fourteen the header
tells the student to memorise word for word) instructed 「신고 우선」 on that ground, while
Q10d (tier **T0**, added by critic #7 as the guard) listed the same sentence under 「말하면
안 되는 것」. Two T0 answers to one question, each forbidding the other, in a document whose
own header says memorise all of them. And `web/finals.html` — behind the student at the
booth — already said the narrow thing, so the student reciting Q10 would have contradicted
their own screen in front of the judge.

### What was done

**One sentence, copied to every surface.** The finals screen's wording is the model (critic
#8: "the screen is now the most correct document in this repository"):

> 이 측정이 말하는 것은 위성을 일차로 둘 수 없다는 것까지이며, 어떤 소스가 일차여야
> 하는지는 재지 않았습니다.

* `docs/auto/JUDGE_QA.md` Q10 (T0) — the 「신고 우선 … 근거는 … 99 %」 clause is gone,
  replaced by the sentence above; a new 없는 것 line forbids 「사람 신고를 일차로」 the same
  way the existing one forbids the ordering. Q10d's 🛑 header rewritten: the question's
  premise is now false (「저희는 사람 신고를 일차로 두지 않았습니다」) rather than
  unanswerable, and it records what changed and what is still open (「그럼 무엇이 일차여야
  하는가」 — unmeasured, NH-019).
* `docs/detection_floor.md` §10 — the **우선순위표 is gone.** It was a rank table with 사람
  신고 at 1, which is the claim in table form. Replaced by 「소스 | 이 측정이 말하는 것」,
  where the 사람 신고 row reads **「재지 않았습니다」**, and a ⚠ paragraph above it says what
  the table used to be and why it is not that any more. 설계 함의 narrowed to the same
  sentence, keeping 「위성을 일차로 두는 설계는 이 측정과 배치됩니다」, which is still true.
* `docs/auto/finals/DETECTION_FLOOR_CARD.md` — front sentence and the same rank table, both
  narrowed; a new 「이 카드가 보여주지 않는 것」 bullet records the withdrawal beside the
  ordering one, **in words, without reprinting the figure.**
* `docs/SESSION19_REPORT.md` Phase 3 — annotated, not edited. The WFG-053 lap annotated the
  ordering claim in Phase 1 and left the primacy claim in Phase 3 unmarked; it now carries a
  dated withdrawal block. It is a record, so the table and the sentence stay.

**The gate — and this is the half the reviewer sent back.** `tests/test_detection_ordering_is_not_claimed.py`
gains two things, and the first one alone was not good enough to ship:

1. `BANNED_PRIMACY` — a spelling family in the same per-token `<!-- forbidden-ok: -->`
   discipline, over six surfaces including `scripts/finals.template.html` and the built
   `web/finals.html`; plus a positive anchor requiring five of them to carry the permitted
   clause verbatim, whitespace-normalised. **The screen being right while the markdown is
   wrong is now a test failure**, which is critic #8's root objection in one assertion.
2. `priority_violations` — the rule that reads no spellings at all. Any **sentence** naming
   both a priority word (`일차 · 1차 · 우선 · 먼저 · 앞서 · 최초 · 순위 …`) and a
   trigger-source noun (`신고 · 위성 · GK2A · FIRMS · 감시카메라 · 무전`) must also carry a
   **negation morpheme**. It holds whatever words the next author reaches for, and it runs
   over **seven** surfaces including `docs/SESSION19_REPORT.md` and `docs/finals_screen_v2.md`.

This repository already owned rule 2, one file over and scoped to one file:
`tests/test_finals_screen.py::test_every_trigger_priority_sentence_on_the_screen_is_a_negation`,
whose own docstring says 「WFG-062 is the row that generalises this beyond one file」. This
lap should have pointed it at the markdown surfaces in the first place and instead wrote a
second spelling list. It is a separate implementation rather than an import because the
screen template holds each sentence on one line while these documents hard-wrap at ninety
columns: measured, a line-level scan over the five files reports 24 offenders, all 24 of
them the wrap. So the unit here is a sentence rebuilt from its block, and a table row is
one unbroken unit — the reviewer's `| 1순위 | **사람 신고** | … |` escape puts the priority
word and the noun in different cells, and `| 감시카메라 | 최초 발견 0건. … 트리거 소스가
아님 |` puts the negation in a different sentence of the same cell.

### The root objection I recorded before building (`hate`)

> *A tripwire over the spellings I just removed is a gate against my own past, and its
> author picks the mutations. The last one of these was escaped twice, in one review, by two
> two-word edits.*

**Cheapest test: write the mutation list first, run it before writing the patterns, and put
in shapes that are not paraphrases of the ones I deleted.** Nine mutations; the first draft
of the patterns **caught six and missed three**, and the three misses are the finding:

1. `사람 신고가 일차**이고` — emphasis markers between noun and verb. Same class that broke
   the ordering gate twice in one review; reached for a third time by the same author.
2. the **rank-1 table row** — the rank cell precedes 사람 신고 and the 근거 cell puts sixty
   characters between them, so no proximity rule over 신고 → 일차 can see it. A table row
   needs its own rule; this is the shape both §10 and the card actually shipped.
3. 「트리거의 일차 소스는 사람 신고입니다」 — the claim right-to-left. **Proximity in one
   direction is half a rule.**

All three are now rules and all nine mutations fail closed. Recorded in the file at the
`BANNED_PRIMACY` docstring, and in MEMO as the general lesson.

### What the gates found that I had not

Three failures I did not predict, each of which improved the change:

* `test_the_card_carries_no_number_this_test_has_not_been_shown` rejected my card bullet
  because it **reprinted the 99 % figure** while describing its withdrawal. The allowlist's
  own comment says 99 was deliberately never added. The bullet now names the statistic in
  words. A withdrawal note is not a licence to put an unregistered number back on an A4
  sheet a judge holds. `"063"` was added to the allowlist as a row id, beside `"053"`.
* the new primacy gate fired on **my own withdrawal prose** in three places, which is the
  gate working: each got an explicit pragma token rather than a loosened pattern.
* an HTML comment on its own line inside a paragraph or between a table separator and its
  first row splits the block when the document is rendered — and these are documents a
  student reads on paper. Four pragmas moved inline to end-of-line, where the comment is
  invisible in both raw and rendered form.

### What did not work / what I left alone

* **Line-number citations rot.** Adding two pragma lines to `detection_floor.md` moved the
  99 % ban from `:310` to `:311` and invalidated four live cross-references. The two in
  judge-facing prose and the two in the test now cite **§10** instead. The ones in
  `BACKLOG.md`, `NEEDS_HUMAN.md` and past reports are records and were left.
* **`docs/SESSION20_REPORT.md` and the other session reports** were not swept for the same
  claim; only Session 19 carries it.
* **WFG-062** (a general registry of withdrawn claims) is now clearly the right
  generalisation and this lap is the evidence for it: critic #8 said the cheapest test was
  whether one sentence in three files plus one gate closes this, and the honest answer is
  **it took five files and a second hand-rolled gate family**, the fifth file being a
  session report nobody had listed. That is a stronger case for WFG-062 than the one critic
  #8 wrote, and it is written into the row.
* No number moved. No artifact was regenerated. No registry key was added or edited.

### Evidence

* `gates.py --mode full` — **all hard gates PASS** (`verify`, `snapshot-verify`,
  `env-check`, `pytest-full`); `baseline-verify` **WARN**, expected off-laptop, `hard: false`.
  The two words are not the same and this report will not print one and record the other.
* The first full run, before the reviewer's block: `1293 passed, 62 skipped`, **COLD**
  (first full run in this sandbox; the six SRTM-gated tests skip — WFG-039). Against critic
  #8's cold reading at `12bf2d9` (`1273 / 62`) that is **+20 passed, skips unchanged**, like
  for like. After the block was fixed: `1318 passed, 56 skipped`, **WARM** — +25 more tests
  and the six SRTM tests now running because an earlier run in this sandbox downloaded the
  tile (WFG-039 again, third lap in a row, and the reason both numbers are quoted with their
  temperature).
* `tests/test_detection_ordering_is_not_claimed.py` — **21 → 60 tests**: 21 for the ordering
  claim, 20 for the primacy spellings, 19 for the structural rule.
* The binding run is the one recorded in `.auto/gates.json` at the pushed commit, with
  `--assert-head` and `--assert-reported` both exiting 0 immediately before the push.

### Reviewed by

**Reviewed by: subagent (block, then fixed): *the lap's headline deliverable is a string
tripwire, and its strength is asserted without the caveat the test file itself carries, in
a T0 answer the student recites and in the NEEDS_HUMAN entry the author answers.***

The reviewer wrote **twenty** primacy sentences it had not seen and ran them through
`primacy_violations`: **nineteen escaped.** Among them 「사람 신고가 일차 채널입니다」 (one
token off a sentence this lap deleted — 채널 was not in the suffix alternation), 「사람
신고를 **1차** 트리거로 둡니다」 (일차/1차 interchange, only the hangul form patterned), and
the same rank table rewritten with a 「1순위」 header cell. It then ran the same twenty
through the screen's existing negation rule, which caught **eight**. Its `mandela` reading
is the part worth keeping: `test_each_withdrawn_primacy_spelling_is_caught` is parametrised
over nine sentences **the pattern's author wrote in the same session as the patterns** —
leakage pattern #4, a scorer grading buckets it drew itself. Writing the mutations before
the patterns does not fix that; only a mutation set the author did not write is external
ground truth, and the first such set broke it 19 in 20.

**What the block cost, and what was done about it — the fix is not a bigger regex list.**

* The two overclaiming sentences were rewritten. `JUDGE_QA.md` Q10d — a **T0** answer —
  said 「다섯 표면이 어긋나면 …가 실패합니다」; it now says what the check actually is, that
  it is a string-and-structure check rather than a reader of meaning, and — the line that
  matters — **「부스에서 이 검사를 근거로 들지 마십시오」**. `NH-019`'s note said 「the booth
  is safe without you」; it now says option B is in force and says why, without leaning on
  the gate.
* The structural rule above was added, and the reviewer's seven escapes are now mutation
  cases written by someone who had not seen the rule. `1차`, `１차` and `순위` are in the
  word list because its escapes put them there.
* `test_the_two_word_lists_have_not_drifted` reads the word lists out of
  `tests/test_finals_screen.py` and fails if that file grows a word this one lacks, so the
  markdown surfaces cannot end up guarded more weakly than the screen.
* Reviewer finding (d): `detection_floor.md` §10's new 설계 함의 asserted 「실무상 119·마을
  무전이 그 자리에 오겠습니다만」 — an unsourced claim about Korean operational reality in
  the one section whose whole point is that nothing about the human channel was measured.
  **Cut, and cut from its twin in Q10d's ⭕ line too**, where it had been since critic #7.
* Reviewer finding (e): a pragma written as a fourth cell on a three-column table row. The
  row was reworded so no pragma is needed; the three in `finals_screen_v2.md`'s mutation
  table sit **inside** the last cell, adding no column.
* Reviewer finding (c): `docs/finals_screen_v2.md` and `docs/SESSION19_REPORT.md` were the
  sixth and seventh surfaces, unlisted on the day the five-surface claim shipped. Both are
  now in the guard lists.

**What the reviewer verified and found honest, recorded so it is not re-litigated:** the
`+20` census arithmetic and its cold/warm caveat; CHARTER §3 rules 1, 2, 3, 4, 7, 11 and 12
unbroken (no artifact regenerated, no registry key added or edited, no number moved, nothing
deleted, frame not pivoted); no surviving primacy assertion in any judge-facing document;
and the WFG-063-over-WFG-003 deviation as defensible and disclosed. **It also caught one
thing this report got wrong and it is fixed above:** the argument justifies deferring
WFG-003, and does **not** justify putting WFG-067 ahead of it as well — nothing in WFG-067
depends on the Q&A bank.

### Next row

**WFG-003** (P0) — the finals screen audit and the 5-minute demo script. It is the first
`todo` row in table order, it was the row this lap deferred, and the reason for deferring it
is now spent: the Q&A bank it recites from no longer contradicts itself. The reviewer was
right that nothing justified putting **WFG-067** ahead of it as well, so WFG-067 (the finals
screen's SYSTEM INTEGRITY panel prints `commit a562045`, which `git cat-file -t` cannot
resolve in a fresh clone — critic #8's F41, two lines plus a `git cat-file -e` gate) is the
one after it, unless the next critic lap raises it.

---

## In plain terms

**What changed for the project.** Your Q&A cards told you to say one thing about the fire
detection question and, sixty lines later, told you never to say it — both marked 「memorise
this word for word」. The version you must not say is gone from all five places it appeared,
including the screen you will stand next to, and a test now fails if any one of them drifts
away from the others again.

**Why it matters to the judges or the science.** A judge who asks 「so why do you put the
telephone first?」 was, until this lap, going to hear you say a sentence your own booth card
forbids and your own screen contradicts. The honest answer — 「our measurement rules the
satellite out; it does not tell us what should be first, and we did not measure that」 — is
now the only one in the building, and it is the kind of answer that wins the 데이터 해석 row
rather than losing it.

**What you should do.** Reply to this email with one line per item for the decisions listed
below — **NH-020 first**, because the loop has now sent you twenty-six reports and received
nothing back, and it cannot tell whether that means "fine, carry on" or "these are not
reaching me". NH-016's cadence change was dated today. One line each is enough.



## Gates

**ALL GREEN** · mode `full` · head `ec2e813` · 2026-09-04T08:45:02Z · current at `ec2e813`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 18.6 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.2 s | make: *** [Makefile:152: baseline-verify] Error 1 |
| snapshot-verify | PASS | 0.5 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 206.7 s | 1299 passed, 56 skipped, 52 warnings in 205.04s (0:03:25) |

## Commits since the previous report (12bf2d940c45e7bbb32eca03dbe6684c94eab431..HEAD)

- ec2e813 claim WFG-063: the T0 answer that tells the student to say what the bank forbids
- 0965b15 the census differed cold and warm inside this lap too, and the report says which one it quoted
- d8aec94 critic #8: the screen is the most correct document in the repository and no gate points at it

## Needs a human (15 open)

- NH-001 [DECISION] Email delivery needs three repository secrets
- NH-002 [DECISION] Optional: `@claude` on GitHub issues and PRs
- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-010 [DECISION] Expert consultations and the firefighter record (by 2026-09-20)
- NH-011 [DECISION] One real, recorded email send from a network that works in Shanghai (by 2026-09-20)
- NH-012 [DECISION] Portal downloads the loop cannot do (by 2026-10-01)
- NH-013 [FYI] Optional: a stable web address for the visual board
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-016 [DECISION] The critic routine has 30 minutes to land its findings, and needs about 40 (by 2026-09-05)
- NH-017 [DECISION] Three entries were closed on replies the repository cannot see, and one of them was wrong (by 2026-09-06)
- NH-018 [DECISION] Two laps read the same primary sources and disagree on two sentences (by 2026-09-08)
- NH-019 [DECISION] One report time, for one fire, decides what the detection result is allowed to say (by 2026-09-08)
- NH-020 [DECISION] Twenty-five report emails, no reply, and the decision channel has never once been exercised (by 2026-09-06)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
