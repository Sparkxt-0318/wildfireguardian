# WildfireGuardian autoloop · critic · 2026-09-07T2319Z

| | |
|---|---|
| when | 2026-09-07 23:19 UTC · 2026-09-08 08:19 KST |
| branch / head | `auto/dev` / `1bca8ed` |
| kind | critic |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 8, done: 25, parked: 1, title-derivable: 1, todo: 90 |

## What happened this lap

Critic lap #38. Reviewed head `1bca8ed`. Window: the 24 h to 2026-09-07T23:00Z.
Reviewed by: this routine is the independent reviewer, and every measurement below was taken
in this sandbox on the routine's **default** clone before any deepening, read unpiped.

⚠ **What this clone could and could not see, said first because it bounds everything else.**
`git rev-parse --is-shallow-repository` = **true**; `git rev-list --count HEAD` = **50**;
`git log --since='26 hours ago'` returns exactly **50**. So the oldest commit I can resolve is
`590c29a` (2026-09-07T00:43Z) and this report reads the window **from 00:43Z forward, not the
full 24 h**. The hour before it is outside the clone and I did not read it. CHARTER §4's own
paragraph says the depth is not a constant and must be measured per lap; this is that
measurement. No reachability or ancestry claim is made anywhere in this report.

## Your decisions, applied

**Nothing new arrived on either channel, and that is now a nine-day run.** The Gmail search
(`from:siyeong0318@gmail.com subject:"WildfireGuardian autoloop" newer_than:14d`) returns 25
threads and every one of them is a single message this loop sent to itself; a second search for
`subject:"Re: WildfireGuardian autoloop" newer_than:14d` returns **zero** threads. PR #31 has
**no** comments (GitHub MCP, `get_comments`). `docs/auto/decisions_seen.json` still records
`"seen": []`, and the newest applied decision is **NH-031** from a laptop session on 2026-09-06.
Nothing was parsed, nothing was applied, and nothing is quoted here because there is nothing to
quote.

⚠ **Twelve entries are open and two are due today.** **NH-032** and **NH-034** are due
**2026-09-08**, and together they decide what the project's headline margin means against a fair
opponent; until they are answered the student is forbidden to say **any** of the three
fair-opponent margins that exist (9, 27, 5-to-27). **NH-035, NH-038, NH-043** and **NH-044** are
due 2026-09-09. **NH-037** is due 2026-09-10 and this lap moved it (finding 5).

## Baseline, verified rather than read

| check | result |
|---|---|
| `gates.py --mode full` | **exit 0, ALL GREEN** at `1bca8ed`: `verify` PASS 15.1 s, `baseline-verify` **WARN** (the documented CHARTER §3d state), `snapshot-verify` PASS, `env-check` PASS, `pytest-full` PASS 212.2 s |
| pytest counts | `1689 passed, 62 skipped, 2 xfailed` — **COLD**, and **the run downloaded 25.9 MB** partway through (WFG-139) |
| `--assert-head` | exit **0**, `head=1bca8ed`, clean tree |
| `--assert-reported --base 3426135` | exit **0**, 20 substantive paths travelling with a new report |
| GitHub `auto-gates`, runs 204-223 on `auto/dev` | **19 `success`, 1 `cancelled` (218), ZERO `failure`**; run **223** at this exact head is `success` |
| CHARTER §4b | **no finding.** No red run behind a green report anywhere in the window |
| reports carry `Reviewed by:` | every dev and critic report in the window; the research report of 2026-09-06T1838Z still does not (WFG-147, unchanged, not duplicated) |
| every push carried a report | yes |

The cold/warm word is on both counts deliberately: my **cold** `1689 / 62` and the 2208Z dev
lap's **warm** `1695 / 56` are the same tree, and `1689 + 62 = 1695 + 56 = 1751`. The six-test
difference is exactly the terrain tests WFG-139 is about. The 2208Z report did say 「warm」, which
is an improvement on the lap before it, and it did **not** say whether the run downloaded
anything, which is the other half of the same DIRECTION rule. That is WFG-172, already filed,
not duplicated here.

## Findings, ranked

### 1. `fix-before-next-row` (ONE item, CHARTER §14b) — WFG-173: the judged screen's build stamp is sitting exactly on the staleness limit

`web/finals.html:434` carries `"git":"7308b06"`. `git rev-list --count 7308b06..HEAD` answers
**30** at `1bca8ed`. `tests/test_finals_screen.py:540` sets `STAMP_MAX_COMMITS_BEHIND = 30`;
`:732` asserts `behind <= STAMP_MAX_COMMITS_BEHIND`.

**The gate is green today at the limit and red on the first commit anybody pushes**, which on the
sprint's 3-hour dev grid is the next lap. Critic #37 read the same field three hours ago at
**24** and wrote it up as 「inside the limit, not a defect in this window」. That was true then.
Six commits later it is not, and the distance between those two readings is why this is an item
and not a note.

There is a worse failure mode twenty commits further on: when `behind` reaches the clone's own
depth (**50** here) the stamp stops resolving and the gate fails as `Not a valid object name`,
which the test's own message says 「reads as corruption rather than staleness」. That is what
turned critic #33's lap red on a correct tree (WFG-119).

⚠ **Nothing on the screen is wrong today.** Critic #37 re-derived all 28 on-screen keys against
`docs/NUMBERS.json` with zero misses either way, and R1 ticks on that work. This is a liveness
defect, which is why it is filed as minutes rather than as an investigation, and why it costs no
scorecard point.

**Done when** `make finals` has run on the commit being pushed, **and** `make finals-bundle` has
re-pointed `release/kcf-finals-2026/MANIFEST.json` in the same lap (WFG-152), **and**
`gates.py --mode full` is re-run after both. ⚠⚠ **Do not raise the threshold**:
`tests/test_finals_screen.py:754` grades it with literals deliberately not derived from the
constant, so raising it fails rather than slides past. NH-043 is the author's decision about the
gate's cadence and this row does not pre-empt it.

### 2. WFG-139 — eleventh consecutive measurement, and the first ISOLATED reproduction

Ten laps have measured this by watching a 25 MB file appear during a 200-to-350-second
`pytest-full` stage. That proves the suite downloads; it does not prove which line does, and the
row has carried a named suspect since critic #28 without anyone testing it alone. This lap tested
it alone.

After the cold gate run left `data/raw/dem/srtm/N36E129.hgt` (**25,934,402 B**) and
`N36E129.hgt.gz` (8,473,868 B) at mtime **23:02Z**, I deleted both tiles **and** the cached
`data/cache/dem_yeongdeok_2025_srtm_500m_80858ae747.nc` they had produced (all git-ignored, no
tracked artifact touched, `git status --short` clean afterwards) and ran exactly:

    pytest tests/test_spread_warmup.py::test_model_config_ignition_radius_increases_initial_burn

**1 passed in 1.54 s, and both tiles were back on disk at 23:06Z.** One test, no suite, no gate.

**Control, and it is why nobody found this by re-running:** the same single test with the `.nc`
cache still present passes in **0.47 s** and downloads nothing. A warm machine cannot reproduce
this, and the first thing anyone does after seeing a download is re-run.

The test asserts `first_area_ha > 25.0` and `metrics_persistence[0].predicted_area_ha > 25.0`,
both about the **ignition disc**, and it already runs `fuel_source="synthetic"`. The terrain is
incidental to what it asserts, so `dem_source="synthetic"` is the smaller of the two repairs the
row already prescribes and should be tried first, with the six `skipif`-guarded tests untouched.
**Grade it** by repeating the deletion with the network unreachable: green at the cold counts,
`data/raw/dem/` still empty.

⚠ The download buys the run that makes it **nothing**: `skipif` is evaluated at collection, so
the six guarded terrain tests skip in the very run that fetches their tile.

⚠ `docs/auto/JUDGE_QA.md` **Q28 is still uncorrected** and still tells the student 「그 입력을
필요로 하는 테스트는 이유를 적은 채로 건너뜁니다」. **Q40, 211 lines below it, says in bold that
Q28 is wrong on this point** and carries the ❌ do-not-say list. A student reading the deck in
order meets the false card first and the correction only if they reach the end, and the printed
kit reproduces both. Repairing Q28 in place is inside this row's 「Done when」 and needs the
same-lap `make printables` rebuild, which is why this routine cannot do it (DIRECTION's standing
boundary).

### 3. WFG-174 (new, P1, IEEE) — one provenance standard for a Korean agency, a laxer one for the Western literature

`paper/GAPS.md:118-127` and `paper/STATE.json` record that **fifteen of the 29 references are
verified 「via the Crossref record」** while §2 characterises six of them substantively:
`li2017`, `li2019`, `wahlqvist2021`, `cova2003`, `cova2005`, `finney2002`. A Crossref record is a
catalogue entry, which is the exact standard paper lap 16 applied when it retracted the NIFoS
sentence one commit earlier, and the exact standard **WFG-171 has just applied to two printed
booth panels in this same window**.

⚠ No claim here is false and none may be called false: the six paraphrases are title-derivable.
The defect is the asymmetry, and a software or ML judge who notices that this project narrowed a
Korean agency claim to a chapter list on Monday while paraphrasing six Western papers from their
titles gets to ask why. `paper/check_paper.py:203` cannot see the difference because it tests
only for the substring `verified`. Shares its gate half with **WFG-042**; the two should be done
together and the `check_paper.py` fix is one fix.

**P1 and not P0, deliberately.** §14b's judge-facing list contains the manuscript, but this puts
no false sentence in front of anybody and these five KCF judges do not read the bibliography.

### 4. WFG-175 (new, P1, infra) — a paper lap's findings have no route to the backlog, and today one was lost

`docs/auto/reports/2026-09-07T2112Z-manual.md:124` and `paper/GAPS.md:118` both say the finding
above was 「Filed as a dev-lap row」; `paper/STATE.json` says 「FILED, NOT FIXED」. **No such row
existed.** Checked at `1bca8ed` before writing WFG-174:
`git grep -niE 'provenance standard|title-derivable|Crossref record' docs/auto/BACKLOG.md`
returned nothing, and the only near neighbour, WFG-042, is a different defect (a `note` that
names a different paper than its own `url`).

**It is not that lap's slip.** CHARTER §12 confines the paper routine to `paper/` plus its own
report, so the routine is structurally unable to write the row it says it filed, three times, in
three files. The finding survived only because this lap opened `paper/STATE.json` to check the
word budget and read the field to the end.

**Done when** the paper lap writes proposed rows to `paper/PROPOSED_ROWS.md` inside its own
boundary; **and** a gate fails when that file is non-empty and no newer dev or critic report
names the rows in it; **and** `ROUTINE_PROMPTS.md`'s paper prompt says 「propose」 rather than
「file」. ⚠ Do not fix it by widening §12's write boundary: that boundary is what keeps the paper
routine from colliding with dev laps, and three rebases this week already landed on `paper/`.
§14b holds this behind R3, R4 and R8 as loop hygiene.

### 5. NH-037 is now binding on FIVE words, and its own worst case has happened twice in one day

`.auto/venv/bin/python paper/check_paper.py` at `1bca8ed` reports `body_words` **8,995** against
a hard fail at **9,000**, and exits OK. The same lap re-measured the built document with a real
renderer at **23 pages** against the author's **25-page** rule (NH-028), page objects and the
page tree's `/Count` agreeing. **The author's rule has two pages of room; the proxy has five
words.**

The margin was 55 when NH-037 was written, 8 after lap 16, and 5 now. Lap 17's three
reviewer-mandated corrections cost +7 words and were paid for with four one-word syntax
compressions. Five laps in a row have had their writing shaped by the proxy rather than by the
evidence, and what is left to compress is what four reviewers have already been over.

Nothing is red and every caveat is intact. What is new is that the next mandatory correction of
any size now parks the manuscript, which is correct behaviour under CHARTER §3 rule 9 and is
exactly the thing NH-037 asks the author to prevent. The entry is updated in place with this
measurement; its options are unchanged and **B or C** remains what the loop would pick, B being
one `apt` line in `.github/workflows/auto-gates.yml`.

### 6. What the window did well, said plainly because restraint counts both ways

**WFG-171 closed and closed well**, and I read the replacement text on both files rather than
grepping for the absence of the old sentence. The panel now prints the catalogue's eight chapter
names and says the flow 「읽힙니다」; Q16a carries the same register and gains a ⚠ note saying in
its own words that 「있다」 and 「없다」 are the same kind of new claim; the superseded wording is
registered as **`WC-009`** instead of being reprinted on paper; the kit was rebuilt in the same
lap and the bundle re-pointed. The lap's reviewer additionally caught a **circular provenance**:
`references.bib:258` had been certifying the manuscript's wording (「which is what the manuscript
states and all it states」) while the manuscript cited that same note as its source. Breaking that
circle is the single best thing in this window.

### 7. Against this routine, recorded rather than hidden

Critic #37 appended its scorecard row to the Track B table and the Track A table and **not** to
the series table, against `SCORECARD.md`'s own instruction two paragraphs above it (「Append here
**and** to the track table you are scoring」). I backfilled its `64f015b` row from those two rows
verbatim, marked as a backfill with the reason, and appended mine below it. Nothing was invented
and no existing row was edited.

Also mine: `docs/auto/DIRECTION.md` is **76 lines** after this lap, against 59 before it. I
trimmed two superseded critic notes to buy some of that back, and the growth that remains is in
*What not to do*, which is now the page's longest section and the obvious candidate for
compaction by whichever routine next has a reason to touch it.

## The root objection (`hate`)

**This loop keeps writing 「filed」 for work no gate will ever look for.**

The cheapest test: take the last four paper-lap reports, list every item they call filed, and
grep `docs/auto/BACKLOG.md` for each. Today's instance is one for one against. The loop has spent
this entire week building machinery so that a **withdrawn** claim cannot hide in a file nobody
thought to list, and it has no equivalent for a **found defect**, which hides just as easily and
costs exactly as much. WFG-175 is the repair; the objection is larger than the row, because the
same shape appears wherever a routine's boundary and its report's vocabulary disagree.

## `factchk` on new prose in the window

The window's new world-claims are all in the WFG-171 narrowing and they move in the safe
direction: every one of them is now weaker than the sentence it replaced and each states what
opened it. One check I could not complete and record as unfinished rather than passed: the panel
now prints the NIFoS catalogue's **eight chapter names verbatim** on a page a judge will hold,
and I could not re-open `https://book.nifos.go.kr/library/10130/contents/7732761` from this
sandbox (`curl` exit 35, connection reset by peer). `references.bib:258` records that an earlier
lap opened it on 2026-09-06 and the chapter list is quoted from that reading. **NH-039** is the
open request for the guide itself.

## `prism`, five lenses, one line each

- **KCF judge, software professor.** Gates green, CI green, 1,689 tests, and one test in the
  suite reaches Amazon S3; that last fact is what a professor would find, and Q28 currently
  denies it.
- **KCF judge, disaster-response official.** The differentiation panel is now honest about what
  it read, which is the register this lens trusts; the responsibility question still has no card
  (WFG-167, unchanged and unclaimed for a second lap).
- **Fire scientist.** Nothing in this window touched a model, a split, a metric, an arm, a
  coupling or a protocol, so there is nothing for this lens to attack and nothing for it to
  credit.
- **ML reviewer.** Same, and the leakage argument the repository makes about its own splits is
  still unquantified on its own data (WFG-142, P1, correctly parked behind CHARTER §3 rule 2).
- **Statistician.** No new number entered the registry this window; the pass-count comparability
  problem is real, measured, and is exactly WFG-139 plus WFG-172.

## Judge drill

Ten of the bank's hardest cards, answered from files only. Eight resolve to a committed file.
Two do not, and both are already rows rather than new findings: **Q28** answers falsely from
`docs/clean_clone_gates.md:27` and is corrected only 211 lines away in **Q40** (WFG-139), and
**Q16a**'s responsibility half has no card at all (`git grep -niE '책임|법적|면책'
docs/auto/JUDGE_QA.md` still returns zero; WFG-167). No new JUDGE_QA entry was written, because
DIRECTION forbids this routine from editing that file at all.

## Scorecard

| | Track B | Track A |
|---|---|---|
| previous (`64f015b`, critic #37) | 17 · 18 · 18 · 15 · 18 = **86** | 16 · 18 · 20 · 15 · 16 = **85** |
| this lap (`1bca8ed`) | 17 · 18 · 18 · 15 · 18 = **86** | 16 · 18 · 20 · 15 · **17** = **86** |

**One row moves: Track A 제출 자료 16 to 17**, and it is exactly the point critic #37 withheld and
pre-registered, earned by WFG-171's reprinted kit. **Why 17 and not 18:** Q28's false card is
still printed in the same kit. **Track B 제출 자료 holds at 18 on two cancelling moves**: the
broken citation circle (+1) against the provenance double standard now explicit on the same
criterion (-1). **Track A 구현 및 유용성 holds at 20** and the reason is stated rather than
assumed: gates green, CI green, every screen number still maps to a key; what the screen has is a
liveness problem, and that is an item, not a deduction. Everything else held because nothing in
the window touched what it scores. Reasoning per row is in the two track tables.

## In plain terms

- **What the judges would see today.** A booth that is one point better than yesterday. The
  printed panel used to say flatly how the national forest service's system takes its starting
  point; nobody here has ever opened that manual, so the loop rewrote it to say only what the
  library catalogue actually shows, reprinted your pages, and wrote the old sentence into the
  retracted-claims ledger. That is the kind of correction judges reward.
- **What changed since yesterday.** One real repair (above), and three things found. Your screen's
  build stamp is one push away from turning a gate red, so the next lap rebuilds it first; the
  test suite still downloads 25 MB from the internet, and this lap finally proved which single
  test does it, so it is now a minutes-long fix instead of a hunt; and the paper's word limit is
  down to **five words** of room while your own 25-page rule still has **two pages** spare.
- **What you should do.** Two decisions are due today: **NH-032** and **NH-034**. Until you answer
  them you must not say any of the fair-opponent numbers to a judge, and that is the single
  biggest thing holding the story back. Then **NH-037**, where **B** is the answer the loop would
  pick: one line added to a config file so the paper gets measured in real pages instead of
  guessed at in words. Reply to this email with one line per item, like `NH-032: A`.



## Gates

**ALL GREEN** · mode `full` · head `1bca8ed` · 2026-09-07T23:03:45Z · current at `1bca8ed`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 15.1 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.1 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 0.3 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 212.2 s | 1689 passed, 62 skipped, 2 xfailed, 52 warnings in 211.02s (0:03:31) |

## Commits since the previous report (-15)

- 1bca8ed the dev report names the head it ships in, and the word the rebase moved
- fa771d9 WFG-171: the rule the loop wrote covered "없다", and the panel was doing "있다" four lines above it
- fa18fcc WFG-171: the rule the loop wrote covered "없다", and the panel was doing "있다" four lines above it
- 3abf658 §3.5 counts three registration misses, not two, and the third is this routine's own
- 83f75d7 claim WFG-171 (20260907T2120Z)
- 3426135 critic #37: R1 ticks on two independent machines, and the rule the loop wrote covers only half the claim
- 64f015b the dev report names the head it ships in, and the re-run that certified it
- 804e5b6 the reviewer blocked on a number I could not register, and on a tolerance grounded in a false premise
- 6d1d730 WFG-166 + WFG-009: the card that forbade its own sentence, and the first test that presses a button
- e2eafc2 claim WFG-166 with WFG-009 (20260907T1820Z)
- 018dd78 critic #36: closing R1's second clause left R1 blocked by a row parked behind R1
- 7cc4eb7 WC-008: the paper lap's reviewer named two live copies, and registering them found two more
- a737198 report 2026-09-07T1600Z-dev, WFG-165, and the pipe that nearly became somebody else's bug
- b076b23 WFG-162 + WFG-110: the panel stops asserting what it never read, and R1's last blocker gets its reverse index
- 27b76f1 NH-043 was renamed by my own string replace, and the report it generated asked one question twice

## Needs a human (17 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09)
- NH-036 [DECISION] One critic lap told the next one not to edit a file, and that is what kept a false sentence in front of a judge for a window (by 2026-09-10)
- NH-037 [DECISION] The paper's word proxy now stops it a thousand words before your 25-page rule (by 2026-09-10)
- NH-038 [DECISION] Your "product first" rule has spent the last three dev laps on documents, and the readiness line it was written to protect has not moved in five critic laps (by 2026-09-09)
- NH-039 [DECISION] The national wildfire-spread system's manual is an 18 MB PDF the sandbox could not fetch, and one of you can (by 2026-09-12)
- NH-040 [FYI] A critic lap pushed one commit past a red `--assert-reported`, and it is telling you rather than hiding it
- NH-041 [FYI] This lap sent you an email containing only the word PLACEHOLDER, and could not take it back
- NH-042 [DECISION] Two of your own rules collide whenever a withdrawn claim lives in a frozen artifact, and this week they collided three times (by 2026-09-10)
- NH-043 [DECISION] A gate your loop built this morning will go red about twice a day, and the charter tells the lap that meets it to stop working (by 2026-09-09)
- NH-044 [DECISION] The claim the paper just retracted is still live on the page the paper cites for it (by 2026-09-09)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.

---

# ⚠⚠ ADDENDUM, written after the commit: THE PREDICTION IN WFG-173 CAME TRUE IN FOUR MINUTES, AND THIS LAP IS PARKED

**Read this before anything else above.** Everything above was written against a **green** baseline at
`1bca8ed`, and that reading was correct: `gates.py --mode full` exited **0** there. Then this lap committed
its own report, `docs/auto/` only, and that single commit took `git rev-list --count 7308b06..HEAD` from
**30** to **31**.

    tests/test_finals_screen.py::test_the_screen_is_rebuilt_before_its_stamp_ages_out_of_this_clone  FAILED
    AssertionError: web/finals.html was built at 7308b06, now 31 commits behind HEAD (limit 30).
    assert 31 <= 30

So **WFG-173 is no longer a forecast; it is a live red gate**, and it fired on the report that named it.

**The full run on the parked commit, read unpiped and recorded here rather than summarised:**
`gates.py --mode full` at `84796ea` exits **1**, **RED**. `verify` PASS 14.6 s, `baseline-verify` WARN
(the documented CHARTER §3d state), `snapshot-verify` PASS, `env-check` PASS, `pytest-full` **FAIL**:
**`1 failed, 1694 passed, 56 skipped, 2 xfailed`** in 1447.6 s, **warm** (the SRTM tile is on disk from
this lap's WFG-139 reproduction, which is why 56 skipped rather than the baseline's 62, and no download
occurred in this run). **The one failure is the staleness gate and nothing else.**
`gates.py --assert-head` then exits **1** with 「the recorded run did not pass」, which is the mechanism
that stops this from being pushed to `auto/dev`, and it worked exactly as designed.

**Where this lap's work is.** Parked on **`auto/red/2026-09-07T2319Z`** per CHARTER §3.9, with
`origin/auto/dev` left at `1bca8ed`, which is green. Nothing was force-pushed and nothing is lost.

**Why this routine did not simply fix it.** The remedy is one command, `make finals`, printed by the gate
itself. It regenerates `web/finals.html`, an artifact outside `docs/auto/`, and the critic routine's standing
prompt forbids that in those words: 「You change NO code and NO artifact; you write only under
`docs/auto/`」. The routine that met the red is the one routine that may not clear it.

**⚠ The same trap is set for the next lap, whichever routine it is.** `behind` counts commits, not changes.
CHARTER §4 step 3 has the next dev lap claim its row with a commit and push it **before building anything**:
that claim alone takes `behind` to 31 and turns the gate red before the lap has done any work. `auto/dev` is
not broken. It is **closed**, until some lap runs `make finals` and pushes the rebuilt screen.

**What the next lap should do.** Run `make finals` on the commit you are pushing, then `make finals-bundle`
to re-point `release/kcf-finals-2026/MANIFEST.json` in the same lap (WFG-152), then re-run
`gates.py --mode full`. That is WFG-173's 「Done when」 verbatim, and the gate's own message says the same
thing. Then merge or cherry-pick `auto/red/2026-09-07T2319Z` so WFG-173, WFG-174, WFG-175, the WFG-139
reproduction and this `CRITIC_LATEST.md` reach `auto/dev`.

**⚠ This lap deliberately did NOT write a CHARTER §4 step 2 override into this file.** Critic #33 wrote one
and NH-043 records the problem with it: every critic lap rewrites this file, so the override expires within
hours and the question stays buried. Writing another would have buried it again. The escalation is
**NH-045** (BLOCKER, new this lap) and it points at NH-043's option A or B as the fix for the class.

**Do NOT raise `STAMP_MAX_COMMITS_BEHIND` to get past this.** `tests/test_finals_screen.py:754` grades the
threshold with literals deliberately not derived from the constant, so raising it fails rather than slides
past.
