# WildfireGuardian autoloop · dev · 2026-09-09T1928Z

| | |
|---|---|
| when | 2026-09-09 19:28 UTC · 2026-09-10 04:28 KST |
| branch / head | `auto/dev` / `dbb5350` (pushed; report.py stamped `3ec5737`, the tree it ran on — the reviewer's fixes, the report itself and the rebuilt kit ride in `dbb5350` and `gates.py --mode full` exits 0 there) |
| kind | dev |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 9, done: 33, dropped: 1, parked: 1, title-derivable: 1, todo: 107 |

## What happened this lap

## Your decisions, applied

**Nothing new.** The Gmail search `from:siyeong0318@gmail.com subject:"WildfireGuardian autoloop"
newer_than:14d` estimates **201** threads; the 20 it returned are all reports this loop sent, each a
single-message thread with no reply on it. ⚠ 20 of 201 is not a reading of the mailbox, so the decisive
check is the second search, which covers all of them: `subject:Re WildfireGuardian autoloop newer_than:14d`
returns **nothing at all**, so no reply to any report exists in the window. PR #31 has no comments. `docs/auto/decisions_seen.json` is unchanged and its
`seen` list is still empty — every decision on record came through the laptop channel (NH-020), the last
of them NH-031 on 2026-09-06. **25 NEEDS_HUMAN entries are open and seven are at or past their date**;
the two that hold the most work back are NH-046 (both remaining readiness lines are downstream of it)
and NH-053, which this lap's row ran into head-on and could not settle.

⚠ **One thing I could not reconcile, and I am not guessing at it.** This routine's own prompt cites
「CHARTER §14b as amended 2026-09-07, NH-038 B」, 「CHARTER §5b, NH-035 B」 and 「CHARTER §3.5c as amended
2026-09-07, NH-042 A」. `docs/auto/CHARTER.md` at this head carries none of those amendments — §5b still
says NH-030 option C and three hours — and `docs/auto/NEEDS_HUMAN.md` still lists NH-035, NH-038 and
NH-042 as `open`. I followed the prompt, because the prompt is what runs this lap, and the three rules it
states are strictly safer than the ones in the file (a minutes-only preemption, a slot-relative stale
claim, registration-always-wins). But either the charter is a day and a half behind three of your
decisions, or the prompt is asserting decisions you have not made. **That is one line of reply either
way**, and it is quoted here rather than resolved because CHARTER §6 says a reply the loop cannot map is
quoted, never guessed at.

---

## The technical account

**Slot.** 2026-09-09T1817Z, a dev slot: the research routine takes 18:17 UTC on 09-06, 08, 10, 12 and 14,
and today is the 9th. Baseline `gates.py --mode quick` exit **0** at `b5de13e`. GitHub `auto-gates` run
**295** is `success` at that same head, so CHARTER §4b sets no finding #1.

**Claim.** `75062c1`, pushed before any build. No stale `in-progress` to release: no status cell in the
table held one.

### 1. Critic #52's `fix-before-next-row` item — the schedule document (prose only, as scoped)

`docs/auto/finals/TIMELINE_ROLES.md` shipped six hours before the critic read it and said two things about
itself that were not true.

**(a) The boundary claim was an overclaim, and I re-derived it rather than inheriting it.** §0 said 「각
구간의 경계는 제가 정한 것이 아니라 커밋 기록에 실제로 비어 있는 달력 간격입니다」. Measured on a **full**
clone — 668 commits at `b5de13e`, `--is-shallow-repository` **false** — with the builder's own predicate
(`TZ=UTC`, `--date=format-local:%Y-%m-%d`), the record's empty-day gaps largest first are **32** (06-15 →
07-18), **15** (08-12 → 08-28), **6** (06-06 → 06-13), **5** (07-26 → 08-01), **2** (05-30 → 06-02) and
four of one day. The document splits on the 32, the 15, the 5 and **one of the one-day gaps** and does not
split on the **six-day** one. `scripts/build_timeline_roles.py:40-84` hard-codes all five `start`/`end`/
`anchor` literals and `build()` only counts inside them, so there is no gap-selecting rule anywhere in the
tree: the boundaries **are** a choice. Every figure in critic #52's table reproduced exactly.

⚠ **I took that measurement in a throwaway full clone under the session scratchpad, not by deepening this
one.** That is WFG-217 working as a constraint rather than as a warning: `--unshallow` here raises the
object count past the seven-character `%h` boundary, `build_timeline_roles.py --check` then exits 1 on a
correct artifact and the whole suite goes red — which is exactly what happened to critic #52 on its own
commit. A separate clone answers the same question and leaves this one shallow, where the test skips.

**(b) Both totals were stale within six hours.** §2 stated 「트레일러 **513개** (전체 662개 중)」 as a flat
present-tense fact; at `375be25` the same builder answers **517 of 666**, and at this head **519 of 668**.
They are **not** retyped to today's values — a retyped value re-derives from nothing and would be stale
again by the next lap. They carry the tree they were counted on: **`89da7d3`**. ⚠ That is not the commit
the critic's item suggested (`359fd15`): the artifact records 662 commits, and `359fd15` already held 664,
because two commits landed between the build and the push. 662 is reached at `359fd15~2` = `89da7d3`, the
WFG-027 claim commit. I checked the suggested stamp before writing it and it would itself have been a
false as-of.

**(c) No artifact was rebuilt and no `timeline_*` key moved.** The critic forbade it and WFG-217 is why:
in this sandbox the rebuild writes eight-character anchors into a tracked file and turns **GitHub** red.

**(d) `WC-012` registered in the same commit** (CHARTER §3.5c). Both halves of the one sentence are
registered as separate spellings, because the claim survives a rewrite of its first half — 「구간은 커밋
기록에 실제로 비어 있는 달력 간격입니다」 says the same thing with the agent removed, which is the form a
later lap paraphrasing from a report would produce. That is the WC-007/WC-008 lesson applied to halves of a
sentence rather than to languages. The dated record line in §0 carries both tokens on one pragma.
`check_withdrawn_claims.py` passes over 938 gated files. Registering it also surfaced that **WC-011 had
never been added to `docs/withdrawn_claims.md`'s own table of registered claims** — the registry was
correct and the human-readable list was one row short; both rows are there now, with WC-011's absence
recorded rather than quietly filled.

**(e) `docs/auto/DEMO_SCRIPT_5MIN.md`**, which is **printed**, now carries the as-of clause on the spoken
42일 and answers 「그 경계는 누가 정하셨습니까?」 with 「제가 정했습니다」, with the old answer as an ❌ line.

### 2. WFG-214 — three README lines, and the gate that was requiring the overclaim

`README.md:36`, `:266-267` and `:702` all told the reader that **42 is what a noiseless forecast would
buy**. `docs/oracle_gap.md` §2 establishes otherwise from `data/processed/routing_demo_canonical.npz`, and
I opened that file rather than trusting the prose: it holds `haz_stack` `(5, 181, 156)` float32, the
leave-one-fire-out forward simulation the router plans on, **and** `obs_stack` `(6, 181, 156)` uint8, the
observed FIRMS footprint, which scores nothing. **The oracle is in the grading, not in the planning.** All
three lines now say that, in the mechanism's own words, and link `docs/oracle_gap.md` with its §7 (what the
measurement does not show) and §6 (the re-grading that has not been run).

⚠ **The word 「상한」/「upper bound」 is not settled, in either direction, and that constraint is the row.**
Deleting the word decides NH-053 by omission exactly as asserting it does. So it stays, demoted from
assertion to open question, and every block that uses it now names NH-053.

**That could not be done without changing a gate, and this is the part worth reading.**
`tests/test_readme_round4.py` held `_UPPER_BOUND = r"42 is\s+an\s+\*{0,2}upper[- ]bound\*{0,2}"` and
`_UPPER_BOUND_KO = r"상한"`, and two tests required every README block stating 42 to match one of them.
**The suite was requiring the README to assert the sentence NH-053 asks the author about**, and the only
way to clear it was to write that word. Both patterns are widened to the mechanism wording (the old ones
kept verbatim in the comment as the record), and a new gate binds the word to the escalation instead.

**No committed number moved.** 42, 91, 9 and 27 are untouched; no margin value entered `README.md:210-282`,
whose `Do NOT edit` note permits exactly this rewording; nothing was refit and no figure regenerated.

**And nothing was made worse for the row that comes after.** WFG-212 is about the README's ⚠ density.
Counted before and after: **47** ⚠ in the whole file at `b5de13e` and **47** now; **11** in `README.md:200-362`
and **11** now. The rewrite is longer than what it replaced and adds no new warning marker — the lead
paragraph WFG-212 asks for still has the same job to do, and does not have to re-open this section to do it.

**Five new gates, each shown red before green** (the fifth came from the reviewer, below). Removing NH-053 from the Korean block →
`test_no_block_asserts_the_bound_without_naming_the_open_question` fails. Removing the as-of stamp from §2's
trailer row → `test_the_two_growing_totals_carry_the_commit_they_were_counted_at` fails. Replacing 「06-06 →
06-13 의 엿새」 with a vague phrase → `test_the_unsplit_six_day_gap_is_disclosed` fails. The boundary-claim
spelling is gated tree-wide by the registry rather than by a local assertion.

### The root objection I recorded before building (`hate`)

**「Both edits are claim-weakening edits on the surface a judge reads first, and the honest version of each
one can decide an open question by omission.」** For WFG-214: strike 「상한」 and the README now implies the
bound does not hold, which nothing derives either; that is NH-053 settled in the other direction by a lap
that was told not to settle it. For the schedule document: over-correct to 「이 표는 임의입니다」 and the
partition proof — the strongest thing in the document — goes down with the sentence that was wrong. The
cheapest test for both was the same one: after the edit, can a reader still tell **what is derived** from
**what is asked**? That is why the word stays and is bound to NH-053, and why §3.5 ends by saying that the
boundaries were chosen and the completeness was proved, which are different claims about different things.

### What did not work, and is left as residue

- **`paper/manuscript.md` still lacks the `docs/oracle_gap.md` link**, which is the fourth surface WFG-214
  names. `paper/check_paper.py` reads **8,999 body words against a 9,000-word hard fail** — one word of
  margin. The link cannot be added without trading a caveat away, and CHARTER §3 rule 5 forbids rounding a
  limitation out to make room. That is **NH-037**, already open and already escalated by paper lap 24, and
  `paper/` is the paper routine's file (CHARTER §12). **So WFG-214 goes back to `todo` with residue, not to
  `done`** — three of its four surfaces are closed and the fourth is budget-blocked.
- **I did not touch `web/finals.html` or `README.md`'s 일정 coverage.** That is WFG-218, the next row.
- **The printed kit was rebuilt as the last content action of the lap**, after `sip`, after the reviewer's
  fixes, and after the WFG-219 filing — `WFG_printables_20260909T1908Z.pdf`, 53 pages — and
  `release/kcf-finals-2026/MANIFEST.json` re-pointed at it **after** staging, per MEMO 2026-09-09's own
  ordering rule and the 0949Z / 1250Z / 1619Z precedent. The bundle had been naming the 1611Z kit, which
  would have shipped a judge the schedule document's false sentence.

### The independent review

**Reviewed by: subagent (pass)** — root objection: *「the `_UPPER_BOUND` → `_ORACLE` widening is a strict
superset, so it is a real loosening, and it costs one property the old regex had incidentally: the old
pattern could only be satisfied by a clause that CONTRADICTS the overclaim WFG-214 exists to remove,
whereas the new mechanism alternatives are logically COMPATIBLE with it.」* It handed me the exploit
sentence: a block that states the mechanism correctly and then draws the opposite conclusion — 「the
forecast-aware arm is graded on the very field it planned on, so the oracle is in the grading. 42 is
therefore what this project's own model buys」 — passed the widened suite green.

**It passed rather than blocked because `DIRECTION.md:65-67` orders this lap to 「describe the mechanism;
leave the word alone and point at NH-053」 and the old regex could be cleared only by writing the word, so
keeping it was never an option. I fixed it anyway rather than banking the pass.**
`test_no_block_says_42_is_what_this_projects_own_model_buys` now asserts the property directly instead of
inheriting it from a wording: the claim is banned in the **affirmative only**, with the negator checked in
a 48-character window, because the correct prose on the page says the same phrase with 「not」 in front of
it. **Graded on the reviewer's own sentence, which I did not write:** pasted verbatim into `README.md` the
suite goes **red**; restored, **27 passed**.

Two of its residual weaknesses are closed in the same pass. **`AS_OF` could go silently wrong** if the
artifact were ever rebuilt, since no gate can re-derive it in a shallow clone — the test now pins the two
totals it was measured against (662 / 513), so a rebuild is loud and the next lap re-derives the stamp
deliberately. And **`scripts/build_numbers.py --check` is not a check**: the script has no `argparse` at
all, so every flag is ignored and every invocation rewrites `docs/NUMBERS.json`. The reviewer hit it while
verifying this lap and had to `git checkout --` the wholesale rebuild it produced. ⚠ The row itself first went in quoting the entry count the reviewer saw, and `check-forbidden` refused it as an unlabelled retired registry count — the gate catching this lap's own prose in the same run that certified it. Verified read-only at `3ec5737` and
filed as **WFG-219** (P1, infra, in the P1 band so it displaces no P0 row). That one is worth reading
twice: `docs/NUMBERS.json` is protected by CHARTER §3 rule 2, this routine's own prompt warns against
wholesale rebuilds, **and the flag a careful reader reaches for to avoid one is the flag that does it.**

Three of its notes I am carrying forward rather than fixing: `_blocks_stating_42` treats a whole bullet
list as one block, so 「the block」 is coarser than its docstring claims (pre-existing, and it undercuts the
rationale my new test leans on); the new Korean gates are copy-paste ratchets, not claim detectors, which
is the known WC limit already written down in `docs/withdrawn_claims.md` §4; and two figures in this
report — the Gmail thread count and the `auto-gates` run status — are not checkable from the repository,
which is true and is why both name their source.

### Next row

**WFG-218** (P0, KCF): 일정 counts **0** on `README.md` and **0** on `web/finals.html`, so the schedule
answer this lap just repaired is still unreachable from the two surfaces a judge actually looks at. It is
WFG-027's own disclosed residue, refiled by critic #52 because residue written into a `done` cell is
invisible to CHARTER §4 step 3. After it, WFG-212.

## In plain terms

- **What changed for the project.** Your schedule document was telling judges that nobody chose the five
  phases — that the gaps in the commit record chose them. That is not true: all five are typed into the
  script, and there is a six-day gap it skips over. It now says you chose them, and says what the script
  *does* prove (no commit falls outside the five phases). And the front page stopped claiming that your
  headline 42 is what a *perfect* forecast would buy; it now explains the real mechanism and says plainly
  that the word 「상한」 is a question waiting on you, not a result.
- **Why it matters to the judges or the science.** A software judge falsifies the old schedule sentence in
  thirty seconds by opening the script — the cheapest way this project could have lost points on a 20-point
  sub-item. And a gate in your own test suite was *forcing* the README to assert the claim your open
  question NH-053 is about; that is now fixed, so the repository can no longer be honest and green at the
  same time only by overclaiming.
- **What the author should do.** **NH-053** is now blocking real work — I could repair three README lines
  but not settle the word 「상한」, and the paper cannot carry the fix at all until **NH-037** frees a few
  words. Seven decisions are at or past their date. One reply with a few `NH-###: <answer>` lines clears
  them. And please confirm one thing I flagged above: my routine prompt says you decided NH-035, NH-038 and
  NH-042 on 2026-09-07, but the charter and the ledger in the repository say all three are still open.



## Gates

**ALL GREEN** · mode `full` · head `dbb5350` · 2026-09-09T19:37:17Z · current at `dbb5350`

⚠ **Re-stated on the commit that was actually pushed, and the earlier run is kept beside it rather than replaced.** `report.py` stamps this table with whatever `.auto/gates.json` held when it ran, which was the green `full` pass at `3ec5737` — a real pass, but on a tree without this report, without the reviewer's fixes and without the rebuilt kit. The suite was re-run on `dbb5350`, the commit this lap pushed: **ALL GREEN, 1862 passed, 64 skipped, 3 xfailed**, with `--assert-head` and `--assert-reported` both exiting 0 there. `baseline-verify` WARNs on the two git-ignored `data/raw/**` contracts, which is NH-029 and CHARTER §3d working as the author chose.

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 26.4 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.2 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 0.5 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 476.0 s | 1862 passed, 64 skipped, 3 xfailed, 52 warnings in 474.09s (0:07:54) |

## Commits since the previous report (375be251e99c77a8a88994017fe68d212cb81dbf..HEAD)

- 3ec5737 the schedule document says who chose its boundaries, and the front door stops asserting the bound NH-053 asks about
- 75062c1 claim WFG-214 (20260909T1817Z)
- b5de13e the gate is not sensitive to clone depth, it is sensitive to which other branches are in the clone
- a53912d critic #52: the gate that binds the schedule document to its own history measures the clone, not the tree

## Needs a human (25 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09)
- NH-036 [DECISION] One critic lap told the next one not to edit a file, and that is what kept a false sentence in front of a judge for a window (by 2026-09-10)
- NH-037 [DECISION] The paper's word proxy now stops it a thousand words before your 25-page rule (by 2026-09-10)
- NH-038 [DECISION] Your "product first" rule has spent the last three dev laps on documents, and the readiness line it was written to protect has not moved in five critic laps (by 2026-09-09)
- NH-039 [DECISION] The national wildfire-spread system's manual is an 18 MB PDF the sandbox could not fetch, and one of you can (by 2026-09-12)
- NH-040 [FYI] A critic lap pushed one commit past a red `--assert-reported`, and it is telling you rather than hiding it
- NH-041 [FYI] This lap sent you an email containing only the word PLACEHOLDER, and could not take it back
- NH-042 [DECISION] Two of your own rules collide whenever a withdrawn claim lives in a frozen artifact, and this week they collided three times (by 2026-09-10)
- NH-043 [DECISION] A gate your loop built this morning will go red about twice a day, and the charter tells the lap that meets it to stop working (by 2026-09-09)
- NH-045 [BLOCKER] The staleness gate has closed `auto/dev` to every routine, and the one routine that met it is the one forbidden to clear it (by 2026-09-08)
- NH-044 [DECISION] The claim the paper just retracted is still live on the page the paper cites for it (by 2026-09-09)
- NH-046 [DECISION] Your product's definition-of-done names a command nothing in this project has ever run (by 2026-09-10)
- NH-048 [DECISION] One of the research routine's three literature channels has been dead for two runs, and a working replacement is already proven (by 2026-09-10)
- NH-049 [DECISION] Your critic routine is told to add judge Q&A cards and your own printing gate makes that impossible for it (by 2026-09-11)
- NH-050 [DECISION] You answered two of these questions two days ago and the loop never heard you, because you answered them on the routine page (by 2026-09-10)
- NH-051 [DECISION] The rule your loop has been obeying for three days is not the option it names, and the difference is why one row has been pushed down five times (by 2026-09-11)
- NH-052 [DECISION] The experiment that measures what your own forecast is worth is now one run away, and it would change what 42 means (by 2026-09-12)
- NH-053 [DECISION] The word your front door uses for 42 stopped being right today, and the loop cannot pick its replacement (by 2026-09-12)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
