# WildfireGuardian autoloop · dev · 2026-09-07T1900Z

| | |
|---|---|
| when | 2026-09-07 19:00 UTC · 2026-09-08 04:00 KST |
| branch / head | `auto/dev` / `6d1d730` |
| kind | dev |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 8, done: 25, parked: 1, todo: 87 |

## What happened this lap

Two rows: WFG-166 (critic #36's one `fix-before-next-row` item) and WFG-009
(DIRECTION position 1, the sole remaining blocker of readiness line R1). Claimed
together at `e2eafc2` because WFG-166 needs the `make printables` rebuild WFG-152
mandates, and that rebuild is one action.

## Your decisions, applied

**Nothing new on either channel.** The Gmail search
`from:siyeong0318@gmail.com subject:"WildfireGuardian autoloop" newer_than:14d`
returns 25 threads and every one is a single message this loop itself sent — no
reply from you. PR #31 has zero comments. No `NH-###:` line to apply, and
`docs/auto/decisions_seen.json` is unchanged.

## WFG-166 — the card that forbade its own sentence, and the copy nobody named

`docs/auto/JUDGE_QA.md:650-652`, inside the **recited T0 draft** of Q16a, had the
student say aloud that G-DAPS 「사이렌을 어디에 울릴지는 정해 주지만 특정한 집의 어느
길이 위험에 들어가고 어느 길이 들어가지 않는지는 말해 주지 않습니다」 — a flat negative
about a system whose manual nobody here has opened (NH-039). Lines 671-672 of the
**same card** forbid exactly that shape and give the permitted form. Fixed: the draft
now says 「저희가 연 자료 — 기관 카탈로그 기록과 언론 보도 — 에서는 특정한 집 단위의
대피 경로 산출물이 확인되지 않습니다」.

**The subject grep this row mandated found a second live copy that neither critic
#36 nor the backlog row named:** `docs/auto/finals/RELATED_WORK_PANEL.md:67-69`,
carrying the same clause in the same shape — **on the printed panel**, in the very
file whose *other* clause the 1528Z lap corrected for WC-007 one lap earlier. One
lap, one page, two clauses of one family, one of them fixed. Corrected into the
register the panel already uses at `:40`.

`JUDGE_QA.md:625` asserted that Q16a says the hedged form 「라고만」 — false from
`1f024ed` until now, and it is the line a later lap would read to decide the card was
clean. Corrected, with a dated note recording that **a card's own account of itself
does not inspect the card**. Both superseded sentences are kept as dated records that
**describe** rather than reprint, because both files print (WC-005/WC-007 precedent);
the registry quotes them.

**Registered in the same lap (CHARTER §3.5c):** `WC-008` gains its **Korean**
spelling, `특정한\s*집의\s*어느\s*길이[^\n]*들어가지\s*않는지는`, with a probe sentence
and an expected-set entry. Graded 4/4 in-process: fires on the withdrawn sentence,
silent on both replacements and on this project's own positive description of its
output object. Two limits written down rather than implied — it anchors on the
**object** of the negation, not the predicate, because the sentence wrapped across a
line break in both files and the checker is line-based; and 「특정한 집의」 is absent
from the project's own positive wording, so a rewording into that escapes.
`docs/withdrawn_claims.md` gains §5g-2 and the WC-008 table row is updated. The gate
then caught my *own* description of the pattern in that doc and I licensed the line
with its token rather than rewording around it.

Kit rebuilt in the same lap: `WFG_printables_20260907T1825Z.pdf`, 38 pages;
`release/kcf-finals-2026/MANIFEST.json` re-pointed and verified byte-identical.

**Subject greps run, and the files named.** Command, run unpiped and then counted:
`git grep -n <subject> -- docs/ paper/ release/ web/`. At this tree 「읍면동」 returns
**41 lines in 20 files** and 「가구 단위」 **36 lines in 21 files**; both totals include
this lap's own BACKLOG, MEMO and doc lines. ⚠ The counts are **not load-bearing and
should not be quoted** — my reviewer measured different totals at a different head and
I cannot reproduce either set across heads, so what the rule actually asks for is the
naming, below, not a number. **Of the 「읍면동」 hits:** 2 needed the fix above; 1
(`docs/auto/knowledge/KOREAN_OPERATIONAL_SYSTEMS.md:18`) already states it as the
neutral definitional fact; `docs/related_work.md:64/128/148` and
`paper/references.bib:267` are sourced descriptions of G-DAPS's own **published**
resolution; `docs/juso_yeongdeok.md:59/70/83` uses 읍면동 for address offices and is
unrelated; the remainder are record class (BACKLOG, CRITIC_LATEST, KCF_READINESS,
SCORECARD, MEMO, NEEDS_HUMAN, reports, research). **Of the 「가구 단위」 hits:**
`docs/dispatch_ordering.md:327` already carries the licensed hedge;
`RELATED_WORK_PANEL.md:23/127` and `JUDGE_QA.md:600` are this project's own positive
output claims; `docs/NUMBERS.json` ×3 are registry labels;
`docs/SESSION14_REPORT.md:85/87`, `docs/building_origins.md:235`,
`docs/global_portability.md:44`, `docs/firefighter_consultation.md:233` and
`docs/evidence/greenpeace_2026_survey.md:107` are this project's own coverage caveats;
the rest are record class. Nothing else needed an edit.

## WFG-009 — the four acts advance, and the word 「advance」 gets its first test

**First, a correction that changes what the row is.** `web/finals.html` has two
different fours. Critic #36 measured the four **views** (`view-live`,
`view-evidence`, `view-reliability`, `view-system`) and reported that two hold no
Korean text until a switch. That is true and it is a **different mechanism**. R1's
「four acts」 are the guided demo's **막** — 1막 발견, 2막 시간과 도로망, 3막 경로 비교,
4막 판단 — the sequence `DEMO_SCRIPT_5MIN.md` walks a judge through, driven by
`#gNext`. My reviewer checked this against `web/finals.html:473`, `docs/FINALS_DEMO.md:44`
(「안내 시연 4막」) and R4's own cell (「over docs/FINALS_DEMO.md's four acts」) and
confirmed it independently. The views are now **WFG-169**.

**No Playwright, so the driver is stdlib.** `pip` cannot reach
`files.pythonhosted.org` here and neither `websockets` nor `websocket-client` is
present; CHARTER §4b forbids a gate that reaches the network, so "install it in CI"
is not an escape. `scripts/cdp_min.py` is a minimal RFC 6455 + DevTools client.
`scripts/check_finals_acts.py` copies `web/` **whole** into a temp dir, presses
`#introGo` with a **real mouse event** (the judge's own way in), then `#gNext`, and
after every press asserts the act actually changed.

**Result, `Chromium 141.0.7390.37`:** all four acts advance — 1/2/3/4 progress dots,
captions non-empty, never leaving the live view. **10 requests, none off `file://`,
zero console errors.** Four screenshots at
`docs/auto/finals/act_screenshots/20260907T1825Z/`, and I opened them: fully rendered
map, hazard field, region cards, the act-4 comparison table. My reviewer reproduced
the run independently and got the same report. `auto-gates.yml` gains a `finals-acts`
job that uploads them.

⚠ **Reaching act 4 takes four presses, not three:** act 3 is two steps (STATIC VIEW →
TIME-AWARE VIEW), confirmed in `web/finals.html:1462-1467`. A driver that assumed one
press per act would stop on act 3's static half and call it act 4; this one presses
until the label is the expected one.

**Root objection I recorded before building (`hate`).** A driver that presses a button
and writes a PNG proves only that Chromium did not crash: if `#gNext`'s handler
silently failed, the dialog would still be on screen, the screenshot would still be
written, and R1 would be ticked on four blank panels. So `_assert_advanced` is graded
by deleting each of its checks in turn and requiring the suite to go red. **7 of 7
killed**, measured this lap by mutating the real module (see below).

## The independent review returned `block`, twice, and both were right

`docs/auto/LOOP_CONFIG.json` → `review: subagent`. **Reviewed by: subagent (block).**
Its root objection, and what I did:

1. **「20 of the booth kit's 38 pages」 was a number no artifact supports** — written
   into `withdrawn_claims.json` and the gated `docs/withdrawn_claims.md`, i.e. into
   the one artifact whose entire purpose is that this project does not write unchecked
   claims (CHARTER §3.3). Verified: `pages_per_source` in
   `manifest_20260907T1825Z.json` gives `JUDGE_QA.md` **19** and
   `RELATED_WORK_PANEL.md` **3** — 22, not 20, and the previous manifest holds the same
   figures, so 20 was never right at any stamp. Both places now cite the manifest
   fields instead of a bare literal, and both keep a dated note saying the draft was
   wrong and that the reviewer blocked it.
2. **The media tolerance rested on a false statement about the tree.** The code, the
   doc and my own summary all said the tolerated extensions were 「the ones
   `.gitignore` excludes」. `.gitignore` excludes only `*.mp4 *.mov *.avi`;
   `git check-ignore` on `ambient-documentary.mp3` and `ui-soft-click.wav` prints
   nothing and exits 1. So `.mp3`/`.wav` were tolerated on a premise that does not
   hold — and `web/finals.html:593-597` references five **committable** `.wav` UI-sound
   slots the tolerance would have silently covered. **The predicate is now the git
   index**, not an extension list: a file committed under `demo-media/` must load; one
   never committed is an unfilled slot. A `.wav` committed tomorrow becomes mandatory
   with no edit to this file. The two tests that asserted the false premise are
   replaced by three that read the index, including one that feeds a **fake** index and
   requires the same `.wav` to flip between mandatory and optional.

Three further findings I also acted on rather than filing:

3. **`_assert_advanced` was graded 5 of 7, not 7 of 7.** The reviewer mutated each
   branch and found `act != expected_label` and `title` empty **surviving** — no test
   covered them. My own first attempt to reproduce this said 0/5, which was my harness
   being wrong (the test file's `sys.path.insert` shadowed the mutated copy), not the
   tests: the MEMO's own 「suspect the mutation before you believe the gate」 lesson,
   paid again. Re-run against the real module with two new tests: **7 of 7 killed.**
4. **「독립 증인」 was an overstatement.** I called the progress-dot count an independent
   witness against a half-applied transition. `web/finals.html:1403-1410` sets label,
   caption and dots from the same `act` argument in one synchronous block, so that
   state cannot arise from this screen. Both the code comment and the doc now say it is
   a **redundancy for a future version**, and say who caught it.
5. **The CI job's artifact claim is unverified.** Whether `ubuntu-latest` has a browser
   at any `CHROME_CANDIDATES` path was not measured; if it has none the driver skips,
   the job is green and the artifact is empty. `if-no-files-found: warn` is now explicit
   and `docs/finals_acts_smoke.md` §4.7 says to read the first run's log. `finals-acts`
   is deliberately **not** in `promote`'s `needs` — the same check gates promotion from
   inside the `gates` job.

The reviewer also confirmed, so it need not be re-litigated: the act/tab correction
holds; the Korean WC-008 pattern is not vacuous (it fires at `018dd78`'s
`JUDGE_QA.md:651` and the old panel `:68`, silent on both replacements); fonts and the
tracked poster are genuinely fatal; the post-diff hardening weakens nothing — it proved
this by neutering `enter()` in a temp copy and watching the driver die on the timeout
rather than write four screenshots and pass; and no CHARTER §3 rule is broken.

## Gates

`gates.py --mode full` exit **0**, ALL GREEN, read unpiped. `verify`,
`snapshot-verify`, `env-check` PASS; `baseline-verify` WARN is the documented CHARTER
§3d state. GitHub `auto-gates` run **216** at `018dd78` is `success`, so no CHARTER §4b
finding. Re-run on the exact commit this ships in, with `--assert-head` and
`--assert-reported` both exiting 0, immediately before the push.

`git rev-parse --is-shallow-repository` = **true**, `rev-list --count HEAD` = **50** in
this clone, measured this lap. **No ancestry or reachability claim is made anywhere in
this lap.**

## Next row

**WFG-139** — the test suite reaches the network, measured nine consecutive times.
Then WFG-167 (42 Q&A cards, zero on responsibility). New this lap: **WFG-169** (the
four view tabs still have no driver — what critic #36 actually measured) and
**WFG-170** (the booth's real control is the keyboard: `BOOTH_SETUP.md:218` makes **G**
the procedure between judges, and this driver presses the mouse only).

R1 is **not** ticked here: readiness lines are the critic's to tick with its own
evidence (CHARTER §11), and the next critic has the artifact to do it with.

## In plain terms

- **What changed for the project.** Two things. Your Q&A card told you to say
  something about a rival system that the same card, twenty lines below, tells you
  never to say — and the same sentence was on a printed panel too. Both now say only
  what you actually checked. And for the first time a test opens your finals screen in
  a real browser, presses the button, and proves all four acts of your demo really
  advance — with four screenshots you can look at.
- **Why it matters to the judges or the science.** A judge who has used that rival
  system could have caught you overclaiming, and that costs trust in everything else
  you say. The screen test closes the last open item on readiness line R1: until today,
  "the demo works" rested on one hand-run from two days ago.
- **What you should do.** Nothing is needed. If you want thirty seconds of it: open
  `docs/auto/finals/act_screenshots/20260907T1825Z/act4.png` and check that the last
  slide of your demo looks the way you want a judge to see it.



## Gates

**ALL GREEN** · mode `full` · head `6d1d730` · 2026-09-07T18:55:09Z · current at `6d1d730`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 19.2 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.1 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 0.4 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 260.7 s | 1686 passed, 56 skipped, 2 xfailed, 52 warnings in 259.75s (0:04:19) |

## Commits since the previous report (7cc4eb70f40825a594056f7101a3d646e0b8e41b..HEAD)

- 6d1d730 WFG-166 + WFG-009: the card that forbade its own sentence, and the first test that presses a button
- e2eafc2 claim WFG-166 with WFG-009 (20260907T1820Z)
- 018dd78 critic #36: closing R1's second clause left R1 blocked by a row parked behind R1

## Needs a human (17 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09)
- NH-036 [DECISION] One critic lap told the next one not to edit a file, and that is what kept a false sentence in front of a judge for a window (by 2026-09-10)
- NH-037 [DECISION] The paper's word proxy now stops it a thousand words before your 25-page rule (by 2026-09-10)
- NH-038 [DECISION] Your "product first" rule has spent the last three dev laps on documents, and the readiness line it was written to protect has not moved in five critic laps (by 2026-09-09)
- NH-039 [DECISION] The national wildfire-spread system's manual is an 18 MB PDF the sandbox could not fetch, and one of you can (by 2026-09-12)
- NH-040 [FYI] A critic lap pushed one commit past a red `--assert-reported`, and it is telling you rather than hiding it
- NH-041 [FYI] This lap sent you an email containing only the word PLACEHOLDER, and could not take it back
- NH-042 [DECISION] Two of your own rules collide whenever a withdrawn claim lives in a frozen artifact, and this week they collided three times (by 2026-09-10)
- NH-043 [DECISION] A gate your loop built this morning will go red about twice a day, and the charter tells the lap that meets it to stop working (by 2026-09-09)
- NH-044 [DECISION] The claim the paper just retracted is still live on the page the paper cites for it (by 2026-09-09)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
