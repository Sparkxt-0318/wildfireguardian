# WildfireGuardian autoloop · critic · 2026-09-04T1416Z

| | |
|---|---|
| when | 2026-09-04 14:16 UTC · 2026-09-04 23:16 KST |
| branch / head | `auto/dev` / `83f49bc` |
| kind | critic |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 7, done: 12, parked: 1, todo: 43 |

## What happened this lap

# critic #11 — the dataset labelled 영덕 is 45 km outside 영덕, and a passing test says otherwise

Window `3a70e16..83f49bc` on `auto/dev` (5 commits of substance in 12; the rest are reports
and images). Gates re-run independently at HEAD: **ALL GREEN**, `1367 passed, 62 skipped`.

## Your decisions, applied

**Nothing new arrived by email or on the pull request this lap.** The mailbox search CHARTER
§6 specifies (`from:siyeong0318@gmail.com subject:"WildfireGuardian autoloop" newer_than:14d`)
returns **31 threads**, every one holding exactly one message — the loop's own report; the
newest thread was fetched in full rather than read from the search preview. PR #31 has **zero**
comments. Nothing to apply, so `decisions.py` was not run.

That is no longer the finding it was for eight laps. The author answered **twelve** entries on
2026-09-04 through the channel they chose in NH-020 (an interactive Claude Code session on the
laptop, answered with buttons); `docs/auto/decisions_seen.json` records all twelve, and only
**four** NEEDS_HUMAN entries are open, two of them FYI. NH-020's own finding is closed.

One of those twelve shapes what this lap says about the schedule, so it is quoted here:

> **NH-021** · closed 2026-09-04 · channel: Claude Code session (AskUserQuestion on the laptop)
> · verbatim: *"Do WFG-062 now (the withdrawn-claims registry gate first; booth rows resume
> after)."*

Consequence, applied in this lap's scoring: WFG-003, WFG-037 and WFG-036 are behind a gate row
**by the author's decision**, taken against exactly that trade-off written out for them. Three
previous critic laps called the resulting hold on 구현 및 유용성 「the single most overdue thing
in the sprint」. That adjective is withdrawn in `SCORECARD.md` this window. The consequence is
still scored; the accusation is not.

## The findings, ranked

### F54 · CRITICAL · `data/processed/external/juso_yeongdeok/` is another county

`scripts/extract_juso_yeongdeok.py:32` reads `SIGUNGU = "47920"  # 경상북도 영덕군`. It is not.

Measured this lap by reading coordinates out of the eight committed GeoJSON files: all 239
points fall at **128.65–129.15 E / 36.78–37.05 N**. This repository's own canonical 영덕 box,
the one the router and the forecast run on, is **(129.25, 36.30, 129.55, 36.60)**
(`config/default.yaml:83`, `regions.lookup('yeongdeok_2025').bbox_wgs84`). **The two do not
overlap on either axis** — about 45 km apart diagonally. No external source was needed; the
answer was already committed here.

Two tells were inside the artifact and were written up as facts about 영덕:

1. 영덕 is an East Sea coastal county. Not one point is east of 129.15 E.
2. `samul_eqwav_point` — 지진해일긴급대피장소, tsunami evacuation sites — returned **zero
   rows**, and `docs/juso_yeongdeok.md:12` records that as a property of 영덕. A coastal county
   has them; a landlocked one has none. **That zero was the evidence and it was filed as data.**
   The centroid (36.915 N, 128.871 E) sits beside 봉화읍.

**Carried by:** 8 `juso_yeongdeok_*_count` registry keys, each with `scope: 영덕군 · …` and a
`derivation` naming 「sigungu code 47920 (영덕군)」; `docs/juso_yeongdeok.md`; the 2026-09-04
annotations on NH-005 and NH-012; and WFG-073 / WFG-074, which would have put these points into
the router as 영덕 refuges and 119 depots — both now `blocked(WFG-075)` by this lap.

**Not carried by**, checked rather than assumed: `README.md`, `web/finals.html`,
`paper/manuscript.md`, `docs/auto/JUDGE_QA.md`. **Nothing at the booth is wrong today.**

**And a green test pins it:** `tests/test_juso_yeongdeok.py:11` asserts
`man["sigungu_cd"] == "47920"`. All 1367 tests pass; `make verify`, `snapshot-verify` and
`env-check` pass; the artifact manifest was rebuilt and the baseline deliberately re-frozen,
both correctly and both stated in the commit message. Every mechanism worked as designed, on
the wrong county.

Filed: **WFG-075** (containment, agent-doable) and **NH-022** (the re-cut — `data/raw/juso/` is
git-ignored and laptop-only, so the loop cannot do it).

### F54-root · the objection this lap puts to the loop

**Every gate here checks that a number matches its artifact. None checks that the artifact is of
the thing its label names.** WFG-049 bound prose to a registry; WFG-062 and WFG-071 are about
claim sentences and external figures. All three constrain a *document* against a *number*. F54
is the first defect in eleven windows where the document and the number agree perfectly and
both are wrong about the world. Nine laps of sourcing discipline — agency, as-of date, scope,
URL, forbidden phrasings, digests — and the one field none of it constrains is **scope**,
because scope is prose a lap typed.

**Cheapest test, no external source required:** for every artifact whose name or registry
`scope` names a region this repository knows, assert its geometry lies inside
`regions.lookup(<region>).bbox_wgs84` plus a stated, committed buffer. One function, offline.
It catches this, the next mis-keyed 시군구 code, and any wrong-CRS or wrong-`.prj` assignment —
including the EPSG:5179 inference `docs/juso_yeongdeok.md:26` honestly labels as an inference.
Filed **WFG-076, P0**, with NH-021's standard attached: publish the catch rate against a
mutation set its author did not write. It is the geometric sibling of WFG-071; take them
together.

### F55 · P1 · two backlog rows were filed under IDs already taken

`3fdb888`'s commit message and `reports/2026-09-04T1318Z-manual.md` both say 「WFG-072
(designated sites), WFG-073 (agency depots)」. The rows in the table are **WFG-073** and
**WFG-074**; WFG-072 is the English-claim-rule row filed by the 1310Z cloud lap in the same
half hour. The table was renumbered, the records were not. Third instance of the ID-collision
class in NH-016's critic #7 note, first where the surviving record is wrong rather than merely
expensive. Recorded, not filed as a row, while WFG-062 is the author's chosen next job.

### F56 · P0 · WFG-067, fourth window

`web/finals.html` still carries `"git":"a562045"`; `git cat-file -t a562045` still answers
`fatal: Not a valid object name` in this fresh clone. First line of the panel a judge is
invited to verify the build with, on the one ticked readiness line for the finals screen. One
rebuild plus a one-line `git cat-file -e` gate, outliving four critic laps.

### F57 · P0 · WFG-057, sixth window, and it is disabling this routine's own step 3

`docs/auto/JUDGE_QA.md:17-23` tells the student **33 / 14 / 13 / 6**; the file holds **41 / 15
/ 19 / 7**, counted again this lap. Critic #9 ruled no question may be added while the header
lies. So for a **third** consecutive lap the judge drill's output had to go to backlog rows
instead of `JUDGE_QA` entries marked 「근거 없음」, which is not what CHARTER §4 asks for. A P0
that disables a daily check costs more than its diff.

### F58 · FYI · the only lap in this window that shipped data had no independent reviewer

Every cloud dev report in the 24 h window carries `Reviewed by:` except `0401Z-dev.md`. The
three **manual** reports carry none, and manual laps are not dev laps, so no rule was broken.
But the 1318Z manual lap shipped F54 and was the only lap in the window touching data, a
script, the registry and the tests at once. `LOOP_CONFIG.json` → `review: subagent` reaches dev
laps only, and a laptop lap is exactly where the author's own external data enters. The
structural answer is WFG-076, which needs no reviewer at all.

## What went right, and should not be lost under F54

**`paper/figures/F8_routing_map.png` is the best judge-facing graphic this repository has
produced.** Opened and read this lap. Panel (a) hillshade + P(ignite) at 720 min + the 0.5
isolines + ignition + the walk-network rectangle; panel (b) the network, 50 refuges, all 458
scanned origins classed exactly as the committed artifact, three worked origins with the
fire-blind route against the forecast-aware one, graticule, two scale bars, boxed legend.
Verified against artifacts: 414 + 42 + 2 = 458, and `n_refuges` in
`data/processed/rescue_routing.json` is **50**, matching the caption's 「50 OSM POIs」. The
routes are **recomputed at figure time by the repository's own router** and the recomputed
partition equals the committed one, so it is a re-derivation, not an illustration. And panel
(a) **draws the project's worst limitation instead of describing it** — the hazard core runs
well west of the walk box, which is the 32.6 % coverage caveat in one glance. It is also drawn
over the correct 영덕, coastline on the right, in the same window that committed a 영덕 dataset
45 km inland.

## Scorecard deltas (`SCORECARD.md`, appended at `83f49bc`)

| track | row | 3a70e16 | 83f49bc | why |
|---|---|---:|---:|---|
| B | 연구 목적 | 16 | 16 | nothing touched the problem, the population or the frame |
| B | 설계와 방법론 | 15 | 15 | nothing touched a model, split, metric, arm, coupling or protocol |
| B | 데이터 수집·분석·해석 | 19 | **17** | F54: full reproducibility machinery around a mis-labelled artifact, with a passing test enforcing it. -2 not -1 because the suite certifies the error; not -3 because nothing judge-facing prints it, the defect is one constant, and F8 is real 해석 work |
| B | 창의성 | 15 | 15 | held |
| B | 제출 자료 | 17 | **18** | F8. Stops at 18 on WFG-067 (4th window) and WFG-057 (6th) |
| B | **total** | **82** | **81** | |
| A | all five | 16/15/14/15/13 | unchanged | twelfth window without a `web/` commit, now correctly scored as the author's NH-021 decision rather than slippage; F54 does not reach this track because nothing on the screen consumes it — WFG-073/074 would have, and are blocked |
| A | **total** | **73** | **73** | |

## Gates

`gates.py --mode full` exits **0** at HEAD in a fresh cloud sandbox: `1367 passed, 62 skipped`
in 196 s, COLD (six SRTM-gated tests skipped, WFG-039). Against critic #10's cold `1342 / 62`
at `3a70e16`: **+25 passed, skips unchanged**, fifth comparable window. `verify`,
`snapshot-verify`, `env-check` PASS; `baseline-verify` WARN, expected off-laptop, `hard:
false`, eleventh window and still not a finding.

Sandbox note: `bootstrap.sh` failed twice on PyPI read timeouts before succeeding on the third
attempt. Environmental, not a defect; recorded because a lap that reads a bootstrap failure as
a red baseline would stop for nothing.

## In plain terms

**What the judges would see today.** Almost the same strong project as yesterday, plus one
genuinely beautiful new map in your paper — the terrain, the predicted fire, the walking
network, and three households' routes, with the map honestly showing that the fire spreads
beyond the area you measured. But one of the two files you sent yesterday was cut with the
wrong county code: the "영덕" evacuation sites and 119 stations now in the repository are
actually about 45 km away, in a different county. Nothing a judge sees is wrong, because none
of those numbers has reached the README, the screen, the paper or your Q&A cards.

**What changed since yesterday.** The paper gained its routing map (F8), your two downloads
were ingested, and twelve of your decisions from the laptop session are now recorded and acted
on. Against that, the loop found that its own safety net has a hole: every check it runs asks
「does this number match its file?」 and none asks 「is this file actually about the place it
says?」 — which is exactly how a wrong county passed 1367 green tests, one of which was
asserting the wrong code.

**What you should do.** One thing, on the laptop, when you have ten minutes: look up 영덕군's
시군구 code on 행정표준코드 (code.go.kr) — please read it off the record rather than typing one
from memory — put it into `scripts/extract_juso_yeongdeok.py`, re-run the extractor, and check
the new points land between 129.25 and 129.55 East. Then answer NH-022. Everything else in the
repository is fine and the loop is fixing its own half without you.



## Gates

**ALL GREEN** · mode `full` · head `83f49bc` · 2026-09-04T14:05:01Z · current at `83f49bc`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 14.2 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.1 s | make: *** [Makefile:152: baseline-verify] Error 1 |
| snapshot-verify | PASS | 1.7 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 197.1 s | 1367 passed, 62 skipped, 52 warnings in 195.98s (0:03:15) |

## Commits since the previous report (e923b3fc010b5d891c81717ff9aa4e4fae76a341..HEAD)

- 83f49bc F8: the canonical Yeongdeok case as a routing map in the Moreno look, cited in the manuscript (WFG-060 first half)
- 3fdb888 the author's 주소정보누리집 downloads: the 영덕 subset lands as a committed artifact with a manifest and registered counts (WFG-072/073 filed)
- 41dc45d report: rebased twice mid-lap, and the second rebase is why the email is right

## Needs a human (5 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-022 [DECISION] The 영덕 dataset you sent was cut with the wrong county code, and only your laptop can re-cut it (by 2026-09-08)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
