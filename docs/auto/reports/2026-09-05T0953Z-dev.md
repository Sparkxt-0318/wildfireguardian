# WildfireGuardian autoloop · dev · 2026-09-05T0953Z

| | |
|---|---|
| when | 2026-09-05 09:53 UTC · 2026-09-05 18:53 KST |
| branch / head | `auto/dev` / `57b7b59` |
| kind | dev |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 6, done: 16, parked: 1, todo: 60 |

## What happened this lap

# dev lap 2026-09-05T0920Z — WFG-103

Row claimed: **WFG-103** (P0, KCF, `docs/auto/DIRECTION.md` row 1 and critic #17's one
`fix-before-next-row` item). Claim commit `a063e51`, work commit `92366cb`, rebased onto the paper lap's two commits.

## Your decisions, applied

**Nothing new this lap.** The Gmail search
`from:siyeong0318@gmail.com subject:"WildfireGuardian autoloop" newer_than:14d` returns 30
threads, every one of them a single message and every one of them the loop's own outbound
report — no reply from you in any thread. PR #31 carries no comment at all, so nothing in
`NH-###: …` form on that channel either. `docs/auto/decisions_seen.json` is unchanged and
`decisions.py` was not invoked. No reply line was left unmapped, because there were no
reply lines.

**Still waiting on you:** NH-005, NH-014, NH-025, NH-026, NH-027, NH-028 — six, not the
five this section said before the rebase brought the paper lap's NH-028 in. NH-027 is critic
#17's, opened yesterday: whether the present-perimeter routing arm moves before the finals.

## What the lap did

**The one sentence, and a second copy of it the finding did not have.**

`docs/auto/DEMO_SCRIPT_5MIN.md` 3막 — the segment the document itself calls 「이 작품의
전부」 — was spoken to five judges as:

> **STATIC VIEW.** 거리 기준 직행 경로입니다. 지금 이 순간만 보는 지도라면 이 길을 권했을 겁니다.

The arm that route comes from is fire-blind. I re-read both citations at their exact lines
rather than trusting the finding: `src/wildfireguardian/routing/evacuation.py:270` is
`"""Fire-blind shortest path to the nearest shelter, then scored against the hazard."""`
and `docs/real_roads_real_hazard.md:50` is 「**naive** — the fire-blind shortest walk to the
nearest refuge (the status quo)」. Both verbatim. It now says:

> **STATIC VIEW.** 거리 기준 직행 경로입니다. 불을 전혀 보지 않는 지도가 그리는 길, 이 도구가 없을 때의 기준선입니다.

**The same false sentence was on the judged screen, and critic #17 did not find it.**
`web/finals.html` builds the STATIC VIEW caption from two branches. The `!later` branch
(regions where the direct route is inside the hazard field already at t+0) was already
correct — 「화재를 모르는 지도」 / "A fire-blind map would still draw it". The `later`
branch, which is the common case and the one 의성·안동 hits, said 「지도가 지금 이 순간만
본다면, 이 경로를 권했을 것입니다」 / "A map that only sees the present would recommend
it." Both branches now describe the same arm the same way in both languages. That makes
this a fix on **R1's surface as well as R4's**, and it is the reason the row was not
literally one sentence.

I did **not** enlarge the row into the arm study (WFG-033, P2; scope question NH-027) and
did not write WFG-104's Q&A card. What I did add is a ⚠ note under 3막 that (a) tells a
later lap not to restore the stronger-sounding phrase and why, (b) states that the
present-perimeter comparison has not been run, and (c) says plainly that **no card for that
question exists yet** in §2 or `docs/auto/JUDGE_QA.md`, that WFG-104 writes it, and gives
the student the honest sentence to say until it does. I verified (c) myself by searching
the bank for naive / perimeter / buffer / 기준선 / 베이스라인 / 대조군 / fire-blind: the
only hit is Q23, which is about Dijkstra's optimality, not about the baseline.

**The pace gate went red on purpose, and that is the mechanism working.**
`docs/demo_script_pace.md` predicted this one lap earlier: 「Adding or removing a spoken
sentence therefore turns the gate red on purpose.」 The new sentence is 8 syllables longer.

| | before (tag `20260905t0625z`) | after (tag `20260905t0947z`) |
|---|---:|---:|
| spoken syllables | 1,684 | **1,692** |
| allocation (s) | 29 / 44 / 50 / 60 / 59 / 58 | **28 / 44 / 50 / 61 / 59 / 58** |
| rate spread | 1.02x | **1.03x** |

Only 3막 (+1 s) and 도입 (−1 s) move; the spread widening to 1.03x is inside what
whole-second allocation alone does (the test's band is 5 %, its spread ceiling 1.10x).
**The count was run after the sentence was written, not before** — this was the root
objection below, and the sentence was chosen for what it says rather than for what it costs
the arithmetic. New artifact `data/processed/demo_script_pace/pace_20260905T0947Z.json`
(the script refuses to overwrite), keys registered additively under a new tag with the two
earlier tags left standing as the record, `TAG` moved in the test, the six segment headers
and their cumulative brackets re-allocated, and both prose copies of the budget updated
(§1 of the script, the table in `docs/demo_script_pace.md`). `docs/artifact_manifest.json`
rebuilt; `release/kcf-finals-2026/MANIFEST.json` re-derived for the changed screen (16
files).

**A defect the re-measure exposed in its own test.**
`tests/test_demo_script_pace.py::test_the_artifact_the_registry_points_at_is_committed_and_current`
selected the artifact with `sorted(PACE_DIR.glob("pace_*.json"))[-1]`. That is
`pace_before_039a0de.json` — `b` sorts after every `pace_2026…` name — so the test was
checking the **before** artifact against the live script. It was green only because
1,684 = 1,684 held across WFG-100 by coincidence. The first edit that moved the count is
the first edit that would have turned it red, and it would have named the wrong file while
doing so. It now selects by `TAG`, which is what the registry keys are built from, and the
docstring says why. Written up in `docs/demo_script_pace.md` under 「The measurements, in
order」.

## Root objection (`hate`), recorded before building

**The sentence lives inside a gate that pins its syllable count, so the cheapest path
through this row is to write a replacement of exactly the same length — which optimises the
booth's most important sentence for the arithmetic instead of for the judge.** The cheapest
test was procedural, not analytical: write the sentence first, without looking at the
counter, then run the count and pay whatever it costs. It cost 8 syllables, one second
moved from 도입 to 3막, a new artifact, a new registry tag and two prose tables. The
alternative — trimming the new sentence until 1,684 came back — would have been invisible
in the diff and is exactly the failure CHARTER §3.5 names.

## What did not work / what I left alone

- `curl` to `api.github.com` returns 403 from the agent proxy in this sandbox ("GitHub
  access is not enabled for this session"). CHARTER §4b's step-2 check was done through the
  GitHub MCP instead, which works. **The step-2 command in `ROUTINE_PROMPTS.md` is a `curl`
  one-liner and will fail for every lap in this environment** — filed below as a MEMO note
  rather than as a row, because §14b holds loop mechanics behind the readiness lines.
- `docs/auto/KCF_READINESS.md` records WFG-103 as an open defect on R4's ticked line. I did
  not edit it: that file is the critic's, ticked with its own evidence (CHARTER §10), and
  critic #18 will read this commit.
- `baseline-verify` WARNs, `hard: false`, expected off-laptop; eighteenth consecutive
  window and still not a finding.

## Gates

- `--mode quick` at `4d1a596` (lap start): ALL GREEN.
- GitHub's own run (CHARTER §4b), via the GitHub MCP: `auto-gates` run **119 at `4d1a596`
  is `success`**; 118 (`6236c81`) and 117 (`26e200d`) also `success`. No red run stands
  behind a green report. Nothing to reproduce, so no §4b first job this lap.
- `--mode full` mid-lap caught the new artifact **untracked** (`NOT-IN-REPO`, both new
  keys) and then a stale `docs/artifact_manifest.json`. Both were the gate doing its job on
  this lap's own work; staged by explicit path (CHARTER §3c, no `git add -A`) and rebuilt.
- **Read the gate block below with this caveat, which is structural and not a slip.**
  `report.py` writes that block at the moment the report is generated, so it names the head
  this report was generated at — not the commit the report itself travels in, because that
  commit does not exist yet. The last three laps each shipped a follow-up commit correcting
  it (`1f69c10`, `4d1a596`, `7700b7b`), which is a correction that can never converge. This
  lap says it in prose instead: **after the report is committed, `--mode full` is re-run at
  the exact commit being pushed, and `--assert-head` — which exits non-zero unless
  `.auto/gates.json` records a `full` pass at exactly `HEAD` with a clean tree — is what
  certifies the pushed commit.** Both are reported in the lap's final message and neither
  can be true of a red branch. The gate block below is the run before that one.

Reviewed by: subagent (pass): **「The lap edits the very test that constrains its own change,
in the same commit that makes the gate go red, which is the textbook way to fake green: move
`TAG`, loosen the artifact selection, ship. If the selection change were a relaxation rather
than a defect fix, the whole 'the gate went red on purpose' narrative collapses and the
syllable measurement becomes a number confirming only itself.」**

The reviewer did not take my word for it. Its first nail was to **restore the `a063e51`
version of `tests/test_demo_script_pace.py` onto the current tree and run it**: 2 failed, 7
passed — the registry test fails `1684 != 1692` (the gate did go red on the sentence), and
the artifact test fails naming `pace_before_039a0de.json`, which is the defect claimed. It
then observed something I had not: because `b` > `2`, `sorted(glob)[-1]` could **never** have
selected any `pace_2026…` measurement, so the old selection was a constant check against
1,684 regardless of `TAG` — binding to `TAG` is strictly stronger, and `BAND = 0.05` and the
1.10x spread ceiling are untouched. It re-ran the measurement itself, checked
`evacuation.py:270` and `real_roads_real_hazard.md:50` at those exact lines, and went past the
docstring to the implementation: `naive_route` runs `single_source_dijkstra` weighted on
`length_m` / `time_min` only and touches the hazard solely in `_evaluate_path` afterwards, so
「does not see the fire at all」 is true of the code and not only of its comment. It searched
the 41-card bank itself and confirmed no card exists.

Its `mandela` finding, which I am recording because it is the honest one: **the syllable
metric is self-confirming** — the same script authors nothing but counts what the loop wrote,
scores its own allocation, and no stopwatch or human rate ever enters. That leakage is
disclosed rather than hidden (the doc's 「What this does NOT show」, the registry caveat, R12 /
NH-014, and WFG-105's queued external comparison), and the reviewer's evidence that the gate
was not reshaped to fit this change is that **the escape hatch was written one lap before the
change that used it**: 「Re-measuring after an edit」 and the `TAG` comment both already
existed at `a063e51`.

Two non-blocking flags it raised, both acted on: (1) `origin/auto/dev` had moved two commits
ahead while I built — see below; (2) the new clause 「이 도구가 없을 때의 기준선입니다」 is a
counterfactual the repository labels (`real_roads_real_hazard.md:50` calls `naive` 「the status
quo」) but has never measured. It passes on the repository's own committed wording and is
qualified by the ⚠ note two lines below it, and **WFG-104's card should own that sentence
rather than the script's stage direction** — carried into that row.

## The paper lap made the same correction, independently, while this lap ran

The rebase brought in `872ee14` 「the abstract gave the forecast credit for what a fire-blind
baseline never withheld」 — `wfg-autoloop-paper` reading the same critic finding and fixing the
same claim in `paper/manuscript.md`. Two routines converged on one defect from opposite ends
of the repository and neither could see the other, which is the collision MEMO 2026-09-05
already names for the ci-red routine. Nothing conflicted here (different files), so the
duplication cost nothing this time; it is the second instance of the same structural gap and
is recorded as such.

## Next row

**WFG-037** (P0, `docs/auto/finals/BOOTH_SETUP.md`), which `docs/auto/DIRECTION.md` puts
next and which critic #17 named as its falsifiable test for critic #18: *「if
`docs/auto/finals/BOOTH_SETUP.md` does not exist by then, WFG-084's cap becomes the rule.」*
WFG-104 (the T0 card) is the other P0 that this lap deliberately left for its own lap.

## In plain terms

- **What changed for the project.** The most important sentence of your five-minute demo
  said you were comparing your route against "a map that only sees the fire right now". You
  are not — you are comparing it against a map that does not look at the fire at all. Both
  the script and the finals screen now say that, in Korean and English.
- **Why it matters to the judges or the science.** The old sentence made your opponent
  sound smarter than it is, and your headline number (24.73 %) was being heard against that
  stronger-sounding opponent. Saying plainly what the comparison is protects the number: a
  judge who has to dig this out marks you down, a judge who hears it from you marks you up.
- **What you should do.** Nothing for this change. **Six** decisions are waiting on you
  (NH-005, NH-014, NH-025, NH-026, NH-027, NH-028) — NH-027 is the one that matters most
  here: whether to run the "current fire only" comparison before the competition or after
  it. NH-028 is the paper routine's, opened while this lap was building; this line said
  five until the rebase brought it in, and the count is corrected here rather than shipped,
  because a report whose prose disagrees with the Decisions block beside it is the exact
  defect `26e200d` fixed one lap ago.



## Gates

**ALL GREEN** · mode `full` · head `57b7b59` · 2026-09-05T09:53:54Z · current at `57b7b59`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 21.0 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.2 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 0.6 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.2 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 316.2 s | 1490 passed, 56 skipped, 52 warnings in 314.66s (0:05:14) |

## Commits since the previous report (4d1a5967524aa6e1d1020e285ad4029cb80f4d58..HEAD)

- 57b7b59 a finding is the size of the reader who found it, and a test that picks its subject by sort order
- 92366cb WFG-103: the core act now names its opponent, and the opponent does not see the fire
- 7700b7b the report and its email named the head they were generated at, not the one they ship in
- 872ee14 the abstract gave the forecast credit for what a fire-blind baseline never withheld
- a063e51 claim WFG-103 (20260905T0920Z)

## Needs a human (8 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-025 [DECISION] `Main` cannot follow the green commit until you change one GitHub setting, and until you do, every green push emails you a red run (by 2026-09-08)
- NH-026 [DECISION] One of the five routines pushes on a gate that does not run the test suite, and its prompt is the one the repository cannot show you (by 2026-09-08)
- NH-027 [DECISION] The one experiment that would test the project's headline against a fair opponent is scheduled for after the competition (by 2026-09-08)
- NH-028 [DECISION] The manuscript is full, and three laps have recorded that as a note rather than asked you (by 2026-09-10)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
