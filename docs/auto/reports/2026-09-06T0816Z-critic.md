# WildfireGuardian autoloop · critic · 2026-09-06T0816Z

| | |
|---|---|
| when | 2026-09-06 08:16 UTC · 2026-09-06 17:16 KST |
| branch / head | `auto/dev` / **`ef61e9b`** (pushed; the head this report reviewed is `b70e464`, and the generator stamps that one) |
| gates | `--mode full` **ALL GREEN** re-run at the pushed commit `ef61e9b` (`1568 passed, 56 skipped`, 267.3 s); `--assert-head` and `--assert-reported` both green before the push |
| kind | critic |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 7, done: 16, parked: 1, todo: 74 |

## What happened this lap

Critic lap #25. Window `91d3e05..b70e464` on `auto/dev` — six commits, two of them substantive
(`3e92b69` and `5f927b0`, the 06:17Z dev lap's WFG-007). I changed no code and no artifact; this
lap wrote only under `docs/auto/`.

Reviewed by: this routine is the independent reviewer. Every dev report in the last 24 h carries
its own `Reviewed by:` line, and the 06:17Z lap's is the strongest this loop has produced — its
subagent returned `block` on a number no artifact had produced, the lap fixed it in the script,
the doc, the manifest and the report body, then found it had shipped the old figure into the one
paragraph the author reads and corrected that too, in place, as an annotation rather than a
rewrite.

## Your decisions, applied

**Nothing new.** The Gmail search `from:siyeong0318@gmail.com subject:"WildfireGuardian autoloop"
newer_than:14d` returns thirty threads and every one of them is a report this loop sent, each
holding a single message with no reply on it; `docs/auto/decisions_seen.json` is unchanged, so
`scripts/auto/decisions.py` had nothing to parse or apply. The 2026-09-06 decisions (NH-029, NH-030,
NH-031) came through the laptop session at `4d705df` and were already applied before this window.
No `NH-###:` line appeared on PR #31 either. Four decisions are still waiting on you and they are
listed at the end of this report.

## What I verified rather than read

- `gates.py --mode full` **ALL GREEN** at `b70e464`: `1562 passed, 62 skipped`, cold, 298.9 s.
  Against critic #24's cold `1545 / 62` at `91d3e05`: **+17 passed**, skips unchanged, which is the
  15 new `tests/test_printables.py` cases plus 2 in `tests/test_fair_opponent_line.py`. The
  `baseline-verify` WARN is the documented NH-029 behaviour and is not a failure.
- **GitHub's own runs (CHARTER §4b).** Read through the GitHub MCP, not `curl` (WFG-119 records the
  403). The twenty most recent `auto-gates` runs on `auto/dev`, numbers 140 to 163, spanning
  2026-09-05T20:21Z to 2026-09-06T07:25Z: **nineteen `success`, one `cancelled`** (`9ebf5a5`, critic
  #24's own push, superseded four minutes later by `3656bea`, which is green). **No red run sits
  behind a green report. There is no gate finding and no CHARTER §4b finding this lap.**
- Every dev report of the last 24 h records `Reviewed by:` (1542Z, 2132Z, 0059Z, 0711Z, and the
  0323Z paper lap).
- Critic #24's `fix-before-next-row` item, **WFG-127 (i), is cleared.** Checked on disk at
  `b70e464`: `docs/fair_opponent_line.md` §3 now says the sweep 「separates a spike at 1 km from a
  plateau an operator could aim at in **neither** direction, and this file asserts neither」, with
  the withdrawn wording described rather than quoted so the gate can ban its exact spellings;
  `DEMO_SCRIPT_5MIN.md` 3막 now says 「좋았던 폭 주변이 뾰족한 봉우리인지 넓은 고원인지 이 실험은
  가리지 못합니다」. An item survived one lap unspent and was then spent, which answers the question
  #24 held it open to ask: the item was fine and the mechanism works.

## Critic #24's falsifiable test, answered

#24 left two branches on `docs/auto/KCF_READINESS.md` R7. **Branch (1) is what happened.**
`docs/auto/finals/printables/WFG_printables_20260906T0620Z.pdf` exists: 29 A4 pages, 395,927 bytes,
built from four committed Markdown documents in the screens' own IBM Plex Sans KR with no new
dependency, byte-identical on rebuild, and a manifest carrying the sha256 of every source and every
font. I re-hashed all seven against the tree: **all seven current**, so the paper is not already
stale. WFG-007's agent half is finished and R7 has an object for the first time in nine days.

Per #24's instruction I am not writing about queue position again. The nine-day argument — was it
priority, or was it the lock — is settled as **both**, and both are now paid.

## Findings, ranked

### 1. `fix-before-next-row`: the judged screen prints two wrong counts, for the sixteenth window, and it is now on a ten-hour clock

`web/finals.html:434` prints `n_entries` **326** and `n_reproducible` **268**. `docs/NUMBERS.json`
holds **383** and **325** — I counted both in one process rather than taking either from a report.
The screen was correct when built (`built_utc` 2026-09-05 15:22 UTC, stamp `5f9a3b8`) and went stale
six hours later when WFG-114 registered 57 `pp_uiseong_*` keys.

It qualifies under CHARTER §14b as a judge-facing surface twice over: it is the demo screen, and
`release/kcf-finals-2026/MANIFEST.json` ships `web/finals.html` among its 17 files, so the stale pair
is on the USB stick too. It needs no decision from anyone: `scripts/build_finals.py:629-630` derives
both counts from the registry, and WFG-109's closure records this loop already running the repair in
this sandbox with the only changed line being the payload.

    make finals        PYTHON=.auto/venv/bin/python
    make finals-bundle UPDATE=1 PYTHON=.auto/venv/bin/python

**What is new today is the deadline.** WFG-119 predicted the screen's stamp would cross the sandbox's
shallow boundary 「during 2026-09-06」. Measured at `b70e464`: `5f9a3b8` is **29** commits behind
`HEAD` (23 at critic #24), the branch took **51** commits in my 24 h, and
`git rev-parse --is-shallow-repository` is `true` at depth **50**. That is roughly **ten hours** of
headroom. After it, `test_the_integrity_panel_names_a_commit_this_repository_has` goes RED in every
sandbox on a screen that is not wrong while CI at `fetch-depth: 0` stays green, and CHARTER §9 sends
the first lap that hits it to `auto/red/` instead of building. `make finals` resets that clock as a
side effect. Rows: **WFG-113**, then **WFG-115** (`41498ef` is still not an ancestor on a deepened
250-commit clone, and `make finals` does **not** touch it), then **WFG-117**.

### 2. The root objection: the booth kit was measured against its own sources and never against its own definition of done

`docs/auto/KCF_READINESS.md` **R7** enumerates five printables: 「evidence sheet (A4),
**reconciliation sheet**, related-work and SFTD059T differentiation panel, booth checklist, 29
dispatch sheets sample」. `scripts/build_printables.py:97-101` lists four sources. **The overlap is
one** — the booth checklist. Three of the four documents in the PDF are not on R7's list; three of
R7's five are not in the PDF.

That alone would be a scope note. What makes it the objection is the sentence the build wrote about
the gap. `manifest_20260906T0620Z.json` → `what_this_does_not_show` says the missing items 「do not
exist yet」, and `docs/auto/BACKLOG.md` repeated it in WFG-007's own status cell. **WFG-018 is
`done(20260903T0653Z)` in that same table.** Its artifact is `docs/submission_reconciliation.md`:
13,702 bytes of Korean prose, the file `docs/auto/KCF_READINESS.md` **R6's tick is written on**,
whose own fourth line reads 「부스에서 심사위원이 ... 물었을 때 그 자리에서 펴는 종이. **인쇄본은
양면 한 장입니다**」. It was written to be printed, and the booth PDF says it does not exist.

The judge drill is what makes this concrete rather than tidy. `docs/auto/JUDGE_QA.md` **Q30** is a T0
question, and its answer ends 「그리고 대조표를, 종이 없이, 그 순서대로 읊습니다」; its drill table at
`:987` lists that sheet as item 2. So the repository says three different things about one piece of
paper: the sheet says print it, the drill says recite it without paper, and the printables say it
does not exist. A judge who asks 「제출본 숫자와 지금 값이 다르면 어느 종이를 펴십니까」 gets a
different answer from each.

**The cheapest test is one minute with no run:** open `scripts/build_printables.py:97-101` beside
R7's sentence and count the overlap. It returns 1 of 5. Nothing in the repository reads those two
lists together, which is why an otherwise excellent build — fifteen new tests, a three-layer font
gate, an independent reviewer that returned `block` — could declare a committed artifact absent and
stay green.

Filed as **WFG-130** (P0, minutes). Its done-when offers both repairs, because either closes the gap
and the choice belongs to a lap: bind the builder to R7, or correct R7 to the documents the booth
actually needs. I also added **Q39** to the Q&A bank with a ⚠ 근거 없음 marker and two ❌ sentences,
so that until the row closes the student does not promise a judge a paper they are not holding.

### 3. A P0 deliverable depends on a package no manifest declares

`docs/printables.md` §2 says so in its own voice and it is correct. `matplotlib==3.11.1` and
`fonttools==4.63.0` are pinned in `requirements.txt` (`:44`, `:76`); **`brotli` appears in neither
that file nor `pyproject.toml`** — verified by grep, it occurs only at `scripts/auto/bootstrap.sh:69-70`
(「brotli, which no pin pulls in; conda's wfg311 had it by accident」). fontTools needs it to open the
vendored `.woff2` faces, so without it `make printables` cannot build the booth PDF and
`tests/test_screen_checks.py`'s woff2 reads fail. This is three days old — `docs/auto/BACKLOG.md:179`
records it as cause 1 of the first cloud lap's 17 red items — and it was resolved by teaching
`bootstrap.sh` to install it rather than by declaring it, so `scripts/env_check.py` reports 「the
environment matches requirements.txt」 while the hole stays open. **WFG-131**, P1, parked behind
R1/R3/R7/R8/R9 by CHARTER §14b.

### 4. Information, not a row: one clause in `docs/fair_opponent_line.md` describes the section better than the section is

The file says at `:99-100` that 「the **counts** are convention-dependent like the margin is, **which
is why §3 quotes none of them**」. §3 quotes two, at `:48-49`. Their difference is one of NH-032's two
contested margins.

I am not filing this as a violation and not asking for the numbers to be cut. The gate
(`test_no_contested_margin_reaches_the_booth_script`) is scoped to `DEMO_SCRIPT_5MIN.md`, correctly;
this file is not in the printables PDF, not in the release bundle and not linked from the README; and
the two numbers are the load-bearing evidence for 「a fixed buffer cannot work is **false**」, which is
the honest half of the section and unsupportable without them. What is wrong is only the
self-description, which tells a later reader the section is cleaner than it is, and that is exactly
how a real constraint gets read charitably until it is gone. One clause fixes it. Left to WFG-121's
owner rather than opened as a row.

### Two rows carried from critic #24, both still open, neither a new finding

`docs/multi_region.md:191` still reads 「fire-blind route safe, future-aware router cannot finish in
time」 and `README.md:113` still sends a judge there for 「완전한 분할」 (**WFG-128**, checked unfixed
at `b70e464`). `paper/GAPS.md` G7's test of the headline 42 of 458 is still unrun (**WFG-129**). Both
were filed at `9ebf5a5`, 57 minutes before the 06:17Z lap woke, and that lap took WFG-007 — first in
the table, first on `DIRECTION.md`, and the right choice under CHARTER §14. The rows were readable and
were not read past; that is the queue working.

## Direction check (CHARTER §14, five minutes)

**No row moved. The reorder budget is deliberately unspent for the third critic lap running, and the
reason is not caution: the top two entries on `DIRECTION.md` were both cleared in the window I
reviewed.** WFG-007 shipped its agent half and WFG-127 (i) was spent, so the page's order was right
and the queue below it has not been tested yet. Moving a row now would destroy the only evidence the
next lap has about whether the page or the table is doing the work. The page's date, its three-row
list and its direction note are rewritten; the new order is the judged screen, then WFG-130, then
WFG-128.

**KCF_READINESS lines ticked in the last 24 h: zero, for the second consecutive critic lap.** By the
routine's own rule that is a finding about direction rather than about the product, and here is what
it actually is. It is no longer the queue, and it is no longer the lock. **It is that a row can be
`done` and leave its readiness line ☐ forever, because its done-when and the line's condition
describe different objects.** WFG-007's done-when says 「rehearsal aids + booth checklist」; R7 names
five specific printables. The window built the object nine days of arguing had been about, and the
line still cannot tick. That is WFG-130 and it is one row wide.

Count unchanged at **4 of 11** (R2, R4, R5, R6). R9 now waits on strictly less: the bundle's 17-file
manifest does not carry the printables PDF, and once WFG-130 settles which PDF is the real one,
`make finals-bundle UPDATE=1` is the whole of R9's remaining agent half.

## Scorecard deltas

At `b70e464`, against critic #24 at `91d3e05`:

- **Track B: 84, unchanged** (연구 목적 17 · 설계와 방법론 15 · 데이터 19 · 창의성 15 · 제출 자료 18).
  These five judges are scored on the research and the window's one substantive commit is a booth
  artifact. 설계와 방법론 holds at 15 rather than rising: #24 already scored the width-claim
  correction on the manuscript, and this window ported the same discipline to the two booth documents,
  which completes what 15 was scored for rather than adding to it. 제출 자료 holds at 18 against real
  movement in both directions — up for a deterministic, hash-recorded booth build that documents its
  own failure mode, down because that same build asserts in a committed manifest that a
  `done` artifact does not exist, and the judged screen is in its sixteenth window of wrong counts. A
  row scored on whether the materials say true things about themselves cannot rise on a build that
  got one of its own claims backwards.
- **Track A: 77 → 78**, on 제출 자료 **14 → 15**, the first movement on that row in four windows.
  These five judges see a booth, and until `3e92b69` the booth had no paper. **+1 and not +2** for
  two reasons on the same surface: R7 names five printables and the PDF contains one of them, and the
  manifest declares a committed artifact nonexistent. 구현 및 유용성 holds at **17** for a tenth
  consecutive window and is held down by one screen; **18 is what the two-command repair buys.**

## In plain terms

**What the judges would see today.** For the first time, paper. There is now a 29-page A4 booklet you
can print and hold at the booth: the setup checklist, the five-minute script, the judge Q&A cards and
the detection-floor card, all in the same Korean font as the screens, all built from the repository so
you can prove the paper matches the code. That is the thing nine days of these reports have been
asking for and it exists. Two problems sit next to it. The judged screen still prints 「등록 326 · 재현
가능 268」 when the repository now holds 383 and 325, so a judge who checks finds the screen behind you
disagreeing with your own files. And the booklet is missing the one page your Q&A bank tells you to
open when a judge says 「제출한 숫자와 지금 숫자가 다릅니다」 — the 대조표 — because the build
concluded that page had not been written, when in fact you have had it since 2026-09-03.

**What changed since yesterday.** The 06:17Z lap built the booth printables, and it did it well: no
new software was added, the build is byte-identical when repeated, and it catches the one failure that
would only show up on the paper in a judge's hand (a Korean font that is missing a symbol draws a blank
square silently). The lap before it also fixed the sentence about buffer widths on both booth documents,
so the script no longer claims something the experiment cannot support. All the automatic checks are
green, on this machine and on GitHub, and nothing you decided is waiting unapplied.

**What you should do.** Nothing urgent, and nothing that needs a laptop today. Four decisions are still
open and two of them (NH-032, NH-034) have a 09-08 date on them — they are the same question in two
forms: which version of the 「fair opponent」 experiment the project reports, and what you want said
about the headline number once it does. Until you answer, the booth script says nothing about it, which
is safe but leaves your strongest result unspoken. The rest is the loop's own work: the next lap
rebuilds the screen so its numbers match the files, and then adds the 대조표 to the booklet.



## Gates

**ALL GREEN** · mode `full` · head `b70e464` · 2026-09-06T08:04:02Z · current at `b70e464`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 18.2 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.1 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 1.6 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 278.9 s | 1562 passed, 62 skipped, 52 warnings in 277.04s (0:04:37) |

## Commits since the previous report (3e92b69a920029e93bd70c5c8405751706118a5b..HEAD)

- b70e464 the corrected count had not reached the one section the author reads
- 5f927b0 the reviewer blocked on the one number in the doc that no artifact had produced

## Needs a human (8 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.

---

## Annotation added after this report was generated, before the push

**The gate caught something in my own commit, and what it caught is worth a line.**
`gates.py --mode full` at `e99417d` came back **RED** on `check-number-collisions`, on a row
I did not edit: `docs/auto/BACKLOG.md` WFG-121, where `pp_uiseong_control_late_arrival_earliest_min`
(624.8) and `..._latest_min` (628.2) collided with a bare `3.2` on the same line. The `3.2` is the
section reference 「CHARTER §3.2」 and has been on that row since `a182cc0`; the anchors
`control` / `late` / `uiseong` that pull the two registry keys onto it are the row's own prose
about the present-perimeter control. It is a false positive and always was.

**It went green until now for a reason that is worth writing down:** `scripts/check_number_collisions.py`
excuses a hit when a NARRATIVE marker sits within `MARKER_LOOKBACK = 5` lines above it, and a marker on
an unrelated row two positions up was doing that. Inserting WFG-130 and WFG-131 above WFG-121 moved that
marker to seven lines away and the excuse evaporated. So a row that no one had checked was passing on
an accident of layout, and the accident survived every lap since 2026-09-06T0020Z.

Fixed the way the checker's own message asks for and not by moving my rows out of the way: an explicit
`<!-- collision-ok: 3.2 ... -->` pragma inside WFG-121's last cell, naming the token and saying why the
two numbers are different quantities. The checker's docstring is right that a pragma must sit on the
offending line or the one directly above it, 「a pragma with a five-line reach would quietly excuse
numbers its author never looked at」 — which is exactly what the five-line **marker** reach had just done
to this row. `check_number_collisions.py` now reports 22 marked historical values where it reported 24,
because two of them were never marked on purpose. Re-run green; the `--assert-head` and
`--assert-reported` results below are from the amended commit.

**Not filed as a row.** The lookback constant is deliberate and documented, the fix for this instance is
in the tree, and CHARTER §14b holds loop-hygiene rows behind R1, R3, R7, R8 and R9. Recorded here so the
next lap that meets an unexplained collision on an untouched line knows to look at what moved above it
rather than at what changed on it.
