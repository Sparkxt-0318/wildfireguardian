# WildfireGuardian autoloop · critic · 2026-09-10T1737Z

| | |
|---|---|
| when | 2026-09-10 17:37 UTC · 2026-09-11 02:37 KST |
| branch / head | `auto/dev` / `e59d573` |
| kind | critic |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 9, done: 33, dropped: 1, parked: 1, title-derivable: 1, todo: 115 |

## What happened this lap

Critic lap **#60**. Reviewed head **`71e95ee`**, window 2026-09-09T16:20Z to 2026-09-10T16:44Z:
**61 commits, 46 pushed heads**, 9 dev laps, 4 critic laps, 3 paper laps. This lap changed no code,
no test, no data, no figure and no committed artifact; it wrote only under `docs/auto/`.

⚠ The clone arrived **SHALLOW at 51 commits**, the fourth lap running, which is about the length of
the window itself, so `git log --since` would have returned the whole clone and truncated in silence.
`git fetch --unshallow` was run before anything was counted: `--is-shallow-repository` answers
**false** and `git rev-list --count HEAD` answers **725** at the reviewed head (**726** at this
lap's own commit), so every ancestry statement below is licensed under CHARTER §4. ⚠ That same fetch
is what turned one gate red inside this lap; finding 6 is the whole story and the repair.

## Your decisions, applied

**Nothing new reached me, on either channel, and it is the sixth consecutive lap to say so.** The
Gmail search `from:siyeong0318@gmail.com subject:"WildfireGuardian autoloop" newer_than:14d` returns
**30 threads** on the first page, every one a report this loop sent, and **every one holds exactly
one message**, so no reply is threaded under any of them. **PR #31 has zero comments.** Nothing was
applied with `scripts/auto/decisions.py`, and `docs/auto/decisions_seen.json` is unchanged: its
`applied` list still ends at **NH-031**, closed 2026-09-06 in a Claude Code session on your laptop.
No `NH-###:` line has ever reached the loop by email.

**What is waiting on you, counted at this head rather than inherited.** **22 open entries (21
DECISION + 1 BLOCKER)**, plus 5 open FYI. **13 are at or past their stated date**: NH-032, 034, 045
(09-08); NH-035, 038, 043, 044 (09-09); NH-036, 037, 042, 046, 048, 050 (09-10), where that last
group is due **today** rather than overdue. **NH-049 and NH-051 both come due tomorrow.** ⚠ Two open
entries (**NH-005, NH-014**) carry no date at all and fall out of any date-based count, so the honest
phrasing is 「13 of 22, and 2 undated」. The twenty-second is **NH-055**, filed today.

⚠ **A correction to my own two predecessors, and it is in your favour.** Critic #58 and critic #59
both published 「**NH-046 is now two days past due**」, on `DIRECTION.md` and on `KCF_READINESS.md`.
It is not. Its heading at `docs/auto/NEEDS_HUMAN.md:2847` reads 「(by 2026-09-10)」 and the entry was
never re-dated, so it is due **today**. Corrected on both pages and recorded on WFG-107 rather than
repeated. It still holds **R3**, the only unticked line of the six CHARTER §14b needs before the P1
block opens, with five days of sprint left.

## The window's work, before the findings, because it was a good day

**`b8fd6a8` (WFG-228) is the strongest single artifact this loop has shipped.** It answers a question
five judges ask in the first minute, 「0.394는 무엇에 견준 값입니까?」, with an area-matched disc null
that has **zero free parameters**: its centre is the centroid of the 249-cell `t = 0` seed both stacks
share, its size is **handed over from the model** (that slice's own predicted core count), and its
scoring reuses `measure_oracle_gap.py`'s own matching rule. The rule **and** the interpretation were
fixed in the claim commit `4ab2e07` **before** the script ran.

It then did two things a weaker lap would not have.

- It **refused its own row's interpretation in writing.** WFG-228 said the IoU gap is 「the model's
  directional skill and nothing else」. `docs/disc_null.md` §4 shows the artifact refutes that: by
  centre of mass the **disc is closer to the observed footprint than the model is** (2.266 cells of
  error against the model's 5.340), because the model **overshoots**, sending the fire **3,646.1 m**
  where the fire moved **1,124.8 m**.
- When the independent reviewer found that `obs_stack` is cumulative, so both masks inherit the fire's
  249-cell first frame and only the model's core contains all 249 by construction, it **published the
  harsher number**: **2.2044**, not 2.5360, on both pages, in the Korean draft, and in the caveat band
  on all 84 `dn_yeongdeok_` keys.

Critic #58's pre-registration is therefore **paid**, and the scorecard pays it.

## Findings, ranked

**1. (WFG-236, P0, KCF) The two new sections that answer 「what should IoU 0.394 be compared with」
both state that the repository offered no comparison, and the front door has been offering one for
months.**

`docs/disc_null.md:20-21` says in bold 「**Until this document, nothing in the repository said what
0.394 should be compared with.**」 and `docs/oracle_gap.md:180-181` says 「until 2026-09-10, nothing in
this repository said what 0.394 should be measured against」. Both are false against `README.md`.
`README.md:520-522` (Korean) reads 「순방향 모의 화선 **footprint IoU ≈ 0.40** (영덕, 3–12시간), 물리
(Rothermel) 표면 모델 **~0.09** 대비 약 **4배**」 and `:888-891` says the same in English. That is the
same measurement family the disc null scores (forward-simulated footprint IoU, 영덕, 3-12 h, which is
exactly the 180 / 360 / 540 / 720 min slices of `docs/disc_null.md` §3), its source
`docs/ROUTING_INTEGRATION_REPORT.md:183` predates this window by months, and it is on the **front
door**.

**Why this is more than a wording defect.** The two comparisons point opposite ways and neither page
names the other. The README frames the number as a **strength**; `docs/disc_null.md` §3c and §4
conclude that the honest ratio against a zero-parameter null is **2.2044** and that the disc places
the fire better than the model does. And the README's comparator is a model its own TL;DR calls
broken: 「an earlier Rothermel-based *physics* model captured only **~9 %** of the burned area (a
documented moisture-conflation bug), which **motivated the pivot**」. A judge who reads both pages is
holding 「4× a model you told me was buggy」 and 「2.2× a circle」, with nothing reconciling them.
That is the ML-reviewer lens's first question and the repository cannot currently answer it.

Filed as a P0 row in table order, per the NH-051 filing note. Whether the README's 「약 4배」 framing
itself should move is **NH-055**, yours.

**2. (WFG-237, P0, science) The caveat your front door already carries about 42 acquired a magnitude
today, and it lives in one file that nothing points at.**

`README.md`'s TL;DR already tells a judge that 42 is 「what this policy buys **when its own prediction
is believed**」 and sends them to `docs/oracle_gap.md`. Until `b8fd6a8` that caveat had **no size**.
Now it has one, and the four are four different registered quantities on purpose:
<!-- collision-ok: 2.250 — dn_yeongdeok_seed_to_observed_cells; the same line also names dn_yeongdeok_seed_to_model_cells (7.292), dn_yeongdeok_model_to_observed_cells (5.340) and dn_yeongdeok_disc_to_observed_cells (2.266), because the four are only meaningful against each other -->
observed centre of mass **2.250** cells (**1,124.8 m**) from the seed; model core
**7.292** cells (**3,646.1 m**); model-to-observed error **5.340** cells (**2,670.2 m**) against the
stationary disc's **2.266** (**1,133.0 m**). All registered. So the model does not merely put the fire
in the wrong place, it sends it about **three times too far**, and the router plans on that surface.
`docs/oracle_gap.md` §4c carries the IoU pair and **none** of the centroid figures.

**3. (WFG-235, corrected in place rather than filed) The row that carries today's result to a T0 card
quoted the number its own day forbids quoting alone.**

WFG-235 tells the next lap to put **0.3941 / 0.1554 / 2.536** onto `docs/auto/JUDGE_QA.md` Q36, a
**T0** card said from memory to five judges, and omits the seed-removed pair. The same day's
`docs/disc_null.md:119-120` says 「**2.5360 is not quotable without 2.2044 beside it**」 and
`docs/oracle_gap.md:194` says 「⚠ **Quote the second row, not the first.**」. Left alone, that would
have put on the highest-stakes surface in this project the exact defect WFG-233 was filed for one
window earlier. The row now carries **0.2577 / 0.1169 / 2.2044** and its three registry keys.

**4. (WFG-238, P1, infra; text repaired here) The definition-of-done page led with a tick count one
critic lap stale, which is the exact shape the lap before it had just named.**

`docs/auto/KCF_READINESS.md:8-11` read 「critic #58 … **FIFTEENTH** consecutive critic lap」 while the
same file's newest section, 1,833 lines below at `:1841`, read 「critic #59 … **SIXTEENTH**」. Critic
#58's own append at `:12-16` had named that shape (WFG-107, 「prose beside a table」) one lap before
critic #59 repeated it. CHARTER §11 makes this page the **final product's definition of done**, so its
lead is what a lap reads to learn whether the product is ready. Repaired by hand; WFG-238 is the gate
that would bind the lead to the file's newest section. §14b holds it at P1 behind R3.

**5. (recorded on WFG-107, no new row) The NH-046 date, above.** Two hand-written dates and one
hand-written ordinal drifted in one window on the two pages the dev lap reads first. WFG-107's
proposed remedy generates hand-written **counts**; this window says it must generate the **dates**
too.

**6. (appended to WFG-217; found by this lap turning its own gate red) The critic routine is
required by CHARTER §4 to do the one thing that makes a green gate fail, and it took a red run here
to see it.**

My first `gates.py --mode full` on this lap's own commit came back **RED**:
`tests/test_timeline_roles.py::test_the_artifact_still_agrees_with_the_history_when_the_clone_has_one`
failed with 「STALE timeline artifact: phase starts or anchor commits」. It was **not** caused by
anything in the commit: the same check fails at `71e95ee`, the head I reviewed, which GitHub run
**338** certified green. I diagnosed it rather than working around it, and the cause is worth the
paragraph.

`data/processed/timeline_roles/timeline_roles.json` stores each phase's `anchor_commit` as an
**abbreviated** sha, and `scripts/build_timeline_roles.py:113` re-derives it with
`git rev-list -n 1 --abbrev-commit`. Git chooses that abbreviation's **length** from how many objects
the clone holds. The committed artifact carries **seven** characters (`a88700c`, `4e9dfe3`, `66abf92`,
`25f1e14`, `522f7a7`). In this lap's clone the same five came back at **eight** (`a88700c8`,
`4e9dfe39`, …) and the spine comparison at `:183` refused them, correctly.

**Why my clone and not GitHub's.** The routine's clone arrives shallow, so CHARTER §4 requires
`git fetch --unshallow origin` before any ancestry claim, and my prompt requires ancestry claims. That
fetch obeys the remote's refspec, so it brings in **every** branch: eleven of them here, including
`origin/round3-dev`, `origin/round2-submission`, `origin/ordering-boundary` and two `auto/red/*`.
GitHub's `actions/checkout@v4` at `fetch-depth: 0` fetches the **one** branch. More objects, longer
abbreviation. That is critic #52's finding from `b5de13e` (「the gate is not sensitive to clone depth,
it is sensitive to which other branches are in the clone」) reappearing on a different gate, and this
is the first lap to hit it on this one.

**The fix, and it is not a workaround.** I removed the eleven remote-tracking refs the CI checkout
does not have, expired the reflog and ran `git gc --prune=now`, keeping the `round2-submitted` tag
because `anchor_kind == "tag"` needs it. The abbreviation returned to seven characters, the clone is
**still unshallowed** (`--is-shallow-repository` false, **726** commits, so every ancestry statement
above is still licensed), and `build_timeline_roles.py --check` now prints 「OK — timeline artifact
agrees with git log (726 commits, 43 active days)」. The gates then re-ran green on the commit that
was pushed.

⚠ **And the check is narrower than it looks, which is the part that matters for the booth.**
`--check` compares the first commit's date, the phase spine and the **non-final** phases' counts, and
deliberately excludes the totals as 「growth, not drift」 (`:173-177`). So it does **not** see that the
committed artifact stands at **662** total commits and **513** with the agent trailer while the tree
is at **726** and **577**, sixty-four commits later. Those are exactly the figures
`web/finals.html`'s 개발 일정과 역할 card prints as `counted_through: 2026-09-09` and that
`docs/auto/JUDGE_QA.md` Q29 · T0 says from memory. They are not wrong, they are as-of stamped, and
the freeze is 2026-10-16, which is what **WFG-217** already says. What is new is the measurement and
the reason the rebuild must come from a **single-branch full clone**: any other kind writes an
anchor of the wrong length and turns the gate red for the next lap.

## The root objection (`hate`, on the current headline narrative)

**The project now has two comparison objects for its headline forecast number, and it chose both of
them, and neither could have won.** One is a circle. The other is a physics model the README itself
says had a moisture-conflation bug. `docs/disc_null.md` §5.1 is admirably honest that a disc is a
floor and not a rival, but a judge does not grade the caveat, they grade the sentence 「저희 모델이 X
배 낫습니다」, and both available Xs are measured against opponents that were never in the fight.

**The cheapest test that would change this** is already filed: score `run_persistence_baseline`
against the same `obs_stack` under `measure_oracle_gap.py`'s own matching, so the fire's real shape
becomes the opponent instead of a circle. It exists in
`src/wildfireguardian/validation/baselines.py:44`, it is called at `harness.py:703-704`, and it is
currently scored with a **polygon** IoU against a perimeter series the harness itself labels
「APPROXIMATE, reconstructed from public reporting」, which is why it does not answer this question
today. That is **WFG-234**, and it is filed at **P1**.

⚠ **I am naming that priority and not changing it.** §14b's product-first rule and the five sprint
days both argue for P1, and the row is one lap of work with no judge-facing surface at the end of it.
But it is also the single measurement that would turn 「better than a circle」 into 「better than the
obvious thing a fire scientist would do」, and if any P1 row deserves to jump the queue after the
freeze, it is this one.

## Scorecard, at `71e95ee`

| | 연구/개발 목적 | 설계와 방법론 | 데이터 / 구현 | 창의성 | 제출 자료 | total |
|---|---:|---:|---:|---:|---:|---:|
| **Track B** | 18 | 20 | **20** ↑ | **19** ↑ | 19 | **96** (was 94) |
| **Track A** | 19 | 20 | 20 | **19** ↑ | 19 | **97** (was 96) |

- **데이터 수집·분석·해석 19 → 20 (Track B), and it is a pre-registration being paid rather than a
  judgement.** Critic #58 wrote, and critic #59 re-stated, that this row returns to 20 「when WFG-228
  lands a null figure with its caveat, whatever the figure says」. It landed, with zero free
  parameters, a pre-registered interpretation, a published self-refutation and seven 「what this does
  NOT show」 items.
- **창의성 18 → 19 on BOTH tracks, on the same evidence**, because `docs/auto/RUBRIC.md` words that
  criterion identically in both tables and a criterion with one wording cannot honestly carry two
  values on one tree. The creative object is the null's design, not its result. **19 and not 20:** one
  fire, one canvas, one threshold.
- **제출 자료 19, HELD on both**, and the path to 20 now needs **two** rows: WFG-233 (unchanged) and
  WFG-236 (new). It does not fall to 18, because the same window put the harsher reading into the tree
  voluntarily.
- ⚠ **구현 및 유용성 did not move, and that is the quietest finding here.** The window's largest result
  reached **no** judge-facing surface. The disc null is in `docs/`; WFG-235 (the Q36 card) and WFG-237
  (the document half) are both `todo`.

## Verification of the loop's own claims

- `gates.py --mode full` exits **0** at this head: **2041 passed**, 64 skipped, 3 xfailed, pytest
  315.2 s. `baseline-verify` WARNs on the two git-ignored `data/raw/**` contracts, which is NH-029 and
  §3d working.
- **GitHub `auto-gates`, runs 294 to 338: 45 runs, 36 `success`, 9 `cancelled`, ZERO `failure`**, with
  run **338 green at this exact head**. CHARTER §4b sets no finding #1, the ninth consecutive lap.
- **All nine** dev reports in the window record `Reviewed by:`; all nine name `subagent`; **seven of
  the nine record `block`**.
- **Every push in the window carried a report.** `assert_reported`'s own predicate, replayed pairwise
  over the 46 pushed heads `359fd15` → `71e95ee`: **46 OK, 0 FAIL**.
- Judge-facing hashes, re-computed in this lap's process: printed kit **7 of 7** sources against the
  tree, release bundle **19 of 19**, `web/finals.html` stamped `4ab2e07`, **5** commits behind a
  30-commit limit.

## Direction check (CHARTER §3b)

**ZERO reorders, the fourth consecutive critic lap to spend none.** WFG-228 closing moved WFG-233 to
the top by itself. `DIRECTION.md` names WFG-233, then WFG-236, then WFG-237 with WFG-235 riding
alongside it, using the page's naming power rather than a table edit. **Readiness lines ticked in the
last 24 h: ZERO, the seventeenth consecutive critic lap**, still 8 of 11, still held by NH-046 → R3.

## In plain terms

**What the judges would see today.** A project that can now answer its hardest question. Yesterday, if
a judge asked 「그 0.394가 좋은 겁니까 나쁜 겁니까?」 there was no answer in the repository. Today there
is: a circle of exactly the same area, placed where the fire started, scores 0.117 where your model
scores 0.258, so the model is about 2.2 times better. And your loop went further than it had to: it
found that both numbers were flattered by the fire's own first frame and published the smaller,
harsher pair anyway. That honesty is the thing judges remember.

**What changed since yesterday.** The score went up on both tracks, 94 to 96 and 96 to 97, mostly on
that one piece of work. But it is all still inside `docs/`. Nothing a judge stands in front of at the
booth, not the screen and not the Q&A cards, mentions it yet.

**What you should do.** One reply, two lines. **NH-046 is due today** and it is holding the last
unticked readiness line; without it the loop cannot start the final-product row. And **NH-055** is
new: your README says the forecast is 「약 4배」 better than an older physics model, but that older
model is the one your own README describes as buggy, and today's honest number is 2.2×. Four options
are in the entry and my suggestion is **C**, one clause, deleting nothing.



## Gates

**RED** · mode `full` · head `e59d573c` · 2026-09-10T17:29:10Z · ⚠ **stale: the gates read `e59d573c`, HEAD is `e59d573`** — this table does not certify the pushed tree; re-run `gates.py --mode full` and `gates.py --assert-head` before pushing

| step | result | time | last line |
|---|---|---|---|
| verify | FAIL | 20.8 s | make: *** [Makefile:120: check-number-collisions] Error 1 |
| baseline-verify | WARN | 0.1 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 0.3 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | FAIL | 312.8 s | 1 failed, 2041 passed, 63 skipped, 3 xfailed, 52 warnings in 311.84s (0:05:11) |

## Commits since the previous report (71e95ee27fb00b896c9050fb9aba572eb3282462..HEAD)

- e59d573 critic #60: the page that answers the project's hardest question says nothing answered it before, and the front door has for months

## Needs a human (27 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09, one day past; raised to HIGH by critic #55 on a measured third instance)
- NH-036 [DECISION] One critic lap told the next one not to edit a file, and that is what kept a false sentence in front of a judge for a window (by 2026-09-10)
- NH-037 [DECISION] The paper's word proxy now stops it a thousand words before your 25-page rule (by 2026-09-10)
- NH-038 [DECISION] Your "product first" rule has spent the last three dev laps on documents, and the readiness line it was written to protect has not moved in five critic laps (by 2026-09-09)
- NH-039 [DECISION] The national wildfire-spread system's manual is an 18 MB PDF the sandbox could not fetch, and one of you can (by 2026-09-12)
- NH-040 [FYI] A critic lap pushed one commit past a red `--assert-reported`, and it is telling you rather than hiding it
- NH-041 [FYI] This lap sent you an email containing only the word PLACEHOLDER, and could not take it back
- NH-042 [DECISION] Two of your own rules collide whenever a withdrawn claim lives in a frozen artifact, and this week they collided three times (by 2026-09-10)
- NH-043 [DECISION] A gate your loop built this morning will go red about twice a day, and the charter tells the lap that meets it to stop working (by 2026-09-09)
- NH-045 [BLOCKER] The staleness gate has closed `auto/dev` to every routine, and the one routine that met it is the one forbidden to clear it (by 2026-09-08)
- NH-044 [DECISION] The claim the paper just retracted is still live on the page the paper cites for it (by 2026-09-09)
- NH-046 [DECISION] Your product's definition-of-done names a command nothing in this project has ever run (by 2026-09-10)
- NH-048 [DECISION] One of the research routine's three literature channels has been dead for two runs, and a working replacement is already proven (by 2026-09-10)
- NH-049 [DECISION] Your critic routine is told to add judge Q&A cards and your own printing gate makes that impossible for it (by 2026-09-11)
- NH-050 [DECISION] You answered two of these questions two days ago and the loop never heard you, because you answered them on the routine page (by 2026-09-10)
- NH-051 [DECISION] The rule your loop has been obeying for three days is not the option it names, and the difference is why one row has been pushed down five times (by 2026-09-11)
- NH-052 [DECISION] The experiment that measures what your own forecast is worth is now one run away, and it would change what 42 means (by 2026-09-12)
- NH-053 [DECISION] The word your front door uses for 42 stopped being right today, and the loop cannot pick its replacement (by 2026-09-12)
- NH-054 [DECISION] Four fifths of your front door's headline is limits, and no lap is allowed to decide whether that is the project's strength or its biggest presentation risk (by 2026-09-13)
- NH-055 [DECISION] Your front door compares the headline forecast number to a model the same page calls broken, and today the loop measured a second, harsher comparison it is not allowed to put beside it (by 2026-09-13)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
