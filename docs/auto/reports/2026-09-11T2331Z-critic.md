# WildfireGuardian autoloop · critic · 2026-09-11T2331Z

| | |
|---|---|
| when | 2026-09-11 23:31 UTC · 2026-09-12 08:31 KST |
| branch / head | `auto/dev` / `d291364` |
| kind | critic |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 10, done: 37, dropped: 1, parked: 1, title-derivable: 1, todo: 124 |

## What happened this lap

## Your decisions, applied

**Nothing new arrived.** The Gmail connector returned the 30 newest threads matching
`from:siyeong0318@gmail.com subject:"WildfireGuardian autoloop" newer_than:14d`; every one
carries exactly one message and every one of those is the loop's own send. PR #31's comment
list is **empty**. `docs/auto/decisions_seen.json` still records `"seen": []`, which means
**no decision has ever reached the loop by email**: every applied decision came through a
Claude Code session on the laptop, and the newest is **NH-031, received 2026-09-06**, five
days ago. No reply line was found that could not be mapped, because no reply line was found
at all. That is **WFG-211**, already `todo`, not re-filed.

**26 decisions are open (25 DECISION + 1 BLOCKER) and 2 are undated. NH-046, NH-049 and
NH-051 are past due**, and the sprint ends 2026-09-15.

---

## The lap

Critic #70, reviewed `d291364`, window `8952a5b..d291364`. **Zero code, zero artifacts,
zero figures, zero `docs/NUMBERS.json` changed.** Writes confined to `docs/auto/`.

**One `fix-before-next-row` item, one new P0 row at position 1, one new P1 row, ZERO new
NEEDS_HUMAN entries** (26 are open and the email channel has never produced one; NH-043 was
updated instead). **No §3b row move was spent.**

### Gate state, re-measured here rather than read from a report

- `gates.py --mode full` exits **0** at `d291364` on its **first** run: 2181 passed, 65
  skipped, 3 xfailed, pytest 490.3 s. `baseline-verify` is the known WARN (NH-029).
- `--assert-head` exits 0. `--assert-reported --base` exits 0 at every push boundary in the
  window: `8952a5b`, `b5987b7`, `031214b`, `e0a3c2a`, `edd0ec0`, `8e05ec0`.
- Every report in the window records `Reviewed by:` (sixteen checked; a strict
  `^Reviewed by:` grep misses the bolded spellings and reports six false gaps, which is
  worth one line because it nearly became a finding).
- Kit and bundle re-hashed in this lap's own process: `WFG_printables_20260911T2137Z.pdf`,
  **60** pages, sources **7 of 7**; bundle **19 of 19** and it names that kit. R7, R9 hold.
- Clone SHALLOW at **55** commits, deliberately not deepened. **No ancestry or reachability
  claim is written anywhere in this lap.**

### CHARTER §4b: there WAS a red run, and it does not consume the item

**GitHub `auto-gates` run 386 concluded `failure` at `edd0ec0`**, the first red run in
nineteen laps. Reproduced here: three `tests/test_finals_screen.py` failures, all
「`6f866dd` is not a valid object name」, the **rebase-orphan** class. Run **387** is
`success` at `d291364`.

§4b sets finding #1 for a red run 「while the lap that pushed it reported green」. **This lap
did not.** Its gate table said RED and stale, it superseded its own report 27 minutes later
and annotated the superseded body in place. Nothing was concealed and no judge-facing
surface was ever wrong, so there is no red gate left to fix and the item goes elsewhere.

**What does survive is a false sentence in the permanent record.**
`docs/auto/reports/2026-09-11T2257Z-dev.md:144-153` says the gate is 「satisfiable only by an
ordering, not by a predicate」 and that `edd0ec0` was red 「by construction」. That is false
for **two of the three** failures: `test_the_integrity_panel_names_a_commit_this_repository_has`
exists for exactly the rebase-orphan case (docstring: 「Fails on the a562045 class: a stamp
orphaned by a rebase」) and prints the remedy in its own failure message, 「A lap that rebases
after building rebuilds before it pushes」. Both it and
`test_the_screen_is_rebuilt_before_its_stamp_ages_out_of_this_clone` are satisfiable
**locally, before any push**.

The residual is real and is exactly **one** test.
`test_the_escape_this_gate_cannot_close_is_still_open` does require the stamp to be on
`origin/auto/dev`, and `scripts/build_finals.py:846` stamps `git rev-parse --short HEAD`
with no override, so a lap whose work changes the screen genuinely cannot satisfy it before
its first push, while CHARTER §8 requires `--assert-head` green before any push. That is a
flat collision between two of your own rules. It is **NH-043**, which gained the measured
instance and a fourth option (**D: let `build_finals.py` take the stamp as an argument**),
and **WFG-263** (P1, held by §14b) for the record annotation.

### THE `fix-before-next-row` ITEM

**`README.md:330-331` still carries the scope clause WFG-259 falsified, eleven lines above
the addendum that falsifies it.** The Round-4 bullet says
「⚠ **정정(2026-09-11, WFG-258): 위 두 문장은 이제 「이 절의 상대」, 곧 완충거리를 더한 쪽에만
해당합니다.**」, which defines the surviving un-run claim as covering 「완충거리를 더한 쪽」.
WFG-259 ran that side, at two widths, on 영덕, on 2026-09-11, and `:339-341` on the same
bullet says so. `:338` was narrowed to 「**완충거리를 폭까지 훑어 정식 상대로 세운** 쪽」 and
`:330-331` was not. The bullet also now says 「거기서 나온 수치는 이 README 에 옮겨 적지
않습니다」 twice.

**Why WC-020 could not catch it.** Its registered pattern is the whole subject-plus-region
clause. `:330-331` is the **scope definition of a correction**: it carries the falsified
reading without ever spelling the claim. A registered spelling is a copy-paste ratchet, not
a claim detector (`docs/withdrawn_claims.md` §4).

**Third surface of the same class in three laps, and the rule needed widening.** Critic
#68's was governance prose on a spoken card; #69's was a card's note about itself nine lines
above an edit; #70's is a correction's scope clause eleven lines above its own addendum.
DIRECTION's 「grep the file you just edited for the sentences that DESCRIBE it」 did not fire,
because this sentence does not describe the edit, it **defines the term the edit uses**. The
new line on the page is: **after narrowing a claim, grep the file for every other sentence
naming the OLD subject.**

Exact replacement wording, the grep grade and every bar (no count, no new section pointer,
JUDGE_QA untouched so NH-049's reprint is not triggered) are in `CRITIC_LATEST.md`.

### Root objection (`hate`): the project audits its opponent harder than it audits itself

`docs/auto/DIRECTION.md` says the defensible contribution is 「the **output object and its
measured limits**」. **The output object collapses two operationally opposite outcomes into
one bucket and no page says so.** Four facts, all measured here:

1. `src/wildfireguardian/routing/evacuation.py:468-472` returns `reached=False,
   enters_hazard=True` **before any search runs**, when the origin's own node is already at
   or above `p_cut` at departure, carrying
   `note="origin already at/above the impassable cutoff at departure"`.
2. `grep -rn "impassable cutoff at departure"` over the whole tree returns **one** hit, that
   definition. No script, document, test or committed artifact reads it.
3. `src/wildfireguardian/live/pipeline.py:448-461` branches only on `reached` and
   `enters_hazard`, so that origin lands in **`no_safe_route`**.
4. `src/wildfireguardian/live/pipeline.py:107` prints on the A4 dispatch sheet
   「예산 내 안전한 보행 경로가 없음(우회 포함)」, asserting a budget was consumed and detours
   were tried. **For this member neither happened.**

**`docs/routing_limitations.md` §1 is this exact audit, done well, on `fa_exceeds_budget`,
the bucket defined one line above in the same dict.** It was found, reproduced on a
constructed field, written up, and the sheet sentence was rewritten to state the code
condition and nothing more. The identical audit was never run on its neighbour. §1's own
escape does not cover it: `no_safe_route` is **2** on 영덕, **12** on 의성·안동 and **10** on
울진·삼척, re-read here from `data/processed/real_roads_real_hazard_*.json`.

**And this window is what makes it urgent.** WFG-259 registered exactly this member for the
**opponent** arm, as `origin_removed_by_filter`, and published it as a headline-sized figure
with the page stating plainly that the filter refuses to plan for those origins. The project
names the mechanism precisely when it costs its opponent and not at all when it costs
itself.

**Cheapest test:** read `fa.note` in the scan and count that outcome separately **beside**
`no_safe_route` rather than instead of it, on the committed field. One pass, one label, no
refit, **no committed count moves**. Filed as **WFG-262, P0, position 1** (CHARTER §14b:
larger than minutes, so a row and never a preemption).

### Judge drill

Ten of the bank's hardest cards checked mechanically against the tree (Q1, Q5, Q8, Q10b,
Q12, Q16b, Q23, Q28, Q30, Q35). **Every one cites at least one file path**, 1 to 15 of them,
and the two thinnest were read in full: both carry an explicit 「없는 것」 section. **No card
failed.** The failure is a question with **no card at all**:
「원점이 이미 위험 집합 안에 있는 가구는 이 시스템이 어떻게 처리합니까?」 Its nearest
neighbour, Q23, is about the routing objective and cites `docs/routing_limitations.md`
§2/§4, not §1. Marked 「no evidence yet」 and carried as **WFG-262 (c)** rather than written
into the bank, because **NH-049** bars the critic from touching `docs/auto/JUDGE_QA.md`
without a `make printables` reprint, and NH-049 has been past due since 2026-09-11.

### `factchk` and `prism`

`factchk`: one new claim about the world entered the window, VIIRS active-fire detections at
a **375 m** nominal footprint. Correct, and correctly scoped where written. Nothing else new
in the window asserts anything external.

`prism`, five lenses: the KCF software-professor, fire-scientist, ML-reviewer and
statistician lenses each produced **no finding**, and the report says why in one line each
(pre-registration before any number existed; a refusal to assert a direction in either
direction with a test pinning it; a `d = 0` control that now runs the real code path instead
of short-circuiting to `set(base)`; a cross-tabulation with a column-sum test replacing a
story told over integers). **Only the disaster-response-official lens produced one**, and it
is the root objection.

### Direction check (§3b)

**No row move spent.** WFG-259 closed, so the next `todo` row in table order was WFG-256;
the new root objection was **filed** as WFG-262 at position 1 under §14b, which the charter
distinguishes from a reorder. DIRECTION's date, item, next-three-rows, two new
「what not to do」 lines, readiness paragraph and critic's note are updated; the page is 91
lines.

**KCF_READINESS: ZERO lines ticked for the twenty-seventh consecutive critic lap**, verified
the cheap way: `git diff 8952a5b..d291364 -- docs/auto/KCF_READINESS.md` is **empty** and the
file's newest commit is `c241904`, critic #66. It stands at **8 of 11**. §14b releases the P1
hygiene block when R1, R3, R4, R7, R8 and R9 tick; **five of those six are ticked and R3 is
the only one that is not.** R3 is `blocked(NH-046)`, which came due 2026-09-10.

### Scorecard

**Track B UP to 96** (연구 목적 18, 설계와 방법론 20, **데이터 수집·분석·해석 19 to 20**,
창의성 19, 제출 자료 19). **Track A HOLDS at 97.** The 데이터 move is critic #69's
pre-registered one, earned by WFG-259's pre-registration in the claim commit before any
number existed, its real-code-path identity control, its cross-tabulation, and its reporting
a second half as **not licensed** rather than dropping it.

**제출 자료 holds at 19 and does not fall.** The README defect is real and is this lap's
item, but it is a stale scope clause and a redundancy on a page that states the true scope
eleven lines below, not a false claim standing alone, and the printed kit and bundle are
clean. **Pre-registered for critic #71:** 제출 자료 rises to 20 when `README.md:330-331`
names the same subject `:338` names and the duplication is gone. **Pre-registered for critic
#72:** 데이터 수집·분석·해석 (Track B) and 구현 및 유용성 (Track A) are each re-examined
**downward** if WFG-262 is still open then.

### Findings, ranked

1. `README.md:330-331`, the falsified scope clause. The `fix-before-next-row` item.
2. The output object conflates 「no route found」 with 「already in the fire」. **WFG-262**,
   P0, position 1. Root objection.
3. A red head reached `origin` and the record calls it unavoidable when two of three
   failures were not. **WFG-263** (P1) plus the **NH-043** update.
4. README says 「...옮겨 적지 않습니다」 twice in one bullet. Folded into finding 1.

## In plain terms

- **What the judges would see today.** A project that just did its best single piece of work
  all week: it went and checked the one sentence in its own files that looked like it could
  destroy its headline number, wrote down the rules of the test **before** running it, and
  then reported that the sentence was true but that everyone had been reading it backwards.
  A judge who finds that page finds an answer, not a problem. The front page has one leftover
  sentence from an older correction that now says the opposite of what the same paragraph
  says eleven lines further down, and that is the one small thing the next lap fixes first.

- **What changed since yesterday.** Your score went **up one point on the research track, to
  96 of 100**, for that experiment. Two things are worth knowing. One: for about half an
  hour tonight the shared branch was red and GitHub sent you a 「Run failed」 email; it was
  repaired, the lap told you about it rather than hiding it, and nothing a judge sees was
  ever wrong. Two, and this is the real one: **I found something in your own routing code
  that your files do not tell anyone about.** When a household's own location is already
  inside the fire, the router stops before it even searches, and the printed rescue sheet
  then tells the operator 「no safe walking route within the time budget, detours included」.
  That is the wrong sentence: nothing was searched and no detour was tried. Those are two
  completely different instructions for a rescue team. The project already did this exact
  audit, very well, on the bucket defined **one line above** in the same file, and never did
  it on this one. It is 2 households in 영덕, 12 in 의성·안동, 10 in 울진·삼척, and the fix is
  a label and a sentence, not a re-run of anything.

- **What you should do.** Nothing for the fix; the next lap does it. But **26 decisions are
  waiting and not one has ever reached the loop by email**: every answer you have given came
  through Claude Code on the laptop, and the newest is from **2026-09-06**. NH-046 alone is
  holding both the ninth readiness tick and the entire P1 block, and NH-049, which is also
  past due, is what stopped me writing tonight's finding into your judge Q&A bank. **The
  sprint ends 2026-09-15, four days away.** Opening the repository in Claude Code and saying
  `decisions` answers them as button questions; three of them would unpark real work.



## Gates

**ALL GREEN** · mode `full` · head `d291364` · 2026-09-11T23:17:04Z · current at `d291364`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 29.6 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.2 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 0.5 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 490.3 s | 2181 passed, 65 skipped, 3 xfailed, 52 warnings in 488.38s (0:08:08) |

## Commits since the previous report (8e05ec009f758fb61af14d6ee0ab1913d0bc3b3b..HEAD)

- d291364 report: 2026-09-11T2257Z dev, re-stamped at the head it ships in

## Needs a human (31 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09, one day past; raised to HIGH by critic #55 on a measured third instance)
- NH-036 [DECISION] One critic lap told the next one not to edit a file, and that is what kept a false sentence in front of a judge for a window (by 2026-09-10)
- NH-037 [DECISION] The paper's word proxy now stops it a thousand words before your 25-page rule (by 2026-09-10)
- NH-038 [DECISION] Your "product first" rule has spent the last three dev laps on documents, and the readiness line it was written to protect has not moved in five critic laps (by 2026-09-09)
- NH-039 [DECISION] The national wildfire-spread system's manual is an 18 MB PDF the sandbox could not fetch, and one of you can (by 2026-09-12)
- NH-040 [FYI] A critic lap pushed one commit past a red `--assert-reported`, and it is telling you rather than hiding it
- NH-041 [FYI] This lap sent you an email containing only the word PLACEHOLDER, and could not take it back
- NH-042 [DECISION] Two of your own rules collide whenever a withdrawn claim lives in a frozen artifact, and this week they collided three times (by 2026-09-10)
- NH-043 [DECISION] A gate your loop built this morning will go red about twice a day, and the charter tells the lap that meets it to stop working (by 2026-09-09)
- NH-045 [BLOCKER] The staleness gate has closed `auto/dev` to every routine, and the one routine that met it is the one forbidden to clear it (by 2026-09-08)
- NH-044 [DECISION] The claim the paper just retracted is still live on the page the paper cites for it (by 2026-09-09)
- NH-046 [DECISION] Your product's definition-of-done names a command nothing in this project has ever run (by 2026-09-10)
- NH-048 [DECISION] One of the research routine's three literature channels has been dead for two runs, and a working replacement is already proven (by 2026-09-10)
- NH-049 [DECISION] Your critic routine is told to add judge Q&A cards and your own printing gate makes that impossible for it (by 2026-09-11)
- NH-050 [DECISION] You answered two of these questions two days ago and the loop never heard you, because you answered them on the routine page (by 2026-09-10)
- NH-051 [DECISION] The rule your loop has been obeying for three days is not the option it names, and the difference is why one row has been pushed down five times (by 2026-09-11)
- NH-052 [DECISION] The experiment that measures what your own forecast is worth is now one run away, and it would change what 42 means (by 2026-09-12)
- NH-053 [DECISION] The word your front door uses for 42 stopped being right today, and the loop cannot pick its replacement (by 2026-09-12)
- NH-054 [DECISION] Four fifths of your front door's headline is limits, and no lap is allowed to decide whether that is the project's strength or its biggest presentation risk (by 2026-09-13)
- NH-055 [DECISION] Your front door compares the headline forecast number to a model the same page calls broken, and today the loop measured a second, harsher comparison it is not allowed to put beside it (by 2026-09-13)
- NH-056 [DECISION] A Korean university published a reproducible deep-learning wildfire model four weeks before your finals, and a lap may not decide on its own whether to mention it (by 2026-09-13)
- NH-057 [DECISION] The one thing your project now says it contributes has never once been produced from a real fire, and only your laptop can change that (by 2026-09-13)
- NH-058 [DECISION] Two of your routines built the same row in the same window, and the rule that was supposed to stop that cannot see the paper routine (by 2026-09-13)
- NH-059 [DECISION] Your loop measured the hardest question a judge can ask you, and the answer is uncomfortable enough that no lap may decide on its own whether you say it out loud (by 2026-09-13)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
