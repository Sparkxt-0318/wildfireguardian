# WildfireGuardian autoloop · paper · 2026-09-12T0926Z

| | |
|---|---|
| when | 2026-09-12 09:26 UTC · 2026-09-12 18:26 KST |
| branch / head | `auto/dev` / `f448bdd` |
| kind | manual |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 10, done: 37, dropped: 1, parked: 1, title-derivable: 1, todo: 130 |

## What happened this lap

# Paper lap 35 — the mislabelled-class audit reaches the responder arm, and the page count comes back

**Incorporated:** `2dd3614..f448bdd`, 19 commits in this clone, 16 files outside `paper/` and
`docs/auto/`. `paper/STATE.json` → `last_incorporated_commit` = `f448bdd`.

**Result:** `body_words` 8,999 → **9,000** (margin 1 → **0**; `check_paper.py` fails on
`> LIMIT`, so 9,000 is inside — lap 25's precedent). Built document **23 pages** under Carlito,
**measured, not inherited**. Gates `--mode full` **ALL GREEN** at the pushed head.
`Reviewed by: subagent (block): the repair swapped a word that over-claimed about the world for
one that over-claims about the evidence — and the evidence it over-claimed about is the evidence
the same window created.` All findings repaired before the push.

## What changed in the manuscript, and why

The window ran WFG-264 (`docs/routing_limitations.md` §7), which is the third arm of an audit <!-- forbidden-ok: 264, this is the BACKLOG ROW ID WFG-264 and not the gangneung_donghae_2022 Build-A positive count the gate anchors that value to. Same false-positive class as ba437f5, where the gate read a lap stamp as a camera count. No figure is asserted on this line. -->
laps 33 and 34 already carried across the two **walk-side** outcome classes. Its point is the one
that matters: both earlier audits, and the whole Round-4 review behind them, stayed on the **459
resident** series, while the sheet a judge is physically handed at the booth is the **439
responder** series. `no_surviving_vehicle_ingress` is exactly `rescuer_reachable() == False`, and
**three** distinct return sites collapse into it — a pre-search refusal at the *depot's own* node,
an exhausted Dijkstra that itself merges budget exhaustion with the ceil-rounded hazard gate **and
a drive graph with no depot-to-home path at all**, and reached-but-`enters_hazard`. Two of §7's
three reproductions have **no fire anywhere in the field**.

**This manuscript asserted that class name in its own voice in two places**, with nothing anywhere
qualifying it:

| § | before | after |
|---|---|---|
| §3.4 | 「143 are dispatchable and 24 have no **surviving** vehicle ingress」 | 「…24 have no **confirmed survival-aware** ingress」 |
| §4.6 | 「the count of origins with no **surviving** vehicle ingress by 33.3 % (24 to 32)」 | 「the count of **responder** origins with no **confirmed survival-aware** ingress by 33.3 %…」 |

§3.4 came in at **net zero** (`vehicle` dropped as redundant in a section whose every sentence is
about a crew driving). §4.6 cost **+1**, and that word bought a named non-negotiable: HANDOFF §5
rule 23 forbids merging the two four-way classifications, and §4.6's 24 → 32 is the 439 responder
axis on a *synthetic* hazard field sitting in a section headed 「Sensitivity on the canonical
Yeongdeok field」 whose neighbours are 458-series walk counts, naming the axis nowhere. Nothing was
compressed and no caveat or registered number was traded.

## The reviewer blocked, and it was right on every count

1. **Root objection, sustained.** The first draft repaired both sites to 「no **confirmed** vehicle
   ingress」 — which asserts that *nothing in the run confirmed* vehicle ingress for any of the 24.
   The window's own registered measurement records the opposite for some of them:
   `vus_corridor_reachable_dispatch_slice` = **1** (a home whose stored window is at or above the
   run's own 12-minute margin, reachable only from `assess_ingress`'s *feasible* branch) and
   `vus_corridor_survives_past_eta_dispatch_slice` = **4**. Before the lap the sentences were
   merely unestablished; the draft would have shipped them **contradicted by a committed artifact
   registered in the same window** — lap 20's shape, a repair worse than the defect. The
   mechanism was this routine's own claim of compliance: it said it had adopted 「the register the
   repository's own repaired sheet line uses」, and that line is 「어느 거점에서도 **생존 인지**
   차량 진입 경로가 확인되지 않음」. It adopted half of it. **The accurate word was free.**
2. **Rule 23, verified in the tree before being obeyed** rather than taken on the reviewer's word.
   Repaired at +1.
3. **The draft's reason for not extending §6 is withdrawn as false.** It said the 24 could not
   join that sentence because it is an absolute count rather than an arm of a paired contrast —
   which **applies verbatim to `no_safe_route`, the class lap 34 put in that very sentence**, and
   whose absolute counts this paper publishes (Abstract 2, §4.3 2, Table 2's 2 / 12 / 10). The
   destination survives on two other grounds (§6's list is headed 「Router approximations」 and
   every other member belongs to `routing/evacuation.py`; and after the §3.4 repair the paper no
   longer uses the overstating name at all), but the stated rule was wrong and is retracted.
4. **A red gate**, already repaired when the review landed: UNLABELLED mentions of the bare
   row id `264` on lines this lap wrote — five in `paper/`, then two more in the generated report itself — each now carrying the per-line pragma the incorporated <!-- forbidden-ok: 264, this is the BACKLOG ROW ID WFG-264 and not the gangneung_donghae_2022 Build-A positive count the gate anchors that value to. Same false-positive class as ba437f5, where the gate read a lap stamp as a camera count. No figure is asserted on this line. -->
   window's own documents carry on the identical string.

## The page count went from UNKNOWN back to measured

The 0617Z dev lap repointed the manuscript at `F3b_` and `F8b_`, which moved the figure-inputs
digest; with no renderer in that sandbox it correctly **nulled** `built_pages` rather than
hand-writing an anchor, and said so in capitals — the author's 25-page rule (NH-028) was
**UNVERIFIED at that head**. This lap ran the one `apt` line `paper/README.md` has printed since
lap 9, so `check_paper.py` took its measuring branch: **pages 23, page-tree `/Count` agreeing,
calibri_face Carlito, metrics_ok true**. **Two pages against the author's 25, zero words against
the proxy's 9,000, measured on one document by one run.** The anchor was re-derived **twice** —
the reviewer's repairs moved `body_words`, so `bdb470ea0108d633` is the pre-repair run's and
**`7ed51c97137c9b2c`** is what `STATE.json` records. ⚠ This does **not** close **WFG-116**: that
is the same `apt` line in `.github/workflows/auto-gates.yml`, outside `paper/`, still open — and
until it lands, every lap without a renderer must null the field again, which has now actually
happened rather than being a thing this file warned about.

## Figures

No figure was added or redrawn. All ten redrew **byte-identical**. `F3b_regions.png` and
`F8b_routing_map.png` are new in the incorporated window and ship inside this lap's `.docx` for
the first time, so both were **opened with the Read tool**: boxed legends clear of the data,
nothing clipped or overlapping, F8b's legend counts reading 414 / 42 / 2 against the committed
partition, both carrying the repaired 「no safe walking route **found**」 wording. The `Fig. N`
appearance mapping is unchanged and was confirmed by the reviewer at all eight.

⚠ A new figure costs a page without costing a word, the margin against the author's rule is two
pages, and the `vus_` split is three integers belonging in a sentence — not in a figure drawn to
dodge the word counter, which is the caption bypass lap 19 ruled out.

## Gaps

No gap opened or closed. Seven markers, unchanged. `check_paper.py` reports
`figures 8, tables 4, references 29, gaps 7`.

## ⛔ What the budget refused — measured, not estimated, and both reverted

Of the 24, the run's own direct-corridor screening recorded **4** with a corridor surviving past
the responder's ETA and **1** reachable with the full 12-minute margin. That is evidence **against
this paper's own number**, on a class whose absolute count it publishes twice. The sentence
carrying it: 8,999 → **9,031**, **+32**. The bare register clause alone: **+10**. Both over the
hard fail, both reverted, nothing compressed to fund either. After the repair the manuscript is
**not false** — it reports what the scan established. What it declines is the *reason* and the
counter-evidence. **Laps 13–21, 24, 25, 30, 33 and now 35 have all had their writing shaped by
the proxy rather than by the evidence. NH-037 is the answer and it is still open**, and the margin
is now **zero**.

## For a dev lap — outside CHARTER §12, reported rather than touched

The DIRECTION-mandated **subject** grep (`git grep -n "surviving vehicle ingress" -- docs/ paper/
release/ web/`) returned five hits in four files. `paper/manuscript.md` now returns **zero**. The
other three are outside this routine's paths and were **not** edited:

- `docs/network_drift.md:10` and `:61` — the source document behind §4.6's own 「24 to 32」, where
  the spelling is a **bold table-row header** and a headline blockquote.
- `docs/auto/knowledge/ROUTING_FUNDAMENTALS.md:161`.
- `docs/NUMBERS.json:9328` — doubly barred: CHARTER §3 rule 2 forbids this routine editing the
  registry at all.

Two reviewer findings are also sustained and unfunded here: §4.6 does not say the denominator
moved 439 → 441 (that is the registry entry's `sample` field, not its `caveat`, and the caveat's
own 「neither network is right; the quantity is sensitivity」 half **is** carried); and the F8b
caption's 「the recomputed partition equals the committed one」 rests on a **seventh** hand-copy of
the classification predicate that nothing enforces (**WFG-271**).

## Clone facts

`git rev-parse --is-shallow-repository` → **true**; `git rev-list --count HEAD` → **53**, both
measured this lap. **No ancestry or reachability claim is made anywhere in this lap's output.**

## In plain terms

The paper said 24 homes "have no surviving vehicle ingress"; the repository proved that week that its own code only establishes "we never confirmed a survival-aware route", so both places now say that instead — and the reviewer caught me dropping the words that made it true.

Still missing: the paper cannot say *why* those 24 fall in that class, or that its own screening found a reachable corridor for 1 of them — that sentence costs 32 words and the budget has 0 left.

Your move: answer **NH-037** (the 9,000-word proxy now blocks true sentences, and the real rule — your 25 pages — has two pages spare), and have a dev lap add one `apt` line to `.github/workflows/auto-gates.yml` so the page count stops going unknown.



## Gates

**ALL GREEN** · mode `full` · head `f448bdd` · 2026-09-12T09:25:57Z · current at `f448bdd`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 23.7 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.2 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 0.5 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 382.7 s | 2221 passed, 65 skipped, 3 xfailed, 52 warnings in 381.53s (0:06:21) |

## Commits since the previous report (43bbfd83d64bc2886d8c38913c0099fd7b52ef6f..HEAD)

- f448bdd the lap that pushed past a red --assert-reported reports it, and the re-stamped report is the remedy the gate itself printed

## Needs a human (32 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09, one day past; raised to HIGH by critic #55 on a measured third instance)
- NH-036 [DECISION] One critic lap told the next one not to edit a file, and that is what kept a false sentence in front of a judge for a window (by 2026-09-10)
- NH-037 [DECISION] The paper's word proxy now stops it a thousand words before your 25-page rule (by 2026-09-10)
- NH-038 [DECISION] Your "product first" rule has spent the last three dev laps on documents, and the readiness line it was written to protect has not moved in five critic laps (by 2026-09-09)
- NH-039 [DECISION] The national wildfire-spread system's manual is an 18 MB PDF the sandbox could not fetch, and one of you can (by 2026-09-12)
- NH-040 [FYI] A critic lap pushed one commit past a red `--assert-reported`, and it is telling you rather than hiding it
- NH-041 [FYI] This lap sent you an email containing only the word PLACEHOLDER, and could not take it back
- NH-042 [DECISION] Two of your own rules collide whenever a withdrawn claim lives in a frozen artifact, and this week they collided three times (by 2026-09-10)
- NH-043 [DECISION] A gate your loop built this morning will go red about twice a day, and the charter tells the lap that meets it to stop working (by 2026-09-09)
- NH-045 [BLOCKER] The staleness gate has closed `auto/dev` to every routine, and the one routine that met it is the one forbidden to clear it (by 2026-09-08)
- NH-044 [DECISION] The claim the paper just retracted is still live on the page the paper cites for it (by 2026-09-09)
- NH-046 [DECISION] Your product's definition-of-done names a command nothing in this project has ever run (by 2026-09-10)
- NH-048 [DECISION] One of the research routine's three literature channels has been dead for two runs, and a working replacement is already proven (by 2026-09-10)
- NH-049 [DECISION] Your critic routine is told to add judge Q&A cards and your own printing gate makes that impossible for it (by 2026-09-11)
- NH-050 [DECISION] You answered two of these questions two days ago and the loop never heard you, because you answered them on the routine page (by 2026-09-10)
- NH-051 [DECISION] The rule your loop has been obeying for three days is not the option it names, and the difference is why one row has been pushed down five times (by 2026-09-11)
- NH-052 [DECISION] The experiment that measures what your own forecast is worth is now one run away, and it would change what 42 means (by 2026-09-12)
- NH-053 [DECISION] The word your front door uses for 42 stopped being right today, and the loop cannot pick its replacement (by 2026-09-12)
- NH-054 [DECISION] Four fifths of your front door's headline is limits, and no lap is allowed to decide whether that is the project's strength or its biggest presentation risk (by 2026-09-13)
- NH-055 [DECISION] Your front door compares the headline forecast number to a model the same page calls broken, and today the loop measured a second, harsher comparison it is not allowed to put beside it (by 2026-09-13)
- NH-056 [DECISION] A Korean university published a reproducible deep-learning wildfire model four weeks before your finals, and a lap may not decide on its own whether to mention it (by 2026-09-13)
- NH-057 [DECISION] The one thing your project now says it contributes has never once been produced from a real fire, and only your laptop can change that (by 2026-09-13)
- NH-058 [DECISION] Two of your routines built the same row in the same window, and the rule that was supposed to stop that cannot see the paper routine (by 2026-09-13)
- NH-059 [DECISION] Your loop measured the hardest question a judge can ask you, and the answer is uncomfortable enough that no lap may decide on its own whether you say it out loud (by 2026-09-13)
- NH-060 [DECISION] Your "product first" rule sent a booth-printable defect to the bottom of the board, and two of your routines read the same sentence of it differently (by 2026-09-14)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
