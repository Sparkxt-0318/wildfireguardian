# WildfireGuardian autoloop · paper · 2026-09-05T09:40Z

| | |
|---|---|
| when | 2026-09-05 09:23 UTC · 2026-09-05 18:23 KST |
| branch / head | `auto/dev` / `872ee14` (report generated at `4d1a596`, the head this lap incorporated; it ships in `872ee14` — the correction pattern 4d1a596 established) |
| kind | manual |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 6, done: 16, parked: 1, todo: 61 |

## What happened this lap

Paper lap 7. Incorporated `4d1a596`. Body **7,461** words (was 7,467; gate fails at 7,500),
8 figures, 4 tables, 27 references, 6 gaps. `check_paper.py` exit 0, `gates.py --mode full`
ALL GREEN (1,490 passed, 56 skipped).

## The one thing this lap was for

The abstract was overclaiming, in the sentence the paper is most read on. It said the 42
origins 「reach a refuge only when the router accounts for **where the fire will be**」. The
baseline that contrast is measured against is `naive`, and `naive` is **fire-blind** — it
consults no hazard at all, neither the forecast nor the fire's present position. This
repository says so twice in its own words (`src/wildfireguardian/routing/evacuation.py:270`,
`docs/real_roads_real_hazard.md:50`). So an unknown share of the 42 is bought by knowing
where the fire **is**, not where it will be, and the abstract was attributing all of it to
the forecast.

This is critic #17's F1 finding, which caught the identical defect in the booth script
(`docs/auto/DEMO_SCRIPT_5MIN.md` 3막 called the fire-blind arm 「지금 이 순간만 보는 지도」).
WFG-103 fixed the spoken sentence. Nobody had checked the manuscript for the same sentence.
It was there.

**Fixed:** the abstract now says 「only under the forecast-aware policy」 and adds 「That
contrast is measured against a fire-blind baseline, so it does not separate knowing where the
fire will be from knowing where it is」. §4.3 grew a **third caveat** beside the coverage and
lineage ones, stating the same thing where the counts live; §4.4 carries it locally for its
own 91 and 24.73 %.
F5's caption carries it as well, since a reader looking only at the figure sees exactly those
two bars. New `[GAP]` marker and **G7** row in `paper/GAPS.md`.

The science half is not the paper's to fix: the arm that separates 예측 from 관측 is
**WFG-033(b)** (present perimeter + fixed buffer), it is **P2** — after the finals — and
whether to pull it into the sprint is already open with the author as **NH-027**. The
manuscript now says plainly that the separation has not been measured, which is what it can
honestly say today.

## What else changed

- **The length budget stopped being a note and became a decision (NH-028).** Lap 6 recorded
  in `paper/GAPS.md` that 「no further trim of this kind is available… every remaining
  paragraph carries a registered number and the caveat CHARTER §3 rule 3 binds to it」, and
  put three structural options to the author — in a file the author's decision channel
  (NH-020) never reads. Three laps have now recorded it and none escalated it. Filed as
  **NH-028** with those options, so it appears when the author says 「decisions」.
- **That 「no trim available」 claim was too strong, and this lap falsified it by having to.**
  Arriving with a correction it could not decline to ship and 33 words of margin, the lap
  found prose carrying no registered number and no caveat and cut it: §5's
  county-subset arc (an anecdote making a point already made three times with numbers)
  compressed to one clause; §5's 「and one field answers the responder's mirror-image question too」
  deleted as a duplicate of §1 and §3.4; §1's restatement of the 22–64 min and 0.1–1 ha
  figures cut to 「tens of minutes」 and 「a size floor」, both given in full with their caveats
  in §4.7; §1's 「and this paper is as much about the evaluation design under
  which it was built」 deleted as a duplicate of §1's own claims paragraph; §6's 「Reading
  those shares as estimates for households would need an interval nothing here supports」
  deleted as a restatement of the sentence before it; and the abstract's opening compressed.
  **No number and no caveat left the manuscript** — the failure mode lap 6 recorded twice.
  The lesson is recorded in `GAPS.md`: a section can be dense in numbers and still hold prose
  that repeats another section, and 「every paragraph carries a number」 is not the same claim
  as 「every sentence earns its words」.
- **A venue claim in the ledger was unverified, and half of it was wrong.** Lap 6's option
  (c) read 「IEEE Access measures pages, and the built `.docx` is inside 20」. Checked at the
  source under CHARTER §3.5b: IEEE Access states 「There is no page limit for articles and
  therefore no over-length article charge」 and 「strongly recommend[s] keeping the page count
  under 20 pages」 (<https://ieeeaccess.ieee.org/about/article-processing-charges/>, read
  2026-09-05). So the venue rule is a *recommendation* and the 7,500-word gate is this
  repository's own invention — but 「the built `.docx` is inside 20」 was **never measured**
  and is now struck. The sandbox's LibreOffice will not load the file; the file itself is
  valid (25 zip members, 159 paragraphs, 4 tables, 8 figures, opens in Word). Also recorded:
  secondary sources claim a ~10,000-word and a 10-float limit that appear on no IEEE page —
  the paper has 12 floats, so it is flagged to check at submission rather than obeyed.

## Figures

None added, none redrawn — `make_figures.py` reproduced all eight **byte-identical**, so
nothing new to look at. F5 was read anyway because its caption changed: bars legible, value
labels inside the fills, legend below the axes, arm labelled 「Fire-blind route (shortest
distance)」, which is what the new caption clause asserts. Appearance numbering re-checked
against the built `.docx` and unchanged (F1→1, F2→2, F4→3, F5→4, F8→5, F3→6, F6→7, F7→8);
all four table captions verified still attached to their tables.

## Gates

`--mode quick` was run as the prompt asks, and then **`--mode full`**, because the previous
paper lap pushed on quick and left `auto/dev` red for forty-five minutes — quick does not run
`pytest-full`, CHARTER §3 rule 9 requires full before every push, and §12 grants this routine
no exemption (that is NH-026, open). Full is green here: verify, snapshot-verify, env-check,
pytest-full 1,490 passed. `baseline-verify` warns as it always does in a sandbox without the
raw bundle, and the gate treats it as non-blocking.

## Gaps

Opened **G7** (what the headline contrast may attribute; closes when WFG-033(b) runs, which
is after the sprint unless the author picks NH-027 option A or C). None closed: G2, G3, G4,
G5 and G6 all still need the author's laptop, the author's consent handling, or a dev lap.

## Review

**Reviewed by: subagent (block): the paper's only positive empirical result now has no
external referent in either direction, and this lap's own correction is what removes the last
one.** The reviewer's reasoning: the 42/458 contrast is graded against the *predicted* field
rather than observed burn (§6's first limitation, G4, unrun), and is now also disclosed as
being measured against a baseline that consults no hazard at all (G7) — so with pooled recall
at 0.138 the headline reduces toward 「a router that avoids cells a weak model flags produces
different routes than a router that ignores the map entirely」. It confirmed G7 in source
(`naive_route` runs `single_source_dijkstra` on `length_m` and touches the hazard only to
score afterward) and said shipping it was right, but that it strips out the last remaining
reading without replacing it.

**What I fixed before pushing** — every defect it raised that was mine and checkable:

1. **§4.3's heading still said 「What the forecast changes about who can walk out」**, which
   the section's own new caveat contradicts. Now 「What hazard awareness changes about who
   can walk out」.
2. **§4.4's 91 and 24.73 % carried no local caveat**, only a forward pointer from §4.3 —
   the exact failure class (「a caveat must travel with its number」, CHARTER §12) that
   `GAPS.md` already records twice. §4.4 now states it locally.
3. **My own trim had broken §5's count**: it said 「Three domain researchers replied」 and
   then accounted for one. The second reader's point is named again.
4. **My §5 compression had dropped 「a label checked against the geometry it claims rather
   than against a code table」**, which lap 5 had called the transferable half of that
   episode. Restored.
5. **「private cell-broadcast origination is not lawful in Korea」 → 「not authorized」**, the
   reviewer's sources supporting the weaker and more precise word.
6. **My own record in `GAPS.md` was inaccurate**: it claimed the 22–64 min figure is stated
   in full 「in §4.7 and the abstract」; the abstract says only 「tens of minutes」. Corrected.

Its **bound-caveat audit came back clean** — the specific thing I asked it to hunt, item by
item against the registry, including the `sparsity_*` 「SAMPLED ORIGINS, NOT HOUSEHOLDS」
family and `det_size_floor_ha_tf750`'s 「ORDER OF MAGNITUDE ONLY」. Zero wrong numbers; it
re-derived the arithmetic independently (458 = 414+42+2, 412/2989 = 0.138, the 316.06 % core
growth against `cells_ge_0.5_per_slice` [249, 692, 952, 981, 1036], all four Table 3 rows,
and about twenty more) and all 27 citations resolve with `verified` notes.

**What I did not do, and why.** Its `first_nail` is a genuinely cheaper experiment than the
one the paper defers: mask slice 0 of the committed canonical field as a node filter and
re-run the existing `naive_route` over just the 44 origins whose fire-blind route enters the
hazard, to count how many a present-perimeter router already saves. That would turn §4.3's
「an unmeasured share」 into a number. **It is out of this routine's scope** — it creates an
artifact outside `paper/`, and an unregistered number cannot be written into the manuscript
(CHARTER §3 rule 3). I have written the full recipe into the **G7** row so a dev lap can run
it directly, and flagged it as worth doing before the finals whatever NH-027 decides.

**On the residual objection.** After the six fixes, what remains of the block is a judgment
about how strong the result is, not a rule the paper breaks: the paper now discloses both
limits (G4 and G7) in the abstract, §4.3, §4.4 and the F5 caption rather than hiding either.
I kept the **title** as 「Forecast-conditioned…」 because it describes the method, which is
forecast-conditioned, rather than the contrast's attribution. That is a defensible call and
also a reviewable one — if the author disagrees, the title is one line to change.

## One gate added (in `paper/`, this routine's own)

The reviewer caught `paper/STATE.json` mid-lap holding the previous lap's `body_words` and
gap count, and noted that **nothing gates it**: `check_paper.py` computed exactly those
counts, printed them and threw them away. It now compares them and fails on drift. Only the
counts — `last_incorporated_commit` is deliberately not checked, since it names the code
commit the manuscript was written against and legitimately lags HEAD. Verified both ways: it
fails on a corrupted count and passes on the real one.

## In plain terms

The paper's headline sentence was giving your forecast credit for something a much dumber
map would also have caught, and it now says so — the honest version, in the abstract, §4.3
and the figure. Nothing about the result changed; what changed is what it claims.

What is still missing is the experiment that would settle it: routing the same origins
against a 「where the fire is right now」 baseline. It is already written up as WFG-033(b) and
it is scheduled for after the competition, which is question **NH-027** already waiting for
you.

Two things to do when you next open the repository: answer **NH-027** (whether that
experiment moves into the sprint), and answer **NH-028** — open the `.docx`, see how many
pages it actually is, and tell the loop whether to keep enforcing its self-imposed 7,500-word
limit. IEEE Access does not require it; the repository invented it.



## Gates

**ALL GREEN** · mode `full` · head `4d1a596` · 2026-09-05T09:22:38Z · current at `4d1a596`

⚠ The run above is the pre-commit one `report.py` recorded, at the head this lap
incorporated. The run below is the same gate re-run **after** the commit and the rebase
onto `a063e51`, at the commit this report actually ships in. Appended rather than edited,
per the pattern runs 95, 101 and 107 established and `4d1a596` documented.

**ALL GREEN** · mode `full` · head `872ee14` · 2026-09-05T09:47Z · post-commit, post-rebase

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 14.2 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.1 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 0.3 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 175.3 s | 1490 passed, 56 skipped, 52 warnings in 174.49s (0:02:54) |

The pre-commit run, for the record:

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 14.0 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.1 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 0.3 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 176.2 s | 1490 passed, 56 skipped, 52 warnings in 175.47s (0:02:55) |

## Commits since the previous report (26e200d46b961cb77f1715391da40ab8b206f674..HEAD)

- 4d1a596 the report named the commit it reviewed, not the commit it ships in
- 6236c81 critic #17: the core act tells five judges the comparison is against a map that sees the fire, and the arm is fire-blind

## Needs a human (8 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-025 [DECISION] `Main` cannot follow the green commit until you change one GitHub setting, and until you do, every green push emails you a red run (by 2026-09-08)
- NH-026 [DECISION] One of the five routines pushes on a gate that does not run the test suite, and its prompt is the one the repository cannot show you (by 2026-09-08)
- NH-027 [DECISION] The one experiment that would test the project's headline against a fair opponent is scheduled for after the competition (by 2026-09-08)
- NH-028 [DECISION] The manuscript is full, and three laps have recorded that as a note rather than asked you (by 2026-09-10)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
