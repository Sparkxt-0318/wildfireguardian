# WildfireGuardian autoloop · dev · 2026-09-05T1542Z

| | |
|---|---|
| when | 2026-09-05 15:42 UTC · 2026-09-06 00:42 KST |
| branch / head | `auto/dev` / `9184a72` |
| kind | dev |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 6, done: 16, parked: 1, todo: 66 |

## What happened this lap

# WFG-109 — the withdrawn sentence is out of the source, and the gate is the whole file rather than the two lines

## Your decisions, applied

**Nothing new this lap.** The Gmail search (`from:siyeong0318@gmail.com subject:"WildfireGuardian
autoloop" newer_than:14d`) returned only the loop's own sent reports — every thread is a single
message, none is a reply — and PR #31 has no comments. `docs/auto/decisions_seen.json` is unchanged.
Your four decisions of 2026-09-05 (NH-025 … NH-028) were already applied by the laptop session in
`9442430` and are not re-applied here.

One consequence of yours **did** land in the tree since the last lap and is measured below: your
commit `38620f2` re-froze the baseline, which is NH-029's option A.

## What this lap did

**The row.** `scripts/finals.template.html:1378` (KO) and `:1381` (EN) still carried the STATIC VIEW
caption WFG-103 withdrew — 「지도가 지금 이 순간만 본다면, 이 경로를 권했을 것입니다」 and "A map that
only sees the present would recommend it". The routing baseline is `naive`, which is fire-blind
(`src/wildfireguardian/routing/evacuation.py:270`, `docs/real_roads_real_hazard.md:50`), so that
sentence hands the weaker opponent the stronger name. Nothing was wrong on the judged screen: the
committed `web/finals.html` carried the correction. The next `make finals` would have put the
withdrawn claim back in front of five judges, and the only thing standing in the way was a paragraph
of `docs/auto/finals/BOOTH_SETUP.md` §3.4 asking a student not to run a command.

Both lines now say what the screen says. `make finals` was re-run and **the only line it changed is
the payload, 434** — so the reversion is gone rather than deferred. Inside the payload the changed
values are `built_utc`, `git`, the two gate timings, and the registry counters (320 → 326 entries,
262 → 268 reproducible); all are recomputed at build time, and the counter move is your re-freeze
being read correctly. `make finals-bundle UPDATE=1` re-derived `release/kcf-finals-2026/MANIFEST.json`
(17 files). The row also said to check the rest of `92366cb` rather than assume: it touched
`web/finals.html` by exactly two lines, and they are these two.

**The root objection I recorded against my own plan (`hate`), and acted on.** The row's `done when`
asked for "a test that asserts the template and `web/finals.html` carry the same STATIC VIEW
captions". That test pins the two lines this bug happened to land on and nothing else; the next
hand-edit to any other generated line outlives its source exactly as this one did. `build_finals.py`
substitutes **one** placeholder line, so the two files are a line-for-line identity outside it, and
asserting the identity costs the same as asserting two captions.

`tests/test_finals_template_sync.py` (9) asserts the identity. Mutation-graded **7/7**: hand-edit the
finals output; hand-edit the finals template (WFG-109 itself); edit an *unrelated* generated line;
delete a line from the output; commit the template as the screen unbuilt; hand-edit the console
output; make a failure message name a command that does not exist. **Three of those pass a
caption-only comparison.** Extended to `web/console.html` / `scripts/console.template.html` — the same
placeholder mechanism on a second judged screen — which measured clean (only its payload line 238
differs). `docs/finals_screen_v2.md` §4.2 carries the method, the result, the grading and three things
the gate does **not** cover: the payload line, a template that is wrong on both faces, and the two
screens not built this way. §4.1's ⚠ box had recorded "the two gates read different faces" as a
limitation for laps; that is what this closes.

**What did not work, and was dropped.** I considered registering the withdrawn sentence in
`check_forbidden.py` instead, which would catch it in *any* file. It is the wrong tool: the sentence
is legitimately quoted in the backlog, in three critic reports and in this report, so the rule would
need a pragma on every one of them and would go red on the next lap that discusses it.

## The second thing, which was not my row

`decisions.py list` — the interface NH-020/§6b says you decide in — was reading you NH-028's answer as
part of NH-029's option C. NH-028's closure line (`ref: …#NH-028`, verbatim "Don't worry about the word
count … 25 pages") sat at the **end of the file**, which is inside NH-029's block, and the options
regex ran straight through it.

`decisions.py` is not the defect and I cleared it by replay rather than by reading: applying NH-028
against `8a8a940` places the line correctly, before `## NH-029`. The cause is an apply run on a
checkout whose last entry was still NH-028, merged afterwards with the NH-029 a cloud lap had pushed
below it — a shape the 3-hour cadence makes routine. The line is moved into NH-028, verbatim, with the
reason quoted in place. **WFG-112** files the read-only `decisions.py check` that would have caught it
(every closure's `ref` must match the entry it sits under); it is P1, not P0, because nothing is
corrupt today and CHARTER §14b holds loop mechanics behind the readiness lines.

**NH-029 gained a measurement, not a decision, and stays open for you.** After your re-freeze,
`make baseline-verify` here reports **2** differences, not six, and both are the git-ignored raw
manifests that exist only on your laptop. The four that were in every clone are gone. So
`make all-checks` should now run past `baseline-verify` **on your machine** — R3's own condition — and
the loop cannot verify that here, because here those two files are genuinely absent.

## Evidence

| what | where |
|---|---|
| the two corrected template lines | `scripts/finals.template.html:1378`, `:1381` |
| the gate | `tests/test_finals_template_sync.py` (9 tests, 7/7 mutations) |
| method, result, caveats | `docs/finals_screen_v2.md` §4.2 (table row in §4) |
| bundle re-derived | `release/kcf-finals-2026/MANIFEST.json` (17 files) |
| ledger repair + reason | `docs/auto/NEEDS_HUMAN.md`, NH-028 block |
| the gate that was missing | `docs/auto/BACKLOG.md`, WFG-112 |
| the hole the reviewer opened | `docs/auto/BACKLOG.md`, WFG-113 (P0) |
| lesson + addendum | `docs/auto/MEMO.md`, 2026-09-05 entry |

## Gates and review

`gates.py --mode full` exited **0** (1515 passed, 62 skipped; `baseline-verify` WARN = the two
laptop-only manifests, NH-029). GitHub's own `auto-gates` run on `auto/dev` was **success** at
`38620f2` before this lap started, so there was no red CI run to take as a first job.

**Reviewed by: subagent (block): the one sentence whose job was to disclose the new gate's biggest
hole asserted a compensating control that does not exist.** Fixed, not argued.

§4.2's first "what this does not cover" bullet said the exempt payload line's content is looked at by
`verify_numbers.py` and `check_forbidden.py`. The reviewer ran it instead of reading it:
`verify_numbers.py` **never opens `web/finals.html`** (zero occurrences of `finals`, `web/`, `html`),
and `check_forbidden.py` reads the file but limits every numeric rule to `.md` via
`is_authored_prose()` (`scripts/check_forbidden.py:226-228`), counting the skips into `payload_skips`
— a deliberate, documented exemption. Its nail: change `"n_entries": 326` to `999` on line 434 and
every guard I had named stays green (`test_finals_template_sync.py` 9 passed,
`check_forbidden.py` OK, `test_finals_screen.py` 32 passed). **A falsified number sits on the judged
screen with every gate green.** I re-verified all three against the tree before rewriting.

The bullet now says what is true, names `tests/test_finals_screen.py` as the partial cover it is, and
states plainly that a hand-edited payload **number** is caught by nothing today. **WFG-113** (P0)
files the gate, because the reviewer is right that this is a row and not a footnote: WFG-109 made
line 434 the single exempt line of the new identity gate, and that line is where every judged number
on the screen lives. Three smaller findings taken in the same pass: the §4 table row beside the one I
edited said `test_finals_screen.py (26개)` and the file collects **32**; the relocated NH-028 line left
no marker at its origin, so NH-029 now carries a one-line note saying where its text went (the
repository's own WFG-089 precedent); and the full-gate run recorded at `5f9a3b8` predated this work,
which step 8's re-run and `--assert-head` settle.

## Next row

**WFG-106** — the conditional rescue rate (`mr_uiseong_fa_rescue_rate` = 0.883) reaches no judge, and
울진·삼척's 23.1 %, the number that runs against the project, is spoken nowhere.
`docs/auto/DIRECTION.md` puts it above WFG-104, and critic #19's correction to read first is that it
answers the **dilution** objection, not the **opponent** one. Its caveat string is also wrong on two
of the three keys it is attached to and is fixed while there.

For the critic's direction check: **WFG-113** is P0, judge-facing and was not on the page when
DIRECTION.md was last written. Dev does not write that page (CHARTER §14), so it is flagged here
rather than reordered.

## In plain terms

- **What changed for the project.** The finals screen said the right thing, but the file it is built
  from still said the wrong thing, so one rebuild would have put a sentence you had already withdrawn
  back in front of the judges. That is fixed at the source, and a new check now guards every line of
  the screen's text — with one hole, which the reviewer proved and which I have written down rather
  than smoothed over: the single line that holds the screen's numbers is still unguarded, and
  WFG-113 is the row that closes it.
- **Why it matters to the judges or the science.** The withdrawn sentence made your comparison look
  stronger than it is: it described the thing you beat as a map that watches the fire, when it is a
  map that never looks at the fire at all. Being exact about what you beat is the difference between
  a result a fire scientist trusts and one they discount.
- **What you should do.** One thing, when convenient: run `make all-checks` on your laptop. Your
  re-freeze looks like it fixed the four differences that blocked it everywhere, and only your machine
  can confirm the last two. NH-029 is waiting on that; nothing else needs you.



## Gates

**ALL GREEN** · mode `full` · head `5f9a3b8` · 2026-09-05T15:30:14Z · ⚠ **stale: the gates read `5f9a3b8`, HEAD is `9184a72`** — this table does not certify the pushed tree; re-run `gates.py --mode full` and `gates.py --assert-head` before pushing

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 16.9 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.2 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 0.7 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 244.3 s | 1515 passed, 62 skipped, 52 warnings in 242.71s (0:04:02) |

## Commits since the previous report (944243054a590dd80ca08af4d89716a1fb4ba6fd..HEAD)

- 9184a72 WFG-109: the withdrawn sentence is out of the source, and the gate is the whole file rather than the two lines
- 5f9a3b8 claim WFG-109 (20260905T1520Z)
- 38620f2 bundle plan = git-tracked files only (a local demo video broke the manifest on the laptop); baseline re-frozen after three cloud-lap artifacts

## Needs a human (5 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-029 [DECISION] The baseline freeze is stale, so `make all-checks` cannot pass — on this machine or on yours (by 2026-09-10)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
