# WildfireGuardian autoloop · dev · 2026-09-08T1605Z

| | |
|---|---|
| when | 2026-09-08 16:05 UTC · 2026-09-09 01:05 KST |
| branch / head | `auto/dev` / `28027b8` |
| kind | dev |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 9, done: 29, parked: 1, title-derivable: 1, todo: 96 |

## What happened this lap

Lap 2026-09-08T1518Z (dev). Claim commit `e2c9d5e`; work `f4475f7`.
Rows: **WFG-190** (critic #43's `fix-before-next-row` item, minutes) and **WFG-188**
(position 1 of the table by fall-through).

## Your decisions, applied

**Nothing new to apply, and I checked both channels.** Gmail, query
`from:siyeong0318@gmail.com subject:"WildfireGuardian autoloop" newer_than:14d`: every
matching thread holds exactly one message and every one of them is labelled `SENT` — they
are the loop's own reports coming back to the same address, not replies. PR #31's comments
return `[]` through the GitHub MCP. `docs/auto/decisions_seen.json` is unchanged and
`decisions.py apply` was not called.

**The open entries are unchanged and the Decisions block below is the current list.**
NH-032 and NH-034 are now two days overdue; NH-035, NH-038 and NH-043 are due tomorrow
(09-09); NH-046 is due 09-10 and, since R8 ticked last window, it is the single thing
holding eight P1 infra rows shut (CHARTER §14b, critic #43's F4). The last decision applied
to this repository was NH-031, on 2026-09-06.

⚠ **NH-045 (BLOCKER, due today) reads as answered by events rather than by a reply, and I
am not closing it on that.** It was opened when the finals screen's stamp reached the
staleness limit and shut the branch. Measured here: `web/finals.html` names `25f6b60` and
`git rev-list --count 25f6b60..HEAD` answers **12** against
`tests/test_finals_screen.py::STAMP_MAX_COMMITS_BEHIND` = **30**, so the gate is not near
firing and DIRECTION's 「say so at 20 or more」 rule does not trigger this lap. The entry
still asks you a question about the *mechanism* (which routine may run `make finals`), and
that question is untouched by the rebuild.

## What I did, and why

### Step 2b — the preemption: WFG-190, and it is prose only

Critic #43's one `fix-before-next-row` item is minutes-long, so the routine prompt's step
2b says it is honoured here. Two defects in one paragraph block of the Round-4 section that
ticked R8 last window.

**(a) A universal this repository falsifies.** `README.md:235-239` read 「이 실험이 내놓은
구체적인 margin 값들은 아직 **어느 심사용 자료에도** 싣지 않습니다」. `docs/auto/JUDGE_QA.md`
carries 9, 27 and 5, and `docs/auto/finals/printables/manifest_*.json` binds that file into
the 41-page printed kit under the title 「심사위원 질의응답 카드」. 「심사용 자료」 is defined
nowhere in this repository, so the sentence fails in the one way a scoped sentence could
not: it says 「어느 자료에도」 where naming the set would have been true. It now names the
set it means — finals screen, printed panels, submitted forms — and says the Q&A bank
carries the values, in the 「말하지 말 것」 form, and that the prohibition is the card's whole
reason for existing. The correction is written on the page rather than swapped in silently
(CHARTER §3.7). ⚠ My first draft of that note cited `WC-009` as the rule behind it; the
reviewer showed that `WC-009` says something else, and the id is gone (see the block below).

⚠ **The Q&A card was not touched.** Its block ends with an explicit ❌ forbidding the
student from speaking 9, 27 or 5 at the booth. The card is the correct half; the README
sentence was the wrong one.

**(b) A buffer-conditional result stated unconditionally.** The same block described the
present-perimeter arm with no mention that its buffer width is a free parameter, where
`paper/manuscript.md:493` says 「the buffer width is a second free parameter and nothing in
the data chooses it」 and `docs/present_perimeter_arm.md:116` gives that a section heading
saying it matters more than the headline. The paragraph now says the data did not choose
the width, that changing it moves **both** what the opponent recovers **and** what it
breaks, that the width this run used is a spike and not a plateau, and points at
`docs/present_perimeter_arm.md` §4 and the manuscript's own English sentence. 「상당 부분」
became 「대부분」, which is the direction the artifact actually sits in.

⚠ **No margin value and no new result figure went into `README.md`.** NH-032 and NH-034
are open and DIRECTION forbids it; the fix is qualitative throughout, and even the section
heading of `present_perimeter_arm.md` §4 is referenced by number rather than quoted,
because its title contains one of the contested values.

**The narrowing grep, on the subject and not on the sentence** (DIRECTION's standing rule),
with every file it returned named and dispositioned:

| subject | files | what I did |
|---|---|---|
| 「심사용 자료」 | `README.md`, `BACKLOG.md`, `CRITIC_LATEST.md`, `DIRECTION.md`, `KCF_READINESS.md`, `SCORECARD.md`, `reports/2026-09-08T1429Z-critic.md` | README is the only live surface and it is fixed; the other six are record class, quoting the finding in order to record it (CHARTER §3.5c) |
| 「상당 부분」 | `README.md`, `JUDGE_QA.md`, `finals.html`, `fold_sizes.md`, `dashboard.html`, + 3 record class | README fixed. The other three live instances are **different subjects** — authorship share at `JUDGE_QA.md:1099`, fold dispersion at `fold_sizes.md:31`, shelter-density mapping practice at `finals.html:1991` — and none of them is the present-perimeter recovery. Nothing else carried this claim. |

### Step 3 — the row: WFG-188, and the root objection I recorded against my own plan

WFG-188 reached position 1 by fall-through (WFG-187 and WFG-010 both closed last window).
The bank had 45 cards answering 책임, 개인정보 and 창의성, and none answering the question a
county emergency officer asks before any of them. Confirmed rather than inherited:
`grep -nE '군청|도입 비용|운영 비용|유지보수'` over `docs/auto/JUDGE_QA.md` returns 2 lines
and neither answers it. One of the five finals judges is a public-sector disaster-response
official.

**`hate`, on my own plan, before building it.** *An adoption card is a list of what a county
would need, and a list is a positive object. The honest first item is not on the list at
all.* The repository cannot build a hazard surface for today: hotspot detection is FIRMS
near-real-time, but the weather field the surface is simulated from publishes on a lag, so a
new detection decides **whether and where** to act on a **pre-computed** surface and cannot
move it (`docs/live_pipeline.md` §0). A card that opens with the recipe — two keys, four
layers, one laptop — and mentions the lag later, or not at all, reads at a booth as 「이건
지금 쓸 수 있는 물건입니다」. Every sentence in such a card is *literally true*, which is why
no forbidden-string gate would ever see it. And it would be critic #43's root objection
reproducing one lap after it was written: the strongest version of a limitation living in the
file nobody opens, the softest version on the surface a judge meets.

**The cheapest test, and it is what the card is built around:** put the constraint in the
**opening paragraph** and let an assertion hold that position, not merely its presence.

So Q16c (T1) opens with what it cannot do, then gives the four requirements — two account
keys, the four data layers plus OSM road network and refuges, one Python 3.11 machine with
no server and no GPU, and a human — then the warning that a new region is not a thing you
run but a thing you take apart (the committed DEM defect in `docs/multi_region.md` §4 moved
results), and ends on an explicit refusal: 「비용은 말씀드릴 수 없습니다. 저희는 어느
기관에서도 이걸 운영해 본 적이 없고, 도입 비용을 추정해 본 적도 없습니다.」 The 없는 것 block
carries five items, the fifth being the one I only saw while writing it: the four
requirements are what it takes to **run this once in that region**, not what it takes to
**operate it in a situation room** — no duty roster, no false-positive procedure, no data
refresh cycle, no named owner for an outage.

Bank 45 → 46 and T1 19 → 20; the header count, the tier line, the provenance paragraph and
the §6 drill table were all updated, and `tests/test_judge_qa_bank.py` caught the drill table
when I missed it.

### The gate, and what it cannot catch

`tests/test_adoption_card.py`, 14 assertions after the reviewer's fix (13 as first shipped). Every quantity the card states about this
repository is re-derived from the file that owns it rather than compared to a retyped copy:
the credential families come out of `docs/REPRODUCE.md`'s own §2 table, the pinning claim
from `requirements.txt` itself, the Python version from that file's declaration line, the
dry-run property from `scripts/send_dispatch_email.py`, the DEM-defect section from
`docs/multi_region.md`. Every backticked path in the 근거 block is resolved.

**Graded on six mutations, 5 caught.** M1 delete the lag clause → red. **M2 move the same
clause, intact, into the last paragraph → red**, which is the mutation that matters and the
one a section-scoped assertion would have missed: no lap deletes a caveat on purpose, it
relocates it while tidying. That is critic #41's finding (a scope assertion satisfied by a
string standing in some other claim's neighbourhood) answered by construction. M3 a cost
figure with digits → red. M5 one of the two credential families dropped → red. M6 the
refusal sentence rewritten to 「비용은 크지 않습니다」 → red.

⚠ **The mutation it does NOT catch, run rather than imagined (WFG-186's rule).** **M4: a
cost claim written in words.** 「수백만 원대면 충분합니다」 carries no digit, states a cost, and
passes all thirteen assertions — I inserted it and ran the suite. Every "no number" gate in
this repository matches digits, so the register that escapes is prose. Because naming an
escape is not a substitute for not shipping it (the 1303Z lap's lesson), the card carries the
refusal sentence explicitly and a second assertion binds that sentence.

### The kit

`make printables` rebuilt the booth kit at **20260908T1529Z**, 41 pages, and
`release/kcf-finals-2026/MANIFEST.json` was re-pointed in the same commit, as every edit to
a `SOURCES` file requires (WFG-152).

## What did not work

- **`make printables` refused the first build**, and correctly: the committed font cannot
  draw `④` or `∼`, both of which I had reached for in a numbered list. The fix is the card's
  characters (`1)`–`4)`), not the substitution table — adding a substitution adds a target
  that must itself be proved drawable. Recorded in MEMO.
- **I built the kit twice.** A `factchk` pass over my own card after the first build found
  「무료 키 두 개」 asserting that *both* external services are free, where `docs/REPRODUCE.md`
  says 「free MAP_KEY」 for FIRMS only and says nothing about the cost of a Copernicus CDS
  account. That is an unsourced claim about the world (CHARTER §3.5b). The card now says the
  FIRMS key is free 「그쪽 안내 페이지가 적고 있습니다」 and separately that **this project**
  uses no paid service, which is a repository fact. Fixing prose after a kit build means
  rebuilding the kit, so the first, uncommitted, build (20260908T1528Z) was dropped and only
  20260908T1529Z is committed. MEMO carries the ordering rule.
- **No `docs/<topic>.md` was written for WFG-188**, and that is deliberate rather than
  skipped: the row's own *Done when* is a card plus the kit, it explicitly adds no
  experiment, artifact or registry key, and the method-and-limits prose that would go in such
  a page is in the test module's docstring where the next lap reading the gate will meet it.

## The baseline and the gates

- `gates.py --mode quick` at `82d0680`: **exit 0**, read unpiped.
- `gates.py --mode full`, run **twice**, read unpiped both times. The first run recorded
  its pass at `e2c9d5e` because it started before the work was committed — the reviewer
  caught that and `--assert-head` exited 1 on it. **The run that certifies the pushed
  commit is the second one, at `28027b8` with a clean tree: ALL GREEN, exit 0,
  `1763 passed, 63 skipped, 2 xfailed`, pytest 464.7 s.** `verify`, `snapshot-verify`,
  `env-check` PASS; the `baseline-verify` WARN is the documented CHARTER §3d sandbox state.
- **Neither run downloaded anything:** `du -sb data/raw` answers **201,187** bytes before
  and after, and `data/raw/dem/srtm/` holds 0 files. Cold on the tile.
- **CHARTER §4b, GitHub's own run:** through the GitHub MCP (`curl` against `api.github.com`
  is still refused here, WFG-119). The newest `auto-gates` run on `auto/dev` is **250** at
  `82d0680`, `success`. Runs 243–250 carry **zero** failures. **No §4b finding**, so no red
  run preceded this lap's row.
- `--assert-head` and `--assert-reported` both run immediately before the push.

## The independent review BLOCKED this lap, and it was right

`LOOP_CONFIG.json` → `review: subagent`. A fresh agent that had not seen the work read only
`git diff e2c9d5e..HEAD` and the claims, and ran `hate`, `factchk` and `mandela`.

**Reviewed by: subagent (block): the claim 「the test re-derives every quantity from the file
that owns it」 does not survive contact — `test_the_two_credentials_...` parses
`docs/REPRODUCE.md` §2 into `rows`, asserts `rows` is non-empty, and then never uses it; the
comparison is against `{"FIRMS", "CDS"}` hand-typed one line below, checked as bare
substrings of the whole card. The derivation is decorative, and the docstring states the
property the code does not have.**

**Its first nail, run rather than argued:** rewrite the credential item to 「계정 키 한 개 —
Copernicus CDS 키입니다」 and all thirteen assertions stayed **green**, because `"FIRMS" in
card` was satisfied by the token standing three lines above **inside the ERA5-lag sentence of
the opening constraint paragraph**. That is critic #41's defect — a scope assertion satisfied
by a string in another claim's neighbourhood — shipping for the fourth time, and this time
the innocent neighbour was the very limitation the card had been restructured to lead with.
Two corroborating escapes it also ran: a **third credential row added to `REPRODUCE.md` §2
itself**, card untouched, stayed green (directly falsifying my docstring), and the card
saying 「세 개」 or dropping the count entirely both stayed green, so the 「두 개」 was bound by
nothing at all.

**I did not argue. I fixed it, in `f4475f7`'s follow-up commit:**

- the count now comes from `len(rows)` through a numeral map, so REPRODUCE growing a row
  turns the card red;
- the identities now come from the row **variable names** (generic segments dropped, an
  identifying prefix of ≥3 characters accepted so a card writing 「Copernicus CDS」 satisfies
  `CDSAPI_URL`), with nothing about which services those are written in the test file;
- the identity check is scoped to the card's own `**1)**` enumeration item, via a helper
  whose docstring carries the reviewer's break as its reason;
- the WFG-186 disclosure block now carries **this escape too**, marked as found by the
  reviewer and not by me.

**All four of the reviewer's mutations now go red**, re-run here after the fix: P1 (one key
named) red, N1 (third row in REPRODUCE) red, N2 (「세 개」) red, N3 (count dropped) red. The
original six-mutation grading is unchanged and still stands (M4, the worded cost claim, still
escapes and is still disclosed).

**Its two secondary findings, both acted on.**

1. **The gate pass was over a dirty tree.** `.auto/gates.json` recorded the `full` pass at
   `e2c9d5e`, the *claim* commit, because the run started before the work was committed;
   `--assert-head` exited 1 saying so. The numbers were real and nothing certified them
   against the pushed commit. CHARTER §4 step 8 exists for exactly this, and the gates were
   re-run on the commit actually pushed. The gate table in this report names that head.
2. **A misused registry id on the front door.** My README correction note cited
   `WC-009` as 「the rule that you never write a flat sentence about what a document
   contains」. `WC-009` is real but says something else: it is about a flat present-tense
   assertion of how *another agency's console behaves*, registered from the NIFoS ignition
   point. That generalisation is critic #43's paraphrase in a backlog cell, not the
   registry's text, and a judge who opened the file would have found a rule about something
   else. The citation is removed; the sentence now states the class in its own words with no
   registry id behind it.

⚠ **The lesson I am keeping, in MEMO:** *reading the owning file is not deriving from it —
the assertion has to consume what it read.* And the second-order one, which is the reason
this step exists at all: a WFG-186 disclosure block is written by the same agent that wrote
the gate, from the same model of it, so it lists the limits that agent already knew about and
cannot find the limit the agent was wrong about. Mine named one real escape and missed the
one that mattered.

## Next row

**WFG-128** (P0, one lap), unless the critic moves the page: `docs/multi_region.md:189-194`
carries `fa_exceeds_budget` with no budget qualification and no pointer to
`docs/present_perimeter_arm.md`, on the page the README's own link sends a judge to. It is
the one number in this repository that runs against the project, and it is misstated on a
page a judge reaches from the front door. NH-031 already decided the registry half, so the
prose half needs no open decision.

⚠ **Said plainly for the next lap:** with R8 ticked, **R3 is the last of CHARTER §14b's six
readiness lines**, its row WFG-179 is `blocked(NH-046)`, and no lap may tick it. Eight P1
infra rows wait on one email reply. Nothing in the loop's power shortens that queue.

## In plain terms

- **What changed for the project.** Your front page said the contested margin numbers were
  in none of the judging materials — but they are in your printed Q&A booklet, as the list of
  things you must *not* say. I fixed the sentence to say exactly where they are and where
  they are not, and I added the missing warning that the 1 km buffer in that experiment was
  chosen by a person, not by the data. Then I wrote the Q&A card for the question your
  county-official judge will ask first: 「저희 군에서 쓰려면 무엇이 필요합니까?」
- **Why it matters to the judges or the science.** The new card opens by saying what the
  system *cannot* do — it cannot build today's fire risk map today, because the weather data
  arrives days late — and only then lists what a county would need. That order is the whole
  point: it is the difference between an honest tool and a sales pitch, and a test now fails
  if that sentence ever drifts out of the opening paragraph. The booklet was reprinted; it is
  41 pages and now 46 cards.
- **What you should do.** Two things are waiting on you and only you. **NH-032 and NH-034**
  are two days overdue, and until you answer, the student cannot say any of the three margin
  numbers to a judge. **NH-046** is due 09-10 and it is now blocking eight queued items by
  itself. Three more (NH-035, NH-038, NH-043) are due tomorrow. Easiest path: open Claude Code
  in the repository and type `decisions`.



## Gates

**ALL GREEN** · mode `full` · head `28027b8` · 2026-09-08T16:05:29Z · current at `28027b8`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 25.9 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.2 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 0.8 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 464.7 s | 1763 passed, 63 skipped, 2 xfailed, 52 warnings in 462.72s (0:07:42) |

## Commits since the previous report (dee1bc1ca469b71d1c8786a820a131eac1b659b5..HEAD)

- 28027b8 the reviewer's block: the derivation was decorative, and the docstring claimed the property the code did not have
- f4475f7 WFG-190 + WFG-188: the front door stops stating a universal it falsifies, and the adoption card leads with what it cannot do
- e2c9d5e claim WFG-190 + WFG-188 (20260908T1518Z)
- 82d0680 critic #43: a readiness line ticked, and the section that ticked it is the softest of the four surfaces saying the same thing

## Needs a human (19 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09)
- NH-036 [DECISION] One critic lap told the next one not to edit a file, and that is what kept a false sentence in front of a judge for a window (by 2026-09-10)
- NH-037 [DECISION] The paper's word proxy now stops it a thousand words before your 25-page rule (by 2026-09-10)
- NH-038 [DECISION] Your "product first" rule has spent the last three dev laps on documents, and the readiness line it was written to protect has not moved in five critic laps (by 2026-09-09)
- NH-039 [DECISION] The national wildfire-spread system's manual is an 18 MB PDF the sandbox could not fetch, and one of you can (by 2026-09-12)
- NH-040 [FYI] A critic lap pushed one commit past a red `--assert-reported`, and it is telling you rather than hiding it
- NH-041 [FYI] This lap sent you an email containing only the word PLACEHOLDER, and could not take it back
- NH-042 [DECISION] Two of your own rules collide whenever a withdrawn claim lives in a frozen artifact, and this week they collided three times (by 2026-09-10)
- NH-043 [DECISION] A gate your loop built this morning will go red about twice a day, and the charter tells the lap that meets it to stop working (by 2026-09-09)
- NH-045 [BLOCKER] The staleness gate has closed `auto/dev` to every routine, and the one routine that met it is the one forbidden to clear it (by 2026-09-08)
- NH-044 [DECISION] The claim the paper just retracted is still live on the page the paper cites for it (by 2026-09-09)
- NH-046 [DECISION] Your product's definition-of-done names a command nothing in this project has ever run (by 2026-09-10)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
