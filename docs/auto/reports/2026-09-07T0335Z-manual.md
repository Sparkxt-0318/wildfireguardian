# WildfireGuardian autoloop · paper · 2026-09-07T0247Z

| | |
|---|---|
| when | 2026-09-07 03:35 UTC · 2026-09-07 12:35 KST |
| branch / head | `auto/dev` / `5cca6ce` |
| kind | manual |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 7, done: 21, parked: 1, todo: 84 |

## What happened this lap

# Paper lap 14 — a restraint lap, the row it proved this routine cannot close, and four numbers it did not check

**Reviewer verdict: `block`**, on a registered and doubly-blocked row (WFG-150(a)), plus four
wrong numbers in this lap's own ledger prose. The four are corrected; the row is not this
routine's to close and the report says why. Nothing in the manuscript itself was found wrong.

**Incorporated:** `5cca6ce` (was `850fd83`). **Manuscript:** 8,985 body words, **23 pages**
under Carlito (measured this lap), 8 figures, 4 tables, 29 references, 7 gaps.
**Margins: two pages against the author's 25-page rule (NH-028); fifteen words against the
9,000-word proxy.** `check_paper.py` exit 0; `gates.py --mode full` **ALL GREEN**, exit 0,
`1638 passed, 56 skipped, 2 xfailed` (**warm**), 357.1 s, at the head this lap pushes.

## What moved in the code, and what the manuscript owed it

Since `850fd83` the code moved in four ways, and I checked each against the manuscript
rather than assuming:

1. **`README.md`'s TL;DR gained both binding caveats** (WFG-138, then WFG-148). The
   manuscript already carries both — the fire-blind control in the Abstract, §4.3 and §7,
   the upper-bound/noiseless clause in §4.5 and §6. What it lacks is **locality**: the two
   blocks that carry the 42 name the control and not the bound. That is WFG-150(a), and see
   below — it is not mine to close.
2. **`WC-005` was registered** (`docs/withdrawn_claims.md` §5d): the booth kit's evidence
   sheet, withdrawn by the 2026-09-07T0018Z dev lap and registered in the same lap only
   because its reviewer caught the omission. **This is the one thing the manuscript owed.**
3. **The booth kit was rebuilt** (`docs/printables.md`, `scripts/build_printables.py`).
   Nothing the paper claims.
4. **Two gates were added**, one of which reads this manuscript
   (`tests/test_future_aware_attribution.py`). Relevant to §3.5 and deliberately not bought
   this lap — see "what I declined".

**Nothing in the manuscript had gone false.** Critic #31 ran `factchk` over the only new
external prose in the window — lap 13's related-work paragraph on the two Korean operational
systems and its two `references.bib` entries — against CHARTER §3 rule 5b, and cleared it:
agency, series or publication date, scope, and an explicit statement that no accuracy or
validation figure exists on either page; the manuscript compares accuracy in neither
direction. So there was no mandatory correction to fund, and this lap did not manufacture
one.

## Sections changed

**§3.5 only, one sentence, net +2 words (8,983 → 8,985).** The registration-failure sentence
was about one event and is now about two:

> before — "a September 2026 retraction skipped it, and the hand-applied correction left the
> same claim standing further down a file it had already edited"
> after — "September 2026 retractions skipped it twice, and each hand-applied correction
> left the same claim standing in a file that lap had itself edited or printed"

**edited** is WC-004 (a lap corrected the claim on Q30's card and left it standing in Q35's
block of the same file); **printed** is WC-005 (the lap declared the claim false in three
places, registered it in none, and its reviewer found it alive in four places in
`docs/auto/finals/BOOTH_SETUP.md` — the first of the kit's five `SOURCES`, in the PDF that
lap had just built — while `docs/submission_reconciliation.md`, the sheet the claim said did
not exist, was bound into the same kit). A recurrence about thirteen hours apart, the second
inside the very lap that had copied the first one's lesson into its own notes, is materially
stronger
evidence for §3.5's own thesis — the mechanical scan, not the hand sweep, is the load-bearing
part — than a single anecdote was.

No other section changed. No figure was added or redrawn: **all nine `FIGURES` re-rendered
byte-identically** from the committed artifacts, with `fonts-crosextra-carlito` and
`fonts-nanum` newly installed in this sandbox — neither is in `style.py`'s fallback chain,
which is why installing them moved no figure. F9 stays drawn and unreferenced (G8, NH-032).

## Gaps: 7 opened, 7 closed, net 0 — none moved

No `[GAP]` opened or closed. Two ledger entries were added to `paper/GAPS.md`.

## ⛔ The finding: WFG-150(a) cannot be closed by this routine, and the reason is not the one the row gives

WFG-150(a) asks that the Abstract's and the Conclusion's claim blocks gain the upper-bound
clause. The backlog row says the manuscript half is **blocked on NH-037** (the word budget).
That is true and it is **not the binding blocker**.

`tests/test_future_aware_attribution.py::test_the_manuscript_claim_blocks_do_not_yet_name_it`
is `xfail(strict=True)` **by design**, so that the day the manuscript gains the clause the
test goes red and asks `paper/manuscript.md` to be promoted into `ORACLE_SURFACES` rather
than sitting there as a permanent excuse. That design is right. Its consequence is that
closing the row from inside `paper/` **ships a red suite**. I measured this rather than
reasoned about it: I wrote the clause into both blocks, ran the file, and it went from
`17 passed, 2 xfailed` to `1 failed, 17 passed, 1 xfailed` (`XPASS(strict)`); then I
reverted. The repair — promoting the surface and deleting that test — lives in `tests/`,
which CHARTER §12 does not let this routine touch.

**So WFG-150(a) is a dev-lap row, or a paper lap coordinated with one in the same commit.**
A paper lap that closes it alone fails `gates.py --mode full` and parks under CHARTER §3
rule 9. Recorded in `paper/GAPS.md` at the top, where the next paper lap will read it before
trying.

## What I declined to buy, and why it is NH-037's cleanest evidence

§3.5 ends "This manuscript is scanned by both gates like any other document here." That is
now an undercount: since `786318c` it is scanned by a **third**, which names
`paper/manuscript.md` in its `SURFACES` and refuses any block of it that states the headline
contrast without naming the fire-blind control **in the same block**. That gate is also the
first here whose own **false-negative rate is re-derived on every run** rather than quoted
from a report — scored in both directions against fourteen sentences a reviewer wrote without
having seen its patterns, with the class it cannot reach kept as a `strict` xfail instead of
being regexed away. That is a better instance of §3.5's own point than anything currently in
the paragraph, and it belongs there.

The shortest honest form costs **+12 words**. I had **15**. Spending them would leave the
next mandatory correction with three, which is the state NH-037 says forces a park. **Not
bought.** Recorded in `GAPS.md` and `paper/README.md` so the lap that gets the budget can buy
it immediately.

This is worth the author's attention because it is a **new mode** of the NH-037 problem and a
milder one than lap 13's. Lap 13's budget chose the *wording* of a required sentence twice and
was wrong twice; the reviewer blocked it. This lap's budget decided **whether an optional true
sentence gets written at all**. Nothing wrong shipped. But it is the same mechanism one step
earlier, and it will not stay mild.

## What did not work / what I did not do

- I did **not** add the oracle clause to the manuscript's claim blocks (above).
- I did **not** compress prose to fund an optional addition. `paper/README.md`'s own warning
  — "a lap under this budget will write a wrong sentence before it writes a long one" — was
  earned by lap 13's two reviewer blocks, and the addition here was optional.
- I did **not** touch `tests/`, `docs/NUMBERS.json`, `docs/auto/BACKLOG.md` or anything else
  outside CHARTER §12's paths.
- One self-inflicted red and its fix, recorded because it is exactly the machinery §3.5 is
  about: my first `GAPS.md` draft quoted WC-005's withdrawn English spelling verbatim, and
  `check_withdrawn_claims.py` named the line — `paper/GAPS.md:59` — on the first run. Licensed
  with the per-line `<!-- forbidden-ok: en evidence sheet does not exist -->` pragma, which is
  the sanctioned way to keep a record line (CHARTER §3.5c). The gate caught its own author.

## The independent review

**Reviewed by: subagent (block): the self-graded 42-of-458 headline reaches the Abstract and
the Conclusion carrying the fire-blind caveat but not the caveat that it is scored on the
model's own predicted field — the paper's most-read sentence is the one place its own
evidence discipline is not applied.**

The reviewer's root objection **is WFG-150(a)**, arrived at independently: it read §4.3's own
concession that no origin can enter the hazard under the forecast-aware policy "which is
structural", saw that §6 and §4.5 state the consequence plainly and that neither statement
reaches the two blocks a reader actually reads, and then found the repository's own live
`xfail(strict=True)` gate whose reason string names the same defect. It agrees the row cannot
be closed from `paper/` alone and that recording why was right. So the block stands on a
registered, escalated, doubly-blocked row (NH-037 for the words, `tests/` for the gate), not
on anything this lap introduced — and reverting this lap's change would not touch it.

**It also knocked down four numbers, and all four were mine, in the paragraphs about not
letting unchecked claims stand.** I re-derived each before accepting it; all four were wrong.

1. "pages 1–5 … pages 21–23" of the printed kit — **not derivable at all.** The pre-fix
   `manifest_20260907T0032Z.json` records `pages: 33` while its own `pages_per_source` sums
   to **38**, and the internally-consistent `0059Z` manifest is the build that *fixed* the
   defect. The two registry files disagree besides (`withdrawn_claims.json` "page 2" against
   `withdrawn_claims.md` §5d "1~5"). I had restated the registry instead of checking it.
   Now stated as source order with no pagination.
2. "18 passed" — the baseline is **`17 passed, 2 xfailed`**; I dropped the second xfail from
   both halves. Re-run and corrected. This was the lap's own load-bearing measurement.
3. "three days apart" — `828bbae` is 2026-09-06T11:13Z and the WC-005 lap 2026-09-07T0018Z:
   **about thirteen hours.** Corrected.
4. Two more it found that this routine may not fix, both recorded in `GAPS.md` for a dev lap:
   `withdrawn_claims.json` WC-004 still carries the framing `withdrawn_claims.md` §5a
   refutes (the machine-read surface is the stale one), and the new gate's xfail reason still
   quotes "17 words of headroom", which this lap moved to 15.

**It cleared the manuscript change itself.** It could not break "twice", "edited" or
"printed" — it checked `git show --stat 828bbae` for the first and the printables manifest's
`SOURCES` for the second — and it found the sentence does not overstate. Its one note: the
sentence flattens the asymmetry that WC-005 was caught in-lap, which is "harder on the
project than the record requires, i.e. the safe direction". It also re-derived the
manuscript's headline numbers against the registry and Table 2's internal arithmetic and
found no error. Its cheapest-falsification suggestion for the load-bearing G4 gap — intersect
the Yeongdeok fold's 597 committed false-positive cells with the cells the router actually
refused, instead of waiting for the undistributed FIRMS hindsight field — is a **dev-lap**
item and is not filed here, because `docs/auto/BACKLOG.md` is outside this routine's paths.
It is in this report so the next critic can file it.

## In plain terms

- The paper says one new thing: the check that stops a retracted claim from surviving in the
  repository failed at the same step twice in one day, not once — a stronger argument for the
  automatic scan than the paper had before. Nothing else changed, because nothing else needed
  to. The reviewer then caught me doing the same thing one file over: four numbers I copied
  into the paper's own notes without checking. All four are fixed.
- What is still missing: where the paper states its headline "42 of 458", it does not repeat
  right beside the number that this is an upper bound. The sentence exists in §4.5; it is
  just not next to the number, and that is what the reviewer blocked on. I could not add it —
  the paper is 15 words from its own limit, and the repair also needs a test file this
  routine is not allowed to edit. It needs one dev lap, not you.
- What you should do: answer **NH-037** (I would pick **C** — raise the word limit to the
  measured 9,461-word point *and* add the one `apt` line to CI so a clean machine counts
  pages for real). Until then the paper is deciding what to say by counting words, which is
  the wrong way round.



## Gates

**ALL GREEN** · mode `full` · head `5cca6ce` · 2026-09-07T03:18:07Z · current at `5cca6ce`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 21.8 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.2 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 0.5 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 357.1 s | 1638 passed, 56 skipped, 2 xfailed, 52 warnings in 355.32s (0:05:55) |

## Commits since the previous report (3f881f6d323773e526a4e3a30b18ca34f6016166..HEAD)

- 5cca6ce the critic report names the head it ships in, and how to read its own backlog line
- 900c407 critic #31: the booth kit shipped and the bundle a judge is handed still does not contain it

## Needs a human (13 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09)
- NH-036 [DECISION] One critic lap told the next one not to edit a file, and that is what kept a false sentence in front of a judge for a window (by 2026-09-10)
- NH-037 [DECISION] The paper's word proxy now stops it a thousand words before your 25-page rule (by 2026-09-10)
- NH-038 [DECISION] Your "product first" rule has spent the last three dev laps on documents, and the readiness line it was written to protect has not moved in five critic laps (by 2026-09-09)
- NH-039 [DECISION] The national wildfire-spread system's manual is an 18 MB PDF the sandbox could not fetch, and one of you can (by 2026-09-12)
- NH-040 [FYI] A critic lap pushed one commit past a red `--assert-reported`, and it is telling you rather than hiding it

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
