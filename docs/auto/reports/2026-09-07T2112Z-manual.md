# WildfireGuardian autoloop · paper · 2026-09-07T2047Z

| | |
|---|---|
| when | 2026-09-07 21:12 UTC · 2026-09-08 06:12 KST |
| branch / head | `auto/dev` / `64f015b` |
| kind | manual |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 8, done: 25, parked: 1, todo: 87 |

## What happened this lap

# Paper lap 17 — the registry does not cross a language, the lap's first repair put that in the wrong place, and the count it defended was its own routine's miss

**Incorporated:** `6ee996b..64f015b` (13 files outside `paper/` and `docs/auto/`).
**Document:** 8,995 body words (8,992 → 8,995), 8 figures, 4 tables, 29 references, 7 gaps.
**Pages: 23, re-measured this lap** under Carlito (page objects and page-tree `/Count` both 23).
**Margins: 2 pages under the author's 25 rule; 5 words under the 9,000-word proxy — the tightest yet.**
**Gates:** `gates.py --mode full` ALL GREEN. `check_paper.py` exit 0.
**Reviewed by: subagent (block)** — *"The edit treats a spelling failure as a scope failure, and by doing so leaves the assertion it was written to repair standing verbatim."* Both objections were verified against the commits, both were right, and both are fixed below. Nothing was argued.

## The defect the lap started from, and it is real

§3.5 asserted, of the withdrawn-claims registry:

> and every gated document is read against it, so a withdrawn claim **cannot survive in a
> prose file nobody thought to list**; a data file or a generator escapes it.

The incorporated diff falsifies that first clause. `docs/withdrawn_claims.md` §5g-2 records
**WC-008** registered with an **English** spelling while the same claim went on living in
**Korean** inside `docs/auto/JUDGE_QA.md` Q16a — a **gated prose file**, and specifically the T0
card the student reads aloud to five judges. §5g and §5f record the mirror image ninety minutes
earlier: **WC-007** went in in Korean, its **English** twin alive in
`docs/auto/knowledge/KOREAN_OPERATIONAL_SYSTEMS.md` §3.3. Two claims, one day, opposite
directions, `check_withdrawn_claims.py` green throughout.

## ⚠⚠ The lap's first repair was a category error and the reviewer killed it

The draft appended "or the same claim in the repository's other working language" to the **escape
list**. That is the wrong list, and it leaves the broken half standing:

- The other two members escape by **scope** — a `.json` manifest and a `gated: False` generator
  are never opened at all (`scope.extensions` = `['.md','.html']`). The Korean copy escaped by
  **spelling**, inside a file the scanner *did* open and pass, green, all day. The sentence would
  have read: *prose files are covered, except this prose file.*
- It was an **overclaim in kind**. A data file escapes no matter what is registered; the other
  language escapes only **until a lap registers it**, which the 1820Z lap did the same day
  (`wc008-household-walk-out-negative-ko`). A contingent gap closed within hours would have been
  sold to an IEEE reviewer as a structural property of the instrument.
- The project's own registry files it the other way: §5g calls the cross-language case
  「§4 가 모든 패턴에 대해 적어 둔 한계가 그대로 실현된 것」 — an instance of the "matches spellings,
  not meaning" limit the manuscript states two sentences later.

**The repair puts it where the evidence puts it.** The escape list is back to its lap-15 wording
verbatim. The spelling sentence now opens "It matches spellings, not meaning, **and one language
at a time**" — analytic, since a registered spelling is a spelling in a language, so it needs no
probe — and the probe stays attached to the rewording limit it actually measured. It closes "and
**both limits** are recorded rather than designed away". **+6 words.**

## ⚠⚠ The count was wrong, and the instance it omitted was this routine's own

§3.5 read "September 2026 retractions skipped it **twice**". **It is three.** The first draft of
my ledger asserted "twice" on a justification that was **false** — that WC-008 was registered
inside the lap that withdrew it. Checked against the commits rather than the ledger's summary,
which is how the draft got it wrong:

- **Withdrawal:** paper lap 16, `8ff1b40`, **15:30Z** — retracted the claim from manuscript §2
  and, *in that same commit*, wrote the retracted sentence verbatim and **unlicensed** into
  `paper/GAPS.md:77` and `:79`.
- **Registration:** a **different routine**, dev lap `20260907T1528Z`, `7cc4eb7`, **16:12Z** —
  whose own commit message reads "Three record lines licensed with the token rather than
  reworded … `paper/GAPS.md` 77 and 79".

Both halves of the manuscript's own sentence fit that instance: the correction was hand-applied,
and it left the same claim standing in a file that lap had itself edited. CHARTER §12 forbade
paper lap 16 from touching `withdrawn_claims.json` — but whether a lap was *permitted* to
register is a different question from whether registration happened, and the failure mode the
sentence describes materialised. Declining to count the one instance this routine authored would
have read as self-serving. Now "**three times**" (+1). WC-007 is *not* a fourth: its lap tried a
hand grep, its reviewer blocked, and it registered in-lap (§5f).

## The deleted clause is restored

The draft funded its escape-list edit by deleting ", and that limit is recorded rather than
designed away" and calling it self-congratulation. The reviewer showed it is payload
(`docs/auto/reports/2026-09-05T0317Z-manual.md:31` records it as the reason the paragraph exists
for an IEEE reviewer), and that lap 16 paid its own +11 with meaning-preserving **syntax**
compressions while this draft paid by deleting a sentence's **content**. Restored.

**What paid for the +7 instead:** four syntax compressions, none a caveat, none a number, none a
gap marker — "a document that states" → "a document stating"; "which the scan does read" → "which
it does read"; "a correction applied to a generated file" → "a correction to a generated file";
"like any other document here" → "like any other document". **Net +3.** Every caveat, lineage
warning, registered number and gap marker is byte-identical to lap 16.

## Figures, gaps, pages

- **No figure added or redrawn.** No new artifact under `data/processed/` in the incorporated
  diff, so no result section gained a number, a table or a figure. All nine figures re-rendered
  **byte-identically**, so none needed looking at. `F9_present_perimeter` is still drawn,
  committed and **not** referenced (gap **G8**, NH-032 open).
- **No gap opened or closed**; the count stays at **7**.
- **Pages re-measured, not inherited.** The edits moved `body_words` and therefore
  `built_pages_inputs`, which turned `check_paper.py`'s anchor red exactly as designed. This lap
  ran the `apt` line `paper/README.md` records and `measure_pages.py` returned **23**; the new
  anchor `295bbc7453f1ad14` was printed by the run that measured it, not hand-written.

## Subject grep (DIRECTION.md's rule), seven hits, all named

The one that mattered was mine: **`paper/GAPS.md:189`**, lap 15's ledger, ending "the
completeness claim is now scoped to what the instrument actually reads" — an own-voice
completeness assertion inside `paper/` that is still incomplete. **Annotated in place as
superseded, not deleted** (CHARTER §3.7). Past lap reports are record class and were left alone;
the two knowledge-note copies were already fixed by the 1528Z and 1820Z dev laps; two hits were
`Escape` keycodes and Gleason LCES "escape routes" in `web/`.

## Cleared by the reviewer, and one thing filed rather than fixed

Cleared: all 29 citations resolve in `references.bib` and carry a `verified` note; §2 is
consistent with `docs/related_work.md` rows 13–14 as narrowed at `7cc4eb7` (which closes NH-044
without any manuscript edit) and with both bib notes; §2's "walk out, and by which path" does not
collide with WC-008's registered pattern, which requires "along which path"; no other manuscript
sentence goes false from the incorporated diff.

⚠ **Filed as a dev-lap row, not a manuscript edit:** fifteen of the 29 references are verified
"via the Crossref record", and §2 characterises their *content* substantively (`li2017`,
`li2019`, `wahlqvist2021`, `cova2003`, `cova2005`, `finney2002`). **A Crossref record is a
catalogue entry** — the same standard lap 16 applied when it retracted the NIFoS sentence. The
paraphrases are title-derivable so none is called false, but the paper applies one provenance
standard to the Korean operational systems and a laxer one to the Western evacuation-routing
literature, and `check_paper.py:203` cannot see it because it only tests for the substring
`verified`.

## In plain terms

- The paper now admits its claim-retirement gate works one language at a time — a claim killed in English kept running in Korean, in the card you read aloud to the judges — and it now counts three registration misses this month instead of two, the third being this paper routine's own.
- Still missing: the same seven gaps as before. The biggest is still the hindsight-field routing run that needs your laptop.
- What to do: answer **NH-037** (the 9,000-word budget). Five laps running, the length rule and not the evidence has decided what the paper says, and this lap ended with five words to spare.



## Gates

**ALL GREEN** · mode `full` · head `64f015b` · 2026-09-07T21:12:25Z · current at `64f015b`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 15.4 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.1 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 0.3 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 243.3 s | 1688 passed, 56 skipped, 2 xfailed, 52 warnings in 241.95s (0:04:01) |

## Commits since the previous report (6d1d730df6c3b5b058ff49537413a11ddf981381..HEAD)

- 64f015b the dev report names the head it ships in, and the re-run that certified it
- 804e5b6 the reviewer blocked on a number I could not register, and on a tolerance grounded in a false premise

## Needs a human (17 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09)
- NH-036 [DECISION] One critic lap told the next one not to edit a file, and that is what kept a false sentence in front of a judge for a window (by 2026-09-10)
- NH-037 [DECISION] The paper's word proxy now stops it a thousand words before your 25-page rule (by 2026-09-10)
- NH-038 [DECISION] Your "product first" rule has spent the last three dev laps on documents, and the readiness line it was written to protect has not moved in five critic laps (by 2026-09-09)
- NH-039 [DECISION] The national wildfire-spread system's manual is an 18 MB PDF the sandbox could not fetch, and one of you can (by 2026-09-12)
- NH-040 [FYI] A critic lap pushed one commit past a red `--assert-reported`, and it is telling you rather than hiding it
- NH-041 [FYI] This lap sent you an email containing only the word PLACEHOLDER, and could not take it back
- NH-042 [DECISION] Two of your own rules collide whenever a withdrawn claim lives in a frozen artifact, and this week they collided three times (by 2026-09-10)
- NH-043 [DECISION] A gate your loop built this morning will go red about twice a day, and the charter tells the lap that meets it to stop working (by 2026-09-09)
- NH-044 [DECISION] The claim the paper just retracted is still live on the page the paper cites for it (by 2026-09-09)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
