# WildfireGuardian autoloop · dev · 2026-09-11T0437Z

| | |
|---|---|
| when | 2026-09-11 04:37 UTC · 2026-09-11 13:37 KST |
| branch / head | `auto/dev` / `cd97bc2` |
| kind | dev |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 10, done: 34, dropped: 1, parked: 1, title-derivable: 1, todo: 117 |

## What happened this lap

Lap 2026-09-11T0317Z (dev). **WFG-247 and WFG-248 both done**, in one lap and one kit rebuild,
as `docs/auto/DIRECTION.md` named them. Claim commit `f108419`; work `cd97bc2` plus this lap's
reviewer-block fixes. Baseline `c579c81` green; GitHub `auto-gates` run **357** is `success` at
that exact head, so CHARTER §4b set no finding #1 for the thirteenth consecutive lap.
`CRITIC_LATEST.md` declared **zero** `fix-before-next-row` items, so the top row was taken directly.

## Your decisions, applied

**Nothing new, for the sixth day.** The Gmail search for `from:siyeong0318@gmail.com
subject:"WildfireGuardian autoloop" newer_than:14d` returns twenty threads and every one is a
report this loop sent; there is no reply from you in any of them, and the only `Re:` thread in
the mailbox is GitHub's own notice that PR #31 merged on 2026-09-05. PR #31 has **zero**
comments. `docs/auto/decisions_seen.json` still reads `"seen": []` and the newest applied
decision is still **NH-031, of 2026-09-06**. Nothing was guessed at and nothing was applied.

**24 decisions are open (23 DECISION + 1 BLOCKER), and 2 of them have no date at all.**
**NH-046** is past due on both clocks, **NH-049** and **NH-051** came due today. The sprint ends
**2026-09-15**, four days out.

## What this lap did

### WFG-247 — three populations in one breath, on the last thing five judges hear

The 마무리 said, verbatim: 「보행망 노드 **2,218곳을 전수 탐색**해서 ... **20가구** ... **24가구**
... **세 번째는 0가구**」. Three different populations, and the repository knew all three were
different:

| said | key | what it counts |
|---|---|---|
| 2,218곳 | `l0i_candidates_enumerated` | candidate **sites** surviving the constraint filter |
| 20 / 24 / 0가구 | `l0i_best_single_refuge_saved`, `l0i_best_pair_saved`, `l0i_third_refuge_gain` | `unit: households` on `sample: OSM 건물 124동(잠정)` |
| 지점 | — | one node of the OSM **walking** graph, and the demo's own 도입 claims it as this project's output unit (`JUDGE_QA.md` Q20a) |

And the first of those is not merely unbounded, it is **false**:
`refuge_placement.json :: optimum_h240.constraints` holds `n_walk_nodes_total` **8443** beside
`n_candidates_after_filter` **2218**, so 「보행망 노드 2,218곳」 names the filter's *input* with
its *output*. The reconciliation for the other two existed at `JUDGE_QA.md` Q19 — 190 lines away,
in another card, in another segment — pointed at by nothing, which is the exact shape `WC-013`'s
`say_instead` forbids: **the bound goes in the same block as the claim.**

Four surfaces, and none of them fixed by swapping 가구 for 지점, which would have made a true
sentence false — these really are buildings and `docs/NUMBERS.json` says so:

- the **spoken 마무리** gains one line, 「여기서 세는 **가구**는 판정 단위인 **지점**이 아니라
  **OSM 건물 124동**입니다」, and 「보행망 노드」 becomes 「후보 지점」;
- the **§3 화면/구두 table** labels each row's population and gains a `l0i_household_population` row;
- **금지 item 6** now refuses both errors — the verb (구했다 → 도달 가능해진다) and the bare count
  — and names the two cards that answer the follow-up;
- the **screen's refuge card** names all three populations in one card and stops calling 2,218
  보행망 노드 (`scripts/finals.template.html` → `web/finals.html` via `make finals`);
  `docs/finals_screen_v2.md` §2.4 carries the same under a dated ⚠.

**124 is registered, and deliberately not from the file that already held a 124.**
`scripts/register_refuge_population.py` adds `l0i_household_population` (124) and
`l0i_walk_nodes_total` (8,443) from `refuge_placement.json`, the artifact the numerators come
from. `bld_yeongdeok_n_mapped` in `building_origin_routing.json` is **also 124**, and citing it
would have been **WFG-244's** defect — a coincidence presented as a derivation — committed while
fixing the card that warns about it. The registrar upserts by exact key name and never sweeps its
prefix: the rest of the `l0i_` family was hand-written in Session 22 and has no registrar, so a
prefix sweep copied from the other registrars would have deleted four committed keys the screen cites.

### The root objection I recorded before building, and what it cost

`hate`, written into the claim commit: **마무리 is the segment whose length is budgeted by
syllable count**, so an appended caveat buys honesty at the price of a closing the student cannot
say, and the lap then scores itself green on gates that read a number table and never hear the
sentence. Cheapest test named there: measure the delta, and **rewrite rather than append if it is
positive**.

Measured: 마무리 **328 → 360** syllables, which under the declared 56 s made it the fastest
segment at **6.43** syl/s and blew the spread from 1.02x to 1.12x. The content is irreducible — a
judge must hear which population and that it differs from 지점 — and buying it by deleting another
sentence is what CHARTER §3.5 forbids. So it was paid through the **documented re-measure**:
`pace_20260911T0326Z.json`, `TAG` moved in `tests/test_demo_script_pace.py`, allocation
**36 / 42 / 47 / 58 / 56 / 61**, and the whole script now speaks at **5.92** syl/s where it spoke
at 5.81. The exchange is written into the script's own §1 — which segments paid, and that whether
32 syllables are worth it is the judges' question and not this repository's — rather than absorbed
silently by the allocator.

### ⚠ The re-measure disproved a claim the repository had made about itself (`WC-015`)

`docs/demo_script_pace.md`'s fixed-point section — the section that tells every later re-measure
what to watch for — had said 「It would bite at 61 s (육십일, four syllables)」. This lap's
allocation moved 마무리 to **exactly 61 s** and it did not bite: 육십일 is **three** Hangul blocks,
and `count_syllables(["마지막 61초는"])` and `count_syllables(["마지막 56초는"])` both answer **8**.
The mechanism is real and only the example was wrong, so the procedure stands and the example is
retired, dated, in place.

### ⚠ The reviewer BLOCKED this lap, and it was right twice

`Reviewed by: subagent (block): "The lap's headline act is correcting a page for arithmetic
asserted from memory — and in the same breath it ships two syllable counts that are themselves
asserted from memory and falsely attributed to a measurement."`

Both were real and both are fixed:

1. **The correction's own first draft said 「both count 7」** and attributed the 7 to
   `count_syllables`, which answers **8**. The cause is now recorded on the page because no gate
   can see it: `count_syllables` takes **a list of lines**, and a bare string passed to it is
   iterated character by character, returning a number that is neither the rule's answer nor an
   error. The substantive finding survived unchanged (8 = 8), which is exactly why the wrong
   number was easy to ship — it pointed at a true conclusion.
2. **`DEMO_SCRIPT_5MIN.md` §1 said the added sentence 「그것이 32음절입니다」.** The sentence is 33;
   the net is 32 because 「보행망 노드」 → 「후보 지점」 gives one syllable back. It now states the
   segment total (328 → 360), which is a field of two committed artifacts, instead of an
   unregistered length.

The reviewer also named three things to pick up, and all three are in:

3. **CHARTER §3.5c** — this lap withdrew two claims and had registered neither.
   **`WC-015`** (the 61-second fixed point) and **`WC-016`** (「보행망 노드 2,218곳」) are now in
   `docs/auto/withdrawn_claims.json`, in `docs/withdrawn_claims.md` §5, and in the registry test's
   families and probes, as a new family: *a claim about our own machinery* — neither needed a
   source, a survey or an outside fact, both were settled in seconds by this repository's own
   files. Blast radius measured on **flattened** text before registering: `WC-016` 6 hits across 5
   files, **zero gated**; `WC-015` 3 hits, **one gated** — the correction block that quotes the
   retired sentence in order to retire it, licensed by a line pragma.
4. **The registrar froze 「the demo's closing 56 seconds」 into both new registry entries** — a
   §3.2-frozen caveat carrying a number its own registration lap falsified. It now names the
   segment and says why it names no seconds.
5. **The new gate covers less than the commit message implied.** Two of its five declared surfaces
   (`finals.template.html`, `web/finals.html`) already satisfied the per-block property at
   `f108419`, and `_POPULATION` accepts a population named with no number. Both limits are now in
   the test's docstring beside the two it already stated.

The reviewer independently re-verified and reports as TRUE: both new registry keys really come
from `refuge_placement.json` and hold 124 and 8443; the mutation really turns the new gate red in
both halves and green on restore; the pace arithmetic (328→360, 36/42/47/58/56/61, 1,744→1,776,
5.81→5.92, headers summing to 300, concessions 1/1/2/1 with 1막 unmoved) all reproduces; and the
Q16d clause matches the Zenodo record's abstract, which it checked at the API rather than at this
repository's note.

### WFG-248 — one clause on the card that sends the student to open a DOI

Q16d rested this project's **method** differentiator on 「저쪽은 D\* Lite 로 다시 계획합니다」 while
that record's own abstract calls D\* Lite the **fallback** — 「During API constraints or in
wilderness scenarios, the system delegates to an internal D\* Lite heuristic grid fallback」 — with
OpenRouteService as the primary. The same card, three paragraphs later, tells the student to open
that DOI in front of the judge, so the judge reads the abstract standing next to them. The clause
now reads 「저쪽은 **OpenRouteService 로 경로를 내고**, 망이 닿지 않거나 API 가 막히는 상황에서
**예비로 D\* Lite 격자 계획으로 넘어갑니다**」, with a ⚠ that refuses the short form and quotes the
abstract. Every other fact critic #63 verified on that card is untouched.

### The gate this lap leaves behind

`tests/test_refuge_population_is_named_where_it_is_counted.py` — two properties that fail in
opposite directions. **Per-block:** a declared surface speaking 20가구/24가구 must name the
population in the same block, and *deleting* the count fails a second test rather than passing the
first. **Tree-wide:** no tracked `.md` or `.html` may call 2,218 보행망 노드 again, with CHARTER
§3.5c's record class exempt. **Shown red on its own mutation** on the identical tree — added line
removed, 후보 지점 put back to 보행망 노드 → 2 failed / 11 passed, naming both the file and the
token; restored → 13 passed. The reviewer reproduced the mutation independently.

### What did not work, and what this lap did not do

- **The first kit rebuild was wasted.** `make printables 20260911T0326Z` was built and committed,
  and then the reviewer's block changed `DEMO_SCRIPT_5MIN.md` §1 — a hashed kit source — so the
  kit was rebuilt at `20260911T0412Z`. The 0326Z PDF stays in the tree because CHARTER §3.2
  forbids overwriting it; the manifest points at 0412Z. **The rebuild was paid twice, and the
  second one is the reviewer's fault only in the sense that the first draft was wrong.**
- **Three red gate runs before the green one**, each a real thing the lap had not committed:
  an untracked pace artifact (`verify-numbers`), a screen built by the script rather than by
  `make finals` so its integrity panel said `verified: false`, and a `2.4` in this lap's own
  backlog note colliding with six `l0i_` keys (a section number, now annotated).
- **`docs/demo_script_pace.md` carried two more stale figures** the row did not ask for and this
  lap fixed while it was in there: the sayability bullet still said 5.81 syl/s and 1,744
  syllables, and the 「budget is well-spent」 bullet still gave 3막 61 s when the value moved to 58
  four days ago.
- **No new NEEDS_HUMAN entry.** Both rows were agent-doable and a twenty-fifth open question while
  twenty-four go unanswered would be noise. **NH-049 gained a measurement instead**: this lap
  priced the kit rebuild at **one** `make printables` for two rows, which is option A's premise
  with a figure behind it.

`Reviewed by: subagent (block): the lap corrected a page for asserting arithmetic from memory
while itself shipping two counts asserted from memory — 「both count 7」 where the module answers
8, and 「그것이 32음절」 where the sentence is 33.` Fixed, not argued with; the four further items
it raised are in as well.

**Next row:** **WFG-236** (P0, KCF), which `DIRECTION.md` has named second through four
consecutive critic laps. `docs/disc_null.md:20-21` and `docs/oracle_gap.md:180-181` both assert in
bold that nothing said what **0.394** should be compared with, while `README.md:520-522` and
`:888-891` have compared it to Rothermel's ~0.09 for months. It repairs the two `docs/` sentences
and pays no rebuild; whether the README's framing moves is **NH-055**, yours.

## In plain terms

- **What changed for the project.** The last sentence of your five-minute demo counted three
  different things — walking-path locations, buildings, and the "points" the project says its
  answers come in — and called them all by the wrong names in one breath. Each number now says
  what it is counting in the same sentence you say it, and the judge card that tells you to open a
  rival team's paper no longer describes that paper in a way the paper itself contradicts.
- **Why it matters to the judges.** "Citing your sources clearly" is a named criterion worth 20
  points on both scoring tables, and these were the last words five judges hear and the one card
  that invites a judge to check your homework live. Worth knowing: my independent reviewer
  **stopped this lap** because my own correction had a wrong number in it, and it was right — that
  is fixed, and so are four other things it found.
- **What you should do.** Nothing for this. But **24 decisions are waiting and 2 have no date at
  all**; NH-046 is past due and NH-049 and NH-051 came due today. The sprint ends **2026-09-15**.
  Answering even three of them unblocks work that is parked right now — 112 lower-priority rows
  are waiting behind a single reply.



## Gates

**ALL GREEN** · mode `full` · head `cd97bc2` · 2026-09-11T04:36:23Z · current at `cd97bc2`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 27.9 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.2 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 0.5 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 485.4 s | 2092 passed, 65 skipped, 3 xfailed, 52 warnings in 483.65s (0:08:03) |

## Commits since the previous report (0796336a595a3008b9ff8c4b82ef1e265e01073e..HEAD)

- cd97bc2 the closing 56 seconds names the population of every number it says, and the kit is rebuilt on both repairs
- f108419 claim WFG-247 + WFG-248 (20260911T0317Z)
- c579c81 the report stops naming a certifying sha it cannot know, and names the assertion instead
- d9ed99e the critic report names the head it ships in, and keeps the RED run its own finding is about
- f48876a critic #63: the last number five judges hear counts this project's own result in the unit it withdrew the day before

## Needs a human (29 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09, one day past; raised to HIGH by critic #55 on a measured third instance)
- NH-036 [DECISION] One critic lap told the next one not to edit a file, and that is what kept a false sentence in front of a judge for a window (by 2026-09-10)
- NH-037 [DECISION] The paper's word proxy now stops it a thousand words before your 25-page rule (by 2026-09-10)
- NH-038 [DECISION] Your "product first" rule has spent the last three dev laps on documents, and the readiness line it was written to protect has not moved in five critic laps (by 2026-09-09)
- NH-039 [DECISION] The national wildfire-spread system's manual is an 18 MB PDF the sandbox could not fetch, and one of you can (by 2026-09-12)
- NH-040 [FYI] A critic lap pushed one commit past a red `--assert-reported`, and it is telling you rather than hiding it
- NH-041 [FYI] This lap sent you an email containing only the word PLACEHOLDER, and could not take it back
- NH-042 [DECISION] Two of your own rules collide whenever a withdrawn claim lives in a frozen artifact, and this week they collided three times (by 2026-09-10)
- NH-043 [DECISION] A gate your loop built this morning will go red about twice a day, and the charter tells the lap that meets it to stop working (by 2026-09-09)
- NH-045 [BLOCKER] The staleness gate has closed `auto/dev` to every routine, and the one routine that met it is the one forbidden to clear it (by 2026-09-08)
- NH-044 [DECISION] The claim the paper just retracted is still live on the page the paper cites for it (by 2026-09-09)
- NH-046 [DECISION] Your product's definition-of-done names a command nothing in this project has ever run (by 2026-09-10)
- NH-048 [DECISION] One of the research routine's three literature channels has been dead for two runs, and a working replacement is already proven (by 2026-09-10)
- NH-049 [DECISION] Your critic routine is told to add judge Q&A cards and your own printing gate makes that impossible for it (by 2026-09-11)
- NH-050 [DECISION] You answered two of these questions two days ago and the loop never heard you, because you answered them on the routine page (by 2026-09-10)
- NH-051 [DECISION] The rule your loop has been obeying for three days is not the option it names, and the difference is why one row has been pushed down five times (by 2026-09-11)
- NH-052 [DECISION] The experiment that measures what your own forecast is worth is now one run away, and it would change what 42 means (by 2026-09-12)
- NH-053 [DECISION] The word your front door uses for 42 stopped being right today, and the loop cannot pick its replacement (by 2026-09-12)
- NH-054 [DECISION] Four fifths of your front door's headline is limits, and no lap is allowed to decide whether that is the project's strength or its biggest presentation risk (by 2026-09-13)
- NH-055 [DECISION] Your front door compares the headline forecast number to a model the same page calls broken, and today the loop measured a second, harsher comparison it is not allowed to put beside it (by 2026-09-13)
- NH-056 [DECISION] A Korean university published a reproducible deep-learning wildfire model four weeks before your finals, and a lap may not decide on its own whether to mention it (by 2026-09-13)
- NH-057 [DECISION] The one thing your project now says it contributes has never once been produced from a real fire, and only your laptop can change that (by 2026-09-13)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
