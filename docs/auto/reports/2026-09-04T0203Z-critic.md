# WildfireGuardian autoloop · critic · 2026-09-04T0203Z

| | |
|---|---|
| when | 2026-09-04 02:03 UTC · 2026-09-04 11:03 KST |
| branch / head | `auto/dev` / `5a0466e` |
| kind | critic |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 7, done: 10, parked: 1, todo: 29 |

## What happened this lap

**Critic lap #5.** Scope `a4dc9a7..5a0466e`: one backlog claim, the 0037Z manual lap, the
0100Z cloud dev lap, the 2026-09-04 laptop dev lap and its post-rebase follow-up. No code,
no artifact, no `paper/` file was touched by this lap; the writes are `CRITIC_LATEST.md`,
`KCF_READINESS.md`, `SCORECARD.md`, `BACKLOG.md` (WFG-051), a note under NH-018 and this
report.

**Gates, re-run independently at `5a0466e`: `gates.py --mode full` exits 0.** `1229 passed,
62 skipped` in 163 s. verify PASS (including the new `check-readme-figures` step),
snapshot-verify PASS, env-check PASS, `baseline-verify` WARN as expected off-laptop.
`auto/dev` is GREEN at HEAD for the second consecutive critic lap. Every dev report in the
window records a `Reviewed by:` verdict; `5a0466e` carries none of its own, which is F25 and
is why F22 is a finding.

## The findings, ranked

**F21 · fix-before-next-row · the "about 43 %" sentence is false and is in both opening
paragraphs.** `README.md:210-211` (ko) and `:528` (en) tell the reader that a like-for-like
comparison puts this fire at about 43 % of the 2025 national total, not 95 %. Three
independent checks say the two numbers it divides are not on one basis. (1) 산불영향구역 is
the area inside the fire line, including the ground inside it that did not burn, and it is
normally larger than the surveyed 피해면적; a 45,157 ha 영향구역 under a 99,289 ha 피해면적
for one fire is that relation inverted. (2) The 경향신문 article the repository itself cites
for the 45,157 row, opened this lap, frames it as the 산림청 estimate the joint survey more
than doubled, under the headline 「초기 추산 엉터리」. (3) 경북 99,289 + 경남 3,397 + 울산
1,190 = 103,876 ha, 99.1 % of the 104,788 ha total, leaving 912 ha for the roughly 340 other
fires of the season; the denominator already contains this chain at its surveyed figure. The
repository agrees in the one place a judge will not look: `fire2025_chain_share_of_nationwide_pct
= 94.8`. And the test written to ban exactly this ratio,
`test_the_nationwide_total_is_never_divided_by_the_chain`, scans only lines containing
`104,788`; both ratios sit on the following line, so it passes on the defect it names. The
fix is a deletion of one sentence in each language plus the 함정 6 correction, and widening
the tripwire from the line to the paragraph.

**F22 · fix-before-next-row · `--assert-reported` accepts a report that was edited rather
than written.** `scripts/auto/gates.py:97-127` passes as soon as any path under
`docs/auto/reports/` appears in the range, added or modified. Demonstrated on the window's
own last commit: `.auto/venv/bin/python scripts/auto/gates.py --assert-reported --base
98557b9` exits 0 for a range whose only report change is four lines appended to the previous
lap's file, alongside a 31-line `NEEDS_HUMAN.md` addition. `12b8ac7`, the commit this check
exists to refuse, would have passed the same way. Fix: build the `reports` list with
`--diff-filter=A`, plus two tests.

**F23 · fix-this-sprint · a bound value with a free attribution.** The registry calls
45,157 ha a 중앙재난안전대책본부 figure; the README and the sources table call it 산림청; the
cited article says 산림청, so the registry is the wrong one. 사망 26명 is a 중앙재난안전대책본부
count in the registry and a 경상북도 재난안전대책본부 count in `docs/data_sources.md:190`. And
`README.md:193-199` puts the whole chain parenthetical, 26명 included, under one 아시아경제
link that carries area, hours, homes, displaced and cost, and no death figure. Opened this
lap. `check_readme_figures.py` cannot see any of it: it asserts the provenance fields exist,
never that they agree. Filed as **WFG-051**.

**F24 · fix-this-sprint · `docs/auto/AI_DISCLOSURE.md` was deleted, not archived.** The
removal is the author's call and is documented (NH-008, CHARTER §9, KCF_READINESS R10
withdrawn). CHARTER §3 rule 7 says archive and say why, and `docs/auto/archive/` exists for
it. One `git show` into `docs/auto/archive/AI_DISCLOSURE_retired_2026-09-04.md` with a
three-line header closes it, and it also restores the document the booth answer at
`JUDGE_QA.md:611` used to be able to hand a judge.

**F25 · note · `5a0466e` has no reviewer verdict of its own** and is covered by four lines
appended to the previous lap's report. Honest, and the content is a good-faith escalation.
It is the concrete case that turns F22 from a theory into a finding.

**F11, F12 · open, unchanged.** `paper/` did not move, so F11 (no `## References` section,
WFG-045) cannot have. F12 re-verified: `scripts/auto/report.py:123` still has no `paper`
kind (WFG-044).

## The root objection

Critic #4 asked which *numbers* here can be wrong without a gate noticing. That set is now
much smaller: three laps gave every figure in the opening paragraph a registry key, an
artifact row with agency and as-of date and scope and status, and a URL a lap opened, and
two gates hold the paragraph to them. Checked cold against four primary pages this lap,
every figure the paragraph prints is right, including the 「1986년 이래 역대 최대 피해 면적」
superlative, which the 경상북도 release carries for this chain rather than being borrowed
from the national total.

The question this lap asks is which *sentences* can be wrong without a gate noticing, and
the answer is the ones that reason about the numbers rather than state them.
`check_readme_figures.py` checks that a value appears; it cannot check what the sentence
around the value asserts. F21 is that gap with a judge standing in front of it and F23 is
the same gap one level down, where a value is bound and its agency is free.

The loop already knew about F21. The laptop lap wrote it up as NH-018 item 2 and left the
sentence in place because it "could not open a primary page that states which basis the
104,788 total uses". That page is not needed. Parking a sentence the loop believes is wrong
in front of judges, as an author decision, costs more than a fourth rewrite would have.

## Readiness lines that moved

- **R3** gains this lap's independent re-verification: `gates.py --mode full` exits 0 at
  `5a0466e` in a fresh cloud sandbox, so green-at-HEAD is now two laps running. Still ☐ on
  the booth-laptop half (`BOOTH_SETUP.md`, WFG-037).
- **R8** gains the sourcing half: every figure re-opened at its primary page and holding,
  the artifact, the 16 keys, the gate and 44 tests. Still ☐ for two reasons now, no Round-4
  section and no abstract draft (WFG-010), and F21.
- **R10** is `—` (withdrawn, not failed), which is correct.
- Everything else unchanged. R1, R2, R4, R7 and R9 remain ☐ with eleven days of sprint left.

## Scorecard

A row was appended to both tables, because the diff plausibly moved three scores.
Track B `5a0466e`: 15 / 15 / 18 / 15 / 15 = **78** (was 75). 연구 목적 recovers the point
critic #4 took and stops at 15 because of F21; 제출 자료 rises two on the sourcing
apparatus, the largest single-window rise in the table. Track A `5a0466e`: 15 / 15 / 11 /
15 / 13 = **69** (was 67). 구현 및 유용성 held at 11 for the **sixth consecutive window**,
which is now the largest single risk in the sprint plan: nothing has touched `web/`, the
printables or the release bundle since 09-03, and WFG-017 has been named the next row by two
critic laps running.

## What this lap did not do

No code, test, artifact, figure, `paper/` file or `docs/NUMBERS.json` entry was touched. No
NEEDS_HUMAN entry was closed. NH-018 stays open with the evidence appended under it.

## Landed during this lap, after the scope closed, and not reviewed here

Two manual paper laps pushed `b621441` and `2a02477` while this critic run was in progress:
a 938-line rewrite of `paper/manuscript.md`, plus `references.bib`, `make_figures.py`,
`F1_system.png`, the `.docx`, `GAPS.md`, and a new **NH-019** asking what
`fire_manifest.json`'s `start` field is allowed to mean, which bears directly on the
detection section's `+22 / +34 / +64` minute claims. None of that is inside
`a4dc9a7..5a0466e`, so no finding in this report covers it and `check_paper.py` was not run
against it. It is the next critic lap's first scope. This lap's `dashboard.html` was
rendered before those two reports existed; the next report rebuilds it.

### And they left the branch red

`make verify` fails at `check-number-collisions` on `5f39452`: six collisions, all six on
line 146 of `docs/auto/reports/2026-09-04T0149Z-manual.md` and
`docs/auto/reports/2026-09-04T0150Z-manual.md`, where one sentence describing a fix those
laps had just made carries a single value on the same line as the anchor words of three
other registered keys. The gate names the three; the offending line is not reproduced here,
because reproducing it reproduces the collision. This lap's commit touches
neither file and `make verify` passed on this lap's own prose before the rebase. Filed as
**F26**, the first `fix-before-next-row` item in `CRITIC_LATEST.md`: annotate line 146 of
both files with `collision-ok: 66.7`, or re-wrap the sentence, before claiming any row.
This is the `24751fa` / `8d1decf` class a third time, and `--assert-head` would have caught
it had it been run.

## In plain terms

Three laps spent the whole window on the first paragraph a judge reads, and the numbers in
it are now right and every one has a source you can click. That was worth it.

One sentence in that paragraph is still wrong: it tells the reader this fire was "about
43 %" of the year's national total. It was about 95 %, and your own registry file says 94.8.
The test written to stop exactly this ratio looks at the wrong line and misses it.

Delete that sentence in both languages, in the next lap. It is a deletion, not another
rewrite. Then get someone onto the finals screen: it has not been touched since 09-03 and
it is the only thing the five judges will actually watch.

One more thing, and it is the urgent one. Two paper laps pushed while this report was being
written and they left the branch failing its own checks. Nothing is broken in the project;
a sentence in each of their two reports trips the gate that watches for contradicting
numbers. It is a two-line fix and the next lap has to do it before anything else, because
the rules stop a lap from building on a failing branch.



## Gates

**ALL GREEN** · mode `full` · head `5a0466e` · 2026-09-04T01:51:56Z · current at `5a0466e`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 15.7 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.1 s | make: *** [Makefile:152: baseline-verify] Error 1 |
| snapshot-verify | PASS | 2.2 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 164.5 s | 1229 passed, 62 skipped, 52 warnings in 162.81s (0:02:42) |

## Commits since the previous report (fbe71deb495c01bd9c8b2786696dea25fb14673a..HEAD)

- 5a0466e after the rebase: the registry gate fits the paragraph as the cloud lap left it, and the two disagreements go to the author
- 98557b9 the README's opening figures have keys, a gate, and a push check that sees prose-only commits
- d2f314d the share was a framing, the nationwide total was a different season, and the figures finally have a gate

## Needs a human (13 open)

- NH-001 [DECISION] Email delivery needs three repository secrets
- NH-002 [DECISION] Optional: `@claude` on GitHub issues and PRs
- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-010 [DECISION] Expert consultations and the firefighter record (by 2026-09-20)
- NH-011 [DECISION] One real, recorded email send from a network that works in Shanghai (by 2026-09-20)
- NH-012 [DECISION] Portal downloads the loop cannot do (by 2026-10-01)
- NH-013 [FYI] Optional: a stable web address for the visual board
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-016 [DECISION] The critic routine has 30 minutes to land its findings, and needs about 40 (by 2026-09-05)
- NH-017 [DECISION] Three entries were closed on replies the repository cannot see, and one of them was wrong (by 2026-09-06)
- NH-018 [DECISION] Two laps read the same primary sources and disagree on two sentences (by 2026-09-08)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
