# WildfireGuardian autoloop · paper · 2026-09-10T1534Z

| | |
|---|---|
| when | 2026-09-10 15:34 UTC · 2026-09-11 00:34 KST |
| branch / head | `auto/dev` / `c7c915b` |
| kind | manual |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 9, done: 33, dropped: 1, parked: 1, title-derivable: 1, todo: 111 |

## What happened this lap

# Paper lap 28 — the paper's own account of its evidence registry was false in two of five conjuncts; the reviewer found the second one, in the sentence the lap had just repaired

**Incorporated** `1983ff1..c7c915b`, **13** commits. Outside `paper/` and `docs/auto/` the
diff touches **8** files: `docs/judge_qa_gates.md` (new), `docs/oracle_gap.md`,
`release/kcf-finals-2026/MANIFEST.json`, `scripts/check_region_literals.py`,
`scripts/finals.template.html`, `web/finals.html`,
`tests/test_budget_rule_asymmetry_is_stated.py` (new) and `tests/test_judge_qa_bank.py`.
Two dev laps (WFG-225; WFG-226 + WFG-229) and two critic laps (#58, #59) landed in it.

**Reviewed by: subagent (block): §3.5 was repaired for one false universal and left an
equally false, unhedged one — `json_path` — standing in the same sentence.** Fixed before
the push. A first reviewer was dispatched and returned no verdict; a second, freshly spawned
and held to the same standard of proof, produced the block. Both facts are reported because
`docs/auto/LOOP_CONFIG.json` sets `review: subagent` and one silent attempt is not a review.

## The correction, and what the reviewer added to it

**§3.5 only.** It asserted a universal over the number registry: that each publishable
value's entry names five things — source artifact, **JSON path**, the expression that
re-derives it, its caveat, **and the phrasings that misstate it**. Two of the five are not
on every entry.

| field the sentence named | entries carrying it, non-empty |
|---|---|
| `source_file` | 453 / 453 |
| `derivation` | 453 / 453 |
| `caveat` | 453 / 453 |
| `json_path` | **449 / 453** — `null` on `hazard_npz_sha256`, `armn_noise_pooled_delta`, `armn_far_band_delta_vs_a`, `optpoint_zero_truepositive_fold_tally` |
| `forbidden_phrasings` | **289 / 453** (present on 312, empty on 23 of those) |

So **164 entries name no misstating phrasing at all** (141 no key, 23 empty; 141 + 23 + 289
= 453), and **four name no JSON path**. The sentence now names the three universal conjuncts
and hedges the fifth: *"…naming its source artifact, the expression that re-derives it and
its caveat; most also name the phrasings that misstate it"*, with the JSON-path conjunct
**deleted rather than qualified**.

⚠⚠ **The block's mechanism is worth more than the block.** The lap's first count tested each
field with `str(v.get(f, '')).strip()` — and **`str(None)` is the truthy four-character
string `'None'`**, so every `null` field counted as populated and the method reported
`453/453` for a field that is `449/453`. **It was wrong in the direction that makes a
universal look true**, inside a lap whose entire subject is a completeness claim, and it
reached both ledger files before the reviewer re-derived it in one command. The corrected
predicate is `v is not None and str(v).strip() != ''`.

⚠ **Why the repair is a deletion, costed rather than guessed.** "Almost always its JSON
path" is +2 against a margin of 1 — 9,001, over the hard fail. "Usually its JSON path" fits
at exactly 9,000 but understates 99.1 % badly enough to be its own small falsehood. Deleting
a false clause is the one source of headroom CHARTER §12 permits, and what goes is a
locating detail the two surviving conjuncts already carry between them.

**Provenance of the first half.** `docs/judge_qa_gates.md`, new in the incorporated diff, is
entirely about the phrasings field, and critic #59 sized it at "289 of 453 registry keys
declare". Both are about *enforcement* — whether any document is scanned for a declared
phrasing (`WFG-232`). Neither mentions this manuscript. What the lap added was the prior
question nobody asked: the paper states the field's coverage as universal.

## The second correction: a universal a caption in the same document disproves

"Data and code availability" read *"**Every** measured number is registered in
`docs/NUMBERS.json` and re-derived from its artifact by `make verify` (Section 3.5)."* F6's
caption says the dilation and translation axes and the Monte-Carlo figure quoted in §4.6
come from `forecast_robustness.json` and `dilation_perturbation.json`, **"which do not yet
carry registry keys of their own"** — and §4.6 prints 86 % of 2,000 draws, the 125 m
dilation radius, the same 125 m translation break and the 530 m band. Re-derived against the
registry: **zero** of 453 entries name either artifact, and no entry carries the value 125,
530 or 0.86.

**This is not a practice failure.** CHARTER §12's own rule is "a `docs/NUMBERS.json` value
**or a committed artifact value**", and F6's caption is that second class declared exactly as
required. What was wrong is the availability section's summary of the rule. It now reads
*"**Almost** every measured number is registered…"*, a measured +1.

⛔ **Here the budget chose the form of a mandatory correction — new in this record — and both
fuller forms were built and read off the counter rather than costed in prose.** Naming both
classes the way CHARTER §12 does gives `body_words` **9,006**, six over the hard fail; the
cheaper variant that keeps the hedge and adds only where the exceptions are ("…(Section
3.5), the rest flagged where used") gives **9,003**, three over. Both reverted. Laps 13 and
15 gave the failure mode as "a lap under this budget will write a wrong sentence before it
writes a long one"; **this is its milder cousin — a lap under this budget writes the weaker
of two true sentences.** The shipped sentence is nonetheless true, and the class it does not
name is named in the document on the numbers themselves. The reviewer, asked to rule on
exactly this, did not block on the wording and said it should be escalated rather than
dismissed.

## What was declined

⛔ **The enforcement half.** §3.5 attributes no enforcement to the phrasings field: it names
the field among what an entry carries, then lists what a gate does, and the phrasings are in
neither list. So the corrected sentence is true and complete about what it claims, and **the
manuscript is not false without the enforcement gap** (`WFG-232`: the field is scanned
against exactly two documents, and `make verify` reads it nowhere). One more *optional* true
sentence declined, about 20 words. Milder than lap 21's, which was a limitation.

## What did not move

`docs/NUMBERS.json` is **byte-unchanged** across the range (`git diff --stat` on the path
returns empty — stronger than a key-set or field-by-field comparison, and what was run), so
no key was added, no value moved and **no caveat body moved**; 453 keys.
`docs/auto/withdrawn_claims.json` is byte-unchanged too, so there was no fifth retraction and
§3.5's "skipped it four times" stays **four**. `data/processed/**` and `docs/figures/` are
byte-unchanged; `make_figures.py` redrew F1–F9 and `git status paper/figures/` is **empty** —
all nine byte-identical, so **no figure was added or redrawn and no look-at-it pass is
owed**. `references.bib` unchanged at **29** entries against 29 distinct cited keys, no
citation added, **so no URL needed opening this lap**. **Gaps 7 → 7.**

## Two things checked and found not to be owed

- **`docs/oracle_gap.md` moved and §4.5's citation still holds.** The window re-attributed
  the leave-one-fire-out fit to `scripts/build_canonical_hazard.py:129-130` and narrowed that
  page's own claim about which phrasings are enforced. §4.5 cites the page for the
  *mechanism* only, which the edit **confirms**. `grep -c 0.394 paper/manuscript.md` returns
  **0**, so critic #59's F2 (WFG-228: the "place substantially wrong" reading has no null
  model) reaches this manuscript through a citation, not a sentence.
- **The budget-rule asymmetry the window gated is the sentence §4.4 already carries.**
  §4.4's last paragraph, corrected at lap 27, already says it, and **all eight `_DENIES`
  regexes in `tests/test_budget_rule_asymmetry_is_stated.py` — three `MR_`, two `README_`,
  three `SCREEN_` — were run over that paragraph in this clone and every one comes back
  clear.** The `_REQUIRES` halves are surface-specific (Korean strings, a `classify()`
  pointer, an `NH-031` reference) and are not assertions this manuscript could pass or fail.
  ⚠ `paper/manuscript.md` is in no such test's surfaces; `tests/` is outside CHARTER §12, so
  promoting it is a dev-lap row.

## Build, gates and the anchor

`check_paper.py` **exit 0**: 8,998 body words, 8 figures, 4 tables, 29 references, 7 gaps.
`gates.py --mode full` **exit 0** (read, never piped): verify PASS, snapshot-verify PASS,
env-check PASS, pytest-full **2,023 passed, 64 skipped, 3 xfailed**; `baseline-verify` WARN
is the NH-029 growth case.

✅ **The anchor was re-derived three times and only the last is recorded.** `body_words` moved
at each correction, so lap 27's `built_pages_inputs` turned the gate red exactly as designed.
After the one `apt` line `paper/README.md` has printed since lap 9, `check_paper.py` took its
**measuring** branch — `pages 23, calibri_face Carlito, metrics_ok true` — printing
`6b0702d747ed5beb` at 8,998, then `61b8ee7cf0f331f8` at 8,999, then `6b0702d747ed5beb` again
when the block took the document back to 8,998. The repeats are the digest working: its only
inputs are the ordered figure list with pixel sizes, the table count, the reference count and
`body_words`. **Two pages against the author's 25, two words against the proxy's 9,000,
measured on one document by one run.** ⚠ **A margin of 2 is not headroom** — it exists only
because a false clause was deleted, which is unrepeatable — and 8,998 is **498 words over
CHARTER §12's target of 8,500**: the margin is against the hard fail, not the target.

**Clone shallow:** `--is-shallow-repository` **true**, `rev-list --count HEAD` = **51**, both
measured this lap. **No ancestry or reachability claim is made anywhere in this lap's
output.**

## In plain terms

- The paper claimed every registered number carries a JSON path and a list of ways it must not be described; 449 of 453 carry the first and 289 the second, so both claims are now written the way the registry actually is — and the availability section no longer says "every measured number is registered" when two committed artifacts behind §4.6 are not.
- Still missing: the paper cannot say that those "do not describe it this way" lists are only checked against two documents, and it cannot state the oracle-gap result at all. Both are true, both need words the 9,000-word rule does not have.
- Please answer **NH-037** (raise the word proxy toward the 25-page rule you actually set — this lap it decided the *wording of a required correction*, not just an optional sentence), and **NH-053**, due 2026-09-12.



## Gates

**ALL GREEN** · mode `full` · head `c7c915b` · 2026-09-10T15:34:19Z · current at `c7c915b`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 19.1 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.1 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 0.3 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 297.6 s | 2023 passed, 64 skipped, 3 xfailed, 52 warnings in 296.73s (0:04:56) |

## Commits since the previous report (386786068856d7c9194cd130ca5d08a722bb24f1..HEAD)

- c7c915b critic #59: the sentence written to be pasted into the submitted document explains the weak fold with a number the rest of its own file does not use

## Needs a human (26 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09, one day past; raised to HIGH by critic #55 on a measured third instance)
- NH-036 [DECISION] One critic lap told the next one not to edit a file, and that is what kept a false sentence in front of a judge for a window (by 2026-09-10)
- NH-037 [DECISION] The paper's word proxy now stops it a thousand words before your 25-page rule (by 2026-09-10)
- NH-038 [DECISION] Your "product first" rule has spent the last three dev laps on documents, and the readiness line it was written to protect has not moved in five critic laps (by 2026-09-09)
- NH-039 [DECISION] The national wildfire-spread system's manual is an 18 MB PDF the sandbox could not fetch, and one of you can (by 2026-09-12)
- NH-040 [FYI] A critic lap pushed one commit past a red `--assert-reported`, and it is telling you rather than hiding it
- NH-041 [FYI] This lap sent you an email containing only the word PLACEHOLDER, and could not take it back
- NH-042 [DECISION] Two of your own rules collide whenever a withdrawn claim lives in a frozen artifact, and this week they collided three times (by 2026-09-10)
- NH-043 [DECISION] A gate your loop built this morning will go red about twice a day, and the charter tells the lap that meets it to stop working (by 2026-09-09)
- NH-045 [BLOCKER] The staleness gate has closed `auto/dev` to every routine, and the one routine that met it is the one forbidden to clear it (by 2026-09-08)
- NH-044 [DECISION] The claim the paper just retracted is still live on the page the paper cites for it (by 2026-09-09)
- NH-046 [DECISION] Your product's definition-of-done names a command nothing in this project has ever run (by 2026-09-10)
- NH-048 [DECISION] One of the research routine's three literature channels has been dead for two runs, and a working replacement is already proven (by 2026-09-10)
- NH-049 [DECISION] Your critic routine is told to add judge Q&A cards and your own printing gate makes that impossible for it (by 2026-09-11)
- NH-050 [DECISION] You answered two of these questions two days ago and the loop never heard you, because you answered them on the routine page (by 2026-09-10)
- NH-051 [DECISION] The rule your loop has been obeying for three days is not the option it names, and the difference is why one row has been pushed down five times (by 2026-09-11)
- NH-052 [DECISION] The experiment that measures what your own forecast is worth is now one run away, and it would change what 42 means (by 2026-09-12)
- NH-053 [DECISION] The word your front door uses for 42 stopped being right today, and the loop cannot pick its replacement (by 2026-09-12)
- NH-054 [DECISION] Four fifths of your front door's headline is limits, and no lap is allowed to decide whether that is the project's strength or its biggest presentation risk (by 2026-09-13)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
