# WildfireGuardian autoloop · dev · 2026-09-07T0355Z

| | |
|---|---|
| when | 2026-09-07 03:55 UTC · 2026-09-07 12:55 KST |
| branch / head | `auto/dev` / `0bd4060` |
| kind | dev |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 7, done: 22, parked: 1, todo: 84 |

## What happened this lap

## Your decisions, applied

**Nothing new on either channel.** The Gmail search (`from:siyeong0318@gmail.com
subject:"WildfireGuardian autoloop" newer_than:14d`) returns threads that are all
single messages this loop itself sent — no reply from you in any of them — and PR #31
has no comments. `decisions.py` was therefore not run and
`docs/auto/decisions_seen.json` is unchanged. Nine NEEDS_HUMAN entries remain open
(NH-032, 033, 034, 035, 036, 037, 038, 039, 040) and this lap adds none.

## What I did

**WFG-151 — the booth kit shipped on 09-06 and the release bundle a judge would be
handed still did not contain it.** Critic #31's one `fix-before-next-row` item and
position 1 on `DIRECTION.md`. Claimed at `ff566bc`, shipped at `0bd4060` plus the
reviewer's repairs.

`release/kcf-finals-2026/MANIFEST.json` listed **17 files** and not one was a
printable. `MANIFEST.json` is now **19 files** and `make finals-bundle` still rebuilds
byte-identically. No committed bytes were added: `release/kcf-finals-2026/printables/`
is git-ignored like the rest of the payload (`.gitignore:448`, confirmed with
`git check-ignore -v`), so `git ls-files release/` is still exactly `MANIFEST.json`
and `README_KO.md`.

**The stamp is resolved, not typed, and that was the actual engineering decision.**
The row said 「add the newest-stamp PDF to `PAYLOAD`」. A `PAYLOAD` literal would have
been correct today and wrong at the next `make printables`, because CHARTER §3.2
forbids overwriting a committed artifact, so every kit gets a *new* stamped filename.
`build_finals_bundle.py:newest_printables()` takes the lexicographic maximum of the
**tracked** `WFG_printables_*.pdf` stamps (no clock, no timezone, CHARTER §4b) and
then requires the manifest at that same stamp, so a half-committed kit is an error
rather than a mismatched pair. Probed rather than argued: staging a synthetic
`WFG_printables_20260908T0100Z.pdf` turned three tests red and made the builder exit
2 naming all four files; removing it restored green.

**The shape, which is what critic #31's falsifiable test was actually about.**
`tests/test_finals_bundle.py` gains `R9_ITEMS` — R9's five names read into predicates
over the **committed manifest**, not over `bfb.plan()`. Graded red first against the
committed 17-file manifest at `3f881f6`: `test_the_bundle_carries_every_content_r9_names`
failed naming exactly `printables`, and `test_the_bundle_carries_the_newest_booth_kit_and_not_an_older_stamp`
failed with 「the newest booth kit in the tree is 20260907T0059Z and the bundle does
not carry it; the bundle's printables are []」.

**Evidence paths:** `scripts/build_finals_bundle.py`, `tests/test_finals_bundle.py`,
`release/kcf-finals-2026/MANIFEST.json`, `release/kcf-finals-2026/README_KO.md`,
`docs/finals_bundle.md`, `docs/auto/KCF_READINESS.md` R7 and R9, `.gitignore:448`.

## The root objection I recorded against my own plan, and what it missed

I recorded: *adding the PDF fixes today's omission; only a test bound to R9's own
prose fixes the shape.* That was right as far as it went and it is why `R9_ITEMS`
exists. **It missed the layer the reviewer found.**

## Reviewed by: subagent (block): the reason for an exclusion is the one field no gate reads

I do not argue with a block, and this one was correct.

I wrote into the judge-facing `release/kcf-finals-2026/README_KO.md` that the
dispatch-sheet sample was excluded because 「29장짜리 출동 지시서 표본은 `outputs/` 에
이미 완성된 PDF 로 있어」. **Both halves are false.**
`outputs/dispatch/20260801T163042Z/` holds **33** clusters, and `git ls-files
outputs/dispatch` counts 33 `dispatch_a4.html`, 33 `sms_drafts.json`, 33
`broadcast_script.txt` and exactly **3** `dispatch_a4.pdf` — because
`outputs/dispatch/README.md` commits only the three largest clusters' PDFs, 33 being
6.7 MB of regenerable output. 「29」 traces to an aspiration in
`docs/auto/research/RESEARCH_BRIEF_2026-09-03.md` and to no artifact; `docs/NUMBERS.json`
has no key for it. **A number in prose with no registered artifact, in the file that
goes on the USB stick — CHARTER §3.3.** On a clean clone the student goes looking for
29 finished PDFs and finds 3.

I did not invent it: I read it off `tests/test_printables.py` `R7_ITEMS` and R7's own
line, where it had sat since WFG-130. That is the loop's signature defect one layer
out — not in the contents this time, but in **the reason for an exclusion**, which
`R7_ITEMS` and my `R9_ITEMS` both left ungated. A path existing is compatible with any
story about why it was excluded, and the reason is the part that reads like an
explanation, so it is the part that gets copied onto judge-facing prose.

Fixed in all three places I authored it, plus the upstream `R7_ITEMS` reason and R7's
evidence cell. New gates, both graded red: `EXCLUSION_EVIDENCE` (an excluded item's
named path must be in the tree) and `test_the_dispatch_exclusion_reason_matches_what_is_committed`,
which re-derives 3-of-33 from `git ls-files` and goes red the day a lap commits the
rest — which is exactly when the Korean sentence would silently become wrong again.

The reviewer's two other findings, both taken:
- `test_the_bundle_carries_every_content_r9_names`'s docstring recorded a grading that
  **does not reproduce**: dropping `newest_printables()` from the plan leaves it green
  (the committed manifest still lists the kit). The ablation that fires is
  manifest-side. Corrected, with the reason — a grading record a later lap cannot
  reproduce is worse than none, because it is the sentence that lap will trust.
- `test_r9_still_enumerates_the_contents_this_list_resolves` searched the **whole** R9
  row, and this lap appended ~2,000 words to that row's status cell mentioning three
  of the five names. It now binds to the **definition cell** only. Graded by putting
  the sentinel in the status cell alone and confirming it still goes red.

## What did not work

- **`git checkout <file>` to undo a grading probe, twice.** Both times the file
  carried uncommitted fixes and both times I silently lost them — the R9 correction
  and three test edits. The second loss also made a grading run report a false pass,
  which I caught only because `git status` stopped listing the file. Probes now revert
  from a copy taken at the start of the probe. In the MEMO.
- **The 「29」 could not be fixed everywhere.** R7's own *definition* wording is bound
  by a test and is a readiness definition (the critic's call), and the printables
  manifest's `what_this_does_not_show` repeats it and is a committed artifact that
  changes only at a new stamp (CHARTER §3.2). Filed as **WFG-153**, P1.

## Also verified rather than assumed

The probe left a superseded kit in the bundle folder while `make finals-bundle` still
printed `OK` — the known WFG-108 behaviour (the builder never enumerates the folder).
`check_bundle_copy.py`, which is what travels on the stick, **does** catch it: it
printed `EXTRA printables/WFG_printables_20260906T0620Z.pdf — in the folder and not in
the manifest` and exited 1. The student is covered at the booth, which is what WFG-037
built that checker for.

## Gates

`gates.py --mode full` **ALL GREEN, exit 0**: 1643 passed, 56 skipped, 2 xfailed
(**warm** — the reviewer's run materialised the SRTM tile; the cold count on the same
tree is 62 skipped, WFG-139 again). `baseline-verify` WARN is the known NH-029 state,
the two laptop-only raw manifests, unchanged. GitHub's own `auto-gates` run 192 at
`5cca6ce` is `success`; no red run, so no CHARTER §4b job.

## Next row

**WFG-026** (P0) — the related-work table and SFTD059T differentiation panel, R7's
sole remaining blocker, carrying the two Korean operational systems. ⚠ Whoever takes
it **rebuilds the kit in the same lap** (WFG-152), and should fold in **WFG-153**'s
manifest half at that new stamp.

## In plain terms

- **What changed for the project.** The USB folder you would hand a judge now actually
  contains the printed booth kit — it did not, for a whole day after the kit was
  built. It also picks up the newest kit by itself from now on, so this cannot go
  stale again without something turning red.
- **Why it matters to the judges or the science.** A reviewer caught me telling you,
  in the Korean file inside that folder, that 29 finished dispatch sheets were waiting
  in the repository. There are 3. I had copied that from our own notes instead of
  looking. It is fixed, and there is now a test that re-counts them so the sentence
  cannot drift again.
- **What you should do.** Nothing is required. One thing worth knowing before the
  booth: if you want the village dispatch sheets on paper, print them from the laptop
  first — only 3 of the 33 are ready-made in the repository, and the rest are rebuilt
  with `python scripts/generate_dispatch_outputs.py`.



## Gates

**ALL GREEN** · mode `full` · head `0bd4060` · 2026-09-07T03:54:03Z · current at `0bd4060`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 15.6 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.1 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 0.3 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 208.6 s | 1643 passed, 56 skipped, 2 xfailed, 52 warnings in 207.76s (0:03:27) |

## Commits since the previous report (3f881f6d323773e526a4e3a30b18ca34f6016166..HEAD)

- 0bd4060 WFG-151: the bundle a judge is handed carries the booth kit, and the stamp is resolved not typed
- ff566bc claim WFG-151 (20260907T0320Z)
- 5cca6ce the critic report names the head it ships in, and how to read its own backlog line
- 900c407 critic #31: the booth kit shipped and the bundle a judge is handed still does not contain it

## Needs a human (13 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09)
- NH-036 [DECISION] One critic lap told the next one not to edit a file, and that is what kept a false sentence in front of a judge for a window (by 2026-09-10)
- NH-037 [DECISION] The paper's word proxy now stops it a thousand words before your 25-page rule (by 2026-09-10)
- NH-038 [DECISION] Your "product first" rule has spent the last three dev laps on documents, and the readiness line it was written to protect has not moved in five critic laps (by 2026-09-09)
- NH-039 [DECISION] The national wildfire-spread system's manual is an 18 MB PDF the sandbox could not fetch, and one of you can (by 2026-09-12)
- NH-040 [FYI] A critic lap pushed one commit past a red `--assert-reported`, and it is telling you rather than hiding it

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
