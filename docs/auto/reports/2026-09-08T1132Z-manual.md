# WildfireGuardian autoloop · manual · 2026-09-08T1132Z

| | |
|---|---|
| when | 2026-09-08 11:32 UTC · 2026-09-08 20:32 KST |
| branch / head | `auto/dev` / `26ff1d4` |
| kind | manual |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 8, done: 26, parked: 1, title-derivable: 1, todo: 96 |

## What happened this lap

A one-paragraph correction report, written because `gates.py --assert-reported` asked for it and
CHARTER §4 step 8 says a red `--assert-reported` is never pushed past (NH-040 is the record of the
one lap that did).

## What this covers

Critic lap #42 shipped in `2fa29af` with its report `docs/auto/reports/2026-09-08T1115Z-critic.md`,
and `6baf478` annotated that report with the head it ships in. Both are on `origin/auto/dev` and
both were gated ALL GREEN at their own heads.

Then this lap noticed that **its own two commits are part of the drift its one
`fix-before-next-row` item reports**, and that the number therefore needed correcting before the
next dev lap reads it. `docs/auto/CRITIC_LATEST.md` measured `git rev-list --count 1bca8ed..HEAD`
as **25** at the reviewed head `ceb43ba`, against `STAMP_MAX_COMMITS_BEHIND = 30` in
`tests/test_finals_screen.py:540`. After `2fa29af` and `6baf478` the same command answers **27**,
so the headroom the next dev lap actually meets is **three**, not five.

That matters because it changes what the ordering instruction is for. At 27 a dev lap that claims
its row first and rebuilds the screen later can cross 31 **inside its own lap** — claim 28, work 29,
reviewer fix 30, report 31 — and CHARTER §3.9 then parks it, which is NH-045 exactly. So WFG-187's
「run `make finals` **before** you claim a row」 is a requirement and not a preference, and
`CRITIC_LATEST.md` now says so with the corrected number and with this lap's own contribution to it
named rather than left for the next reader to find.

`gates.py --mode full` exits **0** at `26ff1d4`, read unpiped: `1741 passed, 63 skipped, 2 xfailed`,
pytest 251.1 s. Nothing outside `docs/auto/CRITIC_LATEST.md` changed in that commit.

**Head annotation.** `report.py` stamped the table above at **`26ff1d4`**, the commit this report was
written to cover. The report itself ships one commit later, together with a second correction to
`docs/auto/CRITIC_LATEST.md`; `gates.py --mode full` was re-run on that final commit and
`--assert-head` and `--assert-reported` both exit 0 immediately before the push. Both ids resolve on
`auto/dev`.

⚠⚠ **And the correction had to be corrected once more, which is the finding rather than an
embarrassment.** `--assert-reported` required a report for `26ff1d4`; writing that report is itself
a commit; so the number this whole exchange exists to state moved again while it was being stated.
`git rev-list --count 1bca8ed..HEAD` answers **25** at the reviewed head `ceb43ba` and **29** at the
head this report ships in, against a limit of 30. **The critic routine cannot run `make finals` —
its prompt forbids changing any artifact — so the only routine that can measure this drift is one
that can only add to it.** That is NH-045's complaint arriving as a fact for the third time, and it
is the most direct evidence NH-043 has had: the loop's own bookkeeping consumed four of the five
commits of headroom it found. `CRITIC_LATEST.md` now states both heads and both numbers rather than
one figure that was true once, and WFG-187's ordering — `make finals` **before** the claim — is what
the next dev lap has one commit of slack to obey.

## In plain terms

- What the judges would see today. Nothing different from the critic report you already have; this
  is a bookkeeping correction inside the loop, not a change to anything a judge meets.
- What changed since that report. One number in the note the next build lap reads first. I told it
  the booth screen had five commits of slack before a check turns the branch red; my own two commits
  used two of them, so it has three, and I corrected it rather than let the next lap discover it.
- What you should do. Nothing for this one. The two decisions due today, NH-032 and NH-034, are
  still the thing worth your ten minutes.



## Gates

**ALL GREEN** · mode `full` · head `26ff1d4` · 2026-09-08T11:31:29Z · current at `26ff1d4`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 17.8 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.1 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 0.3 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 251.8 s | 1741 passed, 63 skipped, 2 xfailed, 52 warnings in 251.11s (0:04:11) |

## Commits since the previous report (ceb43ba38109548ffa9ada1e43e6e02e110eaa49..HEAD)

- 26ff1d4 the critic's own two commits are part of the drift it reported, and the headroom is three
- 6baf478 the critic report names the head it ships in, and the run that certified it
- 2fa29af critic #42: the priority moved and the row did not, and the screen is five commits from shutting the branch

## Needs a human (19 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09)
- NH-036 [DECISION] One critic lap told the next one not to edit a file, and that is what kept a false sentence in front of a judge for a window (by 2026-09-10)
- NH-037 [DECISION] The paper's word proxy now stops it a thousand words before your 25-page rule (by 2026-09-10)
- NH-038 [DECISION] Your "product first" rule has spent the last three dev laps on documents, and the readiness line it was written to protect has not moved in five critic laps (by 2026-09-09)
- NH-039 [DECISION] The national wildfire-spread system's manual is an 18 MB PDF the sandbox could not fetch, and one of you can (by 2026-09-12)
- NH-040 [FYI] A critic lap pushed one commit past a red `--assert-reported`, and it is telling you rather than hiding it
- NH-041 [FYI] This lap sent you an email containing only the word PLACEHOLDER, and could not take it back
- NH-042 [DECISION] Two of your own rules collide whenever a withdrawn claim lives in a frozen artifact, and this week they collided three times (by 2026-09-10)
- NH-043 [DECISION] A gate your loop built this morning will go red about twice a day, and the charter tells the lap that meets it to stop working (by 2026-09-09)
- NH-045 [BLOCKER] The staleness gate has closed `auto/dev` to every routine, and the one routine that met it is the one forbidden to clear it (by 2026-09-08)
- NH-044 [DECISION] The claim the paper just retracted is still live on the page the paper cites for it (by 2026-09-09)
- NH-046 [DECISION] Your product's definition-of-done names a command nothing in this project has ever run (by 2026-09-10)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
