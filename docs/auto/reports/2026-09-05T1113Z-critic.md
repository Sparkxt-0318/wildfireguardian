# WildfireGuardian autoloop · critic · 2026-09-05T1113Z

| | |
|---|---|
| when | 2026-09-05 11:13 UTC · 2026-09-05 20:13 KST |
| branch / head | `auto/dev` / `6afd252` |
| kind | critic |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 6, done: 16, parked: 1, todo: 61 |

## What happened this lap

Critic #18. Window `6236c81..6afd252` on `auto/dev`, 7 commits, 1,043 authored insertions
(images, `.docx` and the generated board excluded). No code, data, figure or `docs/NUMBERS.json`
entry was touched by this lap.

## Your decisions, applied

**None — nothing new arrived.** The Gmail search `from:siyeong0318@gmail.com
subject:"WildfireGuardian autoloop" newer_than:14d` returns 49 threads and every one of them is a
single outbound message from the loop with no reply. PR #31 carries no comment in `NH-###:` form.
`docs/auto/decisions_seen.json` is unchanged. **Six entries are open and waiting on you: NH-005,
NH-014, NH-025, NH-026, NH-027, NH-028.**

## The main act: no `fix-before-next-row` item, on purpose

Of the last **eleven** dev claims on this branch, **eight** were a critic's `fix-before-next-row`
item: WFG-063, WFG-069, WFG-070, WFG-067+WFG-075, WFG-087, WFG-095, WFG-100, WFG-103. The last three
ran on three consecutive dev slots (0325Z, 0625Z, 0920Z), all three were scoped 「minutes」, and all
three were on one document, `docs/auto/DEMO_SCRIPT_5MIN.md`. In the same six days
`docs/auto/finals/BOOTH_SETUP.md` has never once been claimed, and its absence holds three readiness
lines (R3's booth half, R7, R9).

Critic #17 set a falsifiable test for me: 「if `BOOTH_SETUP.md` does not exist by then, WFG-084's cap
becomes the rule.」 It does not exist — checked on disk at this head, along with the printables (none)
and R1's mapping table (none). **The test resolves against the loop.** So: the cap is now this
routine's rule and this report is written under it; I set **no** `fix-before-next-row` item; and I
moved one row, WFG-037 above WFG-104, so that table order and `DIRECTION.md` both put
`BOOTH_SETUP.md` first. Both are P0, so CHARTER §3b was not bent either way.

Every one of those eight critic items was real, and most were this routine's best work. The problem
is not that they were wrong. It is that the only lever which can stop the queue running ahead of the
product is held by the routine that fills the queue, and it has not been released in six days.

## Findings, ranked

**F1 · WFG-106 · P0 · judge-facing · the number that answers the strongest objection to the headline
is already registered, already gated, and reaches no judge.** Critic #17's objection was that 24.73 %
is measured against a fire-blind walker. The repository has already computed the version that is not
diluted by the walkers who were never in danger, and it is a registry key —
`mr_uiseong_fa_rescue_rate` = **0.883** — whose own derivation string reads 「of the origins whose
FIRE-BLIND route is unsafe, the share the future-aware router still gets to a refuge」.
`docs/multi_region.md:430-434` prints it for all three fires: 영덕 44/458 unsafe → **95.5 %** rescued;
의성·안동 103/368 (27.99 %) unsafe → **88.3 %** rescued; 울진·삼척 13/393 unsafe → **23.1 %** rescued.
Drilled against the files: `88.3`, `27.99` and `103 / 368` appear **zero times** in `JUDGE_QA.md`,
`DEMO_SCRIPT_5MIN.md`, `paper/manuscript.md` and `docs/finals_screen_v2.md`. The student therefore
carries the diluted number and not the conditional one, and 울진·삼척's 23.1 % — the number that runs
against the project — is spoken nowhere. Costs no compute, adds no claim. ⚠ The key's own caveat says
「SMALL denominator (13–20 origins)」, which is true of 울진's 13 and false of this key's own 103; the
row says to correct it, not delete it.

**F2 · WFG-104, updated rather than duplicated · judge-facing · the fix landed on two surfaces and
left them saying different things.** WFG-103 corrected the baseline sentence on the script *and* on
the judged screen, which is more than the finding asked for. Only one stayed inside what is measured.
The screen says 「화재를 전혀 보지 않는 지도가 그리는 경로입니다」 / 「the route a fire-blind map draws」.
The script (`DEMO_SCRIPT_5MIN.md:108-109`) adds 「**이 도구가 없을 때의 기준선입니다**」 — a
counterfactual about what a resident without this tool actually walks, which the repository labels
(`docs/real_roads_real_hazard.md:50`, 「the status quo」) and has never measured. A judge reads the
screen and hears the script at the same moment. Not made a blocking item: a ⚠ block directly beneath
it already tells the student what to say instead, and WFG-104 is two rows down.

**F3 · direction, acted on rather than filed · readiness has not moved for THREE consecutive critic
laps** (#16, #17, #18; 4 of 11). That fires the 「zero for two consecutive critic laps」 rule. The
action taken is the empty item block above.

**F4 · a correction this routine owes the loop.** Critics #16 and #17 wrote 「nothing has touched
`web/`」. True of their windows, **not** of this one: `web/finals.html` gained two lines at `92366cb`.
R1 is unmoved for a different reason — its condition is a committed mapping table from every
on-screen number to a registry key, and no such table exists at this head.

**F5 · checked and NOT findings**, recorded so no later lap re-derives them: (a) the pace page's
variant table pairs the re-budgeted allocation with a spread belonging to the superseded design budget,
explained on the line directly beneath the table; (b) the
LOFO ceiling probabilities (0.0241 / 0.296 / 0.369) sit below the router's `p_cut = 0.5`, which looks
fatal until Q1 of the bank's two-threshold explanation — the bank is right; (c) `paper/GAPS.md` G7's
「44 origins」 is 영덕's 42 + 2, not a contradiction of 의성·안동's 103.

**Judge drill.** Five questions run against the bank from files only. `p_cut = 0.5` → Q11 answers it
and states its own gap honestly. The 368-origin sampling question → already WFG-096, corrected by
critic #16, no new row. The unaided-resident question → WFG-104. The conditional-rate question →
WFG-106, new. The booth-laptop-failure question → WFG-037, an artifact gap and not a bank gap. No
`JUDGE_QA.md` edit was made: the bank's 41-question count is gated in two places and the rows carry
the work.

## A note on this report's own gate block

`report.py` writes the header and the gate table **before** this lap's commit exists, so both name
`6afd252` — the head of the window this lap reviewed — while the report itself travels in a later
commit. That is a structural property of the tool, not a slip, and it is said here in prose rather
than chased with a follow-up correction commit, which is what the previous three laps each shipped
and which cannot converge (`b036a0f` settled on this pattern).

**What certifies the commit this report actually ships in:** `gates.py --mode full` was re-run after
the commit, and `--assert-head` refuses a push unless `.auto/gates.json` records a `full` pass at
exactly that head with a clean tree. Both ran before the push.

⚠ **The first post-commit run was RED and the gate was right.** `check-number-collisions` fired on
this report's own sibling, `docs/auto/CRITIC_LATEST.md`: the F5(a) paragraph printed a spread
belonging to the superseded design budget next to `demo`/`pace`/`spread` anchors that resolve to the
two re-budget keys. The first repair — a `collision-ok` comment naming all three values — made it
worse by putting three spreads of one quantity on adjacent lines and tripping two further anchors.
The repair that held is the one `1fd774c` established for exactly this trap: name the **keys**, not
the bare numbers. Recorded because a lap whose root objection is that the loop over-serves its own
machinery should say plainly that the machinery caught it twice on its own prose in the same lap.

## Verification of the loop's claims

- `gates.py --mode full` **ALL GREEN at `6afd252`**: `1484 passed, 62 skipped` in 214 s, COLD, against
  critic #17's cold `1484 / 62` — unchanged like for like, eleventh comparable window.
  `baseline-verify` WARN, expected off-laptop, `hard: false`, eighteenth window, still not a finding.
- **GitHub's own runs.** `auto-gates` run **124 at this head is `success`**; 117–123 `success`, 115 and
  116 `cancelled` by the next push. **No red run stands behind a green report.** Last failure is run
  110 (03:20Z), already NH-026/WFG-102.
- **Eight of eight dev reports in the last 24 h record `Reviewed by:`.** `--assert-head` and
  `--assert-reported --base 6236c81` both exit 0.
- **`make finals-bundle` re-run here, not read:** exit 0, byte-identical, 16 files.

## Root objection (`hate`)

**The loop's most reliable behaviour is answering its own critic, and for six days that has been more
reliable than building the booth.** The window under review is a good one — a false description of the
baseline was removed from the script, the screen and the manuscript, found twice independently — and it
moved no score and no readiness line, because the rows are waiting on two files nobody has written. The
cheapest test is not another finding. It is one lap with no critic item in front of it.

## Scorecard

**Track B 84 → 84. Track A 78 → 78. Every row held**, evidence per row in `docs/auto/SCORECARD.md`.
Judge-facing census for the window: **65 of 1,043 authored insertions (6.2 %)** against **493 (47.3 %)
in reports** — the highest report share this census has recorded, in the window whose falsifiable test
was about exactly that.

## In plain terms

**What the judges would see today.** The same booth kit as yesterday and the day before: a good screen,
a good Q&A bank, a five-minute script whose numbers all check out. Still no printed sheets, and still no
written recipe for setting the laptop up on the day. Four of eleven readiness boxes, unchanged for three
checks running.

**What changed since yesterday.** One real improvement: the script and the screen used to tell judges
the comparison was against a map that watches the fire, and it is against a map that does not look at
the fire at all. That is now fixed in three places, including the paper. I also found a number your
project already computes and never says out loud — of the homes whose fire-blind route is actually
unsafe, your router still gets **88.3 %** of them to safety in 의성·안동, and only **23.1 %** in
울진·삼척. The second one is not flattering, and saying both is what makes a judge trust the first.

**What you should do.** Nothing on the code. Two things only. **(1)** Six decisions have been waiting
for you and none has an answer yet — NH-005, NH-014, NH-025, NH-026, NH-027, NH-028; each is one line in
a reply to this email. **(2)** I stopped giving the builder homework this round so it would finally write
`BOOTH_SETUP.md`, the setup recipe for the finals laptop. If tomorrow's check still finds it missing,
that is worth your attention, and I will say so plainly.



## Gates

**ALL GREEN** · mode `full` · head `6afd252` · 2026-09-05T11:03:03Z · current at `6afd252`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 17.9 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.1 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 1.9 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 214.7 s | 1484 passed, 62 skipped, 52 warnings in 213.73s (0:03:33) |

## Commits since the previous report (57b7b592d42d8890fc23494d9d6b629148ff4aa3..HEAD)

- 6afd252 the report said five decisions were waiting and the block beside it listed six
- b036a0f report 2026-09-05T0953Z-dev

## Needs a human (8 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-025 [DECISION] `Main` cannot follow the green commit until you change one GitHub setting, and until you do, every green push emails you a red run (by 2026-09-08)
- NH-026 [DECISION] One of the five routines pushes on a gate that does not run the test suite, and its prompt is the one the repository cannot show you (by 2026-09-08)
- NH-027 [DECISION] The one experiment that would test the project's headline against a fair opponent is scheduled for after the competition (by 2026-09-08)
- NH-028 [DECISION] The manuscript is full, and three laps have recorded that as a note rather than asked you (by 2026-09-10)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
