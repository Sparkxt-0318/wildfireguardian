# WildfireGuardian autoloop · dev · 2026-09-05T1302Z

| | |
|---|---|
| when | 2026-09-05 13:02 UTC · 2026-09-05 22:02 KST |
| branch / head | `auto/dev` / `5aecc5f` |
| kind | dev |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 6, done: 16, parked: 1, todo: 63 |

## What happened this lap

**Row: WFG-037 (P0, KCF) — the booth recipe.** Claimed at `b108043` and taken because
`docs/auto/DIRECTION.md` names it row 1 and critic #18 deliberately set **no**
`fix-before-next-row` item so that this lap would take it. Six days and eleven dev claims had
gone past it; it holds three readiness lines (R3's booth half, R7, R9).

## Your decisions, applied

**None — there was nothing new to apply.** The Gmail search
`from:siyeong0318@gmail.com subject:"WildfireGuardian autoloop" newer_than:14d` returns 50
threads and every one of them is a single outbound message from the loop with no reply in the
thread. PR #31 carries no comment at all, in `NH-###:` form or otherwise.
`docs/auto/decisions_seen.json` is unchanged. Two standing answers still shape this lap and are
recorded rather than re-asked: NH-014 (「Yes, I will run it in that window」, 2026-09-04) is what
this row's document is written *for*, and NH-005 (「Still coming this week」, 2026-09-05) is
untouched by it.

## What I did

`docs/auto/finals/BOOTH_SETUP.md` now exists: Korean, eight sections — when to do what, the
laptop environment, the two USB sticks, what actually renders offline, the packing list, the
ten-minute booth setup, the five keys, and what to do when the laptop dies.

**The one decision that shaped it:** a procedure is a set of predictions about a machine, so
every command in it was **executed in this sandbox and its exit code read**, and the steps that
cannot be executed here — the double-click, the Wi-Fi switch, the USB stick — are marked as the
author's (NH-014 / R12). Three of the claims the repository's own prose already made did not
survive that:

**1. `make all-checks` does not pass, and not only off-laptop (→ NH-029).** Readiness line R3
names it as the booth-laptop check. It aborts at `baseline-verify`: `BASELINE MOVED — 6
difference(s) against 89730db89921`. Only **two** of the six are the missing
`data/raw/firms_data/` manifests that a lap never has. The other four — the `docs/NUMBERS.json`
entry count, and three tracked `data/processed/demo_script_pace/pace_*.json` artifacts — are in
every clone and will abort the same command on the author's laptop. Eighteen critic windows have
recorded 「`baseline-verify` WARN, expected off-laptop, `hard: false`」, which is true of two lines
and was read as covering all six. Re-freezing needs the raw bundle and would, run here, record
the two raw contracts as MISSING and destroy the protection they are there for — so it is
NH-029, with options, and §1.1 of the recipe sends the booth morning to
`python scripts/auto/gates.py --mode full` instead.

**2. `make finals-bundle` does not check a USB copy** — and the Makefile comment, the builder's
docstring, `docs/finals_bundle.md` and the bundle's own Korean README each said, in different
words, that it does. `assemble()` copies every payload file out of the repository over the top
of the bundle **before** anything is hashed. Measured: seven bytes appended to
`release/kcf-finals-2026/web/finals.html`, then the builder — the file was silently overwritten
and the run printed `OK`. The missing half is **`scripts/check_bundle_copy.py`**: reads a folder,
never writes to it, imports nothing outside the standard library, and travels *inside* the
bundle so the borrowed-machine case can run it with no repository. All four surfaces are
corrected, the withdrawn sentence quoted rather than deleted (CHARTER §3.7).

**3. The builder never enumerates the folder it certifies (→ WFG-108, P1).** Found by running
the new checker seconds after a green builder run: the builder said the bundle was rebuilt
byte-identically, and the checker answered `EXTRA web/demo-media/intro-forest-loop.mp4` — a file
this lap's own earlier experiment had left in `release/`. It would have been copied to the
stick. Mitigated today by making the copy check a step in §2 rather than by changing the
builder, which is WFG-036's row.

**Measured rather than asserted, with a headless browser, `file://`, every non-file request
blocked:** all four screens render with **zero** external requests (the static `offline` gate
says this; nothing had run it); `web/console.html` degrades to its pre-computed state with a
sentence instead of a dead screen, so the recipe tells the student not to plan a live run at the
booth; the two `demo-media` console errors are the optional video and audio slots and are normal;
`make finals` rewrites `web/finals.html`'s build stamp and thereby breaks the bundle's
byte-identity, so §3.4 says not to run it after the freeze.

**Evidence paths.** `docs/auto/finals/BOOTH_SETUP.md`; `scripts/check_bundle_copy.py`;
`tests/test_check_bundle_copy.py` (12); `tests/test_booth_setup.py` (9);
`scripts/build_finals_bundle.py`, `Makefile`, `docs/finals_bundle.md`,
`release/kcf-finals-2026/README_KO.md` + `MANIFEST.json` (17 files) for the correction;
`docs/auto/NEEDS_HUMAN.md` NH-029 and the NH-014 note; `docs/auto/BACKLOG.md` WFG-108;
`docs/auto/KCF_READINESS.md` R3 and R9 annotated and **not** ticked (CHARTER §10 — the critic
ticks); `docs/auto/MEMO.md`.

**What did not work / was not done.** I did not change the builder to prune the folder: that is
WFG-036's code and this row is the recipe, so it is filed as WFG-108 with a done-when. I did not
re-freeze the baseline; see NH-029 for why a lap must not. I did not write printables — §7.3's
third fallback step stands on a hole and says so (R7 / WFG-007). Playwright was pip-installed
into `.auto/venv` for the render measurement and is deliberately **not** a repository
dependency: no test imports it, `requirements.txt` is untouched, and the GitHub runner never
sees it.

**Root objection I recorded before building (`hate`).** 「A booth recipe written by an agent that
has never touched the laptop is a document of assertions, and its most dangerous sentences are
the commands: a step that says `make all-checks` and expects green would fail on the author's own
machine, and the student would be debugging a stale baseline freeze on competition morning.」
Cheapest test: run every command it names, here, and read the exit code. The objection was
correct — that is finding 1 — and the same test produced findings 2 and 3.

## The reviewer blocked, and it was right

**Reviewed by: subagent (block): 「the document's load-bearing promise — that every command in it
was executed rather than copied — is falsified by §5.6, the recipe's own booth-morning
verification step.」**

§5.6 said 「우상단 언어가 **KO** … 인지 확인합니다」. That sentence was **copied**, from
`release/kcf-finals-2026/README_KO.md` step 4 and `docs/auto/DEMO_SCRIPT_5MIN.md` §2, and never
run — in the one lap whose whole argument is that a procedure must be executed, and whose MEMO
entry names copying as the anti-pattern. It is also false: `web/finals.html:2154` is
`btnLang.textContent = lang === 'ko' ? 'EN' : 'KO'`, so the button shows the language a press
would switch **to**, and a Korean screen reads **EN**. A student following step 6 literally, ten
minutes after registration, sees EN, believes the language is wrong, and presses the one button
that switches the judged demo into English.

Fixed on all three surfaces, not only the one the reviewer named (MEMO 2026-09-05: read the
finding's claim, then find its instances yourself): `BOOTH_SETUP.md` §5.6,
`release/kcf-finals-2026/README_KO.md` step 4, `docs/auto/DEMO_SCRIPT_5MIN.md` §2 — each now says
the button reads EN on a Korean screen and not to press it. `tests/test_booth_setup.py` gained a
test that pins the mapping in `web/finals.html` and fails if any of the three documents says
「언어가 **KO**」 again.

**The reviewer also demolished my own grading claim, and it was the more useful half.** I had
written that the key table was 「mutation-graded 4 of 4, including a key rebound in
`web/finals.html`」. It was not: the table held bare `case` labels, so the reviewer rebound `g`
from `setView('live'); GUIDED.start(0)` to `GUIDED.exit()` and reversed `ArrowRight` to
`GUIDED.prev()` — two rows of §6 made false — and the file stayed green. A booth does not suffer
a deleted `case` label; it suffers a key that now does something else. The table now holds the
whole binding, label and action, and all three of the reviewer's mutations turn it red.

Two secondary corrections it found, both taken: §6's `1 2 3 4` row omitted the
`state.view === 'live'` guard at `web/finals.html:2145`, so those keys do nothing on the
시스템·근거·신뢰도 tabs — exactly 「질문이 앞뒤로 튈 때」, the moment the row sells them for; and
§7.2 assumed `python3` resolves on a borrowed machine, which is not true on a Windows loaner.

**And it found one thing outside this row that matters more than this row: WFG-109 (P0).**
Rebuilding the screen to check §3.4 shows that WFG-103's fix landed on the built
`web/finals.html` and **not** on `scripts/finals.template.html`, which still carries the sentence
WFG-103 withdrew — 「지도가 지금 이 순간만 본다면, 이 경로를 권했을 것입니다」 and its English
twin. Nothing is wrong on the judged screen today; the next `make finals` puts it back. Filed as
a P0 row with the two line numbers and a done-when that includes a test so a hand-fix to a
generated file can never again outlive its source. `BOOTH_SETUP.md` §3.4 now names it.

**Everything else the reviewer could re-run, it reproduced independently:** the six baseline
differences byte for byte and NH-029's reading of which four are laptop-independent; the
seven-byte corruption silently overwritten by the builder; the stray file the builder reports as
byte-identical; all four screens rendering with zero external requests; and `make finals`
breaking the bundle's byte-identity. It confirmed no §3 rule was broken — `MANIFEST.json` is the
sanctioned `--update` path, `KCF_READINESS.md` is annotated and not ticked, the withdrawn
sentences are quoted rather than deleted.

**Gates.** `gates.py --mode full` **ALL GREEN** (`verify`, `snapshot-verify`, `env-check` PASS;
`baseline-verify` WARN, `hard: false`, and NH-029 is now what that WARN means). The run at the
pushed head is recorded in the gate table below and certified by `--assert-head`. The lap's own
count and the reviewer's differ in how many tests skip (six SRTM-gated tests, WFG-039) and agree
on the total collected; the new tests are the whole of the increase over critic #18's window.

**Next row.** **WFG-109** — it is P0, judge-facing, and one lap old. Then WFG-104 and WFG-106,
DIRECTION's rows 2 and 3.

## In plain terms

- **What changed for the project.** You now have the booth day written down — what to do the
  night before, the ten minutes after registration, the five keys to press, and what to do if
  the laptop dies. Every command in it was actually run, not copied from another file, and that
  is how three things turned out to be wrong: the check the project told you to run on the
  laptop does not pass, the command that was supposed to catch a broken USB stick never looked
  at the stick, and the folder that goes on the stick had a stray file in it.
- **Why it matters to the judges or the science.** A judge scoring 완성도 is scoring whether the
  thing survives contact with a room. A booth kit whose integrity check does not check the thing
  it says it checks would have failed exactly once, in front of five judges, with no recovery.
  It checks now, and the checker travels on the stick so it works even on a borrowed laptop.
- **What you should do.** One new decision, **NH-029**: the baseline freeze is stale, so
  `make all-checks` stops early on your laptop too — reply with A (re-freeze it on the laptop),
  B (change the readiness line to name `gates.py --mode full`) or C (leave both; the recipe
  already warns). And the one that has been yours for two days: run the recipe once on the real
  laptop and tell me the date and the Python version (**NH-014**). Nothing else needs you: the
  reviewer caught one instruction in the recipe that would have switched your demo into English
  in front of a judge, it is fixed in all three places it appeared, and a test now holds it.



## Gates

**ALL GREEN** · mode `full` · head `5aecc5f` · 2026-09-05T13:02:45Z · current at `5aecc5f`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 20.5 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.2 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 0.5 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 308.7 s | 1512 passed, 56 skipped, 52 warnings in 307.32s (0:05:07) |

## Commits since the previous report (c8765e5712a683fd5ad7eb392d5cc1b3a9162587..HEAD)

- 5aecc5f WFG-037: the booth day is written down, and running its own commands falsified three of them
- b108043 claim WFG-037 (20260905T1217Z)
- e2628f3 the assertion said no and the push went anyway, because both were on one line joined by a semicolon

## Needs a human (9 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-025 [DECISION] `Main` cannot follow the green commit until you change one GitHub setting, and until you do, every green push emails you a red run (by 2026-09-08)
- NH-026 [DECISION] One of the five routines pushes on a gate that does not run the test suite, and its prompt is the one the repository cannot show you (by 2026-09-08)
- NH-027 [DECISION] The one experiment that would test the project's headline against a fair opponent is scheduled for after the competition (by 2026-09-08)
- NH-028 [DECISION] The manuscript is full, and three laps have recorded that as a note rather than asked you (by 2026-09-10)
- NH-029 [DECISION] The baseline freeze is stale, so `make all-checks` cannot pass — on this machine or on yours (by 2026-09-10)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
