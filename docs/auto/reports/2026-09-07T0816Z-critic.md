# WildfireGuardian autoloop · critic · 2026-09-07T0816Z

| | |
|---|---|
| when | 2026-09-07 08:16 UTC · 2026-09-07 17:16 KST |
| branch / head | `auto/dev` / `2896c9c` |
| kind | critic |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 7, done: 25, parked: 1, todo: 85 |

## What happened this lap

Critic lap #33. Reviewed head `2896c9c`, window `b70e464..2896c9c`, the 24 h to
2026-09-07T08:00Z, 58 commits. No code, tests, data, figures or `docs/NUMBERS.json` were
touched; writes are confined to `docs/auto/`.

## Your decisions, applied

**Nothing new on either channel.** The Gmail search
(`from:siyeong0318@gmail.com subject:"WildfireGuardian autoloop" newer_than:14d`) returns threads
that are every one a single message this loop itself sent to you; there is no reply from you in
any of them. PR #31 has no comments. `scripts/auto/decisions.py` was therefore not run and
`docs/auto/decisions_seen.json` is unchanged. Twelve NEEDS_HUMAN entries are open, nine of them
DECISION entries; their reply lines are at the end of this report.

## The findings, ranked

### 1. The gate is RED in this sandbox on a correct tree, and this repository predicted it in writing eight days before it happened, then priced the prediction as housekeeping

This is the one `fix-before-next-row` item. It qualifies under CHARTER §14b twice over: it is a
**red gate**, and its subject is **`web/finals.html`, the finals screen**, which §14b names as a
judge-facing surface.

Measured at `2896c9c`, on the routine's own default clone, before any deepening:

- `git rev-parse --is-shallow-repository` = **`true`**; `git rev-list --count HEAD` = **50**.
- `.auto/venv/bin/python scripts/auto/gates.py --mode full` exits **1**:
  `2 failed, 1646 passed, 62 skipped, 2 xfailed`, cold, 247.0 s.
- Both failures are `tests/test_finals_screen.py`:
  `test_the_integrity_panel_names_a_commit_this_repository_has` (`:547`) and
  `test_the_escape_this_gate_cannot_close_is_still_open` (`:650`). Both say the same thing:
  `web/finals.html` names commit **`62b58e1`**, which is not an object in this repository.
- `62b58e1` was built **2026-09-06T09:21Z** and is **55** commits behind `HEAD`.
- After `git fetch --unshallow` (531 commits) both tests pass, the object resolves, and
  `git merge-base --is-ancestor 62b58e1 origin/auto/dev` exits 0. Nothing in the tree is corrupt.
- GitHub `auto-gates` run **201** at this exact head is `success`, because
  `.github/workflows/auto-gates.yml:22` checks out at `fetch-depth: 0`.
- The crossing point is measurable. At `0fc6130` the stamp was **49** back, inside a depth-50
  horizon, and critic #32 reported ALL GREEN. At `f6d8246` (2026-09-07T05:22Z) it reached **50**.
  Every default-clone gate run since has been red.

**WFG-119 wrote this down first.** Its cell, filed 2026-09-05, reads: 「**Predicted and not yet
observed:** the screen's own stamp `5f9a3b8` is 7 commits behind `HEAD` today, and the branch takes
on the order of 40 commits a day, so once a stamp ages past 50 commits without `make finals` being
re-run the ancestry test goes RED in every sandbox while staying GREEN in CI, which checks out at
`fetch-depth: 0`. A red that only the sandbox sees is the mirror image of CHARTER §4b's case and
would be read as a real defect.」 Every clause of that is now confirmed with a date on it. The row
sat at **P1**, explicitly 「parked behind R1, R3, R7, R8 and R9 by CHARTER §14b」. It fired while
parked. Raised **P1 → P0**; that is this lap's one row move.

**The half that matters more than the red: the gate is right and the tree is wrong.**
`_needs_git_history` (`tests/test_finals_screen.py:526`) deliberately declines to skip on a shallow
clone, and its docstring argues the residual is 「a red gate asking for a rebuild, not a wrong screen
shipped」. That argument holds exactly. The screen a judge opens reports a build **55 commits and 23
hours stale**, `make finals` is the remedy the failure text itself prints, and this gate is the only
thing in the repository that noticed. The 0711Z dev lap hit these identical two failures, ran
`--unshallow`, wrote 「They passed once the clone was complete; nothing in the tree was wrong」 under
a heading reading 「Also verified rather than assumed」, and filed nothing.

⚠ One consequence worth stating plainly for the next lap, and it is written at the top of
`CRITIC_LATEST.md`: CHARTER §4 step 2 says 「Red baseline → do not build」. A dev lap arriving at this
head is entitled to read this red as a stop and park. It is not one. The red **is** the item, and
clearing the item clears the red.

### 2. A row filed as "one character" and parked at P1 is now printed in the booth kit, and the measurement behind it is more useful than the row says

`docs/related_work.md:104` and `:187`, and `docs/auto/finals/RELATED_WORK_PANEL.md:43`, all date the
사이언스타임즈 NIFoS article **2026-02-12**. Critic #29 measured **2026-02-13** on 09-06 and filed
**WFG-146**, correctly describing it as one character in a knowledge note, at P1. In the 24 h since,
WFG-026 shipped and the wrong date travelled onto two new surfaces, one of which is
`RELATED_WORK_PANEL.md` — `SOURCES[5]` of kit `20260907T0705Z`, **3 of its 36 printed pages**, and in
`release/kcf-finals-2026/MANIFEST.json`. It is on paper in a judge's hand and on the stick.

I re-fetched the page rather than inheriting the earlier read, and the picture is better specified
than "off by one day". The page's own byline block (beside its 연합뉴스 credit) and its 저작권자 line
both give **2026-02-13**, twice. The three `2026/02/12` strings on the page are Yonhap image CDN paths
(`img8.yna.co.kr/etc/inner/KR/2026/02/12/…`) and the wire id `AKR20260212072300063` — that is the
**연합뉴스 original's** date, which 사이언스타임즈 republished one day later. **Both dates are real and
they belong to different publishers**, and what the repository has done is pair 사이언스타임즈 with
연합뉴스's date. CHARTER §3 rule 5b makes the agency-and-date pairing what licenses an external figure
to be written at all, so the fix is a pairing and not a character: 「사이언스타임즈 2026-02-13
(연합뉴스 2026-02-12 기사 전재)」, or cite 연합뉴스 with 2026-02-12, and say which was chosen.

This is the loop's own standing root objection running backwards. The loop keeps finding that a
**correction** reaches the pages it reads and stops at the surfaces a human meets. This is an
**uncorrected error** travelling the other way, from a note the loop reads onto a page a judge holds,
in a single lap, because the row was priced by the size of its diff instead of by where its subject
was about to go. Row updated with the measurement and an extended surface list, not duplicated.
⚠ It can no longer be taken by a critic or research lap: the panel is a kit `SOURCES` document, so
the lap that edits it must rebuild the kit in the same lap (WFG-152).

### 3. WFG-144's value changed shape: the printed page now answers the question the student cannot

Re-ran the drill against `docs/auto/JUDGE_QA.md`: the only `산림청` hits are the two burned-area
lines at `:399` and `:996`, unrelated, so there is still **no card** for 「산림청·경기도가 이미
산불확산예측을 하고 있는데 무엇이 다릅니까?」 — fifth consecutive lap measured. What is new is that
`RELATED_WORK_PANEL.md` now answers it well, in three printed pages inside the kit. So the failure
mode is no longer a silence but a mismatch: a disaster-response judge asks, the student has no
rehearsed answer, and is standing beside a panel that invites the question. Worse at a booth than
either half alone. Still P1 (§14b allows one move and it went to the red gate). **If you promote one
row from this report, it is still this one.**

### 4. WFG-139 unrepaired, sixth consecutive measurement

`data/raw/` held only `.gitkeep`, `README.md` and the KFS CSV before the gates here. Cold count
`1646 / 62` against the 0711Z lap's warm `1654 / 56`. Unchanged and untaken.

## Root objection (`hate`)

**This loop's rigour habit has been quietly disabling one of its own gates.** Every critic report
since #24 opens with 「clone unshallowed before any measurement」, and for ancestry and reachability
claims that is correct and hard-won — it is what withdrew the `41498ef` finding after five laps got
it wrong inside too short a graph. But `tests/test_finals_screen.py`'s stamp gate is not an ancestry
claim about history; its entire content is **「is the stamp close enough to `HEAD` that a fresh clone
can see it」**, and `--unshallow` answers that yes by construction. The habit that fixed one class of
error is the thing that suppresses the signal of another, and the two laps that hit this red both
made it go away rather than reading it.

**The cheapest test, and it was run:** `gates.py --mode full` on the routine's own default clone with
no deepening at all. Two failures out of 1648, both this one, and both green after `--unshallow`.

## What is green, verified rather than read

- `--assert-reported --base b70e464` over the whole 24 h window (58 commits) exits **0**, 56
  substantive paths. Every push in the window carried a report.
- GitHub Actions through the MCP (`curl` is 403 here, WFG-119): `auto-gates` runs **182 to 201** on
  `auto/dev` are **17 `success`, 3 `cancelled` (each superseded by a green push), no `failure`**;
  run 201 at this head is `success`. **No CHARTER §4b finding.**
- `verify`, `snapshot-verify`, `env-check` PASS; `baseline-verify` WARN is the documented §3d state.
- Every **dev** report in the window carries `Reviewed by:`. The research report still does not
  (WFG-147, known, unchanged).
- **Critic #32's falsifiable test (1) came back in the dev lap's favour:** WFG-026 shipped and
  `WC-006` is in `docs/auto/withdrawn_claims.json`, so CHARTER §3.5c is not advisory in practice.
  Its test (2) likewise: WFG-156 shipped occurrence-level and R7's cell is correct at this head.

## Readiness: R7 TICKS, 6 of 11

Second consecutive lap with a line moving, after nine laps of no movement. Every condition
re-measured here rather than read from the lap that built it: kit `20260907T0705Z`, six `SOURCES`
re-hashed and all six matching, PDF sha256 matching its manifest, `pages_per_source` summing
5+6+17+3+2+3 = **36** = `pages` = the PDF's own `/Type /Page` count, the stamp present in the 19-file
bundle manifest, and the three committed `dispatch_a4.pdf` sample sheets opened on disk. The 0711Z
dev lap explicitly left the four-of-five question to the critic; it is answered on R7's row with its
ground **and** its falsifiable alternative, so no later lap re-derives it.

## Scorecard deltas (`2896c9c`)

**Track B 84 → 85.** 설계와 방법론 **16 → 17**: 「기존의 연구와의 차별점」 is literally this cell, and
`docs/related_work.md` is now 16 entries with resolved identifiers, a fair account of the two Korean
operational systems that concedes NIFoS's 5 m terrain analysis is finer than ours, and a seven-axis
SFTD059T panel. +1 and not +2 because the page's first draft asserted seven citations to four
repository files that do not exist and overclaimed three `verified` notes, and the **independent
reviewer** caught both, not the lap. 제출 자료 **18 held on two movements that cancel**: R7's tick and
an unusually honest provenance block (it names which three of its sixteen entries were not re-opened,
and forbids them entering the manuscript until one is) against a wrong citation date on a printed page.

**Track A 80 → 82.** 설계와 방법론 **16 → 17** on the same evidence, landing harder because this
track's criterion reads 「기존의 **작품**과의 차별점」 and §3 differentiates against an actual competing
작품. 제출 자료 **14 → 15**: the kit went from 33 pages and five sources to 36 and six. 구현 및 유용성
**19 held**, and finding #1 is what stops it being 20: 20 on this row means the product's own gates
are green in the environment that builds it.

## In plain terms

**What the judges would see today.** The strongest booth kit you have had. It is 36 printed pages
now, and the three new ones are the page that answers 「기존 연구와 뭐가 다릅니까」 — sixteen studies,
what each does and does not do, an honest section saying 산림청 and 경기도 already run spread
prediction and where their systems are better than yours, and a panel on the ISEF project closest to
yours. Your readiness checklist moved again: **6 of 11**, two days running. Two problems a judge could
find: the finals screen reports it was built yesterday morning rather than now, and one Korean news
citation on the printed page carries the wrong day.

**What changed since yesterday.** The differentiation page got written, which was the last thing R7
was waiting on, so R7 ticks. The claim registry passed its first real test. And I found the automatic
checks going red on a clean copy of your project, not because anything is broken, but because the
screen's build stamp is now older than the slice of history the cloud machine downloads.

**What you should do.** Nothing urgent, and nothing is on fire. If you want to spend one decision:
promote **WFG-144**, the spoken Q&A card about 산림청 and 경기도. Your printed page answers that
question now and your rehearsal deck does not, and a judge who asks it will be looking at you, not at
the page. The rest is the loop's to fix and the next lap has it as its first job.



## Gates

**RED** · mode `full` · head `2896c9c` · 2026-09-07T08:03:16Z · current at `2896c9c`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 17.1 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.1 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 1.9 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | FAIL | 248.5 s | 2 failed, 1646 passed, 62 skipped, 2 xfailed, 52 warnings in 247.01s (0:04:07) |

⚠⚠ **The RED above is finding #1 and it is deliberately left in this table rather than replaced by a
greener run.** It is the routine's own default clone (`is-shallow-repository` = `true`,
`rev-list --count HEAD` = 50) at `2896c9c`, and the two failures are
`tests/test_finals_screen.py::test_the_integrity_panel_names_a_commit_this_repository_has` and
`::test_the_escape_this_gate_cannot_close_is_still_open`, both because `web/finals.html` names
`62b58e1`, 55 commits back.

**Annotation, so the reader is not misled in either direction.** After `git fetch --unshallow`
(531 commits), `gates.py --mode full` re-run on the commit this report ships in, `d4445d6`, is
**ALL GREEN, exit 0** — `1654 passed, 56 skipped, 2 xfailed`, **warm**, 244.5 s — with
`--assert-head` and `--assert-reported` both exiting 0 before the push. ⚠ **That green is
conditional on the deepening and nothing else.** This commit touches only `docs/auto/` prose and
images; a critic lap may not edit code or `web/finals.html`, so **the same commit is still RED on a
default depth-50 clone**, exactly as `2896c9c` is, and will stay red until the next dev lap clears
WFG-119. The push is correct all the same: the red predates this commit, exists identically at
`2896c9c` which is already on `origin/auto/dev`, and GitHub's own clean-clone run 201 certified that
head `success` at `fetch-depth: 0`. Parking this report on `auto/red/` would bury the only record of
the defect. ⚠ Warm rather than cold is **WFG-139** for a sixth consecutive lap: the cold run above
reports `1646 / 62` and this one `1654 / 56` on trees differing only in this lap's prose, the same
six-test gap in each direction plus the two stamp tests.

## Commits since the previous report (d2640ca740c7aaba8b132dc26e1e423f67a73821..HEAD)

- 2896c9c the dev report names the head it ships in, and which of this lap's two kit stamps to print
- d75d2ba the reviewer's block: the page that refuses to compare accuracy pointed "the measurements are over there" at four files that do not exist

## Needs a human (15 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09)
- NH-036 [DECISION] One critic lap told the next one not to edit a file, and that is what kept a false sentence in front of a judge for a window (by 2026-09-10)
- NH-037 [DECISION] The paper's word proxy now stops it a thousand words before your 25-page rule (by 2026-09-10)
- NH-038 [DECISION] Your "product first" rule has spent the last three dev laps on documents, and the readiness line it was written to protect has not moved in five critic laps (by 2026-09-09)
- NH-039 [DECISION] The national wildfire-spread system's manual is an 18 MB PDF the sandbox could not fetch, and one of you can (by 2026-09-12)
- NH-040 [FYI] A critic lap pushed one commit past a red `--assert-reported`, and it is telling you rather than hiding it
- NH-041 [FYI] This lap sent you an email containing only the word PLACEHOLDER, and could not take it back
- NH-042 [DECISION] Two of your own rules collide whenever a withdrawn claim lives in a frozen artifact, and this week they collided three times (by 2026-09-10)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
