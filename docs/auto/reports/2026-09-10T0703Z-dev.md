# WildfireGuardian autoloop · dev · 2026-09-10T0703Z

| | |
|---|---|
| when | 2026-09-10 07:03 UTC · 2026-09-10 16:03 KST |
| branch / head | `auto/dev` / `5bcfe11` |
| kind | dev |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 9, done: 33, dropped: 1, parked: 1, title-derivable: 1, todo: 106 |

## What happened this lap

## Your decisions, applied

**Nothing new in either channel, and one line is what that deserves.** The Gmail search
`from:siyeong0318@gmail.com subject:"WildfireGuardian autoloop" newer_than:14d` returns
only this loop's own sent reports — every thread on the first page holds exactly one
message; `subject:"Re: WildfireGuardian autoloop" newer_than:14d` returns **zero** threads;
the four non-report hits for `NH-` are the loop's own `ci-red` emails. PR #31 has **zero**
comments. So no `NH-###:` line reached the loop, `decisions.py` was not run, and
`docs/auto/decisions_seen.json` is unchanged. **Twenty DECISION entries stay open, six of
them at or past their stated date; NH-037 and NH-046 both came due yesterday.**

---

## The lap

Row: **WFG-128** (P0, KCF), named by `docs/auto/DIRECTION.md` after critic #56's one §3b
reorder. Claim commit `97978fe`, pushed before any building. No stale `in-progress` to
release. Work commit `5bcfe11`.

### 1. Critic #56's one `fix-before-next-row` item, taken first because it was minutes

`docs/oracle_gap.md` §4 read a size-ratio series as a property of the **forecast** when
three of its four slices are graded against **one** observation. Confirmed against
`data/processed/oracle_gap_yeongdeok.json` rather than inherited: the four non-seed slices
carry `time_gap_min` **153 / 27 / 207 / 285** and `obs_time_min` **333 / 333 / 333 /
1005**. The ratio series is therefore a growing simulated area over a **constant** observed
area for three of its four terms, and the 3 h slice was compared with a footprint 153
minutes later than the forecast time — a matching gap read as forecast bias, on the page
four judge-facing surfaces send a judge to.

§4 now carries a table printing the observation **and** the gap beside every ratio and
every IoU; §4b's IoU series carries its four gaps; line 6's 「10 keys」 is replaced by the
prefix and where to count it, rather than by 25, because a key count in prose goes stale
the next time a registrar grows — which it then did, in this same lap.

Registered as **`WC-014`** in the same commit (CHARTER §3.5c). Blast radius measured over
all **1,134** tracked `.md`/`.html` *before* registering: **12 hits, every one in a
declared record path**, zero in gated prose, so no pragma anywhere. The corrected page does
**not** quote the withdrawn sentence — the WC-005/WC-007 precedent for a surface a judge is
sent to. Both languages went in together; the Korean spelling matches nothing today and is
a ratchet.

### 2. WFG-128

`classify()` in `scripts/run_real_roads_real_hazard_slope.py` passes `time_budget_min` to
`future_aware_route` and **no budget at all** to `naive_route`. So 「fire-blind route safe
**but** future-aware over budget」 is a comparison only one of the two arms can lose — and
that bucket's counts are the ones that run **against** this project.

`docs/multi_region.md` §3.1 now states, in the same section as the counts: the asymmetry,
at the code; that under one rule the **Uiseong-Andong bucket is empty**
(`docs/present_perimeter_arm.md` §2 — those two origins' fire-blind routes arrive at 624.8
and 628.2 minutes, both registered keys); and the two remaining entries separated rather
than lumped. `README.md:145-149` carries the clause in the **same bullet block** as
「각 2곳·3곳」.

**Where I departed from the row's own wording.** Its done-when asks the page to say
Yeongdeok's 0 「cannot be re-read」. True of the walk graph (overwritten 2026-07-24) and the
weaker argument. The bucket's membership test requires the fire-blind route to be **safe**,
so budgeting that arm can only move origins **out** — they fall to `naive_unreachable` at
the first branch — and 0 cannot rise. The page says that, and says it is an argument about
the classifier rather than a measurement. Uljin-Samcheok's 3 is described by the same
mechanism and is **not** called a bound of any kind: NH-053 is open.

**Nothing committed moved.** `mr_uiseong_fa_exceeds_budget` stays 2; NH-031 option A is the
author's decision and `WFG-122` carries it. No margin value reaches any surface.

### 3. The gate, and what it got wrong first

`tests/test_budget_rule_asymmetry_is_stated.py`, 20 assertions. Each surface graded twice —
`_REQUIRES` and `_DENIES` — because the MEMO entry one lap above says a gate over prose
asserts a direction or it asserts nothing. A third test pins the asymmetry in `classify()`
itself, so a lap that budgets the fire-blind arm is told to rewrite the prose rather than
left to discover the pages now understate. Four mutations, **all red**, reported as they
came out: M1 3 red · M2 2 red · M3 5 red · M4 2 red.

⚠ **M2 scored 1, not 2, on the first draft.** The denial pattern was anchored on 「both
arms」 and the page says 「The two arms」, so the *inverse assertion itself* went undetected.
Widened before the docstring claimed anything; the miss is in the docstring and the MEMO.

## The root objection I recorded before building (`hate`)

*A prose clause on two surfaces plus a token-presence test is exactly the gate shape this
repository has already watched pass on a block asserting the opposite.* The cheapest test
was to write the inversion myself and run it. It found a real hole on the first try.

## Reviewed by: subagent (block) — and the block was the most useful thing this lap produced

**Root objection, verbatim in substance:** *the lap invoked the registry to license an
omission, and the registry said yes.* §4's first draft said the per-slice observation times
「carry no registry key, and CHARTER §3 rule 3 says a number you cannot register you do not
write」. Both halves false. `og_yeongdeok_obs_time_min` = 333.0 has been registered since the
document was written; §4's own result table prints it **58 lines above** the sentence
denying it; §7 prints the whole `obs_times` list 57 lines below. And the withheld column was
the **evidence for the section's own load-bearing claim** — 333 appearing three times *is*
「three of the four slices share one observation」. A rule invoked to license an omission is
worse than a wrong number, because the next reader inherits a registry that says no when it
says yes.

**Fixed by registering, not by rewording.** Five `og_yeongdeok_t###min_obs_time_min` keys
added additively through `scripts/register_oracle_gap.py`: 5 keys added, **0 existing values
moved**. §4 gained the observation column, every cell of it a key, and the note now records
the mistake and its general form. That closes **WFG-215's registry half in the same lap as
its prose half** — the whole row, not the preemption.

**Second nail, and it is the WC-004 shape the registration was meant to close.** All 25
`og_yeongdeok_*` entries in `docs/NUMBERS.json` carried the withdrawn sentence verbatim
inside a caveat band whose own first line reads 「Six facts travel together or none may be
quoted」 — an *instruction* to repeat it, read by every lap and by the paper routine, whose
`paper/manuscript.md` §5 is gated prose. The blast-radius sweep could not see it:
`scope.extensions` is `[".md", ".html"]`. The instance is fixed **at its source** in the
registrar's caveat band, re-run additively (25 caveats rewritten, 0 values moved). The class
is filed as **WFG-224**, with the cost of the obvious fix measured rather than assumed.

**Third, smaller, same species as the first.** The test's docstring claimed a WFG-185 block
scope it does not have — `_block_carrying` runs to the next heading, ~50 lines in §3.1 — so
it buys 「cannot leave the section」, not adjacency. The docstring now says which.

**What the reviewer verified and left standing**, in its own words: the `classify()`
asymmetry (`naive_route` does not even accept a time budget), the monotonicity argument for
Yeongdeok's 0 as sound *as written*, the shared-observation measurement to the digit, and
the `WC-014` blast-radius count. ⚠ It was recalled before finishing the mutation grading and
before `gates.py --mode full` finished, and says so; both were re-run to completion here.

## Two gates went red on the registry growth, and both were right to fire

- `tests/test_oracle_gap.py` pinned 「25 keys」 as a **literal**. It now re-derives the count
  from the registrar's own `FIGURES` and `SLICE_FIELDS`, because a literal there has the
  WFG-117 fuse: it goes stale on the next legitimate growth and teaches the lap that hit it
  to think the growth was the defect.
- `web/finals.html` prints the registry count and was stale at **448 / 390** against
  **453 / 395**. Screen rebuilt and the release manifest re-pointed (19 files).
  ⚠ `make finals` stamps `HEAD` and the stamp must already be on `origin`
  (MEMO 2026-09-09), so this lap `reset --soft` back to its own **pushed** claim commit
  before building; the screen stamps `97978fe`. That MEMO note has now paid for itself
  twice.

## What did not work / what I did not do

- **Uljin-Samcheok was not re-scored** under one rule. That is a routing re-run, not a
  minutes-sized row, and the page says so instead of guessing.
- **`docs/auto/DIRECTION.md` is now stale in one bullet** and dev never writes that page
  (CHARTER §14). Its ⚠ 「Do not print a per-slice `obs_time_min` … Only the headline is
  registered」 was true when written and is false at this head: all five are registered.
  **For the next critic lap.**

## Gates

`gates.py --mode full` exits **0** at `5bcfe11`: `verify` PASS, `snapshot-verify` PASS,
`env-check` PASS, `pytest-full` **1985 passed, 64 skipped, 3 xfailed**. `baseline-verify`
WARNs on the two git-ignored `data/raw/**` contracts — NH-029 and CHARTER §3d working as the
author chose. GitHub `auto-gates` run **318** is `success` at `ddb567c`, so CHARTER §4b set
no finding #1 for the sixth consecutive lap.

## Next row

**WFG-117** — DIRECTION says WFG-007's remaining work is the student's and names WFG-117 as
the one to take before it.

## In plain terms

- **What changed for the project.** The one number in your project that makes it look *bad*
  — the handful of places where the forecast-aware route runs out of time and the dumb route
  does not — turns out to have been measured with a stopwatch on only one of the two runners.
  Both pages a judge reads now say so, and say that under a fair rule one of those two counts
  is actually zero. Separately, the page a judge is now sent to for 「what we cannot do」 was
  reading a fairness problem as a forecast error; it now shows, in one table, how far apart in
  time each comparison was.
- **Why it matters to the judges or the science.** Both fixes make the project *look worse on
  paper and be more trustworthy in the room*, which is the trade this project has won on all
  along. And the loop's own reviewer caught the lap doing the thing the lap was fixing —
  withholding a number and blaming a rule — which is the strongest evidence yet that the
  review step is not decorative.
- **What the author should do.** Nothing for this row. But **twenty decisions are open and six
  are past due** — NH-037 (the paper is three words from its hard limit) and NH-046 (which
  alone is holding readiness line R3, and R3 is the last thing blocking the P1 queue) both
  came due yesterday. One reply with six lines clears the overdue ones.



## Gates

**ALL GREEN** · mode `full` · head `5bcfe11` · 2026-09-10T07:02:38Z · current at `5bcfe11`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 19.0 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.1 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 0.3 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 283.2 s | 1985 passed, 64 skipped, 3 xfailed, 52 warnings in 282.36s (0:04:42) |

## Commits since the previous report (9b7d21c8dfecf56409081245f601fa4b40aa8ff6..HEAD)

- 5bcfe11 WFG-128 and critic #56's preemption: the two counts that run against this project were measured with a rule that binds one arm, and the page that says so withheld its own evidence
- 97978fe claim WFG-128 (20260910T0625Z)
- ddb567c the critic report names the head it ships in, and keeps the head it describes
- cb7ca6d the report said six decisions are open and twenty are
- 9e296a6 critic #56: the page a judge is sent to for the limits misreads its own artifact, and the guardrail's line numbers moved

## Needs a human (25 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09, one day past; raised to HIGH by critic #55 on a measured third instance)
- NH-036 [DECISION] One critic lap told the next one not to edit a file, and that is what kept a false sentence in front of a judge for a window (by 2026-09-10)
- NH-037 [DECISION] The paper's word proxy now stops it a thousand words before your 25-page rule (by 2026-09-10)
- NH-038 [DECISION] Your "product first" rule has spent the last three dev laps on documents, and the readiness line it was written to protect has not moved in five critic laps (by 2026-09-09)
- NH-039 [DECISION] The national wildfire-spread system's manual is an 18 MB PDF the sandbox could not fetch, and one of you can (by 2026-09-12)
- NH-040 [FYI] A critic lap pushed one commit past a red `--assert-reported`, and it is telling you rather than hiding it
- NH-041 [FYI] This lap sent you an email containing only the word PLACEHOLDER, and could not take it back
- NH-042 [DECISION] Two of your own rules collide whenever a withdrawn claim lives in a frozen artifact, and this week they collided three times (by 2026-09-10)
- NH-043 [DECISION] A gate your loop built this morning will go red about twice a day, and the charter tells the lap that meets it to stop working (by 2026-09-09)
- NH-045 [BLOCKER] The staleness gate has closed `auto/dev` to every routine, and the one routine that met it is the one forbidden to clear it (by 2026-09-08)
- NH-044 [DECISION] The claim the paper just retracted is still live on the page the paper cites for it (by 2026-09-09)
- NH-046 [DECISION] Your product's definition-of-done names a command nothing in this project has ever run (by 2026-09-10)
- NH-048 [DECISION] One of the research routine's three literature channels has been dead for two runs, and a working replacement is already proven (by 2026-09-10)
- NH-049 [DECISION] Your critic routine is told to add judge Q&A cards and your own printing gate makes that impossible for it (by 2026-09-11)
- NH-050 [DECISION] You answered two of these questions two days ago and the loop never heard you, because you answered them on the routine page (by 2026-09-10)
- NH-051 [DECISION] The rule your loop has been obeying for three days is not the option it names, and the difference is why one row has been pushed down five times (by 2026-09-11)
- NH-052 [DECISION] The experiment that measures what your own forecast is worth is now one run away, and it would change what 42 means (by 2026-09-12)
- NH-053 [DECISION] The word your front door uses for 42 stopped being right today, and the loop cannot pick its replacement (by 2026-09-12)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
