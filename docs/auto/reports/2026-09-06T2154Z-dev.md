# WildfireGuardian autoloop · dev · 2026-09-06T2154Z

| | |
|---|---|
| when | 2026-09-06 21:54 UTC · 2026-09-07 06:54 KST |
| branch / head | `auto/dev` / `8e902dd`, rebased onto the concurrent paper lap as `20b0b7e`; the report ships in the commit after it, and all three ids resolve |
| kind | dev |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 7, done: 17, parked: 1, todo: 84 |

## What happened this lap

Row: **WFG-138** (P0, KCF) — critic #29's one `fix-before-next-row` item, carried verbatim
from critic #28. Claimed at `8cc1e89` (20260906T2117Z), built at `0c7f1c0`.

## Your decisions, applied

**Nothing new on either channel, and this is the one line the charter asks for.** The Gmail
search `from:siyeong0318@gmail.com subject:"WildfireGuardian autoloop" newer_than:14d`
returns 20 threads, every one of them a single message the loop itself sent, all labelled
`SENT`; no thread carries a reply. PR #31 has no comments at all. So no `NH-###:` line
existed to parse, `decisions.py` was not run, and `docs/auto/decisions_seen.json` is
unchanged. Twelve NEEDS_HUMAN entries are open and **nine** of them are DECISION entries
waiting on you; NH-032 in particular is
what keeps every fair-opponent margin off every judge-facing surface, including the two
sentences this lap rewrote.

## What I did, and why

The item had two halves and both were one clause long.

**(a) `README.md`, the 「Headline result」 bullet.** It said 42 of 458 scanned origins reach
a refuge **only** when the router accounts for where the fire will be. `paper/manuscript.md`
carries the same two numbers and then says, in its Abstract: that contrast is measured
against a fire-blind baseline, so it does not separate knowing where the fire will be from
knowing where it is. The baseline is fire-blind in the repository's own code
(`src/wildfireguardian/routing/evacuation.py:270`). The loop had repaired that claim on the
booth script (WFG-103) and the finals template (WFG-109) and in the manuscript, and the
README — the first file a judge, an ISEF reviewer or an IEEE reader opens, and what
`CITATION.cff` points at — was the fourth surface and had never been touched. It now carries
the manuscript's own caveat inside the same bullet, plus the fact that the fair opponent has
run on 의성·안동 only and never on 영덕, which is where the 42 comes from. No number moved;
no contested margin (9, 27, 5, 19) reached the surface (NH-032 is open).

**(b) `docs/auto/JUDGE_QA.md` Q19, the draft answer the student speaks.** The same sentence
states the 42 and the 91 together; the ⚠ block directly beneath it corrected the 91 and said
nothing about the 42. The caveat is now in the quoted draft itself — 「42도 91도 **불을 전혀
보지 않는** 경로와의 비교입니다. 지금 불난 자리를 피하는 경로와의 비교는 의성·안동에서만
했고, 영덕에서는 아직 하지 않았습니다.」 — because a note beside a sentence is precisely what
failed here for two windows.

## The root objection I recorded before building (`hate`), and what testing it changed

*Objection: the row names two surfaces, but the failure mode it describes — a correction that
lands next to the sentence rather than in it — predicts a sixth surface, and repairing two
named files leaves the class open.*

Cheapest test: sweep every tracked `.md` and `.html` outside the report archive for the 42
and for the attribution phrasing. Result — **no sixth surface was broken**:
`docs/auto/DEMO_SCRIPT_5MIN.md`'s 3막 states the control inside the spoken line itself
(「불을 전혀 보지 않는 지도」), `web/finals.html` and `scripts/finals.template.html` carry only
the neutral column label 「시간 인지만 안전」, and every other hit (README:85/103,
`docs/HANDOFF_ROUND3.md`, `docs/multi_region.md`, `docs/building_origins.md`,
`docs/submission_reconciliation.md`, …) is a bucket count 414/42/2, not a causal claim.

**But the objection was half right and the reviewer found the half I missed.** The demo
script — the most judge-facing surface there is, because it is spoken at the booth — states
the same claim and was gated by **neither** test. It is correct today by luck, not by a
check, and 「correct by luck」 is how the README got here. It is now a third surface in the
attribution gate. What the objection changed structurally: both gates read a **class of
sentence across a file**, not the two lines the critic quoted, so the next card or bullet
that states the claim is caught without a new row.

## Evidence

| what | where |
|---|---|
| README bullet, repaired | `README.md:22-33` |
| Q19 draft answer, repaired | `docs/auto/JUDGE_QA.md:693-696` |
| Korean gate (every draft answer, not only Q19) | `tests/test_judge_qa_bank.py::test_no_draft_answer_states_the_future_aware_only_claim_bare` |
| English/Korean surface gate (README, manuscript, demo script) | `tests/test_future_aware_attribution.py` (16 tests + 1 strict xfail) |
| the probe set the gates are scored on, written by the reviewer | `REVIEWER_SET_BARE` / `REVIEWER_SET_CAVEATED` / `REVIEWER_SET_REWORDED` in the same file |
| the control is still called fire-blind in the code | `src/wildfireguardian/routing/evacuation.py:270` |
| the fair opponent's region scope | `docs/present_perimeter_arm.md`, `data/processed/present_perimeter_arm_uiseong_andong_2025.json` |

Gate grades, each by mutation rather than by passing:

| gate | mutation | result |
|---|---|---|
| bank gate | delete the new sentence from Q19's draft | **red**, naming Q19 |
| attribution gate | restore the pre-lap README bullet (`git checkout 8cc1e89 -- README.md`) | **red**, naming `README.md` |
| attribution gate | **move**: pre-lap bullet, caveat planted four bullets below it | **red** (green before the rebuild — the reviewer's nail) |
| attribution gate | strike `fire-blind` from both manuscript blocks holding the claim | **red**, naming `paper/manuscript.md` |
| both, repaired tree | — | 40 passed, 1 xfailed |

`gates.py --mode full` **exit 0** twice, and the two runs are **not like for like** — WFG-139
is why, and the rule from `DIRECTION.md` is that a count without cold-or-warm beside it is not
a count:

- **cold**, first run of the sandbox, tree of `0c7f1c0`: `1603 passed, 62 skipped`, 276.8 s.
  The comparable cold figure last lap was `1599 / 62`; **+4** is this lap's first four tests.
- **warm**, at the pushed head `8e902dd`: `1622 passed, 56 skipped, 1 xfailed`, 269.6 s. Of
  the +19, **6** are WFG-139's terrain tests, which run only because the first suite run in
  this sandbox downloaded the SRTM tile, and **13** are the reviewer-driven rebuild (the
  module went from 3 tests to 16 plus the strict xfail).

`verify`, `snapshot-verify` and `env-check` PASS in both. `baseline-verify` WARN is CHARTER
§3d information, as every lap this week. `--assert-head` and `--assert-reported` both exit 0
immediately before the push.

## What did not work

**The second gate was green on the defect it was written for.** I fixed the README bullet,
wrote `tests/test_future_aware_attribution.py`, watched 3 tests pass, then graded it by
restoring the pre-lap bullet — and it stayed green. The surface writes `**only**` and
`**42 of 458**`, so a literal substring scan sees neither the attribution nor the count
across the emphasis marks and the line break: the gate was reading a sentence that does not
exist in the file it reads. It now flattens `*`, backticks and whitespace before matching.
The first mutation I tried on the manuscript was also unclean (it removed one sentence from a
block that names `fire-blind` twice) and I redid it properly rather than reporting the pass.

**And the mutation I chose was the mutation the gate survived.** After all that, the reviewer
broke the rebuilt gate without deleting anything — it *moved* the caveat four bullets down the
same list and the gate stayed green, because README's TL;DR is one blank-line block. A delete
mutation grades whether a gate can see the token; only a **move** mutation grades whether it
can see the *locality*, and locality is this row's whole subject. Mutate along the axis the
claim is about. All of it is in `docs/auto/MEMO.md` under 2026-09-06T2117Z.

**I did not take WFG-140, and its own done-when is the reason.** The row requires the
freshness test to be **red on today's tree** before a rebuild makes it green, and CHARTER
§3.9 forbids pushing a red `gates.py --mode full` to `auto/dev`. A lap taking WFG-140 alone
has three exits and all are bad: push red, author the gate green by construction (the exact
defect the row exists to prevent), or park a P0 gate on `auto/red/` where nothing reads it.
It ships **with** WFG-134's rebuild inside one lap, in that order. The row now says so.

**This lap is the fourth drift of `docs/auto/JUDGE_QA.md` against the printed manifest** and
it does not exempt itself. It is, however, the first of the four that is a judge-facing
improvement to a spoken answer rather than a correction note — which is critic #29's
falsifiable test 3 answered in the direction it did not predict.

## Reviewed by

**Reviewed by: subagent (pass): the two gates are real gates and all three mutations
reproduce, but the English one does not enforce what its own docstring says it enforces —
`_blocks()` splits on blank lines and README's TL;DR is one 2,159-character block spanning
four bullets, so the gate only required `fire-blind` to appear somewhere in the list. The
mutations that graded it all delete the caveat; none moves it. Move it and the gate is
blind.**

It passed the lap and then broke the gate anyway, without deleting anything: pre-lap
overclaim restored into the bullet, 「(The routing contrast above is against a fire-blind
baseline.)」 planted four bullets below — 3 passed. That is 「a note beside the sentence」,
which is this row's entire subject and which the gate's own failure message calls 「what
failed three times」. Its first nail was worth more than its verdict and I took the fix
rather than the pass:

- a Markdown list item is now its own block, so the caveat must sit in the same bullet;
- `docs/auto/DEMO_SCRIPT_5MIN.md` joins the surfaces (the reviewer found it stated the same
  claim to a judge, gated by neither test — true today only by luck);
- both gates share **one** CONTROL/ATTRIBUTION spelling family instead of two hand-typed
  tokens. The reviewer scored the one-token versions on fourteen sentences it wrote itself:
  **3/7 English, 2/7 Korean**, and the misses included **correct** sentences wrongly flagged
  — the WC-004 direction, where a gate pressures the next lap to delete something true;
- those fourteen sentences are committed as the probe set and re-scored on every run, so the
  number above cannot be quoted from a report while the gate rots;
- the one class no spelling list reaches (a reworded overclaim carrying no 「only」 at all) is
  a `strict=True` xfail, so it is measured rather than unmentioned.

The reviewer also confirmed, by running rather than reading: `naive` is fire-blind at
`evacuation.py:270`; the present-perimeter opponent has exactly one artifact and no 영덕 path
or registry key; `paper/manuscript.md` says verbatim what the README now quotes it as saying;
no committed artifact, registry entry or README opening paragraph was touched; no contested
margin reached a surface; both new tests read only in-repo paths with no clock, timezone or
network. It restored every file it mutated and left the tree clean.

## Next row

**WFG-140 together with WFG-134, in one lap, gate first** — critic #29's own cheapest test,
now with a fourth drift behind it and a written reason why the two cannot be separated.
`docs/auto/DIRECTION.md` names that pair as row 2 and nothing this lap changes the order.

## In plain terms

- **What changed for the project.** The front page of your repository said the forecast is
  what saves those 42 households. Your own paper says that is not what the experiment shows —
  the comparison is against a route that sees no fire at all — and the front page and the
  answer card you read aloud were the last two places still saying the stronger thing. Both
  now say what the paper says, and two tests will go red if that slips back.
- **Why it matters to the judges or the science.** A judge who reads the README and then the
  paper would have found the project contradicting itself about the number it leads with, on
  the exact axis it asks to be judged on. That is the kind of thing a sharp judge finds in
  ten seconds and never forgets. The honest version is still strong: coupling a hazard field
  into the router changes 42 real decisions.
- **What you should do.** Nothing tonight. When you have five minutes, NH-032 is the one
  blocking the most: until you pick which fair-opponent number counts, no margin can go on any
  judge-facing surface, and three sentences the student says are hedged because of it.



## Gates

**ALL GREEN** · mode `full` · head `8e902dd` · 2026-09-06T21:53:44Z · current at `8e902dd`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 17.7 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.2 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 0.6 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 269.6 s | 1622 passed, 56 skipped, 1 xfailed, 52 warnings in 267.85s (0:04:27) |

## Commits since the previous report (1b26c3a8a71ad1d136a09c9ca0e1b5cd0c25da47..HEAD)

- 8e902dd the reviewer broke the gate without deleting anything, by moving the caveat
- 0c7f1c0 WFG-138: the caveat reaches the bullet and the spoken sentence, not the note beside them
- 8cc1e89 claim WFG-138 (20260906T2117Z)
- 850fd83 the critic report names the head it ships in, and its own gate table demonstrates WFG-139 again
- a7e7bac critic #29: the window had no dev lap, and the caveat that reached the 91 stopped at the 42 in the same sentence

## Needs a human (12 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09)
- NH-036 [DECISION] One critic lap told the next one not to edit a file, and that is what kept a false sentence in front of a judge for a window (by 2026-09-10)
- NH-037 [DECISION] The paper's word proxy now stops it a thousand words before your 25-page rule (by 2026-09-10)
- NH-038 [DECISION] Your "product first" rule has spent the last three dev laps on documents, and the readiness line it was written to protect has not moved in five critic laps (by 2026-09-09)
- NH-039 [DECISION] The national wildfire-spread system's manual is an 18 MB PDF the sandbox could not fetch, and one of you can (by 2026-09-12)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
