# WildfireGuardian autoloop · critic · 2026-09-05T0815Z

| | |
|---|---|
| when | 2026-09-05 08:15 UTC · 2026-09-05 17:15 KST |
| branch / head | `auto/dev` / `26e200d` |
| kind | critic |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 6, done: 16, parked: 1, todo: 61 |

## What happened this lap

Critic lap #17 over the window `1049db4..26e200d` on `auto/dev` (6 commits, 25 files,
1,542 insertions). No code, no test, no artifact and no `docs/NUMBERS.json` entry was
touched by this lap; the changes are `docs/auto/` only.

## Your decisions, applied

**None this lap, and the reason is one line: there are no replies to apply.** The Gmail
search `from:siyeong0318@gmail.com subject:"WildfireGuardian autoloop" newer_than:14d`
returns 25 threads, every one of them a report this loop sent to you, every thread exactly
one message long, no reply on any of them. PR #31 carries no comment in the `NH-###: …`
form. `docs/auto/decisions_seen.json` is therefore unchanged, and its `applied` list still
holds only the sixteen items you closed in the Claude Code session on 2026-09-04.

Three entries are open and waiting on you: **NH-005** (Yeongdeok building footprints),
**NH-014** (run the booth recipe once on the real laptop — amended by this lap, see below),
**NH-025** (`Main` cannot fast-forward while it requires a pull-request review) and
**NH-026** (the paper routine pushes on `--mode quick`, and its prompt is not in
`ROUTINE_PROMPTS.md`). This lap adds **NH-027**.

## Findings, ranked

### F1 · WFG-103 · the one `fix-before-next-row` item · judge-facing · one sentence

`docs/auto/DEMO_SCRIPT_5MIN.md:108-109`, inside 3막 — the segment the same document calls
「이 작품의 전부」 — is spoken aloud to five judges:

> **STATIC VIEW.** 거리 기준 직행 경로입니다. **지금 이 순간만 보는 지도라면 이 길을 권했을 겁니다.**

That is not what the arm is. It is fire-blind, and this repository says so twice in its own
words:

- `src/wildfireguardian/routing/evacuation.py:270` — `"""Fire-blind shortest path to the
  nearest shelter, then scored against the hazard."""`
- `docs/real_roads_real_hazard.md:50` — 「**naive** — the fire-blind shortest walk to the
  nearest refuge (the status quo)」

The difference runs in the project's favour, which is what makes it worth a `fix-before-next-row`
slot. A map that sees the fire where it is **now** already refuses the cells that are burning
now, so some unknown share of the **91곳 / 368곳 · 24.73 %** the very next spoken sentence
attributes to knowing where the fire *will* be is bought by knowing where it *is*. The spoken
sentence hands the weaker baseline the stronger description and then reads the headline number
against it. One sentence, every fact already committed, on the document R4 is ticked for.

**Scoped deliberately small.** WFG-103 is the sentence. It is not the arm study and it is not
the Q&A card; those are WFG-104 and NH-027 and they are not the dev lap's to decide.

### F2 · WFG-104 · judge-facing · the bank has no card for the question that sentence invites

`docs/auto/JUDGE_QA.md` holds 41 questions and none is 「불을 전혀 모르는 경로와 비교하신
겁니다. 지금 불이 있는 자리만 피하는 경로와 비교하면 어떻게 됩니까?」 I searched the bank for
naive / perimeter / buffer / 기준선 / 베이스라인 / 대조군; there is no card. The repository
knows the question is coming — `WFG-033` names the arm that answers it, 「(b) static current
perimeter (slice 0, p ≥ p_cut) + fixed buffer 0.5/1/2 km」, with a `done when` of 「says plainly
whether the learned field beats 'current perimeter + buffer' on routing decisions」 — and
**WFG-033 is P2, which under CHARTER §11 means after the finals**. So the measurement that
separates 「예측」 from 「관측」 in the headline is scheduled after the event the headline is
presented at. That may be the right call for a twelve-day sprint; it is not a defect in the
science. What is a defect is that no T0 card says it out loud, the way 4막 already says
「저희가 진 결과도 화면에 있습니다」. The scope question goes to you as **NH-027**.

### F3 · WFG-105 · judge-facing · 5.61 syllables per second is a floor, not a rate

WFG-100 is good work and it closed the defect critic #16 filed. I re-derived every cell of
`docs/demo_script_pace.md` rather than reading it: 161+246+280+338+331+328 = 1,684;
29+44+50+60+59+58 = 300; 338/60 = 5.63; 328/58 = 5.66; and the old budget's implied rates are
4.507 (3막) to 7.289 (마무리), a 1.617x spread with the limitations close fastest and last,
exactly as claimed. Two things the page does not say:

1. **5.61 syl/s is charged against all 300 s as if all 300 s were speech.** §2 of the same
   script guarantees five judge interruptions *inside* those 300 s, and the pace page itself
   says 「a real delivery is longer than its text」. So 5.61 is what the student must beat
   before a breath, a pause or an interruption. It is a floor and it is presented as the rate.
2. **The external comparison is one search away and CHARTER §3.5b permits the search.** I ran
   it. The phonetics literature separates *articulation rate* (physical pauses **excluded**)
   from *speaking rate* (pauses **included**): Yun, 「Effects of gender, age, and individual
   speakers on articulation rate in Seoul Korean spontaneous speech」, *Phonetics and Speech
   Sciences* 10(4), <https://www.eksss.org/archive/view_article?pid=pss-10-4-19>, which reports
   means around **5.2 to 6.4 syl/s** for short units and computes them 「as the duration between
   pauses」; the same distinction is stated in 「Speech rate in Korean across region, gender and
   generation」, <https://koreascience.or.kr/article/JAKO201713647763102.page> (412 speakers).
   **5.61 is a speaking rate sitting inside the published band for a rate that excludes exactly
   what a speaking rate must contain.** That is not proof the script overruns — no lap has held
   a stopwatch and R12 / NH-014 is still the answer — but it is the first external number
   anyone has put beside 5.61 and it points one way. NH-014 is amended with it.

**(b), from `factchk`, same row.** `docs/auto/RUBRIC.md:10-11` quotes the 심사개요 as 「각 5분
**내외**」 and 「5분을 넘지 않는 것을 **권장**」. `docs/auto/DEMO_SCRIPT_5MIN.md:272` restates
that as 「300초는 심사 운영요강이 **정한** 총량」 and `docs/demo_script_pace.md:120` as
「300 s is **fixed by** the 운영요강」 — while line 12 of that same page gets it right
(「recommends and may enforce」). One page, two readings of one quoted rule.

### F4 · loop hygiene, P1, parked by CHARTER §14b · this routine's own count was unreproducible

The best paragraph the loop wrote this window is about me. `docs/demo_script_pace.md:92-108`
records that critic #16's per-segment counts (161/235/279/318/319/318 = 1,630) are reproduced
by **no** convention the dev lap implemented; the nearest variant agrees on the total only
because its per-segment differences cancel; and an earlier draft calling that agreement 「the
best evidence available」 was struck by the lap's own reviewer as an inference the segments
contradict. Critic #16 published a table from a rule it never wrote down. The dev lap was
right on every count, including its conclusion that the defect survives the disagreement —
every convention finds a 1.62x to 1.73x spread and every one puts 마무리 fastest and last.
**The rule for this routine from now on: a critic table of counted things ships its counting
rule or it ships no table.** Recorded in WFG-105's notes rather than as its own row, because
§14b holds loop hygiene behind R1, R3, R4, R7, R8 and R9.

### F5 · not filed as a row · four P0 rows sit below forty P1 rows

`WFG-051` (table line 70), `WFG-076` (90), `WFG-082` (94), `WFG-078` (95) are all `P0`, all
`todo`, all `infra`. CHARTER §3b forbids a P0 below a non-P0; CHARTER §14b holds loop-hygiene
rows behind the readiness lines. They are P0 by their filing and P1 by the rule, and the table
has been recording the contradiction rather than either answer. One reorder per lap means I
cannot fix it here and it is not judge-facing, so it is appended to NH-027 as a second, smaller
question rather than opened as its own entry. The clean fix is to re-label all four P1.

## Verification of the loop's claims

- **`gates.py --mode full` ALL GREEN at `26e200d`** in this fresh cloud sandbox: `1484 passed,
  62 skipped` in 303.7 s, **COLD** (the six SRTM-gated tests skip, WFG-039), against critic
  #16's cold `1464 / 62` at `43710f7`: **+20 passed, skips unchanged**, like for like, tenth
  comparable window. `verify`, `snapshot-verify`, `env-check` PASS; `baseline-verify` WARN,
  expected off-laptop, `hard: false`, seventeenth window and still not a finding.
- **GitHub's own runs (CHARTER §4b).** `auto-gates` on `auto/dev` in the window: run **117
  (`26e200d`, this head) `success`**; 116 and 115 `cancelled` by the next push; 114 and 113
  `success`. **No red run stands behind a green report this window, so there is no finding #1
  of that kind.** The last `failure` is run 110 (`d2418c2`, 03:20Z), the episode already filed
  as NH-026 and WFG-102, outside this window.
- **Every dev report of the last 24 h records `Reviewed by:`** — 1555Z, 1609Z (`self`, and it
  says why in the same line), 1851Z, 2154Z, 0059Z, 0404Z, 0702Z. Seven of seven, clean for a
  second consecutive window. `--assert-head` and `--assert-reported` both exit 0 at this head.
- **`make finals-bundle` re-run by me rather than read:** exit 0, `OK —
  release/kcf-finals-2026/ rebuilt byte-identically, 16 files`.
- **Judge-drill:** I resolved every file path cited in `docs/auto/JUDGE_QA.md`; all resolve,
  three of them as directory-relative shorthands (`check_forbidden.py`,
  `check_number_collisions.py`, `delivery/sms.py`) as earlier laps recorded. **No question in
  the bank was unanswerable from a file this lap**, so no 「근거 없음」 entry was added. What the
  drill found is a question the bank does not contain, which is F2.

## Root objection (`hate`), on the headline narrative

**「예측이 경로를 바꾼다」 is demonstrated against a walker who cannot see the fire, and the only
experiment that would test it against a walker who can is scheduled for after the competition.**
The cheapest test is not the arm study. It is one card in the Q&A bank that names the baseline
plainly and names WFG-033 as the unrun arm. A judge who hears 「불을 전혀 보지 않는 경로와
비교했고, 지금 불만 피하는 경로와의 비교는 아직 돌리지 않았습니다」 scores 과학적 사고 up; a
judge who has to extract it scores it down. That is F1 and F2 and it is the whole of what I
would change before the next row.

## Direction check (CHARTER §14, five minutes)

**One row moved and it is the smallest move this page has recorded: WFG-037 now sits ABOVE
WFG-036 in the backlog table.** `DIRECTION.md` has said since critic #16 that the booth recipe
and the printables 「come before WFG-036 v2」, and the table said the opposite. Both rows are P0,
so no CHARTER §3b rule was broken either way; what was broken is that a dev lap reading table
order and a dev lap reading the page would have claimed different rows. They agree now. The
direction itself is unchanged and I checked it against `paper/GAPS.md` and `docs/auto/knowledge/`
rather than assuming it.

**Critic #16's falsifiable test does not resolve.** It said: 「if the next lap produces
`docs/auto/finals/BOOTH_SETUP.md`, this page is steering; if it produces another gate or another
registry, WFG-084's cap should stop being a proposal and become the rule.」 The next lap produced
neither. It produced WFG-100 — a judge-facing measurement of a judge-facing document, which was
that lap's own `fix-before-next-row` item and therefore the one thing the page had itself put
ahead of BOOTH_SETUP.md. I am not scoring that as a fail. The test re-runs against critic #18
with the confound removed, because WFG-103 is minutes and WFG-037 is the row directly under it.

**Readiness lines ticked in the last 24 h: one, R4, by critic #15 at `43710f7` (0200Z).** The
count is **4 of 11** and no line moved this window, so the 「zero for two consecutive critic
laps」 direction finding does **not** fire. R1 could not move — nothing has touched `web/` since
`deeb147` and the screen's content is unchanged since `dc63a06`. R3's booth half, R7 and R9 are
held by the same two absent artifacts as yesterday, both checked on disk at this head.

**Census, measured over the window:** 1,542 authored insertions, of which **28 (1.8 %)** reached
a judge-facing surface, against 21.8 % and 27.3 % in the two windows before. Reported without an
adjective, because it does not read the way the earlier numbers did: the 28 lines are the
correction to `DEMO_SCRIPT_5MIN.md` and the other 1,504 are the measurement apparatus that
justified it, which is reusable and turns red on the next edit of the script. Whether that is
the right price is exactly WFG-084's open question and this is a third data point for it.

## Scorecard deltas

**Track B 83 → 84** (제출 자료 18 → 19). **Track A 77 → 78** (제출 자료 14 → 15). Both moves are
the same one: WFG-100 closed the defect that explicitly capped both rows one lap ago, and closed
it better than the row asked for — the before state committed as an artifact rather than
asserted, and the disagreement with critic #16's own count published rather than smoothed.
Neither row takes the next step, for the same reason on both tracks: F1, the document those
points are scored on mis-describes the baseline of its own core act. 연구 목적, 설계와 방법론,
데이터 수집·분석·해석, 창의성 and 구현 및 유용성 all held — no model, split, metric, arm,
coupling or protocol moved, and nothing touched `web/`, the printables or the bundle.

## In plain terms

**What the judges would see today.** A booth kit that is nearly assembled and one sentence that
would cost you points. In your five-minute script, the most important 60 seconds says the
comparison is against 「a map that only sees this moment」. It is not — it is against a route
that does not look at the fire at all, which your own code says in plain English. A judge who
notices will think the comparison was made to look better than it is, and that is the opposite
of what this project is for.

**What changed since yesterday.** The five minutes is now counted instead of guessed: 1,684
syllables spread over 300 seconds at one speaking speed, so no segment is secretly impossible.
That earned a point on both scoring tracks (B 83 to 84, A 77 to 78). Everything else held. Still
missing for the booth: the printables and the setup sheet.

**What you should do.** Two things, in this order. **(1)** Answer NH-027 — I have put four
options on it. The short version: your headline compares your smart route against a route that
ignores the fire completely, and the fairer comparison (against a route that avoids the fire
where it is right now) is currently scheduled for after the competition. You can leave it there
honestly, but the booth must say so out loud. **(2)** When you next open the laptop, read §1 of
the demo script aloud with a stopwatch and tell me the six times you actually hit. The script
asks for 5.61 syllables a second with no pauses, and the published figures for Korean speech
put that at the fast end of a measure that does not even count pauses. Nothing here can settle
it; you can, in five minutes.



## Gates

**ALL GREEN** · mode `full` · head `26e200d` · 2026-09-05T08:04:09Z · current at `26e200d`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 21.6 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.2 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 2.4 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 303.7 s | 1484 passed, 62 skipped, 52 warnings in 301.83s (0:05:01) |

## Commits since the previous report (c2693b72cea1a58a2b01b1890c642e17cdabe527..HEAD)

- 26e200d the report told the author three decisions were waiting; the email block beside it listed four
- 5dbdfb8 the report said RED at a head two commits behind the one it shipped with
- 1fd774c the report tripped the collision gate on its own two new keys, which is the gate working
- bbd3442 the reviewer blocked WFG-100 for measuring only the state it had just created, and the before state is now an artifact too

## Needs a human (7 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-025 [DECISION] `Main` cannot follow the green commit until you change one GitHub setting, and until you do, every green push emails you a red run (by 2026-09-08)
- NH-026 [DECISION] One of the five routines pushes on a gate that does not run the test suite, and its prompt is the one the repository cannot show you (by 2026-09-08)
- NH-027 [DECISION] The one experiment that would test the project's headline against a fair opponent is scheduled for after the competition (by 2026-09-08)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
