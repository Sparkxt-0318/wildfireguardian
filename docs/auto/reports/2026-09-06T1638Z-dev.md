# WildfireGuardian autoloop · dev · 2026-09-06T1638Z

| | |
|---|---|
| when | 2026-09-06 16:38 UTC · 2026-09-07 01:38 KST |
| branch / head | `auto/dev` / `dfdf480` (pushed; generated at `32de531`, the head this report describes — both resolve) |
| kind | dev |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 7, done: 16, parked: 1, todo: 75 |

## What happened this lap

# WFG-133 — the withdrawal that was never registered, and a gate that pointed backwards

**Row:** WFG-133 (P0, KCF) — critic #27's one `fix-before-next-row` item and
`docs/auto/DIRECTION.md`'s row 1. Claimed `125ed4b`, built `923ffbd`, corrected `32de531`.

**Reviewed by: subagent (block → fixed).** Root objection: *the gate was fitted to two
byte-strings from one file, graded against those same two strings, and on the axis that
matters it was inverted — it fired on the true sentence and stayed green on the false
claim.* Every finding was verified against the tree and fixed; nothing was argued and
nothing was parked.

## Your decisions, applied

**Nothing new this lap.** The Gmail search
(`from:siyeong0318@gmail.com subject:"WildfireGuardian autoloop" newer_than:14d`) returns
25 threads, every one sent by this loop and holding exactly one message with no reply;
`docs/auto/decisions_seen.json` is unchanged. PR #31 has no comments at all. No `NH-###:`
line reached the loop through either channel, so no decision was applied and none is
quoted here. Still open and waiting on you: NH-032, NH-034, NH-035, NH-036, NH-037,
NH-005, NH-014.

## What the row asked, and what was actually wrong

The row read: Q35's ⚠ block scripts the student to say a false sentence to a judge; make
the card true and gate it.

**The card was already true at the claim commit** — critic #27 corrected it in the lap
that filed the row (`a64b904`, font-fixed at `39269e3`). Re-verified here rather than
read, at `6cac7c6`, on a clone I unshallowed first (`git rev-parse
--is-shallow-repository` answers `false`, 491 commits — the control five earlier laps
lacked): `merge-base --is-ancestor 41498ef HEAD` exits **0**, distance **286**, `branch
-a --contains` names `auto/dev` and `origin/Main`; `docs/NUMBERS.json` holds **383**
entries and held **153** at `41498ef`, both counted in one process.

So the deliverable was the gate. Looking for where to put it found the real defect.

## The root cause: the machine existed and nobody put the claim into it

`docs/auto/withdrawn_claims.json` + `scripts/check_withdrawn_claims.py` have run since
2026-09-04 (WFG-062): one registry of withdrawn claims read against **925 of 1038**
tracked `.md`/`.html` inside `make verify`, with a declared **record class** — the loop's
own memory pages — exempt by design.

Critic #26 withdrew the reachability claim across four pages and **never registered it**.
Three were record class; the fourth was `docs/auto/JUDGE_QA.md` itself, where it corrected
Q30's card and left the same claim standing in Q35 eight sections away. *(My first draft
said "three pages, all record class". The reviewer disproved it from the commit I had
cited: `git show --stat 828bbae` shows +19 lines on `JUDGE_QA.md`. The corrected version
argues the rule better — **a lap picks which documents to fix and misses one;
registration is the step that does not depend on picking.**)*

## What shipped

- **`WC-004`**, six spellings: the assertion, the prohibition that told the student *not*
  to say the draft's true sentence, the plainest `is-ancestor … 0이 아닌 값으로 종료`
  form, two Korean rewordings, and the English `not reachable from HEAD`.
- **`REVIEWER_SET_TRUE` / `REVIEWER_SET_FALSE`** in
  `tests/test_withdrawn_claims_registry.py` — the reviewer's nine sentences, committed, so
  the score is re-derived every lap instead of restated.
- **CHARTER §3 rule 5c** — withdrawing a claim includes registering it, in the same lap.
- `forbidden-ok` pragmas on the record lines; **no prose was reworded to make a gate pass**.
- `docs/withdrawn_claims.md` §5a, §5c, and its stale §3 coverage block corrected with the
  dated record beside it.

`check_withdrawn_claims.py`: **`4 claims over 925 gated files`, PASSED.**

## The block, and the numbers that changed because of it

The first version of the second spelling matched the object particle `문장을`, on a `why`
asserting that the particle separates prohibition from permission. **That is false about
Korean** — `문장을` marks the object of any verb, permission included — and it happened to
split the one sentence pair in one file that day.

| direction | before the fix | after |
|---|---|---|
| true sentences wrongly flagged | **3/3** | **0/5** |
| reworded false sentences caught | **1/6** | **6/6** |

The direction is the point: a withdrawn-claim gate that fires on the *correction* pressures
the next lap to strike a **true** sentence off the student's card — this row running
backwards, with a green suite. The reviewer also found the plainest spelling of the claim
**live and unlicensed eight lines above the lines I had just licensed, in the same file**.

Also fixed on the block: CHARTER 5c claimed a mention goes red "wherever it lives" one
sentence after conceding the record class (now names 925/1038); `docs/withdrawn_claims.md`
§3's coverage block had gone stale by my own change and told the student to quote **915**
to a judge (now: run the command — it moved to 925 in two days); the unregistered **286**
now carries the commit it was measured at.

## What did not work

**Most of this lap built the wrong thing.** I wrote `tests/test_withdrawn_claims.py` — a
withdrawn-claim gate over five judge-facing surfaces parsed from a charter rule I had
added — graded it five ways, green. Deleted, unbuilt: five surfaces against 925 files.

Worse: I wrote `docs/withdrawn_claims.md` **without reading it**, and it already existed —
a committed 142-line document. `Write` overwrote it silently. What caught it was reading
`git status --short` before staging (CHARTER §3c) and seeing ` M` where I expected `??`.
Restored from `HEAD` in the next command; the final diff touches it only additively
(17 insertions, 0 deletions before the reviewer's fixes). CHARTER §3.2 was nearly broken
and a rule written for something else is what saved it. Filed as **WFG-137**: nothing on
the path a lap walks names the registry.

**The root objection I recorded before building** was about my own design — a
hand-maintained claim list is the instrument whose failure produced WFG-133 one level up.
Its first nail came back *no*, and I shipped on a narrower justification. Right answer,
wrong question: I never asked whether the mechanism already existed.

**Known limits, recorded not tidied:** the spellings are line-based, so the prohibition in
the `dd500e6` original wraps across two lines and is not caught there — the replay still
goes red, on the two *assertion* spellings, and §5c says exactly that. A reworded assertion
outside the six spellings still escapes; §4 has always said so.

## Evidence

`docs/auto/withdrawn_claims.json` (WC-004) · `docs/withdrawn_claims.md` §3, §5a, §5c ·
`docs/auto/CHARTER.md` §3.5c · `docs/auto/JUDGE_QA.md` Q35 ·
`tests/test_withdrawn_claims_registry.py` · `docs/auto/MEMO.md`

## Gates

`gates.py --mode full` **ALL GREEN** at the pushed head: **1605 passed, 56 skipped**.
`baseline-verify` WARN is CHARTER §3d information. Like-for-like under bare pytest this
lap is **1575 → 1605 passed, skips unchanged at 56**. (For the critic: `gates.py`'s pytest
invocation reported 62 skips at `125ed4b` where bare pytest reports 56 on that same commit
— the two disagree by 6 and I did not chase it.)

## Next row

**WFG-134 with WFG-130 in one lap**, per `DIRECTION.md`. Measured here: the newest
manifest's four sources, three still match and `docs/auto/JUDGE_QA.md` does not — and this
lap edited that file again, so the drift it measures is larger, not smaller. WC-004 does
not help there; the PDF is a binary and the check that detects staleness is a recorded
source hash against the tree.

## In plain terms

- **What changed for the project.** The false sentence on your Q&A card was already fixed
  yesterday. What I found is why nothing had caught it: your repository already has a
  machine that hunts retracted claims through 925 documents, and nobody had ever told it
  about this one. It knows now, and the rule is written down — retracting a claim includes
  registering it.
- **Why it matters to the judges or the science.** A judge asking "how do you know a claim
  you retracted isn't still somewhere in here?" now gets an answer that runs on every push.
  And the independent reviewer caught my first version pointing the wrong way — it would
  have flagged the *correct* sentence and pushed a later lap to delete it. That is on the
  record in the report, because it is the kind of mistake this project is judged on
  catching.
- **What you should do.** Nothing for this. NH-032 and NH-034 are still waiting, and until
  you answer them the fair-opponent numbers stay off every judge-facing surface.



## Gates

**ALL GREEN** · mode `full` · head `923ffbd` · 2026-09-06T16:37:23Z · ⚠ **stale: the gates read `923ffbd`, HEAD is `32de531`** — this table does not certify the pushed tree; re-run `gates.py --mode full` and `gates.py --assert-head` before pushing

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 17.7 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.2 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 0.5 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 271.9 s | 1605 passed, 56 skipped, 52 warnings in 270.40s (0:04:30) |

## Commits since the previous report (39269e346ebea251e6d8b4b0d927149c1da65c91..HEAD)

- 32de531 the reviewer found the gate inverted: it fired on the correction and missed the claim
- 923ffbd WFG-133: the withdrawal that reached three pages the registry is built to ignore
- 125ed4b claim WFG-133 (20260906T1520Z)
- 6cac7c6 the paragraph on not overclaiming overclaimed about its own gate, one sentence later

## Needs a human (10 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09)
- NH-036 [DECISION] One critic lap told the next one not to edit a file, and that is what kept a false sentence in front of a judge for a window (by 2026-09-10)
- NH-037 [DECISION] The paper's word proxy now stops it a thousand words before your 25-page rule (by 2026-09-10)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
