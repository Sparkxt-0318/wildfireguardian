# WildfireGuardian autoloop · paper · 2026-09-10T2130Z

| | |
|---|---|
| when | 2026-09-10 21:39 UTC · 2026-09-11 06:39 KST |
| branch / head | `auto/dev` / `a6d49d3` |
| kind | manual |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 10, done: 33, dropped: 1, parked: 1, title-derivable: 1, todo: 118 |

⚠ **This report ships in `938dff7` and the head it was generated at, `a6d49d3`, is the head it
REVIEWED.** Both are kept rather than one overwritten. `a6d49d3` is the commit the manuscript
incorporated and the commit the gate table below was written at; `d18ab7d` was this lap's own
commit on that base; `99dee9b` (a WFG-233 claim from another lap) then arrived at `origin`, the
push rebased onto it, and the lap's commit became **`938dff7`**. ⚠ **CHARTER §8's rule is that
the commit you push is the commit the gates read, so `gates.py --mode full` was re-run after the
rebase and exits 0 at `938dff7`** — verify PASS, snapshot-verify PASS, env-check PASS,
pytest-full 2,040 passed / 65 skipped / 3 xfailed — with `--assert-head` OK at that head on a
clean tree. The gate table at the foot of this report names `a6d49d3` because that is when it
was written; it is not the pushed head and is not the certification the push rests on. The only
thing between the two trees is `docs/auto/BACKLOG.md`, one line, another lap's claim marker.

## What happened this lap

# Paper lap 29 — the window brought two obligations and the word proxy refused both

**Incorporated** `c7c915b..a6d49d3`, 14 commits; 11 files outside `paper/` and `docs/auto/`.
One dev lap (WFG-228), one research lap, three critic laps (#59–#61).
**No sentence of `paper/manuscript.md` changed.** `body_words` 8,998, margin **2**, unmoved.
Nothing was compressed and no caveat or registered number was traded.

This is not a quiet lap. Two things arrived that the manuscript owes something to, and the
budget refused both — each measured with `paper/build_docx.py`'s own counter, not costed in
prose.

## 1. The project measured its own hazard field against a null, and the result includes a finding against itself

`docs/disc_null.md` (WFG-228) is the thing this repository has never had: something to compare
`IoU 0.394` with. An **area-matched disc** — centred on the centroid of the 249-cell `t = 0`
seed the observed and forecast stacks agree on exactly, holding exactly as many cells as that
slice's own predicted core, ties broken by `(row, col)`, **zero free parameters**, with the rule
*and the interpretation* fixed in the claim commit `4ab2e07` **before** the script ran — scored
against the same observed FIRMS footprint by the same grader as `scripts/measure_oracle_gap.py`.

Seed removed from both masks: forecast core **0.2577** against the disc's **0.1169**, ratio
**2.2044**, surviving in sign at all four non-seed slices. (As scored, seed included: 0.3941 /
0.1554 / 2.536.)

⚠⚠ **And by centre of mass the disc is closer to the truth than the model is.** The observed
footprint's centre of mass travels **1,124.8 m** from the ignition seed; the forecast core's
travels **3,646.1 m**. That leaves the forecast a **2,670.2 m** centre-of-mass error against
the disc's **1,133.0 m**. The model **overshoots**. What it does better than a circle is
reproduce the burn's **shape and extent** — not its **place**.

**This is the evidence §6's first limitation asks for.** That limitation is the paper's own
「the objection we would raise first」: the routing result is graded against the predicted field
and not against observed burn. The disc null does not close it — no route was run, gap G4 is
untouched — but it is the first external-truth reading of the field the routing rides on.

⛔ **It measures +198 words against a margin of 2.** The registry's caveat band is identical on all 84 new
keys and carries 「Six facts travel together or none may be quoted」 as its third sentence — the floor caveat, the centroid
pairing, `obs_stack` not being ground truth, three of four slices graded against **one**
observation, the seed inflation, and that no route was run — and CHARTER §12 forbids buying
space with a caveat, so none of the six is optional. As a replacement for §6's 「Every control
here perturbs the predicted field, so none admits external truth」, `body_words` goes
**8,998 → 9,196**. ⚠ **The floor was measured too, because the obvious objection is that a lap
inflates a cost to justify writing nothing:** the tightest form carrying all six facts and
quoting only the fair ratio, one telegraphic clause per fact, measures **+143** — that is the
number to hold the budget against, and it is still 141 over. A **number-free** form, which
escapes the band as a quotation rule but not the two caveats that are scope on the claim rather
than on the numbers, measures **+73**. All three reverted.

⚠ **The honest other half: the manuscript is not false without it — and the reviewer replaced
this lap's reason for saying so with a better one.** The draft argued that 「every control here」
refers to §3.5's **five** controls and that all five perturb the predicted field; **that ground
is loose**, two of the five — the column-addition null and the platform-drift floor — being
controls on AUC and not on any predicted field. The sentence survives on a ground the draft
never stated: **the paragraph's subject is the ROUTING result and the disc null ran no route**
(`docs/disc_null.md` §5.3, in its own voice), with the `[GAP:` immediately after it asking for a
routing pass on the observed footprint that is untouched. **G4 is genuinely still open.**
What the paper **is**, is under-reporting the strongest evidence against itself on the
limitation it names first, at a floor of **+143** where lap 24 recorded +41.

## 2. The second refusal is a citation, and that is new in this record

The research lap of 2026-09-10 found **Bokade, Barai, Bhogle, Chapherkar & Kale**, 「A Hybrid
Neural Physical Framework for Wildfire Propagation Modeling and Dynamic Evacuation Routing」,
Zenodo preprint, **2026-09-09**: a U-Net flammability prior into an explicit
cellular-automaton spread engine, then routing that 「incrementally routes traffic away from
actively predicted fires」. **This project's architecture, published independently 45 days
before the finals — deposit 2026-09-09 against the 2026-10-24 finals, 37 days against the
2026-10-16 freeze — and §2 has no line about it** (WFG-239; `IEEE_PLAN.md` G11, before the
freeze).

⚠ **Nothing is false and nothing is retracted.** The row as filed said the manuscript 「claims
the architecture as its contribution」; critic #61 corrected that in place after checking three
places, and this lap re-read all three — the Abstract's 「the transferable contribution is the
evaluation design ... rather than the model」, §1's 「We make three claims」, §7's 「is not a
contribution to spread modelling, and this paper does not present it as one」. What is missing
is a **situating line**.

⛔ Cheapest honest form **+26**; the fuller form carrying country, mode and delta **+46**. Both
against **2**, both reverted. ⚠ A bare `[@bokade2026]` marker costs **zero** body words
(`build_docx.py:187` strips `[@...]` before counting) and was **not** used: attaching it to an
existing sentence attributes that sentence to a paper that does not say it.

✅ **What did land, at zero words: the half that needed a network and an opened URL.**
`references.bib` gains `bokade2026`, **opened at its concept DOI this lap**, quoting five
verbatim passages of the record's own abstract with the deposit date, the five creators, the
resource type 「Preprint」 and the boundaries that must travel inside any citing sentence —
Uttarakhand; road and vehicle routing; unrefereed; and **no reported metric of any kind**, so
no accuracy comparison in either direction and nothing registered. The page's own DOI field
reads `...22668358`, the **version** DOI of the same record and not a duplicate deposit. The
entry is **not yet cited**, which is legal and inert: `check_paper.py:200` checks that every
`[@key]` has a verified entry and not the converse, `build_docx.py` renders only cited ones, so
`references` stays **29** and the built document is the same document. It is added because
CHARTER §12 requires whoever cites a work to have opened it, and a later lap with the words
should not have to re-open it.

## Figures

**`F10_disc_null` added, drawn, looked at, and unreferenced** — the **F9** precedent applied
rather than quoted. Panel (a): seed-removed IoU per slice, with the as-scored pair drawn as a
tick above each bar and each slice's own `time_gap_min` under its group (the registry requires
that pairing). Panel (b): the four centre-of-mass distances at the headline slice — the half
that keeps panel (a) honest, drawn at the same weight. It takes **no appearance number**, so
the mapping stays F1→1, F2→2, F4→3, F5→4, F8→5, F3→6, F6→7, F7→8. **No `G` row was added**:
`check_paper.py:206-209` counts `G` rows one-to-one against `[GAP:` markers, so a row with no
marker turns the gate red — and a marker would be **false**, because nothing is missing from
the artifacts. What is missing is room.

⚠ **It buys nothing off the counter, and that needs saying because the temptation is the one
lap 19 ruled on.** Captions cost no `body_words`, and carrying body argument in one is 「a
false measurement of the document, not a short document」 — no lap may use it. This figure
draws **numbers**; the number-scope caveats that would sit in its caption are the kind Table
1's lineage block, Table 4's clock block and F6's registry-key block already carry. **The
argument still has to be written in the body and still costs words this lap does not have.**

⚠ **The first render was wrong in four ways, all found by looking and none reachable by any
gate**: panel (b)'s y-tick labels ran out of their own panel across panel (a)'s bars and value
labels; panel (a)'s y-label was clipped to 「ntersection over union」; its four two-line x-tick
labels overlapped into each other; and the two panels' x-axis labels collided. Repaired by
moving (b)'s labels above the bars, shortening the y-label, 7.0 pt tick text, and one
figure-level legend below both panels that also declares the one-colour-one-meaning mapping
once (fire = forward-simulated core, blue = disc, grey = the observed footprint's own
movement). **The other nine figures redrew byte-identical**, so no look-at-it pass is owed on
any of them.

## What was checked and found not to be owed

- **The registry moved purely additively, checked not asserted.** `docs/NUMBERS.json`
  **453 → 537** keys, all 84 additions under `dn_yeongdeok_`; compared key by key against
  `git show c7c915b:docs/NUMBERS.json`, **zero** existing keys removed, **zero** existing
  values changed, **zero** existing caveat bodies changed. No number the manuscript prints
  went stale.
- **§3.5's hedge re-derived at the new count**, with lap 28's corrected predicate
  (`v is not None and str(v).strip() != ''`): `source_file`, `derivation` and `caveat` are
  **537 / 537** each, so the three universal conjuncts survive the 84 new entries;
  `forbidden_phrasings` is **373 / 537** (69.5 %) — present on 396 with **23 of those holding an
  empty list** — so 「most」 is still true and still the right word; `json_path` is 533/537 and the manuscript no longer claims it.
- **「skipped it four times」 stays four**, on a check of the range for an *unregistered*
  withdrawal rather than on the registry being byte-unchanged — see finding 3 below, where the
  draft's justification was backwards.
- **WC-013's household register does not reach §2, and its own entry says so.** `say_instead`
  ends by naming what stays untouched — 「Q16's 「가구 단위 폐쇄 시각」 and **Q16a's sentence
  about what other systems' published material does not show**」 — and §2's 「describes no
  output of this paper's kind: which household can still walk out, and by which path」 is the
  manuscript's twin of exactly that. Read against the entry, not against the resemblance. The
  title is settled separately: `DIRECTION.md:42` names `paper/manuscript.md:1` among four
  surfaces where 「household-level」 names the **application** and must not be 「fixed」.
- **§4.5's pointer into `docs/oracle_gap.md` survives that document's move.** It gained §4c
  (the disc null summarised); §4.5 cites it for the **mechanism**, which §2 of that document
  still establishes and §4c does not touch. `grep -c oracle_gap paper/manuscript.md` returns 1.

## Gates and the anchor

`gates.py --mode full` exits **0** at `a6d49d3` (verify PASS, snapshot-verify PASS, env-check
PASS, pytest-full 2,040 passed / 65 skipped / 3 xfailed, 318 s). `baseline-verify` WARNs on the
two git-ignored `data/raw/**` contracts, which is NH-029 and CHARTER §3d working as designed.
`check_paper.py` exits 0.

✅ **The anchor was re-derived, not inherited.** `body_words` did not move, so `check_paper.py`
would have passed with no renderer at all; the one `apt` line `paper/README.md` has printed
since lap 9 was run anyway, and the measuring branch reports **`pages 23, calibri_face
Carlito, metrics_ok true`**, printing **`6b0702d747ed5beb`** — the string `STATE.json` already
carried. A confirmation on an **unmoved** input, the weaker of the two kinds, stated as such.
The `.docx` is **not** rebuilt into the commit: the manuscript did not move, and lap 23's
byte-non-determinism finding makes a rebuilt file 2.9 MB of diff that says nothing.

⚠ Clone **shallow**: `--is-shallow-repository` **true** at depth **50**, both measured this
lap. **No ancestry or reachability claim is made anywhere in this lap's output** (CHARTER §4).

## Word count, page estimate, and NH-037

**Two pages against the author's 25; two words against the proxy's 9,000**, measured on one
document by one run. ⚠ A margin of 2 is **not** headroom, and 8,998 is **498 words over
CHARTER §12's target of 8,500** — the margin is against the hard fail, not the target.

⛔ **What NH-037 has become.** Every earlier casualty in this record was one sentence: an
illustration (laps 13–18, retired as a want at lap 19), a limitation (lap 21), the shape of a
mandatory correction (lap 28). **This lap is the first to have two obligations refused at
once, and the first where one of them is a citation.** Neither is compressible — the disc null
because its caveat band is mandatory and CHARTER §12 forbids trimming it, the Bokade line
because a citation shorter than its own boundaries misattributes. Laps 13 through 21, 24, 25,
28 and now 29 have all had their writing shaped by the proxy rather than by the evidence. The
remedy that removes the class is still not a bigger number: it is **WFG-116's one `apt` line in
`.github/workflows/auto-gates.yml`**, after which a clean clone measures the 25 pages the
author actually set — and 23 is what it measures today.

## The independent reviewer BLOCKED this lap, and was right

**Reviewed by: subagent (block): the lap's defence makes the ledger the deliverable, and the one
completeness integer the lap derived itself is wrong — in the direction that makes the
manuscript's hedge look better supported than it is.**

`forbidden_phrasings` is present on **396** of 537 entries but **23 of those hold an empty
list**, so only **373 (69.5 %)** name any misstating phrasing. The predicate was lap 28's own
repair, `v is not None and str(v).strip() != ''` — which fixes `str(None)` being the truthy
`'None'` and then walks straight into **`str([])` being the truthy `'[]'`**. **The same bug class
as lap 28's, one container deeper, in a lap whose subject is a completeness claim, in the file
whose whole argument is that a hand-typed count goes stale.** The draft also asserted 「empty on
none of those」, affirmatively false about 23 entries. ⚠ Confirmed a second way rather than only
re-run: lap 28's **289 / 453** plus all **84** new `dn_yeongdeok_` entries (84 of 84 carry a
non-empty list) is **373**, and the 23 empty lists are the same 23 in both counts. The manuscript
sentence **survives** — 69.5 % is still 「most」 — so the repair is three ledger edits at **zero
body words**, and lap 28's derivation stands where lap 28 wrote it rather than being overwritten
(CHARTER §3.7).

Five further findings, all verified and repaired before the push:

1. 「four weeks before the finals」 is **45 days** (deposit 2026-09-09 → 2026-10-24; 37 days to
   the 2026-10-16 freeze). It was `BACKLOG.md`'s wording restated as this lap's own without
   deriving it.
2. The caveat band **carries** its six-fact sentence third; it **opens** 「A FLOOR FOR ONE METRIC
   ON ONE FIRE…」. Position mischaracterised, content not.
3. **The 「stays four」 justification was backwards.** An unchanged withdrawal registry is exactly
   what a fifth *skipped* retraction would also produce, since skipping registration leaves the
   file untouched — the registry cannot be its own witness. What supports four is a check of the
   range for an *unregistered* withdrawal: `eff1bc0`, this routine's own lap-28 push, corrected
   two false manuscript universals and added no `WC` row. It is not counted, for a stated reason
   — both were corrections to the paper's account of its **apparatus** rather than withdrawals
   of a **result** claim, and neither left the same claim standing elsewhere (both phrasings
   grepped repository-wide; only record-class copies remain).
4. **The §6 ruling is right; its ground was loose** — corrected in the section above.
5. ⛔ **A correction the draft never costed, and it is the cheapest thing this lap refused.** §6
   offers a **symmetry** — 「consistent with detours around cells that never burned, **and** with
   burned cells the model never flagged left unavoided」 — and **the overshoot breaks it against
   the paper**, making the first branch the measured one. Presenting a broken symmetry as open is
   the nearest thing in this lap to CHARTER §3's 「rounding a limitation away」. Both halves must
   travel, so the amendment measures **+57** with the registered metres and **+37** at its
   tightest. Reverted. **A thirtieth lap must not record this a second time without landing it.**

Two figure defects, found **after** this lap's own look-at-it pass had passed the render: panel
(a)'s bars are the seed-removed IoUs and nothing said so (the axis now names it), and **one
colour carried two meanings** — fire marked both a *displacement from the ignition* (3,646.1 m)
and an *error against the observation* (2,670.2 m) on one axis, so panel (b) is now two labelled
groups with one quantity each. **Looking is not enough on its own**, which is exactly what
`paper/README.md`'s 2026-09-04 figure block already says.

⚠ And the reviewer's description of what 「at zero words」 bought, kept rather than softened:
**`F10` and `bokade2026` are free precisely because both are invisible to every gate in
`paper/`** — an unreferenced figure reaches neither `figure_fingerprint`, the page count, nor the
built document; an uncited entry reaches neither the citation check nor `fmt_ref`. **Two
artifacts landed and nothing reads either.** Defensible under CHARTER §12 — both were drawn and
opened as §12 requires, and the alternative was landing nothing — but that is what happened.

The reviewer independently confirmed: the 14-commit range and its 11 files; `453 → 537` purely
additive with **no field of any kind** changed on any existing key; `verify_numbers.py` 537/537
matching artifacts; the **+26** Bokade measurement reproduced exactly; the whole `bokade2026`
note against the live Zenodo API, including that `...22668358` is the version DOI of the same
record and all five verbatim abstract passages character-for-character; that the six-fact band is
**mandatory** and the cost was **not** inflated; the figure appearance mapping; `built_pages` 23
under Carlito with `metrics_ok true`; and that no ancestry or reachability claim appears anywhere
in this lap's output.

## In plain terms

- **The paper still says what it said this morning, and that is the problem, not the news.**
  Your loop just measured the forecast against a fair floor and found something honest and
  unflattering — the fire's shape it gets right, the fire's *direction* it gets worse than
  doing nothing — and an independent group published this project's architecture on 9
  September. Both belong in the paper. Neither fits.
- **What is missing is room, not evidence.** The cheapest honest versions cost 143 words (the
  disc null), 57 (the one-sentence version of it) and 26 (the citation); the manuscript has
  **2** words left before its gate fails. The built document is **23 pages** against your
  25-page rule, so it is the word proxy stopping this and not your actual limit. The lap's own
  reviewer blocked it once over a miscount in the write-up, which was fixed before pushing; no
  sentence of the paper changed.
- **What to do: answer NH-037.** Option **B or C** is what the loop would pick — one `apt`
  line in `.github/workflows/auto-gates.yml` so a clean machine measures pages and the word
  proxy stops being the binding rule. Reply `NH-037: B` (or `A`, `C`, `D`) to any report
  email. Until then the next lap ships nothing either.



## Gates

**ALL GREEN** · mode `full` · head `a6d49d3` · 2026-09-10T21:13:09Z · current at `a6d49d3`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 21.3 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.1 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 1.5 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 318.0 s | 2040 passed, 65 skipped, 3 xfailed, 52 warnings in 316.95s (0:05:16) |

## Commits since the previous report (be39dea76c5850d5ea4fe5ea0b4b21a06fc50967..HEAD)

- a6d49d3 the critic report names the head it ships in, and keeps the head it reviewed
- 252c9f9 critic #61: the sheet a judge is handed says this project works house by house, 73 lines below its own correction saying point by point

## Needs a human (29 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09, one day past; raised to HIGH by critic #55 on a measured third instance)
- NH-036 [DECISION] One critic lap told the next one not to edit a file, and that is what kept a false sentence in front of a judge for a window (by 2026-09-10)
- NH-037 [DECISION] The paper's word proxy now stops it a thousand words before your 25-page rule (by 2026-09-10)
- NH-038 [DECISION] Your "product first" rule has spent the last three dev laps on documents, and the readiness line it was written to protect has not moved in five critic laps (by 2026-09-09)
- NH-039 [DECISION] The national wildfire-spread system's manual is an 18 MB PDF the sandbox could not fetch, and one of you can (by 2026-09-12)
- NH-040 [FYI] A critic lap pushed one commit past a red `--assert-reported`, and it is telling you rather than hiding it
- NH-041 [FYI] This lap sent you an email containing only the word PLACEHOLDER, and could not take it back
- NH-042 [DECISION] Two of your own rules collide whenever a withdrawn claim lives in a frozen artifact, and this week they collided three times (by 2026-09-10)
- NH-043 [DECISION] A gate your loop built this morning will go red about twice a day, and the charter tells the lap that meets it to stop working (by 2026-09-09)
- NH-045 [BLOCKER] The staleness gate has closed `auto/dev` to every routine, and the one routine that met it is the one forbidden to clear it (by 2026-09-08)
- NH-044 [DECISION] The claim the paper just retracted is still live on the page the paper cites for it (by 2026-09-09)
- NH-046 [DECISION] Your product's definition-of-done names a command nothing in this project has ever run (by 2026-09-10)
- NH-048 [DECISION] One of the research routine's three literature channels has been dead for two runs, and a working replacement is already proven (by 2026-09-10)
- NH-049 [DECISION] Your critic routine is told to add judge Q&A cards and your own printing gate makes that impossible for it (by 2026-09-11)
- NH-050 [DECISION] You answered two of these questions two days ago and the loop never heard you, because you answered them on the routine page (by 2026-09-10)
- NH-051 [DECISION] The rule your loop has been obeying for three days is not the option it names, and the difference is why one row has been pushed down five times (by 2026-09-11)
- NH-052 [DECISION] The experiment that measures what your own forecast is worth is now one run away, and it would change what 42 means (by 2026-09-12)
- NH-053 [DECISION] The word your front door uses for 42 stopped being right today, and the loop cannot pick its replacement (by 2026-09-12)
- NH-054 [DECISION] Four fifths of your front door's headline is limits, and no lap is allowed to decide whether that is the project's strength or its biggest presentation risk (by 2026-09-13)
- NH-055 [DECISION] Your front door compares the headline forecast number to a model the same page calls broken, and today the loop measured a second, harsher comparison it is not allowed to put beside it (by 2026-09-13)
- NH-056 [DECISION] A Korean university published a reproducible deep-learning wildfire model four weeks before your finals, and a lap may not decide on its own whether to mention it (by 2026-09-13)
- NH-057 [DECISION] The one thing your project now says it contributes has never once been produced from a real fire, and only your laptop can change that (by 2026-09-13)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
