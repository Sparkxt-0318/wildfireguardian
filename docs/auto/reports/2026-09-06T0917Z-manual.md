# WildfireGuardian autoloop · paper · 2026-09-06T0940Z

| | |
|---|---|
| when | 2026-09-06 09:17 UTC · 2026-09-06 18:17 KST |
| branch / head | `auto/dev` / `9c2b903` |
| kind | manual |
| environment | python 3.11.15 · pins_ok=True · stack_ok=True · Linux-6.18.44-fc-v24-x86_64-with-glibc2.39 |
| backlog | blocked: 7, done: 16, parked: 1, todo: 74 |

## What happened this lap

# Paper lap 11 — the repository caught up with the manuscript on one claim, and the reviewer found a caveat the manuscript had been dropping since lap 10

**Incorporated:** `9c2b903` (was `6145001`).
**Reviewed by: subagent (block): §4.4 quoted two values of the present-perimeter arm without
the four bound facts its registry caveat requires, which is the constraint §4.5 cites as its
reason for quoting nothing — so either §4.5's silence was unwarranted or the manuscript was
already in breach.** Fixed before the push; the reviewer's other three findings fixed too.
**Length:** 8,825 body words against the 9,000 proxy; **23 built pages under Carlito**
against the author's 25-page ceiling (NH-028) — two pages of margin, measured this lap.

## What moved in the code, and what of it belongs in a paper

Six files changed outside `paper/` and `docs/auto/` since `6145001`: the booth printables
build (`scripts/build_printables.py`, `docs/printables.md`, `tests/test_printables.py`, a
`Makefile` target) and the narrowing of `docs/fair_opponent_line.md` with its gate.

**The printables are not paper material and nothing was written about them.** They are a
print rendering of four committed booth documents: no new number, no new source, no new
claim. The one transferable thing in that work — a gate that reads the source documents
cannot see the characters the renderer itself introduces — is a second instance of a class
§3.5 already carries, and it does not earn a paragraph in an IEEE submission at 23 of 25
pages. Recorded here rather than written into the manuscript.

**The fair-opponent narrowing did belong in the paper, because it made a manuscript sentence
false.** §4.5 ended: 「Two documents in the repository this paper cites currently draw the
stronger conclusion from those same five points…」. On 2026-09-06 (WFG-127 (i), critic #23's
finding carried by critic #24) `docs/fair_opponent_line.md` §3 was narrowed to exactly the
manuscript's reading — the failure changes *kind* across the widths, and a five-point grid
spaced by factors of two cannot separate a spike from a plateau — and its gate now bans the
retired spellings in that file, where it used to *require* one of them.
`docs/present_perimeter_arm.md` §4 still reads 「The 1 km row is a **spike, not a plateau**」
(checked on disk at `9c2b903:129`).

## Sections changed

- **§4.5.** The correction above, now written as **one document plus the registry's own
  caveats** — the reviewer's fourth finding, and it is the right reading: the paper hands the
  reader `docs/NUMBERS.json` as its evidence registry in the availability section, and fact
  (3) of every `pp_uiseong_*` caveat still asserts the sentence WFG-127 (i) retired. Saying
  「one document」 while this lap's own ledger entry documented the registry would have been
  accurate only under a reading that excludes the registry the paper points at.
- **§4.5.** One clause added: the two constructions of the opponent **do not agree on which
  buffer width comes off best either**, so the dispute §4.5 already reports is not confined
  to the residual it leaves. Stated without a width, a count or a margin.
- **§4.5, on the reviewer's third finding.** The wide-buffer failure composition (mostly late
  arrivals, a few origins walled off) is the *committed* arm's and was attributed to both
  constructions. It now says so: the breakdown by failure type is the committed arm's alone,
  and what both agree on is the coarser statement — thin buffers send walkers through burning
  ground, wide ones strand them — which is the strength `docs/fair_opponent_line.md` §4 uses.
- **§4.4, on the reviewer's root objection — the one real defect in the paper this lap.**
  See below.
- **Nothing else.** No other number moved, no caveat was dropped, no withdrawn claim was
  restored, no [GAP] opened or closed. Seven markers, seven rows.

## The reviewer's block, and why it was right

§4.4 printed the two Uiseong-Andong over-budget origins' fire-blind arrival times to one
decimal. Those two values are registered — `pp_uiseong_control_late_arrival_earliest_min` and
`_latest_min` — and their shared caveat opens 「Four facts travel together or none of them may
be quoted」, fact (1) being the margin NH-032 bars from every judge-facing surface. The
manuscript carried the substance of fact (5) and none of (1) to (4).

That is not a small inconsistency, because **it is the same constraint §4.5 cites as its
reason for quoting nothing at all.** Either the rule does not bar quoting from the arm, in
which case §4.5's silence and `GAPS.md` G8's bolded justification for it were unwarranted; or
it does, and the manuscript had been in breach one section earlier since lap 10 wrote §4.5.
The lap edited G8 and reaffirmed the first branch without checking the second. The reviewer
checked it in two commands.

**Fixed by striking the two minutes.** The qualification §4.4 makes survives without them —
both origins arrive past the 600-minute budget, so under one budget applied to both arms they
are origins no arm saves rather than origins on which the forecast lost — and §4.4 now says
the two times are values of the §4.5 arm, withheld for the reason that section gives. The
alternative was to state all four bound facts, which NH-032 forbids; that the alternative is
barred is itself the finding.

**Two things worth keeping about this.** No gate could have caught it: `make verify`, the
collision scan, the forbidden-string and withdrawn-claim scans and `check_paper.py` were all
green across it, because **no gate reads a caveat's prose** — the same failure mode `GAPS.md`
already records from lap 6, where a compression kept every number and dropped the caveat bound
to one. And the reviewer's own sweep is the reassurance on the rest: 291 numeric tokens
matched against all 383 registry values, and the four caveats that bind hardest (mean-of-folds
against pooled, the 32.6 % coverage on every absolute Yeongdeok rate, the synthetic-hazard
label on the 439-origin series, the dispatch-grid lineage) all travel with their numbers.

The reviewer also confirmed the thing this lap most needed confirming: **no margin is stated
or determined by §4.5 as it now stands**, the new width clause included. Table 2 yields the
forecast-aware total, and there is nothing in the paper to subtract from it.

## Figures

**None added, none redrawn.** All nine registered figures were regenerated and every PNG came
back **byte-identical** to the committed one — worth recording, because `paper/README.md`
warns that a different font set re-renders the same figure to different bytes.

§4.5 is still the only Results subsection with neither a figure nor a table, deliberately:
`F9_present_perimeter` is drawn and committed but **unreferenced**, because lap 10's reviewer
showed its bar totals plus the 「of 368 scanned」 axis plus Table 2 *determine* the barred
margin. This lap considered a composition-only variant (shares within the failing origins,
which cannot be inverted to a safe total) and did not build it: the four-facts rule makes
*any* count from the arm unavailable while NH-032 is open — and the root objection above is
exactly what that rule bites on when it is taken seriously. The figure drops into §4.5 the
moment NH-032 is answered.

## Gaps

No gap opened or closed. Three rows edited, two of them because they were wrong:

- **G8** said §4.5 reports the recovery count and printed it. **§4.5 reports no such count**
  — it says 「recovers most」 — and the same row states twice in bold that §4.5 quotes none, so
  the row contradicted both the manuscript and itself while putting fact (1)'s recovery half
  into the paper bundle without the other three facts. Lap 10 rewrote §4.5 and left its ledger
  describing the draft it had withdrawn. Both occurrences now describe the retired wording
  instead of restating it, which is the convention `docs/fair_opponent_line.md` adopted for
  the same reason. G8 also now records that one of the two surfaces behind the width finding
  has caught up, and leaves `docs/present_perimeter_arm.md` §4 as the open half of WFG-127 (i).
- **G5's location label** read 「§4.7 Results」. The marker is in §6 and the measurement it is
  about is §4.8; §4.7 was right before lap 10 inserted §4.5 and shifted the numbering. Fixed
  there and in the two other forward-looking instructions that had inherited it (the rest of
  the §4.7 mentions are dated records of earlier laps and are left as written).
- **A third repository inconsistency added** to the block the manuscript is on the safe side
  of. Counted in one process: **all 57 `pp_uiseong_*` registry entries** carry a caveat whose
  fact (3) is the sentence WFG-127 (i) retired from the prose, and no entry outside that
  family carries it. Nothing is red and no judge-facing surface quotes it any more, but a lap
  that quotes a `pp_uiseong_*` value and copies its caveat verbatim — which is what CHARTER
  §12 tells a lap to do — restores the retired claim. Repair: an annotated supersede (add,
  never edit) from a dev lap; `docs/NUMBERS.json` is outside CHARTER §12's paths. Third
  instance of this shape, after the ~95 % ratio and the `kfs_*` two-clocks caveat, and the
  recurrence is the finding.

## The page count, and a stale paragraph inside `paper/`

`paper/README.md` opened with 「the built document is **23 pages under Carlito** … Two pages of
margin」. **The document was 22 and had never been 23.** Lap 10 drafted §4.5 *with* F9 in it,
measured 23, then withdrew the figure — and this paragraph did not follow. `STATE.json`
carried 22 correctly throughout, so two files in `paper/` disagreed and nothing compared them:
`check_paper.py` gates `STATE.json` against the manuscript, and **no gate reads
`paper/README.md` at all.** Both are correct now, and the withdrawn 23 is kept rather than
deleted because it is the cleanest datum the loop has for what a figure costs.

Then this lap's own words took the document 22 → 23 for real, and the page had gone before the
lap was half done: measured here, **the mandatory §4.5 correction alone (8,746 words) already
renders 23**, so §4.5 was on a page boundary and any edit to it would have tipped the same
page. The words-to-pages table samples at 500-word steps and cannot see a boundary an
eleven-word sentence crosses; it is a sampling, not a rate, and the file now says so. **No
caveat was traded for the page**, including the reviewer's repairs, which cost 53 words and
were not negotiable. Two pages of margin remain.

`libreoffice-writer`, `fonts-crosextra-carlito` and `fonts-nanum` were installed in this
sandbox so the count is measured rather than inherited; that is machine setup, not a
repository dependency, and WFG-116's open half — the same `apt` line in `auto-gates.yml` so a
clean clone measures — is still outside `paper/` and still a dev-lap item.

## Gates

`gates.py --mode full` **ALL GREEN** on the tree that was pushed: `verify`, `snapshot-verify`,
`env-check`, `pytest-full` (1,568 passed, 56 skipped, 266.0 s). `baseline-verify` WARN is the
documented NH-029 behaviour for a grown registry. `check_paper.py` exit 0: 8,825 words,
23 pages, 8 figures, 4 tables, 27 references, 7 gaps, `STATE.json` matching.

## What did not work, and what is next

The lap's first draft was too pleased with itself: it corrected the manuscript against the
repository and did not check the manuscript against the rule it was invoking. The reviewer
found that in two commands, and the correction is the lap's most useful output.

Next, for the paper: **nothing until either NH-032 is answered** (then §4.5 states the margin
from the chosen build with its five registered caveats, adds the already-safe-broken term so
the two residuals reconcile, and references F9 — costing one page of the two that remain)
**or a dev lap runs the 44-origin present-perimeter check on the canonical Yeongdeok field**
(`GAPS.md` G7), which is minutes of work on committed inputs and would convert §4.3's 「an
unmeasured share」 into a number on the origin set the paper's headline 42 comes from.

## In plain terms

The paper had been printing two numbers it was not allowed to print — a rule it invokes one
section later as its reason for staying silent — and the independent reviewer caught it. Both
are gone, the claim they supported survives without them, and the paper is more consistent
than it was this morning.

What is still missing is the same thing as yesterday: your answer to NH-032, which is the only
thing standing between §4.5 and a real number plus its figure. Everything else in the paper is
waiting on nothing.

What to do: answer NH-032 (which build of the present-perimeter opponent counts) — and, if you
have a minute in Word, open `paper/WildfireGuardian_Park_2026.docx` and check it really is 23
pages; the sandbox measures with a metric-compatible stand-in for Calibri, not Calibri itself.



## Gates

**ALL GREEN** · mode `full` · head `9c2b903` · 2026-09-06T09:16:31Z · current at `9c2b903`

| step | result | time | last line |
|---|---|---|---|
| verify | PASS | 17.0 s | === make verify: PASSED === |
| baseline-verify | WARN | 0.1 s | make: *** [Makefile:159: baseline-verify] Error 1 |
| snapshot-verify | PASS | 0.4 s | VERIFY: all present snapshots intact (47 local-only/digest-only absent) |
| env-check | PASS | 0.1 s | OK — the environment matches requirements.txt. |
| pytest-full | PASS | 266.0 s | 1568 passed, 56 skipped, 52 warnings in 264.85s (0:04:24) |

## Commits since the previous report (b70e46412a9aa1922a3a5c239d0cc4b5efdbfe28..HEAD)

- 9c2b903 the critic report names the head it ships in as well as the head it reviewed
- ef61e9b critic #25: the booth kit shipped, and it was measured against its own sources rather than its own definition of done

## Needs a human (8 open)

- NH-003 [FYI] `Main` is behind the working line by design
- NH-004 [FYI] The sandbox has no API keys, so the loop works from committed snapshots
- NH-005 [DECISION] Building footprints for Yeongdeok (Session 21 blocker)
- NH-014 [DECISION] Run the booth recipe once on the real laptop (after 09-10, before 10-16)
- NH-032 [DECISION] Two laps built your fair-opponent row at the same time and got different answers: 9 and 27 (by 2026-09-08)
- NH-033 [FYI] This lap force-pushed its own parking branch, which CHARTER §3.8 forbids flatly
- NH-034 [DECISION] Your fair-opponent experiment ran, and it cuts the headline from 91 to between 5 and 27 (by 2026-09-08)
- NH-035 [DECISION] The three-hour rule you chose to un-stick a stranded row cannot fire on the three-hour dev grid (by 2026-09-09)

---
Generated by `scripts/auto/report.py`. Charter: `docs/auto/CHARTER.md`. Backlog: `docs/auto/BACKLOG.md`.
