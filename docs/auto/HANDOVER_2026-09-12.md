# Handover — the harness paused by the author, 2026-09-12

**Status: every cloud routine is disabled. Nothing runs again until the author
re-enables it.** Written on the author's laptop, in the author's Claude Code session,
from the tree as it stands at the end of that session. This is the page to read first
when the loop is resumed (CHARTER §15).

## 1. What happened today (2026-09-12, laptop session)

The author typed 「decisions」. Every open DECISION in `docs/auto/NEEDS_HUMAN.md` was put
to the author as a button question and recorded verbatim with
`scripts/auto/decisions.py` (channel 「Claude Code session (AskUserQuestion on the
laptop)」; `.auto/decisions_2026-09-12.json` holds the texts). Twenty-four entries closed;
seven remain open and none of them is a DECISION with a pending option (see §5).

Midway the author said 「I think we can stop the entire harness now」 and clarified
**「Pause everything now」** and **「Walk me through them now」**: all remaining decisions
were taken before the stop, and their consequences were applied in this session.

### 1.1 The harness

- Routines **dev, critic, research, ci-red, paper, handover** disabled through the
  RemoteTrigger API (ids in `docs/auto/ROUTINE_PROMPTS.md`). `docs/auto/LOOP_CONFIG.json`
  carries `"paused"`; CHARTER §15 records the pause and the rule amendments below.
- ⚠ The **「Daily checkup」** routine (`trig_01U6cANcmaXaJ1EQoYjWfP3U`) belongs to a
  different repository (BedrockENV) and was disabled by mistake in the same sweep; the
  re-enable call was refused by the session's permission classifier. **The author should
  re-enable it** at claude.ai/code/routines if that project still wants it.

### 1.2 Rule amendments (all in CHARTER §15)

NH-035 A (stale claim = two hours) · NH-042 A (registration wins) · NH-043 A (finals-
screen staleness exception) · NH-049 A (critic writes to `JUDGE_QA_PENDING.md`) ·
NH-058 A (paper routine skips in-progress rows' paper surfaces) · NH-050 A (routine page
is a fourth channel; a session supersedes it) · NH-036 D (§14c stays informal) ·
NH-038/051/060 (§14b moot: the harness is stopping) · NH-048 B (OpenAlex added).

### 1.3 Two laptop runs the cloud could never do

**NH-057 「I want you to run the whole thing.」** `scripts/run_rescue_routing_real_hazard.py`
ran the rescue pipeline on the **real** 영덕 spread surface (the canonical LOFO forward
simulation) over the 2026-07-24 OSM snapshots, and
`scripts/generate_dispatch_outputs.py --full` made the sheets:

| what | where |
|---|---|
| artifact | `data/processed/rescue_routing_real_hazard.json` (444 origins: 281 / 29 / 132 / 2) |
| sheets | `outputs/dispatch_real_hazard/20260912T153043Z/` (86 clusters, nothing sent) |
| method + what it does not show | `docs/rescue_routing_real_hazard.md`; keys `rrh_*` |

Every judge-facing surface that said 「실제 확산면으로 만든 출동 지시서는 아직 없습니다」
now names that directory in the same block, with the run's own bound (origins still
sampled, walk timing flat): README §5 ① and the Round-4 lead, the finals screen (template
and built), JUDGE_QA (the NIFoS/G-DAPS card and Q29a), DEMO_SCRIPT, the creativity card,
the printed related-work panel, WC-013's `say_instead`, DIRECTION. The gate
`tests/test_output_object_claim_bounds.py` was re-pointed from 「says it does not exist」
to 「names the path and the flat-timing bound」. WFG-242 is done.

**NH-052 / WFG-213 「run, and let's see the results from there.」** The between-
observations rule was pre-registered in `docs/regrade_against_observed.md` §2 **before**
`scripts/regrade_against_observed.py` ran; §4 of that page holds the result and
`data/processed/regrade_against_observed_yeongdeok.json` the per-origin rows. Docs only:
nothing registered, no judge-facing surface moved. Headline for the author to read:

| arm | safe on the forecast | safe on 「seen so far」 | safe on 「will be seen」 |
|---|---:|---:|---:|
| fire-blind | 414 | 422 | 333 |
| forecast-aware | 456 | 456 | 354 |
| present perimeter (0 buffer) | 440 | 456 | 335 |

Of the 42 forecast-aware-only origins, 34 stay forecast-aware-only under 「seen so far」
and 9 under 「will be seen」; the fire-blind route is never safe where the forecast-aware
one is not. Under 「will be seen」 102 forecast-aware routes cross a cell the next overpass
shows burning. Whether that is meaningful is the author's call (NH-052: 「If it is
meaningful, I will think about it」).

### 1.4 Document and code consequences applied

- NH-053 A: the 42-margin mechanism stated, 「상한/noiseless」 dropped, on README ×3,
  manuscript §4.5, JUDGE_QA Q36 (dated records keep the old wording).
- NH-034 B: new Q&A card **Q19a**; `web/finals.html` keeps 91.
- NH-032 C: the budgeted present-perimeter arm imported from the parked branch as
  `scripts/run_present_perimeter_arm_budgeted.py`, run (265 / 327 / 354, margin 27),
  registered (`ppb_*`, 79 keys), documented in `docs/present_perimeter_arm_budgeted.md`;
  `docs/fair_opponent_line.md`, Q19/Q19a, README §1, BACKLOG WFG-114/121 updated.
- NH-056 B: one related-work sentence + `@choi2026burnprob` in the manuscript.
- NH-037 A: `paper/check_paper.py` `LIMIT = 9461`; `paper/STATE.json` re-anchored
  (body_words 9155, references 30; pages nulled — no renderer on this laptop either).
- NH-046 A: R3 criterion reworded in `KCF_READINESS.md`.
- NH-060 C: the vehicle-side 사유 line was already honest (`fcc9391`); a test now binds
  it (`tests/test_vehicle_unreachable_sheet_sentence.py`). NH-044 B was already applied
  on 2026-09-07. Printables kit rebuilt at `WFG_printables_20260912T1607Z`.
- `docs/dispatch_sheet_staleness.md` §3 and `dss_*` re-derived: `dss_html_carrying_current`
  is now 2 (the real-hazard run's two 차량 도달 불가 sheets), and the gate now asserts
  「only runs stamped on or after 2026-09-12 carry the current sentence」.
- NH-014 「Yes, run it now」: the booth recipe's automatable parts ran on this laptop
  (R12 in `KCF_READINESS.md`). Not done by the session: the Wi-Fi-off double-click and
  the USB copies.

## 2. Gates

`python scripts/auto/gates.py --mode full` was run at the end of the session on this
laptop; the result is in `.auto/gates.json` and in the final commit message. (If a later
reader finds the tree red, the commit message says which step.)

## 3. Not done, deliberately or for lack of a hand

- **The Wi-Fi-off open of `web/finals.html` and the two USB copies** (BOOTH_SETUP §2–§3):
  the author's, before 10-16.
- **NH-005**: the author will download the 도로명주소 건물 layer; no date given yet.
- **NH-039**: the author chose 「You already know what it does」; the NIFoS manual was not
  fetched and nothing was added to the knowledge note.
- **NH-054 D**: revisit for the paper — no action now.
- **The budgeted arm at 750/1250/1500 m** was not run (post-hoc-maximum caveat stays).
- **Page count of the manuscript** is unverified at this head (no LibreOffice on the
  laptop); NH-028's 25-page rule needs one run of `paper/measure_pages.py`.
- Agent B's regenerated `outputs/dispatch/20260912T152713Z/` was **not** committed (it
  duplicates the committed 33 clusters with the new sentence; the real-hazard run is
  the sheet set that matters). It is on the laptop, uncommitted.

## 4. The author's list after the pause

1. Read `docs/regrade_against_observed.md` §4 and decide whether it goes anywhere.
2. Open one sheet under `outputs/dispatch_real_hazard/20260912T153043Z/` and read its
   banner; that is the document a judge will be handed if asked 「진짜 불로 만든 겁니까?」.
3. Re-enable the BedrockENV 「Daily checkup」 routine if wanted.
4. Booth: Wi-Fi off, double-click `web/finals.html`, USB ×2 (BOOTH_SETUP §2–§3).
5. Download the 건물 layer (NH-005) when convenient; the loop's ingest is scripted.

## 5. Open NEEDS_HUMAN entries

Five remain open and all are FYI entries the author may acknowledge or leave: NH-003
(`Main` is behind `auto/dev` by design), NH-004 (the sandbox has no API keys), NH-033
(a lap force-pushed its own parking branch), NH-040 (a critic pushed past a red
`--assert-reported`), NH-041 (a PLACEHOLDER email). NH-014 and NH-045 were closed in
this session; NH-005's download date is still to come from the author.

## 6. How to resume the harness

1. Remove `"paused"` from `docs/auto/LOOP_CONFIG.json` and add a dated line to CHARTER §15.
2. Re-enable the routines you want at claude.ai/code/routines (ids and prompts:
   `docs/auto/ROUTINE_PROMPTS.md`). The prompts there already carry the 2026-09-12
   amendments; the handover routine should be re-enabled last.
3. The first dev lap reads this page, then BACKLOG in table order as before.
