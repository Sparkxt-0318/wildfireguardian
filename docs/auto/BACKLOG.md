# Backlog — what the loop works on, in order

Conventions: `docs/auto/CHARTER.md` §5 and §11. **P0** ships inside the sprint (by
2026-09-15), **P1** before the freeze (2026-10-16), **P2** after the finals (2026-10-24;
ISEF), **P3** for the IEEE paper. Status: `todo | in-progress(<stamp>) | done(<commit>)
| blocked(<why>) | parked(<why>)`. The dev routine takes the first `todo` row in table
order that is agent-doable and unblocked; `blocked(human)` rows are the author's and are
mirrored in `docs/auto/NEEDS_HUMAN.md`. This table was re-keyed on 2026-09-03 from the
research brief (`docs/auto/research/RESEARCH_BRIEF_2026-09-03.md`) and its backlog
proposal (`BACKLOG_PROPOSAL_2026-09-03.md`), which explain every priority change; the
eight sweeps behind them are under `docs/auto/research/sweeps_2026-09-03/`. Rows the
proposal demoted keep their IDs so earlier reports still resolve.

## Sprint plan, 2026-09-04 to 09-15 (CHARTER §11)

Table order below is the order of work. Target days are for the critic to
measure slippage against, not deadlines a lap may skip past.

| by | rows | what the author sees at the end of that day |
|---|---|---|
| 09-04 | WFG-002, WFG-004 | Q&A bank v2 done; one value per quantity across judge-facing docs |
| 09-05 | WFG-020, WFG-021 | survivor-survey evidence registered; detection-floor panel + GK2A tests |
| 09-07 | WFG-017, WFG-003 | finals screen v2 with the evidence cards; 5-minute script and screen audit |
| 09-08 | WFG-024 | dates and branch re-keyed everywhere (R11); WFG-016 withdrawn 09-04 |
| 09-10 | WFG-036 | **final product bundle v1** (`release/kcf-finals-2026/`, R9) |
| 09-11 | WFG-007, WFG-030, WFG-031 | booth checklist + printables as PDF; report-number gate; CITATION.cff |
| 09-12 | WFG-026, WFG-027, WFG-010 | related-work + SFTD059T panel; schedule/roles timeline; README Round-4 + abstract |
| 09-13 | WFG-025, WFG-009 | the two clean single-variable sweeps; Playwright smoke of the screen |
| 09-14 | WFG-036 v2 | bundle rebuilt with everything above; every KCF_READINESS line ticked except R12 |
| 09-15 | buffer | slippage, critic findings, MEMO; the 09-16 handover writes the post-sprint list |

Left for after the sprint by design: P2 and P3 rows, anything needing the
laptop's raw bundle (WFG-005/006/032/034), and the author-only rows.

| ID | P | goal | title | status | agent-doable | effort | rubric rows |
|---|---|---|---|---|---|---|---|
| WFG-254 | **P0** | KCF | **The T0 card five judges hear says the null circle is placed at the ignition point, and the ignition point this project's own canonical array records is 19.20 km outside every circle the measurement drew.** ⚠⚠ **FILED AT POSITION 1 BY CRITIC #65, 2026-09-11T0810Z (CHARTER §14b), and its Q36 half is this lap's ONE `fix-before-next-row` item** — one clause on a hashed kit source plus the 17-second `make printables` critic #64 priced, which is minutes. **The measurement, taken in this lap's own process from `data/processed/routing_demo_canonical.npz`.** That array carries `ign_xy` = (1138940.54, 1826944.63) beside `grid_extent` = (1126514.93, 1789870.46, 1204514.93, 1880370.46, 500.0). `scripts/measure_disc_null.py` does not read `ign_xy`: `data/processed/disc_null_yeongdeok.json` → `null_rule.centre_from` says, correctly, 「centroid of the t=0 seed」, and that centroid is grid (97.775, 55.120). Mapping `ign_xy` onto the grid gives column 24.85 and row 74.15 or 106.85 depending on which edge row 0 sits at. **The data settles it:** only (74, 25) is burning at `t = 0` (`obs_stack[0] > 0` and in the 249-cell seed); (107, 25) is not observed at any slice. So the recorded ignition point is **38.40 cells = 19.20 km** from the disc's centre — and **the finding does not depend on the convention**, because the other candidate is still 31.60 cells = 15.80 km while the **largest** disc drawn at any of the five slices has radius 18.162 cells (9.08 km) and the headline slice's has 17.355 (8.68 km). **No disc at any slice contains the recorded ignition point, and the headline disc's centre is more than twice its own radius away from it.** ⚠ **And the shorthand is not loose speech, because the seed is not a point.** The 249 cells both stacks agree on at `t = 0` are **226 connected components** (8-connectivity, largest **3** cells), bounding box **23.5 km × 44.5 km**, mean distance from their own centroid **23.2 cells (11.6 km)**. There is no ignition point in that mask to be near. ⚠⚠ **The same card holds the right rule and the wrong one, and the wrong one is the half said out loud.** `docs/auto/JUDGE_QA.md:1513` (**Q36 · T0**, in the 58-page kit a judge carries away) states the method correctly in its analytic block — 「`t = 0` 씨앗의 무게중심에 놓고」 — and then its 부스에서 할 말 quote, which the student says from memory, reads 「같은 면적의 원을 **발화점에** 놓고」. `docs/submission_reconciliation.md:63` uses 발화점 elsewhere to mean the real ignition (「정본은 실제 발화점에서 다시 모사한」), which is why a judge reads the two as the same place. **The same false locative is on five more surfaces:** `docs/disc_null.md:221` (spoken draft), `docs/oracle_gap.md:203` (「centred on the ignition」, the page the README sends a judge to), `paper/manuscript.md:710` (「an equal-area disc **at the ignition**」), `paper/README.md:1052` (quoting it), and ⚠⚠ **`paper/make_figures.py:873`, which renders the group heading 「distance moved from the ignition」 over two bars whose values are `seed_to_observed_m` (1,124.8 m) and `seed_to_model_m` (3,646.1 m) — both measured from the seed centroid, 19.20 km from the ignition the label names.** That heading is baked into `paper/figures/F10_disc_null.png`, committed at `938dff7` in this window. ⚠ **A second clause on the same card is not false but is ambiguous on the one axis the null holds equal by construction.** Q36 says 「모델이 원판을 이긴 것은 **모양과 범위**」 and `docs/disc_null.md:152`, `docs/oracle_gap.md:211`, `paper/manuscript.md:710`, `paper/README.md:894`, `paper/GAPS.md:100` and `:341` say 「shape and extent」. Both masks hold **952** cells at the headline slice and `docs/disc_null.md` §2 says the area is 「handed over from the model」, so on the area reading the claim is impossible. On the reach reading it is true and measurable — the model's core spreads its 952 cells across a 44.5 km-wide box in 37 components while the disc packs the same 952 into a circle 17.4 km across — so the word needs disambiguating, not deleting. **Done when:** every one of the seven files says what the centre actually is (the centroid of the `t = 0` detection seed) rather than 발화점 / 「the ignition」; the 범위 / 「extent」 clause says reach rather than area wherever it stands; the kit is rebuilt at a new stamp and `release/kcf-finals-2026/MANIFEST.json` re-pointed; and a test in `tests/test_judge_qa_bank.py` goes red, naming Q36, if 발화점 returns to that card. Grade by mutation: put the word back and the test must fail. **Constraints:** CHARTER §3 rule 2 forbids regenerating `paper/figures/F10_disc_null.png` — the corrected figure is a **new filename**, and the old one stays with a dated note saying what its heading got wrong; do NOT change any measured value, none of them is wrong; do NOT weaken the centroid finding itself, which is `docs/disc_null.md` §4's honest correction and survives the relabelling unchanged; do NOT touch `docs/submission_reconciliation.md:63`, whose 발화점 is about the simulation's seeding and is correct | todo | true | minutes for the Q&A card plus a kit rebuild; about a lap for the other six surfaces and the replacement figure | B/A 제출 자료 · B 데이터 수집·분석·해석 · R5 · R7 · R9 |
| WFG-255 | **P0** | science | **The footprint this project grades itself against is 55 disconnected pieces spread over 45 km, and no file in the repository says so while two published sentences describe it as one fire with arms.** ⚠ FILED AT POSITION 2 BY CRITIC #65, 2026-09-11T0810Z. **Measured in this lap's own process** from `data/processed/routing_demo_canonical.npz`, 8-connectivity, on the canonical 181×156 / 500 m grid: the `t = 0` observation is **249 cells in 226 components**, largest **3** cells, bounding box 23.5 km × 44.5 km; the **333-minute** observation that produces IoU 0.394 is **937 cells in 55 components**, the largest **656** and the second **135**, bounding box 24.5 km × 45.0 km; the 360-minute forecast core is **952 cells in 37 components** over the same box. The cumulative stack is 55 or 56 components at every later slice too. **Why this is a finding and not a curiosity.** `docs/disc_null.md:153` tells a judge the model 「puts cells along the **arms the fire actually ran down**」 and `docs/oracle_gap.md` §4's centroid reading turns on how far 「the fire」 moved (`seed_to_observed` 2.250 cells). Both sentences read the mask as one advancing fire. What the mask is, measured, is a multi-component detection field whose centre of mass is dominated by where FIRMS happened to have pixels in the first frame — and because `obs_stack` is cumulative and only **86** cells are added between 333 and 2403 minutes, the centroid barely moves for a reason that is about the record rather than about the fire. A KCF judge who is a disaster-response official, or any fire scientist, asks 「이 마스크 안에 산불이 몇 개입니까」 in the first minute, and **no file in this repository answers it**: a search for component / 연결 성분 / disconnected across `docs/`, `paper/` and `README.md` returns nothing about this array. ⚠ **This row asserts the geometry, not a conclusion about it.** It does NOT claim the mask is several fires; the 2025 경북 event was a multi-fire complex and FIRMS gaps also fragment a single perimeter, and this repository cannot presently tell those apart. That is exactly why the number belongs on the page before the interpretation does. **Done when:** a script under `scripts/` measures the component structure of `obs_stack` and the forecast core at every slice from the committed array and writes a new artifact under `data/processed/`; the counts are registered; `docs/oracle_gap.md` and `docs/disc_null.md` each carry one paragraph saying what the graded object is, in the same place they say what IoU it produced; and the 「arms the fire actually ran down」 sentence either earns that reading from the artifact or is rewritten to the reading the artifact supports. A `docs/` page states what the measurement does NOT show. **Constraints:** committed inputs only, no refit, no re-acquisition, and no committed artifact is regenerated (CHARTER §3 rule 2); do NOT move IoU 0.394, the seed-removed ratio <!-- collision-ok: 2.2044 — dn_yeongdeok_bare_iou_ratio, the seed-removed model-over-disc RATIO at the headline slice. It is not an IoU value and this row names it only to say it is NOT disputed; the registered IoUs it collides with are different quantities. -->2.2044 pair or any `dn_yeongdeok_` key, none of which this row disputes; do NOT write 「여러 개의 산불」 or any count of fires on a judge-facing surface from this measurement alone | todo | true | one lap | B 데이터 수집·분석·해석 · B 설계와 방법론 · B/A 제출 자료 |
| WFG-249 | **P0** | KCF | **The privacy card the student says from memory tells a judge that a 가구 in this repository is not a building, and the closing sentence the same lap wrote tells the same judge that the 가구 it counts are OSM buildings. Both are printed in the kit a judge carries away.** ⚠⚠ **FILED AT POSITION 1 BY CRITIC #64, 2026-09-11T0520Z (CHARTER §14b), and it is NOT a preemption:** `docs/auto/JUDGE_QA.md` is a hashed kit source, so the repair costs `make printables` plus a re-pointed `release/kcf-finals-2026/MANIFEST.json` (**NH-049**). **`docs/auto/JUDGE_QA.md` Q20a, a T0 card on the first drill round, states without qualification:** 「이 저장소에서 「가구」는 사람도 주소도 건물도 아니라 **OSM 보행 도로망의 노드 하나**입니다」. **`docs/auto/DEMO_SCRIPT_5MIN.md`'s spoken 마무리, added by WFG-247 in this window, states:** 「여기서 세는 **가구**는 판정 단위인 **지점**이 아니라 **OSM 건물**입니다」 with the building count beside it. Q20a says 건물도 아니라; the closing says 건물입니다. ⚠ **The repair that introduced this was right and this row does not undo it.** WFG-247's sentence is true: the refuge arm really does count buildings and `docs/NUMBERS.json` `l0i_household_population` says so in its own `caveat`. What is now false is Q20a's **scope**, which was written before the refuge arm had a population sentence anywhere. ⚠⚠ **And the demo script cites the card it contradicts:** 금지 item 6 was rewritten in the same commit to read 「`docs/auto/JUDGE_QA.md` Q20a 가 「가구」를 보행망 노드 하나로 정의하고」, so the surface that breaks the definition points the student at the card that states it. **Why this is the lap's first finding.** Q20a is on the T0 list (`docs/auto/JUDGE_QA.md` §6 드릴 순서, 회차 1), which means the student answers the privacy question from memory, and the 마무리 is the last thing each of the five judges hears. A software-professor judge who hears both in one visit has caught the project defining its own noun two ways, on the criterion 「자료의 논리적 구성」 that both rubric tables word identically at 20 points. **Done when:** Q20a's definition is scoped to the layer it is about (the 구조 · 경로 계층, whose `home_node` output is what the rest of that answer describes) and names, in the SAME block, the one other population this project counts 가구 on, so that a student asked 「아까 건물이 아니라고 하셨는데 왜 마무리에서는 건물입니까」 has the answer in hand rather than a contradiction; the kit is rebuilt at a new stamp and the bundle manifest re-pointed. Grade by mutation: strip the scope clause and a test in `tests/test_judge_qa_bank.py` should go red naming Q20a. **Constraints:** do NOT weaken the privacy answer, which is the reason Q20a exists and is correct about the routing output; do NOT touch WFG-247's 마무리 sentence, which `l0i_household_population` supports; do NOT fix this by deleting either sentence (CHARTER §3.5). `WC-013` preserves Q20a's 가구 definition by name in `docs/auto/withdrawn_claims.json`, so read that entry's `say_instead` before editing the line | done(20260911T0620Z) — Q20a's definition now opens 「이 저장소의 **구조 · 경로 계층**에서」 and the card names the other population in the same block (OSM 건물 스냅숏 124동, `l0i_household_population`) with the one sentence that answers 「아까 건물이 아니라고 하셨는데」: 두 계층이 서로 다른 것을 셉니다. The privacy answer is untouched and nothing was deleted. 금지 item 6 no longer cites Q20a as defining 가구 unqualified. Graded by mutation: stripping the scope clause turns `tests/test_judge_qa_bank.py::test_the_privacy_card_scopes_its_household_definition_to_one_layer` RED naming Q20a. Kit rebuilt at `20260911T0706Z` (59 pages) and `release/kcf-finals-2026/MANIFEST.json` re-pointed. ⚠ The card GREW by one paragraph on a T0 memory card, which is the cost this lap's `hate` objection named and did not avoid. | true | minutes plus a kit rebuild | B/A 제출 자료 · R7 · R9 |
| WFG-250 | **P0** | KCF | **The closing sentence now names a population for its three numbers and it is not the population those numbers are a fraction of, so a judge doing the arithmetic in their head gets a result five times worse than the truth.** ⚠⚠ **FILED AT POSITION 1 BY CRITIC #64, 2026-09-11T0520Z (CHARTER §14b), same kit source and therefore the same rebuild as WFG-249: ONE lap, ONE `make printables`.** `data/processed/vulnerability/refuge_placement.json :: optimum_h240.baseline`, re-read in this lap's own process, holds `n_households` and `n_failing` side by side, and the second is the denominator of the result: the refuge search improves reachability for the households that **fail** the horizon, not for the whole building snapshot. **`web/finals.html` says this correctly** (`scripts/finals.template.html:1981`, 「분 지평에서 도달에 실패하는 」 + `R.failing_before` + 「가구 가운데, 대피 지점 한 곳을 더 두면 」), and it is in the card the judge is looking at. **The spoken 마무리 does not**, and the 화면/구두 table at `docs/auto/DEMO_SCRIPT_5MIN.md` §3 is inconsistent with itself about it: the row for the pair result carries 「도달 실패 24가구 전부」 while the row for the single-refuge result carries only the building population. ⚠ **The defect cuts AGAINST this project, which is why it is easy to miss.** Read with the building population as the denominator the result sounds like a small fraction of the village; read with the failing set it is most of the households that could not get out. Both readings are arithmetically available to a judge from the sentence as spoken, and the repository supports only one of them. ⚠ **This is not WFG-247 re-opened.** WFG-247 asked that the population be named where the number is said, and it is. This row is that the named population is the arm's denominator rather than the claim's, and the fix is one clause, not a rewrite. **Done when:** the spoken 마무리 names the failing set in the same breath as the counts, in the screen's own words, so the script and the screen say the same thing; the §3 table labels both result rows the same way; the kit is rebuilt and the bundle manifest re-pointed. Grade by mutation against the committed artifact rather than against the prose. **Constraints:** every 마무리 edit moves `scripts/measure_demo_script_pace.py`'s allocation and `tests/test_demo_script_pace.py` goes red until a NEW pace artifact is registered under a new tag (CHARTER §3.2 forbids editing the existing entries), which WFG-247 paid once in this window and `docs/demo_script_pace.md` records the procedure for; prefer a rewrite over an append, because the 마무리 is now the longest segment of the six | done(20260911T0620Z) — the 마무리 now opens the result with the denominator, in the screen's own words (「240분에 도달 실패인 **24가구** 중 한 곳이면 **20가구**」), and `l0i_failing_denominator_h240` (24, `optimum_h240.baseline.n_failing`) registers it so the denominator is a key rather than prose. §3 labels both result rows 「분모는 도달 실패 24가구」 and carries a row for the denominator and one for the population. Pace re-measured: 360 → 383 syllables in 마무리, allocation 35 / 41 / 47 / 58 / 55 / 64, total 1,799 at 6.00 syl/s, artifact `pace_20260911T0620Z.json`, TAG moved, the exchange written into §1. Graded by two mutations: the pre-WFG-250 block and a 124-first reordering both turn `test_the_spoken_closing_names_the_denominator_the_artifact_holds` RED, the second via a new ORDER clause added after the first grading let it through. | true | minutes plus a kit rebuild and a pace re-measure | B/A 제출 자료 · A 구현 및 유용성 |
| WFG-251 | **P0** | KCF | **The card that sends a judge to another team's DOI asserts a negative about the whole deposited record, and its own limits list, forty lines below, says the file inside that record was never opened.** ⚠⚠ **FILED AT POSITION 1 BY CRITIC #64, 2026-09-11T0520Z (CHARTER §14b), same kit source as WFG-249 and WFG-250: the three are ONE lap and ONE rebuild.** `docs/auto/JUDGE_QA.md` Q16d's **draft answer**, the text the student speaks, says 「기록에는 **성능 수치가 하나도 없습니다** — 확산 쪽도, 경로 쪽도 없습니다」. The same card's 없는 것 item 1 says 「초록과 기록만 읽었고 본문 PDF 는 열지 못했습니다」 and prescribes the scoped form: ⭕ 「**공개된 기록과 초록에는** 경로 성능 수치가 없습니다」. **The deposited record contains that PDF:** the Zenodo API, read independently in this lap, returns exactly one attached file, an IEEE conference template. So the spoken sentence asserts over a document nobody in this project has read, and it widens the prescribed claim from the routing arm to both arms. ⭕ **Everything else on Q16d was re-verified at the API in this lap and none of it moves:** concept DOI and its version record, the title verbatim, the 2026-09-09 date, Preprint, the five authors in the card's order, one attached file, and the card's hardest claim about the abstract, where a digit regex over the full abstract text returns the empty list. WFG-248's D\* Lite fallback clause, added in this window, is also verbatim correct against the abstract. **Why it is a row and not a note.** This is the `WC-013` shape that WFG-247 was filed for, on the card three paragraphs above the instruction 「이 DOI 를 그 자리에서 열어 보이십시오」: the bound exists and sits one screen away from the claim, and 없는 것 lists are read last or not at all. The cost of being caught here is higher than for any other card, because the judge is holding the other team's record while the student speaks. **Done when:** the draft answer's own sentence carries the scope the card's 없는 것 item 1 already prescribes, in that item's words, and does not claim the spread arm unless the abstract is the stated scope; 없는 것 item 1 stays exactly as it is; the kit is rebuilt and the bundle manifest re-pointed. Grade by mutation: restore the unscoped sentence and a test in `tests/test_judge_qa_bank.py` should go red naming Q16d. **Constraints:** do NOT open, cite or characterise the body PDF (it is not in this repository and CHARTER §3.5b requires agency, date and scope on anything fetched); do NOT weaken any other fact on the card; `WC-007` and `WC-009` are the registry entries this exact error class already cost once | done(20260911T0620Z) — Q16d's draft answer now reads 「**공개된 기록과 초록에는** 성능 수치가 하나도 없습니다」 with one clause saying the body PDF was not opened, which is 없는 것 item 1's own prescribed wording; item 1 is untouched and every other fact on the card is untouched. Graded by mutation: restoring the unscoped 「기록에는 성능 수치가 하나도 없습니다」 turns `test_the_related_preprint_card_scopes_its_negative_to_what_was_read` RED naming Q16d. | true | minutes plus a kit rebuild | B/A 제출 자료 · B 설계와 방법론 |
| WFG-252 | P1 | infra | **The scorecard series the routine prompt names by shape is one lap behind the two tables it is built from, and nothing notices.** `docs/auto/SCORECARD.md`'s combined table, the one whose columns are 「ISO date, then the five Track B rows in rubric order, then the Track A five」 exactly as the critic routine prompt specifies, ends at critic #62's head `f93af93`. Critic #63 appended its scores to the Track B table and to the Track A table at head `0796336` and did not append to the combined table, so the series a reader follows skips a lap while the record tables do not. Measured in this lap by parsing the three tables: the combined table carries one row fewer head than each per-track table. **Critic #64 transcribed the missing row rather than leaving the gap**, labelled as a transcription of critic #63's own values and not a re-score, and appended its own beneath it. **Done when:** a test reads the three tables and fails when a head present in either per-track table is absent from the combined one, so the next skip is caught by the suite rather than by a later critic reading the file. **Why P1 and not a preemption:** `docs/auto/SCORECARD.md` is record class and reaches no judge (CHARTER §14b), and the gap is repaired in the same lap that found it; what is left is the gate. ⚠ Related but distinct from **WFG-107** (hand-written censuses drifting) and **WFG-246** (the reorder count asserted rather than derived): this is a table that stops being appended to while its inputs keep growing | todo | true | minutes | infra · 심사 대비 신뢰성 |
| WFG-253 | P1 | infra | **Two rows closed in this window with a commit id that no clone of `origin/auto/dev` can resolve, and `STATE.json` names the same id.** `WFG-247` and `WFG-248` both read `done(cd97bc2)`. Measured in a fresh clone at this head, depth recorded because CHARTER §4 requires it (`git rev-parse --is-shallow-repository` answers `true`, `git rev-list --count HEAD` answers 54): `git cat-file -t cd97bc222878534c1927aee16ba182c13b786dca` fails with 「could not get object info」 and `git log --oneline --all` matches nothing. ⚠ **This is an object-existence measurement, not an ancestry claim**, which is the distinction CHARTER §4 draws and which five critic laps once got wrong (`WC-004`). **The cause is known and benign:** the lap's own report says `report.py` stamped `cd97bc2` and the push then rebased onto the paper routine's commit, so the work shipped as a different id and the pre-rebase commit was never pushed. **It is the third recurrence of one defect class**, after `f93af93` (「the WFG-233 row names a commit that exists」) and `71e95ee` (「the backlog row names a commit that exists」), both within four days, and it is the board that records which commit closed which row. **Done when:** the two rows and `docs/auto/STATE.json` name ids that resolve in a fresh clone, and a check refuses a `done(<id>)` cell whose id `git cat-file -t` cannot resolve, skipping rather than failing when the id is older than the clone's own boundary (the WFG-119 lesson: a gate whose predicate is 「can this clone resolve X」 fires at the clone depth and not at any fixed distance). **Why P1:** `docs/auto/BACKLOG.md` and `docs/auto/STATE.json` are record class and reach no judge (CHARTER §14b). ⚠ The repair belongs to a lap and not to this critic: the two `done` cells record another lap's work and rewriting someone else's closure note is not this lap's to do | todo | true | minutes | infra · 심사 대비 신뢰성 |
| WFG-247 | **P0** | KCF | **The last thing each of the five judges hears is this project's own result counted in the unit this project withdrew the day before, with a third population standing beside it in the same sentence.** ⚠⚠ **FILED AT POSITION 1 BY CRITIC #63, 2026-09-11T0300Z (CHARTER §14b), and it is NOT a preemption:** `docs/auto/DEMO_SCRIPT_5MIN.md` is one of the seven hashed sources of the booth kit, so this costs `make printables` at a new stamp plus a re-pointed `release/kcf-finals-2026/MANIFEST.json` (**NH-049**), which is not minutes. **The sentence, verbatim from `docs/auto/DEMO_SCRIPT_5MIN.md:266-268`, spoken in the 마무리 at 4:04:** 「보행망 노드 **2,218곳을 전수 탐색**해서, 대피 지점 한 곳을 추가하면 **20가구**, 두 곳이면 **24가구** 가 도달 가능해지고, **세 번째는 0가구** 를 더합니다.」 **Three populations, one breath.** 2,218 is `l0i_candidates_enumerated`, candidate SITES on the walk graph. 20 and 24 are `l0i_best_single_refuge_saved` and `l0i_best_pair_saved`, and `docs/NUMBERS.json` records both with `unit: households` on `sample: 영덕 2025, OSM 건물 124동(잠정)` — **OSM buildings, a third population from either**. And 「가구」 is the register `WC-013` withdrew for this project's output object on 2026-09-10 and that the 2026-09-11 lap then cleared off all eight lines of the two printed sources. **The repository already contains the reconciliation and nothing points at it:** `docs/auto/JUDGE_QA.md:947` says 「두 질문은 모집단도 다릅니다. 한쪽은 보행 도로망 노드이고 다른 쪽은 OSM 건물 124개입니다」, 190 lines away in a different card, in a different segment of the demo. `WC-013`'s `say_instead` states the rule this breaks in one line: the bound goes in the SAME block as the claim, never one screen away. **Four surfaces carry it:** `docs/auto/DEMO_SCRIPT_5MIN.md:266-268` (spoken), `:358-361` (the 화면/구두 number table, four rows reading 20가구 / 24가구 / 0가구 / 0가구), `:380` (금지 list item 6, which forbids 「20가구를 구했다」 and licenses 「20가구가 도달 가능해진다」 without touching the population), and `web/finals.html:1978-1995` (「도달 가능해지는 **가구 수**」 as a chart label; :1995 does carry 「모든 가구 수는 OSM 건물 스냅숏 위의 잠정치」 in the same card, which is why the screen is the weaker half of this row and the script is the stronger). ⚠ **This is NOT a `WC-013` violation and must not be filed as one.** `WC-013` withdrew the household register for the walk-or-be-rescued verdict; the refuge-siting result genuinely is measured on buildings and `docs/NUMBERS.json` says so. What is wrong is that the demo claims one unit in its opening and counts in another at its close, and the student has no card in hand for 「아까 단위가 지점이라고 하셨는데 왜 여기는 가구입니까」. ⚠ **Do not answer it by swapping 가구 to 지점**, which would be false — they are buildings. Name the population where the number is said. | done(cd97bc2) — **four surfaces, and the fix was NOT a word swap.** The spoken 마무리 now carries 「여기서 세는 **가구**는 판정 단위인 **지점**이 아니라 **OSM 건물 124동**입니다」 in the SAME block as the counts; 「보행망 노드 2,218곳」 became 「후보 지점 2,218곳」 there and on `docs/finals_screen_v2.md` §2.4 <!-- collision-ok: 2.4 — a SECTION number in a file path reference (「대피 지점 배치」 is §2.4 of docs/finals_screen_v2.md), not a quantity, and not any of the l0i_ values this row cites. --> and in the screen card, because 2,218 is what survived the filter and 8,443 is what went into it; the §3 화면/구두 table labels each row's population and gains a `l0i_household_population` row; 금지 item 6 now refuses BOTH the verb and the bare count. **124 is registered, from `refuge_placement.json :: optimum_h240.baseline.n_households` and NOT from `building_origin_routing.json`, whose `bld_yeongdeok_n_mapped` is also 124** — citing that would have been WFG-244's coincidence-as-derivation while fixing the card that warns about it (`scripts/register_refuge_population.py`, additive, upserts by exact key because the rest of the `l0i_` family has no registrar and a prefix sweep would delete four committed keys). **The added line cost 32 syllables and the claim commit predicted it:** 마무리 328 → 360 took it to 6.43 syl/s under the declared 56 s, so it was paid through the documented re-measure (`pace_20260911T0326Z.json`, allocation 36/42/47/58/56/61, whole script 5.81 → 5.92 syl/s) and the exchange is written into the script's own §1 rather than absorbed. ⚠ **The re-measure disproved a claim `docs/demo_script_pace.md` made about itself** — 「it would bite at 61 s (육십일, four syllables)」; the allocation landed on exactly 61 s and it did not bite, because 육십일 is three blocks; corrected in place with the measurement, mechanism kept. Gate: `tests/test_refuge_population_is_named_where_it_is_counted.py`, per-block plus a tree-wide sweep for the 보행망-노드 mislabel, shown RED on its own mutation and green on restore. `make printables 20260911T0326Z` (58 pp) and `release/kcf-finals-2026/MANIFEST.json` re-pointed, once for both rows. | yes | S-M | 제출 자료, 구현 및 유용성 |
| WFG-248 | **P0** | KCF | **The card that tells the student to open a DOI in front of the judge describes that record in a way the record's own abstract does not support.** ⚠⚠ **FILED AT POSITION 1 BY CRITIC #63, 2026-09-11T0300Z (CHARTER §14b), and it is NOT a preemption:** `docs/auto/JUDGE_QA.md` is a hashed kit source, so it costs the same rebuild as WFG-247 and should be paid in the SAME lap and the SAME `make printables`. **Q16d at `docs/auto/JUDGE_QA.md:829-832` says** 「저쪽은 **D\* Lite 로 다시 계획합니다** — 비용면이 바뀌면 이미 만든 경로를 고쳐 씁니다」, and rests this project's method differentiator on that characterisation. **The record says otherwise, and I read it at the source rather than at the report:** the Zenodo abstract for `10.5281/zenodo.22668358` reads 「The system orchestrates **OpenRouteService vector routing** using spatial polygon clustering to bypass API area limitations. **During API constraints or in wilderness scenarios**, the system delegates to an internal **D\* Lite heuristic grid fallback** that incrementally routes traffic away from actively predicted fires.」 D\* Lite is their **fallback**, not their planner. **Why this is a row and not a nit:** the same card, three paragraphs later at `:848-849`, tells the student 「심사위원이 「그 논문 진짜 있습니까」라고 물으면 이 DOI 를 그 자리에서 열어 보이십시오」. A judge who takes that invitation reads the abstract in front of the student, and the student's differentiator sentence compares this project's PRIMARY mechanism against the other team's FALLBACK. **The fix is one clause, not a rewrite, and the card already holds the correct fact 15 lines below:** `:834` already says 「도로와 **차량** 경로 스택(OpenRouteService)이며」. Move that into the method paragraph so the comparison reads 「저쪽은 OpenRouteService 로 경로를 내고, 망이 닿지 않거나 API 가 막히면 D\* Lite 로 다시 계획합니다」. ⭕ **Everything else on Q16d survived independent verification in this lap's own process, at the Zenodo API and not from the lap report that wrote the card:** `conceptdoi` `10.5281/zenodo.22668357` resolving to record `22668358` and therefore a version relation rather than a duplicate deposit, the title verbatim, `publication_date` **2026-09-09**, `resource_type` **Preprint**, the five authors in exactly the card's order, one attached file named `IEEE_Conference_Template.pdf`, and the card's hardest claim 「기록에는 성능 수치가 하나도 없습니다」 confirmed by a digit regex over the abstract returning the **empty list**. **Do not weaken any of those while fixing this one clause.** | done(cd97bc2) — one clause, as the row said. Q16d now reads 「저쪽은 **OpenRouteService 로 경로를 내고**, 망이 닿지 않거나 API 가 막히는 상황에서 **예비로 D\* Lite 격자 계획으로 넘어갑니다**」, and a ⚠ below it refuses the short form 「저쪽은 D\* Lite 입니다」 with the abstract's own sentence quoted, because the same card sends the student to open that DOI in front of the judge. Every other fact on Q16d is untouched. | yes | S | 제출 자료, 설계와 방법론 |
| WFG-210 | **P0** | KCF | **The 창의성 answer's item ① anchors, on three judge surfaces, on the one document its own gate exists to keep out of the answer.** ⚠⚠ **FILED AT POSITION 1 BY CRITIC #49, 2026-09-09T0820Z.** `tests/test_creativity_card.py:86` sets `OTHER_SYSTEMS = ("NIFoS", "G-DAPS", "산림청", "경기도", "국립산림과학원", …)` and four graded mutations prove the suite goes red if the spoken draft names one of them, in either direction. The anchor that same file registers at `:65` and `:331` for item ① is `docs/auto/knowledge/KOREAN_OPERATIONAL_SYSTEMS.md`, measured at `7f914fd`: **34** occurrences of those system names and **12** of their announced percentages (30 %, 76 %, 88 %). It is inherited, not chosen three times: `docs/creativity_card.md:36` wrote it (WFG-182), `web/finals.html`'s `CREATIVE[0].doc` took it (WFG-194) and `README.md:335` took it again (WFG-207), each lap doing its own row correctly. **The second half is the one a judge feels:** item ① claims the output object is the contribution and no surface gives a path to an instance of it — `git ls-files 'outputs/dispatch/*dispatch_a4.html'` answers **33** and `outputs/dispatch_full/` **113**, and `docs/dispatch_ordering.md` is already linked from `README.md:302`. The screen's own wording even names 「마을 단위 출동 목록」 and then anchors on the landscape note; the README drops that clause entirely. | done(20260909T0917Z) — all four surfaces, not the three the row named: `README.md` item ① names the committed index `outputs/dispatch/README.md`, and `web/finals.html`, `docs/creativity_card.md` §2 row 1 and `docs/auto/JUDGE_QA.md` Q29a each name a sheet **and** the index, with the landscape note kept beside it and its role written in the visible text. The kit was rebuilt in the same lap and `release/kcf-finals-2026/MANIFEST.json` re-pointed after staging. Four new assertions in `tests/test_creativity_card.py` — an instance exists; every surface hands a judge one; item ①'s anchors are never all documents **about** the other systems, decided by reading the anchors' contents; and the classifier that decides that still separates the two anchors — graded by nine mutations, all red, re-runnable with `python scripts/mutate_creativity_gates.py`. ⚠ **The independent reviewer BLOCKED the first version** and was right: `docs/creativity_card.md` §8 and the test header both asserted 「the surfaces with no count assertion name a sheet directly」, which was false of the finals screen, on a page printed for judges. Fixed by making it true (the screen names a sheet) and recording the false sentence. Residue: `docs/auto/DEMO_SCRIPT_5MIN.md` gained one clause pointing at the files but names no anchor of its own, and WFG-206's card-level binding on the screen and the card is still open. ~~⚠ the README half is this lap's one `fix-before-next-row` item and is minutes~~: `README.md:326-355` item ① gains one path to a committed instance of the output object, and its existing `KOREAN_OPERATIONAL_SYSTEMS.md` link may stay as the anchor for the *choice* not to compete on accuracy. The screen and card halves are the row: `docs/creativity_card.md` §2 row 1, `web/finals.html` `CREATIVE[0].doc`, `make finals`, the printed kit rebuilt in the same lap with `release/kcf-finals-2026/MANIFEST.json` re-pointed, and `tests/test_creativity_card.py` extended so an anchor naming an `OTHER_SYSTEMS` document for item ① is red. ⚠ Do NOT delete the landscape note from the card's provenance and do NOT let any comparison sentence into the block; the register rule (WC-007, WC-008, WC-009) is what makes the card safe. | yes | S | 창의성 (20, both tables) · 제출 자료 (출처 명기) |
| WFG-207 | **P0** | KCF | **WFG-194 put 창의성 on three surfaces and the front door is still at zero.** ⚠⚠ **FILED AT POSITION 1 BY CRITIC #48, 2026-09-09T0526Z**, and it is filed as a row rather than as a preemption because it is not minutes: a README section needs care and this lap's one `fix-before-next-row` item is already spent on WFG-208. Measured at `7e222c2` on the raw count of 창의 or 독창: `web/finals.html` **3**, `docs/auto/DEMO_SCRIPT_5MIN.md` **10**, `docs/auto/JUDGE_QA.md` **3**, `docs/creativity_card.md` **24**, **`README.md` 0**. 창의성 is **20 points on both rubric tables** and the 심사기준 names it **first** (「단순 암기 발표 지양; 창의성, 과학적 원리, 과학적 사고 중점」). The README is what a judge opens before the booth and what this loop spent a whole window writing a Round-4 section into, and the word does not occur on it once. | done(20260909T0630Z-lap) — `README.md` §5 「창의성 — 이 작품이 직접 만든 것」 inside Round 4, the same three items in the same descriptive register, each on its own line with its own repository path as a link, plus the 없는 것 limit for item ③, the closing line that the verdict is the judge's, and pointers to `docs/creativity_card.md` and Q29a. ⚠ **The row's own 「done when」 asked for a gate that reads 「the rendered README」 and there is no rendered README** — nothing in this tree builds it, so `README.md` is at once source and artifact, unlike `web/finals.html`. What replaced it: (a) every link target in the block is resolved against the tree, which is what a judge actually exercises on the front door, and (b) the anchors are bound **item by item** — the block is cut at its ①②③ markers and a transposition is red, which is **WFG-206's hole closed on this one surface** (WFG-206 stays open for the screen and the card). Gate: `tests/test_creativity_card.py` gains **six** assertions, graded against the lap's ten mutations (**nine red**; the tenth is M8, the evaluative-register rewrite, green here as everywhere) and the independent reviewer's eight, written without seeing the lap's — the two-source rule the card's §6 stated and this lap had not followed. ⚠ **The reviewer BLOCKED and the block was fixed, not argued.** Its mutation D — rewrite an item's backticked **label** to another real path, leave the link **target** correct — left all eighteen assertions green, so the pairing was bound where the markup is and not where a judge reads; the sixth assertion binds label to target and D is red. It also struck a false measurement out of `docs/creativity_card.md` §7 (「the only `README.md` string in `scripts/`」 — there are seven; the conclusion that nothing BUILDS it survives) and an unregistered line count of the README from a printed page. ⚠ **One mutation had to be re-run**: the first M8 attempt opened with 「세 항목」 and went red on the count gate, because 항목 is on this file's own counter list — a mutation that goes red for the wrong reason reports coverage the gate does not have. One measured false positive excluded: 「Q29a」 is a card ID, not a count. The kit was rebuilt at `20260909T0630Z` and `release/kcf-finals-2026/MANIFEST.json` re-pointed in the same lap (WFG-152). Method and limits: `docs/creativity_card.md` §7. | true | minutes to an hour | 창의성 (20, both tables) · 제출 자료 ⚠ Done when: the README carries a 창의성 block that says what this project made rather than assembled, links `docs/creativity_card.md`, and is graded by a test that reads the rendered README and not a source file (the defect WFG-206 exists for). ⚠ Do NOT touch the README's opening paragraph about the 2025 fire or its Round-2 section (CHARTER §3.12); add below Round 4. ⚠ Do NOT put a present-perimeter margin value anywhere in `README.md:210-282` while NH-032 and NH-034 are open. |
| WFG-178 | **P0** | KCF | ⚠⚠ **ADDED AT POSITION 1 BY CRITIC #39, 2026-09-08T0217Z, AND IT IS THIS LAP'S ONE `fix-before-next-row` ITEM (CHARTER §14b).** **The lap that closed WFG-139 printed the honest remainder, and the count it printed was made wrong by the test that same commit added.** Three judge-facing surfaces say the tile-gated skip set is **six**: `docs/auto/JUDGE_QA.md:980` (Q28's 없는 것 block, 「테스트 여섯 개」), `docs/auto/JUDGE_QA.md:1182` (Q40, which enumerates them as `test_srtm_dem.py` 네 개 + `test_validation_robustness.py` + `test_validation_session3.py` and scripts the student to say 「지형 타일이 있어야 도는 여섯 개는 깨끗한 클론에서 건너뜁니다」 out loud), and `docs/clean_clone_gates.md:85` (「six SRTM tests that `skipif` on the cached tile」). Measured here at `1282198`, cold, with `data/raw/dem/srtm/N36E129.hgt` absent: the tests whose `skipif` predicate is exactly that tile number **seven**, the six named plus `tests/test_raster_ingestion.py:168::test_auto_dem_prefers_srtm_when_the_tile_is_cached`, added by `ab4e71e`, the same commit that wrote the card; the lap's own report at `docs/auto/reports/2026-09-08T0121Z-dev.md:202` calls it 「The 63rd skip is new and is mine」 and the card does not carry it. The card is on paper: kit `20260908T0114Z`, 38 pages, `JUDGE_QA.md` sha256 `c7ab6e505ce0…` re-hashed here against the tree and matching, Q28 and Q40 both inside the 17 printed JUDGE_QA pages. **No gate reads the number:** `grep -rn '여섯 개' tests/` and `grep -rn 'six SRTM' tests/` both return nothing. **Second defect in the same write-up, same minutes:** `docs/clean_clone_gates.md:45` reads 「Result, measured 2026-09-08 on this sandbox at `088203c`」, and `git ls-tree -r 088203c -- tests/conftest.py` is **empty**; the guard the result is about first exists at `ab4e71e`. **Done when:** the three surfaces state seven and name the seventh test, `clean_clone_gates.md:45` names `ab4e71e`, and the printables kit is rebuilt at a new stamp with `make finals-bundle` re-pointed in the **same lap** (WFG-152). **Constraints:** minutes; do not restate the count anywhere a gate cannot re-derive it, and prefer 「`pytest -rs`가 출력하는 목록이 정본」 to a second hand-typed number; `gates.py --mode full` re-run after the rebuild with both asserts before the push. **DONE 2026-09-08T0321Z. The count is no longer stated, it is derived:** `tests/test_tile_gated_skip_count.py` walks every `skipif` decorator in `tests/` with `ast` and splits them into those that reach `data/raw/dem/srtm/N36E129.hgt` (**7**, directly or through a module-level helper whose body names it) and those whose reason says SRTM while gating on something else (**4**, all `test_slope_digraph.py`, which is WFG-180 and is *named on the card* rather than fixed here); it reads sources, not results, so it answers the same on a warm laptop and a clean clone. **12 regexes bind every restatement on Q28, Q40 and `docs/clean_clone_gates.md`, and a regex matching nothing FAILS**, so rewording a bound sentence out of the gate is itself red. Graded by running it, not by reading it: planting an eighth tile-gated test turns 5 assertions red, and `test_the_detector_is_graded_on_planted_source` runs the `ast` pass over source handed to it. `clean_clone_gates.md:45` names `ab4e71e` with `088203c` kept as the record. **A fourth surface the critic did not find and no gate could have:** `docs/auto/withdrawn_claims.json`'s WC-010 `say_instead` — the field telling the student what to say *instead of* the withdrawn claim — read 「테스트 여섯 개」, and `check_withdrawn_claims.py` reads `.md`/`.html` only, so the registry that sweeps 933 files cannot sweep itself (WFG-155 from the inside). ⚠ **The independent reviewer BLOCKED the first version and was right:** it bound 7 sentences and left 5 restatements unanchored, *including both lines the student speaks*, while asserting on the card that all the numbers were machine-checked — the defect being fixed, shipped inside the fix; it proved it by rewriting the spoken 「7개」 to 「3개」 and getting 9 passed. Also fixed on its findings: the 「11」 promised a judge what `pytest -rs` prints while being derived from decorator text, and 4 of the 11 gate on the laptop bundle, so both surfaces now name the machine the count is true on. Kit rebuilt twice (`20260908T0328Z`, then `20260908T0406Z` after the block) with `MANIFEST.json` re-pointed each time | done(b1af739 + the reviewer-block fixes) | true | minutes | B/A 제출 자료 · B 데이터 수집·분석·해석 · R5 · R7 |
| WFG-167 | **P0** | KCF | **The judge drill found a question five judges are likely to ask a life-safety dispatch tool, and 42 cards later the bank has no answer: responsibility.** `git grep -niE '책임\|법적\|면책\|사람이 다치' docs/auto/JUDGE_QA.md` returns **zero** at `7cc4eb7`, counted this lap. The claim exists on exactly two surfaces and nowhere the student rehearses from: `web/finals.html:1393` renders 「**최종 판단은 언제나 사람이 내립니다.**」 and `docs/auto/DEMO_SCRIPT_5MIN.md:217` gives it as a one-line interruption answer for the 재난대응 실무자 lens. One of the five KCF finals judges is a public-sector disaster-response official (RUBRIC 심사개요; the loop's own prism lens list), the interview is 1:1 and about five minutes, and the natural follow-up to a screen that prints a rescue order is 「이 순서대로 갔다가 사람이 못 나오면 누가 책임집니까」 or 「군청이 이걸 실제로 쓰면 법적 지위가 뭡니까」. **This is WFG-144's asymmetry in the other direction and the fourth instance of the shape:** a claim lives on the screen and on the printed script while the bank the student recites from has no card, so the judge's question lands on an unrehearsed student who is standing in front of a screen that already answered it in one line. **Done when:** a new `Q · T0` card in `docs/auto/JUDGE_QA.md` answers responsibility in the register the repository can support — the tool is 의사결정 보조, the operator decides, the delivery layer is dry-run by construction (`scripts/send_dispatch_email.py`, `delivery/sms.py`, CHARTER §3.6) and no message has ever been sent to a resident, no human-subject data was collected, `outputs/dispatch*` are drafts a 이장 reads and edits — with a 없는 것 block that refuses any claim about statutory liability, insurance, or 재난 및 안전관리 기본법 status, because nothing in this repository has read one; the card cites the two existing surfaces so all three say the same thing; `tests/test_judge_qa_bank.py` binds the card's claim to the screen's sentence so they cannot drift apart; and the kit is rebuilt in the same lap (WFG-152). ⚠ Do **not** write a legal opinion, do **not** name a statute, and do **not** promise that the delivery layer will stay dry-run after the finals — say that it is dry-run today and point at the two files | done(20260908T0622Z-lap) — card **Q16b** (T0) in `docs/auto/JUDGE_QA.md` §3, bound by `tests/test_responsibility_and_privacy_cards.py` (13 tests, mutation-graded 5/5 by the lap and again by its reviewer). ⚠ The binding went in a NEW test file rather than in `tests/test_judge_qa_bank.py` as this row said: that file gates the bank's *shape* (counts, tiers, drill table) and this one gates two cards against code, artifacts and two other surfaces. ⚠⚠ **The gate passed and the card still had a booth-breaking sentence in it; the cold read is what found it** — the draft offered to hand the judge the dispatch sheet, which is the one document NOT in the printed kit (Q39, same file). The sentence carried no number and quoted no file, so nothing mechanical could see it. Corrected; the kit built from the uncorrected text (`20260908T0632Z`) was discarded before any commit, as WFG-162's `1534Z` was. ⚠ The independent reviewer's root objection was that the gate's sheet glob was scoped to the same 33 files the card counts — 609 of the tree's 642 tracked sheets were unguarded and a footer-less one planted outside the glob left it green. Closed in the same lap: the footer claim is now checked against all 642 tracked sheets and the card must name the run directory its count describes. Kit rebuilt at `20260908T0633Z` (40 pp), bundle re-pointed | true | one lap | B/A 연구 목적 · A 구현 및 유용성 · R5 · R7 |
| WFG-181 | **P0** | KCF | **The bank has 42 cards, one of the five judges is a public-sector disaster-response official, and there is no card on 개인정보 — for a tool whose output is a household-level rescue list for elderly residents.** Counted here at `47e48b5`: `git grep -ciE '개인정보|프라이버시|privacy' -- docs/auto/JUDGE_QA.md` returns **zero**, and the same grep returns zero on `web/finals.html`, `docs/auto/DEMO_SCRIPT_5MIN.md`, `docs/auto/finals/` and `README.md`. **This is WFG-167's twin from the same lens and it is filed at position 2 rather than 1 deliberately:** 책임 is the sharper of the two because the screen itself invites it, and both are one card in the same file with one kit rebuild between them. **The answer already exists in committed files and is a strong one, which is why this is a card and not a research row.** Verified here: the elderly signal is *statistical density*, not a person — `docs/data_sources.md:101-102,147` name 행정안전부 주민등록 연령별 인구 통계 and KOSIS 시군구별 65세 이상 독거노인 as the sources of `rural_elderly_density`; `outputs/dispatch/README.md` states in its own voice that the 「villages」 are **DBSCAN groupings of dispatch points at eps = 500 m and not 행정리**; the SMS drafts hold no person (`outputs/dispatch/20260801T163042Z/01-거무역리공원-북쪽/sms_drafts.txt` reads 「구조 요청 4곳」, a count of places); `sms.send()` is never called, needs an `approval_token`, is gated by `DEMO_MODE`, and `MANIFEST.json` records `nothing_was_sent: true` (CHARTER §3.6). **I checked the negative rather than asserting it:** `grep -rEo '01[016789][- ]?[0-9]{3,4}[- ]?[0-9]{4}' outputs/` returns hits that are all digit runs inside floats and hashes (`1814997.0179265365` in `outputs/dispatch_full/20260801T183522Z/MANIFEST.json`), so no phone number is in the tree. **Done when:** a new `Q · T0` card answers 「이 목록은 개인정보 아닙니까 / 고령자를 어떻게 특정합니까」 with those file citations; its 없는 것 block refuses any claim about 개인정보보호법 status, about consent, and about what happens if the delivery layer is ever un-dry-run, because nothing here has read a statute (the same refusal WFG-167's row specifies); `tests/test_judge_qa_bank.py` binds the card to `outputs/dispatch/README.md`'s DBSCAN sentence so the two cannot drift; and the kit is rebuilt in the same lap (WFG-152). ⚠ Do **not** write 「개인정보를 전혀 쓰지 않습니다」 as a flat universal — say what the four named sources are and what granularity they arrive at, which is the register `WC-009` prescribes | done(20260908T0622Z-lap) — card **Q20a** (T0), taken in the same lap as WFG-167 because both are one card in the same printables `SOURCES` file and together cost one kit rebuild (critic #40's own reasoning). ⚠ **This row was never pushed as `in-progress` before it was built.** CHARTER §4 step 3 says a claim that is not pushed is not a claim, and only WFG-167 was claimed, at `a6d94ad`. No other lap was running and nothing collided, but the protocol was broken and it was the lap's own independent reviewer that caught it, not the lap. ⚠ The card does NOT enumerate the four sources at their granularity as this row directed: `vulnerability.py` says in its own words that the county scores are placeholders **not yet anchored** to authoritative sources, so the honest answer is that the KOSIS statistics are not ingested at all — the more conservative direction, and the row's premise was the thing that was out of date | true | one lap | B/A 연구 목적 · A 구현 및 유용성 · R5 · R7 |
| WFG-182 | **P0** | KCF | **창의성 is 20 points on both KCF tables and it has not moved once in 39 scorecard rows, and no backlog row has ever named it.** Measured here at `47e48b5`, not read: the 창의성 column of `docs/auto/SCORECARD.md`'s combined series is **15/15 in every one of its 39 rows**, from the first critic lap (`1113388`, 2026-09-03) to critic #39 (`1282198`, 2026-09-08) — `awk -F'|' '/^\| 2026-09-/ && NF>=15 {gsub(/[ *]/,"",$7); gsub(/[ *]/,"",$13); print $7"/"$13}' docs/auto/SCORECARD.md | sort | uniq -c` returns the single line `39 15/15`. Every other column has moved; the total went 72 → 86 entirely on 제출 자료, 데이터 수집·분석·해석 and 설계와 방법론. And `grep -c '창의성' docs/auto/BACKLOG.md` returns **2** across ~180 rows. The KCF 심사기준 says in its own words 「단순 암기 발표 지양; 창의성, 과학적 원리, 과학적 사고 중점」 (RUBRIC 심사개요), and 창의성 plus the live interview is over half the ISEF Grand Award score. **This row is not a research row and must not become one** (CHARTER §3.4: extend, never pivot). It is the observation that the project's creative claim has never been assembled anywhere a judge meets it, only its correctness has. **Done when:** one judge-facing surface states this project's creative claim in the register the repository can support, built **only** from committed artifacts and existing withdrawn-claim records — candidates already in the tree: the **output object** as the differentiator rather than accuracy (`docs/auto/knowledge/KOREAN_OPERATIONAL_SYSTEMS.md` §3, which DIRECTION already names as *the* differentiator), the coupling of a forecast field into a rescue-aware routing objective (`docs/real_roads_real_hazard.md`), and the withdrawn-claim machinery itself as a *method* a judge can open (`docs/auto/withdrawn_claims.json`, `docs/withdrawn_claims.md`) rather than as an apology; and a `Q · T0` card so the student can say it in the interview, where this row is actually scored. ⚠ Do **not** add a new experiment, model, split, arm or region for this row, and do **not** claim novelty against any other system beyond what was opened (`WC-009`) | done(20260908T0920Z-lap) — card **Q29a** (T0) in `docs/auto/JUDGE_QA.md` §5, after Q29, in the **descriptive** register: three items (output object as the contribution; the run where both axes are real at once; withdrawn claims registered for a machine), each pointing at a committed file. No experiment, model, split, arm or region added; no comparison and no novelty claim. Method, result, caveats and what it does not show: `docs/creativity_card.md`. Gated by `tests/test_creativity_card.py` — anchors resolve and all three items are made, no other system named in either direction, no count in digits or Korean numerals, no novelty claim, and the 없는 것 limit that makes item 3 honest. ⚠⚠ **The independent reviewer returned `block` and was right on four counts, all of them the same shape — a population drawn to fit the claim after the claim existed.** (1) The doc said `check_forbidden.py` independently blocks the 처음/최초 shapes; it does not (that rule fires only next to the word for a direct measurement), so a bare novelty claim on a T0 card was caught by **nothing** — the reviewer planted one and got exit 0. (2) The other-system list held only the five systems this repository had already written about, so 「FARSITE 는 …」 and 「소방청 시스템에는 …」 both passed. (3) The count guard's `\d{2,}` sat one digit above 「철회 주장은 9건입니다」. (4) The anchor check was card-level while the doc's table drew it item-level, so deleting item 2 from the spoken draft stayed green. All four fixed, each now a red mutation. ⚠ Two of the lap's own assertions were **green because they could not fail** before its own grading found them: the count check was digit-only and 「여섯 개」 carries no digit, and its Korean-numeral replacement ended in ``, which after a Hangul syllable never matches. **The mutation still green is named rather than omitted** (WFG-186): the evaluative rewrite 「매우 독창적입니다」, which keeps every anchor and every prohibition and turns the card into the self-assessment its design exists to avoid. ⚠ `grep -cE '창의|독창'` still returns **0** on `web/finals.html` and **0** on `docs/auto/DEMO_SCRIPT_5MIN.md`; the row asked for one surface and a T0 card and those were deliberately not widened | true | one lap | B 창의성 · A 창의성 · R2 · R5 |
| WFG-185 | **P0** | KCF | **The card the reviewer fixed still states the defect it was fixed for, and the gate written to catch that class passes on a coincidence.** `docs/auto/JUDGE_QA.md:715` (Q16b, **T0**, the spoken draft) reads 「... `FOOTER_LINES` 가 모든 시트에 찍는 것이라, **커밋된 33장 전부**에 있습니다」. Measured here at `9329400`: `git ls-files '*dispatch_a4.html'` answers **642** tracked sheets, 33 under `outputs/dispatch`, 113 under `outputs/dispatch_full`, 496 under `outputs/live`. 「커밋된」 makes the claim about the repository; 33 describes one run directory. The 0655Z lap's own independent reviewer raised exactly this and the lap's report says the card 「must name the run directory its count describes」, but the fix commit **`4679285` never touched `JUDGE_QA.md`** (`git show --stat 4679285` lists `tests/`, `MEMO`, `BACKLOG`, `STATE`, `dashboard`, images and the report, and no `JUDGE_QA.md`); the scope fix reached the test file and the report's prose only. The gate's scope assertion is `assert COUNTED_RUN in _card("16b")` (`tests/test_responsibility_and_privacy_cards.py:225`) and it is satisfied by `outputs/dispatch/20260801T163042Z/MANIFEST.json` at `JUDGE_QA.md:733`, which sits in the 근거 block under the **`nothing_was_sent`** claim and not under the sheet count, so the assertion would still pass with the count wholly unscoped. ⚠ **The substantive claim is TRUE and understates:** the reviewer measured **0 of 642** sheets missing the footer, so this is a scope defect on the sentence the student says out loud, not a false claim about the world. It is live on `WFG_printables_20260908T0633Z.pdf` (40 pages). **Done when:** the spoken sentence names the run directory its count describes (or drops the count and points at the manifest, which is Q30/WFG-117's register); the assertion binds the scope token to the **sentence carrying the count** rather than to anywhere in the card; the lap records the mutation that proves it (move the run-directory string out of that sentence, which leaves the gate green today and must turn it red after); and the kit is rebuilt and `release/kcf-finals-2026/MANIFEST.json` re-pointed in the same lap (WFG-152) | done(20260908T0920Z-lap) — the spoken sentence now names `outputs/dispatch/20260801T163042Z` **inside the sentence carrying the count**, and adds a denial in the student's own words that 33 is the tree's number; measured here, `git ls-files '*dispatch_a4.html'` answers **642** (33 · 113 · 496) and the counted run holds exactly 33. The assertion moved from `COUNTED_RUN in _card("16b")` to a sentence-scoped binding via a new `_sentence_with` helper. Graded: the pre-fix wording goes red (M1) and so does the same token moved one sentence away (M2); the reviewer added two more that go red (a wrong run directory named; the count changed to 642 with the directory kept). ⚠ **The reviewer's N11 was green and is now bound:** deleting the spoken scope-denial left the gate passing, because naming the directory scopes the count for a reader following the path while the denial is what scopes it for a judge who is only listening. **The mutation still green, named rather than omitted** (WFG-186): a superset phrasing 「`<run>` 를 포함해 커밋된 33장 전부에」 keeps the token in the sentence while the grammar re-attaches the count to the whole tree — co-location is not attachment and nothing here reads Korean grammar. Kit rebuilt at `20260908T0939Z` (41 pages) and `release/kcf-finals-2026/MANIFEST.json` re-pointed in the same lap (WFG-152) | true | minutes | 제출 자료 · R5 · R7 |
| WFG-187 | **P0** | KCF | ⚠⚠ **ADDED AT POSITION 1 BY CRITIC #42, 2026-09-08T1115Z, AND IT IS THIS LAP'S ONE `fix-before-next-row` ITEM (CHARTER §14b as amended by NH-038 B: minutes, and the finals screen is named in that rule's own list).** **The judged screen's build stamp is five commits from the limit that closed `auto/dev` to every routine eight days into the sprint, and this time it is filed BEFORE the limit rather than at it.** Measured here at `ceb43ba`, unpiped: `web/finals.html:434` carries `"git":"1bca8ed"`, `git rev-list --count 1bca8ed..HEAD` answers **25**, and `tests/test_finals_screen.py:540` sets `STAMP_MAX_COMMITS_BEHIND = 30` with `:732` asserting `behind <= STAMP_MAX_COMMITS_BEHIND`. Gates are ALL GREEN at this head; the branch closes at 31. **The arithmetic, said rather than felt:** the screen was rebuilt at `1bca8ed` (2026-09-07T22:21Z) and the branch has taken 25 commits in the 11 h 59 m since, about two an hour. A dev lap on the `17 */3` grid pushes a claim commit plus its work and its report — the six work commits in this window carried 3 to 5 commits each — so the **next** dev lap lands at roughly 28 to 30 and the one after it trips the assert on its claim commit alone, before it has done any work. That is NH-045 replaying with the same cast: the routine that meets the red is parked by CHARTER §3.9 and, for critic and paper laps, forbidden from clearing it. **This row is the one-command version of that, taken while it is still cheap.** ⚠ It is NOT the structural fix and does not duplicate one: WFG-159, WFG-160 and WFG-161 already carry the recurrence, and NH-043 (due 2026-09-09, unanswered) is the author's decision on what a lap may do when the gate fires. **Done when:** the next dev lap runs `make finals` and pushes the rebuilt `web/finals.html` **before it claims a row**, records `git rev-list --count <new stamp>..HEAD` in its report, and confirms `gates.py --mode full` exits 0 on the commit it pushes | done(20260908T1224Z) | true | minutes | 제출 자료 · 구현 및 유용성 · R1 · R9 |
| WFG-010 | **P0** | KCF | README Round-4 section + English abstract draft (**revise**: vocabulary and caveat rules). ⚠⚠ **MOVED TO THE HEAD OF THE `todo` BLOCK BY CRITIC #42, 2026-09-08T1115Z — this lap's ONE row move (CHARTER §14, one reorder per lap; reason on `docs/auto/DIRECTION.md`). Critic #41 raised this row P1 → P0 and left it at table line 151, so the priority changed and the position did not, and a fresh dev lap reading the table in order (CHARTER §5) still reached `WFG-186` — a P1 infra row whose own cell says §14b holds it behind R3 and R8 — before it reached this one. Measured here at `ceb43ba`: the first `todo` row in table order was WFG-186 at line 42, and EIGHT P0 `todo` rows sat below it (WFG-007, WFG-117, WFG-127, WFG-128, WFG-129 at lines 71-75, WFG-036 at 103, this row at 151, WFG-054 at 172). This move fixes the leading violation only; the other seven are recorded in DIRECTION and are not this lap's reorder.** ⚠⚠ **P1 to P0 by critic #41, and the reason is a misreading in the record rather than a change of mind.** Critic #39 recorded this row as 「sitting inside the very block R8 helps hold shut」 and filed it no further. It is not inside that block: CHARTER §14b holds **loop hygiene** behind the readiness lines, this row's goal column is **KCF**, and §14b's own sentence names 「README opening」 first in its list of judge-facing surfaces. So nothing was holding this row except its priority, and R8 is one of the two lines holding the infra block shut while its only blocker sat unpicked as P1. Measured here at `9329400`: `grep -nE '^## Round' README.md` returns `:59 ## Round 2 결과 (2026-07)` and `:75 ## Round 3 (2026-08)` and nothing for Round 4, unchanged across every critic lap this week, and the scorecard has docked Track A 제출 자료 for it in two consecutive windows (`47e48b5`, `9329400`). Keep CHARTER §3.12: the Round-2 section is a record and is not edited; Round-4 material goes **below** Round 3 | done(20260908T1240Z) | true | hours | 제출 자료 · 연구 목적 · R8 |
| WFG-190 | **P0** | KCF | ⚠⚠ **ADDED AT POSITION 1 BY CRITIC #43, 2026-09-08T1429Z, AND IT IS THIS LAP'S ONE `fix-before-next-row` ITEM (CHARTER §14b as amended by NH-038 B: minutes, on the README opening).** **The Round-4 section that ticked R8 is the weakest of four surfaces carrying the same fact, and one of its sentences is a flat universal the repository falsifies.** Two defects, one paragraph block, `README.md:220-239`. **(a)** `README.md:235-239` reads 「이 실험이 내놓은 구체적인 margin 값들은 아직 어느 심사용 자료에도 싣지 않습니다」, while `docs/auto/JUDGE_QA.md:849-852` carries **9**, **27** and **5** and `docs/auto/finals/printables/manifest_20260908T0939Z.json` binds that file into the **41-page** printed kit under the title 「심사위원 질의응답 카드」. 「심사용 자료」 is defined nowhere in this repository, so the sentence is the WC-009 class: never a flat sentence about what a document contains. The Q&A card is CORRECT and is not the thing to change; its block ends with an explicit ❌ forbidding the student from speaking any of the three. **(b)** `README.md:220-225` states the present-perimeter result with no mention that the buffer width is a free parameter, where `paper/manuscript.md:493` says 「The buffer width is a second free parameter and nothing in the data chooses it」, `docs/present_perimeter_arm.md:116` gives it a section heading saying it 「matters more than the 9」, and Q37 at `docs/auto/JUDGE_QA.md:1346` sweeps five widths. `docs/present_perimeter_arm.md:103-104` measures the opponent recovering **86 of 91** at 1 km and its table at `:121` gives **12 of 91** at 250 m with **91** origins walking into the fire, so 「상당 부분」 and the abstract's 「recovers most」 (`README.md:629`, `paper/manuscript.md:23`) are both weaker than the artifact on a result the section's own header promises reads against this project. a `grep -nE` over `tests/test_readme_round4.py` for 상당 부분, 회수, `recovers most` and 완충 returns nothing, so no test binds either sentence. **Done when:** (a) the sentence names the set it means (screen, panels, submitted documents) and says the Q&A bank carries the values only in the 「말하지 말 것」 form; (b) the paragraph names the buffer width as a free parameter the data did not choose and points at `docs/present_perimeter_arm.md` §4; `gates.py --mode full` exits 0 on the pushed commit. **Constraints:** ⚠ do NOT write a margin value or any new result figure into `README.md` while NH-032 and NH-034 are open, the fix is qualitative; do not touch the README's opening paragraph about the 2025 fire; do not edit the Q&A card, it is the correct one | done(20260908T1518Z) | true | minutes | 제출 자료 · 데이터 수집·분석·해석 · R8 |
| WFG-188 | **P0** | KCF | **The bank now answers 책임, 개인정보 and 창의성, and it still has no answer to the question a county emergency officer asks before any of them: what would it take for us to run this?** Measured here at `ceb43ba` across the 45 cards: `grep -cE '비용|예산|도입|운영비|유지보수' docs/auto/JUDGE_QA.md` returns **8**, and every one of the eight is something else — 위음성 예산 (`:93`, `:96`), 시간 예산 (`:606`), a substring of 「바람 벡터 …무관하다는」 (`:568`), 「모듈 도입부」 (`:941`), 「정확도입니다」 (`:1317`), Q38's 예산 초과 (`:1347`) and Q16b's own refusal 「기관이 도입하면 이런 지위가 됩니다」 (`:747`). **No card is about adoption.** Track A scores 「구현 및 유용성 · 해결하고자 하는 문제 또는 필요성 해소 가능성」 and 「개발 목적 · 사회적 기여도」, and one of the five judges is a public-sector disaster-response official — the same judge whose two other questions cost the loop two laps to answer. **The material is already committed and this is an assembly, not research** (CHARTER §3.4): `docs/auto/finals/BOOTH_SETUP.md` is the offline laptop recipe, `release/kcf-finals-2026/` is the bundle, `requirements.txt` is pinned, the sandbox has no API keys and the delivery layer is dry-run by design (28 run records carry `nothing_was_sent: true`). **Done when:** a T1 card answers 「저희 군에서 쓰려면 무엇이 필요합니까」 with each sentence pointing at a committed file, and its 없는 것 block states what is NOT there — no cost figure of any kind, no institution has ever run it, the one consultation is N = 1, and the adoption question is not the student's to answer (Q16b's register); no new experiment, artifact or registry key; kit rebuilt and `release/kcf-finals-2026/MANIFEST.json` re-pointed in the same lap (WFG-152). ⚠ Filed at position 3 rather than 1 deliberately: WFG-187 is minutes and WFG-010 ticks a readiness line, and this row does neither | done(20260908T1518Z) | true | one lap | 구현 및 유용성 · 개발 목적 · 제출 자료 |
| WFG-193 | **P0** | infra | ⚠⚠ **ADDED AT POSITION 1 BY CRITIC #44, 2026-09-08T1700Z, AND IT IS THIS LAP'S ONE `fix-before-next-row` ITEM — a RED GitHub run, which CHARTER §14b as amended by NH-038 B names explicitly alongside the judge-facing surfaces.** **`auto-gates` run 253 (id 34250797625) at `0cca093` is `failure`, and the gates inside it are green.** Read through the GitHub MCP (`curl` against `api.github.com` is still refused here, WFG-119). The job log prints `[gates] ALL GREEN  mode=full head=0cca093 (auto/dev)` and `1763 passed, 63 skipped, 2 xfailed` in 341.5 s, and the step 「Gates (make verify, baseline, snapshot, env, full pytest)」 concluded `success`. What failed is step 7, 「Keep the gate record」 (`actions/upload-artifact@v4`): `##[error]Failed to FinalizeArtifact: Received non-retryable error: Failed request: (403) Forbidden: Error from intermediary with HTTP status code 403 "Forbidden"`. **The consequence is not cosmetic and it is why this is P0:** `.github/workflows/auto-gates.yml:89` gives `promote` `needs: gates`, so `promote` was **skipped**, and CHARTER §4c's fast-forward did not run. `git ls-remote origin Main` answers **`6ecc386`** while `auto/dev` is **`0cca093`** — **`Main` stopped following the last gate-certified commit because an archival upload 403'd.** ⚠ **The workflow already applies the opposite rule twice and this one step is where it was not applied:** the sibling `finals-acts` upload carries `if-no-files-found: warn` with the comment 「an empty upload is a warning rather than a red job」, and the `promote` job itself exits 0 on a refused fast-forward so as to 「Never turn a green gate into a red run」. **Done when:** the 「Keep the gate record」 step can no longer decide the colour of the `gates` job (`continue-on-error: true` on that step is the whole of it, one line), the change is pushed, and the lap reads the resulting run through the MCP and says in its report whether the artifact upload succeeded. ⚠ **Whether the 403 is transient is NOT established and the fix does not depend on it:** run 252 (15:47Z) uploaded fine, `finals-acts` inside run **253 itself** uploaded 2,238,204 B successfully at 16:24:41Z, and the 9,774 B gate record failed six minutes later; runs 223-253 hold exactly this one `failure`. If it recurs after the fix the cause is the artifact service or the account rather than this repository, and **that** is when it becomes a NEEDS_HUMAN entry — not before (DIRECTION: do not open a fifteenth DECISION while fourteen are unanswered and nothing is blocked). **Constraints:** the gate command itself is not touched, `--assert-head` and `--assert-reported` still run before the push, and no gate is weakened — the artifact is a record, not a check. ⚠ **CLOSED by the ci-red routine at `b2cda36`, not by a dev lap.** All three parts of the Done when are met. The one line is in on BOTH uploads rather than only 「Keep the gate record」, because the sibling `finals-acts` upload is the same trap. The change is pushed at `b7c1837`. And the runs were read through the MCP: **run 254** (id 34257393284, `be05c1c`, 17:29Z) is `success` and uploaded BOTH artifacts, `gates-be05c1c` at 9,792 B (17:36:09Z) and `finals-acts-be05c1c` at 2,238,011 B (17:30:03Z) -- an hour after the 403 and WITHOUT the guard, since it predates it. That is the row's own question answered: the artifact upload succeeded. | done(b2cda36) | true | minutes | A 구현 및 유용성 · R1 · R9 <!-- collision-ok: 341.5 — this is the pytest wall-clock in SECONDS printed by `auto-gates` run 253's job log, not a detection latency. It is not `det_yeongdeok_best_anomaly_minutes_after_report` (28.0), which is a GK2A anomaly time in minutes after the report; the anchors after/minutes/report that pull it onto this row are the prose about the run and its 341.5 s pytest stage. --> |
| WFG-199 | **P0** | KCF | ⚠⚠ **ADDED AT POSITION 1 BY CRITIC #45, 2026-09-08T2000Z, AND IT IS THIS LAP'S ONE `fix-before-next-row` ITEM (CHARTER §14b as amended by NH-038 B: one command, on `web/finals.html`, which §14b names as a judge-facing surface).** **Run `make finals` before you claim a row, and re-point `release/kcf-finals-2026/MANIFEST.json` in the same commit (WFG-152).** Measured here at `9c24a8b`, unpiped: `web/finals.html`'s embedded payload carries `"git":"25f6b60"` and `git rev-list --count 25f6b60..HEAD` answers **22** against `tests/test_finals_screen.py:540`'s `STAMP_MAX_COMMITS_BEHIND = 30`. Critic #44 measured **16** at `0cca093` three hours earlier, so the drift is running at about **two commits an hour** and this lap's own report commits take it to about 24. A dev lap costs 3 to 5 commits (claim, work, report), so the NEXT lap lands at roughly 28 and **the lap after it trips the assert on its claim commit alone** — which is NH-045 (BLOCKER, open) replaying for the third time, and the third time the critic routine is the one routine forbidden to clear it. This is filed with headroom rather than at the limit, exactly as WFG-187 was by critic #42, and it is **not** a fix for the class: the class is NH-045's A-or-B recurrence rule and NH-043, both still the author's. **Done when:** `make finals` has run, the stamp names a commit reachable from `origin/auto/dev` (`tests/test_finals_screen.py::test_the_escape_this_gate_cannot_close_is_still_open` requires it, so do NOT stamp an unpushed HEAD), the bundle manifest points at the rebuilt payload, `gates.py --mode full` exits 0, and the lap's report states the new count. | done(9170a37) | true | minutes | 구현 및 유용성 · 제출 자료 |
| WFG-127 | **P0** | KCF | **The booth script now tells the student that no fixed buffer width can be known in advance, and the sweep behind that sentence has five points with ONE of them inside the region the claim is about.** `docs/fair_opponent_line.md` §3 says 「The safe total is a spike, not a plateau (275 / 284 / **345** / 275 / 283 across 250 m → 3 km)」 and 「Move off the best width in either direction and the arm loses ground fast」; `docs/auto/DEMO_SCRIPT_5MIN.md` 3막 turns that into 「어느 폭이 맞는지는 그날 알 수 없고」, which is what the student says at the booth. The measured widths are 250 / 500 / 1000 / 2000 / 3000 m (`data/processed/present_perimeter_arm_uiseong_andong_2025.json` → `buffer_sensitivity`, verified cell by cell by this critic). The nearest neighbours of the winning width are a factor of two away on each side, so **nothing in the run distinguishes a spike at 1 km from a plateau spanning roughly 800 m to 1.5 km**, and a plateau that wide is exactly what an operator CAN aim at. The second leg fails the same way: 「the two defensible builds do not even agree on the best width (1 km versus 500 m)」 is two coarse grids landing on adjacent points, which is what a broad optimum produces, not evidence of unknowability. **Done when:** (i) the two sentences on the two surfaces state the grid they rest on and stop asserting the shape the grid cannot resolve — **this half is the fix-before-next-row item, it is prose only, it needs no run and it is fifteen minutes**; (ii) the existing `scripts/run_present_perimeter_arm.py` sweep is re-run adding 750 / 1250 / 1500 m, routing only on committed inputs, no retrain and no re-acquisition (CHARTER §3.11 untouched, exactly as WFG-114 ran), the new widths are registered additively beside the five that exist, and whichever shape the curve turns out to have is what the two surfaces then say; (iii) a test fails if either surface asserts 「spike」 or 「cannot be known」 while the registered grid has no point between 500 m and 1 km. **⚠ A THIRD SURFACE, found by the 0617Z dev lap while clearing (i), and NOT edited by it.** `docs/present_perimeter_arm.md` §4, one line under the gated table, states 「The 1 km row is a **spike, not a plateau**」 in its own prose. That is the same claim about the same five points as the two surfaces (i) names, on the document the other two send the reader to as the sweep's one home — so (i) as scoped removes the claim from the derived surfaces and leaves it standing at the source. The 0617Z lap did not touch it because critic #24's item says in terms 「Do not touch §4's gated table in `docs/present_perimeter_arm.md`, which is correct」, and the *table* is correct: the defect is the sentence after it, not a cell. Whoever takes (ii)/(iii) must decide the sentence too, and (iii)'s test must cover three surfaces rather than two. **⚠ And (iii) will have to LOOSEN a gate this lap tightened** (the 0617Z lap's independent reviewer, non-blocking): `test_the_doc_does_not_claim_a_fixed_buffer_cannot_work` now bans 「spike, not a plateau」 and 「not knowable on the day」 **unconditionally**, where (iii) as written asks for a ban conditioned on the registered grid (「…while the registered grid has no point between 500 m and 1 km」). Once (ii) registers 750 / 1250 / 1500 m, whichever shape the finer curve actually shows must become sayable again, so the ban has to become conditional in the same commit that adds the widths. Unconditional was the right default while the grid has one point in the region. **Constraints:** no committed value moves; no margin figure reaches any surface while NH-032 is open, and this row needs none — it is about the shape of a curve, not its height. ⚠⚠ **A FOURTH SURFACE, WRITTEN BY THE 2026-09-08T1518Z LAP WHILE CLEARING WFG-190, AND MOVED TO THE HEAD OF THE `todo` BLOCK BY CRITIC #44, 2026-09-08T1700Z — this lap's ONE row move (CHARTER §14, one reorder per lap; reason on `docs/auto/DIRECTION.md`).** `README.md:232`, on the front door, now states 「이 실행이 쓴 폭은 그 sweep 안에서 **고원이 아니라 뾰족한 봉우리**입니다」. That is the same assertion as the third surface this row already records (`docs/present_perimeter_arm.md:129`, 「The 1 km row is a **spike, not a plateau**」), copied onto the one surface §14b names first, by the lap whose task was to stop the README being softer than its sources. Measured at `0cca093`, unpiped: `docs/auto/JUDGE_QA.md:1394` (Q37 · T1) says in bold 「1 km 의 양옆 이웃이 각각 두 배 거리에 있어서, 「1 km 에서 뾰족하다」와 「그 부근이 평평하다」를 이 sweep 으로는 구분할 수 없습니다」 and ends 「재보지 않은 것을 재봤다고 말하지 마십시오」; `:920` says the same; and `docs/auto/DEMO_SCRIPT_5MIN.md:151` has the student say 「좋았던 폭 주변이 뾰족한 봉우리인지 넓은 고원인지 이 실험은 가리지 못합니다」. **So the front door now contradicts the booth script, the Q&A bank and this row, and it says the one thing the bank explicitly forbids the student from saying.** ⚠⚠ **And the gate written for exactly this claim passed, because the claim was written in the other language.** `tests/test_fair_opponent_line.py:177-191` bans the string 「spike, not a plateau」 and its docstring gives this row's own reasoning; `:185` records that it was already hardened once against markdown-asterisk evasion. It reads English. `README.md:232` is Korean, and it uses the exact vocabulary (뾰족한 봉우리 / 고원) that `DEMO_SCRIPT_5MIN.md:151` already uses for the **negation**. This is DIRECTION's standing rule 「a registered spelling reaches ONE language」 firing on a **newly written** claim, which is the case **WFG-168** is filed for and which does not exist yet. **Done when, added to (i)-(iii) above:** (iv) `README.md:232` stops asserting the shape and states the grid it rests on, in the register `DEMO_SCRIPT_5MIN.md:151` already uses; and (v) (iii)'s test covers **four** surfaces, not three, and bans the Korean spelling as well as the English one, in the same commit. ⚠ Prose only, no run, no margin value (NH-032 / NH-034 still open). | done(9170a37) — (i) done by the 0617Z lap, (ii) (iii) (iv) done this lap: the sweep re-ran at 750/1250/1500 m into a NEW artifact (all five committed widths reproduce cell for cell), the shape is a SHOULDER not a spike, the claim is withdrawn as WC-011, and all five surfaces including the THIRD one (docs/present_perimeter_arm.md §4) are rewritten. tests/test_buffer_shape.py holds the conditional gate. ⚠ It runs AGAINST the project: 750 m beats the committed 1 km | true | one lap | B 설계와 방법론 · B 데이터 수집·분석·해석 · A 제출 자료 <!-- collision-ok: 1.5 — every 1.5 on this row is a buffer WIDTH IN KILOMETRES from the present-perimeter sweep (the 「800 m~1.5 km plateau」 the five-point grid cannot resolve), not a time. It is not `det_yeongdeok_best_anomaly_minutes_after_report` (28.0), whose unit is minutes; the anchors after/best/minutes that reach this row are its prose about the sweep's best width. --> |
| WFG-201 | **P0** | KCF | ⚠⚠ **FILED AT POSITION 1 BY CRITIC #46, 2026-09-08T2340Z (CHARTER §14b as amended by NH-038 B: judge-facing, larger than minutes, therefore a P0 row at position 1 and never a preemption; this lap set NO `fix-before-next-row` item, so nothing is displaced by even a minute).** **The headline margin is not a fixed quantity. It is a maximum over a grid, and it can only shrink as anyone looks harder — and no surface says so.** The fair opponent's buffer width is chosen **after the fact, by scanning outcomes**, which is conservative and honest and the right design; but it makes the reported margin 「forecast minus the best post-hoc buffer width」, which is **non-increasing in search effort**: a new width can only tie or beat the incumbent, so the margin can only hold or fall. **Measured, by this repository, today:** `9170a37` added 750, 1250 and 1500 m to a five-point grid and `docs/present_perimeter_buffer_shape.md` §3(c) reports the consequence in its own words — 750 m scores **349** against 1 km's **345**, so 「the forecast's margin over it on this fire is smaller than the committed margin: **5** origins at 750 m against **9** at 1 km」. **The first refinement this project ever ran removed four of the nine origins of margin.** ⚠ **What is already right, so this row does not re-litigate it:** §4 of that document states the neighbouring caveat well (the grid is coarse, it cannot locate the maximum, 349 and 345 are inside what a finer grid could reorder), and `README.md:245-247` says plainly that the rerun went against the project and that the opponent is stronger than the repository had recorded. **What none of them states is the property itself**, and a statistician judge lands it in five seconds: 「그 margin 은 계속 넓히는 격자 위의 최댓값입니다. 격자를 더 촘촘히 하면 얼마입니까?」 **Done when:** (i) `docs/present_perimeter_buffer_shape.md` §4 gains one clause naming the statistic as a post-hoc maximum over the measured grid and stating that refining the grid can only move it downward or leave it, with the 9-to-5 move as the worked instance; (ii) the same sentence, in the register `DEMO_SCRIPT_5MIN.md` already uses, reaches the three surfaces that carry the fair-opponent line — `docs/fair_opponent_line.md` §3, `docs/auto/DEMO_SCRIPT_5MIN.md` 3막 and `docs/auto/JUDGE_QA.md` Q37 — and `README.md:220-247`; (iii) a test fails if any surface states a margin without the post-hoc-maximum qualifier, graded against a mutation that removes the qualifier and one that adds a margin to a surface that had none; (iv) the kit is rebuilt in the same lap and `release/kcf-finals-2026/MANIFEST.json` re-pointed (WFG-152). **Constraints:** ⚠ **no margin value (9, 27, 5, 19, 86) may enter `README.md:220-247` while NH-032 and NH-034 are open** — this row needs none, because it is about how the number is chosen, not what it is; no committed artifact is regenerated (CHARTER §3 rule 2) and no run is needed, since all eight widths are already committed; nothing here reopens WC-011, which is correctly withdrawn and registered. ⚠ **This bears on two decisions that were due 2026-09-08 and are open:** NH-032's whole table is computed at 1 km and its option A is offered as 「margin 9, the more conservative claim」, which at the grid's best measured width is now **5**. Critic #46 wrote that measurement into NH-032 and NH-034 itself rather than filing it as work, because NEEDS_HUMAN is a `docs/auto/` file. | done(20260909T0021Z-lap) — six surfaces, not the row's five (`docs/present_perimeter_arm.md` §4 draws both argmax conclusions and was not named); the gate is `tests/test_post_hoc_maximum.py`, section-scoped, graded against six real-surface qualifier-strikes plus four synthetic mutations; kit rebuilt and `MANIFEST.json` re-pointed. ⚠ **(iii) was re-aimed and the report says why:** no judge-facing surface states a margin at all while NH-032/NH-034 are open, so a margin-only gate had no true positive in the tree; the qualifier attaches to the selection rule and the live argmax conclusions (어깨, 틀리려면 두껍게), with the margin arm kept and exercised against injected text. ⚠ `paper/` is out of scope by CHARTER §12 and fires on three units — **WFG-202**. | true | one lap | B 데이터 수집·분석·해석 · B/A 제출 자료 · B 설계와 방법론 · R5 · R7 |
| WFG-194 | **P0** | KCF | **WFG-182 put 창의성 on the Q&A bank and left it at zero on the two surfaces a judge actually stands in front of.** Measured at `0cca093`, unpiped, with the same command critic #40 filed the original row with (`grep -cE` over the alternation of 창의 and 독창): it answers **2** on `docs/auto/JUDGE_QA.md`, **0** on `web/finals.html` and **0** on `docs/auto/DEMO_SCRIPT_5MIN.md`. WFG-182 is `done` and it closed correctly on its own scope — a card — but the 심사기준 names 창의성 **first** (「단순 암기 발표 지양; 창의성, 과학적 원리, 과학적 사고 중점」), it is **20 points on both rubric tables**, and the judge meets the screen and the five-minute script before the bank, which is a reference the student reads from and not something a judge is shown. A card a judge never asks for cannot score the row. **This is an assembly of claims already in the tree onto surfaces that exist — no new experiment, no new model, no new region (CHARTER §3.4).** The material is already committed: the output object (a household-level rescue order rather than a spread map, `docs/auto/knowledge/KOREAN_OPERATIONAL_SYSTEMS.md` §3), the fair-opponent arm built to attack the project's own headline (`docs/present_perimeter_arm.md`), and the withdrawn-claim registry itself (`docs/auto/withdrawn_claims.json`, `docs/withdrawn_claims.md`). **Done when:** `web/finals.html` and `docs/auto/DEMO_SCRIPT_5MIN.md` each carry the 창의성 claim in the repository's own register, both of those counts are non-zero, and a test binds each sentence to the artifact behind it. ⚠ **Both are gated surfaces:** `DEMO_SCRIPT_5MIN.md` is a printables `SOURCES` file, so the lap that edits it rebuilds the kit and re-points `release/kcf-finals-2026/MANIFEST.json` in the same lap (WFG-152); and `web/finals.html` is rebuilt by `make finals`, never by hand, with the stamp checked against `STAMP_MAX_COMMITS_BEHIND` (WFG-187's class). ⚠ No margin value on either surface while NH-032 and NH-034 are open. ⚠⚠ **This row was first written malformed by critic #44 itself** — the `grep -cE '...'` alternation put an unescaped pipe in the title cell and split the row into 10 fields, which is the eleventh instance of exactly the defect **WFG-191** was filed for, committed by the lap that was reading WFG-191 at the time. The row was rewritten whole rather than patched, and the grep is now described in words instead of quoted. That is a second piece of evidence for WFG-191's own argument: the convention cannot hold this invariant and a gate must. | done(20260909T0321Z-lap) — both surfaces, plus the printed kit as a third the row did not ask for. `web/finals.html` carries a 「창의성 · 이 작품이 직접 만든 것」 block at the foot of the 시스템 구조 tab (three items, each with its repository path, plus a closing line saying the verdict is the judge's); `docs/auto/DEMO_SCRIPT_5MIN.md` carries **one spoken sentence** in 도입 plus two ⚠ Q&A blocks, and the six segment budgets were re-measured to 37 / 42 / 48 / 60 / 57 / 56 (`pace_20260909T0321Z.json`, spread 1.02x, tighter than the 1.03x it replaced) with the nine-second cost written into the script where the student reads it; `docs/creativity_card.md` is now the sixth of seven printables `SOURCES`, kit rebuilt at `20260909T0321Z` and `release/kcf-finals-2026/MANIFEST.json` re-pointed in the same lap (WFG-152). Gate: `tests/test_creativity_card.py` gains eight assertions, graded against eight mutations, **all eight red**. ⚠ **The row's own done-when was not enough and the lap says so:** a grep count is satisfied by typing the word, so the gates bind the **anchor paths and the register** instead — the mutation that keeps 창의성 in the spoken line and swaps the claim for 「저희 프로젝트는 매우 독창적입니다」 goes red on two counts. ⚠ **Found on the way:** `scripts/check_region_literals.py` refused the block because 의성 is the tail of 창의성; the region-name branch is narrowed to names that START a word and `tests/test_check_region_literals.py` grades all four cases including the one it deliberately cannot see. | true | one lap | 창의성 (both tables) · A 제출 자료 ⚠⚠ **Critic #45, 2026-09-08T2000Z: re-measured, unchanged, and WORSE than the row records — the project HAS a 창의성 document and no judge can reach it.** At `9c24a8b`, a `grep -cE` over the alternation of 창의 and 독창 answers **2** on `docs/auto/JUDGE_QA.md`, **0** on `web/finals.html`, **0** on `docs/auto/DEMO_SCRIPT_5MIN.md` and **0** on `release/kcf-finals-2026/README_KO.md`. Meanwhile `docs/creativity_card.md` exists (149 lines, bound by `tests/test_creativity_card.py`, 10 hits) and **is not in the printed kit**: the newest manifest `docs/auto/finals/printables/manifest_20260908T1529Z.json` lists six source documents (BOOTH_SETUP, DEMO_SCRIPT_5MIN, JUDGE_QA, submission_reconciliation, DETECTION_FLOOR_CARD, RELATED_WORK_PANEL) and `docs/creativity_card.md` is not one of them. So the row worth **20 points on BOTH rubric tables**, and named FIRST in the 심사기준 list, has a written answer that reaches a judge only if that judge opens the Q&A bank at the right card. **Done when** should therefore include: 창의성 reaches `web/finals.html` AND `docs/auto/DEMO_SCRIPT_5MIN.md`, and `docs/creativity_card.md` is either added to the printables source list or explicitly recorded as deliberately not printed, with the reason. | ⚠⚠ **CRITIC #48, 2026-09-09T0526Z: this row closed with a FOURTH surface still at zero, and it is the front door.** Re-measured at `7e222c2`, the head this lap reviewed the closure at, on the raw count of 창의 or 독창: `web/finals.html` **3**, `docs/auto/DEMO_SCRIPT_5MIN.md` **10**, `docs/auto/JUDGE_QA.md` **3**, `docs/creativity_card.md` **24** and ⚠ **`README.md` 0**. The row named three surfaces and delivered all three plus the printed kit, so it closed correctly on its own scope; the README was never in that scope and is the page a judge browses before the booth. **This is not a re-opening of this row.** The residue is **WFG-207**, filed at position 1.
| WFG-125 | **P0** | science | **Every margin in the fair-opponent dispute is what a PERFECT forecast is worth, not what this project's forecast is worth, and that is the one question a judge is actually asking.** `docs/present_perimeter_arm.md` §5 says it in the document's own words: 「The forecast-aware arm carries no forecast error here. It plans on the same hazard field it is scored against, so 9 is the margin a **perfect** forecast buys.」 The parked lap says the same of its 27. This is inherited from the committed 91 rather than introduced by either lap — but until now the 91's opponent was fire-blind and the oracle hardly mattered; against a present-aware opponent the whole remaining margin sits inside the gap between an oracle and a model. **The objection reduces to one sentence a judge can say in five seconds — 「비교하신 예보는 정답을 미리 본 예보 아닙니까?」 — and today the honest answer is 「맞습니다」 with nothing after it.** **Done when:** either the same three arms are re-run with the forecast arm planning on a field the model produced rather than the field it is graded on (routing only; no retrain, no re-acquisition, CHARTER §3.11), with the margin reported whatever it says; **or** the row records in writing, with the paths checked, that no such predicted field is committed and names exactly what would produce one and when — an absence stated is an answer, an absence assumed is the WFG-114 round-1 defect. **Start by spending ten minutes on that second branch**: `data/processed/` holds `hazard_*.npz` and the `spread_v2/` outputs, and which of them is a prediction rather than the graded truth is the row's first question, not its assumption | done(20260909T1230Z) | true | one lap | B 설계와 방법론 · B 데이터 수집·분석·해석 ⚠⚠ **RAISED P1 → P0 AND MOVED TO POSITION 3 BY CRITIC #45, 2026-09-08T2000Z — this lap's ONE row move (CHARTER §14; reason on `docs/auto/DIRECTION.md`). No P0 row was moved below a non-P0 row; this row moved UP, from table line 97 to line 49.** **The reason is a measurement, not a re-reading: the input this row's expensive branch needs is ALREADY COMMITTED, so the cost written into this row is wrong.** `data/processed/spread_v2_lofo_oof_cells.csv.gz` holds the shipped model's **leave-one-fire-out out-of-fold** per-cell probabilities — 151,904 rows, columns `fire_id, op_from, row, col, label, dist_band, dist_to_fire_m, far_band, prob` — including **20,749** cells for `yeongdeok_2025` and **82,736** for `uiseong_andong_2025`, the two regions the routing arms use. Those are predictions the model made on a fire it did NOT train on, which is precisely the field 「a field the model produced rather than the field it is graded on」 asks for. Shape check, measured: `hazard_uiseong_andong_2025.npz`'s `haz_stack` is `(5, 135, 128)` and the uiseong OOF cells span rows 23-123 and cols 0-126, i.e. inside that grid, with 18 `op_from` slices against the stack's 5 times. ⚠ **What this lap did NOT verify, and the dev lap must, first:** that the OOF `row`/`col` indexing shares an origin and CRS with the hazard stack the router consumes, and how 18 `op_from` slices map onto the 5 committed hazard times. If they do not align, the row falls back to its written branch — and that record is then worth more, not less, because it can name the file and say exactly why it cannot be used. **This is routing only: no retrain, no re-acquisition, no committed artifact modified (CHARTER §3 rule 2 and §3.11 both hold).** **Why P0.** `docs/auto/JUDGE_QA.md:1393` files the question this row answers as **Q36 · T0** — the bank's own top tier, the one whose header says a card you cannot answer loses you that judge — and its answer today is 「맞습니다」 followed by nothing measured. It sits on 데이터 수집·분석·해석 and 창의성, 40 of the 100 points, on both rubric tables. ⚠ This makes THIRTEEN P0 rows against seven sprint days and that is stated rather than hidden; the written branch is hours, not days, and is a complete discharge of this row. ⚠⚠ **RE-COSTED BY CRITIC #47, 2026-09-09T0230Z, BY MEASURING RATHER THAN RE-READING — and the measurement runs against critic #45's re-costing of this row.** Critic #45 raised this row to P0 on 「the input this row's expensive branch needs is ALREADY COMMITTED, so the cost written into this row is wrong」. The input is committed and **it is a sample, not a field.** Measured at `86f8929` from `data/processed/spread_v2_lofo_oof_cells.csv.gz` against the committed hazard stacks, unpiped: **영덕/yeongdeok_2025** is graded on `data/processed/routing_demo.npz`, `haz_stack` shape **(5, 181, 147)** = **26,607** cells per slice; the out-of-fold file holds **20,749** rows over **5** `op_from` values (which does match the 5 hazard slices in count), about **4,087** cells per operating point = **15.4 %** of a slice, and the union of every distinct `(row, col)` it ever scores is **4,859** = **18.3 %** of the grid, confined to rows 25-99 of 181 and cols 0-90 of 147, with **769** positives. The other two regions are worse or no better: `hazard_uiseong_andong_2025.npz` is **(5, 135, 128)** = 17,280 per slice against **18** `op_from` and a **50.1 %** union; `hazard_uljin_samcheok_2022.npz` is **(5, 155, 92)** = 14,260 per slice against **26** `op_from` and a **16.9 %** union. **So the first branch is not routing-only and is not minutes.** Re-planning the forecast-aware arm on the model's own field needs a rule for the **~82 %** of Yeongdeok cells the out-of-fold never scores, plus a mapping from operating points onto hazard slices for the two regions where those counts differ. ⚠⚠ **And the fill rule is a free parameter of exactly the kind WFG-201 was filed about:** chosen after seeing what it does to the margin, it is a second post-hoc maximum, this time on the project's headline number. Filling the unscored cells from the graded field would reintroduce the very oracle this row exists to remove. **Therefore:** if the first branch is run at all, the fill rule is written down **before** the run and the row reports the margin under it whatever it says; and **the second branch — record the absence with the paths checked — is the one that fits a lap**, and the paths are now checked and quoted here so no lap re-derives them. ⚠⚠ **CRITIC #50, 2026-09-09T1122Z — PROMOTED TO THE FIRST `todo` POSITION, above WFG-027, and this is this lap's ONE CHARTER §3b reorder.** The window did not make this row less urgent, it made it more urgent, and it did so on the front door. Measured at `9c22ff3`: `README.md`'s TL;DR now carries, directly under its single headline number, the sentence 「42 is an upper bound: it is what a *noiseless* forecast would buy, not what this project's own model buys, which is less by an amount no run here measures」, and the Round-4 block repeats the property in Korean at `README.md:261-268`. **So the repository now hands every judge who reads its front door the exact question this row exists to answer, and no file in the tree answers it.** That is a different situation from the one critics #46, #47, #48 and #49 ranked this row in: then the gap was internal, now it is printed on the most-read surface. **Why above WFG-027 specifically, and the argument is not that 일정 does not matter:** 일정 is at literal zero on all four judge surfaces (re-measured here: 일정 answers 0 / 0 / 0 on `README.md`, `web/finals.html`, `docs/auto/JUDGE_QA.md` and 1 on `docs/auto/DEMO_SCRIPT_5MIN.md`, that hit being the adjective 일정한; 로드맵 and 개발과정 answer 0 on all four), but that zero is a **documentation** zero — the student lived the schedule and can answer 「어떤 일정으로 만들었습니까?」 from memory at the booth. This row's zero is a **knowledge** zero: nobody in the room can answer 「그럼 선생님 모델이 실제로 벌어 주는 값은 얼마입니까?」, because no run has measured it. Both rows are minutes on their cheap branch, so the reorder costs WFG-027 one lap at most. **This lap set NO `fix-before-next-row` item, deliberately, so that the next dev lap reaches this row rather than an item.** ⚠⚠ **DONE BY THE 20260909T1230Z DEV LAP — AND THE ROW'S OWN PREMISE WAS WRONG, WHICH IS THE RESULT.** This row, `docs/present_perimeter_arm.md` §5 and the README TL;DR all assumed the forecast-aware arm plans on the **graded truth**, so that removing the oracle meant building a new PLANNING field — which is why critic #47 costed it at a fill rule over the ~82 % of 영덕 cells `spread_v2_lofo_oof_cells.csv.gz` never scores. Measured at `f9183b0`, that is not what the arm does. `data/processed/routing_demo_canonical.npz` holds BOTH `haz_stack` — the **leave-one-fire-out forward simulation** (`scripts/run_forward_sim_region.py` fits on every fire EXCEPT the target), already a model output — AND `obs_stack`, the cumulative FIRMS footprint, on the SAME 181x156 500 m grid, in the SAME file, scored against by nothing. **The arm already plans on a field the model produced; what makes it an oracle is that the GRADER uses that same array as if it were truth.** So the fix needs a different GRADING field, not a new planning field, and a complete one is committed — no fill rule, no refit, no re-acquisition. The out-of-fold sample was the wrong instrument, and critic #47's arithmetic about it was right. **Delivered:** `scripts/measure_oracle_gap.py`, `data/processed/oracle_gap_yeongdeok.json`, `docs/oracle_gap.md`, 10 `og_yeongdeok_*` registry keys via the additive registrar `scripts/register_oracle_gap.py`, and `tests/test_oracle_gap.py` (11 tests, two graded by mutation). **Measured**, at the best time-matched pair (27 min apart, t=0 excluded because both stacks share the seed detection): the simulation puts **952** cells in the fire where **937** burned — within 2 % on AREA — and only **534** are the same cells; IoU **0.394**, false alarms 418, misses 403. That IoU independently re-derives the ≈ 0.40 footprint figure `docs/MODEL_CARD.md` already reports from `yeongdeok_forward_sim.json`, so it CONFIRMS a committed figure and is not a new performance claim; the size-vs-place split and the false-alarm/miss decomposition are what is new. ⚠ **No margin was produced and no committed number moved** — 42, 91, 9 and 27 are untouched, and 「this measures what the model buys」 is a registered forbidden phrasing on all ten keys. The run that WOULD produce the margin is **WFG-213**, filed `blocked(NH-052)`: it changes the meaning of a committed judged headline (42, 91), which CHARTER §6 makes the author's call, and it needs a written-down-first rule for reading `obs_stack` between observation times (the WFG-201 discipline). ⚠ 의성·안동 carries no `obs_stack` at all, so the check is possible on the 42 and NOT on the 9/27; a test pins that. |
| WFG-027 | **P0** | KCF | Schedule & roles timeline from `git log` (Korean; AI-assisted sessions disclosed pending WFG-022) | done(20260909T1517Z) — `docs/auto/finals/TIMELINE_ROLES.md` (Korean, DRAFT-labelled per CHARTER §9): five phases whose boundaries are the calendar gaps in the commit record, not a narrative, with every date and count re-derived from `git log` by `scripts/build_timeline_roles.py` into the committed artifact `data/processed/timeline_roles/timeline_roles.json` and registered as **20** `timeline_*` keys through the additive registrar `scripts/register_timeline_roles.py`. `tests/test_timeline_roles.py` (10 tests) checks the prose against the artifact and **not** against `git log`, because the routine's clone depth is not a constant and a history-walking gate would fire on depth rather than on a defect (the NH-043 class); the one test that does read git skips with its reason on a shallow clone. **Nine mutations graded**: a wrong phase day-count, a wrong phase commit count, a wrong trailer count, a wrong total, a moved phase start, a broken anchor commit, a dropped 「커밋 수는 작업량이 아닙니다」 caveat, a removed DRAFT label and a single-site typo in one of the two 「662개」 all take the suite red — the last was found by this lap's independent reviewer in the lap's own grading (containment, not a count) and the check now counts occurrences. **Judge-facing half:** 일정 answered **0** on `docs/auto/JUDGE_QA.md` and **1** (the adjective 일정한) on `docs/auto/DEMO_SCRIPT_5MIN.md`; it now answers **4** and **4** (re-counted against `git show 89da7d3:<file>`), via a 「카드가 아니라 파일로 답하는 질문」 block in the bank and a paragraph below the judge-type table in the script, both pointing at the file. ⚠ It still answers **0** on `README.md` and **0** on `web/finals.html`; see residue (b). The kit was rebuilt (`WFG_printables_20260909T1532Z.pdf`, 53 pages) and `release/kcf-finals-2026/MANIFEST.json` re-pointed in the same lap, 19 files, no payload added. ⚠ **Residue, and it is named rather than hidden:** (a) the answer is a **block, not a card Q41** — the bank's card ids must be contiguous 1..35 and Q36-Q40 are already consumed by the 「아직 답이 없는 질문」 table, so there is no next card number; that is **WFG-216**. (b) `web/finals.html` was **not** touched, so 일정 still answers 0 on the screen; `make finals` may only stamp a commit already on `origin` (MEMO 2026-09-09) and doing it here would have un-made this lap's commits. Filed in (b) as the screen half of this row's residue. | true | hours | 설계와 방법론 (일정·역할) ⚠⚠ **Critic #45, 2026-09-08T2000Z: this row's rubric cell names a criterion that is scored on BOTH tables and it is at ZERO on every surface a judge reaches.** 설계와 방법론 (20 points, both tracks) lists four bullets and one of them is 「일정 및 팀원(개인의 경우 제외) 역할 배분의 타당성」; for an individual entry the 팀원 half is excluded by the criterion's own text and the **일정** half is not. Measured at `9c24a8b`: `grep -n 일정` answers **0** matches in `docs/auto/JUDGE_QA.md`, **0** in `release/kcf-finals-2026/README_KO.md`, **0** in `web/finals.html`, and its single hit in `docs/auto/DEMO_SCRIPT_5MIN.md:70` is 「일정 크기」 — 「a certain size」, a false positive. So the 46-card bank has no 일정 card, the 5-minute script has no 일정 sentence, and the judged screen and the bundle README say nothing. The material exists (`git log`, `docs/auto/BACKLOG.md`'s sprint plan, `docs/auto/reports/`) and this row estimates **hours**. ⚠ **This lap did NOT move or re-prioritise this row**: its one reorder was spent on WFG-125, and raising a priority without moving the position is the exact defect critic #42 recorded against critic #41. **It is named here as the next critic lap's reorder candidate**, so that lap need not re-derive the measurement. ⚠ **RE-MEASURED BY CRITIC #47, 2026-09-09T0230Z at `86f8929`, unpiped, and the count is still literal zero:** `grep -c '일정'` answers **0** on `web/finals.html`, **0** on `docs/auto/JUDGE_QA.md` and **1** on `docs/auto/DEMO_SCRIPT_5MIN.md` — and that one hit is line **70**, 「2 km 화소는 **일정 크기** 아래의 불을 구조적으로 분해하지 못합니다」, which is the adjective 「일정한」 and not a schedule. So the sub-item 「일정 및 팀원 역할 배분의 타당성」, which is named inside 설계와 방법론 (**20 points on BOTH rubric tables**), has **no judge-facing surface at all**. ⚠ The 팀원 역할 half is excluded for a solo entrant by the 심사기준's own 「개인의 경우 제외」; the **일정** half is not. **The answer exists and does not need deriving** — `docs/auto/CHARTER.md` §1's date table and §11's sprint plan, and `docs/auto/BACKLOG.md`'s own sprint-plan table, carry the dates — so this row is an assembly onto a surface, not a new claim (CHARTER §3.4). ⚠ Both target surfaces are gated: `DEMO_SCRIPT_5MIN.md` is a printables `SOURCES` file and `web/finals.html` is rebuilt by `make finals`, so the lap that takes this row rebuilds the kit at a NEW stamp and re-points `release/kcf-finals-2026/MANIFEST.json` in the same lap (WFG-152, WFG-187's class). | ⚠⚠ **PROMOTED TO P0 AND MOVED HERE BY CRITIC #48, 2026-09-09T0526Z — this lap's ONE §3b reorder, and it is the move critics #45 and #47 both pre-registered and neither could make, because a priority change is not a position change and §3b permits one act.** Re-measured at `7e222c2` on the raw count of the string 일정: **0** on `README.md`, **0** on `web/finals.html`, **0** on `docs/auto/JUDGE_QA.md`, **1** on `docs/auto/DEMO_SCRIPT_5MIN.md` and that hit is line **70**, 「2 km 화소는 일정 크기 아래의」, the adjective. 로드맵 and 개발과정 are zero on all four surfaces as well; 타임라인 occurs **once**, at `web/finals.html:463`, and it is the UI hint 「아래 타임라인으로 시각을 움직여 보십시오」 for the fire-time slider, not a schedule; 계획 occurs eleven times across the three text surfaces and **not one of the eleven is a development schedule** (route planning, the opponent's planner, an agency's announced plan, a plan not held, and 연구 계획서). So a named sub-item of a row worth **20 points on BOTH tables** is at literal zero on every surface a judge meets, and the answer already exists in CHARTER §1 and §11 plus `git log`. The 팀원 half stays excluded by the 심사기준's own 「개인의 경우 제외」 for a solo entrant, so this row is the 일정 half only.
| WFG-212 | **P0** | KCF | **The front door's newest and largest section states no result in this project's favour, and has no lead sentence a judge can carry away.** Measured at `9c22ff3`: `README.md:200-362` (Round 4, 163 lines, the first thing a reader meets after the Round-3 record) carries **11** ⚠ markers and **28** negative-framing tokens — 철회 6, 한계 5, 아닙니다 4, 없습니다 3, 불리 3, 틀린 2, and 이긴 적이 / 사실이 아니 / 못합니다 / 내렸습니다 / 깎는 one each — against **one** affirmative-outcome token, 「더 좋」, and that one is the sentence saying the model-free opponent scored **better** than the repository had recorded. The section's own 「읽는 법」 at `:207-208` tells the reader that only item 4 of four favours the project, and item 4 is a list of files a judge can open, not a result. Above it the TL;DR's one headline number, 42 of 458, carries two ⚠ blocks, the second of which retires it as an upper bound nobody has measured below (WFG-125). ⚠ **This row is NOT a request to soften anything.** CHARTER §3 rule 5 and the withdrawn-claim record are this project's strongest card and every caveat in the section is true and stays. The defect is 「자료의 논리적 구성」, a named criterion of 제출 자료 (20 points on **both** tables): the section documents what Round 4 attacked and never states what the project still claims, so a judge reading the front door for sixty seconds at a booth leaves with the caveats and not the contribution. | done(20260909T2118Z-lap) — the section now opens with a lead block stating the claim that survives Round 4 (the **output object**, an existence claim rather than a performance claim) with its bounds attached in the same block, pointing at `outputs/dispatch/README.md` and `docs/real_roads_real_hazard.md`. It removes no ⚠ line; `tests/test_readme_round4_lead.py` pins ten caveat clauses in the numbered items, floors their ⚠ count at 10 **counted over the numbered items only** so the lead's own markers can never pay for a caveat deleted below it, caps the claim half at five sentences, and bans the margin values, the bare headline number, the 상한 word and a comparative / operational-fitness / precedence family **inside the lead**. ⚠ **The independent reviewer BLOCKED the first version and was right three times over.** (i) `shower` and the reviewer independently found the same overclaim: the two anchors are **two different pipelines**, and the run that produced the committed dispatch sheets has a **synthetic hazard surface and synthetic terrain** (`rescue_routing.json` → `provenance.sources`), so 「공개 데이터만으로 만들어져」 was false of the linked object and 「실제 확산면으로 만든 출동 지시서는 아직 없습니다」 is now stated in the lead. (ii) 「가구 단위」 → 「지점 단위」, because `provenance.sources.origins` is `sampled candidates`. (iii) The reviewer walked through the first gate **three times** with the full suite green — inverting the 「읽는 법」 note while keeping its pinned prefix, appending an operational-readiness and precedence sentence, and hedging the dispatch-ordering zero while keeping the pinned 「이긴 적이」. All three are now refused by property assertions (direction / ceiling / hedge ban) and kept verbatim as regression data. Original ask: a lead paragraph opens `README.md`| yes | S | 제출 자료 (논리적 구성), 개발 목적 |
| WFG-214 | **P0** | KCF | **Five judge-facing surfaces call 42 and 91 「완벽한 예보의 상한」, and this repository's own newest document says that is the wrong reading.** Filed by critic #51, 2026-09-09T1418Z, measured at `8506a2d`. `docs/oracle_gap.md` §2 establishes that the forecast-aware arm does **not** plan on truth: it plans on `haz_stack`, a leave-one-fire-out forward simulation, and what makes it an oracle is that the **grader** treats that same array as truth. Its §5 draws the consequence in the student's own words: 42 is 「자기 예측을 그대로 믿었을 때의 값」 rather than 「완벽한 예보의 값」. **Five surfaces still say the other thing, and none of them links to that file:** `README.md:36` 「what a *noiseless* forecast would buy」, `README.md:266-267` 「잡음 없는 완벽한 예보가 사 줄 값의 「상한」」, `README.md:702` 「an upper bound」, `paper/manuscript.md:512` 「what a *noiseless* forecast is worth」, and `docs/auto/JUDGE_QA.md:1399`, which is the `fix-before-next-row` item and is **not** this row. **Do:** on the four non-bank surfaces, replace the mechanism claim with the mechanism (「채점에 쓰이는 장이 곧 계획에 쓰인 장이고, 그 장은 정답이 아니라 이 불을 학습하지 않은 모델의 출력입니다」) and add a link to `docs/oracle_gap.md`. ⚠⚠ **Three constraints that are the row.** (1) The word 「상한」/「upper bound」 itself is **NH-053** and this row may not settle it: write what the mechanism is, and where the word is used, say the question is open and name NH-053. (2) No margin value reaches any of the four surfaces (9, 27, 5, 19, 86) while NH-032, NH-034 and NH-052 are open, and `README.md:266-267` sits inside the `Do NOT edit` block of `CRITIC_LATEST.md`, which permits this rewording and forbids a margin value there. (3) No committed number moves: 42, 91, 9 and 27 are untouched; this row changes what the words around them say the number **is**. **Placement:** filed here in table order, not at position 1, per NH-038 option B as `NEEDS_HUMAN.md` words it, and NH-051. ⚠⚠ **RE-MEASURED BY CRITIC #52 AT `375be25`: the inventory above is stale by two, and the row is smaller than it was filed.** `docs/auto/JUDGE_QA.md` Q36 was corrected **in full** by the 1619Z dev lap, which was critic #51's `fix-before-next-row` item: it states the mechanism, links `docs/oracle_gap.md`, carries no margin value and leaves 「상한」 open with NH-053 named. `paper/manuscript.md:509-512` had its **mechanism** corrected by paper lap 24 (`bd0da54`) - it now says the arm plans on a leave-one-fire-out simulation rather than the fire and cannot be wrong on the field that grades it - and that lap deliberately left the bound clause 「this project's own model is worth less」 word for word, because settling it is NH-053 and not a lap's. **What is left is therefore three README lines and one link:** `README.md:36`, `:266-267` and `:702`, plus the `docs/oracle_gap.md` link the manuscript still lacks (grep: 0 hits in `paper/manuscript.md`). ⚠ Where the manuscript or a README line keeps the bound, the lap adds the clause saying it is argued and not measured and names NH-053; it does not delete the bound and does not assert it. ⚠⚠ **MOVED BELOW WFG-212 BY CRITIC #53, 2026-09-09T2023Z, AND THIS IS THIS LAP'S ONE §3b REORDER.** Critic #52 moved this row ABOVE WFG-212 so that the mechanism was corrected before a lead paragraph was written over the same lines, and **that reason is now discharged**: `README.md:36`, `:266-284` and `:715-723` all state the mechanism and all three link `docs/oracle_gap.md`. What is left of this row is **one link in `paper/manuscript.md`**, and a dev lap cannot advance it: `paper/check_paper.py` reads **8,999** body words against a 9,000 hard fail (re-run here at `ba06467`, exit 0), so the link cannot be added without trading a caveat away, which CHARTER §3 rule 5 forbids; that is **NH-037**, open and due 2026-09-10; and `paper/` is the paper routine's file under CHARTER §12. A `todo` row at table position 1 whose only remaining work is blocked in another routine's file is a row the next dev lap claims and cannot move. WFG-212 can be moved by a dev lap today. Both rows are P0, so no P0 row falls below a non-P0 row. | done(4f3bd85) — **CLOSED BY CRITIC #54, 2026-09-09T2319Z, ON A MEASUREMENT AND NOT ON A REPORT.** Critic #53 re-measured this row down to one remaining item: 「one link in `paper/manuscript.md`」. Paper lap 25 (`4f3bd85`, 2126Z) added it at `paper/manuscript.md:511` — 「it cannot be wrong there (`docs/oracle_gap.md`)」 — and `grep -c oracle_gap paper/manuscript.md` answers **1** at `3eec471`. The paper routine may not touch `docs/auto/` beyond its own report (CHARTER §12), so no lap could close this row, and it stood `todo` **at table position 1** with no work left in it; the next dev lap would have claimed it and found nothing, six sprint days out. ⚠ The bound clause 「this project's own model is worth less」 is unchanged word for word, which is correct: settling the word 「상한」 is **NH-053**, open, and no lap may do it. ⚠ This closure is a status update on a measurement, **not** a §3b reorder; no row moved position. Residue of the earlier residue: none. todo — **three of the four surfaces done(20260909T1817Z-lap), the fourth is budget-blocked.** **Done:** `README.md:36`, `:266-267` and `:702` all state the mechanism in `docs/oracle_gap.md` §2's own terms (the router plans on `haz_stack`, a leave-one-fire-out forward simulation of a fire the model never trained on, and `obs_stack` sits beside it in the same npz scoring nothing, so **the oracle is in the grading**), all three link `docs/oracle_gap.md` with §7 and §6 named, and no committed number moved. The npz keys were opened rather than read off the prose. ⚠ **The row's constraint (1) is now a gate, and closing it required repairing the suite first:** `tests/test_readme_round4.py` held `_UPPER_BOUND = r"42 is\s+an\s+\*{0,2}upper[- ]bound\*{0,2}"` and `_UPPER_BOUND_KO = r"상한"`, so **the tests REQUIRED the README to assert the sentence NH-053 asks the author about** and the only way to stay green was to write that word. Both are widened to the mechanism wording, the old patterns kept verbatim in the comment as the record, and `test_no_block_asserts_the_bound_without_naming_the_open_question` now requires every block using 「상한」/「upper bound」 about 42 or 91 to name **NH-053** in the same block — graded red by removing NH-053 from the Korean block. The word is settled in **neither** direction: deleting it decides NH-053 by omission as surely as asserting it does. **Outstanding, and it is not agent-doable this sprint:** `paper/manuscript.md` still lacks the `docs/oracle_gap.md` link. `paper/check_paper.py` reads **8,999 body words against a 9,000-word hard fail** — one word of margin — so the link cannot be added without trading a caveat away (CHARTER §3 rule 5 forbids that), `paper/` belongs to the paper routine (CHARTER §12), and the budget itself is **NH-037**, open. A lap takes this half only after NH-037 is answered | yes | one lap | B 제출 자료 · 설계와 방법론 · A 제출 자료 |
| WFG-213 | **P0** | science | **Re-grade the three routing arms against the OBSERVED footprint instead of the predicted one, and report what this project's own forecast buys.** Set up by WFG-125 (`docs/oracle_gap.md`, 20260909T1230Z): the forecast-aware arm plans on `haz_stack` and is graded on `haz_stack`, so it carries no forecast error by construction; `obs_stack` — the cumulative FIRMS footprint — is committed in the SAME `data/processed/routing_demo_canonical.npz`, on the SAME 181x156 500 m grid, and nothing scores against it. **Do:** keep every route the three arms choose today, swap `_evaluate_path`'s hazard sequence for one built from `obs_stack`, score all three under the one rule `docs/present_perimeter_arm.md` §2 already insists on, and report the margin whatever it says. Routing only — no refit, no re-acquisition, no new free parameter beyond the one named next (CHARTER §3.11 holds). ⚠⚠ **Three constraints that are the row, not decoration.** (1) `haz_times` are 0/180/360/540/720 min and `obs_times` are 0/333/1005/1480/1812/2403, so reading `obs_stack` between observations needs a rule; that rule is **written down and committed BEFORE the run**, or it is the post-hoc maximum WFG-201 was filed about. (2) `obs_stack` is FIRMS at a 500 m resample with a detection floor (`docs/detection_floor.md`) — an **observation**, not ground truth — so the result is 「what the model buys against what was seen」 and the doc says exactly that. (3) The result lands in `docs/` and `data/processed/` ONLY; no judge-facing surface moves until the author has read it, and no margin reaches `README.md`, `web/finals.html`, `docs/auto/JUDGE_QA.md` or the manuscript while NH-032, NH-034 and NH-052 are open. ⚠ 의성·안동 carries no `obs_stack` (`tests/test_oracle_gap.py` pins this), so this row is 영덕 and the **42** only; the 9 and the 27 cannot be re-graded this way from committed data, and that asymmetry is reported rather than smoothed. **Placement:** filed here in table order, not at position 1, per NH-038 option B as `NEEDS_HUMAN.md` words it and NH-051. | blocked(NH-052) | yes | one lap | B 데이터 수집·분석·해석 · 창의성 · 설계와 방법론 |
| WFG-218 | **P0** | KCF | **일정 answers 0 on `README.md` and 0 on `web/finals.html`, and the row that was going to fix that is `done`.** Filed by critic #52, 2026-09-09T1719Z, measured at `375be25`. WFG-027 shipped `docs/auto/finals/TIMELINE_ROLES.md` and reached **two** of the four judge-facing surfaces: 일정 counts **4** on `docs/auto/JUDGE_QA.md` and **4** on `docs/auto/DEMO_SCRIPT_5MIN.md`, both of which name the file. It counts **0** on `README.md` and **0** on `web/finals.html`. The 1619Z lap disclosed this honestly and called it residue on its row, and then marked the row **`done(20260909T1517Z)`**. ⚠⚠ **That is the finding, and it is about the loop and not the student:** CHARTER §5 puts residue on a row that goes back to `todo`; residue written into a `done` cell is invisible to CHARTER §4 step 3, which reads `todo` rows only, so no dev lap will ever pick it up. This row is that residue, made visible. **Do:** (a) `README.md` gains, in the section a judge reads first, one sentence naming the five phases and linking `docs/auto/finals/TIMELINE_ROLES.md`; (b) `web/finals.html` gains the same through `scripts/finals.template.html`, not by hand-editing the built file. **Constraints:** the screen may only be stamped with a commit already on `origin` (MEMO 2026-09-09), so run `make finals` before the lap's own work commits, and re-point `release/kcf-finals-2026/MANIFEST.json` after staging. ⚠ Do not restate any total from that document on either surface: link to it and let it carry its own as-of stamp, which is what the `fix-before-next-row` item of critic #52 puts there. **Placement:** filed at the end of the P0 block in table order, not at position 1, per NH-038 option B as `NEEDS_HUMAN.md` words it, and NH-051. | done(20260910T0317Z-lap) — **two README blocks and one screen card, and the screen names no phase literally.** `README.md` gains a TL;DR pointer and an entry in 「심사위원이 그 자리에서 열어볼 수 있는 것」 naming the five phases; `web/finals.html` gains a 근거 card built from a new `timeline` payload key that `build_finals.py` reads from `data/processed/timeline_roles/timeline_roles.json`, so the generated surface cannot drift between builds. **No TOTAL from the document reaches either surface** and both say what its status note asks every quoting surface to say: a record reconstructed from `git log`, not a plan. ⚠ The first draft said 「커밋된 산출물」 about the schedule artifact and turned `test_readme_round4_lead.py` red — WFG-222's section scan reads 커밋된 as an output-object existence claim — so the **prose** moved, not the one-lap-old gate. Gate: `tests/test_timeline_reaches_the_judge_surfaces.py`, graded; the independent reviewer's own mutation (card gutted, payload left intact) went from green to red on it | yes | one lap | B 설계와 방법론 · A 설계와 방법론 · 제출 자료 |
| WFG-220 | **P0** | KCF | **The one surface five judges actually look at is the only judge-facing surface that does not carry the objection this repository spent the whole day correcting everywhere else.** Filed by critic #53, 2026-09-09T2023Z, measured at `ba06467`. `web/finals.html` ACT 3 tells the judge, in as many words, that 「모든 출발지에서 화재 무시 직행 경로와 시간 인지 경로를 비교해 네 가지로 분류합니다. 이 판정이 화면의 점 하나하나입니다」 — every dot on the map is that comparison's verdict. `docs/oracle_gap.md` §2 establishes that those verdicts are graded on `haz_stack`, the very array the time-aware arm planned on, while `obs_stack` sits in the same npz and scores nothing (I opened the npz here: `haz_stack` float32 `(5,181,156)`, `obs_stack` uint8 `(6,181,156)`, and no script under `scripts/` or `src/` reads `obs_stack` for grading — only `measure_oracle_gap.py` reads it at all, plus `make_routing_figures.py` for a contour). **Measured on the built screen at this head:** 채점 **0**, 오라클 **0**, `oracle_gap` **0**, `haz_stack` **0**, `obs_stack` **0**, 정답을 미리 **0**, 정답이 아니 **0** — and the same seven counts are **0** in `scripts/finals.template.html`, so this is not a build-staleness artifact. The screen has a dedicated 「알려진 한계」 panel with **ten** `rel()` cards, and it spends them on walking-network coverage, the ERA5 publication lag, OSM 정자 tagging in the refuge layer and the dispatch-ordering negative result — every one of them a smaller caveat than this one. ⚠ **The sharpest form of the defect:** the screen's single occurrence of 「LOFO」 is the evidence card 「확산 모델 · LOFO 교차검증」, which uses the leave-one-fire-out design to show the model is **good**; nothing on the screen tells the judge that the routing comparison is scored against that same LOFO output rather than against the observation committed beside it. **This is not a contradiction like WFG-214's five surfaces — the screen never said the wrong thing — it is an omission**, and it is an omission on the surface with the longest exposure: five judges, about ten minutes each, offline. **Do:** add one `rel()` card to `scripts/finals.template.html`, not to the built file, stating the mechanism in `docs/oracle_gap.md` §2's own terms and citing `docs/oracle_gap.md`. ⚠⚠ **Four constraints, all inherited from WFG-214 and none of them optional.** (1) The card may **not** settle 「상한」/「upper bound」; where the word would appear it says the question is open and names **NH-053**. (2) **No margin value** (9, 27, 5, 19, 86) reaches the screen while NH-032, NH-034 and NH-052 are open. (3) **No committed number moves**: 42, 91, 9 and 27 are untouched; this row changes what the screen says the number **is**. (4) The screen may only be stamped with a commit already on `origin` (MEMO 2026-09-09), so run `make finals` before the lap's own work commits and re-point `release/kcf-finals-2026/MANIFEST.json` **after** staging, as WFG-218 also requires — **the two rows share that mechanic and a lap taking both pays it once.** **Placement:** filed at the end of the P0 block in table order, **not** at position 1, per NH-038 option B as `NEEDS_HUMAN.md` words it, and NH-051. ⚠ This is **not** a request to soften anything and it removes no caveat; it adds one. | done(20260910T0317Z-lap) — **one `rel()` card, and it is FIRST in the 신뢰성과 한계 grid rather than eleventh.** That placement is the lap's own root objection turned into the fix: the grid already spent ten cards on caveats smaller than this one, so appending a twelfth would have closed the row while leaving the defect it names — the biggest omission with the least prominence — exactly where it was. The card states `docs/oracle_gap.md` §2's mechanism in that section's terms (`haz_stack` is planned on AND graded on; `obs_stack` sits in the same npz on the same 500 m grid and grades nothing), names the SYSTEM view's 경로 판정 step so a judge can aim it, leaves 상한 open and names **NH-053**, carries no margin value, and removes no caveat. ⚠ **The first draft of its gate was token-presence and the independent reviewer broke it:** it kept every token and wrote 「채점은 obs_stack 으로 합니다」, the inverse of §2, and six assertions passed. `tests/test_oracle_card_on_the_finals_screen.py` now asserts the direction and refuses the inverse (`_DENIES`), on the built screen as well as the template, and anchors both against `docs/oracle_gap.md` §2 itself | yes | one lap | A 구현 및 유용성 · 제출 자료 (논리적 구성) · B 데이터 수집·분석·해석 |
| WFG-222 | **P0** | KCF | **The front door corrected its one affirmative claim this lap and the identical claim, in the identical words, is still live on the four surfaces that carry it by design — including the screen five judges stand in front of and a T0 card the student says from memory.** Filed by critic #54, 2026-09-09T2319Z, measured at `3eec471`. WFG-212's independent reviewer broke the lead block's first draft on a **conjunction**: two artifacts named side by side are a claim about their conjunction, and the committed dispatch sheets were produced on a **synthetic** hazard surface, while the run where the walk graph and the spread surface are both real produces no dispatch documents. Re-derived here rather than read: `data/processed/rescue_routing.json` → `provenance.sources` gives `walk_network/drive_network/shelters/depots: osm` but **`hazard: synthetic`**, **`terrain: synthetic`**, **`origins: sampled candidates`**; and `outputs/dispatch/README.md` states the clusters are DBSCAN groupings at eps = 500 m rather than 행정리 and that the committed sheets cover 44 of the artifact's 143 dispatch points. `README.md:212-251` now carries all of that. **The four surfaces that do not:** (a) `README.md:391-395`, §5 item ① — 「**가구 단위**의 …판정과 그 도보 경로」 plus 「그 산출물의 실물이 저장소에 커밋돼 있습니다」, no bound, **180 lines below the block that denies it, in the same file**; (b) `web/finals.html:1580` and `scripts/finals.template.html`, CREATIVE item ① — 「가구 단위의 대피 판정」 / "a per-household evacuation verdict" plus 「그 실물이 저장소에 커밋돼 있습니다」; (c) `docs/auto/JUDGE_QA.md:1172`, Q29a, **tier T0** (`JUDGE_QA.md:1385` lists Q29a among the nineteen spoken 「자기 문장으로, 종이 없이」) — 「가구 단위 구조 순서와 보행 경로가 붙습니다」 then 「실물을 열어 드릴 수 있습니다」; (d) `docs/auto/DEMO_SCRIPT_5MIN.md:67` — 「지도가 아니라 **가구 단위 판정과 걸어 나갈 길**입니다」; and (e) `docs/creativity_card.md`, "per-household", which is one of the seven sources of `WFG_printables_20260909T1908Z.pdf`, the 53-page kit inside `release/kcf-finals-2026/`. Counts at this head: 「가구 단위」 is 1 / 1 / 1 / 5 / 1 across `README.md`, `web/finals.html`, `scripts/finals.template.html`, `docs/auto/JUDGE_QA.md`, `docs/auto/DEMO_SCRIPT_5MIN.md`; 「지점 단위」 is **1 in the whole repository** and it is the line added at 2206Z. ⚠⚠ **Three things make this more than stale wording.** (1) On (a), (b) and (c) item ① sits immediately beside item ② 「두 축이 동시에 실제인 실행」 (`docs/real_roads_real_hazard.md`), which is the exact adjacency the reviewer named: read together they say 「real roads + real fire → these committed sheets」, and that execution does not exist here. (2) `web/finals.html:1574-1576` carries the source comment 「the Q29a card, the booth script and this screen must make the same three claims against the same three files」 — three surfaces were deliberately bound to each other and the README was bound to none of them, and the README is the one that was fixed. (3) `tests/test_readme_round4_lead.py`, written this lap to prevent exactly this, scopes `test_the_lead_block_carries_its_own_bounds` to its `lead` fixture (Round-4 heading → 「### 1.」) while its `items` fixture (「### 1.」 → section end, which **contains** §5) asserts only caveat-fragment presence, a ⚠ floor of 10 and the dispatch-ordering hedge: **no assertion binds §5 item ① to the lead's bounds**, so the section can contradict itself on its own headline claim with the suite green. **Do:** bring all five surfaces to what `README.md:212-251` already says — 지점 단위 rather than 가구 단위 where the sentence is about the committed instances, the 「실제 확산면으로 만든 출동 지시서는 아직 없습니다」 bound in the same block as the claim, and 「출발지는 표본 좌표」. Then extend `tests/test_readme_round4_lead.py` so the bound assertion runs over the **section**, not the lead, and add the same property test for the screen template and the bank. ⚠ **Constraints.** (i) Remove no caveat and no link; this row **adds** bounds. (ii) The claim stays an **existence** claim — do not write a performance sentence to compensate. (iii) No margin value (9, 27, 5, 19, 86) reaches any of these surfaces while NH-032, NH-034 and NH-052 are open. (iv) `make finals` may only stamp `web/finals.html` with a commit already on `origin` (WFG-199 / NH-045), and `release/kcf-finals-2026/MANIFEST.json` is re-pointed **after** staging (WFG-152 / WFG-187). (v) `docs/creativity_card.md` and `docs/auto/JUDGE_QA.md` are printed sources, so the kit is rebuilt **last**, once. **Placement:** filed at the **end of the P0 block**, in table order, per NH-038 option B as `NEEDS_HUMAN.md:2016-2019` words it and per NH-051 — **not** at position 1, which is option D's mechanic. ⚠ **Pairs with WFG-218 and WFG-220:** all three need `make finals` on an origin commit and a re-pointed bundle manifest, and that mechanic is paid **once** if they ship together. | done(20260910T0017Z-lap) — **six surfaces, not the five the row named.** All five listed blocks now carry, IN THE SAME BLOCK, 지점 단위 plus 「화재 위험면과 지형은 합성 · 출발지는 표본 좌표」 and 「실제 확산면으로 만든 출동 지시서는 아직 없습니다」; the sixth is `docs/auto/finals/RELATED_WORK_PANEL.md`, a PRINTED panel that said the same thing about this project's own output and that no list held. ⚠ The word swap alone would have left the conjunction, so two things were ADDED: Q29a gains a 없는 것 item placed FIRST that forbids reading items ① and ② as one sentence and gives the answer to 「이 출동 지시서, 진짜 불로 만든 겁니까?」 (critic #54's one unanswerable drill question), and `DEMO_SCRIPT_5MIN.md` takes its bound in a ⚠ block because §1's syllable budget is measured — 가구 → 지점 is two syllables for two, so the spoken segment's rate did not move. **The gate is structural, not a wording gate:** `tests/test_output_object_claim_bounds.py` holds six declared blocks against the property 「a block that says committed instances exist carries the synthetic-hazard and sampled-origin bounds itself」, cannot be satisfied by deleting the claim, and is GRADED — the mutation strips every bound-carrying sentence from each block and requires the check to fail. `tests/test_readme_round4_lead.py` now runs that assertion over the whole Round-4 SECTION rather than its `lead` fixture, which was the defect. The spelling half is **WC-013** in `docs/auto/withdrawn_claims.json`, registered in the same lap (CHARTER §3.5c), and registering it found THREE copies no list named (`KOREAN_OPERATIONAL_SYSTEMS.md`, `PYROGEOGRAPHY.md`, `WEEKLY_2026-W36.md`). Kit rebuilt ONCE at the end (`WFG_printables_20260910T0017Z.pdf`, 55 pp) and `release/kcf-finals-2026/MANIFEST.json` re-pointed after staging. Method, limits and what it does NOT show: `docs/creativity_card.md` §9. ⚠ A measured limit published rather than papered over: `check_withdrawn_claims.py` scans one line at a time, so a spelling that wraps a source line break is invisible to it — §8 of the card is the worked example | yes | M | 제출 자료 (자료의 논리적 구성) 20 · 창의성 20 · 설계와 방법론 20, 두 배점표 모두 |
| WFG-221 | P1 | KCF | **The Round-4 section's own 「읽는 법」 note tells a judge which items favour the project, and that judgement has not been re-read since two more items were added.** `README.md`'s Round-4 note states 「이 절에서 이 프로젝트에 유리한 항목은 4번뿐입니다」 and names 1번 and 3번 as the ones that cut. It was written when the section had four numbered items; it now has five, and it never accounted for 2번 (the withdrawn-claim registry) either. So the note under-counts the items in this project's favour on the surface a judge reads first. ⚠ **This is filed rather than fixed because the fix runs the wrong way.** The count 「네 항목」 in the paragraph above it was a mechanical miscount and the WFG-212 lap corrected it to 「다섯」; 「4번뿐」 is an **evaluative** claim, and every available correction to it makes this project look better, which is exactly what WFG-212 states it is not a licence to do. A lap should not widen its own favourable count on the front door without the author seeing the question. | done(20260909T2118Z-lap) — filed and closed in the same lap, and the reason it could be closed is that a correction was found that runs the **other** way. The note now says that the only **results** in the section are 1번 and 3번, that **both cut against this project**, that 4번 and 5번 are inventories and 2번 a device, and that **이 절에 이 프로젝트에 유리한 결과는 없습니다** — which is stricter than the 「4번뿐」 it replaces, not laxer, so it needs no author decision. `tests/test_readme_round4_lead.py::test_the_reading_note_still_says_the_results_cut_against_this_project` pins the DIRECTION (「깎는」 and 「유리한 결과는 … 없습니다」) rather than the wording, after the reviewer inverted the previous note while keeping its pinned prefix. Original ask: either the note is re-stated to say which items are audits and which are inventories| yes | S | 제출 자료 (논리적 구성) |
| WFG-215 | P1 | science | **`docs/oracle_gap.md` reads a size-ratio series as model behaviour when three of its four slices are graded against the same observation.** Filed by critic #51, 2026-09-09T1418Z, verified against `data/processed/oracle_gap_yeongdeok.json` and not read from the report. `:107-110` says 「Across the artifact the ratios are 0.74 / 1.02 / 1.05 / 1.05: at 3 h the simulation is **26 % under**」 and reads that as a property of the forecast across the horizon. The artifact says the matching is not uniform: `t=180` pairs with the observation at **333 min** (gap **153 min**), `t=360` with the same 333 min (gap **27 min**), `t=540` with the same 333 min again (gap **207 min**), and only `t=720` moves to 1005 min (gap **285 min**). So the observed denominator is **937 cells held constant** across three of the four, and the 3 h slice is compared against a 5.55 h observation: the 26 % under-prediction is substantially the **matching gap**, not forecast bias, and the apparent stability of IoU at 0.36/0.39/0.39/0.40 is three readings against one observation. **Do:** in §4 and §4b, print `time_gap_min` and `obs_time_min` beside every ratio and every IoU (all 15 per-slice keys are already registered, so this is prose, not new measurement), and say plainly which comparisons share an observation. Then state the one conclusion the artifact does support without qualification: at the **only** well-matched pair (27 min), area agrees within 2 % and place agrees on 534 of 1,355 union cells. **Also in this row, one line:** `docs/oracle_gap.md:6` says the registry holds 「(10 keys)」; `docs/NUMBERS.json` holds **25** `og_yeongdeok_*` keys after the reviewer's fix added the per-slice band. ⚠ **P1 and not P0 deliberately:** this is a qualification inside one `docs/` analysis page, not a judge-facing surface, and CHARTER §14b keeps the P0 block for what a judge meets. ⚠ **But if the paper routine quotes the size-ratio series before this row lands, it carries the matching-gap qualification with it**, or it inherits the misreading as established. ⚠⚠ **CRITIC #56, 2026-09-10T0523Z: THE MEASUREMENT IS CONFIRMED AGAINST THE ARTIFACT, AND THE ROW'S OWN STATED REASON FOR BEING P1 IS FALSE AT THIS HEAD. THE MINUTES-SIZED HALF IS THIS LAP'S ONE `fix-before-next-row` ITEM.** Confirmed first, in one process at `9b7d21c` against `data/processed/oracle_gap_yeongdeok.json`, because a critic that inherits a finding is not checking it: the four non-seed slices pair `t=180` with `obs_time_min` **333** (gap 153), `t=360` with **333** (gap 27), `t=540` with **333** again (gap 207), and only `t=720` with **1005** (gap 285); `observed_cells` reads 937, 937, 937, then 987; `predicted_cells` rises monotonically 692, 952, 981, 1036. So three of the four terms of the ratio series are a growing numerator over a **constant denominator**, and 「at 3 h the simulation is 26 % under」 compares a forecast with an observation **153 minutes later than the forecast time**. **What changed in this window is not the defect. It is who meets it.** The row's placement note says 「P1 and not P0 deliberately: this is a qualification inside one `docs/` analysis page, not a judge-facing surface」. Counted at this head, `docs/oracle_gap.md` is the cited evidence anchor from **seven places on four judge-facing surfaces**: `README.md:43`, `:256`, `:331`, `:784`; `docs/auto/JUDGE_QA.md:1419`, which is card **Q36 at tier T0** and sits in one of the seven hashed sources of the printed booth kit; `paper/manuscript.md:512`; and, **new at `c4eb8d2` in this window**, `web/finals.html:2069`, where it anchors the **first** card of the 알려진 한계 panel. Six of the seven send the judge with the same phrase, 「셀 단위로 재어 두었고」. **The `fix-before-next-row` half, and it is minutes:** print `time_gap_min` beside every ratio and every IoU in §4 and §4b, and say in one sentence that three of the four slices are scored against one observation. `og_yeongdeok_t0min_time_gap_min` through `og_yeongdeok_t720min_time_gap_min` are all registered (25 `og_yeongdeok_*` keys counted at this head), so that half writes no unregistered number. ⚠ **The row's own 「Do」 is wrong in one clause and a lap must not follow it literally.** Per-slice `obs_time_min` is **not** registered; only the headline `og_yeongdeok_obs_time_min` is. Printing the per-slice observation times beside each ratio would therefore write numbers CHARTER §3 rule 3 forbids. Registering those five keys additively through `scripts/register_oracle_gap.py`, and the §4b consequences, are the rest of the row and are **not** the preemption. ⚠ **No judge-facing surface repeats the misreading**, checked here rather than assumed: the strings `26 %` and `0.74` count **0** on `README.md`, `docs/auto/JUDGE_QA.md`, `docs/auto/DEMO_SCRIPT_5MIN.md`, `docs/creativity_card.md`, `web/finals.html` and `paper/manuscript.md`. The defect is contained inside the document a judge is sent to, which is why the repair is minutes rather than a rewrite, and why this is a preemption rather than a P0 row. | done(5bcfe11) — BOTH halves, in one lap. Prose: §4 prints the observation and the `time_gap_min` beside every ratio and every IoU and says three of the four share one observation; §4b's IoU series carries its four gaps; line 6's 「10 keys」 is replaced by the prefix and where to count it. Registry: the five `og_yeongdeok_t###min_obs_time_min` keys are registered additively (0 existing values moved). ⚠ THE ROW'S OWN 「Do」 WAS RIGHT AND CRITIC #56'S CORRECTION OF IT WAS WRONG: #56 said the per-slice `obs_time_min` are unregisterable and the first draft of §4 withheld them ON THAT GROUND, in prose, on a judge-facing page. The lap's independent reviewer blocked on it — the headline key existed all along and §4's own result table prints it. Registered rather than argued. The withdrawn reading is `WC-014` | true | yes | minutes | B 데이터 수집·분석·해석 |
| WFG-216 | P1 | infra | **The Q&A bank cannot be given a new card, because its answered cards and its open questions share one id namespace and the open questions have consumed the next five ids.** Measured at this head: `tests/test_judge_qa_bank.py::test_question_numbers_are_unique_and_contiguous` requires the parsed card bases to be exactly `range(1, N+1)`, and they are **1..35**. The 「아직 답이 없는 질문」 table holds **Q36, Q37, Q38, Q39, Q40** as table rows, which `QUESTION_RE` does not parse, so they are invisible to the contiguity check and simultaneously unusable: **Q36 collides with the open oracle question and Q41 breaks contiguity.** WFG-027 hit this and shipped its 일정 answer as a labelled block rather than a card, which works and is worse — a judge drilling the bank by card finds 46 cards and not this one. ⚠ **This is not a request to relax the contiguity gate**, which is what caught the six alphabet-suffixed questions the counts had been missing for four days (WFG-057). **Do:** either promote an open question to a card when it gains a file (the natural transition, and Q36 gained one today), or give open questions their own prefix (`OQ-#`) so the two namespaces stop sharing. **Done when:** a new card can be added by a lap without renumbering, a test asserts the two namespaces are disjoint, and WFG-027's 일정 block has become a card. ⚠ Related but distinct from **WFG-205 / NH-049**, which is about the critic being unable to *write* to the bank at all; this is about the bank being unable to *accept* a card from anyone. | todo | yes | one lap | 제출 자료 · infra |
| WFG-217 | P1 | infra | **The one gate that binds the schedule document to the history it reports measures the clone, not the tree, and gives three answers on three clones of one commit.** Filed by critic #52, 2026-09-09T1719Z, measured at `375be25`. `scripts/build_timeline_roles.py:110-115` resolves every phase anchor with `%h` / `--abbrev-commit`, whose width git derives from the **clone's object count**. ⚠ **The discriminator is the clone's OBJECT COUNT, not its depth, and this row states the sharper version because the first reading was the weaker one.** Four clone states on one tree in the critic #52 session: shallow at 50 refs (exit 2, `shallow`, test skips); deepened to 101, still shallow (same); `--unshallow` to 666 commits with five origin branches, **20,641** packed objects, `%h` **eight** characters, `--check` exit **1**, and `gates.py --mode full` **RED** on that lap's own commit `a53912d0`; then the four unrelated remote-tracking refs deleted and `git gc --prune=now`, **12,052** packed objects with not one commit reachable from `HEAD` changed, `%h` **seven** characters, `--check` exit **0**. The committed artifact holds the seven-character forms (`a88700c`, `4e9dfe3`, `66abf92`, `25f1e142`'s seven-character `25f1e14`, `522f7a7`). `--check` therefore exits **1** with `STALE timeline artifact: phase starts or anchor commits`, the one class its own comment at `:181-186` says must never move, while GitHub `auto-gates` run **294** ran the same check on the same commit at `fetch-depth: 0` and exited 0 (`1851 passed, 63 skipped` in its job log, so the test ran there rather than skipping). ⚠⚠ **The dangerous half is the obvious repair.** A lap meeting the false STALE will rebuild `data/processed/timeline_roles/timeline_roles.json`, which commits eight-character anchors and turns **GitHub** red instead, on a tracked artifact CHARTER §3 rule 2 protects. **Do:** resolve anchors with `%H` and compare by prefix (or store the full sha in the artifact and print the short form only in the document), so the predicate is the commit and not the clone; add a mutation proving a genuinely moved anchor still goes red. **Do NOT** widen `--check`'s growth exemption to cover the anchors, which would make the gate stop testing the settled past. This is the CHARTER §4 failure class arriving through abbreviation width instead of clone depth, and NH-043 asks about the same class. **Placement:** P1 infra, filed in the P1 band in table order; CHARTER §14b holds loop hygiene until R1, R3, R4, R7, R8 and R9 tick, and R3 does not. ⚠ Until it is fixed, no lap rebuilds that artifact. ⚠⚠ **RE-MEASURED BY CRITIC #53, 2026-09-09T2023Z, AND THE ROW IS NARROWER THAN IT WAS FILED — read this before costing it.** I did not deepen this clone; I made a **fresh full clone** of `https://github.com/Sparkxt-0318/wildfireguardian.git` in the session scratchpad, checked out `auto/dev` at `ba06467`, and measured there: `--is-shallow-repository` **false**, **673** commits reachable from `HEAD`, **12,393** packed objects, `%h` **7** characters, and `.auto/venv/bin/python scripts/build_timeline_roles.py --check` exits **0** (`OK — timeline artifact agrees with git log (673 commits, 42 active days)`). I then fetched **all 13** origin branches into that clone (`+refs/heads/*:refs/remotes/origin/*`) and re-ran it: still **12,393** objects, still `%h` **7**, still exit **0**. **So the red state is not produced by a full clone and not produced by extra branches.** It is produced by `git fetch --unshallow` on an already-shallow clone, which leaves a bloated object store (critic #52 measured 20,641 objects there against 12,393 in a fresh clone) and pushes `%h` to 8; `git gc --prune=now` cures it in one command, as critic #52's own fourth row shows. **Blast radius, corrected:** an agent lap that deepens its own sandbox clone. **NOT** a stranger cloning the repository, **not** the student's laptop, **not** GitHub Actions at `fetch-depth: 0` (runs 290-299 all `success`), and **not** `docs/auto/finals/TIMELINE_ROLES.md` §5's reproduce recipe, which I ran end to end in the fresh clone and which passes. The defect is real and the fix (`--abbrev=7`, or resolve anchors with `%H` and compare on a prefix) is still right; what changes is that it blocks nothing judge-facing and needs no urgency. **Consequence for the guardrail:** `DIRECTION.md`'s 「do not rebuild `timeline_roles.json`」 is correct **for a lap that has unshallowed its sandbox clone** and its stated reason 「turns GitHub red」 is the right worry for that case only; a rebuild from a fresh full clone writes 7-character anchors and is safe. Corrected on that page in the same lap. ⚠ **CRITIC #56, 2026-09-10T0523Z: this row acquired a judge-facing consequence at `c4eb8d2`, recorded here rather than filed as a new row.** WFG-218 put a 개발 일정과 역할 evidence card onto `web/finals.html`, and `scripts/build_finals.py`'s `timeline_phases()` renders that card's as-of stamp from `data/processed/timeline_roles/timeline_roles.json` → `last_commit_date`, which is **2026-09-09** at this head. The built screen therefore prints 「2026-09-09 커밋까지 세었습니다」 and renders phase 5 as 「2026-09-03~진행 중」, and this row is the reason no lap may rebuild that artifact from a deepened clone. The finals are **2026-10-24**. So the guardrail now freezes a date a judge reads on the screen five judges stand in front of. **It is still not urgent and the blast radius is still what critic #53 narrowed it to:** a rebuild from a **fresh full clone** writes seven-character anchors and passes, so the artifact can be refreshed before the freeze even if this row has not landed. **What this adds to the row:** the fix belongs before the **2026-10-16 freeze** rather than merely before the IEEE paper, and whichever lap refreshes that artifact for the booth records in `docs/auto/MEMO.md` that it did so from a fresh full clone and not from a deepened one. ⚠⚠ **Measured by critic #60 at `71e95ee` by turning its own gate red, 2026-09-10T1657Z, and the mechanism is now known exactly.** `tests/test_timeline_roles.py::test_the_artifact_still_agrees_with_the_history_when_the_clone_has_one` **FAILED** in this lap's clone with 「STALE timeline artifact: phase starts or anchor commits」, and the same check fails at `71e95ee`, a head GitHub run **338** certified green. The cause is **abbreviation length**: the artifact stores each phase's `anchor_commit` as an abbreviated sha and `scripts/build_timeline_roles.py:113` re-derives it with `git rev-list -n 1 --abbrev-commit`, whose length git picks from the clone's object count. The committed artifact carries **seven** characters (`a88700c`, `4e9dfe3`, `66abf92`, `25f1e14`, `522f7a7`); this clone produced **eight** (`a88700c8`, `4e9dfe39`, …) and the spine comparison at `:183` refused them. ⚠ **The trigger is the critic routine's own obligation.** CHARTER §4 requires `git fetch --unshallow origin` before any ancestry claim; that fetch obeys the remote refspec and brings in **all eleven** other branches, while GitHub's `actions/checkout@v4` at `fetch-depth: 0` fetches **one**. More objects, longer abbreviation. That is critic #52's `b5de13e` finding (「the gate is not sensitive to clone depth, it is sensitive to which other branches are in the clone」) reappearing on a second gate. **Repaired in-lap without touching the tree:** the eleven extra remote-tracking refs were deleted, the reflog expired and `git gc --prune=now` run, keeping the `round2-submitted` tag because `anchor_kind == "tag"` needs it; the abbreviation returned to seven, the clone stayed unshallowed (**726** commits), and `--check` printed 「OK — timeline artifact agrees with git log (726 commits, 43 active days)」. ⚠ **So this row's rebuild MUST come from a SINGLE-BRANCH full clone**, not merely a full one: any clone holding the side branches writes eight-character anchors and turns the gate red for the next lap. **And the check is narrower than it looks:** `:173-177` excludes the totals as 「growth, not drift」, so nothing sees that the artifact stands at **662** total / **513** trailer commits against a tree at **726** / **577**, which are the figures `web/finals.html`'s 개발 일정과 역할 card prints as `counted_through: 2026-09-09` and `docs/auto/JUDGE_QA.md` Q29 · T0 says from memory. ⚠⚠ **CRITIC #63 MET THE IDENTICAL FAILURE AT `0796336`, 2026-09-11T0300Z, AND THE POINT OF THIS NOTE IS THAT IT RECURS EVERY CRITIC LAP.** `gates.py --mode full` came back **RED** on this lap's first run, on the same single test, with the same `STALE timeline artifact: phase starts or anchor commits`. Measured before repairing anything: committed anchors **seven** characters (`a88700c`, `4e9dfe3`, `66abf92`, `25f1e14`, `522f7a7`), this clone's live derivation **eight** (`a88700c8`, `4e9dfe39`, …), packed objects **16,420**. Repaired exactly as critic #60 repaired it: `git update-ref -d` on the eleven extra remote-tracking refs, `git reflog expire --expire=now --all`, `git gc --prune=now`, leaving **13,011** packed objects and the clone still unshallowed at **745** commits; the abbreviation returned to seven and `--check` printed `OK — timeline artifact agrees with git log (745 commits, 44 active days)` **on the identical tree**. ⚠ **Two consecutive critic laps have now spent part of a lap rediscovering and re-repairing this, and nothing in the routine prompt or CHARTER §4 records the repair as a step**, so the third will spend it too. The cheapest interim relief is one sentence beside CHARTER §4's `--unshallow` instruction naming the prune; the real fix is this row. ⚠⚠ **AND THE SILENT HALF GREW.** `--check` prints **OK** at this head while the artifact stands at **662** total commits and `last_commit_date` **2026-09-09**, against a tree at **745** and **2026-09-11**. That is **83 commits and two active days** behind, up from the **64** critic #60 measured one day ago, growing at roughly twenty commits a day. Phase 5's `end` reads `2026-09-09` in the artifact and `2026-09-11` live, and its commit count **417** against **500**. The check cannot see any of it: `:173-177` exempts the totals as growth and the spine comparison covers phase **starts** and anchor commits only, never an open phase's end. ⭕ **What is NOT wrong, and it is worth recording because it is the project behaving well:** `docs/auto/finals/TIMELINE_ROLES.md` discloses this honestly rather than printing a stale number as current. Its §0 at `:28` states 「이 문서의 수는 산출물이 만들어진 트리의 값입니다 — 2026-09-09, 커밋 `89da7d3` 기준」 and 「저장소는 랩마다 자라므로 오늘 다시 세면 더 큰 수가 나옵니다」, and `:48` tells the student to say the unit aloud. So this is a **widening disclosed gap**, not a false statement, and it stays P1. **Deadline unchanged: the 2026-10-16 freeze**, and the refreshing lap still works from a **single-branch full clone**. ⚠⚠ **RE-MEASURED BY CRITIC #64, 2026-09-11T0520Z, AND THE HALF OF THIS ROW THAT KEEPS COSTING CRITIC LAPS IS SELF-INFLICTED.** Two laps (#60 and #63) met the abbreviation failure, and #63 recorded 「CHARTER §4 mandates `--unshallow`」 plus a cure for it (delete the eleven extra remote-tracking refs, `git reflog expire --expire=now --all`, `git gc --prune=now`). **CHARTER §4 does not mandate deepening.** What §4 says is that no reachability or ancestry claim may be **written** until `--is-shallow-repository` answers `false`, with the depth recorded beside the claim; a lap that writes no such claim owes no deepening. Critic #64 therefore did not deepen, and `gates.py --mode full` went green on its **first** run. ⚠ **And the cost of not deepening, stated so this is not read as free:** in a shallow clone `tests/test_timeline_roles.py:234` **SKIPS** with 「shallow clone: the 2026-05-27 first commit is not present, so the artifact cannot be re-derived here」 and `build_timeline_roles.py --check` **declines** with 「shallow clone: the full history is not present」. So a shallow critic sandbox neither fails nor certifies this gate; GitHub `auto-gates` at `fetch-depth: 0` on a single branch is what certifies it. **The rule this row should carry, for every lap:** do not deepen; run the gates shallow; record that this check skipped; read GitHub's own run for it. ⚠ Critic #64 **did not re-measure the commit half of the drift** and does not repeat a figure for it: a shallow clone cannot count total commits and §4 forbids writing one from it. What it did measure, which needs no history, is the date half: the artifact's `last_commit_date` reads `2026-09-09` and `git log -1 --format=%ad` reads `2026-09-11`, so the disclosed gap is **at least two active days** and still disclosed at `TIMELINE_ROLES.md:28`. | todo | yes | under an hour | infra; no rubric row directly |
| WFG-189 | P1 | infra | **The report email's body is retyped by the lap instead of being invoked, and the same lap has now sent the author the literal word PLACEHOLDER three times in two days.** CHARTER §4 step 9 says to pass `.auto/email.html` **verbatim**; a lap that types the tool call by hand can type anything, and on 2026-09-07 (`1a07a0a7ffa5bafb`) and 2026-09-08 (`1a0812e399d60aad`) it typed `PLACEHOLDER`. NH-041 closed the first instance with a **habit** and said in its own words that the habit had no gate behind it; the class recurred one day later, which is the argument. ⚠ **The 09-08 instance could not be repaired**, because the connector returned `requires re-authorization (token expired)` on the next call, so no real email went out at all and the author's only notice of that lap is the repository. **Done when:** the lap does not construct the body — `scripts/auto/report.py --email` (the flag already exists) reads `.auto/email.html` and the report title itself and performs the send, the lap invokes it, and a test asserts the send path refuses a body that is not byte-identical to `.auto/email.html`. **Constraints:** the send path cannot be exercised while the Gmail connector is unauthorised, so the lap that takes this row builds it behind a dry-run that writes what it *would* send and asserts on that file; do not ship an untested live send. ⚠ This is NOT report bookkeeping and §14b's infra block should not swallow it: the broken thing is the author's only report channel, not the loop's own hygiene | todo | true | one lap | R11 · loop hygiene |
| WFG-191 | P1 | infra | **The backlog's own table does not parse as the table CHARTER §5 defines, and it is not one bad row.** Splitting each backlog row on the pipe character, **11 of the 187** `WFG-` rows yield something other than the 9 columns §5 names: WFG-112, WFG-115, WFG-133, WFG-149, WFG-167, WFG-168, WFG-175, WFG-181, WFG-182, WFG-188 and WFG-191, ranging from 11 fields to 19. The cause in every case is an unescaped pipe inside a quoted `grep` alternation or a regex in the title cell. **The worst instance is at the head of the `todo` block:** WFG-188's column 5, which §5 defines as `status`, reads 「예산」 and its real `todo` sits in column 9, with `goal`, `agent_doable`, `effort` and `rubric rows` each shifted by four. These rows also render with shifted columns wherever BACKLOG.md is read as Markdown, which includes the board image the author receives by email. ⚠ **The lap that filed this row reproduced the defect inside the same edit, twice.** Critic #43's own WFG-190 and WFG-191 cells first went in at 13 and 17 fields, from a quoted `grep` alternation and a status regex, and were caught only by re-parsing the file afterwards; the second attempt to repair WFG-191 by string replacement appended text into the wrong fragment because the row was already split, and the row had to be rewritten whole. That is the argument for a gate rather than for a convention: the reader most motivated to hold this invariant broke it three times in five minutes. Second, smaller half of the same class: §5 defines the status as `done(<commit>)` and **32 of the 46** `done()` cells carry a claim stamp such as `done(20260908T1240Z)` rather than a commit id, against 13 that carry a commit, so the column cannot be followed back to the work. **Done when:** every backlog row splits into exactly the 9 columns §5 names, with the pipes inside title cells removed or escaped; a test asserts that shape on every row and asserts the status cell is one of the five forms §5 lists (`todo`, `in-progress(...)`, `done(...)`, `blocked(NH-###)`, `dropped(...)`); the lap reporting it names one malformation the check could NOT catch, per WFG-186's rule; and the `done()` argument convention is either enforced or the charter corrected to match what laps actually write, whichever the lap can prove. **Constraints:** never delete a row (CHARTER §3.7); rewrite the alternations rather than the findings they quote; if the charter is the thing that is wrong about `done()`, say so rather than rewriting 32 historical cells | todo | true | one lap | loop hygiene ⚠⚠ **Critic #45, 2026-09-08T2000Z: the count did not grow (10 malformed rows at `9c24a8b`, the same 10 critic #44 measured: WFG-167, WFG-181, WFG-182, WFG-188, WFG-175, WFG-168, WFG-133, WFG-115, WFG-112, WFG-149, by counting cells on every backlog table line whose first cell is a WFG id). What this lap adds is the MECHANISM behind the OTHER recurring table defect, which four critic laps have now each spent their one reorder on without naming it: **new rows are inserted near the TOP of the table, so a new P1 row is born ABOVE the P0 block.** Measured at `9c24a8b`: WFG-189, WFG-191, WFG-192 sit at table lines 49-51 and WFG-196, WFG-197, WFG-198 (all filed in the last 24 h, all P1) at lines 53-55, while six P0 `todo` rows sit BELOW them at lines 85, 86, 87, 88, 114, 117 and one at 184. So the head of the `todo` block returns to a P1 infra row the moment the P0 rows above it close, no matter how many times a critic moves one row up: **critics #40, #42 and #44 were each fixing an instance of an insertion rule.** The gate this row asks for should therefore check ORDER (no non-P0 `todo` row above a P0 `todo` row) as well as SHAPE, or the shape gate will pass on a table that still points a fresh lap at work §14b forbids it to take. Not a new row: WFG-183 and WFG-191 already carry the class and a fourth would be the noise this finding is about. ⚠⚠ **CRITIC #46, 2026-09-08T2340Z: THE ORDER DEFECT IS CONFIRMED AND THE WINDOW'S OWN PROSE DENIES IT.** Counted at `cb9fcc3` by parsing every table line whose first cell is a `WFG-` id: **63 non-P0 `todo` rows sit above the last P0 `todo` row**, and the three rows filed by the 2026-09-08T1817Z research lap sit near the top of the P1 block, above seven P0 `todo` rows and above fifteen older P1 rows. `docs/auto/research/WEEKLY_2026-W37.md` §7 states 「the two new rows enter at the end of the P1 block, which reorders nothing」. **That is false as measured**, and it is this lap's one `factchk` hit on new prose in the window's diff. Filed here rather than as a new row because this row and WFG-183 already carry the class. **What it adds to the `Done when`:** the gate must check **order** (no non-P0 `todo` row above a P0 `todo` row) as well as **shape**, and it must be runnable on demand, so a lap can *check* a position instead of asserting one from memory — which is precisely the step the research lap skipped. ⚠⚠ **RE-MEASURED BY CRITIC #63 AT `0796336`, 2026-09-11T0300Z, WITH THE RULE STATED SO THE NEXT LAP CAN CHECK IT RATHER THAN INHERIT IT.** Rule: split each row on pipes **not preceded by a backslash**, because `\|` is a Markdown escape and renders as a literal pipe rather than a cell boundary; count rows whose cell count differs from the header's ten. Answer at this head: **11 malformed of 240 rows**, and the list has MOVED rather than merely persisted. It is now WFG-181, WFG-182, WFG-188, WFG-215, WFG-197, WFG-175, WFG-168, WFG-115, **WFG-243**, WFG-107 and WFG-112. ⚠ **WFG-133 and WFG-149 have left the list and WFG-243 has joined it, which means the defect is still being INTRODUCED, not merely inherited** — WFG-243 was filed by critic #62 on 2026-09-10 and closed by the dev lap on 2026-09-11, one day old and already unparseable. **Three of the eleven are `**P0**`** (WFG-181, WFG-182, WFG-188, plus WFG-243 which has closed), and for six of the eleven the cell in the status position is not a status at all: WFG-181 reads `프라이버시`, WFG-188 reads `예산`, WFG-175 reads `title-derivable`, WFG-168 reads `않습니다`, WFG-115 reads `grep -c 41498efbf0679276c140b3cbfc0819e5`, and WFG-112 reads a fragment of a Python regex. ⚠⚠ **WFG-182's status cell is an awk program** — `' '/^\| 2026-09-/ && NF>=15 {gsub(/[ *]/` — which is a lap's own board-counting command quoted into the board it counts. That is this row's argument in one cell. **And this is where the two published board counts come from.** Critic #62 published 「238 rows, 98 P0, 18 P0 todo, **106 P1 todo**」 for its own post-lap tree. Applied at `f93af93` and then adjusted for that lap's own two rows, three of those four reconcile **exactly** against this rule (236+2 = 238 rows, 96+2 = 98 P0, 16+2 = 18 P0 todo), so critic #62's arithmetic and its 「after this lap」 labelling were both correct. The fourth does not: this rule answers **111** P1 todo where critic #62 published 106, and **four of the difference are rows this rule counts as P1 because their shifted cells put `P1` in the priority position when it is not their priority**. ⚠ **So the remedy is not a better count and it is not a rule published beside a number.** Two careful laps disagreed by five on a figure neither could be wrong about, because the file they both parsed does not have the shape either rule assumes. **The gate this row already asks for is the whole answer; nothing else in this class is worth another lap's measurement until it lands.** **Placement unchanged and deliberately so:** CHARTER §14b files loop hygiene as P1 until R1, R3, R4, R7, R8 and R9 tick, and R3 does not. ⚠ **Said plainly for the author, because the letter of the rule and the cost point different ways with four days left in the sprint:** the board is the first file every dev lap reads to choose its work, and three P0 rows currently display their status in the wrong column. §14b keeps this row P1 and that is the author's standing steer, obeyed here; the risk it defers is that a P0 row gets skipped or claimed twice. |
| WFG-246 | P1 | infra | **The direction page published 「ZERO reorders」 for a lap that reordered nine rows, and the number is the part that is wrong rather than the decision.** Filed by the dev lap of 2026-09-11T0025Z, re-derived at `a823c33` by parsing all 238 table rows in document order. `docs/auto/DIRECTION.md` names WFG-235 + WFG-240 + WFG-241 + WFG-243 as the first lap and adds 「that bundle is already table order, so no row moved」, and `docs/auto/CRITIC_LATEST.md` records 「**ZERO** §3b reorders, the sixth consecutive critic lap to spend none」. Counted here: **nine P0 `todo` rows sit ABOVE WFG-235** — WFG-007, WFG-117, WFG-129, WFG-121, WFG-106, WFG-036, WFG-101, WFG-119, WFG-230 — every one `agent_doable: true` and none `blocked`. **The naming is legitimate** (CHARTER §14 lets the page name the next row when it gives a reason, and the shared-rebuild reason is a good one, which is why this lap took the bundle); what is false is that it was free. ⚠ **Why this is not a nitpick and not WFG-107 again.** §14 caps the critic at ONE reorder per lap precisely so that the order a judge-facing sprint runs in is auditable, and a reorder recorded as zero spends the cap without logging it — six consecutive laps of 「ZERO, measured」 now cannot be checked against anything. WFG-107 is hand-written censuses drifting; this is a **ledger entry contradicted by the table it describes**. **Done when:** the reorder count is derived from the table rather than asserted — one helper that, given the page's named first row, reports how many higher-priority `todo` agent-doable rows it was named above, and a test that fails when DIRECTION claims ZERO while that helper returns non-zero. A dev lap must not rewrite DIRECTION (§14: 「Dev never writes it」), so the remedy is the helper and the test, and the page is the critic's to correct. ⚠ Moves no committed number and reorders nothing itself | todo | true | one lap | infra · 심사 대비 신뢰성 |
| WFG-192 | P1 | infra | **Nothing in this loop measures a standing claim getting weaker on its way to the front door, and critic #43 found it happening twice in one paragraph.** `docs/auto/withdrawn_claims.json` plus `scripts/check_withdrawn_claims.py` catch a **withdrawn** claim being copied forward. There is no equivalent for a **standing** unfavourable result being restated more softly for a wider audience, and the direction of travel is one way: on the present-perimeter arm, `docs/present_perimeter_arm.md:103-104` says **86 of 91**, `paper/manuscript.md:23` says 「recovers most」, and `README.md:223-225` says 「상당 부분」; on the buffer's freedom, `paper/manuscript.md:493` states it outright and `README.md:220-225` does not mention it at all. Both were found by hand and neither is bound by any test. **Done when:** for each unfavourable result the README's Round-4 section names (present-perimeter recovery, dispatch ordering, detection floor), the repository holds a committed pairing of the README's sentence against the source document's own strongest sentence, and a test fails when the source carries a quantifier or a caveat the README's sentence drops; the lap reporting it names one weakening the check could **not** catch (WFG-186's rule). **Constraints:** this is a gate over prose registers, not a rewriter; it must not edit the sentences it grades. §14b holds this behind R3 | todo | true | one lap | 제출 자료 · loop hygiene |
| WFG-195 | P1 | infra | **SETTLED: the 403 was transient, not a full artifact store.** This row asked which of two causes made `FinalizeArtifact` refuse run 253, and the answer came from a run that needed no fix to produce it. **Run 254** (id 34257393284, `be05c1c`, 17:29Z) uploaded `finals-acts-be05c1c` at **2,238,011 B** (17:30:03Z) and `gates-be05c1c` at **9,792 B** (17:36:09Z), both `success`, one hour after run 253's 403 and before the guard existed. A store at its limit does not accept another 2.25 MB, so the quota reading is dead and the ~555 MB estimate that motivated it was wrong. Critic #44's reading was right and this routine's was not. NH-047 is closed with this row; no author decision was ever raised, which is the outcome DIRECTION's rule was protecting. ⚠ The guard STAYS regardless (see the constraint): what run 253 proved is that an archival upload CAN fail for reasons outside this repository, and a green gate must not depend on one. **Constraints:** do not remove the `continue-on-error` guard; the loop may not open the billing page or spend money (CHARTER 3.6) | done(b2cda36) | true | minutes | loop hygiene |
| WFG-196 | P1 | infra | **`test_the_four_acts_advance_in_a_real_browser` now skips when Chromium will not start, and a skip that becomes permanent would take R1's evidence with it on both surfaces at once.** `auto-gates` #255 (`b7c1837`) went red on that test with `no page target on port 51449 within 30.0s (Connection refused)` while the sibling `finals-acts` job drove the same screen to four screenshots ninety seconds earlier on its own runner, and while the sandbox and GitHub's own runs at `0cca093` and `be05c1c` all passed it. The ci-red lap made a launch failure skip rather than fail, matching the driver's own stated rule that a gate going red at random is worse than no gate, and kept every other CDPError hard. **That trade is not free and this row is the other half of it:** a Chromium that stops starting permanently would now skip here in silence, and the `finals-acts` job carries `if-no-files-found: warn` with a comment saying an empty upload is a warning rather than a red job, so readiness line R1 could lose its evidence on the test surface and the artifact surface simultaneously and no gate would say so. **Done when:** the driver retries the browser launch (kill the process, fresh profile directory, one more attempt) and FAILS rather than skips when the second attempt also gets no page target, so a transient startup is absorbed without the permanent case going quiet; and something fails when a full `auto-gates` run produces neither a passing browser test nor four uploaded screenshots, which is the condition R1 actually cares about. **Constraints:** do not simply raise the 30 s timeout, which is a guess at a cause not measured -- the observed failure was `Connection refused` for the whole window, which reads as a browser that died at startup rather than one that was slow; do not make the test hard again without the retry, or #255 recurs; the screenshots are R1 evidence so their retention stays the author's call | todo | true | one lap | loop hygiene · R1 ⚠⚠ **ci-red 2026-09-08T2209Z, run 260 (`7eeccab`): the first half is done(this commit) and the CAUSE is now measured rather than attributed to the runner.** This row and the commit that filed it both read the failure as 「a browser that died at startup」. It did not die: the runner's own cleanup line prints `Terminate orphan process: pid (2032) (chrome)`, so Chromium was alive for the whole window. `_free_port()` bound an ephemeral port, closed it, and handed the number to Chromium, which bound it again some hundreds of milliseconds later; anything on the machine can take it in that gap, and Chromium does not fail when it cannot bind -- it logs `bind() failed: Address already in use (98)`, runs on with no DevTools endpoint, and the driver polls a port nobody listens on. Reproduced deliberately in the sandbox by holding the port across the launch, which prints that exact line and reproduces `Connection refused` for the full window. Fixed at the root: Chromium is asked for port 0, picks its own port, and the driver reads it back from `DevToolsActivePort`, so the race cannot occur; the launch also retries once, keeps Chromium's stderr instead of discarding it, and notices a dead process instead of waiting out the timeout. **Still open, and the reason this row stays `todo`:** the second half of its done-when, something that fails when a full `auto-gates` run yields neither a passing browser test nor four uploaded screenshots. The test-surface half of the silent-skip worry is narrower than it was, because the skip now discriminates by exception TYPE (`BrowserLaunchError`, raised at one place by the launcher) rather than by matching the words 「no page target on port」 in any message, and it is reached only after both attempts fail. |
| WFG-197 | P1 | KCF | **Korea publishes the lead times at which the elderly-evacuation decision is made, and this project's horizon contains them — but no surface says so.** 국립산림과학원's 2026-02-12 strategy sets a 「준비(Ready)-실행 대기(Set)-즉시 실행(Go)」 ladder in which 고령자 등 안전 취약계층 begin preemptive evacuation at **화선 도달 8시간 전** and are to have moved by **5시간 전** (헤럴드경제 2026-02-12, <https://biz.heraldcorp.com/article/10675702>, opened by the research lap 2026-09-08; full record and scope in `docs/auto/knowledge/KOREAN_OPERATIONAL_SYSTEMS.md` §Update 2026-09-08). `docs/MODEL_CARD.md:344` puts this project's horizon at **3–12 h**, evaluated at 3/6/9/12 h, so 5 h and 8 h lie strictly inside it. Today the only answer to 「왜 3–12시간입니까?」 is the FIRMS overpass cadence, which is true and sounds like an excuse. **Do:** (a) a `docs/auto/JUDGE_QA.md` card for 「왜 3–12시간 예보입니까?」 answering with the 5–8 h decision points; (b) the matching operational-relevance line in the manuscript's Discussion (IEEE_PLAN G10). **Three constraints that are the row, not decoration:** the card MUST carry the ≈ 0.40 forward-sim envelope figure (`docs/MODEL_CARD.md:348`) in the same card, so the honest limit travels with the framing; it MUST state the two-clocks limit (「화선 도달 8시간 전」 runs backwards from arrival at a place, our horizon runs forward from forecast issue, and they coincide only under instant detection — see `docs/detection_floor.md`); and it MUST NOT assert or imply sufficiency at 5–8 h, which nothing here measures. The 8 h/5 h/76 %/88 %/30 % figures stay in the knowledge note with agency, date and scope (CHARTER §3 rule 5b, §13) and enter no card, no README, no manuscript and no `docs/NUMBERS.json`. Source is a **press restatement of an agency plan**, not a primary document (NH-039 is the primary source and is still unreachable); say 「정책이 상정하는 시점」, never 「검증된 기준」 ⚠ **RESEARCH LAP 2026-09-10: THIS CONSTRAINT IS RELAXED, AND ONLY THIS ONE.** The agency's own briefing has now been opened and quoted verbatim — 대한민국 정책브리핑 부처 브리핑, 「첨단 과학 기반 산불 전방위 대응 체계 가동」, 산림청 국립산림과학원, 2026-02-12, <https://www.korea.kr/briefing/policyBriefingView.do?newsId=156744521> — carrying 「'준비-실행 대기-즉시 실행' 체계의 주민 대피 가이드라인을 확립하였습니다」, 「8시간 산불 확산 예측정보를 바탕으로 해서 실행 대기 상태에 돌입합니다」, 「고령자 등 안전 취약계층을 중심으로 선제적 대피를 완료하게 됩니다」 and 「불길이 도달하기 5시간 전인 즉시 실행 단계에서는 대피 대상 주민이 지체 없이 안전한 곳으로 대피하도록 하였습니다」. The card may therefore cite the **agency's own briefing on the government portal** rather than a newspaper; the 사용자 가이드 (NH-039) is still unreachable and is no longer needed for this row. **Every other constraint above is unchanged and still binding**: the ≈ 0.40 envelope figure in the same card, the two-clocks limit, no sufficiency claim, and no figure into any card, README, manuscript or `docs/NUMBERS.json`. ⚠ **Do NOT write the 「위험 구역」 / 「잠재적 위험 구역」 zoning framing.** A search summary described the doctrine as a spatial zoning by predicted arrival time; that wording is **not on the page this lap opened** and is recorded UNVERIFIED in `KOREAN_OPERATIONAL_SYSTEMS.md` §Update 2026-09-10. Confirm at a document or do not write it | todo | yes | hours | 제출 자료 · 창의성 · B 데이터 수집·분석·해석 ⚠ **CRITIC #46, 2026-09-08T2340Z: MEASURED AND DELIBERATELY NOT PROMOTED, so the next critic lap need not re-derive it.** The judge drill at `cb9fcc3`: a case-insensitive count of `Ready-Set-Go`, `화선` or `8시간` answers **0** on `docs/auto/JUDGE_QA.md`, **0** on `web/finals.html` and **0** on `docs/auto/DEMO_SCRIPT_5MIN.md`, and **none of the bank's 46 cards asks why the forecast horizon is 3 to 12 hours**. Q9 and Q10 answer the neighbouring question with ERA5's ~5-day lag and the satellite cadence, which is the *data* answer; the research lap that filed this row called that 「a technical excuse」 to the disaster-response judge, and **one of the five judges is a public-sector disaster-response official** whose own agency announced the doctrine on 2026-02-12. The answer is already written and sourced in `docs/auto/knowledge/KOREAN_OPERATIONAL_SYSTEMS.md` §Update 2026-09-08, so this costs prose only, no run and no new source. **Why it stays P1 this lap:** critic #46 filed WFG-201 as P0 at position 1, and adding a second P0 row in one lap to a block this critic is simultaneously calling unservable is the behaviour DIRECTION.md has criticised for three days. **This row is the NEXT critic lap's promotion candidate, ahead of WFG-027.** ⚠ The row's own boundaries are load-bearing and unchanged: the card carries the ≈ 0.40 envelope figure and the two-clocks limit (「화선 도달」 is arrival at a place, this project's horizon is time from forecast issue, and `docs/detection_floor.md` exists to say detection is not instant) in the same card; it states only 「정책이 대피 결정을 상정하는 시점」 and never 「우리 예보가 그 시점에서 충분하다」; and **no** number from the knowledge note reaches `README.md`, the manuscript or `docs/NUMBERS.json` (CHARTER §13, §3 rule 5b). ⚠ **CRITIC #49, 2026-09-09T0820Z: RE-MEASURED AT `7f914fd` AND AGAIN NOT PROMOTED, and this lap says why rather than repeating the measurement.** `grep -ciE 'Ready.?Set.?Go|화선|8시간'` answers **0** on `docs/auto/JUDGE_QA.md`, **0** on `web/finals.html` and **0** on `docs/auto/DEMO_SCRIPT_5MIN.md`; on `README.md` it answers 5 and **none of the five is this anchor** — they are 화선 used as a modelling term at `:102`, `:427`, `:432`, `:523`, `:626`. So the gap is unchanged and real. Critic #48 pre-registered this row as the next promotion, which would have moved **WFG-125** to fourth place for a third consecutive critic lap. **This lap declines that**, because the deferral of the one row that measures what the model buys is now a larger direction problem than this row is a gap (DIRECTION.md, NH-038). Promote it when a science row has run, or when a judge-facing surface loses its 3–12 h horizon answer entirely. |
| WFG-198 | P1 | IEEE | **The route-existence result is stated as this project's own construction, and it is an instance of a published method — saying so makes the claim smaller and far harder to attack.** Opanasopit & Louis (2026-08-06, *Frontiers in Built Environment*, 10.3389/fbuil.2026.1856100, <https://doi.org/10.3389/fbuil.2026.1856100>, opened by the research lap 2026-09-08) identify **isolated communities** by connected-component analysis on the surviving road graph and measure population-level accessibility by a Census-Block road-length ratio; on Clatsop County, Oregon they report **70 isolated communities** and access falling from **38,100 to 11,900** residents under combined hazards. Same graph object, same question, different hazard. Checked this run: `docs/auto/knowledge/ROUTING_FUNDAMENTALS.md` §2 and §5 carry evacuation and shelter-location literature and **no** isolation/accessibility literature, so nothing situates our claim. **Do:** (a) one related-work line in `paper/manuscript.md` (IEEE_PLAN G9) and one `references.bib` entry, opened at its DOI and marked `verified` by the lap that adds it (CHARTER §12); (b) a `docs/auto/JUDGE_QA.md` card for 「기존 접근성 분석과 뭐가 다릅니까」. **The sentence both deliverables carry:** they delete edges under a *static post-event hazard footprint* and ask who is disconnected; this project deletes an edge's usability *as a function of when a walker would traverse it* under a *moving forecast* field, and asks who can still get out in time — static severance versus time-dependent severance. **The boundary must be in the same sentence:** different hazard family (earthquake/tsunami/flood), different country, different unit (Census Blocks, vehicle roads, a road-length ratio rather than a routed arrival time), so it validates the method class and **never** any fire number here. No number from it is registered | todo | yes | hours | 연구 · B 데이터 수집·분석·해석, 과학적 기여 |
| WFG-186 | P1 | infra | **Every gate this loop grades is graded 「N for N」 against the mutations its own author imagined, and the next reader has found a hole three laps running.** The 0655Z lap graded `tests/test_responsibility_and_privacy_cards.py` **5 for 5** and shipped it as trustworthy; its independent reviewer reproduced all five, invented three of its own, and **M7 found 609 of 642 tracked sheets unguarded**. The widened gate was graded again and called trustworthy; this critic lap then found its scope assertion is satisfied by a string sitting in a different claim's 근거 block (WFG-185). The same shape ran at `82ec346`/`4f887c7`: nine tests, graded, and the reviewer still had to rewrite the line the student speaks (WFG-178). **The defect is in the reporting, not the testing.** A mutation score is published as a fraction whose denominator the grading lap chooses, so 「5 for 5」 reads to the author as coverage and actually means 「every mutation I thought of」, which is 100 % by construction and cannot fall. **Done when:** `docs/auto/CHARTER.md` §4 step 5 requires a lap reporting a mutation score to write down, in the same report, **one mutation it could not make the gate catch**, or to state that it tried to find one and say what it tried; and `tests/` holds a check that a report whose text contains a mutation score also contains that clause, so the omission goes red instead of being found by the next reader. CHARTER §14b holds this behind R3 and R8 | todo | true | one lap | R11 · loop hygiene |
| WFG-179 | P1 | infra | **Readiness line R3 names a command that nothing in this project runs, and has since the checklist was written.** `docs/auto/KCF_READINESS.md:1118` reads 「`make all-checks` green on a clean clone (CI) and on the booth laptop recipe in `docs/auto/finals/BOOTH_SETUP.md`」. `Makefile:215` defines `all-checks: verify baseline-verify snapshot-verify env-check test`, and `baseline-verify` is a **hard** prerequisite there; measured here at `1282198` it exits **2** (`.auto/baseline-verify.log`, the two acquisition manifests are absent), which CHARTER §3d records as the documented and permanent state of every clone that is not the author's laptop. So `make all-checks` cannot go green on a clean clone by construction. `.github/workflows/auto-gates.yml:31` runs `.auto/venv/bin/python scripts/auto/gates.py --mode full`, which records `baseline-verify` as a soft WARN. **R3's CI half is therefore graded by a different command than the line names, and no lap has ever run the named one.** This is checklist mechanics, not a judge-facing surface, so CHARTER §14b puts it at P1 behind the readiness block; it is filed rather than fixed because changing a readiness line's wording is the author's (NH-046). **Done when:** R3 states which command grades it and on which machine, or `all-checks` gains the same soft-WARN treatment `gates.py` gives `baseline-verify` when the manifests are absent; whichever the author picks in NH-046. **Constraints:** do not tick R3 on `gates.py` while the line says `all-checks`; do not weaken `baseline-verify` on the laptop, where CHARTER §3d makes it hard on purpose ⚠⚠ **STATUS CHANGED todo -> blocked(NH-046) BY CRITIC #43, 2026-09-08T1429Z, as a step-5 update and not a reorder.** This row's own *Done when* ends 「whichever the author picks in NH-046」 and NH-046's text says a lap that edits a readiness line to match what the loop already does is a lap grading its own homework. A `todo` row with `agent_doable: true` that no lap is permitted to finish is CHARTER §5b's lock-with-no-key one column over. With R8 ticked at `dee1bc1`, **R3 is the last of CHARTER §14b's six lines**, so this row and NH-046 are what hold eight P1 infra rows shut. | blocked(NH-046) | true once NH-046 is answered | minutes | R3 · B 데이터 수집·분석·해석 |
| WFG-180 | P1 | infra | **Four skip messages say SRTM for a file that is not the SRTM tile, on the one output a judge would use to check WFG-139's card.** `tests/test_slope_digraph.py:145`, `:160`, `:174` and `:209` carry `reason="SRTM DEM absent"`, and their predicate is `DEM.exists()` where `tests/test_slope_digraph.py:24` sets `DEM = REPO / "data/raw/firms_data/yeongdeok_2025_dem.tif"`, a GeoTIFF from the laptop bundle and not `data/raw/dem/srtm/N36E129.hgt`. Measured here on the cold run at `1282198`: `pytest -rs` prints **four** lines reading 「SRTM DEM absent」 that are not tile-gated, beside the seven that are. A judge who follows Q40 to `pytest -rs` counts eleven SRTM-looking skips against a card that says six, and the four that make the arithmetic wrong are a naming defect, not a coverage one. **Done when:** the four reasons name the artifact they gate on (`yeongdeok_2025_dem.tif`, laptop bundle), and a test asserts that a skip reason naming SRTM gates on the SRTM tile path. **Constraints:** message text only, no predicate changes, no artifact touched. ⚠⚠ **UPDATED BY CRITIC #40, 2026-09-08T0524Z: this row is NOT 「minutes」 and taking it as four string edits ships a red tree and a stale kit.** The 2026-09-08T0407Z lap wrote 「Four string edits, zero risk」 and `tests/test_tile_gated_skip_count.py` landed in the same lap, which changes the cost. That gate derives a second set, `srtm_named_only` — tests whose `skipif` says SRTM while gating on something else — and **that set IS this defect**: renaming the four reasons takes it from **4 to 0** and the derived `both` total from **11 to 7**. Read at `47e48b5`, four rows of its `SURFACES` tuple then go red, and all four are judge-facing prose, not test code: `docs/auto/JUDGE_QA.md` 「나머지 \*\*4개\*\*는 이름만 SRTM」 and 「「SRTM」이 들어간 스킵 줄이 \*\*11개\*\*」 (both inside Q40, a T1 card with a spoken line), and `docs/clean_clone_gates.md` 「\*\*4\*\* more skips say SRTM」 and 「prints \*\*11\*\* SRTM-looking skips」. `docs/auto/JUDGE_QA.md` is a printables `SOURCES` file, so the same lap must rebuild the kit at a new stamp and re-point `release/kcf-finals-2026/MANIFEST.json` (WFG-152) or `tests/test_printables.py::test_the_newest_printable_is_not_stale_against_the_tree` goes red too. **Nobody wrote this coupling down:** the Q40 card tells a judge that eleven is what they will see and names WFG-180 as a defect this project will fix, and the gate now pins the card to the defect's continued existence, so the fix invalidates the printed booth PDF. **Revised done-when:** the four reasons renamed **and** the four bound sentences corrected to 7 / 0 (or reworded and re-anchored in `SURFACES` in the same edit, since a regex that matches nothing also fails) **and** the kit rebuilt **and** the bundle re-pointed **and** `gates.py --mode full` re-run after the rebuild. **Revised effort: one lap** | todo | true | one lap | B 데이터 수집·분석·해석 · R3 |
| WFG-183 | P1 | infra | **A dev report told the author that three commit ids resolve on `auto/dev`, and two of them exist nowhere.** `docs/auto/reports/2026-09-08T0407Z-dev.md` writes 「`report.py` stamped the tables at **`b1af739`** … This report ships one commit later, in **`ebba1aa`** … **All three ids resolve on `auto/dev`**」. Checked here by two channels rather than one, because this clone is shallow (`git rev-parse --is-shallow-repository` = `true`, depth **50**) and a shallow clone is not evidence of absence: `git cat-file -t b1af739` and `git cat-file -t ebba1aa` both answer `fatal: Not a valid object name`, **and the GitHub API answers `No commit found for SHA: b1af739`** on a server that holds the whole branch. Both ids are the lap's own pre-rebase commits, which the rebase onto the concurrent paper lap rewrote to `82ec346` and `4f887c7`. The same annotation says so **four paragraphs later** (「`docs/auto/STATE.json`'s `last_report_commit` names `b1af739`, which the rebase rewrote to `82ec346`」), so the report contradicts itself and the false half is the one that reads as a certification. The email the author received carries `auto/dev @ b1af739` in its header line, an id they cannot open on GitHub. **The lap's work is not in question:** the tree it pushed is `47e48b5`, `gates.py --mode full` is ALL GREEN there (re-run here, exit 0, `1722 passed, 63 skipped, 2 xfailed`), and `auto-gates` run **231** at that sha is `success`. This is the report's bookkeeping, not its content. **Done when:** the head annotation is written **after** the rebase from `git rev-parse HEAD` rather than from `report.py`'s pre-rebase stamps, or a check in `report.py`/`gates.py` refuses to emit a report whose named ids do not `cat-file -t`; and `docs/auto/STATE.json`'s `last_report_commit` is re-derived post-rebase instead of being annotated as unfixable. CHARTER §14b holds this behind R3 and R8 as commit-id bookkeeping | todo | true | one lap | R11 · loop hygiene |
| WFG-184 | P1 | infra | **Three consecutive critic laps got the scorecard append wrong, and the series the charter asks the critic to keep is now out of order.** `docs/auto/SCORECARD.md` carries three tables: the combined series (header at `:24`) and the per-track detail tables (`:69` Track B, `:111` Track A). Read here at `47e48b5`: critic **#37** appended to neither detail table (critic #38 said so and backfilled it); critic #38's backfilled rows landed **mid-table** — `:80` puts the 2026-09-07 `1bca8ed` row directly after the 2026-09-04 `12bf2d9` row at `:79`, and `:122` does the same on Track A — so both detail tables now run 09-05, 09-03, 09-03, 09-03, 09-04 … 09-04, **09-07**, 09-04 …; and critic **#39** appended to the combined series only, leaving both detail tables ending at `64f015b` (critic #37's head) while the series runs two laps further. **Why this is a finding and not tidying:** the scorecard is the only place the project measures itself against the rubric over time, the charter's instruction is 「append a dated row; never rewrite history」, and a series a reader cannot read in order stops being evidence. **Done when:** `tests/` holds a check that each of the three tables is in non-decreasing date order and that every `docs/auto/reports/*-critic.md` has a row in all three, so a lap that skips one goes red instead of being found two laps later. ⚠ **Do not reorder the existing rows to make the check pass** — annotate in place, the way this project handles every other superseded record. CHARTER §14b holds this behind R3 and R8 | todo | true | one lap | R11 · loop hygiene |
| WFG-173 | **P0** | KCF | ⚠⚠ **ADDED AT POSITION 1 BY CRITIC #38, 2026-09-07T2319Z, AND IT IS THIS LAP'S ONE `fix-before-next-row` ITEM (CHARTER §14b: minutes, and the finals screen is named in that rule's own list).** **The judged screen's build stamp is sitting on the staleness gate's exact limit, so the next commit anybody pushes turns `gates.py --mode full` red.** Measured here on the routine's DEFAULT clone before any deepening (`is-shallow-repository` = `true`, `rev-list --count HEAD` = **50**): `web/finals.html:434` carries `"git":"7308b06"`, and `git rev-list --count 7308b06..HEAD` answers **30** at `1bca8ed`. `tests/test_finals_screen.py:540` sets `STAMP_MAX_COMMITS_BEHIND = 30` and `:732` asserts `behind <= STAMP_MAX_COMMITS_BEHIND`, so the gate is green today **at** the limit and red at limit+1. Critic #37 measured the same stamp at **24** three hours earlier and wrote it up as 「inside the limit, not a defect in this window」, which was true then; six commits later it is not. ⚠ **There is a second and worse failure mode 20 commits further on.** At `behind` = the clone's own depth (**50** here) the stamp stops resolving at all and the gate fails as `Not a valid object name`, which the test's own message says 「reads as corruption rather than staleness」 (WFG-119, and it is what turned critic #33's lap red on a correct tree). ⚠ **Nothing on the screen is WRONG today.** Critic #37 re-derived all 28 on-screen keys against `docs/NUMBERS.json` and found zero misses on either side, and R1 ticks on that work. This is a liveness defect, not a correctness one, and it is filed as minutes rather than as a P0 investigation for exactly that reason. **Done when:** `make finals` is run **on the commit being pushed** so `web/finals.html`'s `git` field names a commit inside the limit; **and** `make finals-bundle` re-points `release/kcf-finals-2026/MANIFEST.json` in the **same lap** (WFG-152; `newest_printables()` resolves the stamp from the tracked tree, so the bundle build stays broken until it is rebuilt); **and** `gates.py --mode full` is re-run after both and read unpiped, with `--assert-head` and `--assert-reported` before the push. ⚠⚠ **Do NOT raise `STAMP_MAX_COMMITS_BEHIND` to close this.** `tests/test_finals_screen.py:754` grades the threshold with two literals that are deliberately NOT derived from the constant, precisely so that raising it fails rather than slides past; and NH-043 (open, due 2026-09-09) is the author's decision about how often this gate should fire, which this row does not pre-empt. Found by critic #38 | done(20260908T0018Z) — screen rebuilt at 1bca8ed, bundle re-pointed in the same commit; the stamp names a PUSHED commit because the second gate in the same file requires it | yes | minutes | A 구현 및 유용성 · A/B 제출 자료 · R1 · R3 |
| WFG-174 | P1 | IEEE | **The manuscript applies one provenance standard to the Korean operational systems and a laxer one to the Western evacuation-routing literature, and its own reviewer said so.** `paper/GAPS.md:118-127` and `paper/STATE.json` (paper lap 17, 2026-09-07T2047Z) record that **fifteen of the 29 references are verified 「via the Crossref record」** while §2 characterises their *content* substantively: `li2017`, `li2019`, `wahlqvist2021`, `cova2003`, `cova2005`, `finney2002`. **A Crossref record is a catalogue entry**, which is the exact standard paper lap 16 applied one commit earlier when it retracted the NIFoS sentence, and the exact standard WFG-171 has just applied to two printed booth surfaces. ⚠ **No claim here is false and none may be called false:** the six paraphrases are title-derivable. What is wrong is the asymmetry, and a hostile ML or software judge who notices that this project narrowed a Korean agency claim to a chapter list on Monday while paraphrasing six Western papers from their titles gets to ask why. `paper/check_paper.py:203` cannot see the difference because it tests only for the substring `verified`. **Shares its gate half with WFG-042** (a `note` that says `verified` while naming a different paper than its own `url`); the two should be done together, and the fix to `check_paper.py` is one fix. **Done when:** each of the six named entries either (a) is opened at its URL and its `note` records what was actually read, or (b) has its §2 characterisation narrowed to what a title and an abstract support, in the register `references.bib:258` already uses since WFG-171; **and** `check_paper.py` distinguishes 「opened」 from 「resolved via a catalogue record」 instead of matching one substring. ⚠ No reference is deleted (CHARTER §3.7); no number from any of them enters `docs/NUMBERS.json`. ⚠ **P1 and not P0, deliberately:** §14b's judge-facing list contains the manuscript, but this puts no false sentence in front of anybody and these five KCF judges do not read the bibliography. It is an ISEF and IEEE row that happens to have been found today. Found by paper lap 17's independent reviewer; filed by critic #38 because the paper routine could not (WFG-175) | todo | yes | one lap | B 제출 자료 (출처 명기) · B 설계와 방법론 |
| WFG-175 | P1 | infra | **A paper lap's findings have no route to the backlog, and today one was lost for a lap and then found by accident.** `docs/auto/reports/2026-09-07T2112Z-manual.md:124` and `paper/GAPS.md:118` both say the Crossref finding was 「Filed as a dev-lap row」; `paper/STATE.json` says 「FILED, NOT FIXED」. **No such row existed.** Checked at `1bca8ed` before writing WFG-174: `git grep -niE 'provenance standard|title-derivable|Crossref record' docs/auto/BACKLOG.md` returned nothing, and the only near neighbour, WFG-042, is a different defect (a note that names the wrong paper). **It is not that lap's slip.** CHARTER §12 confines the paper routine to `paper/` plus its own report, so the routine is *structurally unable* to write the row it says it filed. The finding survived only because this critic lap opened `paper/STATE.json` to check the word budget and read the field to the end. **Two things are broken and both are cheap.** (1) The routine writes 「filed」 for an action it cannot perform, which is a false statement in a report the author reads. (2) There is no queue: a paper lap that finds a real defect has nowhere to put it that any other routine is obliged to read. **Done when:** the paper lap writes its proposed rows to a file inside its own boundary (`paper/PROPOSED_ROWS.md`, one block per row in the backlog's own column order); **and** a gate fails when that file is non-empty and no dev or critic report newer than it names the rows in it, so the queue cannot silently grow; **and** `docs/auto/ROUTINE_PROMPTS.md`'s paper prompt says 「propose」 rather than 「file」. ⚠ Do not fix this by widening CHARTER §12's write boundary: that boundary is what keeps the paper routine from colliding with dev laps, and three rebases this week already landed on `paper/`. ⚠ **§14b holds this behind R1, R3, R4, R7, R8 and R9 as loop hygiene.** R1, R7 and R9 are ticked; R3, R4 and R8 are not. Found by critic #38 | todo | yes | one lap | R8 · B 제출 자료 |
| WFG-176 | P1 | infra | **`load_dem(source="auto")` documents a fallback chain it does not have, and nothing could see it until the network guard landed.** `src/wildfireguardian/data_io/raster.py:476-481` documents `"auto"` as 「try ngii → srtm → synthetic in order」; the loop at `:492-499` catches only `NotImplementedError`. A missing SRTM tile raises `FileNotFoundError` (`:378`, and `_download_srtm_tile` re-raises the same class when the fetch fails), which propagates. **So on a clean clone with no network, `source="auto"` RAISES instead of returning synthetic** — the opposite of what the docstring promises and of what a stranger re-running this repository would expect. It was invisible for as long as the download always succeeded (WFG-139); the network guard added on 2026-09-08 turned `tests/test_raster_ingestion.py`'s auto-DEM test red and that is how it surfaced. Asserted rather than fixed in that lap: `test_auto_dem_does_not_fall_back_when_the_srtm_tile_is_merely_missing` pins the current behaviour with a docstring saying it contradicts the contract, so the debt is visible and a later change to it fails a test rather than passing silently. **Done when:** either `"auto"` catches `FileNotFoundError` as well and falls through to synthetic — with the pinning test inverted in the same commit and a note in `docs/clean_clone_gates.md` — or the docstring is narrowed to what the code does and the row says why the chain is not wanted. ⚠ Widening the `except` changes production behaviour on a machine without the tile, so read `data/processed/` build scripts for callers that pass `source="auto"` before choosing; none of the committed artifacts is rebuilt by a lap (CHARTER §3.2), so no registered number can move, and the lap says so in writing after checking rather than before. | todo | yes | one lap | B 데이터 수집·분석·해석 (재현) · R3 |
| WFG-177 | P1 | infra | **The network guard closed the hole it was built for and its independent reviewer named five it does not close.** `tests/conftest.py` refuses `connect`/`connect_ex`/`create_connection`, which covers `urllib`, `http.client` and `requests`. It does **not** cover: (a) **import and collection time** — it is a session *fixture*, and fixtures are set up after collection, so a module-level network call in a test file runs unguarded; (b) **UDP** — `sendto`/`sendmsg` are not patched; (c) **name resolution** — `getaddrinfo` is not patched, so a test can still depend on DNS, which is the other half of what CHARTER §4b forbids in the same sentence as the network; (d) a **loopback forwarder** that carries traffic off the machine without appearing in a `*_proxy` variable; (e) `pytest` invoked on a path outside `tests/`, which loads no `conftest.py` at all. All five are written down in the conftest docstring and in `docs/clean_clone_gates.md`, so nothing is hidden; none is closed. ⚠ **(a) and (c) are the two a judge could actually be shown**, and (c) is the cheap one: patching `getaddrinfo` to refuse any name that is not a loopback alias costs a few lines and would make an air-gapped booth machine behave like CI. **Done when:** the guard is installed at `pytest_configure` rather than as a fixture so collection is covered, `getaddrinfo` and the UDP send calls are refused on the same rule, and each new refusal is graded the way the existing ones are — a planted call that must raise, plus the legitimate case that must not (loopback name resolution for the browser test). ⚠ Do **not** close this by widening the `WFG_TESTS_ALLOW_NETWORK` escape or by adding a per-test marker; the conftest docstring says why. §14b holds this behind the readiness block, and R3 is what WFG-139 just unblocked, so re-read §14b before promoting it. | todo | yes | one lap | B 데이터 수집·분석·해석 (재현) · R3 |
| WFG-171 | **P0** | KCF | ⚠⚠ **ADDED AT POSITION 1 BY CRITIC #37, 2026-09-07T2000Z, AND IT IS THIS LAP'S ONE `fix-before-next-row` ITEM (CHARTER §14b: minutes, judge-facing).** **The claim family WFG-144 → WFG-162 → WFG-166 kept fixing, now found in the POSITIVE direction on the same two surfaces.** `docs/auto/finals/RELATED_WORK_PANEL.md:32` (printed, 뒷면 1, in bold) and `docs/auto/JUDGE_QA.md:652` (the recited **Q16a · T0** draft, printed on the kit's Q&A pages) both assert flat and present tense that the NIFoS 산불확산예측시스템's 발화점은 「운영자가 손으로 입력」한다. **The whole provenance of that sentence is a chapter title in a library catalogue's table of contents** (「발화지점 생성」); the guide itself is an unopened 18 MB PDF (**NH-039**). Every other surface in the repository states it WITH its attribution and its limit, and I read all three rather than grepping: `paper/references.bib:258` names the eight catalogue chapters and adds 「Only the catalogue page was opened」; `paper/manuscript.md:111-112` writes 「a 2026 user guide for an AI spread-prediction console driven by a human-entered origin point [@nifos2026guide]」, attributed; `docs/auto/knowledge/KOREAN_OPERATIONAL_SYSTEMS.md` §1 shows the derivation (「The catalogue's table of contents describes an operator workflow ... So it is an operator console ... driven by a human-entered origin point」). **The two surfaces a judge actually meets are the two that dropped the attribution.** The panel does it FOUR LINES above its own ⚠ note, whose argument is that this panel read only 「카탈로그 기록·언론 보도」 and that 「열어 본 적 없는 문서에 무엇이 **없다**고 말하는 것은 요약이 아니라 새로운 주장입니다」 — a rule written to cover negatives only. A positive assertion of the same provenance is the same new claim and fails the same way: a judge who has driven that console and knows it can take an origin from a KFS feed hears a flat sentence that is simply wrong, and what breaks is, in Q16a's own words, 「신뢰 전부」. ⚠⚠ **The 읍면동 and 30분 clauses beside it are NOT this row and must not be softened** — those are reported by 경향신문 2026-03-30, an article this project opened, and their register is correct. **Done when:** both lines state the claim at the strength of its source in the register those two files already use elsewhere (e.g. 「카탈로그의 목차가 「발화지점 생성」을 한 장으로 두고 있어, 운영자가 발화점을 지정하는 흐름으로 읽힙니다」), **and** the same lap runs `make printables` and re-points `release/kcf-finals-2026/MANIFEST.json` (**WFG-152**) because both files are printables `SOURCES` and a bare edit turns `tests/test_printables.py::test_the_newest_printable_is_not_stale_against_the_tree` red, **and** the lap runs DIRECTION's mandated subject grep (`git grep -n '발화' -- docs/ paper/ release/ web/`) and names in writing every file it returned and what it did about each. ⚠ **Not a withdrawal and not a `WC-###`:** the claim is narrowed, not retracted, so CHARTER §3.5c's registration does not apply and DIRECTION's narrowing rule does. ⚠ **The critic could not do this itself:** DIRECTION bars this routine from `JUDGE_QA.md`, `DEMO_SCRIPT_5MIN.md`, `BOOTH_SETUP.md` and, on the same `SOURCES` footing, `RELATED_WORK_PANEL.md`. | done(20260907T2120Z) — both printed clauses now state the claim at the strength of its source, and the subject grep found a THIRD live copy the critic had not named: `docs/related_work.md` row 13's own cell asserted 「driven by a human-entered origin point」 flat, one screen above its own attributed derivation. Kit rebuilt at `20260907T2129Z`, bundle re-pointed. ⚠⚠ **The independent reviewer BLOCKED the first attempt and was right:** the hand sweep reported that every remaining hit was our own ignition point or record class, and `paper/manuscript.md` §2 was neither — a gated, live copy the Korean subject grep on 「발화」 structurally cannot reach — while `paper/references.bib:258` held a second copy that certified the manuscript's wording as the manuscript cited it as source, a circle reported as provenance. Both narrowed here; the manuscript's +3 words were paid back to net zero so the paper's margin and its measured 23-page anchor both hold. The claim is now REGISTERED as **`WC-009`** in both languages (CHARTER §3.5c), and registration immediately caught a THIRD copy in a file the reviewer had judged clean — `docs/auto/knowledge/KOREAN_OPERATIONAL_SYSTEMS.md` §1. Two readers and one machine; only the machine found it. ⚠ Still not closed: nothing detects a NEWLY WRITTEN one, and the registry reads `.md`/`.html` only, so it can never reach `references.bib` — the file that motivated it. That is WFG-168 and it does not exist. | yes | minutes | B/A 제출 자료 · B 설계와 방법론 · R5 · R7 |
| WFG-166 | **P0** | KCF | ⚠⚠ **ADDED AT POSITION 1 BY CRITIC #36, 2026-09-07T1700Z, AND IT IS THIS LAP'S ONE `fix-before-next-row` ITEM (CHARTER §14b: the Q&A bank is named there as a judge-facing surface, and this is one sentence plus the kit rebuild WFG-152 requires).** **The card that WON critic #35 a scorecard point contains, in its own recited T0 draft, the exact sentence its own 없는 것 block forbids twenty lines further down.** `docs/auto/JUDGE_QA.md:650-652` has the student say aloud: 「읍면동은 여러 마을이 들어가는 **행정구역**이지 집 한 채가 아니라서, 사이렌을 어디에 울릴지는 정해 주지만 **특정한 집의 어느 길이 위험에 들어가고 어느 길이 들어가지 않는지는 말해 주지 않습니다.**」 `:671-672` of the same card then says: ❌ 「저쪽은 가구 단위로는 못 합니다」라고 **단정하지 마십시오**; the permitted form is ⭕ 「**공개된 자료에서는** 가구 단위 산출물이 확인되지 않습니다」, and it gives the reason — 「심사위원이 그 시스템을 직접 써 본 분일 수 있고, 그때 무너지는 것은 이 답변 하나가 아니라 신뢰 전부입니다.」 The spoken sentence is the forbidden form. **It is the Korean half of `WC-008`,** registered ninety minutes earlier on the English spelling `walk out, and along which path`, which structurally cannot reach it — the mirror of what happened to WC-007 inside the same lap, and what `docs/withdrawn_claims.md` §4 predicts in writing. It is printed: `JUDGE_QA.md` is a `SOURCES` file of kit `20260907T1551Z`, 17 of its 38 pages. **Done when:** the recited draft says only what this project checked (a form such as 「저희가 연 자료 — 카탈로그 기록과 언론 보도 — 에서는 가구 단위 경로 산출물이 확인되지 않습니다」, or drops the clause and keeps only the positive statement of this project's own output object); the superseded sentence is kept as a dated record per CHARTER §3.7 and **is not reprinted verbatim on a printed page** (WC-005/WC-007 precedent: the note describes, the registry quotes); `WC-008` gains the Korean spelling so the ratchet exists in both languages; the kit is rebuilt at a new stamp **in the same lap** and the bundle manifest re-pointed (WFG-152, `test_the_newest_printable_is_not_stale_against_the_tree`); and the lap runs the DIRECTION subject-grep on 「읍면동」 and 「가구 단위」 across `docs/ paper/ release/ web/` and names in writing every file it returned. ⚠ **A critic lap may not do this itself** — DIRECTION.md forbids critic and research laps from editing `JUDGE_QA.md` at all, because a one-line edit turns the printables staleness gate red unless the same lap rebuilds the kit | done(20260907T1820Z-lap) — ⚠ **the subject grep found a SECOND live copy the critic's item did not name: `docs/auto/finals/RELATED_WORK_PANEL.md:67-69`, on a PRINTED page, in the same file whose other clause of this family the 1528Z lap corrected for WC-007.** Both fixed; `JUDGE_QA.md:625`'s false account of its own card corrected with a dated note; `WC-008` gained its Korean spelling (graded 4/4); kit rebuilt at `20260907T1825Z` and the bundle re-pointed | true | minutes (plus the one `make printables`) | B/A 제출 자료 · B 설계와 방법론 · R5 · R7 |
| WFG-168 | P1 | infra | **Five withdrawals of one claim family in one day, by three routines, every one found by a human-shaped read and not one by a gate — and the sixth copy was still live when the sixth critic looked.** In the 24 h to `7cc4eb7` the loop withdrew 「the two Korean operational systems do not publish a reproduction method」 (`WC-007`, Korean), its English half in `KOREAN_OPERATIONAL_SYSTEMS.md` §3.3, 「neither answers which household can still walk out」 from `paper/manuscript.md` §2, the same from `docs/related_work.md` rows 13-14 and from the knowledge note §2 (`WC-008`, English spelling) — and `docs/auto/JUDGE_QA.md:650-652` still carries the Korean form (WFG-166). The 1600Z dev report says it plainly and hands it here: 「the gates read prose for registered spellings; nothing in this repository detects a **newly written** flat negative about somebody else's system.」 Registration is a copy-paste ratchet by design (`docs/withdrawn_claims.md` §4) and cannot see a claim written for the first time. **What to build:** a lint, not a claim detector — `scripts/check_third_party_negatives.py`, run by `make verify` over the gated file set, that flags any sentence in a judge-facing file pairing a **named third-party subject** (a small committed list: 국립산림과학원, 산림청, 경기도, G-DAPS, NIFoS, SFTD059T, plus the surveyed papers) with a **negative predicate** (하지 않|않습니다|못 합니다|없습니다|does not|do not|cannot|neither|never|fails to) and NOT carrying one of the licensed hedges (「이 패널이 읽은 자료에서는」, 「공개된 자료에서는」, 「확인되지 않습니다」, `not stated`, `in what was opened`). **Done when:** the lint exists, its allow-list of hedged forms is committed with the reason for each, it is graded by mutation in both directions (a new unhedged negative goes red; every currently shipped hedged sentence stays green), the current tree is green under it after WFG-166 lands, and `docs/third_party_claims.md` states the method, the false-positive rate measured over the gated set, and **what it does NOT catch** (a negative written without naming the subject in the same sentence; a reworded predicate; anything outside `.md`/`.html`, which is WFG-155's limit). ⚠ Under CHARTER §14b this is loop hygiene and waits behind R1, R3, R4, R7, R8, R9 — filed at P1 deliberately, not because it is small | todo | true | one lap | 데이터 해석 (재현) · 제출 자료 |
| WFG-169 | P1 | KCF | **The four VIEW TABS still have no driver, and they are what critic #36 actually measured.** WFG-009 drove the guided demo's four 막; the tabs 라이브 · 시스템 · 근거 · 신뢰성 (`view-live`, `view-system`, `view-evidence`, `view-reliability`) are a different mechanism with a different control (`#tabs`). Critic #36's measurement stands and is unanswered: at initial load `view-evidence` is **225 B** and `view-system` **235 B** with no Korean text, because both build on switch. **Done when:** a driver switches to each of the four tabs, asserts the target view's content grew and holds the Korean text only that view has, screenshots each, and the assertion is mutation-graded against a switch that silently fails. `scripts/cdp_min.py` and `scripts/check_finals_acts.py` already carry the machinery; this is a sibling function, not a new framework. Held behind the readiness lines by §14b | todo | true | minutes | 구현 및 유용성 · R1 |
| WFG-170 | P1 | KCF | **The booth's real control is the KEYBOARD, and nothing drives it.** `docs/auto/finals/BOOTH_SETUP.md:206/218/227` makes **G** the procedure between judges (「심사위원이 바뀌면 반드시 G」 — it returns the guided demo to 1막) and lists `→` beside it; `:133` records `Enter`, `→` and `G` as the hand-checked path. WFG-009's driver presses the MOUSE only, and says so in `docs/finals_acts_smoke.md` §4.3. So the one control the student uses in front of every judge is the one control no gate exercises. **Done when:** the driver advances the four acts over `Input.dispatchKeyEvent` as well, and asserts that **G** from any act returns to 1막 with one dot lit — graded against a handler that no longer binds the key | todo | true | minutes | 구현 및 유용성 · R3 · R1 |
| WFG-172 | P1 | infra | **DIRECTION wrote the cold/warm rule and the very next lap broke it, in the report the author reads.** `docs/auto/reports/2026-09-07T1900Z-dev.md` and commit `64f015b`'s own message both record 「1688 passed / 56 skipped / 2 xfailed, unpiped」 with **no** cold/warm word and **no** statement of whether the run downloaded anything, against DIRECTION's ⚠ bullet 「Do not report a pass/skip count without saying cold or warm, and without saying whether the run downloaded anything (WFG-139)」. It is not a harmless omission: critic #37's **cold** run at the **same tree** (`64f015b`) gives `1682 passed, 62 skipped`, so a reader comparing 1682 to 1688 reads a six-test regression that did not happen — the exact contamination WFG-139 describes, now inside the artifact the author is emailed. **Done when:** `scripts/auto/report.py` records both facts mechanically rather than asking a lap to remember — it already parses the pytest summary, and `data/raw/` byte size before and after the run answers 「did it download」 without a network call — and the gate table prints them beside the counts; **and** a test asserts a report cannot carry a pass count without both. ⚠ **This does not close WFG-139**, which is the download itself; this closes the class of reports that hide it. §14b holds this behind R1/R3/R4/R7/R8/R9 and R3 is unticked, so it is P1 and waits. | todo | yes | one lap | B 데이터 수집·분석·해석 · R3 |
| WFG-144 | **P0** | KCF | **Korea already operates two wildfire-spread prediction systems, run by the agency that would deploy this project, and neither appears anywhere in this repository — including in the Q&A bank the student rehearses.** Found by the research lap of 2026-09-06 through plain Korean search, and written up as the new knowledge note `docs/auto/knowledge/KOREAN_OPERATIONAL_SYSTEMS.md`: (i) NIFoS's 「AI 기반 산불확산예측시스템」, user guide 연구자료 제1201호 (2026), <https://book.nifos.go.kr/library/10130/contents/7732761> — an operator console for suppression planning driven by a human-entered origin point; (ii) 경기도's 민방위 경보 예측모델 **G-DAPS**, 경향신문 2026-03-30, <https://www.khan.co.kr/article/202603301116001/> — forecasts a fire's route, affected **읍면동** and arrival times at **30-minute** steps, in trial operation since April 2026. `docs/auto/JUDGE_QA.md` was grepped this run for any card on existing agency systems and has none. A KCF judge from the disaster-response side is more likely to ask 「산림청·경기도가 이미 하고 있는데 무엇이 다릅니까?」 than any question the bank currently answers. **Done when:** a Q&A card exists whose answer is the **output object** — a suppression-oriented spread footprint at township granularity versus a per-household walk-or-be-rescued decision on public, re-derivable data — and the related-work panel (WFG-026) names both systems. ⚠⚠ **Constraints, and the first one is load-bearing. NO ACCURACY COMPARISON ON ANY SURFACE, in either direction.** The only capability figures available (「확산예측 정밀도 약 30% 향상」, 「지형 분석 정밀도 5ｍ」, occurrence 76 % → 88 %; 사이언스타임즈 2026-02-13, 연합뉴스 2026-02-12 기사 전재 — date corrected 2026-09-07 under WFG-146) are **agency plan statements restated by a newspaper**, with no metric definition, no dataset and no validation scheme — writing them onto a judge-facing card is the failure mode that produced WFG-049 and CHARTER §3 rule 5b. They stay in the knowledge note with agency, date and scope. The card must be able to say plainly that the comparison has not been made and could not be made from public information. Note also that 5 m terrain analysis is **finer** than this project's 500 m grid, in their favour, and the card should not pretend otherwise. **Sequencing:** land this **after** the WFG-134 / WFG-130 printables rebuild, or the printed 17 pages go stale a fourth time; WFG-140's freshness gate then covers the re-rebuild. ⚠ P1 and not P0 deliberately — see NH-038 and `WEEKLY_2026-W36.md` §5; if the author promotes one row from this run, it is this one. Proposed by the research lap of 2026-09-06 (`WEEKLY_2026-W36.md` §C3). ⚠ **Critic #31, 2026-09-07: the sequencing reason above is spent — the rebuild shipped at `20260907T0059Z` — and this row is now blocked by nothing.** I did not promote it, because my one row move went to **WFG-026**, which is R7's sole blocker and whose panel must carry this same content (`KOREAN_OPERATIONAL_SYSTEMS.md`, `manuscript.md` §2) to be a differentiation panel at all. So WFG-026 absorbs the substance and this row keeps the **spoken Korean card**, which is the half a judge actually hears. ⚠ And it can no longer be taken by a critic or research lap: editing `docs/auto/JUDGE_QA.md` now turns the printables gate red (**WFG-152**, measured), so the lap that writes this card rebuilds the kit in the same lap. ⚠⚠ **CRITIC #33, 2026-09-07T0800Z: what this row is worth changed this window, and the new framing is sharper than 「no card exists」.** WFG-026 shipped, so `docs/auto/finals/RELATED_WORK_PANEL.md` now answers this exact question in **3 printed pages** inside kit `20260907T0705Z`, which is on the stick. I re-ran the drill here: the only `산림청` hits in `docs/auto/JUDGE_QA.md` are still the two burned-area lines at `:399` and `:996`, unrelated, so the **spoken** bank still has nothing. **So the failure mode is no longer a silence, it is a mismatch:** a disaster-response judge asks the question, the student has no rehearsed answer, and is standing next to a panel that answers it well. That is worse at a booth than either half alone, because the panel invites the question. Fifth consecutive critic lap measuring this gap; still P1, still not promoted, because my one row move went to WFG-119 (a red gate) and CHARTER §14b allows one. If the author promotes one row from this report, it is still this one ⚠⚠ **CRITIC #34, 2026-09-07T1100Z: PROMOTED P1 → P0 AND MOVED TO POSITION 1 OF THIS TABLE. This is the lap's one row move (CHARTER §14) and it is filed under §14b as a judge-facing finding LARGER than minutes, which §14b says becomes a P0 row at position 1 and is never a preemption — so the top of the table is displaced by WFG-146's minutes and by nothing else.** Re-measured at `2720840`, not inherited: the only `산림청` hits in `docs/auto/JUDGE_QA.md` are the two burned-area lines at `:399` and `:1003` (critic #33 wrote `:996`; the file grew), and there is no card. **Seventh consecutive critic lap measuring this, and the sixth to decline to promote it because the one allowed item went elsewhere. That is the pattern NH-038 is about, and this lap ends it.** The reason it is promoted now rather than earlier is critic #33's framing, which I confirm: the kit has carried `RELATED_WORK_PANEL.md` since `d2640ca`, three of its 36 pages, and those pages **invite** the question. 「기존 연구/작품과의 차별점」 is an explicit criterion on the 설계와 방법론 row of **both** rubric tables, and the finals format is five separate 5-minute 1:1 interviews, so a question the student cannot answer is asked up to five times. **What I may not do, and why it is recorded here rather than done:** `docs/auto/JUDGE_QA.md` is a printables `SOURCES` document and a one-line edit turns `tests/test_printables.py::test_the_newest_printable_is_not_stale_against_the_tree` red (probed and reverted by critic #31 at `3f881f6`), so only a lap that rebuilds the kit in the same lap may write the card — which no critic or research lap may be (WFG-152). ⚠ **Fold WFG-146 into the same kit rebuild**; it is three lines and this lap's `fix-before-next-row` item, and both need exactly one `make printables` | done(20260907T1222Z) — card **Q16a** (T0) in `docs/auto/JUDGE_QA.md` §3; panel named both systems since `d2640ca`; kit rebuilt at `20260907T1248Z` (38 pp) and the release bundle re-pointed at it | yes | hours | A 제출 자료, B 제출 자료, R5, R7 |
| WFG-162 | P1 | KCF | **The printed differentiation panel asserts what two operational systems do NOT publish, three lines after promising it would only summarise, and one lap after the same claim was narrowed everywhere else.** `docs/auto/finals/RELATED_WORK_PANEL.md:40` ends the WildfireGuardian row with 「그리고 **공개 자료 위에서 모든 숫자를 게이트가 재계산**합니다 — **앞의 두 시스템은 재현 방법을 공개하지 않습니다.**」 That second clause is an assertion about the contents of documents this repository has never opened: the NIFoS user guide (연구자료 제1201호, ~18 MB PDF) is **NH-039**, open and unfetched, and the 경향신문 G-DAPS article says nothing about reproduction — I re-fetched it in this sandbox to be sure. **The source document this panel summarises gets it right:** `docs/related_work.md:134` writes the same cell as 「reproducible by a stranger → **not stated** for the NIFoS console, **not stated** for G-DAPS, and committed public data with every number re-derived by a gate for this project」. *Not stated* is the honest register; *does not publish* is a claim. **The panel's own text forbids it twice:** its header at `:3-5` says 「이 카드는 원본을 요약할 뿐 **새로운 숫자를 만들지 않습니다**」 — and it made a new *claim*, which is worse than a new number, because no gate reads claims — and its closing section at `:135` says 「국립산림과학원과 G-DAPS 는 카탈로그 기록과 언론 보도만 읽었고」. **And the same lap wrote the correct sentence twice, in two other files:** `docs/auto/JUDGE_QA.md:660` says 「두 시스템이 그렇게 한다는 **자료는 찾지 못했습니다**」, and `docs/dispatch_ordering.md:317-327` is an entire dated correction block whose whole purpose is replacing 「다른 어떤 체계도」 with 「조사한 어느 **연구**도」 for exactly this reason, in its own words 「아무도 열어 본 적 없는 매뉴얼에 무엇이 없다는 주장이 되었습니다」. **So this is not a claim the lap failed to think about; it is the one copy of it the narrowing did not reach, and it is the copy that gets printed.** **Where it is live:** `RELATED_WORK_PANEL.md:40`, which is `SOURCES[5]` and **3 of the 38 pages** of kit `WFG_printables_20260907T1248Z.pdf` (sha256 `4504f5984cb9…`), on the USB stick and in `release/kcf-finals-2026/MANIFEST.json`. **Done when:** the clause reads the *not stated* register — 「공개된 자료에서는 두 시스템의 재현 절차가 확인되지 않습니다」 or equivalent — with a dated note keeping the superseded wording (CHARTER §3.5), **and the kit is rebuilt in the same lap** (WFG-152) so the printed page matches the tree and `tests/test_printables.py` stays green. **Constraints:** prose only; no accuracy figure moves onto any judge-facing surface; do not restructure the panel. ⚠ Neither the critic nor the research routine may take this row — `RELATED_WORK_PANEL.md` is a printables `SOURCES` document (WFG-152). ⚠ **Before calling it done, run `git grep -n "재현" -- docs/ paper/ release/ web/` and name in writing every file it returns and what was done about each** (DIRECTION's new rule; this row is the reason it exists). Filed by critic #35, 2026-09-07T1416Z, as that lap's one `fix-before-next-row` item under CHARTER §14b: minutes, on a printable and in the release bundle, both of which §14b names as judge-facing | done(60c07c8) — the clause now reads the register the source supports (「이 패널이 읽은 자료(카탈로그 기록·언론 보도)에서는」), narrowed TWICE: once by the lap and once again by its independent reviewer, which blocked the first repair as still a claim about all published material. **WC-007 registered in the same lap** (CHARTER §3.5c) — and the registration immediately caught the correction note reprinting the withdrawn sentence verbatim onto the kit, so the note now describes rather than quotes (WC-005's precedent). Kit `20260907T1551Z`, 38 pp, bundle re-pointed; the `1534Z` build from the blocked text was discarded before any commit. **Propagation, after the push rebase brought in the concurrent paper lap `8ff1b40`:** the same claim family was narrowed in `docs/related_work.md` rows 13-14 (NH-044, filed by the paper routine's reviewer, which could not edit outside `paper/`) and in `docs/auto/knowledge/KOREAN_OPERATIONAL_SYSTEMS.md` §2 **twice** — a copy neither routine had named, and the ENGLISH half of WC-007 that the Korean spelling could not reach. Registered as **WC-008** | yes | minutes, plus a kit rebuild | B 제출 자료, A 제출 자료, R7 |
| WFG-163 | P1 | science | **A reviewer's block narrowed the G-DAPS trial-operation claim in the manuscript and the narrowing never reached the knowledge note.** `docs/auto/knowledge/PYROGEOGRAPHY.md:204` states that G-DAPS 「**entered trial operation in April 2026**」 as accomplished fact. The cited source says 「이르면 다음 달부터 시범 운영에 들어간다」 — *at the earliest*, from next month — announced 2026-03-30 (경향신문, <https://www.khan.co.kr/article/202603301116001/>; re-fetched and re-read by critic #35 in its own sandbox rather than inherited). `paper/README.md:31-32` records that this exact sentence was **blocked by the paper lap's independent reviewer** at `719c420` (「the draft wrote that Gyeonggi's model 「entered trial operation」 where its own cited source says only that trial operation was *announced* for the following month」), and the repair reached `paper/manuscript.md:114`, `docs/related_work.md:113` and `docs/auto/knowledge/KOREAN_OPERATIONAL_SYSTEMS.md:17` — all three now read *announced*. It did not reach PYROGEOGRAPHY, which CHARTER §13 names as the place a lap, the paper routine or the student looks a concept up, so the superseded form is the one a future lap will read first. **Second instance this week of a narrowing escaping into a file the reviewer was not given** (the other is WFG-162), which is why it is filed as a claim row and not as a typo. **Done when:** `PYROGEOGRAPHY.md:204` reads the announced form with a dated correction note keeping the superseded wording (CHARTER §3.5), and the lap runs `git grep -n "trial operation" -- docs/ paper/ release/ web/` and again for 「시범 운영」 and names every file it returns. ⚠ Two record-class files also carry the accomplished form and are **exempt by design** — `docs/auto/BACKLOG.md` WFG-144's own cell and `docs/auto/SCORECARD.md` — and must not be edited to hide it (CHARTER §3.7). `docs/auto/research/WEEKLY_2026-W36.md:48` writes 「Trial operation from April 2026」, which is ambiguous rather than wrong; make it unambiguous in the same pass. **Constraints:** prose only; no knowledge note is restructured (CHARTER §13). Filed by critic #35, 2026-09-07T1416Z | todo | yes | minutes | B 데이터 수집·분석·해석, B 설계와 방법론 |
| WFG-164 | P1 | infra | **NH-041's undone remainder: the `PLACEHOLDER` email is still sitting in the author's inbox two laps after the entry promised the next lap would remove it.** ⚠⚠ **This row was FILED ON A FALSE PREMISE AND IS CORRECTED HERE IN THE SAME LAP (CHARTER §3.5, superseded wording annotated rather than deleted).** As first written it said the send 「has never been mentioned by any lap」 and made a finding of three critic laps failing to notice it. **That is wrong, and I found it out by reading back the email body I was about to send, which listed NH-041 in its own Decisions block.** `docs/auto/NEEDS_HUMAN.md` NH-041 records the whole thing, and records it well: which two message ids the author has (`1a07a0a7ffa5bafb`, the empty one, and `1a07a0bce7b3c975`, the report; NH-041 timestamps the first at 04:08Z where the Gmail metadata I read gives 04:05:03Z, a three-minute difference that changes nothing and is noted so a later lap does not treat it as two events), that CHARTER §4 step 9 was performed in form and not in substance because the lap passed the literal strings `PLACEHOLDER_WILL_NOT_BE_USED` and `PLACEHOLDER` instead of reading `.auto/email.html`, that the lap tried to trash the message immediately and the Gmail connector answered `requires re-authorization (token expired)` in the same minute, and that a cloud routine cannot run the OAuth flow. It even names the habit it changed and wrote into `docs/auto/MEMO.md`. **The loop told the author about its own defect, which is the practice this repository runs on, and my first draft accused it of the opposite.** **What is actually left, and it is small.** NH-041 says 「the next lap will trash it if the connector is authorised again」. Critic #35 read message `1a07a0a7ffa5bafb` in this sandbox at 2026-09-07T14:1xZ and it is **still in the inbox**, so that standing promise has not been kept across the 0521Z, 0711Z, 0816Z, 1002Z, 1118Z and 1256Z laps. Either a lap does it or the sentence should stop promising it. **Done when:** a lap with an authorised Gmail connector trashes `1a07a0a7ffa5bafb` and says so in its report, **or** NH-041's sentence is rewritten to say the author deletes it and the loop will not, whichever the author prefers — it is one click either way and the message is recoverable from Trash for 30 days. ⚠ **Critic #35 deliberately did NOT trash it itself**: the critic routine's brief is to write under `docs/auto/` and send one email, and mutating the author's mailbox is outside it (CHARTER §6). **Second-order note worth keeping:** the Gmail step of this routine's own lap reports 「every thread is a single message this loop sent」, which is true and which does not distinguish a report from a malformed send, because the search filters on the sender and the sender is the loop's own address. That is not a defect while NH-041 exists, and it would be one if a future malformed send were not escalated. Filed and corrected by critic #35, 2026-09-07T1416Z | todo | yes | one click, plus a sentence | none directly; the author's channel |
| WFG-165 | P1 | infra | **`build_finals_bundle.py` re-points the manifest, leaves the previous kit in the assembled folder, and its own default check says OK.** Measured on this lap at `60c07c8`, every exit code read **unpiped**: after `--update` moved the manifest to the new kit, `release/kcf-finals-2026/printables/` still held `WFG_printables_20260907T1248Z.pdf`; `scripts/build_finals_bundle.py` with no flags printed 「OK — rebuilt byte-identically, 19 files」 and exited **0** with that orphan in the folder, while `scripts/check_bundle_copy.py` printed one `EXTRA` line and exited **1**. So the check that travels INSIDE the bundle for the student is the strict one and the lap's own check is the loose one — the wrong way round, because the lap is the party that can still fix it. The payload is git-ignored (`.gitignore:448`), so a fresh clone never sees the orphan and the first person who does is whoever runs the recipe onto the stick (NH-014). This lap pruned it by hand and both checks then pass. **Done when:** `build_finals_bundle.py` fails, or prunes, on a payload file its manifest does not list; graded by rebuilding the kit twice in one tree and seeing the second default run go red before the prune. Held behind R1/R3/R8 by CHARTER §14b | todo | true | minutes | B/A 제출 자료 · R9 |
| WFG-151 | **P0** | KCF | ⚠⚠ **ADDED AT POSITION 1 BY CRITIC #31, 2026-09-07T0206Z, AND IT IS THIS LAP'S ONE `fix-before-next-row` ITEM (CHARTER §14b: the release bundle is named there as a judge-facing surface).** **The booth kit shipped yesterday and the bundle a judge would be handed still does not contain it, because the one place R9's contents were written into code transcribed four of R9's five names and dropped the fifth.** `release/kcf-finals-2026/MANIFEST.json` lists **17 files** and not one of them is a printable; I read it at this head. `scripts/build_finals_bundle.py:57` `PAYLOAD` names the four screens, `web/assets`, `web/demo-media`, `CITATION.cff`, `LICENSE` and `check_bundle_copy.py`, and no PDF. `docs/auto/KCF_READINESS.md` R9 asks for 「`web/` whole, **printables**, `README_KO.md` … 」 and R9's own cell gives 「the printables the line names (R7 / WFG-007 — none exist)」 as one of the two reasons it cannot tick. **They exist now.** `3e92b69` built the first kit at 2026-09-06T06:51Z; the bundle manifest was rebuilt **2 h 43 m later** at `1ec1d06` (09:34Z) and gained only a new `web/finals.html` hash. `d0a739e` built `WFG_printables_20260907T0059Z.pdf` (33 pages, sha256 `a4970b12cdd1…`, tracked) and the bundle did not move again. **And the reason no gate said so is the shape this loop fixed one directory over yesterday:** `tests/test_finals_bundle.py:41` `test_the_manifest_is_committed_and_covers_every_planned_file` compares the manifest to `bfb.plan()` — the builder's own plan — so the manifest and the builder can agree with each other forever while both omit the same file; and `:74` `test_the_bundle_carries_the_four_screens_the_booth_opens`, whose docstring says 「R9 names `web/` whole」, is the only place R9's list is transcribed, and it asserts the four screens, the fonts, `CITATION.cff`, `LICENSE` and `README_KO.md` and never the printables. That is WFG-140's defect (a manifest read against itself) in `tests/test_finals_bundle.py`. **Done when:** (a) the newest-stamp printables PDF and its manifest are in `PAYLOAD` and in the committed `MANIFEST.json`, and `make finals-bundle` still rebuilds byte-identically; (b) a test binds R9's **named contents** to the plan, in the shape WFG-130 used for `R7_ITEMS` — R9's names resolved to paths, with a written reason for anything deliberately excluded — and it is **graded red** by removing the printables entry before the fix makes it green; (c) `release/kcf-finals-2026/README_KO.md` says in one line what the paper in the folder is and that the manifest carries its sha256. ⚠ **No new committed bytes:** `release/kcf-finals-2026/web/` is git-ignored (`.gitignore:439`) and the payload is generated, so this does **not** breach DIRECTION's 「do not commit the bundle payload」; only `MANIFEST.json` and `README_KO.md` are tracked and both already are. ⚠ Do **not** overwrite `WFG_printables_20260906T0620Z.pdf`, `…T0032Z.pdf` or any manifest (CHARTER §3.2); the bundle carries the **newest stamp** and `docs/printables.md` already says why the `0032Z` build must not be printed. ⚠ Do not edit `docs/auto/JUDGE_QA.md`, `docs/auto/DEMO_SCRIPT_5MIN.md` or `docs/auto/finals/BOOTH_SETUP.md` in the same lap without rebuilding the kit (WFG-152). ⚠⚠ **SHIPPED 2026-09-07T0320Z.** `MANIFEST.json` is **19 files**; the two new ones are `printables/WFG_printables_20260907T0059Z.pdf` and its manifest, git-ignored like the rest of the payload so no committed bytes were added. The stamp is **resolved, not typed**: `newest_printables()` takes the lexicographic maximum of the tracked `WFG_printables_*.pdf` stamps and then requires the manifest at that same stamp, so the next `make printables` moves the plan and `make finals-bundle` fails until the kit's own lap rebuilds the bundle — probed by staging a synthetic later stamp (3 tests red, builder exit 2) and by a half-committed kit (`SystemExit` naming the missing manifest). `R9_ITEMS` binds R9's five names to predicates over the **committed manifest** rather than `bfb.plan()`, bound in turn to R9's **definition cell** (not the whole row, whose status half this lap wrote); graded red against the 17-file manifest, failing on exactly `printables`. ⚠ **The independent reviewer BLOCKED this row and was right**: the exclusion reason 「the 29 dispatch sheets are already committed PDFs」, copied from `R7_ITEMS` and never checked against the tree, reached the judge-facing `README_KO.md`. 33 clusters, 3 committed PDFs, and 「29」 traces to a research-brief aspiration — a CHARTER §3.3 break in the shipped bundle. Fixed in all three places, plus `EXCLUSION_EVIDENCE` and `test_the_dispatch_exclusion_reason_matches_what_is_committed` so an excused item can no longer ship on an unchecked sentence. The reviewer also found the docstring's grading record irreproducible (the ablation that fires is manifest-side, not plan-side); corrected. Residue for the critic: **WFG-153** | done(20260907T0320Z) | true | twenty minutes | 제출 자료 · 구현 및 유용성 (R9) |
| WFG-026 | **P0** | KCF | ⚠ **MOVED P1 → P0 BY CRITIC #31, 2026-09-07T0206Z — this lap's one row move (CHARTER §14, one reorder per lap, reason on `docs/auto/DIRECTION.md`).** Related-work table with Crossref-resolved DOIs only + one-panel differentiation from ISEF 2026 SFTD059T. **The reason is not the row's own merit, it is that this row is the sole remaining blocker of a readiness line that has not moved in eight consecutive critic laps.** `docs/auto/KCF_READINESS.md` R7 names five printables; three are in the `20260907T0059Z` kit, the 29 dispatch sheets are excused in writing (already committed PDFs), and the fifth — 「related-work and SFTD059T differentiation panel」 — **is not written**, which the kit's own `what_this_does_not_show` states in those words. So R7 = ☐ with exactly one thing missing, and CHARTER §14b holds five P1 infra rows (WFG-119, 131, 132, 137, 141, 149) behind R7 while R7's only blocker sat at **P1 itself**. That is an ordering defect, not a priority judgement. **The panel should now carry the two Korean operational systems** (`docs/auto/knowledge/KOREAN_OPERATIONAL_SYSTEMS.md`, `paper/manuscript.md` §2 「Operational systems in Korea」, `references.bib` `nifos2026guide` / `khan2026gdaps`), which makes this row absorb most of **WFG-144**'s content; WFG-144 stays filed for the spoken Korean card. ⚠ **No accuracy comparison with NIFoS or G-DAPS in either direction, on any surface** (DIRECTION): the differentiator is the output object. ⚠ A lap that writes the panel and wants it in the printed kit must **rebuild the kit in the same lap**, because the panel becomes a `SOURCES` entry and WFG-140's gate reads sources against the tree. | done(20260907T0630Z) | true | one lap | 설계와 방법론 (기존 연구와의 차별점) · 제출 자료 (R7) |
| WFG-153 | **P0** | KCF | ⚠⚠ **RAISED P1 → P0 BY CRITIC #32, 2026-09-07T0500Z — this lap's one row move (CHARTER §14, one reorder per lap, reason on `docs/auto/DIRECTION.md`), AND ITS (a) HALF IS THIS LAP'S ONE `fix-before-next-row` ITEM (CHARTER §14b: the release bundle is named there as a judge-facing surface).** The sentence the 0355Z lap declared **false** is live on the stick: `release/kcf-finals-2026/printables/manifest_20260907T0059Z.json:95` carries it, and so does the repository original, because the manifest is a stamped artifact that only a kit rebuild can move. **The 0355Z lap corrected it in three places and registered it in none, which CHARTER §3.5c forbids in those words** — `docs/auto/withdrawn_claims.json` holds WC-001 to WC-005 and no sixth. ⚠ **Registration was PROBED here rather than argued, at `0fc6130`:** adding the two spellings to the registry and running `scripts/check_withdrawn_claims.py` exits **1** naming exactly one file, `docs/finals_bundle.md:86`, which is outside `docs/auto/` and which this routine may not edit — so the critic cannot register it either, and the probe was reverted (`git status` clean, rescan `PASSED === 5 claims over 929 gated files`). ⚠⚠ **And the scan reached NEITHER place the claim is actually live**, because `withdrawn_claims.json` → `scope.extensions` is `[".md", ".html"]`: not the manifest (`.json`), and not `scripts/build_printables.py:648`, which is where the sentence is authored — and which a line-based scan would miss even with `.py` added, because the phrase is split across implicit-concatenation lines at `"print "` / `"directly"`. That is **WFG-155**. **Sequencing:** (a) rides WFG-026's kit rebuild (WFG-152), which is the only lap allowed to move the manifest. Original row text follows. **R7's line says 「29 dispatch sheets sample」, the tree has 33 clusters and 3 committed PDFs, and neither number is registered.** Found by WFG-151's independent reviewer after the false half of the same sentence — 「already committed PDFs that print directly」 — reached the judge-facing `release/kcf-finals-2026/README_KO.md`. Measured: `outputs/dispatch/20260801T163042Z/` holds 33 cluster directories; `git ls-files outputs/dispatch` counts 33 `dispatch_a4.html`, 33 `sms_drafts.json`, 33 `broadcast_script.txt` and exactly 3 `dispatch_a4.pdf`; `outputs/dispatch/README.md` §「What is committed」 says only the three largest clusters' PDFs are committed because 33 would be 6.7 MB of regenerable output. 「29」 originates in `docs/auto/research/RESEARCH_BRIEF_2026-09-03.md` as a thing to print, not as a count of anything, and `docs/NUMBERS.json` has no key for it. WFG-151 corrected the reason in `R7_ITEMS`, in R7's evidence cell, in `docs/finals_bundle.md` and in the bundle README, and added `EXCLUSION_EVIDENCE` so an exclusion reason is checked against the tree. **What is left is the two places WFG-151 could not touch:** (a) R7's own **definition** wording 「29 dispatch sheets sample」, which is bound by `tests/test_printables.py::test_r7_still_enumerates_the_five_printables_this_list_resolves`, so rewording it and the mapping is one edit and is the **critic's** call on a readiness definition, not a dev lap's; (b) `docs/auto/finals/printables/manifest_20260907T0059Z.json` `what_this_does_not_show`, which repeats it and is a committed artifact, so it changes only at a **new stamp** (CHARTER §3.2) — i.e. it rides the next kit rebuild, with WFG-140's freshness gate covering it. **Done when:** R7 names a number the tree supports or no number at all, the mapping moves with it, and the next kit's `what_this_does_not_show` says 「regenerable from a committed artifact」 rather than 「already committed PDFs」. ⚠ Do not re-render the 30 missing PDFs into the repository (CHARTER §3.2; 6.7 MB of regenerable output is exactly what `outputs/dispatch/README.md` declined). | done(20260907T0630Z) | yes | minutes | 제출 자료 · R7 |
| WFG-133 | **P0** | KCF | ⚠⚠ **ADDED AT POSITION 1 BY CRITIC #27, 2026-09-06T1400Z, AND IT IS THIS LAP'S ONE `fix-before-next-row` ITEM (CHARTER §14b: judge-facing surface).** **`docs/auto/JUDGE_QA.md` Q35 scripts the student to say a false sentence about this repository to a judge.** Q35 is **T1** and is the reproducibility question (「화면 아래 적힌 커밋을 받아서 이 화면을 그대로 다시 만들 수 있습니까?」). Its ⚠ block tells the student **not** to say the draft's true sentence 「현재 브랜치에서 닿는 커밋입니다」 and to say instead 「레지스트리 카드의 각인은 … 지금 브랜치에서 닿지 않습니다」. Measured at `dd500e6` on a clone **fully unshallowed** (`git rev-parse --is-shallow-repository` answers `false`, 488 commits, so the instrument is not the confounder this time): `git merge-base --is-ancestor 41498ef HEAD` exits **0**, `git rev-list --count 41498ef..HEAD` answers **283**, `git rev-list HEAD \| grep -c 41498efbf0679276c140b3cbfc0819e5265e7733` answers **1**, and `git branch -a --contains 41498ef` names `auto/dev`, `origin/auto/dev` and `origin/Main`. The block also asserts the card prints **326**, where the screen prints **383** (WFG-113, `1ec1d06`). Critic #26 withdrew this measurement at 1100Z and wrote 「**`JUDGE_QA.md` Q35 is correct as written and must not be edited**」 into `CRITIC_LATEST.md`, `KCF_READINESS.md` R1 and `DIRECTION.md`. That sentence is true of Q35's **draft answer** and false of the ⚠ block that overrides it, and the effect of writing it was to protect the false half from repair for a full window. **The withdrawal reached every loop-internal page and stopped one file short of the one a human reads aloud.** ⚠ **Critic #26's instruction not to edit Q35 is hereby lifted for the ⚠ block only; the draft answer above it is correct and stays.** A dated ⚠⚠ correction note is already on Q35 at `HEAD` (critic #27) with the measured table, so nobody rehearses the false block tonight; the row's job is to make the card itself true and to gate it | done(20260906T1520Z) — **the card was already true at the claim commit; the gate was missing because the withdrawal had never been REGISTERED.** Critic #27 made Q35's prose true in the lap that filed this row (`a64b904`, font-corrected at `39269e3`). Re-verified here at `6cac7c6` on an **unshallowed** clone (`git rev-parse --is-shallow-repository` = `false`, 491 commits): `merge-base --is-ancestor 41498ef HEAD` exits **0**, distance **286**, `branch -a --contains` names `auto/dev` and `origin/Main`; the registry holds **383** entries and held **153** at `41498ef`, both counted in one process. ⚠ **The root cause is one level up from where this row put it, and the lap found it by nearly rebuilding a machine that already exists.** WFG-062 shipped `docs/auto/withdrawn_claims.json` + `scripts/check_withdrawn_claims.py` on 2026-09-04: one registry of withdrawn claims, checked against **every tracked `.md`/`.html`** inside `make verify`, with a declared **record class** (reports, MEMO, BACKLOG, SCORECARD, CRITIC_LATEST, KCF_READINESS, DIRECTION, CHARTER, ...) that is deliberately not scanned. Critic #26 withdrew the reachability claim across **four** pages and never registered it. ⚠ **This lap's first draft said 「three pages, all record class」 and the independent reviewer disproved it from the cited commit**: `git show --stat 828bbae` shows critic #26 also edited `docs/auto/JUDGE_QA.md` (**+19 lines**), which is NOT record class. The true statement is narrower and stronger: the withdrawal *did* reach the student's card file, but only Q30's card, and left the same claim standing in Q35 eight sections away. A lap picks which documents to correct and misses one; registration is what makes the machine read all 925. **What shipped:** the claim is registered as **`WC-004`** with two spellings (the assertion `브랜치에서 닿지 않`, and the instruction `닿는 커밋입니다」 문장을` that told the student *not* to say the true sentence — the object particle is what separates it from the current card's true `문장은 옳고`), each with a probe sentence taken verbatim from `JUDGE_QA.md` at `dd500e6` rather than invented from the regex; the two record lines that quote the claim in order to withdraw it carry `forbidden-ok` pragmas; and **CHARTER §3 rule 5c** now makes registration part of the withdrawal itself, in the same lap. Checker: `4 claims over 925 gated files`, PASSED. ⚠⚠ **The reviewer BLOCKED the first version and was right: the second spelling was INVERTED.** It matched the object particle `문장을` on a `why` that claimed the particle distinguishes prohibition from permission — false about Korean — so it fired on all **3/3** natural true phrasings of the position this repository now holds and missed **5/6** reworded false ones, which would have pressured a later lap to delete a TRUE sentence off the student's card. Fixed to match the prohibition itself (`말하지\s*[마말]`, both Korean negative-imperative endings) and four spellings the reviewer found were missing were added, one of them **live and unlicensed eight lines above the lines this lap had just licensed, in the same file**. Re-measured: **0/5** false positives, **6/6** caught. The reviewer's nine sentences are committed as `REVIEWER_SET_TRUE`/`REVIEWER_SET_FALSE` so the score is re-derived every lap, not restated. Also graded by replaying the real defect: with `JUDGE_QA.md` from `dd500e6` the checker reports unlicensed mentions and `make verify` goes red. ⚠ **A gate this lap built and then deleted unbuilt** — a 5-surface list parsed from the charter — is recorded in the report as the lap's own near-miss; it would have been a weaker reimplementation of a better mechanism, and the reason it was nearly shipped is **WFG-137**. Doc: `docs/withdrawn_claims.md` §5a | yes | minutes | A 제출 자료, B 제출 자료, R1, R5 |
| WFG-138 | **P0** | KCF | ⚠⚠ **ADDED AT POSITION 1 BY CRITIC #28, 2026-09-06T1736Z, AND IT IS THIS LAP'S ONE `fix-before-next-row` ITEM (CHARTER §14b: the README is named there as a judge-facing surface).** **The README's TL;DR asserts the exact attribution the project's own manuscript abstract disclaims one sentence later, and the disclaimer was written by this loop.** `README.md:22-26`, the 「Headline result」 bullet, reads 「On the canonical Yeongdeok field, **42 of 458** scanned origins reach a refuge **only** when the router accounts for where the fire **will be**, and **2** have no safe walking route at all ([Round 3]; the 32.6 % coverage caveat applies)」. `paper/manuscript.md`'s Abstract carries the same two numbers and then says: 「That contrast is measured against a fire-blind baseline, so it does not separate knowing where the fire will be from knowing where it is.」 `paper/GAPS.md` G7 records that the abstract 「had the same overclaim ... and it was corrected this lap」, and names the two other surfaces the loop already repaired for the same claim: the booth script's 3막 sentence (**WFG-103**, `92366cb`) and the finals template (**WFG-109**). **The README is the fourth surface and it was never touched.** The baseline is fire-blind in this repository's own words: `src/wildfireguardian/routing/evacuation.py:270` 「Fire-blind shortest path to the nearest shelter, then scored against the hazard」 and `docs/real_roads_real_hazard.md:50` 「the fire-blind shortest walk to the nearest refuge (the status quo)」. ⚠ **Why this is the item and not a note.** The README is the first thing a KCF judge, an ISEF reviewer or an IEEE reader opens, it is what `CITATION.cff` points at, and its own TL;DR advertises a withdrawn-claim record three bullets below the unqualified sentence. A reader who opens the manuscript next finds the repository contradicting itself on the number it leads with, on the axis the project asks to be judged on. **This is also the third instance in three windows of critic #27's root objection** (a correction that reached the loop's pages and the manuscript and stopped at the surface a human meets) and the mechanism the last window built does not reach it: this claim was **narrowed, not withdrawn**, so it is not in `docs/auto/withdrawn_claims.json` and `check_withdrawn_claims.py` cannot see it. ⚠⚠ **CRITIC #29, 2026-09-06T2015Z — CARRIED FORWARD AS THIS LAP'S ONE `fix-before-next-row` ITEM (not re-filed), AND WIDENED BY ONE LINE, BECAUSE THE JUDGE DRILL FOUND A FIFTH SURFACE AND IT IS THE ONE THE STUDENT READS ALOUD.** `docs/auto/JUDGE_QA.md` **Q19**'s draft answer contains the sentence 「영덕에서 458개 원점 중 **42**개, 의성·안동에서 368개 중 **91**개가 **시간 인지 경로에서만** 대피 지점에 닿습니다」. The ⚠ block immediately beneath it, written by critic #22 and tightened by critic #23, corrects the **91** (「91은 불을 전혀 보지 않는 대조군과의 비교입니다」) and says nothing at all about the **42** in the same sentence. Both numbers come from the same fire-blind control (`src/wildfireguardian/routing/evacuation.py:270`), and 영덕 needs the caveat **more**, not less: the present-perimeter opponent has only ever been run on 의성·안동 (`data/processed/present_perimeter_arm_uiseong_andong_2025.json`), so for the 42 the fair opponent has never been run at all. **So the correction reached one half of one sentence and stopped.** That is critic #27's root objection at its smallest scale yet: not one file short of the human surface, but one clause short inside the human surface. A dated ⚠⚠ note with the sentence to say is on Q19 at this head (`docs/auto/JUDGE_QA.md`, critic #29) so nobody rehearses the bare 42 tonight; 50 judge-qa, printables and fair-opponent tests are green over it. ⚠ **Why the item is carried and not replaced:** the 18:17Z dev slot was ceded to research (CHARTER §14), so **no dev lap ran in this critic window at all** and WFG-138 has never yet been in front of a dev lap. Critic #28's falsifiable test on it (「if README:22-26 still says 「only」 at the next critic head, the finding is about the dev lap's step 3」) **could not be run and its verdict is not reported**; it runs cleanly for the first time at the next critic head, because the 20:17Z slot is a dev slot. **Done when:** the bullet carries the manuscript's own caveat, in the manuscript's own words or a faithful Korean/English equivalent, so that the README says no more than `paper/manuscript.md` says. Suggested minimal edit, additive, no number moves and no registered value changes meaning: append to the bullet 「That contrast is measured against a **fire-blind** baseline, so it does not separate knowing where the fire will be from knowing where it **is**; the present-perimeter opponent has been run on 의성·안동 only (`docs/present_perimeter_arm.md`, WFG-129 for 영덕)」. **And (b), added by critic #29:** `docs/auto/JUDGE_QA.md` **Q19**'s draft answer no longer states the 42 without the fire-blind clause — the correction goes into the draft the student speaks, not only into the ⚠ note above it, since the ⚠ note is what already failed to reach it. Grade (b) by mutation: put the bare sentence back and a test in `tests/test_judge_qa_bank.py` should go red naming Q19. **Constraints:** this is the 「Headline result」 bullet and **not** the README's opening paragraph about the 2025 fire, which CHARTER §3.5b and `DIRECTION.md` protect — do not touch that paragraph. Put **no** fair-opponent margin (9, 27, 5, 19) in the sentence; NH-032 is open and `DIRECTION.md` bars every margin from every judge-facing surface. Do not enlarge this row into WFG-129's experiment or WFG-125's oracle question; it is one clause. ⚠⚠ **DEV LAP 2026-09-06T2117Z — BOTH HALVES DONE, AND THE GATE I WROTE FOR (a) WAS GREEN ON THE DEFECT IT WAS WRITTEN FOR UNTIL I GRADED IT.** (a) `README.md`'s 「Headline result」 bullet now carries the manuscript's own caveat in the same bullet, naming `evacuation.py:270`, `paper/manuscript.md` and `docs/present_perimeter_arm.md`, and saying that on 영덕 the fair opponent has never been run; no number moved and no margin (9/27/5/19) reached the surface. (b) Q19's **draft answer** — the sentence the student speaks — now reads 「42도 91도 **불을 전혀 보지 않는** 경로와의 비교입니다. 지금 불난 자리를 피하는 경로와의 비교는 의성·안동에서만 했고, 영덕에서는 아직 하지 않았습니다.」, inside the quoted draft rather than in a ⚠ block beside it. **Two gates, both graded by mutation, both red on the pre-lap text and green after:** `tests/test_judge_qa_bank.py::test_no_draft_answer_states_the_future_aware_only_claim_bare` (red naming Q19 when the sentence is removed; it reads **every** card's draft, not only Q19) and the new `tests/test_future_aware_attribution.py` (README + manuscript, red on the pre-lap README bullet and on a manuscript block with `fire-blind` struck out). ⚠ **The first spelling of the second gate passed on the pre-lap README** because the surface writes `**only**` and `**42 of 458**` and a literal scan sees neither through the emphasis marks and the line break; it now flattens `*`, `` ` `` and whitespace before matching. MEMO 2026-09-06T2117Z. ⚠ This lap is the **fourth** drift of `docs/auto/JUDGE_QA.md` against the printed manifest (critic #29's root objection) and it does not exempt itself — but unlike the first three it is a judge-facing improvement to a draft answer, not a correction note, which is critic #29's own falsifiable test 3 answered in the direction it did not predict. ⚠⚠ **THE LAP REVIEWER PASSED THIS AND THEN BROKE THE ENGLISH GATE WITHOUT DELETING ANYTHING:** it restored the pre-lap overclaim and **moved** a caveat four bullets down the same TL;DR list, and the gate stayed green, because `_blocks()` split on blank lines and README's TL;DR is one block holding four bullets — so the gate asked only that the word appear somewhere in the list, which is 「a note beside the sentence」, this row's whole subject. Rebuilt in-lap: a Markdown list item is its own block, `docs/auto/DEMO_SCRIPT_5MIN.md` joins the two English surfaces, both gates now share one CONTROL/ATTRIBUTION spelling family (the reviewer scored the one-token versions 3/7 and 2/7 on its own sentences, **flagging correct sentences** among the misses — the WC-004 direction), the reviewer's fourteen sentences are committed as the probe set and re-scored on every run, and the one class no spelling list reaches (a reworded overclaim with no 「only」) is a `strict=True` xfail rather than an unmentioned hole. Graded by **delete and move**, both red | done(20260906T2117Z) | yes | minutes | A 제출 자료, B 제출 자료, B 데이터 수집·분석·해석, R8 |
| WFG-148 | **P0** | KCF | ⚠⚠ **ADDED AT POSITION 1 BY CRITIC #30, 2026-09-06T2317Z, AND IT IS THIS LAP'S ONE `fix-before-next-row` ITEM (CHARTER §14b: the README opening is named there as a judge-facing surface).** **The README's headline bullet now carries the first of the two caveats the project's own manuscript says bind that comparison, and not the second, and the gate written in the same lap certifies the bullet as caveated.** `paper/manuscript.md:506` reads, in this loop's own words: 「Two caveats bind the whole comparison. The forecast-aware arm plans on the same hazard field it is graded against, so whatever it is worth against a present-perimeter policy is what a *noiseless* forecast is worth; this project's own model is worth less, by an amount no run here measures.」 WFG-138 put caveat one (the control is fire-blind) into `README.md:22-33` at `0ed15aa`. Caveat two is absent from the README entirely: a case-insensitive `grep -E` over `README.md` for the words `perfect`, `oracle`, `upper bound`, `상한` and `noiseless` returns **nothing** at `524f13c`. **It applies to the 42 for the same reason it applies to the 91.** `docs/present_perimeter_arm.md` §5 states it and says the property is 「a property the 91 has always had, inherited not introduced」; the 42 is the 영덕 sibling of that design, and `docs/real_roads_real_hazard.md` says both arms are handed the same `HazardSequence` while `src/wildfireguardian/routing/evacuation.py:270` says the fire-blind arm is 「then scored against the hazard」. So the forecast-aware arm plans on the field it is graded against on 영덕 too, and the 42 is what a noiseless forecast buys, not what this project's model buys. **The gate makes this harder to notice, not easier.** `tests/test_future_aware_attribution.py:135` `_is_caveated(block)` returns True when the block holds any one spelling of the CONTROL family (`fire-blind`, `불을 보지 않`, ...) and asks nothing else, so the README bullet is green under the new gate while half of what the manuscript calls binding is missing from it. A gate that certifies 「this sentence is caveated」 is only as wide as its list of caveats, and this list was written from the one defect in front of the lap. **The judge already has the question and the bank already has the answer.** `docs/auto/JUDGE_QA.md` **Q36** (「비교하신 예보 경로는 정답을 미리 본 예보 아닙니까?」, filed by critic #23) is in the 근거 없음 table and tells the student to say 「맞습니다. 그건 완벽한 예보가 사는 값의 상한이고, 저희 모델이 실제로 사는 값은 아직 재지 않았습니다」 **before the judge digs**. The README is the one surface that invites the question and does not answer it. ⚠ **This row does NOT touch `docs/auto/JUDGE_QA.md`.** Q36 already carries the sentence, and the printables manifest is already stale against that file for a fourth time (WFG-134); a fifth drift bought nothing. **Done when:** (a) `README.md`'s 「Headline result」 bullet states, inside the same bullet, that the forecast-aware arm plans on the hazard field it is scored against, so the 42 is an upper bound on what a noiseless forecast buys and not a measurement of this project's model, citing `paper/manuscript.md` and `docs/present_perimeter_arm.md` §5; additive prose, **no number moves**, no contested fair-opponent margin reaches any surface (NH-032 is open); (b) `tests/test_future_aware_attribution.py` gains an ORACLE spelling family and `_is_caveated` requires **both** families on `README.md` and `paper/manuscript.md`, graded by restoring the pre-repair README bullet (must go **red**, naming the file) and by a **move** mutation of the new clause into a neighbouring bullet (must go **red**; that is the mutation the previous gate survived, MEMO 2026-09-06T2117Z); (c) `docs/auto/DEMO_SCRIPT_5MIN.md` is checked and left alone unless it states the attribution, because it is a printables source and editing it drifts the kit again. ⚠ **Budget: twenty minutes and one clause. Extend the module WFG-138 shipped; do not write a new one.** Then claim **WFG-134 with WFG-140 and WFG-130** in the same lap and do not idle (CHARTER §11): R7 and R9 wait on nothing else, and this is the fifth consecutive dev lap the loop's own corrections would otherwise take. ⚠⚠ **DEV LAP 2026-09-07T0018Z: (a) and the README half of (b) shipped; the done-when's manuscript half is AMENDED IN WRITING on a measurement rather than met, and (c) was measured and deliberately not acted on.** **(a) shipped:** the 「Headline result」 bullet now carries, in the same bullet as the number, 「the forecast-aware arm plans on the same hazard field it is scored against, so 42 is an upper bound: what a noiseless forecast would buy, not what this project's own model buys」, citing `paper/manuscript.md` §4.5 and `docs/present_perimeter_arm.md` §5. Additive, no number moved, no fair-opponent margin on the surface. **(b) shipped for README:** `tests/test_future_aware_attribution.py` gains the `ORACLE` family and `test_the_headline_contrast_names_its_oracle_bound`, graded red BOTH ways on today's tree — restoring the pre-repair bullet fails naming `README.md`, and moving the new clause into the neighbouring bullet also fails, which is the mutation the first gate survived. ⚠ `_is_caveated` was NOT widened to require both families, and that is a decision with a reason: it is the predicate `test_the_gate_never_fires_on_a_correctly_caveated_sentence` scores over four reviewer sentences that name the control and say nothing about a noiseless forecast, and every one of them is CORRECT. Folding the second family into that predicate makes the gate fire on true sentences, which MEMO 2026-09-06T1520Z calls the worse direction. The second caveat is a per-surface requirement instead. **(b) amended for the manuscript, on a number:** `paper/check_paper.py` reports `body_words` **8,983** against a hard fail at **9,000**, so the two clauses the manuscript's Abstract and Conclusion blocks would need do not fit, and the budget itself is the open question in NH-037. The manuscript is not silent — §4.5 states the caveat in the words every other surface quotes — so what is missing there is locality, not the claim. Recorded as a `strict=True` xfail, `test_the_manuscript_claim_blocks_do_not_yet_name_it`, so the gap is re-measured on every run and the day the clause lands the test fails and asks for promotion. Carried by **WFG-150**. **(c) measured, not acted on:** `docs/auto/DEMO_SCRIPT_5MIN.md:107` DOES state the attribution (「458곳 중 42곳 ... 시간 인지 경로에서만」) and does carry the fire-blind control inside the spoken block. It was left alone because it is a 61-second spoken script whose companion answer already exists as `docs/auto/JUDGE_QA.md` **Q36**, and because this row's own ⚠ says a fifth printables drift buys nothing. Also carried by **WFG-150** | done(20260907T0018Z) | yes | minutes | A 제출 자료, B 제출 자료, B 데이터 수집·분석·해석, R8 |
| WFG-134 | **P0** | KCF | **The booth printables PDF is stale against the Q&A bank it prints, the staleness is exactly the text this window withdrew, and no gate reads for it.** `docs/auto/finals/printables/manifest_20260906T0620Z.json` records `docs/auto/JUDGE_QA.md` at sha256 `2c8451211e5f97eb…`; the file at `dd500e6` hashes `af955a30fa500391…`. The manifest's other three sources still match, so the drift is one file and it is the 17-page one. `WFG_printables_20260906T0620Z.pdf` was built at `3e92b69` (06:20Z) and WFG-117 rewrote Q30 at `fc05320` (12:xxZ), so **the paper the student physically carries to the booth still holds the pre-WFG-117 Q30 with the 「326 · 268」 warning that this window removed, plus Q35's false block**. `tests/test_printables.py` has 15 tests: `test_the_manifest_records_what_the_pdf_was_built_from` checks the manifest *has* a sha256 per source, and `::test_every_pdf_has_its_manifest_beside_it` checks the PDF matches its own recorded hash. **Nothing compares a recorded source hash against the working tree**, which is the one comparison that detects a stale printable, so the suite is green on a booth kit that is out of date. CHARTER §3.2 forbids overwriting the committed PDF: a corrected build gets a new stamp and sits beside it. ⚠ **CRITIC #28, 2026-09-06T1736Z: re-measured at `e95fe28`, and the drift is LARGER than the cell above records.** The manifest still says `2c8451211e…`; the tree now hashes **`7d5ac4c9c5…`**, not `af955a30fa…`, because the WFG-133 lap edited Q35 again at `923ffbd`/`32de531` after critic #27 measured it. The other three sources still match, checked here one by one. So the printed 17 Q&A pages now carry the pre-WFG-117 Q30 **and** Q35's ⚠ block with **no** retraction note on it, because the ⚠⚠ correction critic #27 wrote landed at `a64b904`, after the `3e92b69` build. This row rebuilds; **the gate that would have caught it is now its own row, WFG-140**, per critic #27's pre-registered branch, so that the gate cannot be lost if the rebuild slips a fourth window ⚠⚠ **CRITIC #30, 2026-09-06T2317Z: FOURTH drift, re-hashed here in one process at `524f13c`, and this one is not like the first three.** The manifest still records `docs/auto/JUDGE_QA.md` at `2c8451211e5f97…`; the tree hashes **`5ac45ea8103f11…`**. The other three sources still MATCH one by one (`BOOTH_SETUP.md` `ef7342dacf…`, `DEMO_SCRIPT_5MIN.md` `b1aae78f35…`, `DETECTION_FLOOR_CARD.md` `84648d4d6e…`) and the PDF still matches its own recorded sha256, so `tests/test_printables.py` stays fully green over a stale kit. The series on that one file is `af955a30fa…` (#27) → `7d5ac4c9c5…` (#28) → `175da9e50c…` (#29) → **`5ac45ea810…`** (here). ⚠ **Critic #29 called all three earlier drifts correction notes rather than judge-facing improvements. This fourth one is the opposite, and that makes the printed pages worse rather than merely older:** the WFG-138 lap put the fire-blind caveat into Q19's spoken draft at `0ed15aa`, so the 17 printed Q&A pages the student physically carries now hold the Q19 answer **without** the caveat this repository has since decided is mandatory and has gated in two test modules. The paper in the student's hand and the file the gates read now disagree about what the student is allowed to say. This is the strongest form the row has had and it is still `todo` ⚠⚠ **DEV LAP 2026-09-07T0018Z: rebuilt at a new stamp, and the fourth drift is closed.** `WFG_printables_20260907T0059Z.pdf`, **33 A4 pages** (5 · 6 · 17 · 3 · 2), sha256 `a4970b12cdd1…`, from **five** sources with `docs/submission_reconciliation.md` added (WFG-130). `WFG_printables_20260906T0620Z.pdf` and its manifest are untouched beside it (CHARTER §3.2). The freshness gate that would have caught all four drifts shipped in the same lap and was **red on the pre-rebuild tree** before the rebuild made it green (WFG-140). The printed Q&A pages that held Q19 without the WFG-138 caveat now hold it. ⚠⚠ **The independent reviewer BLOCKED this lap and was right three times, all three fixed before the push.** (1) **CHARTER §3.5c broken by this lap:** it declared 「the A4 evidence sheet does not exist yet」 false in three places and registered nothing, and the claim was still standing at four places in `docs/auto/finals/BOOTH_SETUP.md` — `SOURCES` entry #1, so the first build's pages 1-5 told the student the kit did not exist while pages 21-23 were the sheet it said did not exist. Now **WC-005**, swept, and rebuilt at `20260907T0059Z`. (2) **The page decomposition did not sum.** 6+7+18+4+3 = 38 against a `pages` of 33; `build_printables.py` counted `r.page - start + 1` while the h1 block had already turned the page, so every value was one too large and the error telescoped. The 06:20Z manifest had the same defect (6+7+17+3 = 33 against 29) and this lap re-derived it onto four surfaces under a true sentence saying it was re-derived. Fixed at the source, and `test_the_per_source_page_counts_sum_to_the_page_count` is the two-second check nobody had run. (3) The 「약 439 KB」 in `docs/printables.md` traced to nothing and is gone | done(20260907T0018Z) | yes | one lap | A 제출 자료, A 구현 및 유용성, R7, R9 |
| WFG-062 | **P0** | infra | `tests/test_detection_ordering_is_not_claimed.py` gates ONE withdrawn sentence shape over FOUR named documents, and `scripts/check_forbidden.py`'s five `claim` rules are the only other sentence-level gate in the tree. Every other gate reads a value, which is critic #5's and critic #6's shared root objection two windows running. Generalise: a registry of withdrawn CLAIMS (id, banned spellings, the artifact that withdrew it, the pragma token) that any document can be checked against, so a fifth document asserting the ordering tomorrow is caught. Absorbs the per-row hand-rolled gates (dev lap 20260904T0419Z). ⚠ **2026-09-06T1520Z: this row's registry is BUILT and running — `docs/auto/withdrawn_claims.json`, `scripts/check_withdrawn_claims.py`, `make check-withdrawn-claims` inside `make verify`, `4 claims over 925 gated files` — and the row's title still reads as though it were not, which cost the WFG-133 lap most of its build (WFG-137). What is genuinely left is the narrower half this cell already names: the two STRUCTURE rules (`priority_violations`, `english_ordering_violations`) reconstruct sentences across wrapped markdown and do not reduce to a registry line, so `tests/test_detection_ordering_is_not_claimed.py` still holds them. Retitle this row to that remainder.** **Critic #8 named the cheapest test of this row and the WFG-063 lap ran it (20260904T0820Z): 「if narrowing one sentence in three files and adding one gate closes WFG-063 and WFG-067, WFG-062 is the right generalisation」. The answer is stronger than that. WFG-063 alone took FIVE files — the fourth was `docs/SESSION19_REPORT.md`, a session report nobody had listed, which still carried the rank table and the primacy sentence unannotated — and it needed a SECOND hand-rolled claim family in the same test file, whose first draft caught six of nine mutations. There are now two claim families, six regex rules, two pragma-token vocabularies and two guard lists in one file named for only one of the claims. The next withdrawn claim makes it three.** **Raised P1 → P0 by critic #9 (F47), with a measurement rather than an adjective.** Critic #9 wrote twenty affirmative human-primacy sentences from the withdrawn CLAIM (not from the deleted sentences), having not seen the patterns, and ran them through the two shipped detectors: `primacy_violations` caught **0 of 20**, `priority_violations` caught **2 of 20**, and **18 of 20 are invisible to both** (script and full table in the critic #9 report). Three escape classes, none of them a spelling: a source noun outside `SOURCE_NOUNS` (「119 상황실이 일차 트리거입니다」), a priority word outside `PRIORITY_WORDS` (「사람 신고가 트리거의 출발점입니다」), and — the one that matters — **a negation morpheme anywhere else in the sentence** (「위성이 1 ha 아래를 보지 못하므로 최초 인지는 사람 신고입니다」), which is the shape every honest sentence in these documents already has. Any replacement must publish its catch rate against a mutation set its author did not write | **done(e350571)** — `docs/auto/withdrawn_claims.json` holds three claims (WC-001 ordering, WC-002 primacy, WC-003 both in English) with the 15 spellings copied byte for byte from the three tuples, each carrying what withdrew it, the artifact, the pragma token and what to say instead; `scripts/check_withdrawn_claims.py` (in `make verify`) reads EVERY tracked `.md`/`.html` minus a 73-file record class whose twelve members each carry a written reason. **Coverage 11 → 915 files.** Nothing deleted: the older file keeps its four families and a bidirectional drift test reconciles them. **The row's catch-rate clause is answered by declining to claim a new number:** the patterns are the same patterns, so sensitivity to a rewording is unchanged by construction, and the three externally measured scores stand as they were (critic #9 0/20 and 2/20; the 0855Z reviewer 1/20 and 8/20; WFG-070's reviewer 9/18). The dimension that DID move is measured instead and mechanically: a document in no guard list is gated by construction. First whole-tree run found 11 unlicensed mentions in two files the spelling families never read (SESSION19_REPORT 10, finals_screen_v2 1), all already withdrawn in prose and now licensed by pragma; no prose edited. One measured false positive in 915 files. The two structural rules are NOT absorbed and the reason is written down (WFG-072 keeps that half) | true | one lap | 데이터 해석 (재현) · 제출 자료 |
| WFG-128 | **P0** | KCF | **The one number in this repository that runs against the project is stated, on the page the README sends a judge to, in a form the project's own measurement has already contradicted.** `docs/multi_region.md:191` reads 「`fa_exceeds_budget` — fire-blind route safe, future-aware router cannot finish in time — is **0 for Yeongdeok** ... It is **2** for Uiseong-Andong and **3** for Uljin-Samcheok.」 `docs/present_perimeter_arm.md:46-63`, written by the WFG-114 lap after its own reviewer caught it, establishes the opposite: the committed classification scores the fire-blind route under **no** time budget while the forecast-aware router enforces the 600-minute one internally, those two Uiseong-Andong origins' fire-blind routes arrive at **624.8** and **628.2** minutes, and under one rule applied to both arms the bucket is **empty** — they are origins no arm saves, not origins on which the forecast lost. So the sentence's stated meaning does not hold for either of its own two members. Verified here rather than read from a lap: `grep -n fa_exceeds_budget docs/multi_region.md` returns that one line and it carries no caveat, no budget qualification and no pointer to `present_perimeter_arm.md`; `README.md:113` names the same bucket as 「예산 초과」 with 「각 2곳·3곳」 and sends the reader to this file for 「완전한 분할」. **The author already decided this.** NH-031 closed 2026-09-06, option A verbatim: 「Add a new key, annotate the old — register the budgeted reading (`mr_uiseong_fa_exceeds_budget_budgeted` = 0) beside the committed 2 and caveat the old entry; nothing committed moves.」 `WFG-122` carries the registry half at P1 and is `todo`. **The prose half needs neither the key nor NH-032**: it prints no margin, moves no committed value and adds no number that is not already in `present_perimeter_arm.md`. `paper/manuscript.md` §4.4 gained exactly this qualification in the 0323Z paper lap; `docs/multi_region.md` is its twin and did not — the WFG-126 shape, one window later and in the other direction. **Done when:** `docs/multi_region.md` §3.1 states that the two arms are scored under different time rules, that under one rule the Uiseong-Andong bucket is empty, and that Uljin-Samcheok's 3 has **not** been re-read and Yeongdeok's 0 cannot be (its walk graph of that vintage is not recoverable); the README bullet at `:113` carries a one-clause pointer to it; and a test fails if either surface prints that bucket's count without the qualification, graded by deleting the clause. **Constraints:** no committed value moves; no margin from the fair opponent reaches any surface while NH-032 is open, and this row needs none. ⚠⚠ **MOVED TO THE HEAD OF THE P0 BLOCK BY CRITIC #56, 2026-09-10T0523Z, and this is this lap's one CHARTER §3b reorder.** It moves above WFG-007 and WFG-117, P0 above P0, so §14's ordering rule is untouched. **Reason, measured at `9b7d21c` rather than inherited:** WFG-007's own status cell reads 「the agent half is done; the student half is not」 and its outstanding clauses are (a) the student printing the kit and ticking the checklist and (b) WFG-130, so a dev lap falling back to table order reaches a row with nothing takeable in it. `docs/auto/DIRECTION.md` went stale this window because the two rows it named, WFG-218 and WFG-220, both closed at `c4eb8d2`, and table order is what a dev lap uses when the page names nothing. **The defect this row names is still live, and I re-read the line rather than the row:** `docs/multi_region.md:187-192` now reads 「is **0 for Yeongdeok at 600 minutes** ... It is **2** for Uiseong-Andong and **3** for Uljin-Samcheok」, so a budget clause was added for the **future-aware** arm since the row was filed, and the sentence still does not say that the **fire-blind** arm is scored under **none**, still does not say that under one rule applied to both arms the Uiseong-Andong bucket is empty, and still carries no pointer to `docs/present_perimeter_arm.md`. `README.md:146` still sends a judge to this file for 「완전한 분할」. The done-when is unchanged and is still minutes. | done(5bcfe11) — §3.1 states the asymmetry at `classify()`, states the Uiseong-Andong bucket empty under one rule with `present_perimeter_arm.md` §2 behind it, and separates the other two entries; `README.md:145-149` carries the pointer in the same bullet block; `tests/test_budget_rule_asymmetry_is_stated.py` grades both surfaces in both directions, mutation-graded four ways. ⚠ The row's own 「Yeongdeok's 0 **cannot** be re-read」 was REPLACED by a stronger argument and the page says so: the bucket's membership test requires the fire-blind route to be safe, so budgeting that arm can only remove members and 0 cannot rise. The data loss is still true and is now the second reason, not the first. ⚠ Neither surface names 3 as a bound of any kind (NH-053 open). `mr_uiseong_fa_exceeds_budget` stays **2**; the registry half is `WFG-122` and is still `todo` | true | minutes | B 데이터 수집·분석·해석 · B/A 제출 자료 · R5 |
| WFG-007 | **P0** | KCF | **MOVED TO THE FRONT BY CRITIC #23, 2026-09-06 — this lap's one row move, and it is the only move that makes the table and `docs/auto/DIRECTION.md` say the same thing.** WFG-121 sat above this row and is now spent: its (a) shipped at `a182cc0`, its (b) is `blocked(NH-032)`, and its (c) needs WFG-100's pace re-allocation, which is a different row. A dev lap falling back to table order would have re-taken a row with nothing takeable in it while R7 entered its ninth day. P0 above P0, so CHARTER §14's ordering rule is not touched. **Raised P1 -> P0 and moved here by critic #20, 2026-09-05; this is the lap's one row move.** Critic #19 wrote the falsifiable test on `docs/auto/DIRECTION.md`: 「if WFG-109 is closed and the printables still do not exist, then WFG-007's priority is the defect, not its position」. WFG-109 is `done(20260905T1520Z)` and `docs/auto/finals/` holds two `.md` files and no PDF. This row alone holds readiness R7 and half of R9, which have not moved for five consecutive critic laps. Rehearsal aids + booth checklist (**revise**: `web/` is a directory, two USBs, affine drift on the booth laptop) | todo — **the agent half is done; the student half is not.** `done`: `scripts/build_printables.py` builds `docs/auto/finals/printables/WFG_printables_20260906T0620Z.pdf`, **29 A4 pages** (checklist 6, demo script 7, Q&A 17, detection card 3), from four committed Markdown sources in the screens' own IBM Plex Sans KR, with no PDF dependency beyond matplotlib + fontTools (both already declared); `make printables`, `docs/printables.md`, `tests/test_printables.py` (15 tests), manifest with every source and font sha256, byte-identical rebuilds. **Outstanding, and the row stays `todo` for them:** (a) the student prints it once and ticks the checklist with the screen open offline (the done-when's second half, human); (b) ⚠ **CRITIC #25, 2026-09-06T0800Z: half of this clause is false and the false half is the agent's half.** It reads 「the A4 evidence sheet (WFG-018) and related-work table (WFG-026) are not yet written, so they are not in the PDF」. WFG-026 is `todo` and genuinely absent. **WFG-018 is `done(20260903T0653Z)` in this same table (line 50)** and its artifact is `docs/submission_reconciliation.md` — 13,702 bytes of Korean prose whose own fourth line says 「인쇄본은 **양면 한 장**입니다」, i.e. it was written to be printed. `docs/auto/KCF_READINESS.md` R6 is ticked on that file. R7 enumerates 「reconciliation sheet」 as one of its five printables and `scripts/build_printables.py:97-101` does not list it. The correction is **WFG-130**, and it is minutes; (c) `outputs/dispatch*`'s 29 sheets are already committed PDFs and were deliberately not copied in. Released from a dead 03:20Z claim by the 06:17Z lap (CHARTER §5b), second stranding in two days | true (print/laptop: human) | hours | 제출 자료 |
| WFG-117 | **P0** | KCF | ⚠⚠ **MOVED TO POSITION 2 BY CRITIC #26, 2026-09-06T1100Z — this lap's one row move, and it is this lap's one `fix-before-next-row` item. The defect inverted inside one window: the warning is now the thing that is wrong.** WFG-113 landed at `1ec1d06` and `web/finals.html` now prints `n_entries` **383** / `n_reproducible` **325**, which is exactly what `docs/NUMBERS.json` holds, counted here in one process (383 entries, 325 reproducible, 58 not). `docs/auto/JUDGE_QA.md` Q30 is **T0** — the student answers it from memory, with no paper — and its ⚠⚠ block still tells them 「`web/finals.html`의 검증 레지스트리 카드는 아직 **326 · 268**을 인쇄합니다」, 「화면을 가리키라는 조언은 이제 학생을 낡은 숫자로 데려갑니다」 and 「지금 이 한 질문에 서로 다른 수가 셋 있습니다」. **All three sentences are false at this head.** The screen and the registry agree; there are two counts in play, not three, and the stale one is the 295 / 261 / 34 in the draft answer above the warning. A student rehearsing today is told to distrust the one surface that is now correct and to volunteer a drift that no longer exists, on the question a judge asks about why today's numbers should be believed. That is worse than the defect critics #21 and #22 filed, because a wrong warning survives a correct repair. ⚠ A dated correction note is on Q30 as of critic #26 so the student does not rehearse the stale block; the row's job is to make the answer itself true and gated. **Done when:** Q30's draft answer states the count the way the bank can keep true — it names `docs/NUMBERS.json` and the screen as the two places to read it and quotes no literal, or quotes one that a test re-derives; the two superseded ⚠ blocks are kept as dated records (CHARTER §3.7) and clearly marked superseded; `tests/test_judge_qa_bank.py` gains a check that **fails when any registry count literal in the bank disagrees with `docs/NUMBERS.json`**, graded against a mutation that moves the registry and one that moves the bank; and the bank states, once, that this count changes on every lap that registers a key, so a corrected literal is a defect with a shorter fuse rather than a fix | todo — **(a) done(`fc05320`), (b) outstanding; set back to `todo` by this lap's own independent reviewer, which returned `block` (CHARTER §5: `in-progress` is only ever held by a running lap, and a row whose done-when is not fully met is not `done`).** The reviewer's root objection, verbatim in the 1230Z report: a done-when clause satisfied by a **pre-existing** gate is a clause this row did not close — 「remove it from the diff and the row's registry-side grading disappears entirely」. **(b), the outstanding half, is one clause:** the done-when asks this row's own gate to be graded 「against a mutation that moves the registry」, and it is not; that mutation is caught by `tests/test_finals_payload_rederives.py::test_the_registry_card_counts_the_registry_it_ships_beside`, which shipped with WFG-113 at `1ec1d06` and is unchanged in this lap's diff. A next lap either writes a registry-side grading that belongs to this bank, or amends the done-when in writing with the measurement below as the reason. ⚠ Do **not** close it by re-adding a live count literal: that is the defect this row exists to remove. ⚠ **The reviewer's actual root objection was not the grading clause; it was in the prose, and it was right.** The first version of this lap's draft answer accounted for the irreproducible values in **two** buckets (the overwritten OSM graph, and past runs kept for the reconciliation sheet) while the card four paragraphs below recorded that that very split had never been verified. The reviewer counted the registry in one command and found a **third** bucket — `reproducibility.status == "external"`, agency-published figures whose re-verification means opening the source again — and it was the **largest** of the three. So the T0 honesty answer named two of three kinds and omitted the biggest, while inviting the judge to open `docs/NUMBERS.json` alongside them. Counted again here independently: of 58 irreproducible entries, 16 carry the overwritten-OSM reason, 24 are `external`, 18 are past runs not re-executed in this environment. **Fixed in this lap:** the draft now names three kinds and no proportion, points at the `reproducibility.status` field to count them live, and the 없는 것 line records that the count this card called unmeasured took one command. A fourth gate, `test_the_cards_account_of_the_irreproducible_covers_every_bucket`, derives the bucket set from the registry and fails when it grows one the card does not describe — graded red both ways (a new status value in the registry; a bucket phrase removed from the card). **(a), what did ship:** — Q30's recited draft now holds **no** registry count: it says 「대부분」 re-derives, names `docs/NUMBERS.json` and the screen's card as the two places to count with the judge, and states once, addressed to laps rather than to the student, that a literal typed here is a defect with a shorter fuse (critics #21, #22 and #26 each wrote the then-correct pair and all three went stale inside a lap). The four superseded blocks are kept as dated records (CHARTER §3.7) behind a greppable marker `[기록 · YYYY-MM-DD · 오늘의 값이 아닙니다]`. Three gates in `tests/test_judge_qa_bank.py`, all mutation-graded red: the recited draft holds no multi-digit number and still names both places; every 「등록 N」/「재현 가능 N」/「재현 불가 N」 in the bank opens a dated record block; and the word 「대부분」 the student says aloud is re-derived against the registry in-process. ⚠ **The done-when's 「a mutation that moves the registry」 is graded, but by the neighbouring gate rather than by these** — a live literal was rejected on a measurement (unshallowed clone, 485 commits: the registry count changed 44 times across 45 distinct values, 10 of them on the four sprint days), because gating one would turn the suite red two to four times a day on a rehearsal document. Verified by running it: adding a key to `docs/NUMBERS.json` without rebuilding the screen turns `test_the_registry_card_counts_the_registry_it_ships_beside` red and leaves these three green. Method, caveats and what it does not show: `docs/judge_qa_gates.md` | true | one lap | B/A 제출 자료 · R5 · R1 |
| WFG-129 | **P0** | science | **The cheapest test of the number the booth leads with is fully specified, runnable in this sandbox in minutes, and lives in the one file no dev lap reads.** `paper/GAPS.md` G7, rewritten by the 0323Z paper lap, specifies it exactly: mask slice 0 of the committed canonical field (p ≥ 0.5, 249 cells, `data/processed/routing_demo_canonical.npz`, shape [5,181,156]) as a node filter and re-run the existing `naive_route` over **only the 44 origins whose fire-blind route enters the hazard**, counting how many a present-perimeter-only router already saves. Zero buffer, one region, 44 origins, every input committed, no refit and no re-simulation; `F8(a)` in `paper/make_figures.py` already loads and renders that same slice-0 mask. G7's own words: 「That is minutes of work against WFG-033(b)'s two laps, and it converts §4.3's 「an unmeasured share」 from a hedge into a number ... **A dev lap should run this before the finals whatever the author decides on NH-027.**」 **Why it is P0 and why it is filed here.** The headline the student says out loud and the judged screen prints is **42 of 458 on 영덕**, and it has never met the fair opponent. On the one region where the fair opponent was built (WFG-114, 의성·안동) most of that region's contrast did not survive it. CHARTER §4 step 1 does not list `paper/GAPS.md`, and CHARTER §12 forbids the paper routine from writing outside `paper/`, so the instruction sits where neither routine can act on it. Checked here: `44 origins` and the slice-0 recipe appear **zero** times in `docs/auto/BACKLOG.md`; WFG-033(b)'s detail section (`:888`) holds only the full two-lap P2 version. **Done when:** a new script under `scripts/` writes a new artifact under `data/processed/` giving, for the 44 origins whose fire-blind route enters the hazard on the canonical 영덕 field, how many a slice-0 present-perimeter node filter already saves; it refuses to write unless it first re-derives the committed 414 / 42 / 2 partition (the WFG-114 reproduction-gate pattern); the numbers are registered additively; `docs/<topic>.md` states the method, the result and what it does **not** show; and `paper/GAPS.md` G7 records what closed. **Constraints:** routing only on committed inputs, no retrain and no re-acquisition (CHARTER §3.11 untouched, exactly as WFG-114 ran). Zero buffer, so this is **not** WFG-033(b) and does not pre-empt NH-027. It reports a count on 영덕, which is a different object from the contested 의성·안동 margin, so NH-032 does not bar it — but nothing from it reaches a judge-facing surface until this critic or the author has read it | todo | true | one lap | B 설계와 방법론 · B 데이터 수집·분석·해석 · B 연구 목적 |
| WFG-130 | **P0** | KCF | **The booth printables omit the one paper the Q&A bank tells the student to open in front of a judge, and the manifest says that paper does not exist.** `docs/auto/JUDGE_QA.md` Q30 (T0) ends 「그리고 대조표를, 종이 없이, 그 순서대로 읊습니다」 and its drill table (`:987`) lists 「대조표(`docs/submission_reconciliation.md`)」 as item 2. That document's own purpose line reads 「부스에서 심사위원이 「제출한 서식의 숫자와 지금 저장소의 숫자가 다릅니다」라고 물었을 때 그 자리에서 펴는 종이. 인쇄본은 **양면 한 장**입니다」. So the repository says three different things about one sheet: the sheet says print it, the drill says recite it without paper, and `docs/auto/finals/printables/manifest_20260906T0620Z.json` `what_this_does_not_show` says 「The A4 evidence sheet (WFG-018) ... [is] NOT in this file; the first two do not exist yet」 — which is false, WFG-018 is `done(20260903T0653Z)` (line 50) and the file is 13,702 bytes. ⚠ The deeper shape, and it is why this is P0 rather than a typo: `scripts/build_printables.py:97-101` assembles the PDF from four documents **the loop itself writes**, and `docs/auto/KCF_READINESS.md` **R7** enumerates five printables — 「evidence sheet (A4), reconciliation sheet, related-work and SFTD059T differentiation panel, booth checklist, 29 dispatch sheets sample」. The overlap is **one** (the booth checklist). Three of the four documents in the PDF are not on R7's list and three of R7's five are not in the PDF. The kit was measured against its own sources and never against its own definition of done, which is why R7 cannot tick on a build that is otherwise excellent. **Done when:** `docs/submission_reconciliation.md` is a `SOURCES` entry (Korean title, e.g. 「제출본 대비 정본 대조표」), a new-stamp PDF and manifest are built beside the existing ones (CHARTER §3.2 — do not overwrite `20260906T0620Z`), the manifest's `what_this_does_not_show` names only what is genuinely absent (WFG-026, and the dispatch sheets with their reason), `docs/printables.md` §1's table and §3's page counts are re-derived rather than retyped, and **either** `tests/test_printables.py` asserts that every R7 item that exists in the tree is a `SOURCES` entry **or** R7's wording is corrected to the five documents the booth actually needs and the test binds to that list — graded by removing one source and seeing it go red. ⚠ **DEV LAP 2026-09-06T1230Z: the committed kit is now stale against its own Q&A source, and no test says so.** `manifest_20260906T0620Z.json` records `docs/auto/JUDGE_QA.md` at sha256 `2c845121…`; WFG-117 edited that file in this lap and it now hashes `6c09095d…`. The whole `tests/test_printables.py` suite stays green, because the manifest is checked for internal consistency and never against the tree it describes — so any lap that edits any of the four sources silently invalidates the printed booth kit, the same shape as the defect WFG-117 just closed one document over. This row already rebuilds at a new stamp (CHARTER §3.2), so the repair rides along; **what it should also add is the gate**: a test that re-hashes every `SOURCES` path and fails when the newest manifest disagrees, with the rebuild command in the failure message. Graded by touching one source. Q30's 「종이 없이」 and the sheet's 「그 자리에서 펴는 종이」 are then reconciled in one direction, in writing. ⚠ **CRITIC #28, 2026-09-06T1736Z: the gate half of this cell is now WFG-140 and is no longer this row's to carry.** It has been a sub-clause of a rebuild row for two windows and the rebuild has been displaced in both; a gate that ships only if a rebuild ships is not a gate. This row keeps the contents half (the reconciliation sheet as a `SOURCES` entry, the `what_this_does_not_show` correction, the R7 wording) ⚠⚠ **DEV LAP 2026-09-07T0018Z: shipped.** `docs/submission_reconciliation.md` is a `SOURCES` entry titled 「제출본 대비 정본 대조표」 and prints as 3 of the new build's 33 pages; U+2212 MINUS SIGN needed a `SUBSTITUTIONS` entry, found by the coverage gate rather than by a reader, and a minus printing as a blank would have turned the sheet's signed deltas into unsigned numbers. The manifest's `what_this_does_not_show` no longer says the sheet does not exist: it names the two R7 items genuinely absent (WFG-026's panel, not written; the dispatch sheets, already committed PDFs) and records the false sentence as the thing WFG-130 corrects. `docs/printables.md` §1 and §3 are re-derived from `manifest_20260907T0059Z.json` (33 pages, 22 uncovered symbols) rather than retyped, and the superseded paragraphs are kept as dated 〔기록〕 blocks (CHARTER §3.7). R7's wording is **not** rewritten to match the build; instead `tests/test_printables.py` gains `R7_ITEMS`, which resolves R7's five names to paths with a written reason for each exclusion, `test_every_r7_printable_that_exists_is_actually_printed` (graded red by removing the sheet from `SOURCES`) and `test_r7_still_enumerates_the_five_printables_this_list_resolves`, which binds the mapping to R7's own line so a reword cannot outlive it. ⚠ The dev lap added the evidence to R7 and did **not** tick it; the critic ticks (CHARTER §10) | done(20260907T0018Z) | true | minutes | B/A 제출 자료 · R7 · R9 |
| WFG-140 | **P0** | KCF | **The booth kit has no freshness gate, and critic #27 pre-registered that this row is filed separately if the kit is still stale at the next critic head. It is.** Re-hashed here at `e95fe28`, all four `SOURCES` against the working tree: `BOOTH_SETUP.md` `ef7342dacf…` **matches**, `DEMO_SCRIPT_5MIN.md` `b1aae78f35…` **matches**, `DETECTION_FLOOR_CARD.md` `84648d4d6e…` **matches**, and `docs/auto/JUDGE_QA.md` manifest `2c8451211e…` against tree **`7d5ac4c9c5…`** — **stale, and the drift is larger than when critic #27 measured it** (`af955a30fa…` at `dd500e6`), because the WFG-133 lap edited Q35 again at `923ffbd`/`32de531`. The PDF's own self-hash still matches, so `tests/test_printables.py` is fully green over a kit whose largest source has moved twice since the build. **The gate is currently a sub-clause of two rebuild rows** (WFG-134's cell and WFG-130's 「what it should also add is the gate」), which means it ships only if a rebuild ships, and a rebuild has been displaced three windows running (see NH-038). Critic #27's own words: 「If any is still stale, then R7's kit is a snapshot with no freshness gate and WFG-134 is understated: file the gate as P0 rather than as part of the rebuild.」 This row is that filing. **Done when:** `tests/test_printables.py` gains a test that reads the **newest** `manifest_*.json` in `docs/auto/finals/printables/`, re-hashes every `SOURCES` path against the working tree, and fails naming the drifted path with `make printables` in the failure message; graded by touching one source file and seeing it go red, and by confirming it is red **now**, before the rebuild, on the `JUDGE_QA.md` drift above. ⚠ The test must go red on today's tree — a gate authored alongside a rebuild is green by construction and proves nothing (the `1ec1d06` lesson, `paper/GAPS.md` G8 point 2). If WFG-134's rebuild lands first in the same lap, replay the pre-rebuild manifest to grade it. ⚠⚠ **CRITIC #29, 2026-09-06T2015Z — THIRD DRIFT, RE-HASHED HERE AT `1b26c3a`, AND THE THIRD ONE WAS WRITTEN BY THE LAP THAT FILED THIS ROW.** All four `SOURCES` re-hashed against the working tree in one process: `BOOTH_SETUP.md` `ef7342dacf…` **matches**, `DEMO_SCRIPT_5MIN.md` `b1aae78f35…` **matches**, `DETECTION_FLOOR_CARD.md` `84648d4d6e…` **matches**, and `docs/auto/JUDGE_QA.md` manifest `2c8451211e5f97…` against tree **`175da9e50c5ce9…`**. The PDF's own sha256 still matches its manifest, so `tests/test_printables.py` stays fully green. The series of measured tree hashes on that one file is now `af955a30fa…` (critic #27, `dd500e6`), `7d5ac4c9c5…` (critic #28, `e95fe28`), `175da9e50c…` (here) — and **all three drifts were written by a critic or dev lap adding a correction note to the bank, not by any judge-facing improvement**: critic #27's ⚠⚠ note on Q35 at `a64b904`, the WFG-133 lap at `923ffbd`/`32de531`, and critic #28's own `050731a`. This lap adds a fourth for the same reason (the Q19 note under WFG-138), and says so rather than exempting itself. **The argument this makes is the root objection of critic #29's report:** the kit's staleness is now being used to sequence judge-facing work behind a rebuild (`WFG-144`'s cell: 「land this **after** the WFG-134 / WFG-130 rebuild, or the printed 17 pages go stale a fourth time」) while the laps making that argument spend the same freshness freely on their own corrections. A rebuild has been displaced three windows (NH-038). Once this gate exists the sequencing argument dissolves, because drift is detected in one gate run and no row needs to wait behind a rebuild. **That is why this row and WFG-134 should be taken in one lap and why the gate half is the load-bearing half.** **Constraints:** no overwrite of `WFG_printables_20260906T0620Z.pdf` or its manifest (CHARTER §3.2); the gate reads the newest manifest only, so older stamps stay valid records. ⚠⚠ **DEV LAP 2026-09-06T2117Z: THIS ROW CANNOT BE TAKEN ALONE, and that is a consequence of its own done-when.** The test is required to be **red on today's tree** before a rebuild makes it green; CHARTER §3.9 forbids pushing a tree whose `gates.py --mode full` is red to `auto/dev`. So a lap that takes WFG-140 without WFG-134 in the same lap has three exits and all three are bad: push red (forbidden), author the gate green by construction (the defect the row exists to avoid), or park a P0 gate on `auto/red/` where nothing reads it. Take it **with** WFG-134's rebuild, in that order within one lap: write the gate, watch it go red on the drift, rebuild at a new stamp, watch it go green. This lap deliberately did not take it for that reason and its own edit to `docs/auto/JUDGE_QA.md` is a fourth drift the rebuild must absorb ⚠⚠ **DEV LAP 2026-09-07T0018Z: shipped, red first.** `tests/test_printables.py::test_the_newest_printable_is_not_stale_against_the_tree` reads the newest `manifest_*.json`, re-hashes every `SOURCES` path against the working tree, and fails naming the drifted path with `make printables` in the message. **Graded red on today's pre-rebuild tree**, printing `docs/auto/JUDGE_QA.md: manifest 2c8451211e... tree 5ac45ea810...` — the exact pair critic #30 measured — and green after the WFG-134 rebuild, which is the order the row required and the reason it could not be taken alone. Older stamps stay valid records: only the newest manifest is held to the tree. A second test, `test_every_source_the_build_prints_is_recorded_by_the_newest_manifest`, covers the other direction, where a lap adds a `SOURCES` entry and does not rebuild | done(20260907T0018Z) | yes | minutes | A 제출 자료, R7, R9 |
| WFG-139 | **P0** | infra | ⚠⚠ **The test suite downloads 25 MB from Amazon S3 in the middle of every cold run, CHARTER §4b forbids exactly that, `docs/clean_clone_gates.md:27` says the opposite in two words, and `docs/auto/JUDGE_QA.md` Q28 cites that line to a judge.** Measured here, not read. On a clone made at 16:57Z with no `data/raw/` at all, `gates.py --mode full` produced `data/raw/dem/srtm/N36E129.hgt` (25,934,402 bytes) and `N36E129.hgt.gz` (8,473,868 bytes) at **17:02Z**, during `pytest-full`. The caller is `tests/test_spread_warmup.py::test_model_config_ignition_radius_increases_initial_burn` (`tests/test_spread_warmup.py:156`), which passes `dem_source="srtm"` with **no skip guard**; `load_dem(source="srtm")` calls `_srtm_dem_for_region(..., allow_download=True)` (`src/wildfireguardian/data_io/raster.py:521`) which calls `_download_srtm_tile` (`:282`) against `https://elevation-tiles-prod.s3.amazonaws.com/skadi/...` (`:252`). **Four consequences, each measured.** (1) **CHARTER §4b is violated**: 「No test may depend on the local clock, the timezone, the network, or files outside the repository.」 If S3 or the network is unreachable the loader **raises** rather than skipping (`:306`), so `auto-gates` goes RED on an outage and the loop reads it as a regression. (2) **`docs/clean_clone_gates.md:27` reads 「No network, no keys, no `.env`.」 That is false**, and it is one of the three files `docs/auto/JUDGE_QA.md` **Q28 · T2** (「낯선 사람이 이걸 다시 돌릴 수 있습니까?」) cites; Q28's card also tells the student 「그 입력을 필요로 하는 테스트는 이유를 적은 채로 건너뜁니다」, which is true of the FIRMS/ERA5 tests and **false of this one**. Same shape as WFG-133: a card that scripts the student to say something untrue about their own repository. (3) **Six tests never run in CI, ever.** The four `tests/test_srtm_dem.py` real-data tests (`:81`, `:94`, `:109`, `:170`) plus `tests/test_validation_robustness.py:57` and `tests/test_validation_session3.py:171` are `skipif`-guarded on that same tile, `skipif` is evaluated at **collection**, and every CI job is a fresh clone — so the download always lands after the decision to skip. Those six are the physical-plausibility checks on the terrain the router walks over (ocean clipped to 0 m, max elevation ≥ 400 m, east strip lower than west). They pass only on a machine that has already run the suite once. (4) **The loop's pass counts are not comparable across laps and it has twice diagnosed this wrongly.** Cold `1599 passed, 62 skipped`; warm `1605 passed, 56 skipped`; same tree, same commit `e95fe28`, both measured here, and the six-test delta is exactly this tile. The 0059Z dev lap wrote 「Six tests that skip on a cold clone (`data/cache/**` DEM and OSM caches, all git-ignored) ... expected rather than a discrepancy to chase」 and the 1638Z dev lap wrote 「the two disagree by 6 and I did not chase it」. **Both named the wrong cause** (`data/cache/**`; it is `data/raw/dem/srtm/`) and the second declined to look. Critic laps report cold counts and dev laps report warm ones, so every 「+N passed」 across the two kinds is contaminated. **Done when:** `tests/test_spread_warmup.py:141` no longer reaches the network — either a `skipif` on the same tile predicate the other six use, or `dem_source="synthetic"` if the assertion is about ignition radius rather than terrain (read the test: it asserts a first-snapshot area, so synthetic is very likely sufficient and is the smaller change) — **and** a test asserts that no test fetches over the network, **and** `docs/clean_clone_gates.md:27` either becomes true or says what actually happens, **and** Q28's 「건너뜁니다」 clause is corrected. Graded by deleting `data/raw/dem/srtm/` and running the full suite with the network blocked: it must be green with the cold counts. ⚠ **Do not close this by committing the tile** — it is 25 MB and `data/raw/**` is git-ignored by design (CHARTER §4 sandbox facts). ⚠ **Do not close it by deleting the six skip-guarded tests**; CHARTER §3.7. **Why P0 and not P1:** CHARTER §14b holds loop hygiene behind R1/R3/R4/R7/R8/R9, and this is not loop hygiene — it is the product's own test suite, it puts a false sentence on a judge-facing card (Q28), and it is a live red-gate generator 48 days before the finals. It is placed **below** the judge-facing rows above it so it displaces none of them  ⚠⚠ **CRITIC #34, 2026-09-07T1100Z: SEVENTH CONSECUTIVE MEASUREMENT, AND THIS ONE IS TAKEN ON THIS LAP'S OWN CLOCK RATHER THAN BY A PASS/SKIP DELTA.** At container start `data/raw/` held exactly two files, `.gitkeep` and `README.md`, both mtime **2026-09-04T16:57:27Z** (the image, not this run). `bash scripts/auto/bootstrap.sh` finished at **10:59:40Z**; `gates.py --mode full` ran from about that moment and its `pytest-full` stage took **260.6 s**. Afterwards `data/raw/` holds three more files: `kfs_fire_statistics/산림청_산불통계데이터_20250911.csv` (195,891 B, mtime **10:57:59Z**) and `dem/srtm/N36E129.hgt` (**25,934,402 B**) with `N36E129.hgt.gz` (8,473,868 B), **both mtime 11:02:33Z** — inside the pytest window. **The test suite downloaded 25.9 MB over the network in the middle of a gate run**, which CHARTER §4b forbids in those words and which `JUDGE_QA.md` Q28 tells a judge does not happen. My counts are therefore neither cleanly cold nor warm and I say so: `1650 / 62` on a run that fetched the DEM partway through ⚠⚠ **CRITIC #38, 2026-09-07T2319Z: ELEVENTH CONSECUTIVE MEASUREMENT, AND THE FIRST ISOLATED REPRODUCTION. THE DIAGNOSIS IN THIS ROW IS CORRECT AND THE FIX IS MINUTES.** Ten laps have measured this by watching a 25 MB file appear during a 200-to-350-second `pytest-full` stage, which proves the suite downloads but not which line does. This lap ran the row's own named suspect **alone**. At container start `data/raw/` held `.gitkeep`, `README.md` and the KFS CSV; `gates.py --mode full` (ALL GREEN, exit 0, **COLD**, `1689 passed, 62 skipped, 2 xfailed`, 212.2 s) left `dem/srtm/N36E129.hgt` (**25,934,402 B**) and `N36E129.hgt.gz` (8,473,868 B) at mtime **23:02Z**. I then deleted both tiles **and** the cached `data/cache/dem_yeongdeok_2025_srtm_500m_80858ae747.nc` they had produced (both git-ignored, no tracked artifact touched, `git status --short` clean afterwards) and ran exactly `pytest tests/test_spread_warmup.py::test_model_config_ignition_radius_increases_initial_burn`. **1 passed in 1.54 s, and both tiles were back on disk at 23:06Z.** One test, no suite, no gate: it is that line. (Control: the same single test with the `.nc` cache still present passes in **0.47 s** and downloads nothing, which is why no earlier lap saw it in a re-run and why a warm machine can never reproduce this.) **What this changes.** The row's 「Done when」 already prescribes the two candidate repairs; the reproduction says which is right and makes the grading recipe exact. The test's assertions are `first_area_ha > 25.0` and `result.metrics_persistence[0].predicted_area_ha > 25.0`, both about the **ignition disc**, and it already runs `fuel_source="synthetic"`; the terrain is incidental to what it asserts, so `dem_source="synthetic"` is the smaller change and should be tried first, with the six `skipif`-guarded tests left exactly as they are. **Grade it by re-running the deletion above with the network unreachable: the suite must be green at the COLD counts and `data/raw/dem/` must stay empty.** ⚠ The download also buys the run that makes it **nothing**: `skipif` is evaluated at collection, so the six guarded tests skip in the very run that fetches their tile, and my cold 62 skipped against the 2208Z dev lap's warm 56 is that same six, at a tree whose totals otherwise agree (1689+62 = 1695+56 = 1751). ⚠ `docs/auto/JUDGE_QA.md` **Q28 is still uncorrected** and still tells the student 「그 입력을 필요로 하는 테스트는 이유를 적은 채로 건너뜁니다」; **Q40, 211 lines below it, says in bold that Q28 is wrong on this point** and gives the ❌ do-not-say list. A student reading the deck in order meets the false card first and the correction only if they reach the end, and the printed kit reproduces both. Fixing Q28 in place is part of this row's 「Done when」 and needs the same-lap `make printables` rebuild (WFG-152).| done(20260908T0018Z) — the guard found THREE tests, not one; `data/raw/` 201,187 B before and after a cold run | yes | one lap | B 데이터 수집·분석·해석 (재현), A 제출 자료, R3 |
| WFG-131 | P1 | infra | **A P0 deliverable now depends on a package no manifest declares, and `make check-declared-deps` cannot see it.** `docs/printables.md` §2 says so in its own voice and correctly: `matplotlib==3.11.1` and `fonttools==4.63.0` are pinned in `requirements.txt` (`:44`, `:76`), but **`brotli` appears in neither `requirements.txt` nor `pyproject.toml`** — verified here by grep, it occurs only at `scripts/auto/bootstrap.sh:69-70` (「brotli, which no pin pulls in; conda's wfg311 had it by accident」). fontTools needs it to open the vendored `.woff2` faces, so without it `make printables` cannot build the booth PDF and `tests/test_screen_checks.py`'s woff2 reads fail. This is three days old — `docs/auto/BACKLOG.md:179` records it as cause 1 of the first cloud lap's 17 red items on 2026-09-03 — and it was resolved by teaching `bootstrap.sh` to install it rather than by declaring it, so every environment that is not built by `bootstrap.sh` is still missing it. It now sits under a **P0** artifact and under CHARTER §1's second goal (「reproducible by a stranger」) and R3's booth-laptop half. `scripts/env_check.py` reports 「the environment matches requirements.txt」 because an *extra* installed package is not a mismatch, so the gate is green on the hole. **Done when:** `brotli` is pinned in `requirements.txt` with the comment naming its two consumers (fontTools woff2 → `tests/test_screen_checks.py`, `scripts/build_printables.py`), `pyproject.toml` agrees, `make check-declared-deps` stays green, and a check fails when a module the test suite imports at run time is absent from the declared set — graded by removing the pin and seeing it go red. **Parked behind R1, R3, R7, R8 and R9 by CHARTER §14b** if a lap reaches it before those tick | todo | true | minutes | R3 · 데이터 수집·분석·해석 |
| WFG-132 | P1 | infra | **The newest dev report and the email built from it name a branch head and a work commit that do not exist anywhere in this repository.** `docs/auto/reports/2026-09-06T1000Z-dev.md`'s header table reads `branch / head` = `auto/dev` / `a464d1a`, its body reads 「work `a68c2d0`」, and the 10:15:47Z report email repeats 「auto/dev @ a464d1a」. Checked here on a clone deepened to 300 commits: `git cat-file -t a464d1a` and `git cat-file -t a68c2d0` both answer `fatal: Not a valid object name`, and `git rev-parse origin/auto/dev` is `b2bdaf0`. The lap rebased after `report.py` stamped the header and neither id survived. Two other dev reports in the same 24 h name rewritten heads (`9184a72` at 1542Z, `bf4cb31` at 2132Z) but **both say so in the same cell**; this one does not, so it is the first report in the window that tells the author a commit id with no annotation and no object behind it. Three commits in this window are titled 「the report names the head it ships in as well as the head it reviewed」, so the loop has been repairing this by hand once per lap for three days and it recurred on the fourth. **This is loop hygiene under CHARTER §14b and it waits behind R1, R3, R4, R7, R8 and R9** — it is filed, not scheduled. **Done when:** `report.py` resolves the head at the moment it writes the header (or the push step rewrites the two cells after the final rebase), a test asserts that every commit-shaped id in a report under `docs/auto/reports/` resolves with `git cat-file -e` unless the same cell annotates it as rewritten, and the check is graded against a report naming a rebased-away id | todo | true | one lap | — |
| WFG-121 | **P0** | KCF | ⚠ **CRITIC #22, 2026-09-05T2330Z — READ NH-032 BEFORE THIS ROW PRINTS 9 ANYWHERE. THIS IS THIS LAP'S ONE `fix-before-next-row` ITEM.** The 9 this row is told to print is contested by a **second green measurement of the same experiment** that says **27**, and **5** at its sweep's best buffer. WFG-114 was built twice, concurrently, by two laps that could not see each other; the second parked on `auto/red/20260905T2248Z` rather than force a rebase across 15 files. Both re-derive the committed 91 node-for-node first, both use the canonical slope arm, both grade with `_evaluate_path`; they differ only in how the *opponent* is built (prune-and-shortest-path with no time budget, versus the time-expanded router on a frozen field, budget-capped, refusing departure from inside the buffer). **The author's decision at `4d705df` was made from a `docs/auto/NEEDS_HUMAN.md` that did not yet carry NH-032 or NH-034** — this critic lap imported them from the parked branch in the very next commit — so the instruction 「keep the headline, add the fair-opponent line」 stands, and only the *number* is in question. **Nothing here overrules the author.** Do the parts of this row that no answer changes: the buffer finding (250 m walks 91 origins into the fire, 2 km leaves 80 unable to finish inside the 600-minute budget, so no fixed width works and an operator cannot know which one they are on) and the sentence that the 91 is a comparison against a **fire-blind** control. **Do not print a margin figure until NH-032 is answered.** If the author confirms 9, this row proceeds unchanged and nothing is lost. See WFG-124. **The fair-opponent line on every judge-facing surface** (author, 2026-09-06: "Keep the headline, add the fair-opponent line"). Wherever 「368곳 중 91곳」 / "91 of 368" appears — README opening, `web/finals.html` and the 3막 script, `docs/auto/JUDGE_QA.md`, the manuscript abstract and §4 — the next sentence states, from `data/processed/present_perimeter_arm_uiseong_andong_2025.json` and its registered keys, that against a present-perimeter route with a 1 km buffer the forecast's margin is 9 origins of 368, and that no fixed buffer works (250 m: 91 walk into the fire; 2 km: 80 arrive too late). One lap; gates bind the numbers | todo — **(a) done(20260906T0020Z)**: the fire-blind sentence and the closed-limitation correction are on the booth script, `docs/fair_opponent_line.md` holds the line and the buffer finding, `tests/test_fair_opponent_line.py` gates all four (mutation-graded). **(b) outstanding, `blocked(NH-032)`**: no margin figure may reach a surface. **(c) outstanding and NOT blocked by NH-032**: the *spoken* 3막 line is unchanged, because adding a sentence moves the registered `demo_pace_*` allocation and CHARTER §3.2 forbids editing a registered value — the spoken half needs a NEW pace tag (a re-allocation of all six segment times, WFG-100 machinery), which is its own lap. | true | one lap | 제출 자료 · 데이터 해석 <!-- collision-ok: 3.2 — the only "3.2" on this row is the section reference 「CHARTER §3.2」, not a measurement; the anchors control/late/uiseong that pull pp_uiseong_control_late_arrival_{earliest,latest}_min onto it are the row's prose about the present-perimeter control. Made explicit by critic #25, 2026-09-06: this row passed only because a NARRATIVE marker happened to sit inside MARKER_LOOKBACK of it, and inserting two rows above it moved that marker out of reach. An accidental excuse is not an excuse. --> |
| WFG-122 | P1 | science | **NH-031 A:** register `mr_uiseong_fa_exceeds_budget_budgeted` (= 0, the same origins scored under the 600-minute budget the forecast-aware arm already obeys) additively beside the committed `mr_uiseong_fa_exceeds_budget` = 2, annotate the old entry's caveat with the scoring asymmetry, and change nothing else; a test proves both keys resolve. ⚠⚠ **WFG-225's independent reviewer, 2026-09-10T0920Z, made this row's argument sharper than the row did, and it is now a judge-facing dependency rather than a registry tidy-up.** Its root objection, on a lap it passed: the new 알려진 한계 card on `web/finals.html` asserts in words that under one rule the Uiseong-Andong bucket is **비어 있습니다**, and 「the load-bearing assertion the card actually puts in front of five judges ... is a zero asserted in words, and it is the one figure on the card with no `docs/NUMBERS.json` key of its own ... `mr_uiseong_fa_exceeds_budget` = 2 is pinned to `data/processed/multi_region_comparison.json` by `make verify`, while the counter-claim standing beside it on the same screen is pinned to nothing」. Because the zero is spelled as a Korean word it is invisible to the registry machinery, so 「the 624.8 / 628.2-minute origins that make it true could stop being those origins and every one of the 51 assertions would still be green」. **What that changes here:** this row's `Done when` should also re-point `tests/test_budget_rule_asymmetry_is_stated.py`'s 「버킷은 비어 있」 assertion at the new key instead of at a regex over the card's own sentence, on all four graded surfaces. That is the difference between a gate that fails when the claim becomes false and one that fails when somebody rewords it. ⚠ Left at P1 and NOT reordered by the dev lap that recorded it (CHARTER §14: reorders are the critic's, one per lap) — the critic is asked to weigh it | todo | true | hours | 데이터 해석 (재현) |
| WFG-124 | **P0** | science | **Your own row was built twice, in parallel, and the two green answers differ by more than the answer.** WFG-114 landed on `auto/dev` at `c8a3eee` reporting a margin of **9** of 368 (fire-blind 263 / present+1 km 345 / forecast-aware 354). Ninety minutes later the 2132Z lap finished the same row independently, could not rebase (15 conflicting files), parked green on **`auto/red/20260905T2248Z`**, and reported a margin of **27** (265 / 327 / 354) — and **5** at the sweep's best buffer, 0.5 km. Both re-derive the committed 91 node-for-node before writing anything; both use the canonical slope/DiGraph arm; both grade with `_evaluate_path`. The gap is entirely in how the *opponent* is built: `auto/dev` prunes the refused nodes and runs `naive_route` (distance-minimising, no time budget, so it never fails the 600-minute cap), the parked lap runs the same time-expanded router against a frozen binary hazard (time-minimising, budget-capped, and able to refuse to let someone inside the buffer start at all). **So the spread across two defensible implementations of the same experiment (9 to 27, and 19 under the strict origin rule on `auto/dev` alone) is wider than the effect either one reports.** A statistician judge will say exactly that sentence. The parked branch also carries one measurement `auto/dev` does not have and that a fire scientist will want: of the 25 origins the 1 km arm walls off, 11 have a forecast-aware route and **10 of those 11 escape across ground that never burns at all**, so the residual gap reads 「the buffer was drawn too wide」 rather than 「the forecast saw the future」. **Done when:** the author's **NH-032** answer is applied; the surviving arm (or both, as a named range) is the one `docs/present_perimeter_arm.md` reports; the escape analysis and the 265/263 control question are carried over or explicitly declined in writing with the reason; and a test fails if the doc reports one arm without naming the convention that produces it. **Constraints:** nothing on a judge-facing surface carries a margin until NH-032 and NH-034 are answered (the parked lap's own words); no committed value moves; do not delete the red branch  ⚠ **Filed by critic #22 as WFG-121/122/123 and renumbered to 124/125/126 in the same lap:** the author pushed  with rows of their own at WFG-121 and WFG-122 while this lap's gates were running. The author's ids win (pushed first, and the author's own decision text quotes them; CHARTER precedent WFG-040/094/114). | blocked(NH-032) | true | one lap | B 설계와 방법론 · B 데이터 수집·분석·해석 · R5 |
| WFG-126 | **P0** | IEEE | **RAISED P1 -> P0 BY CRITIC #23, 2026-09-06. This is a priority change and not a position move (critic #22's WFG-104 precedent), and the reason is that the window that filed this row also proved it is a CHARTER §3.5 violation rather than staleness.** `docs/fair_opponent_line.md:32-35`, written this window, states the rule in the loop's own words: 「A limitation that has been closed and is still spoken is a fabricated limitation, which CHARTER §3.5 forbids in the same breath as a fabricated result.」 The dev lap applied that rule to the booth script and left the manuscript saying the same false thing to a reviewer. One surface was repaired and its twin was not, in one window. **The manuscript tells a reviewer the fair-opponent experiment has not been run, and it ran nine hours ago.** `paper/manuscript.md:386` still carries `[GAP: the arm that separates them, a present-perimeter baseline …]`, mirrored in `paper/GAPS.md`. That is the same shape as the defect the WFG-114 lap caught in WFG-104's own instruction and refused to ship — a limitation that is false at the moment it is read — except that here it is inside the submitted material rather than in a backlog note. CHARTER §14b names the manuscript as a judge-facing surface. **Done when:** the `[GAP]` marker and its `paper/GAPS.md` mirror either state the arm as run and cite `docs/present_perimeter_arm.md`, or are rewritten as the narrower gap that genuinely remains (which opponent, and the perfect-forecast bound of WFG-122) — **without** printing a margin while NH-032 is open. **Owner:** the `wfg-autoloop-paper` routine, whose paths these are (CHARTER §12); filed here so it is not discovered a third time | done(91d3e05) — **closed by the 0323Z paper lap inside the window that filed it, and verified here at this head rather than read from its report.** `paper/manuscript.md` §4.3 no longer says the arm 「is specified in the project backlog and scheduled after this sprint」; it says the arm has been run on a different region, cites §4.5, and the `[GAP]` that remains is the narrower true one — the present-perimeter baseline over the canonical Yeongdeok 458. `paper/GAPS.md` G7 is marked NARROWED with the same wording and the closure recorded. No margin reached the manuscript: §4.5 quotes no count from the arm at all. The narrowed G7 is now **WFG-129** | true | minutes | B 제출 자료 · B 설계와 방법론 |
| WFG-114 | **P0** | science | **Renumbered from a duplicate `WFG-108` by critic #20, 2026-09-05: two live rows carried that id. The bundle row below (P1, `make finals-bundle` certifies a folder it never enumerates) keeps `WFG-108` because it was filed first and is quoted in four places including `docs/auto/finals/BOOTH_SETUP.md:86`, which is judge-facing; CHARTER precedent is that an id others quote wins (see WFG-040, WFG-094). The author's decision text (NH-027) names no id, so nothing the author wrote moves.** **The fair opponent for the headline (NH-027, author 2026-09-05): present-perimeter + buffer routing on 의성·안동.** WFG-033(b)'s first slice: the `naive` baseline is fire-blind; add the arm that refuses the cells burning at slice 0 (p ≥ p_cut) plus one fixed buffer (1 km), same origins, same refuges, same budget, on the committed hazard field, and report how many of the 91 forecast-aware-only origins it recovers — whatever the number says. New filenames, additive registry, a `docs/present_perimeter_arm.md`, the Q&A card (WFG-104) updated with the measured number | done(20260905T1820Z) — ⚠ **re-claimed at 20260905T2132Z by a later lap while this one was finishing (critic #21's NH-030 had reported the row abandoned; it was not — the 1820Z lap was on its second rebuild after three reviewer blocks). That claim is superseded by this row, not lost: the 2132Z lap should stop, read `docs/present_perimeter_arm.md`, and take the next row, which is WFG-104.** **The experiment, and it qualifies the headline.** On the CANONICAL arm at a 1 km buffer the present-aware arm recovers **86 of the 91** forecast-only origins, so the forecast's margin on this fire is **9 of 368**, not 91 (fire-blind 263, present+1 km 345, forecast-aware 354, all three under ONE 600-minute budget rule). But 1 km is the crossing of two failure regimes, not a constant: at 250/500 m the arm walks **91/80** origins into the fire as it grows, at 2/3 km **80/73** cannot finish inside the 600-minute budget, and an operator cannot know which width they are on. ⚠ The margin also depends on a modelling convention by more than its own size — under the strict origin rule it reads 19 rather than 9 — so both rules are computed for every origin and `walk_out` (you may leave the buffer, never re-enter) is primary, because the strict rule strands origins that plainly have a road out and every one of them flatters this project. ⚠ The forecast-aware arm plans on the field it is scored against, so 9 is a PERFECT forecast's margin. `scripts/run_present_perimeter_arm.py` (refuses to write unless it first reproduces the committed canonical arm — all seven bucket counts and the ids of every bucket the artifact stores), `data/processed/present_perimeter_arm_uiseong_andong_2025.json`, `docs/present_perimeter_arm.md`, `scripts/register_present_perimeter.py` (52 `pp_uiseong_*` keys, every cell of the band), `tests/test_present_perimeter_arm.py` (19, mutation-graded). **⚠ The lap's reviewer BLOCKED this row TWICE and was right every time:** round 2 found that the fire-blind control was scored with no time budget while the other two columns were held to 600 minutes — two rules in one table, inflating both the control and the buffer's apparent cost — so the control is 263 not 265 and the buffer breaks 4 not 6; it also found the v1 caveat string still shipping inside the artifact, and a false regeneration command the doc's own citation had created. Round 1 it had been built on the flat arm (denominator 96) on the false claim that the canonical DEM is unreachable in the cloud — the same raster is committed to the snapshot store, `data/snapshots/srtm-dem_uiseong-andong-2025_20260802_e4df23a9.tif`, and CHARTER §4 says to work from there — and it had used the strict origin rule alone. Both fixed; the withdrawal is recorded in `docs/present_perimeter_arm.md` §6 rather than deleted. **The two judge-facing surfaces named in this row's done-when are NOT done and moved to WFG-104** (the card) and WFG-100's pace budget (3막's sentence) | true | one lap | 설계와 방법론 · 데이터 해석 |
| WFG-022 | P0 | KCF | Five questions to the KCF 운영사무국 (date, track, 기여 ② restatement, AI disclosure, 제출 자료 scope) ⚠⚠ **STATUS CORRECTED BY CRITIC #47, 2026-09-09T0230Z — this row has been `blocked(human)` for five days on a question the author answered on 2026-09-04, and it is one of the two blockers on WFG-024, the P0 row that ticks readiness line R11.** `docs/auto/NEEDS_HUMAN.md` NH-008 is **closed**, verbatim: 「Everything is fine here. Don't worry about this, and continue with the project.」, and the lap that recorded that closure wrote its consequence in the same entry: 「**No contact with the 운영사무국 will be made.**」 All five sub-answers are recorded there as the assumptions the loop now carries instead of an answer (finals date stays 10-24 per NH-006; the 참가부문 track is not re-verified; 기여 ② keeps its current wording; no AI-disclosure artifact is required, NH-008 superseded; 제출 자료 is assumed to be the 기제출 서식2 plus the booth demo). **So the work of this row will never be done, and `blocked` is the wrong word for that — `dropped` is.** ⚠ **This records the author's existing decision; it does not make one.** If the author wants the 운영사무국 contacted after all, reply and the row returns to `blocked(human)`. | dropped(NH-008 closed 2026-09-04 by the author: no contact with the 운영사무국 will be made; the assumptions carried instead are recorded in NH-008) | **false** | hours | Pass/Fail · all rows |
| WFG-023 | P0 | infra | Protect `Main`; ratify `auto/dev`; decide the two HANDOFF §4 items; approve/veto decimation; close NH-001/002/006 ⚠ **RE-READ BY CRITIC #47, 2026-09-09T0230Z: three of this row's five items are discharged and the row never said so.** `Main` and `auto/dev`: **settled** — `docs/auto/CHARTER.md` §3 rule 1 (「Never push to `Main`. Work on `auto/dev`」) and §4c, the author's decision of 2026-09-05 that `Main` follows the last green commit through the `promote` job in `.github/workflows/auto-gates.yml`. NH-001, NH-002 and NH-006: **all three are `closed`** in `docs/auto/NEEDS_HUMAN.md`. **What genuinely remains and keeps this row `blocked(human)`:** the two `docs/HANDOFF_ROUND3.md` §4 items and the approve/veto on decimation. ⚠ **Neither of those gates a documentation re-key**, which is why WFG-024 is released from this row in the same lap. | blocked(human — the two HANDOFF §4 items and the decimation approve/veto only) | **false** | hours | — |
| WFG-018 | P0 | KCF | 제출본 대비 정본 reconciliation sheet as NEAR-labelled prose (Korean, one page) | done(20260903T0653Z) | true | hours | 제출 자료 · 데이터 해석 |
| WFG-019 | P0 | science | Operating-point evidence package: per-fire recall/FNR at 0.3, PR curve, nested LOFO threshold calibration as a negative result, MODEL_CARD appendix | done(20260903T1224Z) | true | one lap | 데이터 수집·분석·해석 · 설계와 방법론 |
| WFG-002 | P0 | KCF | Judge Q&A bank v2 (**revise**: corrected numbers, four new questions, deprecated phrasings purged) | done(20260903T1536Z) | true | one lap | 연구 목적 · 설계와 방법론 · 데이터 해석 |
| WFG-004 | P0 | KCF | SSOT sweep (**revise**: fix README:731, reconcile `fold_sizes.md` vs `NUMBERS.json` on the primary AUC, annotate superseded values) | done(20260903T1622Z) | true | one lap | 제출 자료 |
| WFG-020 | P0 | KCF | Greenpeace 2026 survivor survey registered as evidence + the "85% drove" answer | done(20260903T1821Z) | true (fallback NH) | hours | 연구 목적 · 데이터 수집 |
| WFG-021 | P0 | KCF | Detection-floor panel (Session 19 as recorded) + tests for `src/wildfireguardian/detection/gk2a.py` | **done for the sprint** — **(a) done(b557e9d)**, **(b) done(f5f8498)**, **(c) parked(needs the NOAA archive at run time; not runnable in the cloud sandbox — parked by critic #6 so this row stops sitting above WFG-017 as a `todo` a lap must first read and reject)** | true | one lap | 데이터 수집·분석·해석 · 설계와 방법론 |
| WFG-017 | P0 | KCF | `web/finals.html` refresh v2: evidence cards for operating point, detection floor, horizon grounding, refuge placement, reconciliation; rebuilt with `--verify` | done(a562045) | true (fallback: student runs `make finals`) | one lap | 제출 자료 · 구현 및 유용성 |
| WFG-087 | **P0** | KCF | **A T0 booth answer explains a gap with a blocker this repository removed the same day.** `docs/auto/JUDGE_QA.md:585-603` is Q18, 「그중 지정 대피소가 있습니까?」, and its 「없는 것」 says the comparison against the agency-designated list is missing because 「포털 다운로드가 로그인·CAPTCHA로 막혀 있고 NH-012에 학생 작업으로 올라가 있습니다」. Both halves are stale: **NH-012 is closed** (author deferred it post-finals, so it is not pending student work), and the 영덕 designated-site subset was **re-cut on 시군구 47770 and verified from the data** at `6f33eca` (NH-022 closed), so the repository holds 지진해일긴급대피장소, 무더위쉼터 and six more layers for 영덕 today. `paper/manuscript.md:656-671` already says so in the opposite direction: 「a designated list now exists that no result here uses」, comparison 「runnable now」. Fix: rewrite Q18's 「없는 것」 to say what is true — the designated categories in hand are earthquake, tsunami and heat, none of them a 지정 *산불* 대피소; the OSM points have not been compared against them; WFG-073 is the row that would; and the 행안부 national shelter file remains deferred under NH-012. Also correct NH-012's WFG-075 amendment, which still reads 「the repository holds **no** agency-designated 대피장소 list for 영덕」. Do not widen: two paragraphs, two files, no new claim | done(20260905T0025Z) | true | minutes | 제출 자료 · 데이터 수집·분석·해석 |
| WFG-095 | **P0** | KCF | **The two sentences that keep the booth script out of this repository's own withdrawn claims are the two it tells the student to drop first.** `docs/auto/DEMO_SCRIPT_5MIN.md` marks a droppable sentence per segment with **[버림]**, and in 1막 that marker sits on a sentence carrying its claim AND its caveat together (709단계/0건 with 「오경보율의 측정이 아니라 상한」), which is correct - dropping it loses both. In 2막 (`:70-71`) and 3막 (`:85-86`) the marker sits on a **caveat-only** sentence while the claim it guards stays in the non-droppable text. Drop 2막's and the student has just spoken two different 기록된 발생일시 clocks in consecutive segments (1막's 22/34/64분 delays, 2막's 79.23 % within 240분) with nothing left saying they are different samples - the exact conflation WFG-053 withdrew and NH-018/NH-019 were opened over. Drop 3막's and the student has just recited 24.73 % (의성·안동), 9.17 % (영덕), 15.14 % and 23.67 % (울진·삼척) side by side with nothing left saying the differences carry OSM mapping density - while §4 item 4 of the same document lists 「지역 간 순위」 as a sentence this script never says. The script's own never-say list is enforced by sentences it marks as the first to go. Measured this lap: deleting either caveat outright is **uncaught by every claim gate in the tree** (`check_forbidden.py`, `test_withdrawn_claims_registry.py`, `test_detection_ordering_is_not_claimed.py`, `test_demo_script_5min.py` all exit 0). Done when: every **[버림]** marker sits on a sentence that carries its own claim, so that dropping it drops the number too - either by moving the marker onto the claim+caveat pair, or by moving the caveat into the segment's ⚠ block where the script's non-droppable warnings already live; and the segment times still sum to 300 s. Cheapest test: delete each **[버림]** line in turn and read what the remaining text asserts | done(20260905T0325Z) | true | minutes | 제출 자료 · 데이터 해석 |
| WFG-100 | **P0** | KCF | **The five minutes the whole booth turns on is a budget nobody has measured, and the six segments cannot all be right.** `docs/auto/DEMO_SCRIPT_5MIN.md` §5 says the six segment times were set from 「문장 수와 한국어 발화 속도」. Measured this lap over the spoken blockquotes only (Hangul syllables plus numerals read as sino-Korean, ⚠ blocks and tables excluded): 도입 161 syllables in 25 s, 1막 235 in 45, 2막 279 in 55, 3막 318 in 75, 4막 319 in 55, 마무리 318 in 45. **1,630 syllables against 300 s is 5.43 syllables per second sustained, and the implied rate ranges 4.24 (3막) to 7.07 (마무리) — a 1.67× spread.** Whatever the true comfortable rate for spoken Korean is, one number cannot satisfy both ends: if 3막's 4.24 is right the script needs 385 s; if 마무리's 7.07 is right it needs 231 s and 3막 is 30 s too long. **The segment that must be spoken fastest is 마무리, the limitations close, and it is last.** `docs/auto/RUBRIC.md` 심사개요 says 「발표는 5분을 넘지 않는 것을 권장; 길어지면 중단될 수 있음」, so the material that gets cut when the clock runs out is the material the 과학적 사고 row is scored on — five times, once per judge. The three **[버림]** sentences buy back 84 syllables, about 17 s, and §2 of the same document guarantees interruptions that will spend it. This is not an argument that the script is too long; it is that its internal budget is unmeasured. **Done when:** the six segment times are re-derived from a per-segment syllable count at ONE rate, the table and every cumulative bracket updated to still sum to 300 s (proportional re-budget at 5.43 syl/s is 30 / 43 / 51 / 58 / 59 / 59 s), any segment still over its share trimmed of text that carries neither a number nor a caveat, and `tests/test_demo_script_5min.py` gains a check that each segment's spoken syllable count over its seconds stays inside a stated band. No caveat is deleted to buy seconds (CHARTER §3.5). The half no test reaches is still R12 / NH-014: a human reading it aloud with a stopwatch. **Cheapest test, if the row is doubted:** read 마무리 aloud once and time it. | done(c2693b7 + this lap's reviewer fixes) — the six segment times are now the largest-remainder allocation of a measured syllable count at ONE rate: 1,684 syllables, 5.61 syl/s, 29 / 44 / 50 / 60 / 59 / 58 s, spread 1.62x → 1.02x, and 마무리 · 한계 goes 45 → 58 s. No caveat deleted (proportional allocation puts no segment over its share). `scripts/measure_demo_script_pace.py`, `data/processed/demo_script_pace/pace_20260905T0625Z.json` + `pace_before_039a0de.json`, `docs/demo_script_pace.md`, `tests/test_demo_script_pace.py` (9, mutation-checked). The half no test reaches — whether 5.61 syl/s is sayable — is untouched and is still R12 / NH-014 | true | minutes to one lap | A 구현 및 유용성 · A/B 제출 자료 · R4 |
| WFG-103 | **P0** | KCF | **The core act of the booth script tells five judges the comparison is against a map that sees the fire now; it is against a map that does not see the fire at all.** `docs/auto/DEMO_SCRIPT_5MIN.md:108-109`, inside 3막 — the segment the same document calls 「이 작품의 전부」 — is spoken aloud: 「**STATIC VIEW.** 거리 기준 직행 경로입니다. **지금 이 순간만 보는 지도라면 이 길을 권했을 겁니다.**」 The arm that route comes from is fire-blind, and this repository says so twice in its own words: `src/wildfireguardian/routing/evacuation.py:270` (`"""Fire-blind shortest path to the nearest shelter, then scored against the hazard."""`) and `docs/real_roads_real_hazard.md:50` (「**naive** — the fire-blind shortest walk to the nearest refuge (the status quo)」). The difference runs in the project's favour: a map that sees the fire where it is *now* already refuses the cells burning now, so part of the **91곳 / 368곳 · 24.73 %** the next spoken sentence attributes to knowing where the fire *will* be is bought by knowing where it *is*. Filed by critic #17 as its one `fix-before-next-row` item (CHARTER §14b). **Do not enlarge this row into the arm study** — that is WFG-033 (P2) and the scope question is NH-027; this row is one sentence. | done(92366cb) — the script's 3막 sentence AND the same claim on the judged screen (`web/finals.html`, the `later` branch of the STATIC VIEW caption, KO and EN; critic #17's finding named only the script). Pace gate went red on purpose and was re-measured: 1,692 syllables, 28/44/50/61/59/58, new tag `20260905t0947z`. A defect in `test_the_artifact_the_registry_points_at_is_committed_and_current` (it selected the *before* artifact by sort order) was found and fixed by the re-measure | true | minutes | B/A 제출 자료 · B 데이터 수집·분석·해석 · R4 |
| WFG-037 | P0 | KCF | Booth recipe `docs/auto/finals/BOOTH_SETUP.md`: exact steps for the judged laptop (env, `make all-checks`, open `file://` with Wi-Fi off, key bindings, two USB copies, fallback if the laptop dies), plus NH-014 asking the author to run it once | done(20260905T1217Z) — `docs/auto/finals/BOOTH_SETUP.md`, Korean, eight sections, **every command in it executed in this sandbox and its exit code read** rather than transcribed. Gated by `tests/test_booth_setup.py` (9, mutation-graded 4/4: a path that does not exist, a `make` target that does not exist, a rebound key in `web/finals.html`, the checker dropped from the bundle payload). Three things the writing found by running: (a) **`make all-checks` aborts at `baseline-verify` on the laptop too** — four of its six differences are tracked-file drift, not the missing raw bundle, so R3's own command is not runnable and §1.1 sends the student to `gates.py --mode full` instead (**NH-029**); (b) **`make finals-bundle` does not check a USB copy**, because it overwrites the bundle from the tree before hashing — measured by appending seven bytes to the bundle's `finals.html` and getting `OK`; the missing half is `scripts/check_bundle_copy.py` (stdlib only, reads and never writes, travels in the bundle, `tests/test_check_bundle_copy.py` 12, graded against a flipped byte, a truncation, a deletion and a stray file); (c) **the builder never scans the folder**, so a file left by an earlier run rides onto the stick while the run reports `byte-identically` — found by the new checker seconds after a green builder run, filed as **WFG-108**. Also measured, not asserted: all four screens open from `file://` with **zero** external requests under a headless browser that blocked everything but the file itself, `web/console.html` degrades to its pre-computed state with a sentence rather than a dead screen, and `make finals` rewrites `web/finals.html`'s build stamp and so breaks the bundle's byte-identity — §3.4 tells the student not to run it after the freeze. **The lap's independent reviewer returned `block` and was right:** §5.6 told the student to check that the language button reads **KO**, a sentence copied from `README_KO.md` and `DEMO_SCRIPT_5MIN.md` and never run — `web/finals.html:2154` labels the button with the language a press switches TO, so a Korean screen reads **EN** and following the step switches the judged demo into English. Corrected on all three surfaces and pinned by a test. The same review showed the key table was grading bare `case` labels rather than what the keys do, so it now holds the whole binding and turns red on a rebound action. It also found **WFG-109** (P0), which is this row's most valuable output and is not this row. NH-014 updated; R12 is still the author's | true | hours | 구현 및 유용성 |
| WFG-106 | **P0** | KCF | **The number that answers the SECOND-strongest objection to the headline is already registered, already gated, and reaches no judge.** ⚠ **Corrected by critic #19, 2026-09-05, before a lap writes a card on it.** Critic #18 filed this row as 「the number that answers the strongest objection outright」 and that is wrong, because it names the wrong objection. There are two, and they are different questions. (i) **Dilution** — 「91 of 368 includes 265 walkers who were never in danger」. This row answers that. (ii) **The opponent** — 「you compared against a router that does not look at the fire at all; what about one that avoids where the fire IS?」, which is critic #17's root objection, WFG-104's card and NH-027's experiment. This row does **not** answer (ii) and cannot: the conditional rate's denominator is defined by the fire-blind arm — 「of the origins whose **FIRE-BLIND** route is unsafe」 is the derivation string's own wording — so 88.3 % inherits exactly the opponent that is in dispute. A card that offers 88.3 % as the answer to (ii) would be a stronger overclaim than the sentence WFG-103 just withdrew. Write the card for (i), say in it which question it answers and which it does not, and point at WFG-104 for the other. The row's value is undiminished: the repository has already computed the version that is not diluted by the walkers who were never in danger, and it is a registry key: **`mr_uiseong_fa_rescue_rate` = 0.883**, whose own derivation string reads 「naive_into_FA_safe / (naive_into_FA_safe + no_safe_route + both_enter) — of the origins whose FIRE-BLIND route is unsafe, the share the future-aware router still gets to a refuge」. `docs/multi_region.md:430-434` prints the whole row for all three fires: 영덕 **44 / 458 = 9.61 %** unsafe → **95.5 %** rescued; 의성·안동 **103 / 368 = 27.99 %** unsafe → **88.3 %** rescued; 울진·삼척 **13 / 393 = 3.31 %** unsafe → **23.1 %** rescued. Drilled this lap against the files: `88.3`, `27.99` and `103 / 368` appear **zero times** in `docs/auto/JUDGE_QA.md`, `docs/auto/DEMO_SCRIPT_5MIN.md`, `paper/manuscript.md` and `docs/finals_screen_v2.md`. So the student stands at the booth with the diluted number (91 of 368, of which 265 were never at risk) and not the conditional one, and 울진·삼척's **23.1 %** — the number that runs against the project and is this repository at its most honest — is spoken nowhere. **Done when:** `JUDGE_QA.md` carries one T0 card giving the conditional rate for all three fires with its key and `docs/multi_region.md` as the citation, saying plainly that the denominator is the origins whose fire-blind route is unsafe and that the three rates do not order with core growth (`multi_region.md:436`); and `DEMO_SCRIPT_5MIN.md` 3막 gains at most ONE sentence of the same, subject to WFG-100's budget gate (the pace test goes red on purpose; re-measure per `docs/demo_script_pace.md`). ⚠ **Carry the key's own caveat and fix it while there:** the caveat on `mr_uiseong_fa_rescue_rate` reads 「Conditional rate on a SMALL denominator (13–20 origins)」, which is true of 울진·삼척's 13 and **false of this key's own 103 and of 영덕's 44** — critic #19 read all three entries in `docs/NUMBERS.json` and the caveat string is byte-identical on all three, so it is wrong on two of the three keys it is attached to. Correct it to name each region's denominator, do not delete it. Do **not** run the WFG-033(b) arm here; this row spends no compute and adds no claim, it publishes three numbers the tree already re-derives | todo | true | hours | B 데이터 수집·분석·해석 · B/A 제출 자료 · R5 |
| WFG-104 | **P0** | KCF | **The strongest methodological objection to the headline has no card in the Q&A bank, and the experiment that would answer it is scheduled for after the competition.** `docs/auto/JUDGE_QA.md` holds 41 questions and none of them is 「불을 전혀 모르는 경로와 비교하신 겁니다. 지금 불이 있는 자리만 피하는 경로와 비교하면 어떻게 됩니까?」 (critic #17 searched the bank for naive / perimeter / buffer / 기준선 / 베이스라인 / 대조군 — no card). The repository already knows the question: **WFG-033** names the arm that answers it — 「(b) static current perimeter (slice 0, p ≥ p_cut) + fixed buffer 0.5/1/2 km」 — with a `done when` of 「says plainly whether the learned field beats 'current perimeter + buffer' on routing decisions」, and WFG-033 is **P2**, i.e. after 2026-10-24. ⚠ **PREMISE CHANGED BY WFG-114, 2026-09-05 (20260905T1820Z lap): the arm HAS now been run.** The instruction below to write 「the present-perimeter arm has not been run」 is now FALSE and a card saying it would be a fabricated limitation. **Write the T0 card on the measured result instead**, in the student's own voice, from `docs/present_perimeter_arm.md` and the `pp_uiseong_*` keys: the baseline is fire-blind (`docs/real_roads_real_hazard.md:50`); against a **present-aware** opponent at a 1 km buffer the forecast's margin is **9 origins of 368**, not 91; and the reason that is not a concession is that 1 km sits on the crossing of two failure regimes (250/500 m walks 91/80 origins into the fire; 2/3 km leaves 80/73 unable to finish in the 600-minute budget) which an operator cannot identify on the day, while the forecast-aware arm reaches 354 with no width to choose. Carry the key caveat verbatim and the forbidden phrasings (「예보는 필요 없다」, 「1 km가 최적」). *(Superseded original: 「Write the T0 card, do not run the arm … that the present-perimeter arm has not been run, where the plan for it lives (WFG-033)」 — kept as a record, not an instruction.)* Model it on 4막's 「저희가 진 결과도 화면에 있습니다」 paragraph, which is this repository at its best. ⚠ **BLOCKED BY CRITIC #22, 2026-09-05T2330Z, ON THE NUMBER ITSELF — NH-032.** The 2132Z lap built this same arm concurrently and got **27** where the lap on `auto/dev` got **9**, and **5** at the sweep's best buffer; the two differ only in how the opponent is built (prune-and-shortest-path with no budget, versus the time-expanded router on a frozen field, budget-capped, refusing departure from inside the buffer). Both are green, both re-derive the committed 91 first, both are defensible readings of 「a county office with a perimeter map」. Its work is at `auto/red/20260905T2248Z` and its own escalation says it plainly: 「Until you answer, no judge-facing surface should carry either margin.」 So **this card is not writable today**: a T0 card quoting 9 would put on a judge-facing surface a number this repository's own second measurement contradicts by a factor of three. Unblocks on **NH-032** (which opponent) and **NH-034** (whether it goes on the screen at all). A ⚠ 근거 확정 전 note is on the Q19 answer that carries the 91, giving the student the sentence to say meanwhile. **What IS writable today and is not blocked:** the half of the card that says the baseline is fire-blind and that the fair-opponent arm HAS been run, pointing at `docs/present_perimeter_arm.md`, with no margin in it. Add the matching one-sentence interruption answer to `docs/auto/DEMO_SCRIPT_5MIN.md` §2 under the ML-reviewer lens **only if** §1's syllable budget can absorb it (`docs/demo_script_pace.md` re-measure procedure; the pace gate goes red on purpose). **Carried in from WFG-103's reviewer (2026-09-05):** the script's 3막 now says the baseline is 「이 도구가 없을 때의 기준선」, which is a counterfactual the repository *labels* (`docs/real_roads_real_hazard.md:50` calls `naive` 「the status quo」) but has never measured. It ships qualified by a ⚠ note, but **that sentence should be owned by this card rather than by the script's stage direction** — the card is where a claim about what a resident without the tool actually does belongs, with its label and its limit. **Confirmed and sharpened by critic #18, 2026-09-05, which also found the two surfaces now disagree:** the screen says only what is measured (`web/finals.html`, KO 「화재를 전혀 보지 않는 지도가 그리는 경로입니다」, EN 「This is the route a fire-blind map draws」) while the script (`docs/auto/DEMO_SCRIPT_5MIN.md:108-109`) adds 「이 도구가 없을 때의 기준선입니다」 — a counterfactual about what a resident without this tool actually walks, which this repository labels (`docs/real_roads_real_hazard.md:50` 「the status quo」) and has never measured. Same lap, same fix, two surfaces, and only one of them added the unmeasured clause; a judge reads the screen and hears the script at the same time. **The card must also carry the number that answers the objection outright — see WFG-106**, whose 88.3 % is the undiluted form of the 24.73 % this row is about. | blocked(NH-032) — the margin half only; see the critic #22 note above for the half that is writable now | true | one lap | B 설계와 방법론 · B/A 제출 자료 · R5 |
| WFG-036 | P0 | KCF | **Final product bundle** `release/kcf-finals-2026/`: `web/` whole (finals + console + field view), printables PDF, `README_KO.md` 10-line run recipe, `CITATION.cff`, `make finals-bundle` byte-identical rebuild; definition of done = `docs/auto/KCF_READINESS.md` R1–R11 | todo — **v1 `done(20260905T0355Z)`**: `release/kcf-finals-2026/` exists with the four screens, fonts, `LICENSE`, `CITATION.cff`, a ten-step Korean `README_KO.md` and `MANIFEST.json`; `make finals-bundle` re-derives every SHA-256 and refuses a bundle the manifest does not describe (`scripts/build_finals_bundle.py`, `tests/test_finals_bundle.py`, `docs/finals_bundle.md`). The payload is generated and git-ignored, and that trade is argued in the doc. **v2 outstanding, due 09-14:** printables (blocked on WFG-007) and the booth recipe (WFG-037). ⚠ **Critic #31, 2026-09-07: both of those blockers are gone and the bundle did not notice.** WFG-007 is `done(20260906T0620Z)` and WFG-037 is `done(20260905T1217Z)`; the kit is 33 pages at `20260907T0059Z`, and `MANIFEST.json` at `3f881f6` still lists **17 files, none of them a printable**. The printables half of v2 is carved out as **WFG-151** (position 1, with the gate that lets a bundle omit a file R9 names), so this row keeps the booth-recipe half and the byte-identical-rebuild property; it is **updated, not duplicated** | true | two laps (v1 by 09-10, v2 by 09-14) | 구현 및 유용성 · 제출 자료 |
| WFG-101 | **P0** | KCF | **The headline 24.73 % was 3.53 % in this repository's own committed record, and no judge-facing document says so.** `docs/dem_defect_2026-08-02.md:67-70`: re-acquiring the DEMs at `9ba83b4` — Uljin-Samcheok's raster had filled the East Sea with a ramp to −497 m, and that region trains every other fire's leave-one-fire-out model — moved 의성·안동's predicted envelope +38 % (95 → 131 core cells) and its future-aware-only share **from 3.53 % to 24.73 %**. `docs/decision_shift.md:118-151` holds the origin-level ledger of the same event (346/13/0/9 → 263/91/12/2, 90 origins changed bucket, arithmetic reconciled), and `docs/multi_region.md:8-18` puts it in a warning blockquote at the top of the page — **the first thing a judge who opens that file reads**. Drilled this lap against the files: `docs/auto/JUDGE_QA.md` (41 questions) contains no `3.53`, no `DEM 재취득` and no question about the stability of the routing shares; `docs/auto/DEMO_SCRIPT_5MIN.md` says 24.73 % three times and never that it moved. A software professor or a fire scientist asks 「이 숫자가 얼마나 흔들립니까?」 and the honest answer — 7.0× on one self-found data defect that the project corrected and recorded — is the best 과학적 사고 answer this project owns and the student has no card for it. The same paragraph covers 영덕 3.70 % → 9.17 % (`multi_region.md:16`, the reverted-run lineage), which the script also quotes without its history. Distinct from **WFG-096**, which is about sampling uncertainty; this is about sensitivity to a corrected input, and it is measured rather than absent. **Done when:** `JUDGE_QA.md` carries one T0 card citing `dem_defect_2026-08-02.md` and `decision_shift.md` with the before/after and why the after is the value quoted, its own 41-count updated in both places (`:17`, `:930`) and the drill table extended in the same commit; and `DEMO_SCRIPT_5MIN.md` 3막 gains ONE sentence of the same fact, subject to WFG-100's second budget. | todo | true | hours | B 데이터 수집·분석·해석 · B/A 제출 자료 · R5 |
| WFG-115 | P1 | KCF | ⚠⚠ **CRITIC #26, 2026-09-06T1100Z — THE PREMISE OF THIS ROW IS FALSE AND IS WITHDRAWN. P0 -> P1, and the title above is kept as a record rather than deleted (CHARTER §3.7).** `41498ef` **IS** an ancestor of `HEAD`. Measured here on a clone deepened to **300** commits: `git merge-base --is-ancestor 41498ef HEAD` exits **0**; `git rev-list HEAD | grep -c 41498efbf0679276c140b3cbfc0819e5265e7733` answers **1**; `git rev-list --count 41498ef..HEAD` answers **277**; and `git branch -a --contains 41498ef` names `auto/dev`, `origin/auto/dev` and `origin/Main` alongside the two branches this row lists. **The object sits 277 commits back, and every lap that tested it measured inside a shorter graph than that.** Critic #20 raised the row in the sandbox's default **depth-50** clone; critic #21 deepened by **120** (170 commits) and re-confirmed; critic #24 deepened to **250** and re-confirmed 「so the shallow boundary is not the confounder」. 250 < 277, so it still was. Five consecutive critic laps (#20, #21, #23, #24, #25) published this as fact into `docs/auto/KCF_READINESS.md` R1, into `docs/auto/DIRECTION.md`, into `docs/auto/SCORECARD.md` (critic #20 docked Track A 구현 및 유용성 18 -> 17 naming it, and that deduction is withdrawn by critic #26's row), and into `docs/finals_screen_v2.md` §4.3, which now cites this row in the screen's own explainer. **`docs/auto/JUDGE_QA.md` Q35 is CORRECT as written** — the panel's commit is in this repository and is reachable from this branch — and must not be edited to say otherwise. **Nothing on the judged screen is wrong about reachability and no lap may change that line on this row's old premise.** ⚠ **What survives, and it is the smaller, real defect.** `registry.built_at_commit` is `41498efbf067`, a commit at which `docs/NUMBERS.json` held **153** entries, while the card beside it prints **383**; `build_numbers.py` is deliberately not re-run (WFG-040), so the line is **stale by construction and mislabelled**, not unreachable. A judge reading 「built at commit 41498ef」 next to 383 entries has a fair question and this repository has no sentence answering it. **Done when:** the provenance line says what it actually names (the commit the registry's schema was last built at) rather than 「built at」, or the line is dropped, with `docs/finals_screen_v2.md` §4.3's WFG-115 bullet corrected in the same lap; a test asserts ancestry for **every** commit-shaped field in the payload **and runs `git fetch --unshallow` or skips with a stated reason on a shallow clone** (WFG-119 is why this clause exists); `docs/auto/KCF_READINESS.md` R1's cell and `docs/auto/DIRECTION.md` carry the withdrawal. ⚠⚠ **CRITIC #27, 2026-09-06T1400Z: the last clause of this cell was 「Q35 needs no change」 and it was wrong, which is WFG-133.** The withdrawal reached `CRITIC_LATEST.md`, `KCF_READINESS.md` R1 and `DIRECTION.md` and stopped at the file the student reads aloud: Q35's ⚠ block still carries the false measurement and still instructs the student to say it. Q35's **draft answer** needs no change, which is what critic #26 verified; Q35's **⚠ block** is a separate object and is false. Fix it on **WFG-133**, not here | todo | true | one lap | B/A 제출 자료 · R1 |
| WFG-118 | P1 | infra | **The tail of this table is in filing order, not priority order, so the fallback the charter defines would take a P1 infra row before three P0 judge-facing ones.** CHARTER §5 says the dev routine takes the first `todo` row in table order; CHARTER §14 forbids a P0 row sitting below a non-P0 row. Measured at `492364c` by parsing the table: `todo` **P1** rows WFG-107 (pos 22) and WFG-108 (pos 23) sit above `todo` **P0** rows WFG-110 (25), WFG-113 (28) and WFG-115 (29), with P1 WFG-111 (26) and WFG-112 (27) interleaved between them. Rows have been appended at the bottom as they were filed since about WFG-105 and the priority column has not been used to re-sort. Today the harm is latent because `docs/auto/DIRECTION.md` names the next rows and CHARTER §14b says the page wins over the table — but critic #20's own finding is that the page went stale for a full lap after an author push, and the table is exactly what a lap falls back to when that happens. Critic #21 moved **one** row (WFG-115, its whole reorder budget) and left the rest, because the defect is systemic and one move per lap cannot fix it. **Parked behind R1, R3, R7, R8 and R9 by CHARTER §14b** — it is loop hygiene and the printables come first. **Done when:** the `todo` rows below the done block are sorted by priority with ties keeping filing order, and a test (or a `gates.py` step) fails when a `todo` P0 row appears below a `todo` non-P0 row in `docs/auto/BACKLOG.md`, graded by swapping two rows and seeing it go red | todo | true | minutes | — |
| WFG-119 | **P0** | infra | **Every cloud lap runs its ancestry checks against a truncated commit graph, and no lap has ever said so.** `git rev-parse --is-shallow-repository` answers `true` in this sandbox and `git rev-list --count HEAD` answers **50**: the routine's checkout is a depth-50 clone, so the branch's history before roughly the last day is simply absent. `git merge-base --is-ancestor` cannot answer correctly across a shallow boundary, and that command is the instrument behind WFG-067's gate (`tests/test_finals_screen.py:523`, `:550`, `:649`) and behind every 「not reachable from HEAD」 claim four critic laps have written, WFG-115 included. Two consequences, one checked and one predicted. **Checked:** critic #21 ran `git fetch --deepen=120` (170 commits) and re-tested WFG-115 — `41498ef` is still not an ancestor, so that finding survives with the confounder removed, and the same deepening is what made the 24-hour window reachable at all (the window's base, `baf6962`, sits below the depth-50 boundary). **Predicted and not yet observed:** the screen's own stamp `5f9a3b8` is 7 commits behind `HEAD` today, and the branch takes on the order of 40 commits a day, so once a stamp ages past 50 commits without `make finals` being re-run the ancestry test goes RED in every sandbox while staying GREEN in CI, which checks out at `fetch-depth: 0` (`.github/workflows/auto-gates.yml:22`, `:67`). A red that only the sandbox sees is the mirror image of CHARTER §4b's case and would be read as a real defect. ⚠ Filed with it, because it is the same class: the routine prompt's own step 2 command, `curl https://api.github.com/repos/.../actions/runs`, now returns **403** 「GitHub access is not enabled for this session」 in this sandbox; the runs have to be read through the GitHub MCP instead ⚠ **CRITIC #58, 2026-09-10T1120Z: IT IS NOT 403 TODAY.** `curl -s -o … -w '%{http_code}' 'https://api.github.com/repos/Sparkxt-0318/wildfireguardian/actions/runs?branch=auto/dev&per_page=50'` returned **HTTP 200** and a full 50-run page in this lap's sandbox, and the window's run census was taken from it. Recorded, **not** acted on: a capability that is 403 on some laps and 200 on others is not something a gate or a routine step may depend on, so the GitHub MCP stays the documented path and `curl` stays a convenience that a lap must test before trusting. The stored critic prompt tells this routine to `curl`; on a lap where it answers 403 the MCP is the fallback and that is not a finding., which critic #21 did (and critic #24 after it: `curl` was not attempted, the MCP `actions_list` was). **Parked behind R1, R3, R7, R8 and R9 by CHARTER §14b.** ⚠ **CRITIC #24, 2026-09-06: the prediction now has a date on it.** Measured at `91d3e05`: `git rev-parse --is-shallow-repository` is `true`, `git rev-list --count HEAD` is **50** before deepening, the screen's stamp `5f9a3b8` is **23** commits behind `HEAD`, and the branch took **53** commits in the last 24 h. At that rate the stamp crosses the depth-50 boundary in **roughly twelve hours** — i.e. during 2026-09-06 — after which `_stamp_is_reachable`'s `cat-file -e` fails in every sandbox while CI at `fetch-depth: 0` stays green, and `test_the_integrity_panel_names_a_commit_this_repository_has` goes RED on a screen that is not wrong. The test's own docstring calls that limit 「narrow and safe ... a red gate asking for a rebuild, not a wrong screen shipped」, and it is safe only if a lap is free to rebuild: CHARTER §9 sends a red-gate lap to `auto/red/<stamp>` instead of building, so the first lap to hit it spends itself parking. **The `make finals` half of WFG-113 resets this clock as a side effect**, which is one more reason to run it before the boundary rather than after ⚠⚠ **CRITIC #26, 2026-09-06T1100Z: this row's predicted failure is not in the future. It has already happened, five times, and it is the most expensive thing in this file.** The truncated graph did not turn a gate red; it produced **false findings that five critic laps published as measured fact** — see the withdrawal at the top of **WFG-115**. Each of those laps wrote 「re-run rather than read」 and each re-ran the same command inside the same short instrument; the two laps that suspected the instrument deepened by an amount they chose (**120**, then **250**) rather than by the amount that settles the question, and reported the same wrong answer with more confidence. **Deepening by a guess is not a control.** **Done when:** `scripts/auto/bootstrap.sh` deepens or unshallows the clone (or the tests that assert ancestry skip with a stated reason on a shallow clone rather than failing), CHARTER §4's sandbox-facts paragraph records the shallow checkout, and `docs/auto/ROUTINE_PROMPTS.md` step 2 names the MCP rather than the `curl` that no longer works, and **every reachability or ancestry claim any lap writes is made only after `git rev-parse --is-shallow-repository` answers `false`, with that fact recorded beside the claim** ⚠ **DEV LAP 2026-09-06T1230Z, on a clone where `git rev-parse --is-shallow-repository` answers `false` (485 commits): critic #26's conclusion holds and its number does not.** `git merge-base --is-ancestor 41498ef HEAD` exits 0, so the withdrawal of WFG-115's premise stands. But `git rev-list --count 41498ef..2c8ecbd` answers **280**, not the **277** critic #26 published — that lap deepened to 300, and 300 is still not the graph. ⚠ **The head is pinned on purpose, and this lap's own reviewer had to teach it that:** the first draft of this note wrote `HEAD`, and `HEAD` moved during the lap — at `fc05320` the same command answers **281** and the clone is **486** commits, not 485. An ancestry distance is a function of the head it is measured at, so publishing one without naming that head is the same class of error this note faults critic #26 for. Measured at `2c8ecbd`: distance **280**, clone **485** commits, `is-shallow-repository` **false**. The rule it wrote in the same breath (「no ancestry claim until `is-shallow-repository` answers false, record the depth beside it」) is exactly the rule its own headline number does not satisfy, one turn of the same screw. 277 appears in `docs/auto/DIRECTION.md`, `CRITIC_LATEST.md` and the 1112Z critic report; a dev lap does not write DIRECTION (CHARTER §14), so the correction is recorded here for critic #27. ⚠⚠ **CRITIC #33, 2026-09-07T0800Z: THE PREDICTION FIRED, THIS ROW GOES P1 → P0, AND IT IS THE ONE `fix-before-next-row` ITEM. This row is eligible under CHARTER §14b twice over: it is a red gate, and its subject is `web/finals.html`, the finals screen, which §14b names as judge-facing.** Measured, not read. The routine's default clone at `2896c9c` is `is-shallow-repository` = `true` with `rev-list --count HEAD` = **50**. `gates.py --mode full` exits **1**: `2 failed, 1646 passed, 62 skipped, 2 xfailed`, cold, 247.0 s, and both failures are `tests/test_finals_screen.py::test_the_integrity_panel_names_a_commit_this_repository_has` and `::test_the_escape_this_gate_cannot_close_is_still_open`, each saying `web/finals.html` names commit `62b58e1` which is not an object in this repository. `62b58e1` is **55** commits behind `HEAD`, built 2026-09-06T09:21Z. After `git fetch --unshallow` (531 commits) both pass, `62b58e1` resolves, and `git merge-base --is-ancestor 62b58e1 origin/auto/dev` exits 0. GitHub `auto-gates` run **201** at this exact head is `success` (`fetch-depth: 0`, `.github/workflows/auto-gates.yml:22`). **That is this row's own sentence, word for word, with dates on it:** 「once a stamp ages past 50 commits without `make finals` being re-run the ancestry test goes RED in every sandbox while staying GREEN in CI」. The crossing point is measurable: at `0fc6130` the stamp was 49 back (inside a depth-50 horizon, and critic #32 reported ALL GREEN); at `f6d8246`, 2026-09-07T05:22Z, it reached 50 and every default-clone gate run since has been red. ⚠⚠ **The gate is RIGHT and the tree is WRONG, and this is the half two laps have now got backwards.** `_needs_git_history` (`tests/test_finals_screen.py:526`) deliberately does **not** skip on a shallow clone and its docstring argues the residual is 「a red gate asking for a rebuild, not a wrong screen shipped」. That argument holds: the screen a judge opens reports a build 55 commits and 23 hours stale, `make finals` is the remedy the failure text prints, and this gate is the only thing in the repository that noticed. ⚠ The docstring's *premise* is stale and should be corrected in the same lap: it says 「the cloud sandbox clones shallow (294 commits deep, but flagged shallow)」 and this sandbox cloned at **50**. **The 0711Z dev lap hit these exact two failures, unshallowed, wrote 「They passed once the clone was complete; nothing in the tree was wrong」 under 「Also verified rather than assumed」, and filed nothing** — so the habit every critic since #24 has treated as rigour (unshallow before measuring) is, for this one gate, the thing that suppresses its signal, because the gate's entire content is 「is the stamp close enough that a fresh clone can see it」 and unshallowing answers yes by construction. ⚠⚠ **CHARTER §4 step 2 says 「Red baseline → do not build」, so until this is cleared the next dev lap is entitled to read a diagnosed red as a stop.** It is not one: the red IS the item, and clearing the item clears the red. **Done when:** (a) `make finals` is re-run on the commit being pushed so `web/finals.html`'s `git` stamp names a pushed commit within the shallow horizon, and (b) the recurrence is closed rather than reset, because the branch takes on the order of 40 commits a day and a rebuild alone buys about 1.3 days before this returns (critic #26 already watched the counter reset 29 → 6 and read that as the row being handled). Two shapes, and the lap picks one with its reason in the report: a gate that fails when the stamp is more than N commits behind `HEAD` while N is comfortably under 50, which catches it *before* it crosses; or `make finals` folded into the push path so the stamp can never age. Also (c) correct the `_needs_git_history` docstring's 294 to what a lap actually measures. **Constraints:** rebuilding `web/finals.html` regenerates a tracked judge-facing artifact, so read CHARTER §3.2 first and run `scripts/check_screen_assets.py`; do not weaken or skip either test to get green (§3.9 and the docstring's own argument); the two `test_finals_screen.py` failures must be reproduced red before the fix and shown green after it, in the report ⚠⚠ **DEV LAP 2026-09-07T0918Z: critic #33's (a)(b)(c) are done and TWO CLAUSES OF CRITIC #26'S OWN DONE-WHEN ARE ALSO CLOSED, but clause (i) is outstanding, so this row goes back to `todo` with a residue note (CHARTER §5) rather than to `done`.** Reproduced RED first at `7308b06` on the routine's own default clone (`is-shallow-repository` = `true`, 51 commits): `tests/test_finals_screen.py` **2 failed, 30 passed**, both naming `62b58e1`. **(a) done.** `make finals` rebuilt at `7308b06`, a commit already on `origin/auto/dev`; **exactly one line changed, 434, the payload**; `make finals-bundle UPDATE=1` carried the sha256 into the 19-file MANIFEST (the bundle's `web/` payload is git-ignored). Both tests green: **34 passed**. **(b) done.** Neither existing test touched (§3.9). New gate `test_the_screen_is_rebuilt_before_its_stamp_ages_out_of_this_clone` asserts the stamp is at most `STAMP_MAX_COMMITS_BEHIND` = **30** commits behind `HEAD`, treats an unresolvable stamp the same way, and prints the **measured** clone size, the remedy and this row id. ⚠⚠ **The first draft of this closure claimed the new gate fires 「24 commits earlier」 and that was FALSE; the independent reviewer nailed it and the number is corrected everywhere it was written** (here, MEMO, `docs/finals_screen_v2.md` §4.4). The old gates do not fire at a fixed distance: a clone holding D commits resolves a stamp at most D−1 behind `HEAD`, so they fire at **D**, the clone's own depth — re-measured here, `HEAD~51` resolves and `HEAD~52` is `fatal: Needed a single revision` in a 52-commit clone. **The lead is (clone depth − 30)**, about **20** at the measured depth-50 checkout, and **unbounded** in CI at `fetch-depth: 0` where those gates never fire from staleness at all. 「55」 was only where the stamp sat when critic #33 looked. Two probe points do not locate a boundary — the same error this row faults critic #26 for. Graded by mutating the payload stamp and restoring it: at **31 behind** the two reachability tests stay **GREEN** and only the new gate fires; at the real `62b58e1` all three fire. ⚠ **The threshold's own grader was VACUOUS in its first version and the reviewer caught that too:** it built its 「over the limit」 case from `STAMP_MAX_COMMITS_BEHIND + 1` and then judged with the same constant, so it could not fail for any threshold (passed at 5, 20 and 45) — the WFG-156 class one function below where this row records it. Rewritten as `test_the_staleness_threshold_and_its_helper_are_both_graded` with **two hard-coded distances (29, 31) not derived from the constant**; the grid is now red at 5, 20, 28, 31, 32, 45 and green only at 29 and 30. Its skip bound is also a literal, so raising the threshold cannot silently skip its own grader. **(c) done.** `_needs_git_history`'s 「294 commits deep」 replaced by the measurement and by the point it hid: **the depth is not a constant** (50 here on two laps, 294 earlier, 531 unshallowed). **Critic #26's done-when, clause by clause. (ii) CLOSED:** CHARTER §4's sandbox-facts paragraph now records the shallow checkout, the three consequences the loop has paid for, and the rule that deepening by a guess is not a control — it had **zero** occurrences of 「shallow」 or 「depth」 before this lap. **(iii) CLOSED:** `docs/auto/ROUTINE_PROMPTS.md` step 2 now names the GitHub MCP call and states that `curl` against `api.github.com` returns 403 here. ⚠⚠ **(i) OUTSTANDING, and the earlier residue note for it was itself wrong:** it said 「`scripts/auto/bootstrap.sh` still clones shallow」, and that script **clones nothing** — it only builds the venv. The shallow checkout is made by the routine harness before any repository script runs, which is *why* clause (i) is not a one-line edit and may not be doable from inside the repository at all. The argument for leaving it: deepening would end the WFG-115 false-finding class but would also delete the only signal that has ever reported this staleness, so the deliberate carrier (b) had to exist first. It now does, so (i) can be decided on its merits by the next lap or by the author. ⚠ Also found by the reviewer and fixed in this lap: `docs/auto/JUDGE_QA.md`'s citation of 「the two lineage checks」 was by **line number** (`:544`, `:550`, `:649`) and this lap's own edit made all three point elsewhere — the WFG-133 class, on the file the student reads aloud. Re-cited by **test name**, and the kit rebuilt in the same lap because JUDGE_QA is a printables `SOURCES` document (WFG-152). `docs/finals_screen_v2.md`'s live gate-inventory count went 32 → 34; the 32 inside the WFG-109 record paragraph was left alone (§3.5). Documented: `docs/finals_screen_v2.md` §4.4  ⚠ **CRITIC #34, 2026-09-07T1100Z: (a) (b) (c) VERIFIED GREEN, AND CRITIC #33's FALSIFIABLE TEST 1 IS ANSWERED IN THE LAP'S FAVOUR.** Measured on the routine's **default** clone before any deepening (`is-shallow-repository` = `true`, `rev-list --count HEAD` = **50**): `gates.py --mode full` exits **0**, ALL GREEN, `1650 passed, 62 skipped, 2 xfailed`, pytest 260.6 s; `web/finals.html` names `7308b06`, **6** commits behind `HEAD`, well inside the new limit of 30. The recurrence was **not** reset — a depth-independent carrier gate exists, and its grader's two distances (29, 31) are hard-coded literals rather than derived from the constant, which is the correct repair of the vacuous first version the lap's own reviewer caught. ⚠ **Two residues, both filed rather than carried, because CHARTER §14b holds hygiene behind R1, R3 and R8.** (1) The threshold's stated frequency is optimistic against this branch's own measured rate — **WFG-160**. (2) When the new gate does fire, no rule tells the dev lap what to do with it, and CHARTER §4 step 2 currently says stop — **NH-043** and **WFG-161**. ⚠ **Clause (i) is now safe to take in a way it was not before.** The lap's argument for leaving it was that deepening would delete the only signal that has ever reported this staleness; gate (b) is depth-independent, so that objection is spent and the decision is on its merits| todo — **(a) (b) (c) and critic #26's (ii) (iii) done(20260907T0918Z); clause (i), the clone depth itself, outstanding and now a decision rather than work** | true | hours | R1, R3 |
| WFG-225 | **P0** | KCF | **The correction that landed on the README and `docs/multi_region.md` this window did not reach the screen five judges stand in front of, and the screen prints the two counts it corrects, as a labelled row, for the region it opens on.** Filed by critic #57, 2026-09-10T0825Z, measured at `16e6824` and not read from a report. `web/finals.html`'s `renderPanel()` loops `DATA.buckets` and writes one row per bucket as mark + Korean label + `r.counts[b.key]`, so the region panel prints **◆ 예산 초과 2** for `uiseong_andong_2025` and **◆ 예산 초과 3** for `uljin_samcheok_2022`; `DATA.default_region` is `uiseong_andong_2025`, so the **2** is on the first panel a judge sees, and the bucket is also a map legend entry (blue diamond, `#2563eb`). Those are exactly the two counts `5bcfe11` qualified six hours earlier: `docs/multi_region.md` §3.1 and `README.md:145-149` now say the two arms are scored under different time rules, that `classify()` in `scripts/run_real_roads_real_hazard_slope.py` passes `time_budget_min` to the future-aware router and **no budget at all** to `naive_route`, and that under one rule the Uiseong-Andong bucket is **empty**. The screen says none of it: the string 예산 appears on **one** line of `web/finals.html` (`:434`, inside the embedded data payload, as the bucket label), and the 알려진 한계 panel's **ten** `rel(...)` cards (`web/finals.html:2067, 2071, 2076, 2080, 2084, 2088, 2092, 2108, 2112, 2116`) contain no card about the budget rule. **This is the fifth-surface shape critic #53 named, one window later and on the same surface:** `tests/test_budget_rule_asymmetry_is_stated.py` grades `docs/multi_region.md` and `README.md` (`:63-64`) and nothing else, so the gate written to keep the correction honest cannot see the screen. ⚠ **The number is not wrong and must not be changed.** `mr_uiseong_fa_exceeds_budget` is 2, it is registered, NH-031 option A says nothing committed moves, and the registry half is `WFG-122`. What is missing is the caveat, which is why this is a 제출 자료 defect and not a 구현 defect (critic #53's own distinction). **Done when:** the 알려진 한계 panel carries a card stating that the two arms are scored under different time rules and that under one rule the Uiseong-Andong bucket is empty, anchored on `docs/multi_region.md` §3.1 and `docs/present_perimeter_arm.md` §2; the card is added to `scripts/finals.template.html` and not only to the built `web/finals.html` (WFG-109's lesson); the screen is rebuilt and `release/kcf-finals-2026/MANIFEST.json` re-pointed; and `tests/test_budget_rule_asymmetry_is_stated.py` gains the screen as a third graded surface, mutation-graded by deleting the card. **Constraints:** no committed value moves; no margin value reaches the screen while NH-032, NH-034 and NH-052 are open; the word 상한 is not settled here (NH-053). ⚠ **Filed in the table like any other P0 row and NOT at position 1**, because NH-051 is open and 「position 1」 is NH-038 option **D**'s mechanic while this routine's prompt cites option **B**, whose own words put the row 「in the table like any other」. `docs/auto/DIRECTION.md` names it as the next row instead, which is §14's own mechanism and is reversible by one line. | done(e043c1f) — the caveat landed in **two** places, not the one the done-when asks for. The `rel(...)` card is in the 알려진 한계 panel as specified, but that panel is a different rendered VIEW from the 지역 패널 where `renderPanel()` prints the count, so a card there alone would have satisfied this row and left the defect standing (`WC-004`'s shape with a view boundary in place of eight sections). `renderPanel()` therefore also prints a pointer between the bucket loop and the panel's hairline rule, guarded on a non-zero count. Both went into `scripts/finals.template.html` and reached `web/finals.html` through `make finals`; `release/kcf-finals-2026/MANIFEST.json` re-pointed. ⚠ The card writes **no number**: `mr_uiseong_fa_exceeds_budget` stays 2 and WFG-122 is still the registry half. ⚠ The card names both regions by reading `DATA.regions[<key>].label` rather than by typing them, which is what `scripts/check_region_literals.py` asks for. `tests/test_budget_rule_asymmetry_is_stated.py` 20 → 51 assertions over four surfaces, adjacency graded separately from the card's content because they fail independently; mutations M5-M10 in its docstring **as they came out**, including **M10 (a typed region label) which scores ZERO here** and is caught instead by `check_region_literals.py` (exit 1), whose SCOPE covers the template and not the built screen. `web/finals.html` is not one of the seven hashed printables sources, so no kit rebuild was needed | true | hours | B/A 제출 자료 · B 설계와 방법론 · R1 |
| WFG-226 | **P0** | KCF | **The Q&A card that answers the 예산 초과 question tells the student to say the repository contradicts itself, and it stopped doing that six hours ago — and the stale card is on the paper in the booth kit.** Filed by critic #57, 2026-09-10T0825Z. `docs/auto/JUDGE_QA.md:1421`, card **Q38 · T1**, opens 「**아닙니다. 그리고 오늘 저장소는 이 질문에 두 가지로 답합니다 — 그게 결함입니다.**」 and then attributes to `docs/multi_region.md:191` the reading 「fire-blind route safe, future-aware router cannot finish in time」, i.e. 「예보 쪽이 진 경우」, with no qualification. Both halves are now false at `16e6824`: `5bcfe11` rewrote `docs/multi_region.md` §3.1, and `:191` now reads 「asserted in `tests/test_partition_categories.py`」 with the ⚠⚠ asymmetry block starting at `:195`, so the repository gives **one** answer and the card's own framing 「그게 결함입니다」 describes a defect that is closed. **The card's substantive answer is still right** — the two arms are scored under different rules, the bucket is empty under one rule, 624.8 and 628.2 minutes, do not extend the explanation to Uljin-Samcheok's 3 — so this is a re-framing, not a rewrite, and it should now **credit** the fix rather than report the contradiction. ⚠ **It is on paper.** `docs/auto/JUDGE_QA.md` is one of the **seven** hashed `SOURCES` of `docs/auto/finals/printables/manifest_20260910T0140Z.json`, and this lap re-hashed all seven against the tree, **seven of seven matching**, which is how the staleness is provable: the bank has not moved since the 0140Z print, and the print therefore carries the same stale card. **Done when:** Q38's lead states the repository's single current answer and points at `docs/multi_region.md` §3.1 by section rather than by line number (the line moved once already and this row is the cost of that); the ❌ lines are kept; the kit is rebuilt with `make printables` at a new stamp and `release/kcf-finals-2026/MANIFEST.json` re-pointed, because a bank edit without the rebuild takes `tests/test_printables.py::test_the_newest_printable_is_not_stale_against_the_tree` red (NH-049). **Constraints:** do not weaken the ❌ 「그 2가구에서는 저희 예보가 졌습니다」; do not attach the same explanation to Uljin-Samcheok's **3**; no margin value. ⚠ Same NH-051 filing note as WFG-225: in the table, not at position 1. | done(ba76b8c) — Q38's lead now states the repository's single current answer, credits `5bcfe11`, and cites `docs/multi_region.md` **§3.1 by section**; the stale lead is kept as a dated ⚠⚠ record the way Q36 and Q40 keep theirs. Both ❌ lines untouched, no explanation extended to Uljin-Samcheok's 3, no margin value. Three gates (`test_the_budget_card_does_not_recite_a_contradiction_that_is_closed`, `..._keeps_its_own_correction_as_a_dated_record`, `..._cites_multi_region_by_section_not_by_line`), each red on its own mutation. Kit rebuilt at `20260910T1233Z` and `release/kcf-finals-2026/MANIFEST.json` re-pointed, once for this row and WFG-229 together (NH-049) | true | minutes+rebuild | B/A 제출 자료 · R5 · R7 |
| WFG-228 | **P0** | science | **The headline statistic of the document four judge-facing surfaces send a judge to has no null model, and this repository already ships the machinery for one.** Filed by critic #58, 2026-09-10T1120Z, measured at `d3ca754` in one process. `docs/oracle_gap.md` §4 concludes 「the model gets the size nearly exactly right and the **place substantially wrong**」 from **IoU 0.394** (534 cells in both, 418 predicted-not-burned, 403 burned-not-predicted), and that reading now stands on `README.md`'s TL;DR, the finals screen's first 알려진 한계 card (WFG-220), `docs/auto/JUDGE_QA.md` Q36 at tier **T0** and `paper/manuscript.md`. **Nothing in the repository says what 0.394 should be compared with.** `src/wildfireguardian/validation/baselines.py` implements `run_persistence_baseline` (`:44`) and `run_isotropic_baseline` (`:68`) and neither has ever been scored on this metric; `docs/MODEL_CARD.md` §「Footprint IoU — honest figure」 gives ~0.07 for the new-ring-only (≈persistence) metric and **nothing** for the forward-sim envelope on any canvas. So 「자리는 절반쯤 맞힙니다」 is offered to five judges with no way to tell whether it is a criticism of the model or a compliment to it, and 「0.394는 무엇에 견준 값입니까?」 is a question the ML-reviewer and statistician lenses both ask in the first minute. **Do: an AREA-MATCHED DISC null, whose rule is written down BEFORE the run (the WFG-201 discipline) and has ZERO free parameters.** For each forward-sim slice, take the disc centred on the centroid of the `t = 0` seed (both stacks share it: 249 cells, `obs_stack[0]` and the `p >= 0.5` core of `haz_stack[0]` are identical) whose cell count equals **that slice's predicted core count** (692 / 952 / 981 / 1036), and score it against the **same** observed footprint under the **same** time-matching rule `scripts/measure_oracle_gap.py` already uses. The disc holds constant the one thing the model got right (area) and destroys the one thing routing depends on (direction), so the difference between the two IoUs is the model's directional skill and nothing else. No refit, no re-acquisition, no fill rule, no threshold sweep, no new free parameter, and it reads the one committed npz. **Done when:** a new script under `scripts/`, a new artifact under `data/processed/`, the per-slice disc IoU registered additively under its own prefix with a caveat that says what it is not, and one short subsection in `docs/oracle_gap.md` §4 giving both numbers side by side. ⚠⚠ **PRE-REGISTER THE INTERPRETATION IN THE ROW'S OWN COMMIT MESSAGE, BEFORE LOOKING AT THE ANSWER, AND PUBLISH IT WHICHEVER WAY IT COMES OUT.** If the disc scores at or above 0.394, that is the finding and it goes on the page in those words; a lap that measures this and then decides the comparison was unfair is doing the thing WFG-201 was filed about. ⚠ **Moves no committed number.** 42, 91, 9, 27 and the four registered per-slice IoUs are untouched; this adds keys and does not edit one (CHARTER §3 rule 2). ⚠ **Do not use `run_isotropic_baseline` itself** unless a head-fire rate can be sourced without a Rothermel run: it takes `head_fire_rate_m_min` as an argument, and a rate chosen after seeing the margin is a free parameter. The area match removes the need for one. | done(b8fd6a8) — the disc null is built, registered (84 keys, `dn_yeongdeok_`) and documented (`docs/disc_null.md`, `docs/oracle_gap.md` §4c). Model **0.3941** vs disc **0.1554**, ratio **2.536**, gap stable across all four slices while the time gap under them swings 258 min. ⚠ **The row's own interpretation is refused in writing and the artifact refutes it:** the gap is joint placement-and-shape skill, not 「directional skill and nothing else」 — by centre of mass the model OVERSHOOTS (observed 2.25 cells from the seed, model 7.292) and the disc's centre-of-mass error (2.266) is SMALLER than the model's (5.34). Both halves ride in the caveat band on every key. Pre-registration held: rule and interpretation fixed at `4ab2e07` before the run. 15 tests, two shown red on their own mutation. Also corrected the row's 「never scored on this metric」 about the persistence/isotropic baselines — `harness.py:703-704` does score them, with a POLYGON IoU against an APPROXIMATE reconstructed perimeter, not this raster truth; that is **WFG-234**. **WFG-235** carries both halves to Q36.  ⚠⚠ **The independent reviewer BLOCKED and was right twice.** (a) The lap certified the CLAIM commit, not the work commit, and `make verify` was red at the real head: the page shipped the LARGEST disc radius (18.162) while only the HEADLINE slice's 17.355 was registered — every slice's radius is now a key, with a `collision-ok` pragma and a test binding the line to `max()`. (b) **The raw 2.536 was inflated by an initial condition.** `obs_stack` is cumulative, so the 249-cell t=0 seed is a SUBSET of the observation being scored; the model contains all 249 by construction, the disc recovers 92. Seed removed from all three masks: model **0.2577**, disc **0.1169**, ratio **2.2044** — the finding survives in sign at all four slices and loses about a fifth. Both figures ship on both pages, in the band on all 84 keys, and in the Korean draft. 18 tests. ⚠ `5b78ffd` and `163df7f`, quoted by an earlier draft of this row and of the report, no longer resolve: `HEAD` moved back to the pushed claim commit so `web/finals.html` could be stamped at a commit already on `origin`, and the paper routine's `eff1bc0` then rebased the result. The work is **`b8fd6a8`**, the board `3a38395`, the report correction `2231f75`. Moves no committed number | true | one lap | B 데이터 수집·분석·해석 · B 설계와 방법론 |
| WFG-229 | **P0** | KCF | **The schedule document now publishes the number that answers 「이거 학생이 만든 게 맞습니까?」, and the card the student drills from carries no number at all.** Filed by critic #58, 2026-09-10T1120Z. `docs/auto/finals/TIMELINE_ROLES.md:81` states 「`Co-Authored-By: Claude` 트레일러 **513개** (전체 662개 중 · 2026-09-09 `89da7d3` 기준)」, and both halves are registered (`timeline_agent_trailer_commits` 513, `timeline_total_commits` 662, plus the five per-phase splits, of which 5기 is **416 of 417**). That is **77.5 %** of the tree, and the document that says it is this repository's answer to 「일정 및 팀원(개인의 경우 제외) 역할 배분의 타당성」, a named sub-item of a **20-point row on BOTH rubric tables** (`docs/auto/RUBRIC.md:31`, `:44`). The document is right to publish it; CHARTER §9 is why. **The problem is the drill instrument.** `docs/auto/JUDGE_QA.md` Q29 · **T0** (「무엇을 직접 만들었습니까?」) answers the question in words, well, and names **no number**: `grep -c 513 docs/auto/JUDGE_QA.md` answers **0**, and so does 662. A T0 card is said **from memory**, and a judge who has the schedule document open has the arithmetic the student has not drilled. That is the WC-004 shape with a document boundary in place of eight sections: the honest number is in the repository and the student meets it for the first time in front of a judge. **Done when:** Q29's answer carries the two figures with their as-of commit and their meaning — that the trailer is a mechanical string test, that `TIMELINE_ROLES.md` §3 already states what it does and does not prove, and the CHARTER §9 practices that make the split explainable rather than embarrassing — and the 없는 것 line says the trailer count is not a measure of who decided anything. ⚠ **Say the units.** This card would put **513** and **662** in a bank that already has to keep **42일** apart from **42곳**; write 「커밋 **513개**」, never a bare 513. ⚠ **RIDE WITH WFG-226.** Both edit `docs/auto/JUDGE_QA.md`, which is one of the seven hashed `SOURCES` of the printed kit, so either one alone needs `make printables` at a new stamp plus a re-pointed `release/kcf-finals-2026/MANIFEST.json` or `tests/test_printables.py::test_the_newest_printable_is_not_stale_against_the_tree` goes red (NH-049). Doing them in one lap pays that rebuild once. ⚠ Same NH-051 filing note as WFG-225 and WFG-226: in the table, not at position 1. | done(ba76b8c) — Q29's **spoken** draft now carries 커밋 513개 / 전체 커밋 662개 with the as-of commit `89da7d3`, and says in the recited sentence that the trailer is a mechanical string test and that a commit is not a unit of work; the 없는 것 line says the count is not a measure of who decided anything. Four gates re-derive both figures from `docs/NUMBERS.json` in-process, so re-running `scripts/build_timeline_roles.py` turns the card red until it is updated. ⚠ The caveat gate first scored **zero** on the mutation deleting the phrase from the draft, because the 없는 것 block repeats it — it reads the draft now (WFG-138's shape, one card over). Why a literal is allowed here and not in Q30: `docs/judge_qa_gates.md` §Update 2026-09-10 | true | minutes+rebuild | A/B 설계와 방법론 · A/B 제출 자료 |
| WFG-230 | **P0** | science | **The oracle document says three of its four slices share one observation; all four share it to within 5 %, and the fire gives the whole comparison one informative frame.** Filed by critic #58, 2026-09-10T1120Z, counted from `data/processed/routing_demo_canonical.npz` rather than from the artifact's summary. `docs/oracle_gap.md:124-127` reads 「**Three of the four slices — 180, 360 and 540 minutes — are scored against the same observation**, the 333-minute one the headline pair uses; only `t = 720` matches a later one」, and a reader takes the fourth row to be the independent one. It is not. The observed cumulative footprint measures **249 / 937 / 987 / 988 / 1021 / 1023** cells at **0 / 333 / 1005 / 1480 / 1812 / 2403** minutes, so the `t = 720` slice's denominator (**987**) is **5.3 %** larger than the denominator the other three use (**937**), and **91.6 %** of this fire's final FIRMS-detected footprint is already present at the **first** post-ignition observation. Two consequences the document should state in its own §4, because both make its existing caveat stronger rather than weaker: (a) the area-ratio series 0.74 / 1.02 / 1.05 / 1.05 runs over a denominator that is effectively constant across **all four** terms, not three, so the series is a reading of the simulated area growing against a fixed footprint and is not a reading of the model at any of the four times; (b) FIRMS gives this fire essentially **one** informative frame, which is the instrument's limit and is the honest reason the well-matched 27-minute pair is the only quotable one. **Do:** one short paragraph in `docs/oracle_gap.md` §4 stating (a) and (b) with the cell counts, and register the six observed cumulative cell counts additively (they are read from a committed array and re-derive from it, so `scripts/register_oracle_gap.py` is the place). ⚠ **Nothing here contradicts the document** and no `WC-###` is opened: `WC-014` already withdrew the reading of that series as forecast behaviour, and this sharpens the replacement sentence. ⚠ **Do not touch the headline.** 0.394 and the 27-minute pair are unaffected; the place disagreement is still the stable finding, and this row is the evidence for why it is the ONLY one. ⚠ Do not write 「the fire stopped growing at 333 minutes」 — the repository has **no** observation between 0 and 333 minutes and cannot say when the growth happened, which is itself the point. | todo | true | minutes | B 데이터 수집·분석·해석 · B 제출 자료 |
| WFG-233 | **P0** | KCF | **The one sentence in this repository written to be pasted into the SUBMITTED 작품설명서 explains the weak fold with a number the rest of the same file does not use, and cites nothing for it.** Filed by critic #59, 2026-09-10T1426Z, measured at `3867860`. `docs/MODEL_CARD.md` §「작품설명서 (Korean writeup) correction mapping」 ends with a suggested replacement sentence, and that sentence is not a note about the repository: it is the text the author is told to put into a document the judges receive. It reads 「평균 ROC-AUC는 **0.89**입니다(6개 산불, 범위 0.68–0.97; 0.68은 **탐지 약 17건**의 소규모 산불 폴드입니다)」 at `:524-525`. Every other place the same file explains the same fold uses a different quantity: the headline blockquote at `:52-53` says 「the 0.68 fold, `gangneung_2023`, has only **~8 positives**」, the per-fire table at `:86` says 「**~8 positives**, fold far too small for a stable estimate」, and `docs/fold_sizes.md:10` gives **396 rows, 8 positive cells, 2 overpasses, 1 transition, 0.26 % of the evidence**. **Both numbers are true and they measure different things**, which is exactly why the sentence is a defect: 17 is the FIRMS detection count for the whole fire and 8 is the count of positive 500 m cells in the fold, and the pasted sentence says which one it means only through the single word 「탐지」. `README.md:489` is the ONE surface that writes both and writes them correctly (「양성 약 8건(탐지 약 17건)뿐인 소규모·잡음 폴드」), so the repair already exists in the repository and has simply not reached the sentence that leaves it. **On sourcing, which is the sharper half.** 「17」 is registered under NO key in `docs/NUMBERS.json` (453 keys at this head; the only registered 17 is `juso_yeongdeok_samul_coolingcen_point_count`), so `make verify` never re-derives it, and neither `README.md:489` nor `docs/MODEL_CARD.md:524` names a source for it. It does trace: `data/processed/detection/firms_first_detection.json :: gangneung_2023.n` is exactly **17** and reaches `data/processed/detection/gk2a_detection_floor.json :: per_fire.gangneung_2023.firms.n` through `scripts/gk2a_detection.py:366`. ⚠⚠ **And there is a second 17, in the directory that must not be cited**: `data/processed/spread_v2/audit.json` gives `gangneung_2023.detections_csv` **17** and `detections_in_grid` **17** over dates 2023-04-11 to 04-13, and that whole directory is Build A, carrying `LEGACY_DO_NOT_CITE.md` and the instruction 「If you are writing a document: do not open these files」. The two 17s are scoped differently (whole fire versus a bbox and date window) and agree by coincidence, so a lap that sources this number by grepping will land in the legacy file first. **This is 「사용된 자료에 대한 출처 명기」, a named criterion of a 20-point row on BOTH tables, on the only text in the repository addressed to a submitted document.** **Done when:** `docs/MODEL_CARD.md:524-525` carries both figures with their units and their meanings the way `README.md:489` already does (양성 셀 8개 · 탐지 17건), names the citable artifact beside them, and drops 「약」 for a value the artifact gives exactly; the mapping table row above it is left alone; and the count is registered additively under its own key with a caveat saying it is a whole-fire FIRMS detection count and NOT the fold's positive-cell count, so the next lap cannot swap one for the other. **Constraints:** ⚠ moves no committed number (0.89, 0.905, 0.682, 0.107 and the fold table are untouched; this adds a key and edits no value, CHARTER §3 rule 2); ⚠ do NOT cite `data/processed/spread_v2/audit.json` for it; ⚠ do not touch the 작품설명서 itself, which is outside this repository (CHARTER §3.12), and do not tell the author what their submitted document currently says. ⚠ NOT a `fix-before-next-row`: `docs/MODEL_CARD.md` is on none of CHARTER §14b's listed surfaces, so it is a row even though the prose half is minutes. ⚠ Same NH-051 filing note as WFG-228 and WFG-230: in the table, not at position 1. **DONE 2026-09-10T2121Z lap.** Claim `99dee9b`, work `e8b6e01` (stamped `2ecc6d2`, which the rebase onto the paper routine's `b729110` rewrote), report `196ea42`, rebuilt board `16d7933`, report head-correction `ae12608`. `docs/MODEL_CARD.md:522-560` carries 「양성 셀이 8개」 and 「FIRMS 탐지는 17건이지만, 이는 화재 전체를 센 값이고 폴드의 양성 셀 수가 아닙니다」 with a `src:` line and a registry key for each; 「약」 dropped for both, which the artifacts give exactly; the mapping table above it untouched. Registered additively as `foldev_gangneung2023_fold_positive_cells` (8) and `foldev_gangneung2023_firms_whole_fire_detections` (17) by `scripts/register_fold_evidence.py`; registry 537 → 539; `docs/arm_protocol.json` declares the `fold_evidence` arm. ⚠ **The independent reviewer BLOCKED and the block is the durable part of this row:** the lap described the `cross_check` on the 17 as a second derivation 「reaching the same value through `scripts/gk2a_detection.py`」, in the registry caveat on both keys, in the arm's `reason_no_control`, in two test docstrings and in its summary. `scripts/gk2a_detection.py:366` is `rec["firms"] = firms.get(label)`, a verbatim copy, and the two records are byte-identical for this fire down to `delay_h`. It is a **transcription check**, it catches a hand-edit and nothing more, and **no independent second derivation of either count exists in this repository** — now stated in all five places. The row's own wording (「reaches … through」) was accurate; the upgrade to 「derived by」 was the lap's. Also from the block: `folds.0` is pinned to `gangneung_2023` (the registrar resolves by `fire_id`, the pasted citation is positional, and they can drift with every gate green), and the Korean particle was repaired. `tests/test_fold_evidence_registry.py`, 12 tests. `gates.py --mode full` exits 0 (2052 passed). Moves no committed number. | done(e8b6e01) | true | minutes + one registration | A/B 제출 자료 · B 데이터 수집·분석·해석 |
| WFG-235 | **P0** | KCF | **A judge asking what 0.394 should be compared with now has an answer in the repository and none on the card they will hear it from.** `docs/auto/JUDGE_QA.md` Q36 is a **T0** card — said from memory — and it carries `docs/oracle_gap.md` §4's 「자리는 절반쯤 맞힙니다」 with nothing after it. Since WFG-228 (`docs/disc_null.md`, `dn_yeongdeok_` prefix) the repository can say that an area-matched disc scores **0.1554** where the model scores **0.3941**, a ratio of **2.536**, ⚠⚠ **and this row must NOT stop there: corrected by critic #60, 2026-09-10T1657Z at `71e95ee`.** The raw pair is the one the same day's own pages forbid quoting alone. `docs/disc_null.md:119-120` says in bold 「**2.5360 is not quotable without 2.2044 beside it**」 and `docs/oracle_gap.md:194` says 「⚠ **Quote the second row, not the first.**」, because `obs_stack` is cumulative and the 249-cell `t = 0` seed sits inside the observation being scored: the model's core contains all 249 by construction while the disc recovers 92. **The seed-removed pair is model `0.2577`, disc `0.1169`, ratio `2.2044`** (`dn_yeongdeok_bare_model_iou`, `dn_yeongdeok_bare_disc_iou`, `dn_yeongdeok_bare_iou_ratio`), and it is the pair a T0 card said from memory to five judges must carry. Writing `2.536` onto Q36 without `2.2044` beside it would put on the highest-stakes surface in this project the exact defect WFG-233 was filed for on `docs/MODEL_CARD.md`, one window earlier. AND that by centre of mass the model overshoots — observed **2.25** cells from the seed, model **7.292**, model-to-observed error **5.34** against the disc's **2.266**. Both halves or neither: the first alone tells a judge the model has directional skill it does not have. ⚠ The card is not WRONG today, so this adds material rather than fixing a defect. **Done when:** Q36 (and Q36's 근거 block) carries both numbers with the `dn_yeongdeok_` keys behind them and the overshoot sentence in the same block, never separated; the draft Korean in `docs/disc_null.md` §6 is the starting text and is labelled a draft where it lands (CHARTER §9). ⚠⚠ **Pays a `make printables` rebuild at a new stamp and a re-pointed `release/kcf-finals-2026/MANIFEST.json` (NH-049), because JUDGE_QA.md is one of the seven hashed sources** — so it should ride with another JUDGE_QA row rather than pay that alone. Moves no committed number. | done(20260911T0025Z-lap) — Q36 carries BOTH pairs with their `dn_yeongdeok_` keys (model **0.3941** / disc **0.1554** / ratio **2.536**, seed-removed model **0.2577** / disc **0.1169** / ratio **2.2044**) plus all four centroid displacements in the SAME block, with the explicit ruling that the gap is shape and extent and NOT direction, the disc named a floor and not a competitor, the 27-minute figure labelled a time gap and NOT a margin (the card's standing 「어떤 여유값도 말하지 마십시오」 untouched), and 「moved no committed number」. A shortened spoken form is in the card's 부스에서 할 말 quote; the bank's own first line carries the 「Status: DRAFT」 label CHARTER §9 asks for. ⚠ **THE ROW'S PREMISE IS CORRECTED IN PLACE:** it says Q36 「carries `docs/oracle_gap.md` §4's 「자리는 절반쯤 맞힙니다」 with nothing after it」 — that phrase is in NO part of `docs/auto/JUDGE_QA.md` (grepped at `a823c33`, zero hits in the bank; it lives at `docs/oracle_gap.md:226` as 「자리는 절반 정도만 맞힙니다」). What Q36 actually did was send a judge to that document with no null baseline to read 0.394 against — the same defect one level of indirection out, so the deliverable was right and the quoted premise was not. ⚠ Two figures of `docs/disc_null.md` §6's draft deliberately NOT carried across: the 249-cell seed count (not a registered key) and the metre forms 3,646 m / 1,125 m, which are **WFG-237**. Rebuild paid once with WFG-240, WFG-241 and WFG-243 | true | one lap | A 설명·표현 · B 데이터 수집·분석·해석 |
| WFG-236 | **P0** | KCF | **The two new sections that answer 「what should IoU 0.394 be compared with」 both state that the repository offered no comparison, and the front door has been offering one for months.** Filed by critic #60, 2026-09-10T1657Z, measured at `71e95ee`. `docs/disc_null.md:20-21` says in bold 「**Until this document, nothing in the repository said what 0.394 should be compared with.**」 and `docs/oracle_gap.md:180-181` says 「until 2026-09-10, nothing in this repository said what 0.394 should be measured against」. **Both are false against `README.md`.** `README.md:520-522` (Korean) reads 「순방향 모의 화선 **footprint IoU ≈ 0.40** (영덕, 3–12시간), 물리(Rothermel) 표면 모델 **~0.09** 대비 약 **4배**로, 표면물리가 놓치는 수관화·비화(crown/spotting) 영역을 포착합니다」, and `README.md:888-891` says the same in English (「roughly **4×** the Rothermel surface model's **~0.09**」). That is the **same measurement family** the disc null scores (forward-simulated footprint IoU, 영덕, 3-12 h, which is exactly the 180 / 360 / 540 / 720 min slices of `docs/disc_null.md` §3), it is on the **front door**, and its source `docs/ROUTING_INTEGRATION_REPORT.md:183` (「far above the mechanistic CA baseline (~0.09)」) predates this window by months. **Why this is more than a wording defect, and why it is the ML-reviewer lens's first question.** The two comparisons point opposite ways and neither page names the other. The README frames the number as a **strength**; `docs/disc_null.md` §3c and §4 conclude that the honest ratio against a zero-parameter null is **2.2044**, not 2.5360, and that **by centre of mass the disc is closer to the truth than the model is**. And the README's comparator is a model the README's own TL;DR calls broken: 「an earlier Rothermel-based *physics* model captured only **~9 %** of the burned area (a documented moisture-conflation bug), which **motivated the pivot**」. A judge holding both pages is holding 「4× a model you told me was buggy」 and 「2.2× a circle」, with nothing reconciling them. **Done when:** (a) both false negatives are replaced by the true narrow statement, which is that no null had been scored **on this metric against this truth**, and the README's Rothermel comparison is named and linked where they stood; (b) `docs/disc_null.md` §5 lists the Rothermel CA baseline beside `run_persistence_baseline` and `run_isotropic_baseline` among the opponents not scored against `obs_stack`; (c) `docs/oracle_gap.md` §4c carries the same pointer. ⚠ **This row changes no number and edits nothing on `README.md`.** Whether the README's 「약 4배」 framing should itself move is **NH-055**, and it is the author's. ⚠ Do not delete the ~0.09 comparison and do not soften the README (DIRECTION.md; NH-054). Moves no committed number. | todo | true | one lap | B 제출 자료 · B 데이터 수집·분석·해석 |
| WFG-237 | **P0** | science | **The caveat the front door already carries about 42 acquired a magnitude today, it is 3,646 m against 1,125 m, and it lives in one file that nothing points at.** Filed by critic #60, 2026-09-10T1657Z, measured at `71e95ee`. `README.md`'s TL;DR already tells a judge that 42 is 「what this policy buys **when its own prediction is believed**」 and sends them to `docs/oracle_gap.md`. Until `b8fd6a8` that caveat had **no size**. `docs/disc_null.md` §4 now gives it one, at the headline slice: the **observed** footprint's centre of mass travels **2.250** cells (**1,124.8 m**) from the `t = 0` seed while the **model's** core centre of mass travels **7.292** cells (**3,646.1 m**), and the model-to-observed centre-of-mass error is **5.340** cells (**2,670.2 m**) against the stationary disc's **2.266** (**1,133.0 m**). All registered under the `dn_yeongdeok_` prefix. **So the model does not merely put the fire in the wrong place, it sends it about three times too far, and the router plans on that surface.** That is the disaster-response-official lens's first question at the booth (「그 예보를 믿고 대피시키면, 실제로는 그만큼 가지도 않은 불에서 사람을 멀리 보내는 것 아닙니까?」) and the repository can now answer it with a number instead of a hedge. `docs/oracle_gap.md` §4c today carries the IoU pair and **none** of the centroid figures. **Done when:** `docs/oracle_gap.md` §4c gains one short paragraph giving the four centroid figures with their registry keys and one sentence on what they do and do not imply for 42 (one fire, one slice, one canvas; they are about the forecast the router believed, not about the routing decision itself), and the evidence chain the TL;DR points at reaches it. ⚠ **Moves no committed number:** 42, 91, 9 and 27 are untouched and no route is re-run. ⚠ Do not write this onto `README.md` (NH-054) and do not write it onto `docs/auto/JUDGE_QA.md` without the `make printables` rebuild NH-049 names; **WFG-235** is the card half and this is the document half. | todo | true | one lap | B 데이터 수집·분석·해석 · A 개발 목적 |
| WFG-240 | **P0** | KCF | **The printed 관련연구 panel names this project's spatial unit and its own differentiator in the household register that `WC-013` withdrew, in two places, on the paper a judge is handed, and neither spelling can be seen by any gate.** Filed by critic #61, 2026-09-10T2000Z, measured at `be39dea` in this lap's own process. Two lines of `docs/auto/finals/RELATED_WORK_PANEL.md`: `:46` reads 「**WildfireGuardian.** ... 공간 단위는 **가구(집)**이고」 as the third row of the three-way comparison whose other two rows are NIFoS and G-DAPS's 「**읍면동**」 (`:43`), and `:119-120` reads 「**조사한 범위에서 찾지 못한 것**은 그 발상을 농촌 **가구 단위의 도보 대피**에 ...」, which is the sentence that states what this project does that the survey did not find. **The same file already carries the correction, 73 lines above the second one.** `:23` says 「이 프로젝트가 만드는 것은 예측 그 자체가 아니라 **지점 단위의 대피 경로와 구조 출동 순서**」 and `:27-31` carries the ⚠ 2026-09-10 정정 block naming WFG-222 and WC-013. That block scopes itself to 「**이 문단**의 단위 낱말이 한 단계 좁아졌습니다」, so a lap reading it is told one paragraph moved, and the rest of the sheet kept the old register. **It is on the printed kit, verified rather than assumed:** `docs/auto/finals/printables/manifest_20260910T1233Z.json` lists this file among its seven `sources` and all **seven** hash equal to the tree at this head (re-computed here), so `WFG_printables_20260910T1233Z.pdf` carries both lines. **Why no gate sees it, and this is the part worth keeping.** `WC-013`'s Korean pattern is anchored on 「가구 단위」 **plus** one of {걸어서 나갈, 대피 판정, 구조 순서, 출동 순서, 판정과 걸어, 인명}, deliberately, so that Q20a's definition and Q16's quantity do not trip. 「공간 단위는 가구(집)」 does not contain 「가구 단위」 at all, and 「가구 단위의 **도보 대피**」 matches no alternative in the list. **And even a spelling that covered it would still miss this one**: the phrase wraps the source line break between `:119` (「농촌 가구」) and `:120` (「단위의 도보 대피에」), which is the exact one-line-at-a-time limit `WFG-223` was filed for. `docs/creativity_card.md:721-726` predicted this instance in writing (「a fourth, differently-worded copy of the same claim escapes both gates」); this is it, and it is printed. **Done when:** both lines carry the same register and the same bound as `:23` and the 정정 block (`README.md:220-251` is the model wording per WC-013's `say_instead`); the 정정 block stops scoping itself to 「이 문단」 and names the sheet; a spelling covering 「가구 단위의 도보 대피」 is registered in `docs/auto/withdrawn_claims.json` in the **same lap** (CHARTER §3.5c), with its blast radius measured over every tracked `.md` and `.html` before it is added; and the lap runs `make printables` at a new stamp and re-points `release/kcf-finals-2026/MANIFEST.json` (**NH-049**), so R7 and R9 stay true of the corrected text. ⚠ **Moves no committed number.** ⚠ **Do not touch** Q20a's definition of 「가구」 as one OSM walk-graph node, Q16's 「가구 단위 폐쇄 시각」, or Q16a's sentence about what other systems' published material does not show: WC-013 preserves all three by name. ⚠ **Do not weaken the panel's 「최초·처음이라고 말하지 않습니다」 block at `:123-125`**, which is the strongest paragraph on the sheet. ⚠ Filed **in the table**, not at position 1 (**NH-051**). ⚠⚠ **CORRECTED IN PLACE BY CRITIC #62, 2026-09-10T2305Z at `f93af93`: this row names two places in its file and there are FOUR.** Re-swept in this lap's own process: besides `:46` and `:119-120`, the sheet also reads at `:45-46` 「정하는 것은 **어느 집을 먼저, 어느 길로**입니다」, which is the same comparison sentence one clause earlier and is the project's own statement of what it decides, and at `:73-74` 「지형을 곱게 본다고 「**이 집 사람이** 걸어 나갈 수 있는가」가 답해지지는 않습니다」, which is the sheet's differentiator against 산림과학원's 5 m terrain plan. Neither contains 「가구 단위」 either, so neither is gate-visible. **All four lines are this row's**, and the fix must cover them together or the sheet contradicts itself again 27 lines later. The identical wording on `docs/auto/JUDGE_QA.md` Q16a, which the panel's own card says speaks 「같은 말」, is **WFG-243**, and one lap should take WFG-235, WFG-240, WFG-241 and WFG-243 together so the `make printables` rebuild is paid once. | done(20260911T0025Z-lap) — all FOUR lines the corrected row names, plus a FIFTH the row did not: `:18-19`, the 앞면 「한 문장」 itself, bold and the first sentence a judge reads, which held 「그래서 이 집 / 사람이 지금 걸어 나갈 수 있는가」 **split across the two source lines**. Critic #62 swept for 「이 집 사람」, counted six and missed it, because its own grep was line-based — the same blindness as `scripts/check_withdrawn_claims.py`. The 정정 block no longer scopes itself to 「이 문단」: it names the sheet, lists the four lines that moved, and records that scoping a correction to one paragraph is itself the defect. A second block states which two 「가구」·「집」 usages are deliberately KEPT and why (G-DAPS's 읍면동 explanation; WC-008's other-systems sentence), so the next lap does not 「fix」 a protected line. 「최초·처음이라고 말하지 않습니다」 at `:123-125` untouched. Six spellings registered on `WC-013` in the same lap, each blast-radius-measured first; `make printables` at `20260911T0034Z` (57 pp) and `release/kcf-finals-2026/MANIFEST.json` re-pointed (19 files). Moved no committed number | true | one lap (with WFG-235, WFG-241, WFG-243) | A/B 제출 자료 · B 설계와 방법론 |
| WFG-241 | **P0** | KCF | **The one judge question this week's research made unavoidable has no card, and the row that was going to write it is filed at P1/IEEE at the bottom of a 233-row table.** Filed by critic #61, 2026-09-10T2000Z, carved out of **WFG-239** so the KCF half is reachable before the freeze. The question is 「비슷한 걸 만든 사람이 이미 있지 않습니까?」 and the repository now holds its answer: Bokade, Barai, Bhogle, Chapherkar & Kale, 「A Hybrid Neural Physical Framework for Wildfire Propagation Modeling and Dynamic Evacuation Routing」, Zenodo preprint, **2026-09-09**, concept DOI <https://doi.org/10.5281/zenodo.22668357>, full record in `docs/auto/knowledge/ROUTING_FUNDAMENTALS.md` §Update 2026-09-10. **The bank does not carry it and the two cards that would be reached instead both point away.** `docs/auto/JUDGE_QA.md` Q29a is the card for 「이 프로젝트에서 새로 만든 것이 무엇입니까?」, and its 없는 것 item 4 at `:1246-1247` tells the student 「문헌 조사는 저희가 열어 본 자료의 범위이고, **그 범위 밖에서 같은 일을 한 연구가 있는지는 모릅니다**」. That was true when it was written and it is the right posture; what has changed is that something **inside** the range now did it, and the card's own item 3 sends the judge to Q16a and to the 관련연구 패널, neither of which names it either. So a judge who names the preprint meets a student saying 「모릅니다」 about a paper the repository read, opened and filed a row on. **Do:** one `docs/auto/JUDGE_QA.md` card, tier **T1** (it is a name and two boundaries, not a memorised number), carrying the sentence WFG-239 already drafted: the delta is **not** routing against a forecast, an independent group made the same choice in the same month, it is the **output object and its measured limits**, and they report **no evaluation of the routing layer at all**; plus the replanning half (they repair a path with D* Lite when the cost surface changes, this project prices an edge by when a walker would traverse it on a time-expanded graph, `docs/auto/knowledge/ROUTING_FUNDAMENTALS.md` §1 and §3). **Boundaries in the card itself, not in a note:** Uttarakhand, India; a road and vehicle routing stack; **no reported routing metric**; an **unrefereed Zenodo preprint** whose attached file is an IEEE conference template, which is not an IEEE publication. It supplies **no** number and nothing from it is registered. ⚠ **Do not imitate its closing claim 「guaranteed safe evacuation」.** ⚠ **Do not compare accuracy in either direction** (DIRECTION.md). ⚠ **Do not write 「최초」 or 「처음」** and keep the panel's 「심사위원이 우리가 모르는 연구를 대면 받아 적으십시오」 instruction as the card's closing line. **Done when:** the card exists with its 근거 block naming the DOI and the knowledge note, Q29a's item 4 gains one clause saying the repository has since found one such study and pointing at the new card, and the lap runs `make printables` at a new stamp with `release/kcf-finals-2026/MANIFEST.json` re-pointed (**NH-049**). **It should ride with WFG-235 and WFG-240 in one rebuild** rather than paying that cost three times. ⚠ Moves no committed number. ⚠ Filed **in the table**, not at position 1 (**NH-051**). | done(20260911T0025Z-lap) — card **Q16d** (T1) 「비슷한 걸 만든 사람이 이미 있지 않습니까?」. It answers YES, names the five authors and the CONCEPT DOI `10.5281/zenodo.22668357`, states the delta as **the output object and its measured limits, never the architecture** (they ship an interactive simulation environment and report no evaluation metric of any kind; this project ships a per-point verdict plus `docs/disc_null.md` and `docs/oracle_gap.md`), and carries the replanning half (D* Lite repairs a path when the cost surface changes; this project prices an edge by WHEN a walker would traverse it on a time-expanded graph). All four boundaries are IN the card: Uttarakhand India, a road-and-vehicle routing stack, no reported routing metric, an unrefereed Zenodo preprint whose attached file is an IEEE conference template. Its 없는 것 block refuses 「저쪽은 평가를 안 했습니다」 in favour of ⭕「**공개된 기록과 초록에는** 경로 성능 수치가 없습니다」 (WC-007 / WC-009 lesson), refuses accuracy comparison either way, refuses 「guaranteed safe evacuation」, and closes on 「받아 적으십시오. 반박하지 마십시오」. Q29a's 없는 것 item 4 gains a dated clause: one such study was found INSIDE the range, pointing at Q16d, with 「모릅니다」 kept true of everything outside that one paper. Header 46 → 47, T1 20 → 21, §6 drill table lists Q16d after Q16c, both bank parsers agree. One dated paragraph appended to `docs/auto/knowledge/ROUTING_FUNDAMENTALS.md` (CHARTER §13) correcting its 「duplicate record」 wording: `...22668358` is the VERSION DOI of the same record. NO number from the note reached the card. Rebuild paid once with the other three | true | one lap | A/B 제출 자료 · B 설계와 방법론 |
| WFG-243 | **P0** | KCF | **The card the student says from memory names this project's own output object, and its own differentiator against the two Korean systems, in the household register `WC-013` withdrew; it is on the printed kit; and it is a tier-T0 card.** Filed by critic #62, 2026-09-10T2305Z, measured at `f93af93` in this lap's own process. `docs/auto/JUDGE_QA.md` **Q16a**, tier **T0** (`:646-647`), answers 「산림청·경기도가 이미 산불확산예측을 하고 있는데, 무엇이 다릅니까?」 and states the delta at `:657-659`: 「저희가 내는 것은 그 예측 자체가 아니라 **가구 하나하나에 대한 「지금 걸어 나갈 수 있는가, 없다면 누구를 먼저 데리러 가는가」** 이고, 공간 단위가 읍면동이 아니라 **집**입니다」. That is `WC-013`'s withdrawn claim exactly: this project's own output object, in the household register, with the output-object phrase attached. `:667` repeats it as 「지형을 곱게 본다고 「**이 집 사람이** 걸어 나갈 수 있는가」가 답해지지는 않습니다」. **The same file already carries the correction 580 lines away**: `:1238` says 「「지점」도 실제 가구 주소가 아닙니다」, and `WC-013`'s `say_instead` is 지점 단위. **It is on the printed kit, verified rather than assumed:** `docs/auto/finals/printables/manifest_20260910T1233Z.json` lists `docs/auto/JUDGE_QA.md` among its seven `sources`, and all **seven** hash equal to the tree at this head, re-computed in this lap's own process, so `WFG_printables_20260910T1233Z.pdf` carries all three lines. **And a T0 card is spoken from memory to five judges**, which is a wider surface than paper: WFG-240 is printed, this is printed **and** said. **This is the same defect as WFG-240 and the two files are the same text by their own statement:** Q16a's 근거 block at `:672-674` names `docs/auto/finals/RELATED_WORK_PANEL.md` as 「부스에서 펴는 인쇄 문안 — **이 카드와 같은 말을 합니다**」. WFG-222 corrected five surfaces and this comparison pair was in none of them. **Why no gate sees any of it.** `WC-013`'s Korean pattern needs 「가구 단위」 plus one of {걸어서 나갈, 대피 판정, 구조 순서, 출동 순서, 판정과 걸어, 인명}. 「가구 하나하나에」 contains no 「가구 단위」; 「걸어 나갈」 is not 「걸어서 나갈」; 「이 집 사람이」 and 「공간 단위가 … 집입니다」 contain neither token. The `:657-658` phrase also wraps the source line break, which is `WFG-223`'s measured limit, so even a spelling that covered the words would miss this one. `docs/creativity_card.md:721-726` predicted a fourth differently-worded copy; this lap counted **six**. **Done when:** (a) `docs/auto/JUDGE_QA.md:657-659` and `:667` carry the 지점 단위 register with the synthetic-hazard and sampled-origin bound in the **same block** (`README.md:220-251` is the model wording per `WC-013`'s `say_instead`); (b) `docs/auto/finals/RELATED_WORK_PANEL.md:45-46` (「정하는 것은 **어느 집을 먼저, 어느 길로**입니다. 공간 단위는 **가구(집)**이고」) and `:73-74` (「이 집 사람이 걸어 나갈 수 있는가」) are corrected with `:119-120`, which is WFG-240's half; (c) `docs/global_portability.md:147` (「출력 … **가구별 대피 가능 여부와 여유 시간**」) and `docs/SESSION14_REPORT.md:178` (「가구별 대피 판정」) are corrected too, both gated `.md` and neither on the kit, so they cost no rebuild; (d) a spelling covering this wording family is registered in `docs/auto/withdrawn_claims.json` in the **same lap** (CHARTER §3.5c) with its blast radius measured over every tracked `.md` and `.html` first, and the registry entry states in its `why` that a line-wrapped phrase still escapes; (e) the lap runs `make printables` at a new stamp and re-points `release/kcf-finals-2026/MANIFEST.json` (**NH-049**). ⚠ **Do not touch** Q20a's definition of 「가구」 as one OSM walk-graph node (`:968-975`), Q16's 「가구 단위 폐쇄 시각」 (`:600`), Q16a's own `:659-661` sentence about what other systems' **published material** does not show, or `:680-681`'s ⭕/❌ pair: `WC-013` preserves all four by name and this row must not widen into them. ⚠ **Do not weaken Q16a's 「저희가 그 둘보다 정확하다고 말하지 않는다」** or the panel's 「최초·처음이라고 말하지 않습니다」 block. ⚠ Moves no committed number. ⚠⚠ **Take this with WFG-235, WFG-240 and WFG-241 in ONE lap**: all four edit a hashed printables source and each alone pays the same rebuild plus a re-pointed bundle manifest. ⚠ Filed **in the table**, not at position 1 (**NH-051**), and not as a `fix-before-next-row` item because the rebuild is a price tag rather than minutes (CHARTER §14b). | done(20260911T0025Z-lap) — (a)-(e) all paid, and the corrected count is **EIGHT lines of two files**, not the five the row and WFG-240 named between them. The three neither row named: `docs/auto/finals/RELATED_WORK_PANEL.md:18-19` (the 앞면 「한 문장」, line-wrapped, WFG-240's residue note has it) and `docs/auto/JUDGE_QA.md` Q29a at **:1189** and **:1192** (「어느 집을 먼저 …」) — a **T0** card, on the printed kit, in the SAME card WFG-222 corrected on 2026-09-10 and **two lines below that card's own 「지점 단위 구조 순서」**, so one sentence named the project's unit both ways. (c)'s two off-kit copies done, and `docs/global_portability.md`'s comparison table had THREE 가구 cells and not the one the row quoted; a dated 정정 note says the values and resolutions did not move, only the word. (d) SIX spellings registered on `WC-013`, each blast-radius-measured over every tracked `.md`/`.html` BEFORE registering: five came back zero outside the record class, `어느 집을 먼저` had one gated hit at `docs/withdrawn_claims.md:260` — **the sentence that had predicted in writing that this wording would escape** — now licensed by a line pragma with a dated note; and `가구별\s*(?:대피|진입)` was **narrowed to `가구별\s*대피` after measurement** because it fired on `docs/submission_reconciliation.md:35`, Q16's legitimate `ingress_survival_time_min` quantity and itself on the printed kit. Each `why` records that a line-wrapped phrase still escapes. ⚠⚠ **AND THE GATE FOR THAT CLASS EXISTS NOW**: `tests/test_withdrawn_claims_registry.py::test_the_household_register_is_absent_even_where_a_line_break_hides_it` re-scans every gated file flattened and reports only what the line-based checker cannot see, shown RED on its own mutation (checker printed 「PASSED — 14 claims over 940 gated files」 while the new test named the file and token) before being trusted. The critic's second-cheapest test was run BEFORE the rebuild as asked: wrap-aware re-grep of all seven kit sources, **0** line hits and **0** wrap-only hits. Nothing protected was touched (Q20a's `:968-975`, Q16's `:600`, Q16a's `:659-661`, `:680-681`'s ⭕/❌). Moved no committed number | true | one lap (with WFG-235, WFG-240, WFG-241) | A/B 제출 자료 · B 설계와 방법론 |
| WFG-244 | **P0** | KCF | **The block added to the submitted-document sentence 90 minutes ago asserts a repository-wide negative about its own evidence, and the file it names in the next sentence holds both counts.** Filed by critic #62, 2026-09-10T2305Z, measured at `f93af93`. `docs/MODEL_CARD.md:548-556`, written by the WFG-233 lap at `e8b6e01`, states 「⚠ **Neither count has a second, independent derivation in this repository.**」 and then, four lines later, 「⚠ The legacy Build A `spread_v2` audit file also reports 17 for this fire, over a bbox and a date window … **its 17 agrees with the whole-fire count by coincidence rather than by derivation**」. **Two defects, and the second is the larger one.** (1) 「by coincidence」 is a provenance mechanism the lap did not measure. Read in this lap's own process, `data/processed/spread_v2/audit.json :: fires[5]` is `fire_id: gangneung_2023`, `bbox_wgs84 [128.75, 37.7, 129.05, 37.95]`, `dates ["2023-04-11", "2023-04-13"]`, and it gives `detections_csv` **17** **and** `detections_in_grid` **17**. Both are FIRMS detection counts for the same fire over the same event window as `data/processed/detection/firms_first_detection.json :: gangneung_2023.n`, so agreement is the expected result of two builds counting the same FIRMS bundle, not a coincidence. The claim travelled from WFG-233's own row text (「The two 17s are scoped differently … and agree by coincidence」), filed by critic #59 and equally unmeasured there, into a block that tells the student 「Say that if a judge asks what corroborates the 17」. (2) **The same record holds `total_positives` 8 and `spread_cells_total` 8**, which is the fold's positive-cell count, the other number the block is about. So the repository-wide negative 「Neither count has a second … derivation in this repository」 is asserted over a file the block is already holding open, and the block never says the 8 is in it. **Why this is not a nitpick.** The block is attached to the one text in this repository written to be pasted into the **submitted** 작품설명서, and its subject is what corroborates a number. WFG-233's own independent reviewer blocked that lap for overstating a corroboration; this is the same class understated, one layer up, in the same commit, and both are unmeasured claims about provenance (CHARTER §3 rule 5). **Done when:** (a) 「by coincidence rather than by derivation」 is replaced by what was measured (the legacy record's own fields, its bbox and date window, and that the two agree because both count FIRMS detections for this fire), or by an explicit 「not checked」; (b) the repository-wide negative is narrowed to what is true and useful, which is that **no citable** second derivation exists, and the legacy record's `total_positives` 8 is named as the thing a grepping lap will find; (c) a test pins the narrowed wording the way `test_the_registry_never_calls_the_transcription_check_a_corroboration` pins the other half. ⚠ **Do NOT cite `data/processed/spread_v2/audit.json` as a source for either number** (WFG-233's own constraint, `LEGACY_DO_NOT_CITE.md`); naming what a file contains in order to warn a lap off it is not citing it, and `docs/MODEL_CARD.md:553-556` already does exactly that for the 17. ⚠ Moves no committed number, adds no registry key, regenerates no artifact. ⚠ `docs/MODEL_CARD.md` is on **none** of CHARTER §14b's listed surfaces and on neither the printed kit's seven sources nor the bundle's nineteen, both re-hashed here, so this pays **no** rebuild and is not a `fix-before-next-row` item. ⚠ Filed **in the table**, not at position 1 (**NH-051**). | todo | true | minutes | A/B 제출 자료 |
| WFG-245 | **P0** | KCF | **The protected other-systems sentence on a T0 card now sits one line below a corrected register and the pair reads as though neither system does anything.** Filed by the dev lap of 2026-09-11T0025Z, which corrected the register and deliberately did NOT widen into this. `docs/auto/JUDGE_QA.md` Q16a now says at `:657-661` that what this project produces is 「지점 단위의 … 판정과 그 도보 경로」 and that its 공간 단위 is 「보행 그래프의 한 지점」 — and the very next sentence, `WC-008`-protected and named by DIRECTION as untouchable, still reads 「저희가 연 자료 — 기관 카탈로그 기록과 언론 보도 — 에서는 특정한 집 단위의 대피 경로 산출물이 확인되지 않습니다」. The same pairing is on the printed panel at `:92-98`. Both sentences are true and each is right on its own; read consecutively by a judge they invite 「so neither of you produces a 집 단위 output — what is the difference?」, which is the one question this card exists to answer. ⚠ **This is NOT a request to edit the protected sentence.** The cheap fix is one clause of connective tissue: say that the comparison is between 읍면동 and something smaller, that what the other systems' published material does not show is any unit below 읍면동 at all, and that this project's something-smaller is a sampled walk-graph point rather than an address. `:680-681`'s ⭕/❌ pair already gives the student the honest form and the card never reaches it in reading order. **Done when:** Q16a and the panel each carry one connecting clause in the same block as the pair, `WC-008`'s and `WC-013`'s wordings both intact and neither weakened, and the lap re-runs the wrap-aware sweep before `make printables`. ⚠ Pays a `make printables` rebuild at a new stamp and a re-pointed `release/kcf-finals-2026/MANIFEST.json` (**NH-049**), so it should ride with the next row that edits `docs/auto/JUDGE_QA.md` (**WFG-237**'s card half, or **WFG-236**) rather than pay that alone. ⚠ Moves no committed number. ⚠ Filed **in the table**, not at position 1 (**NH-051**) | todo | true | one lap | A/B 제출 자료 · B 설계와 방법론 |
| WFG-242 | P1 | science | **The project has just staked its whole contribution on the output object, and the run where both of its axes are real cannot emit one; only the author's laptop can settle that.** Filed by critic #61, 2026-09-10T2000Z, from this lap's `hate` root objection, measured rather than argued. `docs/auto/DIRECTION.md`'s thesis now reads 「its defensible contribution ... is the **output object and its measured limits**」. The committed instances of that object are `outputs/dispatch/20260801T163042Z/**`, and every judge-facing surface says honestly that the run behind them had **화재 위험면과 지형은 합성** and **출발지는 표본 좌표** (`data/processed/rescue_routing.json` -> `provenance`), ending 「**실제 확산면으로 만든 출동 지시서는 아직 없습니다**」. **Why no cloud lap can close that sentence, checked in this lap's own process rather than assumed.** `scripts/generate_dispatch_outputs.py:43` reads a per-origin artifact (`destinations`, `dispatch_top20`, `unreachable_homes`, `four_way_counts`) and takes `--source`, so the obvious move is to point it at the real-hazard run. The real-hazard run's committed artifacts do not carry those fields: `data/processed/real_roads_real_hazard_canonical.json` holds `arms.slope_digraph_canonical` with `n_origins_scanned` **458**, `counts`, `n_shelter_nodes` **46**, `n_nodes` **8443**, `n_edges` **21982** and **no per-origin record at all**. Re-running the routing to produce them needs the OSM walk graph at `data/cache/osm/yeongdeok_2025`, which is git-ignored and absent from every sandbox: `tests/test_rescue_routing_real.py:71-109` skips on exactly that path in the green run at this head. **So this row is two halves and only one of them is a lap's.** The agent-doable half: make the real-roads-real-hazard writer emit per-origin records under a new filename, additively, so a later lap can generate sheets without the cache; that half still needs the cache to run once. The other half is the author's and is **NH-057**. **Done when:** either a dispatch document exists that was made on a real spread surface, with its own stamp under `outputs/dispatch/`, and the 「아직 없습니다」 sentence on every surface is replaced by a path; or the reason it cannot be made is written into `docs/real_roads_real_hazard.md` as a named limitation and quoted on the panel, which is itself the answer to the disaster-response judge's first question. ⚠ **Moves no committed number and regenerates no artifact** (CHARTER §3 rule 2): every output is new files under a new stamp. ⚠ **Do not re-acquire OSM data** (CHARTER §3 rule 11). ⚠ Do not let this become a reason to soften the 「아직 없습니다」 sentence anywhere; until a file exists, that sentence is the truth and it is why the project is trusted. | blocked(NH-057) | no (needs the OSM cache on the author's laptop) | one lap after NH-057 | B 데이터 수집·분석·해석 · A 구현 및 유용성 |
| WFG-238 | P1 | infra | **The definition-of-done page leads with a tick count one critic lap stale, which is the exact shape the lap before it had just named.** Filed by critic #60, 2026-09-10T1657Z, measured at `71e95ee` **and repaired by hand in the same lap**, so this row is for the gate rather than for the text. `docs/auto/KCF_READINESS.md:8-11` read 「**Tick count, critic #58, 2026-09-10T1120Z at `d3ca754`** ... ZERO lines ticked, for the **FIFTEENTH** consecutive critic lap」 while the same file's newest section, 1,833 lines below at `:1841`, read 「2026-09-10T1426Z · critic #59 · ZERO lines ticked, the **SIXTEENTH** consecutive critic lap」. Critic #59 appended its section and left the lead alone. This is the **WFG-107 shape** (hand-written prose beside generated or appended truth), and critic #58's own append at `:12-16` had named that shape one lap earlier and written that the repository 「has now paid for that shape six times」. CHARTER §11 makes this page **the final product's definition of done**, so its lead is the sentence a lap reads when it wants to know whether the product is ready. **Done when:** a test asserts that the tick-count paragraph in the first 20 lines of `docs/auto/KCF_READINESS.md` names the same lap number and stamp as the file's last `^## ` section heading, and that the ordinal word for consecutive zero-tick laps agrees with the count of such sections. ⚠ **Loop hygiene, so CHARTER §14b holds it at P1** until R1, R3, R4, R7, R8 and R9 tick; **R3 is the only one unticked** and it is `blocked(NH-046)`, two days past due. Moves no committed number.  ⚠⚠ **THIRD live instance, critic #65, 2026-09-11T0810Z at `f7ee58d`: the lead was TWO laps stale.** It named 「critic #62 ... NINETEENTH」 while critic #63 (`f48876a`) and critic #64 (`0a66c90`) had each appended a section to `docs/auto/KCF_READINESS.md`, both at the bottom of the file. ⚠ The same two laps also split the file's ordering convention: #58 to #62 are newest-first under the lead, #63 onward are appended newest-last. Critic #65 repaired the lead by hand in the same commit as its own append and did NOT move any section, because moving one invalidates every line citation made against it. The gate this row asks for is what makes the hand repair unnecessary.| todo | true | one lap | (loop hygiene, no rubric row) |
| WFG-227 | P1 | science | **Two of the classifier's six buckets are exactly zero in every region and every variant, and no judge-facing surface says whether the classifier can produce them at all.** Filed by critic #57, 2026-09-10T0825Z, from the judge drill (CHARTER §4, critic step 3): the question 「여섯 개 분류 중 둘이 세 지역·모든 변형에서 정확히 0입니다. 그 둘은 나올 수 있는 분류입니까, 아니면 코드가 낼 수 없는 분류입니까?」 cannot be answered from a file today. Measured at `16e6824` against `data/processed/multi_region_comparison.json`: `both_enter` and `naive_unreachable` are **0** in all three regions and in all three Yeongdeok variants, alongside `unclassified` **0**. `docs/multi_region.md:46-47` states the fact — 「`both_enter`, `naive_unreachable` and `unclassified` are zero in all three regions and are omitted」 — and states no reason. Counted across the judge-facing set, the two names appear **0** times in `README.md`, **0** in `docs/auto/JUDGE_QA.md`, **0** in `docs/auto/DEMO_SCRIPT_5MIN.md` and **0** in `docs/creativity_card.md`; only `docs/multi_region.md` names them (1 and 2). An always-empty partition class is the standard degenerate-partition objection a software-engineering judge or an ML reviewer raises, and the repository's own strongest answer is already half-written: `docs/multi_region.md:227-235` argues the reachability monotonicity for `fa_exceeds_budget` and says origins 「fall to `naive_unreachable` at the first branch」, which is a claim **about** one of the two empty buckets. **Do:** in `docs/multi_region.md` §2, one short paragraph saying, per bucket, whether the membership test is reachable on these inputs and why it is empty here (fire-blind arm carries no budget, so nothing reaches `naive_unreachable`; state the `both_enter` condition and whether any origin can meet it on a hazard field with these thresholds), citing `classify()` in `scripts/run_real_roads_real_hazard_slope.py` by line. If a bucket is unreachable **by construction** on the committed inputs, say that in those words rather than leaving a zero. **Then** the drill question becomes a Q&A card on the next kit rebuild. ⚠ **No new measurement and no new number** — this is a statement about the classifier's branches, read from the code. ⚠ **P1 and not P0:** no judge has asked it, nothing printed is wrong, and `docs/multi_region.md` does state the fact; what is missing is the reason. It is filed rather than carded because NH-049 makes a critic card impossible. | todo | true | minutes | B 데이터 수집·분석·해석 · B 설계와 방법론 |
| WFG-231 | P1 | infra | **The ruling that keeps three correct 「household-level」 lines from being 「fixed」 lives in the one file a lap sweeping for them will not open.** Filed by critic #58, 2026-09-10T1120Z. `README.md:3` (the repository's one-line description, the first sentence a judge or a stranger reads), `CITATION.cff:5` and `src/wildfireguardian/__init__.py:1` all say 「household-level evacuation and rescue routing」, and `paper/manuscript.md:1` is titled 「…for household-level wildfire evacuation and rescue in rural Korea」. **None of them is a WC-013 breach and none should be changed.** The reasoning exists and is good: `paper/GAPS.md:389` draws the distinction — 「routing **for** household-level wildfire evacuation」 names the **application**, not a per-household **result** — and `:375` records that no registered spelling covers 「household-level decisions」, so the withdrawal gate is green on it by design rather than by accident. WC-013's own `spellings` block explains at length why it anchors on 「가구 단위」 **plus** the output-object phrase and deliberately never on the bare term (a bare pattern would fire on Q20a's definition, Q16's 「가구 단위 폐쇄 시각」 and Q16a's other-systems sentence, all three legitimate). **The defect is location, not content.** A lap that greps `household` across the tree lands on four judge-facing hits and finds the ruling only in `paper/GAPS.md`, a file about the manuscript's gaps. The failure mode is not hypothetical for this loop: WFG-222's own withdrawal note records that the identical wording stood for one window on the finals screen because a lap fixed the surfaces a critic happened to list. **Done when:** `docs/withdrawn_claims.md` §5 (or WC-013's `say_instead`, which is where a lap reading the registry lands) carries two sentences saying that 「household-level」 as the name of the **application** is deliberately not registered, naming those four lines, and that a lap wanting to change them opens a row rather than editing. ⚠ **Do not register a spelling for it** — that is what WC-013 argued against in writing, and a pattern loose enough to catch these would fire on the three legitimate 「가구」 uses in the bank. This row adds a note, not a gate. ⚠ **P1 and held behind R3 by CHARTER §14b**, and it is filed knowing what NH-038 measured about the P1 queue: 9 done against 101 todo, released only when R1, R3, R4, R7, R8 and R9 tick, of which R3 needs the author (NH-046, one day past due). Recorded so the next lap that CAN work it does not re-derive the ruling. | todo | true | minutes | 제출 자료 · record |
| WFG-232 | P1 | infra | **A registered `forbidden_phrasings` spelling can be written into any document but two and no gate will see it.** Filed by the 2026-09-10T1222Z dev lap; **its first version of this row said 「no gate reads the field」 and that was false**, refuted by that lap's own independent reviewer in one command, and the corrected row is below. ⚠ Read the correction as part of the row: the mechanism was a recursive `grep -rln` for the field name over `scripts/`, `src/`, `tests/` and the Makefile, **piped into `head`**, which cut the list at ten lines with two test files below the cut — **a truncated listing reads exactly like a complete one**, the same class as WFG-107's generated count beside hand-written prose. **What IS enforced:** the field's *content*, by four tests — `tests/test_present_perimeter_arm.py::test_the_caveat_travels_with_every_key`, `tests/test_full_coverage.py::test_registry_forbids_substituting_the_drift_values`, `tests/test_sparsity_and_page_budget.py::test_registry_forbids_reading_sparsity_as_household_dispersion` and `tests/test_oracle_gap.py`, each for its own prefix. Empty the `pp_uiseong_*` lists and the first goes red (1 failed, 19 passed). A phrasing cannot be silently deleted. **What is NOT enforced, and is the row:** whether any document *writes* one. That is graded for exactly two files — `tests/test_oracle_gap.py::test_the_forbidden_phrasings_are_registered_and_absent_from_the_doc` over `docs/oracle_gap.md`, and `tests/test_judge_qa_bank.py::test_the_bank_writes_none_of_the_timeline_forbidden_phrasings` over the Q&A bank, added by WFG-229. Every other prefix and every other document is unscanned, and `make verify` is not involved in either direction: appending a registered phrasing to a tracked document leaves **all eleven** of its sub-targets at exit 0. `scripts/check_forbidden.py` is the tree-wide prose scanner and keeps its own hand-written HARD/LABEL list, which carries none of the `og_yeongdeok_*` or `timeline_*` spellings. ⚠ **The absence rule is not the general rule.** Binding every key's list as 「absent from every document」 goes red immediately: the record class (reports, MEMO, BACKLOG, CRITIC_LATEST) exists to quote a forbidden spelling in order to record it — the problem `docs/withdrawn_claims.json` already solved with a gated-file list plus a per-line licensing pragma (CHARTER §3.5c). **Done when:** the write-side is bound by a gate that reuses the withdrawn-claims machinery's file scoping and its per-line pragma, graded against a mutation that writes a phrasing into a gated file and one that writes it into a record-class file; the lap states the measured cost (how many tracked `.md` files the scoping leaves gated, and how many pragma lines the existing tree needs); and any prose that appeals to the field as a protection is checked against what the gate actually does. ⚠ Registers nothing and moves no value. ⚠ Do not delete a phrasing from any key: four tests read the lists and they are a record of readings this project refuses ⚠ **CRITIC #59, 2026-09-10T1426Z, the write-side gap sized, because the row asks for a gate without saying how much tree it has to cover.** Counted over `docs/NUMBERS.json` at `3867860` in one process: **289 of 453** keys declare `forbidden_phrasings`, spread over **27** key prefixes (the five largest are `pp` 57 of 57, `mr` 52 of 52, `og` 30 of 30, `timeline` 20 of 20, `ppshape` 18 of 18). The write side is bound for **two documents and two prefixes** (`docs/oracle_gap.md` for `og_yeongdeok_*`, `docs/auto/JUDGE_QA.md` for `timeline_*`), so **25 of the 27 prefixes have no document scanned for their spellings at all**. I also re-ran the reader enumeration WITHOUT a `head`, which is this row's own lesson: `grep -rln forbidden_phrasings --include=*.py .` returns **five** test files and **eight** scripts, and the five tests are the four this row names plus `tests/test_judge_qa_bank.py`, so the corrected row is right. The eight scripts are registrars that WRITE the field, not readers that enforce it (`scripts/measure_demo_script_pace.py:251` declares three spellings for the `demo_*` prefix and `tests/test_demo_script_pace.py` does not read them, which is one concrete instance of the 25). | todo | true | one lap | loop hygiene · B 제출 자료 |
| WFG-107 | P1 | infra | **Three consecutive laps have hand-written the count of open decisions wrong, beside a generated block that had it right.** `report.py` builds the 「Decisions needed」 block from `NEEDS_HUMAN.md` and has been correct every time. The prose next to it has not: `26e200d` fixed 「three」 against a block listing four, `6afd252` fixed 「five」 against a block listing six, and critic #18 wrote 「five」 again in `.auto/summary.md`, `CRITIC_LATEST.md` and its report, omitting **NH-005** for the third time — caught by reading the generated email against the prose before the send, not by a gate. The count is derived data that three different routines keep re-deriving by eye, and NH-005 is the entry that falls out every time because it sits above the block of entries a lap reads when it greps the tail of the file. **Done when:** `report.py` exposes the open-DECISION list it already computes as a substitution token the summary can use (or, cheaper, a gate that fails when a lap's summary states a decision count that disagrees with the block `report.py` generated for the same report). Do not fix it by asking laps to count more carefully; two laps have already tried that and this is the third. **Parked behind R1, R3, R7, R8 and R9 by CHARTER §14b** — it is loop hygiene, it costs the author nothing today because the generated block is authoritative and correct, and the printables and `BOOTH_SETUP.md` come first | todo ⚠⚠ **CRITIC #57, 2026-09-10T0825Z: FIFTH RECORDED INSTANCE, AND THE ERROR HAS GROWN RATHER THAN REPEATED.** Counted at `16e6824` with `grep -oE '^## NH-[0-9]+ · [A-Z]+ · open' docs/auto/NEEDS_HUMAN.md`: **19 DECISION + 1 BLOCKER = 20 open** for the author, plus 5 FYI. The 20 is right and every lap has it right. **The overdue count is not:** the `0703Z` dev report and critic #56 both say 「six of them at or past their stated date」, and the true figure is **13** — NH-032, 034 and 045 due 09-08; NH-035, 038, 043 and 044 due 09-09; NH-036, 037, 042, 046, 048 and 050 due 2026-09-10. That is this row's exact failure mode one level in: a hand-written number beside a generated block, wrong by more than a factor of two, and it is the number that tells the author how late their own queue is. ⚠ This row is P1 infra and CHARTER §14b holds it behind R3, which is `blocked(NH-046)` — so the row cannot be worked, and the measurement is recorded here so the next lap that CAN work it does not have to re-derive it. ⚠⚠ **CRITIC #58, 2026-09-10T1120Z: SIXTH INSTANCE, AND THIS ONE IS MINE.** My own first pass at the overdue count returned **14** and was wrong: it counted NH-049, which is due **2026-09-11**. Parsed properly over `^## NH-\d+ · (DECISION|BLOCKER) · open` with the date read from the same header line, the answer at `d3ca754` is **20 open for the author and 13 at or past their date** (NH-032, 034, 045 due 09-08; NH-035, 038, 043, 044 due 09-09; NH-036, 037, 042, 046, 048, 050 due 09-10). Two open entries, **NH-005 and NH-014, carry no date at all** and fall out of any date-based count silently, which is the specific trap this row exists to close: the next lap should say 「13 of 20 dated entries are  ⚠⚠ **CRITIC #59, 2026-09-10T1426Z: SIXTH RECORDED INSTANCE, AND THIS TIME IT IS THE BACKLOG'S OWN SIZE RATHER THAN THE DECISION COUNT.** Critic #58 published 「P1 is 9 done against **102** `todo`, up one this lap, and mine is the 102nd」 on `docs/auto/DIRECTION.md`, in `CRITIC_LATEST.md` and in its report. Counted here in one process over every `^| WFG-` row of the table, taking the status cell as the fourth field from the end and stripping `**` (the same parse that survives WFG-107's own trap of a `|` inside a title cell): at `f4ef66e`, **the commit critic #58 itself pushed**, P1 `todo` is **107**; at `d3ca754`, the head it reviewed, **106**; at `3867860`, this head, **108**. `done` was right at **9** on both heads, so only the `todo` half is wrong, and by five. ⚠ The number is load-bearing: it is the evidence NH-038 uses to call the P1 queue a write-only ledger, and 「mine is the 102nd」 names a position that row never occupied (WFG-231 was the 107th). Corrected on `DIRECTION.md` by this lap and appended to NH-038. **The remedy this row already proposes covers it exactly**: a count that appears in prose beside a generated block belongs in `report.py`, and a backlog census is the same shape as the decision census. overdue and 2 carry no date」 rather than a bare number. This row is still held behind R3. ⚠ **Two further instances, measured by critic #60 at `71e95ee`, and both are DATES rather than counts, which the proposed remedy does not yet cover.** (a) **NH-046 is due TODAY, 2026-09-10, and is not past due**: its heading at `docs/auto/NEEDS_HUMAN.md:2847` reads 「(by 2026-09-10)」 and the entry was never re-dated, yet critic #58 and critic #59 both published 「NH-046 is now two days past due」 on `DIRECTION.md` and on `KCF_READINESS.md`. It is the entry holding **R3**, the only unticked line of the six CHARTER §14b needs, so the figure is read by every lap. Corrected on both pages by critic #60. (b) **`docs/auto/KCF_READINESS.md`'s lead tick count was one critic lap stale** (「critic #58 … FIFTEENTH」 above a `:1841` section saying 「critic #59 … SIXTEENTH」); repaired by hand and filed as **WFG-238**, which is the gate half. **So the remedy should generate the decision DATES too, not only the counts**: a due date and an ordinal in hand-written prose drift exactly like a census does. ⚠⚠ **SEVENTH INSTANCE, and it is a re-drift rather than a new one, recorded by critic #62 at `f93af93`.** Critic #58 published 「**102** P1 rows wait on one reply」; critic #59 counted it at three heads, got **106 / 107 / 108**, and recorded the correction on this row; **critic #61 wrote 102 again**, on `docs/auto/DIRECTION.md` and on `docs/auto/KCF_READINESS.md`, one lap after the correction. Counted here at `be39dea`, at `f93af93` and in this lap's working tree, all three answer **106**. Also: critic #61 published the board triple 「**236** rows / 94 P0 / 15 `todo`」, whose parts reproduce exactly but at **two different heads** — 233 / 94 / 15 at `be39dea`, the head it reviewed, and 236 only after its own three rows landed in `252c9f9`. **Both are the same shape: a hand-written census beside a generated block, and a corrected number that a later lap re-inherits from the page rather than re-deriving.** The remedy must therefore emit the count, the date, the ordinal **and the head each was measured at**, because a number without its head cannot be checked by the next lap and is what makes a corrected figure re-drift. | true | minutes | — |
| WFG-108 | P1 | KCF | **`make finals-bundle` reports `byte-identically` about a folder it never looks at, so a stale file rides onto the USB stick.** `scripts/build_finals_bundle.py` builds a manifest from `plan()` — the payload list — and compares it against the committed `MANIFEST.json`. It never enumerates `release/kcf-finals-2026/` itself, and `assemble()` copies in without pruning, so any file an earlier run left behind stays in the folder, is invisible to the check, and is copied to the stick with everything else. Found 2026-09-05 (WFG-037) by running the new `scripts/check_bundle_copy.py` immediately after a green builder run: the builder reported the bundle rebuilt byte-identically and the checker answered `EXTRA web/demo-media/intro-forest-loop.mp4`, a file this lap's own earlier experiment had put in `web/demo-media/` and then removed from the tree. **Done when:** the builder removes files under `release/kcf-finals-2026/` that its plan does not name (or refuses and says which), with a test that plants a stray file and asserts the run is not green. Do not widen it into a general `release/` cleaner and do not touch the manifest format. **Mitigated today, not fixed:** `docs/auto/finals/BOOTH_SETUP.md` §2 makes `python3 scripts/check_bundle_copy.py release/kcf-finals-2026` a step before the copy, so the stray file is caught by the recipe rather than by the builder | superseded by WFG-114 (done 2026-09-05: the arm was run by the cloud lap; result in docs/present_perimeter_arm.md) | true | minutes | 구현 및 유용성 · R9 |
| WFG-109 | **P0** | KCF | **WFG-103's fix landed on the built screen and not on the template it is built from, so the next `make finals` puts the withdrawn sentence back in front of five judges.** `web/finals.html` is generated by `scripts/build_finals.py` from `scripts/finals.template.html`; WFG-103 (`92366cb`) corrected the STATIC VIEW caption in the **output** by hand and left `scripts/finals.template.html:1378` and `:1381` carrying the sentence it withdrew — KO 「지도가 지금 이 순간만 본다면, 이 경로를 권했을 것입니다」 and EN 「A map that only sees the present would recommend it」. Both are the claim WFG-103 established is false: the arm is `naive`, which is **fire-blind** (`src/wildfireguardian/routing/evacuation.py:270`, `docs/real_roads_real_hazard.md:50`), not a map that sees the fire now. Found 2026-09-05 by WFG-037's lap reviewer and reproduced by the lap: `make finals` rewrites exactly those two captions and nothing else but the build stamp, and the tree was restored. **Nothing is wrong on the judged screen today** — the committed `web/finals.html` carries the corrected sentences — so this is a reversion waiting for whoever next rebuilds, and `docs/auto/finals/BOOTH_SETUP.md` §3.4 already tells the student not to. **Done when:** the two template lines say what the built screen says, `make finals` is run so the generated file and the template agree, the bundle manifest is re-derived, and a test asserts the template and `web/finals.html` carry the same STATIC VIEW captions so a hand-fix to the output can never again outlive its source. Check the rest of that commit for the same shape before assuming it is only these two lines | done(20260905T1520Z) — the two template lines now say what the built screen says; `make finals` re-run and the ONLY line it changed is the payload (434), so the reversion is gone rather than deferred; `make finals-bundle UPDATE=1` re-derived the manifest (17 files). Checked the rest of `92366cb` for the same shape rather than assuming: it touched `web/finals.html` by exactly 2 lines and they are these two. The gate is `tests/test_finals_template_sync.py` (9), and it is deliberately NOT a comparison of the two STATIC VIEW captions — the builder substitutes ONE placeholder line, so the whole file is an identity and that is what is asserted; mutation-graded 7/7, of which three (an unrelated generated line, a deleted line, the template committed unbuilt) a caption comparison would have passed. Extended to `web/console.html`, the same mechanism on a second judged screen, which measured clean. Documented in `docs/finals_screen_v2.md` §4.2 with what it does not cover (the payload line, a template that is wrong on both faces, the two screens not built this way) | true | one lap | B/A 제출 자료 · B 데이터 수집·분석·해석 · R4 |
| WFG-110 | **P0** | KCF | **Readiness R1 has been ☐ for nineteen critic laps on a condition nobody had ever sized, and it is six table rows wide.** R1 asks that 「every on-screen number maps to a `docs/NUMBERS.json` key (mapping table committed)」. Critic #18 searched and wrote 「no such table exists」; that is too strong and the correction matters, because the table that exists runs the **other way**. `docs/auto/DEMO_SCRIPT_5MIN.md` §3 maps *every number the script says* to a key and marks each 화면 or 구두 — script → key. R1 asks screen → key, which is the direction that answers a judge pointing at the screen and saying 「이 숫자는 어디서 나온 겁니까」. Measured by critic #19 on disk at `92bfc4f`, using the repository's own criterion for 화면 (§3's preamble: the judgement is whether `scripts/finals.template.html` references the key, not whether the built page contains it): the template references **28** registry keys, §3's table names **22** of them, and **6 are in no committed mapping table** — `objective_canonical_longest_walk_saving_min`, `oof_average_precision`, `rescue_dispatch_count`, `responder_exposure_shortest_path_mean`, `responder_exposure_survival_aware_mean`, `slope_canonical_fa_routes_changed_60m`. **Done when:** a committed screen → key table (a new `docs/finals_screen_numbers.md`, or a second table in `docs/finals_screen_v2.md`) covers all 28 with the card each is rendered on, and a test re-derives the 28 from the template so a new card cannot ship unmapped. R1's other half — 「opens from `file://` with Wi-Fi off, all four acts advance」 — gained its first evidence this window in `docs/auto/finals/BOOTH_SETUP.md` §3 (headless Chromium, network blocked, 0 external requests on all four screens), and critic #19 did **not** reproduce that browser run and does not tick on it | done(60c07c8) — `docs/finals_screen_numbers.md` maps all 28 keys `scripts/finals.template.html` references to the 8 cards that draw them, with view, kicker and what the card does with the value, and no registry VALUE. `tests/test_finals_screen_numbers.py` (7 tests) binds it, graded 9-of-9 red by mutation. **R1's other half — 「opens from `file://` with Wi-Fi off, all four acts advance」 — is untouched, so R1 does not tick on this row alone** | true | one lap | B/A 제출 자료 · R1 |
| WFG-111 | P1 | KCF | **R5's promise is that every T0 answer cites a file, and nothing checks that the file is there.** `tests/test_judge_qa_bank.py` gates phrasing and tiers, not paths. Critic #19 extracted every path-shaped citation in `docs/auto/JUDGE_QA.md` at `92bfc4f` — 75 distinct — and resolved each from the repository root: **3 do not resolve**, all as bare names whose full path is given elsewhere in the same file (`check_forbidden.py` and `check_number_collisions.py` at `:751-752`, which are `scripts/…` at `:14`; `delivery/sms.py` at `:672`, which is `src/wildfireguardian/delivery/sms.py`). Nothing is factually wrong today and no answer is unsupported; what is missing is the gate, and the failure it would catch is a renamed or deleted file leaving a T0 answer pointing at nothing between now and 10-24, with the bank green. **Done when:** a test resolves every path-shaped citation in `JUDGE_QA.md` from the repository root and fails on the first that does not, the three bare names are written as full paths, and  ⚠ **CRITIC #35, 2026-09-07T1416Z: re-measured mechanically, and the count has moved in the right direction.** I ran this row's own drill over the five documents the printables kit prints, using the same path regex as `tests/test_related_work_paths.py`: `docs/auto/JUDGE_QA.md` holds **158** backticked repository paths of which **1** does not resolve from the root — `delivery/sms.py` at `:856`, the shorthand for `src/wildfireguardian/delivery/sms.py`, which is also the spelling CHARTER §3.6 itself uses. Two of the three bare names critic #19 found have since been written out. `docs/auto/DEMO_SCRIPT_5MIN.md` (33 paths), `docs/auto/finals/DETECTION_FLOOR_CARD.md` (12) and `docs/submission_reconciliation.md` (14) are clean; `docs/auto/finals/BOOTH_SETUP.md`'s one hit at `:98` is the prose ellipsis `release/.../web/finals.html` and is a false positive the gate will need to exclude. **Nothing is factually wrong today and no answer is unsupported.** ⚠ Note for whoever takes this: `tests/test_related_work_paths.py:36-42` excludes `JUDGE_QA.md` and `BOOTH_SETUP.md` **with a stated reason** — they name artifacts a later lap creates, and sweeping them in recreates the allowlist problem, which is WFG-157. That reasoning is sound and this row is not a complaint about it; what this row adds is that Q16a, shipped `1f024ed`, is now the first card in the bank whose 근거 line is itself the deliverable of a reviewer block about pointing at things that are not there, so the bank has become the place this class of defect lands | todo | true | hours | B/A 제출 자료 · R5 |
| WFG-112 | P1 | infra | **The escalation ledger recorded one author decision inside another entry, and the list the author reads on the laptop showed it as that entry's option C.** `9442430` wrote NH-028's closure line (`ref: ...#NH-028`, verbatim 「Don't worry about the word count ... 25 pages」) at the END of `docs/auto/NEEDS_HUMAN.md`, which by then was inside NH-029's block. `decisions.py list` reads options with `\*\*Options:\*\*\s*(.*?)(?:\n\n|\Z)`, so with no blank line between them the closure was concatenated onto NH-029's option C and read back to the author, through NH-020/§6b's own channel, as part of a question that is still open. **`decisions.py` is not the defect and was cleared by replay:** applying NH-028 against `8a8a940` places the line correctly before `## NH-029`. The cause is an apply run on a checkout whose last entry was still NH-028, merged afterwards with the NH-029 a cloud lap had pushed below it, which is a shape the sprint's 3-hour cadence makes routine. The line was relocated to NH-028 at 20260905T1520Z, text unchanged, with the reason quoted in place; **nothing is corrupt today** and that is why this is P1 under CHARTER §14b and not P0. **Done when:** `decisions.py check` (new subcommand, read-only) exits non-zero when any `**CLOSED ...**` line's `ref: ...#NH-###` differs from the `## NH-###` entry it sits under, when a closed entry carries no closure line, or when a closure line is not separated from the preceding paragraph by a blank line; it runs in `gates.py --mode quick`; a test grades it against the real `9442430` text as the positive case | todo | true | hours | infra · R10 |
| WFG-113 | **P0** | KCF | ⚠ **CRITIC #22, 2026-09-05T2330Z: this row now has a LIVE instance rather than only a mutation.** The screen's payload prints `n_entries` **326** and `n_reproducible` **268**; `docs/NUMBERS.json` at `f118bfe` holds **383** and **325**. Nothing is hand-typed here — the values are correct for the build that produced them and stale for the tree that ships them, which is the same hole from the other side: no gate compares the screen's registry card to the registry. A judge who follows the student's own 「화면의 카드를 보십시오」 lands on the stale pair. The bank half is **WFG-117**; this row should gate the screen's counts against `docs/NUMBERS.json` at verify time, not only at build time. **A number hand-typed into the judged screen's payload is caught by nothing.** Found by WFG-109's independent reviewer, which ran the check rather than reasoning about it: it changed `"n_entries": 326` to `999` on `web/finals.html:434` and every named guard stayed green — `tests/test_finals_template_sync.py` 9 passed (that line is its one documented exemption), `check_forbidden.py` `OK`, `tests/test_finals_screen.py` 32 passed. `verify_numbers.py` **never opens the file** (zero occurrences of `finals`, `web/`, `html`); `check_forbidden.py` reads it but `is_authored_prose()` limits every numeric rule to `.md` (`scripts/check_forbidden.py:226-228`) and counts the skips into `payload_skips`, which is deliberate and documented in `docs/forbidden_check_scope.md` — a retired number in a generated payload is that run's own record, not a claim. The gap is not that exemption; it is that **nothing asserts the payload was produced by the builder at all.** Every judged number on the screen lives on that one line, and WFG-109 made it the single exempt line of the new identity gate, so the exemption is now load-bearing. **Done when:** a test re-derives the payload the way `build_finals.py` does (or re-runs the builder to a temp path) and asserts every value the screen displays matches, tolerating only the build-provenance keys `built_utc`, `git` and `integrity.gates[*].seconds`; graded against a flipped `n_entries`, a flipped per-region count and a flipped comparison-table cell. P0 and not P1 because it is a judge-facing surface under CHARTER §14b, not loop mechanics; `docs/finals_screen_v2.md` §4.2 states the hole in the meantime. ⚠ **CRITIC #24, 2026-09-06: the live instance is now 57 entries wide and the repair is one command this loop has already proven.** Counted at `91d3e05` by parsing the payload out of `web/finals.html:434` and `docs/NUMBERS.json` in the same process: the card prints `n_entries` **326** / `n_reproducible` **268**, the registry holds **383** / **325**. `scripts/build_finals.py:629-630` computes both as `len(reg["numbers"])` and a sum over `reproducible`, so **`make finals` re-derives them from the registry and needs no decision from anyone**; the screen's stamp is `5f9a3b8`, the WFG-109 claim commit, and the payload's `built_utc` is `2026-09-05 15:22 UTC`, so the counts were correct when they were built and went stale six hours later when WFG-114 registered 57 `pp_uiseong_*` keys at `c8a3eee`. **The reason nobody rebuilds is gone and no row says so:** WFG-109 was the standing reason (`make finals` would have put a withdrawn caption back on the judged screen), it is `done(20260905T1520Z)`, and its own closure records that the lap re-ran `make finals` and 「the ONLY line it changed is the payload (434)」, then `make finals-bundle UPDATE=1` to re-derive the 17-file manifest. So the two-command repair is proven in this sandbox at `18c724a`. **This row's gate still subsumes it** — a test that re-derives the payload would be RED today — but a lap that reaches this row should run the two commands **first**, verify the card against `docs/NUMBERS.json`, and then write the gate, rather than leaving 57 stale entries on the judged screen for the length of the gate's design. Note that `make finals` does **not** touch `registry.built_at_commit` (`41498ef`), which comes from `docs/NUMBERS.json` → `built_at_git_commit` and is **WFG-115** | done(20260906T0920Z) — **both halves, in that order.** (1) The two-command repair ran first, as critic #25's `fix-before-next-row` item asked: `make finals` changed exactly one line, 434, the payload, and the card now prints **383 · 325** against the registry's 383 · 325; `make finals-bundle UPDATE=1` carried the new sha256 into the 17-file manifest. The screen's `git` stamp moved `5f9a3b8` → `62b58e1`, which resets WFG-119's shallow-clone clock as predicted. `registry.built_at_commit` is untouched at `41498ef` and stays **WFG-115**. (2) The gate the done-when asks for is `tests/test_finals_payload_rederives.py` (3 tests, ~10 s): it re-runs `build_finals.py` into a temp path and compares the two payloads structurally, tolerating exactly `built_utc`, `git` and `integrity.gates[*].seconds`; the integrity block, which exists only under `--verify`, is checked against **`build_finals.GATES` itself** rather than a list this lap typed. Graded by mutation, all four red: registry `n_entries`, per-region `n_scanned`, comparison cell `counts.naive_into_FA_safe`, and a gate `ok`. Every failure message carries the two-command repair. `docs/finals_screen_v2.md` §4.2's record of the hole is annotated rather than rewritten and §4.3 states the method, the grading and the four things the gate does not do | true | one lap | B/A 제출 자료 · B 데이터 수집·분석·해석 · R4 |
| WFG-120 | P1 | infra | ⚠ **Renumbered from `WFG-117` (and briefly `WFG-118`) on the rebase, 2026-09-05: critic #21 pushed rows with both of those ids first, so they keep them (CHARTER precedent, WFG-040/094/114).** **The artifact manifest names the wrong producer for the experiment three regions of routing numbers rest on.** `docs/artifact_manifest.json` gives `regenerate: python scripts/build_multi_region_comparison.py` for `data/processed/real_roads_real_hazard_uiseong_andong_2025.json`. That script does not write it — it READS it and builds the cross-region table; the producer is `scripts/run_multi_region_routing.py`. Found by WFG-114's reviewer while checking that lap's own provenance fix (the same defect class, and WFG-114 fixed its own row with a `Regenerate:` clause in the registrar's derivation, which is the manifest's documented override). Pre-existing, so it was filed rather than fixed inside that lap. The manifest's own `_README` is the argument for doing it: 「a wrong regeneration command is worse than an absent one: it looks like provenance and is not」. **Done when:** the entry names the script that writes the file, by whichever mechanism the manifest supports (an explicit `Regenerate:` clause on one of its registry entries, or a literal path in the writing script), and a check confirms the named command is a script that actually writes that path rather than one that mentions it | todo | true | minutes | R1 · 제출 자료 |
| WFG-116 | P1 | infra | **The paper's page ceiling is measured by a gate that cannot run on any machine the loop owns.** `paper/check_paper.py` `page_check()` enforces the author's 25-page rule (NH-028) only when LibreOffice **Writer** is present, and returns 「word budget only」 otherwise. Verified here at `ce262fe`: `/usr/bin/soffice` exists, `.auto/venv/bin/python paper/measure_pages.py --why` prints 「No LibreOffice Writer on this machine」, and `libreoffice-writer` and `fonts-crosextra-carlito` appear nowhere in `.github/workflows/`, `scripts/auto/bootstrap.sh`, `requirements.txt` or `Makefile` (grep, zero hits). So the branch that fails is dead in every cloud lap and every `auto-gates` run, and `paper/STATE.json` `built_pages: 21` is the one number in that file nothing re-derives — the drift check skips it unless `metrics_ok`. The file's own docstring says the risk the word proxy cannot see is a **figure**, which costs a page and no words. Not urgent: the document is 21 pages against 25 and 7,461 words against 9,000. **Done when:** `auto-gates.yml` installs `libreoffice-writer fonts-crosextra-carlito fonts-nanum` for the paper job so the clean-clone run measures, or `check_paper.py` fails when `built_pages` is recorded and no run has re-derived it since the last figure changed; graded by adding a figure and seeing the gate go red | todo | true | hours | B/A 제출 자료 |
| WFG-105 | P1 | KCF | **5.61 syllables per second is a floor, not a rate, and the external number nobody had looked up sits under it.** WFG-100's re-budget is sound and its arithmetic re-derives (critic #17 checked every cell: 161+246+280+338+331+328 = 1,684; 29+44+50+60+59+58 = 300). Two things `docs/demo_script_pace.md` does not say. **(a)** The rate is charged against all 300 s as if all 300 s were speech, while §2 of the same script guarantees **five judge interruptions inside those 300 s** and the pace page itself says 「a real delivery is longer than its text」 — so 5.61 is what the student must beat before a breath, a pause or an interruption. State it as a floor. **(b)** The comparison is one search away and CHARTER §3.5b permits the search; critic #17 ran it. The phonetics literature separates *articulation rate* (physical pauses **excluded**) from *speaking rate* (pauses **included**): Yun, 「Effects of gender, age, and individual speakers on articulation rate in Seoul Korean spontaneous speech」, *Phonetics and Speech Sciences* 10(4), <https://www.eksss.org/archive/view_article?pid=pss-10-4-19>, reporting means around **5.2–6.4 syl/s** for short units and computing them 「as the duration between pauses」; same distinction in 「Speech rate in Korean across region, gender and generation」, <https://koreascience.or.kr/article/JAKO201713647763102.page> (412 speakers). **5.61 is a speaking rate sitting inside the published band for articulation rate.** Write it into `docs/demo_script_pace.md` 「What this does NOT show」 with agency, date and scope per CHARTER §3.5b, as evidence and not as a verdict — the stopwatch is still R12 / NH-014. **(c)** Smaller, same family: `docs/auto/RUBRIC.md:10-11` quotes the 심사개요 as 「각 5분 **내외**」 and 「5분을 넘지 않는 것을 **권장**」; `docs/auto/DEMO_SCRIPT_5MIN.md:272` restates it as 「300초는 심사 운영요강이 **정한** 총량」 and `docs/demo_script_pace.md:120` as 「300 s is **fixed by** the 운영요강」, while line 12 of that same page gets it right. One page, two readings of one quoted rule. **Also record here, for this routine rather than the product (CHARTER §14b, parked):** critic #16 published a per-segment syllable table (1,630) that no convention the dev lap implemented reproduces (`docs/demo_script_pace.md:92-108`). The rule from now on — a critic table of counted things ships its counting rule or it ships no table. | todo | true | one lap | B/A 제출 자료 · R4 · R12 |
| WFG-088 | P1 | infra | **The gate that shipped to stop prose drifting from the tree shipped with prose that had drifted from the tree.** `docs/withdrawn_claims.md` §3 is headed 「결과 (재현 가능)」 and prints a console transcript that does not reproduce at HEAD in three of five lines: `spellings : 15` (now **16**), `tracked in scope : 988` (now **989**), `record class : 73 files` (now **74**). `e3ac1e4` added the sixteenth spelling and `9570dde` added five more registry lines, both in the same lap, and §3 was not re-pasted. The sentence below it, 「숫자는 `test_the_coverage_this_row_bought_is_recorded_and_re_derived` 가 다시 계산합니다」, is not true of those three: that test asserts a floor (`len(gated) >= 900`) and two pins (12 declared paths, 10 pinned files). The judge-quotable pair 「11 → 158, 전체 915」 is unaffected and still correct. Done when: §3 re-pasted from a live run, the re-derivation sentence narrowed to what the test actually pins, and (worth adding while there) critic #14's outside measurement recorded beside the other three in §4.2 — **verbatim 6/6, rewordings 1/20** on a 26-sentence set written without reading the patterns, which confirms §4.2 rather than contradicting it | todo | true | minutes | 제출 자료 |
| WFG-089 | P1 | infra | **The scorecard's series table carries a malformed duplicate row.** `docs/auto/SCORECARD.md:34` is a second `83f49bc` row whose Track A cells were overwritten by critic #11's prose (「**The first fall in 데이터 since `12b8ac7`…**」), with the correctly formed `83f49bc` row directly beneath it at `:35`. Critic #13 repaired a blank-line split in this table and left this. The prose survives verbatim in the Track B table's 「what moved」 column, so removing the malformed row loses no record — but it is a history edit, so it is filed rather than done: critic #14 declined to touch it under CHARTER §3 rule 7. Done when: the duplicate row is either removed with a one-line note saying where its text still lives, or annotated in place as superseded, and the series table parses as one table | todo | true | minutes | 제출 자료 |
| WFG-003 | P0 | KCF | Finals screen audit + 5-minute demo script (keep). **Critic #14, 2026-09-04: this is the next `todo` row in table order and no reorder was needed — WFG-062 is `done(e350571)`, so NH-021 is satisfied on its own terms and the booth rows resume where the table already put them.** Previously: **Critic #13, 2026-09-04: considered for promotion above WFG-062 under CHARTER §14b and deliberately NOT moved.** Critic #12's re-scope condition is met and the 1851Z dev lap already put the question to the author as **NH-024** with option C spelled out (「hand the booth rows their place back」). A critic that reorders while its own escalation is open makes the escalation theatre, so the order stands until the author answers. This row ticks R4 and half of R1, and its sprint date is 09-07 | done(20260905T0025Z) | true | one lap | 제출 자료 · 구현 및 유용성 |
| WFG-090 | P1 | science | **Is age alone the right rescue-priority proxy?** (이해평, 2026-09-04). Write `docs/vulnerability_priority.md`: what the rescue ordering actually keys on today, why, and a literature-backed alternative (mobility, living alone, health) as a documented arm on committed data; no numbers move, the shipped ordering stays the control | todo | true | one lap | 설계와 방법론 · 데이터 해석 |
| WFG-091 | P1 | KCF | **Which agency acts on the dispatch recommendation?** (이해평). A section in the decisions doc and the Q&A bank stating honestly which body (산림청 진화대 vs 소방 vs 읍면동) the model assumes acts on each output, and the inter-agency split as a stated limitation | todo | true | hours | 제출 자료 · 연구 목적 |
| WFG-092 | P1 | science | **SILVIS global WUI map check for 의성·안동·청송·영양·영덕** (Radeloff). Open the map, compare with what the repository knows (structure density, distance to forest), read Carlson et al. 2022's distance thresholds, and write a dated note in `docs/auto/knowledge/WUI_BUILDINGS_AS_FUEL.md` with a screenshot and the thresholds; decide (a) recompute a Korea-appropriate WUI boundary or (b) state that the project does not rely on the classical WUI and why. The author replies to Radeloff with the finding | todo | true | one lap | 설계와 방법론 (기존 연구와의 차별점) |
| WFG-093 | P1 | science | **Pedestrians leave mapped paths** (Wilson). Document the network-graph assumption as a limitation in `docs/routing_limitations.md` and write a feasibility note on cost-surface (raster) routing for off-path movement: adopt as a post-finals arm or decline with reasons. Knowledge note `ROUTING_FUNDAMENTALS.md` gets the dated update | todo | true | one lap | 설계와 방법론 |
| WFG-094 | P1 | infra | **Renumbered from `WFG-090` on the push rebase of 2026-09-05: a concurrent research lap had already taken 090-093 on `origin`, and an id others quote wins.** **The finals screen asks the registry for a key that has never existed, and the gate written to catch exactly that does not see it.** `scripts/build_finals.py` `REGISTRY_KEYS` lists `mr_yeongdeok_walk_time_increase_pct`; `docs/NUMBERS.json` holds `mr_uiseong_walk_time_increase_pct` and `mr_uljin_walk_time_increase_pct` but no 영덕 one, and `registry_slice()` skips a missing key silently (`if not e: continue`). Measured by the WFG-003 audit: **40 of the 41 declared keys are present in the built `web/finals.html`; this one is absent.** No card reads it today, so nothing on screen is wrong and this is not judge-facing - which is why it is P1 and not a fix-before-next-row item. But `docs/finals_screen_v2.md` §1 says a card whose key is missing 「아무것도 그리지 않고 조용히 사라집니다」 and names `test_the_v2_cards_have_every_registry_key_they_read` as the gate against it; that test checks the keys **cards read**, not the keys the builder **declares**, so a future card added on this key would disappear silently. Done when: either the key is registered by an additive registrar with its own artifact, or it is removed from `REGISTRY_KEYS` with a one-line note saying why; plus a test that every declared key resolves in the registry (the cheap version of the same rule). Never re-run `scripts/build_numbers.py` wholesale to add it (WFG-040) | todo | true | minutes | 제출 자료 (재현) |
| WFG-096 | P1 | KCF | **The number the demo script calls 「이 작품의 전부」 is the one number in it with no uncertainty statement anywhere in the repository.** `docs/auto/DEMO_SCRIPT_5MIN.md` 3막 has the student say 「368곳 중 91곳, 24.73 %」 and 「458곳 중 42곳, 9.17 %」. Drilled this lap against the files: `mr_uiseong_fa_only_pct` and `mr_yeongdeok_fa_only_pct` resolve in `docs/NUMBERS.json` with matching values, and **nothing in `docs/`, `paper/` or `docs/auto/JUDGE_QA.md` says what their sampling uncertainty is or whether the origins are independent.** `JUDGE_QA.md` Q8 answers the independence question for the cell-level DeLong intervals of the spread model only. A statistician judge - one of the five lenses this project scores itself against - asks the same question of the routing percentages, and the bank has no card. The honest answer is probably that an interval is the wrong instrument, plus the neighbouring-origin dependence and the OSM-density caveat 3막 already carries. Nobody has written it down. **⚠ CORRECTED BY CRITIC #16, 2026-09-05 — the guidance this row shipped with was false and would have put a false sentence into the Q&A bank.** This row said the 368 and 458 origins are 「a census of that region's walk-network origins, not a probability sample」. They are not a census. `data/processed/real_roads_real_hazard_canonical.json` gives `n_nodes` 8,443 against `n_origins_scanned` 458 at `origin_scan_stride` 18, and `scripts/run_real_roads_real_hazard.py:117-131` then drops nodes already at the hazard cutoff at t = 0 and nodes outside a band around the core: a systematic subsample intersected with a hazard-dependent frame, 5.4 % of the graph <!-- collision-ok: 5.4 — this is the origin sample's share of the WALK GRAPH's 8,443 nodes, not a future-aware-only share; it is not bld_yeongdeok_fa_only_pct (8.4) and not mr_yeongdeok_fa_only_pct (9.17). -->. The paper lap's reviewer caught the same sentence in `paper/manuscript.md` §6 six hours after this row was filed and it was rewritten there (`2b7c3a0`); the corrected framing now exists **only** in the manuscript, and this row is the second place the false one was living. The card must state the sampling frame — deterministic systematic subsample, then hazard-filtered, so no design-based standard error is defined — not an interval and not a census. Filed as a row rather than as a new JUDGE_QA question because the bank asserts its own count of 41 in two places (`:17`, `:930`) and its drill table is gated on covering all 41; adding a card is the row's work, not a critic's edit. Done when: `docs/multi_region.md` states in one paragraph what these percentages are and are not a sample of, and `JUDGE_QA.md` carries the card with its count updated in the same commit | todo | true | hours | 데이터 수집·분석·해석 · 제출 자료 |
| WFG-097 | P1 | infra | **The booth script's number half is gated well and its prose half is not gated at all.** Measured from outside this lap with 16 mutations written from the document before reading the test: `tests/test_demo_script_5min.py` **caught 9 of 16**, and the tree's other claim gates take it to **10 of 16** - a far better score than the string tripwires this repository has measured before (2/20, 1/20). Everything numeric is caught: a spoken number drifting from the table, a table value drifting from the registry, a key that does not resolve, a 구두 row relabelled 화면, a new spoken number missing from the table, segment times that stop summing to 300, a cumulative time contradicting its segment, a 화면 row citing a real key the template never renders, and a count contradicting its own percentage. **Uncaught, all six on the prose half:** (1) the CHARTER §9 DRAFT label can be deleted from the file - `tests/test_judge_qa_bank.py::test_the_draft_label_is_on_the_file` guards `JUDGE_QA.md` and nothing guards this one; (2) a **화면** row can be downgraded to **구두** freely, which is the safe direction but means the table cannot be trusted to be tight; (3) the ⚠ block at `:88-93` distinguishing 26.6 % (경사 보정) from 15.14 % (시간 인지 우회) can be deleted - the one place the script says the screen and the mouth carry different quantities; (4)(5)(6) every **[버림]** and never-say caveat can be deleted (see WFG-095). One measurement worth recording for the other side: the mutation that put the withdrawn detection-ordering claim back into 1막 **was caught, by `tests/test_withdrawn_claims_registry.py`** - WFG-062 catching a fresh claim in a document written after it, which is the coverage that row was bought for and the first evidence of it working on new prose. Done when: the DRAFT label is asserted for this file the way it is for the Q&A bank, and the ⚠ blocks are asserted present by their anchors. P1 and **not** a fix-before-next-row item under CHARTER §14b: this is gate work, and it waits behind R1, R3, R7, R8 and R9 | todo | true | minutes | 제출 자료 |
| WFG-098 | P1 | KCF | **The re-cut document that Q18 now sends a judge to does not list the layer Q18 tells the student to name.** `docs/auto/JUDGE_QA.md` Q18 was repaired this window (WFG-087) and now teaches four numbered lines including 「지진해일긴급대피장소 **92개**」, citing `docs/juso_yeongdeok.md`. That file's contents table at `:58` lists what the 사물주소도형 cut holds for 영덕 as 「지진옥외대피장소, 무더위쉼터, 인명구조함, 소화전, 비상급수시설, 버스정류장」 — **지진해일긴급대피장소 is not in it**, and the 92 appears nowhere in the file's prose. It survives only in the header sentence at `:3` (「the 지진해일긴급대피장소 layer is populated」) and in the registry as `juso_yeongdeok_samul_eqwav_point_count`, which I confirmed this lap resolves at exactly **92** (as do `..._eqout_...` at 64 and `..._coolingcen_...` at 17, so the Q18 numbers themselves are all correct). This is the layer whose **0 rows** were the evidence that the first cut was the wrong county (`:20-21`), which makes it the one layer this document cannot afford to omit from its own contents. The parenthetical 「인명구조함 and 비상급수시설 have no 영덕 rows」 in the same cell should be re-checked against the re-cut at the same time. Done when: the `:56` table lists every layer the re-cut actually produced with its count, and the counts are the registry values rather than retyped. Nothing is deleted; the 정정 block stays as the record (CHARTER §3 rule 7) | todo | true | minutes | 제출 자료 (출처) · 데이터 해석 |
| WFG-099 | P1 | infra | **The English ordering gate can be switched off by any 「not」 in the clause, including one that has nothing to do with the comparison.** `english_ordering_violations` — the SHIPPING rule, the one `EN_GUARDED` runs on four judge-facing surfaces — drops a clause matching any of `EN_NEGATION_PATTERNS` (`\bnot\b`, `\bno\b`, `\bnever\b`, `\bneither\b`, `\bwithout\b`, `\bnothing\b`, `\bunknown\b`, `\bunmeasured\b` and five more). That branch is load-bearing and correct in intent: it is what stops the rule firing on the prose that WITHDRAWS the claim. But it tests the **clause**, not the **claim**, and the two come apart. Measured this lap on the shipping rule, three mutations of one withdrawn-claim sentence: 「The satellite trigger fired 22 minutes after the human report.」 **caught**; 「…after the human report, **not** the other way round.」 **missed**; 「…after the human report at **no** fewer than three fires.」 **missed**; 「…after the human report, though the operator is **unknown**.」 **missed**. The first mutation *reinforces* the claim and the rule reads it as a withdrawal. This is not hypothetical drift: `2b7c3a0` reworded `paper/manuscript.md` §4.7 from 「a description of three events **rather than** an operating characteristic」 to 「…, **not** an operating characteristic」 and that one word took the sentence out of both arms' counts — the mechanism behind three of the four hits the 2026-09-05 ci-red re-registration accounted for, which was recorded there as 「the prose moved」 without naming why. Same failure family as the file's own 「a string tripwire, not a claim detector」 admission, and probably not fully fixable inside a regex rule — WFG-071's value-driven instrument is the real answer. Done when: either the negation branch is narrowed to negations that scope the comparison itself (and the three mutations above join the parametrized set), or the limit is stated in the rule's own docstring and in the mutation corpus, so the next lap inherits the measurement rather than the impression that a negation means a withdrawal. Filed by `wfg-autoloop-ci-red`, not by the lap that fixed the red | todo | true | hours | 데이터 해석 · 제출 자료 |
| WFG-102 | P1 | infra | **A commit overtaken inside about two minutes is never checked on a clean machine, and the red it caused lands on the next innocent push.** Measured on this window: `2b7c3a0` (the paper routine, 03:18Z) broke two tests in `tests/test_detection_ordering_is_not_claimed.py`; its own `auto-gates` run **109 was cancelled** by the superseding push, so run **110 went red at `d2418c2`** — a bare `claim WFG-095` marker that changes no code — and the 0439Z ci-red report reproduced both failures identically in the sandbox, so the clean-runner premise that routine exists to test was false here: the branch was simply red. Two causes, both outside a test file. (a) **The concurrency rule in `.github/workflows/auto-gates.yml` cancels superseded runs**, which is the hole; a cancelled run should either not cancel the `gates` job or the next run should report the range it covers. (b) **The paper routine pushed on `scripts/auto/gates.py --mode quick`** (`docs/auto/reports/2026-09-05T0317Z-manual.md:115`), and `--mode quick` does not run `pytest-full`, which is the step that was red. CHARTER §3 rule 9 requires `--mode full` before a push and CHARTER §12 grants the paper loop no exemption. The prompt half is **NH-026** (author). **Done when:** a superseded push can no longer leave `auto/dev` unchecked on a clean machine, and the fix names in its commit body which of the two causes it closes. Held behind R1, R3, R4, R7, R8 and R9 by CHARTER §14b — this is loop mechanics, filed so it is not rediscovered, not so it is done next. | todo | true | hours | infra (not scored) |
| WFG-063 | P0 | KCF | **The trigger recommendation lost its evidence and the T0 answer kept it.** `docs/detection_floor.md` §10 now says in bold that 「신고의 99 %가 목격 신고」 may not carry a conclusion (unregistered, interim), and the booth card dropped it — but `docs/auto/JUDGE_QA.md:240`, a **T0** answer, still names it as one of the two grounds for 신고 우선. And with it gone, nothing left in any judge-facing document supports 「사람 신고를 일차로」: the size floor rules the **satellite out**, it does not rule the **human in**. Restate the claim as the one the measurement carries (「위성을 일차 트리거로 둘 수 없습니다」) in `JUDGE_QA.md` Q10, `detection_floor.md` §10 row 1 and `DETECTION_FLOOR_CARD.md:28,78`, and delete the 99 % clause from Q10 (critic #7, F35) | **done(20260904T0820Z)** — narrowed in five surfaces (JUDGE_QA Q10 · Q10d, detection_floor §10 with its 우선순위표 replaced, DETECTION_FLOOR_CARD front + 트리거 표, SESSION19_REPORT Phase 3 annotated as a record) and gated: `tests/test_detection_ordering_is_not_claimed.py` gains a second claim family that scans five judge-facing surfaces **including the built `web/finals.html`** for the banned spellings AND requires all five to carry the permitted clause verbatim | true | hours | 데이터 해석 · 제출 자료 · 구현 및 유용성 |
| WFG-067 | P0 | KCF | **The finals screen's integrity panel prints a commit id that does not exist in this repository.** `web/finals.html` carries `"git":"a562045"` and the RELIABILITY tab renders it as 「SYSTEM INTEGRITY · build 2026-09-04 07:11 UTC · commit a562045」 — the first line of the panel whose entire job is to let a judge verify the build. `git cat-file -t a562045` answers `fatal: Not a valid object name`. `scripts/build_finals.py:815` `git_head()` stamps `git rev-parse --short HEAD` at build time and the lap then rebased, so the hash died between the build and the push; no test in `tests/test_finals_screen.py` asserts the stamp resolves. The same dead id is quoted in `docs/auto/finals/screens_20260904T0630Z/README.md:3` (the provenance line of the nine committed screenshots), in `docs/auto/BACKLOG.md` WFG-017's `done(a562045)` and in three reports. Rebuild after the rebase so the stamp names a real commit, and add the one-line gate that makes it structural: the stamp in the built `web/finals.html` must satisfy `git cat-file -e`. Do **not** try to make the stamp equal `HEAD` — the commit that carries the build is always one later; the requirement is that it resolve (critic #8, F41) | **done(20260904T1521Z)** — `web/finals.html` rebuilt at `d5e2562` (a commit already on `origin/auto/dev`, so a rebase cannot orphan it); the gate asserts REACHABILITY from HEAD, not existence, because `git cat-file -e` passes for a rebased-away commit and would have stayed green on the machine that made this defect | true | hours | 데이터 해석 (재현) · 제출 자료 (출처) |
| WFG-064 | P1 | paper | **Two of the seven restyled figures have colliding labels, and the lap that shipped them said all seven were looked at.** `paper/figures/F2_lofo_auc.png`: the `0.878` bar label is struck through by the red 「mean of folds 0.89」 rule, and 「pooled 0.905」 sits on the x-axis over the tick labels. `F7_dispatch_ordering.png`: panel b's 「deadline first wins」 teal is the same teal as panel a's 「nearest first」, two meanings on one colour inside one figure. Nudge the labels (offset a value label that falls within ~1 % of a reference rule; place the pooled label inside the axes) and give panel b its own hue. Rubric row is literally 「그래픽 및 범례의 명확성」 (critic #7, F36) | **done(e28377c)** — closed by critic #8, which opened both PNGs at `12bf2d9` and looked: F2 writes every value inside its bar in white with the two reference lines named in a boxed legend in the lower right, and F7 reads vermilion = deadline-first / teal = nearest-first in **both** panels with panel b saying 「ahead」 rather than 「wins/loses」. The paper lap also found and fixed two defects nobody had named (F1's arrow landing on a box corner, F5's last label touching the frame) and wrote both rules into `paper/README.md`. The row could not close itself: CHARTER §12 forbids the paper routine touching `BACKLOG.md` — that mechanism is WFG-068 | true | hours | 제출 자료 |
| WFG-065 | P1 | KCF | **The most quotable fire-behaviour figure about the motivating event is in no judge-facing document.** 8.2 km h⁻¹ forward spread (국가산림위성정보활용센터, from S-NPP/VIIRS thermal detections, 2025-03-22 onward; a Korean record) lives only in `docs/auto/knowledge/PYROGEOGRAPHY.md:45`. It is the first thing a fire-behaviour judge asks about 의성. Register it under CHARTER §3.5b with agency + as-of + scope + the URL a lap opened, add it to `docs/data_sources.md` table A and answer it in `JUDGE_QA.md`, or write down in one line why it stays out (critic #7, F38; the 1.5× / 고성 2019 5.2 km h⁻¹ comparison did **not** verify in this lap's search and must not travel with it) | todo | true | hours | 연구 목적 · 제출 자료 (출처) |
| WFG-066 | P1 | infra | **A bibliographic record was written from memory in a repository whose rule 5 is 「no fabricated citations」.** `docs/auto/knowledge/PYROGEOGRAPHY.md:169` carries `[UNVERIFIED — not opened; author list from memory]`. Critic #7 checked it: Sullivan, Sharples, Matthews & Plucinski (2014), *Environ. Model. Softw.* **62**: 153–163 is **correct in every field**, confirmed against the FRAMES catalog record (frames.gov/catalog/53980) and the ScienceDirect listing. So replace the tag with `[verified 2026-09-04 · FRAMES catalog + ScienceDirect listing]` and delete the phrase. The row is the rule, not the entry: add to CHARTER §13 that a note may carry `[UNVERIFIED]` for a *claim* it could not open, and may never carry an author list, year, volume or page range that was not read off a record (critic #7, F37) | todo | true | minutes | 제출 자료 (출처) |
| WFG-068 | P1 | infra | **The paper routine can do a backlog row's work but cannot mark it done, so a row it finishes stays `todo` and the next dev lap rediscovers it.** CHARTER §12 says the paper lap 「touches nothing outside `paper/` (plus its own report)」, which is the right isolation and also means `BACKLOG.md` is out of reach. WFG-064 is the first instance: the paper lap fixed both halves at `e28377c`, wrote it up under the row's own name, and left the row reading `todo`; critic #8 closed it by opening the two PNGs. Give the routine a write it can make — the cheapest is a committed `paper/BACKLOG_CLAIMS.md` the paper lap appends `{row, commit, what}` to and the next dev or critic lap drains — or state in CHARTER §12 that the paper lap names its completed rows in its report and the daily critic closes them. Either way the rule must be written down, because the failure mode is silent: a `todo` row that is already done costs a whole lap to rediscover (critic #8, F44) | todo | true | minutes | 데이터 해석 (재현) |
| WFG-069 | **P0** | KCF | **The document that spent this window forbidding a conclusion opens by stating it.** `docs/detection_floor.md:13` is the first sentence of §0: 「한국의 산불 탐지는 사실상 전부 사람입니다」. Its only support is the next clause, 「산불 신고의 99 %가 목격 신고」 — the value §10 of the same file now forbids in bold from carrying any conclusion in a judge-facing document (unregistered, no registry key, an interim year-to-date tally as of 2023-04-28). The whole of WFG-063 was 「the size floor rules the satellite out, it does not rule the human in」, and §0 rules the human in flatly, eight lines above the measurement, in the document `JUDGE_QA.md` Q10 · Q10d name as their 근거. Both new gates are blind to it: it carries no `PRIORITY_WORDS` token, so `priority_violations` never looks at it, and the 99 % clause on the next line is pragma-licensed as 「background with its source」. **And the scope is wrong twice over under CHARTER §3.5b:** §0 writes 「보도된 해에」 for a figure the article states as 「올해」 at 2023-04-28, i.e. about four months of accumulation, which §10 itself calls a 잠정치 — an interim presented as a settled annual fact, which is the exact rule 5b exists for. Fix: narrow §0's opening sentence to what is sourced (the 152 cameras with 최초 발견 0건, which is a count and not a share), and give the 99 % clause the interim label §10 already wrote for it, or drop the clause. No number moves and no artifact is touched (critic #9, F48) | **done(20260904T1020Z)** — §0 now opens on the count the source supports (경북 감시카메라 152대, 2022년 및 2023-04-28 까지 최초 발견 0건) with the article URL and the read date, and says 「그 자리에 무엇이 오고 있는지는 이 저장소가 재지 않았습니다」; the 99 % clause stays as background carrying its agency, as-of date and 연중 누계·잠정 scope, and supports no conclusion. §10 lost the same defect in its own first line (「측정이 우선순위를 정해줍니다」, the opening of the section whose two ⚠ blocks deny it) and its camera row gained the period and the source. Gated by `tests/test_external_figures_carry_their_scope.py`, CHARTER §3 rule 5b made mechanical for the first time — **catch rate 12/20** against the reviewer's outside mutation set, six of the eight escapes closed, the two that cannot be closed parametrised as still open. Coverage is two files, not the seven `GUARDED` lists | true | hours | 데이터 해석 · 제출 자료 (출처) |
| WFG-070 | **P0** | KCF | **The withdrawn ordering claim is alive, in English, in the document the routine tells every lap to read.** `docs/auto/research/RESEARCH_BRIEF_2026-09-03.md:75`, under the heading 「The ten hardest judge questions, with the answers that survive the verdicts」, answers question 7 with 「a satellite trigger would have fired +22/+34/+64 min **after the human report** … So the design is **report-first, satellite-confirm**」. `docs/auto/research/sweeps_2026-09-03/R3_science_gaps.md:22` says the same. That is the claim WFG-053 and WFG-063 spent two laps withdrawing from five surfaces, and neither file carries the dated annotation `docs/SESSION19_REPORT.md` got. Both claim families in `tests/test_detection_ordering_is_not_claimed.py` are **Korean-only** and neither `PRIMACY_GUARDED` nor the new `GUARDED` list contains `paper/manuscript.md`, `README.md` or `docs/auto/research/`, so the English spelling of the claim is ungated everywhere. Same file, question 10, cites `docs/auto/AI_DISCLOSURE.md`, retired to `docs/auto/archive/` on 2026-09-04 (critic #10, F52) | **done(20260904T1224Z)** — all three research surfaces annotated (prose unedited) with a **visible** withdrawal marker at each point of use, not only at the top of the file; the dead `AI_DISCLOSURE.md` path corrected to the archive; and two English rules added, kept separate on purpose: `english_ordering_violations` (structural, both sides of the comparison required, **8/16** on a set frozen before it and **9/18** on the independent reviewer's own set — the same 50 % twice) and `BANNED_EN_SPELLINGS` (the three literal spellings this row's contract names, which the structural rule misses standalone). It **found a third instance the row did not know about**, `R7_rubric_gap.md:110`. The reviewer blocked the first push because the number that chose the design (「37 hits」) was in a comment and in no runnable thing and did not reproduce; variant A is now a committed function and the reproducible figure is **27**. Six limits are written down and none is fixed — they are WFG-072 | true | one lap | 제출 자료 · 데이터 해석 |
| WFG-071 | P1 | infra | **The third claim gate was built beside the registry that already holds its answer.** `tests/test_external_figures_carry_their_scope.py` demands that a block printing an external figure carry that figure's **agency, as-of date and scope** — the exact three fields `docs/NUMBERS.json` already stores, and stores for **all 16** of its `provenance: external` keys. The gate reads none of them: `EXTERNAL_FIGURES` is a second, hand-written registry holding **2** figures that appear in no registry at all. Five of the 16 are printed inside files this same gate lists as `GUARDED` (`JUDGE_QA.md` 99,289 · 26명; `web/finals.html` 3,819 · 3,587; `paper/manuscript.md` 99,289 · 3,819 · 3,587 · 2,246 · 104,788) where it does not look. Drive it off the registry instead, and the same pass becomes language-neutral, because it matches the **value**, not the sentence. In NH-021's scope: if the author answers 「gate first」 this and WFG-062 are the pair (critic #10, F53) | todo | true | one lap | 데이터 해석 · 제출 자료 |
| WFG-072 | P1 | infra | **The English claim rule names three surfaces it does not cover, and naming them is all WFG-070 did.** `english_ordering_violations` (`tests/test_detection_ordering_is_not_claimed.py`) guards four English surfaces and scored **8 of 16** on sentences written after its patterns were frozen. Three gaps are written down and none is closed: (a) `paper/manuscript.md` is excluded because its one hit is a false positive inside a `[GAP: …]` marker and licensing it would put an HTML pragma in a file `paper/build_docx.py` converts — so the English claim is **ungated in the manuscript**, which is the IEEE surface; (b) a claim naming only one side of the comparison (「we designed the interface report-first」) passes by construction; (c) a claim spanning a semicolon passes, which is the price paid for seeing `R3_science_gaps.md:22`. This row is MEMO 2026-09-04's own gate applied to the lap that wrote it: a stated limit must become a row or the honesty documents the hole for the reader who already knew. Cheapest first step is not a fourth string family — it is WFG-071's, driving the rule off `docs/NUMBERS.json` so the match is on the **value** and therefore language-neutral (WFG-070, this lap) | todo | true | one lap | 데이터 해석 · 제출 자료 |
| WFG-016 | — | — | ~~AI ledger current~~ | **withdrawn(2026-09-04)** — author's instruction; organisers require no disclosure artifact (NH-008). Never started, nothing produced | false | — | — |
| WFG-086 | P1 | infra | **Both ends of one pipeline are exempt from the withdrawn-claims gate, and the far end leaves the repository by email.** `docs/auto/withdrawn_claims.json` exempts `docs/auto/BACKLOG.md` (rows quote withdrawn sentences as their own definition of done) and `docs/auto/dashboard.html` (generated by `scripts/auto/report.py` from BACKLOG.md). Both exemptions are individually right and together they mean a withdrawn claim can travel from a backlog row into a **rendered HTML page that is mailed to the author**, read by nobody's gate. Found by the independent reviewer of dev lap 20260904T2119Z, which had already accepted the record class as 7.4 % of the tree and could not fault it otherwise. Cheapest fix is not to un-exempt either file: it is to make `report.py` **strip or pragma** the claim spellings when it renders a record path into a non-record artifact, or to render the board with the row text elided where a registered spelling appears. Either way the rule wants writing down in CHARTER §13 or the registry's `_readme`: **a generated artifact inherits the scope of its source, and a record may not be rendered into something that is not one** | todo | true | hours | 데이터 해석 · 제출 자료 |
| WFG-085 | P1 | KCF | **The withdrawn ordering claim is the FIRST SENTENCE of a document the Q&A bank cites, and no pattern in the tree matches its spelling.** `docs/SESSION19_REPORT.md:3` is the file's 「한 문장 결론」: 「GK2A 기반 트리거는 언제나 사람의 **신고 뒤에** 울렸을 것입니다 — +22분, +34분, +64분」. Every WC-001 pattern keys on 사람보다 / 신고 대비 / 신고보다 / 기준 시각은 신고, and 「사람의 신고 뒤에」 is none of them, so it survived WFG-053, WFG-063, WFG-070 and the whole-tree registry run of WFG-062 that found ten OTHER lines in the same file (dev lap 20260904T2119Z). That lap added a visible withdrawal marker directly under the sentence — the annotation existed 76 lines lower, inside Phase 1 — and **deliberately did not add the pattern**, because a pattern its author writes and then grades with its own sentence is the leakage `mandela` names and the objection that shaped that lap. So this row is the sensitivity half, and it must be done the way WFG-070's three literal spellings were: **add the spelling that is IN the tree, and publish the catch rate against a mutation set the author did not write.** Cheapest first step is a possessive-genitive variant of the WC-001 rule (`사람(의)? 신고 …` + a lateness/earliness verb) registered in `docs/auto/withdrawn_claims.json`, with the expected set in `test_the_registry_holds_nothing_the_families_do_not` updated in the same commit | todo | true | hours | 데이터 해석 · 제출 자료 |
| WFG-024 | P0 | infra | Re-key branch name and dates after WFG-023/022 (HANDOFF §5.1 → `auto/dev`; STATE/CHARTER dates; BACKLOG brief path) ⚠⚠ **UNBLOCKED BY CRITIC #47, 2026-09-09T0230Z, AND THIS IS THE ROW THAT TICKS READINESS LINE R11.** The block was on WFG-022 and WFG-023 and **neither blocker gates this work**, which is a different statement from 「the blockers are done」 and is the one this lap can make: re-keying a branch name in a document needs the branch decision, not the 운영사무국's answer to five questions about the finals, and the branch decision is settled in `docs/auto/CHARTER.md` §3 rule 1 and §4c. The date re-key needs NH-006, **closed** 2026-09-04. WFG-022 is now `dropped` on the author's own words and WFG-023's remaining items (two HANDOFF §4 items, decimation) touch nothing here. **The live defect, measured at `86f8929`:** `docs/HANDOFF_ROUND3.md:898` is rule 1 of the §5 block that `CLAUDE.md` and this charter both bind every lap to, and it reads 「**Never push to `Main`. All work stays on `round3-dev`.**」 — a live instruction naming a branch the loop abandoned, inside the one rule set the loop may never break. `docs/HANDOFF_ROUND3.md:10-11` and `:67` carry the same name; `:10` is already annotated as history and is fine. The stale **10-18** date survives in `docs/auto/research/RESEARCH_BRIEF_2026-09-03.md:271`, `sweeps_2026-09-03/R7_rubric_gap.md:1` and `sweeps_2026-09-03/R4_hardware_field.md:3`; ⚠ **all three are dated 2026-09-03 research records and the annotate-never-edit rule (CHARTER §3.5, §3.7) applies to them** — the fix there is a dated superseding note, not a rewrite, and R11's wording should say so. **Done when:** `docs/HANDOFF_ROUND3.md` §5 rule 1 names `auto/dev` with the superseded text annotated beside it, the three research records carry a dated 10-24 note, and R11's evidence column cites the commit. | todo | true | hours | infra · R11 |
| WFG-005 | P2 | science | Decision-level uncertainty (**revise**: demote to P2, blocked — needs the raw bundle and forward simulation; Mac-only; human-approved) | blocked(raw bundle, Mac-only; P2) | false in sandbox | — | — |
| WFG-006 | P2 | science | Wind-direction sensitivity (**revise**: demote to P2, blocked — forward simulation needs the raw bundle; Mac-only) | blocked(raw bundle, Mac-only; P2) | false in sandbox | — | — |
| WFG-008 | P3 | science | Ignition-likelihood layer (**revise**: park to P3 — pre-ignition risk forecasting is what 서식1 says the project is not; theme-drift risk) | parked(theme-drift risk; P3) | — | — | — |
| WFG-009 | **P0** | infra | ⚠⚠ **RAISED P1 → P0 BY CRITIC #36, 2026-09-07T1700Z — this lap's ONE row move (CHARTER §14 direction check), because `WFG-110` closing made this row the SOLE remaining blocker of readiness line R1, and §14b holds eleven infra rows behind R1.** Headless smoke of `web/finals.html`: open from `file://`, abort every non-`file://` request, assert no `pageerror`/console error, assert each `@font-face` family loaded with `document.fonts.check` per family (not `document.fonts.status`, which reports `loaded` even when every face errored), **advance the four acts and screenshot each**, package `web/` whole into a temp dir and run from there. **Why it is now R1 and not housekeeping.** R1 reads 「opens from `file://` with Wi-Fi off, **all four acts advance**, every on-screen number maps to a `docs/NUMBERS.json` key (mapping table committed)」. The second clause closed at `60c07c8` (WFG-110: `docs/finals_screen_numbers.md`, 28 keys, 8 cards, 7 tests, mutation-graded 9/9 — re-derived independently by this critic, 28 parser keys, all present in the doc, all present in the registry's 383). **The first clause has never been exercised by anything in this repository.** Its only evidence is one hand-run under a headless browser recorded in `docs/auto/finals/BOOTH_SETUP.md` §3 on 2026-09-05 (WFG-037), on a build the screen has been rebuilt past; critics #19, #34 and #35 each declined to tick on it and said so. **Measured by critic #36 rather than argued:** Chromium is present in this sandbox at `/opt/pw-browsers/chromium-1194/chrome-linux/chrome` and `--headless=new --virtual-time-budget=8000 --dump-dom file://$PWD/web/finals.html` exits **0** and returns a **3,449,612-byte** post-JS DOM holding all four view containers (`view-live`, `view-evidence`, `view-reliability`, `view-system`) with the four Korean act labels 라이브 · 근거 · 신뢰성 · 시스템, and the built file contains **zero** `http(s)` `src`/`href` references. ⚠ **What that run does NOT show, and why it is not a tick:** at initial load `view-live` holds 1,157,688 bytes and `view-reliability` 2,063,451, while **`view-evidence` holds 225 bytes and `view-system` 235, with no Korean text in either** — consistent with those two views building on switch, which a `--dump-dom` cannot exercise because it presses no key. So the half of R1 that says 「advance」 is exactly the half no measurement covers. **Done when:** all four acts are advanced by a driver and screenshotted, the four screenshots are uploaded as CI artifacts by an `auto-gates.yml` job, the run asserts no console error and no non-`file://` request on any act, and the doc states which browser and which version took the measurement. ⚠ **First risk to check in the first ten minutes:** whether `npm`/`@playwright/test` or a Python `playwright` can be installed in this sandbox at all — `pip` timed out twice on `files.pythonhosted.org` during this lap's own bootstrap. If not, drive the four acts over `--remote-debugging-port` with the stdlib rather than adding a dependency; the row is the four screenshots, not the framework | done(20260907T1820Z-lap) — all four acts advance under `Chromium 141.0.7390.37`, 1/2/3/4 progress dots, 10 requests none off `file://`, zero console errors; four screenshots at `docs/auto/finals/act_screenshots/20260907T1825Z/` and an `auto-gates.yml` `finals-acts` job uploading them per push. ⚠ **R1's 「four acts」 are the guided demo's 막, NOT the four view tabs** — critic #36 measured the tabs, which is a different mechanism (WFG-169). Driver is stdlib-only (`scripts/cdp_min.py`) because Playwright cannot be installed here; `docs/finals_acts_smoke.md` | true | one lap | 구현 및 유용성 · 제출 자료 · R1 |
| WFG-025 | P1 | science | Two clean single-variable sweeps: pre-movement delay (routing axis, 3 regions) and walking speed (vulnerability layer, 영덕) | todo | true (verify dry run first) | one to two laps | 설계와 방법론 (변수 통제) · 데이터 해석 |
| WFG-028 | P1 | KCF | Two or three structured expert consultations (이장, 119 상황실, 사회복지사) + close firefighter §8 blanks | partly done (three written expert replies 2026-09-04, paraphrased in docs/auto/research/EXPERT_REPLIES_2026-09-04.md; 이장/119/사회복지사 still the author) | **false** (agent drafts protocol) | hours each | 개발/연구 목적 · Q&A |
| WFG-029 | P1 | KCF | One recorded email send from a Shanghai-workable path (agent builds Gmail-API/OAuth adapter; student authorises once) | todo | partial | hours | 구현 및 유용성 · 제출 자료 |
| WFG-030 | P1 | infra | Report-number check: every number in `docs/auto/reports/*.md` and `JUDGE_QA.md` must grep to a registry key or artifact | todo | true | hours | 데이터 해석 (재현) |
| WFG-031 | P1 | infra | `CITATION.cff` with true fields (no fabricated dates) | todo — the **file** landed with WFG-036 v1 at the repository root and in the bundle, with no `doi:` and no `date-released:` and a comment saying why; what is left is the author-only half, a tagged release and a Zenodo DOI from a browser session after the finals | true (DOI: human) | minutes | 제출 자료 (출처) |
| WFG-040 | P1 | infra | `scripts/build_numbers.py` overwrites the registry with 65 of its 278 entries — make it refuse, or make it merge (**renumbered** from a duplicate `WFG-036`, critic 20260903T1748Z) | todo | true | hours | 데이터 해석 (재현) |
| WFG-041 | P1 | infra | The lineage gate's ±2-line label window is satisfied by an unrelated keyword, so `JUDGE_QA.md:46` passes it | todo | true | hours | 데이터 해석 (재현) |
| WFG-042 | P1 | IEEE | A `verified` citation in `references.bib` can disagree with the paper at its URL, and `check_paper.py` cannot tell | todo | true | hours | 제출 자료 (출처) |
| WFG-043 | P0 | KCF | Source and register the 2025 fire's scale figures (deaths, burned area, homes) that open the README. **Rewritten by critic 20260903T2347Z (F16/F17/F18): the paragraph was rewritten at `12b8ac7` and is now wrong in the other direction — 45,157 ha stated for a chain that burned 99,289 ha, 영덕 8명 against this repository's own correction to 10, and eleven figures with no URL** | done(fbe71de) | true (author granted standing permission to source public data, NH-015 closure) | hours | 연구 목적 · 제출 자료 (출처) |
| WFG-038 | P1 | infra | The full suite reports two different skip counts on one commit and the gate is green for both — make the (collected, passed, skipped) triple a gate | todo | true | hours | 데이터 해석 (재현) |
| WFG-039 | P1 | infra | The test suite downloads an 8.4 MB (gzipped; 25.9 MB on disk) SRTM tile mid-run, so first-run and re-run pass/skip counts differ by six — make the download opt-in (**this is the cause of WFG-038's symptom**) | todo | true | hours | 데이터 해석 (재현) |
| WFG-044 | P1 | infra | `scripts/auto/report.py` has no `paper` kind, so the paper routine files its report as `manual` and overwrites `STATE.json` → `last_report_kind` with it (critic 20260903T1947Z) | todo | true | minutes | 데이터 해석 (재현) |
| WFG-045 | P1 | IEEE | `paper/manuscript.md` cites 21 works and has no `## References` section, and `check_paper.py` checks no section at all against CHARTER §12 (critic 20260903T1947Z) | todo | true | hours | 제출 자료 (출처) |
| WFG-046 | P0 | infra | Every lap pushes commits no gate has read: `gates.py` runs at step 5 and everything after it (the report, and any post-review fix) is unchecked, so `auto/dev` has gone red twice this way (`24751fa`, `8d1decf`). **Widened by critic 20260903T2147Z (F14):** not just the report. Make `report.py` gate its own prose AND add a `--assert-head` check that refuses a push when `.auto/gates.json` → `git_head` is not `HEAD` | done(509819d) | true | hours | 데이터 해석 (재현) |
| WFG-047 | P0 | infra | `in-progress` is written as a lock with no release, so a row a lap left unfinished is invisible to every later lap: WFG-021 (a)+(c) and WFG-016 are stranded, and `KCF_READINESS` R2 depends on WFG-021 (a) (critic 20260903T2147Z) | done(509819d) | true | minutes | 데이터 해석 (재현) |
| WFG-049 | P0 | infra | A commit that carries only unregistered prose is invisible to every gate this repository owns: `12b8ac7` rewrote the judge-facing README in two languages and closed three NEEDS_HUMAN entries with no report, no reviewer and no `STATE.json` update, and `--assert-head`, `report.py`'s prose gate and `make verify` all passed on it (critic 20260903T2347Z, F19) | done(2026-09-04 laptop lap: registry keys fire2025_*, check-readme-figures, --assert-reported) | true | hours | 데이터 해석 (재현) |
| WFG-048 | P1 | infra | The three FIRMS first-detection delays that `docs/detection_floor.md` §4 and §8 put beside the GK2A delays (+117 / +151 / +17 min, from `data/processed/detection/firms_first_detection.json` → `delay_h` 1.95 / 2.52 / 0.28) have no registry key, so the one comparison a judge is most likely to ask about is the one number on that page that `make verify` cannot re-derive (dev lap 20260903T2217Z, while writing WFG-021 a) | todo | true | hours | 데이터 해석 (재현) |
| WFG-050 | P1 | infra | The motivating-event figures are pinned by `tests/test_motivating_event_figures.py` against `docs/data_sources.md`, a sibling document the same lap wrote in the same commit — leakage patterns #3/#4/#5, named by the 2026-09-04T0017Z dev lap's own reviewer. Table A's rows carry bare news URLs whose content can change or vanish. Snapshot each cited page (sha256 + retrieval date, the way `docs/evidence/greenpeace_2026_survey.md` already does) and assert README <-> snapshot instead of README <-> sibling doc | todo | true | hours | 데이터 해석 (재현) · 제출 자료 (출처) |
| WFG-051 | P1 | infra | The `fire2025_*` apparatus binds a figure's **value** to the registry and leaves its **attribution** free: `check_readme_figures.py` only asserts that `agency` / `as_of` / `scope` / `source_url` are non-empty, never that they agree with what the prose says. Three live disagreements: the registry calls 45,157 ha a 중앙재난안전대책본부 figure while `README.md:204-205` and `docs/data_sources.md:194` call it 산림청 (the cited 경향신문 article says 산림청, so the registry is the wrong one); the registry calls 사망 26명 a 중앙재난안전대책본부 count while `docs/data_sources.md:190` calls it 경상북도 재난안전대책본부; and `README.md:193-199` puts 사망 26명 under an 아시아경제 link that carries no death figure at all. Fix the artifact, give 26명 its own citation, and extend the gate to compare the README's inline link and the sources table's 출처 column against the registry's `agency` and `source_url` (critic #5, F23). **Raised P1 → P0 by critic #6 (F30): the disagreement has now propagated into `paper/manuscript.md:36-38`, which calls 26명 "the provincial disaster headquarters' count" — a third spelling — so the same figure now carries three different agencies across three judge-facing documents, and the README link a judge would click still carries no death figure** | todo | true | hours | 제출 자료 (출처) · 데이터 해석 (재현) |
| WFG-052 | P1 | infra | No gate reads judge-facing prose for *structural* damage, only for numbers: `d2f314d` left BOTH opening stanzas duplicated verbatim on consecutive lines (`**보호 대상**` and `**Motivating event**`), and they survived the eight commits since, a critic run that read that exact diff, `make verify`, `check_readme_figures.py` and 36 paragraph tests, because every one of them flattens or greps the paragraph and none looks at it as text. The 2026-09-04T0400Z dev lap removed the Korean twin, missed the English one, and its own reviewer found it — which is the row's point twice over. Both removed; that test now asserts each anchor appears exactly once. The general gate is still the row. Cheapest form: fail on two consecutive identical non-empty lines in `README.md` and `docs/*.md` | todo | true | minutes | 제출 자료 |
| WFG-053 | P0 | KCF | **The booth card, the T0 Q&A answer and the design doc's 평결 all say the satellite rang after the human call; this project's own paper says the measurement cannot say that.** Narrow every judge-facing document to the paper's wording (delays behind the *recorded occurrence time*, no ordering claim), keep the size floor, which is true either way. Agent-doable and needs nothing from the author: the paper already did it (critic #6, F27) | done(2026-09-04T0419Z) | true | one lap | 데이터 해석 · 제출 자료 (출처) · 구현 및 유용성 |
| WFG-054 | P0 | infra | `scripts/auto/decisions.py apply` appends the Gmail message id to `decisions_seen.json` even when `apply_one` recorded nothing, so an author reply naming an id the file does not carry is written nowhere and the message is never read again. Its own docstring promises the opposite (`recorded, as noted, never guessed at`). Record `seen` only on a real change, write unmapped lines to a committed place, and test both directions (critic #6, F28) | todo | true | hours | 데이터 해석 (재현) |
| WFG-202 | P1 | KCF | ⚠⚠ **MOVED FROM TABLE POSITION 1 TO HERE BY CRITIC #47, 2026-09-09T0230Z — this lap's ONE §3b row move (CHARTER §14; reason on `docs/auto/DIRECTION.md`). No P0 row moved below a non-P0 row; a non-P0 row moved BELOW the P0 block, which is the only direction §3b's rule permits.** The row was filed at position 1 by the 0056Z dev lap as WFG-201's follow-on. It is correct and its premise is re-verified here a case-sensitive count of the three spellings `argmax`, `post-hoc` and `maximum over` in `paper/manuscript.md` answers **0** at `86f8929`). But it is **P1**, it is the paper routine's under CHARTER §12, and at position 1 it sat above **twelve** P0 `todo` rows — and CHARTER §4 step 3 tells the dev lap to take the first `todo` row in **table order**, so the next dev lap would have spent a sprint slot on a row no dev lap is supposed to build. The paper routine does not read this table's order, so moving the row down costs the manuscript nothing. **`paper/` draws the same post-hoc-argmax conclusions as the six surfaces WFG-201 fixed, and the gate cannot reach it.** `tests/test_post_hoc_maximum.py` states the property, scopes the ban to the section a judge reads, and covers six surfaces; `paper/` is the paper routine's under CHARTER §12 and no dev lap edits it, so it is deliberately outside `SURFACES`. **The hole is real and was measured** by WFG-201's independent reviewer at `d78d39b`: `unqualified_post_hoc_claims` fires on **two** units of `paper/GAPS.md` and **one** of `paper/README.md`. `paper/manuscript.md` §4.5 states the same change-of-kind finding and is the surface a reviewer reads. **Do:** the paper lap writes the qualifier — the opponent's width is the argmax read after the run, so its score is a maximum over the measured grid and the forecast's margin over it is non-increasing as widths are **added** to that grid — into `paper/manuscript.md` §4.5 and the two `paper/` pages that fire, in the manuscript's register, and then **adds those paths to `SURFACES`** in `tests/test_post_hoc_maximum.py`, which is the whole remaining fix. **Constraints:** the word budget is `paper/README.md`'s (NH-037 open, the proxy stops the manuscript ~1,000 words under the author's 25-page rule), so this may have to be funded by a trim the lap reports; no number moves and no artifact is regenerated; the qualifier is about **how the number is selected** and carries no margin value while NH-032 and NH-034 are open. **Done when:** `unqualified_post_hoc_claims` returns empty for every `paper/` path and those paths are in `SURFACES` with the mutation test green on them. | todo | yes | one paper lap | B 데이터 수집·분석·해석 · IEEE |
| WFG-055 | P1 | IEEE | `paper/check_paper.py` enforces a word proxy (7,479 of 7,500 used) for a constraint CHARTER §12 states in **pages**, measures no section against the §12 list, and the loop's own recount puts the built `.docx` nearer 21 pages than 20. Get one real page count out of the built document, re-derive the words-per-page constant from it, and add a section gate. Absorbs WFG-045 (critic #6, F29) | todo | true | one lap | 제출 자료 |
| WFG-056 | P1 | infra | `gates.py --assert-reported` reads `origin/auto/dev` at push time and writes nothing, so no later lap can audit whether a given push actually carried a report — which is the check the critic prompt asks be verified every day and the one thing in this repository that cannot be. Append `{utc, base, head, verdict}` to a committed ledger under `docs/auto/` on every run (critic #6, F32) | todo | true | hours | 데이터 해석 (재현) |
| WFG-057 | **P0** | infra | A question numbered `Q10a` is invisible to every test in `tests/test_judge_qa_bank.py`: `QUESTION_RE` matches `Q(\d+)`, so a lettered question escapes the tier-count anti-padding guard, the contiguity check and the 근거/없는 것 requirement. Four such questions now exist (Q10a, Q10b, and the three this lap added). Widen the pattern to accept a letter suffix and restate the three header counts in the same commit (critic #6, F34). **Raised P1 → P0 and the stated fix corrected by critic #8 (F43): widening the pattern for the letter suffix is not enough.** `QUESTION_RE` is `^\*\*Q(\d+) · (T[012])\.` and the escaping headers are `**Q10c · T1 (크리틱 #6).`, `**Q10d · T0 (크리틱 #7).` and `**Q34 · T2 (크리틱 #7).` — the **parenthetical between the tier and the period** defeats the pattern on its own, so `Q34`, which carries no letter at all, escapes too. Six questions are now invisible, not four. The reason this is P0: `JUDGE_QA.md:17-23` tells the student 「T0 (14개) — 이것만 완전히 외우십시오」, the file actually holds **38 questions and 15 T0s**, and the fifteenth T0 is **Q10d**, the entry whose whole job is to stop the student saying a sentence this repository cannot support. The student who obeys the bank's own drill plan memorises the fourteen and never reaches the guard. **Re-measured by critic #9 at `ce31b91` (F49) and it moved the wrong way inside one window:** the file now holds **41 question headers — T0 15, T1 19, T2 7** — while `:17-23` still says 「33개」 and 「T0 (14개) · T1 (13개) · T2 (6개)」. Every one of the three tier counts is now wrong, not one of them. The drift is +3 headers in the last two windows (38 at `8e0a6ad`, 40 at `12bf2d9`, 41 at `d8aec94`) and no lap has touched the header since. Critic #8's own report quoted 「38 questions」 against a head that held 40, which is this defect catching the report that named it. **Re-measured by critic #12 at `c65dc56` (F61) and it has not moved in three windows:** the file holds **41 question headers, T0 15 · T1 19 · T2 7**, and `:17-23` still reads 「33개」 · 「T0 (14개)」 · 「T1 (13개)」 · 「T2 (6개)」. Identical to critic #9's and critic #11's counts. Twenty-two commits and fourteen reports have landed since critic #9 first measured it, on a row marked **P0** and **minutes**. The harm is unchanged and is the reason for the priority: the drill plan tells the student 「T0 (14개) — 이것만 완전히 외우십시오」, and the fifteenth T0 is **Q10d**, the entry whose whole job is to stop the student asserting the withdrawn ordering claim | **done(20260904T1817Z)** — `QUESTION_RE` now reads a letter suffix and a parenthetical provenance, so the bank's 41 headers are all visible to the counts, the contiguity check, the 근거/없는 것 requirement and the drill table (was 33). Header now states 41 · T0 15 · T1 19 · T2 7, and §6 names every id including Q10d, Q34, Q35, Q30a, Q30b. Contiguity widened to base-number 1..N + no dangling suffix, because document order is genuinely not sorted (Q34/Q35 sit beside the section they answer) | true | minutes | 제출 자료 |
| WFG-058 | P1 | paper | Manuscript figures restyled to the look of Moreno et al. (2025), which the author chose on 2026-09-04: framed panels, panel letters, hairline bar edges, framed legends, a warm red / blue / grey palette. `paper/style.py` rewritten, `paper/make_figures.py` labels every multi-panel figure; rules in `docs/auto/knowledge/FIGURE_STYLE_REFERENCE.md` | done (2026-09-04 laptop lap) | true | one lap | 제출 자료 |
| WFG-059 | P3 | science | Buildings as exposure, not fuel — a Korean BFM-lite **after the finals**: 건축물대장 (needs the author's API key) joined to 도로명주소 건물DB footprints → footprint fraction, structure-separation distance, construction era and structure type per cell; then an ablation in the leave-one-fire-out protocol. The FireDX pipeline the author supplied was read and deliberately not adopted before the freeze; the decision and the four reasons are in `docs/auto/knowledge/WUI_BUILDINGS_AS_FUEL.md` | todo (post-finals) | partly | days | 데이터 수집 · 연구 기여 |
| WFG-060 | P2 | paper | Study-area map of the six fires in the Moreno Fig. 1 style: DEM hillshade from data already in the repo, one graded circle per fire (size and colour by burned area, five-step legend), lat/lon graticule, scale bar, boxed legend. Offline build only, every burned-area figure from the registry | partly done (F8 routing map, 2026-09-04); six-fire map blocked | true | one lap | 제출 자료 |
| WFG-061 | P1 | science | `docs/horizon_grounding.md` reads the KFS 산불통계 CSV's `발생일시` column as a 신고 시각 and cites no source for that reading, exactly as `docs/detection_floor.md` §1 did before WFG-053. The two are **different datasets** (public-portal CSV vs `fire_manifest.json`), so the claim that they "share the same weakness in the same data" was wrong and was withdrawn by the WFG-053 lap; the column's own provenance is still unchecked. Find what 산림청 says `발생일시` means in the 산불통계데이터 spec, or state that it is unknown. The 79.23 %/240-min horizon numbers do not move either way (dev lap 20260904T0419Z). **Widened by the WFG-017 lap's reviewer (2026-09-04): this is not doc-vs-nothing, it is doc-vs-artifact.** `data/processed/detection/kfs_containment_duration.json`'s own header `⚠_reference_time` states flatly 「`발생일시` is a REPORTED start time, not observed ignition. Every duration is report-to-containment」, and all four registry entries the finals screen reads (`kfs_cum_le_240_pct`, `kfs_n_usable_events`, `kfs_containment_median_min`, `kfs_area_ge100ha_median_min`) carry that caveat, as do `det_gk2a_delay_*_min`. So the artifact asserts what §2 says it could not confirm. The artifact is a committed record and is not edited (§3 rule 2); what this row must produce is either the upstream source (then annotate the entries) or a written statement that the assertion is unsourced. Until then judge-facing prose states BOTH and reads the distribution as 기록 → 진화 only | todo | true | hours | 데이터 해석 (재현) · 데이터 수집 |
| WFG-011 | P2 | ISEF | ISEF plan memo (**revise**: route-existence questions, SFTD base rate, age rule, hand-written documents) | todo | true | one lap | — |
| WFG-032 | P2 | science | Leak-free 영덕 fold + hindsight-oracle routing arm (agent writes the script; student runs on the Mac) | todo | partial | one lap + one Mac day | 데이터 해석 · IEEE Table V |
| WFG-033 | P2 | science | Coupling-ablation routing-only arms on committed hazard fields (fire-blind / static perimeter + buffer / spread_v2), three regions (absorbs WFG-012) | todo — **(b)'s first slice is done**: WFG-114 ran the static-perimeter + buffer arm on 의성·안동 at five widths (250 m / 500 m / 1 km / 2 km / 3 km) on the **canonical** (slope 60 m / DiGraph) arm, against the reproduced committed canonical classification — `docs/present_perimeter_arm.md`. **Outstanding:** the other two regions and this row's own 「says plainly whether the learned field beats 'current perimeter + buffer'」 across regions. On 의성·안동 alone the answer so far is: it beats it by **9 origins of 368** at the best width tried, and by 79/70/79/71 at the other four | true | two laps | 설계와 방법론 · 데이터 해석 |
| WFG-034 | P2 | science | Refuge-density decimation (100/75/50/25%, 20 seeds, 3 regions) — only after written approval in WFG-023 | blocked(approval in WFG-023) | true, blocked(approval) | two laps | 데이터 해석 · 창의성 |
| WFG-013 | P2 | science | Open building-footprint coverage check for 영덕 (keep) | todo | true | one lap | 데이터 수집 |
| WFG-014 | P3 | IEEE | Paper skeleton in `paper/` (**revise**: vocabulary, caveats, AI acknowledgment, no preprint before December) | done(0ff1b36) | true | weeks | — |
| WFG-035 | P3 | IEEE | Register every number the manuscript will cite; reconcile the two HGB means; per-fire/spatial-block CIs; new-ring IoU beside cumulative | todo | true | one lap | 데이터 해석 |
| WFG-015 | P3 | IEEE | Reproducibility package + Zenodo release checklist (keep; DOI minted by the student) | todo | true (release: human) | one lap | 데이터 해석 (재현) |
| WFG-075 | **P0** | science | **The dataset committed as 영덕 is not 영덕, and a test pins the mistake.** `scripts/extract_juso_yeongdeok.py:32` sets `SIGUNGU = "47920"` with the comment `# 경상북도 영덕군`. Measured this lap from the eight committed GeoJSON files, every selected point falls at lat **36.78–37.05 N, lon 128.65–129.15 E**; this repository's own canonical 영덕 box is **(129.25, 36.30, 129.55, 36.60)** (`config/default.yaml:83`, `regions.lookup('yeongdeok_2025').bbox_wgs84`). **The two boxes do not overlap on either axis** — about 45 km apart diagonally. Two corroborations from the artifact alone: 영덕 is an East Sea coastal county and the set contains **zero** points east of 129.15 E; and `samul_eqwav_point` (지진해일긴급대피장소) has **zero** rows, which `docs/juso_yeongdeok.md:12` writes up as a fact about 영덕 — a coastal county has tsunami evacuation sites, a landlocked one does not, so that zero was the tell. The centroid (36.915 N, 128.871 E) is next to 봉화읍. Carried by: 8 `juso_yeongdeok_*_count` registry keys, each with `scope: 영덕군 · …` and a `derivation` naming 「sigungu code 47920 (영덕군)」; `docs/juso_yeongdeok.md`; the 2026-09-04 annotations on NH-005 and NH-012 telling the author that 영덕's designated sites are now in the repository; WFG-073 and WFG-074, which would put these points into the router as 영덕 refuges and depots. **Nothing judge-facing prints them yet** (README, `web/finals.html`, `paper/manuscript.md`, `JUDGE_QA.md` all clean), so this is contained until one of those rows runs. **And the suite enforces it:** `tests/test_juso_yeongdeok.py:11` asserts `man["sigungu_cd"] == "47920"`, and all 1367 tests pass. **Agent-doable half (this row):** annotate the eight registry entries additively; put the correction at the top of `docs/juso_yeongdeok.md`; correct the two NEEDS_HUMAN annotations; keep WFG-073/074 blocked. The re-cut needs the laptop (the zips are git-ignored under `data/raw/juso/`), which is **NH-022** (critic #11, F54) | done(20260904T1521Z: annotated; re-cut with 47770 on the laptop the same day, NH-022 closed) | true (annotation half; the re-cut is NH-022) | hours | 데이터 수집·분석·해석 · 제출 자료 (출처) |
| WFG-076 | P1 | infra | **Every gate here checks that a number matches its artifact; none checks that the artifact is of the thing its label names.** That is critic #11's root objection and F54 is its first measured cost: nine laps of sourcing discipline (agency, as-of date, scope, URL, forbidden phrasings, digests) and the one field none of it constrains is **scope**, because scope is prose a lap typed. The cheapest general fix is geometric and needs no external source: for every artifact whose name or `scope` carries a region this repository already knows, assert its geometry lies inside `regions.lookup(<region>).bbox_wgs84` (or a stated, committed buffer of it), and fail otherwise. One function, runs offline, catches F54, the next mis-keyed 시군구 code, and any wrong-CRS or wrong-`.prj` assignment too — including the EPSG:5179 assumption `docs/juso_yeongdeok.md:26` records as an inference rather than a reading. This is the geometric sibling of WFG-071's 「drive it off the registry, match the value not the sentence」; take them together. **Must publish its catch rate** against a mutation set its own author did not write, per NH-021 and WFG-062 (critic #11, F54 root) | todo | true | one lap | 데이터 수집·분석·해석 (재현) · 제출 자료 |
| WFG-077 | P1 | infra | Pre-commit and gate check that refuses any staged file containing three or more email addresses or phone numbers, or a path under `outreach/` (NH-023: a harvested contact list reached a public commit through `git add -A`). Add to `make verify` and to the dev prompt's step 8 | todo | true | hours | 제출 자료 |
| WFG-073 | P1 | science | Designated sites as refuge candidates for 영덕: the author's 주소정보누리집 download (2026-09-04) gives agency-listed 지진옥외대피장소 and 무더위쉼터 points (`data/processed/external/juso_yeongdeok/`, `docs/juso_yeongdeok.md`). Measure how many of the router's current 영덕 refuges coincide with a designated site (within the walk-network snap distance), how many designated sites are reachable under the canonical forecast, and whether swapping them in changes the decision-shift counts — a new arm, new filenames, no committed artifact modified | todo (unblocked 2026-09-04: WFG-075 done, NH-022 closed) | true | one lap | 데이터 수집 · 설계와 방법론 |
| WFG-074 | P1 | product | Responder depots and notification targets from the 민원행정기관 layer: 119안전센터, 파출소 and 읍면동 offices for 영덕 with road addresses and phone numbers (`juso_yeongdeok_minwon_agencies_count`). Load them through the existing `rescue.load_depots` path as a documented alternative to the synthetic depots; the alert sheet names the nearest 읍면동 office. No numbers move; a new arm | todo (unblocked 2026-09-04: WFG-075 done, NH-022 closed) | true | one lap | 제출 자료 · 창의성 |
| WFG-082 | P1 | infra | **(Filed as WFG-077 by critic #12 at `3d77e01`; renumbered to WFG-082 by critic #13, 2026-09-04, because the 1731Z lap had already used WFG-077 for the NH-023 staging gate fifteen minutes later at `1c5ae23` and two rows carried one ID. The other id is the one kept, because it is quoted in `CHARTER.md:465`, `MEMO.md:753` and two reports, against two mentions of this one. Reports that say WFG-077 and mean this row are `docs/auto/reports/2026-09-04T1713Z-critic.md:71` and the CRITIC_LATEST of that lap.)** **A report can certify a commit it does not travel in, and `--assert-reported` cannot tell.** `docs/auto/reports/2026-09-04T1627Z-manual.md` prints 「**ALL GREEN** · mode `full` · head `7988769` · current at `7988769`」 and ships inside `c65dc56`, which added eight rewritten GeoJSON artifacts, 166 changed lines of `docs/NUMBERS.json`, a re-frozen `docs/baseline_phase13.json`, the extractor, the tests and 86 lines under a **new top-level `outreach/` directory** that the report does not mention at all. The gate run it prints saw none of that. `report.py` computes its 「stale」 marker when the report is **written**, so a report written at HEAD and then committed alongside further changes is never marked stale, which is the loophole CHARTER §4 step 8 does not close. `--assert-reported` exits 0 here (I ran it against both `89730db` and `7988769`) because it asks only whether a **new report file travels with** the substantive paths, never whether the report **describes** them, and `--assert-head` exits 0 only because a later lap re-ran the gates. Two changes, both small: (a) `report.py` records the head it certified and `--assert-head` refuses a push when the newest report's stated head is not an ancestor-or-equal of the gate head; (b) `--assert-reported` prints the substantive paths it counted and fails when a path outside every directory named in the report body is among them, so a new top-level directory can never again ride in unnamed. Critic #12 verified independently that `c65dc56` **is** green (`gates.py --mode full` exit 0, `1376 passed, 62 skipped`), so no harm materialised; the machinery certified none of it (critic #12, F56) | todo | true | hours | 데이터 해석 (재현) · 제출 자료 |
| WFG-078 | P1 | infra | **Eight registry values were edited in place and the caveats that carried the correction were deleted, which is CHARTER §3 rules 2 and 3 inverted, and no gate objected.** `c65dc56` overwrote the eight `juso_yeongdeok_*_count` values in `docs/NUMBERS.json` (74 to 55, 27 to 64, 0 to 92, 99 to 17, 6 to 23, 28 to 3, 5 to 0, 0 to 0), replaced every `caveat` and `scope_status`, and dropped five `forbidden_phrasings` per key. Rule 2 says registry entries already registered are 「add, never edit a value」; rule 3 says 「superseded values are annotated, never deleted」. The replacement caveat states the position in as many words: 「The wrong first values are kept in git history (3fdb888), not here.」 The author's NH-022 reply authorised overwriting the **files** and re-freezing the baseline and says nothing about the registry. `scripts/check_number_collisions.py` has no add-never-edit rule, so the tree cannot tell an annotation from an overwrite. Either write the exception into CHARTER §3 (a value that was never about the thing it named is not 「superseded」 and may be replaced, with the old value annotated in the entry rather than only in the log) or restore the eight entries as annotated supersessions; either way add the gate, because the rule and the practice now disagree and nothing in the tree records which won (critic #12, F57) | todo | true | hours | 데이터 해석 (재현) · 제출 자료 (출처) |
| WFG-081 | **P0** | KCF | **A T1 answer tells the student to admit, at the booth, a defect that was fixed nine hours earlier, and to quote a commit id that is no longer on the screen.** `docs/auto/JUDGE_QA.md:412-425` is Q35, 「화면 아래 「commit a562045」 라고 적혀 있습니다…」, and it answers 「🛑 근거 없음 - 오늘 이 질문에는 「아니오」 라고 답해야 합니다 (백로그 WFG-067)」 with a draft reply opening 「그 줄은 지금 틀렸습니다」. WFG-067 is `done(20260904T1521Z)`: `web/finals.html` now carries `"git":"d5e2562"` and critic #12 verified `git merge-base --is-ancestor d5e2562 HEAD` succeeds, so the panel resolves. A judge reading the screen sees `d5e2562`; a student reciting the bank volunteers a fault about `a562045`, which is on neither the screen nor the judge's mind. Rewrite Q35 to the answer that is now true (the stamp names a reachable commit, the gate asserts reachability rather than existence because `git cat-file -e` passes for a rebased-away object, and here is what a judge can run), drop the 🛑 banner, and re-tier it. Same class as WFG-057: the bank drifts from the tree and nothing reads them against each other (critic #12, F60) | **done(20260904T1817Z)** — Q35 rewritten: the answer is now 「예」, the a562045 story is kept below it as the project's best reproducibility example (existence vs reachability), and the 근거 line points at the two tests that now gate the stamp. The stale open-question table that still listed Q10d (closed by WFG-063) is corrected in the same pass | true | minutes | 제출 자료 · 구현 및 유용성 |
| WFG-079 | P1 | KCF | **The document recording the 영덕 correction reprints, eight lines below its own prohibitions, both things the correction withdrew.** `docs/juso_yeongdeok.md` says at `:15-17` 「**킬로미터 거리는 여기에 쓰지 않는다.**」 because WFG-075's amendment withdrew the 45 km figure as never computed (measured: nearest point 30.5 km, farthest 65.6 km), and at `:29-31` 「**어느 군인지는 여기에 쓰지 않는다.** 47920이 실제로 어느 시군구인지 이 랩은 행정표준코드(code.go.kr) 원부를 열어 확인하지 못했고, WFG-066은 「기록에서 읽지 않은 식별자는 적지 않는다」를 표준으로 세워 둔 규칙이다.」 Then `:61` writes both: 「filtered on 시군구 code 47920, **which is 봉화군**, not 영덕군; critic #11 caught it from the coordinates alone (every point **45 km inland**, no tsunami sites)」, and the commit subject of `c65dc56` names 봉화군 too. The corroboration for 봉화군 is the address field of the first cut (74 of its road-address strings) plus a search summary, which is evidence but is not the record `:29` demands. Fix: either open 행정표준코드 and cite it at `:61` and `:29`, or take the county name and the kilometre figure out of `:61`. No number in any judge-facing document moves either way (critic #12, F58) | todo | true | minutes | 제출 자료 (출처) · 데이터 해석 |
| WFG-080 | P1 | science | **The lap that wrote 「a zero-row layer is a wrong-filter signal, not a fact」 into the MEMO registered two new zeros as facts in the same commit.** `docs/auto/MEMO.md` (2026-09-04): 「a "zero rows" result for a layer that must exist in the region is a wrong-filter signal, not a fact.」 `docs/juso_yeongdeok.md:58`, of the corrected cut: 「(인명구조함 and 비상급수시설 have no 영덕 rows)」, stated as a fact about 영덕, and registered the same way (`juso_yeongdeok_samul_lifesav_point_count` = **0**, `juso_yeongdeok_samul_emerwat_point_count` = **0**). 영덕 is the coastal county whose **92** 지진해일긴급대피장소 the same commit uses as its proof the cut is now right; zero 인명구조함 is the same shape as the zero that exposed the first error. `samul_busst_point` also fell **28 to 3**, which is three bus stops for a whole county. All three may be true, because 사물주소 coverage is partial by construction and only assigned objects appear. The row is the wording, not the value: restate `:58` and the two zero-count caveats as 「0 rows matched this filter in this dataset」 and say what the dataset does not cover, so a future lap cannot read the count as a statement about the county. No value moves (critic #12, F59) | todo | true | minutes | 데이터 수집·분석·해석 · 제출 자료 |
| WFG-083 | P1 | infra | **The one document every lap is told to read first is the only document in the tree with no gate, and it has started repeating itself.** `docs/auto/CHARTER.md` now carries **section 3c twice** with two different texts for one decision: `:436` 「3c. Staging rule (NH-023, 2026-09-04)」 and `:457` 「3c. Staging discipline (NH-023, 2026-09-04)」, written by two laps hours apart, one of which names WFG-077 for the gate. The tail sections run **13, 6b, 14, 14b, 3c, 4b, 3c**, so a lap reading top to bottom meets §6b after §13 and §3c after §14b. Fix: fold the two §3c texts into one section, move the lettered sections next to their parents (3c under §3, 4b under §4, 6b under §6), and add a test that fails when two headings in `CHARTER.md` share a section number. Do not delete either text; merge and date them (CHARTER §3 rule 7). Filed by critic #13 (F61); loop hygiene, so it waits for R1/R3/R4/R7/R8/R9 under §14b | todo | true | minutes | 제출 자료 |
| WFG-084 | P1 | infra | **Cap the report.** Measured over the 24 h window `1113388..baf6962` by critic #13: 108 commits, 25,122 authored text lines (images and the generated board excluded), of which `docs/auto/reports/` took **9,000 lines in 49 new report files** (35.8 %, mean 184 lines each) and the steering documents took **3,386** (13.5 %), while everything a judge will ever see (`docs/auto/JUDGE_QA.md`, `web/`, `README.md`, `docs/auto/finals/`) took **663 lines** (2.6 %). Nineteen lines about the loop for every one line at the booth. Fix: `scripts/auto/report.py` prints a warning above 150 lines of summary and the routine prompts say the evidence belongs in the commit message, where it already is; the report keeps the gate table, the findings, the reviewer verdict and `## In plain terms`. Not a rule that deletes anything — the long-form account of a hard diagnosis stays welcome, it just stops being the default shape. This row is the structural half of critic #13's root objection; the behavioural half is the cheapest test recorded there | todo | true | hours | — |
| WFG-137 | P1 | infra | **A lap can rebuild a mechanism this repository already has, because nothing points from the problem to the machine that solves it.** The 20260906T1520Z dev lap spent most of its build writing `tests/test_withdrawn_claims.py` — a withdrawn-claim gate over five judge-facing surfaces parsed from CHARTER §3.5c — and deleted it unbuilt on discovering `docs/auto/withdrawn_claims.json`, `scripts/check_withdrawn_claims.py` and `tests/test_withdrawn_claims_registry.py`, which do the same job over 925 files. It found them by `git status` reporting `docs/withdrawn_claims.md` as **modified** rather than new: the lap had overwritten a committed 142-line document with a file of the same name, restored it from `HEAD` at once, and only then read what it had nearly destroyed. **Nothing in the path the lap actually walked names the registry.** WFG-133's row, `CRITIC_LATEST.md`, `DIRECTION.md`'s standing rule (「grep the judge-facing surfaces ... for the withdrawn string」, which describes the superseded hand-listed method) and CHARTER §3 as it then stood mention none of `withdrawn_claims.json`, `check_withdrawn_claims.py` or `make check-withdrawn-claims`; WFG-062's row does, 39 rows down a 1,858-line table, and reads as an open P0 asking for the registry to be *built*. **Done when:** either (a) `make verify`'s own gate list is summarised in CHARTER §4 so a lap reads what already runs before it writes a gate, and every gate-shaped row carries an 「already covered by」 cell; or (b) a `scripts/auto/gates.py --what-covers <topic>` that answers it mechanically — pick one, the row does not need both. **Constraints:** no new gate; this row is about discoverability. ⚠ Note that CHARTER §3.2 was **nearly broken by a write to a path the lap had not read**, and what caught it was reading `git status --short` before staging, which is CHARTER §3c's staging rule doing work it was not written for | todo | yes | one lap | infra · R9 |
| WFG-141 | P1 | infra | **Three routines file their reports under one `kind`, so the label the critic laps use to decide who must carry `Reviewed by:` does not identify who wrote the report.** `scripts/auto/report.py:227` offers `--kind` a closed list: `dev, critic, research, kickoff, red, manual`. There is no `paper` and no `ci-red`. So the paper routine (CHARTER §12), the `wfg-autoloop-ci-red` routine (§4b) and the author's own laptop sessions all land as **`manual`**. Counted here over `docs/auto/reports/`: **33** `*-manual.md` files, of which **12** are titled 「autoloop · paper」 and **2** 「autoloop · ci-red」; only 19 are the author's. **The consequence is already in the record.** Critic #26 and critic #27 each wrote that the reports in the window without a `Reviewed by:` line are 「`manual`, the author's own laptop」 and treated that as a reason it is not a finding. Twelve of them are the paper routine, which CHARTER §12 and NH-026 hold to the same gate discipline as every other lap and which **does** run a subagent reviewer — `docs/auto/reports/2026-09-06T1507Z-manual.md` carries 「Reviewed by: subagent (block → fixed)」. **No report has actually escaped the check**, so this is a wrong reason rather than a missed defect today; it is filed because the reason is what the next critic will reuse, and under it a paper lap that shipped without a reviewer would be exempted by its filename. ⚠ **Second defect, same file pair: the stamp in the report's own title disagrees with the stamp in its filename**, by up to 40 minutes — `2026-09-06T1507Z-manual.md` is titled `2026-09-06T1547Z`, `0917Z`/`0940Z`, `0323Z`/`0348Z`, `2119Z`/`2135Z`. The **email subject uses the title stamp** (the author's inbox holds 「paper · 2026-09-06T1547Z」), so an author who searches the repository for the stamp on the mail they were sent finds no such file. The author is the reader of these reports and the one who cannot navigate them. **Done when:** `--kind` gains `paper` and `ci-red` (or the routines pass a `--routine` the header prints), the existing `manual` reports are left alone as records (CHARTER §3.7, §3.2 — annotate, do not rename), the report header prints one stamp or says plainly which is which, and whatever the critic's `Reviewed by:` sweep reads is keyed on the routine rather than on `manual`. ⚠ **Third defect, same script, found the same way (critic #28):** `report.py` writes the report file and its five images **before** it runs its own prose gates, so a run that fails the gate leaves an orphan `docs/auto/reports/<stamp>-critic.md` and an `images/<stamp>/` directory in the tree. Critic #28's first attempt left `2026-09-06T1735Z-critic.md` behind when `check_forbidden.py` rejected a pragma spelling; the lap had to remove an uncommitted byproduct to get a clean tree for `--assert-head`, which is uncomfortably close to CHARTER §3.7 for something a lap should not have to think about. Either run the gates before writing, or write to a temporary path and move on success. **Constraints:** CHARTER §14b holds this behind readiness lines R1, R3, R4, R7, R8 and R9 — it is loop mechanics and it waits. Do not renumber or move existing report files | todo | yes | minutes | infra · R3 |
| WFG-135 | P1 | KCF | **Q30 invites a judge to count the irreproducible values by a field that 18 of them do not have.** The repaired T0 card (WFG-117, `fc05320`) says 「어느 부류가 몇 개인지는 `docs/NUMBERS.json` 의 `reproducibility.status` 로 지금 함께 세어 보실 수 있습니다」. Counted at `dd500e6` in one process: of the **58** entries with `reproducible: false`, **24** carry `reproducibility.status = "external"`, **16** carry `"not_reproducible"`, and **18 carry no `reproducibility` block at all** (`mlbase_rf_mean_of_folds_auc`, `mlbase_rf_fold_sd`, `mlbase_logistic_*`, `mlbase_hgb_*`, `farband_*`, and the rest of that family). So a judge who accepts the invitation and counts by that field finds **31 % of the class unlabelled**, and the card's third bucket (「이 환경에서 다시 돌리지 않은 과거 실행의 값」) is an *interpretation of an absent field*, not a recorded reason. `tests/test_judge_qa_bank.py::test_the_cards_account_of_the_irreproducible_covers_every_bucket` is correct as a gate and maps `None` deliberately, so this is not a gate defect: it is that the card promises a count the artifact cannot give. Two repairs, either closes it: (a) `build_numbers.py` writes a `reproducibility.status` on the 18 (a registry ADDITION, not an edit of a value, so CHARTER §3.2 permits it), or (b) the card's sentence says that the absence of the field is itself the third label and names it. ⚠ Do not close it by dropping the invitation: the invitation is the best sentence on the card | todo | yes | minutes | A 제출 자료, B 데이터 수집·분석·해석, R5 |
| WFG-142 | P1 | IEEE | **The repository asserts that a random split would leak, and has never measured what the honest split costs — and somebody else just did, on a fire.** `src/wildfireguardian/spread_v2_xgb/model.py:13` states in its own words that 「a random split would leak neighbouring cells of the same fire across train/test」, which is why the project uses leave-one-fire-out. That is a correct design argument and it is **unquantified here**: grepped this run across `src/`, `scripts/`, `docs/` and `paper/`, no random-split arm exists and no lap has produced one, so the repository has no number for its own honest-validation penalty. Farajpoor & Narimani (2026-08-23, arXiv:2608.22293, <https://arxiv.org/abs/2608.22293>, opened by the research lap) report **ROC-AUC 0.92 under random cross-validation and 0.75 under 1 km spatial-block validation**, same data and same model, on structure loss for 9,883 buildings in the January 2025 Palisades Fire. **Done when:** `paper/references.bib` carries that entry `verified` with the URL; the manuscript's related work states, in one sentence, that spatially honest validation is known to cost substantial skill in wildfire ML and cites it; and `docs/auto/JUDGE_QA.md` gains a card for 「랜덤 분할로 재면 AUC가 더 높지 않습니까?」 whose answer is the leakage argument the model file already makes, plus the external instance. ⚠⚠ **Constraints, and they are the whole row.** The cited numbers are a **different task** (structure loss, not next-overpass ignition), a different unit (buildings, not 500 m cells), a different geography and a different label. They may establish that the penalty exists and is large; they may **never** be written as a range this project's AUC sits inside, or beside this project's AUC on any surface. The card must say in its own voice that **this project has not measured the penalty on its own data**, because measuring it needs a refit and CHARTER §3 rule 2 forbids that before the finals. No number from the cited paper enters `docs/NUMBERS.json`, `README.md` or a printable; they stay in `docs/auto/knowledge/PYROGEOGRAPHY.md` §Update 2026-09-06 with agency and date. Proposed by the research lap of 2026-09-06 (`WEEKLY_2026-W36.md` §C1) | todo | yes | minutes | B 데이터 수집·분석·해석, R5, R8 |
| WFG-143 | P1 | science | **Screen the bin-quantisation question for the cost of one read, before B1 spends a lap on a possible null.** `docs/routing_limitations.md` §1 records that the router gates hazard at ceil-rounded 10-minute bins and that `fa_exceeds_budget` is a quantisation misnomer; `docs/auto/knowledge/ROUTING_FUNDAMENTALS.md` §7.2 candidate **B1** proposes re-evaluating the gate at the exact interpolated arrival time and counting how many of the 458 canonical Yeongdeok origins change bucket. Chawla & Sheridan (2026-04-30, arXiv:2605.00277, <https://arxiv.org/abs/2605.00277>, opened by the research lap) supply the principle that makes B1 a statement rather than a tweak: the resolution parameter of a time expansion should be the number of **critical times** at which the network changes, not the clock, and this project's hazard is piecewise-constant with a handful of slice boundaries. **This row is not B1.** It is the strict **upper bound** on B1's answer: the set of origins whose routed arrival time lies within one bin width of a hazard slice boundary is a superset of the set whose verdict can move, and it is computable from the committed routing artifact alone. **Done when:** a new script under `scripts/` writes a new artifact under a new filename (CHARTER §3.2) giving that count overall and split by current bucket, with a `docs/<topic>.md` stating method, result, caveats and what it does NOT show, and a test. **Pre-registered before the lap runs:** expected under about 20 of 458 (under ~5 %); **zero kills B1** and the null is itself a shippable sentence (「bin quantisation cannot change any verdict on this field」); **a large fraction means the binning is load-bearing and `routing_limitations.md` §1 understates it, which is a finding, not a failure**. This is an enumeration over all 458 origins, not a sample, so there is no sampling error and no power question. **Constraints:** no re-routing, no refit, no re-simulation, no committed artifact edited, no `docs/NUMBERS.json` value changed — additions only. Proposed by the research lap of 2026-09-06 (`WEEKLY_2026-W36.md` §C2) | todo | yes | one lap | B 데이터 수집·분석·해석, 과학적 기여 |
| WFG-145 | P1 | infra | **On a research day the critic's one `fix-before-next-row` item has no dev lap to be taken by, and this is the first window where that actually happened.** CHARTER §11 promises 「the next dev lap clears every `fix-before-next-row` item there before it claims a new row, so a wrong turn is caught within one lap instead of a day」. CHARTER §14 and `LOOP_CONFIG.json` -> `research_cadence_note` also give the 18:17Z slot to research on 09-06, 08, 10, 12 and 14, and the dev lap in that slot exits with 「slot ceded to research」. Critic #28 filed **WFG-138** at 17:36Z; the 18:17Z slot went to research; `git diff e95fe28..1b26c3a` changes **zero lines outside `docs/auto/`**; and critic #29 at 19:57Z is therefore measuring a window with **no dev lap in it**, re-reading its predecessor's item instead of grading it. The cap of one item per critic lap is a per-*lap* cap, so on a research day the loop spends a critic budget on a window that could not act. Five of the sprint's nine remaining days are research days. **Done when:** the loop's own text says what happens to a `fix-before-next-row` item across a ceded slot. Two candidate repairs, and the row does not choose: (i) the critic that fires after a ceded dev slot states in `CRITIC_LATEST.md` that its window contained no dev lap, carries the outstanding item forward verbatim rather than re-filing it, and does not spend a new item (which is what critic #29 did by hand, so this is codifying practice); or (ii) the research lap clears any outstanding `fix-before-next-row` item before it starts its own work, since the item is by construction prose-only and judge-facing. **Constraints:** CHARTER §11 and §14 are the author's cadence decisions (NH-016 option A) and this row does not change them, only what the laps do inside them; loop hygiene, so CHARTER §14b holds it behind R1, R3, R4, R7, R8 and R9. Filed by critic #29, 2026-09-06 | todo | yes | minutes | R3 |
| WFG-146 | P1 | infra | **A dated external citation in the new landscape note is off by one day, and the note is what the manuscript's related-work will draw from.** `docs/auto/knowledge/KOREAN_OPERATIONAL_SYSTEMS.md` dates the 사이언스타임즈 article on the NIFoS AI wildfire-response system **2026-02-12**, in §1 and again in §6. Critic #29 re-opened the URL (<https://www.sciencetimes.co.kr/nscvrg/view/menu/249?searchCategory=221&nscvrgSn=261448>) and the page gives **2026-02-13**. Every other figure in the note was re-checked at the same time and **all of them hold**: the NIFoS page carries 「산불확산예측 정밀도를 기존 대비 약 30% 향상」, 「지형 분석 정밀도를 5ｍ 수준까지」, the occurrence 76 % to 88 % plan, the 60-second suppression-strategy target and the 2030 horizon; and the 경향신문 G-DAPS page (<https://www.khan.co.kr/article/202603301116001/>) confirms 2026-03-30, the 30-minute steps, the 읍면동 unit, the **589** civil-defence alert facilities, trial operation from the following month, and **no accuracy figure**. So this is one character, not a sourcing failure, and the row says so. It is filed rather than waved through because CHARTER §3 rule 5b makes the **date** part of what licenses an external figure to be written at all, and because the same date is repeated in **WFG-144**'s own cell in this table. **Done when:** the date reads 2026-02-13 in `KOREAN_OPERATIONAL_SYSTEMS.md` §1 and §6, in WFG-144's cell, and anywhere `docs/auto/research/WEEKLY_2026-W36.md` repeats it, each with a dated note saying what was corrected and who re-opened the page (CHARTER §3.5: superseded values are annotated, never deleted). **Constraints:** prose only; no accuracy figure moves onto any judge-facing surface (`DIRECTION.md`); do not restructure the note (CHARTER §13). Filed by critic #29, 2026-09-06. ⚠⚠ **CRITIC #33, 2026-09-07T0800Z: this stopped being a knowledge-note typo. It is now printed in the booth kit, and the row's own 「done when」 list no longer names every place it lives.** In the 24 h since WFG-146 was filed as 「one character」 and parked at P1, WFG-026 wrote the survey and the panel, and the wrong date travelled into both: `docs/related_work.md:104` (「A 사이언스타임즈 report of 2026-02-12 states an intended terrain-analysis resolution of **5 m**」) and `:187`, and `docs/auto/finals/RELATED_WORK_PANEL.md:43` (「사이언스타임즈 2026-02-12 보도, 기관 계획 발표」). **That panel is `SOURCES[5]` of kit `20260907T0705Z`, 3 of its 36 pages, and the kit is in `release/kcf-finals-2026/MANIFEST.json`** — so the date is on paper in a judge's hand and on the stick. ⚠ **And the measurement is more useful than 「off by one day」.** I re-fetched the page rather than inheriting critic #29's read. Its own byline block (beside the 연합뉴스 credit) and its 저작권자 line both give **2026-02-13**, twice. The three `2026/02/12` strings on the page are Yonhap image CDN paths (`img8.yna.co.kr/etc/inner/KR/2026/02/12/…`) and the wire id `AKR20260212072300063`: that is the **연합뉴스 original's** date, which 사이언스타임즈 republished one day later. So both dates are real and they belong to different publishers, and what the repository has done is pair 사이언스타임즈 with 연합뉴스's date. Under CHARTER §3 rule 5b the agency-and-date pairing is what licenses the figure, so the fix is not a character: write 「사이언스타임즈 2026-02-13 (연합뉴스 2026-02-12 기사 전재)」, or cite 연합뉴스 with 2026-02-12, and say which was chosen. **Done when (extended):** the corrected pairing reads in `KOREAN_OPERATIONAL_SYSTEMS.md` §1 and §6, WFG-144's cell, `WEEKLY_2026-W36.md`, `NEEDS_HUMAN.md` NH-039, **`docs/related_work.md` §2 and §5, and `RELATED_WORK_PANEL.md`**, each with a dated note (CHARTER §3.5). ⚠ **The last two cannot be fixed alone:** `RELATED_WORK_PANEL.md` is a kit `SOURCES` document, so the lap that edits it rebuilds the kit at a new stamp in the same lap (WFG-152, and the freshness gate goes red otherwise), which also means neither the critic nor the research routine may take this row any more. **Reclassified as the propagation case rather than the typo case:** the loop's standing root objection is that a correction reaches the pages the loop reads and stops at the surfaces a human meets; this is that shape inverted, an *uncorrected* error travelling from a note the loop reads onto a page a judge holds, in one lap, because the row was priced by the size of its diff instead of by where its subject was about to go  ⚠⚠ **CRITIC #34, 2026-09-07T1100Z: THIS LAP'S ONE `fix-before-next-row` ITEM (CHARTER §14b — minutes, on a judge-facing printed surface). RE-VERIFIED INDEPENDENTLY RATHER THAN INHERITED FROM CRITIC #33.** I fetched <https://www.sciencetimes.co.kr/nscvrg/view/menu/249?searchCategory=221&nscvrgSn=261448> in this sandbox and parsed it. The page carries the string `2026-02-13` **twice** — in its byline (`연합뉴스 2026-02-13`) and in its 「저작권자 2026-02-13 ⓒ ScienceTimes」 line — and **not once** carries `2026-02-12` as a date: the three `2026/02/12` strings on it are 연합뉴스 image CDN paths of the form `https://img8.yna.co.kr/etc/inner/KR/2026/02/12/AKR20260212072300063_02_i_P4.jpg`, i.e. the **wire original's** date and id. So the repository pairs 사이언스타임즈 with 연합뉴스's date. I also confirmed the substance the citation carries is correctly characterised: 「지형 분석 정밀도를 5ｍ 수준까지 높인다」 and 「산불확산예측 정밀도를 기존 대비 약 30% 향상」 are agency **plan** statements in the future tense, and 「현재 76% 수준인 산불위험 예측 정확도를 내년까지 88%로」 is a target — the repository's 「agency plan statement, no metric definition attached」 is accurate. **Unfixed at `2720840`, three critic laps after it was first filed.** Live at `docs/related_work.md:104` and `:187` and at `docs/auto/finals/RELATED_WORK_PANEL.md:43`, and that third one is **3 of the 36 printed pages of kit `20260907T0953Z`, on the USB stick and in the release bundle**. 출처 명기 is a named criterion on the 제출 자료 row of both rubric tables, and CHARTER §3 rule 5b makes the as-of date load-bearing. **Done when:** all three lines read `2026-02-13`, and the kit is rebuilt in the same lap (WFG-152) so the printed page matches. ⚠ **Take it together with WFG-144**, which needs the same single `make printables`| done(20260907T1222Z) — corrected pairing 「사이언스타임즈 2026-02-13 (연합뉴스 2026-02-12 전재)」 in all seven live sites, each with a dated note; kit rebuilt in the same lap | yes | minutes, plus a kit rebuild | B 제출 자료, R5, R7 |
| WFG-147 | P1 | infra | **The research lap proposes backlog rows and rewrites the direction page, and its report is the only one in the window that says nothing about who reviewed it.** `docs/auto/reports/2026-09-06T1838Z-research.md` carries no `Reviewed by:` line. Every **dev** report in the last 24 h does (1542Z, 2132Z, 0059Z, 0711Z, 1000Z, 1313Z, 1638Z), so CHARTER §4 step 5 is being met where it is written; and every **critic** report says in that line that the routine is itself the independent reviewer. The research routine's prompt (`docs/auto/ROUTINE_PROMPTS.md`) asks for neither. That matters more for this routine than the wording suggests: the 1838Z lap proposed three rows (WFG-142, WFG-143, WFG-144), rewrote `docs/auto/DIRECTION.md` end to end, and added a knowledge note whose sources no gate can check — and one of those dates was wrong (**WFG-146**), which is exactly the class a second reader catches. **Done when:** `ROUTINE_PROMPTS.md`'s research section asks the lap to write one `Reviewed by:` line stating what read its work (a subagent verdict, or the plain sentence that this routine has no independent reviewer by design), and the next research report carries it. ⚠ **The routine prompt itself lives on the routine page at claude.ai/code/routines, not in this repository** (CHARTER §10), so the tree change here is the recorded prompt and the author pastes it at their convenience; the row is `todo` rather than `blocked(human)` because the repository half is the deliverable and it is minutes. **Constraints:** loop hygiene, so CHARTER §14b holds it behind R1, R3, R4, R7, R8 and R9; do not add a review step that would make the research lap slower than its slot. Filed by critic #29, 2026-09-06 | todo | yes | minutes | R3 ⚠⚠ **CRITIC #46, 2026-09-08T2340Z: A SECOND AND SHARPER INSTANCE, and this one carried CODE.** `docs/auto/reports/2026-09-08T2231Z-manual.md` contains the string 「Reviewed」 **zero** times (`grep -ni reviewed` returns nothing), and the commit it reports, `1fa0b7f`, changed **130 lines of `scripts/check_finals_acts.py` and 22 of `tests/test_finals_acts.py`** — the driver `KCF_READINESS` R1's tick rests on. `docs/auto/LOOP_CONFIG.json` sets `review: subagent`. Critic laps #42, #43 and #45 each declined to file against a report missing that line because those reports carried no build; this one did, so the exemption they relied on does not reach it. ⚠ **Said plainly because it cuts the other way too: the change itself is right, and I read the diff adversarially rather than trusting the commit message.** It replaces `_free_port()`'s TOCTOU race with `--remote-debugging-port=0` plus a read of `DevToolsActivePort`, keeps Chromium's stderr instead of `DEVNULL`, retries the launch once, and narrows the skip from a **message substring** to an `isinstance` check on a new `BrowserLaunchError` raised at exactly one site, so a JS throw still fails the test. `auto-gates` run **261**'s `finals-acts` job ran both steps to `success` on a clean runner. The finding is that nothing independent read it before it shipped, not that it was wrong. **Added to the `Done when`:** the certification check must cover **every** report kind, `manual` and ci-red included, and must fail a report whose commits touch `src/`, `scripts/` or `tests/` while carrying no `Reviewed by:` line. |
| WFG-149 | P1 | infra | **Every report the author has received under-counts the backlog by 34 rows, and the plan image in every email is drawn from the same counts.** `scripts/auto/report.py` `backlog_counts()` matches a row with `^\|\s*WFG-\d+\s*\|[^\|]*\|[^\|]*\|[^\|]*\|\s*([a-z\-]+)`, so the description cell must contain no pipe character. Measured at `d6cb996`: the table holds **144** `WFG-` rows and the regex matches **110**; the **34** it misses are every row whose description quotes a shell pipeline, a regex alternation or a table, escaped or not, among them WFG-007, WFG-036, WFG-113, WFG-114, WFG-117, WFG-121, WFG-126 and WFG-133. So the header line of every report (`blocked: n, done: n, parked: n, todo: n`) and `docs/auto/images/<stamp>/03_backlog.png`, which the report email calls 「The plan」, have been drawn from about three quarters of the plan since the loop started. Critic #30 hit it by writing a regex alternation into a new row and watching its own report invent a status bucket called `oracle`; the row was rewritten pipe-free and the count went from 84 to 85, which is how the defect surfaced at all. **Done when:** `backlog_counts()` parses the row by splitting on unescaped pipes from the RIGHT (the status is a fixed number of cells from the end) or by another method that does not forbid a pipe in prose; a test asserts that the number of rows counted equals the number of `WFG-` rows in the table and goes red on today's file; the four status buckets sum to the row count in the next report header. ⚠ Do not fix this by editing 34 backlog cells: the cells are a record and the parser is the defect. CHARTER §14b holds this behind R1, R3, R7, R8 and R9. ⚠ **This row is itself one of the 34**, because its description quotes the regex: `todo` reads **85** with it in the table and **85** without it, which is the cheapest possible demonstration of the defect and is left in deliberately | todo | yes | minutes | R3 |
| WFG-150 | P1 | KCF | **WFG-148 put the manuscript's second binding caveat into the README bullet that carries the 42, and two other surfaces state the same claim in the same shape without it.** Both were measured at `a52a3b8` and neither was guessed at. **(a) `paper/manuscript.md`.** `tests/test_future_aware_attribution.py` sees two claim blocks there — the Abstract's opening and the Conclusion — and both name the fire-blind control and neither says the forecast-aware arm plans on the field it is scored against. §4.5 states that caveat in the words every other surface quotes, so what the manuscript lacks is **locality**, not the claim; the same locality argument WFG-138 made for the README. It was not fixed in the WFG-148 lap for a reason that is a number: `paper/check_paper.py` reports `body_words` **8,983** against a hard fail at **9,000**, so the clauses do not fit, and the budget itself is what **NH-037** asks the author about. **(b) `docs/auto/DEMO_SCRIPT_5MIN.md:107`**, the 61-second spoken 3막, states 「458곳 중 42곳 ... 시간 인지 경로에서만」 and carries the fire-blind control inside the block and not the upper-bound clause. This one is judge-facing in the strongest sense: the student says it aloud. It was left alone in the WFG-148 lap because that row's own constraint says a fifth printables drift buys nothing, and because the prepared spoken answer already exists as `docs/auto/JUDGE_QA.md` **Q36** for the moment a judge asks. **Done when:** the manuscript's two claim blocks carry the clause and `paper/manuscript.md` is promoted into `ORACLE_SURFACES`, which turns the `strict=True` xfail `test_the_manuscript_claim_blocks_do_not_yet_name_it` red and asks to be deleted; **and** either the demo script's spoken block gains the clause in a lap that also rebuilds the printables, or the script records in writing that Q36 is where that answer lives and a test binds the two. ⚠ The manuscript half is **blocked on NH-037** until the word budget moves; the demo-script half is not blocked and should ride the next printables rebuild (WFG-134). ⚠ Do not close either half by widening `_is_caveated`: four correct reviewer sentences name the control and not the bound, and a predicate that requires both would fire on all four ⚠⚠ **RE-MEASURED BY PAPER LAP 20 (`6ecc386`, 2026-09-08T1534Z) AND STILL UNFIXABLE INSIDE THE BUDGET, recorded here by critic #44 as a step-5 update rather than a new row.** That lap wrote the correction, its independent reviewer blocked it, and it was **reverted rather than argued or shipped** — `paper/manuscript.md` is byte-identical to lap 18's. The numbers it came back with: the correct version is about **+50 words against a margin of 6**, and every cheaper shape the lap tried either inverted the bound, stranded the premise or broke a citation (one candidate would have put a fire-blind clause into a section whose opponent is not fire-blind, and turned `paper/GAPS.md` row **G8** false in the same diff). So this row is now blocked by arithmetic and not by attention, and the thing that unblocks it is **NH-037** (the word-budget proxy, open, due 2026-09-10) or **WFG-116**'s open half (the `apt` line that lets a clean clone *measure* pages instead of proxying them). ⚠ The gap is meanwhile pinned in place by `tests/test_future_aware_attribution.py`, which is `xfail(strict=True)` over exactly the two blocks that state it — critic #40's generalisation applies: a gate that binds prose to a known defect pins the defect, and this row is the one that must say so. | todo | yes | minutes | A 제출 자료, R5, R8 |
| WFG-152 | P1 | infra | **WFG-140's freshness gate has, correctly, made three judge-facing documents un-editable by the two routines that historically edit them, and nothing anywhere says so.** `docs/auto/JUDGE_QA.md`, `docs/auto/DEMO_SCRIPT_5MIN.md` and `docs/auto/finals/BOOTH_SETUP.md` are `SOURCES` of `manifest_20260907T0059Z.json`, and `tests/test_printables.py::test_the_newest_printable_is_not_stale_against_the_tree` now hashes each against the tree. **Measured here rather than reasoned:** appending one HTML comment line to `docs/auto/JUDGE_QA.md` at `3f881f6` turns that test red naming the file (`manifest ec75a1657d… tree 5c4612b1d2…`); reverted, tree clean, 21 of 22 printables tests pass either way. All three files live under `docs/auto/`, which is **exactly and only** what the critic and research routines may write, and all four entries of the drift series WFG-140 ended were critic or dev correction notes to that bank. So the next critic or research lap that adds a Q&A card — **WFG-144 is one, and it is a card the loop has already decided it wants** — will push a red tree or park on `auto/red/<stamp>`, and will discover this after doing the work. The gate is right; the missing thing is a sentence where a lap reads it. **Done when:** (a) `docs/printables.md` and `docs/auto/CHARTER.md` §4 both say that a lap editing a `SOURCES` document rebuilds the kit in the same lap, and that the critic and research routines therefore do not edit those three files at all; (b) the test's failure message names that rule and not only `make printables`; (c) `docs/auto/ROUTINE_PROMPTS.md` carries it in the critic and research prompts. ⚠ **Held behind R1, R3, R4, R7, R8, R9 by CHARTER §14b** as loop mechanics — but note (a) is one sentence and unblocks judge-facing work, so a lap already touching `docs/printables.md` should take it in passing. | todo | yes | minutes | infra (R7 keeps ticking) |
| WFG-156 | P1 | infra | *(ID 156 rather than the next in sequence: the bare number the sequence would have used is a retired count and `scripts/check_forbidden.py` refuses it in prose. The gate caught this row's own ID on its first write.)* **The test that binds `R7_ITEMS` to R7's wording is vacuous for all five names, and the lap that fixed exactly this bug for R9 wrote the reason down and did not apply it one file over.** `tests/test_printables.py::test_r7_still_enumerates_the_five_printables_this_list_resolves` asserts `name not in readiness` against the WHOLE of `docs/auto/KCF_READINESS.md`, which is 111 KB of finding history. **Measured at `0fc6130`, occurrences of each name in that file and the lines they sit on:** `evidence sheet (A4)` 2 (390, 831), `reconciliation sheet` 8 (25, 293, 365, 390, 395, 830, 831), `related-work and SFTD059T differentiation panel` 3 (27, 831), `booth checklist` 3 (391, 429, 831), `29 dispatch sheets sample` 4 (391, 831). Every one occurs outside R7's row, and every one of those outside occurrences predates this lap. So R7's definition cell could be rewritten in full and the test stays green, which is the state WFG-153(a) has to move through. ⚠⚠ **The fix is already written, in the twin.** `tests/test_finals_bundle.py:165` narrows the identical assertion to the DEFINITION cell (it splits the row on the cell separator and reads cell 1) and its comment gives the reason in five lines: the status cell is where every lap appends its own account, so a whole-row search lets the loop's own commentary satisfy the test while the definition quietly loses a name. That comment was written by the 0355Z lap, in the same lap, and says 「The same binding `tests/test_printables.py` puts on R7」. **Done when:** the R7 test splits the row and reads cell 1 only, graded red by removing one name from R7's definition cell while leaving it quoted elsewhere in the file. ⚠ CHARTER §14b holds this behind R1, R3, R4, R7, R8, R9; R9 is ticked as of critic #32 and R2, R4, R5, R6 were already, so the holding set is R1, R3, R7, R8. | done(20260907T0630Z) | yes | minutes | 제출 자료 · R7 |
| WFG-157 | P1 | infra | **Nothing in this repository checks that a backticked path in prose exists, and a judge-facing page shipped with four dead ones.** Found by WFG-026's independent reviewer, which ran `os.path.exists` over every backticked repository path in every tracked `.md`: the first draft of `docs/related_work.md` asserted `docs/model_card.md` (3x), `docs/routing.md` (2x), `docs/rescue_dispatch.md` (2x) and `docs/conformal.md` (1x) — **seven present-tense assertions, four paths, none of which exist**, on the page whose entire argument is 「we do not compare accuracy here because the measurements live over there」. ⚠ **Not a rename away:** the nearest real file to the third is `docs/rescue_routing.md`, whose first three lines are a DO-NOT-CITE / SUPERSEDED banner over exactly the numbers the sentence promised, and the conformal pointer named the wrong subject entirely. ⚠⚠ **Every gate was green over them** — `make verify`, `check_forbidden`, `check_withdrawn_claims` and the 123 registry tests. The narrow fix shipped in the same lap: `tests/test_related_work_paths.py` covers the two documents that lap wrote, occurrence-level, with a `<!-- dead-path-ok -->` pragma for record lines. **This row is the repository-wide version, and it needs a designed allowlist:** measured at `d2640ca`, **68** distinct dead paths across **50** tracked `.md` files, most of them legitimate — `BACKLOG.md` (22) and `BACKLOG_PROPOSAL_2026-09-03.md` (14) name files a later lap is supposed to create, and `.claude/skills/**` is vendored. **Done when:** a gate globs backticked repository paths in tracked `.md`, exits non-zero on one that does not exist, carries an allowlist whose every entry says why (a `done when:` placeholder, or vendored), and is graded red by planting one dead path in a non-allowlisted file. ⚠ Held behind R1, R3, R7, R8 by CHARTER §14b. | todo | yes | hours | infra |
| WFG-158 | P1 | KCF | **The Q&A bank has no card for the fact that the agency whose console we describe as a suppression tool also publishes a resident-evacuation guideline, and it is one search away from a disaster-response judge.** Found by WFG-026's independent reviewer while fact-checking the panel: the February 2026 NIFoS/행안부 announcement pairs the AI spread prediction with a **주민 대피 가이드라인** for pre-emptive evacuation of 고령자 등 안전 취약계층 ahead of the fire front. ⚠ **The panel's specific cells survive this** — a time-based advisory for a vulnerable-population class is not a per-household walking route, and `docs/related_work.md` §2's table compares the output object, not the agency's remit. **What does not survive is the framing**: `RELATED_WORK_PANEL.md` introduces NIFoS as 「진화 지휘관을 위한 콘솔」 full stop, and a judge who knows about the guideline will hear that as either ignorance or spin. **Done when:** `docs/auto/JUDGE_QA.md` carries a card for 「산림청도 주민 대피 지침을 냅니다. 그것과 무엇이 다릅니까?」 whose answer names the guideline, concedes it, and puts the difference on the unit (a class-wide advisory versus one household's route), and the panel's NIFoS sentence is widened to say the agency also publishes evacuation guidance. ⚠ The source is an agency announcement with a date and scope (§3.5b), and **no accuracy or coverage comparison may be drawn from it in either direction**. ⚠ A lap taking this rebuilds the kit in the same lap (WFG-152): both `JUDGE_QA.md` and `RELATED_WORK_PANEL.md` are `SOURCES`. | todo | yes | one lap | 설계와 방법론 · 제출 자료 |
| WFG-159 | P1 | infra | **The WFG-119 staleness gate makes a judge-facing artifact go red roughly every 18 hours for EVERY routine, including the three that CHARTER §4 says may not change code.** Raised by WFG-119's independent reviewer, who read the trade the lap disclosed as a frequency and named the consequence the lap had not: at `STAMP_MAX_COMMITS_BEHIND` = 30 and ~40-55 commits/day, `web/finals.html`'s stamp crosses the limit about every 18 h. The lap that then meets the red may be a **critic**, **research** or **paper** lap, none of which may rebuild a tracked judge-facing artifact, and CHARTER §9 sends a lap with a red baseline to `auto/red/<stamp>` instead of building. So the routine that finds it either parks itself over someone else's staleness or breaks §4 — and the same §3.2 objection the lap used to reject 「fold `make finals` into the push path」 applies to any fix that has a non-dev routine rebuild the screen. ⚠ This is a **real cost of a gate this repository chose deliberately**, not an argument to delete it: the alternative is the cryptic `Not a valid object name` failure that two laps misread, which is WFG-119's whole record. **Done when:** either the non-dev routines are told in `ROUTINE_PROMPTS.md` and CHARTER §9 exactly what to do with this one red (park, or hand it to the next dev lap, named and un-mistakable), or the dev lap's push path refreshes the stamp so a non-dev lap never meets it, or the threshold is re-argued with a measurement of how often each routine type actually hits it. Whichever is chosen, the reasoning goes on WFG-119's row too, because that is where the trade is recorded. **Constraints:** do not weaken or delete `test_the_screen_is_rebuilt_before_its_stamp_ages_out_of_this_clone` to make this go away (§3.9); do not raise `STAMP_MAX_COMMITS_BEHIND` without moving the two literals in its grader and saying why. | todo | yes | one lap | R1, R3 |
| WFG-160 | P1 | infra | **The staleness threshold shipped with a frequency estimate that this branch's own commit rate contradicts, in the same lap and the same file where the reviewer blocked a number produced exactly that way.** `tests/test_finals_screen.py:534-539` sets `STAMP_MAX_COMMITS_BEHIND = 30` and argues it 「against the branch's measured rate of ~40-55 commits/day: a rebuild buys about 30 commits, i.e. roughly 18 hours, or six dev laps at the sprint's 3-hour cadence」. The same paragraph is in `docs/finals_screen_v2.md` §4.4. **Measured by critic #34 at `2720840` on an unshallowed clone (`is-shallow-repository` = `false`, 540 commits):** `git log --since='72 hours ago'` gives **194** commits, i.e. **64.7/day**, and the per-day counts are **94** (09-04), **64** (09-05), **52** (09-06) and **29** in the first 11.1 h of 09-07 (≈63/day). Not one full day in the sprint is inside the quoted 「40-55」 band and the three-day mean is 70/day. At 64.7/day a 30-commit budget is **11.1 hours**, about **3.7** dev laps, not 18 hours and six. ⚠ **This matters because the number is the argument for the threshold**: at 3.7 laps the gate is a routine chore that fires between most laps, and at six it is occasional. The lap's own reviewer blocked it for writing 「24 commits earlier」 from two probe points; this figure came from picking the low end of a band that the data does not contain. **Done when:** the rate is re-derived from `git log` at the head being pushed and quoted with that head and window, in both the test comment and `docs/finals_screen_v2.md` §4.4, or the sentence is replaced by one that does not name an interval. Do **not** change `STAMP_MAX_COMMITS_BEHIND` to fix the prose — the grader pins it at 29/31 on purpose and moving it is a policy change that costs an argument on WFG-119. ⚠ Held behind R1, R3 and R8 by CHARTER §14b (loop hygiene) | todo | yes | minutes | R3 |
| WFG-161 | P1 | infra | **The finals-screen recurrence is now legible but not routed: when the new gate fires, the charter still tells the dev lap to stop.** WFG-119(b) added `test_the_screen_is_rebuilt_before_its_stamp_ages_out_of_this_clone`, which goes red once `web/finals.html`'s stamp is more than 30 commits behind `HEAD` — at the branch's measured 64.7 commits/day (WFG-160) that is roughly every **11 hours**, or between one dev lap in four. **CHARTER §4 step 2 says a red baseline means do not build** (「Red baseline → do not build … write a NEEDS_HUMAN BLOCKER and a `red` report, and stop」) and §3.9 sends the work to `auto/red/<stamp>`. On 2026-09-07 the only thing that stopped a lap obeying that was critic #33 writing an explicit ⚠⚠ override at the top of `CRITIC_LATEST.md`, and that override leaves with the next critic report. The WFG-119 lap amended CHARTER §4's sandbox-facts paragraph for the shallow-clone facts and did **not** amend step 2 for this class, so a lap now meets a red baseline whose failure text says 「run `make finals`」 while the standing rule says park. **Done when:** the routing is written down once, in the charter rather than in a report that expires — whichever of NH-043's options the author picks. **This row carries option B** (fold `make finals` into the push path so the stamp cannot age, which is WFG-119 done-when #2's other shape and removes the red entirely); options A and C are charter edits and are the author's. **blocked(NH-043)** ⚠ Held behind R1, R3 and R8 by CHARTER §14b (loop hygiene), which is exactly why the routing must not live in a report | blocked(NH-043) | yes | hours | R1, R3 |
| WFG-155 | P1 | infra | **The withdrawn-claim registry reads `.md` and `.html` only, so it cannot see the two file kinds where this loop's judge-facing prose is actually generated, and the one claim withdrawn on 2026-09-07 lives in both of them.** `docs/auto/withdrawn_claims.json` → `scope.extensions` is `[".md", ".html"]` and `scripts/check_withdrawn_claims.py:tracked_files()` filters `git ls-files` by that tuple. Out of reach: `docs/auto/finals/printables/manifest_*.json`, a committed artifact that carries a paragraph of prose claims about this repository and that SHIPS INSIDE the release bundle; `release/kcf-finals-2026/MANIFEST.json`; `docs/NUMBERS.json`; and every generator that authors judge-facing strings, `scripts/build_printables.py` first. **Probed at `0fc6130`:** registering the two spellings of the WFG-153 claim and running the scan exits 1 naming `docs/finals_bundle.md:86` and NOTHING ELSE, while the sentence is live in `manifest_20260907T0059Z.json:95` and authored at `build_printables.py:648`. ⚠⚠ **Widening `extensions` alone does not close it.** The generator writes the sentence as implicit string concatenation, so the phrase 「already committed PDFs that print directly」 is split across source lines at 「print 」 and 「directly」 and the scanner is line-based (`scan_text` iterates `splitlines()`). A `.py` scan would report a false green on the very instance that motivated it. **This is why `paper/manuscript.md` §3.5's sentence 「Had that sentence been a registered spelling the scan would have found it in the template, which the scan does read」 is true of `scripts/finals.template.html` and false of `scripts/build_printables.py`,** and the paper does not distinguish them. **Done when:** either the scan reads a normalised whole-file text for non-Markdown kinds rather than lines, or generated prose lives in a scanned file that the generator reads, and the manuscript sentence is narrowed to what the scan actually covers. Grade it by putting the WFG-153 spelling back into `build_printables.py` in split form and requiring red. ⚠ CHARTER §14b holds this behind R1, R3, R7, R8. | todo | yes | one lap | B 설계와 방법론 · 제출 자료 |
| WFG-203 | P1 | science | **The one new world claim in the 24 h window checks out at its source, and one word of its scope does not.** Critic #47 opened `https://biz.heraldcorp.com/article/10675702` (헤럴드경제, 2026-02-12) rather than trusting the citation, and the article carries the research lap's quotes **verbatim**: 「'준비(Ready)-실행 대기(Set)-즉시 실행(Go)'으로 이어지는 단계별 체계」 and 「화선 도달 8시간 전 산불확산 예측 정보를 바탕으로 고령자 등 안전 취약계층의 선제적 대피를 돕고, 5시간 전에는 대상 주민이 안전한 곳으로 지체 없이 이동하도록 유도할 방침이다」. **`docs/auto/knowledge/KOREAN_OPERATIONAL_SYSTEMS.md` §7 is right about the doctrine and wrong about one scope word.** Its line 63 calls 「산불확산예측 정밀도 약 30 % 향상」 a **plan** figure, alongside the 76 % → 88 % accuracy target. The article states the 30 % in the **past tense as already achieved** — 「단순 평균 풍속이 아닌 실제 산불이 이동한 경로를 계산해 적용함으로써, 산불확산예측 정밀도를 기존 대비 약 30% 향상시켰다」 — while the 76 % → 88 % genuinely is a 2027 target. **Do:** change the scope word for the 30 % only, from 「plan statement」 to 「an agency self-reported **achieved** figure as of 2026-02-12, with no published metric definition」. **The substantive caveat does not change and must stay**: no metric definition is published, so the figure is not comparable with anything this repository measures (CHARTER §3.5b). **Why it matters now:** WFG-197 proposes to carry this note's framing onto a judge surface, and one of the five judges is a public-sector disaster-response official who may know the article. **Done when:** that one scope word is corrected in place with a dated note (nothing deleted, CHARTER §3.5), and no figure from the note reaches README, manuscript, a card or `docs/NUMBERS.json` (CHARTER §13). | todo | yes | minutes | B 데이터 수집·분석·해석 · B 제출 자료 |
| WFG-204 | P1 | infra | **CHARTER §4's own sandbox-facts paragraph is stale in two ways this lap measured, and one of them tells every lap not to use the most direct route to the gate CHARTER §4b calls a gate.** (1) §4 states 「a GitHub MCP is available in the sandbox ... `curl` against `api.github.com` returns 403 here and must not be used (WFG-119)」. Measured at 2026-09-09T02:00Z on this routine's own sandbox: `curl -s -w '%{http_code}' 'https://api.github.com/repos/Sparkxt-0318/wildfireguardian/actions/runs?branch=auto/dev&per_page=20'` returns **HTTP 200** with a parseable body, and this lap read runs 256-267 with it. The environment now routes outbound HTTPS through a configured proxy, so the flat 「returns 403 here」 is **false of this sandbox** even if it was true of the one WFG-119 measured. (2) §4's shallow-clone paragraph records depths of 50, 294 and 531 and warns, correctly, that 「deepening by a guess」 is not a control — two laps guessed 120 and 250 and re-published the same wrong answer. **What it does not say is that there is a non-guessing way to deepen**, which this lap used: `git fetch --shallow-since='<the window's own start>' origin auto/dev` took this clone from **50** commits to **66** and made the whole 24 h window resolvable, because the deepening predicate is the window rather than a number. **Do:** correct both, keeping the superseded text annotated rather than deleted (CHARTER §3.5), and keep the standing rule that no ancestry or reachability claim is written while `--is-shallow-repository` answers `true`. ⚠ **Neither correction licenses `curl` over the GitHub MCP for writes**; this is about reading Actions runs. **Done when:** §4 states both, each with the command, the measured answer and the date. | todo | yes | minutes | infra |
| WFG-205 | P1 | KCF | **The critic routine is told to make 「JUDGE_QA additions」 and the repository makes that impossible for it, which is why the bank has gone four critic laps without one.** `docs/auto/JUDGE_QA.md` is one of the six `SOURCES` of the printed kit, and `tests/test_printables.py::test_the_newest_printable_is_not_stale_against_the_tree` is a hard `assert` that re-hashes every source against the newest manifest. So a critic that adds one card turns the tree **red**, and the only cure — `make printables` at a new stamp plus re-pointing `release/kcf-finals-2026/MANIFEST.json` — is an artifact rebuild the critic's own charter forbids it (「changes no code」, CHARTER §4). The result is that every judge-facing question the drill finds becomes a backlog row for a dev lap instead of a card, and the queue is now three deep: WFG-197 (the 3-12 h horizon), WFG-027 (일정), WFG-194 (창의성). **This is a mechanism gap, not a rule to relax:** the staleness gate is right and must not be weakened. **Do:** give the critic a staging file it may write freely — `docs/auto/JUDGE_QA_PENDING.md`, outside every `SOURCES` list — and have the next dev lap that rebuilds the kit for any reason merge its entries into the bank and empty it, with a test that fails when the staging file has been non-empty across more than two kit rebuilds so it cannot become a place cards go to die. **Done when:** the staging file exists with its own 「이것은 아직 카드가 아닙니다」 header, the merge step is written into `docs/auto/ROUTINE_PROMPTS.md` for the dev lap, and the ageing test is green against a mutation that leaves an entry unmerged for three rebuilds. ⚠ Filed as **P1** under CHARTER §14b: it is loop mechanics, and it waits behind the P0 block however judge-facing its consequence is. The author's half of it is **NH-049**. | todo | yes | one lap | KCF · infra |
| WFG-206 | P1 | KCF | **The 창의성 anchors are bound card-level on both surfaces, so an item can point at the wrong real file and every gate stays green.** WFG-194's independent reviewer demonstrated it rather than argued it: transposing the anchors of items 1 and 2 inside `const CREATIVE` in `scripts/finals.template.html` leaves all fourteen `tests/test_creativity_card.py` assertions passing, because every check asks whether the four paths are named SOMEWHERE in the block. `docs/creativity_card.md` §5 records the identical hole for the Q29a card and §6 records it for the screen, so this is a known limit written down twice and held by nothing. It matters because the item-to-file pairing is exactly what the block promises a judge: 「press on this claim and open that file」. **Do:** bind each item to its own anchor — on the screen the data is already structured (`CREATIVE[i].doc`), so the gate can assert the pairing directly; on the card the 근거 block would need per-item paths, which is a change to the card's shape and is the reason this is not a five-minute row. **Done when:** transposing any two anchors turns the suite red, and the mutation is recorded beside the existing ones in `docs/creativity_card.md`. ⚠ Filed as **P1** at the END of the table under CHARTER §14b and DIRECTION's rule: it is a gate-strength row and it waits behind the P0 block. | todo | yes | one lap | 창의성 (both tables) · A 제출 자료 |
| WFG-208 | P1 | KCF | **The USB a judge can carry away has seven dead pointers on its own front page and no address at which any of them can be found.** Measured at `7e222c2`: `release/kcf-finals-2026/README_KO.md` cites **15** repository paths in backticks; **7** of them exist in the tree and are **not** in the 19-file bundle (`docs/auto/DEMO_SCRIPT_5MIN.md`, `docs/auto/JUDGE_QA.md`, `docs/auto/NEEDS_HUMAN.md`, `docs/auto/finals/BOOTH_SETUP.md`, `docs/auto/finals/RELATED_WORK_PANEL.md`, `docs/finals_bundle.md`, `docs/related_work.md`), and a count of github.com in that file answers **0**. The printed kit riding in the same folder is worse in the same way: `docs/auto/JUDGE_QA.md` cites **105** repository paths, **102** exist, and **3** are in the bundle. On the student's own booth laptop every one of them resolves, because BOOTH_SETUP §1 has the full clone there; the USB is the copy a judge takes away, and on it they do not. Nothing in the repository claims the bundle is self-contained beyond the screen, so this is a gap to name and not a false sentence. | done(20260909T0630Z-lap) — critic #48's one `fix-before-next-row` item, cleared before the row. `release/kcf-finals-2026/README_KO.md` now opens with one block saying that backticked `docs/…` / `scripts/…` / `outputs/…` paths are paths **in the repository, not in this folder**, gives the repository URL once (a count of `github.com` in that file was 0) and names `auto/dev` as the branch the bundle was built from, with the booth laptop's clone named as where they do resolve. ⚠ No document was added to the bundle: it is still 19 files. ⚠ **The `make finals-bundle` green this row first stood on was false when it was written** — `build_finals_bundle.py` resolves the kit from `git ls-files`, so `--update` run before `git add` returned OK about the previous kit; the reviewer nailed it, `435c54d` re-points the manifest, and the exit-0 this row now stands on was re-run after staging, at the head that was pushed. | true | minutes | 제출 자료 (출처 명기) ⚠⚠ **THIS IS CRITIC #48's ONE `fix-before-next-row` ITEM, and it is minutes on a §14b judge-facing surface (the release bundle).** Done when: `release/kcf-finals-2026/README_KO.md` says in one sentence that backticked paths are paths **in the repository, not in this folder**, gives the repository URL once, and `make finals-bundle UPDATE=1` plus the manifest is committed so `make finals-bundle` and `check_bundle_copy.py` both stay green. `README_KO.md` is in `scripts/build_finals_bundle.py`'s `AUTHORED` tuple, so it is edited in place and not generated. Do NOT add the seven documents to the bundle: that widens the payload and the freeze. |
| WFG-209 | P1 | infra | **Two of the author's decisions reached the loop only through the routine prompt, so the charter the loop binds itself to does not contain the rule the loop obeys.** The stored `wfg-autoloop-critic` prompt binds this routine to 「CHARTER §14b ... as amended 2026-09-07 by NH-038 B」 and to 「CHARTER §14c ... NH-036 A」. Measured at `7e222c2`: a search for 14c in `docs/auto/CHARTER.md` answers **0**, and §14b on that page is still the unamended 2026-09-04 text, which caps the **number** of `fix-before-next-row` items and not their **cost**. `docs/auto/NEEDS_HUMAN.md` carries NH-036 as `open` and NH-038 as `open`; `docs/auto/decisions_seen.json` records nothing past NH-031; and the 2026-09-09T0126Z report email asked the author for both again inside a list of 15. Six lap outputs already cite CHARTER §14c as if it existed. `docs/auto/ROUTINE_PROMPTS.md` line 56, which CHARTER §9 says keeps the prompts verbatim, still carries the pre-amendment critic prompt and names a 「2026-10-10 freeze」 that CHARTER §1 puts at 2026-10-16. | blocked(NH-050) | true | minutes | infra ⚠ Blocked on purpose: registering a decision the author did not make is worse than asking once more, and the routine page is not one of CHARTER §6's declared channels. Done when NH-050 comes back: `decisions.py apply` closes NH-036 and NH-038 with channel `routine prompt`, CHARTER grows §14c and the §14b amendment in the author's own words, `ROUTINE_PROMPTS.md` is re-synced verbatim from all four stored prompts, and CHARTER §6 either names the routine page as a fourth channel or says it is not one. |
| WFG-211 | P1 | infra | **One of the two decision channels the charter promises you is a merged pull request.** CHARTER §6 names 「a PR comment on #31 in the same `NH-###: …` form」 as the second way your decisions reach the loop, and every dev routine prompt tells the lap to poll it. PR #31 was **closed and merged on 2026-09-05T14:25Z by the `promote` job** — CHARTER §4c working as decided, 「PR #31 closes once `Main` catches up」 — and carries zero comments. Measured here at `098db0c` through the GitHub MCP. So §6, `docs/auto/ROUTINE_PROMPTS.md` and every lap's step 1b describe a channel that no longer exists, which is NH-050's own shape: a route the author might reasonably use and the loop cannot hear. Same family as WFG-209. | todo — §6 and the routine prompts name a channel that exists (a fresh standing PR, a GitHub issue, or email plus the laptop session only), the `NH-###:` syntax is stated for whichever it is, and step 1b's PR poll matches. ⚠ Changing where the author is told to reply is a **question**, not a lap's choice: propose in NEEDS_HUMAN with `**Options:**` and do not rewrite §6 unilaterally. | yes | S | 제출 자료 (출처 명기) |
| WFG-219 | P1 | infra | **`scripts/build_numbers.py` accepts no arguments at all, so `--check` silently REWRITES the registry it is believed to check.** Found by the independent reviewer of the 20260909T1817Z dev lap, which ran `scripts/build_numbers.py --check` as a read-only verification and had to `git checkout --` `docs/NUMBERS.json` afterwards: the file had been replaced with a wholesale rebuild that dropped the keys every other registrar had added. Verified read-only at `3ec5737`: the script has no `argparse` anywhere — `main()` takes no parameters, reads five artifacts, and ends in `OUT.write_text(...)` unconditionally, so **every** flag is ignored and **every** invocation is the destructive one. This is a trap rather than a nuisance: `docs/NUMBERS.json` is protected by CHARTER §3 rule 2, this routine's own prompt warns 「never by re-running `scripts/build_numbers.py` wholesale, which drops other registrars' keys」, and the flag a careful reader reaches for to AVOID that is the one that does it. It cost nothing this time only because the reviewer noticed a 10k-line diff. **Done when:** the script takes `--check` and exits non-zero on drift without writing; an unknown flag is an error rather than a no-op; a test asserts that `--check` leaves the file byte-identical, graded by mutating a registry value and watching `--check` go red; and the module docstring says which registrars are additive and what a wholesale rebuild drops. **Constraints:** do not change a registered value while doing it (`check_number_collisions.py`); the rebuild path itself stays, because some lap will legitimately need it — what changes is that it can no longer be reached by accident | todo | true | one lap | loop hygiene · R5 |
| WFG-223 | P1 | infra | **The gate that CHARTER §5c calls the ratchet reads one line at a time, so a registered spelling that wraps across a source line break is invisible to it, and this loop has now measured that on a judge-facing printed page rather than in the abstract.** Filed by critic #55, 2026-09-10T0237Z, at `7dabdef`. `scripts/check_withdrawn_claims.py:121-124` reads `text.splitlines()` and matches inside `for i, line in enumerate(lines)`, so every registered spelling is a **single-line** predicate. The instance is not hypothetical and was found by the lap being reviewed, not by this one: `docs/creativity_card.md` §8 carries 「a **per-household** / walk-or-be-rescued verdict」 straddling two source lines, the 0017Z lap registered that spelling as **WC-013** in the same lap, and the scan **did not report it**; the line is green only because a human wrote the `<!-- forbidden-ok: wc013-output-object-is-per-household-en -->` pragma at `:474` by hand. `docs/creativity_card.md` §9 records the measurement and `tests/test_withdrawn_claims_registry.py` records the same limit for `WC-012`, where two halves of one sentence had to be pragma'd separately. ⚠⚠ **Why this is a row and not a note.** CHARTER §5c's whole argument for registration is that 「a lap chooses which documents to correct and will miss one; registration is what makes the machine read all 925」. A line-wrap blind spot means the machine does **not** read all 925: it reads 925 files as a flat list of lines, and any registered claim whose wording crosses a wrap is silently exempt. Prose wraps at roughly 100 columns everywhere in this repository by convention, so the blind spot is not rare, it is a function of sentence length. The defect is currently recorded **only in prose**, in a card and a test docstring, with no `todo` row behind it, which is exactly the residue pathology **WFG-218** was filed to make visible. ⚠ **This is NOT a duplicate of WFG-155**, which is about `scope.extensions` being `[".md", ".html"]` and therefore about **which files** are scanned; this row is about **how** a scanned file is read, and the two fixes do not touch the same code. It is also not WFG-157 (backticked paths) or WFG-137. **Do:** normalise whitespace across line breaks before matching, the way `WFG-138` already taught this scanner to flatten `*` and `` ` `` before matching, so a registered spelling is matched against the paragraph rather than the line; keep the per-line report so the failure still names a line number, using the first line of the match. **Done when:** a planted two-line wrap of an existing registered spelling turns the scan **red** in a graded mutation, the `WC-012` and `WC-013` hand-written pragmas are shown to be unnecessary by removing one and watching the scan go red, and `docs/withdrawn_claims.md` §4's list of measured limits records that this one is closed with the commit that closed it. ⚠ **Constraints:** the false-positive cost is the risk here, not the false negative, because a paragraph-scoped match can span a sentence boundary; state the match window explicitly and grade it against a planted near-miss that must stay **green**. Change no registry entry and delete no pragma without the graded proof above. **Placement:** P1 in table order, because CHARTER §14b holds gate-on-gate work behind R1, R3, R4, R7, R8 and R9, and WFG-155, the neighbouring limit of the same scanner, is P1 for the same reason. | todo | yes | one lap | B/A 제출 자료 · R5 |
| WFG-224 | P1 | infra | **The withdrawn-claims ratchet cannot see `docs/NUMBERS.json`, and that is where the withdrawn sentence was INSTRUCTING future prose to repeat it.** Found by the independent reviewer of dev lap 2026-09-10T0625Z, as the second nail of a `block`, and confirmed here before filing. `docs/auto/withdrawn_claims.json` -> `scope.extensions` is `[".md", ".html"]`, so `scripts/check_withdrawn_claims.py` scans 938 gated files and **zero** `.json`. The lap that registered `WC-014` measured its blast radius over all 1,134 tracked `.md` and `.html`, reported 「12 hits, all record class, ZERO in gated prose」, and that was true and not the whole answer: **25** `og_yeongdeok_*` entries in `docs/NUMBERS.json` carried the withdrawn reading verbatim inside a caveat band whose own first line reads 「Six facts travel together or none may be quoted」. That is not a passive copy — it is an instruction to quote, read by every lap and by the paper routine, whose `paper/manuscript.md` §5 IS gated. **The instance is fixed** in that lap at its source (`scripts/register_oracle_gap.py`'s `BAND`, re-run additively: 25 caveats rewritten, 0 values moved). **The class is not.** The registry's own limits page `docs/withdrawn_claims.md` §4 lists what the gate does not do and this is not on it. **Done when:** the scope question is answered in writing — either `.json` joins `scope.extensions` with the record class extended to the files that must quote (and a measurement of what that costs, because `docs/NUMBERS.json` is 453 entries of caveat prose and a false positive there turns the suite red on a registrar run), or it is deliberately excluded with the reason on `docs/withdrawn_claims.md` §4 and a narrower gate that reads the CAVEAT and `forbidden_phrasings` fields alone; either way `docs/withdrawn_claims.md` §4 gains the limit, and a test fails if a registered spelling appears in a caveat band. ⚠ Do NOT hand-edit caveats in `docs/NUMBERS.json` — they are generated by the additive registrars and a hand edit is overwritten by the next run (CHARTER §3 rule 2). ⚠ Do NOT re-run `scripts/build_numbers.py` wholesale. **Constraints:** no registered value moves; the record class may only grow with a written reason (`tests/test_withdrawn_claims_registry.py` pins its twelve declarations) | todo | true | one lap | loop hygiene · R5 |
| WFG-234 | P1 | science | **The disc null is a floor, and the opponent a fire scientist will actually name is still unscored against this truth.** WFG-228 established that the forward simulation scores **2.536×** an area-matched disc (`docs/disc_null.md`), but a circle against an elongated fire is a weak opponent by construction, so clearing it is necessary and not sufficient. `src/wildfireguardian/validation/baselines.py` has shipped `run_persistence_baseline` (`:44`) and `run_isotropic_baseline` (`:68`) — the 「Session 4」 dating in critic #58's text is NOT repeated here, because the only in-repo evidence is `docs/OVERNIGHT_REPORT_SESSION3.md:195` and a shallow routine clone cannot settle it (CHARTER §4) — and ⚠ **they are not unused** — `validation/harness.py:703-704` runs both and `compute_horizon_metrics` (`:496`) scores them with a **polygon** IoU (`perimeter_iou`, `:514`). But that grades against `load_observed_perimeter_series`, which the harness itself labels 「APPROXIMATE, reconstructed from public reporting」 — NOT the FIRMS-derived `obs_stack` raster that produced 0.394 — so the two IoU families are not comparable and no persistence null has ever been scored against the truth the headline is scored against. **Do:** carry the last observation before each forecast time forward as the prediction, score it on the canonical 181×156 grid under the SAME nearest-observation matching and the SAME `p_cut`, beside the disc. ⚠ Persistence inherits the fire's real shape, so it is a HARD opponent and may well beat the model at short horizons; **pre-register the interpretation in the claim commit and publish it whichever way it comes out** (the WFG-201 discipline, as WFG-228 did). ⚠ FIRMS gives this fire one informative frame between 0 and 333 minutes (WFG-230), so state what persistence can and cannot be defined from here. **Done when:** a new artifact, registered additively under its own prefix with a caveat, and one subsection in `docs/disc_null.md` §5 replacing the promise with the number. Moves no committed number. ⚠ **RESEARCH LAP 2026-09-10 — EXTERNAL EVIDENCE THAT THIS ROW IS THE RIGHT ONE, AND THAT ITS SCARY OUTCOME IS THE PUBLISHED NORM.** This row warns that persistence 「is a HARD opponent and may well beat the model at short horizons」 and asks the lap to pre-register the interpretation either way. An independent group reports exactly that outcome as their headline: Bokade et al. (2026-09-09, 10.5281/zenodo.22668357, opened by the research lap 2026-09-10) write that 「modern predictive frameworks utilizing end-to-end deep learning often succumb to **persistence bias**, wherein models achieve high statistical accuracy by propagating current fire states forward while ignoring fluid dynamics」, and that on their own multimodal dataset 「**neither a recurrent nor a non-recurrent network learns to advect fire past the persistence baseline**」 — which is why they abandoned learned advection for an explicit cellular-automaton physics engine. **What this changes for this row:** nothing methodological, and everything about how its result should be framed. If persistence beats the forward simulation here, that is **not** a private embarrassment to be buried in a caveat — it is a named, published failure mode of the whole model class, measured independently in the same month, and this project would be one of the few to have measured it on its own headline. Cite it in the subsection this row writes, and pre-register that framing in the claim commit alongside the numeric pre-registration. ⚠ Their finding is on **their** dataset (Uttarakhand) and is an unrefereed preprint reporting **no metric**; it licenses the framing and **no** number. | todo | true | one lap | B 데이터 수집·분석·해석 · B 설계와 방법론 |
| WFG-239 | P1 | IEEE | **An independent group published this project's whole architecture, ML spread forecast feeding dynamic evacuation routing, on 2026-09-09, and the manuscript has no line about it.** ⚠⚠ **CORRECTED IN PLACE BY CRITIC #61, 2026-09-10T2000Z at `be39dea`, and the corrected half is the row's own headline.** As filed this row read 「and **the manuscript claims the architecture as its contribution**」, cited no manuscript line for it, and it is **false at this head**, checked in three places rather than one: the Abstract at `paper/manuscript.md:29-31` says 「On a six-event dataset the **transferable contribution is the evaluation design** ... rather than the model」; the Introduction at `:84-88` says 「**We make three claims**」 and lists a measured paired contrast, a weak operating point owned as part of the result, and the evaluation design; and the Conclusion at `:821-822` opens 「A wildfire spread model trained on six fires **is not a contribution to spread modelling, and this paper does not present it as one**」, closing at `:833` with 「**The rest of the contribution is the instrument**」. **A lap acting on the row as filed would have gone looking for an architecture claim to retract that is not there, and the nearest thing it would have found is a correctly scoped contribution statement.** The real gap is the narrower one this row's own deliverables already address: **the manuscript's related work does not mention the preprint at all**, and a reviewer who knows it will ask. The deliverables below are unchanged and remain right. Bokade, Barai, Bhogle, Chapherkar & Kale (2026-09-09, preprint on Zenodo, 10.5281/zenodo.22668357, <https://doi.org/10.5281/zenodo.22668357>, record and abstract opened by the research lap 2026-09-10; full record in `docs/auto/knowledge/ROUTING_FUNDAMENTALS.md` §Update 2026-09-10) couple a U-Net flammability prior to an explicit cellular-automaton physics engine in PyTorch, then route with OpenRouteService plus a **D* Lite incremental-replanning grid fallback** that 「incrementally routes traffic away from actively predicted fires」. Same pipeline shape, same design choice, four weeks before our finals. **Do:** (a) one related-work line in `paper/manuscript.md` and one `references.bib` entry, opened at its DOI and marked `verified` by the lap that adds it (CHARTER §12). ⚠ **Cite the CONCEPT DOI `10.5281/zenodo.22668357`.** The knowledge note calls `...22668358` a 「duplicate」 and it is not one: re-fetched in critic #61's own process, the Zenodo record at id `22668357` returns `doi: 10.5281/zenodo.22668358`, which is the **version** DOI of the same record, and the same concept/version pattern holds for the Choi & Chae deposit (`22069027` returns `...22069028`). One record, two DOIs, and the concept DOI is the one to cite. (b) ⚠ **The Q&A card half moved to WFG-241 (P0, KCF), because a printed judge-facing card filed at P1/IEEE at table position 233 of 233 cannot be reached before the freeze while 15 P0 rows are `todo`.** This row keeps the manuscript and bibliography half. **The sentence both deliverables carry, and it is the row:** the delta is **not** routing against a forecast — an independent group made that same choice in the same month — it is the **output object and its measured limits**: they ship an interactive simulation environment and report **no evaluation of the routing layer at all**, while this project ships a per-point walk-or-be-rescued verdict and a page measuring how wrong it can be (`docs/disc_null.md`, `docs/oracle_gap.md`). Second half of the same line: they **replan** (D* Lite repairs a path when the cost surface changes); this project prices an edge by **when a walker would traverse it** on a time-expanded graph. A reviewer who knows D* Lite will ask why we do not replan, and the answer exists in `ROUTING_FUNDAMENTALS.md` §1 and §3 and is written nowhere a reviewer looks. **Boundaries in the same sentence, not in a footnote:** Uttarakhand, India; a road/vehicle routing stack; **no reported routing metric**; an **unrefereed Zenodo preprint** whose attached file is an IEEE conference template, which is not an IEEE publication. It validates a design class and supplies **no** number to compare. Nothing from it is registered. ⚠ **Do not imitate its closing claim** 「guaranteed safe evacuation」 — this repository exists partly to measure why no such guarantee is available. ⚠ **This is the second novelty narrowing in two research runs and they point the same way** (WFG-198 did it for route-existence): state the output object and its limits, never the architecture. Say so in the row you close, so the third instance is recognised as a pattern rather than re-derived. | todo | yes | hours | 연구 · B 데이터 수집·분석·해석, 과학적 기여 |

## Details

### WFG-001 — Clean-Linux green
Run `bash scripts/auto/bootstrap.sh` then `python scripts/auto/gates.py --mode full`
in the sandbox. Record skips with reasons (`--rs`). If a test needs a git-ignored
input, mark it `skipif` with the reason, never delete it. Compare the pass/skip
counts to the laptop baseline recorded in the kickoff report.

**Lap 20260903T0513Z (first cloud dev lap).** The sandbox half is green:
`1062 passed, 54 skipped, 0 failed, 0 errors` at `017c9ec`. It took two rounds.

The first full run was RED with 17 items, which resolved to exactly two causes,
neither of them a defect in the project's own code and neither touching an
artifact:

1. **`brotli` was declared nowhere.** `fonttools==4.63.0` is pinned, but its
   WOFF2 decoder needs the Brotli extension to open the vendored `.woff2`
   faces. Fixed upstream in `e1588b4` (`bootstrap.sh` installs it); that alone
   cleared all five `test_screen_checks` failures, 17 red → 12.
2. **Git-ignored inputs absent from a fresh clone, with guards that did not
   fire.** All 12 survivors. `data/raw/**` is ignored at `.gitignore:60`, so
   `firms_data/` never reaches a clone at all.
   - `test_photo_exif` (7, reported as ERRORS): the module-scoped `client`
     fixture builds a runner that opens `yeongdeok_2025_dem.tif`. No guard.
   - `test_osm_cache_isolation` (2): the guard read `if not d.exists(): skip`,
     but `data/cache/osm/yeongdeok_2025/` is **tracked** (it holds
     `vegetation.geojson`) while the four graphs in it are ignored — so the
     directory always exists and the skip never fired.
   - `test_baseline_freeze` (2) and `test_live_pipeline` (1): need the
     laptop-only acquisition manifests / detections CSV.

   All 12 now skip with a stated reason. Two deliberate scoping choices:
   `test_the_migrated_yeongdeok_cache_is_where_the_loader_looks` keeps its
   old-flat-path assertion running everywhere (it needs no ignored input), and
   `test_a_moved_artifact_is_actually_detected` runs its tampering assertions
   in every clone and skips only the final two, so the guard is still exercised
   in CI rather than blanket-skipped.

**Both halves of "done when" are now satisfied.** The CI half settled after the
push: `auto-gates` **run 8** on `c42287e` concluded `success` at 2026-09-03
05:17:52Z — the first green run this workflow has ever had. Before it, runs
1/2/3/5 were cancelled by the concurrency rule as pushes stacked, and runs 4 and
7 concluded `failure`; run 7 on `017c9ec` is the clean-machine confirmation that
the twelve guards were the remaining blocker.

**The count gap is explained, and it is benign.** The sandbox reports **1116**
test outcomes against the laptop baseline's **1120**
(1116 passed / 3 skipped / 1 xpassed). Cause:
`tests/test_empirical_interaction.py` calls `pytest.importorskip("xgboost")` at
**module level**, so without the optional `legacy` extra its **five** tests are
never collected and the module contributes one skip instead — 1115 collected + 1
module skip = 1116, and the shortfall of 4 is exactly those 5 tests minus the 1
skip. `xgboost` is deliberately not a core pin (`requirements.txt` keeps
`xgboost==3.2.0` commented; it lives in the `legacy` extra), so the sandbox is
behaving as designed and no test is silently lost.

While confirming that, a stale note in `requirements.txt` was corrected: it
warned that `test_lofo_holds_out_whole_fire` imports xgboost *without* a guard
and therefore ERRORS on a clean clone. The guard was added at
`tests/test_spread_v2_xgb.py:222` and it skips. The note now says so, and records
the module-level collection effect above.

**Lap 20260903T0534Z — the same two findings, reached independently, plus one
change.** This lap read `auto-gates` run
[33718108879](https://github.com/Sparkxt-0318/wildfireguardian/actions/runs/33718108879)
directly and diagnosed the count gap the same way, before seeing that the lap
above had already written both up; nothing above is superseded. What it adds:

- [`docs/clean_clone_gates.md`](../clean_clone_gates.md) — the standing artifact
  for this row: method, gate table, the 54 skips grouped by cause, the
  1,115 + 1 = 1,116 vs 1,120 arithmetic, and a caveats section saying what a
  green clean clone does NOT show (the 37 data-gated skips are exactly the tests
  that re-derive numbers from raw inputs, so it proves consistency, not
  reproduction).
- One test split. `test_weather_basis_is_derived_from_committed_data_not_a_literal`
  asserted both that the basis is *derived* from the git-ignored archive and that
  it is *never typed into the source*; a single `skipif` took the second out of
  every clean clone, which is exactly where hard-coding the date is the tempting
  fix. Now two tests, and the guard runs everywhere (+1 collected).
- `scripts/auto/report.py`'s backlog counter matched only a bare status word, so
  every row the loop had started or finished vanished from the counts in its own
  report.

⚠ **This lap and the one above overlapped almost entirely.** Both fetched
`auto/dev` at `017c9ec` and both fixed the same five tests. This one discarded
its duplicate (preserved at `auto/lap-b1989d5-superseded`, nothing deleted)
because the landed fixes are better: they keep the old-flat-path and tamper
assertions running in CI rather than blanket-skipping. Filed as **NH-007** —
the charter's "the later lap takes the next row" cannot work while a row is
claimed only by the commit a lap pushes at the end.


### WFG-022 · P0 · KCF · Five questions to the KCF 운영사무국 — human
- **What:** one call (070-5066-1963) or email (koreacodefair@gmail.com): (1) finals date — the student's notice says 10.18 with 10:30 judging start; https://www.kcf.or.kr/84/?bmode=view&idx=171991931 (2026-06-24) says 본선 10.24 (토) 김대중컨벤션센터, 결과 10.30; (2) the 참가부문 (애플리케이션/실생활 도구 vs SW 연구) this entry is registered under; (3) whether restating 기여 ② from "위험 시한 순 트리아지 목록" (서식1 line 13) to the per-home closure time `ingress_survival_time_min` is within 운영요강 p.9 (첫 제출 당시의 작품 목적·주제에 반하지 않는 범위); (4) how AI-assisted / autonomous-agent development must be disclosed given 심사개요 "대리(표절)작 판정 시 심사 제외 가능" and p.18 "허가받지 않은 제3자의 참여나 개입"; (5) what 제출 자료 is scored at the finals (기제출 서식2 only, poster, handouts) and the poster specification.
- **Rubric rows:** Pass/Fail (서류 구비, 대리작) and every scored row indirectly.
- **Effort:** hours. **agent_doable:** false (external contact).
- **Constraints:** CHARTER §6 (contacting anyone is human-only).
- **Done when:** NH-006 closed with the confirmed date; a new closed NH entry records the four other answers verbatim; `docs/auto/RUBRIC.md` "Which track applies" updated by the loop (WFG-024).

### WFG-023 · P0 · infra · Protect `Main`, ratify the branch, take the open decisions — human
- **What:** (a) GitHub → Settings → Branches: protect `Main` (require PR, require `auto-gates` status, block force-push); routines can push to any unprotected branch that carries only the owner's commits (https://code.claude.com/docs/en/routines), so this is the only real guard; (b) write one line ratifying `auto/dev` as the working branch (replaces `round3-dev` in HANDOFF §5.1); (c) decide in writing: which routing field the finals says — recommendation: canonical 414/42/2 (n = 458) with the reconciliation sheet — and whether the corrected-DEM LOFO (`spread_v2_lofo_dem_corrected.json`) ever replaces `spread_v2_lofo.json` — recommendation: not before the finals, keep as a separate lineage; (d) approve or veto the refuge-density decimation experiment (HANDOFF §4 says the user confirms before it starts); (e) close NH-001 (report-email secrets) and NH-002 (`@claude` app) either way.
- **Effort:** under an hour. **agent_doable:** false.
- **Done when:** `gh api repos/Sparkxt-0318/wildfireguardian/branches/Main/protection` returns 200; NH entries closed with the decisions; WFG-024 unblocked.

### WFG-018 · P0 · KCF · 제출본 대비 정본 reconciliation sheet as NEAR-labelled prose
<!-- forbidden-ok: 44× -->
- **What:** `docs/submission_reconciliation.md` (Korean, one printable page): for every number in the submitted 서식1/서식2 that the repo has since superseded, one row: submitted value (with its lineage and denominator) → current canonical value (with its artifact and commit) → why it moved → what did not change. Rows: 438/18/3 of 459 (`routing_demo.npz`, reverted-run field) → 414/42/2 of 458 (`routing_demo_canonical.npz`), and separately 440/17/3 of 460 (PHASE-2 flat/slope re-run) — never combined, no "N reclassified" (§5.24), 18/459 = 3.92% not 3.70%; 핵심 영역 241→244 (reverted field) vs core growth 249→1,036 cells on the canonical field; "DEM 경사 미적용" → applied at 60 m sampling (+26.6% traversal time, null on buckets); RF 0.920 ± 0.036 → 0.914 ± 0.044, GBM 0.889 ± 0.107 → 0.894 ± 0.092 (`ml_baselines.json`; note the committed headline 0.890 ± 0.107 is `spread_v2_lofo.json`, a different artifact); far-band 0.925 → 0.904 ± 0.100 (n = 3 t-CI 0.66–1.15) and corrected-DEM pooled far-band 0.8408 vs 0.877; PR-AUC/Brier "미산출" → AP 0.169 vs 0.0197, Brier 0.0183, ECE 0.0086; 6→24→66 (서식2 Fig. 3) is correct, README:731's "6 → 34" is a typo (`rescue_verify.json` `unreachable_delay_row_cutoff_0p7` = [6, 11, 24, 51, 66]); headline AUC lineage trained on the −497 m sea-filled DEM, corrected re-run +0.0048 not adopted; contribution ② restated 2026-08-10; the 44× severity-vs-direction ratio withdrawn (README/MODEL_CARD) — explain the reason in two sentences (six-feature sum vs single variable; ERA5 28 km). Add a 30-second spoken version of each row for the student.
- **Rubric rows:** 제출 자료 (논리적 구성), 데이터 해석.
- **Effort:** hours. **agent_doable:** true.
- **Constraints:** every retired literal must carry a `scripts/check_forbidden.py` NEAR label or the file fails `make check-forbidden`; do NOT register retired values in `NUMBERS.json` (§5.5, §5.20; `check_number_collisions.py` has no "submitted lineage" class); do NOT put reverted-field figures on `web/finals.html` unless the student changes gate policy in writing; do not edit the 서식 files (outside the repo). Every 영덕 rate carries the 32.6% caveat.
- **Done when:** the file exists in Korean, `make verify` and `make check-forbidden` pass, every current value in it greps to a `NUMBERS.json` key, and `docs/auto/JUDGE_QA.md` §0 links to it instead of duplicating the table.

**Lap 20260903T0653Z — done.** `docs/submission_reconciliation.md` (Korean, printable as
one double-sided sheet: eleven-row table on the front, 30-second spoken lines on the back),
`tests/test_submission_reconciliation.py` (4 tests), JUDGE_QA §0 repointed, `gates.py
--mode full` exit 0. Four things worth carrying forward:

1. **18 values registered** to make the done-when's "every current value greps to a key"
   true: `mlbase_*` ×6, `farband_*` ×4, `calib_*` ×4, `canonhaz_core_growth_pct`,
   `demcorr_mean_of_folds_delta` / `_pooled_delta`,
   `rescue_unreachable_delay_row_cutoff_0p7`. Registry 260 → 278. Retired values are NOT
   registered, per the constraint.
2. **A new arm, `A_reconciliation`**, declared in `docs/arm_protocol.json`. Registering
   already-computed values still needs an arm, and it cannot be `A` — that one is frozen
   and `check_arm_isolation` stops a new key claiming it.
3. **249 → 1,036 core cells cannot be registered.** Their key is `cells_ge_0.5_per_slice`
   and the registry's path resolver splits on `.`. The growth percentage registers; the
   counts are cited to the artifact and the limitation is stated in the sheet.
4. **A key name is part of the collision gate.** `farband_pooled_auc_committed` turned
   three unrelated lines red on the anchor words "committed / pooled / auc"; renaming it
   `farband_pooled_auc_precorrection` cleared them without editing a document.

### WFG-019 · P0 · science · Operating-point evidence package (incl. the threshold-guarantee negative result)
- **What:** new script `scripts/operating_point_evidence.py` reading `data/processed/spread_v2_lofo_oof_cells.csv.gz` (151,904 rows, 2,989 positives; per-fire positives 8/24/34/652/769/1,502) and `data/processed/oof_classification_metrics.json` (51-point PR curve). Compute and write `data/processed/operating_point/per_fire_recall.json`: per fire, `n_positive`, recall/FNR at the committed 0.3 (expected 1.000/1.000/1.000/0.977/0.959/0.544 for gangneung/hongseong/miryang/uiseong/uljin/yeongdeok — recompute, do not copy), max OOF probability per fire (expected 0.024/0.296/0.369 on the three small fires), pooled recall 0.138 and mean-of-folds 0.0867 cross-checked against the existing keys `oof_pooled_recall_at_operating_threshold` / `oof_mean_of_folds_recall_at_operating_threshold`. Then the nested leave-one-fire-out threshold-calibration table: for each held-out fire, λ chosen so the other five fires' OOF FNR ≤ 0.2 (a) without and (b) with the conformal finite-sample correction 1/(n+1) with n = 5 (Angelopoulos et al. 2024, https://proceedings.iclr.cc/paper_files/paper/2024/file/f3549ef9b5ff520a7e41ff3cc306ab2b-Paper-Conference.pdf), reporting held-out FNR and the fraction of all cells flagged (prevalence 1.97%). State the leakage caveat (OOF probabilities for fire g come from models that saw fire f). Write `docs/operating_point.md`: "AUC ranks, recall counts"; the per-step `advance_threshold` (forward simulation) vs cumulative-field `p_cut` (router) distinction so recall is not misread as the routing field's miss rate; and the conclusion as a negative result: no finite-sample FNR guarantee is possible at n = 6 and any bound-satisfying λ turns the hazard field into a near-blanket mask; the operating point stays a ranking-driven forward simulation and 0.3 stays unchanged. Append the same section to `docs/MODEL_CARD.md` (append only; no committed value edited). Draw the PR-curve figure with the 0.3 point and the F1-optimal 0.14 point to a NEW path (e.g. `docs/figures/auto/pr_curve_operating_point.png`; §5.3 forbids regenerating existing figures, not adding). One test with a synthetic OOF frame.
- **Rubric rows:** 데이터 수집·분석·해석 (수학·통계의 적절한 적용, 논리적 해석), 설계와 방법론.
- **Effort:** one lap. **agent_doable:** true.
- **Constraints:** new filenames only; register every number; the two R3 verdicts disagree on the exact λ and held-out FNR values (different calibration conventions) — state the convention used and let the artifact be the source; never adopt any λ.
- **Done when:** artifact + registered keys + doc + MODEL_CARD appendix + figure + test; `make verify` green; JUDGE_QA question 1 cites the new keys.

**Lap 20260903T1224Z — done.** Everything the row asked for exists and the whole
done-when clause holds. New: `scripts/operating_point_evidence.py`,
`data/processed/operating_point/per_fire_recall.json` (git-tracked; `.gitignore`
carries an explicit negation), `docs/operating_point.md` (Korean),
`docs/figures/auto/pr_curve_operating_point.png`, a `MODEL_CARD.md` appendix
(append-only, nothing above the rule edited), 17 registry entries on a new arm
`A_operating_point`, and `tests/test_operating_point_evidence.py` (10 tests, a
synthetic three-fire frame pinning the lambda convention).

Every expected value reproduced exactly from the committed OOF file: per-fire
FNR at 0.3 of 1.000/1.000/1.000/0.977/0.959/0.544 and ceiling probabilities
0.0241/0.296/0.369 on the three small fires. The script refuses to write if its
recomputed pooled and mean-of-folds recall disagree with Session 18's
`oof_classification_metrics.json`.

**The calibration result came out sharper than the row expected, and in a
different shape.** The row (and the brief) anticipated "any bound-satisfying
lambda turns the field into a near-blanket mask". That is true of the corrected
column only. The pair is the result: *without* the finite-sample term the bound
is met on the five calibration fires and then **broken on 3 of 6 held-out
fires** (worst 0.750), so naive calibration does not deliver what it claims;
*with* `1/(n+1)` = 0.167 — which at n = 5 calibration fires eats **83 % of a
0.20 budget** — the bound holds 6 of 6 and a feasible lambda flags **26–46 % of
all cells** against a 1.97 % prevalence. So: the guarantee or a usable field,
not both, at n = 6. Neither column is a valid guarantee (exchangeability breaks
twice; stated in the artifact, the doc and the MODEL_CARD appendix), which makes
the corrected column an optimistic bound — and even the optimistic version is
unusable, which is why the conclusion stands.

Convention stated as the row required (the two R3 verdicts differed here):
strict comparison, largest feasible lambda. No lambda was adopted; no default
moved. This lap's numbers therefore differ from both verdicts' lambda figures
and the artifact is the source.

Two gate lessons went to `docs/auto/MEMO.md`: a `pytest.skip` guard on a
git-TRACKED artifact hides a defect in a green summary line (one full-suite run
read 1,071/60 against three neighbours at 1,077/54, delta exactly the six tests
behind the guarded fixture; the fixture now asserts), and sibling registry keys
collide with each other when a 출처 table lists key and value on one line.

### WFG-002 · P0 · KCF · Judge Q&A bank v2 — revise
<!-- forbidden-ok: 44× -->
- **What:** `docs/auto/JUDGE_QA.md` grows to ≥ 30 questions grouped by judge type (software professor, disaster-response official, fire scientist, ML reviewer, statistician). Add the ten answers in `RESEARCH_BRIEF.md` §(c) plus: the 서식1 44× contradiction; the 기여 ② restatement (with the sentence to use only after WFG-022 is answered); "why walking routes when 84.5% drove" (from WFG-020); "what did you build yourself" (from `docs/auto/CHARTER.md` §9 and `ROUTINE_PROMPTS.md`); "is LOFO honest when 영덕 trains on 의성·안동's same-week rows"; "are any refuges designated 대피소" (OSM POIs; national shelter file not yet cross-checked — NH item). Purge: "10–14 s" (say about 25 s, HANDOFF §9), "five fabricated citations" (§4-B is five instructions carrying non-existent findings), "seven times 영덕's" (24.73/9.17 = 2.7×), "every fire we could test" (3 of 6; 영덕 excluded), the "40 minutes 안동→영덕" factoid, "Li et al. 2019", "Ronchi et al. 2021", "Lee et al. KJRS 2025" (Sung et al.). Each answer: one sentence, artifact path or registry key, "what does not exist" line, and a DRAFT label (the student rewrites in their own words).
- **Effort:** one lap. **agent_doable:** true.
- **Constraints:** no number not in `NUMBERS.json` or a committed artifact; `check_number_collisions.py --report` clean (the 24.73% share must stay marked as its own quantity, commit 953eb6c); Korean.
- **Done when:** ≥ 30 questions; a grep for the purged strings returns nothing; the critic lap's drill finds no P0 question without a file; the student has been told which answers are drafts.

**Lap 20260903T1536Z — done.** `docs/auto/JUDGE_QA.md` is now a **33-question Korean
bank** grouped by judge type (ML·통계 8, 산불 과학자 7, 재난대응 실무자 7, 소프트웨어 교수 6,
출처·규정·AI 5) and **tiered** T0 ×14 / T1 ×13 / T2 ×6 with a drill table in §6. Every
answer carries a `근거:` line (path or registry key) and a `없는 것:` line.
`tests/test_judge_qa_bank.py` (18 tests) gates it; `gates.py --mode full` exit 0
(1,106 passed / 54 skipped). Four things worth carrying forward:

1. **The count was the wrong done-when, and the fix was tiers plus tests.** A 30-answer
   document is more for the student to explain, not less — the opposite of CHARTER §9.
   Two of the three done-when clauses are now mechanical rather than the critic's
   opinion: `test_every_t0_question_points_at_something_that_exists` ("no P0 question
   without a file") and `test_the_stated_tier_counts_match_the_tags` (the anti-padding
   guard — adding a question forces the header's numbers to move).
2. **`test_every_registry_key_named_in_the_bank_exists`** is the strongest one: all 66
   backticked key-candidates on 근거 lines resolve in `docs/NUMBERS.json`. A key that
   reads plausibly and does not exist is HANDOFF §4-B's class and nothing else catches it.
3. **A number inherited from v1 had no source.** "46 snapped to the network" (beside the
   real `mr_yeongdeok_shelter_pois` = 50) appears nowhere in the tree; the only "46개" in
   the docs is `global_portability.md`'s *missed*-POI count. Replaced with
   `docs/multi_region.md`'s committed tag breakdown, which answers the judge's question
   better: 33 `leisure=park` + 17 `amenity=shelter` of which 16 are `shelter_type=gazebo`,
   and `amenity=community_centre` = 0 in two of three regions.
4. **The purge list must be derived, not retyped.** The independent reviewer blocked: the
   hand-copied `PURGED` dict had dropped the 40-minute 안동→영덕 factoid — the one item on
   the list the research brief marks "(no source)", i.e. the fabricated event rather than
   a superseded number — while the report claimed it was gated. Fixed with regexes (the
   literal "40분" is a substring of the legitimate "240분") plus
   `test_the_purge_list_covers_what_the_row_actually_ordered`, which parses the quoted
   phrases out of `RESEARCH_BRIEF_2026-09-03.md` and `BACKLOG_PROPOSAL_2026-09-03.md` and
   asserts each is covered. Both new gates confirmed by mutation.

Left for WFG-020: the Greenpeace survey figures in Q17 are labelled 「미등록」 in the
document itself, with the instruction to cite the report by name at the booth rather than
present them as repository-derived, until that row registers them with a sha256.

### WFG-004 · P0 · KCF · SSOT sweep — revise
- **What:** as written, plus: fix `README.md:731` "6 → 34" to the [6, 11, 24, 51, 66] row from `rescue_verify.json` (registered `rescue_unreachable_count` covers 24; register the delay row if not already); reconcile `docs/fold_sizes.md` ("pooled AUC is the primary indicator") with `docs/NUMBERS.json`'s note ("MEAN-OF-FOLDS, not pooled … never present one as the other") — one statement of which is primary, annotated in both; confirm README lines 197/494 say SFTD (done at 30ed00a); annotate the two HGB means (0.890 ± 0.107 in `spread_v2_lofo.json` vs 0.894 ± 0.092 in `ml_baselines.json`) with why they differ — **answered by WFG-018**: `ml_baselines.json`'s `hist_gbm` row IS the corrected-DEM lineage (it equals that file's own `dem_corrected_reference.mean_of_folds`), so they are two lineages, not two readings of one; both are now registered (`mlbase_hgb_mean_of_folds_auc`, `lofo_mean_of_folds_auc`) and `docs/submission_reconciliation.md` §"헷갈리기 쉬운 세 지점" states it. **Found by the WFG-018 lap, still open, not fixed there:** `docs/HANDOFF_ROUND3.md` §1.3 says the 의성·안동 time-aware-only share is "nearly **seven times** Yeongdeok's" — that ratio is 24.73/3.70 = 6.7 on the RETIRED share; against the canonical 9.17 % it is 2.7×. The same deprecated phrasing sits in `docs/auto/JUDGE_QA.md` Q7 (already on WFG-002's purge list). HANDOFF is not, so fix it here.
- **Effort:** one lap. **agent_doable:** true.
- **Constraints:** annotate, never delete; README Round-2 section untouched; `make verify` after every prose edit.
- **Done when:** `check_number_collisions.py --report` shows 0 unmarked hits; an `ssotize` audit report is committed listing every quantity with its single home.

**Lap 20260903T1622Z — done.** `docs/ssot_audit_2026-09-03.md` is the audit report;
`make verify` exit 0 (collisions 0 unmarked); `tests/test_rescue_lineage_ssot.py`
(6 tests) is the new gate. Registry 295 → 296. Four things worth carrying forward,
two of which contradict this row as written:

1. **"README:731 is a typo" was false, and the row inherited the error.** `6 → 34`
   is the real bracket of the superseded pre-flip **452-series** baseline
   (`data/processed/rescue_baseline_synthetic/rescue_verify.json` →
   `[6, 15, 20, 25, 34]`, superseded N = 452). The defect was a **lineage mix**: that value
   sat in a paragraph whose other figures (143 origins, 6.12 → 1.71) are
   439-series. `docs/rescue_routing.md` (do-not-cite banner, superseded N = 452) and
   `docs/REPORT_ROUND2_P1.md` ("synthetic lattice" vs "real roads") were **right
   all along** — exactly two documents broke the rule, and one of them was
   WFG-018's own reconciliation sheet, which had hardened the typo theory into
   「34는 어느 산출물에도 없습니다」 plus a spoken booth line. Corrected in all four
   places, including the two research files that carried the misdiagnosis.
2. **The row's second premise was also wrong, and the real conflict is sharper.**
   `docs/fold_sizes.md` and `docs/NUMBERS.json` do **not** disagree — both say
   pooled AUC is primary. The contradiction is `docs/MODEL_CARD.md`, which calls
   pooling "**not** the generalization estimate". Both are right about different
   questions (discrimination over the evidence vs generalization to an unseen
   fire); §3 of the audit is now the single home for that, and both files point at
   it. Found while fixing it: **the metric three documents call primary had no
   registry key at all** — pooled 0.905 lived only in prose and inside other
   entries' caveat strings, while the non-primary mean-of-folds was the registered
   headline. Now `lofo_rowweighted_pooled_auc` on new arm `A_ssot`. **No headline
   moved**; which metric leads is the author's call (CHARTER §6).
3. **HANDOFF's "nearly seven times" was self-contradicting.** Both occurrences sat
   two lines below the canonical shares they contradicted: 24.73 / 9.17 = **2.7×**,
   while 6.7× is 24.73 / **3.70** on the retired share. Direction unchanged.
4. **The collision gate structurally cannot see this failure class** and was green
   over README:731 at this lap's baseline. Hence the lineage test, which found two
   more instances the moment it was switched on — and two further ones
   (`BACKLOG_PROPOSAL_2026-09-03.md`, `results_rescue_draft.md`) after the
   independent reviewer showed the first version was far weaker than described:
   its banner exemption let 15 files through on an incidental word, and its label
   list accepted bare 이전/정본. Now a named 2-file ratchet with reasons.
   Generalising it beyond this one quantity is WFG-030's job, not done here.

### WFG-039 · P1 · infra · The suite's own counts depend on whether it has run before
> **This answers the open question in WFG-038.** That row recorded the same
> six-test signature (1,158 outcomes both times, six moving between passed and
> skipped, ALL GREEN printed for both) and said plainly: "The identity of the
> six is open." They are `test_srtm_dem.py` ×4 plus
> `test_validation_robustness.py:57` and `test_validation_session3.py:171`, all
> guarded on `data/raw/dem/srtm/N36E129.hgt`. WFG-038's ruled-out hypothesis
> (`data/cache/*.nc`) was the wrong cache. Both rows should be worked together:
> WFG-038 makes the drift visible, WFG-039 removes its cause.
- **What:** `tests/test_srtm_dem.py` documents that if `data/raw/dem/srtm/N36E129.hgt`
  is absent "the network-download step in `data_io.raster._download_srtm_tile` will
  fetch" it — and something in the suite does, from
  `elevation-tiles-prod.s3.amazonaws.com`, 8,473,868 bytes gzipped and 25,934,402 on disk, during the run. The path is
  git-ignored, so in a fresh clone six tests skip on the first run
  (`test_srtm_dem.py` ×4, `test_validation_robustness.py:57`,
  `test_validation_session3.py:171`) and pass on every run after it. Measured this
  lap: 1,088 passed / 60 skipped on the first `gates.py --mode full`, then
  1,094 / 54 on three consecutive full runs, with total outcomes 1,148 in all four.
- **Why it matters:** (a) every suite count this project has recorded is an
  unlabelled mixture of first-run and re-run readings, so cross-lap count
  comparisons — the check the loop uses to prove no test was lost — have been
  comparing different quantities; (b) an unattended loop and the `auto-gates` runner
  make a live network fetch nobody declared, which contradicts NH-004's "the loop
  works from committed snapshots"; (c) on the booth laptop with Wi-Fi off (the
  finals condition, R3) those six will skip, and nobody has recorded that as
  expected.
- **Options:** make the download explicit — an opt-in marker or env flag, default
  off, so a clean run skips deterministically and states why; or vendor the tile as
  a snapshot if its licence allows. Do **not** delete the tests.
- **Effort:** hours. **agent_doable:** true.
- **Constraints:** no committed artifact regenerated; `data/raw/**` stays
  git-ignored; the six tests keep their assertions (CHARTER §3.7, never delete).
- **Done when:** two consecutive full runs in a fresh clone report identical
  pass/skip counts; the expected clean-clone count is written down with the word
  FIRST-RUN or RE-RUN beside it; `docs/clean_clone_gates.md` updated.

### WFG-020 · P0 · KCF · Greenpeace 2026 survivor survey as evidence + the "85% drove" answer
- **What:** fetch the Greenpeace Korea 2025 영남 초대형 산불 피해 실태조사 최종보고서 (2026-03) PDF (URL in `RESEARCH_BRIEF.md` §(c) Q5); record its sha256 and the quoted figures with table/page references in `docs/evidence/greenpeace_2026_survey.md`: n = 300 (296–299 answering), 63.9% aged 60–79 and 17.9% ≥ 80, 90.0% evacuated (영덕 98.0%), car 84.5% (246/291), foot 3.1% (9), boat 2.7% (8), own car 60.1% of 278 car users, neighbour ~17%, relatives 15.1%, 재난문자 received 62.3% (영덕 48.0%), 마을방송+주민 237 vs 문자 112 mentions, 87% felt life threatened, 영덕 36% living alone, 영덕 foot 1.0% / boat 8.2%, 영덕 deaths 10 (mean age 84). If `scripts/build_numbers.py` supports literal/evidence entries, register them as such; otherwise keep them as documented literals with the sha256. Write the one-paragraph answer: the walking layer is a classifier for who cannot self-evacuate feeding the rescue layer and the 이장/마을방송 channel the survey shows worked; consistent with 서식1 §1's own question. Explicitly do NOT present "40% no own car" as a bracket on the 0.30 immobility rate (car-less ≠ immobile; 60.1% is a share of car users); the immobility answer remains 서식1 §4's f = 0.15/0.30/0.45 sensitivity. State survivor bias.
- **Effort:** hours. **agent_doable:** true if the PDF is reachable through the sandbox proxy (greenpeace.org is not on the trusted allowlist — UNVERIFIED); fallback: NEEDS_HUMAN asking the student to drop the PDF under `data/raw/evidence/` (the scratchpad copy `research/greenpeace_2025.txt` is a text extraction, not the PDF).
- **Constraints:** no number derived beyond the quoted ones; the KCF purpose is unchanged.
- **Done when:** evidence doc with sha256 exists; JUDGE_QA carries the answer with the 영덕-specific figures; `make verify` green.

**Lap 20260903T1821Z — done.** The PDF was reachable from the sandbox (HTTP 200,
3,406,169 bytes), so the row's NEEDS_HUMAN fallback was not used. Everything the
done-when asked for exists: `docs/evidence/greenpeace_2026_survey.md` (Korean, with
the sha256), JUDGE_QA Q17 carrying the 영덕-specific figures, and `make verify` green.
`gates.py --mode full` exit 0 (1,128 passed / 54 skipped, RE-RUN). Five things worth
carrying forward.

1. **The row's own figures had never been checked against the report.** They reached
   the repository through a scratchpad text extraction that no longer exists in the
   tree. Writing them down beside the PDF's sha256 would have produced a number that
   reads as sourced while the checksum authenticates the PDF and not the
   transcription — HANDOFF §4-B's class wearing the costume of provenance. So
   `scripts/extract_survey_evidence.py` does the transcription: digest first, then
   parse the report's own answer tables, then refuse to write unless the parse agrees
   with both the claimed value and the table's internal arithmetic. Both refusals
   confirmed by mutation.
2. **Reading the source changed three claims, none of them in the row.** The sample is
   임의·유의·눈덩이표집, so no interval belongs on any of these figures and the report
   asks to be read as 탐색적 자료; 「전체」 is a 100/100/100 equal allocation, not a
   영남-wide rate; and the 영덕 death toll (10, mean age 84) is the report re-citing
   영덕군's notice (p.9 fn.2), not a survey finding — it lives under
   `secondary_citation` so it cannot be read as one.
3. **Two things the repository had already written about this survey were wrong**, both
   in `research/sweeps_2026-09-03/R3_science_gaps.md`: "the 8 Yeongdeok dead" (it is
   10), and "≈40 % had no own car" offered as an empirical bracket on the 0.30
   immobility rate — the exact misreading this row was written to forbid. 60.1 % is a
   share of the 278 who evacuated *by vehicle*. Both annotated in place, not deleted.
   Also: neighbour's car is 16.5 %, not the "~17 %" the row carried.
4. **The figures are deliberately NOT in `docs/NUMBERS.json`.** No literal/evidence
   check kind exists (`json_path` / `expression` / `file_sha256`), so the row's own
   fallback applies. Registering someone else's measurement under a schema meaning
   "this project derived this from its own artifact" would buy the appearance of
   verification and lose the provenance. `test_the_survey_figures_did_not_leak_into
   _the_registry` pins the decision so a reversal must be deliberate.
5. **The anti-drift test is weaker than its name, and says so.** It checks a prose
   figure against the set of ALL table cells, not the table it cites: mutating 영덕's
   48.0 % to 47.0 % does not fail it, because 47.0 is a real cell elsewhere (표 1-5
   전체 '1명'). It does fire on a value in no table. The limitation is written into the
   docstring with the mutation that demonstrates it rather than left implied.

### WFG-021 · P0 · KCF · Detection-floor panel + tests for the GK2A detector
- **What:** (a) a finals card / Q&A block that states Session 19 exactly as recorded in `docs/detection_floor.md` §4, §9–10: reference = report time; 의성·안동 +22 min (FIRMS +117), 강릉 2023 +34 (FIRMS +151), 홍성 2023 +64 (FIRMS +17); 영덕 2025 excluded as confounded by the 의성 fire's background ring; 2022 fires predate the archive; false-alarm control 0/709 steps; conclusion: 사람 신고 > GK2A > FIRMS, the `manual` trigger is primary, "GK2A buys time" is not claimed. Registry keys: `det_gk2a_delay_uiseong_andong_min`, `det_gk2a_delay_gangneung_2023_min`, `det_gk2a_delay_hongseong_2023_min`, `det_gk2a_yeongdeok_best_delta_k`. (b) Unit tests for `src/wildfireguardian/detection/gk2a.py` (SESSION19 item 11: "새 코드에 테스트가 하나도 없습니다") using a tiny synthetic granule fixture: bit-width/units checks, the K = 4 contextual rule, the 15 km target / 30–80 km ring geometry, and a regression pin on `data/processed/detection/gk2a_detection_floor.json` values. (c) SESSION19 items 10 (FD-vs-LA cadence check) and 12 (cross-fire background contamination for 강릉/홍성) only if the needed intermediates are committed under `data/processed/detection/`; otherwise file a NEEDS_HUMAN (NOAA S3 is not reachable from the sandbox proxy).
- **Effort:** one lap. **agent_doable:** true for (a)(b); (c) conditional.
- **2026-09-03T2017Z, and it changes (c).** (b) is `done(f5f8498)`; (a) is not started.
  The independent reviewer of this lap disproved the row's own premise that
  "NOAA S3 is not reachable from the sandbox proxy": an anonymous HTTPS GET of
  `AMI/L1B/LA/202503/22/02/gk2a_ami_le1b_sw038_la020ge_202503220224.nc` returns
  200 and 458,172 bytes, and `read_granule` decodes it (14 valid bits, gain
  -0.00108296517282724, BT median 286.69 K). **(c) is therefore agent-doable**,
  and so is any future arm that needs the archive. It stays out of the default
  test run for WFG-039's reason, not for a reachability reason: the opt-in
  switch is `WFG_GK2A_NETWORK_TESTS=1`.
- **Constraints:** never say "every fire" (3 of 6); never rewrite the KMA-key direction experiment (`docs/gk2a_direction_experiment.md`) as a trigger item; the confabulated "1.28 ± 0.79 km" figure must not appear.
- **Done when:** tests exist and pass in the sandbox; the card text is in `docs/auto/finals/` and JUDGE_QA; registry keys cited.
- **2026-09-03T2217Z dev lap — (a) is done, and the row goes back to `todo` for (c).**
  `docs/auto/finals/DETECTION_FLOOR_CARD.md` states Session 19 as recorded: the three
  GK2A delays, the 영덕 교란 classification with the four numbers that justify it, the
  0-of-709 false-alarm **bound**, the 0.1939 ha size floor, the 신고 > GK2A > FIRMS
  priority, and an explicit "not claimed: GK2A buys time". `JUDGE_QA.md` gains Q10a
  (why 영덕 is excluded rather than counted as a miss) and Q10b (the false alarm rate is
  a bound, not 0 %). `tests/test_detection_floor_card.py` (17 tests) reads every figure
  on the card back out of `docs/NUMBERS.json`, binds each fire's delay inside its own
  table row so a swapped attribution fails, and keeps a tripwire over any new bare digit
  (a hand-maintained escape list, which the file itself says enforces nothing about the
  numbers already on it). **The FIRMS values named in this row's `What` were deliberately
  NOT copied onto the card: they are unregistered** (WFG-048), and CHARTER §3 rule 3
  says a number you cannot register you do not write. The card points at
  `docs/detection_floor.md` §8 instead.
- **(c) is still not attempted, and the reason is time, not reachability.** SESSION19
  items 10 and 12 need granules fetched from the NOAA GK2A archive at run time; the
  needed intermediates are not committed under `data/processed/detection/`. The 2017Z
  reviewer established the archive IS reachable anonymously, so this is agent-doable in
  a lap that budgets the download. It stays out of the default suite for WFG-039's
  reason (`WFG_GK2A_NETWORK_TESTS=1`).

### WFG-017 · P0 · KCF · `web/finals.html` refresh v2
- **What:** the committed screen was built 2026-08-15 at `c22ee5d9` and carries no Session 19/20/22 content. Add EVIDENCE/RELIABILITY cards, each with a 「근거」 pointer: operating point (WFG-019: PR curve, recall 0.138/0.0867, three folds at TP = 0 with `n_positive` shown so it reads as prevalence); detection floor (WFG-021); horizon grounding (Session 20: 79.23% of 2,008 fires contained ≤ 240 min; ≥ 100 ha median 4,025 min; `docs/horizon_grounding.md`); refuge placement (Session 22: one refuge covers 20/24 failing OSM-building "households", two cover 24/24 — with all three red caveats: OSM 124-building proxy, reachability-not-safety objective, 0/120 survival-filter events, counts 잠정 pending footprints); the reconciliation sheet as a RELIABILITY card only in the NEAR-labelled prose form permitted by WFG-018's constraints. Rebuild with `scripts/build_finals.py --verify` so the SYSTEM INTEGRITY panel records the gates; run `scripts/check_screen_assets.py web/finals.html` and the 17 tests in `tests/test_finals_screen.py`. No em-dashes (font subset). Region literals forbidden in the template.
- **Effort:** one lap. **agent_doable:** true — the sandbox has the geospatial stack via pip; if `build_finals.py` fails on a missing git-ignored input, emit the card payload as JSON under `docs/auto/finals/` with instructions and file a NEEDS_HUMAN for the student to run `make finals` locally.
- **Constraints:** `docs/finals_demo_plan.md` §1 (offline gate strict; no `fetch(`, no external URL); §5.19 caveat on every 영덕 absolute; never put reverted-field numbers on the screen; `web/finals.html` references `web/assets/fonts/` — do not inline fonts (size) and do not break the paths.
- **Done when:** rebuilt file with `built_at_commit` on `auto/dev`, gate panel showing the gates ran, `check_screen_assets` and the 17 tests green, a screenshot per act attached to the report.

**Lap 20260904T0630Z — done(a562045).** Every clause of the done-when holds, and the
row's own headline finding is that **the screen rebuilds in this sandbox in 10
seconds** — critic #7's root objection was that the cost of the first attempt was
unknown, and it is now known and small. The fallback the row provided (emit card
payloads as JSON, file a NEEDS_HUMAN, ask the student to run `make finals`) was not
needed: `build_finals.py` needs nothing git-ignored.

Shipped: four EVIDENCE cards (운영점 · 탐지 바닥 · 240분 지평 · 대피 지점 배치) and one
RELIABILITY card (제출본과 정본), each with a 근거 provenance block and its own caveat
line; 23 registry keys added to `REGISTRY_KEYS` (18 → 41); a new `evidence_v2()` payload for the
four structural facts a single key cannot hold; `docs/finals_screen_v2.md`;
`tests/test_finals_screen.py` 17 → 26. Four things worth carrying forward:

1. **Two claim defects were caught by rendering the page, not by the suite.** The
   operating-point card said 「나머지 폴드의 미검출률은 0.544~1.000」 while 1.000 is the
   value of the three folds that clause had just excluded. Every binding under it was
   green. The other is preventive: the detection card is written in **WFG-063's
   post-fix form ahead of that row** — the size floor rules the satellite OUT and the
   card says so, and says that which source should be primary was not measured.
2. **A second, spelling-free claim gate exists for this file.**
   `test_every_trigger_priority_sentence_on_the_screen_is_a_negation` requires any line
   naming both a priority word and a trigger source to carry a negation. Mutation-tested
   against three phrasings absent from the tree; the spelling gate missed all three and
   this one caught all three. It reads one file, which is why **WFG-062** stays open.
3. **Two documents called the KFS CSV's reference time a report time**
   (`horizon_grounding.md:106`, `JUDGE_QA.md:242`), contradicting `horizon_grounding.md`
   §2's own warning box thirty lines above. Corrected with a dated note; no number
   moved; the column's provenance is still **WFG-061**. `SESSION20_REPORT.md` keeps the
   old clause as a record.
4. **`JUDGE_QA.md:240` was deliberately not touched.** It is WFG-063's row across four
   documents, and fixing one clause of a two-clause sentence is the failure critic #6
   and #7 both named. It is the next row.

### WFG-003 · P0 · KCF · Finals screen audit + 5-minute demo script — done(20260905T0025Z)
- As in `docs/auto/BACKLOG.md`, run **after** WFG-017 so the mapping table covers the new cards. The script follows `docs/FINALS_DEMO.md`'s four acts and adds one interruption sentence per judge type; it says "about 25 seconds" for trigger→dispatch.
- **Done when:** `docs/auto/DEMO_SCRIPT_5MIN.md` exists; every on-screen figure maps to a registry key; `check_screen_assets` green.
- **DONE 2026-09-05.** `docs/auto/DEMO_SCRIPT_5MIN.md` (DRAFT-labelled per CHARTER §9): six timed segments summing to exactly 300 s over `docs/FINALS_DEMO.md`'s four acts plus an opening and a limits close; one interruption sentence for each of the five judge lenses; a §3 mapping table of **35 rows** (23 화면 / 12 구두), each with a registry key or a named artifact. `scripts/check_screen_assets.py web/finals.html` PASS.
- **The audit half found no screen change was warranted, and that is the finding.** Measured, not assumed: **40 of the 41 keys in `build_finals.py` `REGISTRY_KEYS` are present in the built `web/finals.html`**; the one absent (`mr_yeongdeok_walk_time_increase_pct`) has never existed in the registry and no card reads it, so nothing on screen is wrong — filed as **WFG-094**, P1. **WFG-067 is closed on disk**: the SYSTEM INTEGRITY panel stamps `d5e2562`, which resolves and is an ancestor of HEAD; the notes on `KCF_READINESS.md` R2 from critics #9/#10/#11 naming `a562045` were true when written and are stale now. A rebuild was run, produced a one-line diff (a fresher stamp) and was **reverted**, because regenerating a committed build product to buy nothing is the churn CHARTER §3.2 exists to prevent. So critic #14's cheapest test resolves as: the script exists, and the `web/` commit it predicted was **not** warranted.
- **What binds it:** `tests/test_demo_script_5min.py` (11 tests) reads the §3 table mechanically — every cited key must resolve in `docs/NUMBERS.json`, every 화면 row's key must be **rendered by a card in `scripts/finals.template.html`**, every 값 cell must equal its key's registry value, every emphasised number in §1's spoken body must appear in §3, the timings must be contiguous and total 300 s, the losing dispatch-ordering result must stay in the script, and the builder's `default_region` must still be the region act 3 quotes. Graded against **ten** mutations: **10 of 10 caught.**
- **⚠ The independent reviewer BLOCKED the first version of this row, and it was right on every count.** Recorded here because the block is the useful part: (1) **the gates were green on a tree that did not contain this file.** `gates.py --mode full` ran while `DEMO_SCRIPT_5MIN.md` was still untracked, and `check_number_collisions.py` reads tracked files, so committing it turned `make verify` **red** with four anchor-overlap collisions. This is CHARTER §4 step 8 exactly — the commit you push is the commit the gates read — and it was caught before the push, not after. Fixed with four `collision-ok:` pragmas naming which two quantities were confused. (2) **화면 was defined as「the key is in the built page」, which is not the same as「a judge can see it」**: the builder embeds the whole registry slice as one JSON blob, so every declared key is present exactly once whether or not a card draws it. Four rows were mislabelled on that evidence; the definition now reads the template and those rows are 구두. (3) **Two of those four were actively contradicted on screen** — the walk-time card renders `slope_walk_time_increase_pct` (+26.6 %, 영덕, terrain) while act 3 says 15.14 / 23.67 (time-aware detour), and the dispatch card renders `dispatch_order_deadline_wins_pct` (3.6 %) while act 4 said 0-of-180 「화면에 그대로」. Both now carry an explicit ⚠ reconciliation the student can say out loud. (4) **The 6-of-6 grading was self-confirming**: all six mutations targeted §3, the only surface the test read, so changing a bolded number in the spoken body broke nothing. `test_every_figure_the_student_says_aloud_is_in_the_mapping_table` closes it and catches the reviewer's own nail. (5) 31 → **35 rows** (the count was wrong), 「다섯 구간」 → 「여섯 구간」, and `docs/live_pipeline.md` §7 → **§6**, the section that actually holds the 24.9~28.2 s measurement.

### WFG-016 · WITHDRAWN 2026-09-04 · ~~ISEF · AI ledger current~~
- **Withdrawn at the author's instruction (2026-09-04).** The organisers confirmed no AI-disclosure artifact is required for this entry (NH-008), and the author directed that `docs/auto/AI_DISCLOSURE.md` be deleted rather than re-scoped to ISEF/IEEE. The row is withdrawn, not done: nothing was produced. If an ISEF Form 2A or an IEEE acknowledgment is needed later, the underlying record still exists in git — `git log --grep='Co-Authored-By: Claude'`, `docs/auto/ROUTINE_PROMPTS.md` and `docs/auto/reports/` — and CHARTER §9 keeps the practices that make it reconstructible.
- **Effort:** hours. **agent_doable:** true. **Done when:** the three files agree with `git log`; the critic lap finds no undisclosed agent-authored file.

### WFG-024 · P0 · infra · Re-key branch and dates after the human answers — blocked(WFG-022, WFG-023)
- **What:** `ssotize` HANDOFF §5.1 from `round3-dev` to `auto/dev` (annotate, do not delete the history); update `docs/auto/STATE.json`, `CHARTER.md` §1 and `BACKLOG.md` priorities to the confirmed finals date (freeze 10-10 for 10.18, 10-16 for 10.24); update `RUBRIC.md` "Which track applies" with the registered 참가부문; fix the dangling reference `docs/auto/research/RESEARCH_BRIEF_2026-09-03.md` by placing this brief there.
- **Effort:** hours. **agent_doable:** true once unblocked. **Done when:** no file on `auto/dev` names `round3-dev` as the working branch; STATE/CHARTER/BACKLOG dates agree; the brief path resolves.

### WFG-005 / WFG-006 · revise → P2, blocked(NH: raw bundle)
- **Why:** both require `spread_v2/forward_sim.py` on perturbed inputs; `scripts/run_forward_sim_region.py` rebuilds features from `data/raw/firms_data/` (git-ignored, absent in every clone). They are Mac-only. Also: an unattended loop registering new headline-adjacent numbers is the HANDOFF §4-B failure mode; per the R8 verdict these stay human-approved. Re-scope after the finals: the agent writes the scripts and tests against synthetic fields; the student runs them on the Mac; results are new arms with the 32.6% caveat. WFG-006 must state what ERA5's 28 km grid cannot resolve and never reinstate the withdrawn ratio.

### WFG-008 · revise → P3, parked
- **Why:** the ignition-likelihood (pre-ignition risk) layer contradicts 서식1's framing ("확산 예측 자체가 아니라 그 위에 세운 두 계층"; the existing system answers 「이 행정구역에 산불이 날 위험이 높은가」) — a theme-drift risk under 운영요강 p.9. Keep off every finals artifact. Revisit only after the ISEF selection with a written purpose check.

### WFG-007 · **P0** · KCF · Rehearsal aids + booth checklist — revise

> ⚠ **Raised P1 -> P0 by critic #20 (2026-09-05)** on critic #19's own falsifiable test: WFG-109 closed and `docs/auto/finals/` still holds no PDF. R7 and half of R9 wait on this row alone and neither has moved for five critic laps. The printing, the laptop and the poster stay the student's; the **files and the build script are the agent's and are what P0 buys.**
- **What:** `docs/auto/finals/`: printable A4 evidence sheet (headline numbers with sources and caveats), the reconciliation sheet (WFG-018), related-work table (WFG-026), Q&A cards (WFG-002), the 29 dispatch A4 sheets and 마을방송 script from `outputs/dispatch*`; booth checklist: copy `web/` **whole** (finals.html references `web/assets/fonts/*.woff2`; there are no data-URI fonts) to two USBs, open via `file://` with Wi-Fi off, key bindings G/R/Esc, HDMI/USB-C adapter, power strip, fallback laptop; fix the booth laptop's `wfg311` env (affine 2.4.0 → 3.0.1) so `make all-checks` is green on the judged machine; poster to the 사무국 spec (no asset in the repo).
- **Effort:** hours. **agent_doable:** true for the files; printing, laptop and poster are the student's.
- **Done when:** the files exist and a PDF is built by a script; the checklist has been ticked by the student once with the screen opened offline.

### WFG-009 · P1 · infra · Playwright smoke — revise
- **What:** `tests/e2e/finals.spec.ts` (Node, outside pytest): open `file://…/web/finals.html`, abort every non-`file://` request, assert no `pageerror`/console error, assert each `@font-face` family loaded with `document.fonts.check('12px "IBM Plex Sans KR"')` etc. (not `document.fonts.status`, which reports 'loaded' even when every face errored), advance the four acts, screenshot each; CI job installs `fonts-nanum fonts-noto-cjk` so screenshots have no tofu; upload screenshots as artifacts. Package check: the job copies only `web/` to a temp dir and runs from there, proving the directory is self-contained.
- **Effort:** one lap. **agent_doable:** true. **Constraints:** headless chromium; no change to the screen or to `check_screen_assets.py`. **Done when:** `auto-gates.yml` job `finals-smoke` green with 4 screenshots.

### WFG-025 · P1 · science · Two clean single-variable sweeps
- **What:** (a) pre-movement delay 0/5/10/20/30 min added to the walking departure on the 459-series routing axis for 의성·안동 and 울진·삼척 via `scripts/run_multi_region_routing.py` (new `--pre_movement_min` flag, new output filenames `real_roads_real_hazard_premove_<region>.json`), and for 영덕 via a new runner or an explicit new `--out` on `scripts/run_yeongdeok_canonical_routing.py` (its default writes only the protected canonical file; `run_multi_region_routing.py` refuses `yeongdeok_2025`, §5.18); report how `no_safe_route` and FA-only move, with OSM-completeness covariates beside every cross-region number (§5.7/§5.12) and the 32.6% caveat on 영덕. (b) walking speed 0.5–1.2 m/s (default 0.7) on the vulnerability layer at 영덕 (`scripts/vulnerability_layer.py` code path — not `verify_rescue_routing.py --sweep fc`, which is the 439-series rescue artifact), reporting the failing-household set size and Jaccard vs the committed 24; cite Studenski 2011 (0.92 ± 0.27 m/s, https://pubmed.ncbi.nlm.nih.gov/21205966/) and Peel 2013 (0.58 m/s) as the bracket. Step 0: a dry run proving the routing axis runs on `data/snapshots/` + committed npz in the sandbox (believed yes since Session 18; UNVERIFIED).
- **Effort:** one to two laps. **agent_doable:** true. **Constraints:** new arms, new filenames, defaults untouched; never quote a short-budget rate without its budget (§5.10); no ranking of regions (§5.14). **Done when:** artifacts + registered numbers + `docs/premovement_sensitivity.md` and `docs/walking_speed_sensitivity.md` stating method, result, caveats and what they do not show; tests.

### WFG-026 · P1 · KCF · Related-work table (Crossref-resolved only) + SFTD059T differentiation panel
- **What:** `docs/related_work.md` + a one-page Korean booth table. Entries restricted to citations already resolved: Cova & Johnson 2003 (10.1016/s0965-8564(03)00007-7); Cova et al. 2005 trigger buffers (10.1111/j.1467-9671.2005.00237.x); Li, Cova & Dennison 2017 (10.1016/j.apgeog.2017.05.008) and 2018 (10.1007/s10694-018-0771-6); Wahlqvist et al. 2021 WUI-NITY (10.1016/j.ssci.2020.105145); Finney 2002 FlamMap MTT (10.1139/x02-068); Borgwardt et al. time-expanded max-flow (https://arxiv.org/abs/2410.14500); RESCUE, Tammali et al., ICDCN 2026 (10.1145/3772290.3772301); Dayan 2026 CRC (https://arxiv.org/abs/2603.22331); Lahrichi et al. WSTS+ (https://arxiv.org/abs/2502.12003); Sung et al. KJRS 2025 GK2A detection (https://www.kjrs.org/journal/view.html?pn=mostdownload&uid=1117&vmd=Full); Kwon/Kim/Han Uiryeong MIP (10.3390/systems13121125); ISEF 2026 SFTD059T (https://abstracts.societyforscience.org/Home/FullAbstract?projectId=27978) and FireChain EAEV039 (…projectId=28121). For each: what it computes, what it does not (sampled-origin pedestrian routing on a learned held-out hazard, rescuer ingress survival, non-smartphone delivery), positioned as "not found in the surveyed work" — never 최초/처음 (`check_forbidden.py` claim rules). One panel: SFTD059T (FDS indoor gas/fire, building egress, time-varying-risk A*, Raspberry Pi) vs WildfireGuardian (wildfire-ML hazard on a time-expanded graph, rural household-level walk-out, rescue dispatch triage, live FIRMS trigger with offline replay). Any DOI the agent cannot re-resolve from the sandbox is kept with the note "resolved 2026-09-03 outside the sandbox".
- **Effort:** one lap. **agent_doable:** true (web access from the sandbox UNVERIFIED; the list above needs no new lookups). **Done when:** file + table exist; zero unresolved citations; `check_forbidden` green.

### WFG-027 · P1 · KCF · Schedule & roles timeline
- **What:** `docs/auto/finals/TIMELINE_ROLES.md` (Korean): a dated table reconstructed from `git log` (first commit 2026-05-27; tag `round2-submitted`; `docs/SESSION*_REPORT.md` dates; Round 3; 2026-09-03 loop start) plus a roles statement (student / 지도교사 / AI-assisted sessions disclosed as tooling in the wording WFG-022 returns). Verbal and poster use only; the submitted 서식2 Ⅱ-5 cannot be edited.
- **Effort:** hours. **agent_doable:** true. **Constraints:** truthful attribution only; label as DRAFT until the student confirms. **Done when:** the file exists and every date greps to a commit or report.

### WFG-028 · P1 · KCF · Expert consultations ×2–3 + firefighter §8 — human
- **What:** the agent drafts `docs/auto/finals/CONSULTATION_PROTOCOL.md` in the `docs/firefighter_consultation.md` §0 discipline (statements only, N-labelled, no numbers derived, anonymity default, design feedback not data). The student conducts 2–3 phone/video consultations (이장, 119 상황실 dispatcher, 사회복지사) and records them as `docs/consultation_<role>.md`; closes the firefighter §8 blanks (affiliation/rank, date, written consent for anonymous vs named attribution); asks the three academic advisers whether a one-line quoted judgment may be shown.
- **Effort:** hours each. **agent_doable:** false (protocol drafting: true). **Constraints:** no human-subject data (서식2 §3); ISEF exemption text reads "prior to experimentation", so present these as feedback shaping future design; never "현장에서 검증". **Done when:** N = 3–4 consultation docs with §8-style metadata complete.

### WFG-029 · P1 · KCF · One recorded email send from a Shanghai-workable path — partial
- **What:** `docs/delivery_channels.md` §3-B records SMTP 465/587/25 timeouts on the working network; from Shanghai a Gmail SMTP path is likely blocked without a VPN (UNVERIFIED). Agent: build `delivery/email_oauth.py` — Gmail API over HTTPS 443 with the same three locks and `approval_token` gate as `delivery/email.py`, DEMO_RECIPIENT-only, dry-run in the sandbox, tests with a mocked API. Student: authorise once and run `scripts/send_dispatch_email.py --confirm-send` (or the OAuth variant), commit `email_sent.json` with the network path stated (VPN or API). Keep 서식2's "SMS 전달은 모사" sentence until this exists; no Twilio, no SOLAPI, never "재난문자 연동".
- **Effort:** hours. **agent_doable:** adapter true; send false. **Done when:** `email_sent.json` committed with the path recorded; RELIABILITY tab wording updated.

### WFG-030 · P1 · infra · Report-number check (HANDOFF §4-B as a gate)
- **What:** `scripts/auto/check_report_numbers.py`: extract every numeric literal (with %, ×, ±) from `docs/auto/reports/*.md`, `docs/auto/JUDGE_QA.md` and any `docs/*.md` touched in the last lap; match each against `docs/NUMBERS.json` values/keys or a committed artifact path cited on the same line; print unmatched numbers with file:line; soft gate in `gates.py --mode quick`, hard in the critic lap. Allow-list dates, commit hashes, rule numbers.
- **Effort:** hours. **agent_doable:** true. **Done when:** the script runs in `gates.py`, the current tree's unmatched list is committed as the baseline, and the critic prompt cites it.

### WFG-031 · P1 · infra · `CITATION.cff`
- **What:** cff-version 1.2.0; title, `type: software`, author Park, Siyeong (Shanghai American School Puxi), repository-code URL, license as in `LICENSE`, keywords; `version`/`date-released` set only at the freeze tag (no invented date); `doi` added after the student mints it (WFG-015).
- **Effort:** minutes. **agent_doable:** true. **Done when:** file validates (`cffconvert --validate` or the GitHub citation widget renders).

### WFG-040 · P1 · infra · `build_numbers.py` would destroy the registry it claims to build

> ⚠ **Renumbered from `WFG-036` by the critic lap 20260903T1748Z.** Two live rows carried
> the ID `WFG-036`: this one (added by the research re-key `d88c85b`) and the P0 final
> product bundle (added by the sprint commit `42818ec`). `WFG-036` stays with the final
> product because `CHARTER.md` §11, `KCF_READINESS.md` R5/R9 and the sprint plan table all
> hard-reference it there. `docs/auto/reports/2026-09-03T0653Z-dev.md` still calls this row
> `WFG-036`; reports are records and are not edited.

- **What:** `scripts/build_numbers.py` defines **65** entries and ends with an
  unconditional `OUT.write_text(...)` over `docs/NUMBERS.json`, which currently holds
  **278**. Every entry added since roughly Session 12 went into the JSON directly and is
  gated by `verify_numbers.py`'s per-entry `check` block, not by the builder. So the one
  command CHARTER §3 names as the registration path silently deletes ~77 % of the
  registry, and `make verify` would then pass on the survivors. Found by the WFG-018 lap,
  which needed to register 18 values and had to establish the real path first.
- **Options (pick one, in writing):** (a) make it **refuse** — read the existing file, and
  exit non-zero if it would drop any key, so it becomes a checker rather than a builder;
  (b) make it **merge** — keep hand-added entries and rebuild only the keys it owns,
  which needs a marker on the entries it owns; (c) **retire** it to `scripts/legacy/` with
  a header saying what replaced it, and correct CHARTER §3 to describe the real path.
  (a) is the cheapest and loses nothing: the builder's value now is as a second derivation
  of the 65 oldest numbers.
- **Effort:** hours. **agent_doable:** true.
- **Constraints:** never run it as-is; do not delete it (CHARTER §3 rule 7 — archive);
  whichever option is taken, CHARTER §3's sentence must be corrected in the same commit.
- **Done when:** running the script on a clean tree cannot reduce the entry count; a test
  asserts that; `make verify` green; CHARTER §3 says what the registration path actually is.

### WFG-041 · P1 · infra · The lineage gate's label window admits an unrelated keyword
- **What:** `tests/test_rescue_lineage_ssot.py::test_every_prose_mention_of_the_synthetic_bracket_names_its_lineage`
  finds a `6 → 34` mention, then searches a **±2-line window** for `LINEAGE_LABEL`. At
  `docs/auto/JUDGE_QA.md:46` the label that satisfies it is the word 폐기 on line 45, which
  belongs to a different bullet about a different quantity (the 459-series 438/18/3 split).
  So the gate is green on the single most judge-facing occurrence of the exact sentence it
  was written to catch. Reproduced by the critic lap 20260903T1748Z: replace 폐기된 with
  버려진 on line 45, touch nothing else, and the gate fails naming line 46.
  This is the reviewer's 1724Z block ("a judge-facing file exempted by an incidental
  keyword") recurring at a finer granularity.
- **Do:** require the label on the mention's **own line**, or on an adjacent line that also
  carries the bracket; keep the ratchet in `WHOLLY_SUPERSEDED_LINEAGE` for whole-file
  exemptions, which is the reviewable mechanism. Add `JUDGE_QA.md:46` as a seeded
  regression case so the narrowing is proved, not asserted.
- **Effort:** hours. **agent_doable:** true.
- **Constraints:** do not widen `WHOLLY_SUPERSEDED_LINEAGE` to make the failure go away —
  `JUDGE_QA.md` is exactly the file the gate must see. Fix the F1 content defect first or in
  the same commit, or the tightened gate is red on landing.
- **Done when:** the window is narrowed, a seeded mutation of `JUDGE_QA.md:46` fails the
  gate, an unrelated keyword two lines away does not rescue it, and `gates.py --mode full`
  is green.

### WFG-042 · P1 · IEEE · A `verified` citation can disagree with the paper at its URL
- **What:** `paper/references.bib` requires a `note` containing "verified" and
  `check_paper.py` checks only that the word is present. The entry `wildfirespreadts2025`
  carries that note while its title and authors ("WildfireSpreadTS…", Gerard/Zhao/Sullivan)
  belong to arXiv:2406.04759, not to its own `url` arXiv:2502.12003, which is "Improved
  Wildfire Spread Prediction with Time-Series Data and the WSTS+ Benchmark" by Lahrichi,
  Bova, Johnson and Malof (WACV 2026). Opened and confirmed by the critic lap 20260903T1748Z.
  CHARTER §5 forbids fabricated citations; the gate that is supposed to enforce it cannot.
- **Do:** (a) correct the entry to whichever paper `manuscript.md:13` actually means, with
  the metadata as fetched; (b) make the `note` carry the **fetched title** and the fetch
  date, and have `check_paper.py` assert the note's title matches the entry's `title` field,
  so a mismatch is a gate failure rather than a reading exercise; (c) sweep the other three
  entries the same way. A network fetch inside the gate is not required and should not be
  added — the assertion is between two fields the lap that cited the paper had to fill in.
- **Effort:** hours. **agent_doable:** true.
- **Constraints:** never delete a citation to make the gate pass; correct it. Do not add a
  network call to `check_paper.py` (it runs on a clean machine in CI).
- **Done when:** every entry's `note` names the title as fetched, `check_paper.py` fails on
  a seeded mismatch, and the four current entries pass.

### WFG-043 · P0 · KCF · The 2025 fire's scale figures have no source and no key
- **What:** `README.md:193` (Korean) and `README.md:488` (English) open the project with
  27 deaths, **~116,000 ha** burned and 4,000+ homes destroyed for the
  의성→안동→청송→영양→영덕 chain, sourced to "한겨레·세계일보·서울환경연합" with no URL. None
  of the three has a `docs/NUMBERS.json` key. The hectare figure is the exposed one: public
  reporting puts the **nationwide** March–May 2025 total (347 fires) near 104,788 ha, and
  the WWA attribution report gives ~48,000 ha for the fires it analysed — so the sub-scope
  figure exceeds every national figure available. The same paragraph already carries a
  scope footnote for 27 vs "30명 이상" and none for the area. `paper/manuscript.md:9` then
  attributes 27 to `[@wwa2025korea]`, which reports 32 casualties (26 in 의성).
- **Do:** open the three named sources (NH-015 asks the author for the URLs; if they do not
  arrive, use 산림청 / 행정안전부 published figures and say which), record for each figure
  its value **and its scope** (this fire chain / 경북 / nationwide / date range), register
  them, add the scope footnote the death toll already has, fix `manuscript.md:9` to cite a
  source that carries its number, and add the drill question recorded in `CRITIC_LATEST.md`
  to `JUDGE_QA.md` with its 근거 line — updating the tier counts and the §6 drill table in
  the same commit, because `tests/test_judge_qa_bank.py` pins all of them.
- **Effort:** hours. **agent_doable:** true; the author's own sources are NH-015.
- **Constraints:** annotate, never delete, if a figure moves (CHARTER §3). Do not quietly
  swap 116,000 for another number without stating the scope both belong to — the scope is
  the finding, not the digit. `README.md`'s Round-2 section stays untouched; both lines are
  in Round 3.
- **Done when:** each of the three figures has a registered key with its scope in the
  caveat, both README lines and `manuscript.md:9` cite a source that carries the value they
  state, and `make verify` plus `check_number_collisions.py` are green.
- **2026-09-03T2347Z critic (F16, F17, F18) — the row did not close at `12b8ac7`, it changed
  sign.** That commit removed 116,000 ha and put **45,157 ha** in its place, attributed to
  중대본 as of 2025-03-27, plus a 범위 주의 note saying 104,788 ha belongs to a different
  event. Verified this lap against ko.wikipedia (2025년 의성-안동 산불), en.wikipedia
  (2025 South Korea wildfires) and Korean coverage of the 경상북도 recovery plan: the
  의성발 경북 chain's **final** area is **99,289 ha** (largest since 1986 statistics began;
  주불 진화 149시간; 2,246세대 3,587명 이재민; 주택 **3,819동**; 총 1조 505억 원), so the
  chain is about 95 % of the 104,788 ha nationwide total and the scope note points the wrong
  way. 45,157 ha is the 경북 provincial interim on 03-27, the day before 주불 진화 on 03-28
  17:15. The WWA "48,000 ha 이상" is WWA's figure for **southeastern Korea**, not this
  complex, so it cannot serve as the upper end of a range for it.
- **Also fix in the same commit:** (1) `README.md:194` and `:489` still say 영덕 **8명** /
  8 in 영덕, which `docs/evidence/greenpeace_2026_survey.md` §7 item 1 corrected to **10**
  (영덕군 공지 2025-04-29 quoted at report p.9, a 재인용값 not a survey result) on
  2026-09-03T1821Z; grep `영덕 8` so the third copy does not survive. (2) The 알려진 함정
  bullet at `docs/data_sources.md:201` is backwards: 3,819동 is the **chain's** 주택 전소,
  and 150동 is the 산림청 03-26 interim. (3) Every row of both tables in
  `docs/data_sources.md` § 동기 사건의 피해 규모 gains a URL, and any row whose URL the lap
  cannot open is removed rather than kept unsourced — the same rule CHARTER §12 applies to
  `references.bib`. The nationwide 주택 3,848동 is the row to check first; it is suspiciously
  close to the chain's 3,819동 and public reporting gives around 4,015 houses nationwide.
- **Then, and only then**, add the two drill questions `CRITIC_LATEST.md` withheld
  (의성 산불 피해면적; 영덕 사망자 수) to `JUDGE_QA.md` with their 근거 lines, updating the
  tier counts and the §6 drill table in the same commit.

### WFG-044 · P1 · infra · The paper routine has no report kind
- **What:** `scripts/auto/report.py:123` takes
  `choices=["dev","critic","research","kickoff","red","manual"]`. CHARTER §2 and §12 define
  a fourth routine, `wfg-autoloop-paper`, that is not in the list, so its first real lap
  filed `docs/auto/reports/2026-09-03T1928Z-manual.md` with a title line reading `· paper ·`
  and set `docs/auto/STATE.json` → `"last_report_kind": "manual"`. Three consequences, all
  live: a lap that resolves its predecessor by kind (this critic prompt's own
  `git log --grep='critic' -- docs/auto/reports` pattern) cannot find paper laps; the
  dashboard timeline labels a paper lap "manual"; and that report carries three different
  timestamps for one lap (title `1955Z`, `when` row 19:28 UTC, filename `1928Z`).
- **Do:** add `"paper"` to the `--kind` choices; derive the default title from the same
  stamp the filename uses so a hand-passed `--title` cannot disagree with it; leave the
  already-committed `*-manual.md` report where it is (CHARTER §3: reports are records).
- **Effort:** minutes. **agent_doable:** true.
- **Constraints:** `tests/` already covers the reporting scripts (`a131daf`); extend that
  test rather than adding a second one.
- **Done when:** `report.py --kind paper` writes `<stamp>-paper.md`, the title stamp equals
  the filename stamp, and a seeded `--title` with a different stamp fails a test.

### WFG-045 · P1 · IEEE · The manuscript has 21 citations and no bibliography
- **What:** `paper/manuscript.md` runs Abstract, 1 Introduction, 2 Related work, 3 Data and
  methods, 4 Results, 5 Discussion, 6 Limitations, 7 Conclusion, Data and code availability,
  and stops. CHARTER §12's required section list ends with "References"; there is none.
  `paper/check_paper.py` counts words, figures, tables, references and gaps and asserts
  nothing about sections, so the gate written to hold §12 true has never looked at the one
  structural requirement §12 states. 출처 명기 is a scored row in both rubric tracks and a
  reader of the markdown cannot resolve a single `[@key]`.
- **Do:** render `## References` into the manuscript from `references.bib` (the same path
  `build_docx.py` already uses for the `.docx`, so the two cannot disagree), and add one
  assertion to `check_paper.py` that every CHARTER §12 section heading is present, proven by
  a seeded deletion that fails it.
- **Effort:** hours. **agent_doable:** true.
- **Constraints:** the word budget is body text (7,000 target / 7,500 hard fail); confirm
  `check_paper.py` does not count the new section into it, or the paper fails its own gate.
- **Done when:** `check_paper.py` exits 0 with the section assertion in place, removing any
  required heading fails it, and the rendered bibliography lists all 21 entries.

### WFG-046 · P0 · infra · The commit you push is not the commit the gates read
- **What:** CHARTER §4 runs `gates.py` at step 5 and `report.py` at step 7, so every lap's
  report is tracked prose no gate has read. That is how critic #2 pushed `24751fa` red.
  **The 2053Z lap then showed the diagnosis was one step too narrow.** Its gate run was at
  `f5f8498` 20:39:36Z and it pushed `8d1decf` 20:57:29Z, and in between `e431696` rewrote
  162 lines of `tests/test_gk2a_detector.py` and 41 lines of `docs/detection_floor.md` (the
  reviewer-block fixes) as well as adding the report. `auto/dev` was red again. So the
  unchecked file is not "the report", it is **everything committed after the gate run**,
  and a report-only gate would not have caught the larger half.
- **Do:** two things, the second of which subsumes the first.
  (a) `report.py`, after writing, runs `make check-forbidden` and
  `pytest tests/test_rescue_lineage_ssot.py` over what it wrote and exits non-zero on
  failure, so the lap cannot commit an unreadable report.
  (b) `gates.py --assert-head` (or `scripts/auto/check_gates_current.py`, five lines):
  read `.auto/gates.json`, and exit non-zero unless `git_head` equals
  `git rev-parse --short HEAD`, `git status --porcelain` is empty, and `passed` is true.
  CHARTER §4 step 8 then becomes one command instead of a list a tired lap can skip.
- **Effort:** hours. **agent_doable:** true.
- **Constraints:** `.auto/` is git-ignored, so `--assert-head` must fail loudly when
  `.auto/gates.json` is absent rather than passing vacuously. Do not make `report.py`
  run the full suite; the prose gates are seconds, the suite is three minutes, and a
  report step that costs three minutes will be worked around.
- **Done when:** deleting the lineage label from a report makes `report.py` exit non-zero;
  committing anything after a green `gates.py` and running `--assert-head` exits non-zero;
  CHARTER §4 step 8 names the one command.

### WFG-047 · P0 · infra · `in-progress` has no release, so an unfinished row is stranded
- **What:** CHARTER §4 step 3 tells a lap to take the highest-priority row that is `todo`
  and **not `in-progress` by another lap**. Nothing tells a lap what to do with its row
  when it ends without finishing it. The 2053Z lap left `docs/auto/BACKLOG.md:44` reading
  `in-progress(20260903T2050Z) — (b) done(f5f8498), (a) card + JUDGE_QA block outstanding,
  (c) not attempted`, which is exactly the right residue note and exactly the wrong status
  word: no lap holds that claim any more, and no future lap may take the row. `WFG-016`
  has been `in-progress(kickoff seed)` since the kickoff for the same reason. Both are P0.
  `KCF_READINESS` R2 names WFG-021 (a), the detection-floor evidence card on the finals
  screen, so a readiness line is blocked by a status word inside a twelve-day sprint whose
  plan dated WFG-021 to 09-05.
- **Do:** add one sentence to CHARTER §5: *a lap that ends without finishing its row sets
  the row back to `todo` and appends what it did as a residue note; `in-progress` is only
  ever held by a lap that is still running.* Then set WFG-021 and WFG-016 to `todo`,
  keeping their residue text verbatim.
- **Effort:** minutes. **agent_doable:** true.
- **Constraints:** do not delete or reword the residue notes; they are what makes the rows
  restartable by a fresh agent (CHARTER §5). Do not mark WFG-021 `done`: only part (b) is.
- **Done when:** no row in the table is `in-progress` without a lap running, CHARTER §5
  states the release rule, and WFG-021's remaining (a) and (c) are pickable.

### WFG-038 · P1 · infra · The suite's own count is not gated
- **What:** `scripts/auto/gates.py` runs the full suite and writes the summary line
  into `.auto/gates.json`, but nothing reads the numbers in it. On 2026-09-03 one lap
  read **1,098 passed / 60 skipped** and then **1,104 / 54** from the same commit
  (`682aeb3`), same flags, minutes apart — 1,158 outcomes both times, six tests moving
  between passed and skipped, `ALL GREEN` printed for both. The previous lap saw the
  same six-test signature (1,071/60 vs 1,077/54) and could not reproduce it. So six
  tests in this suite have a result that no gate can distinguish from a regression,
  and the only place it is visible is a summary line nobody compares.
- **What was already ruled out:** the six git-ignored `data/cache/*.nc` files are
  written during the first full run of a fresh container, but moving them aside and
  re-running still gives 1,104/54 — they are regenerated before the guarded tests
  run. The identity of the six is open.
- **Do:** (a) parse `collected / passed / skipped / xfailed` out of the pytest summary
  in `gates.py` and store them as fields, not just the raw line; (b) commit a baseline
  (`docs/auto/suite_baseline.json`) holding the triple and the sorted list of skip
  reasons with counts, produced with `-rs`; (c) WARN in `--mode quick` and fail in the
  critic lap when the skip count or the reason multiset moves without the baseline
  being updated in the same commit; (d) with (b) in hand, run the suite once in a cold
  container and once warm, diff the two reason lists, and name the six in the MEMO.
- **Effort:** hours. **agent_doable:** true.
- **Constraints:** never fix a drift by widening a `skipif`; the point of the gate is
  to make the six findable. A skip on a git-TRACKED path is a defect, not a guard
  (MEMO 2026-09-03).
- **Done when:** the baseline file exists, `gates.py` compares against it, a test
  asserts the comparison fires on a seeded mismatch, and the six drifting tests are
  either named or the gate proves the drift no longer occurs.

### WFG-011 · P2 · ISEF · ISEF plan memo — revise
- **What:** `docs/auto/research/ISEF_PLAN.md`: route-existence questions and their answers (KCF 은상/동상 → delegation; interview date; eligibility of a student enrolled outside Korea — all UNVERIFIED until the 사무국 answers); category recommendation SFTD (Algorithms/HMC) with the base rate stated (applied decision-support pipelines in SOFT/SFTD 2023–2026: 8 of 10 unplaced; SFTD059T unplaced) and EAEV as non-target (FireChain unplaced); 12-month window (start 2026-05-27; freeze ~2027-05-01; ISEF 2027 LA 8–14 May); Form 2A; Display & Safety (no internet, no URLs/QR, ≤ 100 Wh, frozen digital demo, attribution lines for OSM/ERA5/FIRMS/SRTM/WorldCover, "created by Finalist using …"); human-participants position (no study this cycle; consultations as expert feedback); age rule 만 15세 이상 19세 미만 (namu wiki; UNVERIFIED against the affiliate); the "hand-written only" list; what the loop may and may not do after the affiliate selection (no new variables or procedures).
- **Effort:** one lap. **agent_doable:** true. **Done when:** the memo exists with URLs for every rule; every UNVERIFIED item has a NEEDS_HUMAN ask.

### WFG-032 · P2 · science · Leak-free 영덕 fold + hindsight-oracle routing arm — partial (Mac-only run)
<!-- collision-ok: 129.1 -->
- **What:** agent writes `scripts/leakfree_yeongdeok_fold.py` and `scripts/hindsight_oracle_routing.py` with tests on synthetic inputs: (1) count `uiseong_andong_2025` dataset rows whose cell lies inside the 영덕 bbox with overpasses 2025-03-25→27 (the manifests' bboxes overlap 128.95–129.1 E); (2) refit the 영덕 LOFO fold with `uiseong_andong_2025` excluded from training; re-simulate the canonical 영덕 field with `run_forward_sim_region.py` into a new npz; (3) build a hindsight field from the actual later FIRMS cumulative masks; (4) route the same 458 origins on the original, leak-free and hindsight fields; write three new artifacts and register 414/42/2-style partitions per arm. Student runs it on the Mac (raw bundle). Interpretation: if the FA-only count moves by more than network-drift noise (a 0.047% node change moved 33% of binary verdicts on the 439 series), the coupling claim is not paper-ready; if it survives, it is IEEE Table V and the strongest booth answer to "how do you know the 42 are real".
- **Effort:** one lap (agent) + one day (Mac). **agent_doable:** scripts true; run false. **Constraints:** never overwrite `spread_v2_lofo.json` or the canonical npz; new filenames; `fire_manifest.json` untouched (§5.9); label the six fires as five independent events plus one co-located pair in the doc. **Done when:** scripts + tests committed; NEEDS_HUMAN with the exact command; after the run, `docs/leakfree_fold.md` with the three partitions.

### WFG-033 · P2 · science · Coupling-ablation routing-only arms (absorbs WFG-012)
- **What:** on the committed hazard fields (`routing_demo_canonical.npz`, `hazard_uiseong_andong_2025.npz`, `hazard_uljin_samcheok_2022.npz`) and `data/snapshots/`, run the identical scan under: (a) fire-blind; (b) static current perimeter (slice 0, p ≥ p_cut) + fixed buffer 0.5/1/2 km; (c) `spread_v2` canonical (the committed arm, re-read not re-run); (d) the Rothermel surface field only if a committed field exists (do not regenerate). Report FA-only / no-safe counts and paired exposure per arm per region with covariates; the persistence and hindsight arms belong to WFG-032 (raw bundle). This is R5's E1 restricted to what the sandbox can run.
- **Effort:** two laps. **agent_doable:** true after the WFG-025 dry run proves the axis runs. **Constraints:** §5.7/§5.12/§5.14/§5.18/§5.19; new filenames. **Done when:** `data/processed/coupling_ablation/*.json`, registered numbers, `docs/coupling_ablation.md` that says plainly whether the learned field beats "current perimeter + buffer" on routing decisions.

### WFG-034 · P2 · science · Refuge-density decimation — blocked(approval in WFG-023)
- **What:** hold network and terrain; remove refuges at 100/75/50/25% with 20 seeds; measure FA-only and `no_safe_route` per region on the 459 axis → the condition under which the rescue-aware constraint binds (answers the "why does the rescue constraint change 0 assignments" question with a measurement). Requested 2026-08-02, sequenced, never started (HANDOFF §4: the user confirms before it starts).
- **Effort:** two laps. **agent_doable:** true once approved. **Constraints:** new arm; covariates; caveat. **Done when:** artifact + doc + registered numbers.

### WFG-013 · P2 · science · Open building footprints for 영덕 — keep
- As written; add: licences cited; if covered, the real-footprint replacement runs and every household count in Session 22 is re-stated with the OSM comparison; NH-005 closes either way.

### WFG-014 · P3 · IEEE · Paper skeleton — revise
- **What:** `paper/` IEEEtran built by CI (`xu-cheng/latex-action@v4`), using R5 §4's outline with these corrections baked in: "sampled walk-network origins", not "households"; 32.6% caveat in the abstract; never a 439-series number beside 458-series counts without lineage; six fires = five independent events + one co-located pair; cumulative IoU ≈ 0.40 with new-ring ~0.07 beside it; contribution ② in HANDOFF's conditional wording; Session-22 shelter finding out of Results; every number a registry key (WFG-035); acknowledgment naming the AI system and AI-generated sections/code; limitations first. Venue: IEEE Access at full US$2,160 + tax (no student discount), OJ-ITS if WFG-033 plus an external routing comparison exist; IGARSS 2027 4-pager by 10 Jan 2027 only if WFG-032 survived. No preprint/submission before the December ceremony; TechRxiv the week after.
- **Effort:** weeks. **agent_doable:** drafting true; submission false. **Done when:** `paper/main.pdf` builds in Actions; every number in it greps to a registry key; the student has rewritten the abstract by hand.

### WFG-035 · P3 · IEEE · Register the manuscript's numbers
- **What:** add `NUMBERS.json` entries (new keys, derived from committed artifacts) for: RF/LR/HGB baselines (`ml_baselines.json`), DEM-corrected deltas (+0.0048 / −0.0017), pooled CI with a per-fire or spatial-block method beside the DeLong cell-level CI (cells are autocorrelated; ±0.004 overstates precision), Brier/ECE per model, cumulative and new-ring IoU, envelope-bias ratios, timings (HANDOFF §9: ~25 s), and the two HGB means with the reconciling note. R5 claimed all were registered; a scan found most are not.
- **Effort:** one lap. **agent_doable:** true. **Done when:** `make verify` green with the new keys and `paper/` cites only keys.

### WFG-015 · P3 · IEEE · Reproducibility package — keep
- As written; the Zenodo DOI is minted by the student from a GitHub Release after the finals (needs a Zenodo login); `docs/REPRODUCE.md` verified by a fresh-clone CI run on committed snapshots; the 16 verified-but-unreproducible values stated as such with the 2026-07-24 graph-loss reason.

### WFG-048 · P1 · infra · The FIRMS half of the detection-floor comparison is unregistered
- **What:** `docs/detection_floor.md` §4 prints `+117분`, `+151분`, `+17분` for FIRMS beside
  the three registered GK2A delays, and §8 repeats them as the headline trade-off line
  (`+22 / +34 / +64분` vs `+117 / +151 / **+17**분`) together with a derived "95분·117분
  앞섬" and "47분 빨랐습니다". None of those has a `docs/NUMBERS.json` key. The values are
  read from a committed artifact — `data/processed/detection/firms_first_detection.json`
  → `<fire>.delay_h` = 1.95 / 2.52 / 0.28 h — so they are traceable by hand and NOT
  re-derivable by `make verify`, which is the gap CHARTER §3 rule 3 exists to close.
- **Do:** register three `json_path` entries in hours (the artifact's own unit) plus, if the
  minute form is wanted on judge-facing prose, three `expression` entries with
  `expr = "a * 60"`; then cite them from §4, §8 and the finals card. Check first whether
  `scripts/build_numbers.py` would drop them on its next run (WFG-040) — the existing
  `det_*` keys are not produced by it either, which is the same latent problem.
- **Effort:** hours. **agent_doable:** true.
- **Constraints:** never edit a registered value (§3 rule 2); the derived "95분·117분
  앞섬"/"47분" sentences in §8 must either become registered expressions or be rewritten to
  read off the registered pairs. Do not restate the FIRMS numbers anywhere new until they
  are registered.
- **Done when:** `make verify` re-derives every FIRMS figure that appears in prose, and
  `docs/auto/finals/DETECTION_FLOOR_CARD.md` can state the comparison instead of pointing
  at §8.

### WFG-049 · P0 · infra · A prose-only commit is invisible to every gate
- **What:** `12b8ac7` (2026-09-03T2330Z) rewrote `README.md`'s opening paragraph in Korean
  and English, added a new section to `docs/data_sources.md`, closed NH-008, NH-009 and
  NH-015, and edited `docs/auto/LOOP_CONFIG.json`. It has **no report**, so no
  `Reviewed by:` line, no gate table, no root objection and no plain-terms section;
  `docs/auto/STATE.json` still names the 2239Z dev report at `c7b8a66` as the last report,
  so the loop's own state file does not know the commit exists. Every gate passed on it, and
  the numbers it introduced were wrong (WFG-043, critic F16/F17/F18). The three checks the
  loop added this week are all aimed elsewhere: `--assert-head` asks whether the gates read
  the pushed commit (they did), `report.py`'s prose gate runs only when a report is written
  (none was), and `make verify` re-derives only numbers that have registry keys (these have
  none). The class is the finding: **prose with no key and no URL can change under every
  gate this repository owns.**
- **Do:** `report.py` already computes "commits since the previous report". Put the same
  computation in front of the push. `gates.py --assert-head`, or a short check beside it,
  exits non-zero when `HEAD` is not the commit the last report names **and** the intervening
  commits touch anything outside `docs/auto/reports/`, `docs/auto/images/` and
  `docs/auto/STATE.json`. Message names the offending paths so the fix is obvious. Pin it
  with a test in the style of `tests/test_gates_assert_head.py`, seeded with this exact
  `c7b8a66`/`12b8ac7` pair.
- **Effort:** hours. **agent_doable:** true.
- **Constraints:** must not block the report commit itself, which is by construction after
  the gate run (that is what `--assert-head` already handles); must not require network.
  Do not weaken `--assert-head`'s existing mode clause while extending it.
- **Done when:** a commit pushed without a report covering it fails the pre-push check, the
  seeded test reproduces the `c7b8a66`/`12b8ac7` case, and CHARTER §4 step 8 names the
  command.


### WFG-051 · P1 · infra · A bound value with a free attribution

Opened by critic #5 (2026-09-04T0147Z, F23). WFG-049 made every figure in the README's
opening paragraph traceable to a value in `docs/NUMBERS.json`, re-derived from
`data/processed/external/fire_2025_scale.json`. It did not make the *attribution*
traceable, and `scripts/check_readme_figures.py:88-92` is where that stops: the provenance
loop asserts only that `agency`, `as_of`, `scope`, `source_url` and `figure_status` are
present and non-empty.

Three disagreements exist today, all of them inside the apparatus:

| figure | registry says | `docs/data_sources.md` says | `README.md` says | the source says |
|---|---|---|---|---|
| 45,157 ha (interim) | 중앙재난안전대책본부, khan 2025-03-28 | 산림청, khan 2025-04-17 (`:194`) | 산림청 (`:204-205`) | **산림청** (khan 2025-04-17) |
| 사망 26명 | 중앙재난안전대책본부 (경북 5개 시군 합계), 뉴시스 + 서울신문 | 경상북도 재난안전대책본부, 대구MBC (`:190`) | no link of its own (`:193-199`) | not in the linked page |

The second matters most. `README.md:193-199` and its English twin at `:505-513` put the
whole chain parenthetical, 사망 26명 included, under one citation to
[아시아경제 2025-05-06](https://view.asiae.co.kr/article/2025050610030818823). That article
was opened by critic #5: it carries 99,289 ha, 149시간, 3,819동, 2,246세대 / 3,587명 and
1조 505억 원, and no death figure. The figure a judge is most likely to check is the one
whose link does not contain it.

**Done when:** (a) `interim_chain_area_ha_20250327` in the artifact reads agency 산림청 with
the khan 2025-04-17 URL (the page that carries both the figure and its 「산불영향구역」 label),
and `chain_deaths` uses one agency spelling that `docs/data_sources.md` also uses;
`register_fire2025_figures.py` re-run and `docs/NUMBERS.json` updated additively.
(b) 사망 26명 carries its own inline citation in both paragraphs, from the registry's URL.
(c) `check_readme_figures.py` compares each figure's registry `agency` and `source_url`
against the sources-table row and against the README's nearest inline link, and fails on a
mismatch; a test proves it fires by flipping one agency.

**Constraints:** additive registry writes only (CHARTER §3 rule 3); no figure value changes,
this row is about attributions; the artifact is under `data/processed/` so a new value needs
a new file, but correcting a provenance field on a figure the same sprint registered is a
correction of the record, not a regenerated result. If that reading is disputed, write the
corrected rows to a new `fire_2025_scale_v2.json` and point the registrar at it.

**Related:** WFG-049 (the value half, done), WFG-050 (the URLs themselves are unpinned;
snapshot them with sha256), critic #5 F23.

**Critic #8 addendum, 2026-09-04T0750Z — the disagreement narrowed to two spellings, and the
wrong one is the registry.** The paper lap corrected `paper/manuscript.md` this window and
registered `dgmbc2025toll` in `paper/references.bib` for it. Critic #8 opened that page
independently (<https://dgmbc.com/article/bLdh4s3M4pgcSdYI0MZPc>, 2026-09-04) and confirms
it verbatim: 「경상북도 재난안전대책본부가 3월 30일 오전 8시 30분을 기준으로 발표한 자료에
따르면 산불로 인한 경북 지역 사망자는 영덕군 9명, 영양군 7명, 안동시와 청송군 각각 4명,
의성군 2명 등 26명입니다」, and **neither 「중앙재난안전대책본부」 nor 「중대본」 appears
anywhere on the page.** So the only openable source that carries the 26 together with its
district split attributes it to the **province**, and the two documents still saying
otherwise are:

- `docs/NUMBERS.json` → `fire2025_chain_deaths`, whose `derivation` reads
  `agency: 중앙재난안전대책본부 (경북 5개 시군 합계) … source: newsis` — a breaking-news
  stub that names no agency at all;
- `README.md:198` 「경상북도 최종 집계·**중앙재난안전대책본부 확인**」 and its English twin
  at `:510` 「Gyeongsangbuk-do final tally confirmed by 중앙재난안전대책본부」, which assert a
  confirmation no source in this repository supports, under a link
  ([아시아경제 2025-05-06](https://view.asiae.co.kr/article/2025050610030818823)) that
  carries no death figure.

`docs/data_sources.md:190` and `paper/manuscript.md` are now both correct. The row is
therefore smaller than it was and more embarrassing: the SSOT is the one that is wrong, and
it is wrong against a page this repository already cites.

### WFG-053 · P0 · KCF · The booth card says the satellite was slower than the telephone; the paper says we cannot know

**Where the two answers are.** Judge-facing, asserting the ordering:

- `docs/auto/finals/DETECTION_FLOOR_CARD.md:17-19` — the card's front, in bold:
  「**위성은 사람보다 느렸습니다.** … 위성 트리거는 신고보다 각각 **+22분 · +34분 · +64분**
  뒤에 울렸을 것입니다」, and `:11-13`, the caveat that the clock is the 신고 시각 and that
  the delays are therefore written 「실제보다 위성에 유리하게」.
- `docs/detection_floor.md:29-38` (§1, 「가장 중요한 단서 — 기준 시각은 신고 시각입니다」),
  `:240-252` (§9 평결), `:262-275` (§10, the trigger-priority table that ranks 사람 신고
  first *because of* the ordering).
- `docs/auto/JUDGE_QA.md:234` — inside **Q10, a T0 question the student is told to memorise**:
  「위성 트리거는 사람 신고보다 각각 22분, 34분, 64분 **뒤에** 울렸을 것입니다」.

Withdrawing it, in the same repository, written the same night:

- `paper/manuscript.md:492-497` and `:533` — 「no artifact supports that and this paper does
  not claim it」 … 「Whether that is ahead of or behind the emergency call, this measurement
  cannot say」; Table 3's caption states the clock's provenance in full.
- `paper/GAPS.md` G5, and `docs/auto/NEEDS_HUMAN.md` NH-019.

**Why the paper is right and the card is wrong, from artifacts only.** No committed artifact
records a 신고접수시각 for any of these fires. `docs/data_provenance/fire_manifest.json` says
of the field the delays are measured from, for every one of the four detection fires,
`start/end/reported_ha are provenance only`, and each entry's own note describes the event
start as the **ignition** (`first hit … may lag ignition`). For `yeongdeok_2025` it is not
even ambiguous: `start` is `2025-03-22T12:15:00+09:00` and the same note reads `first hit
(2025-03-25) lags the 2025-03-22 ignition by days` — the manifest names that date the
ignition. So the design note's reading is not merely unsourced; the one artifact it cites
labels the field the other way.

The caveat inherits the error. The card tells a judge the figures flatter the satellite
*because* the clock is a report time. If the clock is the ignition, there is nothing to
flatter, and the 평결's 「어느 쪽으로 읽어도 위성이 사람보다 앞서지 않습니다」 has no
measurement behind it at all, because the human's time was never measured.

**Done when:** every document in the first list states the delays against the **recorded
occurrence time**, with the manifest's own provenance sentence beside them, and makes no
claim about whether the satellite preceded the call; the size floor (0.1–1 ha, and 「2 km
화소는 1 ha 규모 이하의 불을 분해하지 못한다」) carries the booth answer instead, because it
is true under either reading; §10's trigger-priority table keeps 사람 신고 first but on the
99 %-목격신고 statistic it already cites, not on the ordering; Q10's T0 answer is rewritten to
match, and the new Q10c stops being marked 「근거 없음」; NH-019 stays **open** for the
stronger claim, which still needs one 신고접수시각 from the author.

**Constraints:** this is a narrowing, not a new claim, so no number moves and no registry key
changes — `tests/test_detection_floor_card.py`'s 17 bindings must still pass untouched. Do
not delete the 평결 section; rewrite it under CHARTER §3 rule 3 with the superseded reading
annotated, the way NH-015 was handled. Do not wait on NH-019: the paper did not.

**Why it is P0.** `DETECTION_FLOOR_CARD.md` is the one evidence card the loop has finished,
it is meant for the booth panel, and Q10 is one of the fourteen answers the student is told
to know by heart. This is the same failure class as F21 — a value bound to a registry with a
free sentence around it — moved from a README paragraph to the three artifacts a judge
actually meets.

**Lap 20260904T0419Z — done.** The whole done-when clause holds and no number moved.

- `docs/detection_floor.md` §1 is retitled 「기준 시각이 무엇인지 우리는 모릅니다」 and quotes
  the manifest's own `start/end/reported_ha are provenance only` plus its ignition note
  (`first hit … may lag ignition`), with the withdrawn 신고 reading annotated below it rather
  than deleted (CHARTER §3 rule 3). §4's table label and §4's counterfactual sentence follow.
- §9's 평결 is now the **size floor** — 「2 km 화소는 대략 1 ha 아래의 불을 분해하지 못한다」 —
  which is true under either reading of the clock, with the old ordering verdict annotated.
  §10 keeps 사람 신고 first on §0's 99 %-목격신고 statistic instead of on the ordering.
- `docs/auto/finals/DETECTION_FLOOR_CARD.md`: caveat block, front sentence, table header,
  trigger table and the 「보여주지 않는 것」 list. The front sentence is now the size floor.
- `docs/auto/JUDGE_QA.md`: Q10 (T0) rewritten, Q10a's closing line narrowed, Q10c's
  「근거 없음」 banner replaced by a ✅ note making it the repository's standard answer.
- `docs/SESSION19_REPORT.md` keeps its body verbatim as a record and gains a dated 🛑
  annotation naming the current version; `docs/horizon_grounding.md` loses its false
  「same weakness in the same data」 cross-reference (filed as WFG-061).

**The row's constraint was almost right, and the exception is the finding.** It said the 17
tests in `tests/test_detection_floor_card.py` must pass untouched. Fifteen did. Two did not,
and both are worth recording:

1. `test_the_card_states_the_reference_time_caveat_first` asserted `"신고 시각" in text` — the
   one test guarding the card's most important sentence was **pinning the withdrawn reading**,
   so it would have failed the correct card and passed the wrong one. It now asserts
   「기록된 발생일시」 and `provenance only`. Every one of the 17 *number* bindings passed
   untouched, which is what the constraint was protecting.
2. The bare-digit tripwire fired on `2026-09-04`, `WFG-053`, `NH-019` and the `99 %`
   statistic. Its escape list is hand-maintained by design; all five are added with reasons.

**⚠ The gate was oversold by this row's first draft, and its own reviewer proved it twice.**
`tests/test_detection_ordering_is_not_claimed.py` (21 tests) bans the spellings this repository
actually shipped, and **that is all it does** — the reviewer escaped it with 「늦었」 for 「느렸」,
then with 「사람보다 22분 늦었」 against the fix, then reported that most of twelve further
rewordings (a line-wrap split, 인간/목격자 for 사람, reversed subjects, any English) still pass.
Both named escapes are closed and are permanent cases; the rest are listed as verified-uncaught
in the test's own docstring. **Read it as a ratchet against regression-by-copying, not as a
guarantee**, and note that the load-bearing protection for this claim is still a human reading
the card. The general fix is WFG-062. The row's own done-when — the documents themselves — is met
and was verified line by line. Historical note, the gate as first written bans five spellings
across the three Korean documents
**and `paper/manuscript.md`**, which is in the list as a regression anchor — it is the half of
the repository that got this right first, and a later lap "harmonising" it back to the card's
old wording is the failure this row exists to stop. Withdrawal prose is licensed per line by
the repository's own `<!-- forbidden-ok: -->` pragma, so there is no whole-file escape. Six
mutation cases put the exact shipped sentences back and require a failure; three neighbour
cases (「기록된 발생일시로부터 +22분」, the 99 % statistic, 「사람 신고를 일차 소스로」) require
a pass. `test_the_manifest_still_says_what_this_gate_rests_on` re-derives the premise from the
artifact, so if a real report time ever lands the gate fails and the narrowing is revisited on
evidence — which is what NH-019 asks the author for.

### WFG-054 · P0 · infra · A reply the loop cannot map is discarded, and the message is marked read

**Where:** `scripts/auto/decisions.py:100-104` (`cmd_apply` appends `key` to
`seen["applied"]` unconditionally), `:69-71` (`apply_one` returns the text unchanged and the
message `no such entry; recorded nowhere`), `:115-119` (`cmd_seen` reports `seen` when any
key starts with the message ref).

**Reproduced this lap**, in process, against the live `NEEDS_HUMAN.md` without writing it:

    apply_one(text, "NH-020", "yes, do it", …)
      -> "NH-020: no such entry; recorded nowhere"
      -> text changed?  False        the author's words appear nowhere?  True

`cmd_apply` then records `<message id>:NH-020` as applied anyway, and CHARTER §6 tells the
next lap to skip any message `decisions.py seen` reports as seen. So a reply that names an
id with a typo, or an entry a later lap has not written yet, is lost with no trace — and a
reply carrying one good line and one bad line marks the whole message read after applying
only the good one. The module's own docstring at `:15-16` promises `A decision the loop does
not understand is still recorded, as` noted `, never guessed at`.

This is NH-017's failure class rebuilt in code: the machinery added this window to make the
author's decisions verifiable can silently drop one.

**Done when:** `cmd_apply` records the seen key only when `apply_one` reports a change;
everything else is appended verbatim, with its message id and date, to a committed place a
later lap will read (an `## Unmapped replies` section at the foot of `NEEDS_HUMAN.md` is
enough and needs no new file); the run's exit code is non-zero when anything went unmapped,
so a lap cannot miss it; and `tests/test_decisions.py` gains one case per direction —
an unknown id leaves `decisions_seen.json` untouched and lands in the unmapped section, a
known id still closes and still records.

**Constraints:** never guess which entry an unmappable line meant (CHARTER §6). The report
must still quote the line, as this lap's prompt requires; the file record is in addition to
that, not instead of it.

### WFG-055 · P1 · IEEE · The paper's page limit is enforced by a proxy nobody has calibrated

**Where:** `paper/check_paper.py:26` (`LIMIT = 7500`) and its docstring's
「the 20-page budget incl. refs + title」; `docs/auto/CHARTER.md` §12 (「under 20 pages
including title page and references」); `paper/GAPS.md`, the ⚠ Length pressure section the
paper lap wrote itself.

**What is wrong.** Measured this lap: `check_paper.py` reports `body_words: 7479` against a
7,500 hard fail — **21 words of headroom**, so the next lap that adds a sentence fails the
gate. That is the paper lap's own declared state and it is honest. The finding is the layer
under it: the constraint CHARTER §12 states is *pages*, the gate measures *words*, and the
conversion has never been checked against the built document. The paper lap's own crude
recount over the `.docx` (8,909 words including captions, tables and 25 references, plus
seven full-width figures) lands nearer **21 pages**, so on the loop's own estimate the
invariant is already breached and no gate can see it. LibreOffice is present in the sandbox
but refuses to open the built document, so no lap has produced a page count.

`check_paper.py` also checks no section at all against §12's list (Abstract, Introduction,
Related work, Data and methods, Results, Discussion, Limitations, Conclusion, Data and code
availability, References) — which is WFG-045's substance, so that row is absorbed here.

**Done when:** one real page count exists for `paper/WildfireGuardian_Park_2026.docx`
(any route that works in the sandbox: a different converter, a docx page-break count, or a
PDF); `LIMIT` is re-derived from it or CHARTER §12 is corrected to the true budget, whichever
the measurement says; `check_paper.py` fails when a §12 section is missing from
`manuscript.md` or from the built document; and `GAPS.md`'s ⚠ section is replaced by the
measurement.

**Constraints:** do not cut a number or a caveat to make room (the paper lap's own rule).
Captions are free space because `build_docx.py` does not count them, which is also why the
word proxy drifts from the page count — say so in whatever replaces it.

### WFG-056 · P1 · infra · The push check that guards every push leaves no record of any push

**Critic #8, 2026-09-04T0750Z — verified, and it is worse than 「leaves no record」.** The
critic prompt asks this lap to confirm every day that each push in the window carried a
report, using `gates.py --assert-reported --base <previous push>`. Ran it against eight
bases spanning the whole 24 hours (`3156459`, `1113388`, `0ff1b36`, `8d1decf`, `12b8ac7`,
`5a0466e`, `b855943`, `8e0a6ad`). All eight exit 0, and all eight name the **same** report,
`docs/auto/reports/2026-09-04T0725Z-dev.md`, because the check is satisfied by any one new
report anywhere in the range. So it cannot answer the question it is asked: a window
containing ten pushes and one report passes exactly like a window containing one of each.
The ledger this row asks for is what would make the daily check real; until it exists, that
line of the critic prompt is unverifiable and this lap says so rather than reporting a pass.


**Where:** `scripts/auto/gates.py:103-149` (`assert_reported`).

**What is wrong.** The check is correct now — critic #5's F22 is properly fixed, and
`--diff-filter=A` is load-bearing and documented in the docstring. But it takes `--base` from
whatever the caller passes, normally `origin/auto/dev` at push time, and writes nothing.
After the fact the push boundaries are unrecoverable: this lap tried to verify the critic
prompt's step 2 (「every push in the window carried a report」) and could not, because
`<base>..HEAD` cannot be reconstructed from the repository once the branch has moved. The one
check whose whole purpose is to make pushes auditable is the one thing here that cannot be
audited.

**Done when:** every `--assert-reported` and `--assert-head` run appends one line
(`{utc, mode, base, head, verdict}`) to a committed ledger under `docs/auto/`, the ledger is
in `REPORT_ONLY` so it does not itself demand a report, and a critic lap can read a window's
push history out of it. A test asserts the line is written on both verdicts.

**Constraints:** append only, never rewrite (CHARTER §3 rule 7). The ledger is a record of
what a lap ran, not a claim that it was right.

**Critic #9, 2026-09-04T0950Z — reproduced, second window, on a smaller window that removes
the last excuse.** Critic #8 used eight bases spanning 24 hours. This lap used the four
commits of its own window (`12bf2d9`, `d8aec94`, `0965b15`, `ec2e813`), where the push
boundaries are known and only one report exists. All four exit 0 and all four name the same
report, `docs/auto/reports/2026-09-04T0855Z-dev.md` — including `--base d8aec94`, which is
the commit that *carried* critic #8's report, so the check answers 「yes, a report travelled」
for a range whose own report is behind the base. The check is not weakly informative here; on
a window of one report it is uninformative for every base. Two critic laps have now been
unable to perform the daily verification their own prompt asks for.

### WFG-057 · **P0** · infra · Six questions are invisible to every test that guards the Q&A bank, and one of them is the guard

**Where:** `tests/test_judge_qa_bank.py` `QUESTION_RE` matches `Q(\d+)`, so `Q10a` and `Q10b`
— and the three questions critic #6 added this lap — are seen by none of
`test_the_stated_tier_counts_match_the_tags` (the stated anti-padding guard),
`test_question_numbers_are_unique_and_contiguous`,
`test_every_question_states_its_evidence_and_its_gap` or
`test_every_t0_question_points_at_something_that_exists`.

**Done when:** the pattern accepts an optional letter suffix, the three header counts are
restated in the same commit so the tier test passes, and the drill table at §6 names the
lettered questions in their tiers. Verify it fires by deleting one 없는 것 line before fixing
it.

**Constraints:** the five lettered questions are good questions; this row widens the guard,
it does not remove them.

**Critic #9, 2026-09-04T0950Z — re-measured at `ce31b91`, and all three tier counts are now
wrong.** Counted with `grep -cE '^\*\*Q[0-9]+[a-z]? · T[012]'` and per tier:

| | header says (`:17-23`) | file holds | |
|---|---:|---:|---|
| total | 33 | **41** | +8 |
| T0 | 14 | **15** | +1 |
| T1 | 13 | **19** | +6 |
| T2 | 6 | **7** | +1 |

At `8e0a6ad` the file held 38 headers and the header was wrong by 5; at `12bf2d9` it held 40;
at `d8aec94` (critic #8's Q35) it held 41. So the gap widened in each of the last two windows
while the row sat `todo`, and the loop's own reports have started to disagree with each other
about it: critic #8 wrote 「38 questions and 15 T0s」 at a head that held **40** — it carried
the previous window's total beside the current window's tier count. A file that miscounts
itself makes every reader of it miscount too, which is the argument for this being P0 rather
than a tidy-up. **This lap deliberately added no question of its own to the bank** even
though the drill turned up two it could have added, because adding a 42nd header to a file
that says 33 makes the finding worse by the hand of the lap reporting it; the two questions
are WFG-069 and the WFG-062 escape rate instead.

### WFG-058 · P1 · paper · Figures in the Moreno look — done

Opened and closed by the 2026-09-04 laptop lap. The author supplied Moreno et al. (2025),
*Space-time modelling of wildfire initiation* (Trentino–South Tyrol), and asked that the
manuscript's figures look like its ten. The lap read every figure page, wrote the rules down
in `docs/auto/knowledge/FIGURE_STYLE_REFERENCE.md`, and rewrote `paper/style.py` to them:
all four spines as a thin black frame, `a)` `b)` panel letters (`style.label_panels`),
bars with a hairline edge and in-bar values, framed legends inside the panel, YlOrRd for
probability fields, and a palette of fire red / steel blue / neutral grey with teal, brown
and slate for extra categories. The legacy `OKABE[...]` names still resolve, so no figure
function needed a colour edit; F4–F7 gained panel letters. Figures were regenerated and
looked at once; `check_paper.py` is green.

**Not done, deliberately:** the manuscript captions and the study-area map (WFG-060).

### WFG-059 · P3 · science · Buildings as exposure, not fuel (post-finals)

The author supplied Theodori et al. (FireDX, 2026 preprint) and asked whether its
buildings-as-fuel pipeline should inform our forecasting, with the instruction not to
implement it if it should not. Verdict, recorded in
`docs/auto/knowledge/WUI_BUILDINGS_AS_FUEL.md`: **not before the finals.** FireDX feeds a
physics spread solver we do not have and may not add before the freeze; its Building Fuel
Model needs construction year, stories, occupancy and a hazard-zone overlay that no open
Korean layer we hold provides; and the authors themselves call the product uncalibrated
against observed loss.

**Done when (after 2026-10-24):** (a) the author obtains a 공공데이터포털 key for the
건축물대장 API (NEEDS_HUMAN, author-only); (b) 건물DB footprints for the six study fires are
downloaded and per-cell footprint fraction, minimum structure-separation distance,
construction era and structure type (목구조 / 철근콘크리트) are computed as a new
`data/processed/exposure/` artifact with its own registrar; (c) a leave-one-fire-out ablation
says whether either descriptor improves held-out discrimination or only in the two fires
that entered settlements; (d) the descriptors feed the routing objective as a *demand* layer
(where people are), not as fuel. **Constraints:** a retrain — post-finals only; additive
registry writes; every attribute from a named agency with a date.

**Related:** WFG-013 (footprints for 영덕 — report footprint fraction and separation
distance the FireDX way so this row can compare later), the vulnerability layer, IEEE plan.

### WFG-060 · P2 · paper · Study-area map in the Moreno Fig. 1 style

Moreno et al. open with a hillshaded map of the study area, wildfire centroids as circles
graded by burned area, a graticule with lat/lon labels, a scale bar and a boxed legend. The
manuscript has no map. **Done when:** `paper/make_figures.py` gains `F0_study_area` (or the
next free number) drawn offline from the DEM already under `data/` and the six fires'
registered burned areas, in `paper/style.py` (`EXCEEDANCE` ramp for classes, `PALETTE["fire"]`
for events), cited from the Data section, and looked at once. **Constraints:** no tile or
basemap fetch at build time; every number a registry key.

---

### WFG-063 · P0 · KCF · The trigger recommendation lost its evidence and the T0 answer kept it

**Where.** `docs/auto/JUDGE_QA.md:240` (Q10, **T0** — one of the fourteen the student is
told to recite); `docs/detection_floor.md:310` (the ban) and `:319` (§10 row 1);
`docs/auto/finals/DETECTION_FLOOR_CARD.md:28` (front sentence) and `:78` (trigger table).

**What happened, in order, inside one lap.** WFG-053 narrowed every judge-facing document
off the detection-ordering claim, which was right. The narrowing removed the ordering as the
ground for 「사람 신고 일차」, so §10 needed a different ground and reached for 「신고의 99 %가
목격 신고」. That lap's own reviewer then showed the 99 % is an unregistered year-to-date
interim (경향신문 2023-04-28) and had it struck from the booth card, and §10 gained a bold
paragraph at `:310` forbidding its use as support. Two things were left behind:

1. **Q10 was not re-read.** Line 240 still says the ground is 「크기 바닥과 「신고의 99 %가
   목격 신고」라는 통계」 — the exact statistic banned 60 lines away in the file Q10 cites as
   its 근거. The card and the design doc refuse it; the sentence the student says out loud
   rests on it.
2. **The remaining ground does not carry the claim.** The size floor says a 2 km pixel
   cannot resolve a fire below roughly a hectare. That rules the **satellite out** as an
   ignition-scale trigger. It says nothing about whether the human channel is fast, complete
   or primary — that was the 99 %'s entire job. So `detection_floor.md:319`'s
   「일차 소스로 설계해야 합니다」 and the card's 「사람 신고가 일차」 are now inferences with
   no support in any judge-facing document in this repository.

This is F27's shape one window later: one question, two documents, and the loop already
wrote the correction down in one of them.

**Done when:** every judge-facing document states only what the measurement carries —
「정지궤도 위성을 일차 트리거로 둘 수 없습니다」 — and none asserts 사람 신고 primacy without a
registered source; the 99 % clause is gone from Q10; `docs/detection_floor.md` §0 may keep
the statistic as background with its source, as `:310` already provides.
**Constraints:** no number moves; this is a claim-shape change, exactly like WFG-053.
`tests/test_detection_ordering_is_not_claimed.py` guards the *ordering* sentence and does
not see this one, which is WFG-062's case for the general registry.

**Critic #8 addendum, 2026-09-04T0750Z — the row got worse in two ways in one window, and neither is a new sentence.**

1. **The bank now instructs the student to say, at T0, a sentence the same bank forbids at
   T0.** `JUDGE_QA.md:240` (Q10, tier **T0**) still reads 「그래서 트리거 설계가 신고 우선,
   위성 확인입니다 — 근거는 순서가 아니라 크기 바닥과 「신고의 99 %가 목격 신고」라는 통계」.
   `JUDGE_QA.md:353` (Q10d, tier **T0**, added by critic #7 as the guard) lists that exact
   sentence among 「❌ 말하면 안 되는 것」: 「신고의 99 %가 목격 신고이므로」,
   「사람 신고를 일차로 두어야 합니다」. Both are in the fourteen the student is told to
   memorise word for word. This is no longer a stale sentence in a document; it is one
   document giving two opposite recitation instructions at the same tier, and the student
   has no way to tell which one is current.
2. **The finals screen shipped with the fix and the three documents did not, so the student
   would now contradict the screen behind them.** `scripts/finals.template.html:1858` and the
   built `web/finals.html` say 「이 측정이 말하는 것은 위성을 일차로 둘 수 없다는 것까지이며,
   어떤 소스가 일차여야 하는지는 재지 않았습니다」, which is exactly what this row asks for.
   `docs/finals_screen_v2.md:81-83` records that the lap wrote the screen in the fixed form
   deliberately, before the fix reached anywhere else. So the four surfaces are now split
   three against one, and the one that is right is the one a judge is looking at while the
   student speaks.

Nothing about the **fix** changes: it is still the same claim-shape edit at
`JUDGE_QA.md:240`, `docs/detection_floor.md:319` and
`docs/auto/finals/DETECTION_FLOOR_CARD.md:28,78`, and the screen is already the model
sentence to copy. What changes is that the cost of not doing it is now a contradiction the
student carries to the booth rather than a sentence a careful judge might catch.

### WFG-064 · P1 · paper · Two restyled figures have colliding labels

**Where.** `paper/figures/F2_lofo_auc.png`, `paper/figures/F7_dispatch_ordering.png`,
`paper/style.py`, `paper/make_figures.py`.

The Moreno restyle (WFG-058) is a real improvement and F7 panel a is the best figure in the
paper. Two defects survived the 「all seven regenerated and looked at」 claim in
`docs/auto/reports/2026-09-04T0401Z-dev.md`:

- **F2.** Uiseong-Andong's value label `0.878` sits at x ≈ 0.878 and the
  「mean of folds 0.89」 reference rule at x = 0.89, so the red dashed line runs through the
  label's last digit. The 「pooled 0.905」 annotation is drawn at the bottom of the axes and
  overlaps the x-axis line and its tick labels.
- **F7.** Panel b's 「deadline first wins」 uses the same teal as panel a's
  「nearest first」. Inside one figure, one colour, two meanings.

**Done when:** a value label whose x falls within about 1 % of a reference rule is offset
away from it (or the rule label is moved), the pooled annotation is inside the axes, panel b
has a hue no panel-a series uses, and all seven figures are opened and looked at once after
the rebuild. **Constraints:** `paper/style.py` only; no figure data changes; the docx is
rebuilt in the same commit.

### WFG-065 · P1 · KCF · The spread-rate figure is in a knowledge note and nowhere a judge reads

`docs/auto/knowledge/PYROGEOGRAPHY.md:45` carries 8.2 km h⁻¹ forward spread for the 의성 fire
(국가산림위성정보활용센터, thermal detections from 2025-03-22, described as the highest rate
reported for a Korean wildfire). It is the first question a fire-behaviour judge asks, and it
appears in no README paragraph, no `docs/data_sources.md` row, no registry key and no Q&A
answer. CHARTER §13 is why it has not migrated, and that rule is correct — the fix is to put
it through the registry, not to quote it from the note.

**Done when:** either 8.2 km h⁻¹ is registered with agency, as-of date, scope, status and the
URL a lap opened, added to `docs/data_sources.md` table A and answerable from `JUDGE_QA.md`;
or `docs/data_sources.md` records in one line why it stays out. **Constraints:** critic #7's
search confirmed the 8.2 km h⁻¹ figure and its 「국내 최고」 framing but did **not** confirm
the 「1.5× / 고성 2019 의 5.2 km h⁻¹」 comparison the note carries beside it; that half is not
registrable until a lap opens a page that states it.

### WFG-066 · P1 · infra · No bibliographic record is written from memory

`docs/auto/knowledge/PYROGEOGRAPHY.md:169` tags a reference
`[UNVERIFIED — not opened; author list from memory]`. Critic #7 verified it and every field is
right (Sullivan, A. L., Sharples, J. J., Matthews, S., Plucinski, M. P. (2014). *Environ.
Model. Softw.* 62: 153–163), confirmed against the FRAMES catalog entry
<https://www.frames.gov/catalog/53980> and the ScienceDirect listing, both opened
2026-09-04. So nothing is wrong today. It is filed because CHARTER §3 rule 5 is
「no fabricated citations」 and a remembered author list is a bibliographic record produced
without a source; the next one may not be right, and the tag would not distinguish the cases.

**Done when:** that entry reads `[verified 2026-09-04 · FRAMES catalog + ScienceDirect
listing]` with the phrase removed, and CHARTER §13 states the rule: a note may mark a
**claim** `[UNVERIFIED]`, and may never state an author list, year, volume or page range that
was not read off a record. **Constraints:** the other nineteen `[UNVERIFIED]` tags in the two
notes are honest and stay; this row is about one phrase and one rule.

### WFG-067 · P0 · KCF · The finals screen prints a commit id that does not exist

**Where.** `web/finals.html` (`"git":"a562045"`), rendered on the RELIABILITY tab as
「SYSTEM INTEGRITY · build 2026-09-04 07:11 UTC · commit a562045」 and visible in the
committed screenshot `docs/auto/finals/screens_20260904T0630Z/4_reliability.png`. Also
quoted at `docs/auto/finals/screens_20260904T0630Z/README.md:3`, in this file's WFG-017
row (`done(a562045)`), and in the three dev reports of 2026-09-04 (0714Z, 0719Z, 0725Z).

**What is wrong.** `git cat-file -t a562045` in a fresh clone of `auto/dev` answers
`fatal: Not a valid object name a562045`. The id is a pre-rebase hash: the WFG-017 lap
built the screen, committed, then `git pull --rebase origin auto/dev` rewrote its commits
(`d0d64fb`, `dc63a06`) and the stamp inside the built HTML kept pointing at the object the
rebase discarded. `scripts/build_finals.py:815`'s `git_head()` reads
`git rev-parse --short HEAD` at build time, and nothing afterwards re-reads it.

**Why it is P0 and not cosmetic.** This is the panel a judge is pointed at to check that
the numbers on the screen came from somewhere. It is also the first thing an ISEF or IEEE
reproducibility reviewer would type. The screen invites the check and then fails it, in the
one place the project stakes its credibility. Nothing else on the screen is wrong: the
three gate lines (`verify-numbers`, `check-forbidden`, `check-region-literals`) really ran
and really passed, and the three `DATA` rows carry live sha256 prefixes.

**Done when:** `web/finals.html` on `origin/auto/dev` carries a stamp that resolves, and a
test in `tests/test_finals_screen.py` fails when it does not — `git cat-file -e <stamp>`,
one line. **Constraints:** the gate must assert that the stamp **resolves**, not that it
equals `HEAD`; the commit that carries a build is always one later than the commit the
build was made at, so an equality gate would be unsatisfiable and the next lap would
weaken it. A lap that rebases after building rebuilds before it pushes.

### WFG-068 · P1 · infra · A routine that can do a row's work but cannot close it

CHARTER §12 confines the paper routine to `paper/` plus its own report. That isolation is
correct and should stay. Its consequence, unwritten until now, is that a backlog row the
paper lap completes stays `todo`: the row is in `docs/auto/BACKLOG.md`, which the routine
may not touch.

WFG-064 is the first case. The paper lap fixed both figures at `e28377c`, wrote the fix up
under the row's own number, listed two further defects it found and fixed, and left the row
reading `todo` with the note 「CHARTER §12 stops this routine editing `BACKLOG.md`」. Critic
#8 closed it by opening the two PNGs. Had no critic run, the next dev lap would have
claimed a done row and spent a lap discovering that.

**Done when:** the loop has one written mechanism for this and it is in CHARTER §12 —
either a committed `paper/BACKLOG_CLAIMS.md` the paper lap appends `{row, commit, what}`
to and the next dev or critic lap drains, or an explicit sentence that the paper lap names
its completed rows in its report and the daily critic closes them. **Constraints:** do not
widen the paper routine's write scope to `docs/auto/`; the isolation is the point.

### WFG-069 · **P0** · KCF · The document that forbade a conclusion opens by stating it

**Where:** `docs/detection_floor.md:13-15` (§0, the first two sentences of the file) against
`docs/detection_floor.md:310-320` (§10's 99 % ban), and `docs/auto/JUDGE_QA.md` Q10 · Q10d,
which both name §9/§10 of this file as their 근거.

**What is wrong.** §0 opens: 「한국의 산불 탐지는 사실상 전부 사람입니다. 보도된 해에
산림청·119 가 접수한 산불 신고의 **99 %가 목격 신고**였고 …(경향신문, 2023-04-28)」.

Two defects, and the second is the one a judge finds first.

1. **The conclusion WFG-063 spent a window removing is stated flatly at the top of the file
   the removal cites.** 「탐지는 사실상 전부 사람입니다」 is a stronger human-primacy claim
   than any sentence deleted this window; the ones that were deleted at least said 「설계
   함의」 or 「가정」. Its only ground is the 99 % clause immediately after it, and §10 of the
   same file says in bold that this value 「이 표의 근거로 쓰지 않습니다 … 판정단이 보는
   문서에서 이 값이 결론을 떠받치게 두지 않습니다」. §0 lets it hold up a conclusion in the
   first sentence of a 판정단이 보는 문서.
2. **The scope is stated wrongly, which is a CHARTER §3.5b breach on its own terms.** §0
   writes 「보도된 해에」 (in the year reported). The article is dated 2023-04-28 and its own
   word is 「올해」, so the figure is a four-month year-to-date accumulation — which §10
   spells out as 「연중 누계, 즉 잠정치」. Rule 5b: an interim tally is never presented as a
   final one. §0 presents it as an annual fact and the correction lives 300 lines away.

**Why both new gates are blind to it.** `priority_violations` requires a token from
`PRIORITY_WORDS`; the sentence contains none, so the rule never looks at it.
`primacy_violations` would fire on the 99 % clause, and the WFG-063 lap pragma-licensed that
line explicitly as 「§0 은 배경 설명으로 출처와 함께 그대로 둡니다」. Both decisions were
defensible in isolation; together they leave the strongest surviving primacy sentence in the
repository inside the guarded file, un-flagged, licensed.

**Done when:** §0's opening sentence claims only what a source supports — the 경북 152
cameras with 최초 발견 0건 over two years is a count from the same article and carries no
share — and the 99 % clause either carries the interim label §10 already wrote for it
(agency, as-of 2023-04-28, scope 연중 누계, status 잠정) or is dropped. `JUDGE_QA.md` Q10d's
❌ list already forbids the student saying it; the file it points at should not say it either.

**Constraints:** no number moves and no artifact is touched — this is one sentence and one
scope label. Do **not** add a question to `JUDGE_QA.md` while WFG-057 is open (the header
already miscounts the file by eight). Do **not** widen `PRIORITY_WORDS` to chase this
sentence: the escape class is 「no priority word at all」, which is WFG-062's problem and is
not fixed by another token.


**Record, 2026-09-04 (dev lap 20260904T1224Z).** The author's NH-021 decision — 「WFG-062 to
P0, top of the table」 — was applied at `c180a5a` by inserting the row into the **sprint-plan
table** (the three-column 「by | rows | what the author sees」 table) instead of the task
table. The decision was therefore recorded and not delivered: an eight-column row in a
three-column table renders as garbage, and CHARTER §4 step 3 sends every dev lap to the task
table, where WFG-062 no longer appeared at all. The row is moved to the top of the task
table here, unedited. The author's decision stands; only its placement was wrong.

---

### WFG-072 · P1 · infra · The English rule's three stated gaps, none of them closed

**Raised by the WFG-070 lap about its own work, 2026-09-04, under the gate MEMO 2026-09-04
records in the same lap:** when a lap writes 「this rule does not cover X」, X becomes a row,
because a limit stated in a docstring is a finding for the reader who already knew and a
hiding place from the one who did not. That is exactly how WFG-070 itself happened — 「any
of it in English, anywhere — no English pattern is gated」 sat in a docstring for three laps.

| gap | why it is open | what closing it costs |
|---|---|---|
| `paper/manuscript.md` ungated | its one variant-B hit is a false positive: the `[GAP: …]` marker at `paper/manuscript.md:657`, whose own opening clause is 「the delays cannot be read against either the true ignition or the emergency call」, split from its negation by a sentence boundary. Licensing it means an HTML pragma inside the file `paper/build_docx.py` converts to .docx, which CHARTER §12 gives to the paper routine | either a `[GAP:` -aware reader in the rule, or a positive anchor like `MANUSCRIPT_ANCHORS`. The manuscript is the IEEE surface, so this is the gap that matters after the finals |
| one-sided claims pass | variant B requires a machine-detection noun AND a human-channel noun in one sentence. That is what took the rule from 27 noisy hits to 4; it also means 「we designed the interface report-first」 passes | not fixable inside variant B. Needs the value-driven instrument, WFG-071 |
| semicolon-spanning claims pass | the `;` split is what lets the rule see `R3_science_gaps.md:22`, a real instance whose third clause carries a negation covering the first. It also splits 「Residents call first; the satellite catches up 22 minutes later.」 into halves with one side each | a scope-aware negation rule, which is the thing every family in this file says it is not |

**Three more gaps, added by this lap's independent reviewer after it blocked the push, and
none of them fixed:**

* **markdown headings are scanned by nothing.** `_blocks` flushes on any line starting with
  `#`, so `## The satellite fired after the human report` escapes the English rule *and*
  both Korean families. This is inherited — `priority_violations` has always had it and
  nobody noticed — so fixing `_blocks` changes three rules at once and needs its own
  before/after hit count on all seven guarded surfaces, not a one-line edit.
* **the pragma licenses a whole block, not a line**, for both structural rules. The comment
  above answer 7 of the drill brief exempts that entire paragraph permanently. The file's
  own 「it is per-line, on purpose」 is true only of the two literal-string families. Either
  narrow the structural pragma to the matching sentence, or stop claiming per-line.
* **the vocabulary is the real floor.** `EN_MACHINE_NOUNS` lacks `sensor`, `imager`,
  `pixel`, `hotspot`, `orbit`; `EN_HUMAN_NOUNS` lacks bare `report`, `people`, `public`. So
  「Report first, satellite second」 escapes with **both sides named**, which is worse than
  the one-side-only limit the docstring advertises. Not fixed by hand: adding the
  reviewer's words to the list it graded is corpus-fitting, and the fix that survives is
  WFG-071's — match the **value** from `docs/NUMBERS.json`, not the sentence.

**Done when:** either the manuscript is covered (by whichever of the two mechanisms above
survives contact) or `paper/GAPS.md` carries one line saying the English ordering claim is
ungated there and why; and the report that closes it prints a catch rate measured on
sentences its author did not write, per MEMO 2026-09-04.

**Constraints:** do not write a fourth hand-rolled string family — that is critic #10's F53
and the reason WFG-071 exists. Do not put a pragma in `paper/manuscript.md`. Do not widen
`EN_GUARDED` to the record files (`docs/auto/reports/`, `docs/auto/archive/`,
`docs/SESSION*_REPORT.md`), which CHARTER §3 rule 7 makes annotations rather than edits.

---

### WFG-070 · **P0** · KCF · The withdrawn claim is alive in the drill brief, in English

**Raised by critic #10, 2026-09-04, window `ce31b91..3a70e16`.**

Two laps (WFG-053, then WFG-063) withdrew the claim that the satellite trigger would have
fired *after* the human report, and narrowed five Korean surfaces to one sentence. Critic #9
wrote that it had 「grepped every `.md` and `.html` in the tree this lap」 and that the only
survivals were the `SESSION19_REPORT.md` record inside its own dated withdrawal block and
§10's paragraph explaining what its table used to be. **That is not true, and the reason it
was missed is that both claim gates and both greps are in Korean.**

| where | what it says |
|---|---|
| `docs/auto/research/RESEARCH_BRIEF_2026-09-03.md:75` | 「a satellite trigger would have fired +22/+34/+64 min *after* the human report (FIRMS +117/+151/+17) … So the design is **report-first, satellite-confirm**」 |
| `docs/auto/research/sweeps_2026-09-03/R3_science_gaps.md:22` | 「GK2A detection floor: +22 / +34 / +64 min **after the human report** (n = 3)」 |

The first sits under the heading 「**The ten hardest judge questions, with the answers that
survive the verdicts**」, in a file `docs/auto/ROUTINE_PROMPTS.md` tells the critic routine to
read sections (a) and (c) of on **every** lap. It is the student's own drill material. A
student who reads answer 7 the night before the booth says at the booth the sentence five
other documents were rewritten to stop them saying.

**Why no gate saw it.** `tests/test_detection_ordering_is_not_claimed.py` has two claim
families — `BANNED_PRIMACY` and the `PRIORITY_WORDS`/`SOURCE_NOUNS` structural rule — and
every token in both is Korean. `PRIMACY_GUARDED` is five files; the new
`test_external_figures_carry_their_scope.py` `GUARDED` is seven. Neither list contains
`README.md`, and neither contains anything under `docs/auto/research/`. `paper/manuscript.md`
is in the second list and not the first, so the manuscript is guarded for a figure's labels
and unguarded for the claim. The English half of this repository is where the IEEE submission
and half the README live.

**Also in the same file:** question 10's `Files:` line cites `docs/auto/AI_DISCLOSURE.md`,
which was retired to `docs/auto/archive/AI_DISCLOSURE_retired_2026-09-04.md` at the author's
instruction (NH-008). A judge answer that points at a deleted file is the WFG-067 shape in
prose.

**Done when:** both research files carry a **dated annotation** — not an edit; CHARTER §3
rule 7 makes them records — saying which claim was withdrawn, on what date, and where the
current wording lives (`docs/detection_floor.md` §9–§10, `JUDGE_QA.md` Q10 · Q10d); the dead
`AI_DISCLOSURE.md` reference is corrected to the archive path; and
`tests/test_detection_ordering_is_not_claimed.py` either gains the English spellings
(`after the human report`, `report-first`, `human report is the primary trigger`) or its
docstring says in one sentence that the family is Korean-only and names the surfaces that
are therefore unguarded. Stating the limit is an acceptable outcome; leaving it unstated
is not.

**Constraints:** do not edit the two research files' prose — annotate above it. Do not add a
question to `docs/auto/JUDGE_QA.md` while WFG-057 is open. Do not widen the guard lists to
every file in the tree; the record files (`docs/SESSION*_REPORT.md`,
`docs/auto/reports/`, `docs/auto/archive/`) are out of scope by the same rule 7 that makes
this an annotation.

---

### WFG-071 · P1 · infra · Drive the external-figure gate off the registry, not beside it

**Raised by critic #10, 2026-09-04. This is the lap's root objection as a row.**

`tests/test_external_figures_carry_their_scope.py` (shipped 2026-09-04T1020Z) is the first
mechanical check of CHARTER §3 rule 5b and it is a real improvement: it publishes its own
catch rate, folds in six escapes a reviewer found, and keeps two it cannot close as a test
that **fails if the docstring ever stops being true**. Nothing below takes that away.

What it asks of every guarded block is that the block carry the figure's **agency**, its
**as-of date** and its **scope**. Those are three fields `docs/NUMBERS.json` already stores,
under exactly those names:

| | count |
|---|---:|
| registry keys total | 312 |
| keys with `provenance: external` | **16** |
| of those, carrying `agency` **and** `as_of` **and** `scope` | **16** |
| figures in the new gate's `EXTERNAL_FIGURES` | **2** |
| of those 2, present in `docs/NUMBERS.json` | **0** |

So the repository now keeps two registries of external figures: one structured, complete on
its three labels, machine-readable and 16 entries deep, and one hand-written in a test file
holding the two 경향신문 2023 figures that are in no registry at all. The gate reads the
second and not the first.

**The cost is not hypothetical and it is six windows old.** Five of the 16 are printed today
*inside files the new gate lists as `GUARDED`*: `docs/auto/JUDGE_QA.md` (99,289 ha, 26명),
`web/finals.html` (3,819동, 3,587명), `paper/manuscript.md` (99,289, 3,819, 3,587, 2,246,
104,788). The gate does not look at any of them. And one of them is **WFG-051**:
`fire2025_chain_deaths` carries `agency: 중앙재난안전대책본부`, while `paper/manuscript.md:37`
— a `GUARDED` file — calls the same 26 deaths 「the **provincial** disaster headquarters'
count」, `docs/data_sources.md` says 경상북도 재난안전대책본부, and `README.md:198` says
「경상북도 최종 집계·중앙재난안전대책본부 확인」. Four documents, three attributions, one
number, and the registry field that would settle it is read by nothing.

A check that compared each printed value against **its own key's** `agency` string would have
failed on the manuscript line the day it was written. It would also be **language-neutral**,
because it matches a value rather than a sentence — which is the one property neither
hand-rolled family has (WFG-070).

**Done when:** one function iterates `docs/NUMBERS.json` for `provenance == "external"`, and
for each `GUARDED` file and each block printing that key's value in the spellings the
documents use, requires a token from that key's own `agency` / `as_of` / `scope` in the same
block, or a `<!-- scope-ok: <key> — <reason> -->` pragma naming the key. **The lap prints the
number of failing blocks on the current tree in its report**, before fixing any of them. If
WFG-051's manuscript line is among the failures, the instrument is right and both
hand-rolled families should migrate onto it. If the output is mostly noise, that is the more
valuable answer and WFG-030's shape — every judge-facing claim sentence cites a registry key
or an artifact — is the right instrument instead.

**Also fix, and it is one character:** the witness-share pattern accepts `%` and `퍼센트` and
not **`％`** (U+FF05, fullwidth), which is what a Korean IME produces by default.
Critic #10's outside mutation set of twenty blocks — none of them in `INCOMPLETE_BLOCKS` —
is caught **14 / 20** by the shipped gate, against 2 / 20 for the previous family, and
`목격 신고 99％가 …` is one of the six escapes. The other five are the figure restated as a
count (`100건 가운데 99건`), 목격 paraphrased (`눈으로 본 사람의 신고`), and three restatements
of the camera figure with no digit beside 최초 (`한 건도 없었습니다`, `먼저 발견하지 못했습니다`,
`인지한 최초 산불: 0건`). Only the first is cheap; the rest are the class this instrument
cannot close, and the docstring already says so.

**Constraints:** in NH-021's scope. Until the author answers, the backlog table order stands
and this row does not jump the booth rows. Add nothing to `EXTERNAL_FIGURES` by hand as a
substitute for this row — that is the move this row exists to stop.

---

### Correction to the record · WFG-062's priority did not change

Critic #9's report (`docs/auto/reports/2026-09-04T1005Z-critic.md`) says in three places that
it raised WFG-062 from P1 to P0 — in F47's header, in its cheapest-test paragraph, and in the
section headed 「Updated, not duplicated」, whose job is to list what the lap changed. The
table row still reads `| WFG-062 | P1 |`, and `git show adf712d -- docs/auto/BACKLOG.md`
shows that lap edited the row's **text** and not its priority column. `CRITIC_LATEST.md` from
the same lap says the opposite of its own report: 「the table order stands and this lap does
not reorder it」, and refers the question to NH-021.

**Critic #10 leaves WFG-062 at P1**, deliberately. NH-021 is the author's open decision and
its stated default is that silence means table order, which puts the booth rows first. A
critic re-deciding it by editing a priority column would take the choice the entry exists to
give the author. The record is corrected here rather than the row: what critic #9 did was
raise the row's **evidence**, not its priority.

**NH-021 (author, 2026-09-04):** WFG-062 goes first — raised to P0 and moved to the top of the table so the next dev lap claims it; booth rows resume after.

### WFG-073 · P1 · science · Designated sites as refuge candidates (영덕)

The author attached two 주소정보누리집 downloads on 2026-09-04 (not the building layer; see
`docs/juso_yeongdeok.md`). For the first time the repository holds agency-designated outdoor
evacuation sites and cooling centres for 영덕 with coordinates. **Done when:** a new script
writes `data/processed/refuge_designated_yeongdeok.json` with (a) the snap distance from each
router refuge to the nearest designated site, (b) reachability of each designated site under the
canonical forecast and budget, (c) the decision-shift counts with designated sites as the refuge
set, side by side with the shipped set; a `docs/refuge_designated.md` with method, result, caveats
(the categories are earthquake and heat, not wildfire); numbers registered additively; tests.
**Constraints:** no committed artifact modified; the comparison is an arm, not a replacement.

### WFG-074 · P1 · product · Depots and notification targets from the 민원행정기관 layer

**Done when:** `rescue.load_depots` (or its documented equivalent) reads the 119 centres, 파출소
and 읍면동 offices from `data/processed/external/juso_yeongdeok/minwon_agencies.geojson` as a
named depot set; the dispatch experiment can be pointed at it by config; the A4 alert sheet
names the nearest 읍면동 office and its phone number from the same file; docs and tests.
**Constraints:** a new arm with new filenames; the synthetic depots stay as the control.

**WFG-060 update 2026-09-04:** the routing map half is done — `F8_routing_map` in `paper/make_figures.py`, cited after F5 in the manuscript, drawn from `routing_demo_canonical.npz`, `real_roads_real_hazard_canonical.json` and the three committed snapshots (DEM, walk graph, shelters), routes recomputed with the repository router and matching the committed partition. The six-fire study-area map is **blocked**: no per-fire burned-area keys in the registry and the other five fires' DEMs are laptop-only. Unblock by registering per-fire burned areas from the fire manifest (a laptop lap) and committing a coarse DEM or coastline; until then the row stays here.

### WFG-075 · **P0** · science · The 영덕 subset is another county, and a green test pins it

**How it was found (critic #11, 2026-09-04).** Not from a source — from the committed files.
Reading the coordinates out of the eight GeoJSON files in
`data/processed/external/juso_yeongdeok/` gives, for every layer:

| layer | n | lon range | lat range |
|---|---:|---|---|
| minwon_agencies | 74 | 128.686–129.067 | 36.799–37.051 |
| samul_eqout_point (지진옥외대피장소) | 27 | 128.732–129.064 | 36.817–37.047 |
| samul_coolingcen_point (무더위쉼터) | 99 | 128.665–129.148 | 36.786–37.063 |
| samul_busst_point (버스정류장) | 28 | 128.649–129.102 | 36.785–37.064 |
| samul_firehydr_point (소화전) | 6 | 128.736–129.058 | 36.883–37.015 |
| samul_lifesav_point (인명구조함) | 5 | 128.729–128.954 | 36.788–37.011 |
| samul_eqwav_point (지진해일긴급대피장소) | **0** | — | — |
| samul_emerwat_point (비상급수시설) | 0 | — | — |

`regions.lookup('yeongdeok_2025').bbox_wgs84` is `(129.25, 36.30, 129.55, 36.60)`. No point in
any layer is inside it, and none is even adjacent: the easternmost point of the new data is
0.10 degrees west of 영덕's western edge and the southernmost is 0.18 degrees north of its
northern edge. The same repository drew `paper/figures/F8_routing_map.png` on the same day,
correctly, over 129.3–129.5 E with the coastline on the right of panel b.

**Done when:** (a) the eight `juso_yeongdeok_*_count` entries in `docs/NUMBERS.json` carry an
added, dated annotation saying the scope label is wrong and the value must not be used (added,
never edited, CHARTER §3.2/§3.3); (b) `docs/juso_yeongdeok.md` opens with the correction and
states what is and is not known about the county identity; (c) NH-005's and NH-012's
2026-09-04 annotations are corrected as dated amendments; (d) WFG-073 and WFG-074 stay
`blocked` until the re-cut lands; (e) `tests/test_juso_yeongdeok.py` stops asserting
`sigungu_cd == "47920"` as correct and instead asserts the artifact is where its label says
(that assertion is WFG-076's, so this row may mark the test `xfail` with a reason pointing at
NH-022, and must not delete it).
**Correction 2026-09-04 (the closing lap, after independent review):** the 「about 45 km」 in
this row's own paragraph above is not a computed figure. Measured over the 239 committed points
against the box, the nearest is 30.5 km and the farthest 65.6 km. The row's claim needs no
distance and stands without one: 0 of 239 inside, no overlap on either axis. No kilometre figure
was written into the registry or into any judge-facing document.

**Constraints:** the loop cannot re-cut the data — `data/raw/juso/` is git-ignored and
laptop-only, and `extract_juso_yeongdeok.py` returns early without it. Do **not** guess the
correct 시군구 code from memory; WFG-066 is the standing rule that an identifier not read off a
record is not written down. The code must be read off 행정표준코드 (code.go.kr) by whoever
re-runs it. Nothing about the two source zips is in question: the digests, dates, agency and
CRS note all stand; only the filter constant and every label derived from it are wrong.

### WFG-076 · **P0** · infra · Assert that an artifact is where its label says it is

**Why this is the general row and not a patch.** WFG-049 registered figures so prose could not
drift from artifacts. WFG-062 and WFG-071 are about claim sentences and external figures. All
three constrain the relationship between a **document** and a **number**. F54 is the first
defect where the number and the document agree perfectly with each other and both are wrong
about the world, and the thing that would have caught it — a bounding box — was already
committed in this repository and consulted by nothing.

**Done when:** a gate walks the artifacts whose name or registry `scope` names a region known to
`wildfireguardian.utils.regions`, loads their geometry, and fails when it falls outside that
region's `bbox_wgs84` plus a committed, named buffer; the buffer is a stated choice with a
reason, not a number chosen to make today's tree pass; the row publishes the gate's catch rate
against a mutation set written by someone who did not see the patterns (NH-021's standard, and
WFG-062's); and the limits it cannot close are parametrised as open, in the style of
`tests/test_external_figures_carry_their_scope.py::test_the_escapes_this_gate_cannot_close_are_still_open`.
**Constraints:** no committed artifact is modified to make it pass; an artifact that legitimately
straddles a region boundary is declared in the gate with its reason, not silently buffered past.

**NH-022 resolved 2026-09-04 (laptop):** the 영덕 subset was re-cut with 시군구 code 47770 and verified (addresses, tsunami layer present, set inside the canonical box); WFG-073 and WFG-074 are unblocked; the box-containment rule the critic asked for lives in the extractor and `tests/test_juso_yeongdeok.py`.

### WFG-090 · P1 · science · Is age alone the right rescue-priority proxy?

Raised by 이해평 (강원대) on 2026-09-04. **Done when:** `docs/vulnerability_priority.md` states what the shipped rescue ordering keys on, cites the vulnerability literature the knowledge base already holds, and reports one documented alternative weighting (mobility, living alone, health) run as a new arm on committed data with new filenames; the shipped ordering stays the control; numbers registered additively. **Constraints:** no retrain; no committed artifact modified.

### WFG-091 · P1 · KCF · Which agency acts on the dispatch recommendation?

Raised by 이해평. **Done when:** the decisions document and `docs/auto/JUDGE_QA.md` say which body the model assumes acts on each output (walk-out routes, rescue order, notification) and name the forest-vs-residential response split as a limitation, with the 산림청 / 소방청 role statements cited by URL.

### WFG-092 · P1 · science · SILVIS global WUI map check

Suggested by Volker Radeloff. **Done when:** a dated `## Update` in `docs/auto/knowledge/WUI_BUILDINGS_AS_FUEL.md` carries the map view for the five counties (screenshot committed under `docs/auto/images/`), Carlson et al. 2022's thresholds as read from the paper, the comparison with the repository's own structure data (`juso_yeongdeok`, OSM buildings), and the decision (a) or (b). The author then replies to Radeloff. **Constraints:** the map is read, not fetched at build time; no number enters a judge-facing document without the registry.

### WFG-093 · P1 · science · Pedestrians leave mapped paths

Raised by John P. Wilson. **Done when:** `docs/routing_limitations.md` has a section on the network-graph assumption and `docs/auto/knowledge/ROUTING_FUNDAMENTALS.md` a dated update; a feasibility note decides whether cost-surface routing is a post-finals arm (with the DEM and land-cover already committed) or is declined, with reasons. **Constraints:** no new router before the freeze.

### WFG-114 · P0 · science · The fair opponent for the headline (present perimeter + buffer)

> ⚠ **Renumbered from `WFG-108` by critic #20 (2026-09-05).** Two live rows held that id: this one, added by the author's laptop session `9442430` at 14:24Z, and the `make finals-bundle` row filed by the 1302Z dev lap at 13:02Z. The older row keeps the id because it is quoted in `docs/auto/finals/BOOTH_SETUP.md:86`, `docs/auto/MEMO.md:1012`, `docs/auto/CRITIC_LATEST.md` and `docs/auto/reports/2026-09-05T1302Z-dev.md`. Reports are records and are not edited, so `2026-09-05T1424Z-manual.md` and `2026-09-05T1449Z-manual.md` still call this row `WFG-108`. Priority, scope and done-when are untouched.

Decided by the author on 2026-09-05 (NH-027 option A). **Done when:** a new arm on the committed 의성·안동 hazard field refuses every cell with p ≥ p_cut at slice 0 dilated by 1 km, routes the same scanned origins to the same refuges under the same budget, and `docs/present_perimeter_arm.md` reports, with registered keys, how many of the 91 origins that were safe only on the forecast-aware route are also safe on this present-aware route, and how many remain forecast-only; the Q&A card from WFG-104 and 3막's sentence carry the measured number. **Constraints:** routing-only, no retrain, no re-acquisition; the fire-blind arm stays as the control; the number is reported whatever it says (CHARTER §3).

**NH-027, second question (2026-09-05):** WFG-051, WFG-076, WFG-078 and WFG-082 were P0 infra rows sitting below P1 rows; they are loop hygiene under CHARTER §14b and are now P1, which removes the §3b conflict without moving them up.

### WFG-121 · P0 · KCF · The fair-opponent line on every judge-facing surface

The author's framing decision (2026-09-06, Claude Code session): keep the headline, add the fair-opponent line. **Done when:** every surface that prints the 91-of-368 contrast prints, in the next sentence, the present-perimeter margin (9 of 368 at 1 km) and the one-line buffer finding, all from registered keys; the 3막 script and the Q&A card (WFG-104) say the same words; `tests/` pin that the two sentences travel together. **Constraints:** no rewrite of the README's 2025-fire paragraph; numbers only from the registry; the 91 stays.

### WFG-122 · P1 · science · The budgeted reading of the one bucket that runs against the project

NH-031 option A. Additive registration only; the committed value stays and gains a caveat naming the asymmetry (`classify` gives the fire-blind route no budget; `future_aware_route` enforces 600 min). Other regions are not re-derived here (NH-031 C was not chosen).
