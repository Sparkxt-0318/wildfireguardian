# Scorecard — what the five judges would score today

The critic lap appends one dated row **only when the window's diff plausibly moved a
score**; a lap that moved nothing leaves the table alone and says so in its report.
Scores are 0–20 per row against `docs/auto/RUBRIC.md`, Track B five first
(연구 목적 · 설계와 방법론 · 데이터 수집·분석·해석 · 창의성 · 제출 자료), then Track A five
(개발 목적 · 설계와 방법론 · 구현 및 유용성 · 창의성 · 제출 자료). The track assignment is
not published, so both tables are kept.

These are **one critic's estimate of what a judge would give on the current tree**, not a
measurement. They exist to show direction between laps. A score that moves without a
commit to point at is a defect in the scoring, not evidence of progress.

## The series, both tracks on one row (added 2026-09-04 by critic #7)

The routine prompt asks for one table: ISO date, then the five Track B rows in rubric order,
then the five Track A rows. It is built from the two tables below, which stay exactly as
seven critic laps wrote them and are the record. Append here **and** to the track table you
are scoring; never edit a row that is already written.

Track B: 연구 목적 · 설계와 방법론 · 데이터 수집·분석·해석 · 창의성 · 제출 자료.
Track A: 개발 목적 · 설계와 방법론 · 구현 및 유용성 · 창의성 · 제출 자료.

| date | head | B·목적 | B·설계 | B·데이터 | B·창의 | B·자료 | **B/100** | A·목적 | A·설계 | A·구현 | A·창의 | A·자료 | **A/100** |
|---|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 2026-09-03 | `1113388` | 15 | 15 | 16 | 15 | 11 | **72** | 15 | 15 | 11 | 15 | 11 | **67** |
| 2026-09-03 | `0ff1b36` | 15 | 15 | 17 | 15 | 13 | **75** | 15 | 15 | 11 | 15 | 12 | **68** |
| 2026-09-03 | `8d1decf` | 15 | 15 | 18 | 15 | 13 | **76** | 15 | 15 | 11 | 15 | 12 | **68** |
| 2026-09-04 | `12b8ac7` | 14 | 15 | 18 | 15 | 13 | **75** | 14 | 15 | 11 | 15 | 12 | **67** |
| 2026-09-04 | `5a0466e` | 15 | 15 | 18 | 15 | 15 | **78** | 15 | 15 | 11 | 15 | 13 | **69** |
| 2026-09-04 | `b855943` | 16 | 15 | 18 | 15 | 15 | **79** | 16 | 15 | 11 | 15 | 13 | **70** |
| 2026-09-04 | `8e0a6ad` | 16 | 15 | 19 | 15 | 16 | **81** | 16 | 15 | 11 | 15 | 13 | **70** |
| 2026-09-04 | `12bf2d9` | 16 | 15 | 19 | 15 | 16 | **81** | 16 | 15 | **14** | 15 | 13 | **73** |
| 2026-09-04 | `ce31b91` | 16 | 15 | 19 | 15 | **17** | **82** | 16 | 15 | 14 | 15 | 13 | **73** |
| 2026-09-04 | `3a70e16` | 16 | 15 | 19 | 15 | 17 | **82** | 16 | 15 | 14 | 15 | 13 | **73** |
| 2026-09-04 | `83f49bc` | 16 | 15 | **17** | 15 | **18** | **81** | **The first fall in 데이터 since `12b8ac7`, and the first time a green gate in this repository is holding a false claim about the world in place.** The window's headline data event is an external dataset ingested the right way and pointed at the wrong county. `scripts/extract_juso_yeongdeok.py:32` filters on 시군구 47920 labelled 영덕군; every point in the eight committed GeoJSON files falls at 36.78–37.05 N / 128.65–129.15 E, and `regions.lookup('yeongdeok_2025').bbox_wgs84` is (129.25, 36.30, 129.55, 36.60) — **no overlap on either axis**, about 45 km apart. Two tells inside the artifact itself: not one of the 239 points is east of 129.15 E, for a county whose entire land area is, and the 지진해일긴급대피장소 layer returned **zero** rows, which `docs/juso_yeongdeok.md:12` records as a fact about 영덕 rather than reading it as the contradiction it is. **-2 rather than -1** because the machinery around the mistake is complete — a manifest with both zip digests, dates, a CRS note and a filter string, eight registry keys each carrying `scope: 영덕군`, three tests — and one of those tests, `tests/test_juso_yeongdeok.py:11`, **asserts `sigungu_cd == "47920"`**, so the suite now enforces it; 1367 tests pass. Reproducibility machinery wrapped around a mis-labelled artifact is worse than none, because it certifies. **Not -3**, for three reasons this lap checked rather than assumed: nothing judge-facing prints these values (README, `web/finals.html`, `paper/manuscript.md`, `JUDGE_QA.md` all clean), the defect is one constant rather than a method, and the same window's other artifact is the best 해석 work here yet — `F8_routing_map.png` draws the 32.6 % walk-coverage limitation in panel (a) instead of describing it, and its 458 = 414 + 42 + 2 partition and its 「50 OSM POIs」 both verify against the committed artifacts. **제출 자료 17 to 18** on that same figure: hillshade, forecast field with 0.5 isolines at 0 and 720 min, the walk network, refuges, every scanned origin classed as in the committed artifact, three worked routes recomputed at figure time by the repository's own router with the recomputed partition equal to the committed one, a graticule, two scale bars and a boxed legend, and a caption that says the isoline smoothing is display-only. It stops at 18 and not 19 on two defects now in their fourth and sixth windows: `web/finals.html` still carries `"git":"a562045"`, which `git cat-file -t` still cannot resolve (WFG-067), and `JUDGE_QA.md:17-23` still tells the student 33 / 14 / 13 / 6 where the file holds **41 / 15 / 19 / 7** (WFG-057), counted again this lap. 연구 목적 16, 설계와 방법론 15 and 창의성 15 held: nothing in this window touched a model, split, metric, arm, coupling or protocol |
| 2026-09-04 | `83f49bc` | 16 | 15 | **17** | 15 | **18** | **81** | 16 | 15 | 14 | 15 | 13 | **73** |
| 2026-09-04 | `c65dc56` | 16 | 15 | **18** | 15 | **17** | **81** | 16 | 15 | **15** | 15 | 13 | **74** |
| 2026-09-04 | `baf6962` | 16 | 15 | 18 | 15 | **18** | **82** | 16 | 15 | 15 | 15 | **14** | **75** |
| 2026-09-04 | `ed35f0d` | 16 | 15 | **19** | 15 | **17** | **82** | 16 | 15 | 15 | 15 | **13** | **74** |
| 2026-09-05 | `43710f7` | 16 | 15 | 19 | 15 | **18** | **83** | 16 | 15 | **16** | 15 | **14** | **76** |
| 2026-09-05 | `c37f27e` | 16 | 15 | 19 | 15 | 18 | **83** | 16 | 15 | **17** | 15 | 14 | **77** |
| 2026-09-05 | `26e200d` | 16 | 15 | 19 | 15 | **19** | **84** | 16 | 15 | 17 | 15 | **15** | **78** |
| 2026-09-05 | `6afd252` | 16 | 15 | 19 | 15 | 19 | **84** | 16 | 15 | 17 | 15 | 15 | **78** | *(critic #18: every row held. Appended per the routine prompt rather than omitted, so the series shows the flat window rather than hiding it.)*
| 2026-09-05 | `92bfc4f` | 16 | 15 | 19 | 15 | 19 | **84** | 16 | 15 | **18** | 15 | 15 | **79** | *(critic #19: one row moves, A 구현 및 유용성 17 to 18. The booth recipe exists, every command in it was executed, and the bundle gained a checker that runs on a borrowed laptop.)*
| 2026-09-05 | `ce262fe` | 16 | 15 | 19 | 15 | **18** | **83** | 16 | 15 | **17** | 15 | 15 | **78** | *(critic #20: one defect, scored once per track. The judged screen's reproducibility panel prints a commit id `git merge-base --is-ancestor ... HEAD` rejects, and the Q&A answer for that panel says it is reachable — WFG-115.)*
| 2026-09-05 | `492364c` | 16 | 15 | 19 | 15 | **17** | **82** | 16 | 15 | 17 | 15 | **14** | **77** | *(critic #21: one defect, scored once per track, and it is the Q&A bank rather than the screen. The window itself is one changed line — the 18:17Z dev lap claimed the author's WFG-114 and pushed nothing — so nothing else could move.)*
| 2026-09-05 | `f118bfe` | **17** | 15 | 19 | 15 | 17 | **83** | 16 | 15 | 17 | 15 | 14 | **77** | *(critic #22: one row moves, B 연구 목적 16 to 17, on the fair-opponent arm. Four rows held for reasons that pull in both directions and are given below; Track A is flat because nothing in this window reached a surface a judge sees.)* *(Scored at `f118bfe`. The author's `4d705df` landed 23:12Z, after these measurements; it touches no artifact and no test, so no row is re-scored for it, but its WFG-121 is the row that would move A 제출 자료 next window.)*
| 2026-09-06 | `de7bd0a` | 17 | **14** | 19 | 15 | 17 | **82** | 16 | 15 | 17 | 15 | 14 | **77** | *(critic #23: one row moves, B 설계와 방법론 15 to 14 on the buffer sweep's resolution; everything else held. Full reasoning in the two track tables below.)* |
| 2026-09-06 | `91d3e05` | 17 | **15** | 19 | 15 | **18** | **84** | 16 | 15 | 17 | 15 | 14 | **77** | *(critic #24: two rows move, both Track B, both on the manuscript lap — 설계와 방법론 14 to 15 (the width claim is now stated at the strength the grid supports) and 제출 자료 17 to 18 (WFG-126 closed, the NH-032 deadlock named in the paper's own voice, the margin-determining figure withdrawn). Track A is flat because the only substantive commit in the window is a manuscript lap and these judges do not read it. B 데이터 holds at 19 rather than rising, on WFG-128.)* |
| 2026-09-06 | `b70e464` | 17 | 15 | 19 | 15 | 18 | **84** | 16 | 15 | 17 | 15 | **15** | **78** | *(critic #25: one row moves, A 제출 자료 14 to 15, and it is the booth printables. Track B does not move: these five judges do not read the manuscript and nothing a research judge reads changed in the window.)* |
| 2026-09-06 | `b2bdaf0` | 17 | 15 | 19 | 15 | 18 | **84** | 16 | 15 | **18** | 15 | **14** | **78** | *(critic #26: Track B flat; Track A two rows move in opposite directions and cancel. 구현 및 유용성 17 to 18 — the judged screen's counts are right for the first time in sixteen windows and have a gate that re-derives them, and critic #20's deduction for the `41498ef` stamp is withdrawn because the stamp IS an ancestor of HEAD. 제출 자료 15 to 14 — the Q&A bank's T0 Q30 now warns the student against a screen that is correct.)* |
| 2026-09-06 | `dd500e6` | 17 | 15 | 19 | 15 | 18 | **84** | 16 | 15 | 18 | 15 | 14 | **78** | *(critic #27: every row held, and the hold on A 제출 자료 is this lap's finding rather than a note. The window's repair of Q&A Q30 is real and gated — the card now names three reason-buckets instead of two, carries no live count, and ships a test that goes red when the registry grows a bucket the card does not describe — which on its own is +1. It is spent, exactly, on what the same file still does: Q35 (**T1**, the reproducibility question) tells the student not to say the true sentence 「현재 브랜치에서 닿는 커밋입니다」 and to say instead 「지금 브랜치에서 닿지 않습니다」, which is false at this head and on an unshallowed clone (`is-ancestor` exits 0, the object is 283 back, `branch --contains` names `auto/dev` and `origin/Main`), and which critic #26 withdrew everywhere except here while writing 「Q35 … must not be edited」 (WFG-133). And the booth PDF the student physically carries is stale against that same file by exactly the text this window withdrew (manifest `2c8451211e5f97eb…`, tree `af955a30fa500391…`, WFG-134). Following this table's own precedent from critic #8's F42: a 제출 자료 row does not rise while the document the student recites from tells them to say something false. B 데이터 19 held on two moves that cancel: the new registry-side bucket gate is genuine 재현 machinery (+), and 18 of the 58 irreproducible entries carry no `reproducibility.status` while the card invites a judge to count by that field (WFG-135) (-). 연구 목적, 설계와 방법론 and 창의성 held on both tracks: nothing in this window touched a model, split, metric, arm, coupling or protocol, and the manuscript did not move.)* |
| 2026-09-06 | `e95fe28` | 17 | 15 | **18** | 15 | **17** | **82** | 16 | 15 | 18 | 15 | **13** | **77** | *(critic #28: two rows move on each track and all four fall, and none is a regression. The clean-clone reproducibility claim and the README's headline attribution were both wrong before this window and were scored without them by every earlier lap. Reasoning in the two track tables and in `docs/auto/reports/2026-09-06T1736Z-critic.md`.)*
| 2026-09-06 | `1b26c3a` | 17 | **16** | 18 | 15 | 17 | **83** | 16 | **16** | 18 | 15 | 13 | **78** | *(critic #29: one row moves on each track, 설계와 방법론 15 to 16 on both, and it is the same evidence: the project's differentiation was framed against academic work for twenty-eight windows and the two Korean systems that already do this operationally were absent. The window contains no dev lap — the 18:17Z slot was ceded to research — so nothing else could move and nothing else did. Reasoning in the two track tables and in `docs/auto/reports/2026-09-06T2015Z-critic.md`.)*
| 2026-09-06 | `524f13c` | 17 | 16 | 18 | 15 | 17 | **83** | 16 | 16 | 18 | 15 | 13 | **78** | *(critic #30: every row held on both tracks, and the flat window is the finding rather than the absence of one. 제출 자료 held because the repair of the README's headline attribution (+1) is spent exactly on two things measured in the same window: the second of the manuscript's two binding caveats is missing from the same bullet and the gate built for it is green on that (WFG-148), and the booth printables drifted a fourth time, the first drift that makes the printed pages worse rather than older. Reasoning in the two track tables and in `docs/auto/reports/2026-09-06T2317Z-critic.md`.)*
| 2026-09-07 | `3f881f6` | 17 | 16 | 18 | 15 | **18** | **84** | 16 | 16 | 18 | 15 | **14** | **79** | *(critic #31: one row moves on each track, 제출 자료, on the booth kit verified fresh here rather than read — five source sha256, the PDF sha256, and 5+6+17+3+2 = 33 = the PDF's own page count. Held one short of 19 and 15 by this lap's one finding: the bundle a judge is handed still does not contain that kit.)*
| 2026-09-07 | `0fc6130` | 17 | 16 | 18 | 15 | 18 | **84** | 16 | 16 | **19** | 15 | 14 | **80** | *(critic #32: Track B flat; Track A one row moves, 구현 및 유용성 18 to 19, and it is the delivery vehicle critic #31 pre-registered as what this row was waiting for. R9 ticked, 5 of 11.)*
| 2026-09-07 | `2896c9c` | 17 | **17** | 18 | 15 | 18 | **85** | 16 | **17** | 19 | 15 | **15** | **82** | *(critic #33: R7 ticks, 6 of 11; WFG-026 moves 설계와 방법론 on both tracks; gates RED in the default shallow clone, WFG-119)*
| 2026-09-07 | `2720840` | 17 | 17 | 18 | 15 | 18 | **85** | 16 | 17 | 19 | 15 | 15 | **82** | *(critic #34: every row held on both tracks, and the flat window is the honest read — the window's work was a gate repair, which is real and is not something these five judges meet. `gates.py --mode full` is ALL GREEN, exit 0, on the routine's DEFAULT shallow clone at this head, which is the reading critic #33's red was about. Track A 구현 및 유용성 holds at 19 rather than taking the point critic #33 pre-registered, and the cell below says exactly why: the blocker #33 named is cleared, and the point it buys is held by critic #32's older and still-standing one, WFG-110's six unmapped numbers on the judged screen. Root objection is on `docs/auto/DIRECTION.md`, not in this table.)* |
| 2026-09-07 | `b54ca28` | 17 | **18** | 18 | 15 | 18 | **86** | 16 | **18** | 19 | 15 | 15 | **83** | *(critic #35: one row moves on both tracks, 설계와 방법론 17 to 18, on WFG-144's spoken card Q16a — the differentiator reached the surface a judge meets in a 1:1 interview, not only the printed panel. 제출 자료 holds on two moves that cancel: WFG-146's citation pairing corrected in seven places with the kit rebuilt in the same lap, against a new unfounded negative claim printed on that same panel, WFG-162. Gates ALL GREEN, exit 0, on the default shallow clone; zero failed GitHub runs in the window. Root objection is about how a narrowing travels, not about direction.)* |
| 2026-09-07 | `7cc4eb7` | 17 | 18 | 18 | 15 | 18 | **86** | 16 | 18 | 19 | 15 | 15 | **83** | *(critic #36: every row holds on both tracks, and the flat window is the finding. Each of the window's three real gains bought a point that was spent, in the same window, on a defect measured here rather than inherited: WFG-110's screen-to-key index (28 keys re-derived independently) against R1's untested 「all four acts advance」 half; WFG-162's printed panel repair and WC-007/WC-008 against the Korean half of WC-008 still live on the T0 card the student recites; the withdrawn-claims registry work against the fact that five withdrawals of one claim family in one day were all found by readers and none by a gate. Gates ALL GREEN, exit 0, cold, on the default shallow clone; 28 of 30 GitHub runs `success`, 2 `cancelled`, zero `failure`. Root objection: a registered spelling reaches one language.)* |
| 2026-09-07 | `64f015b` | 17 | 18 | 18 | 15 | 18 | **86** | 16 | 18 | **20** | 15 | **16** | **85** | *(critic #37. ⚠ **BACKFILLED BY CRITIC #38, 2026-09-07T2319Z, AND THE OMISSION IS ITS OWN SMALL FINDING.** Critic #37 appended its row to the Track B table (line 104) and to the Track A table (line 145) and **not** to this series table, against this file's own instruction two paragraphs above: 「Append here **and** to the track table you are scoring」. Nothing is invented here: both five-number groups are transcribed verbatim from those two rows at `64f015b`, and the reasoning stays where that lap wrote it. Its window: Track A 구현 및 유용성 19 to 20 on WFG-009's four-act driver, re-run by that lap and confirmed on GitHub's own `ubuntu-latest` runner in `auto-gates` run 219; Track A 제출 자료 15 to 16 on WFG-166's reprinted kit; **R1 ticks, 7 of 11**; Track B flat. Held one short on Track A 제출 자료 by WFG-171.)* |
| 2026-09-07 | `1bca8ed` | 17 | 18 | 18 | 15 | 18 | **86** | 16 | 18 | 20 | 15 | **17** | **86** | *(critic #38: one row moves, Track A 제출 자료 16 to 17, and it is exactly the point critic #37 withheld. WFG-171 shipped at `fa18fcc`/`fa771d9`: both printed surfaces now state the NIFoS origin-point claim at the strength of its source, it is registered as **WC-009**, the kit was rebuilt in the same lap (`WFG_printables_20260907T2149Z.pdf`) and the bundle re-pointed, and the reviewer additionally broke a circular provenance in `references.bib:258` that had been certifying the manuscript's wording while the manuscript cited it as its source. On this track the kit is the object handed over, so the reprint is what earns the point. **Track B 제출 자료 held at 18 on two moves that cancel**, and the second is new: the same 출처 명기 criterion now carries an explicit double standard, fifteen of 29 references verified 「via the Crossref record」 while §2 characterises six of them substantively (**WFG-174**, found by the paper lap's own reviewer and filed nowhere until this lap, **WFG-175**). **연구 목적 17, 설계와 방법론 18, 데이터 18, 창의성 15 held**: nothing in this window touched a model, a split, a metric, an arm, a coupling or a protocol, and WFG-139 stands unrepaired for an **eleventh** measurement, this one an isolated single-test reproduction. **Track A 구현 및 유용성 20 held**, and the reason it is not docked is stated rather than assumed: `gates.py --mode full` is ALL GREEN, exit 0, COLD, on the default shallow clone, and every number on the screen still maps to a registry key. What the screen has instead is a **liveness** problem, `web/finals.html` at 30 of a 30-commit staleness limit, which is this lap's one `fix-before-next-row` item (**WFG-173**) and not a deduction, because nothing a judge sees is wrong today.)* |
| 2026-09-08 | `1282198` | 17 | 18 | **19** | 15 | **17** | **86** | 16 | 18 | 20 | 15 | **16** | **85** | *(critic #39: two rows move on Track B in opposite directions and one falls on Track A, and both movements come out of the same lap. **+1 B 데이터 수집·분석·해석, 18 to 19**, for WFG-139: the clean-clone reproducibility claim stopped being believed and started being enforced, and I re-derived it rather than reading it — a COLD `gates.py --mode full` at this head left `du -sb data/raw` at **201,187** bytes before and after, with `data/raw/dem/srtm/` empty, where eleven consecutive laps had watched 25.9 MB arrive mid-run. That is the 연구 재현 가능성 clause of that row, measured. **-1 B 제출 자료, 18 to 17, and -1 A 제출 자료, 17 to 16**, one defect scored once per track: the same commit that wrote the honest remainder onto the printed card made it wrong, and 「여섯 개」 at `JUDGE_QA.md:980` and `:1182` and 「six」 at `clean_clone_gates.md:85` are **seven** at this head, the seventh being the test `ab4e71e` itself added. It is on 17 of the kit's 38 pages and in a spoken T1 line. 연구 목적 17, 설계와 방법론 18, 창의성 15 held on both tracks: no model, split, metric, arm, coupling or protocol moved this window, and a network guard is not a scientific result. A 구현 및 유용성 holds at 20.)*|
| 2026-09-08 | `47e48b5` | 17 | 18 | 19 | 15 | **18** | **87** | 16 | 18 | 20 | 15 | **17** | **86** | *(critic #40: one row moves on each track and it is the same row, 제출 자료, restoring exactly what critic #39 docked. **+1 B 제출 자료 17 to 18, +1 A 제출 자료 16 to 17.** Two independent pieces of evidence, both re-derived here at this head rather than read. First, WFG-178 closed: `pytest -rs` over the whole suite in this tile-less clone prints **60 skip lines, 11 naming SRTM, 7 tile-gated, 4 gating on the laptop bundle**, which is exactly what `JUDGE_QA.md` Q28 and Q40 and `docs/clean_clone_gates.md` now say, and `tests/test_tile_gated_skip_count.py` binds twelve of those sentences to the tree including the two the student SPEAKS. The printed kit was rebuilt twice in the same lap (`20260908T0406Z`, 38 pages) and `release/kcf-finals-2026/MANIFEST.json` re-points at it, so the paper matches the tree. Second, 출처 명기 on the manuscript: the paper lap opened **12 abstracts in full**, marked **4 as catalogue-record-only**, narrowed **3 sentences** to what their titles support, and re-sourced §3.1's two-minute GK2A cadence to this repository's own ingestion code instead of to a cited paper. **Why 19 and not 20 on Track B 제출 자료:** `paper/check_paper.py` run here answers `body_words 8994` against a hard fail at **9,000** and `pages: null` on any machine without LibreOffice, so the document is **six words** from parking and the limit the author actually set is measured nowhere the loop can reach (NH-037). A 제출 자료 stays one below B because the README still has no Round-4 section (R8) and 제출 자료 on this track is what the judge is handed. **연구 목적 17, 설계와 방법론 18, B 데이터 19, A 구현 20 held: in the 49 commits this clone can read, `git diff --name-only 0fc6130..HEAD` returns NOTHING under `src/`, `data/`, `docs/figures/` or `docs/NUMBERS.json`.** **창의성 15/15 held, for the 40th consecutive row, and that is this lap's root objection rather than a hold — see WFG-182.**)*|
| 2026-09-08 | `9329400` | 17 | 18 | 19 | 15 | 18 | **87** | **17** | 18 | 20 | 15 | 17 | **87** | *(critic #41: one row moves, and only on Track A. **+1 A 개발 목적 16 to 17**; every other row on both tracks holds. Track A 개발 목적 is the only rubric row whose text names 「작품 작동을 위한 제약에 대한 이해」, and this window is the first that made those constraints explicit and testable: Q16b (책임) and Q20a (개인정보) landed at `c2a7980` with **eight** 없는 것 clauses between them, each pointing at a committed file, and `tests/test_responsibility_and_privacy_cards.py` re-derives every quantity they state. Verified here rather than read: `sms.send` is called nowhere in `src/` or `scripts/`, `email_sent.json` appears **0** times in the tree, 28 JSON run records carry `nothing_was_sent: true` (a 29th match is `outputs/dispatch/README.md`, which is prose), `immobile_fraction` is **0.3** in `data/processed/rescue_routing.json`, and `vulnerability.py`'s module docstring does declare its county scores placeholders **not yet anchored to authoritative data**. **Why 17 and not 18, and why 제출 자료 holds on both tracks:** the sentence the student says out loud still carries an unscoped self-count. `JUDGE_QA.md:715` reads 「커밋된 33장 전부에 있습니다」 while `git ls-files '*dispatch_a4.html'` answers **642** (33 `outputs/dispatch`, 113 `outputs/dispatch_full`, 496 `outputs/live`), and the gate written to stop exactly that class passes on a coincidence (F1, WFG-185). The claim is true and understates, so this is a scope defect and not a false claim, but it is live on 40 printed pages. **창의성 15 and 15, the 41st consecutive pair** (WFG-182), and this lap measured why: `grep -ncE '창의|독창'` returns **0** on `docs/auto/JUDGE_QA.md`, **0** on `web/finals.html` and **0** on `docs/auto/DEMO_SCRIPT_5MIN.md`.)*
| 2026-09-08 | `ceb43ba` | 17 | 18 | 19 | **16** | **19** | **89** | 17 | 18 | 20 | **16** | 17 | **88** | *(critic #42: 창의성 moves on BOTH tracks for the first time in 42 rows, and Track B 제출 자료 takes the point critic #41 pre-registered when it wrote 「gain and deduction cancel」. Full reasoning in the two track tables below.)* |
| 2026-09-08 | `dee1bc1` | **18** | 18 | 19 | 16 | 19 | **90** | **18** | 18 | 20 | 16 | 17 | **89** | *(critic #43: one row moves on each track and it is the purpose row on both, 연구 목적 / 개발 목적 17 to 18, on the same evidence: the front door now states the research purpose, the honest ladder and an English abstract, and **R8 ticked** at `dee1bc1` after three consecutive critic laps at zero. **제출 자료 is held at 19 / 17 deliberately**, and 데이터 수집·분석·해석 at 19, because WFG-190 is a live defect on that exact page: `README.md:235-239` states a universal that `docs/auto/JUDGE_QA.md:849-852` falsifies, and `README.md:220-225` drops a free parameter that `paper/manuscript.md:493` names. A row scored on 논리적 구성 does not rise on the page whose new prose I just filed against. Gates ALL GREEN exit 0, 1749 passed; auto-gates 230-249 carry zero failures.)* |
| 2026-09-08 | `0cca093` | 18 | 18 | 19 | 16 | 19 | **90** | **19** | 18 | 20 | 16 | 17 | **90** | *(critic #44: **One row moves, Track A 개발 목적 18 to 19, on the adoption card — and 제출 자료 is HELD in both tracks rather than raised or docked, which is the deliberate half of this scoring.** **A 개발 목적 18 to 19.** That row's own rubric text is 「작품 작동을 위한 제약에 대한 이해」, and the new card (`docs/auto/JUDGE_QA.md`, the 46th; the bank's header 46 = T0 19 + T1 20 + T2 7 sums by `grep -cE '^\*\*Q[0-9]+[a-z]? · T[0-9]'`) is that sentence answered against the judge who would have to run it: it refuses to give a cost 「저희는 어느 기관에서도 이걸 운영해 본 적이 없고, 도입 비용을 추정해 본 적도 없습니다」, and its 「없는 것」 block leads with 「비용 수치가 하나도 없습니다」, names N = 1 on the firefighter consultation, and separates 「running this once in that county」 from 「running it in an operations room」. `tests/test_adoption_card.py` (327 lines) binds it and the kit was rebuilt in the same lap (`WFG_printables_20260908T1529Z.pdf`, `release/kcf-finals-2026/MANIFEST.json` re-pointed). **제출 자료 HELD at 19 (B) and 17 (A), and the hold is the finding.** Up this window: `README.md:220-239` stopped stating the universal critic #43 filed (「어느 심사용 자료에도」 is now 「부스에서 말하지 않습니다」) and the Round-4 section now names the buffer as a free parameter and points at `docs/present_perimeter_arm.md` §4. Down this window, on the same paragraph: `README.md:232` now asserts 「고원이 아니라 뾰족한 봉우리」 — the shape `docs/auto/JUDGE_QA.md:1394` and `:920` say the five-point sweep **cannot resolve**, and which `docs/auto/DEMO_SCRIPT_5MIN.md:151` has the student disclaim out loud. The two cancel; raising the row on a section that gained one defect while losing two would be scoring the diff rather than the state, and docking it would double-count a finding filed at position 1 (**WFG-127**, fourth surface). **B 설계와 방법론 18, held for the same reason and not a different one:** the sweep's resolution is a methodology fact, the repository states it correctly on three surfaces and incorrectly on the fourth, and 18 already reflects the three. **데이터 수집·분석·해석 19 (B) / 구현 및 유용성 20 (A) held: nothing was measured this window.** The paper lap wrote a correction, its reviewer blocked it, and it was reverted rather than shipped — `paper/manuscript.md` is byte-identical to lap 18's (`6ecc386`). That is restraint and it is scored as a hold, not a loss. **창의성 16 held in both tracks:** WFG-182 closed on the bank and the count is still **0** on `web/finals.html` and **0** on `docs/auto/DEMO_SCRIPT_5MIN.md` (WFG-194). ⚠ **No row is docked for `auto-gates` run 253 being red** (WFG-193): the gates inside it passed, and a broken artifact upload that stopped `Main` following is loop machinery a judge never opens.)* |
| 2026-09-08 | `9c24a8b` | 18 | 18 | 19 | 16 | 19 | **90** | 19 | 18 | 20 | 16 | 17 | **90** | *(critic #45: **no row moves on either track — the first all-hold window since `2720840` on 2026-09-07, and it is the honest reading of a three-hour window that contained two CI-repair laps and one research lap and touched no judge-facing surface.** The window's real work was real: `Main` follows again, two red `auto-gates` runs are closed, the browser gate's skip was verified rather than asserted, the manuscript's related-work section narrowed four overclaims about papers it cites, and DIRECTION.md went from 54,923 bytes to one screen with the old page archived verbatim. **None of it is a thing five judges see in ten minutes.** **B 데이터 수집·분석·해석 19 and A 구현 및 유용성 20 held: nothing was measured.** Pre-registered so the next lap can hold me to it — B 데이터 goes to **20** when WFG-125 reports a number from the out-of-fold field this lap found committed (`data/processed/spread_v2_lofo_oof_cells.csv.gz`), or when it discharges its written branch by naming that file and saying precisely why it cannot be used. **창의성 16 held on both tracks**, and the reading got worse rather than better: `docs/creativity_card.md` exists and is in neither the printed kit nor any of the three surfaces a judge stands in front of (WFG-194). It is not docked, because that describes the same state critic #44 scored and not a regression; it moves to 17 when 창의성 reaches `web/finals.html` or `DEMO_SCRIPT_5MIN.md`. **설계와 방법론 18 held on two moves that cancel:** up, the manuscript stopped overclaiming what @cova2005, @li2017, @li2019, @kim2021gk2a, @dozier1981 and @studenski2011 say, which is this row's 「기존의 연구와의 차별점」 criterion made accurate rather than merely present; down, the same row's 「일정」 bullet is at **zero** on every judge-facing surface (WFG-027, F3). **제출 자료 19 (B) / 17 (A) held:** `README.md:232`'s 「고원이 아니라 뾰족한 봉우리」 is unrepaired for a second window, and nothing reached the kit or the screen. **연구 목적 18 / 개발 목적 19 held.** ⚠ **No row is docked for `auto-gates` run 255 being red:** it was a browser that never started, on a runner, and a judge never opens CI.)* |
| 2026-09-08 | `cb9fcc3` | 18 | **19** | 19 | 16 | 19 | **91** | 19 | **19** | 20 | 16 | 17 | **91** | *(**critic #46. ONE row moves on each track and it is the same row, 설계와 방법론 18 to 19, on a control design rather than on a result.** ⚠ **This lap's own correction first, because a scorecard is a record.** I first scored this window at `7cfe29a` (23:00Z) as an all-hold on the reading that the 21:17Z dev lap had claimed two P0 rows and died. The evidence at that head was real and **the inference was wrong**: the lap was slow, not dead, and pushed `9170a37` at about 23:20Z closing **both** rows. Nothing was pushed carrying that scoring. **What actually happened is the strongest kind of lap this loop produces.** `9170a37` added the three widths the fair-opponent sweep was missing (750, 1250, 1500 m) on committed inputs with no retrain and no re-acquisition, into a **new** file: safe totals across 250 m to 3 km are **275, 284, 349, 345, 318, 305, 275, 283**. The top is a **shoulder** (750 m **349**, 1 km **345**, four origins apart on a grid whose step is 250 m), the claim was withdrawn on four surfaces and registered as **WC-011**, a **fifth** surface (`paper/GAPS.md` G8) was found by the registration sweep rather than by memory, and WFG-199 was cleared first so the screen's stamp went from 27 behind to **8**. **설계와 방법론 18 to 19, and the evidence is this row's own wording rather than the result.** Track B scores 「변수의 정의와 제어의 적절성」 and Track A 「설계의 기술적 완성도」: `--sweep-extra-m` **adds** widths and defaults to empty so the five-point grid is unchanged when the flag is absent; output goes to a separate file so no committed artifact moved (CHARTER §3 rule 2); the run **refuses to write unless it first reproduces the committed canonical arm**; and all five shared widths come out **cell for cell** identical, bound mechanically by `tests/test_buffer_shape.py::test_the_five_shared_widths_reproduce_cell_for_cell`. That is a control, not a claim, and it is what makes three new points readable beside five old ones. **데이터 수집·분석·해석 19 (B) held, and I am declining the point deliberately.** Critic #45 pre-registered the 20 for WFG-125 — the margin this project's **own model** buys rather than what a noiseless forecast buys — and that is untouched. The new document's own §4 says so in its own voice: 「every margin here is what a noiseless forecast would buy ... That difference is still unmeasured」. A row scored on 「예상 결과에 대한 논리적인 해석 및 결론」 does not reach 20 while the headline comparison measures a bound the model does not reach. **제출 자료 19 (B) / 17 (A) held on two moves that cancel.** Up: `README.md` carries a dated withdrawal note that explains the mistake in the student's own register, Q37 went from 「다섯 가지 · 구분할 수 없습니다」 to 「여덟 가지 · 어깨 모양」, the kit was rebuilt as `WFG_printables_20260908T2156Z.pdf` with `MANIFEST.json` re-pointed in the same lap, and **all six `SOURCES` re-hash to the tree**, checked here in one process. Down, and it is this lap's root objection: **the reported margin is a maximum over a grid chosen post hoc, so it can only shrink as anyone looks harder — the first refinement ever run took it from 9 origins to 5 — and no surface states that property.** `README.md:245-247` says the rerun went against the project and §4 says the grid is coarse; neither says the statistic is non-increasing in search effort. **WFG-201, P0 at position 1.** **창의성 16 held on both tracks**, fourth window at the same reading: `grep -cE '창의|독창'` is 0 on `web/finals.html` and 0 on `DEMO_SCRIPT_5MIN.md`, and `docs/creativity_card.md` is in no printed kit (WFG-194). Pre-registered again: **17** when 창의성 reaches the screen or the script. **연구 목적 18 / 개발 목적 19 held**, and the judge drill names what would move them: none of the bank's 46 cards asks why the horizon is 3 to 12 hours while national doctrine puts the elderly-evacuation decision at 8 h and its completion at 5 h before fire-line arrival (WFG-197, kept P1 on purpose). **A 구현 및 유용성 20 held** — gates ALL GREEN at this head (1786 passed), `auto-gates` 262 and 263 green, `Main` following at `cb9fcc3`, stamp 8 behind 30. There is no 21.)* |
| 2026-09-09 | `86f8929` | 18 | 19 | **20** | 16 | 19 | **92** | 19 | 19 | 20 | 16 | 17 | **91** | *(critic #47: **one row moves, on Track B only — 데이터 수집·분석·해석 19 to 20**, on WFG-201's post-hoc-maximum qualifier reaching six judge-facing surfaces with a mutation-graded test that names its own leakage. Track A held at 91: the change is statistical interpretation and Track A has no row for it. 창의성 held at 16 on both, measured: 0 on the finals screen, 0 on the five-minute script.)* |
| 2026-09-09 | `5f4e32b` | 18 | 19 | 20 | 16 | 19 | **92** | 19 | 19 | 20 | 16 | 17 | **91** | *(critic #48: **no row moves on either track, and that is the honest reading of the window rather than a courtesy.** `86f8929..5f4e32b` holds one paper lap (`4b0010a`, manuscript §4.5) and three commits repairing a report header; the one dev lap in it, `9a97e53`, is a bare claim of WFG-194 whose work had not landed when this lap read the tree. Nothing a KCF judge meets changed. The two scoring holes this lap measured, 일정 at literal zero on all four judge-facing surfaces and 창의성 at zero on the README as well, are **already priced into 설계와 방법론 19 and 창의성 16** and are not new information, so they are neither a further deduction nor a raise; they are filed as WFG-027 promoted to P0 and a measurement on WFG-194. `제출 자료` is HELD at B 19 and A 17 despite WFG-208: seven dead pointers on the USB's own front page is a real 출처 명기 defect, and it is worth less than a point against a window in which every one of the manuscript's 29 references carries a verified note recording what was actually opened and what it does not say.)* ⚠ **WFG-194 landed under this lap at `b451376`, after the window closed**, and put 창의성 onto `web/finals.html`, `docs/auto/DEMO_SCRIPT_5MIN.md` and the printed kit. **That is critic #49's window to score, not this one's**, and it is named here so no later lap reads this hold as having seen it.
| 2026-09-09 | `7f914fd` | 18 | 19 | 20 | **17** | 19 | **93** | 19 | 19 | 20 | **17** | 17 | **92** | *(critic #49: one row moves on both tracks, 창의성 16 to 17, on the pre-registration critic #45 wrote in this file.)* |
| 2026-09-09 | `9c22ff3` | 18 | 19 | 20 | **18** | 19 | **94** | 19 | 19 | 20 | **18** | 17 | **93** | *(critic #50: one row moves on both tracks, 창의성 17 to 18, on the pre-registered WFG-210 closure verified in the tree; 제출 자료 held on two cancelling moves — a clean `factchk` on three new world claims against 논리적 구성 on the front door, WFG-212. See the two detail tables.)* |
| 2026-09-09 | `8506a2d` | 18 | 19 | 20 | 18 | **18** | **93** | 19 | 19 | 20 | 18 | **16** | **92** | *(critic #51: one row moves on both tracks and it is the same row, 제출 자료, DOWN. The repository now gives two answers to what its headline number means, on six surfaces, and the right one is unreachable. Detail in both track tables below.)*
| 2026-09-09 | `375be25` | 18 | 19 | 20 | 18 | 18 | **93** | 19 | 19 | 20 | 18 | 16 | **92** | *(critic #52: NO row moves on either track. The window closed the 일정 zero and corrected two of the five oracle surfaces, and the document that closed the zero shipped with two false self-descriptions. See both detail rows.)* |
| 2026-09-09 | `ba06467` | 18 | 19 | 20 | 18 | **19** | **94** | 19 | 19 | 20 | 18 | **17** | **93** | *(critic #53: **one row moves on both tracks and it is the same row, 제출 자료, back UP.** Critic #51 docked it because the repository gave two answers to what its headline number means and the right one was unreachable; all five surfaces it named now state the mechanism and four of them link the file. Held below the ceiling on 논리적 구성, which is WFG-212, and on the finals screen's silence, which is WFG-220. See both detail tables.)* |
| 2026-09-09 | `3eec471` | 18 | 19 | 20 | 18 | **18** | **93** | 19 | 19 | 20 | 18 | **16** | **92** | *(critic #54: one row moves on both tracks and it is the same row, 제출 자료, DOWN. The window's own lap corrected the front door's one affirmative claim and left the identical claim, in the identical words, on the four surfaces that carry it by design. See the two track tables below.)* |
| 2026-09-10 | `7dabdef` | 18 | 19 | 20 | 18 | **19** | **94** | 19 | 19 | 20 | 18 | **17** | **93** | *(critic #55: one row moves on both tracks and it is the same row, 제출 자료, UP. This is critic #54's pre-registered restoration, paid on its own stated condition and verified in the tree: WFG-222 closed on eleven surfaces, 「가구 단위」 now answers 0 on all four asserting surfaces, the printed kit was rebuilt and the bundle manifest re-pointed, and seven of seven printed sources hash equal to the tree. +1 and not +2 because the finals screen still carries no word of the oracle objection (WFG-220) and 일정 still answers 0 on both the README and the screen (WFG-218). Full reasoning in the Track B row of the same date.)*
| 2026-09-10 | `9b7d21c` | 18 | 19 | **19** | 18 | **20** | **94** | 19 | 19 | 20 | 18 | **18** | **94** | *(critic #56: **two rows move on Track B and they offset, so Track B is HELD at 94; one row moves on Track A, which is UP to 94.** 제출 자료 UP on both tracks on critic #55's pre-registration, verified in the tree; 데이터 수집·분석·해석 DOWN on Track B only, because Track A has no such row and the defect is an interpretation defect.)* |
| 2026-09-10 | `16e6824` | 18 | **20** | **20** | 18 | **19** | **95** | 19 | **20** | 20 | 18 | **17** | **94** | *(critic #57: **three rows move on Track B, net +1 to 95; two move on Track A and they offset, so Track A HELDs at 94.** 설계와 방법론 UP to 20 on both tracks, paying critic #56's own pre-registration on the condition it stated: WFG-128 closed. 데이터 수집·분석·해석 UP to 20 on Track B, because WFG-215 closed BOTH halves. 제출 자료 DOWN on both tracks, because three separate defects landed on that one row in this window: the finals screen prints the two counts the window corrected with no caveat, card Q38 is stale on the printed paper, and `docs/oracle_gap.md` names the wrong script for the array it is about. Full reasoning in both detail rows.)* |
| 2026-09-10 | `d3ca754` | 18 | 20 | **19** | 18 | **20** | **95** | 19 | 20 | 20 | 18 | **18** | **95** | *(critic #58: **two rows move on Track B and they offset, so Track B HELDs at 95; one row moves on Track A, which is UP to 95.** 제출 자료 UP on both tracks, on the finals screen stating the budget-rule asymmetry inside the panel that prints the count and on the oracle page's script pointer being repaired. 데이터 수집·분석·해석 DOWN on Track B only, on WFG-228 — the new anchor document's headline statistic has no comparator — because Track A has no such row. Pre-registered: it returns to 20 when WFG-228 lands a null figure with its caveat, whatever the figure says. Full reasoning in the two track rows of the same date.)*
| 2026-09-10 | `3867860` | 18 | 20 | 19 | 18 | **19** | **94** | 19 | 20 | 20 | 18 | **19** | **96** | *(critic #59: **one row moves on each track and it is the same row, 제출 자료, in opposite directions, so the two tracks converge on 19 for a criterion both tables word identically.** Track A UP on critic #58's own stated condition, now paid: WFG-226 and WFG-229 closed at `ba76b8c` with the kit reprinted at `20260910T1233Z` and `release/kcf-finals-2026/MANIFEST.json` re-pointed, re-hashed here at 7 of 7 sources and 19 of 19 bundle entries. Track B DOWN on WFG-233, a defect that predates this window and is scored because the row is 「today's state」: the one sentence written to be pasted into the submitted 작품설명서 explains the weak fold with a number the rest of the same file does not use, cites nothing, and is registered nowhere. 데이터 수집·분석·해석 HELD at 19, WFG-228 still `todo`.)*
| 2026-09-10 | `71e95ee` | 18 | 20 | **20** | **19** | 19 | **96** | 19 | 20 | 20 | **19** | 19 | **97** | *(critic #60: **two rows move on Track B and one on Track A, and one of the moves is a pre-registration paid on exactly its stated condition.** 데이터 수집·분석·해석 UP to 20 on Track B: critic #58 wrote that it returns to 20 when WFG-228 lands a null figure with its caveat, whatever the figure says, and `b8fd6a8` landed it, corrected it against its own reviewer and published the harsher half. 창의성 UP to 19 on **both** tracks, on the same evidence, because `docs/auto/RUBRIC.md` words that criterion identically in both tables and a move that applies to one applies to the other. 제출 자료 HELD at 19 on both: WFG-233 is still `todo` and WFG-236 is new this window, against a window that also shipped the seed-removed correction onto two pages and into the caveat band on all 84 `dn_yeongdeok_` keys. Full reasoning in the two track rows of the same date and head.)*
| 2026-09-10 | `be39dea` | 18 | 20 | 20 | 19 | **18** | **95** | 19 | 20 | 20 | 19 | **18** | **96** |
| 2026-09-10 | `f93af93` | 18 | 20 | 20 | 19 | **17** | **94** | 19 | 20 | 20 | 19 | **17** | **95** | *(critic #62: **one row moves on both tracks and it is the same row, 제출 자료 18 to 17, so Track B is DOWN to 94 and Track A DOWN to 95.** One 제출 자료 defect closed and two opened. WFG-233 closed well at `e8b6e01`: the sentence bound for the SUBMITTED 작품설명서 now carries both counts with a `src:` line and a registry key each, which is 「사용된 자료에 대한 출처 명기」 paid. Against it, **WFG-243** puts the household register `WC-013` withdrew on a **T0** card said from memory and on the printed kit, and **WFG-244** puts two unmeasured provenance claims into the same submitted-document block the same commit repaired. 「자료의 논리적 구성」 is the first named sub-item of this 20-point row on both tables. Five open 제출 자료 defects, three on paper. See the two detail tables for the measurements.)* |
| 2026-09-11 | `0796336` | 18 | 20 | 20 | 19 | **19** | **96** | 19 | 20 | 20 | 19 | **19** | **97** | *(critic #63's values, **TRANSCRIBED by critic #64 and not a re-score.** This table stopped at the row above while both per-track tables gained a row at `0796336`, so the series skipped a lap and the record tables did not. The ten scores and the two totals are copied verbatim from the Track B and Track A rows of that head; the reasoning lives there and is not duplicated. Critic #63's summary of its own move: 제출 자료 17 to 19 on both tracks, paid on the defect that took the row down, with the withdrawn household register re-swept off the printed kit and the one publication that takes this project's architecture away from it cited on a card whose every fact that lap re-verified at the Zenodo API. The gate that would have caught the skip is **WFG-252**.)* |
| 2026-09-11 | `5482ba8` | 18 | 20 | 20 | 19 | 19 | **96** | 19 | 20 | 20 | 19 | 19 | **97** | *(critic #64: **no row moves on either track, and the one row that was pre-registered to move had its condition MET and the rise DECLINED.** Critic #63 wrote that 제출 자료 reaches 20 on both tracks when WFG-247 and WFG-248 close with the kit rebuilt and the manifest re-pointed. Both closed, the kit was rebuilt twice, and I re-hashed the object rather than reading the report: kit 7 of 7 sources, bundle 19 of 19 entries, bundle naming the newest stamp, all recomputed in this lap's own process. The rise is declined because the same repair introduced **WFG-249**, a flat contradiction between a T0 privacy card that says a 가구 here is not a building and the closing sentence that says the 가구 it counts are buildings, both printed in the same 58-page kit; and because **WFG-251** stands on one of those two documents, with **WFG-250** on the other. A 20 is a claim that nothing was found on the submitted material. No fall either, which the same pre-registration allowed for: nothing drifted against a hash, and the false noun that cost this row a point two laps ago is gone and registered as `WC-016`. Full reasoning in the two track rows of this head.)* |
| 2026-09-11 | `f7ee58d` | 19 | 20 | 19 | 19 | 18 | **95** | 19 | 20 | 20 | 19 | 18 | **96** | *(critic #65's values, **TRANSCRIBED by critic #66** because that lap appended to neither this table nor the Track B table. ⚠ Both of its rows went into the **Track A** table, at lines 235 and 236, so that table shows 구현 및 유용성 falling to 19 and holding at 20 at the same head. ⚠ Track B 연구 목적 reads 18 at `5482ba8` and 19 here while that row's narrative calls it HELD; the 19 is transcribed unchanged (CHARTER §3.7) and is what makes this row 95 rather than 94. ⚠ Its report and `DIRECTION.md` both say 「Track B 97 to 95」; Track B's previous value is 96 on this table's own row above. **WFG-252**.)* |
| 2026-09-11 | `d67de57` | 18 | 20 | 18 | 19 | 19 | **94** | 19 | 20 | 20 | 19 | 19 | **97** | *(critic #66, reviewed `d67de57` and shipped at `c241904`: **Track B DOWN to 94, Track A UP to 97.** ⚠ **These cells were corrected mid-lap.** The lap holding WFG-254 pushed while this row was being written, so 제출 자료 goes 18 to 19 on BOTH tracks on critic #65's pre-registered condition, paid in full and re-measured here at the rebased head. 데이터 수집·분석·해석 19 to 18 on Track B is the one new defect (**WFG-256**), and 연구 목적 19 to 18 is the withdrawal of the unevidenced point in the row above, scored here rather than inherited.)* |
| 2026-09-11 | `dec00f4` | 18 | 20 | **19** | 19 | **18** | **94** | 19 | 20 | 20 | 19 | **18** | **96** | *(critic #67, reviewed `dec00f4`: **Track B HOLDS at 94 with two rows moving in opposite directions, Track A DOWN to 96.** 데이터 수집·분석·해석 18 to 19 on WFG-129, the best-designed experiment of the week; 제출 자료 19 to 18 on both tracks on WFG-258, four judge-facing lines asserting what this tree disproves, Q19's among them and on the printed paper. Evidence in the per-track rows below.)* |
| 2026-09-11 | `b6778e7` | 18 | 20 | 19 | 19 | **19** | **95** | 19 | 20 | 20 | 19 | **19** | **97** | *(critic #68, reviewed `b6778e7`: **Track B UP to 95, Track A UP to 97.** One row moves on each and it is the same row, 제출 자료 18 to 19, because WFG-258 (a) closed with the kit reprinted and the bundle re-pointed and this lap re-hashed both. 19 and not 20 because the same edit put the loop's own governance on the judged surfaces. Evidence in the two track tables below.)*
| 2026-09-11 | `8952a5b` | 18 | 20 | 19 | 19 | 19 | **95** | 19 | 20 | 20 | 19 | 19 | **97** | **No row moves on either track, and the row that had a case is the one that cancels.** Window `de0bcd3..8952a5b`, gates green on the first run here (2158 passed), GitHub run 382 green at this head. **제출 자료 had a real case for 20 and it cancels exactly.** The gain: `README.md:37` stopped printing an internal entry id on the English front door, `:335-336` stopped saying a measured number is withheld because the author has not ruled, and the same clause came out of the two sentences Q19 prescribes to five judges, with the kit reprinted to `WFG_printables_20260911T1904Z.pdf` (59 pages, sources 7 of 7) and `release/kcf-finals-2026/MANIFEST.json` re-pointed (19 of 19). All four hashes recomputed in this lap's own process. The deduction, measured on the same kit: the card shipped in it now contradicts its own student note eight lines apart (`docs/auto/JUDGE_QA.md:1013` says the card does not raise the count first, `:1018` has the student raise it first), and `README.md:50`, `:265` and `:351` still print **NH-053** in the identical defect class critic #68 named and scoped out. 「자료의 논리적 구성」 is the criterion and a page that misdescribes itself is exactly that row. **19 holds. 20 needs both: the card consistent with itself, and the front door free of entry ids.** **데이터 수집·분석·해석 holds at 19, and here is the pre-registration critic #70 should hold me to:** WFG-260 landed a real gain on this row, which is why 19 was earned last lap, and it does not buy the same point twice. **WFG-259 closing by route (i)** takes it to **20**, because the row's own criterion is 「연구 재현 가능성」 and `docs/present_perimeter_yeongdeok.md:165`'s 「at 500 m, 15 of the 16 flip」 is the last unre-derivable integer on the page the README's TL;DR links. Re-measured here: seven `ppy_yeongdeok_` keys, none of them the 15 or either width. ⚠ **The downward condition, also pre-registered:** if a lap answers WFG-259 by route (ii) and deletes the sentence rather than measuring it, this row does not move at all, and if `docs/present_perimeter_yeongdeok.md` §5 is weakened anywhere in the process it goes **18**. **연구 목적 18, 설계와 방법론 20, 창의성 19 hold on both tracks, and 구현 및 유용성 holds at 20**: no model, split, arm, coupling, region or protocol changed in this window, the screen still opens, and the bundle verifies 19 of 19 here. ⚠ **Not scored and reported instead:** KCF_READINESS is 8 of 11 with zero cells changed for the twenty-sixth consecutive critic lap, verified by diffing all twelve R-row status cells across the window. Five of CHARTER §14b's six named lines tick; only R3 does not, and R3 is `blocked(NH-046)`, past due since 2026-09-10. |
| 2026-09-11 | `d291364` | 18 | 20 | **20** | 19 | 19 | **96** | 19 | 20 | 20 | 19 | 19 | **97** | *(critic #70, reviewed `d291364`: **Track B UP to 96, Track A HOLDS at 97.** One row moves, 데이터 수집·분석·해석 19 to 20, the move critic #69 pre-registered for WFG-259 closing by route (i), earned by a pre-registration written into the claim commit before any number existed, an identity control that runs the real code path, a cross-tabulation that replaced a story told over integers, and a second half the lap reported as NOT licensed rather than dropping. 제출 자료 holds at 19 on both tracks: `README.md:330-331` carries the scope clause WFG-259 falsified eleven lines above the addendum that falsifies it, which is this lap's `fix-before-next-row` item, but the kit (7 of 7, 60 pages) and the bundle (19 of 19) are clean, re-hashed here. GitHub run 386 concluded `failure` at `edd0ec0`, the rebase-orphan class, disclosed by the lap and green again at run 387; WFG-263 and the NH-043 update carry it. Root objection and WFG-262 at position 1: the project audits its opponent harder than it audits itself.)* |
| 2026-09-12 | `e7ba085` | 18 | 20 | 20 | 19 | 19 | **96** | 19 | 20 | 20 | 19 | 19 | **97** |
| 2026-09-12 | `cd18782` | 18 | 20 | 20 | 19 | **18** | **95** | 19 | 20 | 20 | 19 | **18** | **96** |

## Track B — SW 연구

⚠ **Read the two detail tables below by their date column, not by their order.** Critic #40 measured the
append discipline on 2026-09-08 and found three consecutive laps got it wrong: critic **#37** appended to
neither detail table (critic #38 said so and backfilled it); critic **#38**'s backfilled rows landed
mid-table, so a 2026-09-07 row sits directly after a 2026-09-04 one in both tables; and critic **#39**
appended to the combined series only, leaving both detail tables two laps behind it. **Nothing is being
reordered to fix this** — CHARTER §3.7 and the charter's own 「never rewrite history」 forbid it, and a
series edited to look tidy is worth less than one annotated where it is wrong. The check that would catch
it at the lap instead of two laps later is **WFG-184**.

| date | window head | 연구 목적 | 설계와 방법론 | 데이터 수집·분석·해석 | 창의성 | 제출 자료 | /100 | what moved |
|---|---|---:|---:|---:|---:|---:|---:|---|
| 2026-09-05 | `f118bfe` | **17** | 15 | 19 | 15 | 17 | **83** | **+1 연구 목적 (16 to 17), the second 17 this table has recorded, and it is earned by a result that runs against the project.** 「과학적으로 검증 가능한 접근 방법 설정」 is the rubric wording, and this window is the clearest instance of it in the repository's history: the author's own row (NH-027 A, 「report the number whatever it says」) built the fair opponent to the project's headline and published the number that hurt — the forecast's margin over a present-aware planner is **9** of 368 on `auto/dev`, not 91 — together with a five-width sensitivity band showing 1 km is a spike between two failure regimes rather than a constant, both origin conventions computed with the harsher one refused as the headline because it flatters the project, and a fabricated limitation withdrawn in writing in `docs/present_perimeter_arm.md` §6. The lap's own reviewer blocked it three times and **every defect it found ran in the project's favour**; all were fixed before the push. It stops at 17 and not 18 for the reason the document itself gives in §5: the forecast-aware arm plans on the same hazard field it is graded against, so 9 is what a **perfect** forecast is worth, and the margin of this project's actual model is unmeasured (WFG-125). **설계와 방법론 15 held, and the hold is this window's second finding.** 「변수의 정의와 제어의 적절성」 is exactly where the window's defect lands: the same experiment was built twice, concurrently, by two green laps, and they got **9** and **27** (and **5** at the best buffer, **19** under the strict origin rule) because 「a county office with a perimeter map」 admits at least two defensible implementations — prune-and-shortest-path with no time budget, versus the time-expanded router on a frozen field, budget-capped, refusing departure from inside the buffer. **The spread across implementations is wider than the effect.** A statistician judge says that sentence in five seconds, and today the project has no single answer to it (WFG-124, NH-032). **데이터 19 held on two moves that cancel:** the arm registers **57** `pp_uiseong_*` keys covering every cell of the band rather than only the numbers the prose names, with mutation-graded tests that go red if a later lap reports one origin rule or puts the harsher one on the headline (+); against that, the parked branch carries a measurement `auto/dev` does not — of the 25 origins the 1 km arm walls off, 11 have a forecast-aware route and **10 of those 11 escape across ground that never burns at all**, which reads 「the buffer was drawn too wide」 rather than 「the forecast saw the future」 — so the branch that ships holds the weaker analysis of its own residual (-). **제출 자료 17 held, also on two moves that cancel:** `docs/present_perimeter_arm.md` is a model of the form (method, both conventions, the band, §5 「what this does NOT show」, §6 the withdrawal) (+); and `paper/manuscript.md:386` still carries `[GAP: … a present-perimeter baseline …]`, so the submitted manuscript tells a reviewer the experiment has not been run nine hours after it ran (WFG-126), while `JUDGE_QA.md` Q30's protective note now quotes a registry count (326 / 268) that the same window moved to **383 / 325** and points the student at a screen printing the stale pair (WFG-117, WFG-113) (-). **창의성 15 held:** nothing in this window touched a model, split, metric, coupling or protocol; the arm is a control, not a method |
| 2026-09-03 | `1113388` | 15 | 15 | 16 | 15 | 11 | **72** | baseline (first critic lap) |
| 2026-09-03 | `0ff1b36` | 15 | 15 | 17 | 15 | 13 | **75** | +1 데이터: the survey evidence card, digest-gated extractor and doc-to-artifact tests. +2 제출 자료: 21 verified references (F2 closed, N4 closed), a 7,204-word manuscript, F3 fixed. 연구 목적 held at 15 by F5, still open |
| 2026-09-03 | `8d1decf` | 15 | 15 | 18 | 15 | 13 | **76** | +1 데이터 (재현) only: 545 test lines under a detector that had none, six registry keys bound to the artifact, and a Korean §12 that states what the tests do NOT show. Everything else held. Nothing in the window touched the screen, the printables, the bundle or `paper/`. The branch being red at HEAD (F13) is a process defect and is not scored here, because a judge scores the tree the author brings to the booth, not the loop |
| 2026-09-04 | `12b8ac7` | 14 | 15 | 18 | 15 | 13 | **75** | **-1 연구 목적**, the first fall in this table. `12b8ac7` rewrote the opening paragraph and replaced an overstated figure with an understated one: 45,157 ha for a chain that burned 99,289 ha, plus a scope note telling the reader the 104,788 ha nationwide total is a different event when about 95 % of it is this chain, plus 영덕 8명 against this repository's own correction to 10 at `f2eecf9` (F16, F17). Unsourced-and-too-big scored 15; wrong-in-two-directions-with-no-URL scores 14. 데이터 18 and 제출 자료 13 both **held** on two offsetting moves: `DETECTION_FLOOR_CARD.md` with 17 registry-binding tests is the first judge-holdable card that cannot drift (+), and `docs/data_sources.md` gained eleven figures with no URL in the document named 데이터 출처 (-) |
| 2026-09-04 | `5a0466e` | 15 | 15 | 18 | 15 | 15 | **78** | **+1 연구 목적 (14 to 15) and +2 제출 자료 (13 to 15).** The opening paragraph's figures are now correct and every one traces to a primary page a lap opened: critic #5 re-opened the 경상북도 release (99,289 ha / 149시간 / 3,819동 / 2,246세대 3,587명 / 1조 505억), the 경향신문 joint-survey article and the 산림청 season-end release, and all of them hold. 제출 자료 gains the two points critic #4 took off it and one more: `docs/data_sources.md` table A now carries a URL on every row, the eleven-figures-no-URL section is gone, `data/processed/external/fire_2025_scale.json` gives each figure agency + as-of + scope + status + the verified quote, 16 `fire2025_*` keys are registered, and `make verify` now refuses a paragraph that drops a final figure or states an interim one as final. That is the strongest sourcing standard in the repository, in the document named 데이터 출처. 연구 목적 stops at 15 and not 16 because of F21: the scope note still tells the reader this fire is 「about 43 %」 of the national total, which is false and contradicts the repository's own registered 94.8. 데이터 18 and 설계와 방법론 15 held; nothing in the window touched a model, a split or a metric |
| 2026-09-04 | `b855943` | 16 | 15 | 18 | 15 | 15 | **79** | **+1 연구 목적 (15 to 16), the first 16 this table has recorded.** The 「약 43 %」 sentence critic #5 called false is gone from both languages, deleted rather than rewritten a fourth time, and the lap that deleted it checked the critic's reasoning and found one of its three premises wrong: 산림청 told 경향신문 that 산불영향구역 and 피해면적 「개념이 달라서 단순 비교할 수 없고」, which is a better reason to refuse the ratio than the directional one the critic gave. The opening paragraph now states figures that are all sourced and makes no claim about them that is not sourced. 데이터 18, 설계 15, 창의성 15 and 제출 자료 15 all held; see the window note for the two moves that cancel in 데이터 |
| 2026-09-04 | `8e0a6ad` | 16 | 15 | 19 | 15 | 16 | **81** | **+1 데이터 (18 to 19) and +1 제출 자료 (15 to 16).** 데이터 recovers the point critic #6 withheld: the detection-ordering split it named is closed, the booth card and the design doc now say what the paper says, no number moved, and a 15-test gate holds the withdrawal. What earns it rather than a hold is that the loop **withdrew a headline verdict it liked** and kept the weaker conclusion that survives. 제출 자료 rises on the figure work: seven figures restyled to a published standard with the rules written down in `docs/auto/knowledge/FIGURE_STYLE_REFERENCE.md` so the next figure inherits them, and a 14,000-word knowledge base whose every reference carries one of three verification tags — a sourcing discipline no rubric asked for, applied to documents no judge will read. Neither row reaches 20 or 17: WFG-063 (the trigger recommendation now has no source), WFG-051 (one death toll, three agencies, third window), WFG-064 (two figures with colliding labels) and WFG-066 (one bibliographic record written from memory) are all open. 연구 목적 16, 설계와 방법론 15 and 창의성 15 held; nothing in this window touched a model, split, metric, arm or coupling |
| 2026-09-04 | `12bf2d9` | 16 | 15 | 19 | 15 | 16 | **81** | **Every row held, and the hold on 제출 자료 is the finding.** 데이터 19 held on two moves that cancel: `tests/test_finals_screen.py` binds 26 assertions from the built screen back to the committed artifacts byte for byte, which is real 재현 machinery on the surface that had none (+), and the SYSTEM INTEGRITY panel that same screen renders prints `commit a562045`, an object `git cat-file -t` cannot resolve in a fresh clone (F41, WFG-067) (-). Nothing touched a model, split, metric, arm or coupling. 제출 자료 16 held rather than rising to 17, and both halves are worth naming: WFG-064 is **closed** (F2 writes its values inside the bars with the two reference rules in a boxed legend, F7 reads vermilion = deadline-first and teal = nearest-first in both panels, and the paper lap found and fixed two further defects nobody had named), and `references.bib` gained an entry that **corrects the registry** from a page the lap opened. That is a clear +1 on 「그래픽 및 범례의 명확성」 and 「출처 명기」. It is spent on 「자료의 논리적 구성」 by F42: `JUDGE_QA.md:240` (Q10, **T0**) tells the student to say 「신고의 99 %가 목격 신고」, and `:353` (Q10d, **T0**) lists that same sentence under 「말하면 안 되는 것」. One document, two opposite recitation orders, same tier, and the student is told to memorise both. A 제출 자료 row does not rise while the document the student recites from contradicts itself. 연구 목적 16, 설계와 방법론 15, 창의성 15 held |
| 2026-09-07 | `1bca8ed` | 17 | 18 | 18 | 15 | 18 | **86** | **Every Track B row holds, and the hold on 제출 자료 is two real movements that cancel rather than a quiet window.** **연구 목적 17, held:** nothing this window changed what the research is for; the diff is one narrowing, one kit rebuild and one paper lap, and no question the project asks moved. **설계와 방법론 18, held:** 「기존의 연구와의 차별점」 is where WFG-171 landed, and what it did was make the differentiation *weaker and truer* rather than broader, so the row neither rises nor falls; the differentiator is still the output object and it is still stated in `docs/related_work.md` §3 and `KOREAN_OPERATIONAL_SYSTEMS.md` §3. **데이터 수집·분석·해석 18, held, and the hold is the finding:** nothing in this window touched a model, a split, a metric, an arm, a coupling or a protocol, and 「연구 재현 가능성」 is where **WFG-139** sits for an **eleventh** consecutive measurement. This lap did not re-measure it the way the previous ten did; it isolated it. After a COLD `gates.py --mode full` (ALL GREEN, exit 0, `1689 passed, 62 skipped, 2 xfailed`, 212.2 s) left the 25.9 MB SRTM tile on disk at 23:02Z, I deleted the tile and the `.nc` cache it produced and ran the single named suspect: `tests/test_spread_warmup.py::test_model_config_ignition_radius_increases_initial_burn`, **1 passed in 1.54 s, both tiles back at 23:06Z**. A row that scores 「a stranger can re-run this」 cannot rise while one test in the suite reaches Amazon S3 and `JUDGE_QA.md` Q28 tells a judge it does not. **창의성 15, held:** no method moved. **제출 자료 18, held on two cancelling moves.** **+1:** WFG-171 narrowed the NIFoS origin-point claim on every surface including two printed ones, registered it as `WC-009`, and broke a **circular provenance** in `references.bib:258`, where the bib note had been certifying the manuscript's wording (「which is what the manuscript states and all it states」) while the manuscript cited that same note as its source. Breaking a citation circle is a 출처 명기 gain of exactly the kind this row scores. **-1:** the same criterion now carries an explicit double standard on the same page. `paper/GAPS.md:118-127` records that **15 of the 29 references are verified 「via the Crossref record」** while §2 characterises six of them substantively, which is the standard this project rejected for a Korean agency two commits earlier. Found by the paper lap's own reviewer, and filed **nowhere** until this lap (**WFG-174**, **WFG-175**). ⚠ Not scored here: the two Pass/Fail rows. 서류 is the author's under the NH-008 closure; 위험성 is not at risk, the delivery layer stays dry-run. |

| 2026-09-04 | `ce31b91` | 16 | 15 | 19 | 15 | **17** | **82** | **+1 제출 자료 (16 to 17), and it is the point critic #8 named and withheld.** That lap held this row at 16 for one reason, written down: `JUDGE_QA.md:240` (Q10, **T0**) told the student to say 「신고의 99 %가 목격 신고」 and `:353` (Q10d, **T0**) listed the same sentence under 「말하면 안 되는 것」, so 「자료의 논리적 구성」 could not rise while the document the student recites from held two opposite recitation orders at the same tier. That is closed at `ce31b91` and closed the expensive way: one sentence — 「이 측정이 말하는 것은 위성을 일차로 둘 수 없다는 것까지이며, 어떤 소스가 일차여야 하는지는 재지 않았습니다」 — copied to Q10, `detection_floor.md` §10 (whose 우선순위표 is replaced by a 「이 측정이 말하는 것」 table where the 사람 신고 row reads 「재지 않았습니다」), the booth card's front and trigger table, and `docs/SESSION19_REPORT.md` Phase 3, which was **annotated rather than edited** because it is a record. Verified independently this lap by grep over every `.md` and `.html` in the tree: the only surviving affirmative-primacy strings are the SESSION19 record inside its own dated withdrawal block and §10's paragraph explaining what the table used to be. Also worth the point on 출처 명기: the card's new 「보여주지 않는 것」 bullet describes the withdrawn 99 % statistic **in words**, because the card's own registry gate refused to let it reprint an unregistered number while withdrawing it. It stops at 17 and does not reach 18 on three things this lap measured and could not fix: the bank's header says 33 questions and 14 T0 while the file holds **41 / 15 / 19 / 7** (F49, WFG-057, P0, worse than last window), 사망 26명 still carries 중앙재난안전대책본부 in the registry and 「경상북도 최종 집계·중앙재난안전대책본부 확인」 in `README.md:198` with nothing in the repository supporting the confirmation (WFG-051, fifth window), and `docs/detection_floor.md:13` opens the very file this fix cites by asserting 「한국의 산불 탐지는 사실상 전부 사람입니다」 on the statistic §10 forbids (F48, WFG-069). 연구 목적 16, 설계와 방법론 15, 창의성 15 and 데이터 19 all held: nothing in this window touched a model, split, metric, arm, coupling or protocol, and the window's new machinery is a string gate whose measured catch rate against an outside mutation set is 2 of 20 (F47) — said explicitly so that 39 new tests are not read as 재현 progress |
| 2026-09-04 | `3a70e16` | 16 | 15 | 19 | 15 | 17 | **82** | **Every row held, and 제출 자료 is the row this lap argued itself out of raising.** The window is one dev lap (WFG-069) and critic #9's own report commit. What earns a +1 and does not get it: `docs/detection_floor.md` §0 no longer opens by asserting 「한국의 산불 탐지는 사실상 전부 사람입니다」 on a statistic §10 of the same file forbids in bold. It opens instead on a **count** — 경북 산불감시카메라 152대, 대당 약 6천만 원, **최초 발견 0건**, over **2022년 한 해와 2023년 4월 28일까지** — with the agency, the article date and the URL in the same paragraph, and it says in as many words that what comes in the satellite's place 「이 저장소가 재지 않았습니다」. **Critic #10 opened the source article rather than trusting the citation** (경향신문 2023-04-28, <https://www.khan.co.kr/article/202304281446001>): 「경북에는 152개의 산불감시카메라가 설치돼 있다 … 대당 6000만원 정도다」, 「감시카메라로 산불을 먼저 발견한 건수는 '0'건 이었다」, and the 99 % figure carries the article's own 「올해」, which is the interim scope §0 now prints beside it. Every element of the new paragraph holds against the primary source. That is the strongest 출처 명기 work yet done in a judge-facing Korean document. **What spends it, and it is new information this lap:** critic #9's window certified that a grep of 「every `.md` and `.html` in the tree」 left no surviving instance of the withdrawn ordering claim. Both claim gates and that grep are **Korean-only**, and the claim is alive in English at `docs/auto/research/RESEARCH_BRIEF_2026-09-03.md:75` — 「a satellite trigger would have fired +22/+34/+64 min *after the human report* … the design is report-first, satellite-confirm」 — under the heading 「the ten hardest judge questions, with the answers that survive the verdicts」, in the file the routine tells every lap to read, and again at `docs/auto/research/sweeps_2026-09-03/R3_science_gaps.md:22` (WFG-070, P0). 출처 명기 rose and 자료의 논리적 구성 fell by the same amount, so the row holds at 17 rather than moving in either direction. It does not reach 18 on three unchanged defects: the bank header still says 33 / 14 / 13 / 6 where the file holds **41 / 15 / 19 / 7** (WFG-057, **fifth** window, and it is now blocking this routine's own judge-drill output for a second lap — a critic that cannot add a question without making the miscount worse cannot do step 3 of its job); WFG-051, **sixth** window, now sharper — `fire2025_chain_deaths` carries `agency: 중앙재난안전대책본부` while `paper/manuscript.md:37` calls the same 26 deaths 「the **provincial** disaster headquarters' count」, four documents and three attributions; and the 8.2 km h⁻¹ spread rate is still in no document a judge is handed (WFG-065, fourth window). 연구 목적 16, 설계와 방법론 15, 데이터 19 held: nothing in this window touched a model, split, metric, arm, coupling or protocol. **창의성 15 held, and it was the closest call.** `tests/test_external_figures_carry_their_scope.py` contains `test_the_escapes_this_gate_cannot_close_are_still_open`, which fails when the gate starts catching something its own docstring says it cannot — executable documentation of a limit, and the first instrument in this repository whose job is to keep its author honest rather than its documents consistent. That is real novelty. It is novelty in the **loop's process**, not in the 작품 a judge scores, and the same window's evidence is that the process produced its **third** parallel hand-rolled string family (WFG-071) rather than the general one WFG-062 asks for. Credited in words, not in a point |

| 2026-09-04 | `c65dc56` | 16 | 15 | **18** | 15 | **17** | **81** | **데이터 recovers one of the two points F54 cost, and 제출 자료 pays one back, so the total holds at 81 on a changed composition.** **데이터 17 to 18.** The wrong county is corrected and I verified it without trusting the lap that fixed it: reading the eight committed GeoJSON files directly, `minwon_agencies` holds 55 features whose road-address strings contain 영덕군 **56 times and 봉화군 zero times**, `samul_eqwav_point` (지진해일긴급대피장소) is populated at **92** rows where the first cut returned zero, and every non-empty layer now overlaps `regions.lookup('yeongdeok_2025').bbox_wgs84` with 57 to 84 % of its points inside. The extractor and `tests/test_juso_yeongdeok.py` now assert the address check and set containment, so this artifact carries the check its absence caused. Why +1 and not +2, which is what F54 took: the same commit registered `juso_yeongdeok_samul_lifesav_point_count` = 0 and `juso_yeongdeok_samul_emerwat_point_count` = 0 and wrote at `docs/juso_yeongdeok.md:58` 「(인명구조함 and 비상급수시설 have no 영덕 rows)」 as a fact about the county, in the same commit whose MEMO lesson reads 「a "zero rows" result for a layer that must exist in the region is a wrong-filter signal, not a fact」 (WFG-080); and eight registry values were **edited in place** with their caveats deleted, against CHARTER §3 rules 2 and 3, the replacement caveat stating the position outright as 「The wrong first values are kept in git history (3fdb888), not here」 (WFG-078). A 재현 row does not take the full recovery in a window where the registry stopped being append-only and no gate noticed. **제출 자료 18 to 17.** For it: WFG-067 closed, so the screen's own SYSTEM INTEGRITY line resolves to a commit reachable from HEAD (`d5e2562`), which is the defect that capped this row for four windows. Against it, three defects all in documents the student recites from: `docs/juso_yeongdeok.md` contradicts itself twice inside one file, reprinting at `:61` both the withdrawn 45 km figure its own `:15` forbids and the county name its own `:29` refuses to write without the record (WFG-079); `JUDGE_QA.md` Q35 still tells the student to answer 「아니오」 and 「그 줄은 지금 틀렸습니다」 about the very stamp that was fixed, quoting `a562045`, which is on neither the screen nor the judge's mind (WFG-081); and the bank header still reads 33 / 14 / 13 / 6 where the file holds **41 / 15 / 19 / 7**, counted again this lap and unmoved for three windows (WFG-057). Two of those three were written **in this window**, which is why the net is negative rather than a hold. **연구 목적 16, 설계와 방법론 15, 창의성 15 held:** nothing in this window touched a model, split, metric, arm, coupling or protocol. |
| 2026-09-04 | `baf6962` | 16 | 15 | 18 | 15 | **18** | **82** | **+1 제출 자료 (17 to 18), and it is exactly the point critic #12 named and withheld.** That lap capped this row on three defects in documents the student recites from; **two of the three closed in this window and I verified both from the files rather than from the laps that fixed them.** **WFG-057:** `docs/auto/JUDGE_QA.md` now says 41 questions and T0/T1/T2 = 15/19/7, and counting the file directly gives **41 questions, 15 / 19 / 7** - the header, the four drill rounds and the file finally agree, and the drill table names every one of the 41 by number where it had been naming 33. The regex now reads the lettered suffix and the parenthesised provenance, so the next question added makes the header and the table wrong together and the gate red. That miscount had stood for six windows and, worse, it was disabling this routine's own judge drill. **WFG-081:** Q35 no longer scripts the student to open with 「아니오, 그 줄은 지금 틀렸습니다」 about a defect fixed nine hours earlier. The rewritten answer is 「예」, and it holds: `web/finals.html` carries `"git":"d5e2562"`, `git cat-file -t` resolves it and `git merge-base --is-ancestor d5e2562 HEAD` succeeds in this fresh clone. The item keeps the history as 내력 rather than deleting it, which turns the repaired defect into the file's best worked example of what reachability means and why existence is the weaker check. Its two supporting claims also verify: `tests/test_finals_screen.py` holds **32** tests and two of them read that line. **Judge-drill evidence for the point, not just the repairs:** I resolved every one of the **77 distinct file paths** the bank cites; all 77 exist (five are directory-relative shorthands - `check_forbidden.py`, `check_number_collisions.py`, `delivery/sms.py`, `routing_demo.npz`, `routing_demo_canonical.npz` - resolving under `scripts/`, `src/wildfireguardian/` and `data/processed/`). I then drilled the hardest T0, Q1, against the artifacts: pooled recall **0.138**, mean-of-folds **0.0867**, average precision **0.169**, prevalence **0.0197**, threshold 0.3 and router cut 0.5 all re-derive from `data/processed/operating_point/per_fire_recall.json` and `docs/NUMBERS.json` exactly as the answer states. **No question in this bank went unanswerable from a file this lap**, so no 「근거 없음」 entry was added; the one that was already open, Q34 (spread rate, WFG-065), is open for a fourth window. **Why 18 and not 19.** The third defect is untouched: `docs/juso_yeongdeok.md` still contradicts itself inside one file, its `:61` correction paragraph writing both the 「약 45 km」 its own `:15` forbids in bold and the 봉화군 its own `:29` refuses to name without 행정표준코드 (WFG-079), and `:58` still records two zero-row layers as facts about the county against its own commit's MEMO lesson (WFG-080). **데이터 18, 연구 목적 16, 설계와 방법론 15, 창의성 15 all held:** nothing in this window touched a model, split, metric, arm, coupling or protocol, and WFG-078 (eight registry values edited in place, caveats deleted) is unchanged. The window's six red `auto-gates` runs are not scored, on critic #3's standing precedent that a judge scores the tree the author brings to the booth and not the loop - and they are closed anyway (`21b8740`, verified: run 95 at `baf6962` is `success` and `gates.py --mode full` exits 0 here) |
| 2026-09-04 | `ed35f0d` | 16 | 15 | **19** | 15 | **17** | **82** | **B holds at 82 and the two moving rows move against each other, which is the honest reading of this window.** **데이터 18 to 19:** `paper/manuscript.md:656-671` now states that every refuge in every result is an OpenStreetMap point, names the agency-designated inventory that no result uses, gives both data dates and the county-versus-walk-box extent mismatch, and weakens the category claim to what the seven-layer cut supports — a concession made from the data rather than extracted by a critic, which is the behaviour this row is for. WFG-062 adds the second half: the record of what this repository withdrew is now machine-read against **915 gated files** of 989 tracked, not 11, and `docs/withdrawn_claims.md` §4 publishes in bold what that did NOT buy. I measured the unbought half from outside — a 26-sentence probe set written without reading the patterns: **verbatim 6/6, rewordings 1/20** — and it confirms the document rather than contradicting it. **제출 자료 18 to 17:** `docs/auto/JUDGE_QA.md:585-603`, Q18, is a **T0** answer telling the student that the designated-shelter comparison is blocked by a portal login, while `6f33eca` put the re-cut 영덕 designated-site layers in the tree and NH-012 is closed. Scored on the same surface critic #13 raised this row on and in the opposite direction, because it is the same kind of defect: the bank teaching a sentence the tree has moved past (WFG-087) |
| 2026-09-05 | `43710f7` | 16 | 15 | 19 | 15 | **18** | **83** | **+1 제출 자료 (17 to 18), and it is the first time in this table that the point comes from a document a judge is spoken to from rather than a document a judge might read.** `docs/auto/DEMO_SCRIPT_5MIN.md` is 231 lines of Korean that turn the finals screen into a five-minute performance a student can run five times: six segments summing to exactly 300 s, a droppable sentence per segment, one interruption answer for each of the five judge lenses, and a 35-row table binding every spoken number to a registry key or a named artifact with a 화면/구두 label saying whether the judge will also see it. I resolved all 33 keys against `docs/NUMBERS.json` myself and every value matches the spoken text. The 화면/구두 column is the part I would have expected the loop to get wrong and did not: the lap's own reviewer caught four rows marked 화면 whose keys sit in the built page without any card rendering them, and the shipped test judges 화면 by whether `scripts/finals.template.html` references the key, not by whether the key appears in `web/finals.html`. That is the correct discriminator and I checked it holds. `JUDGE_QA.md` Q18 also stopped teaching a blocker the tree removed the same day (WFG-087, critic #14's item, closed). **Why 18 and not 19:** the two caveats that keep this script inside the repository's own withdrawn claims are the two sentences it marks **[버림]**, first to be dropped when the student runs long (WFG-095), and I measured that deleting either is invisible to every claim gate in the tree. A document scored on 논리적 구성 does not reach 19 while its own never-say list is enforced by its most disposable sentences. **데이터 수집·분석·해석 · 19, held.** The window registered no new result and withdrew none. The one thing that would have moved it down and did not, because it is a gap rather than an error: the headline 24.73 % / 9.17 % the script calls 「이 작품의 전부」 carry no uncertainty statement anywhere in the tree and the Q&A bank has no card for the question (WFG-096, found in this lap's drill). **연구 목적 16, 설계와 방법론 15, 창의성 15, held.** No model, split, metric, arm or coupling moved, and a demo script is not methodology. Said explicitly so that 231 good lines are not read as science. |
| 2026-09-05 | `c37f27e` | 16 | 15 | 19 | 15 | 18 | **83** | **Every Track B row held, and the hold on 제출 자료 is this lap's argument with itself.** The window's paper work is the best in the series: §6 now states the sampling frame of the routing shares instead of inventing an interval (458 origins are every 18th of 8,443 walk-graph nodes and then hazard-filtered, 5.4 % of the graph, so no design-based standard error is defined), three written expert replies enter §5 and §6 as limitations rather than as endorsements, the withdrawn-claims registry is described in §3.5 **with its measured limit** (it matches spellings, not meaning), and all 27 references carry a `verified` note whose URL a lap opened - I spot-checked five, including `radeloff2005` against its Crossref record (Ecol. Appl. 15(3):799-805, doi 10.1890/04-1413). That is 출처 명기 done properly. **Why it does not take the point:** the manuscript is at 7,467 words against a 7,500 hard fail with 33 of margin, and `GAPS.md` hands the structural choice to the author rather than the loop, so 논리적 구성 is now constrained by a budget the loop cannot spend; and the two artifacts this row ultimately needs, the printables (R7) and the README Round-4 section with the abstract (R8), did not move again. **데이터 수집·분석·해석 19, held, and the reason it did not fall is worth writing down.** Two things in this window pull the other way. The design comparison inside `tests/test_detection_ordering_is_not_claimed.py` re-registered from 11/1 to 8/0 hits, which is a weaker measurement of the choice it records, though the lap replaced the degenerate `a > b * 4` with a fixed corpus that no prose rewrite can drain - better instrument, smaller sample. And **WFG-096, the row filed last window to fix the missing uncertainty statement, shipped with the false premise the paper lap's reviewer had just removed from §6**: it told the next lap the origins are a census. Corrected in place this lap. The row holds because the manuscript now says the true thing and the false thing lived only in a backlog cell. **What would move it and is filed rather than scored:** the headline 24.73 % was 3.53 % before this project found and corrected the DEM defect that trains every other fire's model (`docs/dem_defect_2026-08-02.md:67-70`), and no judge-facing document says so (WFG-101). That is a presentation gap on top of a strong result, not an error in the result. **연구 목적 16, 설계와 방법론 15, 창의성 15, held.** No model, split, metric, arm or coupling moved. |
| 2026-09-05 | `26e200d` | 16 | 15 | 19 | 15 | **19** | **84** | **+1 제출 자료 (18 to 19), and it is the point critic #16 named and withheld one lap ago on exactly this row.** That lap wrote 「A row scored on 논리적 구성 does not rise while the document's own time budget cannot be true at both ends (WFG-100)」. WFG-100 is closed and it is closed better than the row asked for. `docs/demo_script_pace.md` counts **1,684** spoken syllables under a written-down convention, allocates 300 s by largest remainder so the six segments run **5.55 to 5.66** syl/s where they ran 4.51 to 7.29, and **no sentence was deleted to buy seconds**. I re-derived every cell rather than reading the table: 161+246+280+338+331+328 = 1,684, 29+44+50+60+59+58 = 300, 338/60 = 5.63, 328/58 = 5.66, and the old budget's 4.507 to 7.289 is a 1.617x spread with 마무리 fastest and last, exactly as claimed. Two things earn the point beyond the arithmetic. **The before state is committed as its own artifact** (`data/processed/demo_script_pace/pace_before_039a0de.json`, measured `--from-ref 039a0de`) because the lap's own reviewer blocked it for asserting the before numbers from memory — a repository that re-derives the state it destroyed is doing 데이터 해석 on its own documents. **And the disagreement with me is published rather than smoothed**: §「What could not be reconciled」 states that no variant reproduces critic #16's 1,630, that the nearest one agrees on the total only by cancellation, and that an earlier draft calling that agreement 「the best evidence available」 was struck by the reviewer as an inference the segments contradict. That is the paragraph a judge scoring 과학적 사고 would most want to find, and it is about the loop's own critic. **Why 19 and not 20:** F1. The document those 19 points are scored on mis-describes the baseline of its own core act — 3막 tells five judges the comparison is against 「지금 이 순간만 보는 지도」 while `evacuation.py:270` and `real_roads_real_hazard.md:50` both say the arm is fire-blind (WFG-103). A 제출 자료 row does not reach 20 while the highest-value 60 seconds of the booth hands the weaker opponent the stronger description. **데이터 수집·분석·해석 19, held.** No result was registered and none withdrawn; a syllable count is not a scientific result. The thing that would move this row is filed and not scored, because it predates this window and scoring my own discovery would be scoring the critic and not the tree: the present-perimeter routing arm (**WFG-033(b)**, P2, i.e. after the finals) is the only measurement that separates 「예측」 from 「관측」 in the headline 24.73 %, and the Q&A bank has no card saying so (WFG-104, NH-027). **연구 목적 16, 설계와 방법론 15, 창의성 15, held** — no model, split, metric, arm, coupling or protocol moved. |
| 2026-09-05 | `6afd252` | 16 | 15 | 19 | 15 | 19 | **84** | **Every row held, and the flat window is the finding.** **연구 목적 16:** nothing touched the purpose; the frame is unchanged. **설계와 방법론 15:** no model, split, metric, arm, threshold or coupling moved (`git diff 6236c81..HEAD` touches no file under `src/`). **데이터 수집·분석·해석 19:** the window's interpretation work is real — the paper routine found the fire-blind overclaim in the abstract independently of critic #17 finding it in the script, and corrected it in four places (`paper/manuscript.md` abstract, §4.3, §4.4, F5's caption) plus a new GAP G7 that names the arm and a cheaper variant of it. It does not reach 20 because the arm that would separate 예측 from 관측 is still unrun (WFG-033, P2, NH-027), and because **the number that would answer the same judge without running anything is registered, gated, and in no judge-facing file** — `mr_uiseong_fa_rescue_rate` = 0.883, with 영덕 95.5 % and 울진·삼척 23.1 % beside it in `docs/multi_region.md:430-434` (WFG-106, filed this lap). **창의성 15:** unchanged. **제출 자료 19:** WFG-103 corrected the baseline sentence on **both** surfaces it lived on, the script and the judged screen, and the pace re-measure that followed caught a genuine defect in its own test (`sorted(glob(...))[-1]` was selecting the *before* artifact). Against that, a row scored on 논리적 구성 does not rise while the two documents a judge sees at the same moment disagree: the screen says only what is measured, the script adds 「이 도구가 없을 때의 기준선입니다」, which is unmeasured (WFG-104). Held, not raised |
| 2026-09-05 | `92bfc4f` | 16 | 15 | 19 | 15 | 19 | **84** | **Every Track B row holds, and holding is the right answer rather than a slippage note.** **연구 목적 16, 설계와 방법론 15.** Neither moved and 설계 has not moved in nineteen laps; what caps it is unchanged and is not a documentation problem: the headline is measured against a **fire-blind** arm, the present-perimeter arm that would make the comparison fair is WFG-033(b), it is P2 (after the finals), and NH-027 has been with the author since 09-05. **데이터 수집·분석·해석 19, and this is the row I most wanted to move.** The window produced a genuine reproducibility finding: `make all-checks`, the command `KCF_READINESS.md` R3 names, does not pass on any machine, and four of its six differences (`registry_entries: 320 -> 326` and three tracked `pace_*.json`) are in every clone rather than sandbox conditions (NH-029, verified by running `make baseline-verify` here). But the score is the state, not the loop's self-knowledge: the tree is no more reproducible today than yesterday, it is only more honestly described, and the freeze is still stale. Against a rise: I re-read all three `mr_*_fa_rescue_rate` entries in `docs/NUMBERS.json` and the caveat string is byte-identical on all three, reading 「SMALL denominator (13-20 origins)」 while the denominators are 13, **44** and **103** (WFG-106). **제출 자료 19.** For it: `BOOTH_SETUP.md` is 256 lines that name their own gaps (§7 says the paper fallback stands on printables that do not exist; §8 lists four things the document does not do). Against it: R1 measured for the first time at six on-screen registry keys in no committed mapping table (WFG-110), and the template still carries a sentence the built screen withdrew (WFG-109). |
| 2026-09-05 | `ce262fe` | 16 | 15 | 19 | 15 | **18** | **83** | **제출 자료 19 to 18, and it is one line on the judged screen.** The 검증 레지스트리 evidence card prints 「built at commit 41498ef」 (`web/finals.html:1924`). Run here: the object exists, `git merge-base --is-ancestor 41498ef HEAD` **exits non-zero**, and `git branch -a --contains` puts it only on `origin/auto/lap-b1989d5-superseded` and `origin/ordering-boundary`. It is the WFG-067 failure shape in the WFG-067 panel, unread by the WFG-067 gate (`tests/test_finals_screen.py:544`, `:550`, `:649` all read the OTHER stamp), and `docs/auto/JUDGE_QA.md` Q35 tells the student to say that hash is 「현재 브랜치에서 닿는 커밋」. A row scored on 출처 명기 does not hold while the reproducibility panel sources itself to a commit a judge cannot reach and the spoken answer denies it (WFG-115). ⚠ It is stale as well as unreachable: `built_at_git_commit` dates from a 153-entry registry and the card beside it prints 326. **데이터 수집·분석·해석 19, held, on two moves that cancel.** Up, and it is the window's best work: the author executed NH-029 option A at `38620f2` and I re-ran `make baseline-verify` here — six differences are now two, both the git-ignored laptop-only manifests, and the re-freeze preserved every protection (both `untracked_contracts` hashes and all four `protected` hashes byte-identical against `38620f2^`; `tracked_processed` 127 to 130). Down: `paper/STATE.json` `built_pages: 21` is now a number **no machine in this loop can re-derive** — `page_check()` needs LibreOffice Writer, which greps to zero hits in `.github/workflows/`, `bootstrap.sh`, `requirements.txt` and `Makefile`, and the drift check skips the field unless `metrics_ok` (WFG-116). A row scored on 재현 가능성 cannot rise on a measurement only one dead sandbox could make. **연구 목적 16, 설계와 방법론 15, 창의성 15, all held.** No model, split, metric, arm or protocol moved. 설계 has not moved in twenty laps and what caps it is unchanged: the headline's opponent is fire-blind. That is no longer a critic's complaint — the author closed NH-027 with 「Run it in the sprint now, P0」 and the row (WFG-114) is at the top of the table. It will move this row when it is run, and not before. |
| 2026-09-05 | `492364c` | 16 | 15 | 19 | 15 | **17** | **82** | **제출 자료 18 to 17, and the judge drill found it rather than the diff, because the diff is one line.** `docs/auto/JUDGE_QA.md` Q30 is **T0** — the question 「그러면 오늘의 숫자는 왜 믿습니까」, which is the question this entire project's 제출 자료 case rests on — and its drafted answer has the student say 「등록된 값 295개 중 261개가 ... 나머지 34개는 재현 불가」, then split the 34 into 16 + 18. I counted `docs/NUMBERS.json` myself: **326** entries, **268** reproducible, **58** not, so all three spoken figures are stale and the decomposition covers 34 of 58. The 검증 레지스트리 card on the screen behind the student prints **326 · 268**. A row scored on 출처 명기 and 논리적 구성 does not hold 18 while the document the student speaks from and the screen they stand in front of give a judge two different answers to 「얼마나 재현됩니까」, on the highest drill tier, with no gate reading either (`tests/test_judge_qa_bank.py` contains no registry count). **-1 and not -2** for three reasons checked rather than assumed: the bank's own self-count is correct (41 questions, 15 / 19 / 7, re-counted here), the registry itself is right and it is the prose about it that is stale, and a ⚠ 근거 없음 note is on Q30 as of this lap so the student is told to point at the card instead of reciting. WFG-117, and this lap's one `fix-before-next-row` item. **연구 목적 16, 설계와 방법론 15, 데이터 수집·분석·해석 19, 창의성 15, all held:** nothing in this window touched a model, split, metric, arm, coupling or protocol, and nothing could have — the window is a single backlog status cell. ⚠ Recorded against 데이터 rather than for it: WFG-114, the present-perimeter arm that gives the headline its fair opponent and is the standing root objection's only scheduled answer, was claimed at 18:20Z and has no artifact behind it (NH-030). The 19 is for what is committed, not for what is claimed |
| 2026-09-06 | `de7bd0a` | 17 | **14** | 19 | 15 | 17 | **82** | **설계와 방법론 15 to 14, and it is the one row a statistician judge moves this window.** This row is scored on 「변수의 정의와 제어의 적절성」. The present-perimeter arm has exactly one free variable — the buffer width — and nothing in the problem chooses it, which the arm's own document says. It was swept at **five** points: 250, 500, **1000**, 2000, 3000 m, read cell by cell from `data/processed/present_perimeter_arm_uiseong_andong_2025.json` → `buffer_sensitivity` by this critic rather than from any lap's prose (safe totals 275 / 284 / **345** / 275 / 283). The winner's nearest measured neighbours are a factor of two away on **each** side, so the run cannot separate a spike at 1 km from a plateau spanning roughly 800 m to 1.5 km — and this window is where the project started **arguing** from that unresolved shape, on the booth script, in the sentence the student says out loud. The deduction is for the design, not for the prose: a one-variable sensitivity study whose grid is coarser than the structure it reports is the classic finding, and the fix is three more widths on a script that already exists (**WFG-127**, routing only, no retrain, no re-acquisition). **데이터 수집·분석·해석 19, held.** Nothing this window re-measured anything; it wrote about a measurement made last window. **제출 자료 17, held, and it is two real effects cancelling rather than a quiet window.** Up: `docs/fair_opponent_line.md` is the best submission-material discipline the loop has shown — one home for one contested line, a gate whose docstring records the mutation that defeated its first scope, and a §4 that says out loud 「this is the honest version, and it is less flattering than the one this file first wrote」 after checking the brief it was handed and finding it wrong. Down: the same window left the **manuscript** telling a reviewer at `paper/manuscript.md:386` that this very experiment 「is specified in the project backlog and scheduled after this sprint」. It ran on 2026-09-05 and its artifact is committed. The loop wrote the rule for that defect **in this window's own diff** (`docs/fair_opponent_line.md:32-35`: 「A limitation that has been closed and is still spoken is a fabricated limitation」), applied it to the booth script, and did not apply it to the paper. Raised to P0 here as WFG-126. **연구 목적 17, 창의성 15, held**; nothing in the window touched either |
| 2026-09-06 | `91d3e05` | 17 | **15** | 19 | 15 | **18** | **84** | **+1 설계와 방법론 (14 to 15) and +1 제출 자료 (17 to 18); 데이터 holds at 19 on two moves that cancel, and the hold is this window's finding.** **설계와 방법론 14 to 15.** Critic #23 docked this row for a sentence asserting a shape a five-point grid cannot resolve. The manuscript's new §4.5 states the change of *kind* across the widths — routes crossing live ground at the narrow end, budget-expired arrivals at the wide end — and then says outright that the grid holds a single point in the region a 「which width」 claim would be about, asserting **neither** spike nor plateau; and its reviewer removed 「roughly 800 m to 1.5 km」 as an unregistered number, 「a narrow optimum at 1 km」 as a presupposition of two registered forbidden phrasings, and 「a plateau that wide is a width an operator could aim at」 as an unmeasured affirmative claim in the other direction. This row is scored on 「변수의 정의와 제어의 적절성」 and the window defined the free variable and stated the control's limit instead of the conclusion. It does not reach 16: WFG-127(i) is still live on `docs/fair_opponent_line.md` §3 and `DEMO_SCRIPT_5MIN.md` 3막, and §4.5's own last sentence points at them. **제출 자료 17 to 18.** For Track B the manuscript *is* the 제출 자료. WFG-126 closed inside the window that filed it (verified at this head: §4.3's `[GAP]` no longer says the arm is 「scheduled after this sprint」); §4.5 names the NH-032 deadlock in the paper's own voice and quotes no count from the arm; F9 was drawn, found to determine the contested margin in two subtractions off its own axis, and withdrawn from the manuscript while staying in `FIGURES`; and the 22-page count is measured under Carlito rather than converted from words. It does not reach 19 because the paper is pointing at a defect on two documents it cites rather than at one that is fixed. **데이터 19, held, and not inherited.** Up: the best interpretation discipline this loop has produced — a section that reports a result and refuses to number it because the registry's own all-or-none caveat and NH-032 leave no legal count, said in writing rather than as timidity. Down, and it is why 20 is not available: `docs/multi_region.md:191` states `fa_exceeds_budget` as 「fire-blind route safe, future-aware router cannot finish in time」 with **2** for 의성·안동, while `docs/present_perimeter_arm.md:46-63` measured those two origins' fire-blind arrivals at 624.8 and 628.2 minutes and concluded the bucket is **empty** under one rule. The manuscript took that qualification into §4.4 this window; its twin document did not, and `README.md:113` links the twin as 「완전한 분할」. **WFG-128.** **연구 목적 17 and 창의성 15, held** — nothing in the window touched purpose or method novelty. |
| 2026-09-06 | `b70e464` | 17 | 15 | 19 | 15 | 18 | **84** | **Every row holds, and the hold is correct rather than lazy: the window's one substantive commit is a booth artifact, and Track B is scored on the research.** **설계와 방법론 15, re-earned rather than inherited.** Critic #23 docked this row to 14 for a sentence asserting a shape a five-point grid cannot resolve; #24 restored it on the manuscript's §4.5. The 06:17Z lap ported the same discipline onto the two booth-side documents — `docs/fair_opponent_line.md` §3 now says the grid separates a spike from a plateau 「in neither direction, and this file asserts neither」, and `DEMO_SCRIPT_5MIN.md` 3막 says 「좋았던 폭 주변이 뾰족한 봉우리인지 넓은 고원인지 이 실험은 가리지 못합니다」 — so the correction is now on every surface rather than one, which is what 15 was already scored for. No further move. **데이터 수집·분석·해석 19, ninth window.** Nothing measured this window; the two open tests of the headline (WFG-129's G7 run, WFG-125's real-model margin) are still unrun, and 20 is what running them would buy. **제출 자료 18, and it holds against a real movement in each direction.** Up: the repository now builds its own booth paper deterministically, with every source and font sha256 recorded, and `docs/printables.md` §4 documents a failure mode (a subset font drawing blanks silently) that it turned into a three-layer gate — that is 「자료의 논리적 구성」 and 「출처 명기」 done properly. Down: the same build asserts, in a committed manifest, that `docs/submission_reconciliation.md` 「does not exist yet」 when it is `done(20260903T0653Z)` and 13,702 bytes (WFG-130), and `web/finals.html` still prints 326 / 268 against a registry holding 383 / 325 in its sixteenth window. A row scored on whether the materials say true things about themselves cannot rise on a build that got one of its own claims backwards. **연구 목적 17 · 창의성 15** unchanged; no window touched either. |
| 2026-09-06 | `b2bdaf0` | 17 | 15 | 19 | 15 | 18 | **84** | **Every row holds, and the hold is the right answer rather than a quiet one: the window is one booth-screen repair and one manuscript lap, and neither changes what a research judge is scored on.** **연구 목적 17, 설계와 방법론 15, 창의성 15, held** — no model, split, metric, arm, coupling or protocol moved. **제출 자료 18, held.** The 0940Z paper lap narrowed two more sentences in the honest direction: §4.5 stopped printing two arrival times that are values of the withheld arm, and its closing sentence now says **one** repository document still draws the stronger conclusion from the five-point sweep rather than two, because `docs/fair_opponent_line.md` was narrowed while the paper was being revised. `paper/GAPS.md` G8 also records that all **57** `pp_uiseong_*` registry caveats still assert the width claim WFG-127(i) retired, which is a real finding filed against a surface the paper cannot touch. That is careful work; it is also work a KCF research judge reads at the same 18 as yesterday. **데이터 수집·분석·해석 19, held, and the reason it does not move is this lap's own finding rather than the window's.** Credit exists: `tests/test_finals_payload_rederives.py` re-runs `build_finals.py` and compares payloads structurally, exempting three build-provenance fields and checking the integrity block against the builder's own `GATES` constant rather than a hand-typed list, graded against four mutations. That is the right shape for a reproducibility gate. Against it, and this is why the row does not rise: **the same window proved this loop can carry a false measurement for five consecutive laps** — `41498ef` is an ancestor of `HEAD` (277 commits back, three independent checks on a 300-deep clone) and five critic laps published the opposite as re-run fact, each re-running the same command inside the same truncated graph, two of them deepening by an amount they chose. A row scored on 「연구 재현 가능성」 and 「예상 결과에 대한 논리적인 해석」 does not rise in the window that shows the reviewer's own instrument was never validated. **The defect never reached a judge-facing number**, which is why this is a hold and not a fall. |
| 2026-09-06 | `e95fe28` | 17 | 15 | **18** | 15 | **17** | **82** | **Two rows fall and the tree did not get worse: the estimate was wrong.** **데이터 수집·분석·해석 19 to 18.** This row carries 연구 재현 가능성, and the repository's own clean-clone claim is false. On a clone made at 16:57Z with no `data/raw/` at all, `gates.py --mode full` wrote `data/raw/dem/srtm/N36E129.hgt` (**25,934,402 bytes**) at 17:02Z, fetched from `elevation-tiles-prod.s3.amazonaws.com` by `tests/test_spread_warmup.py:156` (`dem_source="srtm"`, no skip guard), while `docs/clean_clone_gates.md:27` reads 「No network, no keys, no `.env`.」 and `docs/auto/JUDGE_QA.md` **Q28** cites that file to a judge. CHARTER §4b forbids a test depending on the network in those words. Six terrain-plausibility tests keyed on that tile (`test_srtm_dem.py:81/:94/:109/:170`, `test_validation_robustness.py:57`, `test_validation_session3.py:171`) **have never run in CI**, because `skipif` is evaluated at collection and the download always lands after it; they are the checks that the DEM the router walks over has ocean clipped to 0 m, reaches 400 m and falls to the east. **WFG-139.** **The credit in the same window is real and is why the fall is one point and not two:** WFG-133 registered the withdrawn reachability claim as `WC-004` across 925 gated files, and the independent reviewer caught the first version of the gate pointing backwards (3/3 true sentences wrongly flagged, 1/6 reworded false ones caught), with the fix graded against the reviewer's own nine sentences, committed as `REVIEWER_SET_TRUE`/`REVIEWER_SET_FALSE` so the score re-derives every lap: **0/5 and 6/6**. That is the best instrument-grading in this repository. **제출 자료 18 to 17.** `README.md:22-26` asserts that 42 of 458 origins reach a refuge 「**only** when the router accounts for where the fire **will be**」; `paper/manuscript.md`'s Abstract carries the same two numbers and then says that contrast 「does not separate knowing where the fire will be from knowing where it is」, the baseline being fire-blind (`src/wildfireguardian/routing/evacuation.py:270`). `paper/GAPS.md` G7 records the abstract's own correction and names the two surfaces already repaired for the same claim (WFG-103 the booth script, WFG-109 the finals template). The README is the fourth and was never touched. **WFG-138.** **연구 목적 17, 설계와 방법론 15, 창의성 15 held:** nothing in this window touched a model, split, metric, arm, coupling or protocol. ⚠ Neither defect is new to the tree; the 19 and the 18 were scored without them by earlier laps, mine included. |
| 2026-09-06 | `1b26c3a` | 17 | **16** | 18 | 15 | 17 | **83** | **One row moves, 설계와 방법론 15 to 16, and it is the row that scores 「기존의 연구와의 차별점」.** For twenty-eight critic windows this project's answer to 「무엇이 다릅니까」 was framed against academic work and against the occurrence-side 국가산불위험예보시스템. The window's one substantive commit (`e888c8e`) establishes, with opened sources, that **Korea already runs two operational wildfire-spread systems** — NIFoS's 「AI 기반 산불확산예측시스템」 operator console (user guide 연구자료 제1201호) and 경기도's **G-DAPS** civil-defence alert model (30-minute steps, 읍면동 resolution, 589 alert facilities, trial from April 2026) — and states the differentiator as the **output object** rather than accuracy: a suppression footprint and a township siren decision against a per-household walk-or-be-rescued decision on public, re-derivable data. A methodology row rises when the study knows what it is not, and it did not know this yesterday. **I re-opened both live URLs rather than accepting them**, and every figure holds except one date (2026-02-13, not 2026-02-12; **WFG-146**). **+1 and not +2, and the point comes back off if this does not travel:** none of it is on a judge-facing surface, the Q&A card is **WFG-144** at **P1**, and the research lap's own sequencing puts it behind a printables rebuild that has been displaced four windows. A differentiator a judge never hears is a note, not a method. **데이터 수집·분석·해석 18 held:** WFG-139 is unrepaired and no data work happened. **제출 자료 17 held:** WFG-138 is unchanged and the booth kit drifted a third time, but both are the same defects critic #28 already docked this row for, and I do not score one defect twice — the new half of WFG-138 (Q19's uncorrected 42) is the same claim on a fifth surface, not a new one. **연구 목적 17, 창의성 15 held:** nothing touched a model, split, metric, arm, coupling or protocol, and `git diff e95fe28..1b26c3a` changes zero lines outside `docs/auto/`. |
| 2026-09-06 | `524f13c` | 17 | 16 | 18 | 15 | 17 | **83** | **Every row held. 제출 자료 held on two moves that cancel, and the second of them is this lap's one finding.** **+1**: the README's headline bullet no longer asserts what `paper/manuscript.md`'s Abstract disclaims. WFG-138 closed both halves at `0ed15aa`/`20b0b7e`, the caveat sits inside the bullet and inside Q19's spoken sentence rather than beside them, and `tests/test_future_aware_attribution.py` plus the new bank test grade themselves by mutation, including a **move** mutation, which is the axis the claim is about. **-1**: the same bullet now carries the first of the **two** caveats `paper/manuscript.md:506` says bind that comparison and not the second. 「The forecast-aware arm plans on the same hazard field it is graded against, so whatever it is worth ... is what a *noiseless* forecast is worth; this project's own model is worth less, by an amount no run here measures.」 a case-insensitive `grep -E` over `README.md` for the words `perfect`, `oracle`, `upper bound`, `상한` and `noiseless` returns **nothing** at this head. It binds the 42 for the same reason it binds the 91 (`docs/present_perimeter_arm.md` §5 calls it 「a property the 91 has always had, inherited not introduced」; `docs/real_roads_real_hazard.md` hands both arms the same `HazardSequence`; `evacuation.py:270` scores the fire-blind arm against that same field), and the new gate is **green** on it because `_is_caveated` accepts one CONTROL spelling and asks nothing else. This is a newly measured omission on a sentence every earlier lap scored without it, which is this table's own convention for when a score moves down on an old defect. Filed as **WFG-148**. **데이터 수집·분석·해석 18 held:** no model, split, metric, arm or protocol moved, and WFG-139 is unrepaired, though this window produced the cleanest evidence for it anyone has, a cold `1616 / 62` here against the dev lap's warm `1622 / 56` on a tree differing only in prose, six tests in each direction. **연구 목적 17, 설계와 방법론 16, 창의성 15 held:** the manuscript's new Related-work paragraph is the evidence critic #29 already scored, now reaching the paper, and it does not move a row twice. |
| 2026-09-07 | `3f881f6` | 17 | 16 | 18 | 15 | **18** | **84** | **One row moves, 제출 자료 17 to 18, and it is the largest single movement this row has had in a week.** The evidence is measured in one process at this head and not read from the lap that made it: `manifest_20260907T0059Z.json`'s **five** `SOURCES` all hash equal to the tree (`99b2168f4b…`, `b1aae78f35…`, `ec75a1657d…`, `237de4f4ae…`, `84648d4d6e…`), the PDF's own sha256 matches (`a4970b12cdd1…`), `pages_per_source` sums 5+6+17+3+2 = **33** = `pages`, and the file really carries **33** `/Type /Page` objects. That ends the `af955a30fa…` → `7d5ac4c9c5…` → `175da9e50c…` → `5ac45ea810…` drift series, and it puts `docs/submission_reconciliation.md` — the one paper the bank tells the student to open when a judge says 「제출한 숫자와 지금 숫자가 다릅니다」 — on paper in the student's hand for the first time; Q39, which used to script 「대조표는 화면에서 열어 드리겠습니다」, is now true and quotes **no** page count, which is the WFG-117 lesson applied rather than repeated. **It stops at 18 and not 19 on this lap's one finding**, which is on the same row: `release/kcf-finals-2026/MANIFEST.json` lists 17 files and not one is a printable, so the artifact a judge would carry away on a USB does not contain the kit that just got good (WFG-151). **연구 목적 17, 설계와 방법론 16, 데이터 18 and 창의성 15 held**: nothing in this window touched a model, split, metric, arm, coupling or protocol. ⚠ 데이터 in particular does **not** move for WFG-148's ORACLE caveat — a caveat added is honesty about interpretation, not a measurement made, and what the 42 buys against a present-perimeter opponent on 영덕 is still unmeasured and still waiting on NH-032/NH-034 |
| 2026-09-07 | `0fc6130` | 17 | 16 | 18 | 15 | 18 | **84** | **Every row held, and the flat window is correct rather than lazy: the only research object this window touched is one manuscript sentence and a ledger, and these five judges score the research through the artifacts, which did not move.** **연구 목적 17, held.** Nothing in the window touches what the project is for. **설계와 방법론 16, held, and the finding is what keeps it from rising.** The window added two genuinely good gates — `R9_ITEMS` reading a readiness line's own five names into predicates over the committed manifest, and `EXCLUSION_EVIDENCE`, which asserts that an excused item's named path is really in the tree, a slot that had never had an assertion behind it. Against that, the project's own evidence machinery was measured here and is narrower than the manuscript says: `withdrawn_claims.json` → `scope.extensions` is `[".md", ".html"]`, so the artifacts and generators that author judge-facing prose are outside it, and §3.5's sentence 「the scan does read」 the template is true of `scripts/finals.template.html` and false of `scripts/build_printables.py` (WFG-155). A row about 기술적 우수성 does not rise in the same window in which the loop's most-advertised mechanism is shown to have an undeclared boundary. **데이터 수집·분석·해석 18, held.** No experiment ran; nothing was re-analysed. **창의성 15, held.** **제출 자료 18, held on two movements that cancel, and saying so is the point.** Up: the manuscript's §3.5 registration sentence is now about **two** events rather than one, and the paper lap's reviewer knocked down four numbers in that lap's own ledger prose about not letting unchecked claims stand — two page ranges that are not derivable from a manifest whose own `pages` and `pages_per_source` disagreed, a test baseline that dropped an xfail, and 「three days」 for thirteen hours — all four re-derived and corrected **before** the push. That is the discipline this row scores. Down, exactly cancelling it: the same window declared a sentence false, corrected it in three places and registered it in none, and the sentence is live in the provenance file that ships inside the release bundle (WFG-153, NH-042). A 출처 명기 row cannot rise while a provenance record misstates what the repository contains. |
| 2026-09-07 | `2896c9c` | 17 | **17** | 18 | 15 | 18 | **85** | **R7 ticks and the loop's own gate went red on a correct tree in the same measurement, and only one of those two is a research movement.** **연구 목적 17, held.** Nothing this window touched what the project is for. **설계와 방법론 16 to 17, and this is the row WFG-026 was written for.** 「기존의 연구와의 차별점」 is this cell, and until this window the project's answer to 「무엇이 다릅니까」 lived in a knowledge note and one manuscript paragraph. `docs/related_work.md` now gives **16** entries, each with a resolved DOI or permanent URL, what it computes and what it does **not** compute; a §2 that states the two Korean operational systems fairly and concedes NIFoS's 5 m terrain analysis is finer than this project's grid, in their favour; and a §3 panel differentiating the nearest neighbour, ISEF 2026 SFTD059T, on seven axes while conceding what the two share. It declines an accuracy comparison in either direction and gives three binding reasons. **+1 and not +2, for a reason on this same row:** the page's first draft asserted seven present-tense citations to four repository files that do not exist, and claimed all of entries 1 to 12 carried a `verified` note when three did not. The **independent reviewer** found both, not the lap, and both were repaired before the push with `tests/test_related_work_paths.py` shipped behind them. Caught-and-repaired-before-push is the discipline this row scores; caught-by-the-reviewer-rather-than-by-the-author is what holds the second point. **데이터 수집·분석·해석 18, held.** No experiment ran and nothing was re-analysed; WFG-139 is unrepaired for a sixth consecutive lap. **창의성 15, held.** Nothing touched a model, split, metric, arm, coupling or protocol. **제출 자료 18, held on two movements that cancel, and both are named.** Up: **R7 ticks** on evidence re-measured here (six `SOURCES` hashes, the PDF hash, 36 = 5+6+17+3+2+3 against 36 `/Type /Page` objects, the stamp in the 19-file bundle manifest), `WC-006` is registered so CHARTER §3.5c held under its first real test, and the survey's provenance block says **in its own voice** which three of its sixteen entries were *not* re-opened from this sandbox and forbids them entering the manuscript until one is. That last is 「사용된 자료에 대한 출처 명기」 done better than most published work. Down, exactly cancelling it: `docs/related_work.md:104`/`:187` and `docs/auto/finals/RELATED_WORK_PANEL.md:43` date the 사이언스타임즈 article **2026-02-12**, and the page's own byline and 저작권자 line read **2026-02-13** (the `2026/02/12` on that page is the 연합뉴스 wire original it republished, carried in the image CDN paths and the id `AKR20260212072300063`). The repository has paired one publisher with another's date, on a page that **prints inside the kit and ships on the stick**. A 출처 명기 row cannot rise on a lap that put a wrong citation date onto paper a judge holds. WFG-146, filed 09-06 as 「one character」 and parked. |
| 2026-09-07 | `2720840` | 17 | 17 | 18 | 15 | 18 | **85** | **Every Track B row holds, and holding is the right answer rather than a slippage note: nothing in this window touched a model, a split, a metric, an arm, a protocol or the manuscript's results.** **연구 목적 17, 설계와 방법론 17, 창의성 15 held** — the window's substantive commit (`3baa564`) is a test and a docstring. **데이터 수집·분석·해석 18 held, and the hold is re-earned rather than inherited.** A Track B judge reads this row as 「연구 재현 가능성」, and I measured the thing that scores it rather than reading the last lap's number: at container start `data/raw/` held only `.gitkeep` and `README.md` (both mtime 2026-09-04T16:57:27Z); bootstrap finished 10:59:40Z; `pytest-full` ran 260.6 s; and afterwards `data/raw/dem/srtm/N36E129.hgt` (**25,934,402 B**) and its `.gz` carry mtime **11:02:33Z**. The suite fetched 25.9 MB over the network in the middle of the gate run — the **seventh** consecutive critic lap to find WFG-139 unrepaired and the first to catch the download by its own clock rather than by a pass/skip delta. It does not fall below 18 because this is the same defect already priced in at `e95fe28`, not a new one; it cannot rise while it stands. **제출 자료 18 held.** The window's judge-facing gain is a current build stamp on the finals screen, which is a 완성도 matter and belongs to Track A's 구현 row, not here. What stops 19 on this row is 출처 명기, a named criterion: `docs/related_work.md:104`/`:187` and `docs/auto/finals/RELATED_WORK_PANEL.md:43` date the 사이언스타임즈 NIFoS article 2026-02-12, and I fetched the page in this sandbox — its byline reads `연합뉴스 2026-02-13` and its 저작권자 line `2026-02-13 ⓒ ScienceTimes`, twice, while the only `2026/02/12` strings on it are 연합뉴스 image CDN paths carrying the wire id `AKR20260212072300063`. Third critic lap; WFG-146, and this lap's one `fix-before-next-row` item. **Not scored here:** the two Pass/Fail rows — 서류 is the author's under the NH-008 closure, 위험성 is not at risk, the delivery layer stays dry-run. |
| 2026-09-07 | `b54ca28` | 17 | **18** | 18 | 15 | 18 | **86** | **One row moves on BOTH tracks, 설계와 방법론 17 to 18, and it is the row that scores 「기존의 연구와의 차별점」.** Critic #33 gave this row its previous point when `docs/auto/finals/RELATED_WORK_PANEL.md` shipped, and both #33 and #34 then wrote that a panel a student cannot speak from is worse at a booth than either half alone, because the printed page invites the question. That asymmetry closed this window: `docs/auto/JUDGE_QA.md:637-694` holds **Q16a · T0**, and the finals format is five separate 1:1 interviews, so the differentiator is now on the surface a judge actually reaches rather than only on the one they glance at. I read the card rather than grepping for it: it answers on the **output object** and refuses the accuracy comparison in either direction, refuses 「저쪽은 못 합니다」 in favour of 「공개된 자료에서는 확인되지 않습니다」, and refuses any 경상북도·영덕 coverage claim. **제출 자료 held on two moves that cancel, and the second is this lap's one item.** **+1**: WFG-146's citation pairing 「사이언스타임즈 2026-02-13 (연합뉴스 2026-02-12 전재)」 landed in seven live sites with dated notes and the kit was rebuilt in the same lap, and 출처 명기 is a named criterion on this row of both tables; I re-fetched the article myself as a fourth independent check. **-1**: `RELATED_WORK_PANEL.md:40`, 3 of the 38 printed pages, asserts 「앞의 두 시스템은 재현 방법을 공개하지 않습니다」 about documents nobody has opened (NH-039), where the survey it summarises writes 「not stated」 at `docs/related_work.md:134` and the same lap wrote the narrowed form into two other files. A page that promises in its own header to add no new claims, adding one, on paper, is exactly what this row docks. **WFG-162.** **데이터 수집·분석·해석 held:** nothing in this window touched a model, a split, a metric, an arm, a coupling or a protocol, and WFG-139 is unrepaired for an eighth measurement. **연구 목적 / 개발 목적 and 창의성 held:** no method moved. **Track A 구현 및 유용성 holds at 19** on WFG-110, unchanged for a fourth lap and still R1's only sized blocker. |
| 2026-09-07 | `7cc4eb7` | 17 | 18 | 18 | 15 | 18 | **86** | **Every row holds, and holding is the argument rather than the absence of one.** **제출 자료 18, held on two moves that cancel, and the second is this lap's one item.** **+1**: `docs/finals_screen_numbers.md` is 출처 명기 for the judged screen in the direction a judge asks it — all **28** registry keys `scripts/finals.template.html` references, mapped to the **8** cards that draw them, with view and kicker, writing no registry VALUE so the same number cannot acquire a second home; 7 tests, mutation-graded 9 of 9 red. I re-derived it rather than reading it: `scripts/finals_screen_keys.py` returns 28, all 28 are in the doc, all 28 are keys of `docs/NUMBERS.json`. Add to that `WC-007` and `WC-008` registered in the same day and `docs/related_work.md`'s four dead paths kept as a **gated record** (`test_the_paths_named_as_dead_are_still_dead`), which is the correct shape for CHARTER §3.7. **-1**: `docs/auto/JUDGE_QA.md:650-652`, inside the very card this table gave a point to four hours ago, still has the student say aloud that G-DAPS 「특정한 집의 어느 길이 위험에 들어가고 어느 길이 들어가지 않는지는 말해 주지 않습니다」, twenty lines above the same card's ❌ 「저쪽은 가구 단위로는 못 합니다」라고 단정하지 마십시오. It is the Korean half of `WC-008`, registered ninety minutes earlier in English, and it is printed on 17 of the kit's 38 pages. A 제출 자료 row does not rise while the document the student recites from tells them to say the thing it forbids on the next page — this table's own precedent from critic #8's F42 and critic #27's. **WFG-166.** **설계와 방법론 18, held.** The differentiation work is genuinely better than it was: the printed panel now states its own reading scope, and `RELATED_WORK_PANEL.md` no longer asserts what NIFoS's unread manual does not contain. Nothing in the window touched a variable definition, a control or a research design. **데이터 수집·분석·해석 18, held.** Nothing touched a model, a split, a metric, an arm, a coupling or a protocol, and **WFG-139 is unrepaired for a ninth measurement** — taken on this lap's own clock, `N36E129.hgt` written at 17:02:33Z inside a run that began about 17:00Z. 연구의 재현 가능성 is a named criterion on this row and a suite that downloads 25.9 MB of terrain to pass is not it. **연구 목적 17 and 창의성 15, held**; no method moved. **Not scored:** the two Pass/Fail rows — 서류 is the author's under the NH-008 closure, 위험성 is not at risk, the delivery layer stays dry-run. |
| 2026-09-07 | `64f015b` | 17 | 18 | 18 | 15 | 18 | **86** | **Every Track B row holds, and the hold on 제출 자료 is two real movements that cancel rather than a quiet window.** **연구 목적 17, 설계와 방법론 18, 창의성 15, held:** nothing in this window touched a model, a split, a metric, an arm, a coupling or a protocol; the two rows that shipped are a Q&A correction and a browser driver. **데이터 수집·분석·해석 18, held, and re-earned rather than inherited.** `gates.py --mode full` exits **0**, ALL GREEN at this head on the routine's default clone (`is-shallow-repository` = `true`, `rev-list --count HEAD` = **50**): `1682 passed, 62 skipped, 2 xfailed`, 289.3 s, **COLD**. It does not rise because **WFG-139 is unrepaired for a TENTH measurement and this one is on my own clock**: `data/raw/` held `.gitkeep` and `README.md` at container start and holds `dem/srtm/N36E129.hgt` (25,934,402 B) with mtime **20:02Z** afterwards, inside a `pytest-full` stage that ran 19:59:37Z to 20:04:26Z. A research row does not reach 19 while the suite that certifies its numbers fetches 25.9 MB over the network mid-run and six terrain tests have never executed in CI. It does not fall, because nothing regressed and the reproducibility story got materially better in a way this row can see: `scripts/check_finals_acts.py` and `scripts/cdp_min.py` make the delivered artifact's behaviour **measurable by a stranger with no dependency install**, which is exactly what 「연구 재현 가능성」 asks, and the same driver ran identically on GitHub's clean `ubuntu-latest` runner (run **219**, job `finals-acts`). **제출 자료 18, held on two moves that cancel.** **+1**: WFG-166 closed properly — the flat negative about G-DAPS is gone from `JUDGE_QA.md:650-652` and from the printed panel, `WC-008` gained its Korean spelling in the same lap, the kit was rebuilt at `20260907T1825Z` (38 pages) and the manifest re-pointed and verified byte-identical; I read the replacement text on both files rather than grepping for the old one. **-1**: four lines above that repair, on the same printed panel and in the same recited card, `RELATED_WORK_PANEL.md:32` and `JUDGE_QA.md:652` assert in bold that the NIFoS console's 발화점은 「운영자가 손으로 입력」한다 — a positive factual claim about another agency's system whose entire provenance is a catalogue chapter title on an 18 MB PDF nobody has opened (NH-039), and which `references.bib:258`, `manuscript.md:111-112` and `KOREAN_OPERATIONAL_SYSTEMS.md` §1 all state **with** its attribution. 출처 명기 is a named criterion on this row, and a page that drops the attribution its own sources carry is the defect this row scores. **WFG-171.** **Not scored here:** the two Pass/Fail rows — 서류 is the author's under the NH-008 closure, 위험성 is not at risk, the delivery layer stays dry-run. |
| 2026-09-08 | `47e48b5` | 17 | 18 | 19 | 15 | **18** | **87** | **One row moves, 제출 자료 17 to 18, and it is the point critic #39 docked one lap earlier for a defect that is now closed and verified by a second channel.** **제출 자료 17 to 18.** `pytest -rs` over the whole suite here prints 11 SRTM-named skips, 7 of them tile-gated and 4 gating on `data/raw/firms_data/yeongdeok_2025_dem.tif`; Q28, Q40 and `docs/clean_clone_gates.md` all state those three integers and `tests/test_tile_gated_skip_count.py` re-derives them from the `skipif` decorators with `ast`, binding twelve sentences including both spoken lines and failing on a regex that matches nothing. That is 출처 명기 turned on the project's own suite, which is the one class of number this repository had never registered. Beside it, the paper lap replaced catalogue-record provenance with 12 opened abstracts, marked 4 as catalogue-only, and narrowed 3 sentences to their titles. **Why not 19:** `paper/check_paper.py` here reads `body_words 8994` against a 9,000 hard fail and `pages: null`, so the manuscript is six words from parking under a proxy that CI cannot cross-check against the 25-page rule the author set (NH-037, WFG-116). **연구 목적 17 held** — nothing changed what the research is for. **설계와 방법론 18 held** — the reference narrowing removed overreach rather than sharpening the differentiator. **데이터 수집·분석·해석 19 held** — no model, split, metric, arm or protocol moved; `git diff --name-only 0fc6130..HEAD` returns nothing under `src/`, `data/` or `docs/NUMBERS.json` across the 49 commits this clone can read, and I do not claim anything about commits older than its boundary. **창의성 15 held, the 40th consecutive 15**, and the flatness is the finding: the total has gone 72 to 87 without this row moving once (WFG-182). |
| 2026-09-08 | `9329400` | 17 | 18 | 19 | 15 | 18 | **87** | **No Track B row moves, and that is the reading rather than an absence of work.** The window shipped two T0 judge cards and a 13-test gate, all of it about what the product **is not**; nothing touched a model, split, metric, arm, coupling or protocol, so 설계와 방법론 holds at 18. **데이터 수집·분석·해석 holds at 19.** Q20a is a real provenance statement, and an unflattering one: it says in the project's own voice that the elderly signal the router uses is a single assumed `immobile_fraction` of 0.3, that the county-level elderly score is a **placeholder not yet anchored to authoritative sources**, and therefore that the KOSIS statistics are not ingested at all. I re-derived all three from `data/processed/rescue_routing.json` and `src/wildfireguardian/utils/vulnerability.py` rather than reading the card. But a card that states existing provenance is not new data work, and this row scores 체계성 and 재현 가능성. **제출 자료 holds at 18**: two T0 cards, a kit rebuilt to 40 pages and the bundle re-pointed in the same lap are a real gain, and against it `JUDGE_QA.md:715`'s 「커밋된 33장」 is an 출처 명기 defect of the same class critic #39 docked (F1, WFG-185), on a printed page. Gain and deduction cancel; I would rather hold than ratchet. **연구 목적 17 and 창의성 15 hold**; the 창의성 hold is now 41 consecutive rows and this lap measured the reason (0 matches for 창의/독창 on all three judge-facing surfaces, WFG-182). |
| 2026-09-08 | `ceb43ba` | 17 | 18 | 19 | **16** | **19** | **89** | **Two rows move, and one of them has not moved since the series began.** **창의성 15 to 16, the first movement in 42 rows.** Nothing about the project's actual creativity changed this window and I am not scoring it as if it had: no experiment, model, split, arm, coupling or region was added, and CHARTER §3.4 bars adding one. What changed is that the claim became **sayable**. This row is scored in a 5-minute booth interview under a 심사기준 whose first line is 「단순 암기 발표 지양; 창의성, 과학적 원리, 과학적 사고 중점」, so the instrument is the interview, and until `5845953` the student had no answer to 「무엇을 새로 만들었습니까」 anywhere: `grep -ncE '창의|독창'` returned **0** on all three judge-facing surfaces (critic #41's measurement, re-run here and now **2** on `docs/auto/JUDGE_QA.md`). Q29a answers it in three items, each pointing at a committed file, and I re-derived item 2 rather than reading it — `docs/real_roads_real_hazard.md`'s first table does carry the three cells, and its third is the run with a real OSM walk graph and a real `spread_v2` forward-sim over 459 origins. **Why 16 and not 17**, pre-registered so the next critic can hold me to it: two of the three surfaces are still silent (`grep -cE '창의|독창'` returns 0 on `web/finals.html` and 0 on `docs/auto/DEMO_SCRIPT_5MIN.md`, which the card's own 없는 것 block states), and the card deliberately makes no novelty claim, so a judge hears what was built and not why it is new against the literature. The next point needs the screen or the script to carry it, or a contribution that is new rather than newly legible. **제출 자료 18 to 19, and it is critic #41's own pre-registration returned.** That lap wrote 「two T0 cards, a kit rebuilt ... are a real gain, and against it `JUDGE_QA.md:715`'s 「커밋된 33장」 is an 출처 명기 defect ... Gain and deduction cancel」. **The deduction is closed** (WFG-185): the spoken sentence now names the run directory inside the sentence carrying the count and adds the student's own denial that 33 is the tree's number, the assertion moved to a sentence-scoped binding, and the kit was rebuilt to `WFG_printables_20260908T0939Z.pdf` (41 pages) with the bundle manifest re-pointed in the same lap. The gain stands alone. **연구 목적 17, 설계와 방법론 18 and 데이터 수집·분석·해석 19 hold**: nothing this window touched a model, split, metric, arm, coupling or protocol, and a card that assembles existing provenance is not new data work. ⚠ **The deduction I am NOT taking, recorded so it is not a surprise next lap:** `web/finals.html` is 25 commits behind a limit of 30 (WFG-187). It costs nothing today because the screen still opens and every number still maps; it costs 제출 자료 and 구현 및 유용성 the moment the stamp ages out |
| 2026-09-08 | `dee1bc1` | **18** | 18 | 19 | 16 | 19 | **90** | **One row moves, 연구 목적 17 to 18, and it is the first time the front door states the research purpose in either language.** **연구 목적 17 to 18.** This row scores 「명확하고 집중된 목적성」 and 「과학적으로 검증 가능한 접근 방법 설정」. Before this window a judge who opened `README.md` met a TL;DR and two round sections; at `dee1bc1` they meet `:200` 「Round 4 (2026-09 — 본선 준비)」, whose own reading note at `:207-208` says 「이 절에서 이 프로젝트에 유리한 항목은 4번뿐입니다」, and `:596` 「Abstract (draft)」, which states the question, the design, the operating point (pooled cell recall **0.138**, three of six held-out fires with no true positive) and names the transferable contribution as the **evaluation design** rather than the model. That is the row's text, in the row's own words, on the surface a judge reaches first. **R8 ticked** on the same evidence. **설계와 방법론 18, held:** the present-perimeter arm is the differentiator and it did not change this window; what changed is how it is described, which is the next cell. **데이터 수집·분석·해석 19, held, and the hold is a deduction that cancels a rise.** The window added no experiment and re-derived nothing; against that, WFG-190(b) is a live 논리적 해석 defect: `README.md:220-225` states the present-perimeter result with no mention that its buffer width is a free parameter, where `paper/manuscript.md:493` says 「The buffer width is a second free parameter and nothing in the data chooses it」 and `docs/present_perimeter_arm.md:116` gives that a section heading saying it 「matters more than the 9」. **창의성 16, held:** nothing this window touched the creative claim; `docs/creativity_card.md` and Q29a are critic #42's point and are not scored twice. **제출 자료 19, held, and this is the row the window's work would otherwise have raised.** The Round-4 section and the abstract are real 제출 자료 gains and they are exactly where WFG-190(a) sits: `README.md:235-239` claims the margin values are in no 심사용 자료, while `docs/auto/JUDGE_QA.md:849-852` carries 9, 27 and 5 and `docs/auto/finals/printables/manifest_20260908T0939Z.json` binds that file into the 41-page kit as 「심사위원 질의응답 카드」. 자료의 논리적 구성 does not rise on a page that contradicts another page in the same kit. |
| 2026-09-08 | `0cca093` | 18 | 18 | 19 | 16 | 19 | **90** | **One row moves, Track A 개발 목적 18 to 19, on the adoption card — and 제출 자료 is HELD in both tracks rather than raised or docked, which is the deliberate half of this scoring.** **A 개발 목적 18 to 19.** That row's own rubric text is 「작품 작동을 위한 제약에 대한 이해」, and the new card (`docs/auto/JUDGE_QA.md`, the 46th; the bank's header 46 = T0 19 + T1 20 + T2 7 sums by `grep -cE '^\*\*Q[0-9]+[a-z]? · T[0-9]'`) is that sentence answered against the judge who would have to run it: it refuses to give a cost 「저희는 어느 기관에서도 이걸 운영해 본 적이 없고, 도입 비용을 추정해 본 적도 없습니다」, and its 「없는 것」 block leads with 「비용 수치가 하나도 없습니다」, names N = 1 on the firefighter consultation, and separates 「running this once in that county」 from 「running it in an operations room」. `tests/test_adoption_card.py` (327 lines) binds it and the kit was rebuilt in the same lap (`WFG_printables_20260908T1529Z.pdf`, `release/kcf-finals-2026/MANIFEST.json` re-pointed). **제출 자료 HELD at 19 (B) and 17 (A), and the hold is the finding.** Up this window: `README.md:220-239` stopped stating the universal critic #43 filed (「어느 심사용 자료에도」 is now 「부스에서 말하지 않습니다」) and the Round-4 section now names the buffer as a free parameter and points at `docs/present_perimeter_arm.md` §4. Down this window, on the same paragraph: `README.md:232` now asserts 「고원이 아니라 뾰족한 봉우리」 — the shape `docs/auto/JUDGE_QA.md:1394` and `:920` say the five-point sweep **cannot resolve**, and which `docs/auto/DEMO_SCRIPT_5MIN.md:151` has the student disclaim out loud. The two cancel; raising the row on a section that gained one defect while losing two would be scoring the diff rather than the state, and docking it would double-count a finding filed at position 1 (**WFG-127**, fourth surface). **B 설계와 방법론 18, held for the same reason and not a different one:** the sweep's resolution is a methodology fact, the repository states it correctly on three surfaces and incorrectly on the fourth, and 18 already reflects the three. **데이터 수집·분석·해석 19 (B) / 구현 및 유용성 20 (A) held: nothing was measured this window.** The paper lap wrote a correction, its reviewer blocked it, and it was reverted rather than shipped — `paper/manuscript.md` is byte-identical to lap 18's (`6ecc386`). That is restraint and it is scored as a hold, not a loss. **창의성 16 held in both tracks:** WFG-182 closed on the bank and the count is still **0** on `web/finals.html` and **0** on `docs/auto/DEMO_SCRIPT_5MIN.md` (WFG-194). ⚠ **No row is docked for `auto-gates` run 253 being red** (WFG-193): the gates inside it passed, and a broken artifact upload that stopped `Main` following is loop machinery a judge never opens. |
| 2026-09-08 | `9c24a8b` | 18 | 18 | 19 | 16 | 19 | **90** | **critic #45.** **no row moves on either track — the first all-hold window since `2720840` on 2026-09-07, and it is the honest reading of a three-hour window that contained two CI-repair laps and one research lap and touched no judge-facing surface.** The window's real work was real: `Main` follows again, two red `auto-gates` runs are closed, the browser gate's skip was verified rather than asserted, the manuscript's related-work section narrowed four overclaims about papers it cites, and DIRECTION.md went from 54,923 bytes to one screen with the old page archived verbatim. **None of it is a thing five judges see in ten minutes.** **B 데이터 수집·분석·해석 19 and A 구현 및 유용성 20 held: nothing was measured.** Pre-registered so the next lap can hold me to it — B 데이터 goes to **20** when WFG-125 reports a number from the out-of-fold field this lap found committed (`data/processed/spread_v2_lofo_oof_cells.csv.gz`), or when it discharges its written branch by naming that file and saying precisely why it cannot be used. **창의성 16 held on both tracks**, and the reading got worse rather than better: `docs/creativity_card.md` exists and is in neither the printed kit nor any of the three surfaces a judge stands in front of (WFG-194). It is not docked, because that describes the same state critic #44 scored and not a regression; it moves to 17 when 창의성 reaches `web/finals.html` or `DEMO_SCRIPT_5MIN.md`. **설계와 방법론 18 held on two moves that cancel:** up, the manuscript stopped overclaiming what @cova2005, @li2017, @li2019, @kim2021gk2a, @dozier1981 and @studenski2011 say, which is this row's 「기존의 연구와의 차별점」 criterion made accurate rather than merely present; down, the same row's 「일정」 bullet is at **zero** on every judge-facing surface (WFG-027, F3). **제출 자료 19 (B) / 17 (A) held:** `README.md:232`'s 「고원이 아니라 뾰족한 봉우리」 is unrepaired for a second window, and nothing reached the kit or the screen. **연구 목적 18 / 개발 목적 19 held.** ⚠ **No row is docked for `auto-gates` run 255 being red:** it was a browser that never started, on a runner, and a judge never opens CI. |
| 2026-09-08 | `cb9fcc3` | 18 | **19** | 19 | 16 | 19 | **91** | **One row moves, 설계와 방법론 18 to 19, and it is earned by a control rather than by a result.** ⚠ **Correction first:** I scored this window once at `7cfe29a` as an all-hold, on the reading that the 21:17Z lap had claimed two P0 rows and died. The lap was **slow, not dead**; it pushed `9170a37` at about 23:20Z and closed both. Nothing was pushed carrying that scoring. **설계와 방법론 18 to 19.** This row scores 「변수의 정의와 제어의 적절성」 and 「문제해결 방법과 및 연구 설계 적합성」, and the WFG-127 run is the cleanest instance of both in this repository's history. `scripts/run_present_perimeter_arm.py` gained `--sweep-extra-m`, which **adds** widths and defaults to empty, so the committed five-point grid is unchanged when the flag is absent; the output went to a **new** file, so `present_perimeter_arm_uiseong_andong_2025.json` still holds exactly five widths and a test asserts it; the runner **refuses to write unless it first reproduces the committed canonical arm**, which it did in 111 s; and all five shared widths come out **cell for cell** identical across six columns, bound by `tests/test_buffer_shape.py::test_the_five_shared_widths_reproduce_cell_for_cell`. Three new points are only worth reading beside five old ones if the five land in the same place, and this lap proved that rather than assuming it. **데이터 수집·분석·해석 19, held, and the decline is deliberate.** The measurement is good and it runs against the project (750 m scores **349** against 1 km's **345**, so the fair opponent is stronger than the committed artifact reports). But critic #45 pre-registered the 20 for **WFG-125**, and the new document's own §4 says why that is still the right bar: 「every margin here is what a **noiseless** forecast would buy over a present-perimeter policy — an upper bound, not what this project's own model buys. That difference is still unmeasured」. 「예상 결과에 대한 논리적인 해석 및 결론」 does not reach 20 while the headline comparison measures a bound the model does not reach. **제출 자료 19, held on two moves that cancel.** Up: a dated withdrawal note on the README in the student's own register, WC-011 registered in the same commit across both languages plus the recited booth sentence, a fifth surface found by the registration sweep rather than by memory (`paper/GAPS.md` G8), and a new document whose §4 「What this does NOT show」 is four paragraphs long and leads with 「it is one fire, one region, one hazard realisation」. Down: **the reported margin is a maximum over a post-hoc grid and can only shrink as anyone looks harder** — 9 origins to 5 on the first refinement ever run — and no surface states that property. **WFG-201.** **연구 목적 18 and 창의성 16, held**; no method moved and 창의성 is still 0 on the screen and 0 on the script. |
| 2026-09-09 | `86f8929` | 18 | 19 | **20** | 16 | 19 | **92** | **One row moves, and only on Track B: 데이터 수집·분석·해석 19 to 20.** Evidence, all at `86f8929`: `9170a37` and `d78d39b` between them measured the fair opponent at three widths the grid was missing, found the result went AGAINST this project (the opponent scores 349 at 750 m against 345 at 1 km, so its best width was never measured before), registered the retired shape claim as **WC-011** across five surfaces, and then wrote the **property** onto six judge-facing surfaces: the width is the argmax read after the run, so the opponent's score is a maximum over the measured grid and the forecast's margin over it is non-increasing as widths are added. `tests/test_post_hoc_maximum.py` grades that against mutations, scopes the ban to the section a judge reads rather than the file (the WC-004 failure mode), computes the property from the artifacts rather than quoting a document, and names its own limit in its docstring: it grades a FORM, not a meaning, which is mandela leakage #4 and it says so. Stating that your own headline statistic is a post-hoc maximum, before a judge finds it, is 「수학 및 통계 방법의 적절한 적용」 and 「예상 결과에 대한 논리적인 해석」 at the top of that row's band. **What I am NOT paying, and why:** 창의성 holds at 16 on both tracks — `docs/creativity_card.md` was written and is referenced by nothing under `release/` or `scripts/`, so a count of 창의 or 독창 is still **0** on `web/finals.html` and **0** on `DEMO_SCRIPT_5MIN.md`, and a card a judge never receives cannot score a 20-point row. 제출 자료 holds: the kit's six SOURCES are unchanged in composition and all six re-hash. |
| 2026-09-09 | `5f4e32b` | 18 | 19 | 20 | 16 | 19 | **92** | **No row moves.** 연구 목적 18, 설계와 방법론 19, 데이터 수집·분석·해석 20, 창의성 16, 제출 자료 19, all held. The window contains one paper lap and three report-header repairs. **데이터 수집·분석·해석 20 is held rather than re-earned:** `4b0010a` retired a count in §4.5 that its own incorporated diff had already falsified and put the post-hoc-maximum statement in the manuscript, which is the same discipline that bought the 20 one lap earlier, on a smaller surface. **설계와 방법론 19 is held rather than docked** although this lap measured 일정, a named sub-item of that criterion, at literal zero on every judge-facing surface: that zero is not new (critics #45 and #47 both measured it) and the 19 was set with it in place, so docking now would be scoring the same defect twice. It is instead the reason WFG-027 is P0 and moved. **창의성 16 is held** for the same reason, with one new fact recorded on WFG-194: the README is at zero too.  ⚠ **WFG-194 landed under this lap at `b451376`, after the window closed**, and put 창의성 onto `web/finals.html`, `docs/auto/DEMO_SCRIPT_5MIN.md` and the printed kit. **That is critic #49's window to score, not this one's**, and it is named here so no later lap reads this hold as having seen it. |
| 2026-09-09 | `7f914fd` | 18 | 19 | 20 | **17** | 19 | **93** | **One row moves, 창의성 16 to 17, and it is paid on the pre-registration rather than on this lap's judgement.** Critic #45 wrote here, in this table, 「it moves to 17 when 창의성 reaches `web/finals.html` or `DEMO_SCRIPT_5MIN.md`」, and critic #46, #47 and #48 each held it at 16 on the measured zero. **In this window it reached all four surfaces and I counted rather than read the reports:** a raw count of 창의 or 독창 answers **3** on `web/finals.html`, **10** on `docs/auto/DEMO_SCRIPT_5MIN.md`, **2** on `README.md`, **3** on `docs/auto/JUDGE_QA.md` and **27** on `docs/creativity_card.md`, where the first three answered 0, 0 and 0 one window ago; the card is now the **7th** `SOURCES` entry of the printed kit (`WFG_printables_20260909T0700Z.pdf`, `release/kcf-finals-2026/MANIFEST.json` re-pointed in the same lap). **What earns the point is the register, not the word count.** The block on every surface says what was built, names the file behind it, states no quantity, makes no novelty claim and ends by handing the verdict to the judge, and `tests/test_creativity_card.py` holds those four properties against ten mutations from the lap and eight more from an independent reviewer who had not seen them — its mutation D, a backticked label rewritten to a different real path while the link target stayed correct, passed all eighteen assertions and is why a nineteenth exists. On a 20-point row whose own text is 「프로젝트 진행에 있어 뛰어난 창의성」, an answer that refuses to grade itself and can be checked line by line is worth the point. **Why 17 and not 18, and this is the deduction pre-registered for critic #50:** item ①, the one that claims the output object is the contribution, anchors on `docs/auto/knowledge/KOREAN_OPERATIONAL_SYSTEMS.md` on three of the four surfaces — the document `tests/test_creativity_card.py:86` spends four graded mutations keeping out of the spoken answer, carrying **34** mentions of those systems and **12** of their announced percentages — and **no surface gives a path to an instance of the object it names** (33 `dispatch_a4.html` under `outputs/dispatch/`, 113 under `outputs/dispatch_full/`). **WFG-210.** **제출 자료 19, HELD rather than raised.** Up: WFG-208 closed, so the USB a judge carries away now names the repository (`github.com` in `release/kcf-finals-2026/README_KO.md`, 0 before, 1 now) and the kit was rebuilt with its manifest re-pointed in the same lap. Down: the same window put the WFG-210 anchor defect onto the front door, which is a 출처 명기 fault on the page this row is most about. The two cancel, and raising a row on a section I filed a finding against in the same hour is scoring the diff rather than the state. **연구 목적 18, 설계와 방법론 19, 데이터 수집·분석·해석 20, all held: nothing in this window touched a model, a split, a metric, an arm or a protocol.** ⚠ **WFG-125 is unrun for a third consecutive critic lap** and the 데이터 row's 20 is not in danger from that — it is already at 20 — which is precisely why nothing in the scoring pushes that row to the front. That is NH-038's question and it is written there. |
| 2026-09-09 | `9c22ff3` | 18 | 19 | 20 | **18** | 19 | **94** | **One row moves on both tracks, 창의성 17 to 18, and it is the raise critic #49 pre-registered here in this table, paid on evidence I verified in the tree rather than read from the lap's report.** Critic #49 wrote 「Why 17 and not 18, and this is the deduction pre-registered for critic #50: item ①, the one that claims the output object is the contribution, anchors on `docs/auto/knowledge/KOREAN_OPERATIONAL_SYSTEMS.md` on three of the four surfaces ... and no surface gives a path to an instance of the object it names」. **That defect is closed on all four surfaces and every part of the closure is checkable:** `web/finals.html`'s `CREATIVE[0].doc` now reads `outputs/dispatch/20260801T163042Z/01-거무역리공원-북쪽/dispatch_a4.html · outputs/dispatch/README.md · docs/auto/knowledge/KOREAN_OPERATIONAL_SYSTEMS.md §3`, instance first and the landscape note last with its role written into the visible text; `docs/creativity_card.md:36`, `README.md:326-355` and `docs/auto/JUDGE_QA.md` Q29a each do the same. The named sheet is tracked (`git ls-files` answers it) and **every markdown link target in `README.md:200-362` resolves in the tree**, checked here one by one. The gate binds the anchors' **contents** rather than their names, and the lap reported its own M8 mutation green — a threshold no document reaches, which made the assertion vacuously true — and closed it in both directions. On a 20-point row an answer whose every claim now hands the judge the artifact behind it, and whose gate can fail, is worth the point. **제출 자료 HELD and the hold carries this lap's root objection.** Up, and it is real: `factchk` on every new world claim in the window found **nothing** — Opanasopit & Louis (*Frontiers in Built Environment* 12, 2026-08-06), Chen et al. <!-- forbidden-ok: Chen --> (*PLOS ONE*, 2026-09-01) and 헤럴드경제 (2026-02-12) were each opened here and each confirmed the repository's wording, down to 70 isolated communities, 38,100 → 11,900 residents, R² 0.9251, 0.57–0.77 and the verbatim 「화선 도달 8시간 전 … 5시간 전에는」. 출처 명기 is at the top of its band. Down, on the other named criterion of the same row: 「자료의 논리적 구성」 on the front door. `README.md:200-362` is 163 lines carrying **11** ⚠ markers and **28** negative-framing tokens against **one** affirmative-outcome token, and that one says the model-free opponent scored better than the repository had recorded. The section documents what Round 4 attacked and never states what the project still claims. **WFG-212.** The two cancel. **연구 목적 / 개발 목적 and 설계와 방법론 held: nothing this window touched a model, a split, a metric, an arm, a protocol or the purpose statement**, and 일정 is still at literal zero on all four judge surfaces (re-measured: 0 / 0 / 0 / 1, the one being the adjective 일정한), which was already priced into the 19. ⚠ **WFG-125 is unrun for a FOURTH consecutive critic lap and this lap promoted it to the first `todo` position**, which is the only lever a critic has; the 데이터 row is already at 20, so nothing in the scoring pushes it forward, and that is NH-038's question. **데이터 수집·분석·해석 20, held at the ceiling; nothing was measured this window.** |
| 2026-09-09 | `8506a2d` | 18 | 19 | 20 | 18 | **18** | **93** | **One row moves and it is a DEDUCTION: 제출 자료 19 to 18, on 「자료의 논리적 구성」, the row's own first named criterion.** **What I measured, at this head, not read from a report.** `docs/oracle_gap.md` shipped at 12:51Z and establishes in §2 that the forecast-aware arm does **not** plan on truth: it plans on `haz_stack`, a leave-one-fire-out forward simulation, and the oracle is that the **grader** treats that same array as truth. Its §5 says the consequence in the student's voice: 42 is 「자기 예측을 그대로 믿었을 때의 값」 rather than 「완벽한 예보의 값」. **Five surfaces still say the other thing:** `README.md:36` 「what a *noiseless* forecast would buy」, `README.md:266-267` 「잡음 없는 완벽한 예보가 사 줄 값의 「상한」」, `README.md:702` 「an upper bound」, `paper/manuscript.md:512` 「what a *noiseless* forecast is worth」, and `docs/auto/JUDGE_QA.md:1399`, which is **printed in the booth kit** (7 hashed `SOURCES`) and tells the student to say 「그 뒤가 비어 있습니다」 about a question that now has a file behind it. Before this window the five surfaces agreed with each other; after it the submitted material disagrees with itself about what a judged headline number means, and none of the five links to the document that corrects them. **That is a regression in 논리적 구성 measured inside the window, not an inherited state**, and it is the second live defect on this row beside WFG-212, which critic #50 filed and which is untouched. WFG-214, NH-053, and the `fix-before-next-row` item. **The raise I am NOT giving, and why, so the next lap can hold me to it.** This window contains the best analysis of the sprint: the lap inverted a premise five laps had assumed, opened the artifact instead of re-costing it, took its reviewer's block on a false independence claim without arguing, and registered the per-slice time gaps as keys so a stranger can check it. Critic #45 pre-registered exactly this as the raise for **데이터 수집·분석·해석**, and that row is **already at 20**, so the work has nowhere to land. **데이터 20 is HELD rather than docked**, and the deduction is pre-registered instead: `docs/oracle_gap.md:107-110` reads the size-ratio series 0.74 / 1.02 / 1.05 / 1.05 as model behaviour across the horizon (「at 3 h the simulation is 26 % under」), but the artifact shows `t=180`, `t=360` and `t=540` are all graded against the **same** observation at 333 min, with gaps of 153, 27 and 207 minutes, so the observed denominator is 937 cells held constant and the 3 h under-prediction is substantially the matching gap. The artifact is honest (every gap is a registered key); the prose over-reads it. **If WFG-215 is still open at critic #52, 데이터 goes to 19.** **연구 목적 18 and 설계와 방법론 19 held, on two moves that cancel on the second row:** up, 「변수의 정의와 제어의 적절성」 gained a real insight, the discovery that the oracle sits in the grading step and not the planning step, which changes what the whole arm comparison means; down, that insight reached no surface that states the design, and 일정 is still at literal zero (re-measured 0 / 0 / 0 / 1, the one being the adjective 일정한). **창의성 18 held: nothing this window touched 창의성.** ⚠ **No row is docked for CI:** the three `failure` runs in the window (253, 255, 260) are all inside critic #50's window, all closed by `wfg-autoloop-ci-red` within the hour, and runs 286 to 289 since critic #50 are all `success`, with 289 green at this exact head. |
| 2026-09-09 | `375be25` | 18 | 19 | 20 | 18 | 18 | **93** | **No row moves, and the honest reading is that one of them is now correctly scored at a number it was over-scored at yesterday.** **설계와 방법론 19, HELD, and re-justified rather than inherited.** This row has five sub-items on Track B and 「일정 및 팀원(개인의 경우 제외) 역할 배분의 타당성」 is one of them. Critic #51 wrote that it read literal **0** on all four judge-facing surfaces for a fifth consecutive lap and scored the row 19 anyway; that 19 was carrying a named sub-item at zero. This window closed it: `docs/auto/finals/TIMELINE_ROLES.md` with `scripts/build_timeline_roles.py`, `data/processed/timeline_roles/timeline_roles.json`, 20 registry keys, 10 tests of which 9 are graded by mutation, and `commits_outside_phases` = 0 as a partition proof rather than a selection. 일정 now counts 4 on `docs/auto/JUDGE_QA.md` and 4 on `docs/auto/DEMO_SCRIPT_5MIN.md`. **So why not 20?** Because the document's own §0 says 「각 구간의 경계는 제가 정한 것이 아니라 커밋 기록에 실제로 비어 있는 달력 간격입니다」 and `scripts/build_timeline_roles.py:40-84` hard-codes all five boundaries, splitting on a one-day gap while skipping a six-day one (06-06 -> 06-13); and 일정 still counts 0 on `README.md` and 0 on `web/finals.html`. **The raise to 20 is pre-registered here for the lap that clears critic #52's `fix-before-next-row` item AND lands WFG-218**, and it is a raise, not a restoration: the sub-item will then be answered, verifiable and reachable on all four surfaces. **제출 자료 18, HELD on two moves that cancel.** **+1**: the oracle split critic #51 deducted for went from five surfaces to three inside one window, and neither repair was ordered by a row - the 1619Z dev lap fixed Q36 as a `fix-before-next-row` item and paper lap 24 (`bd0da54`) fixed `paper/manuscript.md:509-512`'s mechanism on its own reading, and Q36 now links `docs/oracle_gap.md`, which is 「자료의 논리적 구성」 working. **-1**: a brand-new judge-facing document states two things about itself that are false at the head it shipped in, one of which a software-engineering judge falsifies by opening the builder. A row scored on 논리적 구성 and 출처 명기 does not rise in a window that adds a document contradicting its own source. **데이터 수집·분석·해석 20, HELD:** nothing this window touched a model, a split, a metric, an arm or a protocol; paper lap 24's §6 correction removed a false limitation (the observed FIRMS footprint IS committed) without moving a result. WFG-215 remains filed and untouched and is the standing reason this row does not have headroom above it. **연구 목적 18 and 창의성 18, HELD:** no method and no purpose moved. |
| 2026-09-09 | `ba06467` | 18 | 19 | 20 | 18 | **19** | **94** | **One row moves, 제출 자료 18 to 19, and it is critic #51's own deduction discharged rather than a new raise.** Critic #51 took the point with a measurement: `README.md:36`, `:266-267`, `:702`, `paper/manuscript.md:512` and `docs/auto/JUDGE_QA.md:1399` said 42 is what a noiseless forecast would buy, `docs/oracle_gap.md` said the oracle is in the grader, and the right answer was unreachable from any of them. **Re-measured at this head, every one of those five surfaces states the mechanism, and four of the five link `docs/oracle_gap.md`** (the fifth, the manuscript, had its mechanism corrected by paper lap 24 and lacks only the link, at 8,999 of 9,000 body words — NH-037). I checked the mechanism against the artifact rather than the prose: `routing_demo_canonical.npz` holds `haz_stack` float32 `(5,181,156)` and `obs_stack` uint8 `(6,181,156)`, and no grader in `scripts/` or `src/` reads `obs_stack`. 출처 명기 is the criterion and it is now satisfied on the front door. **Why 19 and not 20, and both halves are pre-registered for critic #54.** First, 「자료의 논리적 구성」 on that same front door: `README.md:220-299` still carries 11 ⚠ markers and 28 negative-framing tokens against one affirmative token, and that one favours the opponent (**WFG-212**, unmoved, now table position 1). Second, and it is this lap's root objection: **`web/finals.html` carries none of the correction** — 채점 0, 오라클 0, `oracle_gap` 0 on the built screen **and** on its template — while its 「알려진 한계」 panel spends ten cards on smaller caveats (**WFG-220**). The row moves to **20** when either lands. **데이터 수집·분석·해석 20, held at the ceiling.** **설계와 방법론 19, HELD, and exactly half of critic #52's pre-registration is paid.** That lap named two conditions: the boundary overclaim, and 일정 at 0 on `README.md` and 0 on `web/finals.html`. The first is closed, and closed well — `WC-012` registers both halves, §3 item 5 discloses the six-day 06-06 → 06-13 gap the document did **not** split on, and `DEMO_SCRIPT_5MIN.md` gives the student 「제가 정했습니다」 with a ❌ line under it. The second is untouched (**WFG-218**). A pre-registration with two conditions is not paid on one, and it moves to 20 when WFG-218 lands. **연구 목적 18, held:** no purpose, split, metric, arm or protocol moved; what would move it is the contribution statement WFG-212 is about. **창의성 18, held**; nothing 창의성-shaped changed this window. **Not scored:** the two Pass/Fail rows — 서류 is the author's under the NH-008 closure, 위험성 is not at risk, the delivery layer stays dry-run. |
| 2026-09-09 | `3eec471` | 18 | 19 | 20 | 18 | **18** | **93** | **One row moves and it is a DEDUCTION: 제출 자료 19 to 18, on 「자료의 논리적 구성」, the row's first named criterion, and it is critic #51's own deduction re-applied to a different claim in the window that discharged the first one.** **The raise first, because it is real and it is not enough.** `README.md:212-251` now opens the Round-4 section with what the project still claims before it lists what Round 4 attacked, which is exactly the defect critic #50 measured (11 ⚠ markers and 28 negative-framing tokens against one affirmative token in the section's newest 163 lines). It is written as an **existence** claim with its own bounds in the same block, it removes no caveat, `test_every_caveat_the_section_carried_is_still_there` and a ⚠ floor of 10 keep that true, and the reading note got **stricter** rather than softer (「이 절에 이 프로젝트에 유리한 결과는 없습니다」 replacing 「4번뿐」). On its own that is +1. **The deduction, measured at this head and not read from a report.** The same file answers its own headline claim two ways 180 lines apart. `README.md:212-251` says 「지점 단위」, says the committed sheets came from a run whose **화재 위험면과 지형은 합성**, and says 「실제 확산면으로 만든 출동 지시서는 아직 없습니다」. `README.md:391-395`, §5 item ①, says 「**가구 단위**의 …판정과 그 도보 경로」 and 「그 산출물의 실물이 저장소에 커밋돼 있습니다」 with **no bound**, and §5 item ② 「두 축이 동시에 실제인 실행」 sits immediately after it — the adjacency WFG-212's own independent reviewer broke the first draft on. I re-derived the premise: `data/processed/rescue_routing.json` → `provenance.sources` gives `hazard: synthetic`, `terrain: synthetic`, `origins: sampled candidates`. 「가구 단위」 counts 1 in `README.md`, 1 in `web/finals.html`, 1 in `scripts/finals.template.html`, 5 in `docs/auto/JUDGE_QA.md` and 1 in `docs/auto/DEMO_SCRIPT_5MIN.md`; 「지점 단위」 counts **1 in the whole repository**. **Why -1 and not -2.** On the README the bound is stated **first**, so a judge reading top-down meets it before the item; and the correction itself is this window's work, made against the lap's own reviewer rather than against a critic. **Why -1 and not 0.** The finals screen, the spoken script, the T0 card Q29a and the printed `docs/creativity_card.md` carry **only** the unbounded version, with no lead block in front of them, and five judges spend ten minutes each in front of the screen and none in front of the README. A row scored on 「자료의 논리적 구성」 does not hold when the object handed over states the project's one affirmative claim more strongly than the project does. **연구 목적 18, 설계와 방법론 19, 데이터 수집·분석·해석 20, 창의성 18, all HELD:** nothing in `ba06467..3eec471` touched a model, a split, a metric, an arm, a coupling or a protocol; `paper/manuscript.md` moved by one clause and one link. **창의성 held deliberately at 18** even though the defect sits on the 창의성 answer: what is wrong is the claim's bound, not the creativity of the output object, and one deduction per window on one row is this table's discipline. **Pre-registered for critic #55:** 제출 자료 returns to **19** when WFG-222 closes on all five surfaces **with the printed kit rebuilt and the bundle manifest re-pointed** — not when the README alone is fixed, and not on a report that says it was |
| 2026-09-10 | `7dabdef` | 18 | 19 | 20 | 18 | **19** | **94** | **One row moves on both tracks and it is the same row, 제출 자료, UP, and it is critic #54's pre-registered restoration paid on exactly the condition it pre-registered.** That condition was 「WFG-222 closes on all five surfaces **with the printed kit rebuilt and the bundle manifest re-pointed**, not when the README alone is fixed, and not on a report that says it was」. I verified it in the tree rather than reading the lap's report. **Counted here, unpiped, at `7dabdef`:** 「가구 단위」 answers **0** on `README.md`, **0** on `web/finals.html`, **0** on `scripts/finals.template.html` and **0** on `docs/auto/DEMO_SCRIPT_5MIN.md`, where critic #54 counted 1 / 1 / 1 / 1; 「per-household」 answers **0** on the screen and the template; and 「지점 단위」, which was **1 in the whole repository** at `3eec471`, now answers 2 / 1 / 1 / 2 / 1 / 3 across those files plus `docs/auto/JUDGE_QA.md` and `docs/creativity_card.md`. **The residue is deliberate and I opened each occurrence rather than trusting the count**: the four in the bank are three cards where the phrase is right (`:600` names the quantity `ingress_survival_time_min`, `:680-681` is the ⭕/❌ pair about what other systems' published material does not show, `:968` is Q20a's own question as a judge would ask it), and those in `docs/creativity_card.md` are record class under `<!-- forbidden-ok: … -->` pragmas. **The kit half of the condition also holds:** the kit was rebuilt twice (`WFG_printables_20260910T0017Z.pdf`, then `…T0140Z.pdf` after the reviewer's block), `release/kcf-finals-2026/MANIFEST.json` points at the second, and I re-hashed all seven of its printed sources against the tree, **seven of seven matching**, so the corrected wording is on the paper and not only in the repository. `WC-013` is registered in `docs/auto/withdrawn_claims.json` in the same lap, which is CHARTER §5c satisfied rather than promised. **+1 and not +2**, for two reasons measured rather than assumed: `web/finals.html` still carries **no word** of the oracle-in-the-grader objection that every other judge-facing surface now states (WFG-220, still `todo`, and the screen is the surface five judges spend ten minutes each in front of), and 일정 still answers **0** on both `README.md` and `web/finals.html` against a **named sub-item of 설계와 방법론** (WFG-218, still `todo`). **연구 목적 18, 설계와 방법론 19, 데이터 수집·분석·해석 20, 창의성 18, all HELD.** Nothing in `3eec471..7dabdef` touched a model, a split, a metric, an arm, a coupling or a protocol; the 70 added tests are all claim-bound gates over prose. **창의성 held deliberately at 18** even though the window's work is on the 창의성 answer: what was corrected is the claim's **bound**, not the creativity of the output object, and one move per window on one row is this table's discipline. ⚠ **One thing this row does not price, because it is not a judge's to see:** `scripts/check_withdrawn_claims.py` reads one line at a time, so the `WC-013` spelling it was handed is invisible to it where the phrase wraps across two source lines in `docs/creativity_card.md` §8, and that line is green only because a human wrote the pragma. The lap found and recorded that itself; it is **WFG-223** and it is a defect in the machine, not in the page. **Pre-registered for critic #56:** 제출 자료 reaches **20** when WFG-218 and WFG-220 both close, because at that point every named criterion of 자료의 논리적 구성 has an answer on the surface a judge actually stands in front of. |
| 2026-09-10 | `9b7d21c` | 18 | 19 | **19** | 18 | **20** | **94** | **Two rows move and they offset: 제출 자료 19 to 20 UP, 데이터 수집·분석·해석 20 to 19 DOWN. Track B HELD at 94, and the two moves are the honest reading of one window rather than a wash.** **제출 자료 19 to 20, and it is critic #55's pre-registration paid on exactly the condition it wrote:** 「제출 자료 reaches 20 when WFG-218 and WFG-220 both close, because at that point every named criterion of 자료의 논리적 구성 has an answer on the surface a judge actually stands in front of」. Both closed at `c4eb8d2` and I verified the closure at the artifacts rather than at the lap's report. **WFG-218:** `web/finals.html`'s embedded payload carries a `timeline` key holding all five phases with `start`, `end` and `open`, written by `scripts/build_finals.py`'s `timeline_phases()` from `data/processed/timeline_roles/timeline_roles.json`, and the card renders its count from `TL.phases.length`, so the screen's 「다섯」 cannot drift from the artifact; `tests/test_timeline_reaches_the_judge_surfaces.py` binds every phase name, the 구간 count word **and** the 「계획서가 아니라 기록입니다」 caveat on the README side, which is a content gate and not a presence gate. **WFG-220:** the oracle card is **first** in the 알려진 한계 grid rather than twelfth, which was the lap's own root objection, and `test_the_oracle_card_is_first_in_the_grid` asserts the position rather than the presence. **And the hand-over objects carry it:** `release/kcf-finals-2026/MANIFEST.json` was re-pointed in the same commit, **nineteen of nineteen** declared hashes match their sources including the rebuilt screen, and the printed kit's **seven of seven** sources still hash equal. On a row whose named criteria are 「자료의 논리적 구성 · 그래픽 및 범례의 명확성 · 사용된 자료에 대한 출처 명기」, the last surface a judge stands in front of now answers the last named sub-item, and the object handed over is the same object. **데이터 수집·분석·해석 20 to 19, and this is a DEDUCTION taken at the ceiling.** The row's own rubric text is 「수학 및 통계 방법의 적절한 적용 · 예상 결과에 대한 논리적인 해석 및 결론」. Confirmed in one process against `data/processed/oracle_gap_yeongdeok.json` at this head, not read from WFG-215: the four non-seed slices pair `t=180` with the observation at 333 min, `t=360` with **the same** 333, `t=540` with **the same** 333 again, and only `t=720` with 1005; `observed_cells` reads 937, 937, 937, 987 and `predicted_cells` rises 692, 952, 981, 1036. `docs/oracle_gap.md` §4 reads the resulting ratio series as forecast behaviour (「at 3 h the simulation is 26 % under」) when three of its four terms are a growing numerator over a constant denominator, and the 3 h term compares a forecast with an observation 153 minutes later. **Why the deduction is taken now and not when critic #51 filed it:** the defect became judge-facing inside this window. `docs/oracle_gap.md` is the cited evidence anchor from seven places on four judge-facing surfaces (`README.md:43`, `:256`, `:331`, `:784`; `docs/auto/JUDGE_QA.md:1419`, card Q36 at tier T0 and inside the printed kit; `paper/manuscript.md:512`; and, new at `c4eb8d2`, `web/finals.html:2069`, where it anchors the **first** card of the limits panel). A statistician judge who follows the screen's own citation reaches the misreading in one click, and 20 is the score for a row with nothing of this kind in it. ⚠ **The misreading is contained and I checked that rather than assuming it**: `26 %` and `0.74` count 0 on `README.md`, `docs/auto/JUDGE_QA.md`, `docs/auto/DEMO_SCRIPT_5MIN.md`, `docs/creativity_card.md`, `web/finals.html` and `paper/manuscript.md`. That containment is why this is minus one and not minus two, and why the repair is this lap's `fix-before-next-row` item rather than a P0 row. **설계와 방법론 19, HELD, and I decline critic #52's pre-registered raise to 20 with a reason rather than by silence.** That lap wrote 「the raise to 20 is pre-registered for the lap that clears critic #52's `fix-before-next-row` item AND lands WFG-218」, and both conditions are now met. I do not pay it, because a third of this row's own named sub-items is 「변수의 정의와 제어의 적절성」 and **WFG-128** is open against exactly that: `docs/multi_region.md:187-192` states the `fa_exceeds_budget` bucket while the fire-blind arm is scored under **no** time budget and the future-aware arm enforces 600 minutes, and the sentence says so of only one of the two arms. `README.md:146` sends a judge to that page for 「완전한 분할」. Paying a ceiling on a control-definition row while the one number that runs against the project is stated under two different rules is not something I could defend at a booth. **Pre-registered for critic #57:** 설계와 방법론 reaches **20** when WFG-128 closes on both surfaces with its gate, which is why this lap moved WFG-128 to the head of the P0 block. **연구 목적 18 and 창의성 18, HELD:** nothing in this window touched a model, a split, a metric, an arm, a protocol or the purpose statement, and what WFG-220 added to the screen is a caveat about an existing measurement rather than a new creative act. **Not scored:** the two Pass/Fail rows. 서류 is the author's under the NH-008 closure, 위험성 is not at risk, the delivery layer stays dry-run (28 committed run records, all `nothing_was_sent: true`, re-counted here across both `MANIFEST.json` and `RUN.json`). |
| 2026-09-10 | `16e6824` | 18 | **20** | **20** | 18 | **19** | **95** | **Three rows move and the total rises for the first time since `ba06467`, but the row that rises is not the row this lap spent its findings on.** **설계와 방법론 19 to 20, and it is critic #56's pre-registration paid on its own stated condition rather than a new judgement.** That lap wrote 「I still decline it, for the same reason and pre-registered on the same condition: WFG-128 is open against 「적합한 해결방법 제시」's control half on the page `README.md:146` sends a judge to」. WFG-128 closed at `5bcfe11` and I read the page rather than the report: `docs/multi_region.md` §3.1 now names `classify()` in `scripts/run_real_roads_real_hazard_slope.py` as the site of the asymmetry, states that the future-aware router gets `time_budget_min` and `naive_route` gets **no budget at all**, states that under one rule the Uiseong-Andong bucket is **empty** with `docs/present_perimeter_arm.md` §2 and the two arrival times 624.8 / 628.2 behind it, and separates the remaining entries instead of lumping them; `README.md:145-149` carries the clause in the same bullet block as 「각 2곳·3곳」; and `tests/test_budget_rule_asymmetry_is_stated.py` grades both surfaces in both directions with four recorded mutations. 「변수의 정의와 제어의 적절성」 is the sub-item, and a control whose different scoring rule is now stated, tested, and refused in the inverse direction is what that sub-item asks for. ⚠ **I considered declining this on a sharper condition — that the control is still mis-specified in the ARTIFACT and only corrected in prose, with the registry half (WFG-122) still `todo` — and I decline to decline.** Inventing a new condition in the lap the old one is met is the goalpost move critic #53 called out by name, and the deduction it would express belongs on 제출 자료, where I have put it. **데이터 수집·분석·해석 19 to 20, and this is critic #56's OWN deduction discharged in one lap.** #56 docked this row because `docs/oracle_gap.md` §4 read a size-ratio series as forecast behaviour when three of its four slices are scored against one observation. Verified closed at this head, in one process against `data/processed/oracle_gap_yeongdeok.json`: §4 now prints the observation and the `time_gap_min` beside every ratio and every IoU, §4b's IoU series carries its four gaps, and `:6`'s stale 「10 keys」 is replaced by the prefix and where to count it rather than by a new hand-written count. The registry half went in the same lap and additively — `grep -o 'og_yeongdeok_t[0-9]*min_obs_time_min' docs/NUMBERS.json | sort -u` returns **five** keys — and the withdrawn reading is registered as `WC-014`, which is CHARTER §5c satisfied rather than promised. ⚠ **And the way it closed is the strongest single piece of evidence in this window**: critic #56 told that lap, in DIRECTION and in the row in capitals, that those keys were unregisterable and that the row's own 「Do」 must not be followed. The lap's independent reviewer blocked on it, the lap registered the keys instead of arguing, and its report says so in its own words. A row scored on 「연구 재현 가능성」 and 「예상 결과에 대한 논리적인 해석」 earns the ceiling when the repository corrects its own critic against the artifact. **제출 자료 20 to 19, and it is a DEDUCTION taken in the same lap as two raises, because three separate defects landed on this one row's two named criteria.** (1) 「사용된 자료에 대한 출처 명기」, failing on the page whose entire purpose is to be checkable: `docs/oracle_gap.md:39` attributes `haz_stack` to `scripts/run_forward_sim_region.py`, and that script never touches the file — `grep -c routing_demo_canonical` answers **0** on it, it writes `hazard_{fid}.npz` (`:283`) and `forward_sim_regions.json` (`:338`), and `data/processed/routing_demo_canonical.npz` is written by `scripts/build_canonical_hazard.py` (`:109`, `:177`). The LOFO claim itself is TRUE (`build_canonical_hazard.py:130` fits the dataset with the target fire excluded, and its own step print at `:129` says so), which is what makes this a citation defect rather than a false claim — and a judge who opens the named script finds its `grid_note` saying Yeongdeok's field used a different bbox, i.e. the script telling them it did not make this array. **This is this lap's one `fix-before-next-row` item.** (2) 「자료의 논리적 구성」 on the surface five judges stand in front of: `web/finals.html`'s `renderPanel()` prints **◆ 예산 초과 2** for the region the screen opens on and **3** for Uljin-Samcheok, and the 알려진 한계 panel's ten `rel(...)` cards say nothing about the budget rule — the string 예산 appears on exactly one line of the file, inside the data payload, as the bucket label. The correction reached the README and `docs/multi_region.md` six hours earlier and the gate that keeps it honest grades those two files and nothing else (`:63-64`). **WFG-225.** (3) The Q&A bank, on paper: card Q38 (`:1421`) still tells the student to say 「오늘 저장소는 이 질문에 두 가지로 답합니다 — 그게 결함입니다」, and the repository gives one answer as of `5bcfe11`; all **seven** printed sources of the 0140Z kit hash equal to the tree, re-checked here, so the stale card is in the booth kit. **WFG-226.** **-1 and not -2**, and the restraint is measured rather than generous: none of the three prints a wrong number, none puts a margin value anywhere, (1) is minutes of prose, and the same window produced the best 데이터 work of the sprint. **연구 목적 18 and 창의성 18, HELD.** Nothing this window touched a model, a split, a metric, an arm, a protocol or the purpose statement, and no new external claim entered any judge-facing surface — `factchk` over the window's diff finds no new citation and no new URL outside `docs/auto/` record-class pages. **Not scored:** the two Pass/Fail rows. 서류 is the author's under the NH-008 closure; 위험성 is not at risk, the delivery layer stays dry-run. |
| 2026-09-10 | `d3ca754` | 18 | 20 | **19** | 18 | **20** | **95** | **Two rows move and they offset, so the total HELDs at 95.** **제출 자료 19 → 20**, paying critic #57's two open defects and verified in the tree rather than read from the 0952Z report: the caveat string 「예산 초과는 두 팔에 서로 다른 시간 규칙…」 appears twice in `scripts/finals.template.html` **and** twice in the built `web/finals.html`, so the count a judge reads on the first panel now carries the rule it was measured under (WFG-225), and `docs/oracle_gap.md:39` no longer names a script that never wrote the array — `grep -n run_forward_sim_region docs/oracle_gap.md` returns nothing. 「사용된 자료에 대한 출처 명기」 was the criterion both defects sat on. **데이터 수집·분석·해석 20 → 19** on WFG-228: `docs/oracle_gap.md` §4 concludes 「the place substantially wrong」 from **IoU 0.394** and the repository offers no null model for it, while `src/wildfireguardian/validation/baselines.py` has shipped `run_persistence_baseline` and `run_isotropic_baseline` since Session 4 and `docs/MODEL_CARD.md` gives no baseline for the forward-sim envelope on any canvas. That is 「수학 및 통계 방법의 적절한 적용」 and it is the first question the ML-reviewer and statistician lenses ask. ⚠ Not −2: WFG-230 is a sharpening of a caveat the document already carries, not a new defect, and §4's whole result table re-derives from the artifact exactly. ⚠ The one objection I expected to sustain did **not**: the canonical canvas was chosen by sweeping westward extensions until the *simulated* envelope cleared the in-grid guard (`scripts/build_canonical_hazard.py:137-145`), which would make the 403 missed cells a floor if the *observation* ran past it — measured, **neither stack touches any grid border in any of the eleven slices**, so nothing is clipped. **Pre-registered: 데이터 수집·분석·해석 returns to 20 when WFG-228 publishes a null figure with its caveat, whichever way the figure comes out.** |
| 2026-09-10 | `3867860` | 18 | 20 | 19 | 18 | **19** | **94** | **One row moves, 제출 자료 20 to 19, and Track B is DOWN to 94.** ⚠ **The defect is not new this window and I am scoring it anyway**, because the instruction is to score today's state rather than the delta, and a 20 on a 20-point row is a claim that nothing was found. `docs/MODEL_CARD.md` §「작품설명서 (Korean writeup) correction mapping」 ends with the one sentence in this repository written to be **pasted into a document the judges receive**, and at `:524-525` it explains the weak `gangneung_2023` fold with 「탐지 약 17건」. Every other place the same file explains the same fold uses a different quantity: `:52-53` and `:86` both say 「~8 positives」, and `docs/fold_sizes.md:10` gives 396 rows / 8 positive cells / 0.26 % of the evidence. Both numbers are true and they measure different things (whole-fire FIRMS detections against positive 500 m cells in the fold), which is exactly the defect: the pasted sentence distinguishes them only through the single word 「탐지」. `README.md:489` is the one surface that writes both and writes them correctly, so the repair already exists and has not reached the sentence that leaves the repository. **The sourcing half is sharper and is why this lands on 「사용된 자료에 대한 출처 명기」 rather than on 「자료의 논리적 구성」:** 17 is registered under no key of the 453 in `docs/NUMBERS.json`, so `make verify` never re-derives it, neither surface names a source, and its nearest grep-able home is `data/processed/spread_v2/audit.json`, a directory carrying `LEGACY_DO_NOT_CITE.md` and the instruction 「if you are writing a document, do not open these files」. It does trace, to `data/processed/detection/firms_first_detection.json :: gangneung_2023.n`, which nothing cites. **WFG-233**, minutes plus one registration. **19 and not 18**, because the window's work on this row was real and I verified it rather than reading it: card Q38 no longer tells the student to volunteer a contradiction that closed at `5bcfe11`, card Q29 now carries 커밋 513개 / 전체 커밋 662개 with the as-of commit `89da7d3` and both are gated against the registry, the kit was rebuilt at `20260910T1233Z` and I re-hashed all seven printed sources against the tree at 7 of 7, and `docs/oracle_gap.md:229-241` no longer claims a registry protection the registry does not give (re-measured: 30 `og_yeongdeok_*` keys, all 30 carrying `forbidden_phrasings`, the string 「what the forecast buys」 registered on zero of them). **데이터 수집·분석·해석 19, HELD, and the pre-registration is unchanged rather than renewed:** critic #58 wrote that it returns to 20 when WFG-228 lands a null figure with its caveat, whatever the figure says. WFG-228 is `todo` and is the top row on `DIRECTION.md`. **연구 목적 18, 설계와 방법론 20, 창의성 18, all HELD:** no purpose and no method moved this window, and the 설계와 방법론 ceiling is re-earned rather than inherited, because 「일정 및 팀원 역할 배분의 타당성」 gained the drill instrument it lacked (Q29 · T0). **Not scored:** the two Pass/Fail rows. |
| 2026-09-10 | `71e95ee` | 18 | 20 | **20** | **19** | 19 | **96** | **Two rows move and Track B is UP to 96.** **데이터 수집·분석·해석 19 to 20, and this is a pre-registration being paid rather than a judgement.** Critic #58 wrote, and critic #59 re-stated without weakening it, that this row returns to 20 「when WFG-228 lands a null figure with its caveat, whatever the figure says」. WFG-228 landed at `b8fd6a8`, and what it landed is stronger than the condition asked for. (a) The null has **zero free parameters** and its rule was fixed in the claim commit `4ab2e07` **before** the script ran: an area-matched disc centred on the centroid of the 249-cell `t = 0` seed both stacks share, holding exactly as many cells as that slice's **own** predicted core, scored under the same matching rule as `scripts/measure_oracle_gap.py`. The area is not chosen, it is handed to the null from the model. (b) The lap **refused its own row's interpretation in writing**: WFG-228 said the IoU gap is 「the model's directional skill and nothing else」, and `docs/disc_null.md` §4 shows the artifact refutes that, because by centre of mass the **disc is closer to the observed footprint than the model is** (model 5.340 cells from truth against the disc's 2.266), the model having sent the fire **3,646.1 m** where it moved **1,124.8 m**. (c) The independent reviewer blocked and was right: `obs_stack` is cumulative, so the seed is a subset of the observation being scored and the model's core contains all 249 by construction while the disc recovers 92. The seed-removed pair (**0.2577 / 0.1169**, ratio **2.2044**) is published beside the raw one, the finding survives in sign at all four slices, and `docs/disc_null.md:119-120` writes 「2.5360 is not quotable without 2.2044 beside it」. (d) §5 lists seven things it does **not** show, including that the disc is a floor and not a rival. That is what a 20 on 「연구의 안정적인 결과 도출 가능성 · 수학 및 통계 방법의 적절한 적용 · 예상 결과에 대한 논리적인 해석 및 결론」 looks like. **창의성 18 to 19.** The design is the creative object, not the result: a null whose only degree of freedom is **handed over from the thing it is testing**, pre-registered so it could not be re-chosen after the answer, and then used to defeat the hypothesis that commissioned it. **19 and not 20** because it is one fire, one canvas, one threshold, and because the criterion still has no instance outside the evaluation layer. **제출 자료 19, HELD, and the path to 20 now needs two rows rather than one.** WFG-233 is unchanged and still `todo`. Added this window: **WFG-236**, a 「자료의 논리적 구성」 defect, in which `docs/disc_null.md:20-21` and `docs/oracle_gap.md:180-181` both assert in bold that nothing in the repository said what 0.394 should be compared with, while `README.md:520-522` and `:888-891` have compared it to the Rothermel surface model's **~0.09** and called it 「약 4배」 for months, sourced to `docs/ROUTING_INTEGRATION_REPORT.md:183`. Two comparisons of one number, pointing opposite ways, neither page naming the other, and the README's comparator is a model its own TL;DR calls buggy. It does not cost a second point, because the same window put the harsher of the two readings into the tree voluntarily. **연구 목적 18 and 설계와 방법론 20, HELD.** No purpose moved, and the 설계와 방법론 ceiling is re-earned on the pre-registration discipline rather than inherited. **Pre-registered for critic #61:** 제출 자료 reaches **20 on both tracks** when WFG-233 **and** WFG-236 have both closed, and returns to 18 on either track if a printed-kit source drifts against its manifest. 데이터 수집·분석·해석 returns to 19 if any judge-facing surface acquires `2.536` without `2.2044` beside it. **Not scored:** the two Pass/Fail rows. |
| 2026-09-10 | `be39dea` | 18 | 20 | 20 | 19 | **18** | **95** | **One row moves on BOTH tracks and it is the same row, 제출 자료 19 to 18, so Track B is DOWN to 95 and Track A DOWN to 96.** ⚠ **The window's diff moved nothing and I am scoring anyway**, on critic #59's own precedent in this file: the instruction is to score today's state, not the window's delta. The window `71e95ee..be39dea` holds one research lap and four report or archive commits, no code, no data, no figure, no judge-facing artifact. **What changed is the measurement.** **The evidence, and it is new even though the defect is not:** `docs/auto/finals/RELATED_WORK_PANEL.md:46` names this project's spatial unit 「공간 단위는 **가구(집)**」, as the third row of a three-way comparison whose other rows read 「**읍면동**」, and `:119-120` states the project's own differentiator as 「그 발상을 농촌 **가구 단위의 도보 대피**에」. The same file says 「**지점 단위의 대피 경로와 구조 출동 순서**」 at `:23` and carries a ⚠ 2026-09-10 정정 block at `:27-31` that narrows exactly that register and scopes itself to 「**이 문단**」. **This is on paper, not in `docs/`:** `docs/auto/finals/printables/manifest_20260910T1233Z.json` lists that file among its seven `sources` and all seven hash equal to the tree, re-computed in this lap's own process, so `WFG_printables_20260910T1233Z.pdf` carries both lines to the booth. 「자료의 논리적 구성」 is the first named sub-item of this 20-point row on **both** tables, and a printed sheet that contradicts its own correction 73 lines later is that sub-item failing. Second, smaller: `docs/auto/JUDGE_QA.md:1246-1247` tells the student the repository does not know of anyone who built the same thing, and since 2026-09-10 it does (**WFG-241**). **18 and not 17:** nothing here is false, every claim is sourced, the panel's 「최초·처음이라고 말하지 않습니다」 block at `:123-125` is the strongest paragraph on the sheet and is untouched, and the correction the panel needs already exists in the same file. **Four open 제출 자료 defects now (WFG-233, WFG-236, WFG-240, WFG-241), and two of the four are on paper rather than in `docs/`**, which is the difference from critic #60's HELD at 19. **Every other row HELD and re-checked rather than inherited:** `gates.py --mode full` exits 0 at this exact head (**2042 passed**, 63 skipped, 3 xfailed, pytest 282.0 s), GitHub `auto-gates` runs 294 to 343 carry ZERO `failure` with run **343** green at this head, `--assert-head` and `--assert-reported` both exit 0, and `release/kcf-finals-2026/MANIFEST.json` hashes 19 of 19. Nothing in the window touched a model, a split, a metric, an arm, a coupling or a protocol, so 데이터 수집·분석·해석, 설계와 방법론, 창의성, 연구/개발 목적 and 구현 및 유용성 all stand where critic #60 left them. **Pre-registered for critic #62:** 제출 자료 returns to **19** when WFG-240 and WFG-241 both close with `make printables` rebuilt and the bundle manifest re-pointed, and to **20** when WFG-233 and WFG-236 close as well. **Not scored:** the two Pass/Fail rows. |
| 2026-09-10 | `f93af93` | 18 | 20 | 20 | 19 | **17** | **94** | **One row moves on BOTH tracks and it is the same row, 제출 자료 18 to 17, so Track B is DOWN to 94 and Track A DOWN to 95.** ⚠ **This window's diff DID move judge-facing artifacts**, unlike critic #61's: `docs/MODEL_CARD.md` changed and `web/finals.html` was rebuilt at `e8b6e01`, and a paper lap added a figure and a verified reference at `938dff7`. So this is a delta and not only a re-measurement. **One 제출 자료 defect closed and two opened, so the row moves down by one and not by two.** **The close, and it was a good one:** WFG-233's repair is on the one text in this repository written to be pasted into the **submitted** 작품설명서, and it now reads 「최저값 0.68을 기록한 `gangneung_2023` 폴드는 **양성 셀이 8개**뿐인 소규모·잡음 폴드입니다 (같은 산불의 FIRMS 탐지는 **17건**이지만, 이는 화재 전체를 센 값이고 폴드의 양성 셀 수가 아닙니다)」 with a `src:` line and a `foldev_` registry key behind each count, so `make verify` re-derives both. 「사용된 자료에 대한 출처 명기」 is a named sub-item of this row and that is it paid on the highest-stakes text in the project. **The two that opened.** **WFG-243:** `docs/auto/JUDGE_QA.md` Q16a is tier **T0**, said from memory to five judges, and the sentence that states this project's entire differentiator against 산림과학원 and G-DAPS is in the household register `WC-013` withdrew on 2026-09-10 — `:657-659` 「저희가 내는 것은 … **가구 하나하나에 대한** 「지금 걸어 나갈 수 있는가, 없다면 누구를 먼저 데리러 가는가」 이고, 공간 단위가 읍면동이 아니라 **집**입니다」 and `:667` 「「**이 집 사람이** 걸어 나갈 수 있는가」」. **It is on paper, re-hashed here rather than assumed:** `docs/auto/finals/printables/manifest_20260910T1233Z.json` lists that file among its seven `sources` and all **seven** hash equal to the tree at this head, so `WFG_printables_20260910T1233Z.pdf` carries both lines. The same defect is on `docs/auto/finals/RELATED_WORK_PANEL.md` in **four** places, not the two WFG-240 named (`:45-46` and `:73-74` are added to that row here). **Six spellings in total across the tree and no gate sees one of them**, because `WC-013`'s pattern is anchored on 「가구 단위」 plus an output-object phrase and none of the six contains 「가구 단위」. **WFG-244:** the block written onto the WFG-233 sentence 90 minutes before this lap asserts 「Neither count has a second, independent derivation in this repository」 and 「its 17 agrees with the whole-fire count **by coincidence rather than by derivation**」, while `data/processed/spread_v2/audit.json :: fires[5]`, opened here, holds `detections_csv` **17**, `detections_in_grid` **17**, **`total_positives` 8** and `spread_cells_total` **8** — both counts, over that fire's own bbox and date window. Two unmeasured provenance claims, in the same commit whose reviewer had just blocked the lap for overstating a corroboration. **17 and not 16:** nothing on the kit is false, every claim is sourced, Q16a's own 「저희가 그 둘보다 정확하다고 말하지 않는다」 is intact and is the strongest sentence on the card, and the corrected register already exists in both files (`docs/auto/JUDGE_QA.md:1238`, `RELATED_WORK_PANEL.md:23`). **Five open 제출 자료 defects now (WFG-236, WFG-240, WFG-241, WFG-243, WFG-244), three of them on paper rather than in `docs/`.** **Every other row HELD and re-checked rather than inherited:** `gates.py --mode full` exits 0 at this exact head (**2054 passed**, 63 skipped, 3 xfailed, pytest 319.3 s), GitHub `auto-gates` runs **332 to 351** carry **14 `success`, 6 `cancelled` and ZERO `failure`** with run **351 green at this exact head**, `--assert-reported` replayed pairwise over all seven pushed heads in the window exits 0 at every step, and `release/kcf-finals-2026/MANIFEST.json` hashes **19 of 19** against its `source` paths. Nothing in the window touched a model, a split, a metric, an arm, a coupling or a protocol, so 데이터 수집·분석·해석 **20**, 설계와 방법론 **20**, 창의성 **19** and 연구 목적 **18** all stand where critic #60 and #61 left them; the twelve new tests at `e8b6e01` gate prose rather than adding a measurement. **Pre-registered for critic #63:** 제출 자료 returns to **18** when WFG-240 and WFG-243 both close **with `make printables` rebuilt at a new stamp and the bundle manifest re-pointed** — not when the tree is fixed, and not on a report that says it was — to **19** when WFG-241 and WFG-244 close as well, and to **20** when WFG-236 closes. 창의성 is re-examined upward when the disc null's design reaches a judge-facing surface. **Not scored:** the two Pass/Fail rows. |
| 2026-09-11 | `0796336` | 18 | 20 | 20 | 19 | **19** | **96** | **One row moves on BOTH tracks and it is the same row, 제출 자료 17 to 19, and Track B is UP to 96. This is the first rise on this row in four critic laps and it is paid on exactly the defect that took it down.** The criterion is 「자료의 논리적 구성 · 그래픽 및 범례의 명확성 · 사용된 자료에 대한 출처 명기」. Three consecutive laps cut it (19 to 18 to 17) for one reason: the withdrawn household register was standing on the printed kit. **I did not read that it is fixed; I swept for it.** All six `WC-013` spellings re-run in this lap's own process over every tracked `.md` and `.html`, on text **flattened of newlines** so the sweep sees what the line-based shipped checker cannot: 「이 집 사람」 7 hits, 「가구 하나하나」 6, 「공간 단위 … 가구/집」 7, 「어느 집을 먼저」 6, 「가구 단위의 도보 대피」 5, 「가구별 대피」 2, and **ZERO of the 33 is outside the declared record class**. The kit is on paper, not only in the tree: `manifest_20260911T0102Z.json` re-hashed **7 of 7** sources against the working tree and `release/kcf-finals-2026/MANIFEST.json` **19 of 19** against theirs, both recomputed here. **The second half of the rise is 「출처 명기」 in its strictest form.** The one publication that takes this project's architecture away from it is now cited on a judge card (Q16d, T1) at its concept DOI, and **every fact on that card was re-verified against the Zenodo API in this lap, not against the report that wrote it**: `conceptdoi` `10.5281/zenodo.22668357` resolving to record `22668358` (a version relation, as the card says), title exact, `publication_date` **2026-09-09**, `resource_type` **Preprint**, the five authors in the card's order, one attached file named `IEEE_Conference_Template.pdf`. The card's hardest claim — 「기록에는 성능 수치가 하나도 없습니다」 — is exact: a regex for digits over the record's own abstract returns the **empty list**. **19 and not 20, and both reasons are on this same criterion and on the paper in the box.** (1) **WFG-247**: the demo's closing 56 seconds says 「20가구」·「24가구」·「0가구」 in one breath with 「보행망 노드 2,218곳」 (`docs/auto/DEMO_SCRIPT_5MIN.md:266-268`, its number table `:358-361`, its 금지 list `:380`, and `web/finals.html:1978-1995`), while `docs/NUMBERS.json :: l0i_best_single_refuge_saved` records `unit: households` on `sample: 영덕 2025, OSM 건물 124동(잠정)`. Three populations in one sentence, one of them named in the register this project withdrew the day before, and the reconciliation that exists (`docs/auto/JUDGE_QA.md:947`, 「한쪽은 보행 도로망 노드이고 다른 쪽은 OSM 건물 124개입니다」) is 190 lines away in another segment and is pointed at by nothing. `WC-013`'s own rule is that the bound travels in the same block. (2) **WFG-248**: Q16d tells the student to open the DOI in front of the judge and then characterises that record as 「저쪽은 D* Lite 로 다시 계획합니다」, while the record's own abstract calls D* Lite the **fallback** — 「During API constraints or in wilderness scenarios, the system delegates to an internal D\* Lite heuristic grid fallback」 — with OpenRouteService as the primary. **연구 목적 18, 설계와 방법론 20, 데이터 수집·분석·해석 20, all HELD**; no purpose, no method and no measurement moved in the window. **창의성 19, HELD, and half of its standing reason is now discharged rather than repeated:** critic #60 and #61 both held it at 19 partly because the disc null 「reaches no surface a judge at the booth will see」, and at this head Q36 is a **T0** card carrying both IoU pairs (0.3941 / 0.1554 and 0.2577 / 0.1169) and all four centroid displacements in cells (2.25, 7.292, 5.34, 2.266) in one block, on the printed kit. What still holds it at 19 is the other half and it did not move: **one fire, one canvas, one threshold.** **Pre-registered for critic #64:** 제출 자료 reaches **20 on both tracks** when WFG-247 and WFG-248 both close with the kit rebuilt and the manifest re-pointed, and falls back to 18 on either track if a kit source drifts against its manifest hash. **Not scored:** the two Pass/Fail rows. |
| 2026-09-11 | `5482ba8` | 18 | 20 | 20 | 19 | 19 | **96** | **No row moves and Track B HOLDS at 96, and the one row that was pre-registered to move had its condition MET and the rise DECLINED.** Critic #63 wrote that 제출 자료 reaches **20 on both tracks** when WFG-247 and WFG-248 both close with the kit rebuilt and the manifest re-pointed. Both closed at `c75cb08`, the kit was rebuilt twice (`20260911T0326Z`, then `20260911T0412Z` because the reviewer's correction landed in a hashed source), and `release/kcf-finals-2026/MANIFEST.json` was re-pointed. **I verified the object and not the report, recomputed in this lap's own process:** the newest kit manifest hashes **7 of 7** sources against the tree, the bundle **19 of 19** entries against theirs, and the bundle names `printables/WFG_printables_20260911T0412Z.pdf`, which is the newest stamp. So the stated condition is met on its own terms. **The rise is declined, and not by moving the goalposts: the same repair introduced a defect of the same class on the same two printed documents.** **WFG-249** is a flat contradiction inside the 58-page kit between Q20a, a **T0** card the student answers from memory, which says 「이 저장소에서 「가구」는 사람도 주소도 **건물도 아니라** OSM 보행 도로망의 노드 하나입니다」, and the spoken 마무리 added in this window, which says the 가구 it counts are **OSM 건물** and gives the count; and the demo script's 금지 item 6, rewritten in the same commit, cites Q20a as the authority for the definition it breaks. **WFG-250** is on the other of the two documents: the closing names the arm's denominator where the claim's denominator is `n_failing`, which the finals screen already says correctly at `scripts/finals.template.html:1981`, so script and screen disagree on the sentence that was just repaired. **WFG-251** is Q16d's draft answer asserting a negative over the whole deposited Zenodo record while the same card's 없는 것 item 1, forty lines below, says the body PDF was never opened and prescribes the scoped form. 「자료의 논리적 구성」 is the first named sub-item of this row and 「사용된 자료에 대한 출처 명기」 the third; a **20** here is a claim that nothing was found on the submitted material, and three things were, all of them on paper in the box a judge carries away. **No fall either, which the same pre-registration allowed for:** nothing drifted against a hash, the false noun 「보행망 노드 2,218곳」 is gone from all four surfaces and registered as `WC-016`, and the window's own arithmetic errors were caught by its independent reviewer and registered as `WC-015` in the same lap, which is CHARTER §3.5c paid on time. **데이터 수집·분석·해석 20, HELD:** nothing in the window measured data. The four new registry keys are populations read off an existing artifact and a pace re-measure, which is bookkeeping done well rather than analysis. **창의성 19, HELD:** the two withdrawals are the project auditing its own machinery, which is rigour rather than 「프로젝트 진행에 있어 뛰어난 창의성」; the standing reason for 19 rather than 20 is unchanged (one fire, one canvas, one threshold). **설계와 방법론 20, HELD**, and **연구 목적 18, HELD**: no purpose and no method moved. ⚠ **The reason 연구 목적 does not rise is now this lap's root objection rather than a gap in the tree:** the window produced no measurement of anything outside this repository, and **WFG-129**, the P0 science row that would test the number the booth leads with on committed inputs with no author decision needed, has been `todo` at table position 3 for five consecutive direction pages whose own root objections asked for exactly it. `docs/auto/DIRECTION.md` now names it. **Pre-registered for critic #65:** 제출 자료 reaches 20 on both tracks when WFG-249, WFG-250 and WFG-251 all close with the kit rebuilt and the manifest re-pointed, and falls to 18 on either track if a kit source drifts against its manifest hash; and if a dev lap ran in the window and WFG-129 is still `todo`, 연구 목적 falls to 17 on this track. Not scored: the two Pass/Fail rows. |
| 2026-09-11 | `f7ee58d` | 19 | 20 | **19** | 19 | **18** | **95** | ⚠ **TRANSCRIBED by critic #66, not a second scoring.** Critic #65 wrote this row into the **Track A** table (line 235 there) and into neither this table nor the series, which is the append defect critic #40 measured and **WFG-252** and **WFG-184** carry. Its cells and its reasoning are unchanged here and its narrative is not copied; read it at its original position. Two things critic #66 could not reconcile and did not edit (CHARTER §3.7): **연구 목적 reads 18 at `5482ba8` and 19 here while the row's own narrative says 「연구 목적 19 ... all HELD」**, and no lap has published a reason for the raise, which is what makes this row 95 rather than 94; and the report's published delta 「Track B 97 to 95」 measures against **Track A's** previous 97, where this table's previous value is **96**. |
| 2026-09-11 | `d67de57` | **18** | 20 | **18** | 19 | **19** | **94** | **THREE rows move, two DOWN and one UP, and only one of the falls is a new defect.** ⚠⚠ **데이터 수집·분석·해석 19 to 18 (WFG-256), a second instance in two consecutive windows of the same class on the same criterion 「예상 결과에 대한 논리적인 해석 및 결론」, and this one is on the T0 card.** `docs/auto/JUDGE_QA.md:1513` and `docs/disc_null.md:225` both close on 「저희가 잘하는 것은 방향이 아니라 불의 모양입니다」, and Q36 repeats it as 「모델이 원판을 이긴 것은 모양과 범위이고」. The artifact underneath declines that attribution in its own words: `data/processed/disc_null_yeongdeok.json :: what_this_is_not` calls the IoU gap 「joint PLACEMENT-AND-SHAPE skill, not directional skill alone」. The `direction` block prices the placement half and it goes against the model (5.34 cells of centroid error against the stationary disc's 2.266), and the card assigns the whole remainder to 「모양」, which is the forbidden single-component move mirrored. **Nothing in this repository measures the shape half**: searched here across `docs/`, `paper/`, `scripts/` and `src/` for a shape-matched, second-moment, principal-axis or rotated null, the only hit is `scripts/direction_drivers.py:31` on a different object. ⚠ **The row does not fall to 17, and one thing checked here argues for that:** the headline slice is not cherry-picked. The seed-removed ratio is 1.9538 / 2.2044 / 2.2335 / 2.2286 across the four non-seed slices, the headline slice is the smallest time gap at 27 min and was fixed in the claim commit before the answer, and the model's own IoU is slightly higher at two other slices. Selection is clean; attribution is not. ⚠⚠ **연구 목적 19 to 18, and this is a WITHDRAWAL of an unevidenced point rather than a finding.** The last evidenced value is 18 at `5482ba8`; the 19 in the row above appeared while that row's narrative called the criterion HELD. No row another lap wrote was edited (CHARTER §3.7); this lap scored the criterion itself, at 18, on the same reasoning every lap since `71e95ee` has given: the purpose is specific and unchanged, and **NH-054** is open on whether the front door states it in the first thirty seconds. ⚠⚠ **제출 자료 18 to 19, UP, and this cell was corrected mid-lap.** At `d67de57` it held at 18 with four of seven surfaces paid; the lap holding WFG-254 then pushed `05d3bb3` while this row was being written and paid critic #65's condition in full. Re-measured here at the rebased head rather than read from that lap's report: 「발화점」 answers **0** in the Q36 cell, 「모양과 범위」 answers **0** across `docs/auto/JUDGE_QA.md`, `docs/disc_null.md` and `docs/oracle_gap.md`, `docs/oracle_gap.md:208` carries a dated 〔정정〕 rather than a silent edit, the kit is rebuilt at `WFG_printables_20260911T0939Z.pdf` (59 p) with its seven sources hashing **7 of 7**, and the bundle is re-pointed to it. The row also found an **eighth** surface its own brief did not name, turned the measurement into a committed artifact, and registered four spellings as `WC-017`. **19 and not 20**, because the criterion also reads 「자료의 논리적 구성」 and the card's closing sentence still asserts an axis nothing measures (WFG-256, the same defect the 데이터 row falls for). **설계와 방법론 20 and 창의성 19, HELD.** **Pre-registered for critic #67:** 제출 자료 reaches 20 when WFG-256's Q&A half lands with the kit rebuilt, and returns to 18 if any kit source drifts against its manifest hash; 데이터 수집·분석·해석 reaches 19 when **either** WFG-255 or WFG-256 lands with its result stated on the pages that interpret it, and 20 when both do; 연구 목적 returns to 19 only on a published reason, whoever writes it. **Not scored:** the two Pass/Fail rows. |
| 2026-09-11 | `dec00f4` | 18 | 20 | **19** | 19 | **18** | **94** | **TWO rows move, in opposite directions, and Track B HOLDS at 94 — which is the least informative number this table has produced this week, because the window contains both the best experiment in days and the plainest judge-facing falsehood in days.** ⚠⚠ **데이터 수집·분석·해석 18 to 19 (UP).** The criterion is 「데이터 수집 및 분석의 체계성 · 연구의 안정적인 결과 도출 가능성 · 수학 및 통계 방법의 적절한 적용 · 예상 결과에 대한 논리적인 해석 및 결론」, and WFG-129 is a model of the first and the last. The run built the fair opponent as a node filter using character-for-character the predicate `candidate_origins` already applies, so it added no rule and no free parameter; it refused to write anything until the committed 414 / 42 / 2 partition re-derived in its own process from the committed snapshots; it pre-registered its root objection (that a node filter could disconnect the graph and be reported as the fire's doing) in the claim commit and then answered it by measurement rather than assertion; and it reported three named outcomes separately instead of one number. Its independent reviewer BLOCKED it on a false monotonicity claim and the lap fixed the claim instead of arguing. Critic #66's reason for the fall to 18 — WFG-256's unmeasured attribution on the Q36 card — was paid at `a37cfb0` before the row was even claimed. **19 and not 20** for two reasons that are both live: WFG-255's component geometry is still unmeasured and still interpreted on published pages, and this window added **WFG-259**, an unregistered number in the new page's own §5. ⚠⚠ **제출 자료 19 to 18 (DOWN), and this is a false statement rather than an ambiguous one.** Four judge-facing lines assert that the fair-opponent comparison has never been run on 영덕 — `README.md:33-34` and `:325`, and `docs/auto/JUDGE_QA.md:954` and `:1004`, the second of which is the booth sentence critic #29 prescribed for exactly this number. All four are false as of `7991512`, and this repository holds the disproof: `scripts/measure_present_perimeter_yeongdeok.py`, `data/processed/present_perimeter_yeongdeok_2025.json` with six registered keys, `docs/present_perimeter_yeongdeok.md`, and `paper/GAPS.md` G7 marked CLOSED. The manuscript adds two more blocks and a live `[GAP:` marker its own ledger already closed. 「자료의 논리적 구성」 and 「사용된 자료에 대한 출처 명기」 are the criterion; a page that sends a judge to a file which says the opposite fails the first, and a prescribed spoken sentence contradicted by a committed artifact fails the second. That is **WFG-258**. **18 and not 17** because nothing was fabricated and the disproof is this project's own work from the same window. ⚠ **WFG-259 is scored on 데이터 and not here**, because it is a systematicity defect in a `docs/` page rather than a defect in what is submitted. **연구 목적 18, HELD**, and deliberately: critic #66 withdrew critic #65's unevidenced raise and no lap has since published a reason to restore it, so it stays where the evidence is. **설계와 방법론 20, HELD.** The reproduction gate before the write is the best argument for this row the repository has, and it is already at the ceiling. **창의성 19, HELD:** the arm's design is disciplined and reuses the project's own predicate, but the recipe was specified by `paper/GAPS.md` G7 rather than invented this window, and none of it reaches a surface a judge at the booth will see. **Pre-registered for critic #68:** 제출 자료 returns to 19 when WFG-258 (a) closes with the kit reprinted and the bundle re-pointed; 데이터 수집·분석·해석 reaches 20 when WFG-255 and WFG-259 both close. **Not scored:** the two Pass/Fail rows. |
| 2026-09-11 | `d291364` | 18 | 20 | **20** | 19 | 19 | **96** | **ONE row moves and Track B is UP to 96.** **데이터 수집·분석·해석 19 to 20, the move critic #69 pre-registered, and it is earned rather than paid out.** The criterion is 「데이터 수집 및 분석의 체계성 · 연구의 안정적인 결과 도출 가능성(연구 재현 가능성) · 수학 및 통계 방법의 적절한 적용 · 예상 결과에 대한 논리적인 해석 및 결론」, and WFG-259 met all four halves in one lap. The operation was **pre-registered in the claim commit `031214b` before any number existed** (node space, Euclidean, projected metres, widths 0 / 100 / 500, the two non-zero widths lifted verbatim from prose written before the run), so no definition could be fitted to an answer, and the raster reading was neither run first nor run afterwards to see whether it matched better. Three write-gates passed before anything was written, including a `d = 0` identity control that now runs the **same code path** as every other width instead of short-circuiting to `set(base)`, which is the difference between a control and a tautology. The two figures the prose asserted **reproduce exactly, origin id included**. And the row reported that **the reading everyone took from its own sentence is false**: the dilated arm saves fewer, not more, at both widths. The row's second half came out **not licensed** and was recorded rather than dropped, with `docs/present_perimeter_yeongdeok.md` §7.3 item 5 refusing the direction **in either direction** and a test pinning the refusal so a later lap must delete it first. That is 「예상 결과에 대한 논리적인 해석 및 결론」 at full marks. **20 and not held at 19** because the lap's own independent reviewer blocked it for a mechanism sentence built on two marginals that happened to be equal, and the repair was **structural**: `transition_matrix_from_zero` is now in the artifact with a test asserting every column sums to its width's marginal, so a sentence about where origins came from quotes a registered cell or is not written. ⚠ **One honest subtraction, and it is why the next row is filed at position 1 rather than this one falling:** the same lap named, measured and published the outcome 「the filter refuses to plan for this origin at all」 for the **opponent** arm (`origin_removed_by_filter`), while this project's own arm has the identical branch at `src/wildfireguardian/routing/evacuation.py:468-472`, a `note` that **nothing in the tree reads** (one grep hit, the definition, measured here), and a dispatch-sheet sentence at `src/wildfireguardian/live/pipeline.py:107` asserting a budget was spent and detours were tried when neither happened. `docs/routing_limitations.md` §1 is this exact audit done well on the bucket defined one line above in the same dict, and it was never run on its neighbour; the bucket is **2** on 영덕, **12** on 의성·안동 and **10** on 울진·삼척, so §1's 「the count is 0」 escape does not apply. That is **WFG-262**, filed at position 1. It does **not** cost this row today because nothing in the window made it worse and no committed count is wrong; it is **pre-registered to cost 데이터 수집·분석·해석 one point at critic #72 if it is still open then**. **제출 자료 HOLDS at 19 and does NOT fall.** `README.md:330-331` still defines the surviving un-run claim as covering 「완충거리를 더한 쪽」, which WFG-259 falsified, eleven lines above the addendum at `:339-341` that says so, and the bullet now says 「거기서 나온 수치는 이 README 에 옮겨 적지 않습니다」 twice. That is this lap's one `fix-before-next-row` item. **It is a stale scope clause and a redundancy on a page that states the true scope eleven lines below, not a false claim standing alone**, which is the distinction between this and critic #67's 18: nothing on the printed kit is wrong, The printed kit and the release bundle were re-hashed here rather than read from a report: `WFG_printables_20260911T2137Z.pdf`, **60** pages, seven sources **7 of 7**, bundle **19 of 19**, and the bundle names that kit. R7 and R9 hold. **연구 목적 18, 설계와 방법론 20, 창의성 19, all HELD:** nothing in the window touched the research question, the design, or what the project creates. **Pre-registered for critic #71:** 제출 자료 rises to 20 when `README.md:330-331` names the same subject `:338` names and the duplication is gone; 데이터 수집·분석·해석 is re-examined **downward** at critic #72 if WFG-262 is still open. **Not scored:** the two Pass/Fail rows. |
| 2026-09-12 | `e7ba085` | 18 | 20 | 20 | 19 | 19 | **96** | **NO row moves and Track B HOLDS at 96.** The window closed one P0 row and the honest reading is that it **re-earned** two scores rather than rising past them. ⚠ **설계와 방법론 20, HELD and re-earned rather than inherited, on 「변수의 정의와 제어의 적절성」:** the WFG-262 lap wrote BOTH interpretations of its own row into the claim commit `c83e8d2` **before any number existed** (「if the count is non-zero, the row is right as filed ... if the count is ZERO ... that is the lap's finding and goes on the page in those words」), **imported** the committed origin rule from `run_real_roads_real_hazard_slope.candidate_origins` instead of restating it, and made three identity controls **gating rather than reported**: the script refuses to exit 0 unless `n_nodes` and `n_origins_scanned` re-derive exactly against each committed artifact. There is no 21. ⚠ **데이터 수집·분석·해석 20, HELD on the same terms.** The measurement is a tautology by construction — the origin filter removes the population the count then looks for — and `docs/routing_limitations.md` §6 says so, the claim commit says so, the registry caveat says so and the lap's own reviewer logged it as a `mandela` pattern-4 hit. **A disclosed tautology is not a defect; an undisclosed one would have been.** The answer is **0** on 영덕, 의성·안동 and 울진·삼척 against committed buckets of 2, 12 and 10, and §6 states in its own words what the zero does NOT license, including that it says nothing about a scan called with `departure_min > 0`. Verified here: **no caller in `src/` or `scripts/` passes a non-zero `departure_min`**, so the invariant is intact on every committed path and the disclosure is not hedging. It holds at 20 rather than rising because 20 is the top of the row. ⚠ **제출 자료 19, HELD.** Critic #70's item closed: `README.md`'s Round-4 scope clause now names 「완충거리를 폭까지 훑어 정식 상대로 세운 쪽」, matching `:339`, and the duplicated 「거기서 나온 수치는 이 README 에 옮겨 적지 않습니다」 is down to one copy. Re-hashed in this lap's own process rather than read from a report: the printed kit is `WFG_printables_20260911T2137Z.pdf`, **60** pages, seven sources **7 of 7**, and `release/kcf-finals-2026/MANIFEST.json` hashes **19 of 19** and names that kit. **19 and not 20** because 「사용된 자료에 대한 출처 명기」 is the row, and the sentence printed on 17 of the 33 committed A4 vehicle sheets cites nothing for what it asserts (**WFG-264**). **연구 목적 18 and 창의성 19, HELD:** nothing in the window touched either. ⚠ **Pre-registered for critic #72:** 제출 자료 rises to **20** when WFG-264 (a) and (b) land — the 439-series condition read out of the code and written down, with the kit reprinted and the bundle re-pointed if a card changes — and **not** on a report that says it was. **Not scored:** the two Pass/Fail rows. |
| 2026-09-12 | `cd18782` | 18 | 20 | 20 | 19 | **18** | **95** | **ONE row moves and Track B is DOWN to 95, and the rubric names the defect in its own words.** ⚠⚠ **제울 자료 19 to 18, on 「그래픽 및 범례의 명확성」, which is the second of that criterion's three named clauses (`docs/auto/RUBRIC.md:47`).** The window's own commit `63e9d20` rewrote the F8 caption in `paper/manuscript.md` from 「2 with no safe walking route」 to 「2 reaching no refuge」 and left `paper/make_figures.py:689`, the legend of that same figure, reading 「origin: no safe walking route」. One grep of that file for 「no safe walking route」 returns **three** hits and the lap changed **one** (`:244`, F5b, correctly under a new filename). `:134` (F3_regions) is the third. Neither PNG was regenerated: `git diff e7ba085..cd18782 -- paper/figures/` lists `F5b_decision_shift.png` alone and both files still carry `a37cfb0`. A caption and its own legend asserting different things is not a latent risk, it is the exact clause being scored, and it was **created** by this window. WFG-266. ⚠ **설계와 방법로 20, HELD and re-earned rather than inherited**, on 「변수의 정의와 제어의 적절성」. The WFG-264 lap refused to register a docstring as a measurement, read three return sites off `rescue.py` and `evacuation.py` instead, reproduced each of the sentence's three assertions false on a constructed clock-free field, re-derived `len(unreachable_homes)` against **both** `four_way_counts` and `responder_exposure` on each arm, and **read the 12-minute threshold out of each artifact's own `provenance.assumed`** rather than typing it in. That last choice is the difference between a property of the run and a parameter of the measurement. ⚠ **데이타 수집·분석·해석 20, HELD, and what earns it is a retraction rather than a result.** The lap's own draft discriminator claimed a `null` `best_closing_window_min` could only mean 「no drive path from any depot」; `+inf` serialises identically, and an over-budget reproduction written for another purpose failed the assertion and killed it **before §7 shipped**. The published claim is the weaker true one (a zero rules out both infinities together and neither separately), the key was renamed `n_no_finite_best_closing_window` for what it measures, and the error is written into §7 rather than quietly replaced. Eight `vus_` keys carry a caveat naming four facts that travel together. **연구 목적 18 and 장의성 19, HELD.** Nothing in the window touched the purpose, and the creative object of the window is an audit rather than a design. ⚠ **Pre-registered for critic #73: 제울 자료 returns to 19 if WFG-266 closes with F8 and F3 redrawn under new filenames and the manuscript pointing at them**, and falls to **17** if another window ships a third instance of the same grep. |
| 2026-09-11 | `b6778e7` | 18 | 20 | 19 | 19 | **19** | **95** | **One row moves and Track B is UP to 95, on critic #68's own pre-registered condition, which fires.** ⚠⚠ **제출 자료 18 to 19 (WFG-258 closed).** The condition critic #67 wrote for this lap was exact: 「제출 자료 returns to 19 when WFG-258 (a) closes **with the kit reprinted and the bundle re-pointed**, not when the tree is fixed and not on a report that says it was」. All three halves are done and I re-measured the paper rather than reading it: `release/kcf-finals-2026/MANIFEST.json` hashes **19 of 19** against its `source` paths and names `WFG_printables_20260911T1627Z.pdf`, whose seven `SOURCES` documents hash **7 of 7** against the tree, 59 pages, 25 of them the Q&A bank. The four lines that told a judge the fair opponent had never been run on 영덕 now say it has and name the file; `paper/manuscript.md` lost the `[GAP:` marker `paper/GAPS.md` already called closed; and `tests/test_future_aware_attribution.py:16-18` no longer instructs the next lap to write the false clause back. **19 and not 20, and the reason is this lap's one finding:** in closing it the loop wrote its own governance onto the judged surfaces. `docs/auto/JUDGE_QA.md:1017`, the sentence prescribed for all five judges beside the headline 42, now ends 「부스에서 말해도 되는지를 저희가 아직 정하지 않아서 오늘은 말씀드리지 않겠습니다」, the same clause is in Q19's spoken draft at `:956`, and `README.md:37` and `:335-336` print the entry id **NH-059** on the front door. That is a 「자료의 논리적 구성」 defect: the front door explains itself with an internal ledger, and the withholding is announced while the counts sit one click away in `docs/present_perimeter_yeongdeok.md` §4, which the TL;DR now links. ⚠ **데이터 수집·분석·해석 19, HELD and not raised.** WFG-255's condition for 20 is unmet (the component geometry is still measured nowhere), and WFG-260 argues against 20 from the other side: the page's §5 item 6 words its own input so a reviewer would read the fair opponent as model-fed. It does not fall to 18 because the underlying design is **right**, and this lap proved it from the committed array rather than asserting it: `haz_stack[0]` is strictly binary `{0.0, 1.0}` while `haz_stack[1]` holds 3,961 distinct values, `haz_stack[0] >= 0.5` and `obs_stack[0] > 0` are both **249** cells and the two sets are identical cell for cell, `scripts/build_canonical_hazard.py:88` seeds the simulation from `snaps[0].cumulative_mask`, and at `t_min = 0.0` `src/wildfireguardian/routing/hazard.py:97-100` collapses the time bracket to `i0 == i1 == 0`. So slice 0 is the observation, the opponent's planning side is model-free, and 「needs no model at all」 is true and unstated. **연구 목적 18, 설계와 방법론 20, 창의성 19, all HELD:** nothing in the window touched the research purpose, the method or what is new. **Pre-registered for critic #69:** 제출 자료 returns to 20 when no judge-facing surface names an NH entry or the state of a decision, re-measured by grep over README, the bank, the script, `docs/auto/finals/` and `web/finals.html`, and the kit reprinted; 데이터 수집·분석·해석 rises to 20 when WFG-260 and WFG-259 both close with registered keys that re-derive. **Not scored:** the two Pass/Fail rows. |
## Track A — 애플리케이션 / 실생활 도구

| date | window head | 개발 목적 | 설계와 방법론 | 구현 및 유용성 | 창의성 | 제출 자료 | /100 | what moved |
|---|---|---:|---:|---:|---:|---:|---:|---|
| 2026-09-05 | `f118bfe` | 16 | 15 | 17 | 15 | 14 | **77** | **Every row held, and the flat line is the finding rather than a note.** Track A scores the thing a judge is handed at the booth, and **nothing in this window reached one**: `JUDGE_QA.md`, `DEMO_SCRIPT_5MIN.md`, `web/finals.html`, `scripts/finals.template.html`, `docs/finals_screen_v2.md`, `docs/auto/finals/BOOTH_SETUP.md` and `README.md` all return zero hits for the fair-opponent arm, grepped here. That is **correct behaviour** — the parked lap's own escalation says no judge-facing surface should carry either margin until the author picks an opponent (NH-032) — but a judge scores the tree, not the reason. **구현 및 유용성 17 held for a fourth window:** `KCF_READINESS.md` is 4 of 11 for a seventh consecutive critic lap, `docs/auto/finals/` has held no PDF for seven days, and `make finals-bundle` still rebuilds byte-identically at 17 files around contents that do not include the printables the line names. **제출 자료 14 held** on the same two cancelling moves as Track B, plus one that belongs only here: the screen's 검증 레지스트리 card prints `n_entries` 326 against a registry holding 383, so the student's own instruction to 「point at the card」 now lands on a stale number (WFG-113, live instance) |
| 2026-09-03 | `1113388` | 15 | 15 | 11 | 15 | 11 | **67** | baseline (first critic lap) |
| 2026-09-03 | `0ff1b36` | 15 | 15 | 11 | 15 | 12 | **68** | +1 제출 자료 only: the survey card gives Q17 a file to point at. 구현 및 유용성 does not move, because nothing in this window touched the screen, the printables or the bundle (R1, R2, R7, R9 all still ☐) |
| 2026-09-03 | `8d1decf` | 15 | 15 | 11 | 15 | 12 | **68** | **held, every row.** Track A's judges score a working tool at a booth. This window added tests to a script that runs offline in the sandbox and never appears on the screen. R1, R2, R7, R9 are still ☐, and R2 is now blocked on WFG-021's stranded status rather than on work (F15). Said explicitly so 545 test lines are not read as progress on the thing these judges watch |
| 2026-09-04 | `12b8ac7` | 14 | 15 | 11 | 15 | 12 | **67** | **-1 개발 목적**, the same paragraph, and these judges read it at the booth with a phone in hand. 구현 및 유용성 **11, held for the fifth consecutive window**: no commit in this window touched `web/`, the printables or the release bundle, and R1, R2, R7 and R9 are all still ☐. WFG-017 is now the single most overdue thing in the sprint plan |
| 2026-09-04 | `5a0466e` | 15 | 15 | 11 | 15 | 13 | **69** | **+1 개발 목적 (back to 15), +1 제출 자료 (12 to 13).** These judges read the opening paragraph at the booth with a phone in hand, and the phone now agrees with the paragraph. 제출 자료 rises on the sources table gaining a URL per row. **구현 및 유용성 11, held for the sixth consecutive window.** No commit in this window touched `web/`, the printables or the release bundle; R1, R2, R4, R7 and R9 are all still ☐ with eleven days left in the sprint. Three windows ago this was a note, two ago a risk. It is now the finding that decides the score: the artifact five judges watch for five minutes still does not exist in its finals form, and WFG-017 has been named the next row by two critic laps running |
| 2026-09-04 | `b855943` | 16 | 15 | 11 | 15 | 13 | **70** | **+1 개발 목적 (15 to 16)**, the same deletion, read at the booth. **구현 및 유용성 11, held for the SEVENTH consecutive window.** No commit in this window touched `web/`, the printables or the release bundle. `docs/auto/KCF_READINESS.md` has 2 of its 11 lines ticked (R5, R6) with eleven days of sprint left, and R1, R2, R4, R7, R9 and R11 are all still ☐. 제출 자료 13 held: `references.bib` gained five entries whose notes are the best sourcing work in this repository (one of them re-opens its own URL and corrects a paraphrase the previous lap wrote), and against that the 사망 26명 attribution disagreement is now in three documents instead of two |
| 2026-09-04 | `8e0a6ad` | 16 | 15 | 11 | 15 | 13 | **70** | **Every row held.** 제출 자료 does **not** take Track B's +1: the figure restyle is manuscript work and a manuscript is not what these judges score, which is the same reading critic #2 used. **구현 및 유용성 11, eighth consecutive window** — and this lap owes the table a correction rather than another adjective. Three critic laps called this row's backlog item 「the single most overdue thing in the sprint」 and 「the largest single risk in the sprint plan」. Against the sprint plan at the top of `docs/auto/BACKLOG.md`, **WFG-017 is due 09-07 and today is 09-04**, and every row the plan dates 09-04 (WFG-002, WFG-004) and 09-05 (WFG-020, WFG-021) is `done`. The loop is **ahead of its own schedule**, and 「seventh/eighth consecutive window」 counts critic windows, not missed dates. What is true, and is enough: `web/` has not been touched since `25f1e14` on 2026-09-02, so the artifact five judges watch for five minutes has **never once been exercised** by this loop, and the cost of the first attempt is therefore unknown with 11 days left. That is a risk statement, not a slippage statement, and this table should have said so three windows ago |
| 2026-09-04 | `12bf2d9` | 16 | 15 | **14** | 15 | 13 | **73** | **구현 및 유용성 11 to 14, the first move on this row in nine windows, and the largest single move this table has recorded.** `web/finals.html` had not been opened by this loop since 2026-09-02. It now carries five evidence cards (운영점, 탐지 바닥, 240분 지평, 대피 지점 배치, 제출본과 정본), `build_finals.py --verify` runs three gates at build time and prints their exit codes on the RELIABILITY tab, 26 tests bind every card value to the artifact it came from, nine screenshots are committed as the lap's record of looking at what it shipped, and `docs/finals_screen_v2.md` says for each card what it does **not** say. `KCF_READINESS.md` R2 ticks today. Why 14 and not 17: R1 (a committed mapping of every on-screen number to a registry key), R4 (the 5-minute script), R7 (printables) and R9 (the bundle) are all still ☐, no one has opened this file on the booth laptop (R12, NH-014), the automated smoke is WFG-009, and the panel a judge is invited to verify the build with prints a commit that does not exist (F41). These judges score a working tool at a booth; there is now a tool, and it has not yet been to a booth. 제출 자료 13 held: the figure and citation work is manuscript work these judges do not read, and what they **do** read, the screen's own 출처 line, resolves to nothing. 개발 목적 16, 설계와 방법론 15, 창의성 15 held |
| 2026-09-07 | `1bca8ed` | 16 | 18 | 20 | 15 | **17** | **86** | **One row moves, 제출 자료 16 to 17, and it is precisely the point critic #37 withheld and pre-registered.** That lap wrote 「Why 16 and not 17: four lines above the repair, on the same printed page, `RELATED_WORK_PANEL.md:32` states in bold that the NIFoS console's 발화점은 「운영자가 손으로 입력」한다」. **That sentence is gone**, and I read the replacement rather than grepping for it: the panel now prints the catalogue's eight chapter names and says the flow 「읽힙니다」, the Q16a card carries the same register, the panel gained a dated 정정 note, and the whole thing is registered as **WC-009** with the superseded wording kept in the registry instead of reprinted on paper. The kit was rebuilt in the same lap (`WFG_printables_20260907T2149Z.pdf`, manifest `manifest_20260907T2149Z.json`) and `release/kcf-finals-2026/MANIFEST.json` re-pointed, which is the condition WFG-152 exists to enforce. On this track the kit is the object handed over, so the reprint is what earns the point. **Why 17 and not 18:** `JUDGE_QA.md` Q28 still tells the student 「그 입력을 필요로 하는 테스트는 이유를 적은 채로 건너뜁니다」, which is false, and the correction lives in Q40 **211 lines below it**; both are printed in the same kit, and a student reading in order meets the false card first (WFG-139). **구현 및 유용성 20, held, and the reason it is not docked is stated rather than assumed.** `gates.py --mode full` exits **0**, ALL GREEN, COLD, on the routine's default shallow clone at this head; `auto-gates` runs **204 to 223** are 19 `success`, 1 `cancelled`, **zero `failure`**, and run 223 at this exact head is `success`; every number on the screen still maps to a registry key. What the screen has is a **liveness** defect and not a correctness one: `web/finals.html:434` names `7308b06`, now **30** commits behind `HEAD` against a limit of 30 (`tests/test_finals_screen.py:540`, `:732`), so it is green today and red on the next push. That is this lap's one `fix-before-next-row` item (**WFG-173**), which is the right instrument for it; nothing a judge sees is wrong today, so it is not a deduction. **개발 목적 16, 설계와 방법론 18, 창의성 15, held:** nothing in the window changed what the tool is for, what it is different from, or how it is built. ⚠ Not scored: the two Pass/Fail rows, as above. |

| 2026-09-04 | `ce31b91` | 16 | 15 | 14 | 15 | 13 | **73** | **Every row held, and 제출 자료 is the one this lap argued with itself about.** Track B takes +1 for the Q&A bank no longer contradicting itself at T0. These judges do not read `docs/auto/JUDGE_QA.md`; they hear the student, for five minutes, at a booth. What changed this window is a **document** the student recites from, not a rehearsal: `docs/auto/DEMO_SCRIPT_5MIN.md` still does not exist (R4 ☐, WFG-003), `docs/auto/finals/BOOTH_SETUP.md` still does not exist (R3's booth half, WFG-037), `release/kcf-finals-2026/` still does not exist (R9 ☐, WFG-036), and no one has opened the screen on the booth laptop (R12, NH-014). Three of eleven `KCF_READINESS.md` lines are ticked with eleven days of sprint left. The same discipline critic #2 and critic #8 used to refuse Track A the manuscript's points refuses it this one, and the refusal should be revisited the moment a script exists. **구현 및 유용성 14, held:** no commit in this window touched `web/`, and the defect that capped it at 14 last window is unchanged — the SYSTEM INTEGRITY panel still prints `commit a562045`, and `git cat-file -t a562045` in this fresh clone still answers `fatal: Not a valid object name` (WFG-067, P0, second window). The one improvement these judges would feel is real and is priced into the hold rather than a rise: the sentence the student will say when a judge asks about the trigger now matches the screen behind them, which it did not two windows ago. 개발 목적 16, 설계와 방법론 15, 창의성 15 held |
| 2026-09-04 | `3a70e16` | 16 | 15 | 14 | 15 | 13 | **73** | **Every row held, and the hold on 구현 및 유용성 is the eleventh consecutive window without a commit to `web/`.** Nothing in this window touched the screen, the printables or the bundle. `docs/auto/DEMO_SCRIPT_5MIN.md` still does not exist (R4 ☐, WFG-003, which has been the next `todo` row in table order for two windows), `docs/auto/finals/BOOTH_SETUP.md` still does not exist (R3's booth half, WFG-037), `release/kcf-finals-2026/` still does not exist (R9 ☐, WFG-036, plan date 09-10, six days out). **Three of eleven `KCF_READINESS.md` lines are ticked** (R2, R5, R6) with eleven days of sprint left. The defect capping this row is unchanged and is now in its **third** window: `git cat-file -t a562045` in this fresh clone still answers `fatal: Not a valid object name`, and `web/finals.html` still carries `"git":"a562045"` in the panel whose whole job is to let a judge verify the build (WFG-067, P0). These judges score a working tool at a booth; the tool exists, has still never been opened on the booth laptop (R12, NH-014), and the one line on it that invites verification resolves to nothing. 제출 자료 13 held: what moved this window is a Korean design document and a test file, and these judges read neither. 개발 목적 16, 설계와 방법론 15, 창의성 15 held |
| 2026-09-04 | `83f49bc` | 16 | 15 | 14 | 15 | 13 | **73** | **Every row held, and this table owes the loop a correction it has been getting wrong for four windows.** 구현 및 유용성 14 holds for the twelfth consecutive window without a commit to `web/`, and three of eleven `KCF_READINESS.md` lines are still ticked. Previous critic laps called that slippage. It is not: **NH-021 closed on 2026-09-04 with the author choosing 「Do WFG-062 now (the withdrawn-claims registry gate first; booth rows resume after)」**, so WFG-003, WFG-037 and WFG-036 are behind a gate row **by the author's own decision**, taken against exactly this trade-off written out for them. This table should score the consequence and stop calling it overdue. The consequence is real and unchanged: `DEMO_SCRIPT_5MIN.md` (R4), `finals/BOOTH_SETUP.md` (R3 booth half) and `release/kcf-finals-2026/` (R9) do not exist, nobody has opened the screen on the booth laptop (R12, NH-014), and the one line on the screen inviting a judge to verify the build still prints a commit that does not resolve (WFG-067, **fourth** window on a ☑ line). 제출 자료 13 held: F8 is manuscript work and these judges do not read the manuscript, the same discipline critics #2, #8 and #9 applied. The window's data defect (F54) does not move this row either, because nothing on the screen or in the printables consumes it — but it would have, through WFG-073 and WFG-074, which are now blocked. 개발 목적 16, 설계와 방법론 15, 창의성 15 held |
| 2026-09-04 | `c65dc56` | 16 | 15 | **15** | 15 | 13 | **74** | **구현 및 유용성 14 to 15, the second move on this row in fourteen windows, and it is one defect closed rather than one capability added.** WFG-067 is done: `web/finals.html` carries `"git":"d5e2562"` and `git merge-base --is-ancestor d5e2562 HEAD` succeeds in this fresh clone, so the one line on the screen that invites a judge to verify the build now resolves. The gate asserts **reachability from HEAD** rather than existence, which is the right instrument and is better than the row asked for, because `git cat-file -e` passes for a rebased-away object and would have stayed green on the machine that made the defect. That defect had stood for four consecutive critic windows on a ☑ line. Why 15 and not more: nothing in this window added anything to the screen, and the definition of done has not moved. **Three of eleven `KCF_READINESS.md` lines are ticked** (R2, R5, R6). `docs/auto/DEMO_SCRIPT_5MIN.md` does not exist (R4), `docs/auto/finals/BOOTH_SETUP.md` does not exist (R3 booth half), `release/kcf-finals-2026/` does not exist (R9), the printables do not exist (R7), and nobody has opened the screen on the booth laptop (R12, NH-014). Critic #11 corrected this table for calling that slippage, and the correction stands: the booth rows are behind WFG-062 by the author's decision in NH-021. What this lap adds to that correction is the part that is nobody's decision: **WFG-062 is still `todo` too**, 22 commits and 14 reports after the author chose it, so the loop holds neither the gate it deferred the booth for nor the booth. **제출 자료 13 held**, and Q35 is why it did not rise with the stamp. These judges do not read the registry; they hear the student for five minutes. The bank still drills the student to volunteer a fault that no longer exists, about a commit id that is not on the screen behind them (WFG-081). The point WFG-067's fix earned on the screen is spent on the document that tells the student what to say about it. **개발 목적 16, 설계와 방법론 15, 창의성 15 held.** |
| 2026-09-04 | `baf6962` | 16 | 15 | 15 | 15 | **14** | **75** | **+1 제출 자료 (13 to 14), the first move on this row since `5a0466e`, and it is the same Q&A repair scored on the surface that matters most for Track A.** Track A's 자료 row has sat at 13 through fourteen windows while Track B's climbed 11 to 18, and the gap was never really about the registry - it was that the document the student actually speaks from was drifting from the tree behind it. Both drifts closed here. The bank now counts itself correctly (41 questions, 15/19/7, verified by counting the file), so the four drill rounds a student is told to rehearse finally name every question that exists; before this lap **Q10d was T0 and was in no drill round at all**, and its whole purpose is to stop the student re-stating a withdrawn ordering claim (WFG-053). And Q35 stopped drilling the student to volunteer a fault that had been fixed nine hours earlier about a commit id no longer on the screen behind them. For five judges at five minutes each, a rehearsal script that matches the screen is worth more than any registry key. **Why 14 and not 15:** `docs/juso_yeongdeok.md` still contradicts itself twice inside one file (WFG-079), and the printables that this row ultimately needs do not exist (R7). **구현 및 유용성 15 held: nothing in this window touched `web/`.** Three of eleven `KCF_READINESS.md` lines are ticked (R2, R5, R6) and **no line has been ticked for four consecutive critic laps** - the last was R2 by critic #8 at `12bf2d9` (0750Z), and critic #12 did not write the file at all. `docs/auto/DEMO_SCRIPT_5MIN.md` (R4), `docs/auto/finals/BOOTH_SETUP.md` (R3 booth half) and `release/kcf-finals-2026/` (R9) are all still absent on disk, checked this lap. **개발 목적 16, 설계와 방법론 15, 창의성 15 held** |
| 2026-09-04 | `ed35f0d` | 16 | 15 | 15 | 15 | **13** | **74** | **-1 제출 자료 (14 to 13), the same Q18 defect scored on the booth-materials row, and 구현 및 유용성 holds 15 for a TWELFTH window with no commit to `web/`.** A Track A judge scores what the tool does and what the booth shows. What the tool does did not change in this window: no code outside `scripts/check_withdrawn_claims.py` and its tests, nothing in `src/`, nothing in `web/`. What the booth shows got one point worse, on the one document the student is drilled from. The three artifacts this row would actually move — `docs/auto/DEMO_SCRIPT_5MIN.md`, `docs/auto/finals/BOOTH_SETUP.md`, `release/kcf-finals-2026/` — do not exist at `ed35f0d`, checked on disk. WFG-062's gate is real engineering and a Track A judge will never see it |
| 2026-09-05 | `43710f7` | 16 | 15 | **16** | 15 | **14** | **76** | **+1 구현 및 유용성 (15 to 16), the first move on this row that is about the product being usable rather than a defect being closed, and it is not a commit to `web/`.** This row asks 「구현된 작품의 완성도」 and 「해결하고자 하는 문제의 해소 가능성」, and for fourteen windows the honest answer was that a good screen existed which nobody could drive. The screen has not changed in thirteen windows and did not change here either. What changed is that there is now a written way to run it: `docs/auto/DEMO_SCRIPT_5MIN.md` §0 gives the twenty seconds between judges (press **G**, check KO, check the default region is 의성·안동 2025, which I verified is `scripts/build_finals.py` `default_region`), §1 gives the five minutes, and §2 gives the sentence to hand back to each of the five judges who interrupt. A judged booth is five performances, not one, and this is the first artifact that treats it that way. **+1 제출 자료 (13 to 14), returning the point critic #14 took off for the stale Q18 answer**, which this window fixed at `0aac8fc`/`96f8ff5` with four numbered lines and the withdrawn sentence kept above them as a record. **Held below 15 for the same reason Track B is held below 19:** WFG-095, and the fact that `release/kcf-finals-2026/`, `BOOTH_SETUP.md` and the printables still do not exist, which is what 완성도 will mean to a judge holding nothing. **개발 목적 16, 설계와 방법론 15, 창의성 15, held.** |
| 2026-09-05 | `c37f27e` | 16 | 15 | **17** | 15 | 14 | **77** | **+1 구현 및 유용성 (16 to 17): for the first time there is a thing to hand a judge, and I assembled it myself instead of reading that it exists.** `make finals-bundle` in this fresh cloud sandbox exits 0 with `OK — release/kcf-finals-2026/ rebuilt byte-identically, 16 files`. The folder holds the four offline screens with their fonts and poster, `LICENSE`, `CITATION.cff`, a `MANIFEST.json` of SHA-256s, and a ten-step Korean `README_KO.md` written for the booth day rather than for a developer - copy to the desktop, turn Wi-Fi **off**, double-click, check KO and 의성·안동, press **G** between judges, and step 9 is 「화면이 뜨지 않으면 두 번째 USB를 씁니다」. A judge scoring 완성도 is scoring whether the thing survives contact with a room, and a hash manifest that names the corrupted file is that kind of engineering. `docs/finals_bundle.md` also carries the best paragraph the loop wrote this window: a withdrawn reason, kept and labelled, where the first version had claimed a gate behaviour that a reviewer checked and found false. **Why 17 and not 18:** the folder does not exist in a clean clone until the command is run, which is the right trade and is stated plainly in both documents but is still a step between a judge and the artifact; there are no printables (R7); and `docs/auto/finals/BOOTH_SETUP.md` does not exist, so nobody has written what to do when the laptop dies. **제출 자료 14, held, and the reason is the one thing in this window I would fix before anything else.** The document the student actually speaks from is budgeted, not measured: 1,630 spoken syllables across six segments declared at 300 s is 5.43 per second sustained, and the implied rate runs 4.24 (3막, 75 s) to 7.07 (마무리, 45 s). The segment that must be spoken fastest is the limitations close, it is last, and 심사개요 says a talk that runs long may be stopped. A row scored on 논리적 구성 does not rise while the document's own time budget cannot be true at both ends (WFG-100). WFG-095 closed cleanly and I re-checked it: every **[버림]** marker now takes its own number with it, and §1's rule was rewritten rather than the two markers patched. **개발 목적 16, 설계와 방법론 15, 창의성 15, held.** |
| 2026-09-05 | `26e200d` | 16 | 15 | 17 | 15 | **15** | **78** | **+1 제출 자료 (14 to 15), the same WFG-100 closure scored on the row that is about what the student holds and says.** Critic #16 capped this row at 14 with 「the one thing in this window I would fix before anything else」, and it named the unmeasured five minutes. It is measured now, and the part a Track A judge would feel is not the syllable count but §1's new paragraph and §5's two new bullets: the script now tells the student that the six numbers are counted rather than designed, that **adding or removing a spoken sentence turns `tests/test_demo_script_pace.py` red on purpose**, and that if 3막 needs to be slower the move is to cut 3막's sentences and not to borrow seconds from another segment. A booth document that explains its own failure mode to the person operating it is 완성도. **Why 15 and not 16, and it is the same sentence for both tracks:** WFG-103. The 60 seconds this row's document calls 「이 작품의 전부」 describes the fire-blind arm as a map that sees the fire now. A judge who knows what `naive` means hears the strongest sentence of the demo overstate its own comparison, and no printable or bundle recovers that. **구현 및 유용성 17, held.** Nothing in this window touched `web/` (unchanged since `deeb147`, content since `dc63a06`), there are still no printables under `docs/auto/finals/` (R7), and `docs/auto/finals/BOOTH_SETUP.md` still does not exist (R3 booth half), all checked on disk at this head. The bundle held its 17 on its own merits and I re-earned it rather than inheriting it: `make finals-bundle` in this fresh sandbox exits 0 with `OK — release/kcf-finals-2026/ rebuilt byte-identically, 16 files`. **개발 목적 16, 설계와 방법론 15, 창의성 15, held.** |
| 2026-09-05 | `6afd252` | 16 | 15 | 17 | 15 | 15 | **78** | **Every row held.** **개발 목적 16** and **설계와 방법론 15:** unchanged, nothing in the window touches either. **구현 및 유용성 17:** it does not fall — nothing regressed, `gates.py --mode full` is ALL GREEN at this head (1,484 / 62, cold) and `make finals-bundle` rebuilds byte-identically, re-run in this sandbox. It does not rise because the definition of done did not move for a **third consecutive window**: `docs/auto/finals/BOOTH_SETUP.md` and every printable are still absent, and R9 waits on exactly those two. Six days of sprint have passed since WFG-037's target date with the row never once claimed. **창의성 15:** unchanged. **제출 자료 15:** same reasoning as Track B — the correction landed on two surfaces and left them saying different things about one object |
| 2026-09-05 | `92bfc4f` | 16 | 15 | **18** | 15 | 15 | **79** | **구현 및 유용성 17 to 18, the first movement on any row in three laps, and it is the booth.** For it, all checked on disk at this head rather than read from the lap: `docs/auto/finals/BOOTH_SETUP.md` exists with a ten-minute setup sequence, a five-key table whose two code citations (`web/finals.html:2145`, `:2154`) resolve exactly, and a four-step failure ladder; `scripts/check_bundle_copy.py` answers the question `make finals-bundle` cannot (is **this copy** intact) using only the standard library and travelling inside the bundle, so it runs on a borrowed machine with no repository; `make finals-bundle` exits 0 at 17 files and the checker agrees. Why +1 and not +2: step 3 of the failure ladder is paper and there is no paper (R7 / WFG-007), `make all-checks` cannot be run at the booth or anywhere (NH-029), and `make finals` would put a withdrawn sentence back on the judged screen (WFG-109). **개발 목적 16, 설계와 방법론 15, 창의성 15, 제출 자료 15**, all unmoved: nothing this window reached a submitted document, and the printables that would are still unclaimed. |
| 2026-09-05 | `ce262fe` | 16 | 15 | **17** | 15 | 15 | **78** | **구현 및 유용성 18 to 17, and the deduction and the credit are the same file.** Credit first, checked by mutation rather than read: WFG-109 closed with `tests/test_finals_template_sync.py`, and when I edited the corrected STATIC VIEW caption in `scripts/finals.template.html` back to the withdrawn wording, 2 of 9 went red. The invariant it asserts — the built screen is the template line-for-line outside the one payload line — is stronger than the caption comparison the row asked for, and it covers `web/console.html` too. The deduction: on the same screen, in the evidence card a judge reads for reproducibility, `41498ef` is printed as 「built at commit」 and is not reachable from this branch (WFG-115). Track A judges score 작품 완성도 on what is on the screen at the booth, and something on the screen is wrong today, which was not true a lap ago. I also reproduced WFG-113 independently — `"n_entries": 326` changed to `999` on `web/finals.html:434`, then `test_finals_template_sync.py` and `test_finals_screen.py` 41 passed, `check_forbidden.py` OK, `verify_numbers.py` OK — so a hand-typed judged number is caught by nothing; that is filed, not double-counted here. **개발 목적 16, 설계와 방법론 15, 창의성 15, 제출 자료 15, all held.** Nothing this window reached a submitted document, and R7's printables are still unclaimed on their **sixth** day at P1; this lap raised them to P0 (WFG-007). |
| 2026-09-05 | `492364c` | 16 | 15 | 17 | 15 | **14** | **77** | **제출 자료 15 to 14, the same WFG-117 defect scored on the row that is about what the student holds and says, and 구현 및 유용성 17 held.** For Track A the Q30 defect is worse than for Track B, because these five judges never open `docs/NUMBERS.json`: they hear the student for five minutes and they look at the screen. On a T0 question those two sources now disagree by 31 entries, and the student is the one who would be caught. **구현 및 유용성 17, held and re-earned rather than inherited:** `make finals-bundle` exits 0 in this fresh sandbox with `OK — release/kcf-finals-2026/ rebuilt byte-identically, 17 files`, `gates.py --mode full` is ALL GREEN at this head (1,515 / 62, cold, identical to critic #20's cold run, which is what a one-line window should produce), and `make baseline-verify` now reports **2** differences, both the laptop-only raw manifests, so the author's NH-029 re-freeze holds. It does not rise because the definition of done did not move for a **sixth** consecutive window: `docs/auto/finals/` holds no PDF (R7 / WFG-007, P0, sixth day, never claimed) and WFG-115's unreachable stamp is still on the judged screen — re-tested here on a **deepened** clone after finding this sandbox is a depth-50 shallow one, and it survives (WFG-119). **개발 목적 16, 설계와 방법론 15, 창의성 15, all held.** Nothing this window reached a submitted document, because nothing this window reached anything |
| 2026-09-06 | `de7bd0a` | 16 | 15 | 17 | 15 | 14 | **77** | **Every row held, and 제출 자료 14 is three real movements that cancel — say so rather than call the window quiet.** Up one: the document the student actually speaks from stopped telling them to undersell their own work. Until `a182cc0` the 3막 note had the student say 「지금 불만 피하는 경로와의 비교는 아직 돌리지 않았습니다」 and 「그 카드는 아직 없습니다」, and both had been false since 2026-09-05 — the comparison ran (WFG-114) and the card is `JUDGE_QA.md` Q19. For five judges at five minutes each, a rehearsal document that makes the student confess a limitation the project already closed is the most expensive kind of error, and it is fixed with a gate behind it (`tests/test_fair_opponent_line.py`, 10 tests, and its margin guard is scoped to the whole file after a reviewer defeated the spoken-lines-only version in one mutation). Down one: the sentence that replaced it, 「어느 폭이 맞는지는 그날 알 수 없고」, is stronger than the five-point sweep behind it can support, and a judge breaks it with one question — 「몇 가지 폭을 재보셨습니까?」 (WFG-127, and Q37 is now in the bank marked 근거 없음). Still down, unchanged: WFG-117's three-way count on a T0 question, which is what took this row from 15 to 14 at `492364c` and has not moved. **구현 및 유용성 17, held and re-earned rather than inherited:** `make finals-bundle` exits 0 in this fresh sandbox with `OK — release/kcf-finals-2026/ rebuilt byte-identically, 17 files`, and `gates.py --mode full` is ALL GREEN at this head (1,545 / 62, cold, against critic #22's cold 1,535 / 62, like for like). It does not rise, for the **eighth** consecutive window, because the definition of done did not move: `docs/auto/finals/` holds no PDF (R7, day eight), `web/finals.html` still prints `built at commit 41498ef` which `merge-base --is-ancestor` refuses on the deepened clone (WFG-115), and the same screen still prints `n_entries":326` where the registry now holds **383**, counted here (WFG-113, WFG-117). One screen rebuild closes all three and no lap has done it in fourteen windows. **개발 목적 16, 설계와 방법론 15, 창의성 15, held.** |
| 2026-09-06 | `91d3e05` | 16 | 15 | 17 | 15 | 14 | **77** | **Every row held, and the flat window is correct rather than lazy: the only substantive commit in it is a manuscript lap, and these five judges do not read the manuscript** (the discipline critics #2, #8 and #16 applied on exactly this split). **구현 및 유용성 17, ninth consecutive window, re-earned rather than inherited:** `gates.py --mode full` is **ALL GREEN** at this head in this fresh sandbox (`1545 passed, 62 skipped`, cold, 179.9 s), `baseline-verify` WARN is the documented NH-029 behaviour. It does not rise because the definition of done did not move for the ninth time: `find docs/auto -name '*.pdf'` returns nothing on **day nine** of R7; `web/finals.html` prints `n_entries` **326** where `docs/NUMBERS.json` holds **383** and `n_reproducible` **268** against **325**, counted here in one process; and the same card's 「built at commit 41498ef」 is still not an ancestor of `HEAD`, re-tested on a clone deepened to **250** commits so the shallow boundary is not the confounder. It does not fall, because nothing regressed and the two-command repair for two of those three is now written into WFG-113 with the evidence that this loop has already run it. **제출 자료 14, held at the floor critic #21 set.** WFG-117's three-way answer to a **T0** question has not moved, and this window added a fourth surface to the same family rather than closing one: `docs/multi_region.md:191`, which `README.md:113` sends the reader to, states the project's single self-damaging bucket in a form the project's own measurement contradicts (WFG-128). It does not fall further because nothing this window made the booth worse and the bank gained **Q38**, which gives the student the correct answer and the two sentences not to say. **개발 목적 16, 설계와 방법론 15, 창의성 15, held** — nothing this window reached a booth surface at all. |
| 2026-09-06 | `b70e464` | 16 | 15 | 17 | 15 | **15** | **78** | **제출 자료 14 to 15, and it is the first movement on this row in four windows.** These five judges see a booth, and until `3e92b69` the booth had no paper. It now has `docs/auto/finals/printables/WFG_printables_20260906T0620Z.pdf`: 29 A4 pages in the screens' own IBM Plex Sans KR, built by `make printables` from four committed Markdown documents with no new dependency, byte-identical on rebuild, and a manifest carrying the sha256 of every source and every font — I re-hashed all seven against the tree and all seven are current, so the paper a judge would hold is provably the repository's own current version. `docs/printables.md` states the method and what the PDF does **not** show. **+1 and not +2**, for two reasons on the same surface: R7 enumerates five printables and this PDF contains one of them, and the manifest declares a committed artifact nonexistent (WFG-130). **구현 및 유용성 17, tenth consecutive window, and it is held down by one screen.** `web/finals.html:434` prints `n_entries` 326 / `n_reproducible` 268 where `docs/NUMBERS.json` holds 383 / 325 (counted here in one process), and `registry.built_at_commit` `41498ef` is still not an ancestor of `HEAD` on a deepened 250-commit clone. This is the surface the demo runs on; a two-command repair that needs no decision has been specified by three critic laps and proven once by this loop. It is my one `fix-before-next-row` item, and 18 is what running it buys. **개발 목적 16 · 설계와 방법론 15 · 창의성 15** unchanged. **Not scored here:** the two Pass/Fail rows — 서류 is the author's under the NH-008 closure, 위험성 is not at risk, the delivery layer stays dry-run. |
| 2026-09-06 | `b2bdaf0` | 16 | 15 | **18** | 15 | **14** | **78** | **Two rows move, in opposite directions, and the flat total hides the most useful window this table has scored in a week.** **구현 및 유용성 17 to 18, for two independent reasons.** First, the screen a judge actually stands in front of is correct: `web/finals.html` prints `n_entries` **383** and `n_reproducible` **325**, and `docs/NUMBERS.json` holds 383 / 325 / 58 not, counted here in one process. It had printed 326 · 268 for sixteen windows. It now also has a gate that re-derives the payload from the builder instead of reading strings, so a hand-typed judged number and a screen left behind by a registry both go red — I checked the gate's own tolerated set (`built_utc`, `git`, `integrity.gates[*].seconds`) and it is three fields, not a door. And the stamp moved to `62b58e1`, **6** commits behind `HEAD`, which resets the shallow-clone clock critic #25 measured at ten hours. Second, **critic #20's deduction on this row is withdrawn.** It took this row 18 to 17 at `ce262fe` for 「something on the screen is wrong today」 — the `41498ef` provenance line — and that was a measurement artifact: the commit is an ancestor of `HEAD`, 277 back, verified three ways here on a 300-deep clone. Nothing on the screen was wrong about reachability then or now. **Why 18 and not 19:** R7's kit holds one of the five printables it names (WFG-130), and the provenance line, while reachable, is stale by construction and mislabelled — the registry held **153** entries at that commit and the card beside it prints 383, and no sentence in this repository answers a judge who notices. **제출 자료 15 to 14, and it is the one thing in this window I would fix before anything else.** `docs/auto/JUDGE_QA.md` Q30 is **T0** — answered from memory, no paper — and its warning block tells the student the screen prints 326 · 268, that pointing at the screen 「학생을 낡은 숫자로 데려갑니다」, and that three counts answer this question. All three are false at this head. These five judges never open the registry; they hear the student and they look at the screen, and the bank now drills the student to disown the one surface that agrees with them. A wrong warning outliving a correct repair is more expensive than the drift it was written about, because nothing looks broken. WFG-117 is re-scoped, moved to position 2, and is this lap's one `fix-before-next-row` item. **개발 목적 16, 설계와 방법론 15, 창의성 15, held.** **Not scored here:** the two Pass/Fail rows — 서류 is the author's under the NH-008 closure, 위험성 is not at risk, the delivery layer stays dry-run. |
| 2026-09-06 | `e95fe28` | 16 | 15 | 18 | 15 | **13** | **77** | **제출 자료 14 to 13, on two judge-facing surfaces measured here rather than read.** (i) `README.md:22-26`, the front page a judge opens first and the file `CITATION.cff` points at, states the attribution `paper/manuscript.md`'s own Abstract disclaims one sentence later, three bullets above the TL;DR's advertisement of a withdrawn-claim record (**WFG-138**). (ii) The booth kit is stale and staler than critic #27 left it: re-hashed here at `e95fe28`, `BOOTH_SETUP.md`, `DEMO_SCRIPT_5MIN.md` and `DETECTION_FLOOR_CARD.md` match and `docs/auto/JUDGE_QA.md` does not, manifest `2c8451211e…` against tree **`7d5ac4c9c5…`** where critic #27 measured `af955a30fa…`, because the WFG-133 lap edited Q35 again after that measurement. So the printed 17 pages carry the pre-WFG-117 Q30 **and** Q35's ⚠ block with **no** retraction note on it, that note having landed at `a64b904` after the `3e92b69` build. WFG-134 rebuilds; the freshness gate is now its own row **WFG-140**, per critic #27's pre-registered branch. **구현 및 유용성 18 held:** the kit exists and the screen works, and a stale printout is a 제출 자료 defect rather than a 완성도 one; nothing was built or broken in the product this window. **개발 목적 16, 설계와 방법론 15, 창의성 15 held.** ⚠ As on Track B, neither defect is new to the tree. |
| 2026-09-06 | `1b26c3a` | 16 | **16** | 18 | 15 | 13 | **78** | **One row moves, 설계와 방법론 15 to 16, on the same evidence as Track B and with a stronger claim on this track than on that one.** Track A's row reads 「기존의 **작품**과의 차별점」, and NIFoS's console and 경기도's G-DAPS are existing *products* run by the agency that would deploy this one, not papers. `docs/auto/knowledge/KOREAN_OPERATIONAL_SYSTEMS.md` names both with agency, date and scope, and it takes the honest side of the comparison in writing: 5 m terrain analysis is **finer** than this project's 500 m grid, in their favour, and no accuracy comparison may be made in either direction from agency plan figures. A judge from the disaster-response side will know at least one of these systems, and until `e888c8e` the student had nothing to say. **Why +1 and not +2:** the student still has nothing to say, because the card is **WFG-144** and it is P1. **구현 및 유용성 18 held, re-earned rather than inherited:** `gates.py --mode full` is ALL GREEN at this head (`1599 passed, 62 skipped`, cold, 273.6 s, identical to critic #28's cold run, which is what a window with no code should produce) and `auto-gates` run 180 at this head is `success`. Nothing was built and nothing broke. **제출 자료 13 held at the floor critic #28 set**, and it does not fall further only because both defects are the ones already scored: `README.md:22-26` is unchanged (**WFG-138**), and the booth kit drifted a **third** time to `175da9e50c…`, re-hashed here — with all three drifts written by the loop's own correction notes rather than by any improvement, which is this report's root objection. **개발 목적 16, 창의성 15 held.** |
| 2026-09-06 | `524f13c` | 16 | 16 | 18 | 15 | 13 | **78** | **Every row held, and the hold on 제출 자료 is this lap's finding rather than a note.** The window's dev lap closed **WFG-138** at `0ed15aa`/`20b0b7e`: `README.md:22-33` now states the fire-blind control inside the headline bullet and names the fair opponent's region scope, `docs/auto/JUDGE_QA.md` Q19's spoken draft carries the same caveat in the sentence rather than in a note beside it, and two gate modules go red if either slips. That is the exact defect critic #28 docked this row for, repaired and gated, and on its own it is **+1**. It is spent, exactly, on what the same window did to the paper the student physically carries. The printables manifest records `docs/auto/JUDGE_QA.md` at `2c8451211e5f97…`; the tree hashes **`5ac45ea8103f11…`**, re-hashed here in one process with the other three sources matching one by one. That is the **fourth** drift (`af955a30fa…` → `7d5ac4c9c5…` → `175da9e50c…` → `5ac45ea810…`) and the **first one that makes the printed pages worse rather than merely older**: the 17 printed Q&A pages now hold Q19 **without** the caveat this repository decided in the same window is mandatory. Critic #27's precedent binds here in its own words, a 제출 자료 row does not rise while the document the student recites from disagrees with the files the gates read. **구현 및 유용성 18 held, re-earned rather than inherited:** `gates.py --mode full` is ALL GREEN at this head, exit 0 (`1616 passed, 62 skipped, 1 xfailed`, cold, 350.4 s), and `auto-gates` runs 165 to 184 on `auto/dev` are 18 `success` and 2 `cancelled` with no `failure`, run 184 at this head `success`. Nothing on the screen or in the demo changed. **개발 목적 16, 설계와 방법론 16, 창의성 15 held:** the manuscript gained the Korean-operational-systems paragraph at `719c420`, but that is the same evidence critic #29 already scored on both tracks, and these five judges do not read the manuscript. The surface they do meet still has no card for it (**WFG-144**, P1). |
| 2026-09-07 | `3f881f6` | 16 | 16 | 18 | 15 | **14** | **79** | **One row moves, 제출 자료 13 to 14, on the same booth-kit evidence as Track B and one point lower for a reason this track cares about more.** The kit is verified fresh here (five source hashes, the PDF hash, 33 = 5+6+17+3+2 = the PDF's own page count), the reconciliation sheet prints for the first time, and Q39's card is true from this build onward. **구현 및 유용성 18 held on two movements that cancel, and saying so is the point:** the deliverable got real — 33 A4 pages a student can put on a booth table — while the delivery vehicle did not, because `release/kcf-finals-2026/MANIFEST.json` still lists **17 files, none of them a printable**, two rebuilds after the kit existed (`3e92b69` 06:51Z, manifest rebuilt `1ec1d06` 09:34Z). On this track the bundle *is* the product handed over, so 제출 자료 takes the +1 and 구현 does not. **개발 목적 16, 설계와 방법론 16 and 창의성 15 held**; nothing in the window changed what the tool is for or how it is built |
| 2026-09-07 | `0fc6130` | 16 | 16 | 19 | 15 | 14 | **80** | **One row moves, 구현 및 유용성 18 to 19, and critic #31 pre-registered exactly this.** That lap wrote 「on this track the bundle *is* the product handed over, so 제출 자료 takes the +1 and 구현 does not」, because `MANIFEST.json` listed 17 files and not one was a printable, two rebuilds after the kit existed. It now lists **19**, and every condition was re-run in this sandbox rather than read: `make finals-bundle` exits 0 with 「rebuilt byte-identically, 19 files」, the folder holds those 19 plus `MANIFEST.json` and nothing else, `python scripts/check_bundle_copy.py release/kcf-finals-2026` exits 0, and `git status --short` is empty afterwards so no committed byte moved. The mechanism matters more than the file count: `newest_printables()` resolves the stamp from the **tracked tree** rather than from a literal, so the next `make printables` breaks the bundle build until the same lap rebuilds it — the freshness rule enforced by construction instead of by a sentence. **R9 is ticked on this evidence; 5 of 11.** **Why 19 and not 20:** R1 is still held by WFG-110's unmapped keys on the judged screen, the booth recipe has never been run on the actual laptop (NH-014), and R7's kit is three of the five printables it names. **제출 자료 14, held, and the hold is this lap's `fix-before-next-row` item.** The kit is fresh and the bundle now carries it, which is worth +1 on this row too; it is spent, exactly, on the sentence riding inside that same bundle. `printables/manifest_20260907T0059Z.json:95` states that the dispatch sheets are already committed PDFs that print directly. The tree has 33 clusters and **3** committed PDFs, re-counted here, and the lap that discovered this corrected it in `README_KO.md` and `docs/finals_bundle.md` and could not reach the manifest. For these five judges the bundle is the object handed over, and a provenance file inside it that misstates the repository is a 제출 자료 defect however few people open it. **개발 목적 16 and 설계와 방법론 16, held**; nothing this window changed what the tool is for or what it is different from. **Not scored here:** the two Pass/Fail rows — 서류 is the author's under the NH-008 closure, 위험성 is not at risk, the delivery layer stays dry-run. |
| 2026-09-07 | `2896c9c` | 16 | **17** | 19 | 15 | **15** | **82** | **Same evidence, and on this track the differentiation panel lands harder while the finals screen takes the finding.** **개발 목적 16, held.** **설계와 방법론 16 to 17, and this row reads the evidence more directly than Track B's does.** The criterion is 「기존의 **작품**과의 차별점」, and §3 of `docs/related_work.md` differentiates against an actual competing 작품 (ISEF 2026 SFTD059T, Korean, high-school, an FDS-grounded risk-aware evacuation router) on seven axes, conceding in writing what the two share (time-varying hazard in the routing cost, which this project does not claim to have invented) before saying what is not found in the surveyed work. Judges may remember that project; the panel is the answer if one does. Same held second point as Track B, for the four dead repository paths the reviewer and not the lap caught. **구현 및 유용성 19, held, and what holds it at 19 is this lap's one finding.** The bundle is unchanged and sound. But `gates.py --mode full` exits **1** in the routine's own default clone at this head: `web/finals.html` names `62b58e1`, **55** commits and 23 hours behind, outside a depth-50 horizon, so `tests/test_finals_screen.py` reports the screen names a commit the repository does not have. GitHub run 201 at the same head is `success` because CI checks out at `fetch-depth: 0`. The delivered artifact still opens offline and every number in it is still right, so this is not a minus on 완성도; it is precisely what stops a **20**, because 20 on this row means the product's own gates are green in the environment that builds it, and a screen reporting a stale build id is a completeness defect a software judge would notice in the integrity panel. WFG-119 predicted this failure in writing and was parked at P1 as housekeeping; it fired while parked, and it is now P0. **창의성 15, held.** **제출 자료 14 to 15.** The kit went from 33 pages and five sources to **36 pages and six**, the added three being the differentiation panel, and R7 ticks; on this track 제출 자료 reads 「자료의 논리적 구성」 first, and a booth kit that now contains the one document answering 「무엇이 다릅니까」 is a structural gain rather than a cosmetic one. It sits at 14 rather than 18, so the same evidence buys room here that it does not buy on Track B. The 2026-02-12 date error is real and is on this kit's printed pages; it costs the second point rather than the first. **Not scored:** the two Pass/Fail rows — 서류 is the author's under the NH-008 closure, 위험성 is not at risk, the delivery layer stays dry-run. |
| 2026-09-07 | `2720840` | 16 | 17 | 19 | 15 | 15 | **82** | **Every Track A row holds, and the one row that could have moved is where this lap does its arguing.** **개발 목적 16, 설계와 방법론 17, 창의성 15 held**; nothing changed what the tool is for or what it is different from. **제출 자료 15 held** — the kit was rebuilt at `20260907T0953Z` because JUDGE_QA's line-number citations were re-cited by test name (a genuine WFG-133-class repair), but the 2026-02-12 date critic #33 said 「costs the second point」 is still printed on 3 of its 36 pages, unfixed for a third lap. **구현 및 유용성 19 held, and I am not paying the point critic #33 pre-registered.** That lap wrote 「20 on this row means the product's own gates are green in the environment that builds it」 and docked the stale build stamp. **That blocker is cleared and I verified it the way that lap asked:** on the routine's DEFAULT clone, before any deepening (`is-shallow-repository` = `true`, `rev-list --count HEAD` = **50**), `gates.py --mode full` exits **0**, ALL GREEN, `1650 passed, 62 skipped, 2 xfailed`; `web/finals.html` names `7308b06`, **6** commits behind `HEAD`; GitHub run **205** at this head is `success`. **The point it buys is spent, exactly, on the blocker critic #32 named first and that #33's finding temporarily hid.** R1 asks that every number on the judged screen map to a registry key; `scripts/finals.template.html` references **28** keys, `DEMO_SCRIPT_5MIN.md` §3 maps 22 of them and maps them the wrong way round, and **6** are in no committed table (WFG-110). A 20 on 구현 및 유용성 while six numbers on the demo screen answer to nothing is not defensible, and this is not a moved goalpost — it is the older one, set at `0fc6130` before the stale stamp existed. It is why WFG-110 is now DIRECTION position 2. **Not scored:** the two Pass/Fail rows — 서류 is the author's under the NH-008 closure, 위험성 is not at risk, the delivery layer stays dry-run. |
| 2026-09-07 | `b54ca28` | 16 | **18** | 19 | 15 | 15 | **83** | **One row moves on BOTH tracks, 설계와 방법론 17 to 18, and it is the row that scores 「기존의 연구와의 차별점」.** Critic #33 gave this row its previous point when `docs/auto/finals/RELATED_WORK_PANEL.md` shipped, and both #33 and #34 then wrote that a panel a student cannot speak from is worse at a booth than either half alone, because the printed page invites the question. That asymmetry closed this window: `docs/auto/JUDGE_QA.md:637-694` holds **Q16a · T0**, and the finals format is five separate 1:1 interviews, so the differentiator is now on the surface a judge actually reaches rather than only on the one they glance at. I read the card rather than grepping for it: it answers on the **output object** and refuses the accuracy comparison in either direction, refuses 「저쪽은 못 합니다」 in favour of 「공개된 자료에서는 확인되지 않습니다」, and refuses any 경상북도·영덕 coverage claim. **제출 자료 held on two moves that cancel, and the second is this lap's one item.** **+1**: WFG-146's citation pairing 「사이언스타임즈 2026-02-13 (연합뉴스 2026-02-12 전재)」 landed in seven live sites with dated notes and the kit was rebuilt in the same lap, and 출처 명기 is a named criterion on this row of both tables; I re-fetched the article myself as a fourth independent check. **-1**: `RELATED_WORK_PANEL.md:40`, 3 of the 38 printed pages, asserts 「앞의 두 시스템은 재현 방법을 공개하지 않습니다」 about documents nobody has opened (NH-039), where the survey it summarises writes 「not stated」 at `docs/related_work.md:134` and the same lap wrote the narrowed form into two other files. A page that promises in its own header to add no new claims, adding one, on paper, is exactly what this row docks. **WFG-162.** **데이터 수집·분석·해석 held:** nothing in this window touched a model, a split, a metric, an arm, a coupling or a protocol, and WFG-139 is unrepaired for an eighth measurement. **연구 목적 / 개발 목적 and 창의성 held:** no method moved. **Track A 구현 및 유용성 holds at 19** on WFG-110, unchanged for a fourth lap and still R1's only sized blocker. |
| 2026-09-07 | `7cc4eb7` | 16 | 18 | 19 | 15 | 15 | **83** | **Every row holds, and the row I am not paying is 구현 및 유용성.** **구현 및 유용성 19, held, and I am declining the point critic #34 pre-registered — but the goalpost is not moved, it is the one critic #32 set at `0fc6130` and it has three parts.** That lap wrote: 「Why 19 and not 20: R1 is still held by WFG-110's unmapped keys on the judged screen, the booth recipe has never been run on the actual laptop (NH-014), and R7's kit is three of the five printables it names.」 Two of the three are now cleared — R7 ticked at 08:00Z and **WFG-110 closed at `60c07c8`**, which I re-derived independently rather than reading. **NH-014 is the third and it is unchanged and it is the author's.** To that I add one measurement of my own, offered as information and not as the docking reason: R1's other clause, 「**all four acts advance**」, has never been exercised by anything in this repository. A dependency-free headless run here (`/opt/pw-browsers/chromium-1194/chrome-linux/chrome --headless=new --virtual-time-budget=8000 --dump-dom`) exits 0, returns a 3,449,612-byte post-JS DOM with all four view containers and the four act labels, and confirms **zero** `http(s)` references in the built file — and shows `view-evidence` at **225 B** and `view-system` at **235 B** with no Korean text at load, against `view-live` 1,157,688 B and `view-reliability` 2,063,451 B. Those two views build on switch and nothing presses the key. On this track 완성도 is the criterion and a screen whose two evidence acts no measurement has ever entered is not a 20. **WFG-009, P1 → P0, is this lap's one row move, and it is the pre-registration: four acts advanced and screenshotted by a driver, and this row goes to 20.** **제출 자료 15, held on the same two cancelling moves as Track B** — the screen-to-key index and the registry work against `JUDGE_QA.md:650-652` printed on 17 of the kit's 38 pages (WFG-166). On this track the kit is the object handed over, so the printed copy is what the deduction is about. **개발 목적 16, 설계와 방법론 18 and 창의성 15, held**; nothing in the window changed what the tool is for or what it is different from. **Not scored:** the two Pass/Fail rows — 서류 is the author's under the NH-008 closure, 위험성 is not at risk, the delivery layer stays dry-run. |
| 2026-09-07 | `64f015b` | 16 | 18 | **20** | 15 | **16** | **85** | **Two rows move, and the first of them is the one critic #36 pre-registered in writing.** **구현 및 유용성 19 to 20, and the goalpost is critic #32's, unmoved.** That lap set three conditions for a 20: WFG-110's unmapped keys, R7's kit, and NH-014. R7 ticked at 08:00Z, WFG-110 closed at `60c07c8`, and critic #36 then wrote 「WFG-009 ... is the pre-registration: four acts advanced and screenshotted by a driver, and this row goes to 20」. It shipped, and I did not take it on the lap's word. I ran `scripts/check_finals_acts.py` myself: `1막 · 발견` 1 dot → `2막 · 시간과 도로망` 2 → `3막 · 경로 비교` 3 → `4막 · 판단` 4, four screenshots, **10 requests, none off `file://`, zero console errors**, exit 0. **Then I read GitHub's own run**, because a driver that only ever runs where it was written proves the machine and not the screen: `auto-gates` run **219** at this exact head ran `finals-acts` on a clean `ubuntu-latest` runner from 19:12:10Z to 19:12:26Z and its log prints the same four labels, the same dot counts and the same request line, then uploaded 5 files (2,238,234 B). That also answers the 1900Z dev report's own open finding 5, which had recorded the CI browser as unmeasured. **R1 TICKS, 7 of 11**, first movement on that line in the checklist's life. **NH-014 is the third condition and it is unchanged — and it is the author's**, tracked separately as R12; a row cannot be held below its evidence by an item no lap can close. **제출 자료 15 to 16.** Critic #36 docked this row for `JUDGE_QA.md:650-652` printed on the kit's Q&A pages; that sentence is gone, the panel's copy of it is gone, and the kit was rebuilt in the same lap (`WFG_printables_20260907T1825Z.pdf`, 38 pages, manifest re-pointed, byte-identical). On this track the kit is the object handed over, so the reprint is what earns the point. **Why 16 and not 17:** four lines above the repair, on the same printed page, `RELATED_WORK_PANEL.md:32` states in bold that the NIFoS console's 발화점은 「운영자가 손으로 입력」한다 — the same claim family in the positive direction, on paper, in the student's hand, with the attribution its own sources carry dropped (**WFG-171**, this lap's one `fix-before-next-row` item). **개발 목적 16, 설계와 방법론 18, 창의성 15, held**; nothing in the window changed what the tool is for or what it is different from, and the stdlib CDP client is engineering restraint rather than research creativity. **Not scored:** the two Pass/Fail rows — 서류 is the author's under the NH-008 closure, 위험성 is not at risk, the delivery layer stays dry-run. |
| 2026-09-08 | `47e48b5` | 16 | 18 | 20 | 15 | **17** | **86** | **One row moves, 제출 자료 16 to 17, on the kit rather than on the manuscript, and it is critic #39's deduction returned.** **제출 자료 16 to 17.** The defect critic #39 docked was on paper: 「여섯 개」 on 17 printed pages and in a spoken T1 line. The 0407Z lap rebuilt the kit **twice** — once for the correction and again after its independent reviewer blocked the first version for restating the count in five unanchored places — shipping `WFG_printables_20260908T0406Z.pdf`, 38 pages, with `release/kcf-finals-2026/MANIFEST.json` re-pointed at it in the same lap (WFG-152). Verified here: the manifest names that PDF, and `gates.py --mode full` including `tests/test_printables.py` is green at this head. **Why 17 and not 18:** `README.md` still has no Round-4 section and no English abstract (R8, `grep -n 'Round 4' README.md` returns nothing), and on this track the README is the front door of what the judge is handed. **개발 목적 16, 설계와 방법론 18 held.** **구현 및 유용성 20 held** — the bundle rebuilt and re-pointed, nothing regressed, and there is no 21. **창의성 15 held, the 40th consecutive 15** (WFG-182). |
| 2026-09-08 | `9329400` | **17** | 18 | 20 | 15 | 17 | **87** | **One row moves, 개발 목적 16 to 17, and it is the row whose own text asks for what this window produced.** Track A 개발 목적 scores 「작품 작동을 위한 제약에 대한 이해」, and until `c2a7980` the bank a student recites from had 42 cards and no answer to the two questions a public-sector disaster-response judge asks a rescue-dispatch tool. Both now have a T0 card, neither answers with the screen's slogan, and between them they carry eight explicit 없는 것 clauses: no statute, no insurance position, no promise that the delivery layer stays dry, no operational-room use (N = 1 consultation), no 「전혀 개인정보를 쓰지 않습니다」, no legal status under privacy law, no claim that deployment preserves the answer, and a coordinate is not anonymous. Re-derived here, not read: `sms.send` uncalled in `src/` and `scripts/`, `email_sent.json` **0** in the tree, 28 JSON records `nothing_was_sent: true`, `immobile_fraction` **0.3**, `vulnerability.py`'s placeholder declaration verbatim. **Why 17 and not 18:** `README.md` has `## Round 2 결과` at `:59` and `## Round 3` at `:75` and still no Round-4 section (R8, WFG-010), which is the front door on this track; and the spoken sentence at `JUDGE_QA.md:715` still says 「커밋된 33장」 where the tree holds 642 tracked sheets (F1, WFG-185). **구현 및 유용성 holds at 20**, **설계와 방법론 18** and **제출 자료 17** hold, **창의성 15** for the 41st consecutive row (WFG-182). |
| 2026-09-08 | `ceb43ba` | 17 | 18 | 20 | **16** | 17 | **88** | **One row moves, 창의성 15 to 16, on the same evidence as Track B and for the same reason: this track's 창의성 row is worded identically (20 points, 「프로젝트 진행에 있어 뛰어난 창의성」) and the instrument is the same 5-minute interview.** See the Track B row above for the measurement and for why 16 and not 17. **개발 목적 holds at 17.** Critic #41 gave that point and wrote 「Why 17 and not 18」 with two blockers named: `README.md` has no Round-4 section (R8, WFG-010) and `JUDGE_QA.md:715` still said 「커밋된 33장」 (WFG-185). **One of the two closed and the other did not** — `grep -nE '^## Round' README.md` still returns only `:59` and `:75` — so the row holds rather than moves, and WFG-010 is now at the head of the `todo` block where it can actually be taken. **제출 자료 holds at 17, deliberately, and this is the row where I decline to ratchet.** This track paid 16 to 17 at `47e48b5` on the booth kit; a second point for another card in the same kit would be scoring volume rather than 논리적 구성 or 출처 명기. Pre-registered: it moves when the material reaches the **screen** or the **README**, which is WFG-010 and WFG-169. **구현 및 유용성 holds at 20** and **설계와 방법론 at 18**; on this track the WFG-187 stamp drift is the standing risk to 구현 및 유용성, recorded and not yet charged |
| 2026-09-08 | `dee1bc1` | **18** | 18 | 20 | 16 | 17 | **89** | **One row moves, 개발 목적 17 to 18, on different evidence from critic #41's raise of the same row one lap earlier.** **개발 목적 17 to 18.** Critic #41 raised this row for 「작품 작동을 위한 제약에 대한 이해」 on the clean-clone skip-count card. The evidence here is the other half of the row's text, 「해결할 실질적 문제 또는 필요성에 대한 명확한 설명」 and 「사회적 기여도」: `README.md:200` opens a Round-4 section that states what the tool is for and, at `:207-208`, tells the reader that three of its four items argue **against** this project. `:596`'s abstract says the same in English. Said plainly because it is the unusual thing: the raise is for a section that is mostly a list of the project's own weaknesses, and that is what 「명확한 설명」 means on a life-safety tool. **설계와 방법론 18 and 구현 및 유용성 20, held:** no code, model, region or screen behaviour changed; `web/finals.html` was rebuilt only to refresh its build stamp (`2c6e366`, 29 commits behind to 0, now 9 against a limit of 30), which closes WFG-187 and protects R1 and R9 rather than improving them. **창의성 16, held**, as on Track B. **제출 자료 17, held, for the same reason Track B's 19 holds:** the Round-4 section is a real gain and WFG-190 is a live defect inside it, and the two cancel. This lap will not raise a row on the page it filed a finding against in the same hour. |
| 2026-09-08 | `0cca093` | **19** | 18 | 20 | 16 | 17 | **90** | **One row moves, Track A 개발 목적 18 to 19, on the adoption card — and 제출 자료 is HELD in both tracks rather than raised or docked, which is the deliberate half of this scoring.** **A 개발 목적 18 to 19.** That row's own rubric text is 「작품 작동을 위한 제약에 대한 이해」, and the new card (`docs/auto/JUDGE_QA.md`, the 46th; the bank's header 46 = T0 19 + T1 20 + T2 7 sums by `grep -cE '^\*\*Q[0-9]+[a-z]? · T[0-9]'`) is that sentence answered against the judge who would have to run it: it refuses to give a cost 「저희는 어느 기관에서도 이걸 운영해 본 적이 없고, 도입 비용을 추정해 본 적도 없습니다」, and its 「없는 것」 block leads with 「비용 수치가 하나도 없습니다」, names N = 1 on the firefighter consultation, and separates 「running this once in that county」 from 「running it in an operations room」. `tests/test_adoption_card.py` (327 lines) binds it and the kit was rebuilt in the same lap (`WFG_printables_20260908T1529Z.pdf`, `release/kcf-finals-2026/MANIFEST.json` re-pointed). **제출 자료 HELD at 19 (B) and 17 (A), and the hold is the finding.** Up this window: `README.md:220-239` stopped stating the universal critic #43 filed (「어느 심사용 자료에도」 is now 「부스에서 말하지 않습니다」) and the Round-4 section now names the buffer as a free parameter and points at `docs/present_perimeter_arm.md` §4. Down this window, on the same paragraph: `README.md:232` now asserts 「고원이 아니라 뾰족한 봉우리」 — the shape `docs/auto/JUDGE_QA.md:1394` and `:920` say the five-point sweep **cannot resolve**, and which `docs/auto/DEMO_SCRIPT_5MIN.md:151` has the student disclaim out loud. The two cancel; raising the row on a section that gained one defect while losing two would be scoring the diff rather than the state, and docking it would double-count a finding filed at position 1 (**WFG-127**, fourth surface). **B 설계와 방법론 18, held for the same reason and not a different one:** the sweep's resolution is a methodology fact, the repository states it correctly on three surfaces and incorrectly on the fourth, and 18 already reflects the three. **데이터 수집·분석·해석 19 (B) / 구현 및 유용성 20 (A) held: nothing was measured this window.** The paper lap wrote a correction, its reviewer blocked it, and it was reverted rather than shipped — `paper/manuscript.md` is byte-identical to lap 18's (`6ecc386`). That is restraint and it is scored as a hold, not a loss. **창의성 16 held in both tracks:** WFG-182 closed on the bank and the count is still **0** on `web/finals.html` and **0** on `docs/auto/DEMO_SCRIPT_5MIN.md` (WFG-194). ⚠ **No row is docked for `auto-gates` run 253 being red** (WFG-193): the gates inside it passed, and a broken artifact upload that stopped `Main` following is loop machinery a judge never opens. |
| 2026-09-08 | `9c24a8b` | 19 | 18 | 20 | 16 | 17 | **90** | **critic #45.** **no row moves on either track — the first all-hold window since `2720840` on 2026-09-07, and it is the honest reading of a three-hour window that contained two CI-repair laps and one research lap and touched no judge-facing surface.** The window's real work was real: `Main` follows again, two red `auto-gates` runs are closed, the browser gate's skip was verified rather than asserted, the manuscript's related-work section narrowed four overclaims about papers it cites, and DIRECTION.md went from 54,923 bytes to one screen with the old page archived verbatim. **None of it is a thing five judges see in ten minutes.** **B 데이터 수집·분석·해석 19 and A 구현 및 유용성 20 held: nothing was measured.** Pre-registered so the next lap can hold me to it — B 데이터 goes to **20** when WFG-125 reports a number from the out-of-fold field this lap found committed (`data/processed/spread_v2_lofo_oof_cells.csv.gz`), or when it discharges its written branch by naming that file and saying precisely why it cannot be used. **창의성 16 held on both tracks**, and the reading got worse rather than better: `docs/creativity_card.md` exists and is in neither the printed kit nor any of the three surfaces a judge stands in front of (WFG-194). It is not docked, because that describes the same state critic #44 scored and not a regression; it moves to 17 when 창의성 reaches `web/finals.html` or `DEMO_SCRIPT_5MIN.md`. **설계와 방법론 18 held on two moves that cancel:** up, the manuscript stopped overclaiming what @cova2005, @li2017, @li2019, @kim2021gk2a, @dozier1981 and @studenski2011 say, which is this row's 「기존의 연구와의 차별점」 criterion made accurate rather than merely present; down, the same row's 「일정」 bullet is at **zero** on every judge-facing surface (WFG-027, F3). **제출 자료 19 (B) / 17 (A) held:** `README.md:232`'s 「고원이 아니라 뾰족한 봉우리」 is unrepaired for a second window, and nothing reached the kit or the screen. **연구 목적 18 / 개발 목적 19 held.** ⚠ **No row is docked for `auto-gates` run 255 being red:** it was a browser that never started, on a runner, and a judge never opens CI. |
| 2026-09-08 | `cb9fcc3` | 19 | **19** | 20 | 16 | 17 | **91** | **One row moves, 설계와 방법론 18 to 19, on the same evidence as Track B and for this track's own wording: 「설계의 기술적 완성도(우수성)」.** ⚠ **Correction first:** this window was scored once at `7cfe29a` as an all-hold on the reading that the 21:17Z lap had died; it was slow, not dead, and closed both its rows at `9170a37`. Nothing was pushed carrying that scoring. The additive flag that defaults to empty, the separate output file, the reproduce-or-refuse guard and the cell-for-cell control on five shared widths are engineering discipline a software-engineering judge reads directly, and they are what this row is for. **구현 및 유용성 20, held, and re-earned rather than inherited:** `gates.py --mode full` exits **0** at this head (`1786 passed, 63 skipped, 2 xfailed`, 346.6 s, up 23 tests), `auto-gates` runs **262** and **263** are `success` at the two newest heads, `git ls-remote origin Main` answers **`cb9fcc3`** so `Main` follows, the kit's **six `SOURCES` all re-hash to the tree** in one process, and `make finals` took the screen's stamp from 27 commits behind to **8** against a limit of 30. There is no 21. **개발 목적 19, held.** 「작품 작동을 위한 제약에 대한 이해」 gained nothing a judge meets; what would move it is the horizon card (**WFG-197**, kept P1 this lap on purpose), since none of the bank's 46 cards says why the forecast looks 3 to 12 hours ahead while national doctrine decides the elderly evacuation at 8 h and completes it at 5 h. **제출 자료 17, held on two moves that cancel.** Up: the kit was rebuilt in the same lap as the correction it carries (`WFG_printables_20260908T2156Z.pdf`, `MANIFEST.json` re-pointed), which on this track is the object handed over. Down: the fair-opponent margin printed inside it is a **post-hoc maximum over a grid** and nothing on any surface says so, while the number itself fell from 9 origins to 5 on the first refinement (**WFG-201**, position 1). **창의성 16, held**, fourth window: 0 on `web/finals.html`, 0 on `DEMO_SCRIPT_5MIN.md`, and `docs/creativity_card.md` referenced nowhere under `release/` or `scripts/` (WFG-194). **Not scored:** the two Pass/Fail rows — 서류 is the author's under the NH-008 closure, 위험성 is not at risk, the delivery layer stays dry-run. |
| 2026-09-09 | `86f8929` | 19 | 19 | 20 | 16 | 17 | **91** | **No row moves on Track A, and that is the honest reading rather than a courtesy.** The window's whole content is a statistics-interpretation improvement on the fair-opponent comparison. Track A has no 데이터 수집·분석·해석 row; its nearest neighbour is 구현 및 유용성, already at **20**, and nothing this window touched changed what the system does or how well it does it. 창의성 holds at 16 for the same measured reason as Track B (0 on the screen, 0 on the script). 제출 자료 holds at 17: the printed kit's composition did not change. Track A **91**, held. |
| 2026-09-09 | `5f4e32b` | 19 | 19 | 20 | 16 | 17 | **91** | **No row moves on Track A either.** 개발 목적 19, 설계와 방법론 19, 구현 및 유용성 20, 창의성 16, 제출 자료 17, all held. Track A has no 데이터 수집·분석·해석 row, so the window's whole content lands nowhere on this table. **구현 및 유용성 20 is held and the bundle defect does not touch it:** `make finals-bundle` rebuilds `release/kcf-finals-2026/` byte-identically at 19 files, exit 0, and WFG-208 is about pointers inside a document, not about whether the product runs. **제출 자료 17 is held rather than docked for WFG-208**, on the same reasoning as Track B: the defect is real 출처 명기 debt on the copy a judge could carry away, and it is one sentence and a URL. If it is still open at the next critic lap it becomes a deduction.  ⚠ **WFG-194 landed under this lap at `b451376`, after the window closed**, and put 창의성 onto `web/finals.html`, `docs/auto/DEMO_SCRIPT_5MIN.md` and the printed kit. **That is critic #49's window to score, not this one's**, and it is named here so no later lap reads this hold as having seen it. |
| 2026-09-09 | `7f914fd` | 19 | 19 | 20 | **17** | 17 | **92** | **One row moves, 창의성 16 to 17, on the same evidence as Track B and for this track's identical wording (20 points, 「프로젝트 진행에 있어 뛰어난 창의성」) measured by the same 5-minute interview.** See the Track B row above for the counts and for why 17 and not 18. **구현 및 유용성 20, held and re-earned rather than inherited:** `gates.py --mode full` exits 0 at this head (`1826 passed, 63 skipped, 3 xfailed`, 470.7 s), `auto-gates` run **279** is `success` at this exact head with no `failure` in runs 264 to 279, and `make finals-bundle` rebuilds `release/kcf-finals-2026/` byte-identically at 19 files. There is no 21. **제출 자료 17, held on the same two cancelling moves as Track B, and on this track the kit is the object handed over:** the bundle gained the address it lacked and the kit gained a seventh source, against the WFG-210 anchor now printed on those same pages. **개발 목적 19 and 설계와 방법론 19 held**; nothing this window changed what the tool is for or what it is different from. **Not scored:** the two Pass/Fail rows — 서류 is the author's under the NH-008 closure, 위험성 is not at risk, the delivery layer stays dry-run. |
| 2026-09-09 | `9c22ff3` | 19 | 19 | 20 | **18** | 17 | **93** | **One row moves on both tracks, 창의성 17 to 18, and it is the raise critic #49 pre-registered here in this table, paid on evidence I verified in the tree rather than read from the lap's report.** Critic #49 wrote 「Why 17 and not 18, and this is the deduction pre-registered for critic #50: item ①, the one that claims the output object is the contribution, anchors on `docs/auto/knowledge/KOREAN_OPERATIONAL_SYSTEMS.md` on three of the four surfaces ... and no surface gives a path to an instance of the object it names」. **That defect is closed on all four surfaces and every part of the closure is checkable:** `web/finals.html`'s `CREATIVE[0].doc` now reads `outputs/dispatch/20260801T163042Z/01-거무역리공원-북쪽/dispatch_a4.html · outputs/dispatch/README.md · docs/auto/knowledge/KOREAN_OPERATIONAL_SYSTEMS.md §3`, instance first and the landscape note last with its role written into the visible text; `docs/creativity_card.md:36`, `README.md:326-355` and `docs/auto/JUDGE_QA.md` Q29a each do the same. The named sheet is tracked (`git ls-files` answers it) and **every markdown link target in `README.md:200-362` resolves in the tree**, checked here one by one. The gate binds the anchors' **contents** rather than their names, and the lap reported its own M8 mutation green — a threshold no document reaches, which made the assertion vacuously true — and closed it in both directions. On a 20-point row an answer whose every claim now hands the judge the artifact behind it, and whose gate can fail, is worth the point. **제출 자료 HELD and the hold carries this lap's root objection.** Up, and it is real: `factchk` on every new world claim in the window found **nothing** — Opanasopit & Louis (*Frontiers in Built Environment* 12, 2026-08-06), Chen et al. <!-- forbidden-ok: Chen --> (*PLOS ONE*, 2026-09-01) and 헤럴드경제 (2026-02-12) were each opened here and each confirmed the repository's wording, down to 70 isolated communities, 38,100 → 11,900 residents, R² 0.9251, 0.57–0.77 and the verbatim 「화선 도달 8시간 전 … 5시간 전에는」. 출처 명기 is at the top of its band. Down, on the other named criterion of the same row: 「자료의 논리적 구성」 on the front door. `README.md:200-362` is 163 lines carrying **11** ⚠ markers and **28** negative-framing tokens against **one** affirmative-outcome token, and that one says the model-free opponent scored better than the repository had recorded. The section documents what Round 4 attacked and never states what the project still claims. **WFG-212.** The two cancel. **연구 목적 / 개발 목적 and 설계와 방법론 held: nothing this window touched a model, a split, a metric, an arm, a protocol or the purpose statement**, and 일정 is still at literal zero on all four judge surfaces (re-measured: 0 / 0 / 0 / 1, the one being the adjective 일정한), which was already priced into the 19. ⚠ **WFG-125 is unrun for a FOURTH consecutive critic lap and this lap promoted it to the first `todo` position**, which is the only lever a critic has; the 데이터 row is already at 20, so nothing in the scoring pushes it forward, and that is NH-038's question. **구현 및 유용성 20, held and re-earned rather than inherited:** `gates.py --mode full` exits 0 at this head (`1830 passed, 63 skipped, 3 xfailed`, 373.4 s), `auto-gates` run **285** is `success` at this exact head, and `make finals-bundle`'s manifest declares 19 files against a two-file tracked footprint that is git-ignored by design. There is no 21. **Not scored:** the two Pass/Fail rows — 서류 is the author's under the NH-008 closure, 위험성 is not at risk, the delivery layer stays dry-run. |
| 2026-09-09 | `8506a2d` | 19 | 19 | 20 | 18 | **16** | **92** | **One row moves and it is the same deduction as Track B, 제출 자료 17 to 16, for this track's identically worded criterion 「자료의 논리적 구성 · 사용된 자료에 대한 출처 명기」.** See the Track B row above for the measurement. On **this** track the deduction is if anything sharper, because the object handed over is the **printed kit**: `docs/auto/JUDGE_QA.md` is one of its seven hashed `SOURCES`, and card Q36 at `:1399` now instructs the student to tell a judge that after 「맞습니다」 the repository has nothing, two hours after the repository acquired something. A judge who takes the kit away and opens `docs/oracle_gap.md` finds the card contradicted by the project's own newest file. **개발 목적 19 held.** 「작품 작동을 위한 제약에 대한 이해」 is exactly what `docs/oracle_gap.md` improved, and I decline to raise it in the same lap I dock the row that says the improvement never reached the reader; the two are the same event scored twice. It moves to 20 when WFG-214 lands. **구현 및 유용성 20, held and re-earned rather than inherited:** `gates.py --mode full` exits 0 at this head (1841 passed, 63 skipped, 3 xfailed, 386.9 s), `--assert-head` and `--assert-reported --base eff2183` both exit 0 over 67 substantive paths, and `auto-gates` run **289** is `success` at this exact head. There is no 21. **설계와 방법론 19 and 창의성 18 held**, for the same measured reasons as Track B. **Not scored:** the two Pass/Fail rows. 서류 is the author's under the NH-008 closure, 위험성 is not at risk, the delivery layer stays dry-run. |
| 2026-09-09 | `375be25` | 19 | 19 | 20 | 18 | 16 | **92** | **No row moves on Track A either, and the two rows that could have are the two Track B names above.** **설계와 방법론 19, HELD.** Track A words this row with **four** sub-items rather than five (`docs/auto/RUBRIC.md:31`) and 「일정 및 팀원(개인의 경우 제외) 역할 배분의 타당성」 is one of them, so the zero that closed this window is a **quarter** of this row's wording rather than a fifth: the case for a raise is if anything stronger here. It is still not paid, for the same two measured reasons in the Track B row - the boundary overclaim, and 일정 at 0 on `README.md` and 0 on `web/finals.html` - and the same raise is pre-registered on the same condition. **제출 자료 16, HELD on the same two cancelling moves**, and on this track the deduction bites slightly harder, because 「사용된 자료에 대한 출처 명기」 is a named criterion and the new document's own stated source (`git log`) does not support the sentence it is cited for. **구현 및 유용성 20, HELD:** the demo surface did not move this window; `web/finals.html` was rebuilt at `89da7d3` for registry-payload freshness only and gained no content, and the one gap found is documentation reachability (WFG-218), not implementation. **개발 목적 19 and 창의성 18, HELD:** no purpose and no method moved. |
| 2026-09-09 | `ba06467` | 19 | 19 | 20 | 18 | **17** | **93** | **One row moves, 제출 자료 16 to 17, the same discharge as Track B for this track's identically worded criterion 「자료의 논리적 구성 · 사용된 자료에 대한 출처 명기」.** See the Track B row for the measurement. On this track the restoration is if anything better earned, because the object handed over is the **printed kit**: `docs/auto/JUDGE_QA.md` is one of its seven hashed `SOURCES`, card Q36 was the card critic #51 docked the row for, and the kit was rebuilt in the same day carrying the corrected card (`WFG_printables_20260909T1908Z.pdf`, 53 pages, `release/kcf-finals-2026/MANIFEST.json` re-pointed after staging — verified here, the manifest names the 1908Z kit and that file is tracked). **개발 목적 19, HELD, and this is where I decline the pre-registration critic #51 wrote.** That lap said 「it moves to 20 when WFG-214 lands」. **WFG-214 has not landed** — it is `todo` with residue, three of four surfaces closed and the fourth budget-blocked behind NH-037 in the paper routine's file. Paying a **ceiling** score on a 20-point row for a row that is not done, on a project whose front door still has WFG-212 open and whose demo screen still has WFG-220, is not something I could defend at a booth. It moves to 20 when WFG-214 closes. **구현 및 유용성 20, held and re-earned rather than inherited:** `gates.py --mode full` exits 0 at this head (1862 passed, 64 skipped, 3 xfailed, 383.2 s), `--assert-head` exits 0, and `auto-gates` run **299** is `success` at this exact head with **no `failure` in runs 279 to 299**. There is no 21. ⚠ **WFG-220 is a 제출 자료 deduction and not a 구현 deduction**, and the distinction is deliberate: the screen runs correctly and shows what it claims to show; what it omits is a caveat, and omitting a caveat is a 자료 defect. **설계와 방법론 19, HELD**, and on this track the case for a raise is stronger than on B, because 「일정 및 팀원 역할 배분의 타당성」 is one of **four** sub-items here (`docs/auto/RUBRIC.md:31`) rather than one of five: a quarter of the row's wording. Half of critic #52's pre-registration is paid and half is not (WFG-218), so it holds. **창의성 18, held.** **Not scored:** the two Pass/Fail rows. |
| 2026-09-09 | `3eec471` | 19 | 19 | 20 | 18 | **16** | **92** | **One row moves and it is the same deduction as Track B, 제출 자료 17 to 16, for this track's identically worded criterion 「자료의 논리적 구성 · 그래픽 및 범례의 명확성 · 사용된 자료에 대한 출처 명기」.** See the Track B row above for the measurement. On **this** track the deduction is sharper for the reason it was sharper on 2026-09-09 `8506a2d`: the object handed over is the release bundle, and `release/kcf-finals-2026/` contains `web/finals.html` and `printables/WFG_printables_20260909T1908Z.pdf` — whose seven sources include `docs/creativity_card.md`, `docs/auto/JUDGE_QA.md` and `docs/auto/DEMO_SCRIPT_5MIN.md` — and contains **no `README.md`**. So the judge who takes the USB away gets the unbounded claim and **cannot** reach the block that bounds it. The kit was built at 1908Z, three hours before the correction landed at 2206Z. **개발 목적 19, 설계와 방법론 19, 구현 및 유용성 20, 창의성 18, all HELD.** **구현 및 유용성 20 is untouched by this:** the artifacts exist, run and rebuild; what moved is what the prose around them claims, which is this row's neighbour and not this row. Track A has no 데이터 수집·분석·해석 row, so the window's manuscript clause lands nowhere here. **Pre-registered for critic #55:** returns to **17** on the same condition as Track B, and on this track the kit rebuild is not optional, because the kit is the object |
| 2026-09-10 | `7dabdef` | 19 | 19 | 20 | 18 | **17** | **93** | **One row moves on both tracks and it is the same row, 제출 자료, UP, and it is critic #54's pre-registered restoration paid on exactly the condition it pre-registered.** That condition was 「WFG-222 closes on all five surfaces **with the printed kit rebuilt and the bundle manifest re-pointed**, not when the README alone is fixed, and not on a report that says it was」. I verified it in the tree rather than reading the lap's report. **Counted here, unpiped, at `7dabdef`:** 「가구 단위」 answers **0** on `README.md`, **0** on `web/finals.html`, **0** on `scripts/finals.template.html` and **0** on `docs/auto/DEMO_SCRIPT_5MIN.md`, where critic #54 counted 1 / 1 / 1 / 1; 「per-household」 answers **0** on the screen and the template; and 「지점 단위」, which was **1 in the whole repository** at `3eec471`, now answers 2 / 1 / 1 / 2 / 1 / 3 across those files plus `docs/auto/JUDGE_QA.md` and `docs/creativity_card.md`. **The residue is deliberate and I opened each occurrence rather than trusting the count**: the four in the bank are three cards where the phrase is right (`:600` names the quantity `ingress_survival_time_min`, `:680-681` is the ⭕/❌ pair about what other systems' published material does not show, `:968` is Q20a's own question as a judge would ask it), and those in `docs/creativity_card.md` are record class under `<!-- forbidden-ok: … -->` pragmas. **The kit half of the condition also holds:** the kit was rebuilt twice (`WFG_printables_20260910T0017Z.pdf`, then `…T0140Z.pdf` after the reviewer's block), `release/kcf-finals-2026/MANIFEST.json` points at the second, and I re-hashed all seven of its printed sources against the tree, **seven of seven matching**, so the corrected wording is on the paper and not only in the repository. `WC-013` is registered in `docs/auto/withdrawn_claims.json` in the same lap, which is CHARTER §5c satisfied rather than promised. **+1 and not +2**, for two reasons measured rather than assumed: `web/finals.html` still carries **no word** of the oracle-in-the-grader objection that every other judge-facing surface now states (WFG-220, still `todo`, and the screen is the surface five judges spend ten minutes each in front of), and 일정 still answers **0** on both `README.md` and `web/finals.html` against a **named sub-item of 설계와 방법론** (WFG-218, still `todo`). **개발 목적 19, 설계와 방법론 19, 구현 및 유용성 20, 창의성 18, all HELD**, for the same reasons and on the same measurements as Track B above. **구현 및 유용성 20 held on evidence rather than inertia**, because it is the row most exposed to a stale demo: `web/finals.html` is offline by gate, its build stamp is inside `tests/test_finals_screen.py`'s 30-commit staleness limit, and the bundle's hash gates are green inside a full `gates.py --mode full` run at this head. **Pre-registered for critic #56:** 제출 자료 reaches **18** when WFG-218 and WFG-220 both close. |
| 2026-09-10 | `9b7d21c` | 19 | 19 | 20 | 18 | **18** | **94** | **One row moves, 제출 자료 17 to 18, and it is the same restoration as Track B for this track's identically worded criterion 「자료의 논리적 구성 · 그래픽 및 범례의 명확성 · 사용된 자료에 대한 출처 명기」.** See the Track B row above for the measurement. On **this** track it is if anything better earned, because the object handed over is the release bundle and the printed kit: `release/kcf-finals-2026/MANIFEST.json` was re-pointed in the same commit that rebuilt the screen, and I re-hashed **all nineteen** declared files against their sources, **nineteen of nineteen matching**, so the USB a judge carries away holds the screen that carries the schedule card and the oracle card, not the previous one. The kit's seven sources still hash equal because `README.md` and `web/finals.html` are not printed sources. **Track A goes to 94 while Track B holds at 94, and the divergence is the point rather than an accident:** this track has **no** 데이터 수집·분석·해석 row, so the `docs/oracle_gap.md` §4 interpretation defect that costs Track B a point has nowhere to land here. Its nearest neighbour, 구현 및 유용성, is about whether the tool works, and the tool works. **구현 및 유용성 20, HELD and re-earned rather than inherited:** `gates.py --mode full` exits 0 at this head (**1958 passed**, 64 skipped, 3 xfailed, 373.8 s), `--assert-reported --base 435c54d` exits 0 over 66 substantive paths, `auto-gates` run **315** is `success` at this exact head with **no `failure` in runs 283 to 315**, and `web/finals.html`'s build stamp is **5** commits behind this head against the 30-commit staleness limit. There is no 21. **설계와 방법론 19, HELD**, and on this track the case for the raise is stronger than on B, because 「일정 및 팀원 역할 배분의 타당성」 is one of **four** sub-items here (`docs/auto/RUBRIC.md:31`) rather than one of five. I still decline it, for the same reason and pre-registered on the same condition: WFG-128 is open against 「적합한 해결방법 제시」's control half on the page `README.md:146` sends a judge to. **개발 목적 19 and 창의성 18, HELD:** no purpose and no method moved. **Not scored:** the two Pass/Fail rows. |
| 2026-09-10 | `16e6824` | 19 | **20** | 20 | 18 | **17** | **94** | **Two rows move and they offset exactly, so Track A HELDs at 94 while Track B rises to 95 — and the divergence is the same structural one as the last two windows rather than a disagreement.** **설계와 방법론 19 to 20**, the same raise as Track B, on the same evidence and the same pre-registration, and on this track the case is stronger rather than weaker: 「일정 및 팀원(개인의 경우 제외) 역할 배분의 타당성」 is one of **four** sub-items here (`docs/auto/RUBRIC.md:31`) rather than one of five, and both halves of the standing pre-registration are now paid — WFG-218 and WFG-220 closed at `c4eb8d2` (일정 reaches the front door and the screen, and the screen says what grades its own dots) and WFG-128 closed at `5bcfe11`. **제출 자료 18 to 17**, the same deduction as Track B, and on **this** track it bites harder for the reason critic #53 and #54 both gave: the object handed over is the release bundle and the printed kit. `release/kcf-finals-2026/MANIFEST.json` declares `web/finals.html`, so the USB a judge carries away holds the screen that prints **◆ 예산 초과 2** with no caveat; and `printables/WFG_printables_20260910T0140Z.pdf` carries card Q38, whose seven sources I re-hashed against the tree at **seven of seven matching**, so the stale card is on the paper rather than only in the repository. Both are 「자료의 논리적 구성」 defects and neither is a 구현 defect — critic #53's own distinction, kept: the screen runs correctly and shows what it claims to show, and what it omits is a caveat. **구현 및 유용성 20, HELD and re-earned rather than inherited:** `gates.py --mode full` exits 0 at this head (**1985 passed**, 64 skipped, 3 xfailed, pytest 482.0 s, **up 27 tests in one window**), `auto-gates` runs 278 to 321 hold **36 `success`, 8 `cancelled` and ZERO `failure`** with run **321 green at this exact head**, and `web/finals.html` is offline by gate inside that same green run. There is no 21. **Track A has no 데이터 수집·분석·해석 row**, so the `docs/oracle_gap.md` §4 repair that pays Track B its second point this window has nowhere to land here; its nearest neighbour, 구현 및 유용성, asks whether the tool works, and the tool works. **개발 목적 19 and 창의성 18, HELD:** no purpose and no method moved. **Pre-registered for critic #58:** 제출 자료 returns to **18** when WFG-225 and WFG-226 both close **with the screen rebuilt, the kit reprinted and the bundle manifest re-pointed** — not when the tree is fixed, and not on a report that says it was. **Not scored:** the two Pass/Fail rows. |
| 2026-09-10 | `d3ca754` | 19 | 20 | 20 | 18 | **18** | **95** | **One row moves, 제출 자료 17 → 18, and Track A is UP to 95.** The move is the finals screen, which is Track A's central artifact: `renderPanel()` now prints the budget-rule pointer between the bucket loop and the panel's hairline rule, guarded on a non-zero count, and a `rel(...)` card carries the mechanism in the 알려진 한계 panel. Both went into `scripts/finals.template.html` and reached `web/finals.html` through `make finals`, which is WFG-109's lesson obeyed, and `release/kcf-finals-2026/MANIFEST.json` is re-pointed. 「그래픽 및 범례의 명확성」 is the criterion: the ◆ 예산 초과 bucket is a **map legend entry**, so the number a judge reads off the legend now has its rule beside it. +1 and not +2 because `docs/auto/JUDGE_QA.md` Q38 is still stale on the same subject and is on paper in the booth kit (WFG-226, `todo`), and because Q29 · T0 carries none of the 513-of-662 arithmetic the schedule document now publishes (WFG-229). **데이터 수집·분석·해석 has no Track A row**, which is why WFG-228 moves Track B and not this table; 구현 및 유용성 is about whether the demo resolves the need, and the null model does not bear on that. |
| 2026-09-10 | `3867860` | 19 | 20 | 20 | 18 | **19** | **96** | **One row moves, 제출 자료 18 to 19, and Track A is UP to 96 while Track B goes DOWN to 94 for the same identically worded criterion. The divergence is a convergence:** the two tracks have carried different values on 제출 자료 for three windows for track-specific reasons, both of which are now discharged, and one measurement applied to both lands them on 19 together. **The raise is critic #58's own pre-registration paid on exactly its stated condition.** That lap gave this row +1 and not +2 and named the two reasons: 「`docs/auto/JUDGE_QA.md` Q38 is still stale on the same subject and is on paper in the booth kit (WFG-226, `todo`)」 and 「Q29 · T0 carries none of the 513-of-662 arithmetic the schedule document now publishes (WFG-229)」. Both closed at `ba76b8c`. I verified the object rather than the report, which on this track is the whole point because what is handed over is the USB and the paper: `release/kcf-finals-2026/MANIFEST.json` declares **19** entries and I re-hashed all 19 against their sources at **19 of 19 matching**; it names `printables/WFG_printables_20260910T1233Z.pdf`, and I re-hashed that kit's **seven** `SOURCES` documents against the tree at **7 of 7 matching**, so the corrected Q38 and the new Q29 are on the paper in the box and not only in the repository. **19 and not 20**, on the same WFG-233 measurement that costs Track B its point: 「사용된 자료에 대한 출처 명기」 is worded identically here, and the sentence addressed to the submitted 작품설명서 cites nothing for a number registered nowhere. **구현 및 유용성 20, HELD and re-earned rather than inherited:** `gates.py --mode full` exits 0 at this exact head (**2024 passed**, 63 skipped, 3 xfailed, pytest 321.1 s), GitHub `auto-gates` runs 287 to 331 hold **38 `success`, 7 `cancelled` and ZERO `failure`** with run **331 green at this exact head**, `web/finals.html` is **11** commits behind a 30-commit staleness limit, and its embedded registry payload reads **453 / 395** against **453 / 395** in `docs/NUMBERS.json`. There is no 21. **설계와 방법론 20, HELD**, and on this track it was always the stronger case: 「일정 및 팀원(개인의 경우 제외) 역할 배분의 타당성」 is one of **four** sub-items here rather than one of five, and the student can now say the schedule from memory with its as-of commit. **개발 목적 19 and 창의성 18, HELD:** no purpose and no method moved. **Track A has no 데이터 수집·분석·해석 row**, which is why WFG-228 holds Track B at 19 and lands nowhere here. **Pre-registered for critic #60:** 제출 자료 reaches **20 on both tracks** when WFG-233 closes with the count registered, and returns to 18 on either track if a kit source drifts against the manifest. **Not scored:** the two Pass/Fail rows. |
| 2026-09-10 | `71e95ee` | 19 | 20 | 20 | **19** | 19 | **97** | **One row moves, 창의성 18 to 19, and Track A is UP to 97.** The move is the same measurement Track B scores, applied here because `docs/auto/RUBRIC.md` words 창의성 **identically** in both tables (「연구목적, 설계와 방법론, 데이터 수집·분석·해석 등의 프로젝트 진행에 있어 뛰어난 창의성」). A criterion with one wording cannot honestly carry two values on one tree, and critic #58's own reason for a one-track move (「Track A has no such row」) does not apply. The creative object is `docs/disc_null.md`'s null design: its one degree of freedom, the area, is **handed over from the model it is testing**, the rule and the interpretation were fixed in `4ab2e07` before the run, and the artifact was then used to refute the hypothesis that commissioned it (§4: by centre of mass the disc beats the model, which overshoots by 3,646.1 m against an observed 1,124.8 m). **19 and not 20:** one fire, one canvas, one threshold, and none of it is yet on a surface a judge at the booth will see. **구현 및 유용성 20, HELD and re-earned rather than inherited:** `gates.py --mode full` exits 0 at this exact head (**2041 passed**, 64 skipped, 3 xfailed, pytest 315.2 s), GitHub `auto-gates` runs 294 to 338 hold **36 `success`, 9 `cancelled` and ZERO `failure`** with run **338 green at this exact head**, `web/finals.html`'s stamp `4ab2e07` is **5** commits behind a 30-commit staleness limit, the printed kit hashes **7 of 7** sources against the tree and `release/kcf-finals-2026/MANIFEST.json` hashes **19 of 19**, all re-computed in this lap's own process. ⚠ **There is no 21, and there is also no rise:** the window's largest result reached **no** judge-facing surface. The disc null is in `docs/`, and WFG-235 (Q36) and WFG-237 (the document half) are both `todo`. **제출 자료 19, HELD**, on the same two rows Track B names: WFG-233 (`todo`) and WFG-236 (new). **개발 목적 19 and 설계와 방법론 20, HELD.** Nothing in the window touched what the tool is for or how it is built. **Track A has no 데이터 수집·분석·해석 row**, which is why WFG-228's payment lands only on Track B. **Pre-registered for critic #61:** 구현 및 유용성 is re-examined upward only when the disc null or the centroid overshoot reaches `web/finals.html` or `docs/auto/JUDGE_QA.md`; 제출 자료 reaches 20 when WFG-233 and WFG-236 both close. **Not scored:** the two Pass/Fail rows. |
| 2026-09-10 | `be39dea` | 19 | 20 | 20 | 19 | **18** | **96** | **The same one row moves for this track's identically worded criterion 「자료의 논리적 구성 · 그래픽 및 범례의 명확성 · 사용된 자료에 대한 출처 명기」, 제출 자료 19 to 18, and Track A is DOWN to 96.** The evidence is the Track B row above, unrepeated here; a criterion with one wording cannot honestly carry two values on one tree, which is critic #60's own reasoning applied in the other direction. **구현 및 유용성 20 HELD** and re-earned: the gates are green at this exact head, GitHub run 343 is green on it, and the bundle hashes 19 of 19. **개발 목적 19, 설계와 방법론 20, 창의성 19, all HELD**; nothing in the window touched what the tool is for, how it is built, or what it creates. **Pre-registered for critic #62:** the same two conditions as Track B. |
| 2026-09-10 | `f93af93` | 19 | 20 | 20 | 19 | **17** | **95** | **The same one row moves for this track's identically worded criterion 「자료의 논리적 구성 · 그래픽 및 범례의 명확성 · 사용된 자료에 대한 출처 명기」, 제출 자료 18 to 17, and Track A is DOWN to 95.** The evidence is the Track B row above and is not repeated here; a criterion with one wording cannot honestly carry two values on one tree. ⚠ **On this track it bites harder, for the reason critic #53 and #54 both gave and which holds again:** what is handed over is the release bundle and the printed kit. `release/kcf-finals-2026/MANIFEST.json` declares **19** files and I re-hashed all nineteen against their `source` paths at **19 of 19 matching**, and it names `printables/WFG_printables_20260910T1233Z.pdf`, whose **seven** source documents I re-hashed against the tree at **7 of 7 matching**. So the withdrawn household register is on the paper inside the box a judge carries away, and it is not only in the repository. **The dev lap's own bundle bookkeeping was correct and is worth recording as such:** `e8b6e01` re-pointed the `web/finals.html` entry's `sha256` in the same commit that rebuilt the screen, which is the discipline WFG-109 was filed to teach, and it is why R9 still ticks while this row falls. **구현 및 유용성 20, HELD and re-earned rather than inherited:** `gates.py --mode full` exits 0 at this exact head (**2054 passed**, 63 skipped, 3 xfailed, pytest 319.3 s, **up 12 passing tests** on critic #61's 2042 at `be39dea`, which is the twelve `tests/test_fold_evidence_registry.py` cases; ⚠ the dev lap reported **2052 passed / 65 skipped** at its own tree and this lap reads **2054 / 63** two prose commits later, so two of the twelve skipped there and ran here; collected is **2120** at both trees against **2108** at `be39dea`, which is exactly the twelve, so this is an environment-dependent skip and not a drift), GitHub `auto-gates` runs **332 to 351** hold **14 `success`, 6 `cancelled` and ZERO `failure`** with run **351 green at this exact head**, `web/finals.html` is offline by gate inside that same green run, and its rebuilt payload reads 539 / 481 registry entries against `docs/NUMBERS.json`. There is no 21. **Track A has no 데이터 수집·분석·해석 row**, which is why the window's registry work lands on neither table as a rise. **개발 목적 19 and 설계와 방법론 20, HELD:** no purpose and no method moved. **창의성 19, HELD:** the disc null's design still reaches no surface a judge at the booth will see. **Pre-registered for critic #63:** the same three conditions as Track B, and 구현 및 유용성 is re-examined upward only when the disc null or the centroid overshoot reaches `web/finals.html` or `docs/auto/JUDGE_QA.md`. **Not scored:** the two Pass/Fail rows. |
| 2026-09-11 | `0796336` | 19 | 20 | 20 | 19 | **19** | **97** | **The same one row moves for this track's identically worded criterion 「자료의 논리적 구성 · 그래픽 및 범례의 명확성 · 사용된 자료에 대한 출처 명기」, 제출 자료 17 to 19, and Track A is UP to 97.** The evidence is the Track B row above and is not repeated here; a criterion with one wording cannot honestly carry two values on one tree. ⚠ **On this track the rise is the more load-bearing of the two, for the reason critic #53, #54 and #62 all gave in the other direction:** what a judge carries away is the release bundle and the printed kit, so a register standing on paper cost this row harder than it cost Track B, and a register **measured** off the paper pays it back the same way. Both re-hashes were recomputed in this lap's own process: kit **7 of 7**, bundle **19 of 19**. **구현 및 유용성 20, HELD and re-earned rather than inherited, and critic #61's pre-registered condition for re-examining it is now MET and still yields no 21:** `web/finals.html` was rebuilt in the window and Q36 puts the disc null on a T0 card, which was the named trigger; there is no 21 because 20 is the row's ceiling. The gate evidence at this exact head: GitHub `auto-gates` run **356** is `success` on `0796336` at `fetch-depth: 0`, and `gates.py --mode full` in this sandbox exits **0** once the clone's extra remote-tracking refs are pruned (see the report's finding 5 — the sandbox-only abbreviation defect, 8 characters against 7 committed, repaired by `git update-ref -d` on the 11 side branches plus `git gc --prune=now`, 16,420 objects to 13,011, after which `build_timeline_roles.py --check` prints OK on the identical tree). **개발 목적 19, 설계와 방법론 20, 창의성 19, all HELD**; nothing in the window touched what the tool is for, how it is built, or what it creates, and 창의성's standing reason is narrowed to n = 1 rather than to reach. **Track A has no 데이터 수집·분석·해석 row**, which is why this window's registry work lands on neither table as a separate rise. **Pre-registered for critic #64:** the same two conditions as Track B. **Not scored:** the two Pass/Fail rows. |
| 2026-09-11 | `5482ba8` | 19 | 20 | 20 | 19 | 19 | **97** | **No row moves and Track A HOLDS at 97.** The 제출 자료 decision is the Track B row above, on the identically worded criterion 「자료의 논리적 구성 · 그래픽 및 범례의 명확성 · 사용된 자료에 대한 출처 명기」, and the evidence is not repeated here; a criterion with one wording cannot honestly carry two values on one tree. ⚠ **On this track the declined rise bites in the direction critic #53, #54, #62 and #63 all named:** what a judge carries away is the release bundle and the printed kit, so a contradiction standing **on the paper** is this track's defect more than Track B's. I re-hashed both objects in this lap's own process rather than reading them from the report: the kit's seven sources match 7 of 7, the bundle's nineteen entries match 19 of 19, and the bundle names the newest kit stamp, which is why **R7 and R9 still tick while this row holds**. **구현 및 유용성 20, HELD and re-earned rather than inherited:** `gates.py --mode full` exits 0 at this exact head on its **first** run (2092 passed, 65 skipped, 3 xfailed), `--assert-head` and `--assert-reported` both exit 0, GitHub `auto-gates` run **361** is `success` on `5482ba8` at `fetch-depth: 0` with **no** run in the window concluding `failure`, and the screen's embedded registry payload reads 543 / 485 against 543 / 485 in `docs/NUMBERS.json`, recounted here. There is no 21. ⚠ **One honest subtraction from that green run, stated because a critic gate is not the CI gate:** this clone is shallow at 54 commits and was deliberately not deepened, so `tests/test_timeline_roles.py:234` **SKIPS** rather than runs and `build_timeline_roles.py --check` declines; GitHub at `fetch-depth: 0` is what certifies that check, and it is green. **개발 목적 19, 설계와 방법론 20, 창의성 19, all HELD:** nothing in the window touched what the tool is for, how it is built, or what it creates. Track A has no 데이터 수집·분석·해석 row, which is why this window's registry work lands on neither table as a rise. **Pre-registered for critic #65:** the same 제출 자료 conditions as Track B; 구현 및 유용성 is re-examined downward if a kit source ever drifts against its manifest hash. Not scored: the two Pass/Fail rows. |
| 2026-09-11 | `f7ee58d` | 19 | 20 | **19** | 19 | **18** | **95** | **TWO rows fall and Track B is DOWN to 95, the first fall on 데이터 수집·분석·해석 since this row reached 20.** ⚠⚠ **데이터 수집·분석·해석 20 to 19 (WFG-255).** The criterion is 「데이터 수집 및 분석의 체계성 · 연구의 안정적인 결과 도출 가능성 · 수학 및 통계 방법의 적절한 적용 · **예상 결과에 대한 논리적인 해석 및 결론**」. I measured the object this project grades itself against, in this lap's own process, from `data/processed/routing_demo_canonical.npz` on the canonical 181×156 / 500 m grid with 8-connectivity: the `t = 0` observation is 249 cells in **226 connected components**, largest **3** cells, bounding box 23.5 km × 44.5 km; the **333-minute** observation that produces the headline IoU is 937 cells in **55 components**, largest 656 and second 135, bounding box 24.5 km × 45.0 km; the 360-minute forecast core is 952 cells in **37 components** over the same box. **No file in this repository states any of that** — a search for component / 연결 성분 / disconnected across `docs/`, `paper/` and `README.md` returns nothing about this array — while `docs/disc_null.md:153` tells a judge the model 「puts cells along the **arms the fire actually ran down**」 and `docs/oracle_gap.md` §4 turns its centroid argument on how far 「the fire」 moved. Those are interpretations of a multi-component detection field presented as readings of one advancing fire. The row does not fall to 18 because the analysis underneath is strong and honest: the disc null is pre-registered, has zero free parameters, publishes the seed-removed pair that costs it a fifth of its own headline, and §4 argues against the row that commissioned it. It falls one because a measurement this cheap should precede the interpretation, not follow it. ⚠⚠ **제출 자료 19 to 18 (WFG-254), and this is a false statement rather than an ambiguous one.** `docs/auto/JUDGE_QA.md:1513`, **Q36 · T0**, says out loud 「같은 면적의 원을 **발화점에** 놓고」. The same array carries `ign_xy`; `scripts/measure_disc_null.py` never reads it; the disc's centre is the centroid of the `t = 0` seed, **38.40 cells = 19.20 km** away, and the **largest** disc drawn at any slice has radius 18.162 cells. The finding survives the other row-origin convention too (31.60 cells = 15.80 km) and the data settles which is right, because only one candidate cell is burning at `t = 0`. **The card holds the correct rule two paragraphs above the wrong one, and the wrong one is the half said from memory.** The kit hashes **7 of 7** against the tree, so it is on page 25 of the paper in the box; the same locative is on `docs/oracle_gap.md:203`, `docs/disc_null.md:221`, `paper/manuscript.md:710`, `paper/README.md:1052`, and rendered as a bar-group heading into `paper/figures/F10_disc_null.png`, committed at `938dff7` in this window. **연구 목적 19, 설계와 방법론 20, 창의성 19, all HELD.** The method is right in `null_rule.centre_from` and in §2 of its own page; only the words naming it on the surfaces are wrong, which is a 제출 자료 defect and not a 설계 one. **Pre-registered for critic #66:** 제출 자료 returns to 19 when all seven surfaces name the centre correctly, the kit is rebuilt and the bundle re-pointed; 데이터 수집·분석·해석 returns to 20 when the component geometry is measured from the committed array, registered, and stated on the same pages that interpret it. **Not scored:** the two Pass/Fail rows. |
| 2026-09-11 | `f7ee58d` | 19 | 20 | 20 | 19 | **18** | **96** | **One row moves and Track A is DOWN to 96, on the identically worded 제출 자료 criterion 「자료의 논리적 구성 · 그래픽 및 범례의 명확성 · 사용된 자료에 대한 출처 명기」.** The evidence is the Track B row above and is not repeated here; a criterion with one wording cannot honestly carry two values on one tree. ⚠⚠ **On this track it bites harder, and on a half Track B does not have: 「그래픽 및 범례의 명확성」.** WFG-254's worst instance is not prose at all — `paper/make_figures.py:873` renders the group heading 「**distance moved from the ignition**」 over two bars whose values are `seed_to_observed_m` **1,124.8 m** and `seed_to_model_m` **3,646.1 m**, both measured from the seed centroid that sits 19.20 km from the ignition the heading names. That is a legend naming the wrong reference on a 300 dpi figure committed at `938dff7`. It is not yet referenced by `paper/manuscript.md`, which lowers its reach and not its wrongness, and CHARTER §3 rule 2 means the repair is a **new filename**. The other half is what a judge carries away: the kit hashes 7 of 7 and the bundle 19 of 19, both recomputed here, so the false locative is on paper and not only in the tree. **구현 및 유용성 20, HELD and re-earned rather than inherited:** `gates.py --mode full` exits 0 at this exact head on its **first** run (**2097 passed**, 65 skipped, 3 xfailed, pytest 493.5 s), `--assert-head` and `--assert-reported --base 6d4a60b` both exit 0 over 70 substantive paths, GitHub `auto-gates` run **364** is `success` on `f7ee58d` at `fetch-depth: 0` with **no** run in the 24-hour window concluding `failure`, `web/finals.html` is offline by gate inside that run, and the 발화점 defect does not reach the screen at all. There is no 21. ⚠ **One honest subtraction from that green run:** this clone is shallow at 50 commits and was deliberately not deepened, so `tests/test_timeline_roles.py:234` **SKIPS** rather than runs; GitHub at `fetch-depth: 0` is what certifies that check, and it is green. **개발 목적 19, 설계와 방법론 20, 창의성 19, all HELD:** nothing in this window touched what the tool is for, how it is built, or what it creates. **Track A has no 데이터 수집·분석·해석 row**, which is why WFG-255 lands on Track B only. **Pre-registered for critic #66:** the same 제출 자료 condition as Track B, plus the replacement figure existing under a new filename with a correct heading. **Not scored:** the two Pass/Fail rows. |
| 2026-09-11 | `d67de57` | 19 | 20 | 20 | 19 | **19** | **97** | **One row moves and Track A is UP to 97.** ⚠ **Track B falls two on rows this table does not have, and rises on the one it shares, so the two tracks diverge by exactly the rows the rubric words differently.** WFG-256's defect is an interpretation defect and **Track A has no 데이터 수집·분석·해석 row**; its nearest neighbour, 구현 및 유용성, asks whether the demo resolves the need, and an unmeasured attribution on a Q&A card does not stop the tool working. The 연구 목적 withdrawal is on a criterion this table does not have: 개발 목적 is worded differently (「해결할 실질적 문제 또는 필요성에 대한 명확한 설명」) and has read 19 since `0796336` with a reason. ⚠⚠ **제출 자료 18 to 19, on the Track B row's evidence and corrected mid-lap for the same reason.** On this track the criterion's other half, 「그래픽 및 범례의 명확성」, is what critic #65 conjoined its extra clause to, and **both halves are now met**: `paper/figures/F10b_disc_null.png` is the corrected figure under a new filename with a correct heading and `F10_disc_null.png` is kept byte for byte as a dated known-stale exception, which is CHARTER §3 rule 2 obeyed rather than worked around. And what a judge carries away is re-measured here at the rebased head: the kit is `WFG_printables_20260911T0939Z.pdf`, 59 pages, seven sources hashing **7 of 7**, and `release/kcf-finals-2026/MANIFEST.json` names it. **19 and not 20** for the same reason as Track B: the card's closing sentence still asserts an axis nothing measures. **구현 및 유용성 20, HELD and re-earned rather than inherited:** `gates.py --mode full` exits 0 at this exact head on its **first** run (2097 passed, 65 skipped, 3 xfailed, pytest 386.5 s), `--assert-reported --base ba76b8c` exits 0 over 68 substantive paths, GitHub `auto-gates` run **369** is `success` on `d67de57` at `fetch-depth: 0` with **no** run in the 24-hour window concluding `failure` (three `cancelled`, 344, 352 and 368, each superseded), the kit hashes 7 of 7 against the tree and the bundle names the newest kit at 59 pages. There is no 21. ⚠ **One honest subtraction from that green run:** this clone is shallow at 50 commits and was deliberately not deepened, so `tests/test_timeline_roles.py:234` **SKIPS** rather than runs; GitHub at `fetch-depth: 0` certifies it and is green. ⚠ **And one standing risk now measured rather than felt, which is why 20 is not safe:** **WFG-257**. The five-minute script has gone 1,684 to 1,799 spoken syllables against a fixed 300 s (5.61 to 6.00 syl/s), 마무리 · 한계 is now the **longest** segment at 64 s where it was the shortest at 45, and 3막, which the pace page calls 「이 프로젝트의 전부」, lost 17 of its 75 seconds without losing a word. That is 완성도 of the thing five judges actually experience. **구현 및 유용성 is re-examined DOWNWARD at critic #67 if the script grows again without the trade recorded**, and downward if a kit source ever drifts against its manifest hash. **개발 목적 19, 설계와 방법론 20, 창의성 19, all HELD:** nothing in this window touched what the tool is for, how it is built, or what it creates. **Not scored:** the two Pass/Fail rows. |
| 2026-09-11 | `dec00f4` | 19 | 20 | 20 | 19 | **18** | **96** | **One row moves and Track A is DOWN to 96, on the identically worded 제출 자료 criterion 「자료의 논리적 구성 · 그래픽 및 범례의 명확성 · 사용된 자료에 대한 출처 명기」.** The evidence is the Track B row above and is not repeated here; a criterion with one wording cannot honestly carry two values on one tree. ⚠⚠ **On this track it bites harder, for the reason critic #53, #54, #62, #63 and #64 all gave: what a judge carries away is the release bundle and the printed kit, and the false clause is ON THE PAPER.** Re-measured in this lap's own process rather than read from a report: `release/kcf-finals-2026/MANIFEST.json` hashes **19 of 19** against its sources and names `WFG_printables_20260911T1226Z.pdf`, whose seven `SOURCES` documents hash **7 of 7** against the tree, 59 pages. `docs/auto/JUDGE_QA.md` Q19's draft answer — the sentence the student speaks from memory — is on that paper telling a judge the fair-opponent comparison has never been run on 영덕, hours after this repository ran it and committed the artifact. **18 and not 17** because nothing was fabricated, no number moved, and the disproof was produced by this project itself in the same window; the defect is that the correction stopped one step short of every human surface, which is WFG-258. **구현 및 유용성 20, HELD and re-earned rather than inherited:** `gates.py --mode full` exits 0 at this exact head on its **first** run (**2127 passed**, 65 skipped, 3 xfailed, pytest 318.3 s), GitHub `auto-gates` run **374** is `success` on `dec00f4` at `fetch-depth: 0` with **no** run in the window concluding `failure` (one `cancelled`, 368, superseded), and the bundle and kit hash as above. There is no 21. ⚠ **Critic #66's pre-registered DOWNWARD condition on this row was checked and does NOT fire:** `docs/auto/DEMO_SCRIPT_5MIN.md` has not changed since `f7ee58d` and `data/processed/demo_script_pace/pace_20260911T0620Z.json` still reads **6.00** syllables per second, so the script did not grow. ⚠ **One honest subtraction from the green run:** this clone is SHALLOW at **50** commits and was deliberately not deepened, so `tests/test_timeline_roles.py:234` **SKIPS** rather than runs; GitHub at `fetch-depth: 0` certifies it and is green. **개발 목적 19, 설계와 방법론 20, 창의성 19, all HELD:** nothing in the window touched what the tool is for, how it is built, or what it creates. **Track A has no 데이터 수집·분석·해석 row**, which is why the window's one real result — the fair-opponent run — pays Track B and lands nowhere here; its nearest neighbour, 구현 및 유용성, asks whether the tool works, and the tool works. **Pre-registered for critic #68:** 제출 자료 returns to 19 when WFG-258 (a) closes **with the kit reprinted and the bundle re-pointed**, not when the tree is fixed and not on a report that says it was; 구현 및 유용성 is re-examined downward if a kit source ever drifts against its manifest hash or the script grows again without the trade recorded. **Not scored:** the two Pass/Fail rows. |
| 2026-09-11 | `d291364` | 19 | 20 | 20 | 19 | 19 | **97** | **No row moves and Track A HOLDS at 97.** 데이터 수집·분석·해석 has **no Track A row**, which is why this window's one real result lands on Track B only; its nearest neighbour, 구현 및 유용성, asks whether the demo resolves the need, and a buffer-sensitivity artifact does not bear on that. **제출 자료 19, HELD, on the identically worded criterion 「자료의 논리적 구성 · 그래픽 및 범례의 명확성 · 사용된 자료에 대한 출처 명기」**; the evidence is the Track B row above and is not repeated, because a criterion with one wording cannot honestly carry two values on one tree. ⚠ On this track the reason the row holds rather than falls is sharper: what a judge carries away is the bundle and the printed kit, and **the defect is on `README.md` only**. The printed kit and the release bundle were re-hashed here rather than read from a report: `WFG_printables_20260911T2137Z.pdf`, **60** pages, seven sources **7 of 7**, bundle **19 of 19**, and the bundle names that kit. R7 and R9 hold. **구현 및 유용성 20, HELD and re-earned rather than inherited.** **Gates re-measured in this lap's own process, not read from a report:** `gates.py --mode full` exits 0 at this exact head on its **first** run (**2181 passed**, 65 skipped, 3 xfailed, pytest 490.3 s), `--assert-head` exits 0, and `--assert-reported` exits 0 at every push boundary in the window. ⚠⚠ **And one red, stated rather than buried:** GitHub `auto-gates` run **386** concluded `failure` at `edd0ec0`, the first red run in nineteen laps, reproduced here as three `tests/test_finals_screen.py` failures of the form 「`6f866dd` is not a valid object name」, the rebase-orphan class. Run **387** is `success` at this head. The lap disclosed the push, superseded its own report and annotated the body, so CHARTER §4b sets no finding #1 and nothing judge-facing was ever wrong; what survives is a false 「by construction」 sentence in the record, filed as **WFG-263** (P1, held by §14b) with a measured instance appended to **NH-043**. ⚠ **One honest subtraction from the green run:** this clone is SHALLOW at **55** commits and was deliberately not deepened, so `tests/test_timeline_roles.py:234` **SKIPS** rather than runs; GitHub at `fetch-depth: 0` certifies it and is green. ⚠ **Critic #69's pre-registered DOWNWARD condition was checked and does NOT fire:** no kit source drifted against its manifest hash, and `docs/auto/DEMO_SCRIPT_5MIN.md` did not grow. ⚠⚠ **But WFG-262 is a 구현 및 유용성 risk on this track and is written down now so a later lap cannot call it new:** the A4 dispatch sheet is the thing 「해결하고자 하는 문제 해소 가능성」 is judged on, and it currently tells an operator the wrong thing about at least one household per region. **구현 및 유용성 is re-examined DOWNWARD at critic #72 if WFG-262 is still open**, and downward if a kit source ever drifts or the script grows again without the trade recorded. **개발 목적 19, 설계와 방법론 20, 창의성 19, all HELD.** **Not scored:** the two Pass/Fail rows. |
| 2026-09-12 | `e7ba085` | 19 | 20 | 20 | 19 | 19 | **97** | **NO row moves and Track A HOLDS at 97.** ⚠⚠ **구현 및 유용성 20, and the decision not to drop it is this row's whole content, so it is argued rather than asserted.** The criterion is 「설계가 의도한 목적에 부합성 · 구현된 작품의 완성도 · 해결하고자 하는 문제 해소 가능성」, and what a judge holds is the A4 sheet. **The case for falling:** `scripts/generate_dispatch_outputs.py:102` and `:134` print 「예산 내 차량 진입로가 화재로 차단됨(우회 포함)」, the same two-clause form `docs/routing_limitations.md` §1 removed from `fa_exceeds_budget` on 2026-08-10 and §6 removed from `no_safe_route` today; measured here, **17 of the 33** committed `dispatch_a4.html` under `outputs/dispatch/20260801T163042Z/` carry it, **1 of the 3** committed PDFs carries it, and `docs/auto/JUDGE_QA.md:1308` sends the student to open that directory in front of judges. **The case for holding, which wins:** the sentence is **not established false** — no lap has read the 439 arm's unreachable condition out of the code, and the WFG-262 lap explicitly refused to rewrite it by analogy, which was the right refusal. Meanwhile the walk sheet **strictly improved** in this window: `no_safe_route` now prints its code condition and nothing more, the pre-search refusal gets its own line, and `docs/live_pipeline.md`'s mapping table is bound cell by cell to `live/pipeline.py` by `tests/test_live_pipeline_doc_matches_code.py` after a month of silent drift. **A row does not fall in the window its artifact got better on a defect that was newly identified rather than newly created.** ⚠⚠ **Pre-registered DOWNWARD for critic #72: this row falls to 19 if WFG-264 is still `todo`**, because from that lap onward the project will have known for two days that a sentence on its own booth printable is unjustified and chosen not to spend a lap reading the condition. ⚠ **And the standing downward conditions were checked and do NOT fire:** no kit source drifted against its manifest hash (7 of 7, re-hashed here), the bundle is 19 of 19, and `docs/auto/DEMO_SCRIPT_5MIN.md` has not changed since `8952a5b`, so the script did not grow. ⚠ **Critic #70's own pre-registered condition is DISCHARGED and does not fire:** it set this row down to 19 at critic #72 「if WFG-262 is still open」, and WFG-262 closed at `77d44b6`. **개발 목적 19, 설계와 방법론 20, 창의성 19, HELD:** nothing in the window touched what the tool is for, how it is built, or what it creates. **Track A has no 데이터 수집·분석·해석 row**, which is why the window's measured null lands on Track B only. ⚠ **One honest subtraction from the green run:** this clone is SHALLOW at **52** commits and was deliberately not deepened, so `tests/test_timeline_roles.py:234` **SKIPS** rather than runs; GitHub at `fetch-depth: 0` certifies it and run **391** is `success`. **Not scored:** the two Pass/Fail rows. |
| 2026-09-12 | `cd18782` | 19 | 20 | 20 | 19 | **18** | **96** | **ONE row moves and Track A is DOWN to 96, on this track's identically worded 제울 자료 criterion 「자료의 논리적 구성 · 그래픽 및 범례의 명확성 · 사욨된 자료에 대한 제원 명기」 (`docs/auto/RUBRIC.md:34`).** Same evidence as Track B: the F8 caption and the F8 legend now say different things, created by `63e9d20`, two of three instances unfixed (WFG-266). A criterion with one wording cannot carry two values on one tree. ⚠⚠ **구현 및 유용성 20, HELD, and critic #71's pre-registered fall does NOT fire because its condition was not met.** That lap wrote 「구현 및 유용성 falls to 19 if WFG-264 is still `todo`」. WFG-264 is `done`, the emitter now prints only the code condition, and a reprint of the kit carries the corrected sentence. Held rather than raised, and the reason is WFG-267: the repaired sentence has **not** reached the three pre-built PDFs a judge is handed, cluster `02-천전공원-일대` is one of the 17 sheets that print the superseded line, and Q39 still instructs 3 pre-built plus 30 reprints, which is the instruction that guarantees two sentences for one code condition in one stack. That gap is newly **identified** rather than newly created, which is the same rule critic #71 applied, so it holds 20 and does not rise. ⚠ **Pre-registered for critic #73: 구현 및 유용성 falls to 19 if WFG-267 (i) is still `todo`**, because by then the project will have known for two days that the paper it hands five judges carries a sentence it has withdrawn in writing, and will have chosen not to spend the minutes it takes to say which page. **개발 목적 19, 설계와 방법로 20, 장의성 19, HELD.** |
| 2026-09-11 | `b6778e7` | 19 | 20 | 20 | 19 | **19** | **97** | **One row moves and Track A is UP to 97, on the identically worded 제출 자료 criterion 「자료의 논리적 구성 · 그래픽 및 범례의 명확성 · 사용된 자료에 대한 출처 명기」.** The evidence is the Track B row above and is not repeated here; a criterion with one wording cannot honestly carry two values on one tree. ⚠ **On this track the rise is the more load-bearing of the two, for the reason critic #53, #54, #62, #63, #64 and #67 all gave in the other direction:** what a judge carries away is the release bundle and the printed kit, so a false clause standing on the paper cost this row harder than it cost Track B, and a clause **measured off the paper** pays it back the same way. Both re-hashes were recomputed in this lap's own process: kit **7 of 7**, bundle **19 of 19**, bundle names the newest kit at 59 pages. **19 and not 20** for the same reason as Track B, and on this track it is sharper still: the withheld-reason clause is **spoken**, prescribed to all five judges beside the headline 42, and printed on page 25 of the one 인쇄물 in the booth. **구현 및 유용성 20, HELD and re-earned rather than inherited:** `gates.py --mode full` exits 0 at this exact head on its **first** run (**2149 passed**, 65 skipped, 3 xfailed, pytest 501.4 s, up 22 passing tests on critic #67's 2127 at `dec00f4`), GitHub `auto-gates` run **378** is `success` on `b6778e7` at `fetch-depth: 0` with **no** run in the window concluding `failure` (one `cancelled`, 368, superseded), and the kit and bundle hash as above. There is no 21. ⚠ **Critic #67's pre-registered DOWNWARD condition on this row was checked and does NOT fire:** `docs/auto/DEMO_SCRIPT_5MIN.md` and `docs/demo_script_pace.md` have not changed since `dec00f4`, so the script did not grow. ⚠ **One standing risk restated because it is now measured rather than felt:** nothing in this repository pins the script's **absolute** rate. `tests/test_demo_script_pace.py:120` pins the spread at 1.10 and `:143` refuses the comfort claim, and the script could reach 7.0 syllables per second with every gate green. That clause is added to **WFG-257** this lap. ⚠ **One honest subtraction from the green run:** this clone is SHALLOW at **50** commits and was deliberately not deepened, so `tests/test_timeline_roles.py:234` **SKIPS** rather than runs; GitHub at `fetch-depth: 0` certifies it and is green. **개발 목적 19, 설계와 방법론 20, 창의성 19, all HELD:** nothing in the window touched what the tool is for, how it is built, or what it creates. **Track A has no 데이터 수집·분석·해석 row**, which is why WFG-260's finding lands on Track B only. **Pre-registered for critic #69:** the same 제출 자료 condition as Track B; 구현 및 유용성 is re-examined downward if a kit source ever drifts against its manifest hash or the script grows again without the trade recorded. **Not scored:** the two Pass/Fail rows. |

## 2026-09-03 · `25f1e14..1113388` — why these numbers

**연구 목적 / 개발 목적 · 15.** The problem is specific, the population is specific, and
the frame (spread forecast → rescue-aware routing → decision layer) is the submitted one.
It loses points on its own opening paragraph: three scale figures with no URL, no registry
key, and one that exceeds the national total (CRITIC F5, WFG-043). A judge who checks the
motivation before the method finds the softest evidence in the repository first.

**설계와 방법론 · 15 (both tracks).** Leave-one-fire-out is the right design and is
enforced, variables and controls are named, and the negative results are kept rather than
buried. Held back by 기존 연구와의 차별점: the related-work table is WFG-026 and unstarted,
and the one related-work citation that exists is not the paper at its URL (F2). 일정·역할
(WFG-027) is also unstarted.

**데이터 수집·분석·해석 · 16 — the strongest row.** Six fires, event-held-out, an operating
point owned rather than hidden (pooled recall 0.138, three folds with zero true positives),
mean-of-folds and pooled kept apart as different quantities, a 296-entry registry that
re-derives, and a lineage gate that found four real instances the moment it was switched
on. It is not 18+ because the loop's own suite instrument is compromised: every recorded
pass/skip baseline is an unlabelled mixture of cold-run and warm-run readings (F8,
WFG-038/039), so the "no test was lost" comparison has been comparing different quantities.

**창의성 · 15.** The coupling — a calibrated held-out hazard field driving a time-expanded
pedestrian router with a rescue-ingress term — is the creative claim and it is real. Nothing
in this window added to it; the window was traceability and scaffolding work.

**제출 자료 · 11 — the weakest row, and the cheapest to move.** 출처 명기 is scored, and it
is where the repository is thinnest: the opening scale figures are unsourced (F5), one
`verified` citation is the wrong paper (F2), SRTM / ESA WorldCover / OpenStreetMap are used
and uncited in the manuscript (N4), and the finals screen, printables, README Round-4
section and release bundle (R1, R2, R7, R8, R9) do not exist yet. Logical 구성 is good; the
graphics are good; the sourcing is not.

**구현 및 유용성 · 11 (Track A only).** `web/finals.html` v2, the 5-minute script, the booth
recipe and the release bundle are all still `todo` (WFG-017, WFG-003, WFG-037, WFG-036).
This row is scored on what a judge can watch at the booth, and that artifact does not exist
in its finals form yet. It should move most between 09-07 and 09-14.

**Not scored here:** the two Pass/Fail rows (서류 구비, 위험성 검토). 서류 is the author's
(WFG-022 / NH-008); 위험성 is not at risk — the delivery layer stays dry-run by design.

## 2026-09-03 · `a278a56..0ff1b36` — why these numbers moved (critic #2)

Two rows moved and three did not. The window was one dev lap (WFG-020, the survivor
survey) and the first real paper lap (605 words to 7,204).

**데이터 수집·분석·해석 · 16 → 17.** `docs/evidence/greenpeace_2026_survey.md` plus
`data/processed/evidence/greenpeace_2026_survey.json` plus
`scripts/extract_survey_evidence.py`, which refuses to read a digit until the source
sha256 matches, is the first time this repository has taken a third-party measurement
and made the transcription itself machine-checkable rather than hand-typed beside a
hash. `tests/test_greenpeace_evidence.py` then binds both the evidence card and the
booth answer to the artifact, and its own docstring records the mutation that shows
where the binding stops. That is the behaviour the row scores. It is +1 and not +2
because the survey evaluates nothing about our model, which the card says plainly.

**제출 자료 · 11 → 13 (Track B), 11 → 12 (Track A).** 출처 명기 was named the weakest
and cheapest row at the baseline, on four specific defects. Two are closed: the
`verified` citation that was not the paper at its URL (F2) and the three uncited data
sources SRTM, ESA WorldCover, OpenStreetMap (N4). `references.bib` is now 21 entries,
each opened at its URL by the lap that added it. The manuscript is a real artifact a
judge can be handed. It is not more than +2 because F5 (the opening scale figures) is
untouched, F11 (21 citations, no bibliography) is new, and R1, R2, R7, R8 and R9 are
still ☐. Track A gets +1 rather than +2 because a manuscript is not what its judges
score.

**연구 목적 / 개발 목적 · 15, held.** The paragraph that states why the project exists
still carries ~116,000 ha (F5). One search, in the first thing a judge reads.

**설계와 방법론 · 15, held; 창의성 · 15, held.** Nothing in this window changed a method
or the coupling. The paper *describes* them better, which is the 제출 자료 row.

**구현 및 유용성 · 11, held (Track A).** No commit in this window touched `web/`, the
printables or the release bundle. Said explicitly so the manuscript is not read as
progress on the thing the booth judges actually watch.

## 2026-09-03 · `1c1561e..8d1decf` — why these numbers

**데이터 수집·분석·해석 · 17 → 18 (Track B).** `scripts/gk2a_detection.py` produces the
+22 / +34 / +64 minute figures and the 0-of-709 false-alarm bound, both of which are
quoted on judge-facing documents, and until this window it had no tests at all.
`tests/test_gk2a_detector.py` binds six registry keys to
`data/processed/detection/*.json`, reproduces the recorded 21.964 K contextual threshold
from the artifact rather than from a literal, and pins the geometry that makes 영덕's
교란 classification mean something. `docs/detection_floor.md` §12 then states, in bold,
which groups are synthetic and that their gains are the test's own and must not be cited
as GK2A calibration. It is +1 and not +2 because the tests mostly ask the code to agree
with itself, which §12 says first, and because the one group with outside ground truth is
opt-in and runs for nobody by default (N14).

**제출 자료 · 13, held (Track B); 12, held (Track A).** No citation, reference or
submission artifact changed. F11 (21 citations, no bibliography) is untouched because
`paper/` did not move.

**연구 목적 / 개발 목적 · 15, held.** F5 is in its third critic lap. The opening paragraph
still carries a burned area larger than the national total, and it is blocked on the
author's sources (NH-015), not on a lap.

**설계와 방법론 · 15, held; 창의성 · 15, held.** The `contextual_flag` extraction is
semantics-preserving by design and by inspection. Nothing in this window changed a method,
a coupling or an experimental arm.

**구현 및 유용성 · 11, held (Track A).** No commit touched `web/`, the printables or the
release bundle. Fourth consecutive window in which that is true, which is the one thing in
this table the sprint plan should worry about: the finals screen is what five judges
actually watch for five minutes, and WFG-017 is still ahead of the loop.

## 2026-09-04 · `9bf15fb..12b8ac7` — why these numbers

**연구 목적 / 개발 목적 · 15 → 14.** The first fall this table has recorded, and it is
deliberate. For three critic laps this row was held at 15 with F5 open: the opening
paragraph carried ~116,000 ha, unsourced and larger than the national total. That is a
figure a judge distrusts. `12b8ac7` replaced it with 45,157 ha, which is the 경북 provincial
interim tally on 2025-03-27, one day before 주불 진화 on 03-28, for a chain whose final
burned area is **99,289 ha** — and added a 범위 주의 note asserting that the 104,788 ha
nationwide figure belongs to a different event, when the chain is about 95 % of it. The
same commit left 영덕 **8명** standing in both languages twelve hours after this repository
corrected that figure to **10** in `docs/evidence/greenpeace_2026_survey.md` §7. One wrong
number a judge can find is a 15; two wrong numbers in opposite directions, one of which
contradicts the project's own committed evidence card, is a 14.

**데이터 수집·분석·해석 · 18, held.** Nothing in this window changed a method, a split, a
metric or an eval; `mandela` fires on nothing. The window's data work is traceability, and
it is good traceability: `tests/test_detection_floor_card.py` binds every figure on the new
card to `docs/NUMBERS.json` and binds each delay inside its own table row, so a swapped
attribution fails rather than passing on a substring. That is worth saying and it is not
worth a point, because the underlying result is the one already scored at `8d1decf`.

**설계와 방법론 · 15, held; 창의성 · 15, held.** No coupling, arm or protocol moved.

**제출 자료 · 13 (Track B) / 12 (Track A), held, on two moves that cancel.** Up:
`docs/auto/finals/DETECTION_FLOOR_CARD.md` is the first evidence card in this project a
judge can hold whose every digit is checked against the registry by a test, and the lap that
wrote it left a number **off** the card because it has no key (WFG-048) rather than typing
it. That is the sourcing standard this row has been asking for since the baseline. Down:
`docs/data_sources.md` gained a section of eleven figures across two tables with an 출처
column of agency names, a 기준일 column, and **not one URL, 보도자료 number or page** — in
the document whose title is 데이터 출처, closing the entry that existed to put sources under
exactly these numbers. The previous text at least named three checkable outlets. A judge
scoring 출처 명기 would read the card and the table in the same sitting.

**구현 및 유용성 · 11, held (Track A). Fifth consecutive window.** Five windows is no longer
a note, it is the sprint's main risk: the artifact five judges watch for five minutes does
not exist in its finals form, and every lap since 09-03 morning has improved something
those judges will never see. WFG-017 should be the next row after WFG-043.

**Not scored here:** the two Pass/Fail rows. 서류 is now the author's own decision under the
NH-008 closure (no contact with the 운영사무국 will be made); 위험성 is not at risk.


---

## Window 2026-09-04 `a4dc9a7..5a0466e` (critic #5)

**연구 목적 · 15, recovered, and capped there by one sentence.** Three laps spent a whole
window on the first paragraph a judge reads, and the figures are now right: checked cold
against four primary pages this lap, not one is wrong. The row does not go back to 16
because the paragraph still asserts, in both languages, that a like-for-like comparison
would put this fire at about 43 % of the national total. 산불영향구역 includes the unburned
ground inside the fire line and is normally the larger of the two statistics, so a 45,157 ha
영향구역 under a 99,289 ha 피해면적 is the relation inverted; the article the repository
cites for the definition calls the 45,157 an undercount that the survey more than doubled;
and `docs/NUMBERS.json` records the share as 94.8. One deleted sentence is the whole fix
(F21) and it is worth a point.

**제출 자료 · 15 (Track B) / 13 (Track A), the largest single-window rise in this table.**
The mechanism is what earns it, not the prose. A figure in the opening paragraph now has a
registry key, an artifact row with agency and as-of date and scope and status, the URL a lap
opened, and the sentence that page actually said; two gates hold the paragraph to it; and
the sourcing rule was applied where it cost something, with two figures removed rather than
kept at lower confidence. A judge who asks 출처가 어디입니까 can be shown a chain from the
screen to the page in four steps.

**설계와 방법론 · 15, 창의성 · 15, 데이터 · 18: held.** No model, split, metric, arm or
protocol moved. Said explicitly so 44 new tests are not read as scientific progress.

**구현 및 유용성 · 11 (Track A), sixth consecutive window.** Eleven days of sprint remain
and the finals screen has not been touched since 09-03. Every lap since then improved
something these five judges will never see. This is now the largest single risk in the
sprint plan, larger than any finding in this report.

**Not scored here:** the two Pass/Fail rows. 서류 is the author's own under the NH-008
closure; 위험성 is not at risk.

---

## Window 2026-09-04 `5a0466e..b855943` (critic #6)

**연구 목적 / 개발 목적 · 15 → 16.** The first 16 in this table. Three critic laps in a row
named the opening paragraph as the softest evidence in the repository; it is no longer that.
Every figure has a registry key, an artifact row carrying agency and as-of date and scope and
status and the sentence the page actually said, a URL a lap opened, and two gates holding the
prose to all of it — and as of this window the paragraph also makes no *claim* about those
figures that is not sourced. What earns the extra point rather than a hold is how the last
sentence went: the lap deleted it, checked the critic's argument for deleting it, found one
of the critic's three premises was wrong (산림청 itself says 산불영향구역 and 피해면적
「개념이 달라서 단순 비교할 수 없고」, so the directional argument does not hold), and wrote
the agency's wording into `docs/data_sources.md` 함정 1 and 함정 6 instead of the critic's.
A loop whose reviewer can be wrong and whose lap checks it is a different object from a loop
that obeys its reviewer.

**데이터 수집·분석·해석 · 18, held, on two moves that cancel.** Up: `paper/manuscript.md`
§4.7 is a real new result written the way this project's best work is written — a negative
control with its numerator at zero reported as an upper bound and not a rate, a sub-pixel
size floor given as an order of magnitude because the flame-temperature assumption moves it
eightfold, one of the four fires excluded as confounded rather than counted either way, and
an n = 3 comparison labelled a description of three events rather than an operating
characteristic. The same lap's reviewer blocked an ordering claim it could not source and
the lap **reverted the claim rather than repeating it**, filing `paper/GAPS.md` G5 and
NH-019. Down, and exactly offsetting: the repository now holds two answers to that same
question. `docs/detection_floor.md` §9, `docs/auto/finals/DETECTION_FLOOR_CARD.md` and
`docs/auto/JUDGE_QA.md` Q10 all still assert that the satellite rang after the telephone,
and the manifest they measure from calls the reference field the ignition (F27, WFG-053).
A row scored on 재현 가능성 and 논리적 해석 cannot rise while the paper and the booth card
answer one question differently.

**설계와 방법론 · 15, held; 창의성 · 15, held.** No model, split, metric, arm, coupling or
protocol moved. Said explicitly so that a 938-line manuscript rewrite is not read as
scientific progress: it is 제출 자료 work and one new result, and the new result is §4.7.

**제출 자료 · 15 (Track B) / 13 (Track A), held.** The references work in this window is the
best sourcing this repository has produced. `references.bib` gained five entries, and the
WWA note does something no gate asked for: it re-opened its own URL and corrected the
paraphrase the previous lap's note carried, which had rendered 「Uiseong was hardest hit,
with 26 deaths, while four occurred in Sancheong」 as "26 of them in Uiseong-gun" — turning a
fire attribution into a county one and dropping the four deaths that reconcile 26 with 32.
Against that: F23 is unfixed in its second window and has now **propagated**. 사망 26명 is a
중앙재난안전대책본부 count in the registry, a 경상북도 재난안전대책본부 count in
`docs/data_sources.md:190`, and "the provincial disaster headquarters' count" in
`paper/manuscript.md:37`, and the README link a judge would click still carries no death
figure at all. And the manuscript is 7,479 words against a 7,500 hard fail, enforced as a
proxy for a 20-page limit the loop's own recount puts at about 21 pages (F29). A row scored
on 출처 명기 and 논리적 구성 does not rise on excellent citations while the same window leaves
one figure with three agencies and a length gate measuring the wrong quantity.

**구현 및 유용성 · 11 (Track A), seventh consecutive window, and this is now the report's
second finding rather than a note.** Since 2026-09-03 morning, every lap has improved
something these five judges will never see. `KCF_READINESS.md` is the definition of done for
the final product and it has **2 of 11 lines ticked**. Eleven days of sprint remain. The
backlog table order puts WFG-017 next once WFG-021 stops reading as `todo`, which this lap
fixed by parking its unrunnable part; nothing else stands between the loop and the screen.

**Not scored here:** the two Pass/Fail rows. 서류 is the author's own under the NH-008
closure; 위험성 is not at risk, the delivery layer stays dry-run.
