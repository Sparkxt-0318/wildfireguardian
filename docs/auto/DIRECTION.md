# Direction — where the project is going, on one screen

*Re-checked 2026-09-12T0157Z by critic #71 at `e7ba085`. Last rewritten 2026-09-10T1817Z by the research routine (CHARTER §14). The dev routine reads this before it claims a row; the critic re-checks it after every dev lap, at most one reorder per lap, with a reason. Only the most recent critic note is kept. **Critic #71 SPENT its one §3b row move, on `WFG-264`: P1 to **P0**, table row 259 to position 1. The reason is below and it is a reading of CHARTER §14b, not a preference; the author's half is **NH-060**.*** <!-- forbidden-ok: 264, this is the BACKLOG ROW ID WFG-264 and not the gangneung_donghae_2022 Build-A positive count the gate anchors that value to. Same false-positive class as ba437f5, where the gate read a lap stamp as a camera count. No figure is asserted on this line. -->

## Thesis (two sentences)

WildfireGuardian forecasts where an already-burning Korean wildfire goes next, and turns that forecast into a per-point walk-or-be-rescued decision for rural elderly residents, on public data a stranger can re-derive. Its defensible contribution is not forecast accuracy and not the architecture: it is the **output object and its measured limits**, a time-dependent decision per point with a page saying how wrong it can be.

⚠ Bokade et al. published this project's whole pipeline on **2026-09-09** (10.5281/zenodo.22668357). Nothing here rests on it, because WFG-251 narrowed Q16d to 「**공개된 기록과 초록에는** 성능 수치가 하나도 없습니다」, which is safe whether or not a lap can reach the record. **Claim the measurement, never the architecture.**

## Before any row: this lap's `fix-before-next-row` item

**NONE. Critic #71 found no minutes-sized defect on a judge-facing surface and no red gate, and spending an item it does not have would displace the top row for nothing.** CHARTER §14b says 「at most ONE per critic lap」, not one every lap, and §7 says a lap that verifies and finds nothing to change reports exactly that. What it checked, so the next lap does not re-check it: the judge-facing set (README, `docs/auto/JUDGE_QA.md`, `docs/auto/DEMO_SCRIPT_5MIN.md`, `docs/auto/finals/`, `web/finals.html`, `paper/manuscript.md`) carries **zero** hits of either sentence this window replaced, `gates.py --mode full` exits 0 on its first run, and GitHub `auto-gates` run **391** is `success` at `664ca6f`.

⚠ **Critic #70's item is genuinely CLOSED and this is not a re-open.** Re-measured here at `e7ba085`: `README.md`'s Round-4 bullet now reads 「곧 완충거리를 폭까지 훑어 정식 상대로 세운 쪽에만 해당합니다」 at the scope clause, which is what `:339` names, and the duplicated 「거기서 나온 수치는 이 README 에 옮겨 적지 않습니다」 is down to one copy, the one carrying the reason. `docs/auto/JUDGE_QA.md` was deliberately untouched, so **NH-049**'s reprint requirement never fired.

⚠ **The lap ALSO caught its own report and the next lap should know how.** Its `report.py` run stamped a **RED, stale** gate table (`09eea5b`, a commit no clone can resolve) into the body and into the email header, on a branch that was green. It noticed before the email went out, re-ran the gates at the pushed head, superseded the 0135Z body under CHARTER §3.7 rather than editing it, and re-stamped as 0146Z, **ALL GREEN at `664ca6f`**. Nothing wrong reached the author. That is the correct handling of the `report.py`-before-gates ordering and it cost a second gate cycle; **WFG-263** is still the row that would stop it costing one.

⚠ **Do NOT `git fetch --unshallow` in a critic or dev sandbox, and CHARTER §4 does not ask you to.** §4 forbids writing an ancestry claim from a shallow clone; it does not require deepening. Unshallowing drags in eleven side branches and turns `tests/test_timeline_roles.py`'s history check RED on a tree that is fine (critics #60 and #63 both paid). **RE-STATED by critic #71 after re-running the case in its own process, which is what CHARTER §14c requires for this note to survive: the clone is shallow at **52** commits and was NOT deepened, and `gates.py --mode full` exits 0 on its FIRST run** at `664ca6f` (2202 passed, 65 skipped, 3 xfailed, pytest 311.2 s). The cost, stated: in a shallow clone that history check **SKIPS** rather than runs (`tests/test_timeline_roles.py:234`), so a green critic gate does not certify it; GitHub at `fetch-depth: 0` does, and run **391** is green at `664ca6f`. Recorded on **WFG-217**. This note names those file lines and that measurement and expires at critic #72 unless that lap re-runs the case.

## Next three rows, and why each is next

1. **WFG-264 (P0, KCF), re-filed at position 1 by critic #71, and it IS this lap's root objection.** The thesis on this page says the contribution is 「the **output object and its measured limits**」. `docs/routing_limitations.md` now holds **six** numbered audits of dispatch-sheet sentences and **every one of them is on the 459 walk series**. The sheet the booth actually opens is the **439 vehicle** series: `docs/auto/JUDGE_QA.md:1308` tells the student 「실물을 열어 드릴 수 있습니다 — `outputs/dispatch/` 에 마을별 A4 출동 지시서」, and Q39 at `:1554` says **30 of its 33** sheets are reprinted for the booth with `scripts/generate_dispatch_outputs.py`, whose `:102` and `:134` emit 「예산 내 차량 진입로가 화재로 차단됨(우회 포함)」. That is the same two-clause form §1 removed from `fa_exceeds_budget` and §6 removed from `no_safe_route` on 2026-09-12. **Measured here: 17 of the 33 committed `dispatch_a4.html` print it, 1 of the 3 committed PDFs carries it, and no file in the repository says what its code condition is.** ⚠ **It is NOT established false and must NOT be repaired by analogy** — that is half (a) of the row and the trap WFG-262 pre-registered against. **The job is to read the 439 unreachable condition out of the code and write it down, the way §1 and §6 did.** <!-- forbidden-ok: 264, this is the BACKLOG ROW ID WFG-264 and not the gangneung_donghae_2022 Build-A positive count the gate anchors that value to. Same false-positive class as ba437f5, where the gate read a lap stamp as a camera count. No figure is asserted on this line. -->

⚠ **Why this displaced WFG-256, and it is a reading of CHARTER §14b rather than a preference.** The lap that found it filed it **P1 at table row 259 of 258**, citing §14b's readiness hold. §14b names what waits: 「loop hygiene (report certification, gate-on-gate, commit-id bookkeeping, mechanics of the routines)」. A booth printable is not on that list, and the same sentence of §14b says 「anything larger, however judge-facing, is filed as a **P0 row at position 1** of the table」. R3 is the only one of the six ticks outstanding, it is `blocked(NH-046)`, NH-046 came due **2026-09-10**, no reply has ever arrived by email, and the sprint ends **2026-09-15** — so filed as it was, the row could not have been started at all. The author's half is **NH-060**, with options both ways; if the author picks B the row goes back to P1 and WFG-256 returns to position 1.

2. **WFG-256 (P0, science)** second, the rotation null, still `todo`, still the only thing that would license a sentence about 「모양」, then **WFG-255**, then **WFG-236**, **WFG-244**, **WFG-245**, **WFG-237**.

3. ⚠ **WFG-234 remains the strongest science row after those**, and WFG-256 is its cheap sibling: a persistence null scored against the headline truth, where a bad result is a **named, published failure mode of the whole model class**. Pre-register it in the claim commit.

⚠ **The board at `e7ba085` after this lap: 259 rows, 14 P0 `todo`**, measured here with the rule written beside it: split each `| WFG-` line on pipes **not** preceded by a backslash, strip each cell, count a row P0 when cell 2 begins `**P0` and `todo` when cell 5 begins `todo` (a residue note after `todo` still counts). 14 again, and the arithmetic is not standing still: **WFG-262 closed** (14 to 13) and **WFG-264 was raised from P1 to P0** (13 to 14). One P1 row (**WFG-265**) was added, which does not touch the P0 count. ⚠ The two counting rules in this repository's history disagree and neither count is the product. That is **WFG-191** and it is the whole answer. Do not publish a board count without the rule that produced it and do not spend a lap reconciling one. <!-- forbidden-ok: 264, this is the BACKLOG ROW ID WFG-264 and not the gangneung_donghae_2022 Build-A positive count the gate anchors that value to. Same false-positive class as ba437f5, where the gate read a lap stamp as a camera count. No figure is asserted on this line. -->

## What not to do

- **Do not start a P1 row while a P0 row is `todo`**, and do not file one above the P0 block. ⚠ **Critic #71's measurement, flagged and NOT fixed because it would cost the lap's one §3b move:** `WFG-263` (P1, infra, `todo`) sits at table position 2 among `todo` rows, above `WFG-256` (P0, `todo`), filed there by critic #70 against this very line. It is harmless today because CHARTER §4 step 3 takes the highest-**priority** `todo` row and not the highest table row, and this page names the row anyway. The next lap with a spare move puts it below the P0 block.
- ⚠⚠ **Before you call a row done, grep the judge-facing set for the NEGATION of what you just measured** — README, `docs/auto/JUDGE_QA.md`, `docs/auto/DEMO_SCRIPT_5MIN.md`, `docs/auto/finals/`, `web/finals.html`, `paper/manuscript.md`. One command. It caught WFG-258 and it would have caught WFG-138.
- ⚠⚠ **NEW, critic #69: and then grep the file you just edited for the sentences that DESCRIBE it.** A card carries notes about itself, and a substitution nine lines above one of them falsifies it silently. That is the whole of this lap's item, and it is CHARTER §5c's failure mode inside a single file for the second consecutive lap.
- ⚠⚠ **NEW, critic #70, and it is the third surface of the same class in three laps: grep for the sentences that DEFINE the terms your edit uses, not only the ones that describe it.** `README.md:330-331` is a **correction's scope clause**: it says which claim the correction leaves standing, and the lap that narrowed the claim eleven lines below never touched it. A registered spelling cannot catch this, because the stale sentence never spells the claim. **The test is semantic and the command is one grep: after narrowing a claim, grep the file for every other sentence naming the OLD subject.**
- ⚠⚠ **NEW, critic #70: if you rebase after building `web/finals.html`, run `make finals` again BEFORE you push.** That is the gate's own failure message (`tests/test_finals_screen.py:596-599`), and not doing it put a RED head on `origin` and a 「Run failed」 email in the author's inbox at `edd0ec0` (GitHub run 386). **Never push a red head to `auto/dev`** (CHARTER §3.9); park on `auto/red/<stamp>`. The one genuinely unsatisfiable case is `test_the_escape_this_gate_cannot_close_is_still_open` and it is **NH-043** / **WFG-161** / **WFG-263**, not a licence.
- ⚠⚠ **Do not write an internal entry id, or the reason an entry is open, onto a judge-facing surface** (critic #68). `NH-###`, 「저자에게 열려 있습니다」, 「아직 정하지 않아서」 are the loop talking to itself. `README.md:50`, `:265`, `:351` still carry **NH-053** this way and are out of scope; they are the author's to rule on.
- ⚠ **Do not put any `ppy_yeongdeok_*` count on a judge-facing surface** while **NH-059** is open, and **do not point at the section that prints them** either. The artifact says 「It is NOT a margin」 and that is true, but a judge hearing 26-of-44 will compute one, and NH-032, NH-034 and NH-052 are open.
- ⚠ **Do not widen WFG-259 into WFG-033(b) or NH-027.** A **buffered** present-perimeter opponent scored as the opponent of record is the author's; WFG-259 only makes an existing sentence re-derivable.
- ⚠ **Do not weaken `docs/present_perimeter_yeongdeok.md` §5.** Its eight items are the best self-criticism in the repository this week. WFG-259 makes item 5 re-derivable; it does not soften it, and **item 6's scoring-side conclusion is TRUE and stays word for word**.
- ⚠⚠ **Do not unshallow the clone.** See the note above.
- ⚠ **Do not re-open WFG-254 or WFG-258.** WFG-258 closed across every surface with the kit reprinted and the bundle re-pointed, re-hashed by critic #69. The 「모양」 half of WFG-254 is **WFG-256**.
- ⚠ **Do not weaken `docs/disc_null.md` §4's centroid finding**, and do not weaken 「방향은 아닙니다」: that half IS measured.
- ⚠ **Do not regenerate `paper/figures/F10_disc_null.png`** (CHARTER §3 rule 2). `F10b_disc_null.png` is the corrected file.
- ⚠ **Do not touch `docs/submission_reconciliation.md:63`.** Its 발화점 is about how the canonical array was seeded and is correct.
- ⚠ **Do not write 「여러 개의 산불」, or any count of fires, from WFG-255's measurement alone.** The row measures components, not fires. ⚠ And do not read WFG-260's **226** components as a count of fires either: it is one fire's detection scatter on one 500 m grid at one time.
- ⚠ **Do not edit a scorecard row another lap wrote** (CHARTER §3.7). See **WFG-252**.
- ⚠ **Do not add a syllable to `docs/auto/DEMO_SCRIPT_5MIN.md` without saying which segment pays for it** (**WFG-257**; the script is at 6.00 syllables per second, unchanged since `f7ee58d`, re-checked at `8952a5b`, and 마무리 is its longest segment). Nothing in this repository pins the **absolute** rate, only the spread. <!-- collision-ok: 6.00 — the SPOKEN RATE of the whole script in syllables per second, which is `syllables_per_second` in `data/processed/demo_script_pace/pace_20260911T0620Z.json`. The gate reads it against the `demo_pace_*_rate_spread` keys (1.02, 1.03, 1.62), which are the RATIO of the fastest segment's implied rate to the slowest (`implied_rate_spread`, unit x, max over min). Two different quantities, and this line names both on purpose: the point of the sentence is that the ratio is gated and the rate is not. Neither value is stale. -->
- ⚠ **Do not "fix" WFG-249 by weakening Q20a's privacy answer.** ⚠ **Do not swap 가구 for 지점 in the demo closing, and do not swap either count** (the denominator is `l0i_failing_denominator_h240` **24**, not the **124**-building population).
- ⚠ **Do not open, cite or characterise the Bokade et al. body PDF**, and do not widen Q16d beyond 「공개된 기록과 초록」.
- ⚠ **Do not touch** Q16's 「가구 단위 폐쇄 시각」, Q16a's lines, the ⭕/❌ pair beside them, or the panel's two deliberately-kept 「집」 lines at `RELATED_WORK_PANEL.md:39-41` (`WC-013`, `WC-008`).
- ⚠ **Do not "re-fix" the household register.** Read `docs/auto/withdrawn_claims.json` before editing any 가구 line; the shipped checker is line-based so a reworded assertion escapes.
- ⚠ **Do not claim the architecture as the contribution.** Claim the output object and its measured limits. **WFG-239** writes the sentence.
- ⚠ **Do not write the 「위험 구역」 / 「잠재적 위험 구역」 zoning framing.** Recorded UNVERIFIED.
- ⚠ **Do not compare accuracy with any domestic system or study**, NIFoS, G-DAPS or the Kangwon National University DL model (**NH-056**).
- ⚠ **Do not quote the model-only IoU pair on any judge-facing surface without the seed-removed pair in the same block** (Q36 carries both).
- ⚠ **Do not edit `README.md`'s IoU bullet (`:520-522`, `:888-891`) while NH-055 is open**, and do not delete the Rothermel comparison in either direction.
- ⚠ **`README.md`'s TL;DR lead is frozen by NH-054 on its ORDERING AND PROPORTION, not on the truth or the length of a parenthetical inside it** (narrowed by critic #67, re-stated by #68 and #69; the measurement is NH-054's own 433-against-1,853 character count). Never rewrite its opening paragraph about the 2025 fire (CHARTER §3.5b).
- ⚠ **Do not edit `docs/auto/JUDGE_QA.md`, `docs/auto/DEMO_SCRIPT_5MIN.md` or `docs/auto/finals/RELATED_WORK_PANEL.md` without `make printables` at a new stamp and a re-pointed `MANIFEST.json`** (NH-049). **RE-STATED by critic #71 after re-hashing in its own process at `e7ba085`**, which is what CHARTER §14c requires: the kit's seven sources hash **7 of 7** (`WFG_printables_20260911T2137Z.pdf`, **60** pages) and the bundle's nineteen entries hash **19 of 19**, and the bundle names that kit. This note expires at critic #72 unless that lap re-measures.
- ⚠ **Do not write 「the fire stopped growing at 333 minutes」.** No observation exists between 0 and 333 minutes (WFG-230).
- ⚠ **Do not "fix" 「household-level」** in `README.md:3`, `CITATION.cff:5`, `src/wildfireguardian/__init__.py:1` or `paper/manuscript.md:1`; they name the **application** (WFG-231).
- ⚠ **Do not cite `data/processed/spread_v2/audit.json` for any number** (WFG-244).
- ⚠ **Do not rebuild `data/processed/timeline_roles/timeline_roles.json` from a deepened clone** (WFG-217; deadline 10-16). The refreshing lap works from a **single-branch full clone**.
- ⚠ **Do not weaken 「실제 확산면으로 만든 출동 지시서는 아직 없습니다」** on any surface (**NH-057**, **WFG-242**).
- **Do not settle 「상한」 / 「upper bound」 in any lap** (NH-053 open). Describe the mechanism.
- ⚠ **Do not put a margin value on any judge-facing surface** while NH-032, NH-034 and NH-052 are open.
- ⚠ **Do not add a word to `paper/manuscript.md` without trimming one** (NH-037, open).
- **Do not change `mr_uiseong_fa_exceeds_budget`** (NH-031 A; registry half is WFG-122).
- **Do not refit anything, and do not regenerate a committed artifact** (CHARTER §3 rule 2).
- **Do not re-open the AI-disclosure question** (NH-008 closed; CHARTER §9).
- **Do not re-propose international portability or real-time weather** (`docs/HANDOFF_ROUND3.md` §13-§14).
- **Do not let a number from a knowledge note reach a card, the README, the manuscript or `docs/NUMBERS.json`** (CHARTER §13).

## Readiness and open decisions

`docs/auto/KCF_READINESS.md` stands at **8 of 11**, unchanged: R1, R2, R4, R5, R6, R7, R8, R9 tick; R3, R11 and R12 do not; R10 was withdrawn 2026-09-04. ⚠⚠ **ZERO lines ticked for the TWENTY-EIGHTH consecutive critic lap.** Verified here the cheap way: `git diff 8952a5b..e7ba085 -- docs/auto/KCF_READINESS.md` is **empty**, and the file's newest commit is still `c241904`, critic #66. ⚠ **The arithmetic of the hold is unchanged and it is now this page's root objection twice over.** CHARTER §14b releases the P1 hygiene block when **R1, R3, R4, R7, R8 and R9** tick. **Five of those six are ticked; R3 is the only one that is not.** R3 is `blocked(NH-046)`, NH-046 is a three-option question that came due **2026-09-10**, and R11's WFG-024 is held behind it. **One unanswered email holds the ninth tick, the whole P1 block, and — until this lap moved it — a defect on a booth printable** (WFG-264, NH-060). The sprint ends 2026-09-15. <!-- forbidden-ok: 264, this is the BACKLOG ROW ID WFG-264 and not the gangneung_donghae_2022 Build-A positive count the gate anchors that value to. Same false-positive class as ba437f5, where the gate read a lap stamp as a camera count. No figure is asserted on this line. -->

**Open decisions: 27 for the author** (26 DECISION + 1 BLOCKER), **2 undated** (NH-005, NH-014), plus 5 open FYI. Say 「N of 27, and 2 undated」 rather than a bare number. **NH-046, NH-049 and NH-051 are past due.** **NH-057**, **NH-059** and **NH-060** are the highest-severity open entries; **NH-060** is new this lap and is the one that decides where WFG-264 sits. ⚠ **No decision has EVER arrived on the email channel:** `decisions_seen.json` records `"seen": []`, every applied decision came through a Claude Code session on the laptop, and the newest is **NH-031, received 2026-09-06**, six days ago. Critic #71 re-confirmed at the Gmail connector that the **30** newest threads matching the report subject in the last 14 days each carry exactly one message and every one is the loop's own send, and that PR #31's comment list is **empty**. That is **WFG-211**, already `todo`, confirmed here and not re-filed. The sprint ends 2026-09-15, **three days out**. <!-- forbidden-ok: 264, this is the BACKLOG ROW ID WFG-264 and not the gangneung_donghae_2022 Build-A positive count the gate anchors that value to. Same false-positive class as ba437f5, where the gate read a lap stamp as a camera count. No figure is asserted on this line. -->

## Critic's last direction note

**2026-09-12T0157Z, critic #71, reviewed `e7ba085`. ONE §3b row move was spent, on WFG-264.** WFG-262 closed with its premise measured false, so the root objection this lap found is a **priority** rather than a defect: **WFG-264 raised P1 to P0 and moved from table row 259 to position 1**. ONE new P1 row (**WFG-265**), ONE new NEEDS_HUMAN entry (**NH-060**, the first in three laps, and it exists because two routines read one sentence of CHARTER §14b differently), **ZERO** `fix-before-next-row` items. <!-- forbidden-ok: 264, this is the BACKLOG ROW ID WFG-264 and not the gangneung_donghae_2022 Build-A positive count the gate anchors that value to. Same false-positive class as ba437f5, where the gate read a lap stamp as a camera count. No figure is asserted on this line. -->

**The one thing that matters on this page.** The lap under review did the hardest thing an agent can do with a task: it was handed a premise, it read code that contradicted the premise, it **wrote both interpretations into the claim commit before any number existed**, it measured, and it published the answer that made its own assignment less dramatic. Zero, on all three regions, structurally. It then shipped the sentence repair anyway, because that repair never depended on the premise, and said so. Nothing in this note takes any of that back, and no lap in this record has scored higher on 「변수의 정의와 제어의 적절성」.

**And the same lap shows where the product is thin, in its own reviewer's words.** The reviewer's root objection was that the repair was **instance-scoped again** — verbatim the lesson `docs/routing_limitations.md` §6 names about itself. The lap fixed one of the two instances and filed the other, correctly refusing to rewrite a sentence whose condition nobody has read. **What it got wrong was where it filed it.** `docs/routing_limitations.md` now holds six numbered audits of dispatch-sheet sentences and **all six are on the 459 walk series**, while the sheet the booth physically opens is the **439 vehicle** series, which carries the same two-clause form on 17 of 33 committed sheets and on 1 of the 3 committed PDFs, with no file saying what its condition is. That row went to **P1, table row 259 of 258**, held behind readiness ticks that one unanswered email has frozen since 09-10, three days from the end of the sprint. **The project audits the sheet it publishes and not the sheet it hands over, and §14b's own sentence says a judge-facing item larger than minutes is a P0 row at position 1.**

**The second thing, and it is in the loop's favour for once.** `report.py` stamped a **RED, stale** gate table into the 0135Z body and into the email header on a branch that was green, naming `09eea5b`, a commit no clone can resolve. The lap caught it **before the email went out**, re-ran the gates at the pushed head, superseded its own body under CHARTER §3.7 rather than editing it, and re-stamped as 0146Z, ALL GREEN at `664ca6f`. Nothing wrong reached the author. **WFG-263** remains the row that would stop this costing a second gate cycle each time.

**Scorecard: NO row moves. Track B HOLDS at 96, Track A HOLDS at 97.** The window's work is a re-earning of 설계와 방법론 20 and 데이터 수집·분석·해석 20 rather than a rise past them, and 구현 및 유용성 **holds at 20 and does not fall**: the walk sheet strictly improved, and the vehicle sentence is newly **identified**, not newly created, and is **unestablished rather than false**. ⚠ **Pre-registered DOWNWARD for critic #72: 구현 및 유용성 falls to 19 if WFG-264 is still `todo`**, because by then the project will have known for two days that a sentence on its own booth printable is unjustified and chosen not to read the condition.

Verified at `e7ba085`, the head reviewed and the head this lap ships in. **GitHub `auto-gates` run 391 is `success` at `664ca6f`**; run **392** was still `in_progress` at `e7ba085` when this was written, and **no run in the window concluded `failure`** (386 at `edd0ec0` was critic #70's and is outside this lap's window boundary; 381 was `cancelled`). `gates.py --mode full` exits **0** on its FIRST run in this sandbox (2202 passed, 65 skipped, 3 xfailed, pytest 311.2 s), `--assert-head` and `--assert-reported` both exit 0, **every one of the fifteen reports in the window records `Reviewed by:`**, the printed kit hashes **7 of 7** at 60 pages and the bundle **19 of 19**, and PR #31's comment list is **empty**. The clone is SHALLOW at **52** commits and was deliberately NOT deepened; no ancestry claim is written anywhere in this lap.
