# Direction — where the project is going, on one screen

*Re-checked 2026-09-11T2300Z by critic #70 at `d291364`. Last rewritten 2026-09-10T1817Z by the research routine (CHARTER §14). The dev routine reads this before it claims a row; the critic re-checks it after every dev lap, at most one reorder per lap, with a reason. Only the most recent critic note is kept. **Critic #70 spent NO §3b row move: `WFG-259` closed, and the new root objection was filed as `WFG-262` at position 1 under CHARTER §14b, which is a filing and not a reorder.***

## Thesis (two sentences)

WildfireGuardian forecasts where an already-burning Korean wildfire goes next, and turns that forecast into a per-point walk-or-be-rescued decision for rural elderly residents, on public data a stranger can re-derive. Its defensible contribution is not forecast accuracy and not the architecture: it is the **output object and its measured limits**, a time-dependent decision per point with a page saying how wrong it can be.

⚠ Bokade et al. published this project's whole pipeline on **2026-09-09** (10.5281/zenodo.22668357). Nothing here rests on it, because WFG-251 narrowed Q16d to 「**공개된 기록과 초록에는** 성능 수치가 하나도 없습니다」, which is safe whether or not a lap can reach the record. **Claim the measurement, never the architecture.**

## Before any row: this lap's `fix-before-next-row` item

**ONE. The front door still carries the scope clause that WFG-259 falsified, eleven lines above the addendum that falsifies it.** `README.md:328-341`, the Round-4 bullet. `:330-331` says 「⚠ **정정(2026-09-11, WFG-258): 위 두 문장은 이제 「이 절의 상대」, 곧 완충거리를 더한 쪽에만 해당합니다.**」, which defines the surviving un-run claim as covering 「완충거리를 더한 쪽」. WFG-259 ran that side, at two widths, on 영덕, and `:339-341` says so on the same bullet. `:338` was narrowed to 「**완충거리를 폭까지 훑어 정식 상대로 세운** 쪽」 and `:330-331` was not. **Make `:330-331` name the same thing `:338` names**, one substitution, and drop ONE of the two now-duplicated 「거기서 나온 수치는 이 README 에 옮겨 적지 않습니다」 (`:335-336` and `:341`). Exact wording, the grep grade and every bar are in `CRITIC_LATEST.md`.

⚠ **WC-020 did not catch this and could not.** Its registered pattern is the whole subject-plus-region clause; `:330-331` is the **scope definition of a correction** and carries the falsified reading without ever spelling it. A registered spelling is a copy-paste ratchet, not a claim detector (`docs/withdrawn_claims.md` §4). ⚠ **Do NOT edit `docs/auto/JUDGE_QA.md` for this**: its Q19 block at `:1006-1013` uses a positional pointer and is correct, checked by critic #70, and leaving it alone keeps the item off **NH-049**'s reprint requirement. ⚠ Put **no** `ppy_yeongdeok_*` count and **no new section pointer** in the replacement; do not touch `README.md`'s opening paragraph (CHARTER §3.5b) or the TL;DR lead's ordering and proportion (NH-054). **NH-059** stays open with all four options reachable.

⚠ **Critic #69's item is genuinely CLOSED and this is not a re-open.** Re-measured by critic #70: `docs/auto/JUDGE_QA.md`'s two spoken spans name the file and no section, the student note states the rule rather than the offer, and the kit reprinted to `WFG_printables_20260911T2137Z.pdf` (**60** pages, sources **7 of 7**) with the bundle at **19 of 19** naming it. The item above is on a different surface and a different sentence.

⚠ **Do NOT `git fetch --unshallow` in a critic or dev sandbox, and CHARTER §4 does not ask you to.** §4 forbids writing an ancestry claim from a shallow clone; it does not require deepening. Unshallowing drags in eleven side branches and turns `tests/test_timeline_roles.py`'s history check RED on a tree that is fine (critics #60 and #63 both paid). **Critic #70 did not deepen: `gates.py --mode full` exits 0 on its FIRST run** at `d291364` (2181 passed, 65 skipped, 3 xfailed). The cost, stated: in a shallow clone that history check **SKIPS** rather than runs (`tests/test_timeline_roles.py:234`), so a green critic gate does not certify it; GitHub at `fetch-depth: 0` does, and run **387** is green at `d291364`. Recorded on **WFG-217**. This note names those file lines and that measurement and expires at critic #71 unless that lap re-runs the case.

## Next three rows, and why each is next

1. **WFG-262 (P0, KCF), filed at position 1 by critic #70 and its root objection.** The thesis on this page says the contribution is 「the **output object and its measured limits**」. The output object collapses two operationally opposite outcomes into one bucket and no page says so. `src/wildfireguardian/live/pipeline.py:107` prints 「예산 내 안전한 보행 경로가 없음(우회 포함)」 on the dispatch sheet for `no_safe_route`, asserting that a budget was consumed and detours were tried. `src/wildfireguardian/routing/evacuation.py:468-472` returns `reached=False` **before any search runs** when the origin's own node is already at or above `p_cut`, with a `note` that **nothing in the tree reads**, measured here: one grep hit, the definition. That origin lands in `no_safe_route`. **This is the exact defect `docs/routing_limitations.md` §1 found and repaired for `fa_exceeds_budget`, the bucket defined one line above in the same dict, and the identical audit was never run on its neighbour.** §1's escape does not apply: the bucket is **2** on 영덕, **12** on 의성·안동, **10** on 울진·삼척. ⚠ And WFG-259 registered this very member for the **opponent** arm as `origin_removed_by_filter` and published it, so the project names the mechanism when it costs its opponent and not when it costs itself. **Fix is a label and a sentence, not a refit: no committed count moves.**

2. **WFG-256 (P0, science)** second, the rotation null, still `todo`, still the only thing that would license a sentence about 「모양」, then **WFG-255**, then **WFG-236**, **WFG-244**, **WFG-245**, **WFG-237**.

3. ⚠ **WFG-234 remains the strongest science row after those**, and WFG-256 is its cheap sibling: a persistence null scored against the headline truth, where a bad result is a **named, published failure mode of the whole model class**. Pre-register it in the claim commit.

⚠ **The board at `d291364` after this lap: 257 rows, 14 P0 `todo`**, measured here with the rule written beside it: split each `| WFG-` line on pipes **not** preceded by a backslash, strip each cell, count a row P0 when cell 2 begins `**P0` and `todo` when cell 5 begins `todo` (a residue note after `todo` still counts). 14 again, and not by standing still: **WFG-259 closed** (14 to 13) and **WFG-262 was filed** (13 to 14). One P1 row (**WFG-263**) was added, which does not touch the P0 count. ⚠ The two counting rules in this repository's history disagree and neither count is the product. That is **WFG-191** and it is the whole answer. Do not publish a board count without the rule that produced it and do not spend a lap reconciling one.

## What not to do

- **Do not start a P1 row while a P0 row is `todo`**, and do not file one above the P0 block.
- ⚠⚠ **Before you call a row done, grep the judge-facing set for the NEGATION of what you just measured** — README, `docs/auto/JUDGE_QA.md`, `docs/auto/DEMO_SCRIPT_5MIN.md`, `docs/auto/finals/`, `web/finals.html`, `paper/manuscript.md`. One command. It caught WFG-258 and it would have caught WFG-138.
- ⚠⚠ **NEW, critic #69: and then grep the file you just edited for the sentences that DESCRIBE it.** A card carries notes about itself, and a substitution nine lines above one of them falsifies it silently. That is the whole of this lap's item, and it is CHARTER §5c's failure mode inside a single file for the second consecutive lap.
- ⚠⚠ **NEW, critic #70, and it is the third surface of the same class in three laps: grep for the sentences that DEFINE the terms your edit uses, not only the ones that describe it.** `README.md:330-331` is a **correction's scope clause**: it says which claim the correction leaves standing, and the lap that narrowed the claim eleven lines below never touched it. A registered spelling cannot catch this, because the stale sentence never spells the claim. **The test is semantic and the command is one grep: after narrowing a claim, grep the file for every other sentence naming the OLD subject.**
- ⚠⚠ **NEW, critic #70: if you rebase after building `web/finals.html`, run `make finals` again BEFORE you push.** That is the gate's own failure message (`tests/test_finals_screen.py:596-599`), and not doing it put a RED head on `origin` and a 「Run failed」 email in the author's inbox at `edd0ec0` (GitHub run 386). **Never push a red head to `auto/dev`** (CHARTER §3.9); park on `auto/red/<stamp>`. The one genuinely unsatisfiable case is `test_the_escape_this_gate_cannot_close_is_still_open` and it is **NH-043** / **WFG-161** / **WFG-263**, not a licence.
- ⚠⚠ **Do not write an internal entry id, or the reason an entry is open, onto a judge-facing surface** (critic #68). `NH-###`, 「저자에게 열려 있습니다」, 「아직 정하지 않아서」 are the loop talking to itself. `README.md:50`, `:265`, `:351` still carry **NH-053** this way and are out of scope; they are the author's to rule on.
- ⚠ **Do not put any `ppy_yeongdeok_*` count on a judge-facing surface** while **NH-059** is open, and **do not point at the section that prints them** either. The artifact says 「It is NOT a margin」 and that is true, but a judge hearing 26-of-44 will compute one, and NH-032, NH-034 and NH-052 are open.
- ⚠ **Do not widen WFG-259 into WFG-033(b) or NH-027.** A **buffered** present-perimeter opponent scored as the opponent of record is the author's; WFG-259 only makes an existing sentence re-derivable.
- ⚠ **Do not weaken `docs/present_perimeter_yeongdeok.md` §5.** Its eight items are the best self-criticism in the repository this week. WFG-259 makes item 5 re-derivable; it does not soften it, and **item 6's scoring-side conclusion is TRUE and stays word for word**.
- ⚠⚠ **Do not unshallow the clone.** See the note above.
- ⚠ **Do not re-open WFG-254 or WFG-258.** WFG-258 closed across every surface with the kit reprinted and the bundle re-pointed, re-hashed by critic #69. The 「모양」 half of WFG-254 is **WFG-256**.
- ⚠ **Do not weaken `docs/disc_null.md` §4's centroid finding**, and do not weaken 「방향은 아닙니다」: that half IS measured.
- ⚠ **Do not regenerate `paper/figures/F10_disc_null.png`** (CHARTER §3 rule 2). `F10b_disc_null.png` is the corrected file.
- ⚠ **Do not touch `docs/submission_reconciliation.md:63`.** Its 발화점 is about how the canonical array was seeded and is correct.
- ⚠ **Do not write 「여러 개의 산불」, or any count of fires, from WFG-255's measurement alone.** The row measures components, not fires. ⚠ And do not read WFG-260's **226** components as a count of fires either: it is one fire's detection scatter on one 500 m grid at one time.
- ⚠ **Do not edit a scorecard row another lap wrote** (CHARTER §3.7). See **WFG-252**.
- ⚠ **Do not add a syllable to `docs/auto/DEMO_SCRIPT_5MIN.md` without saying which segment pays for it** (**WFG-257**; the script is at 6.00 syllables per second, unchanged since `f7ee58d`, re-checked at `8952a5b`, and 마무리 is its longest segment). Nothing in this repository pins the **absolute** rate, only the spread. <!-- collision-ok: 6.00 — the SPOKEN RATE of the whole script in syllables per second, which is `syllables_per_second` in `data/processed/demo_script_pace/pace_20260911T0620Z.json`. The gate reads it against the `demo_pace_*_rate_spread` keys (1.02, 1.03, 1.62), which are the RATIO of the fastest segment's implied rate to the slowest (`implied_rate_spread`, unit x, max over min). Two different quantities, and this line names both on purpose: the point of the sentence is that the ratio is gated and the rate is not. Neither value is stale. -->
- ⚠ **Do not "fix" WFG-249 by weakening Q20a's privacy answer.** ⚠ **Do not swap 가구 for 지점 in the demo closing, and do not swap either count** (the denominator is `l0i_failing_denominator_h240` **24**, not the **124**-building population).
- ⚠ **Do not open, cite or characterise the Bokade et al. body PDF**, and do not widen Q16d beyond 「공개된 기록과 초록」.
- ⚠ **Do not touch** Q16's 「가구 단위 폐쇄 시각」, Q16a's lines, the ⭕/❌ pair beside them, or the panel's two deliberately-kept 「집」 lines at `RELATED_WORK_PANEL.md:39-41` (`WC-013`, `WC-008`).
- ⚠ **Do not "re-fix" the household register.** Read `docs/auto/withdrawn_claims.json` before editing any 가구 line; the shipped checker is line-based so a reworded assertion escapes.
- ⚠ **Do not claim the architecture as the contribution.** Claim the output object and its measured limits. **WFG-239** writes the sentence.
- ⚠ **Do not write the 「위험 구역」 / 「잠재적 위험 구역」 zoning framing.** Recorded UNVERIFIED.
- ⚠ **Do not compare accuracy with any domestic system or study**, NIFoS, G-DAPS or the Kangwon National University DL model (**NH-056**).
- ⚠ **Do not quote the model-only IoU pair on any judge-facing surface without the seed-removed pair in the same block** (Q36 carries both).
- ⚠ **Do not edit `README.md`'s IoU bullet (`:520-522`, `:888-891`) while NH-055 is open**, and do not delete the Rothermel comparison in either direction.
- ⚠ **`README.md`'s TL;DR lead is frozen by NH-054 on its ORDERING AND PROPORTION, not on the truth or the length of a parenthetical inside it** (narrowed by critic #67, re-stated by #68 and #69; the measurement is NH-054's own 433-against-1,853 character count). Never rewrite its opening paragraph about the 2025 fire (CHARTER §3.5b).
- ⚠ **Do not edit `docs/auto/JUDGE_QA.md`, `docs/auto/DEMO_SCRIPT_5MIN.md` or `docs/auto/finals/RELATED_WORK_PANEL.md` without `make printables` at a new stamp and a re-pointed `MANIFEST.json`** (NH-049). Re-measured by critic #69 in its own process at `8952a5b`: the kit's seven sources hash **7 of 7** (`WFG_printables_20260911T1904Z.pdf`, 59 pages) and the bundle's nineteen entries hash **19 of 19**, and the bundle names that kit. This note expires at critic #70 unless that lap re-measures.
- ⚠ **Do not write 「the fire stopped growing at 333 minutes」.** No observation exists between 0 and 333 minutes (WFG-230).
- ⚠ **Do not "fix" 「household-level」** in `README.md:3`, `CITATION.cff:5`, `src/wildfireguardian/__init__.py:1` or `paper/manuscript.md:1`; they name the **application** (WFG-231).
- ⚠ **Do not cite `data/processed/spread_v2/audit.json` for any number** (WFG-244).
- ⚠ **Do not rebuild `data/processed/timeline_roles/timeline_roles.json` from a deepened clone** (WFG-217; deadline 10-16). The refreshing lap works from a **single-branch full clone**.
- ⚠ **Do not weaken 「실제 확산면으로 만든 출동 지시서는 아직 없습니다」** on any surface (**NH-057**, **WFG-242**).
- **Do not settle 「상한」 / 「upper bound」 in any lap** (NH-053 open). Describe the mechanism.
- ⚠ **Do not put a margin value on any judge-facing surface** while NH-032, NH-034 and NH-052 are open.
- ⚠ **Do not add a word to `paper/manuscript.md` without trimming one** (NH-037, open).
- **Do not change `mr_uiseong_fa_exceeds_budget`** (NH-031 A; registry half is WFG-122).
- **Do not refit anything, and do not regenerate a committed artifact** (CHARTER §3 rule 2).
- **Do not re-open the AI-disclosure question** (NH-008 closed; CHARTER §9).
- **Do not re-propose international portability or real-time weather** (`docs/HANDOFF_ROUND3.md` §13-§14).
- **Do not let a number from a knowledge note reach a card, the README, the manuscript or `docs/NUMBERS.json`** (CHARTER §13).

## Readiness and open decisions

`docs/auto/KCF_READINESS.md` stands at **8 of 11**, unchanged: R1, R2, R4, R5, R6, R7, R8, R9 tick; R3, R11 and R12 do not; R10 was withdrawn 2026-09-04. ⚠⚠ **ZERO lines ticked for the TWENTY-SEVENTH consecutive critic lap.** Verified here the cheap way: `git diff 8952a5b..d291364 -- docs/auto/KCF_READINESS.md` is **empty**, and the file's newest commit is `c241904`, critic #66. ⚠ **The arithmetic of the hold is unchanged and is the whole story.** CHARTER §14b releases the P1 hygiene block when **R1, R3, R4, R7, R8 and R9** tick. **Five of those six are ticked; R3 is the only one that is not.** R3 is `blocked(NH-046)`, NH-046 is a three-option question that came due **2026-09-10**, and R11's WFG-024 is held behind it. **One unanswered email holds both the ninth tick and the whole P1 block**, and the sprint ends 2026-09-15. **WFG-263**, filed this lap, is the newest row in that queue.

**Open decisions: 26 for the author** (25 DECISION + 1 BLOCKER), **2 undated** (NH-005, NH-014), plus 5 open FYI. Say 「N of 26, and 2 undated」 rather than a bare number. **NH-046, NH-049 and NH-051 are past due.** **NH-057** and **NH-059** are the highest-severity open entries; **NH-043** gained a measured instance and a fourth option from critic #70. ⚠ **No decision has EVER arrived on the email channel:** `decisions_seen.json` records `"seen": []`, every applied decision came through a Claude Code session on the laptop, and the newest is **NH-031, received 2026-09-06**, five days ago. Critic #70 re-confirmed at the Gmail connector that the **30** newest threads matching the report subject in the last 14 days each carry exactly one message and every one is the loop's own send, and that PR #31's comment list is **empty**. That is **WFG-211**, already `todo`, confirmed here and not re-filed. The sprint ends 2026-09-15, **four days out**.

## Critic's last direction note

**2026-09-11T2300Z, critic #70, reviewed `d291364`. NO §3b row move was spent.** WFG-259 closed by route (i), so the root objection this lap found was **filed** as WFG-262 at position 1 under CHARTER §14b, which the charter distinguishes from a reorder. ONE new P0 row (**WFG-262**), one new P1 row (**WFG-263**), **ZERO** new NEEDS_HUMAN entries (26 are open and no reply has ever arrived by email; **NH-043** was updated instead, with a measured instance and a fourth option), ONE `fix-before-next-row` item.

**The one thing that matters on this page.** The lap under review is the best single lap in this repository's record: it pre-registered its definition in the claim commit **before any number existed**, reported that the measurement contradicted the reading its own row expected, and let its reviewer block it for a mechanism sentence built on two marginals that happened to be equal. Nothing in this note takes that back. **And the same lap's central move points at the hole in the product.** It named, measured and published the outcome 「the filter refuses to plan for this origin at all」 for the **opponent** arm, as `origin_removed_by_filter`. This project's own arm has the identical branch at `src/wildfireguardian/routing/evacuation.py:468-472`, a `note` that **nothing in the tree reads**, and a dispatch-sheet sentence at `src/wildfireguardian/live/pipeline.py:107` that tells an operator a budget was spent and detours were tried when neither happened. `docs/routing_limitations.md` §1 is this exact audit, done well, on the bucket defined **one line above** in the same dict. **The project audits its opponent harder than it audits itself, and the repair is a label and a sentence.**

**The second thing, and it is about the loop rather than the product.** A red head reached `origin` for 27 minutes at `edd0ec0` and GitHub run **386** concluded `failure`. The lap disclosed it, superseded its own report and annotated the body, so nothing was hidden and CHARTER §4b sets no finding #1. But the record it left says the gate was red 「by construction」, and that is false for two of the three failures: they are the rebase-orphan class, whose remedy the gate prints in its own failure message. **A true sentence about an unavoidable case is being used to cover an avoidable one, in the file the next lap reads.** That is WFG-263 and the NH-043 update.

**Scorecard: ONE row moves, Track B UP to 96, Track A HOLDS at 97.** 데이터 수집·분석·해석 19 to **20**, the pre-registered move from critic #69, earned by WFG-259's pre-registration, its identity control, its cross-tabulation and its refusal to license the sentence its own row asked for. 제출 자료 **holds at 19 and does not fall**: the README defect is real and is this lap's item, but it is a redundancy and a stale scope clause on a page that states the true scope eleven lines below, not a false claim, and the printed kit and the bundle are clean at 7 of 7 and 19 of 19, re-hashed here.

Verified at `d291364`, the head reviewed and the head this lap ships in. **GitHub `auto-gates` run 387 is `success` at `d291364`.** ⚠ **Run 386 at `edd0ec0` concluded `failure`** and is the first red run in nineteen laps; it is repaired, disclosed, and does not consume this lap's item. `gates.py --mode full` exits **0** on its FIRST run in this sandbox (2181 passed, 65 skipped, 3 xfailed, pytest 490.3 s), **every report in the window records `Reviewed by:`** (sixteen checked), and `--assert-reported` exits 0 at every push boundary since critic #69. The clone is SHALLOW at **55** commits and was deliberately NOT deepened; no ancestry claim is written anywhere in this lap.
