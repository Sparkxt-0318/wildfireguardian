# Direction — where the project is going, on one screen

*Re-checked 2026-09-11T1959Z by critic #69 at `8952a5b`. Last rewritten 2026-09-10T1817Z by the research routine (CHARTER §14). The dev routine reads this before it claims a row; the critic re-checks it after every dev lap, at most one reorder per lap, with a reason. Only the most recent critic note is kept. **Critic #69 spent NO §3b row move: `WFG-259` is already the first `todo` row in table order, which is where it belongs.***

## Thesis (two sentences)

WildfireGuardian forecasts where an already-burning Korean wildfire goes next, and turns that forecast into a per-point walk-or-be-rescued decision for rural elderly residents, on public data a stranger can re-derive. Its defensible contribution is not forecast accuracy and not the architecture: it is the **output object and its measured limits**, a time-dependent decision per point with a page saying how wrong it can be.

⚠ Bokade et al. published this project's whole pipeline on **2026-09-09** (10.5281/zenodo.22668357). Nothing here rests on it, because WFG-251 narrowed Q16d to 「**공개된 기록과 초록에는** 성능 수치가 하나도 없습니다」, which is safe whether or not a lap can reach the record. **Claim the measurement, never the architecture.**

## Before any row: this lap's `fix-before-next-row` item

**ONE. The booth card contradicts its own student note, and the two together decide NH-059.** `docs/auto/JUDGE_QA.md:1013` tells the student 「이 카드에서는 먼저 꺼내지 않습니다」; `:1018` and `:955` then have the student volunteer 「그 수치는 그 문서 **4절**에 그대로 적혀 있습니다」 to all five judges, unprompted, beside the headline 42. `:1019` and `:957` add 「그 문서로 보여드리는 편이 정확합니다」, which is the offer the 1852Z lap's own reviewer removed, returned in the indicative; `:1028` and `:1036` instruct the student that opening the page is the right move. `docs/present_perimeter_yeongdeok.md` §4 prints all three counts in bold, so this is **NH-059 option A through a side door**. **Answer the question, name the file, name nothing inside it.** Exact replacement wording, the mutation grade, the gate-docstring rider and the NH-054 scoping are in `CRITIC_LATEST.md`.

⚠ **Put NO count on any surface in this item**: not 26, not 16, not 2, not 44, not 226. And do **not** write 「44곳」 anywhere in the replacement: `test_no_ppy_count_reaches_a_spoken_draft` keys on it at `tests/test_judge_qa_bank.py:990` and critic #68's own prescribed wording would have reddened the gate it was written to protect. **NH-059** stays open with all four options reachable. ⚠ Do **not** touch `README.md`'s opening paragraph about the 2025 fire (CHARTER §3.5b), do **not** weaken the fire-blind-control or oracle-in-the-grading caveats, and do **not** touch the WC-019 correction block's substance.

⚠ **Critic #68's item is genuinely CLOSED and this is not a re-open.** Re-measured by critic #69 in its own process: the withheld-for-permission clause is in **zero** spoken spans, `NH-059` appears in `docs/auto/JUDGE_QA.md` exactly once (inside the labelled student note), the kit reprinted to `WFG_printables_20260911T1904Z.pdf` (59 pages, sources **7 of 7**) and the bundle hashes **19 of 19** and names it. The item above is what the repair left behind, not the thing it repaired.

⚠ **Do NOT `git fetch --unshallow` in a critic or dev sandbox, and CHARTER §4 does not ask you to.** §4 forbids writing an ancestry claim from a shallow clone; it does not require deepening. Unshallowing drags in eleven side branches and turns `tests/test_timeline_roles.py`'s history check RED on a tree that is fine (critics #60 and #63 both paid). **Critic #69 did not deepen: `gates.py --mode full` exits 0 on its FIRST run** (2158 passed, 65 skipped, 3 xfailed). The cost, stated: in a shallow clone that history check **SKIPS** rather than runs (`tests/test_timeline_roles.py:234`), so a green critic gate does not certify it; GitHub at `fetch-depth: 0` does, and run 382 is green at `8952a5b`. Recorded on **WFG-217**. This note names those file lines and that measurement and expires at critic #70 unless that lap re-runs the case.

## Next three rows, and why each is next

1. **WFG-259 (P0, science), alone and in full.** It is the first `todo` row in table order and it is critic #69's root objection. `docs/present_perimeter_yeongdeok.md:165` says 「at 500 m, 15 of the 16 flip」 as fact, and `docs/NUMBERS.json` holds **seven** `ppy_yeongdeok_` keys with none of them the 15 or either width; the artifact has no `buffer` or `dilat` key at any depth; the script takes no buffer argument. Re-measured by critic #69 at `8952a5b`. Read with §4, that sentence says a router which sees only where the fire is now, plus half a kilometre, reaches all but one of the origins the headline **42** credits to the forecast, and the page is one click from the front door. **Route (i): run the two widths already named in the prose (100 m and 500 m, so no width is chosen after the answer), write a `buffer_sensitivity` block, register the keys additively, re-point §5 item 5.** ⚠ **New half, critic #69:** the same row now owns the sentence nobody has written. WFG-260 established the opponent's input is the **observation** and that the observation is **226** components with a largest piece of **3** cells, a detection scatter and not a fire line. A sparser burning set removes fewer nodes and saves fewer origins, and §5 item 5's probe says dilation moves origins **into** `saved`. So the fair opponent is weakened by this project's own input coarseness and the bias runs in this project's favour. Both halves are now in one document and the sentence joining them is in no file.

2. **WFG-256 (P0, science)** second, the rotation null, still `todo`, still the only thing that would license a sentence about 「모양」, then **WFG-255**, then **WFG-236**, **WFG-244**, **WFG-245**, **WFG-237**.

3. ⚠ **WFG-234 remains the strongest science row after those**, and WFG-256 is its cheap sibling: a persistence null scored against the headline truth, where a bad result is a **named, published failure mode of the whole model class**. Pre-register it in the claim commit.

⚠ **The board at `8952a5b` after this lap: 255 rows, 14 P0 `todo`**, measured here with the rule written beside it: split each `| WFG-` line on pipes **not** preceded by a backslash, strip each cell, count a row P0 when cell 2 begins `**P0` and `todo` when cell 5 begins `todo` (a residue note after `todo` still counts). 14 and not critic #68's 15 because **WFG-260 closed in this window**; one P1 row (**WFG-261**) was added, which does not touch the P0 count. ⚠ Under that rule **22** rows put something other than a bare priority in cell 2, where critic #64's rule reported **11**; the two rules differ and neither count is the product. That is **WFG-191** and it is the whole answer. Do not publish a board count without the rule that produced it and do not spend a lap reconciling one.

## What not to do

- **Do not start a P1 row while a P0 row is `todo`**, and do not file one above the P0 block.
- ⚠⚠ **Before you call a row done, grep the judge-facing set for the NEGATION of what you just measured** — README, `docs/auto/JUDGE_QA.md`, `docs/auto/DEMO_SCRIPT_5MIN.md`, `docs/auto/finals/`, `web/finals.html`, `paper/manuscript.md`. One command. It caught WFG-258 and it would have caught WFG-138.
- ⚠⚠ **NEW, critic #69: and then grep the file you just edited for the sentences that DESCRIBE it.** A card carries notes about itself, and a substitution nine lines above one of them falsifies it silently. That is the whole of this lap's item, and it is CHARTER §5c's failure mode inside a single file for the second consecutive lap.
- ⚠⚠ **Do not write an internal entry id, or the reason an entry is open, onto a judge-facing surface** (critic #68). `NH-###`, 「저자에게 열려 있습니다」, 「아직 정하지 않아서」 are the loop talking to itself. `README.md:50`, `:265`, `:351` still carry **NH-053** this way and are out of scope; they are the author's to rule on.
- ⚠ **Do not put any `ppy_yeongdeok_*` count on a judge-facing surface** while **NH-059** is open, and **do not point at the section that prints them** either. The artifact says 「It is NOT a margin」 and that is true, but a judge hearing 26-of-44 will compute one, and NH-032, NH-034 and NH-052 are open.
- ⚠ **Do not widen WFG-259 into WFG-033(b) or NH-027.** A **buffered** present-perimeter opponent scored as the opponent of record is the author's; WFG-259 only makes an existing sentence re-derivable.
- ⚠ **Do not weaken `docs/present_perimeter_yeongdeok.md` §5.** Its eight items are the best self-criticism in the repository this week. WFG-259 makes item 5 re-derivable; it does not soften it, and **item 6's scoring-side conclusion is TRUE and stays word for word**.
- ⚠⚠ **Do not unshallow the clone.** See the note above.
- ⚠ **Do not re-open WFG-254 or WFG-258.** WFG-258 closed across every surface with the kit reprinted and the bundle re-pointed, re-hashed by critic #69. The 「모양」 half of WFG-254 is **WFG-256**.
- ⚠ **Do not weaken `docs/disc_null.md` §4's centroid finding**, and do not weaken 「방향은 아닙니다」: that half IS measured.
- ⚠ **Do not regenerate `paper/figures/F10_disc_null.png`** (CHARTER §3 rule 2). `F10b_disc_null.png` is the corrected file.
- ⚠ **Do not touch `docs/submission_reconciliation.md:63`.** Its 발화점 is about how the canonical array was seeded and is correct.
- ⚠ **Do not write 「여러 개의 산불」, or any count of fires, from WFG-255's measurement alone.** The row measures components, not fires. ⚠ And do not read WFG-260's **226** components as a count of fires either: it is one fire's detection scatter on one 500 m grid at one time.
- ⚠ **Do not edit a scorecard row another lap wrote** (CHARTER §3.7). See **WFG-252**.
- ⚠ **Do not add a syllable to `docs/auto/DEMO_SCRIPT_5MIN.md` without saying which segment pays for it** (**WFG-257**; the script is at 6.00 syllables per second, unchanged since `f7ee58d`, re-checked at `8952a5b`, and 마무리 is its longest segment). Nothing in this repository pins the **absolute** rate, only the spread. <!-- collision-ok: 6.00 — the SPOKEN RATE of the whole script in syllables per second, which is `syllables_per_second` in `data/processed/demo_script_pace/pace_20260911T0620Z.json`. The gate reads it against the `demo_pace_*_rate_spread` keys (1.02, 1.03, 1.62), which are the RATIO of the fastest segment's implied rate to the slowest (`implied_rate_spread`, unit x, max over min). Two different quantities, and this line names both on purpose: the point of the sentence is that the ratio is gated and the rate is not. Neither value is stale. -->
- ⚠ **Do not "fix" WFG-249 by weakening Q20a's privacy answer.** ⚠ **Do not swap 가구 for 지점 in the demo closing, and do not swap either count** (the denominator is `l0i_failing_denominator_h240` **24**, not the **124**-building population).
- ⚠ **Do not open, cite or characterise the Bokade et al. body PDF**, and do not widen Q16d beyond 「공개된 기록과 초록」.
- ⚠ **Do not touch** Q16's 「가구 단위 폐쇄 시각」, Q16a's lines, the ⭕/❌ pair beside them, or the panel's two deliberately-kept 「집」 lines at `RELATED_WORK_PANEL.md:39-41` (`WC-013`, `WC-008`).
- ⚠ **Do not "re-fix" the household register.** Read `docs/auto/withdrawn_claims.json` before editing any 가구 line; the shipped checker is line-based so a reworded assertion escapes.
- ⚠ **Do not claim the architecture as the contribution.** Claim the output object and its measured limits. **WFG-239** writes the sentence.
- ⚠ **Do not write the 「위험 구역」 / 「잠재적 위험 구역」 zoning framing.** Recorded UNVERIFIED.
- ⚠ **Do not compare accuracy with any domestic system or study**, NIFoS, G-DAPS or the Kangwon National University DL model (**NH-056**).
- ⚠ **Do not quote the model-only IoU pair on any judge-facing surface without the seed-removed pair in the same block** (Q36 carries both).
- ⚠ **Do not edit `README.md`'s IoU bullet (`:520-522`, `:888-891`) while NH-055 is open**, and do not delete the Rothermel comparison in either direction.
- ⚠ **`README.md`'s TL;DR lead is frozen by NH-054 on its ORDERING AND PROPORTION, not on the truth or the length of a parenthetical inside it** (narrowed by critic #67, re-stated by #68 and #69; the measurement is NH-054's own 433-against-1,853 character count). Never rewrite its opening paragraph about the 2025 fire (CHARTER §3.5b).
- ⚠ **Do not edit `docs/auto/JUDGE_QA.md`, `docs/auto/DEMO_SCRIPT_5MIN.md` or `docs/auto/finals/RELATED_WORK_PANEL.md` without `make printables` at a new stamp and a re-pointed `MANIFEST.json`** (NH-049). Re-measured by critic #69 in its own process at `8952a5b`: the kit's seven sources hash **7 of 7** (`WFG_printables_20260911T1904Z.pdf`, 59 pages) and the bundle's nineteen entries hash **19 of 19**, and the bundle names that kit. This note expires at critic #70 unless that lap re-measures.
- ⚠ **Do not write 「the fire stopped growing at 333 minutes」.** No observation exists between 0 and 333 minutes (WFG-230).
- ⚠ **Do not "fix" 「household-level」** in `README.md:3`, `CITATION.cff:5`, `src/wildfireguardian/__init__.py:1` or `paper/manuscript.md:1`; they name the **application** (WFG-231).
- ⚠ **Do not cite `data/processed/spread_v2/audit.json` for any number** (WFG-244).
- ⚠ **Do not rebuild `data/processed/timeline_roles/timeline_roles.json` from a deepened clone** (WFG-217; deadline 10-16). The refreshing lap works from a **single-branch full clone**.
- ⚠ **Do not weaken 「실제 확산면으로 만든 출동 지시서는 아직 없습니다」** on any surface (**NH-057**, **WFG-242**).
- **Do not settle 「상한」 / 「upper bound」 in any lap** (NH-053 open). Describe the mechanism.
- ⚠ **Do not put a margin value on any judge-facing surface** while NH-032, NH-034 and NH-052 are open.
- ⚠ **Do not add a word to `paper/manuscript.md` without trimming one** (NH-037, open).
- **Do not change `mr_uiseong_fa_exceeds_budget`** (NH-031 A; registry half is WFG-122).
- **Do not refit anything, and do not regenerate a committed artifact** (CHARTER §3 rule 2).
- **Do not re-open the AI-disclosure question** (NH-008 closed; CHARTER §9).
- **Do not re-propose international portability or real-time weather** (`docs/HANDOFF_ROUND3.md` §13-§14).
- **Do not let a number from a knowledge note reach a card, the README, the manuscript or `docs/NUMBERS.json`** (CHARTER §13).

## Readiness and open decisions

`docs/auto/KCF_READINESS.md` stands at **8 of 11**, unchanged: R1, R2, R4, R5, R6, R7, R8, R9 tick; R3, R11 and R12 do not; R10 was withdrawn 2026-09-04. ⚠⚠ **ZERO lines ticked for the TWENTY-SIXTH consecutive critic lap**, verified here by diffing all twelve R-row status cells across the window: not one changed. ⚠ **What is new is the arithmetic of the hold.** CHARTER §14b releases the P1 hygiene block when **R1, R3, R4, R7, R8 and R9** tick. **Five of those six are ticked; R3 is the only one that is not.** R3 is `blocked(NH-046)`, NH-046 is a three-option question that came due **2026-09-10**, and R11's WFG-024 (`agent_doable`, `todo`, one stale sentence) is held behind it. **One unanswered email holds both the ninth tick and the whole P1 block**, and the sprint ends 2026-09-15.

**Open decisions: 26 for the author** (25 DECISION + 1 BLOCKER), **2 undated** (NH-005, NH-014), plus 5 open FYI. Say 「N of 26, and 2 undated」 rather than a bare number. **NH-046, NH-049 and NH-051 are past due.** **NH-057** and **NH-059** are the highest-severity open entries, and NH-059 gained a measured update from critic #69. ⚠ **The channel has now produced nothing for NINE DAYS:** `decisions_seen.json` records `"seen": []`, the newest applied decision is NH-031 of **2026-09-06**, and critic #69 confirmed at the Gmail connector that the **30** newest threads matching the report subject in the last 14 days each carry exactly one message and every one is the loop's own send; PR #31's comment list is empty. That is **WFG-211**, already `todo`, confirmed here and not re-filed. The sprint ends 2026-09-15, **four days out**.

## Critic's last direction note

**2026-09-11T1959Z, critic #69, reviewed `8952a5b`. NO §3b row move was spent.** WFG-259 is already the first `todo` row in table order, which is where the root objection says it belongs. ONE new row (**WFG-261**, P1), ZERO new NEEDS_HUMAN entries (NH-059 was updated instead, deliberately: 26 are open and the channel has been silent nine days), ONE `fix-before-next-row` item.

**The one thing that matters on this page.** Critic #68 found the loop's own governance on the judged surfaces and prescribed a replacement. The replacement worked and it brought a smaller copy of the same mistake: the card now points a judge at the section that prints the counts, and the card's own student note, nine lines above, still says the card does not raise them first. Both sentences were written by laps trying to be careful. **A document that describes itself goes stale the moment you edit it, and nothing in this loop checks that.** That is why this lap's item is one substitution and one note rather than a new rule, and why the new line under **What not to do** is「grep the file you just edited for the sentences that describe it」.

**And the bigger thing is not on the card at all.** The sentence that decides whether 42 can be said out loud sits unregistered on the page the front door now links, and it has sat there for two windows. **WFG-259 is the lap. Do it alone and do it fully.**

**Scorecard: no row moves. Track B holds at 95, Track A at 97.** 제출 자료 had a real case at 20 and it cancels exactly: the front door lost an entry id and the card gained a self-contradiction, in the same window, in the same kit. 데이터 수집·분석·해석 is pre-registered to move to **20** when WFG-259 closes by route (i).

Verified at `8952a5b`, the head reviewed and the head this lap ships in. **GitHub `auto-gates` run 382 is `success` at `8952a5b`**, and **no run in the window concluded `failure`** (two `cancelled`, 368 and 381, each superseded by the next push), so CHARTER §4b sets no finding #1 for the eighteenth consecutive lap. `gates.py --mode full` exits **0** on its FIRST run in this sandbox (2158 passed, 65 skipped, 3 xfailed, pytest 413.6 s), **every report in the window records `Reviewed by:`** (twenty-one checked), and `--assert-reported` exits 0 at every push boundary since critic #68. The clone is SHALLOW at **50** commits and was deliberately NOT deepened; no ancestry claim is written anywhere in this lap.
