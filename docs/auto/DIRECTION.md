# Direction — where the project is going, on one screen

*Re-checked 2026-09-11T0520Z by critic #64 at `5482ba8`. Last rewritten 2026-09-10T1817Z by the research routine (CHARTER §14). The dev routine reads this before it claims a row; the critic re-checks it after every dev lap, at most one reorder per lap, with a reason. Only the most recent critic note is kept, as §14 specifies. **Critic #64 spent ZERO §3b row moves and names its skips instead of publishing a zero.***

## Thesis (two sentences)

WildfireGuardian forecasts where an already-burning Korean wildfire goes next, and turns that forecast into a per-point walk-or-be-rescued decision for rural elderly residents, on public data a stranger can re-derive. Its defensible contribution is not forecast accuracy and not the architecture: it is the **output object and its measured limits**, a time-dependent decision per point with a page saying how wrong it can be.

⚠ Bokade et al. published this project's whole pipeline on **2026-09-09** (10.5281/zenodo.22668357). Critic #64 re-read the record at the Zenodo API in its own process: ORS primary with a D\* Lite grid **fallback**, Preprint, five authors, one attached file, and a digit regex over the full abstract returns the **empty list**. **Claim the measurement, never the architecture.**

## Before any row: this lap's `fix-before-next-row` items

**ZERO, and for a reason this page now states rather than repeats.** Gates are green at this exact head (`--mode full` exit 0, `--assert-head` 0, `--assert-reported` 0 over the whole window) and GitHub `auto-gates` run **361** is `success` at `5482ba8`. All three of this lap's judge-facing findings sit on hashed kit sources, which CHARTER §14b and NH-049 together make un-preemptable. **That is now the third consecutive critic lap in which every judge-facing finding was locked behind one rebuild, so this page carries the measurement the author's NH-049 decision needs: a full kit rebuild is 17 seconds** (`build_printables.py --out-dir` into a scratch directory, 58 pages, tree untouched, timed in this lap), and the only file in the chain a critic may not write is `release/kcf-finals-2026/MANIFEST.json`, three JSON fields. Appended to NH-049.

⚠ **Do NOT `git fetch --unshallow` in a critic or dev sandbox, and CHARTER §4 does not ask you to.** §4 forbids writing an ancestry or reachability claim from a shallow clone; it does not require deepening. Unshallowing obeys the remote refspec, drags in eleven side branches, lengthens `git`'s abbreviation and turns `tests/test_timeline_roles.py`'s history check RED on a tree that is fine. Critic #60 and #63 both paid for that, and #63 recorded an expensive cure (prune the refs, expire the reflog, `gc --prune=now`) for a wound the deepening inflicted. **Critic #64 did not deepen: `gates.py --mode full` went green on its FIRST run**, 2092 passed, 65 skipped. ⚠ Say what that costs, because it is not free either: in a shallow clone that history check **SKIPS** rather than runs (`tests/test_timeline_roles.py:234`, reason printed under `-rs`), so a green critic gate does not certify it. GitHub at `fetch-depth: 0` on a single branch is what certifies it, and run 361 is green. Recorded on **WFG-217**. This note names those file lines and that measurement, and expires at critic #65 unless that lap re-runs the case.

## Next three rows, and why each is next

**This page's naming power is spent on ONE thing and the skips are counted, not asserted (WFG-246).**

1. **ONE lap takes WFG-249 + WFG-250 + WFG-251 and pays `make printables` ONCE.** All three are single-clause repairs on the two hashed documents in the booth kit, all three are on the identically worded 제출 자료 criterion, and all three are at position 1 by CHARTER §14b, so **no row moved**. **WFG-249 is the largest:** Q20a, a **T0** card the student answers from memory, tells a judge that a 가구 here is 「건물도 아니라」 a walk-graph node, while the 마무리 that same window wrote tells the same judge the 가구 it counts are OSM buildings, and the demo script's 금지 item 6 cites Q20a as the authority for the definition it breaks. Both sentences are printed in the 58-page kit the release bundle names. **WFG-250** is that the closing names the arm's denominator instead of the claim's, where the screen already says it correctly. **WFG-251** is one clause of Q16d asserting a negative over a deposited record whose only file the project never opened, which the card's own 없는 것 item 1 already forbids.

2. ⚠⚠ **WFG-129 (P0, science) IS THE NEXT ROW AFTER THAT BUNDLE, AND THIS PAGE HAS NOT NAMED IT FOR FIVE LAPS WHILE ITS OWN ROOT OBJECTION ASKED FOR IT.** It is fully specified in `paper/GAPS.md` G7, runs on committed inputs only, needs no refit, no re-acquisition, no author decision, and G7's own words are 「That is minutes of work」 and 「A dev lap should run this before the finals whatever the author decides on NH-027」. It is the cheapest test of the exact number the booth leads with. **Naming it passes over exactly ONE fully agent-doable P0 `todo` row, WFG-117** (`agent-doable: true`, one lap, a gate on the Q&A bank's registry counts), and over **WFG-007**, whose remaining half its own cell marks 「print/laptop: human」. That is the whole skip list and this page publishes it rather than a zero. **Pre-registered for critic #65: if a dev lap ran in the next window and WFG-129 is still `todo`, that is finding #1 and it is about the loop's direction, not the product.**

3. **WFG-236 (P0, KCF) third.** `docs/disc_null.md:20-21` and `docs/oracle_gap.md:180-181` both assert in bold that nothing said what the headline IoU should be compared with, while `README.md:520-522` and `:888-891` have compared it to Rothermel's for months. The row repairs the two `docs/` sentences; whether the README's framing moves is **NH-055**, the author's. It pays no rebuild. Then **WFG-244**, then **WFG-245** and **WFG-237**.

⚠ **WFG-234 remains the strongest science row after WFG-129**: a persistence null scored against the headline truth, where a bad result is a **named, published failure mode of the whole model class**, in Bokade et al.'s own abstract. Pre-register it in the claim commit. **Take it before other P1 science work.**

⚠ **The board at the head I reviewed (`5482ba8`, before this lap's rows): 242 rows, 15 P0 `todo`. After this lap's five rows: 247 rows, 18 P0 `todo`.** ⚠⚠ **Do not publish a board count without the rule that produced it, and do not spend a lap reconciling one.** Critic #64's rule, stated so the next lap can check it: split each row on pipes not preceded by a backslash, read priority at cell 2 and status at cell 5. At `5482ba8` that answers **106 P1 `todo`** counting only rows whose cell count equals the header's ten, and **108** counting every row by cell position (after this lap, 108 and 110; the two new P1 rows are well formed). Critic #63 published **112** and said its own rule answers **111**; neither is reproducible here. **Three laps, four numbers, one file.** The cause is **WFG-191**: 11 rows carry unescaped pipes and their priority and status render in the wrong columns, the same 11 at `0796336` and at this head, and none of this lap's five rows joins them. The gate WFG-191 asks for is the whole answer; spend no further lap measuring this.

## What not to do

- **Do not start a P1 row while a P0 row is `todo`**, and do not file one above the P0 block.
- ⚠⚠ **Do not unshallow the clone.** See the note above; it is the second most expensive mistake this loop repeats.
- ⚠ **Do not "fix" WFG-249 by weakening Q20a's privacy answer.** That answer is correct about the routing output and is why the card exists. Scope it; do not soften it.
- ⚠ **Do not "fix" WFG-250 by swapping 가구 for 지점, and do not swap either count.** Those counts really are buildings and `l0i_household_population` says so. Name the failing set, in the screen's own words.
- ⚠ **Do not open, cite or characterise the Bokade et al. body PDF** while fixing WFG-251, and do not weaken any other fact on Q16d. Critic #64 re-verified every one at the Zenodo API.
- ⚠ **Do not touch** Q16's 「가구 단위 폐쇄 시각」, Q16a's lines, the ⭕/❌ pair beside them, or the panel's two deliberately-kept 「집」 lines at `RELATED_WORK_PANEL.md:39-41`. `WC-013` and `WC-008` preserve all of these by name.
- ⚠ **Do not "re-fix" the household register.** Critic #63 measured it clean across all six `WC-013` spellings on a flattened sweep. Critic #64 did not re-sweep it and therefore **does not re-state that note**, which is why this bullet says only: read `docs/auto/withdrawn_claims.json` before editing any 가구 line, and note that the shipped checker is line-based so a reworded assertion escapes (`docs/withdrawn_claims.md` §4).
- ⚠ **Do not claim the architecture as the contribution.** Claim the output object and its measured limits. **WFG-239** writes the sentence.
- ⚠ **Do not write the 「위험 구역」 / 「잠재적 위험 구역」 zoning framing.** Recorded UNVERIFIED.
- ⚠ **Do not compare accuracy with any domestic system or study**, NIFoS, G-DAPS or the Kangwon National University DL model (**NH-056**).
- ⚠ **Do not quote the model-only IoU pair on any judge-facing surface without the seed-removed pair in the same block** (Q36 carries both).
- ⚠ **Do not edit `README.md`'s IoU bullet (`:520-522`, `:888-891`) while NH-055 is open**, and do not delete the Rothermel comparison in either direction.
- ⚠ **Do not edit `README.md`'s TL;DR lead in either direction while NH-054 is open**, and never rewrite its opening paragraph about the 2025 fire (CHARTER §3.5b).
- ⚠ **Do not edit `docs/auto/JUDGE_QA.md`, `docs/auto/DEMO_SCRIPT_5MIN.md` or `docs/auto/finals/RELATED_WORK_PANEL.md` without `make printables` at a new stamp and a re-pointed `MANIFEST.json`** (NH-049). Re-measured by critic #64 at this head in its own process: the kit's seven sources hash **7 of 7** and the bundle **19 of 19**, and the bundle names the newest kit. This note expires at critic #65 unless that lap re-measures.
- ⚠ **Do not write 「the fire stopped growing at 333 minutes」.** No observation exists between 0 and 333 minutes (WFG-230).
- ⚠ **Do not "fix" 「household-level」** in `README.md:3`, `CITATION.cff:5`, `src/wildfireguardian/__init__.py:1` or `paper/manuscript.md:1`; they name the **application** (WFG-231).
- ⚠ **Do not cite `data/processed/spread_v2/audit.json` for any number** (WFG-244).
- ⚠ **Do not rebuild `data/processed/timeline_roles/timeline_roles.json` from a deepened clone, and not from any clone holding the side branches** (WFG-217; deadline 10-16). Its `last_commit_date` reads 2026-09-09 against a tree at 2026-09-11, disclosed at `TIMELINE_ROLES.md:28`. ⚠ Critic #64 did **not** re-measure the commit half of that gap and does not repeat critic #63's figure for it: a shallow clone cannot count it and CHARTER §4 forbids writing it from one. The refreshing lap works from a **single-branch full clone**.
- ⚠ **Do not weaken 「실제 확산면으로 만든 출동 지시서는 아직 없습니다」** on any surface (**NH-057**, **WFG-242**).
- **Do not settle 「상한」 / 「upper bound」 in any lap** (NH-053 open). Describe the mechanism.
- ⚠ **Do not put a margin value on any judge-facing surface** while NH-032, NH-034 and NH-052 are open.
- ⚠ **Do not add a word to `paper/manuscript.md` without trimming one** (NH-037, open).
- **Do not change `mr_uiseong_fa_exceeds_budget`** (NH-031 A; registry half is WFG-122).
- **Do not refit anything, and do not regenerate a committed artifact** (CHARTER §3 rule 2).
- **Do not re-open the AI-disclosure question** (NH-008 closed; CHARTER §9).
- **Do not re-propose international portability or real-time weather** (`docs/HANDOFF_ROUND3.md` §13-§14).
- **Do not let a number from a knowledge note reach a card, the README, the manuscript or `docs/NUMBERS.json`** (CHARTER §13).

## Readiness and open decisions

`docs/auto/KCF_READINESS.md` stands at **8 of 11**, re-read by critic #64 at `5482ba8`: R1, R2, R4, R5, R6, R7, R8, R9 tick; R3, R11 and R12 do not; R10 was withdrawn 2026-09-04. ⚠⚠ **ZERO lines ticked for the TWENTY-FIRST consecutive critic lap.** R12 is the author's (NH-014); R3 is `blocked(NH-046)`; R11's WFG-024 is held by §14b until R3 ticks.

**Open decisions: 24 for the author** (23 DECISION + 1 BLOCKER), **2 undated** (NH-005, NH-014), plus 5 open FYI. Say 「N of 24, and 2 undated」 rather than a bare number. **NH-046 and NH-049 are both past due. NH-051 came due today.** **NH-057** is the highest-severity open entry. ⚠ **The channel has produced nothing for five days:** `decisions_seen.json` records `"seen": []`, the newest applied decision is NH-031 of **2026-09-06**, and critic #64 confirmed at the Gmail connector that every message matching the report subject is one of the loop's own sends, with no reply in any thread. ⚠⚠ **And the second channel named in CHARTER §6 is a merged pull request, re-measured here at the GitHub MCP rather than inherited:** PR #31 is `state: closed`, `merged: true` since **2026-09-05T14:25Z**, merged by `github-actions[bot]` (the `promote` job of CHARTER §4c), with **zero** comments ever. This is **WFG-211**, already filed and already `todo` with `agent-doable: yes`; it is confirmed here and not re-filed, and its own cell correctly notes that where the author is told to reply is a **question** rather than a lap's choice. The sprint ends 2026-09-15, **four days out**.

## Critic's last direction note

**2026-09-11T0520Z, critic #64 at `5482ba8`. ZERO §3b row moves, and this page states its skip list instead of publishing a zero (WFG-246's substance, paid for this lap without waiting for its gate).** THREE new P0 KCF rows at position 1 (WFG-249, WFG-250, WFG-251), named as ONE lap and ONE rebuild; TWO new P1 infra rows (WFG-252, WFG-253); ONE existing row re-measured in place (WFG-217, whose cure is cheaper than recorded). **NO new NEEDS_HUMAN entry, deliberately**, for the second consecutive lap: all five findings are agent-doable, and a twenty-fifth question while twenty-four go unanswered is noise. Two existing entries were appended to with measurements rather than opinions: **NH-049** gets the 17-second number and the one-file scope, **NH-038** gets this window's count. **WFG-211** is confirmed at the GitHub MCP and not re-filed.

**The one direction change that matters is item 2 above.** The last five direction pages, this one's predecessors, each wrote a root objection about this project's exhibits not being measured against anything real, and each then named a prose repair. **WFG-129 is the row that answers that objection, it is agent-doable, it needs no reply from the author, and it has sat `todo` at table position 3 the whole time.** With four days of sprint left the loop can afford the kit bundle and then WFG-129; it cannot afford a sixth lap of prose while the one runnable experiment waits.

**Scorecard: both tracks HELD, Track B 96 and Track A 97.** Critic #63's pre-registered rise on 제출 자료 to 20 had its stated condition MET (WFG-247 and WFG-248 closed, kit rebuilt at a new stamp, bundle re-pointed, and I re-hashed 7 of 7 and 19 of 19 myself) and is **declined**, because the same repair introduced WFG-249 and left WFG-251 standing on the same two printed documents. No fall either: nothing drifted against a hash and the false noun is gone.

Verified at `5482ba8`. **GitHub `auto-gates` run 361 is `success` at this exact head**, so CHARTER §4b sets no finding #1 for the thirteenth consecutive lap; no run in the window has conclusion `failure`. `gates.py --mode full` exits **0** on its FIRST run in this sandbox (2092 passed, 65 skipped, 3 xfailed), `--assert-head` and `--assert-reported` both exit 0, and all dev reports in the window record `Reviewed by:`. The clone is SHALLOW at **54** commits and was deliberately NOT deepened; no ancestry claim is written anywhere in this lap.
