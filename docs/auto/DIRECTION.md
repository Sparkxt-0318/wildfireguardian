# Direction — where the project is going, on one screen

*Written 2026-09-04 by the author's session; steered again the same evening (product first). Rewritten by the research routine every second day; the critic checks it after every dev lap (CHARTER §14). The dev routine reads it before claiming a row. **Direction re-checked 2026-09-08T1429Z by critic #43, which SPENT NO REORDER: positions 1 and 2 of the last page (WFG-187, WFG-010) BOTH closed inside this window, at `2c6e366` and `692497a`, so WFG-188 reaches position 1 by fall-through and it is the right row. **R8 TICKED at `dee1bc1`** and the zero-tick streak ends at three critic laps; `KCF_READINESS.md` reads 8 of 11 and **R3 is the last of CHARTER §14b's six lines**, held by NH-046 (author, due 09-10). This lap set ONE `fix-before-next-row` item (**WFG-190**, minutes, two sentences of `README.md`), filed two P1 rows (**WFG-191**, **WFG-192**) and reclassified **WFG-179** `todo` to `blocked(NH-046)` as a step-5 update.** Critic #42's note is kept below.* (Superseded lead: Written 2026-09-04 by the author's session; steered again the same evening (product first). Rewritten by the research routine every second day; the critic checks it after every dev lap (CHARTER §14). The dev routine reads it before claiming a row. **Direction re-checked 2026-09-08T1115Z by critic #42, which SPENT ITS ONE REORDER on the same rule violation critic #40 spent its reorder on three laps ago: at `ceb43ba` the first `todo` row in table order was P1 infra `WFG-186` — whose own cell says §14b holds it behind R3 and R8 — with EIGHT P0 `todo` rows below it, `WFG-010` among them at line 151. Critic #41 raised WFG-010 P1 → P0 and did not move the row, so the priority changed and the position did not. **WFG-010 moved to the head of the `todo` block; nothing else moved.** This lap set ONE `fix-before-next-row` item (**WFG-187**, one command, judge-facing, filed FIVE commits before the limit rather than at it) and filed one new P0 row (**WFG-188**) at position 3. **Zero readiness lines ticked for the THIRD consecutive critic lap.** Critic #41's note is kept below.** (Superseded lead: **Direction re-checked 2026-09-08T0819Z by critic #41, which SPENT NO REORDER: positions 1 and 2 (WFG-167, WFG-181) BOTH closed inside this window at `c2a7980`, WFG-182 falls through to position 1 by fall-through rather than promotion, and it is the right row. I re-ran critic #40's own test on the new subject: `grep -ncE '창의|독창'` returns **0** on `docs/auto/JUDGE_QA.md` (44 cards), **0** on `web/finals.html` and **0** on `docs/auto/DEMO_SCRIPT_5MIN.md`. The same grep for 책임 and 개인정보 now returns 6 and 5 where critic #40 measured zero, so the method that found those two rows is working and 창의성 is what it points at next. This lap set ONE `fix-before-next-row` item (**WFG-185**, minutes, judge-facing) and promoted **WFG-010** P1 to P0 as a step-5 update to an existing row.** Critic #40's note is kept below. (Superseded lead: **Direction re-checked 2026-09-08T0524Z by critic #40, which SPENT ITS ONE REORDER: `docs/auto/BACKLOG.md` had P0 `todo` WFG-167 sitting BELOW five P1 `todo` rows (WFG-179, WFG-180, WFG-174, WFG-176, WFG-177), so a fresh lap reading the table alone — which CHARTER §5 tells it to do — would have taken a P1 infra row that §14b holds behind a block that is still shut. WFG-167 moved to the top of the `todo` block. Position 1 is unchanged and re-verified; what changed below it is that this lap FILED two new P0 rows (WFG-181, WFG-182) at positions 2 and 3, which is step 5 and not a reorder.** Critic #39's note is kept below. (Superseded lead: **Direction re-checked 2026-09-08T0217Z by critic #39, which SPENT NO REORDER: position 1 (WFG-139) closed, position 2 (WFG-167) falls through to position 1 and is still the highest-leverage row on the page, and the two new P1 rows both belong behind the readiness block.** Critic #38's note is kept below. (Superseded lead: **Direction re-checked 2026-09-07T2319Z by critic #38, which SPENT NO REORDER: WFG-171 closed inside the window, position 1 (WFG-139) is unchanged and is now better specified than it has ever been, and the two new rows are both P1 and both belong behind the readiness block.** Critic #37's note is kept below; #36's is in `docs/auto/reports/2026-09-07T1717Z-critic.md`.)*)

## Thesis (two sentences)

⚠⚠ **Corrected 2026-09-07T1100Z by critic #34; the previous sentence is quoted in the note below because it is the root objection.** A route planner and a rescue order that read a hazard field beat ones that ignore hazard entirely, and the repository proves *that* on committed public data for real Korean fires with every number re-derived by a gate. **What it has not measured is the step from 「where the fire is」 to 「where it will be」**: both headline margins are scored against a fire-blind opponent, and the forecast-aware arm plans on the same field it is scored against, so 42 and 91 are what a **noiseless** forecast would buy. The finals product is that proof, at that strength, made legible at a booth: the map, the routes, the honest limits.

## Next three rows, and why

*⚠⚠ **Rewritten 2026-09-08T1115Z by critic #42. Position 1 of the last page (WFG-182) and its `fix-before-next-row` item (WFG-185) BOTH closed inside this window at `5845953`, so WFG-010 falls through to position 1 and no reorder was spent on the page — the reorder was spent on the TABLE, which had not followed the page.** Re-verified here at `ceb43ba` by channels the 1013Z lap did not use: the bank is **45** cards and the header's 45 / T0 19 / T1 19 / T2 7 sums correctly by `grep -cE '^\*\*Q[0-9]+[a-z]? · T[0-9]'`; `grep -cE '창의|독창'` now returns **2** on `docs/auto/JUDGE_QA.md` where critic #41 measured **0**, and still **0** on `web/finals.html` and **0** on `docs/auto/DEMO_SCRIPT_5MIN.md`, which is what the card's own 「없는 것」 says; and Q29a's item 2 checks out against `docs/real_roads_real_hazard.md`, whose first table's third row is the run with a real OSM walk graph and a real `spread_v2` forward-sim over 459 origins.*

*⚠⚠ **Rewritten 2026-09-08T1429Z by critic #43. Positions 1 and 2 of the last page BOTH closed inside this window (WFG-187 at `2c6e366`, WFG-010 at `692497a` with the reviewer's fixes at `ea04478`), so this page turns over by fall-through and NO reorder was spent, on the page or on the table. The table's own order and this page now agree. The one thing that changed structurally is that **R8 ticked**, which leaves R3 as the sole unticked line of §14b's six, and R3 is the author's through NH-046.***

1. **WFG-190 (P0, minutes) - this lap's one `fix-before-next-row` item, and it displaces nothing.** Two sentences in the Round-4 section that just ticked R8. `README.md:235-239` claims the present-perimeter margin values are in no 심사용 자료, while `docs/auto/JUDGE_QA.md:849-852` carries 9, 27 and 5 and is bound into the 41-page printed kit as 「심사위원 질의응답 카드」; and `README.md:220-225` states that result with no mention that its buffer width is a free parameter, where `paper/manuscript.md:493` says 「nothing in the data chooses it」. No test binds either sentence. ⚠ Qualitative fix only: no margin value goes into `README.md` while NH-032 and NH-034 are open.
2. **WFG-188 (P0, one lap) - position 1 by fall-through, and it is the right row.** The bank now answers 책임, 개인정보 and 창의성 and still has no answer to what a county emergency office would need to run this. Confirmed rather than assumed: a `grep -nE` over `docs/auto/JUDGE_QA.md` for 군청, 도입 비용, 운영 비용 and 유지보수 returns **2** lines and neither answers it (`:703` is legal status, `:851` a passing clause). One of the five finals judges is a public-sector disaster-response official.
3. **WFG-054 or WFG-036's open half, whichever the lap can finish.** With R8 ticked and R3 author-held, the readiness checklist has no line a lap can move on its own. That is the honest state of the queue and it is why position 3 is named as a choice rather than as a row: the loop is now waiting on **NH-046** for the infra block and on **NH-032 / NH-034** for the headline, and neither is a lap's to decide.
*(Then **WFG-128**, unchanged and re-stated rather than re-derived: `docs/multi_region.md:189-194` carries `fa_exceeds_budget` with no budget qualification and no pointer to `docs/present_perimeter_arm.md`, on the page the README's own link sends a judge to.)*

*(Superseded, kept as the record.)* *⚠⚠ **Rewritten 2026-09-08T0819Z by critic #41. Positions 1 and 2 of the last page both CLOSED inside this window, so everything below moves up by fall-through and no reorder was spent.** Re-verified here at `9329400`: `git grep -niE '책임|법적|면책' -- docs/auto/JUDGE_QA.md` now returns **6** and `git grep -ciE '개인정보|프라이버시|privacy'` returns **5**, both **zero** one lap ago. **WFG-167 and WFG-181 shipped together at `c2a7980`** and I re-derived their load-bearing claims by channels the lap did not use: `sms.send` is called nowhere in `src/` or `scripts/`, `email_sent.json` appears **0** times in the tree, 28 JSON run records carry `nothing_was_sent: true` (the 29th grep match is `outputs/dispatch/README.md`, which is prose), `immobile_fraction` is **0.3** in `data/processed/rescue_routing.json`, and `vulnerability.py`'s module docstring does declare its county scores placeholders 「NOT yet anchored to authoritative data sources」.*

1. **WFG-182 (P0, one lap) — 창의성 is a fifth of the mark on both tables and no surface a judge meets mentions it.** Position 1 by fall-through. Critic #40 filed it as its root objection and measured the scorecard side (`awk` over the 창의성 columns returns the single line `39 15/15`, now 41 rows). This lap measured the **product** side, which is the half that matters for a booth: 44 Q&A cards, a five-minute demo script and the finals screen, and `grep -ncE '창의|독창'` returns **0** on all three. The 심사기준 names 창의성 first (「단순 암기 발표 지양; 창의성, 과학적 원리, 과학적 사고 중점」). It is an assembly of what is already in the tree onto a surface a judge meets, not a new experiment (CHARTER §3.4).
2. **WFG-010 (P0, hours) — NEW at this priority, and the promotion corrects the record rather than changing a mind.** It ticks **R8**, one of the two readiness lines holding §14b's infra block shut. Critic #39 recorded it as 「sitting inside the very block R8 helps hold shut」 and left it; that is a misreading, because §14b holds **loop hygiene** and this row's goal column is **KCF** while §14b's own sentence names 「README opening」 first among judge-facing surfaces. Nothing was holding it except its priority. Measured here: `grep -nE '^## Round' README.md` returns `:59` Round 2 and `:75` Round 3 and no Round 4. ⚠ CHARTER §3.12: the Round-2 section is a record and is not edited; Round-4 material goes **below** Round 3.
3. **WFG-128 (P0, one lap)** — unchanged and re-stated rather than re-derived: `docs/multi_region.md:189-194` carries `fa_exceeds_budget` with no budget qualification and no pointer to `docs/present_perimeter_arm.md`, on the page the README's own link sends a judge to. NH-031 already decided the registry half.

Ahead of all three, and displacing none of them, sits this lap's one `fix-before-next-row` item **WFG-185**: `docs/auto/JUDGE_QA.md:715` tells the student 「커밋된 33장 전부」 where `git ls-files '*dispatch_a4.html'` answers **642**, and the gate written for exactly that class passes because the run-directory string sits in a different claim's 근거 block. Minutes, judge-facing, and it must rebuild the kit and re-point the bundle in the same lap (WFG-152).

*(Superseded, kept as the record.)* *⚠⚠ **Rewritten 2026-09-08T0524Z by critic #40. Position 1 held and was re-verified rather than read; positions 2 and 3 are new P0 rows this lap filed, and the previous position 2 (WFG-128) drops to 4.** Re-run here at `47e48b5`: `git grep -niE '책임|법적|면책' -- docs/auto/JUDGE_QA.md` still returns **zero** across 42 cards while `web/finals.html:1393` prints 「최종 판단은 언제나 사람이 내립니다」 and `DEMO_SCRIPT_5MIN.md:217` gives 「최종 판단은 언제나 사람」 for the 재난대응 실무자 lens. **WFG-178 shipped at `82ec346`/`4f887c7`** and I re-derived it by a channel the lap did not use: whole-suite `pytest -rs` in this tile-less clone prints 60 skip lines, **11 naming SRTM, 7 tile-gated, 4 on the laptop bundle**, exactly what the cards now say.*

1. **WFG-167 (P0, one lap) — 42 cards and none of them about responsibility.** Unchanged from critic #39's statement of it and re-verified above. One of the five judges is a public-sector disaster-response official, and this is the one question the product's own screen invites and the bank cannot answer.
2. **WFG-181 (P0, one lap) — NEW, and it is WFG-167's twin from the same judge.** `git grep -ciE '개인정보|프라이버시|privacy' -- docs/auto/JUDGE_QA.md` returns **zero**, and so does the same grep on `web/finals.html`, `DEMO_SCRIPT_5MIN.md`, `docs/auto/finals/` and `README.md` — for a tool whose output is a household-level rescue list for elderly residents. ⚠ **It is filed at 2 rather than 1 deliberately** (§14b says a lap-sized judge-facing finding goes to position 1): 책임 is the sharper of the two because the screen itself invites it, and both are one card in the same file, so a lap that takes them together pays for one kit rebuild instead of two. **The answer already exists in committed files** — KOSIS/MOIS *aggregate density* and not a person, DBSCAN clusters and not 행정리, `nothing_was_sent: true` — which is why this is a card and not research.
3. **WFG-182 (P0, one lap) — NEW, and it is this lap's root objection. 창의성 is 20 points on both tables and has never moved.** The 창의성 column of `SCORECARD.md`'s combined series is **15/15 in all 39 rows** before today; `awk` over it returns the single line `39 15/15`. The total went 72 to 86 entirely on the other four rows, and `grep -c '창의성' docs/auto/BACKLOG.md` returns **2** across ~180 rows. This row assembles the creative claim that already exists in the tree onto a surface a judge meets. **It adds no experiment, no model and no region** (CHARTER §3.4).
4. **WFG-128 (P0, one lap)** — unchanged and re-stated rather than re-derived: `docs/multi_region.md:189-194` carries `fa_exceeds_budget` with no budget qualification and no pointer to `docs/present_perimeter_arm.md`, on the page the README's own link sends a judge to. NH-031 already decided the registry half.

*(Superseded, kept as the record.)* *Position 1 emptied by being done. **WFG-139 shipped at `ab4e71e`** and I re-derived it here rather than reading it: a cold `gates.py --mode full` at `1282198` left `data/raw` at **201,187 bytes** before and after, with `data/raw/dem/srtm/` empty. **WFG-173 shipped at `088203c`**, the branch is open and critic #38's parked lap is back on `auto/dev`. So both of the last page's leading items are closed, WFG-167 falls through to position 1, and it is the right row: the judge drill re-run here still returns **zero** matches for `git grep -niE '책임|법적|면책' -- docs/auto/JUDGE_QA.md` while `web/finals.html:1393` and `DEMO_SCRIPT_5MIN.md:217` both print 「최종 판단은 언제나 사람」 and one of the five judges is a public-sector disaster-response official. ⚠ **R3's blocker is gone and R3 still does not tick** (its own wording; WFG-179, NH-046), so §14b's infra block still holds behind R3 and R8.*

*(Superseded lead, kept as the record.)* *Position 1 is unchanged for a second consecutive lap and that is the right answer rather than a stall: **WFG-171 shipped at `fa18fcc`/`fa771d9`** with `WC-009` registered and the kit rebuilt, so position 0 emptied by being done, and position 1 fell through to the row this page already had there. **R3 is the readiness line still holding §14b's infra block shut, and WFG-139 is R3's blocker.** (Superseded lead, kept as the record: 「Nothing moved this lap and that is the finding, not the absence of one. **WFG-009 shipped at `6d1d730`/`804e5b6`** and **WFG-166 with it**, so positions 1 and 0 emptied by being done. **R1 TICKS** on that work — the first movement on that readiness line since the checklist was written, re-derived here on the sandbox AND on GitHub's clean `ubuntu-latest` runner (run 219, job `finals-acts`). §14b's infra block is therefore one line closer to releasing; **R3 is the line still holding it, and R3's blocker is position 1.**」)*

1. **WFG-167 (P0, one lap) — 42 cards and none of them about responsibility.** Re-run at `1282198`: `git grep -niE '책임|법적|면책' -- docs/auto/JUDGE_QA.md` returns **zero**, while `web/finals.html:1393` prints 「최종 판단은 언제나 사람이 내립니다」 and `DEMO_SCRIPT_5MIN.md:217` gives 「최종 판단은 언제나 사람」 as the one-line answer for the 재난대응 실무자 lens. One of the five judges is a public-sector disaster-response official, and this is the one question the product's own screen invites and the bank cannot answer.
2. **WFG-128 (P0, one lap) — the one number in this repository that runs against the project is misstated on the page the README sends a judge to.** `docs/multi_region.md:189-194`, re-read here and unchanged, carries `fa_exceeds_budget` 「0 for Yeongdeok … 2 for Uiseong-Andong and 3 for Uljin-Samcheok」 with no budget qualification and no pointer to `docs/present_perimeter_arm.md`. A judge who follows the README's own link reads the project's single self-damaging bucket in a form the project's own measurement contradicts. NH-031 already decided the registry half; the prose half needs no open decision.
3. **WFG-129 stays UNPROMOTED and position 3 is the head of the 「then」 chain below.** The page's standing reason is unchanged and re-stated rather than re-derived: promoting WFG-129 would produce a fourth contested fair-opponent margin while NH-032 and NH-034 are open and the student is forbidden to say any of the three that exist. Take WFG-117 (b) next unless the table has moved.

Ahead of all three, and displacing none of them, sits critic #39's one `fix-before-next-row` item **WFG-178** (the tile-gated skip set is **seven**, not the 「여섯 개」 on `JUDGE_QA.md:980`, `:1182` and `clean_clone_gates.md:85`, and `clean_clone_gates.md:45` names a commit with no guard in it; kit rebuilt and bundle re-pointed in the same lap). Critic #38's item **WFG-173** is **done** (`web/finals.html` names `1bca8ed`, 4 behind a limit of 30), and critic #37's **WFG-171** is **done**. Then **WFG-129**, WFG-117 (b), WFG-007's human half, WFG-124 (`blocked(NH-032)`), WFG-104, WFG-106, WFG-127, WFG-135, **WFG-142**, **WFG-143**, **WFG-150**, WFG-163, WFG-125, WFG-122, WFG-121 (c), WFG-036 v2 (booth-recipe half only), WFG-101, WFG-010, WFG-096, WFG-024 when its blockers clear, and only then the infra rows — WFG-131, WFG-132, WFG-137, WFG-141, WFG-149, **WFG-152**, **WFG-156**, **WFG-155**, **WFG-160**, **WFG-161**, **WFG-164**, **WFG-165**, **WFG-168**, **WFG-172**, **WFG-174**, **WFG-175**, **WFG-176**, **WFG-177**, **WFG-179**, **WFG-180** — which CHARTER §14b holds behind R1, R3 and R8. **R1 is ticked; R3 and R8 are not, so the block holds. R3's blocker changed identity this lap: WFG-139 is done, and what holds R3 now is the readiness line's own wording (WFG-179, NH-046).**

⚠⚠ **WFG-115's premise is false and stays withdrawn. `41498ef` IS an ancestor of `HEAD`.** Registered as `WC-004` since `923ffbd`. Do not act on the old premise.

## What not to do

- ⚠⚠ **NEW (critic #42): a row raised to P0 is REPOSITIONED in the same edit that raises it.** Critic #41 raised WFG-010 P1 → P0 and left the row at table line 151, below twenty P1 rows, so for two laps the priority and the position disagreed and CHARTER §5 sent a fresh lap to the P1 row anyway. Changing the letter is not the move; moving the row is. The same applies to a row lowered.
- ⚠⚠ **NEW (critic #42): a staleness gate is fixed with headroom, not at the limit.** `web/finals.html` closed `auto/dev` to every routine on 09-07 because the only lap that looked at the stamp looked when it was already at 30. Any lap that reads `git rev-list --count <stamp>..HEAD` and gets **20 or more** says so in its report; a critic lap that gets 20 or more may spend its one item on `make finals`. WFG-187, and the class is WFG-159/160/161 behind NH-043.
- ⚠⚠ **NEW (critic #42): do not let the `fix-before-next-row` queue have one entrance.** Six consecutive dev work commits edited `docs/auto/JUDGE_QA.md` and zero readiness lines ticked across three critic laps. A Q&A card is the cheapest judge-facing surface to fix, which is exactly why it wins every time the mechanism is asked for something small. NH-038 is the author's decision on it and is due 2026-09-09.

- ⚠⚠ **(critic #41): a mutation score is not a coverage number, and the loop must stop publishing it as one.** 「5 for 5」, 「9 tests, graded」 and 「8 for 8」 all mean 「every mutation the grading lap imagined」. Three consecutive laps shipped a gate on such a score and the next reader found a hole each time (`4679285`'s M7; WFG-185's scope assertion; WFG-178's spoken line). A lap reporting a mutation score names one mutation it could **not** make the gate catch, or says what it tried and failed to find. WFG-186.
- ⚠⚠ **NEW (critic #41): a reviewer's fix is not applied until it reaches the surface the reviewer was talking about.** `4679285` fixed the test, wrote the correction into the report's prose, and never touched `docs/auto/JUDGE_QA.md`, which is the file the finding was about. A lap that accepts a reviewer's fix greps the **surface** named in the objection and says in writing what it found there, exactly as the narrowing rule above already requires for claims.
- ⚠⚠ **NEW (critic #41): §14b's infra block covers loop hygiene, and it does not reach a row whose goal column is `KCF`.** WFG-010 sat as P1 for two laps under a reason that did not apply to it, and R8 stayed unticked as a result. Before filing or leaving a row behind the block, read its goal column.

- No new spread model, no retrain, no re-acquired region before 2026-10-16 (CHARTER §3). **This also bars measuring our own random-split-versus-LOFO penalty** — WFG-142 cites an external instance and says in its own voice that we have not measured ours.
- **⚠⚠ No accuracy comparison with NIFoS's 산불확산예측시스템 or 경기도's G-DAPS, on any surface, in either direction.** The only figures available are agency plan statements in a newspaper. The differentiator is the **output object** (`docs/auto/knowledge/KOREAN_OPERATIONAL_SYSTEMS.md` §3). Their 5 m terrain analysis is finer than our 500 m grid and the card says so.
- **⚠ An external paper's number never normalises one of ours.** Farajpoor & Narimani's 0.92 → 0.75 spatial-blocking penalty establishes that the penalty exists and is large; it is a different task, unit, geography and label, and it may not sit beside our AUC anywhere.
- **⚠⚠ No sentence claiming this project has measured the 「will be」 versus 「is」 step.** Every judge-facing surface that states 42 or 91 carries both binding caveats (fire-blind opponent; upper bound for a noiseless forecast). A new sentence about either number carries both caveats or it is not written.
- No fourth rewrite of the README's **opening paragraph about the 2025 fire**.
- No consultation-dependent claim (NH-010); no ratio between the chain and the season areas (NH-018).
- Do not spend a lap on the six-fire study-area map until per-fire burned areas are registered (WFG-060). Do not commit the bundle payload.
- **Do not put any fair-opponent margin (9, 27, 5, 19) on a judge-facing surface** until NH-032 is answered. `JUDGE_QA.md` Q19's do-not-say list is the one exception. ⚠ **This is also why WFG-129 is not promoted:** it would produce a fourth contested margin while two are unanswered and the student is forbidden to say any of them.
- **Do not overwrite `WFG_printables_20260907T1825Z.pdf` or its manifest** (CHARTER §3.2). **Do not release a claim younger than three hours** (§5b; NH-035). **Do not run `make baseline-freeze` in a sandbox.** **Do not use `curl` for the GitHub Actions API** (403 through the proxy; use the MCP).
- ⚠⚠ **Do not write a reachability or ancestry claim until `git rev-parse --is-shallow-repository` answers `false`.** Not 「deepened to N」. `false`. ⚠ **But take the baseline gate reading FIRST, on the default clone, before deepening.**
- ⚠⚠ **No judge-facing sentence says what somebody else's system does NOT do, in either language.** The only permitted register is what this project opened: 「저희가 연 자료(카탈로그 기록·언론 보도)에서는 … 확인되지 않습니다」 / `not stated in what was opened`. ⚠ **And a registered spelling reaches ONE language:** a lap that registers a claim registers both languages, in the same lap, or writes down which one it did not. The lint that would find a *newly written* one is WFG-168 and does not exist yet.
- ⚠⚠ **NEW (critic #37): the same rule binds a POSITIVE assertion about somebody else's system, and until now nothing said so.** 「저쪽은 X를 하지 않습니다」 and 「저쪽은 Y를 합니다」 have identical provenance here — a catalogue table of contents and two newspaper articles — and identical failure modes in front of a judge who has used the system. A judge-facing sentence about another system carries what opened it (「카탈로그의 목차가 …」, 「경향신문 2026-03-30 보도에 따르면 …」) or it is not written. Five of the six instances found this week were negatives and the loop wrote a rule for negatives; the sixth, live on the printed panel and the recited card at `64f015b`, is a positive (**WFG-171**).
- ⚠⚠ **A lap that narrows a claim greps for the SUBJECT of the claim, never for the sentence it just wrote.** `git grep -n "<subject>" -- docs/ paper/ release/ web/`, before it calls the narrowing done, naming in writing every file the grep returned and what it did about each.
- ⚠⚠ **A withdrawal is not applied until it is REGISTERED** in `docs/auto/withdrawn_claims.json`, in the same lap (CHARTER §3.5c). Registration cannot reach a claim that was **narrowed** rather than withdrawn.
- ⚠ **Do not report a pass/skip count without saying cold or warm, and without saying whether the run downloaded anything** (WFG-139). ⚠ **The 1900Z dev lap broke this rule the same day it was written** (1688/56, no cold/warm word), and the cold count at the same tree is 1682/62, so the omission reads as a six-test regression. The gate that would stop it is **WFG-172**.
- ⚠⚠ **NEW (critic #39): do not hand-type a number about this repository's own test suite onto a judge-facing surface.** 「여섯 개」 was written onto three surfaces by the commit that made it seven (WFG-178), and `grep -rn '여섯 개' tests/` returns nothing, so nothing was ever going to catch it. A card about the suite points at `pytest -rs` and its own skip reasons, or it is derived by a gate; a bare integer typed once is a fourth thing to go stale. The same rule binds `1708/63`, 「933 gated files」 and any 「N tests」 sentence.
- ⚠⚠ **Do not treat `make verify` green as evidence that a withdrawn claim is gone.** The registry reads `.md` and `.html` only (WFG-155).
- ⚠⚠ **The critic and research routines must not edit `docs/auto/JUDGE_QA.md`, `docs/auto/DEMO_SCRIPT_5MIN.md`, `docs/auto/finals/BOOTH_SETUP.md` or `docs/auto/finals/RELATED_WORK_PANEL.md` at all.** They are `SOURCES` of the printables manifest, and since `590c29a` a one-line edit to any of them turns `tests/test_printables.py::test_the_newest_printable_is_not_stale_against_the_tree` **red** — probed and reverted at `3f881f6` by critic #31. Only a lap that rebuilds the kit at a new stamp in the same lap may touch them. **This is what stops critic #37 from writing WFG-171's fix itself.** WFG-152.

- ⚠⚠ **NEW (critic #40): WFG-180 is NOT a minutes fix and its own row said it was.** `tests/test_tile_gated_skip_count.py` derives the `srtm_named_only` set, and that set **is** WFG-180: renaming the four reasons takes it 4 → 0 and the derived total 11 → 7, turning four judge-facing sentences red in `docs/auto/JUDGE_QA.md` (a printables `SOURCES` file) and `docs/clean_clone_gates.md`. The lap that takes it corrects the cards, rebuilds the kit and re-points the bundle in the same lap, or it ships a red tree. Generalise it: **a gate that binds prose to a known defect pins the defect in place, and the row that will remove the defect must say so.**
- ⚠⚠ **NEW (critic #40): do not open a new NEEDS_HUMAN DECISION while thirteen are unanswered, unless work is actually blocked.** Thirteen DECISION entries and one BLOCKER are open; **NH-032 and NH-034 went overdue on 2026-09-08** and NH-035, NH-038 and NH-043 come due 09-09. The author's chosen channel (NH-020) needs them at the laptop, and the last decision applied was **NH-031 on 2026-09-06**. Fifteen entries have been opened since and none closed. A lap with a question first checks whether an open entry already carries it and **updates that entry** instead; this lap did exactly that for NH-037 and opened nothing.

## Critic's last direction note

**2026-09-08T1429Z, critic #43. NO reorder spent; ONE `fix-before-next-row` item; two P1 rows; one status correction. And a readiness line ticked.**

Verified rather than read, all at `dee1bc1`, on the routine's **default** clone before any deepening (`is-shallow-repository` = **true**, `rev-list --count HEAD` = **50**; oldest resolvable `7cc4eb7` at 16:12Z on 09-07, so this clone reads about 22 hours and I claim nothing older and no ancestry at all): `gates.py --mode full` is **ALL GREEN**, exit **0** (`1749 passed, 63 skipped, 2 xfailed`, pytest 312.3 s), the run downloaded nothing (`du -sb data/raw` = 201,187 B, `data/raw/dem/srtm/` empty), both asserts exit 0, and `auto-gates` runs 230 to 249 on `auto/dev` carry **zero failures** with run 249 green at this exact head.

**Is the next `todo` row still the highest-leverage one for 2026-10-24? Yes, and no row was moved.** WFG-188 reached position 1 because WFG-187 and WFG-010 both closed, not because anything was promoted. It is a P0 KCF row aimed at the judge most likely to ask a question the bank cannot answer, and I confirmed the gap with a grep rather than inheriting critic #42's.

**Readiness lines ticked in the last 24 h: ONE (R8).** Three consecutive critic laps had recorded zero; that streak ends here, and the direction finding the routine prompt defines does not fire this lap. Two different judge-facing surfaces moved in one window (the finals screen's stamp at `2c6e366`, the README's front door at `692497a`), which is the first window in six where the answer to 「what did the judges gain today」 is not a Q&A card. That measurement is written into **NH-038**, because it is evidence bearing on the author's open question about the product-first rule rather than a point for the loop to award itself.

**The direction fact that matters more than the ordering.** With R8 ticked, **R3 is the last of the six readiness lines CHARTER §14b names**, and R3 cannot be ticked by any lap: its row WFG-179 ends 「whichever the author picks in NH-046」, and NH-046 says a lap doing it would be grading its own homework. This lap set WFG-179 to `blocked(NH-046)`. **Eight P1 infra rows now wait on one email reply, due 2026-09-10.** Nothing in the loop's power shortens that queue; it is on the page so the next lap does not spend a slot discovering it.

⚠ **No `Do NOT edit` note is written here** (CHARTER §14c, NH-036 A). The single scoped prohibition this lap wrote lives in `CRITIC_LATEST.md`, covers `README.md:220-239` only, forbids one thing (a margin value while NH-032 and NH-034 are open), and expires at critic #44 unless that lap re-states it after re-reading those two entries.


⚠ **This page is 226 lines and CHARTER §14 says one screen.** Measured, not felt: `wc -l` answers
**226**, it was 210 before this lap, and the growth is entirely the nested 「Superseded lead:」
chain in the header, which now runs five leads deep in a single paragraph. Each critic lap adds
one and deletes none, correctly, because §3.7 forbids deleting. **The fix is an archive file, not
a deletion, and it is not this lap's to make** while §14b holds hygiene behind R3; it is recorded
here so the next lap that can take it does not have to rediscover why the page stopped being one
screen. No row filed: three is enough from one critic lap and this one has no judge in it.

*(Critic #42's note follows, kept as the record.)*

**2026-09-08T1115Z, critic #42. ONE reorder, spent on a rule violation rather than a preference; ONE
`fix-before-next-row` item; ONE new P0 row.**

Verified rather than read, all at `ceb43ba`, on the routine's **default** clone before any deepening
(`is-shallow-repository` = **true**, `rev-list --count HEAD` = **50**; the oldest resolvable commit is
`2720840` at 10:09Z on 09-07, so this clone reads about 25 hours and I claim nothing about anything
older): `gates.py --mode full` is **ALL GREEN**, exit **0** (`1741 passed, 63 skipped, 2 xfailed`,
pytest 258.3 s), and **the run downloaded nothing** (`du -sb data/raw` = 201,187 bytes before and
after, `data/raw/dem/srtm/` empty). Cold on the tile, warm on `data/cache`. `--assert-head` exits 0
and `--assert-reported --base 2720840` exits 0 over 69 substantive paths. `auto-gates` runs **216 to
241** are 21 `success`, 5 `cancelled`, **zero `failure`**; run 241 at this head is `success`. **No
CHARTER §4b finding.** KCF_READINESS holds at **7 of 11** with **zero lines ticked for the THIRD
consecutive critic lap**.

**Why the reorder.** Same shape as critic #40's, three laps later, and this time the cause is
recorded rather than guessed: the four P0 rows that had been sitting above the P1 infra block all
closed, the block surfaced, and the one P0 row that should have taken their place had been given the
priority but not the position. A rule that lives on this page and is contradicted by the table the
lap actually reads is not a rule. One row moved.

**Why WFG-187 is the item, said against the bar NH-038 B set.** One command on the surface §14b's own
list names. It is critic #38's WFG-173 with five commits of headroom instead of none, and WFG-173 was
filed **after** the branch had shut and critic #38's own lap had to park on `auto/red/`.

**The root objection is that six consecutive dev work commits edited the same file while the
product's definition of done did not move.** `git show --name-only` over `6d1d730`, `fa18fcc`,
`82ec346`, `ab4e71e`, `c2a7980` and `5845953` returns `docs/auto/JUDGE_QA.md` in all six.
`KCF_READINESS.md` holds at 7 of 11 across the same span. `web/finals.html` was touched once, by a
rebuild, and it is the artifact now five commits from closing the branch. Every one of those six laps
picked correctly; the aggregate is that the queue has one entrance, because a Q&A card is the
cheapest judge-facing surface there is. The cheapest test is one the author already holds: **NH-038,
due 2026-09-09**, asks this in the author's own words about three laps. It is now six. This lap opened
no new entry and wrote the measurement into NH-038 instead.

**2026-09-08T0819Z, critic #41. NO reorder, ONE `fix-before-next-row` item, and one existing row re-priced.**

Verified rather than read, all at `9329400`, on the routine's **default** clone before any deepening
(`is-shallow-repository` = `true`, `rev-list --count HEAD` = **50**, so I claim nothing about anything older):
`gates.py --mode full` is **ALL GREEN**, exit **0** (`1735 passed, 63 skipped, 2 xfailed`, pytest 466.2 s), and **the
run downloaded nothing** (`du -sb data/raw` = 201,187 bytes before and after, `data/raw/dem/srtm/` empty). Cold on the
tile, warm on `data/cache`. `--assert-head` exits 0. `auto-gates` runs **216 to 236** are 15 `success`, 5 `cancelled`,
**zero `failure`**; run 236 at this head is `success`. **No CHARTER §4b finding.** KCF_READINESS holds at **7 of 11**
with **zero lines ticked for the second consecutive critic lap**, which fires the direction finding.

**Why no reorder.** Positions 1 and 2 both closed by being done. WFG-182 fell into position 1 and is the strongest row
on the page by the rubric's own weighting. Nothing above it should move up.

**Why WFG-185 is the item, said against the bar NH-038 B set.** It is a one-line correction to a spoken sentence on a
printed page, plus a tightened assertion and the kit rebuild every `SOURCES` edit already requires. That is the same
shape and the same cost as critic #39's WFG-178, which the loop cleared inside one lap. It does not displace WFG-182.

**The root objection is that this loop reports the completeness of its own gates as a fraction it chooses the
denominator for, and a fresh reader has broken that fraction three laps running.** The 0655Z lap graded its new gate
**5 for 5** and shipped it as trustworthy; its own independent reviewer reproduced all five, invented three more, and
**M7 found 609 of 642 tracked sheets unguarded**. The widened gate was graded again and called trustworthy; I then
found its scope assertion is satisfied by a string sitting in a different claim's 근거 block, so the card can state a
wholly unscoped count and stay green. The same shape ran one lap earlier at `82ec346`. 「5 for 5」 reads to the author
as coverage and means 「every mutation I thought of」, which is 100 % by construction and cannot fall. The cheapest
test, and it costs one sentence: **a lap that reports a mutation score also writes down one mutation it could not make
the gate catch.** If it cannot name one, the grading is unfinished, not perfect. That is **WFG-186** for the class and
**WFG-185** for today's instance.

**2026-09-08T0524Z, critic #40. ONE reorder, spent on a rule violation rather than on a preference, and NO
`fix-before-next-row` item.**

Verified rather than read, all at `47e48b5`, on the routine's **default** clone before any deepening
(`is-shallow-repository` = `true`, `rev-list --count HEAD` = **50**; ⚠ the oldest resolvable commit is `0fc6130` at
04:08Z on 09-07, so this clone reads **49 commits / about 25 hours** and I claim nothing about anything older):
`gates.py --mode full` is **ALL GREEN**, exit **0** (`1722 passed, 63 skipped, 2 xfailed`, 277.3 s), and **the run
downloaded nothing** — `du -sb data/raw` answers 201,187 bytes and `data/raw/dem/srtm/` is empty afterwards. Cold on
the tile, warm on `data/cache`. `--assert-head` and `--assert-reported --base 0fc6130` both exit 0.
`auto-gates` runs **211 to 231** are 18 `success`, 3 `cancelled`, **zero `failure`**; run 231 at this head is
`success`. **No CHARTER §4b finding.** KCF_READINESS holds at **7 of 11**, zero lines ticked since critic #39.

**Why the reorder.** The table put P0 `todo` WFG-167 below five P1 `todo` infra rows. CHARTER §5 tells a fresh lap to
take the first `todo` row in table order, §14b holds those infra rows behind a block that is still shut, and only this
page said so. A rule that lives in one file and is contradicted by the file the lap actually reads is not a rule. One
row moved; nothing else.

**Why NO `fix-before-next-row` item, said plainly.** I looked and found nothing that qualifies under §14b as amended
by NH-038 B: no gate is red, and both judge-facing findings I have (WFG-181, WFG-182) are a lap each, so §14b files
them as P0 rows and forbids them as preemptions. Spending the item on something smaller would have displaced WFG-167
for the fifth consecutive lap, which is the exact complaint NH-038 was written to answer. **The next dev lap runs the
table.**

**The root objection is that this project has been graded on honesty for six days and 창의성 has not moved once.**
`awk` over the 창의성 columns of the combined series returns the single line `39 15/15`: every row from the first
critic lap (`1113388`, 09-03) to critic #39 scores 15 and 15, while the totals went 72 to 86 on 제출 자료, 데이터
수집·분석·해석 and 설계와 방법론. 창의성 is **20 points on both KCF tables**, the 심사기준 names it first
(「단순 암기 발표 지양; 창의성, 과학적 원리, 과학적 사고 중점」), and it plus the interview is over half the ISEF
score. The cheapest test, run here: `grep -c '창의성' docs/auto/BACKLOG.md` returns **2** across about 180 rows. The
loop has never aimed a lap at the row worth a fifth of the mark. That is **WFG-182**, and it is a card and an
assembly of what already exists, not a new experiment.

*(Superseded, kept as the record.)* **2026-09-08T0217Z, critic #39. No reorder, and the reason is that the page's own position 1 closed and what fell
into it was already the right row.**

Verified rather than read, all at `1282198`, on the routine's **default** clone before any deepening
(`is-shallow-repository` = `true`, `rev-list --count HEAD` = **50**; ⚠ the oldest resolvable commit is `5cca6ce` at
02:25Z on 09-07, so this clone reads the window from there forward and **not** the full 24 h): `gates.py --mode
full` is **ALL GREEN**, exit **0** (`1708 passed, 63 skipped, 2 xfailed`, 290.9 s, **COLD**), and **the run
downloaded nothing** — `du -sb data/raw` answers 201,187 bytes before and after. `--assert-head` and
`--assert-reported --base 5cca6ce` both exit 0. `auto-gates` runs **207 to 227** are 19 `success`, 2 `cancelled`,
**zero `failure`**; run 227 at this head is `success`. **No CHARTER §4b finding.** KCF_READINESS holds at **7 of
11**; R1 ticked inside this window.

**Why no move.** WFG-167 was position 2 and is now position 1 by fall-through, not by promotion, and it is the
strongest row on the page: the screen a judge stands in front of says 「최종 판단은 언제나 사람이 내립니다」, one of
the five judges is the person who would have to live with that sentence, and the bank has 42 cards and no answer.
Nothing above it should move up, and the two rows this lap filed are both P1 infra behind a block that is still
shut.

**The root objection is that this project registers every number it says about the world and none of the numbers it
says about itself.** The WFG-139 lap built the best mechanism this repository has produced for its own
reproducibility claim, then hand-typed the count that describes what the mechanism leaves open, and the count was
made wrong by a test the same commit added. `docs/NUMBERS.json` has 383 gated entries and
`docs/auto/withdrawn_claims.json` ten gated families, both built because a hand-typed figure about the world got
loose; 「여섯 개」, `1708/63` and 「933 gated files」 are prose that nothing reads. The cheapest test, run here:
`grep -rn '여섯 개' tests/` returns nothing, and no assertion binds a judge-facing skip count to what `pytest`
itself reports. That is **WFG-178** for today and **WFG-172** for the class.

*(Superseded, kept as the record.)* **2026-09-07T2319Z, critic #38.** No reorder; WFG-171 closed inside the window, position 1 (WFG-139) stayed and got a better description of itself, and the root objection was that the loop keeps writing 「filed」 for work no gate will ever look for (WFG-175). Full note in `docs/auto/reports/2026-09-07T2319Z-critic.md`.

*(Superseded, kept as the record.)* **2026-09-07T2020Z, critic #37.** Positions 0 and 1 both closed inside the window (WFG-166, WFG-009), R1 ticked for the first time in the checklist's life on two independent machines, and the root objection was that the loop's rule about another system's capabilities covered only the negative direction while the positive one was live on the printed panel (WFG-171). Full note in `docs/auto/reports/2026-09-07T2020Z-critic.md`.
*(Superseded, kept as the record.)* **2026-09-07T1700Z, critic #36.** Closing WFG-110 left R1 blocked by WFG-009, a row parked behind the very line it unblocks; P1 → P0 was the one row move, and the root objection was that `WC-007`/`WC-008` each escaped in the language they were not registered in. Full note in `docs/auto/reports/2026-09-07T1717Z-critic.md`.

## The research lap's note (2026-09-06T1817Z)

**The blind spot was not in the literature, it was in the landscape.** Plain Korean search turned up two operational Korean wildfire-**spread** systems — NIFoS's console and 경기도's G-DAPS — that no sweep had ever looked for. The differentiator is the output object, never accuracy. New note: `docs/auto/knowledge/KOREAN_OPERATIONAL_SYSTEMS.md`. Full run: `docs/auto/research/WEEKLY_2026-W36.md`; parked ideas with their objections: `IDEAS_PARKED.md`; venue status: `IEEE_PLAN.md`. Two scan channels failed and it is recorded rather than hidden — Semantic Scholar returned **429** to this sandbox on every attempt, and the Scholar Gateway MCP needs an OAuth the author must grant.

*(Critic #33's note is in `docs/auto/reports/2026-09-07T0816Z-critic.md`; #32's in the 0521Z. This page stays one screen, which is why older notes live in the reports.)*
