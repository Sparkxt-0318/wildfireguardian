# Direction — where the project is going, on one screen

*Re-checked 2026-09-12T1125Z by critic #74 at `092c907`. Last rewritten 2026-09-10T1817Z by the research routine (CHARTER §14). The dev routine reads this before it claims a row; the critic re-checks it after every dev lap, at most one reorder per lap, with a reason. Only the most recent critic note is kept. **Critic #74 SPENT NO §3b row move.** WFG-267 and WFG-270 both closed, so table order is now WFG-256 then WFG-255, which is where it should be: after nine prose repairs and three experiments in thirty hours, the next row is an experiment. The one thing this lap wants done first is **minutes**, which CHARTER §14b routes to the `fix-before-next-row` slot and not to a reorder.*

## Thesis (two sentences)

WildfireGuardian forecasts where an already-burning Korean wildfire goes next, and turns that forecast into a per-point walk-or-be-rescued decision for rural elderly residents, on public data a stranger can re-derive. Its defensible contribution is not forecast accuracy and not the architecture: it is the **output object and its measured limits**, a time-dependent decision per point with a page saying how wrong it can be.

⚠ Bokade et al. published this project's whole pipeline on **2026-09-09** (10.5281/zenodo.22668357). Nothing here rests on it, because WFG-251 narrowed Q16d to 「**공개된 기록과 초록에는** 성능 수치가 하나도 없습니다」. **Claim the measurement, never the architecture.**

## Before any row: this lap's `fix-before-next-row` item

**ONE ITEM, and it is minutes on the object a judge physically holds: merge `docs/auto/JUDGE_QA_PENDING.md` P-005 into `docs/auto/JUDGE_QA.md` Q39, run `make printables` at a new stamp, and re-point `release/kcf-finals-2026/MANIFEST.json`.**

Q39 (`docs/auto/JUDGE_QA.md:1554`) tells the student to print the three pre-built `dispatch_a4.pdf` and regenerate the other thirty. Measured here: of those three, **exactly one** (`outputs/dispatch/20260801T163042Z/02-천전공원-일대`) prints 「예산 내 차량 진입로가 화재로 차단됨(우회 포함)」, the sentence the 2026-09-12 emitter repair withdrew, and the thirty regenerated sheets print 「어느 거점에서도 생존 인지 차량 진입 경로가 확인되지 않음」. **The card as printed manufactures a stack that shows one judge two sentences for one code condition.** Q39 is source **3 of the 7** in `manifest_20260911T2137Z.json` (kit `WFG_printables_20260911T2137Z.pdf`, **60** pages), and that kit is named by the release bundle, so this is on paper in the box and not only in the tree.

**Why it is minutes, and why the reason it was not done does not hold.** P-005 is already written and paste-ready; it replaces one Q39 paragraph. The 0920Z dev lap drafted it and did not merge it 「because NH-049 is open」. **NH-049 constrains the critic, not a dev lap** — its own text reads 「the critic has exactly two legal moves」 and its recommended option A reads 「have the next dev lap that rebuilds the kit merge and empty it (this is WFG-205)」. NH-049's own appended measurement prices `make printables` at about a minute batched. ⚠ **One correction to the draft before it is merged:** P-005 reads 「미리 만들어 둔 장은 … 사유가 옛 문장이고」, which reads as all three; it is one of three. ⚠ Merge **P-005 only** this lap; P-001 to P-004 stay with WFG-205, because five cards is not minutes.

**What this lap checked so the next one does not re-check it:** `gates.py --mode full` exits **0** on its FIRST run at `092c907` (**2233** passed, 65 skipped, 3 xfailed, pytest 357.8 s); `--assert-head` and `--assert-reported` both exit 0, and `--assert-reported --base <each push in the window>` exits 0 for all ten; **every `auto-gates` run on `auto/dev` in the window concluded `success`** (399 to 409, run **409** green at `092c907`; **403** and **404** are `cancelled`, superseded by a newer push, not failures); all **39** reports dated 09-11 and 09-12 record `Reviewed by:`; the printed kit hashes **7 of 7** at 60 pages and the bundle **19 of 19** against its own `source` fields; `scripts/probe_dispatch_pdf_fonts.py` re-run here reproduces **38** PDFs, **3** stale, **0** current; and nothing new is on either decision channel.

⚠ **Critic #74 nearly published a correction of a CORRECT finding, and the near-miss is the rule.** It read WFG-272's 「nine line numbers into `paper/make_figures.py`」 as nine *citations*, counted six, and had the rebuttal half-written before re-reading `docs/figure_legend_claims.md:127` and finding the sentence is about nine **grep hits**. Re-run at this head: the grep returns **133, 146, 172, 251, 282, 573, 580, 585, 754** and the document names **133, 146, 173, 252, 283, 574, 581, 586, 755**. WFG-272 is right as filed, and the corrected pointers are now on the row so the fix is two minutes. **Before you contradict a finding, run the command the finding names, not the command you think it names.**

⚠ **Do NOT `git fetch --unshallow` in a critic or dev sandbox.** CHARTER §4 forbids writing an ancestry claim from a shallow clone; it does not require deepening. Unshallowing drags in eleven side branches and turns `tests/test_timeline_roles.py`'s history check RED on a tree that is fine (critics #60 and #63 both paid). **RE-STATED by critic #74 after re-running the case in its own process, which is what CHARTER §14c requires: the clone is shallow at **50** commits and was NOT deepened, and `gates.py --mode full` exits 0 on its FIRST run** at `092c907`. The cost, stated: in a shallow clone that history check **SKIPS** rather than runs — the `skipif` is `tests/test_timeline_roles.py:234-238`, guarding `test_the_artifact_still_agrees_with_the_history_when_the_clone_has_one` at `:239` — so a green critic gate does not certify it; GitHub at `fetch-depth: 0` does, and run **409** is green at `092c907`. ⚠ A second cost, measured here: `git log --since` on a path whose newest commit sits at the clone boundary answers with that boundary commit and `git show` renders the file as `A`, which looks like a change and is not. Recorded on **WFG-217**. This note names those file lines and that measurement and expires at critic #75 unless that lap re-runs the case.

## Next three rows, and why each is next

0. **Before the row: the `fix-before-next-row` item above.** It is minutes and it is never a reorder, so the top row is displaced by minutes at most.

1. **WFG-256 (P0, science), now at table position 1 because WFG-267 and WFG-270 closed:** the rotation null. It is still the only thing that would license a sentence about 「모양」, and Q36's booth answer currently has to end 「그것이 모양 때문인지는 아직 재지 않았습니다」 in front of five judges. ⚠ Pre-register the rule in the claim commit before any number exists, the way WFG-259 and WFG-260 were run.

2. **WFG-255 (P0, science)** second — the 55 disconnected pieces the footprint is graded against, which no file says out loud.

3. Then **WFG-236**, **WFG-244**, **WFG-245**, **WFG-237**. ⚠ **WFG-234 remains the strongest science row after those**; pre-register it in the claim commit.

⚠ **The board at `092c907` after this lap: 269 rows, 13 P0 `todo`**, measured here with the rule written beside it. Split each row whose first cell is a `WFG-` id on pipes **not** preceded by a backslash, strip each cell, count a row P0 when cell 2 begins `**P0` and `todo` when cell 5 begins `todo`. Reconciliation from `4bf34ab`: 267 rows and 15 P0 `todo` there; **WFG-267 and WFG-270 closed** (15 to 13); **WFG-274** (dev lap) and **WFG-275** (here) are P1 and add 2 rows without touching the P0 count. ⚠⚠ **The rule's own failure is unchanged and re-measured here: exactly 12 rows paste a literal unescaped pipe** and the rule reads their `status` cell as a fragment of their own prose — WFG-271, WFG-181, WFG-182, WFG-188, WFG-215, WFG-197, WFG-175, WFG-168, WFG-115, WFG-243, WFG-107, WFG-112. That is **WFG-191**. Critic #74 re-ran the count after writing this lap's four annotations and added none. Do not publish a board count without the rule that produced it.

## What not to do

- **Do not start a P1 row while a P0 row is `todo`**, and do not file one above the P0 block.
- ⚠⚠ **RE-RUN EVERY MEASUREMENT YOU WROTE DOWN, AGAINST THE TREE YOU ARE ABOUT TO COMMIT, IMMEDIATELY BEFORE YOU COMMIT IT.** Not a different grep — the *same* one, again, at the end. **Critic #73 wrote this rule and the very next window produced two more instances**, both in the paper family: `paper/README.md:1514` opens the lap-35 paragraph with 「8,999 to 8,999; margin 1 to 1」 while `:1560` in the same paragraph says 「8,999 to 9,000; margin 1 to 0」 and `paper/STATE.json` records **9000**; and that file's `built_pages_note` header carries the same dead pair. Both were true of the first draft and false of the tree that shipped, because the reviewer's repair landed after the headline was written. That is **WFG-275**. A sentence of the form 「X is at line N」 or 「X went from A to B」 is a command you already know how to run.
- ⚠⚠ **Before you call a row done, grep the judge-facing set for the NEGATION of what you just measured** — README, `docs/auto/JUDGE_QA.md`, `docs/auto/DEMO_SCRIPT_5MIN.md`, `docs/auto/finals/`, `web/finals.html`, `paper/manuscript.md`. ⚠⚠ **And then grep the file you just edited** for the sentences that DESCRIBE it (critic #69), for the sentences that DEFINE the terms your edit uses (critic #70), and for OTHER OCCURRENCES OF THE STRING YOU JUST CHANGED (critic #72).
- ⚠⚠ **NEW, critic #74: a contiguous grep cannot find an emphasised quotation of the string you are registering.** The dev lap's reviewer caught this: `README.md` shipped 「no safe walking route at all」 plain, every document that quoted it bolded the emphasis as `route **at all**`, and the critic's certification 「exactly one gated file, no per-line pragma anywhere」 was false at the head it was measured at. Three further gated files needed the pragma. **`WC-020` had already written the `\*{0,2}` tolerance into its own `why` field, twice, six laps earlier.** When you register a spelling, read the registry's existing entries for the tolerance before you write the pattern.
- ⚠ **Do not read `NH-049` as blocking a dev lap.** It constrains the critic. Its option A assigns the staging-file merge to a dev lap by name (**WFG-205**). A dev lap that merges a card and pays `make printables` is executing the recommended option, not deciding it.
- ⚠ **Do not put any `ppy_yeongdeok_*` count on a judge-facing surface** while **NH-059** is open, and do not point at the section that prints them.
- ⚠ **Do not widen WFG-259 into WFG-033(b) or NH-027**, and **do not weaken `docs/present_perimeter_yeongdeok.md` §5** — item 6's scoring-side conclusion is TRUE and stays word for word.
- ⚠ **Do not re-open WFG-254, WFG-258, WFG-262, WFG-266, WFG-267 or WFG-270.** The 「모양」 half of WFG-254 is **WFG-256**; WFG-262's premise was measured **false** and that result stands; WFG-267's residual is the Q39 merge above, not the row.
- ⚠ **Do not weaken `docs/disc_null.md` §4's centroid finding**, and do not weaken 「방향은 아닙니다」.
- ⚠ **Do not regenerate `paper/figures/F10_disc_null.png`, `F5_decision_shift.png`, `F8_routing_map.png` or `F3_regions.png`** (CHARTER §3 rule 2). `F10b`, `F5b`, `F8b` and `F3b` are the corrected files. NH-042 is still open.
- ⚠ **Do not rewrite any committed sheet under `outputs/`** — they are the record; WFG-267 put a note beside them instead. ⚠ **Do not weaken 「실제 확산면으로 만든 출동 지시서는 아직 없습니다」** (**NH-057**, **WFG-242**).
- ⚠ **Do not touch `docs/submission_reconciliation.md:63`.**
- ⚠ **Do not write 「여러 개의 산불」, or any count of fires, from WFG-255's measurement alone**, and do not read WFG-260's **226** components as a count of fires.
- ⚠ **Do not edit a scorecard row another lap wrote** (CHARTER §3.7). Two more defects are reported on **WFG-252** rather than fixed: the Track B and Track A tables each end with a `2026-09-11 b6778e7` row sitting below the 09-12 rows, and the Track B `cd18782` row spells 「제울 자료」.
- ⚠ **Do not add a syllable to `docs/auto/DEMO_SCRIPT_5MIN.md` without saying which segment pays for it** (**WFG-257**; 6.00 syllables per second, unchanged since `f7ee58d`). <!-- collision-ok: 6.00 — the SPOKEN RATE of the whole script in syllables per second, which is `syllables_per_second` in `data/processed/demo_script_pace/pace_20260911T0620Z.json`. The gate reads it against the `demo_pace_*_rate_spread` keys (1.02, 1.03, 1.62), which are the RATIO of the fastest segment's implied rate to the slowest (`implied_rate_spread`, unit x, max over min). Two different quantities, and this line names both on purpose. Neither value is stale. -->
- ⚠ **Do not add a word to `paper/manuscript.md` without trimming one — and as of this head that is arithmetic, not a style rule.** `body_words` is **9000** against a hard fail of 9000; the margin is **zero**. NH-037 is open and its severity is raised. ⚠ **Do not trim a caveat or a limitation to buy margin**, which is what the last five paper laps refused; and **do not hand-write `built_pages`** — it is **23** with a re-derived anchor and only a machine with LibreOffice Writer may move it.
- ⚠ **Do not "fix" WFG-249 by weakening Q20a's privacy answer**, and **do not swap 가구 for 지점 in the demo closing, or either count**.
- ⚠ **Do not open, cite or characterise the Bokade et al. body PDF**, and do not widen Q16d beyond 「공개된 기록과 초록」.
- ⚠ **Do not touch** Q16's 「가구 단위 폐쇄 시각」, Q16a's lines, the ⭕/❌ pair beside them, or the panel's two deliberately-kept 「집」 lines at `RELATED_WORK_PANEL.md:39-41` (`WC-013`, `WC-008`). ⚠ **Do not "re-fix" the household register** — read `docs/auto/withdrawn_claims.json` first; the shipped checker is line-based so a reworded assertion escapes.
- ⚠ **Do not claim the architecture as the contribution.** Claim the output object and its measured limits (**WFG-239**). ⚠ **Do not write the 「위험 구역」 / 「잠재적 위험 구역」 zoning framing.**
- ⚠ **Do not compare accuracy with any domestic system or study**, NIFoS, G-DAPS or the Kangwon National University DL model (**NH-056**).
- ⚠ **Do not quote the model-only IoU pair on any judge-facing surface without the seed-removed pair in the same block** (Q36 carries both), and **do not edit `README.md`'s IoU bullet (`:520-522`, `:888-891`) while NH-055 is open**.
- ⚠ **`README.md`'s TL;DR lead is frozen by NH-054 on its ORDERING AND PROPORTION, not on the truth or the length of a parenthetical inside it.** Never rewrite its opening paragraph about the 2025 fire (CHARTER §3.5b). ✅ The 「at all」 repair landed inside that freeze and was measured against it: the result half is **435 characters at both heads**, `:25` loses **nothing**.
- ⚠ **Do not edit `docs/auto/JUDGE_QA.md`, `docs/auto/DEMO_SCRIPT_5MIN.md` or `docs/auto/finals/RELATED_WORK_PANEL.md` without `make printables` at a new stamp and a re-pointed `MANIFEST.json`** (NH-049). **RE-STATED by critic #74 after re-hashing in its own process at `092c907`:** the kit's seven sources hash **7 of 7** (`WFG_printables_20260911T2137Z.pdf`, **60** pages, `manifest_20260911T2137Z.json`), the bundle's nineteen entries hash **19 of 19**, and the bundle names that kit. ⚠ `docs/auto/JUDGE_QA_PENDING.md` is **not** one of the seven, which is why cards may be drafted there without reprinting — and why the `fix-before-next-row` item above is a reprint and must be budgeted as one. This note expires at critic #75 unless that lap re-measures.
- ⚠ **Do not write 「the fire stopped growing at 333 minutes」** (WFG-230). ⚠ **Do not "fix" 「household-level」** in `README.md:3`, `CITATION.cff:5`, `src/wildfireguardian/__init__.py:1` or `paper/manuscript.md:1` (WFG-231). ⚠ **Do not cite `data/processed/spread_v2/audit.json` for any number** (WFG-244).
- ⚠ **Do not rebuild `data/processed/timeline_roles/timeline_roles.json` from a deepened clone** (WFG-217; deadline 10-16).
- **Do not settle 「상한」 / 「upper bound」 in any lap** (NH-053 open). ⚠ **Do not put a margin value on any judge-facing surface** while NH-032, NH-034 and NH-052 are open. **Do not change `mr_uiseong_fa_exceeds_budget`** (NH-031 A).
- **Do not refit anything, and do not regenerate a committed artifact** (CHARTER §3 rule 2). **Do not re-open the AI-disclosure question** (NH-008 closed). **Do not re-propose international portability or real-time weather.** **Do not let a number from a knowledge note reach a card, the README, the manuscript or `docs/NUMBERS.json`** (CHARTER §13).
- ⚠ **Do not trust CHARTER §4's 「`curl` returns 403 here and must not be used」.** It is **false** in this sandbox, confirmed a third time by critic #74 at `092c907`: the call returns a full JSON body and exit 0, and this lap read runs 399 to 409 from it. That is **WFG-268**; do not delete the old sentence, date it.
- ⚠ **Do not paste a shell command into a `docs/auto/BACKLOG.md` row without escaping its pipe** (**WFG-191**, twelve rows already do).
- ⚠⚠ **Do not put CHARTER §4 step 8's two assertions on the same shell line as the push, and never join them with `;`.** Use `&&`. Second instance on **NH-040**.

## Readiness and open decisions

`docs/auto/KCF_READINESS.md` stands at **8 of 11**, unchanged: R1, R2, R4, R5, R6, R7, R8, R9 tick; R3, R11 and R12 do not; R10 was withdrawn 2026-09-04. ⚠⚠ **ZERO lines ticked for the THIRTY-FIRST consecutive critic lap.** Verified the cheap way and with the shallow-clone trap named: `git diff <the commit 24 h back>..092c907 -- docs/auto/KCF_READINESS.md` is **empty**. (`git log --since='26 hours ago'` on that path answers `679c187` and `git show` renders the file as `A` there; that is the clone boundary at depth 50, not a change.) ⚠ **The arithmetic of the hold is unchanged.** CHARTER §14b releases the P1 hygiene block when **R1, R3, R4, R7, R8 and R9** tick. **Five of those six are ticked; R3 is the only one that is not.** R3 is `blocked(NH-046)`, NH-046 came due **2026-09-10**, and R11's WFG-024 is held behind it. **One unanswered email holds the ninth tick and the whole P1 block.** The sprint ends 2026-09-15, **three days out**.

**Open decisions: 27 for the author** (26 DECISION + 1 BLOCKER), **2 undated** (NH-005, NH-014), plus 5 open FYI. Say 「N of 27, and 2 undated」 rather than a bare number. **NH-046, NH-049 and NH-051 are past due.** ⚠ **Severity raised by critic #74 on two entries, with the measurement on the entry: NH-037** (the word margin is now **zero** and the first thing the ceiling refused is a limitation the project found against itself) and **NH-049** (its queue has produced a wrong instruction that is already printed, and the loop then declined to merge the correction by misreading the entry). **NH-057**, **NH-059** and **NH-060** remain the highest-severity open entries. ⚠ **No decision has EVER arrived on the email channel:** `decisions_seen.json` records `"seen": []`, every applied decision came through a Claude Code session on the laptop, and the newest is **NH-031, received 2026-09-06**. Seven days. ⚠ **New measurement, with its limit stated: 29 of the 30 newest report threads also carry the `UNREAD` label.** A message the API sends to the author's own address lands in `INBOX` unread and reading it in the Sent view need not clear that, so this is evidence the inbox copy was not opened and **not** proof the report was unread. It is still the only signal there is. That is **WFG-211**, already `todo`, annotated here and not re-filed.

## Critic's last direction note

**2026-09-12T1125Z, critic #74, reviewed `092c907`. ZERO §3b row moves spent** (both closures left table order already correct). **ZERO new P0 rows**, **ONE new P1 row** (**WFG-275**), **ZERO** new NEEDS_HUMAN entries, **TWO** severity raises with their measurements (NH-037, NH-049), **ONE** `fix-before-next-row` item: merge P-005 into Q39 and reprint.

**The one thing that matters on this page, and it is a ratio.** In the last thirty hours this loop closed fifteen rows. **Nine of them are one defect family** — an outcome-class name that asserts a cause its code condition does not establish, chased from `fa_exceeds_budget` to `no_safe_route` to `no_surviving_vehicle_ingress`, across a figure legend, a sheet, an abstract, a README and a card. **Three are experiments.** The family is real and the repairs are good; WFG-271 says why it keeps recurring, which is that the classifier lives in **seven hand-written copies** and nothing fails when one drifts. But the marginal lap is now buying the ninth instance of one wording bug while **WFG-256**, the rotation null, has been `todo` since 2026-09-11 and is the only thing in the backlog that would let the student answer 「모양 때문입니까?」 with a measurement instead of 「아직 재지 않았습니다」. Three days remain. **The cheapest test of whether this reading is right is the next window's own scorecard:** if 데이터 수집·분석·해석 and 창의성 are still 20 and 19 at critic #76, the loop has spent two more days at the ceiling of one row and the floor of another.

**The lap under review was good and the good part is the part that is hardest to do.** It was handed a row that pre-committed its answer to 「which of the three committed PDFs」, and its `hate` pass broke the row before it started: the tree holds **38** committed dispatch PDFs across **12** run directories, and two of the three stale sheets are in a directory the row never names. Answering the row as written would have missed them. Then it wrote 「there is no `pypdf` here, **so** each PDF is classed by its sibling HTML」, caught its own **so** as a non-sequitur, and built a second route out of the standard library that reads each PDF's own embedded font subset — reproduced independently here, **38 probed, 3 stale, 0 current**, identical to the HTML classing. And it struck the word 「대조군」 off its own zero because that zero came from the same scan as the count it was said to control. That is 데이터 수집·분석·해석 at 20 and it is re-earned rather than inherited.

**The second thing, and it is the product rather than the method.** The lap that measured exactly which printed sheet carries the withdrawn sentence then left the card that tells the student which sheets to print saying the wrong thing, and the reason it gave — that NH-049 is open — does not apply to it. So the booth stack is, at this head, still assembled by an instruction that guarantees one judge sees two sentences for one condition. The correction is written, it is a paragraph, and the reprint is a minute. **It should not survive another window, and if it does, 구현 및 유용성 falls back to 19 at critic #75; that is pre-registered here rather than retrospectively.**

**Scorecard: Track A UP to 97, Track B HOLDS at 96.** 구현 및 유용성 **19 to 20**, firing critic #73's pre-registration on its own words, because WFG-267 (i) names not one stale page but three, in a committed file, with a gate that re-derives the list from the tree in both directions. 제출 자료 **19 HOLDS on both tracks**: critic #73 made 20 conditional on the README repair **and** WFG-272, the README half is paid in full and re-measured here, WFG-272 is still `todo`. ⚠ **The fall condition was tested and did not fire**, which is worth saying because it was close: the window's one document of that family, `docs/dispatch_sheet_staleness.md`, cites sections and registry keys and **no `file:line` at all**, deliberately, and §5.5 says why. 연구 목적 **18**, 설계와 방법론 **20**, 데이터 수집·분석·해석 **20**, 창의성 **19** hold. ⚠ **Pre-registered for critic #75: 제출 자료 reaches 20 on both tracks when the Q39 merge lands with the kit reprinted and the bundle re-pointed AND WFG-272 closes; 창의성 reaches 20 when WFG-256 runs and reports either way; 구현 및 유용성 falls to 19 if the Q39 instruction is still unmerged.**

Verified at `092c907`, the head reviewed and the head this lap ships in. **GitHub `auto-gates` run 409 is `success` at `092c907`** and every run in the window (399 to 409) concluded `success` except 403 and 404, both `cancelled` by a newer push, so CHARTER §4b sets no finding #1. `gates.py --mode full` exits **0** on its FIRST run in this sandbox (2233 passed, 65 skipped, 3 xfailed, pytest 357.8 s; `baseline-verify` is the usual sandbox WARN for two absent `data/raw/` manifests, CHARTER §3d), `--assert-head` and `--assert-reported` both exit 0 and `--assert-reported --base` exits 0 for all ten pushes in the window, **all 39 reports dated 09-11 and 09-12 record `Reviewed by:`**, the printed kit hashes **7 of 7** at 60 pages and the bundle **19 of 19** against its own `source` fields, and PR #31's comment list is **empty**. The clone is SHALLOW at **50** commits and was deliberately NOT deepened; **no ancestry claim is written anywhere in this lap.**
