# Direction — where the project is going, on one screen

*Re-checked 2026-09-11T0810Z by critic #65 at `f7ee58d`. Last rewritten 2026-09-10T1817Z by the research routine (CHARTER §14). The dev routine reads this before it claims a row; the critic re-checks it after every dev lap, at most one reorder per lap, with a reason. Only the most recent critic note is kept, as §14 specifies. **Critic #65 spent ZERO §3b row moves and names its skips.***

## Thesis (two sentences)

WildfireGuardian forecasts where an already-burning Korean wildfire goes next, and turns that forecast into a per-point walk-or-be-rescued decision for rural elderly residents, on public data a stranger can re-derive. Its defensible contribution is not forecast accuracy and not the architecture: it is the **output object and its measured limits**, a time-dependent decision per point with a page saying how wrong it can be.

⚠ Bokade et al. published this project's whole pipeline on **2026-09-09** (10.5281/zenodo.22668357). ⚠ **Critic #65 could NOT reach the Zenodo API from this sandbox** (the request returned no JSON) and therefore does **not** restate critic #64's verification as its own. Nothing here rests on it: the WFG-251 repair of 2026-09-11T0620Z narrowed Q16d to 「**공개된 기록과 초록에는** 성능 수치가 하나도 없습니다」 with 「본문 PDF 는 저희가 열지 못했으므로」 beside it, which is the safe form whether or not a lap can reach the record. **Claim the measurement, never the architecture.**

## Before any row: this lap's `fix-before-next-row` item

**ONE, and it is minutes.** `docs/auto/JUDGE_QA.md:1513` — **Q36 · T0**, said from memory to all five judges and printed on page 25 of the 59-page kit — tells a judge 「같은 면적의 원을 **발화점에** 놓고」. **The ignition point this project records is 19.20 km from that circle's centre, and no circle the measurement drew contains it.** The same card states the rule correctly two paragraphs above (「`t = 0` 씨앗의 무게중심에 놓고」), so the fix is one clause, then `make printables` at a new stamp and a re-pointed `release/kcf-finals-2026/MANIFEST.json` — the 17-second rebuild critic #64 priced and the 2026-09-11T0620Z dev lap paid three times in one lap. That is **WFG-254**'s Q&A half.

⚠ **Critic #64 wrote that findings on hashed kit sources are 「un-preemptable」 and that reading is wrong.** NH-049 is about whether the **critic** may write the bank; it says nothing about the **dev lap** that executes a `fix-before-next-row` item, and the last dev lap closed three such rows plus the rebuild inside one lap. CHARTER §14b's test is the size of the fix, not the file's hash status.

⚠ **Do NOT `git fetch --unshallow` in a critic or dev sandbox, and CHARTER §4 does not ask you to.** §4 forbids writing an ancestry or reachability claim from a shallow clone; it does not require deepening. Unshallowing drags in eleven side branches, lengthens `git`'s abbreviation and turns `tests/test_timeline_roles.py`'s history check RED on a tree that is fine (critic #60 and #63 both paid for it). **Critic #65 did not deepen: `gates.py --mode full` went green on its FIRST run**, 2097 passed, 65 skipped, 3 xfailed. Say what that costs: in a shallow clone that history check **SKIPS** rather than runs (`tests/test_timeline_roles.py:234`), so a green critic gate does not certify it; GitHub at `fetch-depth: 0` does, and run **364** is `success` at this exact head. Recorded on **WFG-217**. This note names those file lines and that measurement, and expires at critic #66 unless that lap re-runs the case.

## Next three rows, and why each is next

1. **WFG-254 (P0, KCF), whole.** Seven surfaces say the null circle sits at the ignition point. It does not: `data/processed/routing_demo_canonical.npz` carries `ign_xy`, `scripts/measure_disc_null.py` never reads it, and the disc's centre is the centroid of a `t = 0` detection seed that is **226 disconnected components** (largest **3** cells) across a **23.5 × 44.5 km** box. The gap is **38.40 cells = 19.20 km**, against a largest-disc radius of <!-- collision-ok: 18.162 — dn_yeongdeok_t720min_disc_radius_cells, the LARGEST slice's radius, which is exactly what this sentence claims. The other registered radii (8.913, 14.881, 17.355, 17.681) are different slices, not stale values. -->**18.162** cells. ⚠⚠ The worst instance is not prose: `paper/make_figures.py:873` renders the heading 「**distance moved from the ignition**」 over two bars measured from that centroid, and it is baked into `paper/figures/F10_disc_null.png`, committed at `938dff7` **in this window**. The row also disambiguates 「모양과 범위」 / 「shape and extent」, which is impossible on the area reading (both masks hold **952** cells by construction) and true on the reach reading.

2. ⚠⚠ **WFG-129 (P0, science) IS THE NEXT ROW AFTER WFG-254, AND THIS IS THE SIXTH PAGE TO SAY SO.** Fully specified in `paper/GAPS.md` G7, committed inputs only, no refit, no re-acquisition, no author decision; G7's own words are 「That is minutes of work」. It is the cheapest test of the exact number the booth leads with. ⚠ **Critic #64's pre-registration FIRED and the honest reading is split**: a dev lap did run in the window and WFG-129 is still `todo`, but that lap took critic #64's own item 1 and closed all three of its rows, so the cause is this page's ordering rather than the lap's choice. **Pre-registered for critic #66, with teeth this time: if TWO dev laps have run and WFG-129 is still `todo`, that is finding #1 and critic #66 spends its one §3b row move putting WFG-129 at table position 1.** Naming it here passes over exactly one fully agent-doable P0 `todo` row, **WFG-117**, and over **WFG-007**, whose remaining half its own cell marks 「print/laptop: human」. That is the whole skip list.

3. **WFG-255 (P0, science) third.** The footprint this project grades itself against is **55 components** over a **24.5 × 45.0 km** box at the 333-minute slice (largest 656, second 135), and no file in the repository says so while `docs/disc_null.md:153` tells a judge the model puts cells along 「the **arms** the fire actually ran down」. The row measures the geometry and states it beside the IoU; it asserts no conclusion about how many fires are in the mask, because this repository cannot presently tell a multi-fire complex from a FIRMS-fragmented perimeter. Then **WFG-236**, then **WFG-244**, **WFG-245** and **WFG-237**.

⚠ **WFG-234 remains the strongest science row after WFG-129**: a persistence null scored against the headline truth, where a bad result is a **named, published failure mode of the whole model class**. Pre-register it in the claim commit. **Take it before other P1 science work.**

⚠ **The board at the head I reviewed (`f7ee58d`, before this lap's rows): 247 rows, 15 P0 `todo`. After this lap's two rows: 249 rows, 17 P0 `todo`.** ⚠⚠ **Do not publish a board count without the rule that produced it, and do not spend a lap reconciling one.** Critic #65 used critic #64's rule unchanged: split each row on pipes not preceded by a backslash, read priority at cell 2 and status at cell 5. It answers **110 P1 `todo`** at `f7ee58d` and 110 after this lap (both new rows are P0). The cause of the three-lap disagreement is **WFG-191**: 11 rows carry unescaped pipes and render their priority and status in the wrong columns. The gate WFG-191 asks for is the whole answer; spend no further lap measuring this.

## What not to do

- **Do not start a P1 row while a P0 row is `todo`**, and do not file one above the P0 block.
- ⚠⚠ **Do not unshallow the clone.** See the note above.
- ⚠ **Do not "fix" WFG-254 by changing any measured value.** Every number in `data/processed/disc_null_yeongdeok.json` is right; only the words naming what they are measured **from** are wrong. The centroid finding of `docs/disc_null.md` §4 survives the relabelling unchanged and must not be weakened.
- ⚠ **Do not regenerate `paper/figures/F10_disc_null.png`** (CHARTER §3 rule 2). The corrected figure is a **new filename** and the old one keeps a dated note saying what its heading got wrong.
- ⚠ **Do not touch `docs/submission_reconciliation.md:63`.** Its 발화점 is about how the canonical array was seeded and is correct; that is precisely why the Q36 shorthand misleads.
- ⚠ **Do not write 「여러 개의 산불」, or any count of fires, from WFG-255's measurement alone.** The row measures components, not fires.
- ⚠ **Do not "fix" WFG-249 by weakening Q20a's privacy answer** (closed 2026-09-11; the scope clause is the repair, not a softening).
- ⚠ **Do not swap 가구 for 지점 in the demo closing, and do not swap either count.** The denominator is `l0i_failing_denominator_h240` **24**, not the **124**-building population; `docs/NUMBERS.json`'s own caveat says the two agreeing with `l0i_best_pair_saved` is a coincidence, not a derivation.
- ⚠ **Do not open, cite or characterise the Bokade et al. body PDF**, and do not widen Q16d beyond 「공개된 기록과 초록」.
- ⚠ **Do not touch** Q16's 「가구 단위 폐쇄 시각」, Q16a's lines, the ⭕/❌ pair beside them, or the panel's two deliberately-kept 「집」 lines at `RELATED_WORK_PANEL.md:39-41`. `WC-013` and `WC-008` preserve all of these by name.
- ⚠ **Do not "re-fix" the household register.** Read `docs/auto/withdrawn_claims.json` before editing any 가구 line; the shipped checker is line-based so a reworded assertion escapes (`docs/withdrawn_claims.md` §4).
- ⚠ **Do not claim the architecture as the contribution.** Claim the output object and its measured limits. **WFG-239** writes the sentence.
- ⚠ **Do not write the 「위험 구역」 / 「잠재적 위험 구역」 zoning framing.** Recorded UNVERIFIED.
- ⚠ **Do not compare accuracy with any domestic system or study**, NIFoS, G-DAPS or the Kangwon National University DL model (**NH-056**).
- ⚠ **Do not quote the model-only IoU pair on any judge-facing surface without the seed-removed pair in the same block** (Q36 carries both).
- ⚠ **Do not edit `README.md`'s IoU bullet (`:520-522`, `:888-891`) while NH-055 is open**, and do not delete the Rothermel comparison in either direction.
- ⚠ **Do not edit `README.md`'s TL;DR lead in either direction while NH-054 is open**, and never rewrite its opening paragraph about the 2025 fire (CHARTER §3.5b).
- ⚠ **Do not edit `docs/auto/JUDGE_QA.md`, `docs/auto/DEMO_SCRIPT_5MIN.md` or `docs/auto/finals/RELATED_WORK_PANEL.md` without `make printables` at a new stamp and a re-pointed `MANIFEST.json`** (NH-049). Re-measured by critic #65 at this head in its own process: the kit's seven sources hash **7 of 7** against the tree and the bundle **19 of 19** against theirs, and the bundle names the newest kit, `WFG_printables_20260911T0706Z.pdf`, 59 pages. This note expires at critic #66 unless that lap re-measures.
- ⚠ **Do not write 「the fire stopped growing at 333 minutes」.** No observation exists between 0 and 333 minutes (WFG-230).
- ⚠ **Do not "fix" 「household-level」** in `README.md:3`, `CITATION.cff:5`, `src/wildfireguardian/__init__.py:1` or `paper/manuscript.md:1`; they name the **application** (WFG-231).
- ⚠ **Do not cite `data/processed/spread_v2/audit.json` for any number** (WFG-244).
- ⚠ **Do not rebuild `data/processed/timeline_roles/timeline_roles.json` from a deepened clone, and not from any clone holding the side branches** (WFG-217; deadline 10-16). Critic #65 did **not** re-measure the commit half of its disclosed gap; a shallow clone cannot count it and CHARTER §4 forbids writing it from one. The refreshing lap works from a **single-branch full clone**.
- ⚠ **Do not weaken 「실제 확산면으로 만든 출동 지시서는 아직 없습니다」** on any surface (**NH-057**, **WFG-242**).
- **Do not settle 「상한」 / 「upper bound」 in any lap** (NH-053 open). Describe the mechanism.
- ⚠ **Do not put a margin value on any judge-facing surface** while NH-032, NH-034 and NH-052 are open.
- ⚠ **Do not add a word to `paper/manuscript.md` without trimming one** (NH-037, open).
- **Do not change `mr_uiseong_fa_exceeds_budget`** (NH-031 A; registry half is WFG-122).
- **Do not refit anything, and do not regenerate a committed artifact** (CHARTER §3 rule 2).
- **Do not re-open the AI-disclosure question** (NH-008 closed; CHARTER §9).
- **Do not re-propose international portability or real-time weather** (`docs/HANDOFF_ROUND3.md` §13-§14).
- **Do not let a number from a knowledge note reach a card, the README, the manuscript or `docs/NUMBERS.json`** (CHARTER §13).

## Readiness and open decisions

`docs/auto/KCF_READINESS.md` stands at **8 of 11**, re-counted by critic #65 from the checklist table at `f7ee58d`: R1, R2, R4, R5, R6, R7, R8, R9 tick; R3, R11 and R12 do not; R10 was withdrawn 2026-09-04. ⚠⚠ **ZERO lines ticked for the TWENTY-SECOND consecutive critic lap**, and this window is not excused: it held two dev laps that closed five P0 rows and rebuilt the kit twice. R12 is the author's (NH-014); R3 is `blocked(NH-046)`; R11's WFG-024 is held by §14b until R3 ticks. **There is still no path from any amount of loop work to a ninth tick.**

**Open decisions: 24 for the author** (23 DECISION + 1 BLOCKER), **2 undated** (NH-005, NH-014), plus 5 open FYI. Say 「N of 24, and 2 undated」 rather than a bare number. **NH-046 and NH-049 are past due; NH-051 came due 2026-09-11.** **NH-057** is the highest-severity open entry. ⚠ **The channel has now produced nothing for FIVE DAYS:** `docs/auto/decisions_seen.json` records `"seen": []`, the newest applied decision is NH-031 of **2026-09-06**, and critic #65 confirmed at the Gmail connector that all 25 threads matching the report subject in the last 14 days carry exactly one message each and every one of them is the loop's own send. The second channel named in CHARTER §6 is a merged pull request: **PR #31 returned an empty comment list at the GitHub MCP in this lap.** That is **WFG-211**, already `todo`, confirmed here and not re-filed. The sprint ends 2026-09-15, **four days out**.

## Critic's last direction note

**2026-09-11T0810Z, critic #65 at `f7ee58d`. ZERO §3b row moves.** TWO new P0 rows at positions 1 and 2 (**WFG-254**, **WFG-255**); ONE `fix-before-next-row` item, the Q36 clause, which is minutes; **NO new NEEDS_HUMAN entry, deliberately**, for the third consecutive lap — both findings are agent-doable and a twenty-fifth question while twenty-four go unanswered is noise.

**The one thing that matters on this page.** For six consecutive laps the project has been arguing about what its headline number means — what it should be compared with, whether the comparison is directional skill, which pair of it may be quoted — and this lap found that the sentence five judges hear about that comparison **names the wrong place**, by 19.20 km, against a coordinate the same array has carried the whole time. The pattern is the same one every direction page has written: **the project reasons about the measurement faster than it checks the measurement.** WFG-254 fixes the sentence. **WFG-129 is the row that stops the pattern**, and it has waited six pages.

**Scorecard: both tracks FALL. Track B 97 to 95, Track A 97 to 96.** 데이터 수집·분석·해석 20 to 19 (WFG-255: the graded object's geometry is unmeasured and unstated while two published sentences interpret it) and 제출 자료 19 to 18 on both tracks (WFG-254: a false locative on a T0 card, on the paper in the box, in the manuscript and rendered into a committed figure). Critic #64's pre-registered 제출 자료 conditions are not what moved it; this is a new defect on the same criterion.

Verified at `f7ee58d`. **GitHub `auto-gates` run 364 is `success` at this exact head**, and no run in the 24-hour window concluded `failure` (one `cancelled`, 352, superseded by the next push), so CHARTER §4b sets no finding #1 for the fourteenth consecutive lap. `gates.py --mode full` exits **0** on its FIRST run in this sandbox (2097 passed, 65 skipped, 3 xfailed), `--assert-head` and `--assert-reported --base 6d4a60b` both exit 0 over 70 substantive paths, and every dev report in the window records `Reviewed by:`. The clone is SHALLOW at **50** commits and was deliberately NOT deepened; no ancestry claim is written anywhere in this lap.
