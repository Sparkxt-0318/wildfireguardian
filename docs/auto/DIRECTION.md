# Direction — where the project is going, on one screen

*Re-checked 2026-09-11T0300Z by critic #63 at `0796336`. Last rewritten 2026-09-10T1817Z by the research routine (CHARTER §14). The dev routine reads this before it claims a row; the critic re-checks it after every dev lap, at most one reorder per lap, with a reason. Only the most recent critic note is kept, as §14 specifies. **Critic #63 spent ZERO reorders**, the seventh consecutive critic lap to spend none.*

## Thesis (two sentences)

WildfireGuardian forecasts where an already-burning Korean wildfire goes next, and turns that forecast into a per-point walk-or-be-rescued decision for rural elderly residents, on public data a stranger can re-derive. Its defensible contribution is not forecast accuracy, and **as of 2026-09-09 it is not the architecture either** — it is the **output object and its measured limits**: a time-dependent decision per point, with a page saying how wrong it can be.

⚠ Bokade et al. published this project's whole pipeline — ML spread forecast feeding dynamic evacuation routing away from *actively predicted* fire — on **2026-09-09** (10.5281/zenodo.22668357). They evaluate **none** of it: critic #63 re-read the record at the Zenodo API and a digit regex over its abstract returns the **empty list**. This project ships `docs/disc_null.md`, `docs/oracle_gap.md` and a withdrawn-claims registry. **Claim the measurement, never the architecture.**

⭕ **The bill critic #62 measured on this thesis is PAID, and critic #63 measured that rather than reading it.** All six `WC-013` spellings were re-swept over every tracked `.md` and `.html` on text flattened of newlines, so the sweep sees what the shipped line-based checker cannot: 33 hits in total, **ZERO of them outside the record class**. The printed kit hashes **7 of 7** sources and the release bundle **19 of 19**. The register is off the paper, and it was the reason 제출 자료 fell for three consecutive laps.

## Before any row: this lap's `fix-before-next-row` items

**ZERO, and critic #63 measured it rather than inheriting it.** GitHub `auto-gates` run **356** is `success` at exactly this head. Both of this lap's findings (**WFG-247**, **WFG-248**) sit on hashed printables sources, so each costs `make printables` plus a re-pointed manifest and neither is minutes. Take the top row directly.

⚠ `gates.py --mode full` goes **RED in a critic's own sandbox** and this is not a defect in the pushed head. CHARTER §4 mandates `--unshallow`, which drags in eleven side branches and pushes `git`'s abbreviation from seven characters to eight, so `test_timeline_roles.py`'s history check refuses the committed anchors. Cure, on the identical tree: delete the extra remote-tracking refs, `git reflog expire --expire=now --all`, `git gc --prune=now`. Objects fall 16,420 to 13,011, the abbreviation returns to seven, `--check` prints OK. **Two critic laps in a row have now paid for rediscovering this** (WFG-217).

## Next three rows, and why each is next

Table order at `0796336`. **This page's naming power is spent on ONE thing: it names WFG-247 and WFG-248 as a BUNDLE for one lap, and both are already at position 1 by CHARTER §14b, so no row moved.**

1. **ONE lap takes WFG-247 + WFG-248 and pays `make printables` ONCE.** Both edit a hashed kit source (`DEMO_SCRIPT_5MIN.md`, `JUDGE_QA.md`), each alone costs a rebuild at a new stamp plus a re-pointed `release/kcf-finals-2026/MANIFEST.json` (**NH-049**), and both are single-clause repairs. **WFG-247 is the larger:** the demo's closing 56 seconds — the last thing each of the five judges hears — says 「20가구」 and 「보행망 노드 2,218곳」 in one breath, while `docs/NUMBERS.json` records those counts on **OSM 건물 124동**. Three populations, one sentence, and the reconciliation that exists sits 190 lines away at `JUDGE_QA.md:947`. **WFG-248** is one clause of Q16d calling another team's *fallback* planner their planner, on the card that tells the student to open that team's DOI in front of the judge.

2. **WFG-236 (P0, KCF) second — unchanged from critic #60, #61 and #62.** `docs/disc_null.md:20-21` and `docs/oracle_gap.md:180-181` both assert in bold that nothing said what **0.394** should be compared with, while `README.md:520-522` and `:888-891` have compared it to Rothermel's **~0.09** for months. The row repairs the two `docs/` sentences; whether the README's framing moves is **NH-055**, the author's. It pays no rebuild.

3. **WFG-244 (P0, KCF) third, and it is minutes.** The block at `e8b6e01` asserts 「Neither count has a second, independent derivation」 while `data/processed/spread_v2/audit.json :: fires[5]` holds `detections_csv` 17, `detections_in_grid` 17, `total_positives` 8. Do not cite that file; name what it contains.

⚠ **WFG-245, WFG-237 and WFG-234 follow.** **WFG-234 remains the strongest science row**: a persistence null scored against the headline truth, where a bad result is a **named, published failure mode of the whole model class** — Bokade et al.'s abstract says it outright ("neither a recurrent nor a non-recurrent network learns to advect fire past the persistence baseline"). Pre-register it in the claim commit. **Take it before other P1 science work.**

⚠ **The board at this head: 242 rows, 15 P0 `todo`, 112 P1 `todo`.** ⚠⚠ **Do not publish a board count without the rule that produced it, and do not spend a lap reconciling one.** Critic #62's 「238 / 98 / 18」 reconciles exactly; its 「106 P1 todo」 does not, and the cause is **WFG-191**: **11 of 242 rows carry unescaped pipes** and their priority and status render in the wrong columns. Two careful laps cannot agree on a number whose file does not have the shape either rule assumes. The gate WFG-191 asks for is the whole answer.

## What not to do

- **Do not start a P1 row while a P0 row is `todo`**, and do not file one above the P0 block.
- ⚠⚠ **Do not "re-fix" the household register.** It is measured clean at this head across all six spellings on a flattened sweep. **Do not read that as a corrected register either** — the scanner is line-based and a reworded assertion still escapes (`docs/withdrawn_claims.md` §4). This note names the measurement above and **expires at critic #64 unless that lap re-sweeps**.
- ⚠ **Do not touch** Q20a's definition of 「가구」 as one OSM walk-graph node (`:968-975`), Q16's 「가구 단위 폐쇄 시각」 (`:600`), Q16a's `:659-661`, `:680-681`'s ⭕/❌ pair, or the panel's two deliberately-kept 「집」 lines at `RELATED_WORK_PANEL.md:39-41`. `WC-013` and `WC-008` preserve all of these by name.
- ⚠ **Do not "fix" WFG-247 by swapping 가구 for 지점.** Those counts really are buildings (`l0i_*`, `sample: OSM 건물 124동`). Name the population where the number is said.
- ⚠ **Do not weaken any other fact on Q16d while fixing WFG-248.** Critic #63 re-verified every one at the Zenodo API: concept DOI, version relation, title, 2026-09-09, Preprint, five authors in order, one file, and zero metrics in the abstract.
- ⚠ **Do not claim the architecture as the contribution.** Claim the output object and its measured limits. **WFG-239** writes the sentence.
- ⚠ **Do not write the 「위험 구역」 / 「잠재적 위험 구역」 zoning framing.** Recorded UNVERIFIED.
- ⚠ **Do not compare accuracy with any domestic system or study** — NIFoS, G-DAPS, the Kangwon National University DL model (**NH-056**).
- ⚠ **Do not quote `2.5360` or the pair `0.3941 / 0.1554` on any judge-facing surface without `2.2044` and `0.2577 / 0.1169` in the same block.**
- ⚠ **Do not edit `README.md`'s IoU bullet (`:520-522`, `:888-891`) while NH-055 is open**, and do not delete the `~0.09` Rothermel comparison in either direction.
- ⚠ **Do not edit `README.md`'s TL;DR lead in either direction while NH-054 is open.**
- ⚠ **Do not edit `docs/auto/JUDGE_QA.md` or `docs/auto/finals/RELATED_WORK_PANEL.md` without `make printables` at a new stamp and a re-pointed `MANIFEST.json`** (NH-049). Re-measured by critic #63 at `0796336`: the kit's seven sources hash **7 of 7** and the bundle **19 of 19**. This note expires at critic #64 unless that lap re-measures and re-states it.
- ⚠ **Do not write 「the fire stopped growing at 333 minutes」.** No observation exists between 0 and 333 minutes (WFG-230).
- ⚠ **Do not "fix" 「household-level」** in `README.md:3`, `CITATION.cff:5`, `src/wildfireguardian/__init__.py:1` or `paper/manuscript.md:1` — they name the **application** (WFG-231).
- ⚠ **Do not cite `data/processed/spread_v2/audit.json` for any number** (WFG-244). Naming what it contains in order to warn a lap off it is not citing it.
- ⚠ **Do not rebuild `data/processed/timeline_roles/timeline_roles.json` from a deepened clone, and do not rebuild it from any clone holding the side branches** (WFG-217; deadline 10-16). Its drift is now **83 commits and two days**, disclosed honestly at `TIMELINE_ROLES.md:28` and invisible to its own gate.
- ⚠ **Do not weaken 「실제 확산면으로 만든 출동 지시서는 아직 없습니다」** on any surface (**NH-057**, **WFG-242**).
- **Do not settle 「상한」 / 「upper bound」 in any lap** (NH-053 open). Describe the mechanism.
- ⚠ **Do not put a margin value (9, 27, 5, 19, 86) on any judge-facing surface** while NH-032, NH-034 and NH-052 are open.
- ⚠ **Do not add a word to `paper/manuscript.md` without trimming one** (NH-037, open).
- **Do not change `mr_uiseong_fa_exceeds_budget`** (NH-031 A; registry half is WFG-122).
- **Do not refit anything, and do not regenerate a committed artifact** (CHARTER §3 rule 2).
- **Do not re-open the AI-disclosure question** (NH-008 closed; CHARTER §9).
- **Do not re-propose international portability or real-time weather** (`docs/HANDOFF_ROUND3.md` §13-§14).
- **Do not let a number from a knowledge note reach a card, the README, the manuscript or `docs/NUMBERS.json`** (CHARTER §13).

## Readiness and open decisions

`docs/auto/KCF_READINESS.md` stands at **8 of 11**, re-counted by critic #63 at `0796336`: R1, R2, R4, R5, R6, R7, R8, R9 tick; R3, R11 and R12 do not; R10 was withdrawn 2026-09-04. ⚠⚠ **ZERO lines ticked for the TWENTIETH consecutive critic lap**, and the window was not idle — it rebuilt the kit, added a card and cleared the register, all of which strengthened lines that already tick. R12 is the author's (NH-014); R3 is `blocked(NH-046)`; R11's WFG-024 is held by §14b until R3 ticks, so **112 P1 rows wait on one reply**.

**Open decisions: 24 for the author** (23 DECISION + 1 BLOCKER), **2 undated** (NH-005, NH-014), plus 5 open FYI. Say 「N of 24, and 2 undated」 rather than a bare number. **NH-046 is now past due on both clocks; NH-049 and NH-051 come due today.** **NH-057** is the highest-severity open entry. ⚠ **The channel has produced nothing for five days:** `decisions_seen.json` records `"seen": []`, the newest applied decision is NH-031 of **2026-09-06**, and PR #31 has zero comments. The sprint ends 2026-09-15, **four days out**.

## Critic's last direction note

**2026-09-11T0300Z, critic #63 at `0796336`. ZERO §3b reorders, the seventh consecutive critic lap to spend none, and ZERO `fix-before-next-row` items, measured.** The one direction change is that this page names **WFG-247 + WFG-248 as one lap and one rebuild**, which costs no reorder because CHARTER §14b already puts both at position 1. TWO new rows (WFG-247 P0 KCF, WFG-248 P0 KCF), TWO existing rows re-measured in place (WFG-191, WFG-217) and TWO NEEDS_HUMAN entries appended (NH-046, NH-049). **NO new NEEDS_HUMAN entry, deliberately:** both findings are agent-doable, and a twenty-fifth open question while twenty-four go unanswered would be noise.

**This is a scorecard RISE, the first on 제출 자료 in four laps: Track B 94 to 96, Track A 95 to 97, both on 제출 자료 17 to 19.** It is paid on exactly the defect that took the row down. The withdrawn register is **measured** off the printed kit — not read as fixed, but re-swept in this lap's own process over every tracked `.md` and `.html` on flattened text, six spellings, 33 hits, zero outside the record class — and the one publication that takes this project's architecture away from it is now cited on a card whose every fact I re-verified at the Zenodo API rather than at the report that wrote it. **19 and not 20** for the two rows filed above, both on this same criterion and both on the paper in the box.

Verified at `0796336`. **GitHub `auto-gates` run 356 is `success` at this exact head**, so CHARTER §4b sets no finding #1 for the twelfth consecutive lap. `gates.py --mode full` exits **0** in this sandbox once the clone is pruned (**2073 passed**), and the RED first run is WFG-217's abbreviation defect, not the head's. `--assert-reported` exits 0 across the window; all three dev reports record `Reviewed by:`. The clone opened SHALLOW at 50 commits and was `--unshallow`ed to **745** before anything was counted.
