# 철회된 주장 레지스트리 — 방법, 결과, 한계

*작성: 자율 루프 dev 랩 20260904T2119Z (WFG-062). 방법을 제안한 것은 학생이 아니라
루프입니다 (CHARTER §9). 이 문서는 게이트 하나를 설명합니다; 게이트가 지키는 주장들의
내용은 `docs/detection_floor.md` 와 `docs/auto/JUDGE_QA.md` Q10 에 있습니다.*

## 0. 한 문장

이 저장소가 **철회한 주장**들을 파일 하나(`docs/auto/withdrawn_claims.json`)에 모으고,
추적되는 **모든** 문서를 그 파일에 대고 검사합니다. 어제까지는 "이 주장이 없어야 할
파일"을 손으로 열거하는 방식이었고, 그 목록은 (이 커밋 기준) 988개 중 **11개**를
덮고 있었습니다.

## 1. 왜 필요했는가 — 실제로 새어나간 것은 철자가 아니라 파일이었습니다

이 루프가 대가를 치른 세 번의 누락은 모두 **아무도 목록에 넣지 않은 파일**입니다.

| 언제 | 어디 | 무엇 |
|---|---|---|
| WFG-063 | `docs/SESSION19_REPORT.md` | 「아무도 목록에 넣지 않은 세션 보고서」가 순위표와 일차 주장을 주석 없이 그대로 들고 있었습니다 |
| WFG-070 | `docs/auto/research/RESEARCH_BRIEF_2026-09-03.md`, `sweeps_2026-09-03/R3_science_gaps.md` | 같은 주장이 **영어로** 살아 있었고, 한국어 목록은 영어를 읽지 않았습니다 |
| WFG-070 (같은 랩) | `sweeps_2026-09-03/R7_rubric_gap.md` | 찾으러 간 행 자신도 모르던 세 번째 사례 |

`tests/test_detection_ordering_is_not_claimed.py` 는 주장군 4개와 손으로 쓴 가드 목록
5개를 들고 있습니다. 그 합집합이 **11개 파일**입니다. 이 커밋에서 추적되는
`.md` · `.html` 는 **988개**입니다.

## 2. 방법

- **레지스트리** `docs/auto/withdrawn_claims.json`: 주장 하나당 `id`, 무엇을 주장했는지,
  **무엇이 그것을 철회했는지**(결정과 아티팩트), **대신 무엇을 말해야 하는지**,
  그리고 금지된 **철자들**(정규식 + 프래그마 토큰 + 이유).
- **기본 범위는 전부**: 추적되는 모든 `.md` · `.html`. 예외는 한 종류뿐이고,
  레지스트리 안에 **한 줄씩 이유와 함께** 적혀 있습니다 — 루프 자신의 기록물
  (`docs/auto/reports/`, MEMO, NEEDS_HUMAN, BACKLOG, SCORECARD, CRITIC_LATEST,
  KCF_READINESS, DIRECTION, CHARTER, ROUTINE_PROMPTS, `docs/auto/archive/`,
  생성된 `dashboard.html`). 이 파일들의 일은 철회된 주장을 **철회되었다고 기록하기
  위해 인용하는 것**이고, 심사위원은 이 파일들을 읽지 않습니다.
- **검사기** `scripts/check_withdrawn_claims.py`, `make check-withdrawn-claims`,
  그리고 `make verify` 안에 들어가 있으므로 모든 랩과 모든 푸시(GitHub Actions 포함)에서
  돕니다.
- **면제 방법은 하나**: 그 줄 또는 바로 윗줄의 `<!-- forbidden-ok: <토큰> -->`.
  `scripts/check_forbidden.py` 와 **같은** 프래그마이고 토큰은 주장별로 다릅니다.
  자기만의 뒷문을 가진 게이트는 사람들이 우회하는 법을 배우는 게이트입니다.

## 3. 결과 (재현 가능)

```
$ python scripts/check_withdrawn_claims.py --coverage
claims             : 4
spellings          : 22
tracked in scope   : 1038 files ('.md', '.html')
gated              : 925
record class       : 113 files, 12 declared paths
$ python scripts/check_withdrawn_claims.py
=== check_withdrawn_claims: PASSED === 4 claims over 925 gated files
```

[기록 · 2026-09-04 · 오늘의 값이 아닙니다] 이 블록이 처음 인쇄한 값은 `claims 3` ·
`spellings 15` · `tracked 988` · `gated 915` · `record class 73` 이었습니다. WC-004 를
등록한 2026-09-06 WFG-133 랩이 넷 다 움직였고, **그 랩은 이 절을 낡은 채로 두었다가
독립 검토자에게 CHARTER §3 규칙 3 위반으로 지적받았습니다** — 같은 문서 §5b 가 이미
같은 부류의 결함을 고친 전례를 적어 두었는데도 그랬습니다. 988 → 1038 은 이 랩이 아니라
그 사이 랩들이 문서를 더한 결과입니다.

**11 → 915.** 다만 이 915 는 **읽은 파일 수이지 잡은 주장 수가 아니며**, 구성도
정직하게 말해야 합니다: 생성물 `outputs/**` **652개**, 벤더링된
`.claude/skills/**` **105개**, 사람이 산문을 쓰는 **프로젝트 문서 158개**. 심사위원
앞에서 인용해야 할 짝은 **「사람이 쓰는 문서 11 → 158, 검사 대상 전체는 위 `--coverage` 가 인쇄하는 수」**입니다 — 이 수는 문서가 늘 때마다 움직이므로(2026-09-04 의 915 는 이틀 만에 925 가 되었습니다) 외우지 말고 그 자리에서 명령을 돌리십시오.
(이 구분은 이 랩의 독립 검토자가 지적했고 그 지적대로 적었습니다.) 숫자는
`tests/test_withdrawn_claims_registry.py::test_the_coverage_this_row_bought_is_recorded_and_re_derived`
가 다시 계산합니다.

레지스트리를 처음 전체 트리에 돌렸을 때 **가드 목록 밖에서 11건**이 나왔고, 모두
두 파일에 있었습니다: `docs/SESSION19_REPORT.md` (10건)과
`docs/finals_screen_v2.md` (1건). 둘 다 이미 산문으로는 철회 주석을 달고 있었으나
기계가 읽을 수 있는 면제는 없었습니다. 산문은 한 글자도 고치지 않고 그 줄들에
프래그마를 붙였습니다.

## 4. 이 게이트가 **하지 않는** 것 — 부스에서 근거로 들지 마십시오

1. **의미를 읽지 않습니다. 철자를 읽습니다.** 복사-붙여넣기 래칫입니다.
2. **재작성(rewording)에 대한 민감도는 조금도 개선되지 않았습니다.** 패턴이 같은
   패턴이기 때문이며, 그것이 의도입니다 (`test_the_registry_has_not_drifted_…`).
   외부에서 측정된 숫자는 그대로입니다 — **패턴 작성자가 쓰지 않은** 세 세트:
   크리틱 #9 의 스무 문장에 `primacy_violations` **0/20**, `priority_violations`
   **2/20** (BACKLOG WFG-062, 크리틱 #9 F47); 20260904T0855Z 랩 검토자의 스무 문장에
   `primacy_violations` **1/20**(19건 통과), 화면의 부정 규칙 **8/20**
   (`docs/auto/reports/2026-09-04T0855Z-dev.md`); WFG-070 검토자의 세트에 영어 구조
   규칙 **9/18**. **이 행은 그 숫자들을 바꾸지 않았고, 새 숫자를 주장하지도
   않습니다.**
3. **구조 규칙 두 개는 흡수하지 않았습니다.** `priority_violations` 와
   `english_ordering_violations` 는 줄바꿈된 마크다운 블록에서 문장을 언어별로
   재구성하므로 레지스트리 한 줄로 표현되지 않습니다. 흡수는 다음 행입니다.
4. **범위를 넓히면 거짓 양성이 생깁니다. 2026-09-04 기준 915개 중 1건, WC-004 를 더한 2026-09-06 기준 925개 중 2건.**
   `docs/finals_screen_v2.md:75` 의 「사람 신고를 일차로 **끌어올리지 않습니다**」는 <!-- forbidden-ok: 신고 일차 -->
   정직한 부정문인데 `신고 일차` 철자에 걸립니다. 구조 규칙은 부정을 읽고 통과시키지만
   철자 규칙은 읽지 못합니다. 프래그마로 면제했고, 그것이 우회가 아니라 설계된
   방법입니다.
5. **커밋된 아티팩트가 걸리면 이 루프는 고칠 수 없습니다.** `outputs/dispatch*/` 의
   652개 생성 파일이 범위 안에 있고 CHARTER §3 규칙 2 는 커밋된 아티팩트의 수정을
   금지합니다. 그런 일이 생기면 그것은 NEEDS_HUMAN 항목이지 편집이 아닙니다.
   (오늘은 전부 깨끗합니다.)
6. **기록 클래스가 조용히 자라면 이 게이트는 조용히 작아집니다.** 그래서 **선언된
   경로 12개**와 그 중 **단일 파일 10개**가 테스트에 못 박혀 있고, 늘리려면 같은
   커밋에서 이 문서를 고쳐야 합니다. `docs/auto/reports/` 와 `docs/auto/archive/` 는
   설계상 매 랩 자라므로 **파일 수는 고정하지 않습니다** — 이 테스트의 첫 판은 73 을
   못 박았고, 그랬다면 그것을 실어 나르는 푸시에서 바로 빨간불이 됐을 것입니다.

7. **줄바꿈을 건너뛴 철자는 검사기가 보지 못합니다 — 2026-09-10 에 측정했습니다.**
   `scripts/check_withdrawn_claims.py` 는 **한 줄씩** 읽으므로, 등록된 철자가 원본에서 줄바꿈을
   가로지르면 아예 걸리지 않습니다. WC-013 을 등록한 랩이 자기 문서에서 그 사례를 만났습니다:
   `docs/creativity_card.md` §8 의 「a per-household / walk-or-be-rescued verdict」 는 두 줄에
   걸쳐 있고 게이트는 그 줄을 보고하지 않았습니다. 그 줄의 프래그마는 게이트가 요구해서가 아니라
   **사람이 넣은 것**입니다. WC-012 도 같은 이유로 한 문장을 두 철자로 쪼개 등록했습니다
   (`tests/test_withdrawn_claims_registry.py` 의 주석). §4 의 4번과 함께 읽으십시오: 이 레지스트리는
   **베낀 철자의 래칫**이고, 다시 쓴 문장도 줄이 바뀐 문장도 빠져나갑니다.

## 5. 지금 등록된 주장

| id | 주장 | 무엇이 철회했나 | 철자 |
|---|---|---|---|
| WC-001 | 위성 트리거가 사람의 신고보다 늦었다(또는 빨랐다) | WFG-053 / NH-019 — 기준 시각은 매니페스트의 기록된 발생일시이고 이 저장소에 신고접수시각은 없습니다 | 7 | <!-- forbidden-ok: 신고보다 -->
| WC-002 | 사람 신고를 트리거의 일차 소스로 둔다 | WFG-063 / WFG-069 — 크기 바닥은 위성을 **배제**할 뿐 사람을 **옹립**하지 않습니다 | 6 | <!-- forbidden-ok: 신고 일차 -->
| WC-003 | 같은 두 주장, 영어로 | WFG-070 — 크리틱 #10 이 한국어 목록이 읽지 않는 세 연구 문서에서 살아 있는 것을 찾았습니다 | 3 |
| WC-004 | 레지스트리 카드의 각인(`41498ef`)이 지금 브랜치에서 닿지 않는다 | 크리틱 #26·#27 (2026-09-06) / WFG-133 — 완전히 푼 클론에서 `merge-base --is-ancestor` 는 0으로 종료합니다 (`6cac7c6` 기준 거리 286; 커밋마다 움직이는 값이라 여기서는 기준 커밋과 함께만 적습니다) | 6 | <!-- forbidden-ok: 브랜치 닿지 않 -->
| WC-005 | 부스 인쇄물의 A4 근거 시트 겸 대조표가 아직 없다 | 개발 랩 2026-09-07T0018Z / WFG-130 — `docs/submission_reconciliation.md` 는 13,702 바이트이고 WFG-018 이 2026-09-03 에 `done(20260903T0653Z)` 로 닫은 문서입니다. 인쇄물 manifest 네 판이 그 반대를 적고 다녔습니다 | 3 | <!-- forbidden-ok: 근거 시트 아직 없 -->
| WC-006 | 마을 A4 출동 지시서가 「이미 커밋된 PDF 라서 그대로 인쇄하면 된다」, 그리고 그것이 「29 장」이다 | 개발 랩 2026-09-07T0355Z / WFG-151 이 거짓으로 판정하고 세 곳을 고쳤으나 **등록하지 않았고**(CHARTER §3.5c), 크리틱 #32 가 그 결과를 재고 WFG-153(a) 로 올렸습니다. 트리는 클러스터 **33개**이고 커밋된 `dispatch_a4.pdf` 는 **3개**뿐이며(`outputs/dispatch/README.md`), 「29」는 이 저장소의 어떤 산출물도 세지 않습니다. 2026-09-07T0630Z 랩이 인쇄물 재빌드와 같은 랩에서 등록했습니다 | 2 | <!-- forbidden-ok: wc006-29-dispatch-sheets, wc006-dispatch-committed-pdfs -->
| WC-007 | 국내 두 운영 시스템이 재현 절차를 대외에 내놓은 적이 없다 | 개발 랩 2026-09-07T1528Z / WFG-162 — 크리틱 #35 의 `fix-before-next-row` 항목입니다. 이 패널이 읽은 것은 카탈로그 기록과 언론 보도뿐이고, 국립산림과학원 사용자가이드 원문은 **NH-039** 로 아직 아무도 열지 못했습니다. 원본 조사 `docs/related_work.md:134` 는 같은 칸을 처음부터 「not stated」로 적었고, 요약본인 패널만 한 칸 더 나갔습니다. ⚠ 이 랩도 등록 대신 손 grep 으로 닫으려 했고 **독립 검토자가 그 한 가지로 막았습니다** | 1 |
| WC-008 | 국내 두 운영 시스템이 **가구 단위 대피 경로를 산출하지 못한다** | 논문 랩 2026-09-07T1529Z 가 자기 검토자의 block 으로 `paper/manuscript.md` §2 에서 철회했고, 그 검토자가 §2 의 출처 페이지인 `docs/related_work.md` 13·14 행에 같은 단정이 **그대로 살아 있는 것**을 찾아 **NH-044** 로 올렸습니다 (CHARTER §12 상 논문 루틴은 그 파일을 고칠 수 없습니다). 같은 주장 계열을 열고 있던 개발 랩 2026-09-07T1528Z 가 두 행을 좁히고 같은 랩에서 등록했습니다. ⚠ 등록이 **세 번째 사본**을 찾아냈습니다 — `docs/auto/knowledge/KOREAN_OPERATIONAL_SYSTEMS.md` §2, WFG-163 과 같은 모양입니다 ⚠⚠ 그리고 **네 번째 사본은 한국어였고, 등록이 닿을 수 없는 언어였습니다** — 개발 랩 2026-09-07T1820Z / WFG-166 이 학생이 소리 내어 읽는 Q16a T0 카드에서 찾아 한국어 철자를 등록했고, 같은 랩의 주어 grep 이 **다섯 번째 사본**을 인쇄 패널 `docs/auto/finals/RELATED_WORK_PANEL.md:67-69` 에서 찾았습니다 — 1528Z 랩이 WC-007 로 **같은 파일의 다른 절**을 고치면서 남긴 절입니다 | 2 | <!-- forbidden-ok: wc008-household-walk-out-negative, wc008-household-walk-out-negative-ko -->
| WC-009 | 국립산림과학원 산불확산예측시스템이 **발화점을 운영자가 손으로 입력받는다** | 개발 랩 2026-09-07T2120Z / WFG-171 — 크리틱 #37 의 `fix-before-next-row` 항목입니다. 이 계열에서 **처음으로 「없다」가 아니라 「있다」쪽 단정**입니다. 근거는 도서관 카탈로그 목차의 장 제목 「발화지점 생성」 하나뿐이고, 사용자가이드 원문은 **NH-039** 로 아직 아무도 열지 못했습니다. 인쇄되는 패널 `docs/auto/finals/RELATED_WORK_PANEL.md:32` 이 그 문장을 굵게 싣고 있었고, 네 줄 아래에는 「열어 본 적 없는 문서에 무엇이 **없다**고 말하는 것은 새로운 주장」이라는 자기 주석이 있었습니다 — 주석의 논거는 이 문장을 덮지만 주석의 **문구**는 덮지 못했습니다. ⚠ 이 랩의 손 sweep 은 통과했고 **독립 검토자가 막았습니다**: 한국어 주어 grep(「발화」)은 라틴 문자를 읽지 못하므로 `paper/manuscript.md` §2 와 `paper/references.bib:258` 의 영어 사본에 닿지 못했습니다. ⚠⚠ 그리고 등록이 **검토자가 깨끗하다고 판정한 파일**에서 세 번째 사본을 찾았습니다 — `docs/auto/knowledge/KOREAN_OPERATIONAL_SYSTEMS.md` §1. 사람 둘과 기계 하나가 읽었고, 그 사본을 찾은 것은 기계뿐입니다 | 2 | <!-- forbidden-ok: wc009-nifos-origin-hand-entered, wc009-nifos-origin-hand-entered-ko -->
| WC-010 | 이 저장소의 테스트 묶음이 **아직 망을 타고 있어 오프라인으로 돌릴 수 없다** | 개발 랩 2026-09-08T0018Z / WFG-139 — 이 레지스트리에서 유일하게 **너무 약해서** 철회된 주장입니다. Q40 · T1 카드가 「오늘의 답이 「아니오」입니다」로 답하고 학생에게 「지형 타일을 내려받는 테스트가 하나 남아 있고 그건 저희가 아직 못 고쳤습니다」를 소리 내어 말하게 하고 있었습니다. 그 고백은 그때 사실이었고, **같은 랩이 그 결함을 닫으면서 고백만 인쇄된 카드 위에 남겨 두었습니다**. 철 지난 자기비판은 심사위원 앞에서 철 지난 자랑과 똑같은 값을 치릅니다. ⚠ 이 랩도 등록이 필요했습니다: 독립 검토자가, 이 랩이 방금 쓴 Q28 수정이 같은 랩이 12분 전에 쓴 Q40 수정과 **서로 어긋난다**는 것을 찾았습니다 — CHARTER §3.5c 가 쓰여진 이유인 Q30/Q35 모양 그대로입니다 | 2 | <!-- forbidden-ok: wc010-suite-still-downloads-a-terrain-tile-ko, wc010-suite-still-downloads-a-terrain-tile-en -->
| WC-011 | 이 프로젝트의 공정한 상대가 쓰는 버퍼 폭의 최적점이 **고원이 아니라 뾰족한 봉우리**이고, 따라서 어느 폭이 맞는지는 그날 알 수 없다 | 개발 랩 2026-09-08T2117Z / WFG-127 — 세 폭을 더 재어 어깨임을 **측정**했습니다. ⚠ 이 줄은 WC-011 을 등록한 랩이 이 표에 넣지 않은 것을 2026-09-09T1817Z 랩이 뒤늦게 채운 것입니다. 등록 자체는 그때 되었고(게이트는 초록이었습니다), 사람이 읽는 이 목록만 한 줄 비어 있었습니다 | 3 | <!-- forbidden-ok: wc011-buffer-width-is-a-spike-ko, wc011-buffer-width-is-a-spike-en, wc011-which-width-is-unknowable-ko -->
| WC-012 | 일정 문서의 **다섯 구간 경계를 아무도 고르지 않았고 커밋 기록의 빈칸이 정해 주었다** | 개발 랩 2026-09-09T1817Z / 크리틱 #52 의 `fix-before-next-row` 항목 — `scripts/build_timeline_roles.py:40-84` 가 다섯 경계를 모두 값으로 적어 두고 있어 고르는 규칙이 트리 어디에도 없습니다. 이 레지스트리에서 **측정이 아니라 이 프로젝트가 스스로를 어떻게 꾸렸는지**에 대한 첫 철회이고, 가장 싸게 반증되는 항목입니다(빌더를 열면 끝납니다). ⚠ 함께 서 있는 더 강한 주장은 철회되지 **않았습니다**: `commits_outside_phases` 가 0 이므로 경계를 무엇으로 잡든 그 다섯 구간은 기록 전체를 덮습니다 | 2 | <!-- forbidden-ok: wc012-phase-boundaries-were-not-chosen-ko, wc012-the-gaps-are-the-boundaries-ko -->
| WC-013 | 이 프로젝트가 내놓는 산출물이 **가구 단위**의 대피 판정이고, 저장소에 커밋된 실물이 그것이다 | 개발 랩 2026-09-10T0017Z / WFG-222 — 크리틱 #54 의 root objection. ⚠ **이 주장은 하루 전에 이미 한 번, 한 면에서만 철회되었고 등록되지 않았습니다**: WFG-212 의 독립 검토자가 2026-09-09T2206Z 에 **접속(conjunction)** 으로 막았고 그 랩은 `README.md` 만 고쳤습니다. 같은 문장이 심사위원이 서는 화면, 학생이 외워 말하는 T0 카드, 부스 대본, 인쇄 키트에 그대로 남았고 그것이 CHARTER §3.5c 가 존재하는 이유의 세 번째 재현입니다. 철회된 것은 둘입니다 — **말투**(커밋된 실물의 출발지는 `origins: sampled candidates` 이지 가구가 아니고, Q20a 의 정직한 정의는 200줄 떨어져 있습니다)와 **접속**(①번 옆의 ②번과 이어 읽으면 「실제 도로 + 실제 불 → 이 지시서」가 되는데 그 실행은 없습니다). 접속 쪽은 철자가 없으므로 `tests/test_output_object_claim_bounds.py` 가 구조로 잡습니다. ⚠⚠ **등록이 아무도 이름 부르지 않은 사본 셋을 찾았습니다** — 지식노트 둘과 리서치 주간 기록 하나 | 3 | <!-- forbidden-ok: wc013-output-object-is-per-household-ko, wc013-output-object-is-per-household-en, wc013-a-walking-route-per-household-en -->
| WC-014 | `docs/oracle_gap.md` 의 크기비 계열 0.74 / 1.02 / 1.05 / 1.05 이 **예보의 성질**이고, 3시간 시점에서 모의가 그만큼 과소예측한다 | 개발 랩 2026-09-10T0625Z / 크리틱 #56 의 `fix-before-next-row` 항목(WFG-215 의 분 단위 절반). **이 레지스트리에서 처음으로 숫자가 아니라 숫자의 「읽기」를 철회했습니다** — 아티팩트는 하나도 움직이지 않았고, 틀린 것은 그 계열이 무엇에 관한 것이라고 적혀 있었는지입니다. `data/processed/oracle_gap_yeongdeok.json` 을 한 프로세스로 직접 재어 확인했습니다: 씨앗을 뺀 네 절편의 `time_gap_min` 은 153 / 27 / 207 / 285 이고 그중 **셋(180·360·540분)이 같은 관측 하나로 채점**됩니다. 분모가 고정된 채 분자만 자라는 계열이라 3시간 행의 부족분은 대부분 **정합 간격**이지 예보 편향이 아닙니다. ⚠ **철회되지 않은 쪽이 더 큽니다**: 잘 맞은 27분 쌍의 **위치 불일치**는 그대로입니다. 간격이 길어지면 교집합과 합집합이 함께 커지므로 간격이 만들어낼 수 있는 값이 아닙니다. ⚠ **P1 행이 아니라 선점이었던 이유는 문서의 지위가 한 창 안에서 바뀌었기 때문입니다**: `c4eb8d2` 에서 이 문서가 결선 화면 알려진 한계 패널의 **첫 카드**가 인용하는 근거가 되었고, 인쇄 키트의 **Q36 · T0** 카드도 여기를 가리킵니다. WFG-215 가 적은 「심사위원이 보는 면이 아니다」는 **접수 당시에는 참이었습니다**. ⚠ 등록 전에 추적되는 `.md`·`.html` **1,134개 전부**에 대해 blast radius 를 재었습니다: 12건, **전부 record 경로**(BACKLOG·CRITIC_LATEST·SCORECARD·dashboard·랩 보고서 셋). 게이트 대상 산문에는 **0건** — 유일하게 이고 있던 `docs/oracle_gap.md` 를 이 랩이 고쳤기 때문입니다. 그래서 이 등록은 **어디에도 pragma 를 달지 않습니다**. 고친 면은 철회된 문장을 **인용하지 않습니다**(WC-005·WC-007 선례) | 2 |
| WC-015 | 대본의 초 배분이 자기 자신을 바꾸는 고정점이고, **61초에서는 그것이 물린다 (육십일, 네 음절)** | 개발 랩 2026-09-11T0317Z / WFG-247 — **이 레지스트리에서 처음으로, 바깥 사실이 아니라 이 저장소가 자기 기계장치에 대해 한 산술을 철회했습니다.** `docs/demo_script_pace.md` 의 「One thing the measurement does to itself」 절은 뒤에 올 모든 재측정에게 무엇을 조심하라고 알려 주는 자리이고, 거기에 「61초에서는 물린다」가 엿새 동안 적혀 있었습니다. 이 랩의 재배분이 마무리를 **정확히 61초**로 옮겼고, 물리지 않았습니다. 육십일은 한글 **세** 블록이고, 이 페이지 자신의 규칙(한 블록이 한 음절)으로 `count_syllables(["마지막 61초는"])` 와 `count_syllables(["마지막 56초는"])` 은 **둘 다 8** 을 돌려줍니다. **메커니즘은 참이고 예시만 틀렸으므로 절차는 그대로 둡니다** — 100(백, 한 블록)과 61(육십일, 세 블록)은 여전히 물리는 쌍입니다. ⚠⚠ **이 정정의 첫 초안도 틀렸고 독립 검토자가 랩을 막았습니다**: 「둘 다 7」이라고 적고 그 7을 `count_syllables` 에 돌렸는데, 그 함수는 **줄의 리스트**를 받으므로 맨 문자열을 주면 글자 단위로 세어 오류도 규칙의 답도 아닌 수를 돌려줍니다. 결론(8 = 8)은 두 초안에서 같았고, 그래서 틀린 수가 쉽게 실릴 뻔했습니다. ⚠ 등록 전 blast radius 를 **줄바꿈을 편 본문**으로 재었습니다: 3건, 그중 게이트 대상은 **한 건** — 철회된 문장을 철회하기 위해 인용하는 정정 블록이고 줄 pragma 로 허가했습니다 | 1 |
| WC-016 | 대피 지점 탐색이 **보행망 노드 2,218곳**을 전수 탐색했다 <!-- forbidden-ok: wc016-2218-called-walk-network-nodes-ko --> | 개발 랩 2026-09-11T0317Z / WFG-247 (크리틱 #63 이 1순위로 올린 행) — 숫자는 내내 맞았고 **거기 붙은 명사가 틀렸으며, 심사위원이 따라 말하는 것은 명사입니다.** `data/processed/vulnerability/refuge_placement.json :: optimum_h240.constraints` 는 `n_walk_nodes_total` **8443** 과 `n_candidates_after_filter` **2218** 을 나란히 들고 있고, `l0i_candidates_enumerated` 의 `derivation` 도 「of 8,443 walk-network nodes」라고 이미 적어 두었습니다. 곧 제약 필터의 **입력**을 **출력**의 이름으로 부른 것입니다. 네 면이 이고 있었습니다: 대본의 낭독 마무리와 §3 숫자 대조표, `docs/finals_screen_v2.md` §2.4, 그리고 결선 화면의 대피 지점 카드(`scripts/finals.template.html` → `web/finals.html`). 같은 커밋에서 **8,443 을 `l0i_walk_nodes_total` 로 등록**했으므로 고친 문장에는 아티팩트가 뒤에 있습니다. ⚠ 등록 전 blast radius 를 줄바꿈을 편 본문으로 재었습니다: 5개 파일 6건, **게이트 대상 0건** — 전부 CHARTER §3.5c 의 record 경로라서 이 등록은 **어디에도 pragma 를 달지 않습니다** | 1 |
| WC-017 | 면적을 맞춘 원판을 **발화점에** 놓았다 <!-- forbidden-ok: wc017-disc-at-ignition-ko --> <!-- forbidden-ok: wc017-disc-at-ignition-en --> <!-- forbidden-ok: wc017-centred-on-ignition-en --> <!-- forbidden-ok: wc017-moved-from-ignition-en --> (영문 「an equal-area disc at the ignition」 · 「centred on the ignition」 · 막대 묶음 제목 「distance moved from the ignition」) | 개발 랩 2026-09-11T0921Z / WFG-254 (크리틱 #65 가 1순위로 올린 행) — **측정값은 하나도 틀리지 않았고, 그 중심을 부르는 낱말이 틀렸습니다.** `data/processed/disc_null_yeongdeok.json :: null_rule.centre_from` 은 규칙을 「centroid of the t=0 seed」 라고 스스로 옳게 적어 두었고 `scripts/measure_disc_null.py` 는 `ign_xy` 를 **한 번도 읽지 않습니다**. 그런데 같은 정본 배열이 들고 있는 `ign_xy` 를 격자에 얹으면 칸 (74, 25) 이고, 원판의 중심은 (97.7751, 55.1205) 이라 둘 사이가 `dnc_yeongdeok_seedcentre_to_ignition_cells` **38.3986** 칸입니다 — 어느 시각의 원판 반지름보다도 멀어서 **그 어떤 원도 기록된 발화점을 품지 않습니다**. ⚠ 행 원점 규약을 가정하지 않았습니다: 두 후보를 다 재고 「기록된 발화점은 `t = 0` 프레임에서 타고 있어야 한다」로 관측이 고르게 했으며, 진 쪽 규약에서도 31.6008 칸으로 결론은 같습니다. ⚠ 그리고 이것은 **줄임말이 아니었습니다** — `t = 0` 씨앗은 점이 아니라 격자에 흩어진 칸들이라 그 중 한 칸이 전체를 요약하지 않습니다. 여섯 면이 이고 있었습니다: `docs/auto/JUDGE_QA.md` Q36(**T0**, 외워서 말하고 인쇄물에 실림), `docs/disc_null.md` §6 낭독 초안, `docs/oracle_gap.md` §4c, `paper/manuscript.md` §6, `paper/README.md`, 그리고 `paper/make_figures.py` 가 `paper/figures/F10_disc_null.png` 에 구워 넣은 제목. ⚠⚠ **이 레지스트리가 닿지 못하는 한 곳이 그 PNG 입니다** — 커밋된 아티팩트라 CHARTER §3 규칙 2 가 재생성을 금하므로, 파일은 바이트 그대로 두고 `paper/GAPS.md` 에 날짜를 박은 정정 문단을 남겼으며, 고친 그림은 **새 파일 이름** `F10b_disc_null.png` 로 나갑니다. 그 예외는 WC-017 항목의 `artifact` 칸에 날짜와 함께 적혀 있습니다(NH-042 A). ⚠ 등록 전 blast radius 를 재었습니다: ko 와 `disc at` 은 각각 record 경로 1건, `centred on` 은 4건 중 2건이 record 이고 2건은 이 랩 자신의 정정 문단(줄 pragma 로 허가), `distance moved` 는 6건 중 5건이 record 이고 1건이 pragma 로 허가된 `paper/GAPS.md` 입니다 | 4 |



### 5i. WC-010 — 이 레지스트리에서 처음으로, **너무 강한** 주장이 아니라 **너무 약한** 주장을 철회했습니다 (2026-09-08T0018Z, WFG-139)

앞의 아홉 건은 전부 같은 모양입니다. 저장소가 근거보다 크게 말했고, 랩이 그것을 좁혔습니다.
이번 건은 반대입니다. Q40 · T1 카드는 「이 저장소의 테스트를 인터넷 없이 돌릴 수 있습니까?」에
「⚠ 오늘의 답이 「아니오」입니다」라고 답하고 있었고, **그것은 쓰여진 날에 정확했습니다.** 테스트
묶음은 실제로 콜드 런마다 지형 타일을 내려받고 있었고, 그 카드는 그 사실을 심사위원에게 숨기지
않으려고 쓰인 카드였습니다.

그리고 같은 랩이 그 결함을 닫았습니다. 닫고 나서 카드를 그대로 두었다면, 학생은 이제 존재하지
않는 결함을 부스에서 자백하게 됩니다. **철 지난 자기비판은 철 지난 자랑과 정확히 같은 방식으로
틀립니다** — 심사위원이 확인할 수 있는 것과 학생이 말하는 것이 어긋나고, 어긋나는 방향이
겸손 쪽이라는 이유로 값이 깎이지 않습니다. 이 저장소의 어떤 규칙도, 어떤 게이트도, 어떤 랩의
습관도 그 방향을 보고 있지 않았습니다. 그래서 이 계열은 `stale_self_criticism` 이라는 자기
이름을 갖습니다.

⚠ **그리고 이 랩은 이 파일이 실제로 필요했습니다.** 랩은 Q28 과 Q40 을 손으로 고쳤습니다.
독립 검토자가 읽어 보니, 12분 간격으로 같은 파일에 쓴 두 수정이 서로 어긋나 있었습니다 — Q40 은
「가드가 세 개를 세웠지만 그 중 하나는 망이 아니었다」고 정확히 적었는데, 200줄 위의 Q28 은
「테스트 세 개가 망을 탔다」고 적고 있었습니다. 한 랩이 어떤 문서를 고칠지 고르면 하나를
빠뜨린다는 CHARTER §3.5c 의 논거가, 한 파일 **안에서** 재현된 것입니다.

⚠ **등록이 닿지 않는 곳, 적어 둡니다.** 이 주장의 영어 생활은 거의 전부 기록 계열
(백로그 행, `CRITIC_LATEST.md`, `DIRECTION.md`, 열한 개의 랩 보고서)에 있고 scope 가 설계상
면제하는 파일들입니다. 그래서 오늘 영어 패턴이 지키는 것은 **아직 쓰이지 않은 산문**입니다.
그것이 의도한 쓰임입니다: 다음 주에 옛 보고서를 읽은 랩이 닫힌 결함을 현재형으로 다시 쓰면
그때 걸립니다. 과거형(「the suite downloaded」, 지금 `docs/clean_clone_gates.md` 가 쓰는 형태)은
패턴 밖에 일부러 두었습니다 — 역사는 계속 말할 수 있어야 합니다(CHARTER §3.7).

⚠ **등록하지 **않은** 것 하나, 이유와 함께.** 같은 랩이 `docs/clean_clone_gates.md` 의
「No network, no keys, no `.env`.」 도 거짓이었다고 판정했습니다. 그것은 여기 등록하지 않았습니다.
그 문장은 **지금은 참**이기 때문입니다 — 랩이 참으로 만들었습니다. 참이 된 문장의 철자를 금지
패턴으로 넣으면 게이트는 앞으로 옳은 문장마다 붉게 울립니다. 참이 된 문장에 맞는 처리는 금지가
아니라 **날짜가 붙은 정정**이고, 그 정정은 그 파일 안에 있습니다.

### 5h. WC-009 — 규칙은 **한쪽 방향으로만** 쓰여 있었고, 반대 방향은 인쇄된 종이 위에 살아 있었습니다 (2026-09-07T2120Z, WFG-171)

이번 주에 이 저장소가 잡은 「남의 시스템에 대한 주장」은 여섯 건이고, 그 중 다섯이
**부정**(「저쪽은 X를 하지 않습니다」)이었습니다. WC-007, WC-008, 그리고 그것들이 낳은
모든 철자·DIRECTION 문구·패널 주석이 전부 부정에 대해 쓰였습니다. 여섯 번째는 **긍정**이었고,
같은 인쇄 패널의 네 줄 위에 굵은 글씨로 있었습니다. 출처도 같고(카탈로그 목차의 장 제목),
심사위원 앞에서 무너지는 방식도 같습니다 — 그 콘솔을 실제로 써 본 심사위원이라면
「발화점은 운영자가 손으로 입력합니다」<!-- forbidden-ok: wc009-nifos-origin-hand-entered-ko -->라는
단정이 사실과 다르다는 것을 압니다. **비대칭은 랩에 있었던 것이 아니라 규칙 자체에 있었습니다.**

**이 항목이 기록하는 진짜 교훈은 손 sweep 과 기계의 차이입니다.** 이 랩은 크리틱이 지목한 두 파일을
좁혔고, DIRECTION 이 시키는 주어 grep 을 돌렸고, 거기서 아무도 이름 부르지 않은 세 번째 사본
(`docs/related_work.md` 13행)까지 찾아냈습니다. 그리고 「남은 것은 전부 우리 자신의 발화점이거나
기록용 문서」라고 보고했습니다. **그 보고는 틀렸고, 독립 검토자가 그것 하나로 막았습니다.**
주어 grep 은 「발화」였고, 그 주장은 영어로 `paper/manuscript.md` §2 에 살아 있었습니다.
`paper/references.bib:258` 에는 한 번 더 있었고 — 그 註가 논문의 문구를 「이것이 논문이 말하는
전부」라고 보증하는 동안 논문은 그 註를 출처로 인용하고 있었으므로, **출처라고 보고된 것은 원이었습니다.**

**그리고 등록이 검토자보다 하나 더 찾았습니다.** 철자를 등록하자 게이트가 933개 파일을 읽고
`docs/auto/knowledge/KOREAN_OPERATIONAL_SYSTEMS.md` §1 을 즉시 세웠습니다 — 검토자가 「유도 과정이
보이므로 통과」라고 명시적으로 판정한 파일입니다. 유도 과정은 「운영자 콘솔이다」까지는 지탱하지만
「사람이 손으로 좌표를 넣는다」는 지탱하지 않습니다. **사람 둘이 읽고 기계 하나가 읽었으며, 그
사본을 찾은 것은 기계뿐입니다.** CHARTER §3.5c 가 존재하는 이유가 이 한 줄입니다.

⚠ **인쇄되는 두 파일의 정정 주석은 철회된 문장을 옮겨 적지 않고 설명만 합니다** (WC-005·WC-007 선례).
심사위원이 들고 가는 종이에 철회된 문장을 다시 찍지 않기 위해서이고, 그래서 두 주석 모두 pragma 가
필요 없습니다. 원문은 이 레지스트리에 있습니다.

⚠ **이 등록이 닿지 못하는 곳, 적어두고 넘어가지 않습니다.** 게이트가 읽는 것은 `.md` 와 `.html`
뿐이므로 **이 계열을 촉발한 파일인 `paper/references.bib` 에는 영원히 닿지 않습니다.** 그 사본은
이번 랩에서 손으로 좁혔고, 다시 쓰이면 잡을 것이 없습니다. 그리고 §4 가 모든 철자에 대해 말하는
한계가 여기에도 그대로 적용됩니다 — 「수동으로」로 바꿔 쓰거나 「운영자가」를 빼면 빠져나갑니다.
**새로** 쓰이는 이런 문장을 잡을 린트는 WFG-168 이고, 아직 없습니다.

### 5g. WC-008 — 등록이 **또 한 번** 아무도 이름 부르지 않은 사본을 찾았습니다

WC-007 과 같은 랩, 같은 주장 계열입니다. 다른 점은 **누가 철회했는가**입니다: 이번에는 논문
루틴의 독립 검토자가 `paper/manuscript.md` §2 에서 「neither answers …」를 막았고, 그 검토자가
**§2 가 인용하는 바로 그 페이지** `docs/related_work.md` 13·14 행에 같은 단정이 살아 있는 것을
찾았습니다. 논문 루틴은 CHARTER §12 때문에 `paper/` 바깥을 고칠 수 없어서 **NH-044** 로 올렸고,
같은 창에서 WFG-162 로 같은 계열을 열고 있던 개발 랩이 두 행을 좁히고 여기에 등록했습니다.

**그리고 등록이 세 번째 사본을 찾아냈습니다.** 두 랩 어느 쪽도 이름 부르지 않은
`docs/auto/knowledge/KOREAN_OPERATIONAL_SYSTEMS.md` §2 — CHARTER §13 이 「랩이나 학생이 개념을
찾아보는 곳」이라고 적어 둔 파일입니다. WFG-163 이 이번 주에 이미 같은 모양을 기록했고, 이것이
그 두 번째입니다. 같은 파일 §3.3 에는 「Reproducibility on public data is a claim **neither
agency system makes**」— WC-007 의 **영어판** — 도 살아 있었고, WC-007 의 한국어 철자는 당연히
그것에 닿지 못했습니다. §4 가 모든 패턴에 대해 적어 둔 한계가 그대로 실현된 것이라, 손으로
고쳤습니다.

⚠ 이 패턴의 설계에서 한 가지는 **의도적으로 좁습니다.** 「walk out**,** and along which path」의
쉼표가 걸쇠입니다. 이 프로젝트가 **자기 산출물**을 설명할 때 쓰는 「which household to reach
first and along which walking route」·「walk out and by which path」(쉼표 없음)까지 잡는 패턴이었다면,
그것은 이 프로젝트의 제출 프레임 자체를 막는 게이트가 되었을 것입니다. 반대로 14 행의 철자
「anything below the township; a walking route for one person」은 13 행과 공유하는 어구가 없어
**이 패턴이 닿지 못합니다** — 같은 커밋에서 손으로 고쳤고, 래칫은 없습니다.

### 5g-2. 등록은 **한 언어에만 닿습니다** — WC-008 의 한국어 절반 (2026-09-07T1820Z, WFG-166)

위 §5g 는 「등록이 사본을 찾아낸다」는 이야기입니다. 90 분 뒤 크리틱 #36 이 그 이야기의
**반대쪽**을 찾았습니다. `walk\s*out,\s*and\s+along\s+which\s+path` 는 라틴 문자 어구 안의
쉼표에 걸린 패턴이라 **한국어 문장에는 구조적으로 닿을 수 없습니다.** 그래서 같은 주장이
`docs/auto/JUDGE_QA.md:650-652`, 즉 **학생이 심사위원 앞에서 소리 내어 읽는 Q16a 의 T0 낭독
초안** 안에서 계속 살아 있었습니다. 같은 카드가 **스무 줄 아래**에서 그 형태를 ❌ 로 금지하고
⭕ 형태를 주고 이유까지 적어 두었는데도 그렇습니다. 레지스트리는 그 내내 green 이었습니다.

**두 가지가 여기서 처음 기록됩니다.**

1. **한 랩이 두 언어를 같이 등록한 적이 한 번도 없습니다.** WC-007 은 한국어로 들어가고
   영어판이 90 분 뒤에 나왔고, WC-008 은 영어로 들어가고 한국어판이 90 분 뒤에 나왔습니다.
   이틀도 아니고 하루 안에 **두 번, 서로 반대 방향으로** 같은 일이 일어났습니다. 그래서
   CHARTER §3.5c 와 DIRECTION.md 는 이제 「**한 랩이 두 언어를 같은 랩에서 등록하거나, 어느
   쪽을 하지 않았는지 글로 적는다**」고 말합니다.

2. **주어 grep 이 처음으로 값을 냈습니다.** 크리틱 #35 가 만든 규칙 —— 「방금 쓴 문장이 아니라
   **주장의 주어**로 grep 한다」 —— 을 이 랩이 「읍면동」·「가구 단위」로 실행했고, 크리틱 #36 도
   백로그 행도 이름 부르지 않은 사본을 찾았습니다: `docs/auto/finals/RELATED_WORK_PANEL.md:67-69`.
   이것은 **인쇄되는 패널**이고, 한 랩 전에 1528Z 랩이 **바로 그 파일의 다른 절**을 WC-007 로
   고친 페이지입니다. 한 랩, 한 페이지, 같은 모양의 두 절, 고쳐진 것은 하나. 두 사본 모두
   부스 킷에 인쇄됩니다 — `manifest_20260907T1825Z.json` 의 `pages_per_source` 기준 `JUDGE_QA.md` **19 쪽**, `RELATED_WORK_PANEL.md` **3 쪽**, 전체 38 쪽 중입니다. ⚠ 이 문단의 초고는 「38 쪽 중 20 쪽」이라고 적었고, 어떤 산출물도 뒷받침하지 않는 수였습니다 (CHARTER §3.3). 이 랩의 독립 검토자가 그것 하나로 막았고, 매니페스트가 그 산출물입니다.

⚠ **한국어 패턴의 한계도 영어판만큼 좁고, 여기 적습니다.** (1) 이 패턴은 부정의 **목적어**
<!-- forbidden-ok: wc008-household-walk-out-negative-ko -->
(「특정한 집의 어느 길이 … 들어가지 않는지는」)에 걸려 있고 **서술어에는 걸려 있지 않습니다.**
두 파일 모두에서 그 문장이 줄바꿈으로 끊겨 있었고 이 검사기는 줄 단위이기 때문입니다 ——
목적어가 줄바꿈으로 갈라지게 다시 감싼 사본은 빠져나갑니다. (2) 「특정한 집의」에 걸어 둔
<!-- forbidden-ok: wc013-which-house-first-ko -->
것은 이 프로젝트가 **자기 산출물**을 말할 때 쓰는 「가구 단위의 대피 경로」·「어느 집을 먼저,
어느 길로」에는 그 어구가 없기 때문입니다. 그 둘 중 하나로 바꿔 쓴 주장은 §4 가 모든 패턴에
대해 적어 둔 그대로 빠져나갑니다.

⚠⚠ **2026-09-11 (WFG-243 · WFG-240): 바로 위의 「빠져나갑니다」가 실제로 일어났고, 예고한
<!-- forbidden-ok: wc013-which-house-first-ko -->
그대로였습니다.** 「어느 집을 먼저」는 2026-09-11 부터 `WC-013` 의 등록된 철자이고, 그날
그 어구는 인쇄되는 관련연구 패널과 **T0 카드 Q29a** 두 줄에 살아 있었습니다. 위 인용은 그
철자를 **기록하기 위한** 인용이므로 줄 pragma 로 허가해 둡니다. 같은 랩이 등록한 나머지 다섯
철자와, 이 검사기가 **줄 단위**라서 어떤 패턴도 볼 수 없는 줄바꿈 사례(패널 앞면 한 문장)는
`docs/auto/withdrawn_claims.json` 의 `WC-013` 각 철자 `why` 에 적혀 있습니다.

### 5f. WC-007 — grep 이 **맞았는데도** 막힌 첫 사례이고, 그것이 §3.5c 의 요점입니다

WC-004·WC-005·WC-006 은 전부 「고친 랩의 손 sweep 이 한 파일을 놓쳤다」였습니다. WC-007 은
다릅니다. 2026-09-07T1528Z 랩은 크리틱 #35 가 그 아침에 DIRECTION 에 써 넣은 규칙 —
「방금 쓴 문장이 아니라 **주장의 주어**를 grep 하라」 — 를 실제로 지켰고, 네 가지 계열
패턴을 `docs/ paper/ release/ web/ scripts/` 에 돌렸으며, **살아 있는 사례는 정말로 하나뿐**
이었습니다. 랩의 독립 검토자가 같은 grep 을 독립적으로 다시 돌려 그 결과를 확인해 주기까지
했습니다.

그런데도 검토자는 `block` 을 냈고, 이유는 하나였습니다: **결과가 맞았다는 것은 방법이
맞았다는 뜻이 아닙니다.** CHARTER §3.5c 가 있는 이유가 손 grep 이 반복해서 실패했기
때문이라면, 「이번 내 grep 은 맞았다」는 반론이 아니라 그 패턴 자체입니다. 등록은 랩의
성실성을 신뢰 변수에서 빼는 장치이고, 그래서 랩이 성실했던 날에도 해야 합니다.

**그리고 등록이 실제로 한 가지를 잡았습니다.** 첫 정정문은 철회된 문장을 **원문 그대로**
인용해 두었는데(§3.5 는 지우지 말라고 하니까), 그 파일은 인쇄물 SOURCES 이고 같은 랩이
부스 키트를 다시 굽고 있었습니다. 등록 뒤 `check_withdrawn_claims.py` 를 돌리자 931개 게이트
파일 가운데 정확히 그 줄이 무면허로 걸렸습니다 — 즉 **철회를 기록하려고 쓴 문장이 철회된
문장을 심사위원 손의 종이에 다시 찍고 있었습니다.** WC-005 가 같은 이유로 이미 정한 선례를
따라, 정정문은 원문을 옮기지 않고 **무엇이 있었는지를 서술**하며 원문은 이 레지스트리에
둡니다. 그래서 그 줄에는 `forbidden-ok` 면허가 붙어 있지 않습니다.

⚠ 이 패턴의 한계는 §4 가 모든 패턴에 대해 적어 둔 것과 같고, 이 주장에서 특히 셉니다:
「밝히지 않습니다」·「비공개입니다」·「does not publish」 로 바꿔 쓰면 빠져나갑니다. 등록한 랩이
그 네 가지를 손으로 확인했지만 게이트가 확인한 것은 아닙니다. 이 패턴이 하는 일은 **복사·붙여
넣기 래칫**입니다.

### 5e. WC-006 — 등록이 **네 번째 사례를 찾아냈습니다**. 이것이 §3.5c 의 논거 전부입니다

WC-005 는 「고친 랩이 등록하지 않았다」였고, WC-006 은 **같은 일이 열세 시간 뒤에 또**
일어난 것입니다. 다른 점은, 이번에는 등록 자체가 아무도 몰랐던 인스턴스를 찾아냈다는
것입니다.

2026-09-07T0355Z 랩은 「출동 지시서는 이미 커밋된 PDF 라서 그대로 인쇄하면 된다」를 <!-- forbidden-ok: wc006-dispatch-committed-pdfs, wc006-29-dispatch-sheets -->
스스로 **거짓**이라 적고 세 곳을 고쳤습니다. 등록은 하지 않았습니다. 크리틱 #32 가
살아 있는 곳 **두 곳**을 찾았습니다 — 심사위원에게 건네는 폴더 안의
`manifest_20260907T0059Z.json:95`, 그리고 그것을 만든 `scripts/build_printables.py:648`.

2026-09-07T0630Z 랩이 실제로 등록하자 스캐너가 **네 곳**을 짚었고, 그 중 하나는
아무도 이름을 대지 않았던 것이었습니다:

| 파일 | 무엇이었나 | 처리 |
|---|---|---|
| `docs/printables.md:131-132` | ⚠ **현재 사실로** 서술 — 손으로 훑은 랩도, 크리틱의 프로브도 짚지 못했습니다 | 정정 |
| `docs/auto/JUDGE_QA.md` Q39 | 학생에게 그 문장을 **말하라고** 지시하는 카드 | 정정 |
| `docs/auto/finals/printables/manifest_*.json` | 스탬프가 굳은 산출물(§3.2). 새 스탬프로만 움직입니다 | 이번 랩이 재빌드 |
| `docs/finals_bundle.md:86` | 정정을 **기록**하려고 인용한 줄 | pragma 로 허용 |

크리틱의 프로브가 `docs/printables.md` 를 짚지 못한 이유는 철자가 달랐기 때문입니다.
크리틱은 실제 문장 그대로의 좁은 철자를 넣었고, 이 랩은 두 낱말(`이미`/`already` 와
`커밋된`/`committed`)에 앵커를 건 넓은 철자를 넣었습니다. **즉 §4 의 한계는 그대로
유효하고**(철자를 바꾸면 빠져나갑니다), 여기서 말할 수 있는 것은 그보다 훨씬 좁습니다:
**손으로 훑는 랩은 자기가 떠올린 파일만 읽고, 기계는 트리 전체를 읽습니다.**
이 커밋(`ff63907` 다음, 2026-09-07T0705Z) 기준으로 스캔 대상은 **931 개**입니다. ⚠ 이 수는 랩마다 움직입니다 — 이 랩이 판넬과 조사 문서를 더하면서 929 에서 931 이 되었고, 초안이 적어 둔 「929」는 **그 두 파일을 더한 바로 그 커밋에서 이미 낡은 수**였습니다(이 랩의 독립 검토자가 찾았습니다). §4 가 이 수를 외우지 말라고 하는 이유가 그것이고, 그래서 여기에는 기준 커밋을 함께 적습니다.

### 5d. WC-005 — 등록되지 않은 철회가 **두 번째로** 일어났고, 이번엔 같은 랩 안에서 잡혔습니다

WC-004 의 교훈은 §5a 에 적혀 있습니다: 「랩은 어느 문서를 고칠지 고르고, 반드시 하나를
놓친다. 등록이 기계에게 전부를 읽히는 일이다.」 **그 문장을 자기 MEMO 에 옮겨 적은 바로 그
랩이 같은 실수를 했습니다.**

<!-- forbidden-ok: 근거 시트 아직 없 -->
2026-09-07T0018Z 개발 랩은 WFG-130 으로 「A4 근거 시트는 아직 없습니다」가 거짓임을 세 곳에서
선언했습니다 — 새 manifest 의 `what_this_does_not_show`, `docs/printables.md`, BACKLOG 의 행.
그리고 `docs/auto/withdrawn_claims.json` 에는 아무것도 넣지 않았습니다. 그 랩의 **독립
검토자**가 `block` 을 내면서 찾은 것: 같은 주장이 `docs/auto/finals/BOOTH_SETUP.md` 의 네
곳에 그대로 살아 있었고, 그 파일은 그 랩이 방금 만든 PDF 의 **첫 번째 원본**입니다. 즉
인쇄된 꾸러미의 1~5 쪽이 학생에게 「이 꾸러미는 아직 없습니다」라고 말하고, 같은 꾸러미의
21~23 쪽이 그 없다던 시트였습니다.

검토자가 한 일이 이 규칙의 논거 그 자체입니다: WC-005 를 임시로 등록하고
`check_withdrawn_claims.py` 를 돌리자 「PASSED, 4 claims」가 「FAILED, 1 unlicensed mention」
으로 바뀌면서 `docs/auto/finals/BOOTH_SETUP.md:240` 을 그 자리에서 이름으로 불렀습니다. 손으로
훑은 랩이 놓친 파일을, 등록 한 줄이 한 번에 찾아냈습니다.

**이번엔 같은 랩 안에서 닫았습니다** (CHARTER §3.5c). 다만 한 가지는 다르게 했고 그래서 여기
적습니다: `BOOTH_SETUP.md` 의 〔기록〕 줄들은 철회한 문장을 **그대로 옮기지 않고 바꿔
적었습니다.** pragma 로 licence 를 붙일 수도 있었지만, 그 파일은 학생이 부스에서 손에 드는
종이이고, 종이 위에 철회된 문장을 다시 인쇄할 이유가 없습니다. 그대로의 문장은 이 문서와
WC-005 항목과 그 랩의 보고서에 있습니다 — 전부 record class 입니다. ⚠ 대신 이것은 §4 가 이미
말한 한계를 한 번 더 보여 줍니다: **바꿔 쓴 문장은 철자 레지스트리를 빠져나갑니다.** 그래서
여기서는 그것을 이용한 것이고, 이용했다고 적어 두는 것이 규칙을 지키는 방법입니다.

### 5a. WC-004 를 왜 나중에 등록했는가 — 이 레지스트리의 첫 「등록되지 않은 철회」

WC-001~003 은 모두 탐지 순서 주장 하나의 갈래이고, 같은 랩이 철회와 등록을 함께
했습니다. WC-004 는 다릅니다. 크리틱 #26 이 2026-09-06T1100Z 에 이 주장을 철회했지만
**이 레지스트리에는 넣지 않았습니다.**

⚠ 이 랩의 첫 초안은 「철회가 기록 클래스 세 페이지에만 닿았다」고 적었습니다. **틀린
문장이고, 독립 검토자가 인용된 커밋에서 반증했습니다.** `git show --stat 828bbae` 를
보면 크리틱 #26 은 `docs/auto/JUDGE_QA.md` 도 **+19줄** 고쳤고(Q30 의 정정 블록),
그 파일은 기록 클래스가 **아닙니다**. 사실은 이렇습니다: 철회는 학생이 읽는 카드
파일에 **닿았지만 Q30 까지만 닿았고**, 같은 파일 안의 Q35 블록은 그대로 남았습니다.
그리고 어느 페이지에도 닿지 않은 한 가지가 **이 레지스트리**였습니다.

레지스트리에 넣는 일이 왜 결정적이었는가: 등록은 사람이 어느 문서를 고칠지 고르는
일이 아니라 기계가 **925개 파일 전부**를 훑는 일이기 때문입니다. 크리틱 #26 이
WC-004 를 등록했다면 같은 파일의 Q35 가 그 자리에서 빨간불이 됐을 것입니다. 등록하지
않았기 때문에, 게이트는 초록인 채로 심사위원 앞에서 학생이 읽는 Q35 가 한 창(window)
동안 그 거짓 문장을 **말하라고** 지시하고 있었습니다.

이것이 크리틱 #27 의 근본 반론입니다: **루프는 수정이 이루어졌는지는 재고, 도착했는지는
재지 않습니다.** 이 문서의 §1 이 말하는 누락은 「목록에 없는 파일」이었고, WC-004 가
드러낸 누락은 한 단계 위입니다 — 「레지스트리에 없는 주장」. 기계는 이미 있었고,
아무도 그 기계에 새 주장을 넣지 않았습니다. 그래서 CHARTER §3 규칙 5c 가 그 단계를
철회 자체의 일부로 못 박습니다: 주장을 철회하는 랩은 **같은 랩에서** 여기에 등록합니다.

## 5b. 이 랩의 독립 검토자가 막았고, 그래서 고친 것

검토자는 이 작업을 **block** 했습니다. 논쟁하지 않고 고쳤습니다 (CHARTER §4 5단계).

1. **레지스트리 밖에서 살아 있던 철자 하나.** 「기준**이** 신고 시각이므로」는
   등록된 「기준 시각**은** 신고」와 어순만 다르고, `docs/SESSION19_REPORT.md:229`
   와 `:293` 에 살아 있었습니다 — 이 행이 방금 면제해 준 줄에서 세 줄 떨어진 곳에.
   **WC-001 의 일곱 번째 철자로 등록했습니다.** 등록한 이유가 중요합니다: 패턴을
   쓴 사람이 아닌 검토자가 **트리에서 실제로 찾은** 철자이기 때문입니다. 작성자가
   지어내고 자기 문장으로 채점하는 패턴은 `mandela` 가 말하는 누출입니다.
2. **면제가 닿지 않는 면제.** 「주장 규율 점검」과 「주장하고 싶었으나 검증하지 못한
   것」 두 절에는 어떤 철회 표시도 닿지 않는데 그 안의 줄을 면제했습니다. 두 절 머리에
   **사용 지점 철회 표시**를 붙였습니다 (WFG-070 의 방식).
3. **숫자 두 개가 틀렸습니다.** 검사기 독스트링의 `343`(어떤 명령으로도 나오지
   않는 수, 실제 988)과 테스트 독스트링의 `914`(실제 915). CHARTER §3 규칙 3 은
   절대적이므로 둘 다 고쳤습니다.
4. **커버리지 숫자의 구성**을 §3 에 적었습니다 (위).

검토자가 **깨지 못한 것**도 적어 둡니다. 검토자는 이 diff 의 작성자가 쓰지 않은
문장 27개를 만들어 어떤 가드 목록에도 없는 경로
(`docs/auto/finals/HOSTILE_REVIEW_PROBE.md`)에 심었습니다. 철자를 그대로 쓴
**17문장은 17/17 잡혔고** 같은 트리에서 옛 파일별 게이트는 **전부 초록**이었습니다
— 이 행이 실제로 주장하는 차원은 버텼습니다. 철자를 우회한 **10문장은 0/10**,
합계 17/27. 그 0/10 이 §4.2 가 말하는 바로 그 부류입니다.

## 5c. WC-004 의 독립 채점 — 이 랩의 검토자가 처음 판을 뒤집었습니다

§5b 가 WC-001~003 에 한 일을 WC-004 에도 했고, 결과는 더 나빴습니다. 이 랩의 독립
검토자는 패턴을 **보지 않은 상태에서** 문장 아홉 개를 써서 채점했습니다.

**고치기 전:** 참인 문장 3개 중 **3개를 잘못 잡았고**, 거짓 문장 6개 중 **5개를
놓쳤습니다.** 방향이 뒤집혀 있었던 것입니다. 원인은 두 번째 철자가 금지 문구
(「말하지 마십시오」)가 아니라 목적격 조사 `문장을` 을 잡고 있었고, 등록 항목의
`why` 가 **「목적격 조사가 금지와 허용을 구분한다」**고 적었기 때문입니다. 그것은
한국어에 대해 **거짓인 문장**입니다 — `문장을` 은 허용·복권을 포함한 모든 동사의
목적어를 표시합니다. 그날 그 파일에 존재하던 문장 한 쌍만 우연히 갈랐을 뿐입니다.

이 결함의 방향이 특히 나쁩니다. 참인 문장에 게이트가 걸리면, 다음 랩은 **학생의
카드에서 참인 문장을 지우라는 압력**을 받습니다. WFG-133 을 거꾸로 돌리는 것입니다.

**고친 뒤:** 참인 문장 **0/5** 오탐, 거짓 문장 **6/6** 검출. 철자는 금지 문구를
포함하도록 고쳤고(`말하지\s*[마말]` — 한국어 부정 명령은 `말고` 와 `마십시오` 를 모두
취합니다), 검토자가 트리에서 **놓친 것을 찾아낸** 철자 네 개를 더 등록했습니다:
`0이 아닌 값으로 종료`(같은 파일 `JUDGE_QA.md` 안에, 이 랩이 면제해 준 줄 여덟 줄 <!-- forbidden-ok: is-ancestor 0이 아닌 값 -->
위에서 아무 표시 없이 살아 있었습니다), `조상이 아닙니다`, `브랜치에 없습니다`, <!-- forbidden-ok: 조상이 아닙니다, 브랜치에 없습니다 -->
그리고 영어 `not reachable from HEAD`. <!-- forbidden-ok: en not reachable from HEAD -->

이 아홉 문장은 `tests/test_withdrawn_claims_registry.py` 의 `REVIEWER_SET_TRUE` ·
`REVIEWER_SET_FALSE` 로 **커밋되어 있고**, 위 점수는 매 랩 다시 계산됩니다. 산문이
아니라 테스트입니다.

**줄바꿈 한계 하나를 이 랩이 직접 측정했습니다.** 검사기는 **줄 단위**입니다
(`scan_text` 가 `splitlines()` 로 돕니다). `dd500e6` 의 원본에서 금지 문장은
「… 문장을 / 그대로 말하지 마십시오」로 **두 줄에 걸쳐** 있어서, 금지 철자는 그
줄바꿈된 원본을 잡지 못합니다. 그 커밋을 되돌려 채점하면 게이트는 여전히 빨간불이
되지만, 그것은 **주장 철자 두 개**(`브랜치 닿지 않`, `0이 아닌 값으로 종료`)가 잡기 <!-- forbidden-ok: is-ancestor 0이 아닌 값 -->
때문이지 금지 철자 때문이 아닙니다. 즉 이 레지스트리의 모든 철자는 「한 줄 안에서만」
성립합니다 — WFG-062 행이 흡수하지 못한 **구조 규칙 두 개**가 존재하는 이유와 같습니다.

**거짓 양성 하나가 새로 생겼고 그대로 둡니다.** `docs/auto/finals/screens_20260904T0630Z/README.md:15`
의 「the stamp is not reachable from HEAD」 는 게이트의 동작을 설명하는 **참인 조건문** <!-- forbidden-ok: en not reachable from HEAD -->
이며 41498ef 에 대한 주장이 아닙니다. §4.4 가 말하는 부류이고, 프래그마로 면제했습니다.
925개 중 2건이 현재 측정된 오탐입니다.

## 6. 주장을 하나 더 철회하게 되면

`docs/auto/withdrawn_claims.json` 의 `claims` 에 `WC-###` 를 추가하고, 철자마다
`pattern` · `token` · `why` 를 씁니다. 그리고
`tests/test_withdrawn_claims_registry.py::_probe_sentence` 에 **저장소가 실제로
썼다가 철회한 문장**을 넣습니다 — 정규식에서 거꾸로 만들어낸 문장이 아니라. 목록은
편집하지 않습니다. 그것이 이 행의 전부입니다.
