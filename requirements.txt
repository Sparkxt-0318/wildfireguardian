# WildfireGuardian dependencies
# Python >= 3.10 required.

# --- Scientific core ---
numpy>=1.24
scipy>=1.10
pandas>=2.0

# --- Geospatial ---
geopandas>=0.13
rasterio>=1.3
xarray>=2023.1
shapely>=2.0
pyproj>=3.5

# --- Networks / routing ---
networkx>=3.0
osmnx>=1.6

# --- Machine learning (canonical ignition probability = sklearn HistGradientBoosting) ---
scikit-learn>=1.3
# xgboost is required ONLY by the abandoned spread_v2_xgb (Build A) and the LFMC
# scaffold, NOT by the canonical spread_v2 model. Intentionally not a core
# dependency (see docs/REPORT_ROUND2_P1.md). Install via the `legacy` extra
# (pip install -e ".[legacy]") only if you need to run the superseded build.
# xgboost>=2.0

# --- Visualization ---
matplotlib>=3.7
folium>=0.14
pillow>=10.0

# --- Configuration / I/O ---
pydantic>=2.0
python-dotenv>=1.0
requests>=2.31
tqdm>=4.65
rich>=13.0

# --- Testing / dev ---
pytest>=7.4
jupyter>=1.0
