# Sign-off ledger

**Owner: A6.** One row per pre-registration version, appended, never edited.
The protocol is `research/eval/SIGNOFF.md`. A direction with no row here is
`unsigned`, and silence is not a signature.

| date | direction | prereg file | version | state | record | reasons or conditions |
|---|---|---|---|---|---|---|
| 2026-09-16 | roads | not yet received | none | `unsigned` | none | A3 is drafting. The review machinery is in place and the review pass of `SIGNOFF.md` section 6 runs when it lands |
| 2026-09-16 | roads | `research/roads/PREREGISTRATION.md` | v0.1b | `refused` | `research/eval/signoffs/roads_v0.1b.md` | R1 the label rule is not executable (Q5 smoke mask open by the author's own statement; the `SIDE` rule of 5.1 is unsatisfiable on the document's own geometry; `tau_burn` 0.10 is a dNBR break point applied to RdNBR; 5.6 contradicts itself and has no branch for a lee patch with no detection; the reason-code list is not exhaustive; 4.5's 0.60 contradicts 5.5's 0.70). R2 two CRITICAL leakage items `unresolved`: B4 (no declared sensitivity grid over the buffer distances, although 12.8 asserts there is one) and B3 (no sensitivity over the arrival rule, no check whether arrival exclusions cluster on wide barriers). R3 section 11.5 needs two repairs: `kappa` and gate M1 are defined on the wrong scale for the covariate that enters the model, and instrument 2 measures detection power while instrument 4 gates the informative null on exclusion power. R4 the smallest effect of interest is not ratified at 0.10 and is stated in effective rather than cleared metres: set at 0.05 in `W_cleared` at the median approach angle, 0.10 reported alongside. R5 six items absent outright: P6, P10, P11, P12, P13, P16, plus the `prereg:` block and the versioned file name. R6 F4 must name its parameters by symbol; the operational-barrier-performance vocabulary needs a claim rule with a detector; the day-against-night contrast must be pre-registered. R7 three MAJOR leakage items as numbered conditions: B5, B7, B9. Sections 7.7 and 12.4 accepted. Recoverability 0.80 and attenuation 0.70 ratified as levels, refused as currently applied |
| 2026-09-16 | landslides | not yet received | none | `unsigned` | none | queued behind roads reaching phase 3 |
| 2026-09-16 | suppression | not yet received | none | `unsigned` | none | queued behind landslides reaching phase 3 |
| 2026-09-16 | roads | `research/roads/PREREG_roads_2026-09-16_v0.2.md` | v0.2 | `signed with conditions` | `research/eval/signoffs/roads_v0.2.md` | All seven reasons of the previous refusal are answered and four are answered past what was asked. The state is set by P12 and not by the design: eight of ten datasets are `pending` and the two that are `verified` supply neither a covariate nor a label, so `SIGNOFF.md` section 3 caps this mechanically. Ten numbered conditions, eight blocking for the primary result and two for a named secondary claim. C1 every dataset reaches `verified` before any fit on real labels. C2 `z_side` is a normal-scale cutoff applied to a Student's t statistic on `n_det - 3` residual degrees of freedom, which at the C4 minimum of 5 detections gives a per-tail false side assignment rate near 0.09 against the 0.023 the number implies, and a false side assignment removes a held segment as `NOBURN`; restate as a t quantile, recompute the angle table, and report the two-tier procedure at its own operating characteristic. C3 the `SIDE` exclusion is outcome dependent, because a front that was stopped leaves a non-planar one-sided arrival surface, so it censors against holds; the side-invariant unordered pair of the two bands' burned fractions measures it and joins section 12.9. C4 the grazing truncation reaches `kappa_v` and the recoverability instruments, so `Var(v_true)`, `kappa_v` and instruments 2 and 2b are pinned to the side-passing retained set; reporting a `W_cleared` slope does not remove the interaction. C5 `NOEXIST` is the width measurement's own null result and censors against narrow roads, which is the dangerous direction; declare the classifier's detectable-gap floor and add `NOEXIST` and `WIDTH` to the 12.9 readings. C6 step A of the attribution test passes on geometry alone and must report the count of passes whose implied time margin falls below the arrival-time standard error. C7 RC-011 and the roads detector cover `research/` only; no width sentence leaves that tree until WJ-002 lands. C8 the segment-level fingerprint at v0.3a; the complex-level one reproduces exactly this round. C9 neither label-rule pass can run before data, so P3 is passed on the text and not on the test. C10 the P11 order is reversed for the other two directions. A3's three recorded disagreements are adjudicated: the closed remedy route is conceded in full and registered as reviewer error, the S4 premise is conceded on the mechanics with a new residual that became C6, and the M1 interval ruling is confirmed with its cost a floor rather than the stated figure. The frozen-record class is ruled sound and needing two narrowings; the content pin is added to `research/eval/tests/` this round | <!-- collision-ok: 0.09 -->
| 2026-09-16 | landslides | `research/landslides/PREREG_landslides_2026-09-16_v0.1.md` | v0.1 | `signed with conditions` | `research/eval/signoffs/landslides_v0.1.md` | A document that measures what other documents argue. Zero of 5,118 occurrence records carry a lot number, the years-since-fire geometry is measured rather than asserted, and the registered split's refusal on a shared control identifier is demonstrated and committed; all three re-derive and the split demonstration reproduces byte for byte under A6's own re-run. Both departures from the program brief are ratified: the catchment replaces the slope unit because the address cannot reach a slope unit and a 30 m DEM cannot resolve the initiation hollow, and the areal count replaces the unit-level binary because the record contains no observable zero and a binary would have made the label an artefact of the assignment rule. The measured assignability rule M1 to M4 is genuinely outcome-free and its gate numbers are provably fixed before their inputs, since the measurements need a DEM and a boundary layer that do not exist. The state is set by P12 and not by the design: three of fourteen datasets are `verified`, none of the three carries a covariate or a unit, and two are not registry entries at all. Thirteen numbered conditions, eleven blocking for the primary result and two for named secondary claims. C1 every dataset reaches `verified`, carrying the rainfall variogram undertaking. C2 section 7.1's identification claim is not true of the model section 10.3 registers: with unburned controls in the risk set and a calendar main effect the deficiency is one and both the linear years-since-fire term and its curvature are identified, with free cohort dummies it is two and both remain identified, and it reaches five only when a burned-by-calendar-year interaction is free, so the estimate is carried by an assumption that section 6.5's own reporting-attention mechanism violates with the same sign and time profile as the hypothesis, and the M-A reading rule is calibrated against a matrix the design does not have. C3 the F4 margin of 0.01 nats per cell requires about one nat on every non-zero cell at section 19's own row count, so it is close to certain to fire whatever the truth and its reading would be quoted as evidence against H-SLIDE. C4 section 4.2's 리 and 시군구 bounds are name counts, not place counts: 1,813 tuples against 1,427 names and 173 pairs against the quoted figure, with 263 리 names in more than one place, so resolution must be on the full parent path. C5 the 시도 column carries eighteen names for seventeen provinces and both renames are exact functions of calendar year, so item A9 is a live path from the gazetteer into the aliasing rather than mitigated. C6 the fitted mean is a product of three things and not two, the third being administrative entry multiplicity, and both the count and the damaged-area outcomes are reported so that A6's having seen the clumping distribution steers nothing. C7 the control identifier scheme must be injective after `canonical_fire_id`, which strips underscores and hyphens, so a colliding scheme welds distant control blocks silently instead of refusing. C8 M4 needs a floor and a reading rule, since F3 is keyed on the M1-based gate. C9 no window, no duration and no bound: `OPEN_QUESTIONS.md` Q10's right-censored lower bound is withdrawn, because every years-since-fire value of three is one cohort in one storm window that carries 50.8 per cent of the record, and because the national annual volume rises about twenty-two-fold across the window so the reporting confound runs opposite to the hypothesis. C10 P3 and P6 are passed on the text and not on the test. C11 the vocabulary needs a detector. C12 the species arm and C13 salvage logging are blocking for named secondary claims. A4's reading of item C3 is adjudicated in A4's favour on the mechanism and on it being worse, and A6 goes one step further: C3 and C2 are one item, because post-fire administrative attention is a burned-by-calendar-year interaction and that is precisely the term whose freedom removes the identification. On the support problem A6 rules that it is fatal to the question the direction was set and is not a reason to stop, because the year-zero contrast and the outcome-free assignability and aliasing tables survive and gate U3 lets the direction terminate itself cheaply. Process finding PF-1: the reversed P11 order required by roads C10 was followed and the independence was still partly defeated, because the round-4 briefing named A4's conclusions on six of the ten convergent items before A6 wrote; the successor condition for suppression is an information barrier, not only an order |
| 2026-09-16 | suppression | `research/suppression/PREREG_suppression_2026-09-16_v0.1.md` | v0.1 | `signed with conditions` | `research/eval/signoffs/suppression_v0.1.md` | The best of the three pre-registrations this program has produced. It withdraws its own headline quantity instead of hedging it: every Korean forest fire is suppressed, so the event indicator is zero on every row and a longer record adds only more suppressed fires, which is a stronger statement than leakage item D4 makes and it retires A6's seeded kill shot for this direction. It found a real defect in A6's own `splits.py` and proposed a cheaper repair than the embargo A6 would have imposed. Every measured number re-derives under A6's independent path, both checksums verify, the split refusal is verbatim, `design_numbers.json` re-runs byte for byte, and the fold table of section 4.3 reproduces exactly. The state is set by P12 and not by the design: two of ten datasets are `verified`, eight are `pending`, and an eleventh is not a registry entry. Eleven numbered conditions, eight blocking for the primary result and three for named secondary claims. C1 every dataset reaches `verified`. C2 the evaluation frame at horizon H3 is undefined, and it is not a detail: 68.2 per cent of the record's fires are contained before hour three, all 78 fires reaching 10 ha were still burning then, the shortest ran 3.67 hours, and the frame moves the base rate from 3.88 to 12.21 per cent and the outcome's entropy from 0.1643 to about 0.371 nats per fire, so failing condition F1's margin arithmetic does not transfer between frames. C3 the indicator "still burning at H3" is excluded by the section 10.6 firewall although it is knowable at the horizon and triples the base rate by itself, so B0 is not the honest simple baseline item P8 requires, and either the indicator is admitted with its paragraph or a survival-conditional baseline B2 is added. C4 gate G3 fails on every one of the ten windows the committed record can form, best case 30.3 per cent against a 25 per cent ceiling, and a window containing the 2022 event needs about 96,600 ha of total excess to pass, so the tail arm is not waiting on WJ-001 and should be closed under the pre-registration's own failing condition F3 rather than gated. C5 gate G2's floor of 50 exceedances sits below the count at which gate G4 can pass: run as written, G4 fails at 25, 50 and 78 under the registered prior and fails at 100 as well under the mandatory flat-prior sensitivity, first passing between 100 and 250, and G4 additionally names section 11.6 where it means 11.5 and requires a pre-registered scale that does not exist. C6 the containment clock heaps at 25.59 per cent exactly on the hour against 2.48 per cent for the report clock, section 11.2's bin edges are integer hours, and section 5.1 draws its conclusion from the end of the clock that does not carry the events. C7 the statistics file has no identifier column, a date-plus-county match gives 2,132 candidate pairs for 2,030 history rows with 113 unmatched and 183 multiply matched, section 9.2's defect rule spans both files, and the document never names which file supplies U1's clock although the choice moves the median duration from 2.00 to 0.87 hours. C8 the depth threshold handed to the human is wrong for the gate it opens: the number that opens G1 is 2014, the pre-registration's prose says 2014 for one fold and `DATA_REQUIREMENTS.md` says 2015, and 2015 leaves G1 shut because G1 requires two folds. C9 P7 names the metric per fire while U2's unit is the complex. C10 P3 and P6 are passed on the text and not on the test. C11 the night contrast and the access-distance coefficient are permanently secondary, which is `OPEN_QUESTIONS.md` Q8 route B and A5's own recommendation. Four checklist verdict differences are adjudicated, three in A5's favour: D6, A4 and A8 are conceded to A5, with the A8 distinction between a rule that drops rows on the outcome and a declared refusal to fit accepted as correct, and A9 held against A5 because a county boundary moved inside the observation window. Q3 is answered: `gap_years: 0` is intended and A5's row-level boundary rule is better than the embargo A6's prior read called free. Q4 10 ha primary, Q6 both proposed leakage items accepted with D13 at CRITICAL, Q8 route B, Q9 the withdrawal kept, Q10 no fit. Process finding PF-2: A6's own commit message on its prior leakage read states two of its conclusions in plain text, so the program's information barrier has a channel no attestation covers. PF-3: three of the eleven conditions came from executing the pre-registration's gates rather than reading them, and `SIGNOFF.md` section 6 does not list that as a review step |
